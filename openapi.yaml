openapi: 3.1.0
info:
  title: 'WellnessLiving public API'
  version: 1.1.20260909110320
  description: |
    WellnessLiving API is a RESTful way to access and manage your business data.
    Last updated at: 2026-09-09 11:03:20
servers:
  - url: https://us.wellnessliving.com
    description: 'WellnessLiving server for businesses served from the United States data center'
  - url: https://au.wellnessliving.com
    description: 'WellnessLiving server for businesses served from the Australia data center'
paths:
  '/Studio/Stand/Stand.json':
    delete:
      summary: 'Marks a stand as deleted. Renames the stand record with a removal timestamp suffix to prevent name conflicts, and sets the removal date. Called by the build server after stand servers are removed.'
      parameters:
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stand-Stand
    get:
      summary: 'Returns stand data by name. Retrieves stand status and deserialized stand data for the specified stand name. Verifies that the current user has access to the stand''s project.'
      parameters:
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_stand_data:
                        type: [array, string, "null"]
                        description: 'Information about entities necessary for the functioning of the stand. <i>Structure of an array is identical to the `z_stand_data` field of the {@link StandSql} table.</i> `string` if it needs to set an empty value. `null` will not change.'
                      id_stand_status:
                        type: [number, "null"]
                        description: 'Stand status, one of {@link StandStatusSid}. `null` if not initialized and will be ignored.'
      tags:
        - Stand-Stand
    post:
      summary: 'Registers a new stand and generates a URL for its creation. Validates the microservice environment configuration, creates a stand record, and returns a build URL to trigger stand creation.'
      parameters:
        - name: k_microservice_primary
          in: query
          description: 'Primary microservice for the stand. The main microservice from which the stand is created; other microservices can be added if they are compatible with the primary one, see {@link StandApi::$json_stand_environment}. `null` if value is not defined. If the information is not provided as a request parameter, the stand will not be created.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Task key within which a stand should be created. `null` if not defined, in this case the stand name must be specified manually in {@link StandApi::$s_stand}.'
          schema:
            type: [string, "null"]
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_stand_data:
                  type: [array, string, "null"]
                  description: 'Information about entities necessary for the functioning of the stand. <i>Structure of an array is identical to the `z_stand_data` field of the {@link StandSql} table.</i> `string` if it needs to set an empty value. `null` will not change.'
                json_stand_environment:
                  type: [string, "null"]
                  description: 'A microservice environment for create stand. Each element of the array contains `null` if the value is not defined. If the information is not provided as a request parameter, the stand will not be created. <dl> <dt>string `k_microservice`</dt> <dd>Microservice key. Primary key in {@link \Studio\Microservice\MicroserviceSql} table.</dd> <dt>string `s_environment`</dt> <dd>Environment name. See {@link \Studio\Microservice\MicroserviceSql}.`json_path`.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'Link to redirect for stand creation.'
      tags:
        - Stand-Stand
    put:
      summary: 'Updates stand status and stand data. Updates `id_stand_status` or `a_stand_data` for the specified stand. When the stand becomes ready and both data centers are configured, triggers builds on each.'
      parameters:
        - name: id_stand_status
          in: query
          description: 'Stand status, one of {@link StandStatusSid}. `null` if not initialized and will be ignored.'
          schema:
            type: [number, "null"]
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_stand_data:
                  type: [array, string, "null"]
                  description: 'Information about entities necessary for the functioning of the stand. <i>Structure of an array is identical to the `z_stand_data` field of the {@link StandSql} table.</i> `string` if it needs to set an empty value. `null` will not change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stand-Stand
  '/Core/Request/Example.json':
    get:
      summary: 'Returns the example result for debugging. A diagnostic endpoint used to verify that a given API connection is working correctly. Increments the input argument by one (confirming round-trip data flow), records a debug log entry with the current session and authentication context, and returns the log key so the caller can inspect it.'
      parameters:
        - name: i_argument
          in: query
          description: 'Example argument.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_result:
                        type: number
                        format: int32
                        description: 'Example result.'
                      s_log:
                        type: string
                        description: 'Key of the log record written by the API controller.'
      tags:
        - Request-Example
  '/Core/Captcha/CaptchaRequire.json':
    get:
      summary: 'Checks if a CAPTCHA is required for the given captcha type. Used before rendering a form that may include a CAPTCHA widget. The caller supplies the captcha type (identified by CID) and any constructor arguments it needs; the response tells the frontend whether to show the challenge at all and which reCAPTCHA version is active.'
      parameters:
        - name: a_arguments
          in: query
          description: 'Arguments for creating CAPTCHA object. <i>Structure can be missing in case when no configured captcha in environment.</i> Parameters for `1064`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if system business.</dd> </dl> Parameters for `1072`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if is system business.</dd> </dl> Parameters for `1066`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if is system business.</dd> </dl> Parameters for `1139`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if is system business.</dd> </dl> Parameters for `1508`: <dl> <dt>bool `is_search`</dt> <dd> Flag to determine is it a search action or no. If search action is `true` it increments {@link \Wl\Business\Search\BusinessSearchCaptcha::$ip_actor}. Otherwise {@link \Wl\Business\Search\BusinessSearchCaptcha::$s_back_action}. </dd> </dl>'
          schema:
            type: array
        - name: cid_captcha
          in: query
          description: 'The CID of the CAPTCHA.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_enable_v3:
                        type: boolean
                        description: '`true` if enabled V3 captcha enabled. `false` if only V2 captcha enable.'
                      is_require:
                        type: boolean
                        description: 'This will be `true` if a CAPTCHA is required. Otherwise, this will be `false`.'
      tags:
        - Captcha-CaptchaRequire
  '/Core/Google/GoogleCaptcha.json':
    put:
      summary: 'Saves the user CAPTCHA token for the current session. Accepts the CAPTCHA version, the action name, and the user token obtained from the Google reCAPTCHA widget, and stores them in the session so that subsequent API requests requiring CAPTCHA verification can use them. Site keys for initialize Google reCAPTCHA: * Version 2 (invisible): * Demo/Staging - {@link GoogleCaptchaKeys::CAPTCHA_V2_SITE_KEY_SANDBOX} * Production - {@link GoogleCaptchaKeys::CAPTCHA_V2_SITE_KEY_PRODUCTION} * Version 3: * Demo/Staging - {@link GoogleCaptchaKeys::CAPTCHA_V3_SITE_KEY_SANDBOX} * Production - {@link GoogleCaptchaKeys::CAPTCHA_V3_SITE_KEY_PRODUCTION}'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_version:
                  type: number
                  format: int32
                  description: 'Captcha version ID.'
                text_action:
                  type: string
                  description: 'The action name. Used to determine the place where the CAPTCHA is needed in documentation for endpoints that used the CAPTCHA.'
                text_token:
                  type: string
                  description: 'The user token CAPTCHA.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Google-GoogleCaptcha
  '/Core/Sid/CoreSid.json':
    get:
      summary: 'Returns the list of all items for the given Sid class. Populates enumeration dropdowns and lookup tables on the frontend. <i> Given any class that extends {@link CoreSid}, returns every defined constant with its numeric ID, string identifier, and human-readable title so the client does not need to hard-code enum values. </i>'
      parameters:
        - name: s_class_name
          in: query
          description: 'Name of the Sid class to get list from. Should be a fully qualified class name. <i>Class should be a child of {@link CoreSid}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of items. Keys are IDs, values are arrays with additional information: <dl> <dt>int `sid`</dt> <dd>String ID.</dd> <dt>string `text_title`</dt> <dd>Title of the ID.</dd> </dl>'
      tags:
        - Sid-CoreSid
  '/Core/WebSocket/Subscribe.json':
    post:
      summary: 'Checks whether a listener can subscribe to specified channel. Subscribes in a case of positive result. Called by the WebSocket client immediately after a connection is established, to register interest in a specific channel (identified by CID and key fields). If the current user is not permitted to receive events on that channel, the subscription is rejected. On success, any messages already queued for the channel are returned so the client does not miss events that arrived before the subscription was set up.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_key:
                  type: array
                  description: 'Keys of the channel. Key is name of a key field in a channel controller. Value is its value. Subscriber will only receive notifications about events in which all these values equal values specified here. Parameters for `60`: <dl> <dt>string `k_personnel`</dt> <dd>Key of a staff member.</dd> <dt>string `kip`</dt> <dd>Key of IP address from which this staff member accesses the system.</dd> </dl> Parameters for `1921`: <dl> <dt>int `id_visit`</dt> <dd>Visit status ID. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>string `k_business`</dt> <dd>Key of a business.</dd> <dt>string `uid`</dt> <dd>Key of the user.</dd> </dl> Parameters for `1342`: <dl> <dt>string `k_business`</dt> <dd>Key of a business.</dd> <dt>string `uid_receiver`</dt> <dd>Key of the user.</dd> </dl> Parameters for `1583`: <dl> <dt>string `k_channel`</dt> <dd>Messenger channel key.</dd> </dl> Parameters for `688`: <dl> <dt>string `dtu_class_period`</dt> <dd>Class schedule session date.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key.</dd> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `k_class_period`</dt> <dd>Class schedule key.</dd> <dt>string `k_visit`</dt> <dd>Visit key.</dd> <dt>string `s_secret`</dt> <dd>Visit secret key.</dd> </dl> Parameters for `1869`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid_receiver`</dt> <dd>User key who will receive message.</dd> </dl> Parameters for `735`: <dl> <dt>string|null `k_business`</dt> <dd>Key of a business to which report to subscribe. `null` for system-wide businesses.</dd> <dt>string `s_report`</dt> <dd> Key of a report to subscribe to. <i>Hex-encoded value of {@link \Thoth\ReportCore\Generator\ReportStorageListSql}.<tt>z_report</tt>.</i> </dd> </dl> Parameters for `1105`: <dl> <dt>string `uid`</dt> <dd>Current user key.</dd> </dl> Parameters for `1005`: <dl> <dt>string|null `k_microservice`</dt> <dd>Microservice key. `null` if the value is not set</dd> </dl> Parameters for `19`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `305`: <dl> <dt>string `k_location`</dt> <dd>Location where session booked or cancelled.</dd> </dl> Parameters for `298`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1163`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid_actor`</dt> <dd>Key of the actor.</dd> </dl> Parameters for `1660`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> Parameters for `510`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> Parameters for `1850`: <dl> <dt>int `id_import_custom`</dt> <dd>ID of custom import process. One of {@link \Wl\Import\Custom\CustomSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key within which import is progressing.</dd> </dl> Parameters for `842`: <dl> <dt>string `k_business`</dt> <dd>Business in which the report was generated.</dd> <dt>string `uid`</dt> <dd>User who requested the report generation.</dd> </dl> Parameters for `1543`: <dl> <dt>int `id_report`</dt> <dd>ID of the report.</dd> <dt>string `k_business`</dt> <dd>Business key in which the report was monitoring.</dd> </dl> Parameters for `977`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid`</dt> <dd>UID of the client for which failed to send SMS.</dd> </dl> Parameters for `16`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1834`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `677`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1820`: <dl> <dt>string `k_business`</dt> <dd>Key of a business.</dd> </dl> Parameters for `1657`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> </dl> Parameters for `1421`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant.</dd> <dt>string `k_pay_transaction`</dt> <dd>Pay transaction key, which was used to create ticket.</dd> <dt>string `s_ticket_id`</dt> <dd>Gateway Ticket ID.</dd> </dl> Parameters for `523`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> Parameters for `960`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid_actor`</dt> <dd>Key of the actor. <tt>admin</tt> - for WebSocket channel for which admins subscribed.</dd> </dl> Parameters for `34`: <dl> <dt>string `uid_response`</dt> <dd>Key of user who can grant access to location.</dd> </dl> Parameters for `41`: <dl> <dt>string `k_location`</dt> <dd>Location key.</dd> <dt>string `uid`</dt> <dd>User-receiver of response.</dd> </dl> Parameters for `57`: <dl> <dt>string `k_location`</dt> <dd>Key of location access to which was requested.</dd> <dt>string `uid_request`</dt> <dd>Key of user who requested access to location.</dd> </dl> Parameters for `828`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `s_id`</dt> <dd>Key of the task where addresses should be verified.</dd> </dl> Parameters for `1736`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant.</dd> <dt>string `k_pay_transaction`</dt> <dd>Pay transaction key, which was used to create ticket.</dd> <dt>string `s_exchange_identification`</dt> <dd>Gateway exchange identification.</dd> </dl> Parameters for `549`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `963`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid_receiver`</dt> <dd>Key of the user.</dd> </dl> Parameters for `952`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid`</dt> <dd>UID of the client.</dd> </dl>'
                cid_channel:
                  type: number
                  format: int32
                  description: 'Channel controller CID.'
                s_password:
                  type: string
                  description: 'Subscriber password.'
                s_token:
                  type: string
                  description: 'Request token. This token is only used for asynchronous functions, and identifies a specific request.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_message_broadcast:
                        type: array
                        description: 'All messages in queue. Key is a message key. Value is message data. When a client is initially subscribed for a WebSocket channel, entire message queue is sent to the client. This is needed to send message that were generated before a client has subscribed. Parameters for `60`: <dl> <dt>string|null `dt_remove`</dt> <dd>Date/time when access to specified service will be revoked. <tt>null</tt> if access is revoked.</dd> <dt>string `k_ip_service`</dt> <dd>Key of a service that is changed.</dd> </dl> Parameters for `1921`: <dl> <dt>string `k_visit`</dt> <dd>Key of a book/visit.</dd> </dl> Parameters for `1342`: <dl> <dt>int `i_unread_alert`</dt> <dd>The count with unread alert for the user.</dd> <dt>bool `is_active`</dt> <dd>Whether message is active. This is needed to avoid showing previous messages on page reload.</dd> <dt>bool `is_message`</dt> <dd>Whether this is an alert for an SMS message</dd> <dt>bool `is_sound`</dt> <dd>Whether the user should hear a sound when a notification is sent.</dd> <dt>string|null `k_alert`</dt> <dd>Key of the alert that was just sent. <tt>null</tt> means alert was read.</dd> </dl> Parameters for `1583`: <dl> <dt>array `a_data`</dt> <dd> New information for messenger. <dl> <dt>array `message`</dt> <dd> Message information: <dl> <dt>int `id`</dt> <dd>Message key.</dd> <dt>string `message`</dt> <dd>Message text.</dd> <dt>string `updated_at`</dt> <dd>Date/time when the message has been posted.</dd> </dl> </dd> <dt>array `user_profile`</dt> <dd> User''s information: <dl> <dt>int `id_gender`</dt> <dd>User gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>string `s_first_name`</dt> <dd>User first name.</dd> <dt>string `s_last_name`</dt> <dd>User last name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_photo`</dt> <dd>URL of the user''s profile photo.</dd> </dl> </dd> </dl> </dd> </dl> Parameters for `733`: <dl> <dt>string|null `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `688`: <dl> <dt>string `url_redirect`</dt> <dd>Url link to redirect to join virtual meeting.</dd> </dl> Parameters for `1869`: <dl> <dt>int `i_overdue`</dt> <dd>Number of overdue tasks.</dd> <dt>int `id_task_status`</dt> <dd>New task status of the changed task. One of {@link \Wl\Task\TaskStatusSid}.</dd> <dt>string `k_task`</dt> <dd>Key of the changed task.</dd> </dl> Parameters for `735`: <dl> <dt>int `i_cas_change`</dt> <dd> A CAS (compare-and-swap) number that allows to track changes in the report storage. This number is changed every time content of the report gets updated. If this number is not changed, the content is not updated. <i>Copy of {@link \Thoth\ReportCore\Generator\ReportStorageListSql}.<tt>i_cas_change</tt>.</i> </dd> <dt>int `id_report_status`</dt> <dd>Status of this report. One of {@link \Thoth\ReportCore\Generator\ReportGeneratorStatusSid} constants.</dd> </dl> Parameters for `1105`: <dl> <dt>string `s_code`</dt> <dd>Code for authorization.</dd> <dt>int `text_time`</dt> <dd>Unique string for get code.</dd> </dl> Parameters for `1005`: <dl> <dt>string[] `a_op`</dt> <dd> Names of operation that were executed. Operation names are defined by <tt>OP_*</tt> constants of {@link \Studio\Sprint\Promoting\PromotingChannel}. </dd> <dt>string|null `s_step`</dt> <dd>Name of the step that was set. `null` if step has not changed.</dd> <dt>string|null `text_log`</dt> <dd> A new log item was added during execution of a script. Text of the log item that was just added. `null` if this message does not contain information about newly added log item. </dd> </dl> Parameters for `19`: <dl> <dt>string `text_title`</dt> <dd>New title of the business.</dd> </dl> Parameters for `305`: <dl> <dt>string `dtl_end`</dt> <dd>Session end date/time.</dd> <dt>string `dtl_start`</dt> <dd>Session start date/time.</dd> <dt>string|null `k_appointment`</dt> <dd> Key of the changed appointment. Is set only if changed session is appointment. `null` if changed session is not an appointment. </dd> <dt>string|null `k_class_period`</dt> <dd> Key of the changed class session. Is set only if changed session is class. `null` if changed session is not a class. </dd> </dl> Parameters for `298`: <dl> <dt>string `dt_end_local`</dt> <dd>End of change interval. Empty string if change interval has no end.</dd> <dt>string `dt_start_local`</dt> <dd>Start of change interval.</dd> <dt>string|null `k_class_tab_new`</dt> <dd> New "Book now" tab primary key in {@link \Wl\Classes\Tab\TabSid} table. <tt>null</tt> means system default tab. </dd> <dt>string|null `k_class_tab_old`</dt> <dd> Old "Book now" tab primary key in {@link \Wl\Classes\Tab\TabSid} table. <tt>null</tt> means system default tab. </dd> </dl> Parameters for `1163`: <dl> <dt>bool `is_active`</dt> <dd>Whether message is active. This is needed to avoid showing previous messages on page reload.</dd> <dt>bool|null `is_booking_in_progress`</dt> <dd> Whether booking is in progress. <i>This property will contain value from {@link \Wl\Book\Multiple\MultipleTaskSession::verify()} method, can be null.</i> </dd> </dl> Parameters for `510`: <dl> <dt>array `a_visit`</dt> <dd> New data of changes schedule item: <dl> <dt>string `dtl_end`</dt> <dd>Date/time of end.</dd> <dt>string `dtl_start`</dt> <dd>Date/time of start.</dd> <dt>bool `is_cancel`</dt> <dd><tt>true</tt> if book was canceled; <tt>false</tt> otherwise.</dd> <dt>string `s_key`</dt> <dd>Book key. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of location.</dd> <dt>string `text_location`</dt> <dd>Title of location.</dd> <dt>string `text_note`</dt> <dd>Description of event.</dd> <dt>string `text_title`</dt> <dd>Title of event.</dd> <dt>string `url`</dt> <dd>URL of virtual service. Empty if the visit is not virtual.</dd> </dl> </dd> </dl> Parameters for `1850`: <dl> <dt>int `i_complete`</dt> <dd>Number of successfully processed items.</dd> <dt>int `i_fail`</dt> <dd>Number of failed items.</dd> <dt>int `i_left`</dt> <dd>Number of items left to process.</dd> <dt>string `k_import`</dt> <dd>Key of the import being processing.</dd> <dt>string `url_log`</dt> <dd>Url link to download log file with import progress.</dd> </dl> Parameters for `842`: <dl> <dt>int `i_generation`</dt> <dd>Duration of the generation of the report in seconds.</dd> <dt>int `id_report`</dt> <dd>ID of the report that was generated. One of the {@link \RsReportSid} constants.</dd> <dt>bool `is_need_show`</dt> <dd>Whether need to display a message about report generation, regardless of the generation time.</dd> <dt>string `k_report_accumulation`</dt> <dd>Report accumulation.</dd> <dt>string `text_report`</dt> <dd>Title of the report that was generated.</dd> </dl> Parameters for `977`: <dl> <dt>string `html_error_message`</dt> <dd>The text of the error.</dd> <dt>string `s_key`</dt> <dd>The unique key of the sent SMS.</dd> <dt>int `tl_send`</dt> <dd>The time in UNIX format when socket message was sent.</dd> </dl> Parameters for `16`: <dl> <dt>string `text_title`</dt> <dd>New title of the business.</dd> </dl> Parameters for `1834`: <dl> <dt>array `a_domain_token`</dt> <dd> List of email domain tokens: Empty array if the business does not have a mail domain set up. <dl> <dt>int `id_status`</dt> <dd>Status of the token. One of {@link \Wl\Mail\Domain\DomainVerifyStatusSid} constants.</dd> <dt>string `text_host`</dt> <dd>Host part of the token, which should be added to the DNS records.</dd> <dt>string `text_icon_class`</dt> <dd>CSS class for the icon representing the token status.</dd> <dt>string `text_status`</dt> <dd>Text representation of the token status.</dd> <dt>string `text_type`</dt> <dd>Type of the token. One of {@link \Wl\Mail\Domain\DomainTokenTypeSid} constants in text representation.</dd> <dt>string `text_value`</dt> <dd>Value of the token, which should be added to the DNS records.</dd> </dl> </dd> <dt>int|null `id_domain_status`</dt> <dd>Email domain status. `null` if the business does not have a mail domain set up.</dd> <dt>string `text_domain_status_icon`</dt> <dd> CSS class for the icon representing the email domain status. Empty string if the business does not have a mail domain set up. </dd> </dl> Parameters for `677`: <dl> <dt>int `id_mail_verify_status`</dt> <dd> Status of mail verification. One of the {@link \Wl\Mail\Verify\MailVerifyStatusSid} constants. Or {@link \Wl\Business\Config\Option\MailVerifyStatusOption::DEFAULT_VALUE} if option "Enable Custom Reply To Emails" in Business -> Feature is disabled. </dd> <dt>string `text_email`</dt> <dd>Email address to check.</dd> </dl> Parameters for `1820`: <dl> <dt>string|null `k_login_type`</dt> <dd>Key of the client type. `null` if client type is not set.</dd> <dt>string `uid`</dt> <dd>Key of the user.</dd> </dl> Parameters for `1421`: <dl> <dt>int|null `id_card_system`</dt> <dd> Card type ID. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>int|null `id_pay_exception`</dt> <dd>Transaction exception ID. `null` if no errors occurred.</dd> <dt>int|null `id_ticket_status`</dt> <dd>Ticket status. `null` if not set yet.</dd> <dt>string `m_tip`</dt> <dd>Amount of tips entered by customer on terminal.</dd> <dt>string|null `s_card_holder`</dt> <dd> Cardholder name. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>string|null `s_expire`</dt> <dd> Card expiration date. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>string|null `s_last4`</dt> <dd> Last four digits of the card number. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>string|null `text_message`</dt> <dd>Text message to notify the client. `null` until initialized.</dd> </dl> Parameters for `523`: <dl> <dt>array `a_visit`</dt> <dd> New data of changes schedule item: <dl> <dt>string `dtl_end`</dt> <dd>Date/time of end.</dd> <dt>string `dtl_start`</dt> <dd>Date/time of start.</dd> <dt>bool `is_cancel`</dt> <dd><tt>true</tt> if book was canceled; <tt>false</tt> otherwise.</dd> <dt>string `s_key`</dt> <dd>Book key. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>string `text_location`</dt> <dd>Title of location.</dd> <dt>string `text_note`</dt> <dd>Description of event.</dd> <dt>string `text_title`</dt> <dd>Title of event.</dd> <dt>string `url`</dt> <dd>URL of virtual service. Empty if the visit is not virtual.</dd> </dl> </dd> </dl> Parameters for `960`: <dl> <dt>int `i_unread_sms`</dt> <dd>The count with unread SMS in the business.</dd> </dl> Parameters for `34`: <dl> <dt>bool `is_close`</dt> <dd> `true` if the request has been already processed (question window must not be shown); `false` otherwise (question window must be shown). </dd> <dt>string `k_location`</dt> <dd>Location key.</dd> <dt>string `text_full_name`</dt> <dd>Name of the user requesting access.</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `uid_request`</dt> <dd>Key of user who request access to location.</dd> </dl> Parameters for `41`: <dl> <dt>bool `is_grant`</dt> <dd><tt>true</tt> - access is granted; <tt>false</tt> - denied.</dd> <dt>string `text_full_name`</dt> <dd>Full name of user-receiver of response.</dd> <dt>float `tu_send`</dt> <dd>The time in seconds with fractional part in UNIX format when socket message was sent.</dd> </dl> Parameters for `57`: <dl> <dt>bool `is_inactive`</dt> <dd> `true` if a new assess request has been sent (question window must be not closed); `false` otherwise (question window must be closed). </dd> </dl> Parameters for `828`: <dl> <dt>array `a_invalid`</dt> <dd> List of users for which address verification failed. <dl> <dt>array `a_address`</dt> <dd> Address information. <i>See {@link \Core\Geo\AddressEntity::toArray()} for details.</i> <dl> <dt>string `text_address`</dt> <dd>Address string.</dd> <dt>string `text_city`</dt> <dd>City title.</dd> <dt>string `text_country`</dt> <dd>Country title.</dd> <dt>string `text_postal`</dt> <dd>Postal address.</dd> <dt>string `text_state`</dt> <dd>State.</dd> </dl> </dd> <dt>bool `is_address`</dt> <dd>Whether address string is specified.</dd> <dt>bool `is_checked`</dt> <dd>`true` if address check, `false` otherwise.</dd> <dt>string `text_email`</dt> <dd>User email address.</dd> <dt>string `text_name`</dt> <dd>User name.</dd> <dt>string `text_phone`</dt> <dd>User phone number.</dd> <dt>string `text_note`</dt> <dd>Note.</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url`</dt> <dd>Link to user profile.</dd> <dt>string `url_image`</dt> <dd>Link to user image.</dd> </dl> </dd> <dt>array `a_valid`</dt> <dd> List of users for which address verification succeed. <dl> <dt>array `a_address`</dt> <dd> Address information. <i>See {@link \Core\Geo\AddressEntity::toArray()} for details.</i> <dl> <dt>string `text_address`</dt> <dd>Address string.</dd> <dt>string `text_city`</dt> <dd>City title.</dd> <dt>string `text_country`</dt> <dd>Country title.</dd> <dt>string `text_postal`</dt> <dd>Postal address.</dd> <dt>string `text_state`</dt> <dd>State.</dd> </dl> </dd> <dt>bool `is_address`</dt> <dd>Whether address string is specified.</dd> <dt>bool `is_checked`</dt> <dd>`true` if address check, `false` otherwise.</dd> <dt>string `text_email`</dt> <dd>User email address.</dd> <dt>string `text_name`</dt> <dd>User name.</dd> <dt>string `text_phone`</dt> <dd>User phone number.</dd> <dt>string `text_note`</dt> <dd>Note.</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url`</dt> <dd>Link to user profile.</dd> <dt>string `url_image`</dt> <dd>Link to user image.</dd> </dl> </dd> </dl> Parameters for `1736`: <dl> <dt>int|null `id_card_system`</dt> <dd> Card type ID. Not `null` when {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\PaymentStatusChannel::$id_payment_status} is equal {@link \Thoth\PayProcessor\Nuvei\Code\CSResponseSid::APPROVED}. </dd> <dt>int|null `id_card_type`</dt> <dd> Card type ID. `null` If were unable to determine the card type. See {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\Entity\OCservice\PaymentResponseEntity::$id_card_type}. </dd> <dt>int|null `id_pay_exception`</dt> <dd>Transaction exception ID. `null` if no errors occurred.</dd> <dt>int|null `id_payment_status`</dt> <dd>Payment status. `null` if not set yet.</dd> <dt>string|null `m_tip`</dt> <dd>Tip amount entered by the customer at the terminal. `null` if not initialized yet.</dd> <dt>string|null `s_last4`</dt> <dd> Last four digits of the card number. Not `null` when {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\PaymentStatusChannel::$id_payment_status} is equal {@link \Thoth\PayProcessor\Nuvei\Code\CSResponseSid::APPROVED}. </dd> <dt>string|null `text_card_holder`</dt> <dd> Cardholder name. Not `null` when {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\PaymentStatusChannel::$id_payment_status} is equal {@link \Thoth\PayProcessor\Nuvei\Code\CSResponseSid::APPROVED}. </dd> <dt>string|null `text_message`</dt> <dd>Text message to notify the client. `null` until initialized.</dd> </dl> Parameters for `549`: <dl> <dt>float `f_progress`</dt> <dd>Accumulation process progress.</dd> </dl> Parameters for `963`: <dl> <dt>array `a_info`</dt> <dd> An array with information about dialog: <dl> <dt>string `dtl_last_activity`</dt> <dd>The date and time of the last SMS in the dialog.</dd> <dt>int `i_unread_sms`</dt> <dd>A count of unread messages.</dd> <dt>bool `is_inbound`</dt> <dd>Whether the last SMS is inbound or not.</dd> <dt>string `text_last_sms`</dt> <dd>The text of last sms.</dd> <dt>string `uid`</dt> <dd>The UID of the client. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>int `tl_send`</dt> <dd>The time in UNIX format when socket message was sent.</dd> </dl> Parameters for `952`: <dl> <dt>array `a_info`</dt> <dd> An array with information about SMS: <dl> <dt>string `dtl_message`</dt> <dd>Date and time, when SMS messages were sent, in MySQL format.</dd> <dt>bool `is_automated`</dt> <dd><tt>true</tt> if SMS was sent from task, <tt>false</tt> - if the staff member sent SMS.</dd> <dt>bool `is_outbound`</dt> <dd><tt>true</tt> if staff member sent SMS, <tt>false</tt> - if the client sent the SMS.</dd> <dt>string `k_sms_history`</dt> <dd>The key of SMS history. <i>Primary key in {@link \ASmsHistorySql} table.</i></dd> <dt>string `sid_sms_status`</dt> <dd>Sid of the SMS status.</dd> <dt>string `text_date`</dt> <dd>The day when SMS messages were sent.</dd> <dt>string `text_sender_name`</dt> <dd>The sender''s name.</dd> <dt>string `text_sms_body`</dt> <dd>The body of the SMS.</dd> <dt>string `text_sms_status`</dt> <dd>The title of the SMS status.</dd> <dt>string `text_sms_type`</dt> <dd>The title of the mail type.</dd> <dt>string `text_time`</dt> <dd>The time when SMS was sent.</dd> <dt>string|null `uid_sender`</dt> <dd>The UID of the sender, <tt>null</tt> - if SMS sent from task.</dd> </dl> </dd> <dt>string `s_key`</dt> <dd> The unique key of sent SMS from 2-way SMS chat. The key need to update SMS information in the chat after sent SMS to client. </dd> </dl>'
      tags:
        - WebSocket-Subscribe
  '/Core/Geo/Combobox.json':
    get:
      summary: 'Returns a list of cities to show in combobox list. Powers city autocomplete dropdowns in address forms. Returns up to 100 cities whose names start with the given fragment, each labelled as "City, Region, Country". Pass a locale to restrict results to a single country, or omit it to search worldwide.'
      parameters:
        - name: id_locale
          in: query
          description: 'The locale ID used as a filter. The locale is generally a country. One of the {@link LocaleSid} constants. This will be `0` if all locations are searched.'
          schema:
            type: number
            format: int32
        - name: s_value
          in: query
          description: 'The city name (or a fragment of the city name) used for the search.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of items to show in the combobox list. Each element of the array is a city matching the string with the following structure: <dl> <dt>string `s_value`</dt> <dd>The human-readable name of the city, including the state/province and country.</dd> <dt>string `s_key`</dt> <dd>The <var>k_city</var> value for the city.</dd> </dl>'
      tags:
        - Geo-Combobox
  '/Social/Microsoft/Login.json':
    delete:
      summary: 'Removes the association between a website client and a Microsoft account. Accepts the user''s UID, verifies that the caller is the account owner, and unlinks the Microsoft account from the user''s profile.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Microsoft-Login
    get:
      summary: 'Collects data for the Microsoft login button. Called when rendering the "Sign in with Microsoft" button. Generates the OAuth 2.0 authorization URL the button must link to. When a UID is provided, also reports whether that user already has a Microsoft account linked, so the frontend can show "Link" or "Unlink" instead of the default sign-in label.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exists:
                        type: boolean
                        description: 'If `true`, the user has a bound Microsoft account. Otherwise, this will be `false`.'
                      url_login:
                        type: string
                        description: 'The Microsoft OAuth 2.0 authorization link.'
      tags:
        - Microsoft-Login
    post:
      summary: 'Signs a user in with Microsoft. Accepts the Microsoft authorization code, an optional state parameter for CSRF verification, and an optional redirect URI. Validates the state, exchanges the code for user identity, and signs the user in or creates a new account.'
      parameters:
        - name: is_external
          in: query
          description: 'If authorization is performed in a third-party application, set this flag in case of authorization errors.'
          schema:
            type: boolean
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_code:
                  type: string
                  description: 'The authorization code that the app requested.'
                s_state:
                  type: string
                  description: 'If a state parameter is included in the request, the same value should appear in the response. The app should verify that the state values in the request and response are identical.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Microsoft-Login
  '/Wl/Tag/Tag.json':
    delete:
      summary: 'Removes the tag. Deletes the revenue category from the business. The deletion fails if the tag is currently set as the primary revenue category for any linked asset, class, coupon, promotion, service, or product (promotions that have already been removed are ignored). On success, notifies listeners about the affected linked objects and invalidates the cached tag list of the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
        - name: k_tag
          in: query
          description: 'The tag key. Required for {@link TagApi::delete()}. Optional for {@link TagApi::get()}: if specified, only this tag is returned; if not specified, all tags of the business are returned.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tag-Tag
    get:
      summary: 'Returns revenue categories (tags) of the business. Returns tags along with the bookable assets, classes, coupons, promotions, services, and products assigned to each one.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
        - name: k_tag
          in: query
          description: 'The tag key. Required for {@link TagApi::delete()}. Optional for {@link TagApi::get()}: if specified, only this tag is returned; if not specified, all tags of the business are returned.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tag:
                        type: array
                        description: 'The revenue categories (tags) of the business. Keys refer to tag keys <i>({@link \RsTagSql})</i>. Values have the next structure: <dl> <dt>array[] `a_asset`</dt> <dd> Bookable assets assigned to the tag. Each element has the next structure: <dl> <dt>string `k_id`</dt> <dd>The resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>bool `is_primary`</dt> <dd>Whether the tag is the primary revenue category for the resource.</dd> <dt>string `text_title`</dt> <dd>The resource title.</dd> </dl> </dd> <dt>array[] `a_class`</dt> <dd> Classes and events assigned to the tag. Each element has the next structure: <dl> <dt>string `k_id`</dt> <dd>The class key. <i>Primary key in the {@link \RsClassSql} table.</i></dd> <dt>bool `is_primary`</dt> <dd>Whether the tag is the primary revenue category for the class.</dd> <dt>string `text_title`</dt> <dd>The class title.</dd> </dl> </dd> <dt>array[] `a_coupon`</dt> <dd> Gift cards assigned to the tag. Each element has the next structure: <dl> <dt>string `k_id`</dt> <dd>The coupon key. <i>Primary key in the {@link \RsCouponSql} table.</i></dd> <dt>bool `is_primary`</dt> <dd>Whether the tag is the primary revenue category for the coupon.</dd> <dt>string `text_title`</dt> <dd>The coupon title.</dd> </dl> </dd> <dt>array[] `a_promotion`</dt> <dd> Purchase options (passes and memberships) assigned to the tag. Each element has the next structure: <dl> <dt>string `k_id`</dt> <dd>The promotion key. <i>Primary key in the {@link \RsPromotionSql} table.</i></dd> <dt>bool `is_primary`</dt> <dd>Whether the tag is the primary revenue category for the promotion.</dd> <dt>string `text_title`</dt> <dd>The promotion title.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> Appointment types assigned to the tag. Each element has the next structure: <dl> <dt>string `k_id`</dt> <dd>The service key. <i>Primary key in the {@link \RsServiceSql} table.</i></dd> <dt>bool `is_primary`</dt> <dd>Whether the tag is the primary revenue category for the service.</dd> <dt>string `text_title`</dt> <dd>The service title.</dd> </dl> </dd> <dt>array[] `a_product`</dt> <dd> Store products assigned to the tag. Each element has the next structure: <dl> <dt>string `k_id`</dt> <dd>The product key. <i>Primary key in the {@link \RsShopProductSql} table.</i></dd> <dt>bool `is_primary`</dt> <dd>Whether the tag is the primary revenue category for the product.</dd> <dt>string `text_title`</dt> <dd>The product title.</dd> </dl> </dd> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
      tags:
        - Tag-Tag
  '/Wl/Tag/TagList.json':
    get:
      summary: 'Returns tags of the specified business. Returns all client tags configured for the business in display order, along with flags indicating whether the business has configured a penalty fee for failed automatic payments and whether surcharges are enabled. Used to populate tag pickers and client profile forms.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'The tag list. Each element has the next structure: <dl> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `k_tag`</dt> <dd>The tag key. <i>Primary key in the {@link \RsTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
                      has_fee:
                        type: boolean
                        description: 'Whether a business did set up a penalty fee for failed automatic payments.'
                      has_surcharge:
                        type: boolean
                        description: 'Whether a business did set up surcharges.'
      tags:
        - Tag-TagList
    post:
      summary: 'Saves the list of tags. Can be used to create new tags or update existing ones. Persists the given set of client tags for the business. Tags without a key are created; tags with an existing key are updated with the new title. Requires backend access.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_list:
                  type: array
                  description: 'The tag list. Each element has the next structure: <dl> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `k_tag`</dt> <dd>The tag key. <i>Primary key in the {@link \RsTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
                json_list:
                  type: [string, "null"]
                  description: 'List of tags in json format. See {@link TagListApi::$a_list} for the structure of each tag. `null` to use {@link TagListApi::$a_list} for getting the tag list.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'The tag list. Each element has the next structure: <dl> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `k_tag`</dt> <dd>The tag key. <i>Primary key in the {@link \RsTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
      tags:
        - Tag-TagList
  '/Wl/Schedule/ScheduleAvailableDate.json':
    get:
      summary: 'Finds the nearest class session that can be booked by the current user. Looks no further than {@link ScheduleAvailableDateApi::PERIOD_LIMIT} seconds ahead from the start date.'
      parameters:
        - name: a_class
          in: query
          description: 'Class keys to filter. Empty to search for all classes.'
          schema:
            type: array
        - name: a_day
          in: query
          description: 'IDs of week days from {@link \ADateWeekSid} class. Empty to search for all week days.'
          schema:
            type: array
        - name: a_event
          in: query
          description: 'Event keys to filter. Empty to search for all events.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'Location keys to filter. Empty to search in all locations.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'Staff member keys to filter. Empty to search for all staff members.'
          schema:
            type: array
        - name: a_time
          in: query
          description: 'Time interval: Empty to search for all time. <dl> <dt>int `tl_end`</dt> <dd>End time.</dd> <dt>int `tl_start`</dt> <dd>Start time.</dd> </dl>'
          schema:
            type: array
        - name: dtu_start
          in: query
          description: 'The date/time to start from in UTC.'
          schema:
            type: string
        - name: id_class_tab
          in: query
          description: 'Optional "Book now" tab ID filter. One of {@link TabSid} constants. `0` means this filter is not set. If both {@link ScheduleAvailableDateApi::$id_class_tab} and {@link ScheduleAvailableDateApi::$k_class_tab} are not set, all tabs are used.'
          schema:
            type: number
            format: int32
        - name: is_class
          in: query
          description: '`true` to include classes; `false` to exclude.'
          schema:
            type: boolean
        - name: is_event
          in: query
          description: '`true` to include events; `false` to exclude.'
          schema:
            type: boolean
        - name: is_virtual
          in: query
          description: '`true` to include only virtual classes; `false` to include only in-person; `null` to no filtering.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'Optional book now tab key filter. Empty string means this filter is not set. If both {@link ScheduleAvailableDateApi::$k_class_tab} and {@link ScheduleAvailableDateApi::$id_class_tab} are not set, all tabs are used.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dl_next_available:
                        type: [string, "null"]
                        description: 'Nearest session date available for booking in user''s or business timezone. `null` if there is no suitable session found.'
      tags:
        - Schedule-ScheduleAvailableDate
  '/Wl/Schedule/Cancel.json':
    get:
      summary: 'Cancels session for the client. Cancels the specified appointment or class period for the given user. Staff and admin users may cancel on behalf of any client; regular clients may only cancel their own bookings if the visit is still in a cancellable state. Clears cached schedule data after a successful class period cancellation.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date of the session in UTC.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the back end. Otherwise, this will be `false`. Here, the back end refers to either a staff member or admin from the side of the business.'
          schema:
            type: boolean
        - name: is_late_cancel
          in: query
          description: '<tt>true</tt> is late cancel, <tt>false</tt> reservation is not late cancel. This is required to enable late cancel even if the user is staff.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This will be `null` if not set yet or if a class or event is canceled.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of the business within which the action is performed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be `null` if not set yet or if an appointment is canceled.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Schedule-Cancel
    post:
      summary: 'Cancels session for the client. This method is an alias for partners using the API or SDK. Identical in behavior to {@link CancelApi::get()}; exists as a POST alias for partner integrations that cannot issue GET requests.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date of the session in UTC.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the back end. Otherwise, this will be `false`. Here, the back end refers to either a staff member or admin from the side of the business.'
          schema:
            type: boolean
        - name: is_late_cancel
          in: query
          description: '<tt>true</tt> is late cancel, <tt>false</tt> reservation is not late cancel. This is required to enable late cancel even if the user is staff.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This will be `null` if not set yet or if a class or event is canceled.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of the business within which the action is performed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be `null` if not set yet or if an appointment is canceled.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Schedule-Cancel
  '/Wl/Event/EventEnrollment.json':
    get:
      summary: 'Returns a list of clients enrolled in the specified event classes. An enrollment is considered to exist for any client that has an active (not canceled, not missed) visit, past or future, for one of the specified classes.'
      parameters:
        - name: a_class
          in: query
          description: 'List of event classes for which enrollment must be retrieved.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Key of the business to which the event classes belong.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_enrollments:
                        type: array
                        description: 'A list of clients enrolled in the specified event classes. Key is the class key. <i>Primary key in {@link \RsClassSql} table.</i> Value is a list of clients enrolled in that class. Each element contains: <dl> <dt>bool `is_enrolled_full`</dt> <dd> <tt>true</tt> if the client has a block event enrollment record set for this class (booked the whole event); <tt>false</tt> otherwise. </dd> <dt>string `uid`</dt> <dd> Key of the client who attended or will attend the event. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> </dl>'
      tags:
        - Event-EventEnrollment
  '/Wl/Event/EventList.json':
    get:
      summary: 'Returns the list of events matching the specified filter parameters. Used to display the event schedule on business and location pages. Supports filtering by location, date range, staff, and booking status. Results are cached per business and user context; the cache is invalidated automatically when events are modified.'
      parameters:
        - name: a_class_filter
          in: query
          description: 'List of class keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_day
          in: query
          description: 'List of day the week applied by filter {@link \ADateWeekSid}.'
          schema:
            type: [array, "null"]
        - name: a_enrollment_block_filter
          in: query
          description: 'List of enrollment blocks keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_location
          in: query
          description: 'List of location keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_staff
          in: query
          description: 'List of staff keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_time
          in: query
          description: 'List of time day applied by filter {@link \RsScheduleTimeSid}.'
          schema:
            type: [array, "null"]
        - name: a_uid_staff
          in: query
          description: 'List of staff UIDs applied by filter. `null` if the filter by staff is not applied. Cannot be specified together with {@link EventListApi::$a_staff}.'
          schema:
            type: [array, "null"]
        - name: a_virtual
          in: query
          description: 'List of IDs to include/exclude virtual events. If the only ID is {@link YesNoSid::YES}, only virtual events are included. If the only ID is {@link YesNoSid::NO}, only in-person events are included. Otherwise, no filtering is done.'
          schema:
            type: [array, "null"]
        - name: dl_end
          in: query
          description: 'The end date of the range from which a list of events should be retrieved. <tt>null</tt> if the range has no end date.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'The start date of the range from which a list of events should be retrieved. <tt>null</tt> if the range has no start date.'
          schema:
            type: [string, "null"]
        - name: id_flag
          in: query
          description: 'Defines how the event availability flag filter should be applied. One of {@link \AFlagSid} constants. * {@link \AFlagSid::ON} to show only available events. * {@link \AFlagSid::OFF} to show only unavailable events. * {@link \AFlagSid::ALL} to show all events (available and unavailable).'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'Determines whether the endpoint is used for backend mode.'
          schema:
            type: boolean
        - name: is_cache_reset
          in: query
          description: 'Model cache reset flag.'
          schema:
            type: boolean
        - name: is_ignore_requirement
          in: query
          description: '`true` to show even event restricted by booking policies; `false` to show available events only.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: 'Determines whether you need to retrieve a list of event sessions regardless of the tab specified in {@link EventListApi::$k_class_tab}. * <tt>true</tt> - retrieves a list regardless of the specified tab. * <tt>false</tt> - retrieves a list only for the specific tab.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The event business key to retrieve a list of all event sessions in business. `null` to retrieve events from {@link EventListApi::$k_location}.'
          schema:
            type: [string, "null"]
        - name: k_class
          in: query
          description: 'The event class key to retrieve a list of all event sessions of a specific class. `null` to retrieve a list of event sessions of all classes.'
          schema:
            type: [string, "null"]
        - name: k_class_tab
          in: query
          description: 'The class tab key to retrieve a list of event sessions from a specific tab only. An empty value to retrieve a list of event sessions that don''t belong to any tab. Will be ignored in next cases: * {@link EventListApi::$k_skin} specified. * {@link EventListApi::$is_tab_all} is <tt>true</tt>.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The event location key to retrieve a list of all event sessions in a specific location. Required if {@link EventListApi::$k_business} isn''t specified. `null` if you need to retrieve a list of event sessions in all locations of {@link EventListApi::$k_business}.'
          schema:
            type: [string, "null"]
        - name: k_skin
          in: query
          description: 'The skin key if an event list is used for widget mode. {@link EventListApi::$k_class_tab} will be ignored for widget mode.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Search string to filter events by name.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. Required to apply specific user rules such as age restrictions.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_enrollment_block_list:
                        type: array
                        description: 'List of available enrollment blocks matching the requested parameters. Key is the enrollment block key. <i>Primary key in the {@link \RsEnrollmentBlockSql} table.</i> Value is the title of the first event found in that enrollment block.'
                      a_event_list:
                        type: array
                        description: 'A list of events corresponding to requested parameters. <dl> <dt>array `a_age_restriction`</dt> <dd> Information about age restrictions for this event, has the following structure: <dl> <dt>int|null `i_age_from`</dt> <dd> The minimum age for participation in the event. `null` if there''s no minimum age set or information isn''t available. </dd> <dt>int|null `i_age_to`</dt> <dd> The age limit for participation in the event. `null` if there''s no age limit set or information isn''t available. </dd> <dt>bool `is_age_public`</dt> <dd> `true` if age restrictions are public and available, `false` if they''re hidden. When restrictions are hidden and the current user isn''t a staff member, the age range will be empty. </dd> </dl> </dd> <dt>string[] `a_class_tab`</dt> <dd>List of book now tags connected to this event.</dd> <dt>array `a_logo`</dt> <dd>Data about logo of the event.</dd> <dt>array[] `a_schedule`</dt> <dd> List of scheduled sessions of the event. <dl> <dt>int[] `a_day`</dt> <dd>List of weekday numbers when event occur.</dd> <dt>array `a_repeat`</dt> <dd> Information about event repeating. <dl> <dt>int `i_repeat`</dt> <dd>Count of the periods which are specified in <var>id_repeat</var>.</dd> <dt>int `id_repeat`</dt> <dd>Measuring unit of <var>i_repeat</var> (week, month, year) from {@link \ADurationSid}.</dd> </dl> </dd> <dt>array[] `a_staff_member`</dt> <dd> List of staff members providing event session. <dl> <dt>string `k_staff_member`</dt> <dd>Staff member key.</dd> <dt>string `text_business_role`</dt> <dd>Staff member role title.</dd> <dt>string `text_mail`</dt> <dd>Staff member email address.</dd> <dt>string `text_name_first`</dt> <dd>Staff member first name.</dd> <dt>string `text_name_full`</dt> <dd>Staff member full name.</dd> <dt>string `text_name_last`</dt> <dd>Staff member last name.</dd> <dt>string `uid`</dt> <dd>Staff member uid.</dd> </dl> </dd> <dt>string `dl_end`</dt> <dd>End date of the schedule in <tt>MySql</tt> format.</dd> <dt>string `dl_start`</dt> <dd>Start date of the schedule in <tt>MySql</tt> format.</dd> <dt>bool `is_day`</dt> <dd>Whether this is a single day schedule (start and end dates of the schedule are the same).</dd> <dt>string `k_class_period`</dt> <dd>Class period key.</dd> <dt>string `k_location`</dt> <dd>Location key.</dd> <dt>string `k_resource_location`</dt> <dd> Resource key, which has category {@link \Wl\Resource\ResourceCategoryEnum::LOCATION}. Only one such resource can be selected for a schedule. Empty string, if no off-site location is used. `null` if field is not loaded. </dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `text_room`</dt> <dd>Room where the session takes place.</dd> <dt>string `text_time`</dt> <dd>Start and end time of the scheduled sessions in human readable format.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd>List of search tags connected to this event.</dd> <dt>bool `can_book`</dt> <dd> Whether event can be booked or not. * `true` - there are no restrictions to book this event in general. * `false` - for some reason event cannot be booked. The reason can be found in the following fields: * {@link \Wl\Event\EventListItemEntity::$html_reason} * {@link \Wl\Event\EventListItemEntity::$id_reason} * {@link \Wl\Event\EventListItemEntity::$sid_reason} This field can be used instead of {@link \Wl\Event\EventListItemEntity::$is_bookable} and covers a significant number of checks under its value. </dd> <dt>bool `can_cancel`</dt> <dd>Whether current user can cancel already booked event.</dd> <dt>string `dl_early`</dt> <dd>End date, when early bird price ends in <tt>MySql</tt> format.</dd> <dt>string `dl_end`</dt> <dd>End date of the scheduled session in <tt>MySql</tt> format.</dd> <dt>string `dl_session`</dt> <dd>Local date of the closest session of the event.</dd> <dt>string `dl_start`</dt> <dd>Start date of the scheduled sessions in <tt>MySql</tt> format.</dd> <dt>string `dtu_session`</dt> <dd>Date of the closest session of the event.</dd> <dt>string `html_reason`</dt> <dd> Reason why session can not be booked. Not empty only if {@link \Wl\Event\EventListItemEntity::$is_bookable} is `false`. </dd> <dt>int `i_book_active`</dt> <dd> Number of clients in the active list. For non-block events, this is the sum across all future sessions of the event (same aggregation as used for {@link \Wl\Event\EventListItemEntity::$is_full}). For block events, this is the count for the whole block. </dd> <dt>int `i_capacity`</dt> <dd> Capacity of the active list. For non-block events, this is {@link \Wl\Event\EventListItemEntity::$i_session_future} multiplied by the capacity of a single session (same aggregation as used for {@link \Wl\Event\EventListItemEntity::$is_full}). For block events, this is the capacity of the whole block. </dd> <dt>int `i_session_all`</dt> <dd>Number of all sessions in the event.</dd> <dt>int `i_session_future`</dt> <dd>Number of future sessions in the event.</dd> <dt>int `i_session_past`</dt> <dd>Number of past sessions in the event.</dd> <dt>int `i_wait`</dt> <dd> Number of clients in the wait list. For non-block events, this is the sum across all future sessions of the event. For block events, this is the count for the whole block. </dd> <dt>int|null `i_wait_limit`</dt> <dd> Wait list limit of the event. `null` if wait list is not enabled for this event, or if it is enabled without a limit. </dd> <dt>int `id_reason`</dt> <dd>ID of deny reason.</dd> <dt>bool `is_age_restrict`</dt> <dd>Whether booking of this event restricted because of age rules for {@link \Wl\Event\EventListApi}.</dd> <dt>bool `is_age_restrict_only`</dt> <dd>`true` if this event booking is restricted and restricted because of client''s age only. `false` otherwise.</dd> <dt>bool|null `is_available`</dt> <dd> Whether the event is available for booking or not. The flag refers to the Available/Unavailable filter. `null` if the field is not initialized. </dd> <dt>bool `is_block`</dt> <dd> Whether single sessions of the event can be booked. Depending on current backend/fronted status. <i> Contains one of values: * {@link \RsClassSql::$is_block} - if frontend. * {@link \RsClassSql::$is_block_staff} - if backend. </i> <tt>true</tt> - booking single sessions are not allowed. <tt>false</tt> - booking single sessions are allowed. </dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_bookable`</dt> <dd> Whether event is bookable. * `true` - there are no restrictions to book this event in general. * `false` - for some reason event cannot be booked. <b>Attention!!!</b> Historically, the value of this field may not cover all expected checks, and may be confusing to someone. It is properly used in combination with a number of other flags. If you need a flag that covers most of the checks please use {@link \Wl\Event\EventListItemEntity::$can_book} field. </dd> <dt>bool `is_booked`</dt> <dd>Whether event is already booked.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this event. Otherwise, this will be `false`.</dd> <dt>bool `is_closed`</dt> <dd> Whether booking of the event is closed already. Means that in general it is bookable but currently booking is closed. </dd> <dt>bool `is_full`</dt> <dd>Whether event is full already.</dd> <dt>bool `is_online`</dt> <dd>`true` if this class can be booked by any client; `false` otherwise.</dd> <dt>bool `is_online_private`</dt> <dd> `true` means to show class only for clients who can book online, `false` means to show class for all clients. </dd> <dt>bool `is_open`</dt> <dd>Whether event sessions can be booked after event has started.</dd> <dt>bool `is_promotion_only`</dt> <dd> Whether clients of the business can pay for the event with purchase option only. `true` if event can be paid with purchase option only. `false` if full event purchase or single session purchase are allowed. </dd> <dt>bool `is_prorate`</dt> <dd>Whether event sessions can be booked partially.</dd> <dt>bool `is_single_buy`</dt> <dd>Whether class/event can be paid with single session.</dd> <dt>bool `is_user_booked`</dt> <dd> Whether current user is booked or on the wait list. Unlike {@link \Wl\Event\EventListItemEntity::$is_booked}, this field is also `true` when the user is on the wait list, not only when actually booked into the active list. </dd> <dt>bool `is_virtual`</dt> <dd>Whether event is virtual.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>Whether wait list is enabled for this event.</dd> <dt>string `k_class`</dt> <dd>Class key.</dd> <dt>string `k_class_period`</dt> <dd>Class period key of the closest session of the event.</dd> <dt>string `k_enrollment_block`</dt> <dd>Key of enrollment block that corresponds to current row.</dd> <dt>string `k_location`</dt> <dd>Location key of the closest session of the event.</dd> <dt>string `m_price_max`</dt> <dd>Maximum price per session in the event.</dd> <dt>string `m_price_min`</dt> <dd>Minimum price per session in the event.</dd> <dt>string `m_price_total`</dt> <dd>Price of the entire event.</dd> <dt>string `m_price_total_early`</dt> <dd>Early bird price of the entire event.</dd> <dt>string `sid_reason`</dt> <dd> Code of {@link \Wl\Event\EventListItemEntity::$html_reason}. Is used for testing purposes. Not empty only if {@link \Wl\Event\EventListItemEntity::$is_bookable} is `false`. </dd> <dt>bool `text_age_restrict`</dt> <dd>Text message of the restriction based on age rules.</dd> <dt>string `text_title`</dt> <dd>Title of the event.</dd> <dt>string|null `url_book`</dt> <dd> Link to the start of the booking wizard to book the closed session from this event or the entire event. Can be `null` if there is no available for booking sessions. </dd> <dt>string `xml_description`</dt> <dd>Description of the event.</dd> </dl>'
      tags:
        - Event-EventList
    put:
      summary: 'Resets the event list cache for the specified business and user. Call this after modifying event data to force the next request to rebuild the schedule from the database. Can also conditionally reset without parameters if the caller determines a reset is needed.'
      parameters:
        - name: is_cache_reset
          in: query
          description: 'Model cache reset flag.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The event business key to retrieve a list of all event sessions in business. `null` to retrieve events from {@link EventListApi::$k_location}.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. Required to apply specific user rules such as age restrictions.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Event-EventList
  '/Wl/Event/EventCancelWhole.json':
    post:
      summary: 'Cancels book of event {@link EventCancelWholeApi::$k_class}. Used when a client wants to drop out of a multi-session event entirely. Cancels all remaining upcoming sessions at once, including any waitlisted spots, without requiring the client to cancel each session individually.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business in which the class resides.'
                k_class:
                  type: string
                  description: 'An event key.'
                uid:
                  type: string
                  description: 'A user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Event-EventCancelWhole
  '/Wl/Profile/ProfileCreate.json':
    post:
      summary: 'Creates a new client profile with the provided personal details in the specified business. Creates or retrieves a user account by email or phone, saves personal details such as name, address, phones, birthday, gender, and vaccination status, registers the user in the business, and optionally adds them to the lead report and sets intents.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                dt_birthday:
                  type: string
                  description: 'Date of the user''s birthday in MySQL format.'
                id_gender:
                  type: number
                  format: int32
                  description: 'Gender ID. One of the {@link GenderSid} constants. `0` if not specified.'
                id_lead_source:
                  type: number
                  format: int32
                  description: 'Lead source ID. One of the {@link ModeSid} constants. `0` if not specified.'
                id_vaccination_status:
                  type: number
                  format: int32
                  description: 'Vaccination status ID. One of the {@link VaccinationStatusSid} constants. `0` if not specified.'
                is_lead:
                  type: boolean
                  description: '`true` means to add user to the legacy lead report. `false` means to not add user to the legacy lead report. Note, that this setting does not impact new Lead Management report, which will always include new user. Also lead capture marketing notification will never be triggered with this endpoint.'
                k_business:
                  type: string
                  description: 'The key of the business.'
                k_lead_source:
                  type: string
                  description: 'The key of the lead source. Empty string if not specified.'
                k_location_home:
                  type: string
                  description: 'The key of the home location.'
                text_address:
                  type: string
                  description: 'The address of the user.'
                text_firstname:
                  type: string
                  description: 'First name of the user.'
                text_lastname:
                  type: string
                  description: 'Last name of the user.'
                text_mail:
                  type: string
                  description: 'Email of the user. Required if {@link ProfileCreateApi::$text_phone} not provided.'
                text_phone:
                  type: string
                  description: 'Phone of the user. Required if {@link ProfileCreateApi::$text_mail} not provided.'
                text_phone_home:
                  type: string
                  description: 'Home phone of the user.'
                text_phone_work:
                  type: string
                  description: 'Work phone of the user.'
                uid_referrer:
                  type: string
                  description: 'Referrer user key. Empty string if not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'The key of the user.'
      tags:
        - Profile-ProfileCreate
  '/Wl/Session/Environment.json':
    get:
      summary: 'Returns current session environment. Determines the current business, location, and user for the session, taking into account the application type, franchise settings, and multi-datacenter routing, and returns the resulting environment data used to bootstrap the client application.'
      parameters:
        - name: s_application_id
          in: query
          description: 'Application ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: [array, "null"]
                        description: 'List of businesses which are available in given application. <tt>null</tt> for default application.'
                      a_business_franchise_all:
                        type: [array, "null"]
                        description: 'List of businesses keys, which are connected to franchise. <tt>null</tt> if current business is not part of franchise.'
                      a_business_franchisee:
                        type: [array, "null"]
                        description: 'List of business franchisee keys. Can be limited by levels to which a user can view/access in frontend. See {@link FranchiseSwitchSid}. <tt>null</tt> if current business is not part of franchise or if ''Allow clients to switch between franchise locations'' is OFF.'
                      a_payment_alert:
                        type: [array, "null"]
                        description: 'Payment alert data. `null` if user is not logged or none businesses are available for user, or if it''s not backend. Otherwise, contains data: <dl> <dt>int `i_duration`</dt> <dd> Time left in seconds. <var>null</var> if business account is not created or if service interruption dates aren''t specified. </dd> <dt>bool `is_alert`</dt> <dd>Determines whether to show an alert. Alert should be shown only once per session.</dd> <dt>bool `is_churn`</dt> <dd>Determines whether the business is churned.</dd> <dt>string `url_billing`</dt> <dd>Link to business billing page on the web.</dd> </dl>'
                      a_splash_screen:
                        type: [array, "null"]
                        description: 'Settings of splash screen customization for business: <tt>null</tt> if splash screen in not customized for business. <dl> <dt>array `a_background`</dt> <dd> Background image. <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>array `a_gradient`</dt> <dd> Background color. <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Logo image. <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> </dl>'
                      dtl_now:
                        type: string
                        description: 'Local date with time now in current location {@link EnvironmentApi::$k_location}.'
                      has_form_quiz:
                        type: boolean
                        description: 'Whether exists form to confirm during registration process.'
                      has_home:
                        type: boolean
                        description: '`true` if the "home" page is turned on for the current business; `false` otherwise.'
                      has_merchant:
                        type: boolean
                        description: 'Whether the merchant is configured for the client''s home location or for business {@link EnvironmentApi::$k_business}.'
                      id_currency:
                        type: string
                        description: 'Currency ID. One of {@link \RsCurrencySid} constants.'
                      id_locale:
                        type: number
                        format: int32
                        description: 'Business locale. One of {@link LocaleSid} constants.'
                      id_place:
                        type: number
                        format: int32
                        description: 'Current place ID. One of {@link \RsPlaceSid} constants. For the Application is determined by the template. For the Web version of the site is determined by the current session.'
                      id_plan_achieve:
                        type: number
                        format: int32
                        description: 'Achieve subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Achieve\AchieveSubscriptionSid} constants.'
                      id_plan_base:
                        type: number
                        format: int32
                        description: 'Base subscription plan ID.'
                      id_plan_marketing:
                        type: number
                        format: int32
                        description: 'Marketing subscription plan ID.'
                      id_travel_mode:
                        type: [number, "null"]
                        description: 'Current travel mode ID in business. `null` if travel mode is disabled.'
                      is_aiagent_chat:
                        type: boolean
                        description: 'Whether AI agent chat (CAASI) is available for current business. * `true` if chat is available * `false` if chat is not available'
                      is_application_custom:
                        type: boolean
                        description: 'Determines current business has custom application. If <tt>true</tt> application has custom application, <tt>false</tt> otherwise.'
                      is_billing_required:
                        type: boolean
                        description: '<tt>true</tt> if user must be redirected to billing page to enter credit card; <tt>false</tt> otherwise.'
                      is_booking_appointment_authorize:
                        type: boolean
                        description: '<tt>true</tt> if clients must sign in to book an appointment; <tt>false</tt> otherwise.'
                      is_booking_required_billing:
                        type: boolean
                        description: '<tt>true</tt> if user must be redirected from booking page to billing page to enter credit card; <tt>false</tt> otherwise.'
                      is_business_location_multiple:
                        type: boolean
                        description: 'Determines if a business has multiple locations. <i>See {@link \rs_business_location_multiple()}.</i>'
                      is_client_profile_share:
                        type: [boolean, "null"]
                        description: '`true` id profile details can be shared with the new location while adding a client that already has an existing profile in franchise, `false` - otherwise. * `null` if not franchise.'
                      is_client_select_date_asset:
                        type: boolean
                        description: 'If clients are allowed to select a date and time, then the available asset while asset booking.'
                      is_contract:
                        type: boolean
                        description: '<tt>true</tt> - user has unassigned contract(s); <tt>false</tt> - otherwise.'
                      is_family_relation_display_client:
                        type: boolean
                        description: '`true` is allowed relationship to display, `false` - otherwise.'
                      is_franchisor:
                        type: boolean
                        description: '<tt>true</tt> - if business is franchisor; <tt>false</tt> - otherwise.'
                      is_location_geo:
                        type: boolean
                        description: '<tt>true</tt> to use page of geographical search of locations (possible for franchise case only); <tt>false</tt> to use simple select of locations from list.'
                      is_location_redirect:
                        type: boolean
                        description: '<tt>true</tt> - in backend application user must be redirected to location selection; <tt>false</tt> - otherwise.'
                      is_profile_required:
                        type: boolean
                        description: '<tt>true</tt> if user must be redirected to profile page to enter mandatory fields; <tt>false</tt> otherwise.'
                      is_request_required_information:
                        type: boolean
                        description: 'Indicates whether to obtain customer required information such as required profile data and waiver. Mostly used for selenium tests running purposes to avoid creating profile filled with required information. If <tt>true</tt> all required information must be obtained before client will be allowed to use application, <tt>false</tt> otherwise.'
                      is_traveller:
                        type: boolean
                        description: '<tt>true</tt> if user is traveler; <tt>false</tt> otherwise.'
                      is_waiver_required:
                        type: boolean
                        description: '<tt>true</tt> if it is required to sign online waiver, <tt>false</tt> otherwise.'
                      k_business:
                        type: string
                        description: 'Key of business to be set as current.'
                      k_business_franchisor:
                        type: [string, "null"]
                        description: 'Not <tt>null</tt> for franchise application only.'
                      k_currency:
                        type: string
                        description: 'Currency key.'
                      k_location:
                        type: string
                        description: 'Key of location to be set as current.'
                      text_country_code:
                        type: string
                        description: 'Country code for business locale {@link LocaleSid} constants.'
                      uid:
                        type: [string, "null"]
                        description: 'Current user key. <tt>null</tt> if user is not signed in.'
                      url_login:
                        type: [string, "null"]
                        description: 'Business login URL. `null` if user is not logged in or none businesses are available for user.'
      tags:
        - Session-Environment
    put:
      summary: 'Saves current location of business for current user. Switches the current business and location for the session, forwarding the request to another data center when the target business is hosted elsewhere.'
      parameters:
        - name: s_application_id
          in: query
          description: 'Application ID.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_staff:
                  type: boolean
                  description: '`true` if you want the API to switch to staff back office; `false` otherwise.'
                k_business:
                  type: string
                  description: 'Key of business to be set as current.'
                k_location:
                  type: string
                  description: 'Key of location to be set as current.'
                s_business_change:
                  type: string
                  description: 'Key to check session in a case of business change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Session-Environment
  '/Wl/Quiz/QuizElement72.json':
    delete:
      summary: 'Deletes the quiz with the given key. Permanently removes the quiz and all associated elements.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Quiz-QuizElement72
    get:
      summary: 'Returns quiz element data including settings, elements, and access information for the given quiz. Loads the quiz configuration, element list, and access log for the specified business and user context, applying franchise and privilege checks before returning the result.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_builder
          in: query
          description: '`true` for request quiz from form builder page, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: json_purchase_item
          in: query
          description: 'List of purchase items for which this form is loaded in JSON format. This variable supports two structures: New structure: Each element key has the format `[id_purchase_item]::[k_id]`, where: Old structure: Each element is a string in the format `[id_purchase_item]::[k_id]`. Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl> <dl> <dt>string|null `dl_start`</dt> <dd>Overridden start date of the purchase option.</dd> <dt>string|null `f_price`</dt> <dd>Overridden price of the purchase option.</dd> <dt>int `i_duration`</dt> <dd>Duration of the purchase option.</dd> <dt>int `i_payment_period`</dt> <dd>Payment period for the purchase option.</dd> <dt>int `id_duration`</dt> <dd>Duration type, one of {@link \RsDurationTypeSid} constants.</dd> </dl>'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'UID of the client for which quiz requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether user has privileges to amend form.'
                      i_responses:
                        type: number
                        format: int32
                        description: 'Number of responses for specific quiz.'
                      is_active:
                        type: boolean
                        description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                      is_imported:
                        type: boolean
                        description: '`true` if quiz is imported, `false` otherwise.'
                      is_prevent_franchisee:
                        type: boolean
                        description: 'Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise.'
                      k_business_type:
                        type: [string, "null"]
                        description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_title:
                        type: string
                        description: 'Quiz form title.'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement72
    post:
      summary: 'Creates or updates a quiz with the given elements and settings. When no quiz key is provided, a new quiz is created; when a key is given, the existing quiz is updated in place. Element list and configuration are saved in a single transaction.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_setting:
                  type: array
                  description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                json_element:
                  type: string
                  description: 'List of quiz elements in json format. Order of the element in array corresponds to order of elements on the form.'
                k_business_type:
                  type: [string, "null"]
                  description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                show_numbering:
                  type: boolean
                  description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                text_title:
                  type: string
                  description: 'Quiz form title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz:
                        type: string
                        description: 'Quiz key.'
      tags:
        - Quiz-QuizElement72
    put:
      summary: 'Updates the active status of the given quiz. Toggles whether the quiz is available for respondents to fill out. Inactive quizzes are hidden from the booking and profile flows but their existing responses are preserved.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement72
  '/Wl/Quiz/QuizElement.json':
    delete:
      summary: 'Deletes the quiz with the given key. Permanently removes the quiz and all associated elements.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Quiz-QuizElement
    get:
      summary: 'Returns quiz element data including settings, elements, and access information for the given quiz. Loads the quiz configuration, element list, and access log for the specified business and user context, applying franchise and privilege checks before returning the result.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_builder
          in: query
          description: '`true` for request quiz from form builder page, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: json_purchase_item
          in: query
          description: 'List of purchase items for which this form is loaded in JSON format. This variable supports two structures: New structure: Each element key has the format `[id_purchase_item]::[k_id]`, where: Old structure: Each element is a string in the format `[id_purchase_item]::[k_id]`. Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl> <dl> <dt>string|null `dl_start`</dt> <dd>Overridden start date of the purchase option.</dd> <dt>string|null `f_price`</dt> <dd>Overridden price of the purchase option.</dd> <dt>int `i_duration`</dt> <dd>Duration of the purchase option.</dd> <dt>int `i_payment_period`</dt> <dd>Payment period for the purchase option.</dd> <dt>int `id_duration`</dt> <dd>Duration type, one of {@link \RsDurationTypeSid} constants.</dd> </dl>'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'UID of the client for which quiz requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether user has privileges to amend form.'
                      i_responses:
                        type: number
                        format: int32
                        description: 'Number of responses for specific quiz.'
                      is_active:
                        type: boolean
                        description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                      is_imported:
                        type: boolean
                        description: '`true` if quiz is imported, `false` otherwise.'
                      is_prevent_franchisee:
                        type: boolean
                        description: 'Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise.'
                      k_business_type:
                        type: [string, "null"]
                        description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_title:
                        type: string
                        description: 'Quiz form title.'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement
    post:
      summary: 'Creates or updates a quiz with the given elements and settings. When no quiz key is provided, a new quiz is created; when a key is given, the existing quiz is updated in place. Element list and configuration are saved in a single transaction.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_setting:
                  type: array
                  description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                json_element:
                  type: string
                  description: 'List of quiz elements in json format. Order of the element in array corresponds to order of elements on the form.'
                k_business_type:
                  type: [string, "null"]
                  description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                show_numbering:
                  type: boolean
                  description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                text_title:
                  type: string
                  description: 'Quiz form title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz:
                        type: string
                        description: 'Quiz key.'
      tags:
        - Quiz-QuizElement
    put:
      summary: 'Updates the active status of the given quiz. Toggles whether the quiz is available for respondents to fill out. Inactive quizzes are hidden from the booking and profile flows but their existing responses are preserved.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement
  '/Wl/Report/Data.json':
    get:
      summary: 'Gets data of required report. Loads the specified report for the given business, applying filter, sort, and pagination parameters, and returns the report rows and totals.'
      parameters:
        - name: i_page
          in: query
          description: 'The page of results to show, starting at zero. The API will return 256 results per page.'
          schema:
            type: number
            format: int32
        - name: id_report
          in: query
          description: 'The report ID. The following reports are currently available via the API: <ul> <li>{@link \RsReportSid::LOGIN_LIST} - All Clients Report (Reports tab, features date search)</li> <li>{@link \RsReportSid::LOGIN_LIST_ALL} - All Clients Report (Clients tab) Batch Report</li> <li>{@link \RsReportSid::PAY_TRANSACTION_BATCH} - Batch Report</li> <li>{@link \RsReportSid::PURCHASE_ITEM_LIST_USER} - Sales per Client Report</li> <li>{@link \RsReportSid::PURCHASE_ITEM_ACCRUAL_CASH} - All Sales Report</li> </ul>'
          schema:
            type: number
            format: int32
        - name: id_report_group
          in: query
          description: 'The report group ID.'
          schema:
            type: number
            format: int32
        - name: id_report_page
          in: query
          description: 'The report page ID. One of the {@link \RsReportPageSid} constants. If set, the collection of that page will be used, otherwise a collection of single report will be used.'
          schema:
            type: [number, "null"]
        - name: id_report_view
          in: query
          description: 'The report view ID. One of the {@link \RsReportChartViewSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of business for which the report must be generated.'
          schema:
            type: string
        - name: s_filter
          in: query
          description: 'Filter settings in encoded format. <i>May be decoded by {@link \Core\Tool\UrlEncode\UrlDecode::decode()}.</i>'
          schema:
            type: string
        - name: s_sort
          in: query
          description: 'The field to use for sorting report data.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'The report contents. Contents of this array can vary based on the report that''s loaded.'
                      a_total:
                        type: array
                        description: 'The report total. Contents of this array can vary based on the report that''s loaded.'
      tags:
        - Report-Data
  '/Wl/Report/PageData.json':
    get:
      summary: 'Gets data of required report collection. Loads all reports belonging to the specified report page collection for the given business, applying shared filter, sort, group, and view parameters, and returns the data for each report keyed by report ID.'
      parameters:
        - name: a_page
          in: query
          description: 'The page of results to show for each report in collection, starting at zero (set by default). The API will return 256 results per page. Key is report id from {@link \RsReportSid}, value is the page of results.'
          schema:
            type: array
        - name: id_report_group
          in: query
          description: 'The report group ID. One of the {@link \RsReportGroupSid} constants that describes the time period (day, week, month, or year) for the report to cover.'
          schema:
            type: number
            format: int32
        - name: id_report_page
          in: query
          description: 'The report page ID. One of the {@link \RsReportPageSid} constants. The collection of that page will be used to get list of reports. The following report collections are currently available via the API: * PROFILE_PURCHASE (ID 11) Collection with reports from client profile: memberships, packages, session/duration passes, single sessions, events, gift cards and groupon.'
          schema:
            type: number
            format: int32
        - name: id_report_view
          in: query
          description: 'The report view ID. One of the {@link \RsReportChartViewSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of business for which the report collection must be generated.'
          schema:
            type: string
        - name: s_filter
          in: query
          description: 'Filter settings in encoded format. <i>May be decoded by {@link \Core\Tool\UrlEncode\UrlDecode::decode()}.</i>'
          schema:
            type: string
        - name: s_sort
          in: query
          description: 'The field to use for sorting report data.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'List of the reports contents. Key is the report id from {@link \RsReportSid}, value is report contents. Contents can vary based on the report that''s loaded.'
      tags:
        - Report-PageData
  '/Wl/Report/Access.json':
    get:
      summary: 'Checks access to given report. Accepts either {@link AccessApi::$id_report} (first-generation reports) or {@link AccessApi::$cid_report} (second-generation reports), but not both, and returns `has_access` indicating whether the current user may view the report in the given business.'
      parameters:
        - name: cid_report
          in: query
          description: 'Report CID. It''s used mostly for second generation reports. For first generation reports use {@link AccessApi::$id_report} instead. Only one of these two fields should be sent, but not both.'
          schema:
            type: number
            format: int32
        - name: id_report
          in: query
          description: 'Report ID. It''s used mostly for first generation reports. For second generation reports use {@link AccessApi::$cid_report} instead. Only one of these two fields should be sent, but not both.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'ID of business for which access must be checked.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_access:
                        type: boolean
                        description: '<tt>true</tt> - access is granted; <tt>false</tt> - access is denied.'
      tags:
        - Report-Access
  '/Wl/Promotion/Promotion.json':
    delete:
      summary: 'Soft-deletes the specified promotion if it has no associated sales. Validates staff privileges and checks that the promotion has not been sold before marking it as removed.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-Promotion
    get:
      summary: 'Returns promotion by the specified business and promotion keys. Returns full promotion details including access restrictions, image, pricing, duration, tags, locations, and guest pass settings if applicable. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion information. <dl> <dt>array `a_access`</dt> <dd> Information about services that can be attended with this pass or membership. <dl> <dt>array[] `a_class`</dt> <dd> A list of available classes. <dl> <dt>string `k_class`</dt> <dd>Key of the class.</dd> </dl> </dd> <dt>array[] `a_event`</dt> <dd> A list of available events. <dl> <dt>string `k_class`</dt> <dd>Key of the event.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of available assets. <dl> <dt>string `k_resource`</dt> <dd>Key of the asset.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> A list of available appointment types. <dl> <dt>string `k_service`</dt> <dd>Key of the appointment type.</dd> </dl> </dd> <dt>int `id_class_access`</dt> <dd> Class access flag, one of {@link \AFlagSid} constants. {@link \AFlagSid::ALL} if any class in the business can be visited with this Purchase Option, including classes created later. {@link \AFlagSid::ON} if only selected classes can be visited with this Purchase Option. Selected classes can be found in the list of available classes and events. </dd> <dt>bool `is_class_all`</dt> <dd> This will be `true` if any class in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected classes can be visited with this Purchase Option. Selected classes can be found in the list of available classes and events. Equivalent to <var>id_class_access</var> being {@link \AFlagSid::ALL}. </dd> <dt>bool `is_event_all`</dt> <dd> This will be `true` if any event in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected events can be visited with this Purchase Option. Selected events can be found in the list of available classes and events. </dd> </dl> </dd> <dt>string[] `a_franchise_region`</dt> <dd> A list of franchise regions where a guest may redeem this Guest Pass, in addition to the location that sold it. Only meaningful when <var>is_regional_access</var> is `true` and <var>is_regional</var> is `true`. <i>Each value is primary key in {@link \Wl\Business\Franchise\Region\RegionSql} table.</i> </dd> <dt>array `a_image`</dt> <dd> Information about Purchase Option image. This will be an empty array if the Purchase Option doesn''t have an image. <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>string `url-thumbnail`</dt> <dd>The link to the image.</dd> </dl> </dd> <dt>string[] `a_location`</dt> <dd> A list of locations where this promotion is available. <i>Each value is primary key in {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Login\Type\Sql}. `k_login_type` which is a primary key in {@link \Wl\Login\Type\Sql}. </i> </dd> <dt>string[] `a_member_group`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Member\Group\PromotionMemberGroupSql}. `k_member_group` which is a primary key in {@link \Wl\Member\Group\Sql}. </i> </dd> <dt>string[] `a_tag`</dt> <dd>List of selected tags. <i>Each value is a primary key in {@link \RsTagSql} table.</i></dd> <dt>string `dl_expire`</dt> <dd> The local expiration date. This is the last day when the Purchase Option is active. This is used only when the ''Expires on a certain date'' duration type is set. </dd> <dt>string `dl_terminate`</dt> <dd>Date when promotion should be terminated. `null` if there is no terminate date.</dd> <dt>string `f_revenue_single`</dt> <dd>Approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `f_revenue_single`.</i></dd> <dt>int `i_duration`</dt> <dd> The number of periods for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will be 12. </dd> <dt>int `i_limit`</dt> <dd> The number of visits that the owner can use for the Purchase Option. This will be `0` if the Purchase Option allows for unlimited visits. </dd> <dt>int `i_payment_period`</dt> <dd>The number of `id_duration` periods between payments for memberships.</dd> <dt>int `id_duration`</dt> <dd> The type of period for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will display the ID of the month. For all possible options, see {@link \ADurationSid}. </dd> <dt>int `id_duration_type`</dt> <dd> The duration type. Possible duration types can be ''Without End'', ''Expires on a certain date'', ''Period''. For more information, see {@link \RsDurationTypeSid}. </dd> <dt>int `id_program`</dt> <dd>The Purchase Option type. For more information, see {@link \RsProgramSid}.</dd> <dt>int `id_restriction`</dt> <dd>Purchase restriction for this product, one of {@link \Wl\Promotion\PurchaseRestrictionSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether this promotion is active. <i>Copy of {@link \RsPromotionSql} `is_active`.</i></dd> <dt>bool `is_business_wide`</dt> <dd> Whether this promotion is available for all locations of the business. <i>Copy of {@link \RsPromotionSql} `is_business_wide`.</i> </dd> <dt>bool `is_introductory`</dt> <dd>If `true`, this promotion is for introductory clients. Otherwise, this will be `false`.</dd> <dt>bool `is_online`</dt> <dd>If `true`, this promotion is available for online purchase. Otherwise, this will be `false`.</dd> <dt>bool `is_payroll`</dt> <dd> Whether clients who purchase this item excluded from payroll calculations. <i>Copy of {@link \RsPromotionSql} `is_payroll`.</i> </dd> <dt>bool `is_regional`</dt> <dd> Whether a guest may redeem this Guest Pass at other locations within the regions listed in <var>a_franchise_region</var>, in addition to the location that sold it. `false` means the pass can only be redeemed at the location that sold it. Only meaningful when <var>is_regional_access</var> is `true`. </dd> <dt>bool `is_regional_access`</dt> <dd> Whether the regional access setting can be configured for this Guest Pass promotion and business at all. `false` if the business does not take part in a franchise region, in which case <var>is_regional</var> and <var>a_franchise_region</var> should be ignored. </dd> <dt>bool `is_regional_access_readonly`</dt> <dd> Whether the regional access setting is read-only for this business. `true` for a franchisee location, which can only view the regions selected by the franchisor. </dd> <dt>bool `is_revenue_single`</dt> <dd> If `true` need to add approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `is_revenue_single`.</i> </dd> <dt>bool `is_use`</dt> <dd>Whether promotion can be used for booking. `null` if there is no termination.</dd> <dt>string `k_promotion`</dt> <dd>The key of the promotion. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_tag_primary`</dt> <dd>Primary tag. <i>Primary key in {@link \RsTagSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The price.</dd> <dt>string `text_description`</dt> <dd>The description of the Purchase Option.</dd> <dt>string `text_title`</dt> <dd>The title of the Purchase Option.</dd> <dt>string `url_buy`</dt> <dd>The direct URL to the promotion purchase page.</dd> <dt>string `url_catalog`</dt> <dd>The direct URL to the promotion details page in the online store.</dd> </dl>'
                      o_guest_settings:
                        type: [object, array, "null"]
                        description: 'Guest passes settings for promotion. This will be `null` if there are no guest pass settings for the promotion.'
      tags:
        - Promotion-Promotion
    post:
      summary: 'Saves promotion data. Accepts the full promotion configuration including title, description, access rules, duration, tags, and guest pass settings, then validates and persists the changes.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_promotion:
                  type: array
                  description: 'Promotion information. <dl> <dt>array `a_access`</dt> <dd> Information about services that can be attended with this pass or membership. <dl> <dt>array[] `a_class`</dt> <dd> A list of available classes. <dl> <dt>string `k_class`</dt> <dd>Key of the class.</dd> </dl> </dd> <dt>array[] `a_event`</dt> <dd> A list of available events. <dl> <dt>string `k_class`</dt> <dd>Key of the event.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of available assets. <dl> <dt>string `k_resource`</dt> <dd>Key of the asset.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> A list of available appointment types. <dl> <dt>string `k_service`</dt> <dd>Key of the appointment type.</dd> </dl> </dd> <dt>int `id_class_access`</dt> <dd> Class access flag, one of {@link \AFlagSid} constants. {@link \AFlagSid::ALL} if any class in the business can be visited with this Purchase Option, including classes created later. {@link \AFlagSid::ON} if only selected classes can be visited with this Purchase Option. Selected classes can be found in the list of available classes and events. </dd> <dt>bool `is_class_all`</dt> <dd> This will be `true` if any class in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected classes can be visited with this Purchase Option. Selected classes can be found in the list of available classes and events. Equivalent to <var>id_class_access</var> being {@link \AFlagSid::ALL}. </dd> <dt>bool `is_event_all`</dt> <dd> This will be `true` if any event in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected events can be visited with this Purchase Option. Selected events can be found in the list of available classes and events. </dd> </dl> </dd> <dt>string[] `a_franchise_region`</dt> <dd> A list of franchise regions where a guest may redeem this Guest Pass, in addition to the location that sold it. Only meaningful when <var>is_regional_access</var> is `true` and <var>is_regional</var> is `true`. <i>Each value is primary key in {@link \Wl\Business\Franchise\Region\RegionSql} table.</i> </dd> <dt>array `a_image`</dt> <dd> Information about Purchase Option image. This will be an empty array if the Purchase Option doesn''t have an image. <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>string `url-thumbnail`</dt> <dd>The link to the image.</dd> </dl> </dd> <dt>string[] `a_location`</dt> <dd> A list of locations where this promotion is available. <i>Each value is primary key in {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Login\Type\Sql}. `k_login_type` which is a primary key in {@link \Wl\Login\Type\Sql}. </i> </dd> <dt>string[] `a_member_group`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Member\Group\PromotionMemberGroupSql}. `k_member_group` which is a primary key in {@link \Wl\Member\Group\Sql}. </i> </dd> <dt>string[] `a_tag`</dt> <dd>List of selected tags. <i>Each value is a primary key in {@link \RsTagSql} table.</i></dd> <dt>string `dl_expire`</dt> <dd> The local expiration date. This is the last day when the Purchase Option is active. This is used only when the ''Expires on a certain date'' duration type is set. </dd> <dt>string `dl_terminate`</dt> <dd>Date when promotion should be terminated. `null` if there is no terminate date.</dd> <dt>string `f_revenue_single`</dt> <dd>Approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `f_revenue_single`.</i></dd> <dt>int `i_duration`</dt> <dd> The number of periods for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will be 12. </dd> <dt>int `i_limit`</dt> <dd> The number of visits that the owner can use for the Purchase Option. This will be `0` if the Purchase Option allows for unlimited visits. </dd> <dt>int `i_payment_period`</dt> <dd>The number of `id_duration` periods between payments for memberships.</dd> <dt>int `id_duration`</dt> <dd> The type of period for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will display the ID of the month. For all possible options, see {@link \ADurationSid}. </dd> <dt>int `id_duration_type`</dt> <dd> The duration type. Possible duration types can be ''Without End'', ''Expires on a certain date'', ''Period''. For more information, see {@link \RsDurationTypeSid}. </dd> <dt>int `id_program`</dt> <dd>The Purchase Option type. For more information, see {@link \RsProgramSid}.</dd> <dt>int `id_restriction`</dt> <dd>Purchase restriction for this product, one of {@link \Wl\Promotion\PurchaseRestrictionSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether this promotion is active. <i>Copy of {@link \RsPromotionSql} `is_active`.</i></dd> <dt>bool `is_business_wide`</dt> <dd> Whether this promotion is available for all locations of the business. <i>Copy of {@link \RsPromotionSql} `is_business_wide`.</i> </dd> <dt>bool `is_introductory`</dt> <dd>If `true`, this promotion is for introductory clients. Otherwise, this will be `false`.</dd> <dt>bool `is_online`</dt> <dd>If `true`, this promotion is available for online purchase. Otherwise, this will be `false`.</dd> <dt>bool `is_payroll`</dt> <dd> Whether clients who purchase this item excluded from payroll calculations. <i>Copy of {@link \RsPromotionSql} `is_payroll`.</i> </dd> <dt>bool `is_regional`</dt> <dd> Whether a guest may redeem this Guest Pass at other locations within the regions listed in <var>a_franchise_region</var>, in addition to the location that sold it. `false` means the pass can only be redeemed at the location that sold it. Only meaningful when <var>is_regional_access</var> is `true`. </dd> <dt>bool `is_regional_access`</dt> <dd> Whether the regional access setting can be configured for this Guest Pass promotion and business at all. `false` if the business does not take part in a franchise region, in which case <var>is_regional</var> and <var>a_franchise_region</var> should be ignored. </dd> <dt>bool `is_regional_access_readonly`</dt> <dd> Whether the regional access setting is read-only for this business. `true` for a franchisee location, which can only view the regions selected by the franchisor. </dd> <dt>bool `is_revenue_single`</dt> <dd> If `true` need to add approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `is_revenue_single`.</i> </dd> <dt>bool `is_use`</dt> <dd>Whether promotion can be used for booking. `null` if there is no termination.</dd> <dt>string `k_promotion`</dt> <dd>The key of the promotion. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_tag_primary`</dt> <dd>Primary tag. <i>Primary key in {@link \RsTagSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The price.</dd> <dt>string `text_description`</dt> <dd>The description of the Purchase Option.</dd> <dt>string `text_title`</dt> <dd>The title of the Purchase Option.</dd> <dt>string `url_buy`</dt> <dd>The direct URL to the promotion purchase page.</dd> <dt>string `url_catalog`</dt> <dd>The direct URL to the promotion details page in the online store.</dd> </dl>'
                o_guest_settings:
                  type: [object, array, "null"]
                  description: 'Guest passes settings for promotion. This will be `null` if there are no guest pass settings for the promotion.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-Promotion
  '/Wl/Promotion/PromotionList.json':
    get:
      summary: 'Returns promotion list of the specified business. Returns all active promotions for the given business, with optional inclusion of Enterprise Location promotions when the business is a franchisor and `is_franchise` is set to `true`.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Determines whether to return promotions created by Enterprise Locations (for Enterprise Cloud businesses only). If `true`, promotions created by Enterprise Locations are included. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key used to get the promotions.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'A list of promotions. <dl> <dt>int `id_program`</dt> <dd>The promotion program. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_program_type`</dt> <dd>The type of the promotion program. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether the promotion is active.</dd> <dt>bool `is_component`</dt> <dd>Whether the promotion is a component.</dd> <dt>bool `is_expired`</dt> <dd>Whether the promotion is expired.</dd> <dt>string `k_promotion`</dt> <dd>The key of the promotion. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The title of the promotion.</dd> </dl>'
      tags:
        - Promotion-PromotionList
  '/Wl/Login/Login.json':
    get:
      summary: 'Retrieves information about user. Returns name, gender, photo URL, email, and staff details for the specified user within the given business. Public staff information is returned even without profile access; full client details require access to the user''s profile.'
      parameters:
        - name: i_photo_height
          in: query
          description: 'The height of the requested photo.'
          schema:
            type: number
            format: int32
        - name: i_photo_width
          in: query
          description: 'The width of the requested photo.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business. Users can be in multiple businesses. This can be left as `null` to retrieve system-wide information.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user. `null` on case when is walk-in client.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_postcard:
                        type: boolean
                        description: 'Whether this user can send postcards.'
                      can_send_message:
                        type: boolean
                        description: 'Whether this user can send SMS. If `true` - user can send SMS, otherwise - `false`.'
                      id_gender:
                        type: number
                        format: int32
                        description: 'The gender ID. It will be one of the {@link \AGenderSid} constants.'
                      is_photo_empty:
                        type: boolean
                        description: 'Whether photo is uploaded. `true` if photo is not uploaded, `false` otherwise.'
                      k_staff:
                        type: [string, "null"]
                        description: 'The user''s staff key for the specified business. `null` if staff key not available for this application.'
                      s_first_name:
                        type: string
                        description: 'The first name of the user.'
                      s_last_name:
                        type: string
                        description: 'The surname of the user.'
                      text_mail_client:
                        type: string
                        description: 'The client`s mailing address.'
                      text_mail_staff:
                        type: string
                        description: 'The staff member''s mailing address. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      text_name_first_staff:
                        type: string
                        description: 'The staff member''s first name. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      text_name_full_client:
                        type: string
                        description: 'The user''s login name. This is returned in cases when neither the first name nor the last name have been specified. An empty string is returned in cases where neither the first name, last name, nor login have been specified.'
                      text_name_full_staff:
                        type: string
                        description: 'The staff member''s full name. The user login is returned in cases where neither the first name nor the last name have been specified. An empty string is returned in cases where neither the first name, last name, nor login have been specified. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      text_name_last_staff:
                        type: string
                        description: 'The staff member''s last name. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      uid:
                        type: [string, "null"]
                        description: 'The key of the user. `null` on case when is walk-in client.'
                      uid_staff:
                        type: [string, "null"]
                        description: 'The user''s UID as a staff member for the specified business.'
                      url_photo:
                        type: string
                        description: 'The URL where the user photo can be retrieved.'
      tags:
        - Login-Login
    post:
      summary: 'Retrieves information about a list of users. This is done via "post" method because only "post" allows large requests. Accepts a JSON-encoded array of user keys, validates each one, resolves staff and client roles, and returns name, gender, photo URL, email, and staff details for every user in the list, respecting per-user profile access rules.'
      parameters:
        - name: i_photo_height
          in: query
          description: 'The height of the requested photo.'
          schema:
            type: number
            format: int32
        - name: i_photo_width
          in: query
          description: 'The width of the requested photo.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business. Users can be in multiple businesses. This can be left as `null` to retrieve system-wide information.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_uid:
                  type: [string, "null"]
                  description: 'List of users to get information for serialized as JSON string. `null` for mode of single user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login:
                        type: array
                        description: 'List of information about users: <dl> <dt>int `id_gender`</dt> <dd>User''s gender. One of {@link \AGenderSid} constants.</dd> <dt>string `k_staff`</dt> <dd>User''s key as staff member. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `s_first_name`</dt> <dd>User first name.</dd> <dt>string `s_last_name`</dt> <dd>First letter of user last name.</dd> <dt>string `text_mail_client`</dt> <dd>Client`s mail.</dd> <dt>string `text_mail_staff`</dt> <dd>Staff`s mail.</dd> <dt>string `text_name_first_staff`</dt> <dd>Staff''s first name.</dd> <dt>string `text_name_full_client`</dt> <dd> Full client name. User login is returned in a case neither first name, nor last name specified. An empty string is returned in a case neither first name, nor last name specified, nor login. <i>See description of the {@link \Wl\User\Info\UserInfo::nameFullText()} method.</i> </dd> <dt>string `text_name_full_staff`</dt> <dd> Full staff name. User login is returned in a case neither first name, nor last name specified. An empty string is returned in a case neither first name, nor last name specified, nor login. </dd> <dt>string `text_name_last_staff`</dt> <dd>Staff`s last name.</dd> <dt>string `uid_staff`</dt> <dd>The user''s UID as a staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_photo`</dt> <dd>User photo URL.</dd> </dl>'
      tags:
        - Login-Login
  '/Wl/Collector/DebtList.json':
    get:
      summary: 'Returns a list of client debts for the specified business within the given date range. Used by the Collections module to display outstanding debts to the collector. Requires an active Collections subscription and either the business privilege or emulation access. The default date range covers the previous month relative to the business timezone.'
      parameters:
        - name: dl_end
          in: query
          description: 'Date before which debts should be returned. If set, this is the end of the date window. Only debts added before or on this date will be shown. If left `null` and {@link DebtListApi::$dl_start} has been specified, only debts added after the start date will be returned. If left `null` and {@link DebtListApi::$dl_start} is also `null`, this will return debts from the previous month.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Date since which debts should be returned. If set, this is the start of the date window. Only debts added on or after this date will be shown. If left `null` and {@link DebtListApi::$dl_end} has been specified will return debts added since the beginning of time. If left `null` and {@link DebtListApi::$dl_end} is also `null`, this will return debts from the previous month.'
          schema:
            type: [string, "null"]
        - name: is_request_debt
          in: query
          description: 'Defines whether new debts should be returned or only previously sent debts. If `true` then return new debts only. If `false` then return previously sent debts only.'
          schema:
            type: boolean
        - name: is_test
          in: query
          description: 'Defines whether debts for test or real business should be returned. If `true`, debts from test businesses will be returned. Otherwise, this will be `false` if only debts from real businesses will be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key for which debts should be returned. Use `null` if debt payments from all businesses should be returned.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_debt:
                        type: array
                        description: 'A list of debts for the given business added within the previous month. Each value is an array with the next structure: <dl> <dt>string `dl_client_birth`</dt> <dd>The birth date of the debtor client.</dd> <dt>string `dtu_client_since`</dt> <dd>The date and time in UTC from which the debtor client is a member of the business.</dd> <dt>string `dtu_due`</dt> <dd>The date and time in UTC of the last payment on the debtor client''s account.</dd> <dt>string `dtu_add`</dt> <dd>The date and time in UTC when the client was added to the list of debtors.</dd> <dt>string `dtu_cease`</dt> <dd>The date and time in UTC when the debt was fully ceased.</dd> <dt>int `id_currency`</dt> <dd>The debt currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_business`</dt> <dd>The business key where the debt occurred.</dd> <dt>string `k_collector_debt`</dt> <dd>The debt key, which should be used for debt payment.</dd> <dt>string `k_pay_transaction_debt`</dt> <dd>The transaction key after which the debt occurred.</dd> <dt>string `k_pay_transaction_last`</dt> <dd>The last transaction key on account after the debt occurred.</dd> <dt>string `m_amount_debt`</dt> <dd>The initial amount of the debt.</dd> <dt>string `m_amount_paid`</dt> <dd>The amount paid for debt.</dd> <dt>string `text_business_title`</dt> <dd>The title of the business.</dd> <dt>string `text_client_name`</dt> <dd>The full name of the debtor client.</dd> <dt>string `text_client_address`</dt> <dd>The address of the debtor client.</dd> <dt>string `text_client_mail`</dt> <dd>The email address of the debtor client.</dd> <dt>string `text_client_phone`</dt> <dd>The phone number of the debtor client.</dd> <dt>string `text_client_zip`</dt> <dd>The ZIP code of the debtor client.</dd> <dt>string `text_skip`</dt> <dd>The notification message sent when the debt payment is missed.</dd> <dt>string `uid`</dt> <dd>The user key of the debt owner.</dd> </dl>'
      tags:
        - Collector-DebtList
  '/Wl/Collector/DebtPay.json':
    post:
      summary: 'Registers a debt payment made outside WellnessLiving and applies a credit to the client''s account balance. Used by collectors to record cash or external payments against outstanding debts. The specified amount is credited to the client''s account and the debt status is updated accordingly. Requires an active Collections subscription and the business privilege or emulation access.'
      parameters:
        - name: id_currency
          in: query
          description: 'The currency of the payment. One of {@link CurrencySid} constants. Important! The currency must coincide with currency of the debt that was sent to collections.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business from which the debt originates.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user with the debt.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_method:
                  type: [number, "null"]
                  description: 'The payment method ID. One of {@link \RsPayMethodSid} constants. `null` if the payment method is unknown. In such cases, {@link \RsPayMethodSid::CASH} would be used.'
                k_collector_debt:
                  type: string
                  description: 'The debt key towards which the payment was performed.'
                m_amount:
                  type: string
                  description: 'The amount of money that''s been paid towards the debt.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: string
                        description: 'The transaction key generated to register the payment.'
      tags:
        - Collector-DebtPay
  '/Wl/Collector/DebtTransaction.json':
    get:
      summary: 'Returns a list of debt payment transactions for the specified business within the given date range. Used by the Collections module to audit payments made against debts. The default date range covers the previous day relative to the business timezone. Requires an active Collections subscription and either the business privilege or emulation access.'
      parameters:
        - name: dl_end
          in: query
          description: 'If set, this is the end of the date window. Only debt payments added before or on this date will be shown. If left `null` and {@link DebtTransactionApi::$dl_start} has been specified only debt payments added after the start date will be returned. If left `null` and {@link DebtTransactionApi::$dl_start} is also `null`, this will return debt payments from the previous month.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'If set, this is the start of the date window. Only debt payments added on or after this date will be shown. If left `null` and {@link DebtTransactionApi::$dl_end} has been specified, this will return debt payments since the beginning of time. If left `null` and {@link DebtTransactionApi::$dl_end} is also `null`, this will return debt payments from the previous month.'
          schema:
            type: [string, "null"]
        - name: is_test
          in: query
          description: 'If `true`, debt payments from test businesses will be returned. Otherwise, this will be `false` if only debt payments from real businesses will be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key for which debt payments should be returned. Use `null` if debt payments from all businesses should be returned.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_transaction:
                        type: array
                        description: 'A list of debt payments for a given business added within the previous day. Each value is an array with next structure: <dl> <dt>string `dtu_pay`</dt> <dd>The date and time in UTC of the payment transaction.</dd> <dt>string `m_pay_amount`</dt> <dd>The amount of the payment''s transaction.</dd> <dt>int `id_pay_method`</dt> <dd>The payment method ID that was used for the payment. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_pay_collections`</dt> <dd> Defines whether the payment was performed via a third party service. `true` if the payment was performed via a third party service and this transaction is responsible for an account credit transaction for that payment. See {@link \Wl\Collector\DebtPayApi}. `false` if the payment was performed within the Wellnessliving system. </dd> <dt>bool `is_debt_paid`</dt> <dd> Defines whether the initial debt to which this related transaction is fully paid. `true` if the debt is fully paid. `false` if the debt is partially paid. </dd> <dt>string `text_client_name`</dt> <dd>The full name of the debtor client for whom the payment transaction was performed.</dd> <dt>string `uid`</dt> <dd>The UID of the debtor client for whom the payment transaction was performed.</dd> </dl>'
      tags:
        - Collector-DebtTransaction
  '/Wl/Business/BusinessAccess.json':
    get:
      summary: 'Gets information about businesses where given user is a staff member. Used during staff login to determine which businesses the user can access. Accepts identity via UID or any supported social login identifier, then returns the matching list of businesses to display on the business selection screen. In backend mode, franchisee locations and IP access restrictions are also evaluated.'
      parameters:
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: s_facebook_id
          in: query
          description: 'The staff member''s Facebook ID. This won''t be empty if the staff member is logging in with Facebook. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: s_microsoft_id
          in: query
          description: 'The staff member''s Microsoft key. This won''t be empty if the staff member is logging in with Microsoft.'
          schema:
            type: string
        - name: text_authorization_apple
          in: query
          description: 'The staff member''s Apple authorization code. This won''t be empty if the staff member is logging in with Apple. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: text_google_plus
          in: query
          description: 'The Google Plus user ID. This won''t be empty if the staff member is logging in with Google. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The staff member''s email to determine their UID. This won''t be empty if the staff member is logging in with email or with Google. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Empty if user is not logged in, but their authorization data is known.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: array
                        description: 'The businesses the staff member belongs to. Note that this field is here for backwards compatibility.'
                      a_business_data:
                        type: array
                        description: 'The list of accessible businesses with their corresponding data. Each value is an array with the next structure: <dl> <dt>int `id_region`</dt> <dd> The business region. One of the {@link \Core\Amazon\Region\AmazonRegionSid} constants. This will be `null` if no regions are currently set. </dd> <dt>string `k_business`</dt> <dd>The business key. <i>Primary keys in table {@link \RsBusinessSql}.</i></dd> <dt>string `text_office_address`</dt> <dd>The business address.</dd> <dt>string `text_title`</dt> <dd>The business title.</dd> <dt>string `url_logo`</dt> <dd>The business logo URL. This will be empty if the business hasn''t added a logo.</dd> </dl>'
                      uid_mail:
                        type: string
                        description: 'The staff member key, determined by their email. This will be empty if the UID isn''t empty.'
      tags:
        - Business-BusinessAccess
  '/Wl/Business/Business.json':
    post:
      summary: 'Creates new business. Used during the self-onboarding flow when a new client signs up for WellnessLiving. Creates the business with its default integrations, optional first location, default service categories, and an optional owner staff account - all in a single transaction. Rate-limited per IP and globally to prevent abuse.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_member:
                  type: array
                  description: 'Empty array means to not create a staff member. Otherwise, information about staff member to be created in the business. <dl> <dt>bool `is_enter`</dt> <dd>Should created staff member be automatically signed in or not.</dd> <dt>string `text_mail`</dt> <dd>Email address to be used to sign in.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the staff member.</dd> <dt>string `text_name_last`</dt> <dd>The last name of the staff member.</dd> </dl>'
                id_locale:
                  type: number
                  format: int32
                  description: 'The business locale.'
                is_location_create:
                  type: boolean
                  description: '`true` if location should be also created for the business. `false` if otherwise.'
                is_mail_send:
                  type: boolean
                  description: '`true` to send {@link \RsMailSid::BUSINESS_CREATE} mail when creating a staff member. Otherwise send {@link \RsMailSid::LOGIN_WELCOME} mail.'
                k_business_type:
                  type: string
                  description: 'The key of the business type.'
                k_office_city:
                  type: string
                  description: 'The city key of the business. Optional.'
                s_clarity_session_id:
                  type: string
                  description: 'Microsoft Clarity session ID associated with the business claim session.'
                text_mail:
                  type: string
                  description: 'The email address of the location. Optional.'
                text_office_address:
                  type: string
                  description: 'The address of the business. Optional.'
                text_office_postal:
                  type: string
                  description: 'The postal code of the business. Optional.'
                text_phone:
                  type: string
                  description: 'The phone number of the business, staff and location.'
                text_title:
                  type: string
                  description: 'The title of the business.'
                url_site:
                  type: string
                  description: 'The URL of the business website. Optional.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_business:
                        type: string
                        description: 'The key of the business. When using {@link BusinessApi::post()} returned key of the created business. When using {@link BusinessApi::put()} used for update business.'
                      k_location:
                        type: [string, "null"]
                        description: 'The key of the location for update or after creating. `null` if location is not created.'
                      uid:
                        type: [string, "null"]
                        description: 'The UID of the created staff member or `null` if no staff member should be created.'
                      url_password_change:
                        type: [string, "null"]
                        description: 'Url for password change page. Filled only when creating staff member, otherwise `null`.'
      tags:
        - Business-Business
    put:
      summary: 'Claims business. Used when a prospective client claims an existing unclaimed business record during onboarding. Activates trial features, updates the location contact details, and optionally creates an owner staff account. The business must not already be claimed and the location must belong to it.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_member:
                  type: array
                  description: 'Empty array means to not create a staff member. Otherwise, information about staff member to be created in the business. <dl> <dt>bool `is_enter`</dt> <dd>Should created staff member be automatically signed in or not.</dd> <dt>string `text_mail`</dt> <dd>Email address to be used to sign in.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the staff member.</dd> <dt>string `text_name_last`</dt> <dd>The last name of the staff member.</dd> </dl>'
                is_mail_send:
                  type: boolean
                  description: '`true` to send {@link \RsMailSid::BUSINESS_CREATE} mail when creating a staff member. Otherwise send {@link \RsMailSid::LOGIN_WELCOME} mail.'
                k_business:
                  type: string
                  description: 'The key of the business. When using {@link BusinessApi::post()} returned key of the created business. When using {@link BusinessApi::put()} used for update business.'
                k_location:
                  type: [string, "null"]
                  description: 'The key of the location for update or after creating. `null` if location is not created.'
                s_clarity_session_id:
                  type: string
                  description: 'Microsoft Clarity session ID associated with the business claim session.'
                text_mail:
                  type: string
                  description: 'The email address of the location. Optional.'
                text_phone:
                  type: string
                  description: 'The phone number of the business, staff and location.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_business:
                        type: string
                        description: 'The key of the business. When using {@link BusinessApi::post()} returned key of the created business. When using {@link BusinessApi::put()} used for update business.'
                      k_location:
                        type: [string, "null"]
                        description: 'The key of the location for update or after creating. `null` if location is not created.'
                      uid:
                        type: [string, "null"]
                        description: 'The UID of the created staff member or `null` if no staff member should be created.'
                      url_password_change:
                        type: [string, "null"]
                        description: 'Url for password change page. Filled only when creating staff member, otherwise `null`.'
      tags:
        - Business-Business
  '/Wl/Business/Data.json':
    get:
      summary: 'Returns detailed information about the specified business, including locale, franchise status, services, tips, social URLs, and logo. Used to bootstrap client-facing pages and widgets that need to know the full business profile: which services are enabled, tip and progress log settings, franchise relationship, social media links, and deep links to the schedule, catalog, and sign-up pages. Accepts either a business key or a location request token for widget contexts where the key is not available.'
      parameters:
        - name: i_logo_height
          in: query
          description: 'The maximum height of the business image (in pixels).'
          schema:
            type: number
            format: int32
        - name: i_logo_width
          in: query
          description: 'The maximum width of the business image (in pixels).'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The authorization token. This may be used instead of {@link DataApi::$k_business} to identify a business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service_list:
                        type: array
                        description: 'A list of all business services and their availability data. This is presented as an array, where keys are SIDs from {@link ServiceSid} and values are Boolean. If `true`, at least one service is enabled in the business. Otherwise, this will be `false`.'
                      a_tip_predefine:
                        type: array
                        description: 'The list of predefined tips in percentages.'
                      id_category:
                        type: number
                        format: int32
                        description: 'The business category ID of the business.'
                      id_claim_status:
                        type: number
                        format: int32
                        description: 'Business status for managing claim request behavior. Constant from {@link BusinessClaimStatusSid}.'
                      id_currency:
                        type: number
                        format: int32
                        description: 'The currency ID of the given business (or the system currency ID if the business didn''t pass).'
                      id_locale:
                        type: number
                        format: int32
                        description: 'The Locale ID, used to search geo items.'
                      id_rank_type:
                        type: [number, "null"]
                        description: 'The rank type ID of the business. One of the {@link \RsRankTypeSid} constants. This will be `null` if the business doesn''t have a rank type.'
                      id_region:
                        type: number
                        format: int32
                        description: 'The region ID. This indicates the data center where the information about the business is stored. One of the {@link AmazonRegionSid} constants. Requests made to different regions can lead to known issues such as responses indicating that the business (or its elements) doesn''t exist. This is because databases on different data centers are independent. For example, performing a request to the US cluster for a list of classes for an AU cluster business will return an empty list.'
                      is_apply_surcharge:
                        type: boolean
                        description: 'Determines whether surcharges to client payments are enabled in the business.'
                      is_franchise:
                        type: boolean
                        description: '`true` if business is a franchisor or franchisee.'
                      is_location_multiple:
                        type: boolean
                        description: 'Determines whether the business has multiple locations (including inactive locations).'
                      is_profile_timezone:
                        type: boolean
                        description: '`true` - clients of the business can select a custom time zone in their profile. `false` - the location or business time zone is used.'
                      is_progress_client:
                        type: boolean
                        description: 'If `true`, clients can enter the progress log. Otherwise, this will be `false`.'
                      is_progress_verify:
                        type: boolean
                        description: 'If `true`, verification of the progress log by a staff member is required. Otherwise, this will be `false`.'
                      is_quiz_available:
                        type: boolean
                        description: 'Determines whether quizzes are available in the business.'
                      is_test:
                        type: boolean
                        description: 'Determines if the business operates in testing mode.'
                      is_tip:
                        type: boolean
                        description: 'If `true`, tips are available in the business. Otherwise, this will be `false`.'
                      is_tip_deny:
                        type: boolean
                        description: 'If `true`, the business has the "No tip" option displayed. Otherwise, this will be `false`.'
                      is_tip_sign:
                        type: boolean
                        description: 'If `true`, the client must sign after selecting the tip. Otherwise, this will be `false`.'
                      k_business_franchisor:
                        type: string
                        description: 'The franchisor business key. This will be empty if this business is the franchisor or not in a franchise.'
                      k_business_token:
                        type: string
                        description: 'The business key obtained by the security token {@link DataApi::$text_token}. This can be used on the client side if authorization token should be used instead of the business key (the business key isn''t available).'
                      k_business_type:
                        type: string
                        description: 'Key of the business type.'
                      k_currency:
                        type: string
                        description: 'The currency key of the given business, or the system currency if the business didn''t pass.'
                      s_reply_mail:
                        type: string
                        description: 'The reply-to email address.'
                      s_reply_name:
                        type: string
                        description: 'The reply-to business name.'
                      text_office_address:
                        type: string
                        description: 'The business address.'
                      text_title:
                        type: string
                        description: 'The business title.'
                      url_catalog:
                        type: string
                        description: 'The URL for the online store.'
                      url_enter:
                        type: string
                        description: 'The URL for client sign in.'
                      url_facebook:
                        type: string
                        description: 'The Facebook page.'
                      url_google:
                        type: string
                        description: 'The Google+ page.'
                      url_home_tour:
                        type: string
                        description: 'URL of the home tour page related to the business type.'
                      url_instagram:
                        type: string
                        description: 'The Instagram page.'
                      url_linkedin:
                        type: string
                        description: 'The `LinkedIn` profile.'
                      url_logo:
                        type: string
                        description: 'The logo URL.'
                      url_logo_empty:
                        type: string
                        description: 'The image stub (in cases where the business logo isn''t loaded).'
                      url_redeem:
                        type: string
                        description: 'The URL to redeem gift card.'
                      url_review:
                        type: string
                        description: 'The URL for the client review page.'
                      url_schedule:
                        type: string
                        description: 'The URL for the client web app (schedule).'
                      url_signup:
                        type: string
                        description: 'The URL for client sign up.'
                      url_twitter:
                        type: string
                        description: 'The Twitter page.'
                      url_website:
                        type: string
                        description: 'The business website.'
                      url_youtube:
                        type: string
                        description: 'The YouTube website.'
      tags:
        - Business-Data
  '/Wl/Lead/LeadCheck.json':
    post:
      summary: 'Checks if user with specified email already registered for specified business. Looks up an existing account by the given email address. If no account with that email exists, the user is reported as neither a member nor a lead. If an account exists, the response reports whether that user is already a member of the specified business, and whether that user is already registered as a lead for the specified business.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of business to which the new user must be captured.'
                s_mail:
                  type: string
                  description: 'User email.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_lead:
                        type: boolean
                        description: '`true` if user with specified email is lead of a specified business, `false` - otherwise.'
                      is_member:
                        type: boolean
                        description: '`true` if user with specified email is a member of a specified business, `false` - otherwise.'
      tags:
        - Lead-LeadCheck
  '/Wl/Lead/Lead.json':
    get:
      summary: 'Gets information necessary to display "Lead capture" widget. Returns the list of profile fields configured for the widget, skin styling data, captcha URL if required, and whether a free promotion can be applied when a new lead is created.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of business to which the new user must be captured.'
          schema:
            type: string
        - name: k_skin
          in: query
          description: 'The key of the widget skin. If left empty, then the default skin is used. This will be the Lead Capture widget skin with "Use this widget for the Add Lead form" selected. If your business doesn''t have a skin selected, this endpoint will default to the system-wide default, which may lack fields your business requires when adding a lead or a client.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'A list of profile fields in the business. Every element has the following keys: <dl> <dt>array[] `a_item`</dt> <dd> A list of possible options for an HTML select field. This value is only used if this field is an HTML select. Every element has the following keys: <dl> <dt>string `s_id`</dt> <dd>The option ID.</dd> <dt>string `text_title`</dt> <dd>The option title.</dd> </dl> </dd> <dt>int `id_field_general`</dt> <dd> The type of the general field. This is one of the {@link \RsFieldGeneralSid}. This value is only set if the field is one of the general fields. </dd> <dt>int `id_field_type`</dt> <dd>The field type. This is one of the {@link \RsFieldTypeSid} constants.</dd> <dt>bool `is_require`</dt> <dd>If `true`, then the field is mandatory. If `false`, then the field isn''t mandatory.</dd> <dt>string `k_field`</dt> <dd>The field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>string `text_field`</dt> <dd>The field title.</dd> </dl>'
                      a_skin:
                        type: array
                        description: 'The skin configuration: <dl> <dt>array[] `a_style`</dt> <dd> List of compiled CSS style blocks. Each element: <dl> <dt>string `s_style`</dt> <dd>Compiled CSS style block string.</dd> <dt>string `s_value_name`</dt> <dd>Name of the skin value this style block belongs to.</dd> </dl> </dd> <dt>array `background`</dt> <dd> Background styling for the widget container: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `field-font`</dt> <dd> Font styling for form fields: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `header-text`</dt> <dd> Header section settings: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> <dt>string `text`</dt> <dd>Header text content.</dd> </dl> </dd> <dt>array `info-show`</dt> <dd> Visibility settings for information elements: <dl> <dt>bool[] `a_require`</dt> <dd>Map of required element names. Keys are element names, values are `true`.</dd> <dt>bool[] `a_show`</dt> <dd>Map of visible element names. Keys are element names, values are `true`.</dd> <dt>bool `empty`</dt> <dd>`true` if no elements are visible.</dd> </dl> </dd> <dt>array `submit-background`</dt> <dd> Background styling for the submit button: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `submit-font`</dt> <dd> Font styling for the submit button: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `submit-text`</dt> <dd> Text settings for the submit button: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> <dt>string `text`</dt> <dd>Submit button text content.</dd> </dl> </dd> </dl>'
                      can_use_free_purchase:
                        type: [boolean, "null"]
                        description: 'Whether it is possible to give free promotion when adding a user (only if free promotion is configured in the widget). `true` or `null` if it is possible, `false` if not. `null` used for backward compatibility.'
                      url_captcha:
                        type: string
                        description: 'The URL to load the image with a captcha test. This string is empty if it''s not necessary to pass a captcha test.'
      tags:
        - Lead-Lead
    post:
      summary: 'Saves new user via "Lead capture". Creates a new user account from the submitted lead capture form data, optionally signs in the new user, and associates the lead with a lead source. Returns an error code in `text_sign_in_error` if sign-in was requested but could not be completed.'
      parameters:
        - name: can_use_free_purchase
          in: query
          description: 'Whether it is possible to give free promotion when adding a user (only if free promotion is configured in the widget). `true` or `null` if it is possible, `false` if not. `null` used for backward compatibility.'
          schema:
            type: [boolean, "null"]
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of business to which the new user must be captured.'
          schema:
            type: string
        - name: k_skin
          in: query
          description: 'The key of the widget skin. If left empty, then the default skin is used. This will be the Lead Capture widget skin with "Use this widget for the Add Lead form" selected. If your business doesn''t have a skin selected, this endpoint will default to the system-wide default, which may lack fields your business requires when adding a lead or a client.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_data:
                  type: array
                  description: 'A list of fields containing the lead information. The keys are the field keys and values are field values. If field key is `Address` field key, value may be an array or string. Can be a string if only address field is used. Will be an array if city and postal code are used, with the following keys: <dl> <dt>string `text_address`</dt> <dd>Address.</dd> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in {@link \AGeoSql}.</i></dd> <dt>string `text_postal`</dt> <dd>Postal code.</dd> </dl>'
                is_sing_in:
                  type: boolean
                  description: '`true` if newly created lead should be automatically signed in, `false` otherwise. Lead will not be signed in if: - email is used already for another existing user; - different user is signed in already (can be changed with {@link \Wl\Lead\LeadApi::$is_sing_in_force}). If lead is not signed in, then {@link LeadApi::$text_sign_in_error} will contain an error message.'
                is_sing_in_force:
                  type: boolean
                  description: '`true` if newly created lead should be automatically signed in instead of the currently signed-in user, `false` if currently singed-in user should not be signed out. If lead is not signed in, then {@link LeadApi::$text_sign_in_error} will contain an error message.'
                k_lead_source:
                  type: [string, "null"]
                  description: 'Key of the lead source. Must be `null` if <var>text_lead_source</var> is set. If both parameters are empty, the {@link ModeSid::API} lead source will be used. {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                s_captcha:
                  type: string
                  description: 'The characters entered by the lead for the captcha test. This isn''t necessary if the GET method returned an empty {@link LeadApi::$url_captcha}. This field isn''t necessary if the GET method returned an empty {@link LeadApi::$url_captcha}.'
                text_lead_source:
                  type: [string, "null"]
                  description: 'Lead source title. A new lead source will be created if it does not exist. Must be `null` if <var>k_lead_source</var> is set. If both parameters are empty, the {@link ModeSid::API} lead source will be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_lead_source:
                        type: [string, "null"]
                        description: 'Key of the lead source. Must be `null` if <var>text_lead_source</var> is set. If both parameters are empty, the {@link ModeSid::API} lead source will be used. {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                      text_sign_in_error:
                        type: string
                        description: 'An error code if the lead is not signed in after creation. This field is filled in the POST method. Possible values: - `email-exists` - the email is already used by another lead; - `different-user` - another user is signed in already.'
                      uid:
                        type: string
                        description: 'The key of the new user. Typing is not added because the variable is an integer. Specifying typing may break third party integration.'
      tags:
        - Lead-Lead
  '/Wl/Mail/SendMail.json':
    post:
      summary: 'Sends email. Validates the recipient address, business email, subject, and HTML content, then saves the email pattern and dispatches the message to the specified recipient. Rate limiting is applied per hour and per minute.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_business_mail:
                  type: string
                  description: 'The business''s mailing address.'
                s_business_name:
                  type: string
                  description: 'The business name.'
                s_business_reply:
                  type: string
                  description: 'The business''s reply.'
                s_campaign:
                  type: string
                  description: 'The campaign name.'
                s_html:
                  type: string
                  description: 'The message uncompressed content.'
                s_mail:
                  type: string
                  description: 'The message recipient''s address.'
                s_subject:
                  type: string
                  description: 'The message''s subject line.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mail-SendMail
  '/Wl/Visit/VisitStatus.json':
    get:
      summary: 'Gets visit status. Returns full details for the specified visit, including date, duration, staff, location, virtual service join URL, assigned resources, downloadable calendar data, and whether the visit can still be cancelled. Handles class, appointment, and gym visit types. Used to render the visit detail view and action buttons in the client portal.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'The time zone key. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: k_visit
          in: query
          description: 'The visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_cancel:
                        type: array
                        description: 'Information about whether the given user can cancel an online booking and what consequences the cancellation would have: <dl> <dt>array|null `a_penalty`</dt> <dd> `null` if penalty must be not applied. <dl> <dt>bool `is_flat`</dt> <dd>`true` in a case of flat penalty type; `false` in a case of percentage penalty type.</dd> <dt>string `k_currency`</dt> <dd>Currency <i>primary key in {@link \RsCurrencySql} table</i>.</dd> <dt>string `m_amount`</dt> <dd>Penalty amount.</dd> </dl> </dd> <dt>bool `can_cancel`</dt> <dd>`true` if the booking can be canceled online by the specified user, `false` otherwise.</dd> <dt>bool `is_flag`</dt> <dd>`true` if the client''s account will be flagged instead of charging a monetary fee, `false` otherwise.</dd> <dt>bool `is_late`</dt> <dd>`true` if the cancellation would be considered a late cancel, `false` otherwise.</dd> <dt>bool `is_refund`</dt> <dd> `true` if the visit credit (from the purchase option used to book) will be returned to the user''s profile after cancellation, `false` otherwise. </dd> </dl>'
                      a_resource:
                        type: [array, "null"]
                        description: 'An array of service resources. The key refers to the `k_resource_type`. <i>See {@link \RsResourceTypeSql}.</i> The value is an array with the following key: `k_resource`. <i>See {@link \RsResourceSql}</i>. The array element contains a nested array with `i_index` and `i_quantity`. <i>See {@link \RsResourceBusySql}</i>. This will be empty if not set yet.'
                      a_resource_alias:
                        type: [array, "null"]
                        description: 'An array of service resources. Contains an extended data set, as well as a different format than {@link VisitStatusApi::$a_resource}. Each element contains the following set of data: <dl> <dt>string `k_resource`</dt> <dd>Resource <i>primary key in {@link \RsResourceSql} table</i>.</dd> <dt>string `k_resource_type`</dt> <dd>Resource type <i>primary key in {@link \RsResourceTypeSql} table</i>.</dd> <dt>int `i_index`</dt> <dd>Index of the resource on the layout.</dd> <dt>int `i_quantity`</dt> <dd>Quantity of the resource on the layout.</dd> <dt>string `text_alias`</dt> <dd>Resource''s custom name (alias) on the layout.</dd> <dt>string `text_title`</dt> <dd>Resource''s title.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'The list of keys of staff members that conduct the class.'
                      a_uid_staff:
                        type: array
                        description: 'The list of user IDs of staff members that conduct the class.'
                      dt_date:
                        type: string
                        description: 'The visit date and time in UTC and in MySQL format.'
                      dtl_date:
                        type: string
                        description: 'The visit date in the location''s time zone and in MySQL format.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The service duration (in minutes).'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'The client''s place in a waiting list.'
                      id_mode:
                        type: number
                        format: int32
                        description: 'The source of the visit or the visit change. One of the {@link ModeSid} constants. If you''re unsure about the value to use, keep the default value.'
                      id_visit:
                        type: number
                        format: int32
                        description: 'The status of the visit. One of the {@link VisitSid} constants.'
                      is_event:
                        type: boolean
                        description: 'Determines whether the visit is from an event.'
                      is_request:
                        type: boolean
                        description: 'Whether this visit is requested and requires staff confirmation. * `true` - visit is requested. * `false` - visit is confirmed or denied or this is a system request.'
                      k_class:
                        type: string
                        description: 'The class key.'
                      k_class_period:
                        type: string
                        description: 'The class period key.'
                      k_location:
                        type: string
                        description: 'The key of the location where visit provides.'
                      k_service:
                        type: [string, "null"]
                        description: 'The service key. If ''null'', the visit isn''t from an appointment.'
                      k_staff:
                        type: [string, "null"]
                        description: 'The key of the staff providing the appointment. If `null`, the visit isn''t from an appointment (for example, the visit is from an asset).'
                      s_calendar_file_content:
                        type: string
                        description: 'The .ics file for adding the service to a phone calendar.'
                      text_abbr_timezone:
                        type: string
                        description: 'The text abbreviation of the time zone.'
                      text_location:
                        type: string
                        description: 'The full address of the location for the visit (not the name of the location).'
                      text_staff:
                        type: string
                        description: 'The full name of the staff member who conducts this visit. If there are several staff members conducting the visit, their names will all be listed and separated by commas.'
                      text_title:
                        type: string
                        description: 'The service title.'
                      uid:
                        type: [string, "null"]
                        description: 'User key who made a visit. `null` for a guest visit.'
                      uid_staff:
                        type: [string, "null"]
                        description: 'The ID of the user who is the staff providing the appointment. If `null`, the visit isn''t from an appointment (for example, the visit is from an asset).'
                      url_book_referral:
                        type: [string, "null"]
                        description: 'The direct link to start class/event booking on the WellnessLiving website. `null` for appointments/events/gym visits.'
                      url_book_referral_short:
                        type: [string, "null"]
                        description: 'The shortened direct link to start class/event booking on the WellnessLiving website. `null` for appointments/events/gym visits.'
                      url_virtual_service:
                        type: string
                        description: 'URL of virtual service. Empty if the visit is not virtual.'
      tags:
        - Visit-VisitStatus
    post:
      summary: 'Changes visit status. Applies a status transition to the specified visit (for example, check-in, no-show, or cancellation). Supports optimistic concurrency via an expected-from status, optional late-cancel fee charging, and client notification flags. Requires backend access or appropriate staff privileges.'
      parameters:
        - name: is_charge_fee
          in: query
          description: 'The staff decision to charge (or not charge) a penalty when a client meets late cancel/no-show requirements. If `true`, a late cancel fee should be charged. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'The email pattern key. If `null`, the live email pattern shouldn''t be used.'
          schema:
            type: [string, "null"]
        - name: k_visit
          in: query
          description: 'The visit key.'
          schema:
            type: string
        - name: text_reason
          in: query
          description: 'The reason the visit was canceled.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: number
                  format: int32
                  description: 'The source of the visit or the visit change. One of the {@link ModeSid} constants. If you''re unsure about the value to use, keep the default value.'
                id_visit:
                  type: number
                  format: int32
                  description: 'The status of the visit. One of the {@link VisitSid} constants.'
                id_visit_from:
                  type: [number, "null"]
                  description: 'The status of the visit from which the transition is made. One of the {@link VisitSid} constants. If the visit status is passed, it will be used to check with the actual status in the database. If `null`, the visit hasn''t yet passed. If the status of this parameter is out of date, the API call will refresh it.'
                is_mail:
                  type: boolean
                  description: 'Whether to send email notification. `true` - email notification will be sent. `false` - email notification will not be sent.'
                is_push:
                  type: boolean
                  description: 'Whether to send push notification. `true` - push notification will be sent. `false` - push notification will not be sent.'
                is_sms:
                  type: boolean
                  description: 'Whether to send sms notification. `true` - sms notification will be sent. `false` - sms notification will not be sent.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      id_visit_from:
                        type: [number, "null"]
                        description: 'The status of the visit from which the transition is made. One of the {@link VisitSid} constants. If the visit status is passed, it will be used to check with the actual status in the database. If `null`, the visit hasn''t yet passed. If the status of this parameter is out of date, the API call will refresh it.'
      tags:
        - Visit-VisitStatus
  '/Wl/Visit/VisitTotalCount.json':
    get:
      summary: 'Returns the total visit count for the user at the business. Sums the number of attended visits with the historical visit count carried over for the user, so that visits recorded before the account existed in the system are also counted.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_visit_total:
                        type: number
                        format: int32
                        description: 'Total number of visits. Sum of attended visits and historical visits. <i> Attended visits - {@link \RsVisitSql}. Historical visits - {@link VisitHistoricalCountSql}. </i>'
      tags:
        - Visit-VisitTotalCount
  '/Wl/Video/VideoElement.json':
    delete:
      summary: 'Deletes the specified video. Permanently removes the video, its localized CMS records, and all category associations from the business library. Also deletes the video file and thumbnail from storage. Requires backend access with the video library management privilege.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video
          in: query
          description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Video-VideoElement
    get:
      summary: 'Returns the data for the specified video. Returns detailed information about a single video, including title, description, duration, categories, tags, difficulty level, assigned staff, virtual session settings, and playback progress for the current user. In frontend mode, access is restricted to videos the current user is allowed to watch based on their membership and login type.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video
          in: query
          description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'The keys of the locations where this video is available.'
                      a_staff:
                        type: array
                        description: 'The legacy staff keys associated with the video. For GET requests, this field is populated only for applications listed in {@link VideoElementApi::APPS_USE_OLD_K_STAFF}; otherwise it is not returned. For POST requests, applications outside the allow-list must use {@link VideoElementApi::$a_staff_uid}.'
                      a_staff_info:
                        type: array
                        description: 'A list of staff members associated with the video. Every item has the following structure: <dl> <dt>string `k_staff`</dt> <dd> <b>Deprecated</b> The optional staff member key returned only to legacy applications. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `text_name`</dt> <dd>The staff member''s full name.</dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql}.`uid`.</i></dd> </dl>'
                      a_staff_uid:
                        type: array
                        description: 'The user IDs of the staff members who are on the video (authoritative list for who is assigned to the video). Use this field instead of {@link VideoElementApi::$a_staff} in POST requests.'
                      a_video_category:
                        type: array
                        description: 'The video category keys where this video can be found.'
                      a_video_tag:
                        type: array
                        description: 'The video tag keys.'
                      dtl_publish:
                        type: [string, "null"]
                        description: 'The date and time when the video was published. This will be `null` if the published date is not set yet.'
                      dtl_unpublish:
                        type: [string, "null"]
                        description: 'The date and time when the video was unpublished. This will be `null` if the unpublished date is not set yet.'
                      dtl_upload:
                        type: string
                        description: 'The date when the video was uploaded.'
                      html_embed:
                        type: string
                        description: 'The video embed code.'
                      i_calorie:
                        type: number
                        format: int32
                        description: 'The count of burned calories associated with the video.'
                      i_current_percent:
                        type: number
                        format: int32
                        description: 'The current time as a percentage of the video duration at which the user is at in the video.'
                      i_current_time:
                        type: number
                        format: int32
                        description: 'The current time in seconds at which the user is at in the video.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The video duration in seconds.'
                      i_watch:
                        type: number
                        format: int32
                        description: 'The number of video views.'
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      id_location_select:
                        type: number
                        format: int32
                        description: '{@link YesNoSid::NO} if the video is available in all locations. {@link YesNoSid::YES} if the video is available only in certain locations.'
                      id_source:
                        type: number
                        format: int32
                        description: 'Source of the video. One of {@link VideoSourceSid} constants.'
                      is_calorie:
                        type: boolean
                        description: 'If `true`, the calorie count will be displayed on the video. If `false`, the calorie count will be hidden on the video.'
                      is_converted:
                        type: boolean
                        description: 'If `true`, the video is converted. If `false`, the video isn''t converted.'
                      is_published:
                        type: boolean
                        description: 'If `true`, the video is published. Otherwise, this will be `false`.'
                      is_video_level:
                        type: boolean
                        description: 'If `true`, the video level for the video is enabled. If `false`, the video level for the video is disabled.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
                      k_video_binary:
                        type: string
                        description: 'The binary string video key.'
                      k_video_category_primary:
                        type: string
                        description: 'The video category primary key.'
                      k_video_level:
                        type: string
                        description: 'The video level key. This will be `0` if `All Levels` is selected.'
                      s_preview_video_key:
                        type: [string, "null"]
                        description: 'The string representation of the video''s key. This will be `null` if the video doesn''t have a thumbnail image.'
                      show_calorie:
                        type: boolean
                        description: 'If `false`, the calorie count for videos is hidden. Otherwise, this will be `true`.'
                      show_level:
                        type: boolean
                        description: 'If `false`, video levels are hidden. Otherwise, this will be `true`.'
                      show_view:
                        type: boolean
                        description: 'If `false`, the number of views for videos is hidden. Otherwise, this will be `true`.'
                      text_level_title:
                        type: string
                        description: 'The video level name.'
                      text_title:
                        type: string
                        description: 'The video name.'
                      url_thumbnail:
                        type: string
                        description: 'The URL of the video thumbnail.'
                      url_video:
                        type: [string, "null"]
                        description: 'The URL of the video file. This URL return with domain which you use when you call this API. In case when you make request to local domain and video is {@link VideoElementApi::$is_converted} links in file will have global domain and for correct work you must replace global domain to local. Example for `videojs` player you can override method `beforeRequest`: <code> videojs.Vhs.xhr.beforeRequest = function(options) { if (options.uri.startsWith(''https://www.'')) { options.uri = options.uri.replace(''https://www.'', ''https://us.''); } return options; } </code>'
                      url_video_direct:
                        type: string
                        description: 'The direct URL of the video.'
                      url_video_frame:
                        type: string
                        description: 'The direct URL of the video for iframe.'
                      xml_description:
                        type: string
                        description: 'The video description.'
      tags:
        - Video-VideoElement
    post:
      summary: 'Creates or updates a video for the specified business. Saves a video record with its title, description, categories, tags, difficulty level, staff assignments, and access settings. When no video key is provided, a new record is created; otherwise the existing record is updated. Requires backend access with the video library management privilege and an active video subscription.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: is_converted
          in: query
          description: 'If `true`, the video is converted. If `false`, the video isn''t converted.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video
          in: query
          description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: array
                  description: 'The keys of the locations where this video is available.'
                a_staff:
                  type: array
                  description: 'The legacy staff keys associated with the video. For GET requests, this field is populated only for applications listed in {@link VideoElementApi::APPS_USE_OLD_K_STAFF}; otherwise it is not returned. For POST requests, applications outside the allow-list must use {@link VideoElementApi::$a_staff_uid}.'
                a_staff_uid:
                  type: array
                  description: 'The user IDs of the staff members who are on the video (authoritative list for who is assigned to the video). Use this field instead of {@link VideoElementApi::$a_staff} in POST requests.'
                a_video_category:
                  type: array
                  description: 'The video category keys where this video can be found.'
                a_video_tag:
                  type: array
                  description: 'The video tag keys.'
                dtl_publish:
                  type: [string, "null"]
                  description: 'The date and time when the video was published. This will be `null` if the published date is not set yet.'
                dtl_unpublish:
                  type: [string, "null"]
                  description: 'The date and time when the video was unpublished. This will be `null` if the unpublished date is not set yet.'
                file_video:
                  type: array
                  description: 'Video file.'
                html_embed:
                  type: string
                  description: 'The video embed code.'
                i_calorie:
                  type: number
                  format: int32
                  description: 'The count of burned calories associated with the video.'
                i_duration:
                  type: number
                  format: int32
                  description: 'The video duration in seconds.'
                i_file_upload_size:
                  type: number
                  format: int32
                  description: 'The size of the uploaded file. If the file will be uploaded in parts, its size will be set. <i> In this case, the file will not be uploaded to this API. Method {@link VideoElementApi::_fileVideoGet()} will not return the result. </i>'
                id_location_select:
                  type: number
                  format: int32
                  description: '{@link YesNoSid::NO} if the video is available in all locations. {@link YesNoSid::YES} if the video is available only in certain locations.'
                id_source:
                  type: number
                  format: int32
                  description: 'Source of the video. One of {@link VideoSourceSid} constants.'
                is_calorie:
                  type: boolean
                  description: 'If `true`, the calorie count will be displayed on the video. If `false`, the calorie count will be hidden on the video.'
                is_video_level:
                  type: boolean
                  description: 'If `true`, the video level for the video is enabled. If `false`, the video level for the video is disabled.'
                k_video_category_primary:
                  type: string
                  description: 'The video category primary key.'
                k_video_level:
                  type: string
                  description: 'The video level key. This will be `0` if `All Levels` is selected.'
                s_command:
                  type: string
                  description: 'The action that must be performed to the video thumbnail image.'
                s_file_upload_id:
                  type: string
                  description: 'ID of the uploaded file. If the file will be uploaded in parts, upload ID will be set. In this case, the file will not be uploaded to this API. <i>Method {@link VideoElementApi::_fileVideoGet()} will not return the result.</i>'
                s_file_upload_name:
                  type: string
                  description: 'The real name of the uploaded file. If the file will be uploaded in parts, its name will be set. In this case, the file won''t be uploaded to this API and the POST method won''t return the result.'
                s_thumbnail:
                  type: string
                  description: 'The video thumbnail.'
                text_title:
                  type: string
                  description: 'The video name.'
                xml_description:
                  type: string
                  description: 'The video description.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video:
                        type: string
                        description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
      tags:
        - Video-VideoElement
    put:
      summary: 'Validates video embed code. Checks that the provided embed snippet conforms to the supported embed code format. Throws if the snippet is malformed or uses an unsupported source. Call this before saving to give the user an immediate validation error without persisting the video record.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                html_embed:
                  type: string
                  description: 'The video embed code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
      tags:
        - Video-VideoElement
  '/Wl/Video/VideoList.json':
    get:
      summary: 'Returns the list of videos for the specified business. Returns videos for the business video library with optional filtering by category, tag, difficulty level, staff, location, calorie range, and duration. Supports sorting and pagination for large libraries. In frontend mode, results are restricted to videos the current user is allowed to watch based on their membership and login type.'
      parameters:
        - name: a_calorie
          in: query
          description: 'The calorie range ascribed to the video to use for the search. <dl> <dt>int `i_from`</dt> <dd>The minimum number of calories.</dd> <dt>int `i_to`</dt> <dd>The maximum number of calories.</dd> </dl>'
          schema:
            type: array
        - name: a_duration
          in: query
          description: 'The duration range ascribed to the video to use for the search. The interval is set in seconds. <dl> <dt>int `i_from`</dt> <dd>The minimum duration.</dd> <dt>int `i_to`</dt> <dd>The maximum duration.</dd> </dl>'
          schema:
            type: array
        - name: a_level
          in: query
          description: 'A list of levels to show videos for. A level can refer to the difficulty ascribed to the video.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'A list of locations to show videos from.'
          schema:
            type: array
        - name: a_source
          in: query
          description: 'A list of video sources.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'A list of staff members who appear in videos. <i>TODO wl-72248: remove this property when external developers confirm that they do not use it.</i>'
          schema:
            type: array
        - name: a_staff_uid
          in: query
          description: 'A list of staff members who appear in videos.'
          schema:
            type: array
        - name: a_video_category
          in: query
          description: 'A list of video categories to show videos from.'
          schema:
            type: array
        - name: a_video_tag
          in: query
          description: 'A list of video tags to show videos for.'
          schema:
            type: array
        - name: i_page
          in: query
          description: 'Page to return. Pagination are ignored when count of videos less than {@link VideoListApi::VIDEO_COUNT_PAGINATION}. `null` if you need to return all the videos.'
          schema:
            type: [number, "null"]
        - name: id_order
          in: query
          description: 'Sort order ID. One of {@link \Core\Sid\SortOrderSid} constants. `null` or 0 if order is undefined and depends on sort order.'
          schema:
            type: [number, "null"]
        - name: id_sort
          in: query
          description: 'The sorting type. A constant from {@link \Wl\Video\Catalog\Filter\Sort\FilterSortSid}. `null` or 0 if sort is undefined and custom sort order is used.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'The filter phrase to filter videos by name.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of the client who request list of videos. `null` if user is not signed in. NOTE: Not used directly in API, needed for {@link VideoListApi::KEY} constant.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_level_filter:
                        type: array
                        description: 'All levels to offer in the level filter of the video catalog. Empty array if levels are not shown in this business. Each element: <dl> <dt>string `k_video_level`</dt> <dd>Level key. <i>Primary key in {@link \Wl\Video\Level\LevelSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Level name.</dd> </dl>'
                      a_list:
                        type: array
                        description: 'A list of videos. Each element: <dl> <dt>array[] `a_staff`</dt> <dd> List of staff members associated with the video. <dl> <dt>string `uid_staff`</dt> <dd>UID of the staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `text_name`</dt> <dd>Full name of the staff.</dd> <dt>string `k_staff`</dt> <dd>Staff key. Only for legacy apps. <i>Primary key in {@link \RsStaffSql} table.</i></dd> </dl> </dd> <dt>array[] `a_video_category`</dt> <dd> List of video categories. <dl> <dt>string `k_video_category`</dt> <dd>Video category key. <i>Primary key in {@link \Wl\Video\Category\VideoCategorySql} table.</i></dd> <dt>string `text_title`</dt> <dd>Category name.</dd> </dl> </dd> <dt>array[] `a_video_tag`</dt> <dd> List of video tags. <dl> <dt>string `k_video_tag`</dt> <dd>Video tag key. <i>Primary key in {@link \Wl\Video\Tag\TagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Tag name.</dd> </dl> </dd> <dt>string|null `dtl_publish`</dt> <dd>Date and time when the video becomes published, in the business time zone. `null` if not set.</dd> <dt>string|null `dtl_unpublish`</dt> <dd>Date and time when the video becomes unpublished, in the business time zone. `null` if not set.</dd> <dt>string `dtl_upload`</dt> <dd>Date and time when the video was uploaded, in the business time zone.</dd> <dt>string `html_description`</dt> <dd>HTML description of the video parsed from `xml_description`.</dd> <dt>int `i_calorie`</dt> <dd>Number of calories burned during the video session.</dd> <dt>int `i_current_percent`</dt> <dd>Percentage of the video watched by the current user.</dd> <dt>int `i_current_time`</dt> <dd>Current watch position in seconds for the current user.</dd> <dt>int `i_duration`</dt> <dd>Video duration in seconds.</dd> <dt>int `i_watch`</dt> <dd>Total number of views.</dd> <dt>int `id_source`</dt> <dd>Video source. One of {@link \Wl\Video\VideoSourceSid} constants.</dd> <dt>bool `is_calorie`</dt> <dd>`true` if calorie information is enabled for this video; `false` otherwise.</dd> <dt>bool `is_converted`</dt> <dd>`true` if the video has been converted to HLS format; `false` otherwise.</dd> <dt>bool `is_published`</dt> <dd>`true` if the video is currently published; `false` otherwise.</dd> <dt>bool `is_video_level`</dt> <dd>`true` if a difficulty level is assigned to this video; `false` otherwise.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql}.</i></dd> <dt>string `k_video`</dt> <dd>Video key. <i>Primary key in {@link \Wl\Video\VideoSql}.</i></dd> <dt>string `k_video_binary`</dt> <dd>Binary video key. <i>See {@link \Core\Db\Shard\DbShardKey::keyBinary()}.</i></dd> <dt>string|null `k_video_level`</dt> <dd>Video level key. `null` if no level assigned. <i>Primary key in {@link \Wl\Video\Level\LevelSql} table.</i></dd> <dt>bool `show_calorie`</dt> <dd>`true` if calorie count should be displayed; `false` otherwise.</dd> <dt>bool `show_level`</dt> <dd>`true` if difficulty level should be displayed; `false` otherwise.</dd> <dt>bool `show_view`</dt> <dd>`true` if view count should be displayed; `false` otherwise.</dd> <dt>string `text_level_title`</dt> <dd>Difficulty level title.</dd> <dt>string `text_title`</dt> <dd>Video title.</dd> <dt>string|null `url_thumbnail`</dt> <dd>URL of the video thumbnail image.</dd> <dt>string|null `url_video`</dt> <dd> URL of the video file (HLS stream). `null` if not available. See {@link \Wl\Video\VideoElementApi::$url_video} for details about the video URL. </dd> </dl>'
                      a_page:
                        type: array
                        description: 'Pagination data. Empty array if pagination is not active (total video count is within {@link VideoListApi::VIDEO_COUNT_PAGINATION}). Otherwise contains a list of page entries as: <dl> <dt>bool `is-next`</dt> <dd>`true` for the "next page" navigation entry. Only present on the next-page entry.</dd> <dt>bool `is-previous`</dt> <dd>`true` for the "previous page" navigation entry. Only present on the previous-page entry.</dd> <dt>bool `is-selected`</dt> <dd>`true` if this entry represents the current page. Only present on the selected page entry.</dd> <dt>int `page`</dt> <dd>Page number. `0` represents a skipped range (ellipsis) between non-adjacent pages.</dd> <dt>bool `skip`</dt> <dd>`true` if this entry represents a skipped page range (ellipsis). Only present on skip entries.</dd> </dl>'
                      a_staff_filter:
                        type: array
                        description: 'All staff members to offer in the staff filter of the video catalog. Each element: <dl> <dt>string `text_name`</dt> <dd>Full name of the staff member.</dd> <dt>string `uid_staff`</dt> <dd>User key of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff member key. Only for legacy applications. <i>Primary key in {@link \RsStaffSql} table.</i></dd> </dl>'
                      a_video_tag_filter:
                        type: array
                        description: 'All tags to offer in the tag filter of the video catalog. Each element: <dl> <dt>string `k_video_tag`</dt> <dd>Tag key. <i>Primary key in {@link \Wl\Video\Tag\TagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Tag name.</dd> </dl>'
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      id_order:
                        type: [number, "null"]
                        description: 'Sort order ID. One of {@link \Core\Sid\SortOrderSid} constants. `null` or 0 if order is undefined and depends on sort order.'
                      id_sort:
                        type: [number, "null"]
                        description: 'The sorting type. A constant from {@link \Wl\Video\Catalog\Filter\Sort\FilterSortSid}. `null` or 0 if sort is undefined and custom sort order is used.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
      tags:
        - Video-VideoList
    put:
      summary: 'Updates the custom sort order of videos for the specified business. Reorders the video library according to the provided list. Only takes effect when the library is configured to use the custom sort order. Requires backend access with the video library management privilege.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of shared video keys in their saved order.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
      tags:
        - Video-VideoList
  '/Wl/Review/Review.json':
    post:
      summary: 'Submits user''s review. Validates the captcha, saves the review rating and text for the specified location, and returns reward score information if the business awards points for leaving a review.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_rate:
                  type: [number, "null"]
                  description: 'The rating given to the location (1 to 5 stars). This will be `null` if not set yet.'
                k_location:
                  type: [string, "null"]
                  description: 'The key of a location. This will be `null` if not loaded yet.'
                k_visit:
                  type: [string, "null"]
                  description: 'Visit key. Can be `null` if the review is not connected to a visit.'
                s_text:
                  type: [string, "null"]
                  description: 'The text of the review. This will be `null` if not set yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Reward score for leaving a review.'
                      i_score_facebook:
                        type: number
                        format: int32
                        description: 'Reward score for sharing a review on Facebook.'
                      i_score_twitter:
                        type: number
                        format: int32
                        description: 'Reward score for sharing a review on Twitter.'
                      is_score:
                        type: boolean
                        description: 'If a reward score for leaving a review exists.'
                      is_score_facebook:
                        type: boolean
                        description: 'If a reward score for sharing a review on Facebook exists.'
                      is_score_twitter:
                        type: boolean
                        description: 'If a reward score for sharing a review on Twitter exists.'
                      is_share_points:
                        type: boolean
                        description: 'If a reward score for sharing exists.'
                      is_share_points_none:
                        type: boolean
                        description: 'If a reward score does not exist for leaving a review or sharing the review.'
                      k_login_activity:
                        type: string
                        description: 'The key of the review writing activity. This will be empty if the review was saved but not published.'
                      k_review:
                        type: string
                        description: 'Review key.'
                      uid:
                        type: string
                        description: 'The UID of client who leaves review.'
                      url_share:
                        type: string
                        description: 'The sharing url of the review.'
      tags:
        - Review-Review
  '/Wl/Review/ReviewFeature.json':
    post:
      summary: 'Updates the featured status of the given review for the business. If featuring the review, unfeatures the currently featured review for the business location and marks the specified review as featured; if unfeaturing, clears the featured flag on the given review.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_featured:
                  type: boolean
                  description: 'Determines if the review is featured.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_review:
                  type: string
                  description: 'The review key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Review-ReviewFeature
  '/Wl/Review/ReviewReply.json':
    post:
      summary: 'Saves the reply text and optional status update for the given review. Validates edit access for the current user, persists the reply text and optional review status change, and records the replying staff or admin user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_review_status:
                  type: [number, "null"]
                  description: 'Status of the Review. One of {@link \RsReviewStatusSid} constants. `null` until passed to the api when admin replies to the review.'
                k_business:
                  type: string
                  description: 'Key of the business to which the review belongs. Empty string to defined business automatically, based on value of <tt>k_review</tt>. Although this value may be empty, this behavior is deprecated and will be removed in the future. You MUST pass key of the business always.'
                k_review:
                  type: string
                  description: 'The review key.'
                text_reply:
                  type: string
                  description: 'The reply text for review.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Review-ReviewReply
  '/Wl/Staff/StaffElement.json':
    post:
      summary: 'Update or create staff. Creates a new staff member or updates an existing one in the business. When creating, a new user account is provisioned if no matching email exists. When updating, the target staff member must already belong to the business. Role, location, contact details, employment dates, and custom profile fields can all be set in a single call.'
      parameters:
        - name: k_staff
          in: query
          description: 'The key of the staff member resolved and used internally by this API. This property is populated from {@link StaffElementApi::$uid_staff} in {@link StaffElementApi::post()} and returned in API result for compatibility. Passing `k_staff` in request payload is not supported and causes an error. <i>See {@link StaffElementApi::_normalizeStaffKey()} method.</i>'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: [array, "null"]
                  description: 'The list of locations where the staff member works. `null` means to not change the current value of the field.'
                dl_end:
                  type: [string, "null"]
                  description: 'Employment end date. `null` means to not change the current value of the field.'
                dl_start:
                  type: [string, "null"]
                  description: 'Employment start date. `null` means to not change the current value of the field.'
                id_gender:
                  type: [number, "null"]
                  description: 'Gender of staff member. One of {@link \AGenderSid} constants. `null` means to not change the current value of the field or set gender by default for new staff.'
                id_role:
                  type: [number, "null"]
                  description: 'ID of the default system role. One of {@link \RsPrivilegeRoleSid}. `null` means to not change the current value of the field.'
                is_employ:
                  type: [boolean, "null"]
                  description: 'Whether the staff is currently employed. `null` means to not change the current value of the field.'
                is_microsite:
                  type: [boolean, "null"]
                  description: 'Determines whether the staff member be shown on the directory site of the business. If `null`, the current value of the field shouldn''t be changed.'
                is_password_set_disable:
                  type: boolean
                  description: 'Whether password set is disabled. `true` - if the email is associated to a client profile which has a password set, `false` - otherwise.'
                is_uid:
                  type: boolean
                  description: 'Whether the staff member can to sign in.'
                k_business:
                  type: string
                  description: 'The key of the business in which the staff member is being created or edited. This field is required.'
                k_business_role:
                  type: [string, "null"]
                  description: 'Business role key. `null` if not set.'
                k_city:
                  type: [string, "null"]
                  description: 'City key. `null` means to not change the current value of the field.'
                k_location:
                  type: [string, "null"]
                  description: 'Home location key. `null` if not set.'
                text_address:
                  type: [string, "null"]
                  description: 'Address. `null` means to not change the current value of the field.'
                text_biography:
                  type: [string, "null"]
                  description: 'Staff biography. `null` means to not change the current value of the field.'
                text_city:
                  type: [string, "null"]
                  description: 'Custom city title. `null` means to not change the current value of the field or <var>k_city</var> is specified.'
                text_email:
                  type: [string, "null"]
                  description: 'The staff member''s email address. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_first_name:
                  type: [string, "null"]
                  description: 'The staff member''s first name. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_last_name:
                  type: [string, "null"]
                  description: 'The staff member''s last name. If `null`, the current value of the field shouldn''t be changed.'
                text_password:
                  type: [string, "null"]
                  description: 'The password. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_password_confirm:
                  type: [string, "null"]
                  description: 'Confirmation of the password. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_phone_home:
                  type: [string, "null"]
                  description: 'Home phone. `null` means to not change the current value of the field.'
                text_phone_mobile:
                  type: [string, "null"]
                  description: 'Cell phone. `null` means to not change the current value of the field.'
                text_position:
                  type: [string, "null"]
                  description: 'The staff member''s job title. If `null`, the current value of the field shouldn''t be changed.'
                text_postal:
                  type: [string, "null"]
                  description: 'The staff member''s job title. If `null`, the current value of the field shouldn''t be changed.'
                uid_staff:
                  type: [string, "null"]
                  description: 'User key of a staff member. This is the supported request identifier for staff update operations. Value is normalized into {@link StaffElementApi::$k_staff} in {@link StaffElementApi::post()}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_staff:
                        type: [string, "null"]
                        description: 'The key of the staff member resolved and used internally by this API. This property is populated from {@link StaffElementApi::$uid_staff} in {@link StaffElementApi::post()} and returned in API result for compatibility. Passing `k_staff` in request payload is not supported and causes an error. <i>See {@link StaffElementApi::_normalizeStaffKey()} method.</i>'
      tags:
        - Staff-StaffElement
  '/Wl/Location/List.json':
    get:
      summary: 'Retrieves a list of locations for the given business. Accepts a single business key, a JSON-encoded list of business keys, or an array of business keys, and returns location details including coordinates, address, timezone, directories, and logo URLs. For franchisor businesses, automatically expands to include all franchisee locations.'
      parameters:
        - name: a_business
          in: query
          description: 'A list of business keys. You can specify this instead of {@link ListApi::$k_business} to load data for many businesses in one call.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The business key used internally by WellnessLiving. In case when passed regular or franchisee business returns only locations for requested business. In case when passed franchisor business returns locations for all franchisees of this franchisor.'
          schema:
            type: string
        - name: s_business
          in: query
          description: 'The primary keys of the selected businesses. You can specify this instead of {@link ListApi::$k_business} to load data for many businesses in one call. This is the same as <var>$a_business</var>, but serialized with JSON (to make the URL shorter).'
          schema:
            type: string
        - name: show_remove
          in: query
          description: 'Determines whether removed locations should be returned.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: object
                        description: 'Information about the business''s location(s). If you''ve specified multiple businesses for this endpoint, this will return location information for multiple businesses. Keys refer to location keys. Values refer to nested arrays with the next keys: <dl> <dt>int[] `a_directories`</dt> <dd>List of directories from {@link \RsProjectSid}, where location is published.</dd> <dt>array `a_timezone`</dt> <dd> Information about location timezone: <dl> <dt>string `k_timezone`</dt> <dd>Timezone key. <i>Primary key in the {@link \AGeoTimezoneSql} table.</i></dd> <dt>string `text_abbr`</dt> <dd>User-friendly short timezone abbreviation, for example ''EST'' or ''PST''.</dd> <dt>string `text_name`</dt> <dd>Full timezone name, for example ''America/New_York''.</dd> </dl> </dd> <dt>float `f_latitude`</dt> <dd>The location latitude.</dd> <dt>float `f_longitude`</dt> <dd>The location longitude.</dd> <dt>int `i_shift`</dt> <dd>Timezone shift in hours.</dd> <dt>string `k_business`</dt> <dd>The business key.</dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_region`</dt> <dd>Region key. <i>Primary key in {@link \AGeoSql} table.</i></dd> <dt>string `s_mail`</dt> <dd>The location email address.</dd> <dt>string `s_phone`</dt> <dd>The location phone number.</dd> <dt>string `s_title`</dt> <dd>The location title.</dd> <dt>string `text_address`</dt> <dd>The location''s full address.</dd> <dt>string `text_region_code`</dt> <dd>The location''s region abbreviation.</dd> <dt>string `url_logo`</dt> <dd> The location logo''s URL. This will only be set if the location has a logo and contains small thumbnail of the image. </dd> <dt>string `url_logo_source`</dt> <dd>The location logo''s URL. This will only be set if the location has a logo and contains full uploaded image.</dd> </dl>'
      tags:
        - Location-List
  '/Wl/Location/ListBulk.json':
    get:
      summary: 'Gets location lists for a bulk of businesses. Accepts a JSON-encoded list of business keys, a JSON-encoded list of location keys, or both, and returns short-form location data alongside full location detail objects. Optionally filters results to only locations that have a specific directory integration enabled.'
      parameters:
        - name: id_directory
          in: query
          description: 'The ID of the directory if locations should be filtered by enabling directory integration. `0` if a directory filter isn''t required.'
          schema:
            type: number
            format: int32
        - name: s_business
          in: query
          description: 'A list of businesses. Business primary keys are serialized with JSON. Empty string if you need all locations in the system.'
          schema:
            type: string
        - name: s_location
          in: query
          description: 'A list of locations. Location primary keys are serialized with JSON.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: object
                        description: 'Short-form information about locations. Keys refer to location primary keys. Values refer to sub-arrays with the next keys: <dl> <dt>float `f_latitude`</dt> <dd>The latitude coordinate.</dd> <dt>float `f_longitude`</dt> <dd>The longitude coordinate.</dd> <dt>float `f_rate`</dt> <dd>The location rate.</dd> <dt>int `i_order`</dt> <dd>The order number.</dd> <dt>string `k_business`</dt> <dd>The business''s primary key.</dd> <dt>string `k_location`</dt> <dd>The location''s primary key.</dd> <dt>string `s_title`</dt> <dd>The location title.</dd> <dt>string `text_address`</dt> <dd>The full location address.</dd> <dt>string `url_logo`</dt> <dd>The location logo.</dd> </dl>'
                      a_location_full:
                        type: object
                        description: 'A list of models with full information about each location. <dl> <dt>int[] `a_age`</dt> <dd>A list of ages that are permitted for visiting this location.</dd> <dt>int[] `a_amenities`</dt> <dd>A list of facilities that are available in this location.</dd> <dt>string[] `a_level`</dt> <dd>A list of levels that are suitable for visiting this location.</dd> <dt>array `a_logo`</dt> <dd> Information about the location logo used in WellnessLiving: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl> </dd> <dt>array `a_slide`</dt> <dd> A list of the location images. Every element has the following keys: <dl> <dt>int `i_height`</dt> <dd>Height.</dd> <dt>int `i_width`</dt> <dd>Width.</dd> <dt>string `url_preview`</dt> <dd>URL to image preview.</dd> <dt>string `url_slide`</dt> <dd>URL to full image.</dd> </dl> </dd> <dt>array `a_work`</dt> <dd> The hours of operation for the location. Fields are numbers representing specific days (1 is Monday, 7 is Sunday). Values are objects with the next fields: <dl> <dt>string `s_end`</dt> <dd>The time of day end in MySQL format.</dd> <dt>string `s_start`</dt> <dd>The time of day start in MySQL format.</dd> </dl> </dd> <dt>string|null `dtu_check`</dt> <dd> The date when the location actuality was last checked. `null` if the location has never been checked. Set only in {@link \Wl\Location\ListBulkApi}, otherwise always `null`. </dd> <dt>string `dtu_create`</dt> <dd>The date when the location was created.</dd> <dt>float `f_latitude`</dt> <dd>The latitude coordinate of the location.</dd> <dt>float `f_longitude`</dt> <dd>The longitude coordinate of the location.</dd> <dt>string `html_description_full`</dt> <dd>The full description of the location.</dd> <dt>string `html_description_preview`</dt> <dd> A shorter description of the location. A preview of {@link \Wl\Location\View\ViewApi::$html_description_full}. </dd> <dt>int `i_logo_height`</dt> <dd>Maximum location image height.</dd> <dt>int `i_logo_width`</dt> <dd>Maximum location image width.</dd> <dt>int|null `id_industry`</dt> <dd> The industry of the business. `null` in case when industry didn''t set for chosen business type, or for business types where set value which is absent. </dd> <dt>bool `is_phone`</dt> <dd>`true` if to display phone number on location page. `false` otherwise.</dd> <dt>bool `is_top_choice`</dt> <dd>`true` if WellnessLiving identifies this is a top choice location, `false` otherwise.</dd> <dt>string `k_business`</dt> <dd>The key of the business this location belongs to.</dd> <dt>string `k_business_type`</dt> <dd>The key of the business type this location belongs to.</dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_timezone`</dt> <dd>The timezone.</dd> <dt>string `s_address`</dt> <dd>The physical address of the location.</dd> <dt>string `s_map`</dt> <dd>A string that can be used in navigator programs.</dd> <dt>string `s_phone`</dt> <dd>The phone number for the location.</dd> <dt>string `s_timezone`</dt> <dd>The system name of the time zone.</dd> <dt>string `s_title`</dt> <dd>The title of the location.</dd> <dt>string `text_address_individual`</dt> <dd>The address of the location.</dd> <dt>string `text_alias`</dt> <dd> The special system name, which can be used in custom location links. This is usually based on name of the location, with extra characters removed. However, it can also can be fully customized. </dd> <dt>string `text_business_type`</dt> <dd>The name of the business type.</dd> <dt>string `text_city`</dt> <dd>The city name of the location.</dd> <dt>string `text_country`</dt> <dd>The country name of the location.</dd> <dt>string `text_industry`</dt> <dd>The name of the industry of the business.</dd> <dt>string `text_mail`</dt> <dd>The email address.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the location.</dd> <dt>string `text_region`</dt> <dd>The region name of the location.</dd> <dt>string `text_region_code`</dt> <dd>Region 2 or 3 letters abbreviation of the location. Can be empty, if abbreviation for region is not set.</dd> <dt>string `url_facebook`</dt> <dd>The Facebook URL of the location.</dd> <dt>string `url_instagram`</dt> <dd>The Instagram URL of the location.</dd> <dt>string `url_linkedin`</dt> <dd>The Linked In URL of the location.</dd> <dt>string `url_map`</dt> <dd>The URL of the location in Google Maps.</dd> <dt>string `url_microsite`</dt> <dd>The URL to the location entry in the WellnessLiving Explorer listing.</dd> <dt>string `url_site`</dt> <dd>The website URL of the location.</dd> <dt>string `url_twitter`</dt> <dd>The URL of the location''s Twitter account.</dd> <dt>string `url_web`</dt> <dd>The URL to the business''s Client Web App.</dd> <dt>string `url_youtube`</dt> <dd>The YouTube URL of the location.</dd> </dl>'
      tags:
        - Location-ListBulk
  '/Wl/Location/Location.json':
    post:
      summary: 'Creates new location or edits the existing location. When {@link LocationApi::$k_location} is omitted, creates a new location requiring {@link LocationApi::$k_city}, {@link LocationApi::$text_address}, and {@link LocationApi::$text_title}. When {@link LocationApi::$k_location} is provided, updates only the supplied fields of the existing location. Returns the key of the created or updated location.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of the business. This field is required.'
                k_city:
                  type: [string, "null"]
                  description: 'The key of the city. This field is required for creating a new location.'
                k_location:
                  type: [string, "null"]
                  description: 'The key of the location. This will be `null` if it''s being used to create a new location.'
                k_timezone:
                  type: [string, "null"]
                  description: 'The key of the time zone. This field is optional.'
                text_address:
                  type: [string, "null"]
                  description: 'The address of the location. This field is required for creating a new location.'
                text_email:
                  type: [string, "null"]
                  description: 'The mail address.'
                text_phone:
                  type: [string, "null"]
                  description: 'The phone number.'
                text_postal:
                  type: [string, "null"]
                  description: 'The postal code.'
                text_title:
                  type: [string, "null"]
                  description: 'The title of the location. This field is required for creating a new location.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_location:
                        type: [string, "null"]
                        description: 'The key of the location. This will be `null` if it''s being used to create a new location.'
      tags:
        - Location-Location
  '/Wl/Tax/Tax.json':
    get:
      summary: 'Returns the title for the specified tax. Returns the human-readable display name for the given tax record. Throws if the tax key is invalid or if the tax has been marked as removed.'
      parameters:
        - name: k_tax
          in: query
          description: 'The tax key to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_tax:
                        type: string
                        description: 'The tax title.'
      tags:
        - Tax-Tax
  '/Wl/Tax/TaxList.json':
    get:
      summary: 'Returns taxes of the specified business. Returns all active taxes configured for the business, including each tax''s title, rate, and type. Used to populate tax pickers when creating products, promotions, or invoices.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business for which to get a list of taxes.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of taxes. Each element has the following structure: <dl> <dt>string[] `a_location`</dt> <dd>The locations where the tax is applicable.</dd> <dt>float `f_value`</dt> <dd>The amount of the tax.</dd> <dt>string `k_tax`</dt> <dd>The type of the tax, one of the {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl>'
      tags:
        - Tax-TaxList
  '/Wl/Skin/Skin.json':
    get:
      summary: 'Gets skin data. Returns the skin settings and the main widget page URL for the given skin key.'
      parameters:
        - name: k_skin
          in: query
          description: 'Skin key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_skin:
                        type: array
                        description: 'Skin data. <i>Result of the {@link \RsSkinData::skin()} method.</i>'
                      id_skin:
                        type: number
                        format: int32
                        description: 'Skin type, one of {@link \RsSkinSid} constants.'
                      url_skin:
                        type: string
                        description: 'URL to the main page of the widget.'
      tags:
        - Skin-Skin
    post:
      summary: 'Saves the specified skin fields. Applies the given field values to the skin data and logs the change when any field value has actually changed.'
      parameters:
        - name: k_skin
          in: query
          description: 'Skin key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field:
                  type: array
                  description: 'Skin fields to save. Key is full name of the skin field, it is separated string containing field name and element name. <i>Element name from {@link \Wl\Skin\Application\Application::field_list()}.</i> Value is new field value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Skin-Skin
  '/Wl/Skin/SkinForeign.json':
    delete:
      summary: 'Deletes the existing widget. Permanently removes a widget skin and its foreign ID mapping from the business. Used by external integrators to clean up a widget that is no longer needed.'
      parameters:
        - name: id_skin
          in: query
          description: 'Skin type, one of {@link \RsSkinSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: s_foreign_id
          in: query
          description: 'Foreign skin key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_skin:
                        type: string
                        description: 'Skin key.'
      tags:
        - Skin-SkinForeign
    post:
      summary: 'Creates a new widget. Creates a new widget skin for the business and links it to an external identifier provided by the integrator. The foreign ID must be unique within the business; attempting to create a duplicate triggers an error directing the caller to use PUT instead.'
      parameters:
        - name: id_skin
          in: query
          description: 'Skin type, one of {@link \RsSkinSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: s_foreign_id
          in: query
          description: 'Foreign skin key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data_specific:
                  type: array
                  description: 'Data specific for the skin type. <i>It is passed to {@link \RsSkinData::data_set()} method.</i> All fields are optional and may be omitted if not needed. For Lead Capture widget it may contain: <dl> <dt>bool `is_add_lead`</dt> <dd>`true` if it can be used in staff back office to add leads by staff member.</dd> <dt>bool `is_welcome_email`</dt> <dd>`true` if welcome email should be sent to each added lead.</dd> <dt>string `k_promotion`</dt> <dd> Key of the purchase option to be granted to each added lead for free. <i>Primary key in the {@link \RsPromotionSql} table.</i> </dd> <dt>bool `is_redirect_self`</dt> <dd>`true` if redirection after lead is captured should be done in the same tab, `false` - in a new tab.</dd> <dt>string `url_redirect`</dt> <dd>URL for redirection after lead is captured.</dd> <dt>string `text_confirmation`</dt> <dd>Confirmation text to be shown after lead is added.</dd> </dl>'
                a_fields:
                  type: array
                  description: 'Profile fields, which should be shown on the widget and if they are required or optional. <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>bool `is_required`</dt> <dd>`true` if field should be required, `false` - optional.</dd> </dl>'
                text_name:
                  type: string
                  description: 'Name of the widget.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_skin:
                        type: string
                        description: 'Skin key.'
      tags:
        - Skin-SkinForeign
    put:
      summary: 'Updates the existing widget. Updates the configuration of an existing widget skin identified by the business and external foreign ID. Throws an error if no matching foreign ID is found, directing the caller to use POST instead.'
      parameters:
        - name: id_skin
          in: query
          description: 'Skin type, one of {@link \RsSkinSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: s_foreign_id
          in: query
          description: 'Foreign skin key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data_specific:
                  type: array
                  description: 'Data specific for the skin type. <i>It is passed to {@link \RsSkinData::data_set()} method.</i> All fields are optional and may be omitted if not needed. For Lead Capture widget it may contain: <dl> <dt>bool `is_add_lead`</dt> <dd>`true` if it can be used in staff back office to add leads by staff member.</dd> <dt>bool `is_welcome_email`</dt> <dd>`true` if welcome email should be sent to each added lead.</dd> <dt>string `k_promotion`</dt> <dd> Key of the purchase option to be granted to each added lead for free. <i>Primary key in the {@link \RsPromotionSql} table.</i> </dd> <dt>bool `is_redirect_self`</dt> <dd>`true` if redirection after lead is captured should be done in the same tab, `false` - in a new tab.</dd> <dt>string `url_redirect`</dt> <dd>URL for redirection after lead is captured.</dd> <dt>string `text_confirmation`</dt> <dd>Confirmation text to be shown after lead is added.</dd> </dl>'
                a_fields:
                  type: array
                  description: 'Profile fields, which should be shown on the widget and if they are required or optional. <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>bool `is_required`</dt> <dd>`true` if field should be required, `false` - optional.</dd> </dl>'
                text_name:
                  type: string
                  description: 'Name of the widget.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_skin:
                        type: string
                        description: 'Skin key.'
      tags:
        - Skin-SkinForeign
  '/Wl/Rank/Rank.json':
    get:
      summary: 'Gets belts list of a business. Returns all belts for the specified business, optionally filtered by belt category keys, including each belt''s title and its parent category title.'
      parameters:
        - name: a_rank_category
          in: query
          description: 'Rank category keys. Used to filter belts by belt categories.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_rank_list:
                        type: array
                        description: 'A list of belts, keys, and information. Each element is an array with the following information: <dl> <dt>string `k_rank`</dt> <dd>A belt key. <i>Primary key in {@link \RsRankSql} table.</i></dd> <dt>string `k_rank_category`</dt> <dd>A belt category key. <i>Primary key in {@link \RsRankCategorySql} table.</i></dd> <dt>string `text_rank`</dt> <dd>Belt title.</dd> <dt>string `text_rank_category`</dt> <dd>Belt category title.</dd> </dl>'
      tags:
        - Rank-Rank
  '/Wl/Fitbuilder/Message.json':
    post:
      summary: 'Notifies messengers with new information. Accepts a channel key and a data payload, then dispatches a notification to the Fitbuilder messenger channel associated with that key. Requires the `notify` API privilege.'
      parameters:
        - name: k_channel
          in: query
          description: 'Messenger channel key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Messenger information: <dl> <dt>array `message`</dt> <dd> Message information: <dl> <dt>int `id`</dt> <dd>Message key.</dd> <dt>string `message`</dt> <dd>Message text.</dd> <dt>string `updated_at`</dt> <dd>Date/time when the message has been posted.</dd> </dl> </dd> <dt>array `user_profile`</dt> <dd> User''s information: <dl> <dt>int `id_gender`</dt> <dd>User gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>string `s_first_name`</dt> <dd>User first name.</dd> <dt>string `s_last_name`</dt> <dd>User last name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_photo`</dt> <dd>URL of the user''s profile photo.</dd> </dl> </dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Fitbuilder-Message
  '/Wl/Announcement/AnnouncementList.json':
    get:
      summary: 'Returns a list of announcements for the given business and location. Retrieves published announcements visible to the current user, optionally filtered to a single location. In backend mode the caller can also sort the results by column and direction, and the chosen sort preference is persisted per user so the backend grid remembers it across requests. Access is validated against announcement editor permissions for the business.'
      parameters:
        - name: id_order
          in: query
          description: 'Order ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default order.'
          schema:
            type: [number, "null"]
        - name: id_sort_field
          in: query
          description: 'Sort field ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default field.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key for which need show announcement. `null` in case when need show location for all locations in business.'
          schema:
            type: [string, "null"]
        - name: text_search
          in: query
          description: 'The filter phrase to filter announcements by name.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of announcements. Each element has the following keys: <dl> <dt>string[] `a_location`</dt> <dd>Location keys where the announcement is published. <i>Primary keys in the {@link \RsLocationSql} table.</i></dd> <dt>string `dtl_create`</dt> <dd>Creation date in local MySQL format.</dd> <dt>string|null `dtl_publish`</dt> <dd>Local date and time the announcement is published in MySQL format. `null` if not set.</dd> <dt>string|null `dtl_unpublish`</dt> <dd>Local date and time the announcement is unpublished in MySQL format. `null` if not set.</dd> <dt>string `html_description`</dt> <dd>Announcement description as HTML.</dd> <dt>string `html_description_clear`</dt> <dd>Announcement description text stripped of HTML tags.</dd> <dt>int|null `i_order`</dt> <dd>Sort order. `null` if the announcement is not pinned.</dd> <dt>int `id_status`</dt> <dd>Announcement status ID. One of {@link \Wl\Announcement\AnnouncementStatusEnum} constants.</dd> <dt>bool `is_pin`</dt> <dd>`true` if the announcement is pinned, `false` otherwise.</dd> <dt>string `k_announcement`</dt> <dd>Announcement key. <i>Primary key in the {@link \Wl\Announcement\AnnouncementSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string `text_create_date`</dt> <dd>Formatted creation date.</dd> <dt>string `text_publish_date`</dt> <dd>Formatted publish date.</dd> <dt>string `text_publish_date_short`</dt> <dd>Formatted publish date with month name but without day of week.</dd> <dt>string `text_title`</dt> <dd>Announcement title.</dd> <dt>string `text_unpublish_date`</dt> <dd>Formatted unpublish date.</dd> <dt>string `uid_create`</dt> <dd>Key of the user who created the announcement. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_detail`</dt> <dd>URL of the announcement detail page.</dd> <dt>string `url_image`</dt> <dd>URL of the announcement image.</dd> <dt>string `xml_description`</dt> <dd>Announcement description as XML.</dd> </dl>'
                      id_order:
                        type: [number, "null"]
                        description: 'Order ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default order.'
                      id_sort_field:
                        type: [number, "null"]
                        description: 'Sort field ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default field.'
      tags:
        - Announcement-AnnouncementList
  '/Wl/Drive/ProductImageUpload.json':
    get:
      summary: 'Returns information about the image. Used to initialize an image upload widget for an existing entity. Returns thumbnail and full-size URLs, upload endpoint, dimension constraints, and whether deletion is allowed, so the frontend can render the current image and offer upload or delete actions.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - Drive-ProductImageUpload
    post:
      summary: 'Loads image information for a list of IDs. The POST method is used instead of the GET method because the maximum permitted URI length is restricted. Bulk variant of the GET method for pages that must display many images at once. Accepts a JSON-encoded list of entity IDs and returns the same metadata as the single-item GET, loading all images in one round-trip to avoid N+1 requests.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_id:
                  type: [string, "null"]
                  description: 'A list of image IDs set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}, serialized using JSON. This will be `null` if there''s no data being sent by the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image information for every ID. <i>Every element contains values such as the {@link ImageUploadApi::_get()} result.</i> <dl> <dt>array `a_text_empty`</dt> <dd> Information about the text on the empty upload image. <i>See: {@link \AImageUpload::textEmpty()}.</i> <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl> </dd> <dt>int `i_height_max`</dt> <dd>The maximum height of the image.</dd> <dt>int `i_height_min`</dt> <dd>The minimum height of the image.</dd> <dt>int `i_thumbnail_height`</dt> <dd>The height of the thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_HEIGHT}.</i></dd> <dt>int `i_thumbnail_width`</dt> <dd>The width of thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_WIDTH}.</i></dd> <dt>int `i_width_max`</dt> <dd>The maximum width of image.</dd> <dt>int `i_width_min`</dt> <dd>The minimum width of image.</dd> <dt>bool `is_circular`</dt> <dd><tt>true</tt> if image is treated as circular; <tt>false</tt> otherwise.</dd> <dt>bool `is_delete_allow`</dt> <dd><tt>true</tt> if image deleting is allowed; <tt>false</tt> otherwise.</dd> <dt>string `s_link`</dt> <dd>The image link.</dd> <dt>string `url_empty`</dt> <dd>The URL of the image that should be shown in a case image is not uploaded.</dd> <dt>string|null `url_thumbnail`</dt> <dd>The URL of thumbnail of the image. <tt>null</tt> if image is not uploaded.</dd> <dt>string `url_upload`</dt> <dd>The URL the script where new image should be uploaded.</dd> <dt>string|null `url_view`</dt> <dd>The URL of the full image. <tt>null</tt> if image is not uploaded.</dd> </dl>'
      tags:
        - Drive-ProductImageUpload
    put:
      summary: 'Updates the image. Applies a command (such as delete or replace) to a previously uploaded image, then returns the refreshed image metadata so the frontend can update the widget state without a separate GET call.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_command:
                  type: string
                  description: 'The action that must be performed to the image.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - Drive-ProductImageUpload
  '/Wl/Currency/Currency.json':
    get:
      summary: 'Retrieves information about currencies. Queries all active (non-removed) currencies from the database and returns them as an associative array keyed by currency key, with each entry containing the currency sign, its display position, and the international currency code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_currency:
                        type: array
                        description: 'Keys - currency keys. Values - information about currency: <dl> <dt>bool `is_before`</dt> <dd><tt>true</tt> - locate sign before amount; <tt>false</tt> - locate sign after amount.</dd> <dt>string `s_sign`</dt> <dd>Sign of currency.</dd> <dt>string `sid_currency`</dt> <dd>International code of the currency.</dd> </dl>'
      tags:
        - Currency-Currency
  '/Wl/Holiday/BulkBusinessHoliday.json':
    get:
      summary: 'Returns all holiday records for all locations of the specified business. Retrieves the complete list of holidays across all date ranges for every location belonging to the given business, including both business-level and location-level closed days.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_holidays:
                        type: array
                        description: 'An array consisting of the business''s closed day data for all locations by holiday keys {@link \Wl\Holiday\HolidayApi::$dl_work}. List of holidays. <dl> <dt>true[] `a_class`</dt> <dd> List of effected classes, keys are class keys. Values always `true`. <i>Primary keys from the {@link \RsClassSql} table.</i> </dd> <dt>true[] `a_class_only`</dt> <dd>List of effected classes, keys are class keys. Values always `true`. Used in schedule widget.</dd> <dt>true[] `a_event_only`</dt> <dd>List of effected events, keys are class keys. Values always `true`. Used in schedule widget.</dd> <dt>string `dt_end`</dt> <dd>Date of the end of the holiday.</dd> <dt>string `dt_start`</dt> <dd>Date of the start of the holiday.</dd> <dt>bool `is_class_all`</dt> <dd>`1` if all classes are selected to cancel, `0` - otherwise.</dd> <dt>bool `is_event_all`</dt> <dd>`1` if all events are selected to cancel, `0` - otherwise.</dd> <dt>bool `is_service_all`</dt> <dd>`1` if all services are selected to cancel, `0` - otherwise.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key from the {@link \RsBusinessSql} table.</i></dd> <dt>string `k_holiday`</dt> <dd>Holiday key. <i>Primary key from the {@link \RsHolidaySql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from the {@link \RsLocationSql} table.</i></dd> <dt>string `k_market`</dt> <dd>The market key. <i>Primary key from the {@link \RsMarketSql} table.</i></dd> <dt>string `s_title`</dt> <dd>Name of the holiday.</dd> </dl>'
      tags:
        - Holiday-BulkBusinessHoliday
  '/Wl/Holiday/Holiday.json':
    get:
      summary: 'Returns information about holiday day of business/locations. For the specified business and date, returns whether that date is a business-wide closed day, the closed day title if applicable, and a map of location keys to their closed day titles for any locations also closed on that date.'
      parameters:
        - name: dl_work
          in: query
          description: 'The date working hours are required for. Set in the business''s timezone.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location_holiday:
                        type: array
                        description: 'A list of the location''s closed day titles by location keys on the date {@link \Wl\Holiday\HolidayApi::$dl_work}. Keys are location keys. Values are holiday title strings.'
                      is_business_holiday:
                        type: boolean
                        description: '<tt>true</tt> if the business has a closed day on the date {@link \Wl\Holiday\HolidayApi::$dl_work}, <tt>false</tt> if otherwise.'
                      text_business_title:
                        type: string
                        description: 'The message used for the business''s closed day on the date {@link \Wl\Holiday\HolidayApi::$dl_work}.'
      tags:
        - Holiday-Holiday
  '/Thoth/ExplorerSearch/ClassSession/ClassSessionSearch.json':
    get:
      summary: 'Gets list of class sessions based on search criteria. Returns sessions matching the given geographic area, date range, and optional filters for business, location, staff, experience type, home tour type, price, and location rating. Only sessions within their current booking window are included in the result.'
      parameters:
        - name: a_business
          in: query
          description: 'List of business keys to search by. Empty array to not filter by business keys.'
          schema:
            type: array
        - name: a_experience_type
          in: query
          description: 'List of experience types to search by. Each value is one of {@link ClassSessionExperienceTypeEnum} case values. Empty array to not filter by experience type.'
          schema:
            type: array
        - name: a_home_tour
          in: query
          description: 'List of home tour activity types to search by. Each value is from {@link \RsHomeTourSid}. Empty array to not filter by home tour type.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'List of location keys to search by. Empty array to not filter by location keys.'
          schema:
            type: array
        - name: a_location_rating
          in: query
          description: 'List of location ratings to search by. Values are integers from 1 to 5, or `null`/`0` for unrated locations. Empty array to not filter by location rating.'
          schema:
            type: [array, object]
        - name: a_staff_uid
          in: query
          description: 'List of staff user keys to search by. Each value is a user key (uid) of a staff member. Empty array to not filter by staff.'
          schema:
            type: array
        - name: dl_begin
          in: query
          description: 'Start date of the session search date range in MySQL format (`Y-m-d`). Required. `null` if not set by request.'
          schema:
            type: [string, "null"]
        - name: dl_end
          in: query
          description: 'End date of the session search date range in MySQL format (`Y-m-d`). Required. `null` if not set by request.'
          schema:
            type: [string, "null"]
        - name: f_latitude
          in: query
          description: 'Latitude coordinate for search. Required. `null` if not set by request.'
          schema:
            type: [number, "null"]
        - name: f_longitude
          in: query
          description: 'Longitude coordinate for search. Required. `null` if not set by request.'
          schema:
            type: [number, "null"]
        - name: f_radius
          in: query
          description: 'Search radius in kilometers. Required. `null` if not set by request.'
          schema:
            type: [number, "null"]
        - name: m_price_max
          in: query
          description: 'Maximum price to search by (inclusive). Decimal string in dollars (e.g. `"100.00"`). `null` to not limit by maximum price.'
          schema:
            type: [string, "null"]
        - name: m_price_min
          in: query
          description: 'Minimum price to search by (inclusive). Decimal string in dollars (e.g. `"0.00"`). `null` to not limit by minimum price.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_session:
                        type: array
                        description: 'List of found class sessions. Each item is an associative array with the following structure: <dl> <dt>string|null `dtu_book_begin`</dt> <dd>Date and time when booking for this session starts in UTC. `null` if there is no "too early" limitation.</dd> <dt>string|null `dtu_book_end`</dt> <dd>Date and time when booking for this session ends in UTC. `null` if there is no "too late" limitation.</dd> <dt>string `dtu_session`</dt> <dd>Date and time of the session in UTC.</dd> <dt>string `k_class`</dt> <dd>Class key. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `k_class_period`</dt> <dd>Class period key. <i>Primary key in {@link \RsClassPeriodSql}.</i></dd> <dt>string `k_class_period_session`</dt> <dd>Class session key. <i>Primary key in {@link \Thoth\ExplorerSearch\ClassSession\ClassPeriodSessionSql}.</i></dd> </dl>'
      tags:
        - ClassSession-ClassSessionSearch
  '/Thoth/LayoutBe/Footer/Footer.json':
    get:
      summary: 'Returns the data required to render the site footer for the given business. Loads the business''s white-label status and derives whether the "Powered by WellnessLiving" branding and the Terms and Conditions link should be shown in the footer.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to get footer data for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      show_term:
                        type: boolean
                        description: '`true` to show the "Powered by WellnessLiving" branding and Terms & Conditions links in the footer; `false` for white-label businesses, which must not display WellnessLiving branding.'
      tags:
        - Footer-Footer
  '/Thoth/PayProcessor/StripeCom/PaymentIntentUpdate.json':
    post:
      summary: 'Updates a `Stripe` Payment Intent. Adjusts the amount and surcharge of the Payment Intent created for the Stripe Payment Element form when the cart contents change, keeping the underlying payment transaction in sync.'
      parameters:
        - name: id_pay_actor
          in: query
          description: 'ID of the actor. One of {@link \RsPayActorSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business to update Payment Intent for.'
          schema:
            type: string
        - name: k_business_merchant
          in: query
          description: 'Key of the business merchant to update Payment Intent for.'
          schema:
            type: string
        - name: k_pay_transaction
          in: query
          description: 'Key of the payment transaction to update. Required.'
          schema:
            type: string
        - name: s_payment_intent
          in: query
          description: 'Payment intent ID to update. Required.'
          schema:
            type: string
        - name: uid_purchase
          in: query
          description: 'Payment owner user key. `null` if pay owner is anonymous.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: number
                  format: int32
                  description: 'ID of the currency. Required.'
                m_amount:
                  type: string
                  description: 'Payment amount to set. Required.'
                m_surcharge:
                  type: string
                  description: 'Surcharge amount to set. Required.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - StripeCom-PaymentIntentUpdate
  '/Thoth/PayProcessor/StripeCom/StripeComKey.json':
    post:
      summary: 'Gets the `Stripe` public key for a merchant. Returns the publishable key configured for the business merchant, so it can be used by the frontend to initialize `Stripe` payment elements.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor.'
                k_business:
                  type: string
                  description: 'Key of the business.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant to get the public key for.'
                uid_purchase:
                  type: string
                  description: 'Payment owner user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_public:
                        type: [string, "null"]
                        description: 'Merchant public key. `null` before the key is set. It is set by {@link \Thoth\PayProcessor\StripeCom\StripeComKeyApi::post()}.'
      tags:
        - StripeCom-StripeComKey
  '/Thoth/PayProcessor/StripeCom/StripeComPayInit.json':
    post:
      summary: 'Initializes a `Stripe` payment. Starts a payment transaction and creates a not-captured Payment Intent at `stripe.com`, returning its status and client secret to continue the payment flow on the frontend.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card:
                  type: [array, "null"]
                  description: 'Credit card data. Contents of the payment source as it presents on the payment form. Supplied in case payment source is card. `null` if payment source is other than card or payment is initialized before card data is known. <dl> <dt>array `a_pay_address`</dt> <dd> Payment address for address edit widget. Used only if `is_new` is `true`: <dl> <dt>bool `is_new`</dt> <dd>`true` if user has chosen to add new address, `false` otherwise.</dd> <dt>string `k_geo_country`</dt> <dd>Key of the country. <i>Primary key in {@link \AGeoSql}.</i> Used only if `is_new` is `true`.</dd> <dt>string `k_geo_region`</dt> <dd>Key of the region. <i>Primary key in {@link \AGeoSql}.</i> Used only if `is_new` is `true`.</dd> <dt>string|null `k_pay_address`</dt> <dd> Chosen payment address key. <i>Primary key in {@link \RsPayAddressSql} table.</i> Set even if user decided to add new address. </dd> <dt>string `s_city`</dt> <dd>City name. Used only if `is_new` is `true`.</dd> <dt>string `s_name`</dt> <dd>User''s full name.</dd> <dt>string `s_phone`</dt> <dd>Phone number. Used only if `is_new` is `true`.</dd> <dt>string `s_postal`</dt> <dd>Postal code. Used only if `is_new` is `true`.</dd> <dt>string `s_street1`</dt> <dd>First address line. Used only if `is_new` is `true`.</dd> <dt>string `s_street2`</dt> <dd>Second address line. Used only if `is_new` is `true`.</dd> <dt>string|null `text_mail`</dt> <dd>User''s email. `null` if not specified. Used only if `is_new` is `true`.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>Card CSC code. Used only if `is_new` is `true`.</dd> <dt>int `i_month`</dt> <dd>Card expiration month. Used only if `is_new` is `true`.</dd> <dt>int `i_year`</dt> <dd>Card expiration year. Used only if `is_new` is `true`.</dd> <dt>int `id_card_type`</dt> <dd> Card type. One of {@link \Thoth\WlPay\Bank\Card\CardTypeEnum} constants as int. Used only if `is_new` is `true`. </dd> <dt>bool `is_default`</dt> <dd>If the card should be made the default payment method. Used only if `is_new` is `true`.</dd> <dt>bool `is_new`</dt> <dd>Whether a new card is being added instead of an existing one being selected.</dd> <dt>bool `is_save`</dt> <dd>If the card information should be saved for future transactions. Used only if `is_new` is `true`.</dd> <dt>string `k_pay_bank`</dt> <dd>Selected payment card key. Used only if `is_new` is `false`. `0` if no existing card is selected.</dd> <dt>string `s_name`</dt> <dd>Card nickname. Used only if `is_new` is `true`.</dd> <dt>string `s_number`</dt> <dd>Card number. Used only if `is_new` is `true`.</dd> </dl>'
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                is_pos:
                  type: boolean
                  description: '`true` if card swiper is used, `false` otherwise.'
                is_stripe_elements:
                  type: boolean
                  description: '`true` if Stripe Payment Element form (@link https://stripe.com/docs/payments/elements) is used, `false` otherwise.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_payment_method:
                  type: [string, "null"]
                  description: 'Payment method ID. `null` if Payment intent must be created without existing Payment method. <i> This is the case for card swiper or Stripe Payment Element ({@link https://stripe.com/docs/payments/elements}) - create payment intent first, then create payment method. And vice versa in case of virtual terminal. </i>'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_status:
                        type: number
                        format: int32
                        description: 'Status of this Payment Intent.'
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      s_client_secret:
                        type: string
                        description: 'Payment Intent client secret key.'
                      s_payment_intent:
                        type: string
                        description: 'Payment intent ID.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - StripeCom-StripeComPayInit
  '/Thoth/PayProcessor/StripeCom/StripeComPaymentIntentCancel.json':
    post:
      summary: 'Cancels a `Stripe` Payment Intent. Verifies that the Payment Intent belongs to the current payer authentication session, then refunds the associated payment transaction to release the reserved amount.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor.'
                k_business:
                  type: string
                  description: 'Key of the business.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant, that processes payment.'
                k_pay_transaction:
                  type: string
                  description: 'Pay transaction key, which was used to create payment intent.'
                s_payment_intent:
                  type: string
                  description: 'Payment intent ID.'
                uid_purchase:
                  type: string
                  description: 'Payment owner user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - StripeCom-StripeComPaymentIntentCancel
  '/Thoth/PayProcessor/CyberSource/CsPaSetup.json':
    post:
      summary: 'Starts `CyberSource` Payer Authentication setup. Validates the payment request and card data, starts a payment transaction, and requests `CyberSource` to set up payer authentication, returning the JWT and device collection URL needed to continue the flow.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card:
                  type: array
                  description: 'Credit card data. Contents of the payment source as it presents on the payment form. <dl> <dt>array `a_pay_address`</dt> <dd> Payment address for address edit widget. Used only if `is_new` is `true`: <dl> <dt>bool `is_new`</dt> <dd>`true` if user has chosen to add new address, `false` otherwise.</dd> <dt>string `k_geo_country`</dt> <dd>Key of the country. <i>Primary key in {@link \AGeoSql}.</i> Used only if `is_new` is `true`.</dd> <dt>string `k_geo_region`</dt> <dd>Key of the region. <i>Primary key in {@link \AGeoSql}.</i> Used only if `is_new` is `true`.</dd> <dt>string|null `k_pay_address`</dt> <dd> Chosen payment address key. <i>Primary key in {@link \RsPayAddressSql} table.</i> Set even if user decided to add new address. </dd> <dt>string `s_city`</dt> <dd>City name. Used only if `is_new` is `true`.</dd> <dt>string `s_name`</dt> <dd>User''s full name.</dd> <dt>string `s_phone`</dt> <dd>Phone number. Used only if `is_new` is `true`.</dd> <dt>string `s_postal`</dt> <dd>Postal code. Used only if `is_new` is `true`.</dd> <dt>string `s_street1`</dt> <dd>First address line. Used only if `is_new` is `true`.</dd> <dt>string `s_street2`</dt> <dd>Second address line. Used only if `is_new` is `true`.</dd> <dt>string|null `text_mail`</dt> <dd>User''s email. `null` if not specified. Used only if `is_new` is `true`.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>Card CSC code. Used only if `is_new` is `true`.</dd> <dt>int `i_month`</dt> <dd>Card expiration month. Used only if `is_new` is `true`.</dd> <dt>int `i_year`</dt> <dd>Card expiration year. Used only if `is_new` is `true`.</dd> <dt>int `id_card_type`</dt> <dd> Card type. One of {@link \Thoth\WlPay\Bank\Card\CardTypeEnum} constants as int. Used only if `is_new` is `true`. </dd> <dt>bool `is_default`</dt> <dd>If the card should be made the default payment method. Used only if `is_new` is `true`.</dd> <dt>bool `is_new`</dt> <dd>Whether a new card is being added instead of an existing one being selected.</dd> <dt>bool `is_save`</dt> <dd>If the card information should be saved for future transactions. Used only if `is_new` is `true`.</dd> <dt>string `k_pay_bank`</dt> <dd>Selected payment card key. Used only if `is_new` is `false`. `0` if no existing card is selected.</dd> <dt>string `s_name`</dt> <dd>Card nickname. Used only if `is_new` is `true`.</dd> <dt>string `s_number`</dt> <dd>Card number. Used only if `is_new` is `true`.</dd> </dl>'
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization and void immediately. `false` (or anything that is empty, or do not pass this value) to capture the payment amount.'
                k_business:
                  type: string
                  description: 'Key of the business which receives the payment.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant that should be used to perform the request.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` or an empty string if payment is performed with a standard payment method. Only payment methods based on {@link \RsPayMethodSid::ECOMMERCE} are accepted.'
                m_amount:
                  type: string
                  description: 'Payment amount.'
                m_surcharge:
                  type: string
                  description: 'Surcharge amount. Empty string or null for zero surcharge amount.'
                uid_pay_owner:
                  type: string
                  description: 'UID of the payment owner.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: string
                        description: 'Key of payment transaction that was created.'
                      s_jwt:
                        type: string
                        description: 'JWT token, as returned by Cyber Source.'
                      s_reference:
                        type: string
                        description: 'Reference ID, as returned by Cyber Source.'
                      url_collection:
                        type: string
                        description: 'Device collection URL, as returned by Cyber Source.'
      tags:
        - CyberSource-CsPaSetup
  '/Thoth/PayProcessor/CyberSource/CsCaptureContext.json':
    post:
      summary: 'Generates a capture context for `CyberSource` Flex Microform. Returns a capture context and key ID that the client uses to encrypt the card number before sending it in the follow-on Tokenize Card request. 1) it requires interaction with the user in the browser; 2) Cyber Source requires that we have `localhost` as a domain which is incompatible with our testing cluster configuration.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant that should be used to perform the request.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_client_library_integrity:
                        type: [string, "null"]
                        description: 'Microform Integration JavaScript library integrity property value. `null` until generated.'
                      s_key:
                        type: [string, "null"]
                        description: 'Contents of the key. `null` until generated.'
                      s_key_id:
                        type: [string, "null"]
                        description: 'ID of the key. `null` until generated.'
                      url_client_library:
                        type: [string, "null"]
                        description: 'Microform Integration JavaScript library url. `null` until generated.'
      tags:
        - CyberSource-CsCaptureContext
  '/Thoth/PayProcessor/CyberSource/CsPaEnrollment.json':
    post:
      summary: 'Checks `CyberSource` Payer Authentication enrollment. Verifies whether the payer must complete additional authentication steps for the transaction started by {@link CsPaSetupApi}, returning a JWT and interaction URL when a challenge is required.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_device_info:
                  type: array
                  description: 'User device info: <dl> <dt>int `i_browser_screen_height`</dt> <dd>User browser screen height.</dd> <dt>int `i_browser_screen_width`</dt> <dd>User browser screen width.</dd> </dl>'
                k_pay_transaction:
                  type: string
                  description: 'Key of payment transaction that was previously created with {@link \Thoth\PayProcessor\CyberSource\CsPaSetupApi}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_jwt:
                        type: [string, "null"]
                        description: 'JSON Web Token to include into the request to the second frame.'
                      url_interact:
                        type: [string, "null"]
                        description: 'URL for frontend interaction. This normally means that payer needs to make further steps for the authentication. `null` if no interaction required, and we should proceed with the withdrawal. This normally means that payer is authenticated. If payer is not authenticated, or there is an error occurred during check of the enrollment, this API returns an exception.'
      tags:
        - CyberSource-CsPaEnrollment
  '/Thoth/PayProcessor/CyberSource/CsPaValidate.json':
    post:
      summary: 'Validates the `CyberSource` Payer Authentication result. Retrieves the authentication result for the transaction validated by {@link CsPaEnrollmentApi} and confirms whether the payer authentication succeeded, allowing the merchant to proceed with processing the payment. 1) it requires interaction with the user in the browser; 2) Cyber Source requires that we have `localhost` as a domain which is incompatible with out testing cluster configuration.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_pay_transaction:
                  type: string
                  description: 'Key of payment transaction that was previously created with {@link \Thoth\PayProcessor\CyberSource\CsPaSetupApi}.'
                s_transaction:
                  type: string
                  description: 'Payer authentication transaction ID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CyberSource-CsPaValidate
  '/Thoth/PayProcessor/Nuvei/NuveiOpenOrder.json':
    post:
      summary: 'Opens a `Nuvei` order for the payment transaction. Authenticates the merchant credentials, starts a payment transaction, and requests `Nuvei` to open an order for the requested amount, returning the order and session identifiers needed to continue the payer authentication flow.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      s_nuvei_order:
                        type: [string, "null"]
                        description: 'The order ID provided by Nuvei. `null` in case of errors.'
                      s_nuvei_session:
                        type: [string, "null"]
                        description: 'The session identifier to be used by the request that processes the newly opened order. `null` in case of errors.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Nuvei-NuveiOpenOrder
  '/Thoth/PayProcessor/Nuvei/NuveiTransactionOnAuthSuccess.json':
    post:
      summary: 'Finalizes the `Nuvei` transaction after successful payer authentication. Validates the payer authentication session and encrypted card token, then updates the payment transaction created by {@link NuveiOpenOrderApi} with the gateway transaction ID and card data.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_bank:
                  type: [string, "null"]
                  description: 'Key of a selected payment source. `null` in case when an existed payment source is not used.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Key of payment transaction that was previously created with {@link \Thoth\PayProcessor\Nuvei\NuveiOpenOrderApi}. `null` in case when not initialized yet.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_encrypt:
                  type: [string, "null"]
                  description: 'Contents of the encrypted token. `null` in case when not initialized yet.'
                s_nuvei_order:
                  type: [string, "null"]
                  description: 'The order ID provided by Nuvei. `null` in case when not initialized yet.'
                s_nuvei_session:
                  type: [string, "null"]
                  description: 'The session identifier to be used by the request that processes the newly opened order. `null` in case when not initialized yet.'
                s_response:
                  type: [string, "null"]
                  description: 'Payment gateway-specific response object.'
                s_transaction:
                  type: [string, "null"]
                  description: 'The Gateway transaction ID.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Nuvei-NuveiTransactionOnAuthSuccess
  '/Thoth/PayProcessor/Nuvei/NuveiTransactionCancel.json':
    post:
      summary: 'Cancels the `Nuvei` transaction. Voids the payer authentication session opened by {@link NuveiOpenOrderApi} and schedules the underlying payment transaction to be voided.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Key of payment transaction that was created in {@link NuveiOpenOrderApi}. This will be `null` if not loaded yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Nuvei-NuveiTransactionCancel
  '/Thoth/PayProcessor/Nuvei/NuveiUpdateOrder.json':
    post:
      summary: 'Updates the amount of a previously opened `Nuvei` order. Validates the payer authentication session, then requests `Nuvei` to update the amount of the order opened by {@link NuveiOpenOrderApi} and adjusts the payment transaction accordingly.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Key of payment transaction that was created in {@link NuveiOpenOrderApi}. This will be `null` if not loaded yet.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_nuvei_order:
                  type: [string, "null"]
                  description: 'The order ID that was created in {@link NuveiOpenOrderApi}. This will be `null` if not loaded yet.'
                s_nuvei_session:
                  type: [string, "null"]
                  description: 'The session identifier in which order was created. This will be `null` if not loaded yet.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Nuvei-NuveiUpdateOrder
  '/Thoth/ReportCore/Generator/Query.json':
    post:
      summary: 'Returns contents of a report as a table. If the report is not yet generated, it gets generated. If the report is being generated now, partial content may be returned.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_report:
                  type: number
                  format: int32
                  description: 'CID of the report to show. One of {@link ReportGeneratorReportAbstract} subclasses.'
                i_limit:
                  type: number
                  format: int32
                  description: 'How many rows of the report to return. `0` to not to return content at all. This may be useful to get metadata of the report.'
                i_offset:
                  type: number
                  format: int32
                  description: 'Offset. How many rows of the report to skip at the beginning of the list. Only return rows after the last skipped row. `0` to return contents from the very beginning.'
                is_actual:
                  type: boolean
                  description: 'Defines whether actual or already generated report should be returned. `true` to not attempt search generated report and generate it again. `false` to attempt search generated report.'
                is_backend:
                  type: boolean
                  description: 'Whether report is generating for the backend.'
                is_refresh:
                  type: boolean
                  description: 'Whether this report should be refreshed. `true` to refresh this report if it is already generated. Refreshing of the report may not be queried while report is being generated. `false` to only return contents of the report. If report is not yet generated, it automatically starts the generation in the background.'
                json_filter:
                  type: string
                  description: 'Filters that should be applied to the report. In this array, key is name of a filter field. <i> A filter field is a property of a {@link ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array. </i>'
                k_business:
                  type: string
                  description: 'Key of the business which report should be shown. `0` or an empty string for system-wide reports.'
                s_sort:
                  type: string
                  description: 'Sorting order. A comma-separated list of fields to sort by. Name of a field may be prepended with a `+` or `-` sign to sort in ascending or descending order. Ascending order is the default. You may sort reports by report fields and by cell fields. To sort by a report field, just specify name of that field. To sort by a cell field, specify name of a report field and name of the cell field. Separate with a dot. For example: <tt>o_account.m_amount</tt>. <i> You may specify a field which contains objects of {@link ReportGeneratorCellAbstract} without specification of a name of a cell field. In this case sorting specified by {@link ReportGeneratorCellAbstract::SORT} will be applied. </i> Example value: <tt>s_first,+s_last,-o_account.m_amount,o_address</tt>. Means the following: * sort by <tt>s_first</tt> in ascending order (which is the default). * sort by <tt>s_last</tt> in ascending order (which is specified explicitly). * sort by <tt>m_amount</tt> field of <tt>o_account</tt> cell in descending order. * sort by default field (i.e. the one marked with &#64;`sort` tag) of <tt>o_address</tt> cell in ascending order. Note that you may not sort by fields that are marked with &#64;`store-no` tag.'
                s_sql:
                  type: string
                  description: 'An SQL query that should be executed on report table. See <tt>namespace.Wl/Report/Generator/doc/report-query-sql.md</tt> for details. Empty string to select all columns of the report table.'
                uid_actor:
                  type: string
                  description: 'Actor user. `0` or an empty string for guests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_dynamic:
                        type: array
                        description: 'A list of dynamic fields in this report. Copy of result of {@link ReportGeneratorReportAbstract::generatorDynamic()}. <dl> <dt>Thoth\ReportCore\Generator\ReportGeneratorFieldInfo[] `a_cell`</dt> <dd> Fields of the complex cell. <i>This field is filled by result of the {@link \Thoth\ReportCore\Generator\ReportGeneratorCellAbstract::field()} method.</i> </dd> <dt>array[] `a_customization_element`</dt> <dd> Elements of a customization from that are required by this filter field. Keys are names of required form elements; values are classes of this elements. </dd> <dt>array `a_type`</dt> <dd> A list of scalar types of values that this field can get. Only scalar types are listed here. Complex types is specified in {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$s_class}. Key is name of a scalar type. Value is always `true`. The following scalar types are possible: * `string` * `int` * `float` * `bool` * `array` (only allowed for values cells, not for report fields) Empty array if scalar types are not acceptable. In this case {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$s_class} is specified. </dd> <dt>bool `is_dynamic`</dt> <dd> Whether this field is dynamic or static. `true` if this field is generated dynamically. <i>Generated dynamically by {@link \Thoth\ReportCore\Generator\ReportGeneratorReportAbstract::generatorDynamic()} method.</i> `false` if this field corresponds to a regular property. <i>Property of a subclass of {@link \Thoth\ReportCore\Generator\ReportGeneratorRowAbstract} or {@link \Thoth\ReportCore\Generator\ReportGeneratorCellAbstract}.</i> </dd> <dt>bool|null `is_export`</dt> <dd> Whether this field is used for export. <tt>true</tt> if this field is used for export. <tt>false</tt> if this field is only used for rendering of the report. <i> Fields that should be exported are marked with `{@}export-yes` tag. Fields that should not be exported are marked with `{@}export-no` tag. `null` until filled in by {@link \Thoth\ReportCore\Generator\ReportGeneratorRowAbstract::field()} or {@link \Thoth\ReportCore\Generator\ReportGeneratorCellAbstract::field()}. </i> </dd> <dt>bool `is_hide_by_default`</dt> <dd> Whether this field should be hidden by default. It can later be shown by using the customization form. `true` if this field should be hidden by default. `false` if this field should be shown by default. </dd> <dt>bool `is_hide_if_empty`</dt> <dd> Whether this field should be hidden if the entire column is empty. <b>Note: Currently only implemented in the export. If you need this to affect the HTML report, you will need to implement that!</b> `true` if this field should be hidden if the entire column is empty. `false` if this field should be shown if the entire column is empty. </dd> <dt>bool `is_null`</dt> <dd> Whether this field is nullable. `true` if `null` is an allowed value for this field. `false` if `null` is not allowed. </dd> <dt>bool|null `is_order`</dt> <dd> Whether the ordering by this field is available. `null` means that value is not initialized. <i>Initialized automatically in {@link \Thoth\ReportCore\Generator\ReportGeneratorRowAbstract::field()}.</i> For dynamic fields should be set manually. </dd> <dt>bool `is_show`</dt> <dd> Whether this field should be shown during report render. `true` if this field should be shown during report render. `false` if this field should not be shown during report render. By default, all fields are shown. To not to show a field, it should be marked with &#64;`show-no` tag. </dd> <dt>bool `is_store`</dt> <dd> Whether this field is stored in the report storage. <i> `true` if this field is stored in the report storage. In this case its value should be loaded in {@link \Thoth\ReportCore\Generator\ReportGeneratorReportAbstract::generatorRun()}. `false` if this field should not be stored in the report storage. In this case its value is loaded in {@link \Thoth\ReportCore\Generator\ReportGeneratorRowAbstract::load()}. By default, all fields are stored. To not to store a field, it should be marked with `{@}store-no` tag. </i> </dd> <dt>string|null `s_cast`</dt> <dd> Argument for the MySQL function `cast()`. Allows treating value of the field as certain type, which is required for proper ordering. For example, keys can be string and can be numbers. If key is a number, it should have cast value as `unsigned`. If key is a string, cast can be left null, because string will be used by default. `null` value should be determined based on the prefix. </dd> <dt>class-string<Thoth\ReportCore\Generator\ReportGeneratorCellAbstract>|null `s_class`</dt> <dd> Name of a cell class. <i> Subclass of {@link \Thoth\ReportCore\Generator\ReportGeneratorCellAbstract} which objects can be used as a value for this report field. </i> `null` if complex types are not acceptable (in this case {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$a_type} is not empty). </dd> <dt>string `s_class_css`</dt> <dd>CSS class that is used for formatting of this field. This class will be added to the column header.</dd> <dt>string|null `s_format`</dt> <dd> Name of formatting method that is used for formatting of this field during export. Formatting method may not be provided if {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$s_class} is set. In this case formatting methods may be specified for individual properties of that class. `null` if no additional formatting should be performed during export. See <tt>namespace.Wl/Report/Generator/doc/report-export-format.md</tt> for details. </dd> <dt>string `s_name`</dt> <dd> Name of a public property in which value of this field is stored. <i>This value corresponds name of a public property in a subclass of {@link \Thoth\ReportCore\Generator\ReportGeneratorRowAbstract}.</i> </dd> <dt>string `s_sort`</dt> <dd> A string by which report columns are sorted. Sorting is only considered for report fields, but not for fields of a report cell. <i> It is a strong requirement that sorting values be unique among all static and all dynamic fields of a report. This is due to that this sorting influences indexes under which data is stored in {@link \Thoth\ReportCore\Generator\ReportStorageContentSql}.<tt>json_row</tt>. </i> Having different order leads to that all indexes are changed. </dd> <dt>string `s_type`</dt> <dd> Type of this field, as specified in its PHP doc. Copy of value of &#64;`var` tag of the field. This value may not be specified for dynamic columns. </dd> <dt>string|null `text_title`</dt> <dd> Data to derive title of a column which values are represented by this report field. <i> Do not use this property to get title of the column. This column does not contain title of a column. It only contains data based on which title should be derived. Use {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::titleText()} to get title of the column. If {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$is_dynamic} is `true`, this property contains a string that should be directly shown to the user. Otherwise, this property contains a part of the source of a translated message. Copy of `{@}title` tag of the field. If `{@}title` tag is not specified, {@link \Thoth\ReportCore\Generator\ReportGeneratorCellAbstract::TITLE} is used. If that constant is not set also, source for translated message is generated based on property name. <i>See {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$s_name} property.</i> For dynamic columns, it is required that this title be set. In this case this is not a source for a translated message. `null` for cell properties, because cells properties are not shown as individual columns in the report. `null` is also set if this field should not be show. <i>If {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$is_show} set to `false`</i> </i> </dd> <dt>string|null `text_title_export`</dt> <dd> Title of this field that is used during export. Copy of &#64;`title-export` tag. If &#64;`title-export` tag is not provided, this property contains copy of {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$text_title}. For report cells, if &#64;`title-export` tag is not set, source for translated message for this property is based on concatenation of {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$s_name} for report row field and report cell field. `null` if this field has {@link \Thoth\ReportCore\Generator\ReportGeneratorFieldInfo::$is_export} set to `false`. </dd> <dt>string|null `text_title_info`</dt> <dd>Text of the cell info tooltip.</dd> </dl>'
                      a_field:
                        type: array
                        description: 'A list of fields in this report. This array is effectively a title row for table that is returned in {@link QueryApi::$a_row}.'
                      a_row:
                        type: object
                        description: 'Report data. This is an indexed array in which one row is an indexed array also. Indexes of the columns correspond columns in {@link QueryApi::$a_field}.'
                      a_stale:
                        type: array
                        description: 'A list of stale rows. This array is only filled in when report is being updated now, or due to some other reasons contains mixed version data (some data from the latest generation, and other from one of previous generations). Value is index in {@link QueryApi::$a_row}. If a row is not listed here, it is of the latest version.'
                      a_warning:
                        type: array
                        description: 'Warning list of the report.'
                      dtu_complete:
                        type: string
                        description: 'Date and time when this report has completed generation. `null` if generation of this report is not completed.'
                      dtu_queue:
                        type: string
                        description: 'Date and time when this report was put on generation queue. Effectively, this is the time when a user clicked to view this report.'
                      dtu_start:
                        type: string
                        description: 'Date and time when generation of this report has started. `null` if generation of this report is not started.'
                      i_cas_change:
                        type: number
                        format: int32
                        description: 'A CAS (compare-and-swap) number that allows to track changes in the report storage. This number is changed every time content of the report gets updated. If this number is not changed, the content is not updated.'
                      id_report_status:
                        type: number
                        format: int32
                        description: 'Status of the report. One of {@link ReportGeneratorStatusSid} constants.'
                      s_report:
                        type: string
                        description: 'Key of this report. This key may be used to subscribe to report changes with {@link UpdateChannel}.'
                      text_error:
                        type: string
                        description: 'Text of an error message that occurred during generation of the report. An empty string in no error messages have occurred.'
      tags:
        - Generator-Query
  '/Thoth/WlPay/Account/Account.json':
    get:
      summary: 'Retrieves information about accounts of given user in given business. Returns the list of existing accounts and accounts not yet created for the user within the specified business, including balance, currency, and payment method details. When {@link AccountApi::$is_owner} is `true`, resolves the money owner and includes the debtor status.'
      parameters:
        - name: is_owner
          in: query
          description: 'If `true`, information for the account''s owner is returned. Clients can be configured to pay for a relative''s expenses. For example, a parent can pay for their child. Otherwise, `false` to indicate information strictly for the specified user is returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_account:
                        type: array
                        description: 'A list of the user''s accounts. Keys are account keys. This could be `0` if the user has no accounts in the currency of the given business. Values are account data: <dl> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constant.</dd> <dt>string `k_currency`</dt> <dd>Key of account currency. <i>Primary key in {@link \RsCurrencySql} table.</i></dd> <dt>string|null `k_pay_account`</dt> <dd> ID of payment account. <i>Primary key in {@link \RsPayAccountSql} table.</i> <tt>null</tt> if this is a user account based on system payment method. </dd> <dt>string|null `k_pay_method`</dt> <dd> ID of custom payment method. <tt>null</tt> if this is a user account based on system payment method. <i>Primary key in {@link \Thoth\WlPay\Method\MethodSql}.</i> </dd> <dt>string `m_rest`</dt> <dd>Account balance.</dd> <dt>string|null `s_method`</dt> <dd>Name of a custom payment method. <tt>null</tt> if this is a user account based on system payment method.</dd> </dl>'
                      a_account_nx:
                        type: array
                        description: 'A list of accounts that have not been created for this user yet. Keys are composed of `{k_pay_method}:{k_currency}`. Each element: <dl> <dt>bool `can_negative`</dt> <dd>`true` if the account is allowed to have a negative balance, `false` otherwise.</dd> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_currency`</dt> <dd>Currency key. <i>Primary key in the {@link \RsCurrencySql} table.</i></dd> <dt>string|null `k_pay_account`</dt> <dd>Payment account key. `null` for accounts not yet created.</dd> <dt>string|null `k_pay_method`</dt> <dd> Custom payment method key. `null` for accounts based on the system payment method. <i>Primary key in the {@link \Thoth\WlPay\Method\MethodSql} table.</i> </dd> <dt>string `m_rest`</dt> <dd>Account balance. Always `''0.00''` for accounts not yet created.</dd> <dt>string|null `s_method`</dt> <dd>Name of the custom payment method. `null` for accounts based on the system payment method.</dd> </dl>'
                      is_debtor:
                        type: boolean
                        description: 'Determines whether the user is a debtor. If `true` - the owner of this account is a debtor. If `false` - the user is not a debtor or the information is not returned for the account owner ({@link AccountApi::$is_owner} is `false`).'
      tags:
        - Account-Account
  '/Thoth/WlPay/Form/Environment.json':
    get:
      summary: 'Returns information about payment environment. Called before rendering a payment form to determine which payment methods and card types are available for a given business and location, what surcharges apply, and how the form should behave (tip prompt, optional card save). This endpoint is deprecated; use {@link \Thoth\WlPay\Form\EnvironmentUserApi} for new integrations.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to retrieve payment information for. This will be `0` if not set yet. An empty string or `null` if payments are performed with the WellnessLiving system merchant.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'The key of the currency to retrieve payment information for.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to retrieve payment information for. This will be `0` if not set yet.'
          schema:
            type: string
        - name: uid_owner
          in: query
          description: 'The user ID to retrieve payment information for. `null` if not defined.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_card_system:
                        type: array
                        description: 'A list of supported bank card systems. Keys refer to card types based on card numbers (see {@link \ACardSystemSid}), and values are IDs of card systems (one of {@link \ACardSystemSid} constants).'
                      a_method_staff:
                        type: array
                        description: 'A list of payment methods enabled for staff members. The ID is one of {@link \RsPayMethodSid} constants. The value is always `true`.'
                      a_method_support:
                        type: array
                        description: 'A list of all payment methods that can be used within this business. This array is sorted in the order in which payment methods should be shown to the user. Each element of the array has the following structure: <dl> <dt>int `id_pay_method`</dt> <dd>The ID of type of payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_client`</dt> <dd> Determines whether this method is available for clients. This field is only returned for custom payment methods. </dd> <dt>string|null `k_pay_method`</dt> <dd> The key of the custom payment method. This will be `null` if this payment method isn''t customized. <i>Primary key in {@link \Thoth\WlPay\Method\MethodSql} table.</i> </dd> <dt>string `s_method`</dt> <dd>The name of payment method. This field is only returned for custom payment methods.</dd> </dl>'
                      a_mobile_config:
                        type: [array, "null"]
                        description: 'The configuration array that''s sent to mobile card reader plugin. The structure of this array depends on the payment processor being used. `null` when mobile card readers are not supported, or when actor has no access to them. StripeCom: <dl> <dt>array `a_terminal_location`</dt> <dd> Terminal configuration keyed by location key. <i>Primary key in {@link \RsLocationSql} table.</i> Empty array if no terminals are configured. Each value: <dl> <dt>array `a_terminal`</dt> <dd> Terminal lists grouped by connection type: <dl> <dt>array[] `a_terminal_elevate`</dt> <dd> Elevate-model terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\StripeCom\Terminal\StripeReaderModelSid} constants. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `s_serial_number`</dt> <dd>Terminal serial number.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> <dt>array[] `a_terminal_web`</dt> <dd> Ethernet terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\StripeCom\Terminal\StripeReaderModelSid} constants. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd> Terminal type. One of {@link \Thoth\PayProcessor\StripeCom\Terminal\StripeTerminalTypeSid} constants. </dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `s_serial_number`</dt> <dd>Terminal serial number.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> </dl> </dd> <dt>string `s_location_id`</dt> <dd>Stripe location ID assigned by the gateway.</dd> <dt>string `text_location_name`</dt> <dd>Display name of the location.</dd> </dl> </dd> <dt>bool `has_expire_date`</dt> <dd>Whether the card expiry date entry is required.</dd> <dt>bool `has_readers_selection`</dt> <dd>Whether the card reader selection UI is shown.</dd> <dt>bool `is_support_magtek`</dt> <dd>Whether `MagTek` USB reader is supported.</dd> </dl> Nmi: <dl> <dt>int `id_device`</dt> <dd>Device type identifier. One of {@link \Thoth\WlPay\Cordova\CordovaCcrDeviceSid} constants.</dd> <dt>string `s_key`</dt> <dd>NMI SDK key for the card reader plugin.</dd> </dl> DirectConnect: <dl> <dt>array[] `a_terminal_location`</dt> <dd> Terminal configuration keyed by location key. <i>Primary key in {@link \RsLocationSql} table.</i> Empty array if no terminals are configured. Each value: <dl> <dt>array[] `a_terminal`</dt> <dd> Terminal lists grouped by connection type: <dl> <dt>array[] `a_terminal_elevate`</dt> <dd> Elevate-model terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\DirectConnect\Terminal\DirectConnectReaderModelSid} constants. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `s_serial_number`</dt> <dd>Terminal serial number.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> <dt>array[] `a_terminal_web`</dt> <dd> Ethernet and USB terminals. Always includes one synthetic `MagTek` USB entry appended last. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int|null `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\DirectConnect\Terminal\DirectConnectReaderModelSid} constants. `null` for the synthetic `MagTek` USB entry. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd>Terminal type. One of {@link \Thoth\PayProcessor\Terminal\TerminalTypeSid} constants.</dd> <dt>string|null `k_terminal`</dt> <dd> Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i> `null` for the synthetic `MagTek` USB entry. </dd> <dt>string|null `s_serial_number`</dt> <dd>Terminal serial number. `null` for the synthetic `MagTek` USB entry.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway, or `id_type_N` for the synthetic `MagTek` USB entry.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> </dl> </dd> <dt>string `text_location_name`</dt> <dd>Display name of the location.</dd> </dl> </dd> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether card swipe can be cancelled from the POS terminal.</dd> <dt>bool `has_expire_date`</dt> <dd>Whether the card expiry date entry is required.</dd> <dt>bool `has_readers_selection`</dt> <dd>Whether the card reader selection UI is shown.</dd> <dt>int `id_device`</dt> <dd>Device type identifier. One of {@link \Thoth\WlPay\Cordova\CordovaCcrDeviceSid} constants.</dd> <dt>bool `is_support_magtek`</dt> <dd>Whether `MagTek` USB reader is supported.</dd> </dl> Nuvei: <dl> <dt>array `a_terminal_location`</dt> <dd> Terminal configuration keyed by location key. <i>Primary key in {@link \RsLocationSql} table.</i> Present only when at least one location has an active merchant configured. Each value: <dl> <dt>array `a_terminal`</dt> <dd> Terminal lists grouped by connection type. Both lists contain identical entries - the same terminal appears in both. <dl> <dt>array[] `a_terminal_elevate`</dt> <dd> Elevate-model terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd>Terminal model. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants.</dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd>Terminal type. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants.</dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> <dt>array[] `a_terminal_web`</dt> <dd> Ethernet terminals, plus one synthetic `MagTek` USB entry appended last when {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid::supportsMagTekUsb()} returns `true`. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int|null `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants. `null` for the synthetic `MagTek` USB entry. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd> Terminal type. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants, or {@link \Thoth\PayProcessor\Terminal\TerminalTypeSid::MAGTEK_USB} for the synthetic `MagTek` USB entry. </dd> <dt>string|null `k_terminal`</dt> <dd> Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i> `null` for the synthetic `MagTek` USB entry. </dd> <dt>string|null `s_serial_number`</dt> <dd>Always `null`. Present only in the synthetic `MagTek` USB entry.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway, or `id_type_N` for the synthetic `MagTek` USB entry.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> </dl> </dd> <dt>string `text_location_name`</dt> <dd>Display name of the location.</dd> </dl> </dd> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether card swipe can be cancelled from the POS terminal.</dd> <dt>bool `has_expire_date`</dt> <dd>Whether the card expiry date entry is required.</dd> <dt>bool `has_readers_selection`</dt> <dd>Whether the card reader selection UI is shown.</dd> <dt>bool `is_support_magtek`</dt> <dd>Whether `MagTek` USB reader is supported.</dd> </dl>'
                      a_pay_processor:
                        type: [array, "null"]
                        description: 'Represents information about payment processors. Keys are payment methods IDs, one of {@link \RsPayMethodSid} constants. Value is the following array: `null` if no processors are set up. <dl> <dt>array|null `a_public_keys`</dt> <dd> Public keys configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicKeysInterface::publicKeys()}.</i> `null` if this payment processor does not support public keys. <dl> <dt>string|null `s_checkout`</dt> <dd>Checkout SDK public key. `null` if 3DS is disabled for this merchant.</dd> </dl> </dd> <dt>array|null `a_public_info`</dt> <dd> Public info configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicInfoInterface::publicInfo()}.</i> `null` if this payment processor does not support public info. <dl> <dt>string `s_merchant_site`</dt> <dd>Merchant site identifier assigned by Nuvei.</dd> <dt>string `s_nuvei_id`</dt> <dd>Nuvei merchant identifier.</dd> </dl> </dd> <dt>bool `hide_save_source`</dt> <dd>Whether `save payment method` option should be hidden. `true` if hidden, `false` - otherwise.</dd> <dt>int `id_pay_processor`</dt> <dd>ID of the payment processor. One of {@link \Thoth\PayProcessor\PayProcessorSid} constants.</dd> <dt>null|bool `is_enabled_3ds`</dt> <dd> `true` if 3DS should be performed, `false` if 3DS should not be performed. `null` if this is not defined for payment processor. Only for {@link \Thoth\PayProcessor\PayProcessorSid::NUVEI} </dd> <dt>bool|null `is_test`</dt> <dd> `true` if the merchant is in a test mode, `false` otherwise. `null` if this is not defined for payment processor. </dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant. <i>Primary key in {@link \RsBusinessMerchantSql}.</i></dd> </dl>'
                      dl_now:
                        type: string
                        description: 'Current local date in current location {@link EnvironmentApi::$k_location} or business {@link EnvironmentApi::$k_business} if not set location.'
                      f_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      f_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      id_locale:
                        type: [number, "null"]
                        description: 'The locale ID of the business.'
                      is_save_optional:
                        type: boolean
                        description: '`true` if clients can choose whether their banking and credit card information is saved at checkout, `false` if this information is always saved.'
                      is_save_source:
                        type: boolean
                        description: 'Determines whether newly added payment sources should be saved. This will be `true` if payment sources should be saved, `false` if otherwise.'
                      is_tip:
                        type: boolean
                        description: 'Whether tips are accepted.'
                      m_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
                      m_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
      tags:
        - Form-Environment
  '/Thoth/WlPay/Form/EnvironmentUser.json':
    get:
      summary: 'Returns information about payment environment. Called before rendering a payment form to determine which payment methods and card types are available for a given business and location, what surcharges apply, and how the form should behave (tip prompt, optional card save). This endpoint is deprecated; use {@link \Thoth\WlPay\Form\EnvironmentUserApi} for new integrations.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to retrieve payment information for. This will be `0` if not set yet. An empty string or `null` if payments are performed with the WellnessLiving system merchant.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'The key of the currency to retrieve payment information for.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to retrieve payment information for. This will be `0` if not set yet.'
          schema:
            type: string
        - name: uid_owner
          in: query
          description: 'The user ID to retrieve payment information for. `null` if not defined.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_card_system:
                        type: array
                        description: 'A list of supported bank card systems. Keys refer to card types based on card numbers (see {@link \ACardSystemSid}), and values are IDs of card systems (one of {@link \ACardSystemSid} constants).'
                      a_method_staff:
                        type: array
                        description: 'A list of payment methods enabled for staff members. The ID is one of {@link \RsPayMethodSid} constants. The value is always `true`.'
                      a_method_support:
                        type: array
                        description: 'A list of all payment methods that can be used within this business. This array is sorted in the order in which payment methods should be shown to the user. Each element of the array has the following structure: <dl> <dt>int `id_pay_method`</dt> <dd>The ID of type of payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_client`</dt> <dd> Determines whether this method is available for clients. This field is only returned for custom payment methods. </dd> <dt>string|null `k_pay_method`</dt> <dd> The key of the custom payment method. This will be `null` if this payment method isn''t customized. <i>Primary key in {@link \Thoth\WlPay\Method\MethodSql} table.</i> </dd> <dt>string `s_method`</dt> <dd>The name of payment method. This field is only returned for custom payment methods.</dd> </dl>'
                      a_mobile_config:
                        type: [array, "null"]
                        description: 'The configuration array that''s sent to mobile card reader plugin. The structure of this array depends on the payment processor being used. `null` when mobile card readers are not supported, or when actor has no access to them. StripeCom: <dl> <dt>array `a_terminal_location`</dt> <dd> Terminal configuration keyed by location key. <i>Primary key in {@link \RsLocationSql} table.</i> Empty array if no terminals are configured. Each value: <dl> <dt>array `a_terminal`</dt> <dd> Terminal lists grouped by connection type: <dl> <dt>array[] `a_terminal_elevate`</dt> <dd> Elevate-model terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\StripeCom\Terminal\StripeReaderModelSid} constants. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `s_serial_number`</dt> <dd>Terminal serial number.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> <dt>array[] `a_terminal_web`</dt> <dd> Ethernet terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\StripeCom\Terminal\StripeReaderModelSid} constants. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd> Terminal type. One of {@link \Thoth\PayProcessor\StripeCom\Terminal\StripeTerminalTypeSid} constants. </dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `s_serial_number`</dt> <dd>Terminal serial number.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> </dl> </dd> <dt>string `s_location_id`</dt> <dd>Stripe location ID assigned by the gateway.</dd> <dt>string `text_location_name`</dt> <dd>Display name of the location.</dd> </dl> </dd> <dt>bool `has_expire_date`</dt> <dd>Whether the card expiry date entry is required.</dd> <dt>bool `has_readers_selection`</dt> <dd>Whether the card reader selection UI is shown.</dd> <dt>bool `is_support_magtek`</dt> <dd>Whether `MagTek` USB reader is supported.</dd> </dl> Nmi: <dl> <dt>int `id_device`</dt> <dd>Device type identifier. One of {@link \Thoth\WlPay\Cordova\CordovaCcrDeviceSid} constants.</dd> <dt>string `s_key`</dt> <dd>NMI SDK key for the card reader plugin.</dd> </dl> DirectConnect: <dl> <dt>array[] `a_terminal_location`</dt> <dd> Terminal configuration keyed by location key. <i>Primary key in {@link \RsLocationSql} table.</i> Empty array if no terminals are configured. Each value: <dl> <dt>array[] `a_terminal`</dt> <dd> Terminal lists grouped by connection type: <dl> <dt>array[] `a_terminal_elevate`</dt> <dd> Elevate-model terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\DirectConnect\Terminal\DirectConnectReaderModelSid} constants. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `s_serial_number`</dt> <dd>Terminal serial number.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> <dt>array[] `a_terminal_web`</dt> <dd> Ethernet and USB terminals. Always includes one synthetic `MagTek` USB entry appended last. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int|null `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\DirectConnect\Terminal\DirectConnectReaderModelSid} constants. `null` for the synthetic `MagTek` USB entry. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd>Terminal type. One of {@link \Thoth\PayProcessor\Terminal\TerminalTypeSid} constants.</dd> <dt>string|null `k_terminal`</dt> <dd> Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i> `null` for the synthetic `MagTek` USB entry. </dd> <dt>string|null `s_serial_number`</dt> <dd>Terminal serial number. `null` for the synthetic `MagTek` USB entry.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway, or `id_type_N` for the synthetic `MagTek` USB entry.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> </dl> </dd> <dt>string `text_location_name`</dt> <dd>Display name of the location.</dd> </dl> </dd> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether card swipe can be cancelled from the POS terminal.</dd> <dt>bool `has_expire_date`</dt> <dd>Whether the card expiry date entry is required.</dd> <dt>bool `has_readers_selection`</dt> <dd>Whether the card reader selection UI is shown.</dd> <dt>int `id_device`</dt> <dd>Device type identifier. One of {@link \Thoth\WlPay\Cordova\CordovaCcrDeviceSid} constants.</dd> <dt>bool `is_support_magtek`</dt> <dd>Whether `MagTek` USB reader is supported.</dd> </dl> Nuvei: <dl> <dt>array `a_terminal_location`</dt> <dd> Terminal configuration keyed by location key. <i>Primary key in {@link \RsLocationSql} table.</i> Present only when at least one location has an active merchant configured. Each value: <dl> <dt>array `a_terminal`</dt> <dd> Terminal lists grouped by connection type. Both lists contain identical entries - the same terminal appears in both. <dl> <dt>array[] `a_terminal_elevate`</dt> <dd> Elevate-model terminals. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int `id_model`</dt> <dd>Terminal model. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants.</dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd>Terminal type. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants.</dd> <dt>string `k_terminal`</dt> <dd>Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i></dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> <dt>array[] `a_terminal_web`</dt> <dd> Ethernet terminals, plus one synthetic `MagTek` USB entry appended last when {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid::supportsMagTekUsb()} returns `true`. Each element: <dl> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether swipe can be cancelled from the POS terminal.</dd> <dt>int|null `id_model`</dt> <dd> Terminal model. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants. `null` for the synthetic `MagTek` USB entry. </dd> <dt>int `id_status`</dt> <dd>Terminal status. One of {@link \Thoth\PayProcessor\Terminal\TerminalStatusSid} constants.</dd> <dt>int `id_type`</dt> <dd> Terminal type. One of {@link \Thoth\PayProcessor\Nuvei\Terminal\NuveiTerminalTypeSid} constants, or {@link \Thoth\PayProcessor\Terminal\TerminalTypeSid::MAGTEK_USB} for the synthetic `MagTek` USB entry. </dd> <dt>string|null `k_terminal`</dt> <dd> Terminal key. <i>Primary key in {@link \Thoth\PayProcessor\Terminal\TerminalSql}.</i> `null` for the synthetic `MagTek` USB entry. </dd> <dt>string|null `s_serial_number`</dt> <dd>Always `null`. Present only in the synthetic `MagTek` USB entry.</dd> <dt>string `text_id`</dt> <dd>Terminal ID assigned by the gateway, or `id_type_N` for the synthetic `MagTek` USB entry.</dd> <dt>string `text_name`</dt> <dd>Human-readable terminal label.</dd> </dl> </dd> </dl> </dd> <dt>string `text_location_name`</dt> <dd>Display name of the location.</dd> </dl> </dd> <dt>bool `can_cancel_swipe_from_pos`</dt> <dd>Whether card swipe can be cancelled from the POS terminal.</dd> <dt>bool `has_expire_date`</dt> <dd>Whether the card expiry date entry is required.</dd> <dt>bool `has_readers_selection`</dt> <dd>Whether the card reader selection UI is shown.</dd> <dt>bool `is_support_magtek`</dt> <dd>Whether `MagTek` USB reader is supported.</dd> </dl>'
                      a_pay_processor:
                        type: [array, "null"]
                        description: 'Represents information about payment processors. Keys are payment methods IDs, one of {@link \RsPayMethodSid} constants. Value is the following array: `null` if no processors are set up. <dl> <dt>array|null `a_public_keys`</dt> <dd> Public keys configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicKeysInterface::publicKeys()}.</i> `null` if this payment processor does not support public keys. <dl> <dt>string|null `s_checkout`</dt> <dd>Checkout SDK public key. `null` if 3DS is disabled for this merchant.</dd> </dl> </dd> <dt>array|null `a_public_info`</dt> <dd> Public info configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicInfoInterface::publicInfo()}.</i> `null` if this payment processor does not support public info. <dl> <dt>string `s_merchant_site`</dt> <dd>Merchant site identifier assigned by Nuvei.</dd> <dt>string `s_nuvei_id`</dt> <dd>Nuvei merchant identifier.</dd> </dl> </dd> <dt>bool `hide_save_source`</dt> <dd>Whether `save payment method` option should be hidden. `true` if hidden, `false` - otherwise.</dd> <dt>int `id_pay_processor`</dt> <dd>ID of the payment processor. One of {@link \Thoth\PayProcessor\PayProcessorSid} constants.</dd> <dt>null|bool `is_enabled_3ds`</dt> <dd> `true` if 3DS should be performed, `false` if 3DS should not be performed. `null` if this is not defined for payment processor. Only for {@link \Thoth\PayProcessor\PayProcessorSid::NUVEI} </dd> <dt>bool|null `is_test`</dt> <dd> `true` if the merchant is in a test mode, `false` otherwise. `null` if this is not defined for payment processor. </dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant. <i>Primary key in {@link \RsBusinessMerchantSql}.</i></dd> </dl>'
                      dl_now:
                        type: string
                        description: 'Current local date in current location {@link EnvironmentApi::$k_location} or business {@link EnvironmentApi::$k_business} if not set location.'
                      f_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      f_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      id_locale:
                        type: [number, "null"]
                        description: 'The locale ID of the business.'
                      is_save_optional:
                        type: boolean
                        description: '`true` if clients can choose whether their banking and credit card information is saved at checkout, `false` if this information is always saved.'
                      is_save_source:
                        type: boolean
                        description: 'Determines whether newly added payment sources should be saved. This will be `true` if payment sources should be saved, `false` if otherwise.'
                      is_tip:
                        type: boolean
                        description: 'Whether tips are accepted.'
                      m_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
                      m_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
      tags:
        - Form-EnvironmentUser
  '/Thoth/WlPay/Owner/Owner.json':
    get:
      summary: 'Returns information about payment owner. Must be called before initiating any payment on behalf of a user, to determine the correct payment owner keys to pass to the payment form. Also indicates whether a family-account relationship exists (for example, a parent paying for a child), which affects how the payment form is pre-populated.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` if not passed.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_pay_owner:
                        type: number
                        format: int32
                        description: 'The type of user for which transactions can be made (this property is optional). This is one of the {@link \RsPayOwnerSid} constants.'
                      is_pay_self_only:
                        type: boolean
                        description: 'Is client pay only for self. If parent pays for child this flag will be `false` for both. `true` if client pay only for self, `false` - otherwise.'
                      k_pay_owner:
                        type: string
                        description: 'The payment owner key. This is used for financial transactions.'
                      k_pay_owner_money:
                        type: string
                        description: 'Key of the money owner. <i>Copy of result of {@link PayOwner::ownerMoney()}.</i>'
      tags:
        - Owner-Owner
  '/Thoth/WlPay/Method/List.json':
    get:
      summary: 'Returns list of active payment methods data. Returns the system-level payment methods enabled for the business combined with any custom payment methods configured for the business and accessible to the given user based on their role. When `$is_active` is `false`, inactive custom methods are included as well.'
      parameters:
        - name: is_active
          in: query
          description: 'Whether only active payment methods should be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: show_manual
          in: query
          description: 'Whether payment method {@link \RsPayMethodSid::ACCOUNT_MANUAL} should be included in response.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_method:
                        type: array
                        description: 'A list of payment methods: <dl> <dt>int `id_pay_method`</dt> <dd>The payment method type ID. One of {@link \RsPayMethodSid} constants.</dd> <dt>string|null `k_pay_method`</dt> <dd> Payment method key. <i>Primary key int the {@link \Thoth\WlPay\Method\MethodSql} table.</i> May be <tt>null</tt> if it is a system payment method. </dd> <dt>string `s_title`</dt> <dd>The payment method name.</dd> </dl>'
      tags:
        - Method-List
  '/Thoth/WlPay/Address/Address.json':
    get:
      summary: 'Gets user''s payment addresses information. Returns the list of saved payment addresses for the specified owner type and key, including address fields, country and region details, and the currently selected address. Returns `null` if the current user cannot edit bank accounts for the owner.'
      parameters:
        - name: id_pay_owner
          in: query
          description: 'The ID of the payment owner type. One of {@link \RsPayOwnerSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key, where the payment is performed. <tt>null</tt> if pay owner is not user.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The primary key of a payment owner. This could be the business of the user depending on a {@link \Thoth\WlPay\Address\AddressApi::$id_pay_owner} value.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_address:
                        type: [array, "null"]
                        description: 'The payee''s address information. A list of payment addresses belonging to the owner. Each element: `null` if the current user has no access to the required information, or cannot edit the bank account. <dl> <dt>bool `is_selected`</dt> <dd>`true` if this address is currently selected, `false` otherwise.</dd> <dt>string|null `k_country`</dt> <dd>Country geo key. `null` if the country cannot be determined.</dd> <dt>string `k_pay_address`</dt> <dd>Payment address key. Primary key in the `rs_pay_address` table.</dd> <dt>string `k_region`</dt> <dd>Region geo key.</dd> <dt>string `s_city`</dt> <dd>City name.</dd> <dt>string|null `s_country`</dt> <dd>Country name. `null` if the country cannot be determined.</dd> <dt>string `s_country_code`</dt> <dd>Country abbreviation code, or an empty string if unavailable.</dd> <dt>string `s_name`</dt> <dd>Full name of the address owner.</dd> <dt>string `s_phone`</dt> <dd>Phone number.</dd> <dt>string `s_postal`</dt> <dd>Postal code.</dd> <dt>string|null `s_region`</dt> <dd>Region name.</dd> <dt>string|null `s_region_code`</dt> <dd>Region abbreviation code. `null` if the region cannot be determined.</dd> <dt>string `s_street1`</dt> <dd>First line of the street address.</dd> <dt>string `s_street2`</dt> <dd>Second line of the street address.</dd> <dt>string|null `text_mail`</dt> <dd>Email address of the owner. `null` if the owner is a business.</dd> </dl>'
      tags:
        - Address-Address
  '/Thoth/WlPay/Address/Profile.json':
    get:
      summary: 'Returns default payment address data that is retrieved from user profile. Loads the user''s profile for the given business and returns address fields (name, phone, street, city, postal code, country, region) to pre-populate a payment address form.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose data should be retrieved.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_geo_country:
                        type: [string, "null"]
                        description: 'The country key. This will be `null` if not set yet.'
                      k_geo_region:
                        type: [string, "null"]
                        description: 'The region ID. This will be `null` if not set yet.'
                      s_city:
                        type: [string, "null"]
                        description: 'The name of the city in the address. This will be `null` if not set yet.'
                      s_name:
                        type: [string, "null"]
                        description: 'The client name as listed in their address. This will be `null` if not set yet.'
                      s_phone:
                        type: [string, "null"]
                        description: 'The client phone number. This will be `null` if not set yet.'
                      s_postal:
                        type: [string, "null"]
                        description: 'The client postal or zip code. This will be `null` if not set yet.'
                      s_street1:
                        type: [string, "null"]
                        description: 'The first line of the client address. This will be `null` if not set yet.'
                      s_street2:
                        type: [string, "null"]
                        description: 'The second line of the client address. This will be `null` if not set yet.'
      tags:
        - Address-Profile
  '/Thoth/DriveMs/Api/DriveRegisterVariants.json':
    get:
      summary: 'Registers file variants for the given link (deprecated GET compatibility endpoint). Reads variant and link values from the raw GET parameters and delegates to the POST method. This endpoint exists only for backward compatibility; use {@link DriveRegisterVariantsApi::post()} instead. It is not possible to test it normally because model does not support sending necessary data via get method.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveRegisterVariants
    post:
      summary: 'Registers file variants for the given link. Accepts a drive link and a list of variant file paths keyed by descriptor (either as an array or as a JSON-encoded string), and associates the given variants with the specified link in the drive storage.'
      parameters:
        - name: s_link
          in: query
          description: 'Link to the file.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_variant:
                  type: array
                  description: 'List of variant file paths with key as descriptor.'
                json_variant:
                  type: string
                  description: 'List of variant file paths with key as descriptor in JSON format. This parameter allows passing a large number of files to the controller without exceeding the maximum POST request variable limit. The files should be provided as a JSON-encoded array of strings. It is preferable to use this parameter instead of {@link DriveRegisterVariantsApi::$a_variant}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveRegisterVariants
  '/Thoth/DriveMs/Api/DriveSave74.json':
    post:
      summary: 'Saves the uploaded file content to the specified link. Accepts the raw file content (or an S3 multipart key when `is_multipart` is set in {@link DriveSaveApi::$a_param}), a drive link, and an optional file name. Writes the content to the drive storage and returns the saved file metadata.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_file_content:
                  type: [object, "null"]
                  description: 'The uploaded file data from the HTTP request. `null` if a multipart S3 upload is used instead.'
                a_param:
                  type: array
                  description: 'A set of parameters. If a file is uploaded using an S3 bucket, the array must contain the key `is_multipart`. <dl> <dt>int `i_height_max`</dt> <dd> Checks if image''s height is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_height_min`</dt> <dd> Checks if image''s height is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_max`</dt> <dd> Checks if image''s width is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_min`</dt> <dd> Checks if image''s width is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>bool `is_load_image_info`</dt> <dd>Whether to load information about the image. Optional.</dd> <dt>bool `is_private`</dt> <dd>Whether the file is private. Optional.</dd> <dt>bool `require-image`</dt> <dd>Whether to throw an exception if the uploaded file is not an image. Optional.</dd> <dt>int `timeout`</dt> <dd>Remove the file after the specified number of seconds. `0` for persistent files. Optional.</dd> </dl>'
                s_file_key:
                  type: [string, "null"]
                  description: 'Key of the object in S3. Used when `is_multipart` is set in {@link DriveSave74Api::$a_param}. `null` if not a multipart upload.'
                s_link:
                  type: string
                  description: 'Link to the file.'
                s_name:
                  type: [string, "null"]
                  description: 'File name with extension. `null` if a file name is not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_file:
                        type: array
                        description: 'Information about saved a file. <dl> <dt>bool `is_new`</dt> <dd>`true` if a new file was saved, otherwise `false`.</dd> <dt>bool `is_private_file`</dt> <dd>`true` if the file is private, otherwise `false`.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
      tags:
        - Api-DriveSave74
  '/Thoth/DriveMs/Api/DriveCopy.json':
    get:
      summary: 'Copies a file from the source link to the destination link. Accepts the source and destination drive links and optional copy parameters, validates their lengths, and copies the physical file referenced by the source link to the destination link in the drive storage.'
      parameters:
        - name: a_param
          in: query
          description: 'Additional parameters for the copy operation. <dl> <dt>int `i_height_max`</dt> <dd> Checks if image''s height is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_height_min`</dt> <dd> Checks if image''s height is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_max`</dt> <dd> Checks if image''s width is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_min`</dt> <dd> Checks if image''s width is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>bool `is_load_image_info`</dt> <dd>Whether to load information about the image. Optional.</dd> <dt>bool `is_private`</dt> <dd>Whether the file is private. Optional.</dd> <dt>bool `require-image`</dt> <dd>Whether to throw an exception if the uploaded file is not an image. Optional.</dd> <dt>int `timeout`</dt> <dd>Remove the file after the specified number of seconds. `0` for persistent files. Optional.</dd> </dl>'
          schema:
            type: array
        - name: s_link_destination
          in: query
          description: 'Link to the destination file.'
          schema:
            type: string
        - name: s_link_source
          in: query
          description: 'Link to the source file.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveCopy
  '/Thoth/DriveMs/Api/DriveCopyInstance.json':
    post:
      summary: 'Copies all files from the source instance to the destination instance. Used during server provisioning or data migration to replicate the entire drive file set from one instance to another, for example when cloning a staging environment or moving a tenant to a new server. All file records belonging to the source instance are duplicated under the destination.'
      parameters:
        - name: s_instance_destination
          in: query
          description: 'Instance ID from which links must be copied.'
          schema:
            type: string
        - name: s_instance_source
          in: query
          description: 'Instance ID from which links must be copied.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveCopyInstance
  '/Thoth/DriveMs/Api/DriveDelete.json':
    get:
      summary: 'Deletes the file identified by the given link. Accepts a drive link, validates its length, and removes the corresponding file record from the drive database, making the file unavailable for download.'
      parameters:
        - name: s_link
          in: query
          description: 'Link to the file.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveDelete
  '/Thoth/DriveMs/Api/DriveDownload.json':
    get:
      summary: 'Downloads the contents of the file identified by the given link. Accepts a drive link, retrieves the raw file content from storage, and returns it as a base64-encoded gzip-compressed string. Returns `false` if the file is not found.'
      parameters:
        - name: s_link
          in: query
          description: 'Link to the file.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_content:
                        type: [string, boolean]
                        description: 'The contents of a specified file. Encoded in base64 and compressed with gzip. `false` if a file is not found, otherwise - content file.'
      tags:
        - Api-DriveDownload
  '/Thoth/DriveMs/Api/DriveFile.json':
    post:
      summary: 'Returns file information for each of the given links. Batch lookup for file metadata by drive link. Used when a page or component needs to display details (such as name, dimensions, or type) for several files at once. The JSON input variant exists to avoid hitting POST body size limits when the list is large.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_link:
                  type: array
                  description: 'List of links to the files. It is preferable to use {@link DriveFileApi::$json_list} parameter instead of this.'
                is_cache:
                  type: boolean
                  description: 'Whether use cache. `true` to use cache, `false` otherwise.'
                json_list:
                  type: string
                  description: 'Link list to the file as JSON string. This parameter allows passing a large number of links to the controller without exceeding the maximum POST request size. The links should be provided as a JSON-encoded array of strings. It is preferable to use this parameter instead of {@link DriveFileApi::$a_link}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_file:
                        type: array
                        description: 'List of information about files. <dl> <dt>int `i_height`</dt> <dd>Height of image. Returns if file is image.</dd> <dt>int `i_width`</dt> <dd>Width of image. Returns if file is image.</dd> <dt>int|null `id_type`</dt> <dd>Image type ID. One of {@link \Core\Drive\DriveTypeSid} constants. `null` if file not image.</dd> <dt>bool `is_private`</dt> <dd>Whether link is private.</dd> <dt>bool `is_private_file`</dt> <dd>Whether file is private.</dd> <dt>string `s_link`</dt> <dd>File link.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
      tags:
        - Api-DriveFile
  '/Thoth/DriveMs/Api/DriveGenericInfo.json':
    post:
      summary: 'Returns generic metadata for each of the given file links. Low-level metadata fetch used by the drive client library before performing operations that depend on file attributes such as privacy flag, dimensions, or SHA1 hash. Results are returned as serialized rows to keep the payload compact. Use {@link DriveFileApi} for structured file info instead.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_link:
                  type: array
                  description: 'Link list to the file. This parameter was originally added, but it turned out that a large number of links could be sent to this controller, which led to exceeding the maximum POST request size. Therefore, the {@link DriveGenericInfoApi::$json_list} parameter was introduced, which allows passing the list of links in JSON format. The current parameter is kept for backward compatibility. The client library that sends requests to this controller has been updated and now uses only {@link DriveGenericInfoApi::$json_list}.'
                json_list:
                  type: string
                  description: 'Link list to the file as JSON string. This parameter allows passing a large number of links to the controller without exceeding the maximum POST request size. The links should be provided as a JSON-encoded array of strings. It is preferable to use this parameter instead of {@link DriveGenericInfoApi::$a_link}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: array
                        description: 'Information about files. Every element of array is string in base64.'
      tags:
        - Api-DriveGenericInfo
  '/Thoth/DriveMs/Api/DriveSave.json':
    post:
      summary: 'Saves the uploaded file content to the specified link. Bridges the legacy {@link DriveSaveApi::$s_content} field into the fields expected by the parent implementation: writes the raw content to a temporary file and sets {@link DriveSave74Api::$a_file_content} for regular uploads, or copies the value directly into {@link DriveSave74Api::$s_file_key} for multipart uploads.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_file_content:
                  type: [object, "null"]
                  description: 'The uploaded file data from the HTTP request. `null` if a multipart S3 upload is used instead.'
                a_param:
                  type: array
                  description: 'A set of parameters. If a file is uploaded using an S3 bucket, the array must contain the key `is_multipart`. <dl> <dt>int `i_height_max`</dt> <dd> Checks if image''s height is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_height_min`</dt> <dd> Checks if image''s height is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_max`</dt> <dd> Checks if image''s width is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_min`</dt> <dd> Checks if image''s width is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>bool `is_load_image_info`</dt> <dd>Whether to load information about the image. Optional.</dd> <dt>bool `is_private`</dt> <dd>Whether the file is private. Optional.</dd> <dt>bool `require-image`</dt> <dd>Whether to throw an exception if the uploaded file is not an image. Optional.</dd> <dt>int `timeout`</dt> <dd>Remove the file after the specified number of seconds. `0` for persistent files. Optional.</dd> </dl>'
                s_content:
                  type: string
                  description: 'The contents of a specified file if a regular file is saved. Or this is the key of the object in S3, if the key `is_multipart` exists in the array of parameters {@link DriveSaveApi::$a_param}. or {@link DriveSave74Api::$s_file_key} for multipart uploads instead.'
                s_file_key:
                  type: [string, "null"]
                  description: 'Key of the object in S3. Used when `is_multipart` is set in {@link DriveSave74Api::$a_param}. `null` if not a multipart upload.'
                s_link:
                  type: string
                  description: 'Link to the file.'
                s_name:
                  type: [string, "null"]
                  description: 'File name with extension. `null` if a file name is not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_file:
                        type: array
                        description: 'Information about saved a file. <dl> <dt>bool `is_new`</dt> <dd>`true` if a new file was saved, otherwise `false`.</dd> <dt>bool `is_private_file`</dt> <dd>`true` if the file is private, otherwise `false`.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
      tags:
        - Api-DriveSave
  '/Thoth/DriveMs/Api/DriveUrlSign.json':
    get:
      summary: 'Returns a pre-signed URL to download the specified private file. Accepts file metadata (including the file name and privacy flag) and an optional download name, and generates a time-limited signed URL that grants temporary access to a private file in the drive storage.'
      parameters:
        - name: a_file
          in: query
          description: 'File data. <dl> <dt>int `i_height`</dt> <dd>Height of the image. Only present if the file is an image.</dd> <dt>int `i_width`</dt> <dd>Width of the image. Only present if the file is an image.</dd> <dt>int `id_type`</dt> <dd>Image type code. Only present if the file is an image. One of {@link \Core\Drive\DriveTypeSid} constants.</dd> <dt>bool `is_private`</dt> <dd>Whether the link is private.</dd> <dt>bool `is_private_file`</dt> <dd>Whether the file is private.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
          schema:
            type: array
        - name: s_name
          in: query
          description: 'Name of the file to set on download. `null` if file name not specified.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url:
                        type: string
                        description: 'A pre-signed URL to download a file with restricted access rights.'
      tags:
        - Api-DriveUrlSign
  '/Studio/Task/Branch/Branch.json':
    delete:
      summary: 'Changes a status of the branch for the specified task in the database and runs build for deleting a branch. Sets branch status to deleting and starts the `BranchDelete` build on the deployment instance. Returns the build log URL on success.'
      parameters:
        - name: id_build_mode
          in: query
          description: 'Mode of the <tt>BranchDelete</tt> build.'
          schema:
            type: number
            format: int32
        - name: k_deploy_instance
          in: query
          description: 'Deployment instance key which creates/deletes a branch for the specified task.'
          schema:
            type: string
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_build:
                        type: string
                        description: 'Url to build page. Empty string means that build is not started.'
      tags:
        - Branch-Branch
    get:
      summary: 'Returns branch status and data for a task. Retrieves `id_branch_status`, `id_stand_status`, and additional branch data for the specified task.'
      parameters:
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_branch_data:
                        type: [array, string, "null"]
                        description: 'Addition information about branch and related entities. Has an arbitrary structure. `string` if it needs to set an empty value. `null` will not change.'
                      id_branch_status:
                        type: [number, "null"]
                        description: 'Branch status, one of {@link BranchStatusSid}.'
                      id_stand_status:
                        type: [number, "null"]
                        description: 'Stand status, one of {@link StandStatusSid}. `null` if not initialized and will be ignored.'
                      k_task:
                        type: string
                        description: 'Task key within which a branch should be created/deleted.'
      tags:
        - Branch-Branch
    post:
      summary: 'Registers a new branch for the specified task in the database and runs a build for creating a branch. Validates the task, deployment instance, and microservice, then creates a branch record and starts the `BranchCreate` build. Returns the build log URL on success.'
      parameters:
        - name: k_deploy_instance
          in: query
          description: 'Deployment instance key which creates/deletes a branch for the specified task.'
          schema:
            type: string
        - name: k_microservice
          in: query
          description: 'A microservice key.'
          schema:
            type: string
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
        - name: s_environment
          in: query
          description: 'A microservice environment for create branch.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_branch_data:
                  type: [array, string, "null"]
                  description: 'Addition information about branch and related entities. Has an arbitrary structure. `string` if it needs to set an empty value. `null` will not change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_build:
                        type: string
                        description: 'Url to build page. Empty string means that build is not started.'
      tags:
        - Branch-Branch
    put:
      summary: 'Updates branch data for a task. Saves additional branch data (`a_branch_data`) for the branch of the specified task.'
      parameters:
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_branch_data:
                  type: [array, string, "null"]
                  description: 'Addition information about branch and related entities. Has an arbitrary structure. `string` if it needs to set an empty value. `null` will not change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-Branch
  '/Studio/Task/Branch/BranchBuild.json':
    delete:
      summary: 'Deletes a branch from the database. <tt>BranchDelete</tt> build calls at the end of the executing.'
      parameters:
        - name: s_task_link
          in: query
          description: 'Task link of the branch. <i>Copy of the {@link \StudioTaskSql}.<var>s_link</var> field.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchBuild
    post:
      summary: 'Sets build that creates a branch and information about paths into which catalogs are cloned. <tt>BranchCreate</tt> build calls at the start of the executing.'
      parameters:
        - name: a_path
          in: query
          description: 'Information about paths into which catalogs are cloned. Keys are aliases of the original repository catalogs. Values are paths into which original catalogs are cloned. Each element has the following structure: Set this property when a new branch is created. In other cases, an exception will be thrown. <dl> <dt>int `id_repository_server_type`</dt> <dd>Repository service type ID. One of {@link \StudioRepositoryServerTypeSid} constants.</dd> <dt>string `s_destination`</dt> <dd>Path into which original catalog is cloned/copied.</dd> <dt>string `s_repository`</dt> <dd>Repository name.</dd> <dt>string `s_source`</dt> <dd>Path of the original catalog.</dd> </dl>'
          schema:
            type: array
        - name: k_build
          in: query
          description: 'Build key that creates a branch for the specified task.'
          schema:
            type: string
        - name: s_task_link
          in: query
          description: 'Task link of the branch. <i>Copy of the {@link \StudioTaskSql}.<var>s_link</var> field.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchBuild
    put:
      summary: 'Changes branch status as {@link BranchStatusSid::READY}. <tt>BranchCreate</tt>> build calls at the end of the executing.'
      parameters:
        - name: k_build
          in: query
          description: 'Build key that creates a branch for the specified task.'
          schema:
            type: string
        - name: s_task_link
          in: query
          description: 'Task link of the branch. <i>Copy of the {@link \StudioTaskSql}.<var>s_link</var> field.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchBuild
  '/Studio/Task/Branch/BranchList.json':
    get:
      summary: 'Returns a list of branches for accessible projects. Lists all active branches in projects where the current user has the branch view privilege.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'List of branch. One element contains: <dl> <dt>string[] `a_path`</dt> <dd> Information about paths into which catalogs are cloned/copied. <i>The structure corresponds to the {@link \Studio\Task\Branch\BranchSql}.<var>z_path</var> field.</i> </dd> <dt>string `dtu_create`</dt> <dd>Date when branch was created.</dd> <dt>string `k_deploy_instance`</dt> <dd>Key of deployment instance. <i>Primary key in {@link \StudioDeployInstanceSql} table.</i></dd> <dt>string `k_task`</dt> <dd>Task key. <i>Primary key in {@link \StudioTaskSql} table.</i></dd> <dt>string `text_create`</dt> <dd>Formatted time interval when branch was created.</dd> <dt>string `text_fio`</dt> <dd>Task author name.</dd> <dt>string `text_title`</dt> <dd>Title of the task.</dd> <dt>string `url_task`</dt> <dd>URL of the task.</dd> </dl>'
      tags:
        - Branch-BranchList
  '/Studio/Ip/Bot/Access.json':
    delete:
      summary: 'Revokes bot access from a service. Removes the access record and schedules a service update to apply the change.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'Key of a bot which access should be changed.'
          schema:
            type: string
        - name: k_ip_service
          in: query
          description: 'Key of a service which access should be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Access
    put:
      summary: 'Grants bot access to a service. Inserts an access record and schedules a service update to apply the change.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'Key of a bot which access should be changed.'
          schema:
            type: string
        - name: k_ip_service
          in: query
          description: 'Key of a service which access should be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Access
  '/Studio/Ip/Bot/Edit.json':
    delete:
      summary: 'Deletes a bot. Removes the bot record and schedules service updates for all services the bot had access to.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Edit
    get:
      summary: 'Returns information about a bot. Returns the bot name, network, password, update rules, and active status.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_active:
                        type: boolean
                        description: 'Whether this bot is active.'
                      text_bot:
                        type: string
                        description: 'Name of the bot.'
                      text_network:
                        type: string
                        description: 'Network of the bot. Note that value sent here is ignored in a case {@link EditApi::$text_update} is not empty.'
                      text_password:
                        type: string
                        description: 'Update password.'
                      text_update:
                        type: string
                        description: 'Network update rules.'
      tags:
        - Bot-Edit
    post:
      summary: 'Saves a bot. Creates or updates a bot record. When update rules are provided, the network is derived from them.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Whether this bot is active.'
                text_bot:
                  type: string
                  description: 'Name of the bot.'
                text_network:
                  type: string
                  description: 'Network of the bot. Note that value sent here is ignored in a case {@link EditApi::$text_update} is not empty.'
                text_password:
                  type: string
                  description: 'Update password.'
                text_update:
                  type: string
                  description: 'Network update rules.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_ip_bot:
                        type: [string, "null"]
                        description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
      tags:
        - Bot-Edit
  '/Studio/Ip/Service/Edit.json':
    delete:
      summary: 'Deletes an IP service. Removes the service record from the database.'
      parameters:
        - name: k_ip_service
          in: query
          description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-Edit
    get:
      summary: 'Returns information about an IP service. Returns the service name, group, type, timeout, update rules, and auto-restriction flag.'
      parameters:
        - name: k_ip_service
          in: query
          description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_service_timeout:
                        type: number
                        format: int32
                        description: 'Timeout of the service. One of {@link \Studio\Ip\Service\ServiceTimeoutSid} constants.'
                      id_service_type:
                        type: number
                        format: int32
                        description: 'Type of the service. One of {@link \Studio\Ip\Service\ServiceTypeSid} constants.'
                      is_auto_restricted:
                        type: boolean
                        description: 'Indicates whether automatic session renewal for IP-based access is restricted. If set to `1`, access by IP is restricted and automatic session renewal is disabled if `0`, access by IP is allowed and session can be automatically renewed.'
                      text_group:
                        type: string
                        description: 'Group of services.'
                      text_title:
                        type: string
                        description: 'Name of the service.'
                      text_update:
                        type: string
                        description: 'Network update rules.'
      tags:
        - Service-Edit
    post:
      summary: 'Saves an IP service. Creates or updates a service record. When update rules are provided, the service IP list is updated.'
      parameters:
        - name: k_ip_service
          in: query
          description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_service_timeout:
                  type: number
                  format: int32
                  description: 'Timeout of the service. One of {@link \Studio\Ip\Service\ServiceTimeoutSid} constants.'
                id_service_type:
                  type: number
                  format: int32
                  description: 'Type of the service. One of {@link \Studio\Ip\Service\ServiceTypeSid} constants.'
                is_auto_restricted:
                  type: boolean
                  description: 'Indicates whether automatic session renewal for IP-based access is restricted. If set to `1`, access by IP is restricted and automatic session renewal is disabled if `0`, access by IP is allowed and session can be automatically renewed.'
                text_group:
                  type: string
                  description: 'Group of services.'
                text_title:
                  type: string
                  description: 'Name of the service.'
                text_update:
                  type: string
                  description: 'Network update rules.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_ip_service:
                        type: [string, "null"]
                        description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
      tags:
        - Service-Edit
  '/Studio/Service/Cid/Info.json':
    get:
      summary: 'Returns information about a CID. Retrieves the class name registered for the given CID value.'
      parameters:
        - name: cid
          in: query
          description: 'CID of the class to retrieve information for.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_class:
                        type: string
                        description: 'Name of the class associated with specified CID.'
      tags:
        - Cid-Info
  '/Studio/Service/Cid/Register.json':
    post:
      summary: 'Registers a new CID. If class is registered already, returns its CID.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_class:
                  type: string
                  description: 'Name of the class to register.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      cid:
                        type: number
                        format: int32
                        description: 'CID of requested class.'
      tags:
        - Cid-Register
  '/Studio/Service/Cid/Rename.json':
    put:
      summary: 'Allows to rename a class associated with a CID. The CID must already be registered. Validates the new class name format before updating.'
      parameters:
        - name: cid
          in: query
          description: 'CID of the class to rename.'
          schema:
            type: number
            format: int32
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_class:
                  type: string
                  description: 'New name to assign to a CID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Cid-Rename
  '/Studio/Deploy/Cluster/Cluster.json':
    post:
      summary: 'Create new `Deploy Cluster`. Validates SSH keys, Amazon security group configuration, and repository paths. Creates a new cluster record linked to the specified deployment instance.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_port:
                  type: [number, "null"]
                  description: 'SSH port number that should be used by compilation server to connect instance servers.'
                is_active:
                  type: [boolean, "null"]
                  description: 'Whether this deployment cluster is active.'
                is_fingerprint:
                  type: boolean
                  description: 'Whether to update fingerprints. Additionally, this setting allows you to force the transfer of keys to the server.'
                k_deploy_cluster:
                  type: [string, "null"]
                  description: 'Key of the cluster being saved.'
                k_deploy_compile:
                  type: [string, "null"]
                  description: 'ID of deployment server that serves this cluster.'
                k_deploy_instance:
                  type: [string, "null"]
                  description: '`Deploy Instance` ID.'
                s_cluster:
                  type: [string, "null"]
                  description: 'Cluster description contains either amazon security group id.'
                s_deploy_cluster:
                  type: [string, "null"]
                  description: 'Name of this deployment cluster.'
                s_key_control:
                  type: [string, "null"]
                  description: 'Private key to access all instances of cluster over SSH under control user.'
                s_ssh:
                  type: [string, "null"]
                  description: 'Private key to access all instances of cluster over SSH under instance user.'
                s_user:
                  type: [string, "null"]
                  description: 'Name of instance user.'
                s_user_control:
                  type: [string, "null"]
                  description: 'Name of control user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_deploy_cluster:
                        type: [string, "null"]
                        description: 'Key of the cluster being saved.'
      tags:
        - Cluster-Cluster
  '/Studio/Deploy/Instance/Instance.json':
    delete:
      summary: 'Delete an instance from database. For now only `stand` instances can be deleted physically. Validates that the instance exists and is a stand instance before deleting the record.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Instance-Instance
    post:
      summary: 'Creates new `Deploy Instance` Validates repository paths, variables, project, and optional account, role, and tag assignments.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_role:
                  type: [array, "null"]
                  description: 'List of `Project Role`.'
                a_tag:
                  type: [array, "null"]
                  description: 'List of tags. Each is one of {@link Tag::TAG_LIST}.'
                is_button:
                  type: [boolean, "null"]
                  description: 'Allow users to start builds manually by clicking a button.'
                is_commit:
                  type: [boolean, "null"]
                  description: 'Run build automatically upon commit.'
                is_queue:
                  type: [boolean, "null"]
                  description: 'Allow multiple commits to wait in the build queue. If not allowed, then only a single (the last one) commit may wait in the queue.'
                is_remove:
                  type: [boolean, "null"]
                  description: 'Indicates if instance set as removed.'
                k_amazon_account:
                  type: [string, "null"]
                  description: 'The key of `Studio Amazon Account`.'
                k_deploy_instance:
                  type: [string, "null"]
                  description: 'The key of `Studio Deploy Instance`.'
                k_project:
                  type: [string, "null"]
                  description: 'The key of `Studio Project`'
                s_deploy_instance:
                  type: [string, "null"]
                  description: 'The name of `Studio Deploy Instance`.'
                s_path:
                  type: [string, "null"]
                  description: 'The value contains a list of repository path.'
                s_variable:
                  type: [string, "null"]
                  description: 'The value contains a list of variables.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_deploy_instance:
                        type: [string, "null"]
                        description: 'The key of `Studio Deploy Instance`.'
      tags:
        - Instance-Instance
  '/Studio/Microservice/Editor/List.json':
    get:
      summary: 'Gets a list of microservices. Returns microservices accessible to the current user based on project roles and privileges.'
      parameters:
        - name: id_remove
          in: query
          description: 'Specifies whether to show removed and/or not removed microservices. One of {@link \AFlagSid} constants. * {@link \AFlagSid::ON} to show only not removed microservices. * {@link \AFlagSid::OFF} to show only removed microservices. * {@link \AFlagSid::ALL} to show all microservices (removed and not removed).'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: object
                        description: 'A list of microservices. One element contains: <dl> <dt>bool `is_remove`</dt> <dd>Whether the microservice has been deleted. `true` is the microservice is removed, `false` otherwise.</dd> <dt>string `json_path`</dt> <dd> A list of repositories and paths of this microservice. <i>Copy of {@link \Studio\Microservice\MicroserviceSql}.<var>json_path</var> field.</i> </dd> <dt>string `k_project`</dt> <dd>Project key. <i>Primary key in {@link \StudioProjectSql} table.</i></dd> <dt>string `s_file`</dt> <dd>File name.</dd> <dt>string `s_group`</dt> <dd> The group to which the microservice belongs. <i>See details in the `s_group` field of {@link \Studio\Microservice\MicroserviceSql} table.</i> </dd> <dt>string `s_variable`</dt> <dd> Microservice-specific variables. A set of pairs key=value, separated by newlines. <i>The list of variables is described in class {@link \Studio\Sprint\Promoting\PromotingConfigValue}.</i> </dd> <dt>string `text_project`</dt> <dd>Title of the project.</dd> <dt>string `text_title`</dt> <dd>Title of the microservice.</dd> <dt>string `url_microservice_promoting`</dt> <dd>URL of the promoting page for this microservice.</dd> </dl>'
      tags:
        - Editor-List
  '/Studio/Microservice/Service/InstanceActiveCheck.json':
    post:
      summary: 'Checks activity status of microservice instances. Queries DynamoDB for each provided instance ID and returns active, test, and existence flags for each instance.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_instance_check:
                  type: array
                  description: 'Instance ID list to check.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_instance:
                        type: array
                        description: 'List of instance IDs. Key is instance ID, value is an array with the following fields: <dl> <dt>bool `is_active`</dt> <dd> Whether the instance is active. `true` - the instance is active. `false` - the instance is not active. `null` - unknown, error occurred while checking instance activity. </dd> <dt>bool `is_error`</dt> <dd> Whether there was an error while checking the instance. `true` - an error occurred while checking the instance. `false` - the instance was checked successfully. </dd> <dt>bool `is_exists`</dt> <dd>Whether information about the instance was found. `true` - found, `false` - not found, `null` - unknown.</dd> <dt>bool `is_test`</dt> <dd>Whether the instance is a test instance. `true` - test, `false` - not a test, `null` - unknown.</dd> </dl>'
      tags:
        - Service-InstanceActiveCheck
  '/Studio/Personnel/DebugToken/DebugTokenService.json':
    post:
      summary: 'Return information is the debug token is valid. Validates token expiration, the requesting bot login, and personnel access before confirming.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_passport_bot_login:
                  type: string
                  description: 'Passport bot login that sends the request to check the token. <i>Copy of the {@link PASSPORT_BOT_LOGIN}.</i>'
                s_token:
                  type: string
                  description: 'Debug token to check.'
                url_request:
                  type: string
                  description: 'Full URL of the request which was made to get access with testing token, including protocol, host, and URI.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_valid:
                        type: boolean
                        description: 'Debug token to check and if valid to return `true` otherwise `false`.'
      tags:
        - DebugToken-DebugTokenService
  '/Core/Passport/Login/Info.json':
    get:
      summary: 'Returns information about user that is currently signed in. Used to bootstrap the login widget: determines whether a session is active and provides the URLs needed to redirect an unauthenticated visitor to the password reset or registration flows without hard-coding those URLs on the frontend.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: [string, "null"]
                        description: 'The current user key. This will be `null` if the user isn''t signed in.'
                      url_password_change:
                        type: string
                        description: 'A URL that a user can visit to reset their password.'
                      url_register:
                        type: string
                        description: 'The URL to the registration page.'
      tags:
        - Login-Info
  '/Core/Passport/ChangePassword/ChangePasswordApply.json':
    post:
      summary: 'Saves for user new password. Accepts the login, email, verification code (from the reset password link), and the new password, validates each against the stored state, and updates the user''s password. If the user is not already signed in, a new session is started for them.'
      parameters:
        - name: text_code
          in: query
          description: 'The verification code, which can be obtained from the "reset password" email link. This is a required value.'
          schema:
            type: string
        - name: text_login
          in: query
          description: 'The user''s login, which can be obtained from the "reset password" email link. This is a required value.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The user''s email address, which can be obtained from the "reset password" email link. This is a required value.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_password:
                  type: string
                  description: 'The user''s new password. This is a required value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ChangePassword-ChangePasswordApply
  '/Core/Passport/ChangePassword/ChangePasswordBegin.json':
    post:
      summary: 'Sends to user "password recovery" mail. Accepts the user''s email address and an optional custom reset-page URL, validates the input, optionally checks a CAPTCHA, and sends a password reset email containing a link the user can follow to set a new password using {@link ChangePasswordApplyApi}.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_captcha:
                  type: string
                  description: 'The characters to pass captcha test. Specify this only if server requires captcha.'
                text_mail:
                  type: string
                  description: 'The user''s email. This is a required value.'
                url_reset:
                  type: string
                  description: 'The URL for the password reset page. This link will be used in a password reset email. Specify this only if you want to send the user to a custom password reset page. If empty, the default URL page will be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_error:
                        type: string
                        description: 'The error code. This will be an empty string if the email has been sent successfully.'
      tags:
        - ChangePassword-ChangePasswordBegin
  '/Core/Testing/Automation/CacheFlush.json':
    get:
      summary: 'Flushes all application caches. Clears every cache layer used by the application via `CoreCache::flush_all()`. Intended for use by automation tests that need a clean cache state before or between test runs.'
      parameters:
        - name: s_action
          in: query
          description: 'Name of the action to perform within this endpoint.'
          schema:
            type: string
        - name: s_token
          in: query
          description: 'Request authentication token. A hex-encoded `sha256` signature that proves the caller knows the shared secret. See `checkAccess()` for the expected formula.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Automation-CacheFlush
  '/Core/Locale/Language/LanguageSwitch.json':
    post:
      summary: 'Switches the language. Remembers the requested language for the user who makes this request, within the context this user is currently in, and stores it in the session and in the cookie so that the very next request is served in this language too. The language must be active in this installation, otherwise the request is rejected and nothing is stored.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_language:
                  type: number
                  format: int32
                  description: 'Language to switch to.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Language-LanguageSwitch
  '/Core/Request/Api/KeySecret.json':
    get:
      summary: 'Gets a secret key for signing. Accepts a CSRF code, a session key, and the client origin URL, validates the session key against the current application, and returns a secret key that the client uses to sign subsequent CORS requests. This endpoint must not be called directly from a browser.'
      parameters:
        - name: s_csrf
          in: query
          description: 'The CSRF code from the client side.'
          schema:
            type: string
        - name: s_key_session
          in: query
          description: 'The session key.'
          schema:
            type: string
        - name: url_domain
          in: query
          description: 'Alias of {@link \Core\Request\Api\KeySecretApi::$url_origin}.'
          schema:
            type: string
        - name: url_origin
          in: query
          description: 'Origin for client requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_key_secret:
                        type: string
                        description: 'The secret key for the request signing.'
      tags:
        - Api-KeySecret
  '/Core/Request/Token/TokenPin.json':
    get:
      summary: 'Gets new pin code. First step of push-notification-based security token generation. Issues a short-lived PIN code linked to a push recipient, which is then sent to the user''s device. The client must present this PIN before its expiry to receive the final security token.'
      parameters:
        - name: text_push
          in: query
          description: 'The ID to use to send a security code via a push notification.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtu_expire:
                        type: string
                        description: 'The expiration date/time of the PIN code.'
                      text_pin:
                        type: string
                        description: 'The PIN code.'
      tags:
        - Token-TokenPin
  '/Core/AI/LogTriage/ConnectionCheck.json':
    get:
      summary: 'Returns a fixed connection value and, when requested, selected findings.'
      parameters:
        - name: a_id_source
          in: query
          description: 'IDs of finding sources from {@link TriageSourceSid}.'
          schema:
            type: array
        - name: is_finding
          in: query
          description: '`true` returns findings; otherwise `false` performs only the connection check.'
          schema:
            type: boolean
        - name: s_date_mask
          in: query
          description: 'Date/time mask accepted by {@link LogSearchQuery}. Empty string selects the current UTC date.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Optional case-insensitive message substring.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_finding:
                        type: array
                        description: 'Grouped findings. One element contains: <dl> <dt>string `dl_first_seen`</dt> <dd>Local date of the first usage-statistics record.</dd> <dt>string `dl_last_seen`</dt> <dd>Local date of the last usage-statistics record.</dd> <dt>string `dtu_first_seen`</dt> <dd>UTC date/time of the first matching log or async-task record. Empty for background tasks.</dd> <dt>string `dtu_last_seen`</dt> <dd>UTC date/time of the last matching log or async-task record. Empty for background tasks.</dd> <dt>int `i_occurrence_count`</dt> <dd>Number of matching records.</dd> <dt>int `i_priority_multiplier`</dt> <dd>Usage-statistics priority multiplier. Present for the usage-statistics source.</dd> <dt>int `id_source`</dt> <dd>Finding source from {@link \Core\AI\LogTriage\TriageSourceSid}.</dd> <dt>string `s_object`</dt> <dd>Usage-statistics object. Present for the usage-statistics source.</dd> <dt>string `s_period`</dt> <dd>Usage-statistics aggregation period. Present for the usage-statistics source.</dd> <dt>string `s_priority`</dt> <dd>Usage-statistics priority. Present for the usage-statistics source.</dd> <dt>string `text_message`</dt> <dd>Log message or task description. Present for log and task sources.</dd> </dl>'
                      i_result:
                        type: number
                        format: int32
                        description: 'Connection check value.'
      tags:
        - LogTriage-ConnectionCheck
  '/Core/Spa/Application/SpaApplication.json':
    get:
      summary: 'Gets information of application. Returns which sign-in methods (Apple, Facebook, Google, Microsoft, TJX) are enabled for the application, along with the social sign-in and registration restrictions and the corresponding authorization links.'
      parameters:
        - name: s_application
          in: query
          description: 'Unique ID of application.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_apple:
                        type: boolean
                        description: '`true` - enable for this application sign in with Apple; `false` - disable.'
                      is_facebook:
                        type: boolean
                        description: '`true` - enable for this application sign in with Facebook; `false` - disable.'
                      is_facebook_android:
                        type: boolean
                        description: '`true` - enable for this application sign in with Facebook for Android; `false` - disable.'
                      is_google:
                        type: boolean
                        description: '`true` - enable for this application sign in with Google; `false` - disable.'
                      is_microsoft:
                        type: boolean
                        description: '`true` - enable for this application sign in with Microsoft; `false` - disable.'
                      is_register_deny:
                        type: boolean
                        description: '`true` - registration of new user is denied in application; `false` - registration of new user is allowed.'
                      is_social:
                        type: boolean
                        description: '`true` if sign in via social networks is allowed; `false` otherwise.'
                      is_tjx:
                        type: boolean
                        description: '`true` if sign in via TJX Azure is allowed; `false` otherwise.'
                      text_tjx_button_name:
                        type: string
                        description: 'Tjx button name.'
                      url_facebook:
                        type: string
                        description: 'Facebook authorization link.'
                      url_tjx:
                        type: string
                        description: 'Tjx link.'
      tags:
        - Application-SpaApplication
  '/Core/Drive/ImageUpload/ImageUploadTemporary.json':
    post:
      summary: 'Saves new uploaded image. First step of the two-step image upload flow. Stores the uploaded file in temporary drive storage and returns preview URLs and dimension metadata so the user can review or crop the image before it is permanently saved. Call {@link ImageUploadApi} to commit the temporary image to the entity.'
      parameters:
        - name: k_id
          in: query
          description: 'The key of the image within {@link ImageUploadTemporaryApi::$s_class}. For example, for a user''s photo, specify the user''s key here.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image. For example, for a user''s photo, specify the string `PassportLoginImage` here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                f_image:
                  type: object
                  description: 'The image to be uploaded.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_crop:
                        type: boolean
                        description: 'If `true`, a crop is used. Otherwise, this will be `false` if a crop isn''t used (the image is saved in its original size).'
                      i_height:
                        type: number
                        format: int32
                        description: 'The actual height of the thumbnail image.'
                      i_height_src:
                        type: number
                        format: int32
                        description: 'The height of the original image.'
                      i_rotate:
                        type: number
                        format: int32
                        description: 'The angle of the image rotation compared to the original.'
                      i_width:
                        type: number
                        format: int32
                        description: 'The actual width of the thumbnail image.'
                      i_width_src:
                        type: number
                        format: int32
                        description: 'The width of the original image.'
                      id_type_src:
                        type: number
                        format: int32
                        description: 'The image type ID. One of the {@link \Core\Drive\DriveTypeSid} constants.'
                      is_resize:
                        type: boolean
                        description: 'If `true`, the thumbnail is a resized variant of the original image.'
                      url_thumbnail:
                        type: string
                        description: 'The URL to the resized and rotated image in file storage.'
                      url_view:
                        type: string
                        description: 'The URL to the original image in file storage.'
      tags:
        - ImageUpload-ImageUploadTemporary
  '/Core/Drive/ImageUpload/ImageUpload.json':
    get:
      summary: 'Returns information about the image. Used to initialize an image upload widget for an existing entity. Returns thumbnail and full-size URLs, upload endpoint, dimension constraints, and whether deletion is allowed, so the frontend can render the current image and offer upload or delete actions.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - ImageUpload-ImageUpload
    post:
      summary: 'Loads image information for a list of IDs. The POST method is used instead of the GET method because the maximum permitted URI length is restricted. Bulk variant of the GET method for pages that must display many images at once. Accepts a JSON-encoded list of entity IDs and returns the same metadata as the single-item GET, loading all images in one round-trip to avoid N+1 requests.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_id:
                  type: [string, "null"]
                  description: 'A list of image IDs set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}, serialized using JSON. This will be `null` if there''s no data being sent by the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image information for every ID. <i>Every element contains values such as the {@link ImageUploadApi::_get()} result.</i> <dl> <dt>array `a_text_empty`</dt> <dd> Information about the text on the empty upload image. <i>See: {@link \AImageUpload::textEmpty()}.</i> <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl> </dd> <dt>int `i_height_max`</dt> <dd>The maximum height of the image.</dd> <dt>int `i_height_min`</dt> <dd>The minimum height of the image.</dd> <dt>int `i_thumbnail_height`</dt> <dd>The height of the thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_HEIGHT}.</i></dd> <dt>int `i_thumbnail_width`</dt> <dd>The width of thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_WIDTH}.</i></dd> <dt>int `i_width_max`</dt> <dd>The maximum width of image.</dd> <dt>int `i_width_min`</dt> <dd>The minimum width of image.</dd> <dt>bool `is_circular`</dt> <dd><tt>true</tt> if image is treated as circular; <tt>false</tt> otherwise.</dd> <dt>bool `is_delete_allow`</dt> <dd><tt>true</tt> if image deleting is allowed; <tt>false</tt> otherwise.</dd> <dt>string `s_link`</dt> <dd>The image link.</dd> <dt>string `url_empty`</dt> <dd>The URL of the image that should be shown in a case image is not uploaded.</dd> <dt>string|null `url_thumbnail`</dt> <dd>The URL of thumbnail of the image. <tt>null</tt> if image is not uploaded.</dd> <dt>string `url_upload`</dt> <dd>The URL the script where new image should be uploaded.</dd> <dt>string|null `url_view`</dt> <dd>The URL of the full image. <tt>null</tt> if image is not uploaded.</dd> </dl>'
      tags:
        - ImageUpload-ImageUpload
    put:
      summary: 'Updates the image. Applies a command (such as delete or replace) to a previously uploaded image, then returns the refreshed image metadata so the frontend can update the widget state without a separate GET call.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_command:
                  type: string
                  description: 'The action that must be performed to the image.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - ImageUpload-ImageUpload
  '/Core/Google/Captcha/CaptchaScore.json':
    get:
      summary: 'Returns the overridden score for the reCAPTCHA v3. Returns the current session-level score override value that was set via the PUT method. Requires reCAPTCHA v3 to be enabled; throws an exception otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_score:
                        type: [number, "null"]
                        description: 'Overridden score value for V3 captcha. `null` to reset override.'
      tags:
        - Captcha-CaptchaScore
    post:
      summary: 'Validates the reCAPTCHA v3 token. Accepts a reCAPTCHA v3 user token, sends it to Google for verification, and returns a response ID indicating the outcome. Note that each token can only be validated once; a new token must be generated for subsequent requests.'
      parameters:
        - name: text_token
          in: query
          description: 'The user token CAPTCHA from {@link CaptchaVersionSid::V3} captcha. Be careful when use this endpoint for validate token, because token can be used only once, so if you validate token in this endpoint, you must generate new token for next requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_response:
                        type: number
                        format: int32
                        description: 'Captcha response ID.'
      tags:
        - Captcha-CaptchaScore
    put:
      summary: 'Overrides the score for the reCAPTCHA v3. Sets a custom score value in the range 0.0-1.0 to override the actual reCAPTCHA v3 score for the current session. Pass `null` to clear the override and restore the default behavior. Requires the score override feature to be enabled.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                f_score:
                  type: [number, "null"]
                  description: 'Overridden score value for V3 captcha. `null` to reset override.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Captcha-CaptchaScore
  '/Core/Google/Captcha/GoogleCaptcha.json':
    put:
      summary: 'Saves the user CAPTCHA token for the current session. Accepts the CAPTCHA version, the action name, and the user token obtained from the Google reCAPTCHA widget, and stores them in the session so that subsequent API requests requiring CAPTCHA verification can use them. Site keys for initialize Google reCAPTCHA: * Version 2 (invisible): * Demo/Staging - {@link GoogleCaptchaKeys::CAPTCHA_V2_SITE_KEY_SANDBOX} * Production - {@link GoogleCaptchaKeys::CAPTCHA_V2_SITE_KEY_PRODUCTION} * Version 3: * Demo/Staging - {@link GoogleCaptchaKeys::CAPTCHA_V3_SITE_KEY_SANDBOX} * Production - {@link GoogleCaptchaKeys::CAPTCHA_V3_SITE_KEY_PRODUCTION}'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_version:
                  type: number
                  format: int32
                  description: 'Captcha version ID.'
                text_action:
                  type: string
                  description: 'The action name. Used to determine the place where the CAPTCHA is needed in documentation for endpoints that used the CAPTCHA.'
                text_token:
                  type: string
                  description: 'The user token CAPTCHA.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Captcha-GoogleCaptcha
  '/Core/Geo/Region/Region.json':
    get:
      summary: 'Retrieves information about countries and regions. Populates country and region dropdowns in address forms. Returns one or all countries together with their subordinate regions (states, provinces, territories). Pass a locale to get only the country relevant to a given market, or set `$is_locale_all` to retrieve the full list for multi-country UIs.'
      parameters:
        - name: id_locale
          in: query
          description: 'The locale ID to find regions for. One of the {@link LocaleSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_locale_all
          in: query
          description: 'Determines whether to get regions for all locales. If `true`, this will get regions for all locales. Otherwise, this will be `false`.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_region:
                        type: array
                        description: 'A list of regions grouped by their country. <dl> <dt>string[] `a_region`</dt> <dd> A list of regions in the country. Every element has the next keys: <dl> <dt>string `k_geo`</dt> <dd>The region key.</dd> <dt>string `s_title`</dt> <dd>The name of the region.</dd> </dl> </dd> <dt>int `id_locale`</dt> <dd>The locale ID of the country. One of the {@link \Core\Locale\LocaleSid} constants.</dd> <dt>string `k_geo_country`</dt> <dd>The country key.</dd> <dt>string `s_abbr`</dt> <dd>The country abbreviation.</dd> <dt>string `s_title`</dt> <dd>The name of the country.</dd> </dl>'
      tags:
        - Region-Region
  '/Social/Apple/Login/AppleLogin.json':
    delete:
      summary: 'Removes the association between a website client and the Apple account. Invalidates the tokens and associated client authorizations. Requires the user to be signed in. Accepts the application ID, retrieves the Apple sign-in certificate for that application, and unlinks the Apple account from the current user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-AppleLogin
    post:
      summary: 'Signs user in with Apple. Accepts the application ID, the Apple authorization code, and optional first and last name values. If the user is already signed in, binds the Apple account to their existing account; otherwise, signs them in or creates a new account using the Apple identity.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_application:
                  type: string
                  description: 'The application ID.'
                text_authorization:
                  type: string
                  description: 'The authorization code.'
                text_name_first:
                  type: string
                  description: 'User''s first name.'
                text_name_last:
                  type: string
                  description: 'User''s last name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-AppleLogin
  '/Social/Facebook/Login/Login.json':
    post:
      summary: 'Authorizes user with facebook. Accepts a Facebook access token and an optional application ID. If the user is already signed in, links the Facebook account to their existing account; otherwise, signs them in or creates a new account using the Facebook identity and the configured Facebook credentials.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link FacebookCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link FacebookCredential::CID}. When application passed, the credential loaded by application id.'
                s_token:
                  type: string
                  description: 'The Facebook token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-Login
  '/Social/Google/Plus/Login.json':
    post:
      summary: 'Authorizes user with Google. Accepts a Google server authorization code and an optional application ID, loads the configured Google credential for the application, and completes the OAuth 2.0 flow to sign the user in or create a new account.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link GoogleCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link GoogleCredential::CID}. When application passed, the credential loaded by application id.'
                s_code:
                  type: string
                  description: 'The Google server authorization code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Plus-Login
  '/Wl/Schedule/ClassView/ClassView.json':
    get:
      summary: 'Retrieves information about class. Returns detailed information about one or more class sessions, including staff, location, assigned assets, virtual location links, visit counts, and booking availability. Supports single-session mode for one class period or multi-session mode for batch lookups.'
      parameters:
        - name: a_session_request
          in: query
          description: 'A list of sessions to get information for. Every element has the following keys: `null` if requesting a single session. <dl> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `k_class_period`</dt> <dd>The session key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'The date/time of the session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the action is performed. `null` if key of the business was not passed. Key of the business is required if {@link ClassViewApi} was passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_asset:
                        type: [array, "null"]
                        description: 'Asset list data. <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl>'
                      a_class:
                        type: [array, "null"]
                        description: 'Detailed information about the class. This will be `null` if data isn''t loaded yet. <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int `i_age_from`</dt> <dd>The minimum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_from_month`</dt> <dd>The minimum age restriction (months).</dd> <dt>int `i_age_from_year`</dt> <dd>The minimum age restriction (years).</dd> <dt>int `i_age_to`</dt> <dd>The maximum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_to_month`</dt> <dd>The maximum age restriction (months).</dd> <dt>int `i_age_to_year`</dt> <dd>The maximum age restriction (years).</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int `i_visit`</dt> <dd>Number of usages of promotion required to pay for a single class session.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this session. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl>'
                      a_location:
                        type: [array, "null"]
                        description: 'Location data. This will be `null` if data isn''t loaded yet. <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl>'
                      a_session_result:
                        type: array
                        description: 'A list of sessions with information, received in a multiple session mode. <dl> <dt>array `a_asset`</dt> <dd> Array of asset. <i>Result of {@link \Wl\Service\Asset::assetsUsingResourceBusyList()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl> </dd> <dt>array `a_class`</dt> <dd> Class information. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_classInfo()}</i> <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int `i_age_from`</dt> <dd>The minimum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_from_month`</dt> <dd>The minimum age restriction (months).</dd> <dt>int `i_age_from_year`</dt> <dd>The minimum age restriction (years).</dd> <dt>int `i_age_to`</dt> <dd>The maximum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_to_month`</dt> <dd>The maximum age restriction (months).</dd> <dt>int `i_age_to_year`</dt> <dd>The maximum age restriction (years).</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int `i_visit`</dt> <dd>Number of usages of promotion required to pay for a single class session.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this session. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl> </dd> <dt>array `a_location`</dt> <dd> Location info. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_locationInfo()}</i> <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff list. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_staffInfo()} method.</i> <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd> List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations. </dd> <dt>array[] `a_visits_required`</dt> <dd> List of classes and events, which client should visit before this one. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::visitsRequiredInfo()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: [array, "null"]
                        description: 'Staff member list data. This will be `null` if data isn''t loaded yet. <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl>'
                      a_virtual_location:
                        type: array
                        description: 'List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations.'
                      a_visits_required:
                        type: array
                        description: 'A list of classes and events that clients should visit before this one. <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl>'
      tags:
        - ClassView-ClassView
    post:
      summary: 'Retrieves information about classes. A POST-method alternative to {@link ClassViewApi::get()} for multi-session batch lookups where the session list is too long to encode in a GET query string. The session list is provided as a JSON-encoded string in the request body.'
      parameters:
        - name: a_session_request
          in: query
          description: 'A list of sessions to get information for. Every element has the following keys: `null` if requesting a single session. <dl> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `k_class_period`</dt> <dd>The session key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'The date/time of the session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the action is performed. `null` if key of the business was not passed. Key of the business is required if {@link ClassViewApi} was passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_session_request:
                  type: string
                  description: 'A list of sessions to get information for. A serialized array. See {@link ClassViewApi::$a_session_request} for the array structure. Serialization and sending by POST is necessary to send big lists.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_asset:
                        type: [array, "null"]
                        description: 'Asset list data. <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl>'
                      a_class:
                        type: [array, "null"]
                        description: 'Detailed information about the class. This will be `null` if data isn''t loaded yet. <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int `i_age_from`</dt> <dd>The minimum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_from_month`</dt> <dd>The minimum age restriction (months).</dd> <dt>int `i_age_from_year`</dt> <dd>The minimum age restriction (years).</dd> <dt>int `i_age_to`</dt> <dd>The maximum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_to_month`</dt> <dd>The maximum age restriction (months).</dd> <dt>int `i_age_to_year`</dt> <dd>The maximum age restriction (years).</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int `i_visit`</dt> <dd>Number of usages of promotion required to pay for a single class session.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this session. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl>'
                      a_location:
                        type: [array, "null"]
                        description: 'Location data. This will be `null` if data isn''t loaded yet. <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl>'
                      a_session_result:
                        type: array
                        description: 'A list of sessions with information, received in a multiple session mode. <dl> <dt>array `a_asset`</dt> <dd> Array of asset. <i>Result of {@link \Wl\Service\Asset::assetsUsingResourceBusyList()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl> </dd> <dt>array `a_class`</dt> <dd> Class information. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_classInfo()}</i> <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int `i_age_from`</dt> <dd>The minimum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_from_month`</dt> <dd>The minimum age restriction (months).</dd> <dt>int `i_age_from_year`</dt> <dd>The minimum age restriction (years).</dd> <dt>int `i_age_to`</dt> <dd>The maximum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_to_month`</dt> <dd>The maximum age restriction (months).</dd> <dt>int `i_age_to_year`</dt> <dd>The maximum age restriction (years).</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int `i_visit`</dt> <dd>Number of usages of promotion required to pay for a single class session.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this session. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl> </dd> <dt>array `a_location`</dt> <dd> Location info. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_locationInfo()}</i> <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff list. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_staffInfo()} method.</i> <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd> List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations. </dd> <dt>array[] `a_visits_required`</dt> <dd> List of classes and events, which client should visit before this one. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::visitsRequiredInfo()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: [array, "null"]
                        description: 'Staff member list data. This will be `null` if data isn''t loaded yet. <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl>'
                      a_virtual_location:
                        type: array
                        description: 'List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations.'
      tags:
        - ClassView-ClassView
  '/Wl/Schedule/ClassList/ClassList68.json':
    post:
      summary: 'This method is a modified Get method {@link ClassListApi::get()}. The difference is as follows: - Some data for filtering is now transmitted by the post method. Because the addition of the filters creates a scenario where we can easily reach the maximum URL length of GET requests and the browser refuse to send the request (situations with long class ID, event ID or staff ID lists). - Added generation of a separate ''Quick filter''. This generation is enabled using the flag {@link ClassList68Api::$show_quick_filter}. - Added a filter list of events {@link ClassList68Api::$a_event}. - Added a filter ID of tab {@link ClassList68Api::$id_class_tab}.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_class:
                  type: array
                  description: 'The list of classes keys to filter. Return sessions with matching class IDs. If it''s empty and {@link ClassList68Api::$show_class} is `true`, all classes will be returned.'
                a_class_tab:
                  type: [array, "null"]
                  description: 'List of tabs keys. This will be ignored if {@link ClassList68Api::$is_tab_all} is `true`. If list of tab keys is not empty, {@link ClassList68Api::$id_class_tab} is mandatory. `null` if no filtering by Book Now Tab is required.'
                a_day:
                  type: array
                  description: 'Class filter by day of the week. Array of number representing the days of the week. Return sessions matching the given weekdays. (7 = Sunday, 1 = Monday, ..., 6 = Saturday) Empty array means no filtering.'
                a_event:
                  type: array
                  description: 'The list of event keys to filter. Return sessions with matching event keys. If it''s empty and {@link ClassList68Api::$show_event} is `true`, all events will be returned.'
                a_location:
                  type: array
                  description: 'The list of location keys to filter results. If it''s empty, schedule for all locations will be returned. All given locations should be from the same business, which is sent in {@link ClassListApi::$k_business}.'
                a_time:
                  type: array
                  description: 'Class filter by time of day. List of arrays with start and end time in "HH:MM" format (24h). Include sessions that start between the specified time range. Each value is array with time parameters: Empty array means no filtering. <dl> <dt>int `tl_start`</dt> <dd>Time when the session starts. Example: value `''06:00''`.</dd> <dt>int `tl_end`</dt> <dd>Time when the session ends. Example: value `''14:00''`.</dd> </dl>'
                dt_date:
                  type: string
                  description: 'The list start date in MySQL format.'
                dt_end:
                  type: string
                  description: 'The list end date in MySQL format. <i> If left empty, the default duration is {@link ClassList68Api::DEFAULT_PERIOD} days after {@link ClassList68Api::$dt_date}. </i>'
                id_class_tab:
                  type: [number, "null"]
                  description: 'ID of tab. One of {@link TabSid} constants. This will be ignored if {@link ClassList68Api::$is_tab_all} is `true`. `null` if no filtering by tab is required.'
                is_response_short:
                  type: boolean
                  description: '`true` means to not generate {@link ClassList68Api::$a_session} result. Can be used, if you do not need full information about existing classes and result in {@link ClassList68Api::$a_calendar} is enough.'
                is_tab_all:
                  type: boolean
                  description: 'If `true`, sessions from every class tab are returned. If `false`, use the {@link ClassList68Api::$k_class_tab} or {@link ClassList68Api::$id_class_tab} to filter sessions by class tab.'
                is_virtual:
                  type: [boolean, "null"]
                  description: 'Class filter by type. The class is virtual. `true`: Only virtual classes. `false`: Only in-person. `null` or not set: No filtering.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_class_tab:
                  type: string
                  description: 'The tab key. This will be ignored if {@link ClassList68Api::$is_tab_all} is `true`.'
                k_timezone:
                  type: ["null", string]
                  description: 'Key of timezone. If not set then use default client timezone. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
                s_staff:
                  type: string
                  description: 'The list of staff members to filter. A comma separated list of staff keys.'
                s_staff_uid:
                  type: string
                  description: 'The list of staff user keys to filter. A comma separated list of staff user keys.'
                show_book_status:
                  type: boolean
                  description: 'Whether to compute and add the per-session booking status fields to each element of {@link ClassList68Api::$a_session}: <var>can_book</var>, <var>is_book</var>, <var>is_wait_list</var>, <var>id_deny_reason</var>. <i>These fields mirror {@link ClassViewApi::status()}.</i> It requires evaluating the full booking policy (promotions, family accounts, resource availability, etc.) for every returned session, which is significantly more expensive than the rest of this API. Defaults to `false` so that regular schedule listing calls are not slowed down; enable it only when the caller actually needs these fields.'
                show_cancel:
                  type: boolean
                  description: 'If `true`, canceled sessions will be returned. If `false`, canceled sessions won''t be returned.'
                show_class:
                  type: boolean
                  description: 'If `true`, classes will be included in the response. `false` - otherwise.'
                show_event:
                  type: boolean
                  description: 'If `true`, events are also returned. If `false`, only classes are returned.'
                show_quick_filter:
                  type: boolean
                  description: 'Whether to generate {@link ClassList68Api::$a_quick} a quick filter. If `true`, a quick filter will be generated. `false` otherwise.'
                uid:
                  type: string
                  description: 'The user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_calendar:
                        type: object
                        description: 'Keys are dates of the days inside requested date range, when there is at least one class in the business. If locations are sent as a parameter, then at least one class must exist in the given locations. Each key is a date string in `YYYY-MM-DD` format (local date in the business time zone). Each value is an empty array reserved for future use.'
                      a_quick:
                        type: array
                        description: 'Information about classes/events for quick filter. Every element has the following keys: For generating this filter, all filters are applied except: - {@link ClassList68Api::$a_class}; - {@link ClassList68Api::$a_event}; - {@link ClassList68Api::$show_class}; - {@link ClassList68Api::$show_event}. <dl> <dt>string `text_type`</dt> <dd>Type of class ("class" || "event")</dd> <dt>string `k_class`</dt> <dd>Class/event key. <i>Primary key from {@link \RsClassSql} table.</i></dd> <dt>string `s_class`</dt> <dd>Class/event title.</dd> <dt>int `i_class`</dt> <dd>Total sessions found.</dd> </dl>'
                      a_session:
                        type: array
                        description: 'A list of classes sessions starting with the date {@link ClassListApi::$dt_date} and in the 62 days ahead (or up to {@link ClassListApi::$dt_end}). Every element has the following keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>Keys of class tab. <i>Primary keys in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i></dd> <dt>string[] `a_image`</dt> <dd>The class image. Empty array if there is no image.</dd> <dt>string[] `a_search_tag`</dt> <dd>Tags associated with an individual class.</dd> <dt>string[] `a_staff`</dt> <dd> The list of staff keys for the staff member conducting the session. For legacy third-party apps listed in {@link \Wl\Schedule\ClassList\ClassListApi::APPS_USE_OLD_K_STAFF}, contains <i>primary keys in {@link \RsStaffSql} table</i> `k_staff` for backward compatibility. Empty for all other applications. Use `a_staff_uid` instead. </dd> <dt>bool[] `a_staff_quick_substitute`</dt> <dd> Whether staff or pay rate changed due quick substitution, for each staff member. Aligned by index with <var>a_staff_uid</var>. </dd> <dt>string[] `a_staff_uid`</dt> <dd>The list of staff user keys for the staff member conducting the session.</dd> <dt>string[] `a_virtual_location`</dt> <dd> The list of virtual locations keys. Each value is a location key. <i>Primary keys in {@link \RsLocationSql} table.</i> </dd> <dt>bool|null `can_book`</dt> <dd> Whether current client can book class. Only present if {@link \Wl\Schedule\ClassList\ClassList68Api::$show_book_status} is `true`. `null` if not requested. </dd> <dt>string `dt_date`</dt> <dd>The date/time of the session start in UTC.</dd> <dt>string `dt_time`</dt> <dd>The time of the session start in the local time zone.</dd> <dt>string `dtl_date`</dt> <dd>The date/time of session start in the location''s time zone.</dd> <dt>bool `hide_application`</dt> <dd> Specifies whether the class will be hidden in the White Label Achieve Client App. If `true`, it means that the class won''t be displayed. Otherwise, this will be `false` to indicate that the class will be displayed. </dd> <dt>string `html_description`</dt> <dd>The class description.</dd> <dt>int|null `i_age_from`</dt> <dd> The minimum age restriction. Deprecated and is left only for back compatibility. `null` if {@link \Wl\Schedule\ClassList\ClassList68Api::$a_session}''s <var>is_age_public</var> is `false` or age is not restricted. </dd> <dt>int|null `i_age_from_month`</dt> <dd>The minimum age restriction (months). `null` if <var>is_age_public</var> is `false` or age is not restricted.</dd> <dt>int|null `i_age_from_year`</dt> <dd>The minimum age restriction (years). `null` if <var>is_age_public</var> is `false` or age is not restricted.</dd> <dt>int|null `i_age_to`</dt> <dd> The maximum age restriction. Deprecated and is left only for back compatibility. `null` if <var>is_age_public</var> is `false` or age is not restricted. </dd> <dt>int|null `i_age_to_month`</dt> <dd>The maximum age restriction (months). `null` if <var>is_age_public</var> is `false` or age is not restricted.</dd> <dt>int|null `i_age_to_year`</dt> <dd>The maximum age restriction (years). `null` if <var>is_age_public</var> is `false` or age is not restricted.</dd> <dt>int `i_book`</dt> <dd>Count of visits on this class.</dd> <dt>int|null `i_capacity`</dt> <dd>The capacity of the service. ''null'' indicates that the capacity is not set.</dd> <dt>int `i_day`</dt> <dd>The day of the week when session is occurred. Constant from {@link \ADateWeekSid}.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_wait`</dt> <dd>Number of clients in wait list.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. `null` if limit is not set.</dd> <dt>int `i_wait_spot`</dt> <dd> Position of the current client (<var>uid</var>) in the wait list for this session. `0` if the client is not on the wait list, or if the business hides wait list position (fastest-response wait list promotion). </dd> <dt>int|null `id_deny_reason`</dt> <dd> ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. Only present if {@link \Wl\Schedule\ClassList\ClassList68Api::$show_book_status} is `true`. `null` if not requested. </dd> <dt>bool|null `is_book`</dt> <dd> Whether current class was booked by current client. Only present if {@link \Wl\Schedule\ClassList\ClassList68Api::$show_book_status} is `true`. `null` if not requested. </dd> <dt>bool `is_age_public`</dt> <dd>Whether the age restriction of the class is shown to clients.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>If `true`, this class period was canceled. Otherwise, this will be `false`.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this session. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>If `true`, this is an event. Otherwise, this will be `false`.</dd> <dt>bool `is_special_instructions`</dt> <dd> Whether special instructions are configured for this session and are visible to the current client. The content itself (`html_special` in {@link \Wl\Schedule\ClassView\ClassViewApi}) is not returned here. </dd> <dt>bool `is_virtual`</dt> <dd>If `true`, this class is virtual. Otherwise, this will be `false`.</dd> <dt>bool `is_wait`</dt> <dd>`true` if the current client (<var>uid</var>) is on the wait list for this session; `false` otherwise.</dd> <dt>bool|null `is_wait_list`</dt> <dd> `true` if the current client can only take a place on the wait list; `false` otherwise. Only present if {@link \Wl\Schedule\ClassList\ClassList68Api::$show_book_status} is `true`. `null` if not requested. </dd> <dt>bool `is_wait_list_enabled`</dt> <dd>This will be `true` if user is only on the wait-list. Otherwise, this will be `false`.</dd> <dt>string `k_class`</dt> <dd>The class key. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The key of the session''s location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `s_title`</dt> <dd>The title of the session.</dd> <dt>string `text_room`</dt> <dd>Class room. Empty string if not set.</dd> <dt>string `url_book`</dt> <dd>The direct link to start booking on the WellnessLiving website.</dd> <dt>string `url_virtual_join`</dt> <dd> Link to virtual service. Empty string if the class isn''t virtual, or if the current client (<var>uid</var>) has not booked/waitlisted this session, or joining isn''t available yet. </dd> </dl>'
                      is_timezone_different:
                        type: boolean
                        description: 'If `true`, the list of sessions contains sessions from different time zones. Otherwise, this will be `false`.'
                      is_virtual_service:
                        type: boolean
                        description: 'If `true`, there exists at least one virtual service by a specified {@link ClassList68Api::$k_business} and {@link ClassList68Api::$k_class_tab}, Otherwise, this will be `false`.'
      tags:
        - ClassList-ClassList68
  '/Wl/Schedule/ClassList/ClassList.json':
    get:
      summary: 'Retrieves a list of classes and class information for the given business and date range. Returns upcoming class sessions for a business, with optional filtering by location, day of week, time of day, staff member, and virtual or in-person mode. The result includes per-session details such as staff, capacity, booking counts, and virtual locations, as well as a calendar map indicating which dates have at least one scheduled session.'
      parameters:
        - name: a_class
          in: query
          description: 'The list of classes keys to filter. Return sessions with matching class IDs. If it''s empty, all classes/events will be returned.'
          schema:
            type: array
        - name: a_day
          in: query
          description: 'Class filter by day of the week. Array of number representing the days of the week. Return sessions matching the given weekdays. (7 = Sunday, 1 = Monday, ..., 6 = Saturday) Empty array means no filtering.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'The list of location keys to filter results. If it''s empty, schedule for all locations will be returned. All given locations should be from the same business, which is sent in {@link ClassListApi::$k_business}.'
          schema:
            type: array
        - name: a_time
          in: query
          description: 'Class filter by time of day. List of arrays with start and end time in "HH:MM" format (24h). Include sessions that start between the specified time range. Each value is array with time parameters: Empty array means no filtering. <dl> <dt>int `tl_start`</dt> <dd>Time when the session starts. Example: value `''06:00''`.</dd> <dt>int `tl_end`</dt> <dd>Time when the session ends. Example: value `''14:00''`.</dd> </dl>'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The list start date in UTC and in MySQL format.'
          schema:
            type: string
        - name: dt_end
          in: query
          description: 'The list end date in UTC and in MySQL format. <i> If left empty, the default duration is {@link ClassListApi::DEFAULT_PERIOD} days after {@link ClassListApi::$dt_date}. </i>'
          schema:
            type: string
        - name: is_response_short
          in: query
          description: '`true` means to not generate {@link ClassListApi::$a_session} result. Can be used, if you do not need full information about existing classes and result in {@link ClassListApi::$a_calendar} is enough.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: 'If `true`, sessions from every class tab are returned. If `false`, use the {@link ClassListApi::$k_class_tab} value.'
          schema:
            type: boolean
        - name: is_virtual
          in: query
          description: 'Class filter by type. The class is virtual. `true`: Only virtual classes. `false`: Only in-person. `null` or not set: No filtering.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'The category tab key. This will be `null` if not set yet. This will be ignored if {@link ClassListApi::$is_tab_all} is `true`.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. If not set then use default client timezone. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: s_staff
          in: query
          description: 'The list of staff members to filter. A comma separated list of staff keys.'
          schema:
            type: string
        - name: s_staff_uid
          in: query
          description: 'The list of staff user keys to filter. A comma separated list of staff user keys.'
          schema:
            type: string
        - name: show_cancel
          in: query
          description: 'If `true`, canceled sessions will be returned. If `false`, canceled sessions won''t be returned.'
          schema:
            type: boolean
        - name: show_class
          in: query
          description: 'If `true`, classes will be included in the response. `false` - otherwise.'
          schema:
            type: boolean
        - name: show_event
          in: query
          description: 'If `true`, events are also returned. If `false`, only classes are returned.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_calendar:
                        type: object
                        description: 'Keys are dates of the days inside requested date range, when there is at least one class in the business. If locations are sent as a parameter, then at least one class must exist in the given locations. Each key is a date string in `YYYY-MM-DD` format (local date in the business time zone). Each value is an empty array reserved for future use.'
                      a_session:
                        type: array
                        description: 'A list of classes sessions starting with the date {@link ClassListApi::$dt_date} and in the 62 days ahead (or up to {@link ClassListApi::$dt_end}). Every element has the following keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>Keys of class tab. <i>Primary keys in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i></dd> <dt>string[] `a_image`</dt> <dd>The class image. Empty array if there is no image.</dd> <dt>string[] `a_search_tag`</dt> <dd>Tags associated with an individual class.</dd> <dt>string[] `a_staff`</dt> <dd> The list of staff keys for the staff member conducting the session. For legacy third-party apps listed in {@link \Wl\Schedule\ClassList\ClassListApi::APPS_USE_OLD_K_STAFF}, contains <i>primary keys in {@link \RsStaffSql} table</i> `k_staff` for backward compatibility. Empty for all other applications. Use `a_staff_uid` instead. </dd> <dt>string[] `a_staff_uid`</dt> <dd> The list of staff user keys for the staff member conducting the session. <i>Primary keys in {@link \PassportLoginSql} table.</i> </dd> <dt>string[] `a_virtual_location`</dt> <dd> The list of virtual locations keys. Each value is a location key. <i>Primary keys in {@link \RsLocationSql} table.</i> </dd> <dt>string `dt_date`</dt> <dd>The date/time of the session start in UTC.</dd> <dt>string `dt_time`</dt> <dd>The time of the session start in the local time zone.</dd> <dt>string `dtl_date`</dt> <dd>The date/time of session start in the location''s time zone.</dd> <dt>bool `hide_application`</dt> <dd> Specifies whether the class will be hidden in the White Label Achieve Client App. If `true`, it means that the class won''t be displayed. Otherwise, this will be `false` to indicate that the class will be displayed. </dd> <dt>string `html_description`</dt> <dd>The class description.</dd> <dt>int `i_book`</dt> <dd>Count of visits on this class.</dd> <dt>int|null `i_capacity`</dt> <dd>The capacity of the service. ''null'' indicates that the capacity is not set.</dd> <dt>int `i_day`</dt> <dd>The day of the week when session is occurred. Constant from {@link \ADateWeekSid}.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_wait`</dt> <dd>Number of clients in wait list.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>If `true`, this class period was canceled. Otherwise, this will be `false`.</dd> <dt>bool `is_cancellation_enabled`</dt> <dd>`true` if clients can cancel this session. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>If `true`, this is an event. Otherwise, this will be `false`.</dd> <dt>bool `is_virtual`</dt> <dd>If `true`, this class is virtual. Otherwise, this will be `false`.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>This will be `true` if user is only on the wait-list. Otherwise, this will be `false`.</dd> <dt>string `k_class`</dt> <dd>The class key. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The key of the session''s location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_title`</dt> <dd>The title of the session.</dd> <dt>string `url_book`</dt> <dd>The direct link to start booking on the WellnessLiving website.</dd> </dl>'
                      is_timezone_different:
                        type: boolean
                        description: 'If `true`, the list of sessions contains sessions from different time zones. Otherwise, this will be `false`.'
                      is_virtual_service:
                        type: boolean
                        description: 'If `true`, there exists at least one virtual service by a specified {@link ClassListApi::$k_business} and {@link ClassListApi::$k_class_tab}, Otherwise, this will be `false`.'
      tags:
        - ClassList-ClassList
  '/Wl/Schedule/Page/PageElement.json':
    get:
      summary: 'Retrieves information about one element of schedule. Returns full details for a single visit, including staff, location, timing, assigned assets, virtual join URL, and class-specific or appointment-specific information. Used to render the visit detail page in the client''s schedule view.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business to which the visit belongs.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_visit_info:
                        type: array
                        description: 'Additional visit information about this appointment. Empty array if it''s not an appointment. <dl> <dt>int `id_visit`</dt> <dd>Visit status. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_confirmed`</dt> <dd>`true` means that appointment was requested and confirmed by the staff.</dd> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl>'
                      a_asset:
                        type: array
                        description: 'List of assets: . <dl> <dt>string `s_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> </dl>'
                      a_class_info:
                        type: [array, "null"]
                        description: 'Class data: <tt>null</tt> if it isn''t class visit. <dl> <dt>int `i_book_active`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_wait`</dt> <dd>A total number of booked visits in the waitlist.</dd> </dl>'
                      a_resource_image:
                        type: [array, "null"]
                        description: 'Resource image data. `null` for all services except resources. <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See more {@link \Core\Drive\DriveImage::image()}.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Is set only if `sid_image` equals to `shape`.</dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `sid_image`</dt> <dd>Image kind. String representation of one of {@link \Wl\Resource\Image\ImageSid} constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. String representation of one of {@link \Wl\Resource\Image\ImageIconSid} constants. Is set only if `sid_image` equals to `image`. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. String representation of one of {@link \Wl\Resource\Image\ImageShapeSid} constants. Is set only if `sid_image` equals to `shape`. </dd> <dt>string `url`</dt> <dd>Path to image. <i>See {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> </dl>'
                      a_staff:
                        type: array
                        description: 'A list of staff members involved in the visit. Each element is an array with the following elements: <dl> <dt>string `uid_staff`</dt> <dd>The staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `k_staff`</dt> <dd> Deprecated staff key. Returned only to applications in {@link \Wl\Schedule\Page\PageElementApi::APPS_USE_OLD_K_STAFF}. </dd> <dt>string `s_family`</dt> <dd>The surname of the staff member.</dd> <dt>string `s_name`</dt> <dd>The first name of the staff member.</dd> <dt>string `s_name_full`</dt> <dd>The full name of the staff member.</dd> </dl>'
                      dt_cancel:
                        type: string
                        description: 'The latest date and time for when the visit can be canceled without penalty.'
                      dt_date_global:
                        type: string
                        description: 'The date and time of the visit in UTC.'
                      dt_date_local:
                        type: string
                        description: 'The date and time of the visit in the local time zone.'
                      dtl_location:
                        type: string
                        description: 'Session date/time in timezone of the location where it takes place.'
                      html_description:
                        type: string
                        description: 'The description of the service.'
                      html_special:
                        type: string
                        description: 'The special instructions for the service.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Capacity of the service. <tt>null</tt> indicates that the capacity is not set.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The scheduled duration of the visit. <tt>0</tt> means that session duration is hidden.'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'Estimated place of reservation on the waiting list.'
                      id_note:
                        type: [number, "null"]
                        description: 'The note type ID. This will be set to `null` if notes aren''t allowed. This is one of the {@link \Wl\Visit\Note\Sid\NoteSid} constants. This will be `null` if notes aren''t allowed.'
                      id_virtual_provider:
                        type: [number, "null"]
                        description: 'The virtual provider ID. One of the {@link \Wl\Virtual\VirtualProviderSid} constants. This will be `null` for non-virtual services.'
                      id_visit:
                        type: number
                        format: int32
                        description: 'The visit type ID. One of the {@link VisitSid} constants. This will be `null` if not loaded yet.'
                      is_checkin:
                        type: boolean
                        description: 'If `true`, then this visit is ready to be checked in. If `false`, then this visit can''t be checked in.'
                      is_enable_client_cancel:
                        type: boolean
                        description: 'This will be `true` if clients can cancel the session. Otherwise, this will be `false`.'
                      is_event:
                        type: boolean
                        description: 'If `true`, then this visit is a part of a larger event. If `false`, then this visit is an individual session.'
                      is_in_progress:
                        type: boolean
                        description: '<tt>true</tt> - service is in progress; <tt>false</tt> - otherwise.'
                      is_virtual:
                        type: boolean
                        description: '<tt>true</tt> - service is virtual; <tt>false</tt> - otherwise.'
                      k_appointment:
                        type: [string, "null"]
                        description: 'The appointment key. This will be set only if the visit is an appointment. If the visit is a class or event, this will be `null`.'
                      k_class:
                        type: [string, "null"]
                        description: 'The class key. This will be set only if the visit is a class or an event. If the visit is an appointment, this will be `null`.'
                      k_class_period:
                        type: [string, "null"]
                        description: 'The class period key. This will be set only if the visit is a class or an event. If the visit is an appointment, this will be `null`.'
                      k_location:
                        type: string
                        description: 'The location key.'
                      k_resource:
                        type: [string, "null"]
                        description: 'Resource key.'
                      k_service:
                        type: [string, "null"]
                        description: 'Session key.'
                      s_title:
                        type: string
                        description: 'The name of class or service.'
                      text_location:
                        type: string
                        description: 'Location title. If the visit is assigned a resource whose category is {@link ResourceCategoryEnum::LOCATION}, this is that resource''s title instead of the actual location''s title.'
                      text_room:
                        type: string
                        description: 'Room where session takes place.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
                      uid:
                        type: string
                        description: 'The user key.'
                      url_image:
                        type: string
                        description: 'URL of the image representing the service or resource.'
                      url_virtual_join:
                        type: string
                        description: 'Url to wait page is the beginning of an online service.'
      tags:
        - Page-PageElement
  '/Wl/Schedule/Page/PageList.json':
    get:
      summary: 'Retrieves items of schedule for the client. Returns the client''s upcoming or past visits for a given business, ordered by date. Supports optional date range boundaries to retrieve visits within a specific window. Used to populate the schedule history and upcoming bookings pages in the client portal.'
      parameters:
        - name: dtu_end
          in: query
          description: 'If the date is set, a list of services before this date will be returned. The date and time is in UTC and in MySQL format. If left as `null`, then a list of services that aren''t limited to the end date will be returned.'
          schema:
            type: [string, "null"]
        - name: dtu_start
          in: query
          description: 'If the date is set, a list of services after this date will be returned. The date and time is in UTC and in MySQL format. If left as `null`, then a list of services that aren''t limited to the start date will be returned.'
          schema:
            type: [string, "null"]
        - name: is_past
          in: query
          description: 'If `true`, then all the client previous visits will be retrieved. If `false` or left as `null`, then all the client upcoming visits will be retrieved.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit:
                        type: array
                        description: 'Elements of user''s schedule. Every element has next keys: <dl> <dt>string `dtu_date`</dt> <dd>Date and time of the visit in UTC.</dd> <dt>string `k_business`</dt> <dd>Key of the business in which this visit was made. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_visit`</dt> <dd>Key of a book/visit. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>int `id_visit`</dt> <dd>Visit status ID. One of {@link \Wl\Visit\VisitSid} constants.</dd> </dl>'
      tags:
        - Page-PageList
  '/Wl/Schedule/Tab/Tab.json':
    get:
      summary: 'Gets information about tabs for page "Book now". Returns the set of service booking tabs configured by the business, filtered for the specified location and user. Used to populate the tab strip on the "Book Now" page or embedded widget. Tabs may represent classes, appointments, events, or other bookable services.'
      parameters:
        - name: is_full_list
          in: query
          description: 'Whether to return all tabs or only the tabs for which sessions are available.'
          schema:
            type: boolean
        - name: is_widget
          in: query
          description: 'Whether we are inside the widget or not.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the current business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the current location.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user. This isn''t used on the back end as the back end uses the current user to build a list of tabs. Therefore, the model must depend on the current user ID. If changed to a relative (the current user is changed, but the application isn''t restarted), information about the booking tabs for this user''s relative will be requested. This will be `null` if not set yet.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tab:
                        type: array
                        description: 'An array containing information about tabs to present to the user. Each array index corresponds to a tab. Each array element is an array that contains the following fields: <dl> <dt>int `id_class_tab_object`</dt> <dd>The tab type ID which is one of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>int|null `id_class_tab_system`</dt> <dd> If this tab has redefined a default system Class Tab then it references a constant defined in {@link \Wl\Classes\Tab\TabSid}. Otherwise, this will be `null`. </dd> <dt>string|null `k_class_tab`</dt> <dd>The class tab key. This will be `null` if it''s a system tab.</dd> <dt>string `k_id`</dt> <dd>A unique identifier in the list.</dd> <dt>string `s_title`</dt> <dd>The tab title.</dd> </dl>'
      tags:
        - Tab-Tab
  '/Wl/Book/Process/ProcessGroup.json':
    post:
      summary: 'Processes the group booking: validates input, collects payment, books sessions, and sends confirmation emails. Accepts a list of clients with their purchase items, resources, sessions, and quiz responses, then validates capacity and eligibility, processes payment for all clients at once, creates booking records, and dispatches confirmation emails. Returns visit keys, activity keys, and purchase activity key upon success. errors.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_client:
                  type: array
                  description: 'List of clients to book. Each value is an array with next keys: <dl> <dt>array[] `a_event_session`</dt> <dd> List of event sessions to book. Required for only for event that allows to select sessions to book. Each element has next keys: <dl> <dt>string `dtu_date`</dt> <dd>Session date/time.</dd> <dt>string `k_class_period`</dt> <dd>Session key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl> </dd> <dt>array[] `a_purchase_item`</dt> <dd> Purchase option information. Required if the service requires online payment. Elements have next keys: <dl> <dt>int `i_count`</dt> <dd>Number of options to purchase. Specify only if you want to pay a class booking by Drop-In.</dd> <dt>array `a_config`</dt> <dd> Additional configuration data. Required when `id_purchase_item` is {@link \RsPurchaseItemSid::TUITION}. Has the following keys: <dl> <dt>array[] `a_event_list`</dt> <dd> List of events to register for as part of this tuition. Each element is an array with the following keys: <dl> <dt>string `k_class`</dt> <dd> Event class key to be booked as part of this tuition. <i>Primary key in {@link \RsClassSql} table.</i> </dd> <dt>string `uid`</dt> <dd> Key of the user for whom this event should be booked. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> </dl> </dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd> Kind of option to purchase. One of {@link \RsPurchaseItemSid} constants. Specify only if you want to purchase a new option. </dd> <dt>bool `is_renew`</dt> <dd> `true` if you want to enable auto-renewal for new purchase option. `false` otherwise. Specify only if you want to purchase a new option. </dd> <dt>bool `is_owner`</dt> <dd> `true` if client is owner of this purchase option. This means that this purchase option will be purchased for this client, even if another client can share a similar purchase option. </dd> <dt>string `k_id`</dt> <dd><i>Primary key of option to purchase.</i> Specify only if you want to purchase a new option.</dd> <dt>string `k_login_prize`</dt> <dd> <i>Primary key of user''s prize in {@link \RsLoginPrizeSql} table.</i> Specify if you want to pay by user''s prize. </dd> <dt>string `k_reward_prize`</dt> <dd> <i>Primary key of a prize to redeem in {@link \RsRewardPrizeSql} table.</i> Specify if you want to redeem a prize for payment. </dd> <dt>string `s_signature`</dt> <dd> Signature of the client in base64 format. Specify only if you want to buy a purchase option that requires signature. </dd> </dl> </dd> <dt>string[] `a_quiz_response`</dt> <dd> Answers for questions. Required if the service requires answers for questions. Keys are quiz keys. <i>Primary key from the {@link \Core\Quiz\QuizSql} table.</i> Values are response keys. <i>Primary key from the {@link \Core\Quiz\Response\ResponseSql} table.</i> Or the {@link \Wl\Quiz\Response\QuizResponse::RESPONSE_SKIP} to skip the quiz. </dd> <dt>array `a_repeat`</dt> <dd> Recurring booking configuration: You can test this key only in a case of `1` client. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the session repeats. One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_duration`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> Asset information. Required if the service requires asset booking. Each element has next keys: <dl> <dt>int `i_index`</dt> <dd>Order number of the asset.</dd> <dt>string `k_resource`</dt> <dd>Asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `k_login_promotion`</dt> <dd> Login promotion key. Specify only if you want to pay by already purchased option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_login_promotion_guest_pass`</dt> <dd> Guest pass login promotion key. Specify only if you want to pay with guest pass granted by this login promotion. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd>Session pass to be used to book a session. <i>Primary key from {@link \Wl\Session\Pass\Sql}.</i></dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_pay_installment_template:
                  type: [string, "null"]
                  description: 'The installment template primary key. `null` to not use installment template.'
                text_discount_code:
                  type: string
                  description: 'The discount code to be applied to the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity_book:
                        type: array
                        description: 'Primary keys of users'' activity that correspond to bookings made.'
                      a_visit:
                        type: array
                        description: 'Primary keys of bookings made.'
                      k_login_activity_purchase:
                        type: [string, "null"]
                        description: 'The key of the user''s activity corresponding to the purchase made. `null` if no purchase was made.'
      tags:
        - Process-ProcessGroup
  '/Wl/Book/Process/Process.json':
    get:
      summary: 'Returns the ordered list of booking wizard steps for the given session and client. Evaluates the session, user profile, payment requirements, asset selection, and subscription state to build an ordered list of steps the client must complete to finish the booking. The first step in the list is marked as current, and additional flags about event type, wait-list availability, and payment options are returned.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_family_relation_login_allow:
                        type: [array, "null"]
                        description: 'Relationships who clients are allowed to book for. One of the {@link \RsFamilyRelationSid} constants. This will be `null` if clients aren''t allowed to book for their relationships.'
                      a_path:
                        type: array
                        description: 'All the steps to be performed to make a booking. Every element has the next keys: <dl> <dt>int `id_book_process`</dt> <dd>The step ID. One of the {@link \Wl\Book\Process\ProcessSpaSid} constants.</dd> <dt>bool `is_current`</dt> <dd><tt>true</tt> - this item is current. <tt>false</tt> - this item isn''t current or not set yet.</dd> </dl>'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      is_age_require:
                        type: boolean
                        description: '`true` if this class has age restriction and requires user to specify age. `false` otherwise.'
                      is_card_authorize:
                        type: boolean
                        description: 'Determines if the client must authorize the credit card.'
                      is_event:
                        type: boolean
                        description: 'If `true`, the session being booked is an event. Otherwise, this will be `false`.'
                      is_family_relation_book:
                        type: boolean
                        description: 'Determines whether clients are allowed to book for their relationships without switching profiles.'
                      is_free:
                        type: boolean
                        description: 'Determines whether the class/event is free (price).'
                      is_have_ach:
                        type: boolean
                        description: '`true` if the client has an ach account, `false` otherwise.'
                      is_have_credit_card:
                        type: boolean
                        description: 'If `true`, the client has a credit card. Otherwise, this will be `false`.'
                      is_session:
                        type: boolean
                        description: '`true` - the client can select several sessions per booking. `false` - the client can''t select several sessions.'
                      is_wait:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list. Otherwise, this will be `false`.'
                      is_wait_list_unpaid:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list without payment, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'The key of the location where the session is booked.'
      tags:
        - Process-Process
  '/Wl/Book/Process/Process54.json':
    get:
      summary: 'Returns the ordered list of booking wizard steps, including the quiz step when required. Delegates to the parent implementation and then appends the quiz (or document) step when the class or event requires it, adds the detail step for Client Web App flows, and re-sorts the path into the correct wizard order.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_family_relation_login_allow:
                        type: [array, "null"]
                        description: 'Relationships who clients are allowed to book for. One of the {@link \RsFamilyRelationSid} constants. This will be `null` if clients aren''t allowed to book for their relationships.'
                      a_path:
                        type: array
                        description: 'All the steps to be performed to make a booking. Every element has the next keys: <dl> <dt>int `id_book_process`</dt> <dd>The step ID. One of the {@link \Wl\Book\Process\ProcessSpaSid} constants.</dd> <dt>bool `is_current`</dt> <dd><tt>true</tt> - this item is current. <tt>false</tt> - this item isn''t current or not set yet.</dd> </dl>'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      is_age_require:
                        type: boolean
                        description: '`true` if this class has age restriction and requires user to specify age. `false` otherwise.'
                      is_card_authorize:
                        type: boolean
                        description: 'Determines if the client must authorize the credit card.'
                      is_event:
                        type: boolean
                        description: 'If `true`, the session being booked is an event. Otherwise, this will be `false`.'
                      is_family_relation_book:
                        type: boolean
                        description: 'Determines whether clients are allowed to book for their relationships without switching profiles.'
                      is_free:
                        type: boolean
                        description: 'Determines whether the class/event is free (price).'
                      is_have_ach:
                        type: boolean
                        description: '`true` if the client has an ach account, `false` otherwise.'
                      is_have_credit_card:
                        type: boolean
                        description: 'If `true`, the client has a credit card. Otherwise, this will be `false`.'
                      is_session:
                        type: boolean
                        description: '`true` - the client can select several sessions per booking. `false` - the client can''t select several sessions.'
                      is_wait:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list. Otherwise, this will be `false`.'
                      is_wait_list_unpaid:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list without payment, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'The key of the location where the session is booked.'
      tags:
        - Process-Process54
  '/Wl/Book/Process/Process59.json':
    get:
      summary: 'Returns the booking wizard steps, adjusting the path when the service is already booked for a family member. Delegates to the parent implementation and then, when family-relation booking is enabled and the service is already booked for the selected relative, trims the wizard path down to only the relation and detail steps.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_family_relation_login_allow:
                        type: [array, "null"]
                        description: 'Relationships who clients are allowed to book for. One of the {@link \RsFamilyRelationSid} constants. This will be `null` if clients aren''t allowed to book for their relationships.'
                      a_path:
                        type: array
                        description: 'All the steps to be performed to make a booking. Every element has the next keys: <dl> <dt>int `id_book_process`</dt> <dd>The step ID. One of the {@link \Wl\Book\Process\ProcessSpaSid} constants.</dd> <dt>bool `is_current`</dt> <dd><tt>true</tt> - this item is current. <tt>false</tt> - this item isn''t current or not set yet.</dd> </dl>'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      is_age_require:
                        type: boolean
                        description: '`true` if this class has age restriction and requires user to specify age. `false` otherwise.'
                      is_card_authorize:
                        type: boolean
                        description: 'Determines if the client must authorize the credit card.'
                      is_event:
                        type: boolean
                        description: 'If `true`, the session being booked is an event. Otherwise, this will be `false`.'
                      is_family_relation_book:
                        type: boolean
                        description: 'Determines whether clients are allowed to book for their relationships without switching profiles.'
                      is_free:
                        type: boolean
                        description: 'Determines whether the class/event is free (price).'
                      is_have_ach:
                        type: boolean
                        description: '`true` if the client has an ach account, `false` otherwise.'
                      is_have_credit_card:
                        type: boolean
                        description: 'If `true`, the client has a credit card. Otherwise, this will be `false`.'
                      is_session:
                        type: boolean
                        description: '`true` - the client can select several sessions per booking. `false` - the client can''t select several sessions.'
                      is_wait:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list. Otherwise, this will be `false`.'
                      is_wait_list_unpaid:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list without payment, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'The key of the location where the session is booked.'
      tags:
        - Process-Process59
  '/Wl/Book/Cancel/CancelCan.json':
    get:
      summary: 'Returns information about whether the given user can cancel the booking and the expected consequences. Accepts a business key and a visit key, validates access, and performs a dry-run cancellation to determine whether the visit can be cancelled online, whether a late-cancel penalty applies, and whether the purchase option credit would be refunded.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business within which the action is performed.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_penalty:
                        type: [array, "null"]
                        description: 'Penalty data. `null` if penalty must be not applied. <dl> <dt>bool `is_flat`</dt> <dd>`true` in a case of flat penalty type; `false` in a case of percentage penalty type.</dd> <dt>string `k_currency`</dt> <dd>Currency key. <i>Primary key in {@link \RsCurrencySql} table.</i></dd> <dt>string `m_amount`</dt> <dd>Penalty amount.</dd> </dl>'
                      can_cancel:
                        type: boolean
                        description: '`true` if the booking can be canceled online by the specified user, `false` otherwise. Cancellation is possible only if the current visit status is {@link VisitSid::BOOK} or {@link VisitSid::WAIT}.'
                      is_flag:
                        type: boolean
                        description: '`true` if the client''s account will be flagged instead of charging a monetary fee, `false` otherwise. Meaningful only when {@link CancelCanApi::$is_late} is `true`.'
                      is_late:
                        type: boolean
                        description: '`true` if the cancellation would be considered a late cancel, `false` otherwise. Late cancel applies only to bookings with status {@link VisitSid::BOOK}. Wait-list bookings ({@link VisitSid::WAIT}) are never subject to late cancellation rules.'
                      is_refund:
                        type: boolean
                        description: '`true` if the visit credit (from the purchase option used to book) will be returned to the user''s profile after cancellation, `false` otherwise. For regular (non-late) cancellations, the credit is always returned when the booking was made with a purchase option. For late cancellations, return depends on the business''s Payment Return Policy.'
      tags:
        - Cancel-CancelCan
  '/Wl/Profile/Attendance/AttendanceOverlap.json':
    get:
      summary: 'Returns a list of visits that overlap with the specified service, class, resource, or time data. Checks whether the specified user has any existing bookings that overlap with a given time range or service. Used before scheduling to detect conflicts and prompt staff or the client with a warning.'
      parameters:
        - name: dtu_date
          in: query
          description: 'Date of a selected service.'
          schema:
            type: [string, "null"]
        - name: i_duration
          in: query
          description: 'Duration of a service.'
          schema:
            type: number
            format: int32
        - name: is_appointment
          in: query
          description: 'Whether an asset is booking. Needed in case when a client is allowed to select a date and time, then the available asset.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Primary key of the business to add the user into.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'Asset key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit_list:
                        type: array
                        description: 'List of visits that overlap with the specified data. <dl> <dt>string `dtu_date`</dt> <dd>Date and time of the visit.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_end`</dt> <dd>End datetime of the visit in unix format.</dd> <dt>int `i_end_local`</dt> <dd>Local end datetime of the visit in unix format.</dd> <dt>int `i_start`</dt> <dd>Start datetime of the visit in unix format.</dd> <dt>int `i_start_local`</dt> <dd>Local start datetime of the visit in unix format.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>Class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_enrollment_book`</dt> <dd>Enrollment book key. <i>Primary key in {@link \RsEnrollmentBookSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `text_date`</dt> <dd>Local date of the visit, formatted according to the business locale.</dd> <dt>string `text_time_end`</dt> <dd>Local end time of the visit, formatted according to the business locale.</dd> <dt>string `text_time_start`</dt> <dd>Local start time of the visit, formatted according to the business locale.</dd> <dt>string `text_timezone`</dt> <dd>Name of the timezone used to format `text_date`, `text_time_end` and `text_time_start`.</dd> <dt>string `text_title`</dt> <dd>Title of a service</dd> </dl>'
                      is_overlap:
                        type: boolean
                        description: 'Whether at least one overlap exists.'
      tags:
        - Attendance-AttendanceOverlap
  '/Wl/Profile/Attendance/AttendanceOverlapList.json':
    post:
      summary: 'Checks whether the specified user has any existing bookings that overlap with a given time range or service. Used before scheduling to detect conflicts and prompt staff or the client with a warning.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_session_list:
                  type: array
                  description: 'List of sessions to check for overlaps. Every element has the following structure: <dl> <dt>string `dtu_date`</dt> <dd>Date and time of the service.</dd> <dt>int `i_duration`</dt> <dd>Duration of the service in minutes. Required if `k_resource` is not empty.</dd> <dt>bool `is_appointment`</dt> <dd> Whether an appointment (asset) is booking. Needed in case when a client is allowed to select a date and time, then the available asset. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>Class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_timezone`</dt> <dd>Timezone key. <i>Primary key in {@link \AGeoTimezoneSql} table.</i></dd> </dl>'
                k_business:
                  type: string
                  description: 'Business key.'
                uid:
                  type: string
                  description: 'UID of a user to book for.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date_overlap:
                        type: array
                        description: 'Overlap result for every checked session. Key is `i` from {@link AttendanceOverlapListApi::$a_session_list}. Value has the following keys: <dl> <dt>array[] `a_visit_list`</dt> <dd> List of visits that overlap with the checked session. Same structure as {@link \Wl\Profile\Attendance\AttendanceOverlapApi::$a_visit_list}. Present only if the current user has full access to the target user''s visits (the target user itself or a family relative). Each element has the following keys: Empty for guest-level access, to avoid exposing another member''s visit details. <dl> <dt>string `dtu_date`</dt> <dd>Date and time of the visit.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_end`</dt> <dd>End datetime of the visit in unix format.</dd> <dt>int `i_end_local`</dt> <dd>Local end datetime of the visit in unix format.</dd> <dt>int `i_start`</dt> <dd>Start datetime of the visit in unix format.</dd> <dt>int `i_start_local`</dt> <dd>Local start datetime of the visit in unix format.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>Class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_enrollment_book`</dt> <dd>Enrollment book key. <i>Primary key in {@link \RsEnrollmentBookSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `text_time_end`</dt> <dd>Local end time of the visit, formatted according to the business locale.</dd> <dt>string `text_time_start`</dt> <dd>Local start time of the visit, formatted according to the business locale.</dd> <dt>string `text_title`</dt> <dd>Title of a service</dd> </dl> </dd> <dt>bool `is_overlap`</dt> <dd>`true` if the checked session overlaps with an already booked visit, `false` otherwise.</dd> </dl>'
      tags:
        - Attendance-AttendanceOverlapList
  '/Wl/Profile/Purchase/PurchaseElement.json':
    get:
      summary: 'Retrieves information about 1 purchase item. Loads the full details of a single purchase item for display on the client profile, including pricing, discounts, taxes, refunds, and current status. The item can be identified by purchase item key, redemption code, or login promotion key.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key. Currently used only with {@link \Wl\Profile\Purchase\PurchaseElementApi::$k_session_pass} variable.'
          schema:
            type: [string, "null"]
        - name: k_code
          in: query
          description: 'The key of the redemption code used to obtain some goods. This should be specified only for items obtained via redemption code (not for ordinary purchases or for purchased promotions without purchases). This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_enrollment_book
          in: query
          description: 'The key of an entirely booked event. This must be specified if the purchased item is a whole event and a component of a package.'
          schema:
            type: string
        - name: k_login_product
          in: query
          description: 'The key of a purchased product. This must be specified if the purchased item is a product and a component of a package.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The key of the user''s promotion. This should only be specified for promotions given without a purchase. This isn''t for ordinary purchases or for promotions obtained via redemption codes. This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the purchased item. This should be specified only for ordinary purchases, not for those obtained via redemption codes or as a result of an old import.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'The key of the make-up session used to attend an event.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_component:
                        type: array
                        description: 'List of components (not empty if this purchase element is a package). Every element has keys: <dl> <dt>string `k_enrollment_book`</dt> <dd>Key of enrollment book. <i>Primary key in {@link \RsEnrollmentBookSql} table.</i></dd> <dt>string `k_login_product`</dt> <dd>Key of product. <i>Primary key in {@link \RsLoginProductSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd>Key of promotion. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Session key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> </dl>'
                      a_logo:
                        type: array
                        description: 'An array containing information about the image of the purchased item. Every element has the following keys: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>If `true`, then the purchased item doesn''t have image. If `false`, then the item has an image.</dd> <dt>string `s_url`</dt> <dd>The URL for the image.</dd> </dl>'
                      a_restrict:
                        type: array
                        description: 'This field is used only for promotions. It contains restrictions that will apply to bookings made with this promotion. Every element has the following keys: <dl> <dt>int `i_limit`</dt> <dd>The number of sessions that may be booked during this period.</dd> <dt>int `i_remain`</dt> <dd>The number of sessions that remain available.</dd> <dt>string `s_date`</dt> <dd>The name of the period type.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'The list of taxes paid for the purchased item. Every element has the following fields: <dl> <dt>string `f_tax_discount`</dt> <dd>The amount of tax.</dd> <dt>string `s_tax`</dt> <dd>The name of the tax.</dd> </dl>'
                      can_renew:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion and the user can control the renewal of the promotion. Otherwise, this will be `false`.'
                      dl_purchase:
                        type: string
                        description: 'Local date of purchase in MySQL format.'
                      dl_start:
                        type: string
                        description: 'The start date of the promotion. This is used only if the purchased item is a promotion.'
                      dt_end:
                        type: string
                        description: 'The expiration date of the promotion. This is used only if the purchased item is a promotion.'
                      dt_hold_end:
                        type: string
                        description: 'The end date of the promotion hold. This is used only for promotions on hold. This may be empty if the hold is internal.'
                      dt_hold_start:
                        type: string
                        description: 'The start date of the promotion hold. This is used only for promotions on hold.'
                      dt_purchase:
                        type: string
                        description: 'The purchase date of the purchased item. This is given in GMT in MySQL format.'
                      dt_send:
                        type: string
                        description: 'The date for when the email regarding the gift card should be sent (or was sent) to the receiver. This is only used for gift cards.'
                      f_discount_login_type_percent:
                        type: number
                        format: float
                        description: 'The percentage value of the discount for the client type. This is empty if this discount wasn''t applied.'
                      html_description:
                        type: string
                        description: 'The description of the purchased item.'
                      i_book:
                        type: number
                        format: int32
                        description: 'The number of sessions remaining for a promotion. This is used only for promotions. This will be `0` if the purchased item isn''t a promotion or if there are no remaining sessions.'
                      i_buy:
                        type: number
                        format: int32
                        description: 'The number of purchased items bought at the time of purchase. This field will be `0` for purchase items obtained via a redemption code or as a result of an old import.'
                      i_discount_count:
                        type: number
                        format: int32
                        description: 'The percentage value of the discount given by a reward prize. This field will be `0` if a discount wasn''t applied.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'This field is only added for promotions. Duration of promotion. Measurement unit is <var>id_duration</var>.'
                      i_left:
                        type: number
                        format: int32
                        description: 'The number of bookings remaining in this promotion. This value is used only for non-sequential promotions.'
                      i_limit:
                        type: number
                        format: int32
                        description: 'The number of visits which can be made with this promotion. This is used only for promotions.'
                      i_payment_period:
                        type: number
                        format: int32
                        description: 'The duration of the regular payments interval. This is used only for "membership" type promotions.'
                      i_remain:
                        type: number
                        format: int32
                        description: 'The number of remaining bookings for the promotion. This is used only for promotions.'
                      i_remain_duration:
                        type: number
                        format: int32
                        description: 'The number of minutes remaining for the promotion. This is used only for promotions that work with a limited number of minutes.'
                      i_renew:
                        type: number
                        format: int32
                        description: 'Not <tt>0</tt> only for promotions. How many times promotion was updated.'
                      i_use:
                        type: number
                        format: int32
                        description: 'This is used only for promotions. This is the number of visits that were attended for the promotion.'
                      id_activation:
                        type: number
                        format: int32
                        description: 'Activation mode. One on {@link \RsActivationSid} constants. This field is only added for promotions.'
                      id_discount_commission_type:
                        type: number
                        format: int32
                        description: 'The type of discount given via a reward prize. One on the {@link \RsCommissionTypeSid} constants. This will be empty if this discount wasn''t applied.'
                      id_duration:
                        type: number
                        format: int32
                        description: 'The type of duration for the promotion. This is used only for "membership" type promotions. This determines whether the promotion lasts indefinitely, has a fixed duration, or ends on a specific date. One of the {@link \RsDurationTypeSid} constants.'
                      id_program_type:
                        type: number
                        format: int32
                        description: 'The ID of the promotion program. One of the {@link \RsProgramTypeSid} constants. This is used only for promotions.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'The ID of the purchase item type. One of the {@link \RsPurchaseItemSid} constants.'
                      id_sale:
                        type: number
                        format: int32
                        description: 'The ID of the sale category. One of the {@link \RsSaleSid} constants.'
                      is_active:
                        type: boolean
                        description: 'If `true`, then the purchase item is active. If `false`, then the purchase item isn''t active.'
                      is_component:
                        type: boolean
                        description: 'If `true`, then the purchased item is a component of another purchase item, as is the case of a package. If `false`, then the purchased item isn''t a component of another purchase item.'
                      is_convert:
                        type: boolean
                        description: 'If `true`, then this promotion will be converted to a different promotion after it expires. It `false`, then the promotion won''t be converted to a different promotion after it expires. This can only be true for certain promotions.'
                      is_expire:
                        type: boolean
                        description: 'This is `true` only if the purchased item is an expired promotion. Otherwise, this will be `false`.'
                      is_hold:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion placed on hold. Otherwise, this will be `false`.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the usage of this purchased item is disabled. If `false`, then the usage of this purchased item is enabled.'
                      is_login_type_member:
                        type: [boolean, "null"]
                        description: 'If `true`, then the discount was applied for the member type. If `false`, then the discount was applied for the pass holder type. If `null`, then the discount was applied for the prospect type (this won''t be `null` only if a discount based on a client type was applied).'
                      is_mail:
                        type: boolean
                        description: 'If `true`, then the gift card email notification will be sent to the receiver. This is `true` only if the purchased item is a gift card. Otherwise, this will be `false`.'
                      is_past:
                        type: boolean
                        description: 'If `true`, then the gift card email notification has been sent. This can only be `true` if the purchased item is a gift card. Otherwise, this will be `false`.'
                      is_redeem:
                        type: boolean
                        description: 'This is `true` if the gift card has been redeemed. This can only be true if the purchased item is a gift card. Otherwise, this will be `false`.'
                      is_renew:
                        type: boolean
                        description: 'This is `true` if the promotion is auto-renewable. This can only be true if the purchased item is a promotion. Otherwise, this will be `false`.'
                      is_transfer:
                        type: boolean
                        description: 'This is `true` if the promotion has been transferred. This can only be `true` if the purchased item is a promotion. This is `false` if the promotion hasn''t been transferred or in all other cases.'
                      k_code_result:
                        type: string
                        description: 'The key of the redemption code. This won''t be `0` if the purchased item is obtained via a redemption code or if it''s a component of a package which is obtained via a redemption code.'
                      k_currency:
                        type: string
                        description: 'The key of the currency used to make the purchase.'
                      k_login_promotion_result:
                        type: string
                        description: 'The key of the purchased promotion. This won''t be `0` only for promotions.'
                      k_purchase_result:
                        type: string
                        description: 'The key of the whole purchase which includes the given purchased item.'
                      m_cost_item:
                        type: string
                        description: 'The cost of the purchased item.'
                      m_cost_total:
                        type: string
                        description: 'The total cost of the purchase, including taxes and fees.'
                      m_discount_amount:
                        type: string
                        description: 'The amount of the discount given via a reward prize. This will be empty if the discount wasn''t applied.'
                      m_discount_login_type_amount:
                        type: string
                        description: 'The amount of the discount for the client type. This will be empty if the discount wasn''t applied.'
                      m_discount_manual:
                        type: string
                        description: 'Amount of manual discount. Empty if this discount was not applied.'
                      m_period_price:
                        type: string
                        description: 'The amount to pay for each payment period. This is used only for promotions with the "membership" program type.'
                      m_price:
                        type: string
                        description: 'Source price of item.'
                      m_refund:
                        type: string
                        description: 'The amount refunded. This will be `0` if there are no refunds.'
                      s_code:
                        type: string
                        description: 'The redemption code. This is used only if the purchase is bought using a redemption code.'
                      s_gift_code:
                        type: string
                        description: 'The code to redeem gift card. This won''t be empty only for gift cards. This can be auto-generated or custom (entered manually by the staff member during purchase). The max length is 20 characters, which can include both numbers and letters.'
                      s_gift_recipient:
                        type: string
                        description: 'Name of gift recipient. This won''t be empty only for gift cards.'
                      s_limit_actor:
                        type: string
                        description: 'The name of the staff member who disabled the usage of the purchased item. This will be empty if the purchased item is enabled.'
                      s_mail:
                        type: string
                        description: 'The email address the gift card was sent to. This is used only for gift cards.'
                      s_message:
                        type: string
                        description: 'The text of the message sent to the gift card recipient. This is used only for gift cards. This will be empty if an email notification shouldn''t be sent.'
                      s_payer:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_promotion_convert:
                        type: string
                        description: 'The title of the converted promotion. This is used only for promotions which must be converted to another promotion after expiration.'
                      s_purchase:
                        type: string
                        description: 'The purchase ID number as it appears on the receipt. This value will also include the leading 0s for the ID number.'
                      s_recipient:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_sender:
                        type: string
                        description: 'The name of the sender. This is used only for gift cards.'
                      s_title:
                        type: string
                        description: 'The name of the purchased item.'
                      sid_value:
                        type: string
                        description: 'The string ID of the purchased item. This ID may be used to change the view of the purchased item.'
                      uid_payer:
                        type: string
                        description: 'The key of the user who paid for this purchase.'
                      uid_recipient:
                        type: string
                        description: 'The key of the user who paid for this purchase.'
      tags:
        - Purchase-PurchaseElement
  '/Wl/Profile/Purchase/PurchaseList.json':
    get:
      summary: 'Retrieves a list of user''s purchase items to show in user profile. Returns the complete purchase history for the client in the given business, covering all item types such as memberships, redemption codes, enrollments, products, appointments, and gift cards. Package components are resolved and included inline beside their parent item.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase:
                        type: array
                        description: 'A list of purchased items. Every element contains a sub-array with the following fields: <dl> <dt>bool[] `a_active`</dt> <dd> The value of <var>a_purchase[''is_active'']</var>. For packages, this also contains the values of <var>a_purchase[''is_active'']</var> of every component. </dd> <dt>int[] `a_sale`</dt> <dd> The value of <var>a_purchase[''id_sale'']</var>. For packages, this also contains the values of <var>a_purchase[''id_sale'']</var> of every component. </dd> <dt>string `dt_add`</dt> <dd>The date that the purchase was added.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase type. One of the constants {@link \RsPurchaseItemSid}.</dd> <dt>int `id_sale`</dt> <dd>The ID of the sale category in the store. One of the constants {@link \RsSaleSid}.</dd> <dt>bool `is_active`</dt> <dd>If `true`, then the purchase item is active. Otherwise, this will be `false`.</dd> <dt>bool `is_component`</dt> <dd>If `true`, then the purchase item is a package component. Otherwise, this will be `false`.</dd> <dt>bool `is_package`</dt> <dd>If `true`, then the purchase item is a package, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key from {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_code`</dt> <dd> The redemption code key that was used to make a purchase. This is used only if <var>k_login_promotion</var> and <var>k_purchase</var> are empty. </dd> <dt>string `k_enrollment_book`</dt> <dd> The key of an event that requires clients to book every session at once. This is used only if the event is part of a package. </dd> <dt>string `k_id`</dt> <dd> The key of the purchase type referring to different types of keys depending on the value of <var>id_sale</var>. </dd> <dt>string `k_login_product`</dt> <dd>The key of the purchased product. This is used only if the event is part of a package.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the user''s existing purchase options. This is used only if `k_code` and `k_purchase` are empty.</dd> <dt>string `k_purchase`</dt> <dd> The key of a purchase where no special case rules are in effect. This is used only used if `k_code` is empty. </dd> <dt>string `k_purchase_item`</dt> <dd> The key of a purchase item where no special case rules are in effect. This is used only if <var>k_code</var> is empty. </dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd> In certain cases a session can be canceled and makeup sessions can be granted to a client in lieu of other compensation. This is the key of one of the makeup sessions used to attend an event. This is also present for a repeat purchase of an event. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `s_title`</dt> <dd>The name of the purchase item.</dd> <dt>string `uid`</dt> <dd>The ID of the purchase owner. <i>Primary key {@link \PassportLoginSql} in table.</i></dd> </dl>'
      tags:
        - Purchase-PurchaseList
  '/Wl/Profile/PurchaseList/PurchaseListElement.json':
    get:
      summary: 'Retrieves information about 1 purchase item. Loads the full details of a single purchase item for display on the client profile, including pricing, discounts, taxes, refunds, and current status. The item can be identified by purchase item key, redemption code, or login promotion key.'
      parameters:
        - name: i_image_height
          in: query
          description: 'Image Height in pixels. Please specify this value if you need purchase image to be returned in specific size. In case this value is not specified returned image will have default purchase size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image Width in pixels. Please specify this value if you need purchase image to be returned in specific size. In case this value is not specified returned image will have default purchase size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key. Used with {@link PurchaseListElementApi::$k_session_pass} variable and also with gift card components.'
          schema:
            type: [string, "null"]
        - name: k_code
          in: query
          description: 'The ID of the redemption code used to obtain the item. This should be specified only for items obtained via redemption code (not for ordinary purchases or for purchased promotions without purchases). This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_enrollment_book
          in: query
          description: 'The key of an entirely booked event. This must be specified if the purchased item is a whole event and part of a package.'
          schema:
            type: string
        - name: k_login_product
          in: query
          description: 'The key of a purchased product. This must be specified if the purchased item is a product and part of a package.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The key of the user''s promotion. This should only be specified for promotions given without a purchase. This isn''t for ordinary purchases or for promotions obtained via redemption codes. This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the purchased item. This should be specified only for ordinary purchases and not for purchases obtained via redemption codes or as a result of an old import.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'The ID of the makeup session used to attend an event. This must be specified if the purchase item is a component of a package and a whole event booking. And only for items which aren''t components of package.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_component:
                        type: array
                        description: 'A list of components. This won''t be empty if this purchase element is a package or a coupon. In the case of a package, every element has keys: <dl> <dt>string `k_enrollment_book`</dt> <dd>The booking enrollment key.</dd> <dt>string `k_login_product`</dt> <dd>The key referencing the product a user purchased.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of a user''s Purchase Option.</dd> <dt>string `k_session_pass`</dt> <dd>The key of a user''s session pass.</dd> </dl> <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item type. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>ID of the item. Will be different for different {@link \RsPurchaseItemSid} constants.</dd> <dt>string `text_title`</dt> <dd>Title of the coupon component.</dd> </dl>'
                      a_logo:
                        type: [array, "null"]
                        description: 'An array containing information about the image of the purchased item. Every element has the following keys: `null` in a case when the item has no logo. <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>If `true`, then the purchased item doesn''t have an image. If `false`, then the item has an image.</dd> </dl>'
                      a_restrict:
                        type: array
                        description: 'This field is used only for promotions. It contains restrictions that will apply to bookings made with this promotion. Every element has the following keys: This will be an empty array for other purchased items. <dl> <dt>int `i_limit`</dt> <dd>The number of sessions that may be booked during this period.</dd> <dt>int `i_remain`</dt> <dd>The number of sessions that remain available.</dd> <dt>string `text_date`</dt> <dd>The name of the period type.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'The list of taxes paid for the purchased item. Every element has the following fields: <dl> <dt>string `f_tax_discount`</dt> <dd>The amount of tax.</dd> <dt>string `s_tax`</dt> <dd>The name of the tax.</dd> </dl>'
                      can_renew:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion and the user can control the renewal of the promotion. Otherwise, this will be `false`.'
                      dl_cancel:
                        type: string
                        description: 'The cancellation date of the promotion. Only available if the item is a promotion and has been canceled.'
                      dl_end:
                        type: string
                        description: 'The expiration date of the promotion. Only available if the item is a promotion.'
                      dl_purchase:
                        type: string
                        description: 'The local date of the purchase in MySQL format.'
                      dl_send:
                        type: string
                        description: 'The local date in MySQL format when mail about purchasing was sent (or will be sent) to the user. This won''t be empty only for gift cards.'
                      dl_start:
                        type: string
                        description: 'The start date of the promotion. Only available if the item is a promotion.'
                      dt_cancel:
                        type: string
                        description: 'The local date of cancellation in MySQL format. Only available if the item is a membership.'
                      dt_hold_end:
                        type: string
                        description: 'The end date of the promotion hold. This is used only for promotions on hold. This value may be empty if the hold is eternal.'
                      dt_hold_start:
                        type: string
                        description: 'The start date of the promotion hold. Only available if the item is a promotion and on hold.'
                      dt_purchase:
                        type: string
                        description: 'The purchase date of the purchased item. This is given in UTC in MySQL format.'
                      dt_redeem:
                        type: string
                        description: 'The date in MySQL format when gift card was redeemed. Only available if the item is a gift card.'
                      f_discount_login_type_percent:
                        type: number
                        format: float
                        description: 'The percentage value of the discount for the client type. This will be empty if a discount wasn''t applied.'
                      html_description:
                        type: string
                        description: 'The description of the purchased item.'
                      i_book:
                        type: number
                        format: int32
                        description: 'The number of sessions remaining for a promotion. This value is used only for promotions. This value will be `0` if the purchased item isn''t a promotion or if there are no remaining sessions.'
                      i_book_duration:
                        type: number
                        format: int32
                        description: 'Displays the number of minutes of sessions that were booked by this promotion. This isn''t `0` only for promotions.'
                      i_buy:
                        type: number
                        format: int32
                        description: 'The number of purchased items bought at the time of purchase. This will be `0` for purchase items obtained via a redemption code or as a result of an import.'
                      i_client_cancel_notice_days:
                        type: number
                        format: int32
                        description: 'The number of days notice before the client is able to set cancel date.'
                      i_discount_count:
                        type: number
                        format: int32
                        description: 'The percentage value of the discount given by a reward prize. This will be `0` if a discount wasn''t applied.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The duration of the promotion. The measurement unit is <var>id_duration</var>. This field is only added for promotions.'
                      i_left:
                        type: number
                        format: int32
                        description: 'The number of bookings left with this promotion. This value is used only for non-sequential promotions.'
                      i_limit:
                        type: number
                        format: int32
                        description: 'The number of visits that can be made with this promotion. This value is used only for promotions.'
                      i_payment_period:
                        type: number
                        format: int32
                        description: 'The interval duration of the regular payments. This value is used only for membership-type promotions.'
                      i_remain:
                        type: number
                        format: int32
                        description: 'The number of remaining bookings for the promotion. This is used only for promotions.'
                      i_remain_duration:
                        type: number
                        format: int32
                        description: 'The number of minutes remaining for the promotion. This is used only for promotions that work with a limited number of minutes.'
                      i_renew:
                        type: number
                        format: int32
                        description: 'Displays how many times promotion was updated. This isn''t `0` only for promotions.'
                      i_use:
                        type: number
                        format: int32
                        description: 'The number of visits that were attended for the promotion. This is used only for promotions.'
                      i_use_duration:
                        type: number
                        format: int32
                        description: 'The number of minutes the user has already used with this purchase of this promotion. This isn''t `0` only for promotions.'
                      id_activation:
                        type: number
                        format: int32
                        description: 'The activation mode. One of the {@link \RsActivationSid} constants. This field is only added for promotions.'
                      id_discount_commission_type:
                        type: number
                        format: int32
                        description: 'The type of discount given via a reward prize. One of the {@link \RsCommissionTypeSid} constants. This will be empty if this discount wasn''t applied.'
                      id_duration:
                        type: number
                        format: int32
                        description: 'The type of duration for the promotion. This is used only for membership-type promotions. This determines whether the promotion lasts indefinitely, has a fixed duration, or ends on a specific date. The measurement unit for key <var>i_payment_period</var>. One of the {@link \RsDurationTypeSid} constants.'
                      id_program_type:
                        type: number
                        format: int32
                        description: 'The ID of the promotion program. This is used only for promotions. The measurement unit for key <var>i_payment_period</var>. The ID of promotion program type. One of the {@link \RsProgramTypeSid} constants.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'The ID of the purchase item type. The measurement unit for key <var>i_payment_period</var>. The ID of purchase item type. One of the {@link \RsPurchaseItemSid} constants.'
                      id_sale:
                        type: number
                        format: int32
                        description: 'The ID of the sale category. One of the constants {@link \RsSaleSid}.'
                      is_active:
                        type: boolean
                        description: 'If `true`, then the purchase item is active. If `false`, then the purchase item isn''t active.'
                      is_asset:
                        type: boolean
                        description: 'Whether the purchase item is asset.'
                      is_cancel_pending:
                        type: boolean
                        description: 'If `true`, then a promotion is pending cancellation. Otherwise, this will be `false`.'
                      is_client_cancel_allow:
                        type: boolean
                        description: 'If `true`, then a promotion can be canceled by a client. Otherwise, this will be `false`.'
                      is_component:
                        type: boolean
                        description: 'If `true`, then the purchased item is a component of another purchase item, as is the case of a package. If `false`, then the purchased item isn''t a component of another purchase item.'
                      is_convert:
                        type: boolean
                        description: 'If `true`, then this promotion will be converted to a different promotion after it expires. If `false`, then the promotion won''t be converted to a different promotion after it expires. This can only be true for certain promotions.'
                      is_event:
                        type: boolean
                        description: '`true` if the purchase item is an event; `false` otherwise.'
                      is_expire:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion that has expired. Otherwise, this will be `false` in all other cases.'
                      is_hold:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion and has been placed on hold. Otherwise, this is `false` in all other cases.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the usage of this purchased item is disabled. If `false`, then the usage of this purchased item is enabled.'
                      is_login_type_member:
                        type: [boolean, "null"]
                        description: 'If `true`, then the discount was applied for the member type. If `false`, then the discount was applied for the pass holder type. If `null`, then the discount was applied for the prospect type. This won''t be `null` only if a discount based on a client type was applied.'
                      is_mail:
                        type: boolean
                        description: 'If `true`, then the gift card email notification will be sent to the receiver. This is `true` only if the purchased item is a gift card. Otherwise, this will be `false` in all other cases.'
                      is_past:
                        type: boolean
                        description: 'If `true`, then the gift card email notification has been sent. This can only be `true` if the purchased item is a gift card. Otherwise, this will be `false` in all other cases.'
                      is_redeem:
                        type: boolean
                        description: 'This is `true` if the gift card has been redeemed. This can only be `true` if the purchased item is a gift card. Otherwise, this will be `false` in all other cases.'
                      is_renew:
                        type: boolean
                        description: 'This is `true` if the promotion is auto-renewable. This can only be `true` if the purchased item is a promotion. Otherwise, this will be `false` in all other cases.'
                      is_transfer:
                        type: boolean
                        description: 'This is `true` if the promotion has been transferred. This can only be true if the purchased item is a promotion. This is `false` if the promotion hasn''t been transferred or in all other cases.'
                      k_code_result:
                        type: string
                        description: 'The key of the redemption code. This value will not be `0` if the purchased item is obtained via a redemption code or if it''s a component of a package obtained via a redemption code.'
                      k_currency:
                        type: string
                        description: 'The key of the currency used to make the purchase.'
                      k_id:
                        type: string
                        description: 'The key of the item. This can correspond to one of many values depending on {@link PurchaseListElementApi::$id_sale}.'
                      k_login_promotion_result:
                        type: string
                        description: 'The key of the purchased promotion. This won''t be `0` only for promotions.'
                      k_purchase_result:
                        type: string
                        description: 'The key of the whole purchase, including the given purchased item.'
                      m_cost_item:
                        type: string
                        description: 'The cost of the purchased item.'
                      m_cost_total:
                        type: string
                        description: 'The total cost of the purchase, including taxes and fees.'
                      m_discount_amount:
                        type: string
                        description: 'The amount of the discount via prize. This will be empty if this discount wasn''t applied.'
                      m_discount_login_type_amount:
                        type: string
                        description: 'The amount of the discount for the client type. This value will be empty if the discount wasn''t applied.'
                      m_discount_manual:
                        type: string
                        description: 'The amount of the manual discount. This will be empty if this discount wasn''t applied.'
                      m_period_price:
                        type: string
                        description: 'The amount to pay for each payment period. This is used only for promotions with the membership program type.'
                      m_price:
                        type: string
                        description: 'The source price of item.'
                      m_refund:
                        type: string
                        description: 'The amount refunded. This will be `0` if there are no refunds.'
                      m_terminate_fee:
                        type: [string, "null"]
                        description: 'Cancellation fee amount. `null` if no need charge fee.'
                      s_code:
                        type: string
                        description: 'The redemption code. This value is used only if the purchase is bought via a redemption code.'
                      s_gift_code:
                        type: string
                        description: 'The code to redeem the gift card. This isn''t empty only for gift cards. This can be auto-generated or custom (entered manually by the staff member during purchase). The max length is 20 characters, including numbers and letters.'
                      s_gift_recipient:
                        type: string
                        description: 'The name of gift recipient. This isn''t empty only for gift cards.'
                      s_limit_actor:
                        type: string
                        description: 'The name of the staff member who disabled the usage of the purchased item. This will be empty if the purchased item is enabled.'
                      s_mail:
                        type: string
                        description: 'The email address to which the gift card was sent. This is used only for gift cards.'
                      s_message:
                        type: string
                        description: 'The test of the message sent to the gift card recipient. This is used only for gift cards. This will be empty if an email notification shouldn''t be sent.'
                      s_payer:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_promotion_convert:
                        type: string
                        description: 'The title of the converted promotion. This value is used only for promotions that must be converted to another promotion after expiration.'
                      s_purchase:
                        type: string
                        description: 'The purchase ID number as it appears on the receipt. This value will also include the leading 0s for the ID number.'
                      s_recipient:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_sender:
                        type: string
                        description: 'The name of the sender. This value is used only for gift cards.'
                      s_title:
                        type: string
                        description: 'The name of the purchased item.'
                      sid_value:
                        type: string
                        description: 'The string ID of the purchased item. This ID may be used to change the view of the purchased item.'
                      text_expire:
                        type: string
                        description: 'Expire conditions for the purchased item (only for promotion).'
                      text_guest_pass:
                        type: string
                        description: 'Text describing guest passes available for the purchased promotion.'
                      uid_payer:
                        type: [string, "null"]
                        description: 'The key of the user who paid for this purchase. `null` in a case when there was no money transaction.'
                      uid_recipient:
                        type: string
                        description: 'The key of the user who paid for this purchase.'
      tags:
        - PurchaseList-PurchaseListElement
  '/Wl/Profile/PurchaseList/PurchaseList.json':
    get:
      summary: 'Retrieves a list of user''s purchase items to show in user profile. Returns the complete purchase history for the client in the given business, covering all item types such as memberships, redemption codes, enrollments, products, appointments, and gift cards. Package components are resolved and included inline beside their parent item.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase:
                        type: array
                        description: 'A list of purchased items. Every element contains a sub-array with the following fields: <dl> <dt>bool[] `a_active`</dt> <dd> The value of <var>a_purchase[''is_active'']</var>. For packages, this also contains the values of <var>a_purchase[''is_active'']</var> of every component. </dd> <dt>int[] `a_sale`</dt> <dd> The value of <var>a_purchase[''id_sale'']</var>. For packages, this also contains the values of <var>a_purchase[''id_sale'']</var> of every component. </dd> <dt>string `dt_add`</dt> <dd>The date that the purchase was added.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase type. One of the constants {@link \RsPurchaseItemSid}.</dd> <dt>int `id_sale`</dt> <dd>The ID of the sale category in the store. One of the constants {@link \RsSaleSid}.</dd> <dt>bool `is_active`</dt> <dd>If `true`, then the purchase item is active. Otherwise, this will be `false`.</dd> <dt>bool `is_component`</dt> <dd>If `true`, then the purchase item is a package component. Otherwise, this will be `false`.</dd> <dt>bool `is_package`</dt> <dd>If `true`, then the purchase item is a package, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key from {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_code`</dt> <dd> The redemption code key that was used to make a purchase. This is used only if <var>k_login_promotion</var> and <var>k_purchase</var> are empty. </dd> <dt>string `k_enrollment_book`</dt> <dd> The key of an event that requires clients to book every session at once. This is used only if the event is part of a package. </dd> <dt>string `k_id`</dt> <dd> The key of the purchase type referring to different types of keys depending on the value of <var>id_sale</var>. </dd> <dt>string `k_login_product`</dt> <dd>The key of the purchased product. This is used only if the event is part of a package.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the user''s existing purchase options. This is used only if `k_code` and `k_purchase` are empty.</dd> <dt>string `k_purchase`</dt> <dd> The key of a purchase where no special case rules are in effect. This is used only used if `k_code` is empty. </dd> <dt>string `k_purchase_item`</dt> <dd> The key of a purchase item where no special case rules are in effect. This is used only if <var>k_code</var> is empty. </dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd> In certain cases a session can be canceled and makeup sessions can be granted to a client in lieu of other compensation. This is the key of one of the makeup sessions used to attend an event. This is also present for a repeat purchase of an event. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `s_title`</dt> <dd>The name of the purchase item.</dd> <dt>string `uid`</dt> <dd>The ID of the purchase owner. <i>Primary key {@link \PassportLoginSql} in table.</i></dd> </dl>'
      tags:
        - PurchaseList-PurchaseList
  '/Wl/Profile/Activity/Element.json':
    get:
      summary: 'Retrieves information about activity item. Loads a single activity record by `k_login_activity`, validates access via profile privileges, and returns the activity message, icon, type, reward and credit scores, activity dates in UTC and local time, spend amount, and a share URL.'
      parameters:
        - name: k_login_activity
          in: query
          description: 'The key of the activity item represented by this endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_credit_score:
                        type: array
                        description: 'Messages with description what did user do to get account credits as reward points.'
                      a_reward_score:
                        type: array
                        description: 'Messages with description what did user do to get points.'
                      can_profile:
                        type: boolean
                        description: 'Verifies that current user can view the specified profile.'
                      dt_date_gmt:
                        type: string
                        description: 'The date of the activity in GMT.'
                      dt_date_local:
                        type: string
                        description: 'The date of the activity in the client''s time zone.'
                      html_message:
                        type: string
                        description: 'Description of the action, who and what did.'
                      i_credit_score:
                        type: number
                        format: int32
                        description: 'Total amount of account credits user got for {@link ElementApi::$k_login_activity}. <i>This field is a copy of result of {@link \RsLoginActivity::reward()}.</i>'
                      i_score:
                        type: number
                        format: int32
                        description: 'The total amount of rewards points the client received for the activity.'
                      i_spend:
                        type: number
                        format: int32
                        description: 'The rewards points used to redeem a prize.'
                      id_icon:
                        type: [number, "null"]
                        description: 'The ID of the icon that should be shown for this activity. One of {@link IconSid} constants. This will be `null` is no special icon for this activity.'
                      id_type:
                        type: number
                        format: int32
                        description: 'The ID of the activity item returned by this endpoint. One of {@link \RsLoginActivityTypeSid} constants.'
                      k_id:
                        type: string
                        description: 'Object ID, for example, class period ID for books and visits.'
                      s_message:
                        type: string
                        description: 'The description of the activity. This should include the nature of the activity and the people involved.'
                      url_link:
                        type: string
                        description: 'Link to share activity with social networks.'
      tags:
        - Activity-Element
  '/Wl/Profile/Activity/List.json':
    get:
      summary: 'Retrieves a list of activity items to show in user profile. Returns the client''s activity history for the specified business, filtered by an optional date range. Requires profile-view access and respects activity-type visibility rules for the requesting user.'
      parameters:
        - name: dl_end
          in: query
          description: 'Ending date of the date range. Optional. This is local business timezone date. This day is included in the result.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Starting date of the date range. Optional. This is local business timezone date. This day is included in the result.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the client to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_activity:
                        type: array
                        description: 'An array listing client activities, where each activity is provided as an ID number. The order of items in this array is the order in which the elements should be displayed.'
      tags:
        - Activity-List
  '/Wl/Profile/Term/Term.json':
    get:
      summary: 'Returns online waiver information for the specified user and business. Returns the rendered online waiver text for the business along with the client''s current agreement status. Used to display the waiver page and show whether the client has already signed, including their signature image and the confirmation timestamp.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the current business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dt_agree:
                        type: [string, "null"]
                        description: 'The date/time of the waiver confirmation. <tt>null</tt> if waiver is not confirmed.'
                      html_contract:
                        type: string
                        description: 'The text of the online waiver.'
                      i_minor_age:
                        type: number
                        format: int32
                        description: 'Age of minor which documents can be signed by parent or legal guardian.'
                      ip_agree:
                        type: [string, "null"]
                        description: 'The IP address from which the confirmation was carried out. <tt>null</tt> if waiver is not confirmed.'
                      is_agree:
                        type: boolean
                        description: 'Flag of successful saving agreement. <tt>true</tt> if agreement was sign up successful <tt>false</tt> - otherwise.'
                      s_name:
                        type: string
                        description: 'The user''s name.'
                      url_signature:
                        type: [string, object]
                        description: 'The URL to the image with the client''s signature. <tt>false</tt> if waiver is not confirmed.'
      tags:
        - Term-Term
  '/Wl/Profile/Attach/AttachElement.json':
    delete:
      summary: 'Deletes the attachment. Permanently removes the specified attachment from the client profile and logs the deletion action in the business audit trail.'
      parameters:
        - name: k_attach
          in: query
          description: 'Attachment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Attach-AttachElement
    get:
      summary: 'Gets the data for the attachment editing form. Returns attachment metadata including filename, description, filesize, file type, creation and edit timestamps, download URL, preview URL, and a flag indicating whether the current user has permission to delete the attachment.'
      parameters:
        - name: k_attach
          in: query
          description: 'Attachment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtu_create:
                        type: string
                        description: 'Date and time of the creation.'
                      dtu_edit:
                        type: string
                        description: 'Date and time of the last edit.'
                      s_show_delete:
                        type: string
                        description: 'Flag for showing a delete button.'
                      text_description:
                        type: [string, "null"]
                        description: 'Attachment description. `null` for not update description.'
                      text_filename:
                        type: [string, "null"]
                        description: 'Attachment file name. `null` for not update file name.'
                      text_filesize:
                        type: string
                        description: 'Attachment file size.'
                      text_filetype:
                        type: string
                        description: 'Attachment file type.'
                      url_file:
                        type: string
                        description: 'URL to get attachment file.'
                      url_preview:
                        type: [string, "null"]
                        description: 'URL to get preview attachment data. `null` in case when preview not available.'
      tags:
        - Attach-AttachElement
    post:
      summary: 'In case the attachment key is not specified, it adds a new attachment. In case the attachment key is specified, edits the attachment. When {@link AttachElementApi::$k_attach} is empty, uploads a new file to the client profile; when set, updates the attachment metadata, file content, or visibility flag depending on the provided fields.'
      parameters:
        - name: k_attach
          in: query
          description: 'Attachment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_file:
                  type: array
                  description: 'Uploaded file.'
                is_private:
                  type: [boolean, "null"]
                  description: '`true` if the attachment is private; `false` otherwise. `null` until initialized.'
                text_description:
                  type: [string, "null"]
                  description: 'Attachment description. `null` for not update description.'
                text_filename:
                  type: [string, "null"]
                  description: 'Attachment file name. `null` for not update file name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_attach:
                        type: string
                        description: 'Attachment key.'
      tags:
        - Attach-AttachElement
  '/Wl/Profile/Attach/AttachList.json':
    get:
      summary: 'Returns the list of file attachments for the specified client in the given business. Returns all file attachments uploaded to the client''s profile. In backend mode the result also includes private attachments that are hidden from the client-facing view, with additional metadata such as source, description, and a delete permission flag.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'The filter phrase to filter attach by name.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of client attachments. Each element: <dl> <dt>string `dtu_create`</dt> <dd>Date and time of creation in UTC MySQL format.</dd> <dt>string `dtu_edit`</dt> <dd>Date and time of the last edit in UTC MySQL format.</dd> <dt>int|null `i_height`</dt> <dd>Image height in pixels. `null` if not an image or dimensions are unavailable.</dd> <dt>int `i_show_delete`</dt> <dd> Flag indicating whether to show the delete button. `''1''` to show, `''0''` to hide. Only present in backend mode. </dd> <dt>int|null `i_width`</dt> <dd>Image width in pixels. `null` if not an image or dimensions are unavailable.</dd> <dt>int `id_preview`</dt> <dd>Preview type identifier. One of {@link \Wl\Profile\Attach\AttachPreviewSid} constants.</dd> <dt>bool `is_private`</dt> <dd> `true` if the attachment is private (not visible to the client); `false` otherwise. Only present in backend mode. </dd> <dt>string `k_attach`</dt> <dd>Attachment key. <i>Primary key in the {@link \AAttachSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string|null `k_quiz_response`</dt> <dd> Quiz response key. `null` if the attachment is not from a quiz response. <i>Primary key in the {@link \Core\Quiz\Response\ResponseSql} table.</i> Only present in backend mode. </dd> <dt>string|null `text_description`</dt> <dd>Attachment description. `null` if no description. Only present in backend mode.</dd> <dt>string `text_filename`</dt> <dd>Attachment file name.</dd> <dt>string `text_filesize`</dt> <dd>Human-readable file size (for example, "1.2 MB").</dd> <dt>string `text_filetype`</dt> <dd>File extension in lowercase (for example, "pdf", "jpg").</dd> <dt>string `text_source`</dt> <dd>Source description (for example, quiz title or "Uploaded by staff"). Only present in backend mode.</dd> <dt>string `text_uploaded_by`</dt> <dd>Name of the user who uploaded the attachment. Empty string if unknown.</dd> <dt>string `text_uploaded_date`</dt> <dd>Formatted upload date in the business locale.</dd> <dt>string `url_file`</dt> <dd>URL to download the attachment file.</dd> <dt>string|null `url_preview`</dt> <dd>URL to a preview image (50x50). `null` if preview is not available for this file type.</dd> </dl>'
      tags:
        - Attach-AttachList
  '/Wl/Profile/Setting/Setting.json':
    get:
      summary: 'Retrieves a list of user settings and other additional information for the settings page. Returns the user''s current notification preferences for the business, covering account activity, news, and schedule reminders across email and SMS channels, along with the user''s current language setting.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_account_management_email:
                        type: boolean
                        description: 'Determines whether email notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                      is_account_management_sms:
                        type: boolean
                        description: 'Determines whether SMS notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                      is_news_and_updates_email:
                        type: boolean
                        description: 'Determines whether email notifications related to news and updates from the business regarding their services, availability, and promotions are enabled.'
                      is_news_and_updates_sms:
                        type: boolean
                        description: 'Whether sms notifications related to news and updates from the business regarding their services, availability and promotions are enabled.'
                      is_schedule_and_reminders_email:
                        type: boolean
                        description: 'Determines whether email notifications related to the services a client has booked are enabled.'
                      is_schedule_and_reminders_sms:
                        type: boolean
                        description: 'Determines whether SMS notifications related to the services a client has booked are enabled.'
                      text_language:
                        type: string
                        description: 'SID of language code.'
      tags:
        - Setting-Setting
    post:
      summary: 'Saves the user''s notification settings for the specified business. Saves the user''s notification preferences for the business, updating email and SMS opt-in settings for account activity, news, and schedule reminders.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_account_management_email:
                  type: boolean
                  description: 'Determines whether email notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                is_account_management_sms:
                  type: boolean
                  description: 'Determines whether SMS notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                is_news_and_updates_email:
                  type: boolean
                  description: 'Determines whether email notifications related to news and updates from the business regarding their services, availability, and promotions are enabled.'
                is_news_and_updates_sms:
                  type: boolean
                  description: 'Whether sms notifications related to news and updates from the business regarding their services, availability and promotions are enabled.'
                is_schedule_and_reminders_email:
                  type: boolean
                  description: 'Determines whether email notifications related to the services a client has booked are enabled.'
                is_schedule_and_reminders_sms:
                  type: boolean
                  description: 'Determines whether SMS notifications related to the services a client has booked are enabled.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Setting-Setting
  '/Wl/Profile/Alert/Alert.json':
    get:
      summary: 'Retrieves a list of notices to show in user''s profile. Returns `a_alert` (system alerts including unconfirmed contracts) and `a_warning` (login notes and flags with author and editor information) for the specified user in the given business.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_alert:
                        type: array
                        description: 'A list of alerts. Every element is an array with the following keys: <dl> <dt>string|null `dt_date`</dt> <dd> The date and time in MySQL format in local time when the alert was last modified. This will be `null` if the date isn''t available for this alert. </dd> <dt>bool `is_today`</dt> <dd>This will be `true` if the alert was added today. Otherwise, this will be `false`.</dd> <dt>int `id_profile_note`</dt> <dd>ID of alert type. One of {@link \RsProfileNoteSid} constants.</dd> <dt>string `k_pay_account`</dt> <dd>The key of the client''s account. <i>Primary key in {@link \RsPayAccountSql} table.</i></dd> <dt>string `k_purchase_item`</dt> <dd>The key of the purchase item. <i>Primary key in {@link \RsPurchaseItemSql} table.</i></dd> <dt>string `s_text`</dt> <dd>The alert''s text.</dd> </dl>'
                      a_warning:
                        type: array
                        description: 'A list of warnings. Every element is an array with the following keys: <dl> <dt>string[] `a_location_flag`</dt> <dd>The list of location keys where this note is flagged. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>bool `dt_create`</dt> <dd>The date and time in MySQL format in local time when the warning was created.</dd> <dt>bool `dt_date`</dt> <dd>The date and time in MySQL format in local time when the warning was last modified.</dd> <dt>bool `is_book_restricted`</dt> <dd>`true` means that client is restricted to make bookings, `false` - otherwise.</dd> <dt>bool `is_flag`</dt> <dd>This will be `true` if the client is flagged. Otherwise, this will be `false`.</dd> <dt>bool `is_purchase_restricted`</dt> <dd>`true` means that client is restricted to make purchases. Otherwise, this will be `false`.</dd> <dt>bool `is_today`</dt> <dd>This will be `true` if the warning was added today. Otherwise, this will be `false`.</dd> <dt>string `s_text`</dt> <dd>The text of the warning.</dd> <dt>string `text_author`</dt> <dd>Staff name, who created a note.</dd> <dt>string `text_editor`</dt> <dd>Staff name, who edited a note last time.</dd> </dl>'
      tags:
        - Alert-Alert
  '/Wl/Profile/Alert/AlertEdit.json':
    delete:
      summary: 'Deletes existing note. Permanently removes the login note specified by `k_login_note` from the business, requiring backend access for the current user.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_login_note
          in: query
          description: 'Login note key to edit or get info for. Empty if new entry is being added.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Alert-AlertEdit
    get:
      summary: 'Retrieves login note data. Loads a single login note for editing, including its text, access type, flag settings, booking and purchase restrictions, and location flags. Used to populate the edit form before saving changes.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_login_note
          in: query
          description: 'Login note key to edit or get info for. Empty if new entry is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information or post a note for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_note_data:
                        type: array
                        description: 'Login note information. <dl> <dt>array `a_location`</dt> <dd> List of locations the note applies to. Each element: <dl> <dt>string `html_title`</dt> <dd>HTML-escaped location title.</dd> <dt>bool `is_select`</dt> <dd>`true` if this location is selected for the flag; `false` otherwise.</dd> <dt>string `k_id`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl> </dd> <dt>bool `can_flag`</dt> <dd>`true` if the current user can flag the client; `false` otherwise.</dd> <dt>int `id_login_note_access`</dt> <dd>Login note access type. One of {@link \RsLoginNoteAccessSid} constants.</dd> <dt>int `id_login_note_flag`</dt> <dd>Login note flag type. One of {@link \RsLoginNoteFlagSid} constants.</dd> <dt>int|null `id_source`</dt> <dd> Flag source identifier. One of {@link \Wl\Location\Flag\FlagSourceEnum} constant. `null` if the flag was created manually. </dd> <dt>bool `is_automated`</dt> <dd>`true` if the note was created by an automated process; `false` if created manually.</dd> <dt>bool `is_book`</dt> <dd>`true` if the flagged client is allowed to book; `false` otherwise.</dd> <dt>bool `is_flag`</dt> <dd>`true` if the client is currently flagged; `false` otherwise.</dd> <dt>bool `is_icon`</dt> <dd>`true` if a flag icon should be displayed; `false` otherwise.</dd> <dt>bool `is_location_multiple`</dt> <dd>`true` if the business has multiple locations; `false` otherwise.</dd> <dt>bool `is_purchase`</dt> <dd>`true` if the flagged client is allowed to make purchases; `false` otherwise.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the note is marked for removal; `false` otherwise.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string `k_login_note`</dt> <dd>Login note key. <i>Primary key in the {@link \RsLoginNoteSql} table.</i></dd> <dt>string `s_text`</dt> <dd>Note text content.</dd> <dt>string `text_flag_description`</dt> <dd>Description of the flag source for display.</dd> <dt>string `text_flag_disable`</dt> <dd>Text explaining how to disable the automated flag. Empty string if the flag is manual.</dd> <dt>string `uid`</dt> <dd>User key of the flagged client. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_action`</dt> <dd>URL to submit the login note edit form.</dd> </dl>'
      tags:
        - Alert-AlertEdit
    post:
      summary: 'Creates new text note or updates the existing one. Creates or updates a login note on the client''s profile, setting its text, access restrictions, booking and purchase flags, and the locations where the note applies. Used by staff to attach internal notes or warnings that appear on check-in and booking flows.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_login_note
          in: query
          description: 'Login note key to edit or get info for. Empty if new entry is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information or post a note for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: array
                  description: 'The list of location keys to flag client in.'
                id_login_note_access:
                  type: number
                  format: int32
                  description: 'Login note access type ID.'
                is_book:
                  type: boolean
                  description: 'Whether flagged user can book. Does not matter if {@link AlertEditApi::$is_flag} is `false`.'
                is_flag:
                  type: boolean
                  description: 'Whether to flag the user.'
                is_purchase:
                  type: boolean
                  description: 'Whether flagged user can make purchases. Does not matter if {@link AlertEditApi::$is_flag} is `false`.'
                text_note:
                  type: string
                  description: 'Note text.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_note:
                        type: string
                        description: 'Login note key to edit or get info for. Empty if new entry is being added.'
      tags:
        - Alert-AlertEdit
  '/Wl/Profile/Edit/Create.json':
    post:
      summary: 'Creates a new user. Creates a new user account with the given email, first name, and last name in the specified business, registers the user as a member, and returns the `uid` of the created or existing user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of the business where you''re creating new user.'
                text_firstname:
                  type: string
                  description: 'First name of the user.'
                text_lastname:
                  type: string
                  description: 'Last name of the user.'
                text_mail:
                  type: string
                  description: 'Email of the user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'The key of the user added.'
      tags:
        - Edit-Create
  '/Wl/Profile/Edit/Edit.json':
    get:
      summary: 'Returns current user profile information. Returns the profile field definitions and current values for the specified user, or an empty structure when creating a new account. Used to populate the profile edit form with the correct fields, validation rules, and inheritance options for the business.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      a_phone_inherit:
                        type: array
                        description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                      a_structure:
                        type: array
                        description: 'The values and structure of all fields. Array keys are field IDs (`k_field`). Array values are the field values. The array has the following structure: <dl> <dt>int `id_field_general`</dt> <dd> The ID of a system field. One of the {@link \RsFieldGeneralSid} constants. This value is only defined if <var>id_field_type</var>={@link \RsFieldTypeSid::GENERAL}. </dd> <dt>bool `is_require`</dt> <dd> Indicates whether the value of this field is required. This will be `1` if required or `0` if the field is optional. </dd> <dt>int `id_field_type`</dt> <dd>The type of field. One of the {@link \RsFieldTypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>The field ID (<var>k_field</var>). A copy of the key of this array element.</dd> <dt>string `s_title`</dt> <dd>The title of the field.</dd> <dt>mixed `x_value`</dt> <dd>The value of the field. This value is defined by individual fields.</dd> </dl>'
                      can_password_change:
                        type: boolean
                        description: 'Whether current user can change password of the given user or not. If client is part of multiple businesses, then staff of the business can only request reset of the password, but cannot change it.'
                      is_a2p:
                        type: boolean
                        description: '`true` if the A2P 10DLC registration feature is enabled for this business, `false` otherwise.'
                      is_address_inherit:
                        type: [boolean, "null"]
                        description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
                      is_short:
                        type: boolean
                        description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
                      k_lead_source:
                        type: [string, "null"]
                        description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid_inherit_address:
                        type: [string, "null"]
                        description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-Edit
    post:
      summary: 'Creates a new user. Registers a new user in the business using the submitted profile field values, enforces IP-based registration rate limiting, and handles family relations and phone or address inheritance. Returns the identifier of the newly created account.'
      parameters:
        - name: id_mode
          in: query
          description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: [number, "null"]
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_short
          in: query
          description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: text_business_uid_key
          in: query
          description: 'Compound key delimited with a colon. First part is the business key where the selected client exists. Second part is the uid of the already existing user we want to add. Empty if a non-existent client is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_existed
          in: query
          description: 'The UID of an existing user in another business to add to the current business.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
        - name: uid_relative_key
          in: query
          description: 'UID of the user, whose email was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose email is inherited.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_family_relation:
                  type: [array, "null"]
                  description: 'Family relation data for the newly created user. `null` if no family relation is required: <dl> <dt>int `id_family_relation`</dt> <dd>Relation type. One of {@link \RsFamilyRelationSid} constants.</dd> <dt>bool `is_relative_pay`</dt> <dd>`true` if the relative will pay for this user; `false` or absent otherwise.</dd> <dt>string `uid_from`</dt> <dd> User key of the relative (must be the currently logged-in user). <i>Primary key in the {@link \PassportLoginSql} table.</i> </dd> </dl>'
                a_image_upload:
                  type: array
                  description: 'Information for user''s photo. It is necessary to save simultaneously user and photo in staff member mode. Attention! Data from this field is taken directly from a POST somewhere in the depths of the photo upload.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_new:
                  type: array
                  description: 'A copy of the {@link EditApi::$a_change}. This is used for POST requests when creating a new user.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                is_exception_throw:
                  type: boolean
                  description: '`true` to throw an exception in a case of validation error; `false` to not throw.'
                is_sing_in:
                  type: boolean
                  description: 'This will be `true` to sign in a created user. Otherwise, this will be `false`.'
                is_sms_subscription_marketing:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive marketing SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                is_sms_subscription_presented:
                  type: boolean
                  description: '<tt>true</tt> - user was shown checkboxes to subscribe to marketing and transactional sms messages. This means we need to modify his subscription based on the values in these fields. <tt>false</tt> - otherwise. This means we should not change subscription settings.'
                is_sms_subscription_transactional:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive transactional SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                text_password:
                  type: string
                  description: 'The password to be set for a new user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      s_status:
                        type: [string, "null"]
                        description: 'Status of the request. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid:
                        type: string
                        description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-Edit
    put:
      summary: 'Updates values of profile fields. Saves the updated profile field values for the existing user, propagates phone and address inheritance changes, triggers relevant notifications, and re-indexes the user for search.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_change:
                  type: array
                  description: 'A list of fields to change. Values are the new field values. Specific values depend on an individual field type. Some fields may be strings while others are arrays. If a composite field is changed, all fields should be specified (for example, if you change your address, you''ll need to specify the city). Keys are the `k_field` values.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
  '/Wl/Profile/Edit/EditByToken.json':
    get:
      summary: 'Returns current user profile information. Returns the profile field definitions and current values for the specified user, or an empty structure when creating a new account. Used to populate the profile edit form with the correct fields, validation rules, and inheritance options for the business.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      a_phone_inherit:
                        type: array
                        description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                      a_structure:
                        type: array
                        description: 'The values and structure of all fields. Array keys are field IDs (`k_field`). Array values are the field values. The array has the following structure: <dl> <dt>int `id_field_general`</dt> <dd> The ID of a system field. One of the {@link \RsFieldGeneralSid} constants. This value is only defined if <var>id_field_type</var>={@link \RsFieldTypeSid::GENERAL}. </dd> <dt>bool `is_require`</dt> <dd> Indicates whether the value of this field is required. This will be `1` if required or `0` if the field is optional. </dd> <dt>int `id_field_type`</dt> <dd>The type of field. One of the {@link \RsFieldTypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>The field ID (<var>k_field</var>). A copy of the key of this array element.</dd> <dt>string `s_title`</dt> <dd>The title of the field.</dd> <dt>mixed `x_value`</dt> <dd>The value of the field. This value is defined by individual fields.</dd> </dl>'
                      can_password_change:
                        type: boolean
                        description: 'Whether current user can change password of the given user or not. If client is part of multiple businesses, then staff of the business can only request reset of the password, but cannot change it.'
                      is_a2p:
                        type: boolean
                        description: '`true` if the A2P 10DLC registration feature is enabled for this business, `false` otherwise.'
                      is_address_inherit:
                        type: [boolean, "null"]
                        description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
                      is_short:
                        type: boolean
                        description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
                      k_lead_source:
                        type: [string, "null"]
                        description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid_inherit_address:
                        type: [string, "null"]
                        description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-EditByToken
    post:
      summary: 'Creates a new user. Registers a new user in the business using the submitted profile field values, enforces IP-based registration rate limiting, and handles family relations and phone or address inheritance. Returns the identifier of the newly created account.'
      parameters:
        - name: id_mode
          in: query
          description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: [number, "null"]
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_short
          in: query
          description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: text_business_uid_key
          in: query
          description: 'Compound key delimited with a colon. First part is the business key where the selected client exists. Second part is the uid of the already existing user we want to add. Empty if a non-existent client is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_existed
          in: query
          description: 'The UID of an existing user in another business to add to the current business.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
        - name: uid_relative_key
          in: query
          description: 'UID of the user, whose email was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose email is inherited.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_family_relation:
                  type: [array, "null"]
                  description: 'Family relation data for the newly created user. `null` if no family relation is required: <dl> <dt>int `id_family_relation`</dt> <dd>Relation type. One of {@link \RsFamilyRelationSid} constants.</dd> <dt>bool `is_relative_pay`</dt> <dd>`true` if the relative will pay for this user; `false` or absent otherwise.</dd> <dt>string `uid_from`</dt> <dd> User key of the relative (must be the currently logged-in user). <i>Primary key in the {@link \PassportLoginSql} table.</i> </dd> </dl>'
                a_image_upload:
                  type: array
                  description: 'Information for user''s photo. It is necessary to save simultaneously user and photo in staff member mode. Attention! Data from this field is taken directly from a POST somewhere in the depths of the photo upload.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_new:
                  type: array
                  description: 'A copy of the {@link EditApi::$a_change}. This is used for POST requests when creating a new user.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                is_exception_throw:
                  type: boolean
                  description: '`true` to throw an exception in a case of validation error; `false` to not throw.'
                is_sing_in:
                  type: boolean
                  description: 'This will be `true` to sign in a created user. Otherwise, this will be `false`.'
                is_sms_subscription_marketing:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive marketing SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                is_sms_subscription_presented:
                  type: boolean
                  description: '<tt>true</tt> - user was shown checkboxes to subscribe to marketing and transactional sms messages. This means we need to modify his subscription based on the values in these fields. <tt>false</tt> - otherwise. This means we should not change subscription settings.'
                is_sms_subscription_transactional:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive transactional SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                text_password:
                  type: string
                  description: 'The password to be set for a new user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      s_status:
                        type: [string, "null"]
                        description: 'Status of the request. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid:
                        type: string
                        description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-EditByToken
    put:
      summary: 'Updates values of profile fields. Saves the updated profile field values for the existing user, propagates phone and address inheritance changes, triggers relevant notifications, and re-indexes the user for search.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_change:
                  type: array
                  description: 'A list of fields to change. Values are the new field values. Specific values depend on an individual field type. Some fields may be strings while others are arrays. If a composite field is changed, all fields should be specified (for example, if you change your address, you''ll need to specify the city). Keys are the `k_field` values.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-EditByToken
  '/Wl/Profile/Edit/EditPassword.json':
    post:
      summary: 'Saves new password for user. Changes the user''s password after verifying the current one, enforcing complexity and length rules, and sends a password-change notification email. The old password check can be skipped by staff with the appropriate access level.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_old_password_required:
                  type: boolean
                  description: 'Checks if the current password is required or not to update the password.'
                k_business:
                  type: string
                  description: 'The key ID of business where actions take place. Business is necessary for mail sending only.'
                s_password_confirm:
                  type: string
                  description: 'The confirmation of new password.'
                s_password_new:
                  type: string
                  description: 'The new password.'
                s_password_old:
                  type: string
                  description: 'The old password.'
                uid:
                  type: string
                  description: 'The user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-EditPassword
  '/Wl/Profile/Contract/Contract.json':
    get:
      summary: 'Returns contract information for the specified purchase option. Renders the contract text for the specified purchase option, applying any applicable discounts, and returns the content needed to display the contract acceptance modal to the client.'
      parameters:
        - name: dt_start
          in: query
          description: 'The start date of the contract.'
          schema:
            type: string
        - name: f_manual_discount
          in: query
          description: 'The percentage discount for the item.'
          schema:
            type: number
            format: float
        - name: id_purchase_item
          in: query
          description: 'The type of purchase item. This is one of the {@link \RsPurchaseItemSid} constants. Optional if {@link \Wl\Profile\Contract\ContractApi::$k_purchase_item} is not empty.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The key of the purchase item in the database. The item key. Depends on {@link ContractApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the selected location.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the selected purchase item.'
          schema:
            type: string
        - name: m_discount_flat
          in: query
          description: 'Amount of a flat manual discount.'
          schema:
            type: string
        - name: m_price_custom
          in: query
          description: 'The custom price of the item.'
          schema:
            type: string
        - name: s_discount_code
          in: query
          description: 'The discount code used for the item.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_contract:
                        type: string
                        description: 'The text of the contract.'
                      i_minor_age:
                        type: number
                        format: int32
                        description: 'Age of minor which documents can be signed by parent or legal guardian.'
                      text_title:
                        type: string
                        description: 'Title of purchase option.'
      tags:
        - Contract-Contract
    post:
      summary: 'Completes a sale of a Purchase Option requiring a contract by submitting the signed contract. Accepts an encoded client signature and agreement flag, decodes the signature, and records the signed contract for the specified purchase item.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the selected purchase item.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_agree:
                  type: [boolean, "null"]
                  description: '<tt>false</tt> if user has not agreed to use Electronic Signatures, <tt>true</tt> if user has agreed to use Electronic Signatures, <tt>null</tt> otherwise.'
                s_signature:
                  type: string
                  description: 'An encoded version of the client signature. This is different from the signature needed to communicate with an endpoint.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Contract-Contract
  '/Wl/Profile/Timezone/ProfileTimezone.json':
    get:
      summary: 'Gets a list of timezones with currently selected user''s timezone and the business policy adjustment whether clients are allowed to adjust timezone. Includes the timezone abbreviation, UTC shift, and display order for each entry, and falls back to the business-configured guest timezone when no member is signed in.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, in which user selected timezone.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_timezone:
                        type: array
                        description: 'List of timezones. Keys - timezone keys; <i>Primary keys in {@link \AGeoTimezoneSql} table.</i> Values - sub array with next keys: <dl> <dt>string `k_geo_timezone`</dt> <dd>Timezone key. <i>Primary key in {@link \AGeoTimezoneSql} table.</i></dd> <dt>int `i_shift`</dt> <dd>Shift time zone.</dd> <dt>string `s_file`</dt> <dd>File name.</dd> <dt>int `i_order`</dt> <dd>Order.</dd> <dt>string `text_abbr`</dt> <dd>Timezone abbreviation.</dd> <dt>string `s_title`</dt> <dd>Timezone title.</dd> <dt>string `k_timezone`</dt> <dd>Timezone key.</dd> </dl>'
                      is_profile_timezone:
                        type: boolean
                        description: 'Whether clients are allowed to adjust timezone. `true` if allowed, `false` - otherwise.'
                      k_timezone_select:
                        type: string
                        description: 'Key of the timezone which is currently selected.'
      tags:
        - Timezone-ProfileTimezone
    put:
      summary: 'Updates selected timezone for the site visitor in the given business. Validates the requested timezone against the business timezone deprecation policy, then saves it either to the member''s profile field or, for a guest, to the guest-scoped timezone selection.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, in which user selected timezone.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_deprecate_restrict:
                  type: boolean
                  description: 'If a timezone deprecation validation is needed. `true` if required, `false` - otherwise.'
                k_timezone:
                  type: string
                  description: 'Key of the timezone, which user selected to see site in.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Timezone-ProfileTimezone
  '/Wl/Billing/Bulk/PurchaseItemList.json':
    get:
      summary: 'Gets the list of promotions and products available at the location. Validates access to the business, then loads promotions with their prices and products expanded into their options, keeping only the items available at the requested location.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key to filter available items by.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'The list of products available at the location. Each element has the following structure: <dl> <dt>array[] `a_option`</dt> <dd> The options of the product available at the location. Each element has the following structure: <dl> <dt>string `k_shop_product_option`</dt> <dd>The product option key. <i>Primary key in the {@link \RsShopProductOptionSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The regular price of the option.</dd> <dt>string `text_title`</dt> <dd>The product option title.</dd> </dl> </dd> <dt>bool `is_tax_free`</dt> <dd>`true` if no taxes should be applied to the product, `false` otherwise.</dd> <dt>string `k_shop_product`</dt> <dd>The product key. <i>Primary key in the {@link \RsShopProductSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The product title.</dd> </dl>'
                      a_promotion:
                        type: array
                        description: 'The list of promotions available at the location. Each element has the following structure: <dl> <dt>int `id_program`</dt> <dd>The promotion program. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_program_type`</dt> <dd>The promotion program type. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>bool `is_tax_free`</dt> <dd>`true` if no taxes should be applied to the promotion, `false` otherwise.</dd> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in the {@link \RsPromotionSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The promotion price, excluding taxes.</dd> <dt>string `text_title`</dt> <dd>The promotion title.</dd> <dt>string `text_payment_period`</dt> <dd>Payment period of the promotion.</dd> </dl>'
                      is_surcharge_ach:
                        type: boolean
                        description: 'Whether to apply the ACH surcharge.'
                      is_surcharge_ecommerce:
                        type: boolean
                        description: 'Whether to apply the e-commerce surcharge.'
      tags:
        - Bulk-PurchaseItemList
    post:
      summary: 'Prepares the bulk billing review: the per-client totals and the list of clients that will be billed. Validates access to the business, calculates the per-client subtotal, tax and total for the selected purchase items, and collects each client contact data and payment method label. Clients that a selected item is not available to (by their client type or member group) are removed from billing and returned in {@link PurchaseItemListApi::$a_client_restrict}. Clients that are not eligible for the selected introductory items are flagged with `is_warning` and described in {@link PurchaseItemListApi::$a_client_bill} (`a_warning` key). selected.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key to filter available items by.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_purchase_item:
                  type: array
                  description: 'The list of purchase items to bill. Each element is a string in the `{id_purchase_item}:{k_id}` format, where `id_purchase_item` is one of the {@link \RsPurchaseItemSid} constants and `k_id` is the promotion key for {@link \RsPurchaseItemSid::PROMOTION} or the product option key for {@link \RsPurchaseItemSid::PRODUCT}.'
                is_payment_method_default:
                  type: boolean
                  description: 'Whether to charge the client default payment method (`true`) or bill the client account (`false`).'
                is_receipt_send:
                  type: boolean
                  description: 'Whether to send a receipt to the client email address after billing.'
                is_surcharge_ach:
                  type: boolean
                  description: 'Whether to apply the ACH surcharge.'
                is_surcharge_ecommerce:
                  type: boolean
                  description: 'Whether to apply the e-commerce surcharge.'
                is_tax:
                  type: boolean
                  description: 'Whether to include the pre-configured taxes into the totals.'
                s_uid:
                  type: string
                  description: 'List of client user keys to bill joined with comma.'
                text_note:
                  type: string
                  description: 'A note to store with each client purchase and to show on the receipt. Empty string for no note.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_client_bill:
                        type: array
                        description: 'The result of preparing the clients to bill. Has the following structure: <dl> <dt>array[] `a_warning`</dt> <dd> The inventory and introductory-eligibility warnings produced while preparing the bill. Each element has the following structure: <dl> <dt>string `text_message`</dt> <dd>The user-facing warning message.</dd> </dl> </dd> <dt>array[] `a_client`</dt> <dd> The list of clients that will be billed. Each element has the following structure: <dl> <dt>int `id_pay_method`</dt> <dd> The payment method this client is charged with, which decides whether the client is surcharged and which surcharge applies. One of the {@link \RsPayMethodSid} constants: {@link \RsPayMethodSid::ECOMMERCE} for a stored card, {@link \RsPayMethodSid::ACH} for a stored bank account, and {@link \RsPayMethodSid::ACCOUNT} when the client account is billed, which is never surcharged. </dd> <dt>bool `is_warning`</dt> <dd> `true` if the client has no default payment method on file, has no email on file while a receipt is to be sent, or is not eligible for at least one of the selected introductory items; `false` otherwise. </dd> <dt>string `text_mail`</dt> <dd>The client email address. Empty string if the client has no email.</dd> <dt>string `text_name`</dt> <dd>The client full name.</dd> <dt>string `text_pay_method`</dt> <dd> The payment method label for this client. `Account` when billing to the client account; otherwise the default stored card label (for example, `Visa ****1234`), the default ACH account label when no card is on file, or an account fallback when neither is on file. </dd> <dt>string `text_phone`</dt> <dd>The client cell phone number. Empty string if the client has no cell phone.</dd> <dt>string `uid`</dt> <dd>The client user key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> </dl>'
                      a_client_restrict:
                        type: array
                        description: 'The clients removed from the bulk billing because a selected item is not available to their client type or member group, together with the warnings that explain why. Has the following structure: <dl> <dt>array[] `a_client`</dt> <dd> The clients that fail at least one restriction. Each element has the following structure: <dl> <dt>string `text_login_type`</dt> <dd>The client login type title.</dd> <dt>string `text_mail`</dt> <dd>The client email address. Empty string if the client has no email.</dd> <dt>string `text_member_group`</dt> <dd>Comma-separated titles of the member groups the client belongs to.</dd> <dt>string `text_name`</dt> <dd>The client full name.</dd> <dt>string `uid`</dt> <dd>The client user key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>array[] `a_warning`</dt> <dd> One warning per restricted item that at least one client fails to satisfy. Each element has the following structure: <dl> <dt>string `text_message`</dt> <dd>The user-facing warning message.</dd> </dl> </dd> <dt>bool `has_client_group_restrict`</dt> <dd>`true` if at least one client fails a member group restriction, `false` otherwise.</dd> <dt>bool `has_client_type_restrict`</dt> <dd>`true` if at least one client fails a login type restriction, `false` otherwise.</dd> </dl>'
                      a_total:
                        type: array
                        description: 'The totals of the bulk billing: the price of the selected items for a single client, and what the whole batch adds up to once the client type discount of every client is applied. All amounts are money strings in the currency of the business. Has the following structure: <dl> <dt>array[] `a_discount_list`</dt> <dd> One entry per client type that discounts at least one of the selected items for at least one of the clients. Empty when no client type discount applies. Each element has the following structure: <dl> <dt>int `i_user`</dt> <dd>The number of clients that get this discount.</dd> <dt>string `k_login_type`</dt> <dd>The client type that gives the discount. <i>Primary key in the {@link \Wl\Login\Type\Sql} table.</i></dd> <dt>string `m_discount`</dt> <dd>The discount this client type gives, summed over all its clients and all the selected items.</dd> <dt>string `text_title`</dt> <dd>The client type title.</dd> </dl> </dd> <dt>int `i_pay_account`</dt> <dd>The number of clients that paid by account.</dd> <dt>int `i_pay_card`</dt> <dd>The number of clients that paid by card or ACH.</dd> <dt>string `m_pay_account`</dt> <dd>The total amount paid by account.</dd> <dt>string `m_pay_card`</dt> <dd>The total amount paid by card or ACH.</dd> <dt>string `m_subtotal_after_discount`</dt> <dd>The price of the selected items for all the clients, with the client type discounts applied.</dd> <dt>string `m_subtotal_before_discount`</dt> <dd>The price of the selected items for all the clients, before any discount.</dd> <dt>string `m_subtotal_per_client`</dt> <dd> The price of the selected items for one client, before any discount. It is the same for every client, because a discount depends on the client type. </dd> <dt>string `m_total_batch`</dt> <dd> The amount the whole batch charges, with the client type discounts applied, the taxes accounted and surcharges added. </dd> <dt>string `m_total_surcharge`</dt> <dd> The surcharges of all the clients, each derived from the amount that single client is charged. Only the clients whose payment method the business surcharges are counted in. Zero when the client accounts are billed, or when neither `is_surcharge_ach` nor `is_surcharge_ecommerce` is requested. </dd> <dt>string `m_total_tax`</dt> <dd>The taxes of the selected items for all the clients. Zero when `is_tax` is `false`.</dd> </dl>'
                      s_id:
                        type: string
                        description: 'The review id that identifies this prepared bulk billing. Pass it to {@link BulkBillingApi} to schedule the billing without sending the clients and items again.'
      tags:
        - Bulk-PurchaseItemList
  '/Wl/Billing/Bulk/BulkBilling.json':
    delete:
      summary: 'Cancels a scheduled bulk billing so that it is never billed. Only a batch that has not started billing yet can be cancelled.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bulk-BulkBilling
    post:
      summary: 'Schedules the bulk billing. Reads the prepared data from the temporary session by the review id and schedules the billing. The eligibility and restriction checks were already done during preparation, so they are not repeated here.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_schedule_time:
                  type: [array, "null"]
                  description: 'The scheduled billing time: <dl> <dt>int `i_hour`</dt> <dd>Number of hours. Value between 1 and 12.</dd> <dt>int `i_minute`</dt> <dd>Number of minutes. Value between 0 and 59.</dd> <dt>bool `is_am`</dt> <dd>Whether hours and minutes AM or PM. `true` if AM.</dd> </dl>'
                dl_schedule:
                  type: [string, "null"]
                  description: 'The scheduled billing date (business local time). When `null` the billing runs immediately.'
                k_business:
                  type: string
                  description: 'The business key.'
                s_id:
                  type: string
                  description: 'The unique review id returned by {@link PurchaseItemListApi} that identifies the prepared bulk billing data in the temporary session.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_purchase_batch:
                        type: string
                        description: 'The purchase batch key.'
      tags:
        - Bulk-BulkBilling
    put:
      summary: 'Reschedules a previously scheduled bulk billing to a new date and time. A reschedule always targets an explicit date and time. Only a batch that has not started billing yet can be rescheduled.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_schedule_time:
                  type: [array, "null"]
                  description: 'The scheduled billing time: <dl> <dt>int `i_hour`</dt> <dd>Number of hours. Value between 1 and 12.</dd> <dt>int `i_minute`</dt> <dd>Number of minutes. Value between 0 and 59.</dd> <dt>bool `is_am`</dt> <dd>Whether hours and minutes AM or PM. `true` if AM.</dd> </dl>'
                dl_schedule:
                  type: [string, "null"]
                  description: 'The scheduled billing date (business local time). When `null` the billing runs immediately.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_purchase_batch:
                  type: string
                  description: 'The purchase batch key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bulk-BulkBilling
  '/Wl/Billing/Bulk/BulkBillingUser.json':
    delete:
      summary: 'Removes the client from the batch so that they are not billed. Only a client that has not been billed yet can be removed; a client that was already charged or failed keeps their result. The client is not deleted from the batch - they are marked cancelled and skipped when the billing runs, together with the moment of the change and the acting user.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_purchase_batch
          in: query
          description: 'The purchase batch key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key of the client to remove from the batch.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bulk-BulkBillingUser
  '/Wl/Quiz/Response/Response.json':
    delete:
      summary: 'Deletes the specified quiz response or list of responses for the given business. Validates access privileges and removes the specified response records, updating any related search indexes and activity logs.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response
    get:
      summary: 'Returns quiz response data including element answers, dates, and access information. Loads the response for the specified quiz and user, resolving answers, formatted dates, and access flags such as amendment availability and PDF generation support.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_answer
          in: query
          description: '`true` for load answers for response, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_service_info:
                        type: array
                        description: 'Information about service if response connected to visit. Empty array if the response is not connected to a visit: <dl> <dt>string `text_date`</dt> <dd>Formatted visit date and time in the location''s time zone.</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `text_service`</dt> <dd>Service title.</dd> <dt>string `text_staff_member`</dt> <dd>Comma-separated list of full names of staff members conducting the visit.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether response can be amended by current user.'
                      dtu_response:
                        type: string
                        description: 'Date when response was submitted.'
                      id_source:
                        type: number
                        format: int32
                        description: 'Response source. One of {@link SourceSid} constants.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Response status ID.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_add_date:
                        type: string
                        description: 'Date when response added.'
                      text_amend_date:
                        type: string
                        description: 'Date when response amended.'
                      text_amend_user:
                        type: string
                        description: 'Name of the user who amend the response.'
                      text_complete_date:
                        type: string
                        description: 'Date when response completed.'
                      text_complete_user:
                        type: string
                        description: 'Name of the user who complete the response.'
                      text_response_by:
                        type: string
                        description: 'Name of the user who owned the response.'
                      text_title:
                        type: [string, "null"]
                        description: 'Title of the filled form. `null` in case when not filled yet.'
      tags:
        - Response-Response
    post:
      summary: 'Saves a quiz response with the given element answers. Validates the submitted answers and persists the response record in a transaction. If {@link \Core\Quiz\QuizResponseApi::$is_validate_only} is set, only validation runs and no record is created. Pass {@link \Core\Quiz\QuizResponseApi::$is_skip} to bypass validation for pre-confirmed responses.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_element_file:
                  type: array
                  description: 'Elements'' uploaded files.'
                a_purchase_item:
                  type: array
                  description: 'List of purchase items for which this form is loaded. The list of purchase items. Each element has the format `[id_purchase_item]::[k_id]`, where: Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
                a_quick_config:
                  type: array
                  description: 'Data for Quick Buy. Empty array if this is not a Quick Buy session. <dl> <dt>int `id_table`</dt> <dd>Quick purchase type. One of {@link \Wl\Catalog\QuickPurchaseTypeSid} constant.</dd> <dt>bool `is_quick_buy`</dt> <dd> Whether quick buy mode should be used. It means that specific template will be loaded and specific cart will be used. </dd> <dt>string `k_appointment`</dt> <dd>Appointment key to pay for. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_id`</dt> <dd>Item key within <var>id_table</var> table that Quick Buy performed for.</dd> <dt>string `k_visit`</dt> <dd>Visit key to pay for. <i>Primary key in {@link \RsVisitSql}.</i></dd> <dt>string `uid`</dt> <dd>User that Quick Buy performed for.</dd> </dl>'
                id_source:
                  type: number
                  format: int32
                  description: 'Response source. One of {@link SourceSid} constants.'
                is_backend:
                  type: boolean
                  description: '`true` if API is being used from backend, `false` otherwise.'
                is_draft:
                  type: boolean
                  description: '`true` if need to save response in draft mode, `false` otherwise.'
                is_skip:
                  type: boolean
                  description: 'Whether the quiz needs to be skipped.'
                is_validate_only:
                  type: boolean
                  description: '`true` if need make only validation, `false` need make validation and saving response.'
                json_element:
                  type: string
                  description: 'List of quiz questions with responses in JSON format.'
                k_quiz_login:
                  type: string
                  description: 'Quiz login key.'
                s_secret:
                  type: string
                  description: 'Secret key for access to quiz request.'
                text_appointment_wizard_id:
                  type: [string, "null"]
                  description: 'Booking appointment wizard ID. Not `null` if quiz response was sent during booking appointment process.'
                text_class_wizard_id:
                  type: [string, "null"]
                  description: 'Booking class wizard ID. Not `null` if quiz response was sent during booking class process.'
                uid_response:
                  type: [string, "null"]
                  description: 'UID of the user who makes response for quiz.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz_response:
                        type: [string, "null"]
                        description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
      tags:
        - Response-Response
    put:
      summary: 'Validates, updates and reindex response information for associated user. Used to amend an already-submitted response, for example when a business allows clients to edit their quiz answers after submission. Re-links the response to its owner and triggers downstream reindexing so search and reporting stay consistent.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
        - name: uid_link
          in: query
          description: 'User''s key for the response association.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_quiz_response_key:
                  type: string
                  description: 'Quiz response key list.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response
  '/Wl/Quiz/Response/Response65.json':
    delete:
      summary: 'Deletes the specified quiz response or list of responses for the given business. Validates access privileges and removes the specified response records, updating any related search indexes and activity logs.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response65
    get:
      summary: 'Returns quiz response data including element answers, dates, and access information. Loads the response for the specified quiz and user, resolving answers, formatted dates, and access flags such as amendment availability and PDF generation support.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_answer
          in: query
          description: '`true` for load answers for response, `false` otherwise.'
          schema:
            type: boolean
        - name: is_backend
          in: query
          description: ''
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_service_info:
                        type: array
                        description: 'Information about service if response connected to visit. Empty array if the response is not connected to a visit: <dl> <dt>string `text_date`</dt> <dd>Formatted visit date and time in the location''s time zone.</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `text_service`</dt> <dd>Service title.</dd> <dt>string `text_staff_member`</dt> <dd>Comma-separated list of full names of staff members conducting the visit.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether response can be amended by current user.'
                      dtu_response:
                        type: string
                        description: 'Date when response was submitted.'
                      id_source:
                        type: number
                        format: int32
                        description: 'Response source. One of {@link SourceSid} constants.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Response status ID.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_add_date:
                        type: string
                        description: 'Date when response added.'
                      text_amend_date:
                        type: string
                        description: 'Date when response amended.'
                      text_amend_user:
                        type: string
                        description: 'Name of the user who amend the response.'
                      text_complete_date:
                        type: string
                        description: 'Date when response completed.'
                      text_complete_user:
                        type: string
                        description: 'Name of the user who complete the response.'
                      text_response_by:
                        type: string
                        description: 'Name of the user who owned the response.'
                      text_title:
                        type: [string, "null"]
                        description: 'Title of the filled form. `null` in case when not filled yet.'
      tags:
        - Response-Response65
    post:
      summary: 'Saves a quiz response with the given element answers. Validates the submitted answers and persists the response record in a transaction. If {@link \Core\Quiz\QuizResponseApi::$is_validate_only} is set, only validation runs and no record is created. Pass {@link \Core\Quiz\QuizResponseApi::$is_skip} to bypass validation for pre-confirmed responses.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_backend
          in: query
          description: ''
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_element_file:
                  type: array
                  description: 'Elements'' uploaded files.'
                a_purchase_item:
                  type: array
                  description: 'List of purchase items for which this form is loaded. The list of purchase items. Each element has the format `[id_purchase_item]::[k_id]`, where: Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
                a_quick_config:
                  type: array
                  description: 'Data for Quick Buy. Empty array if this is not a Quick Buy session. <dl> <dt>int `id_table`</dt> <dd>Quick purchase type. One of {@link \Wl\Catalog\QuickPurchaseTypeSid} constant.</dd> <dt>bool `is_quick_buy`</dt> <dd> Whether quick buy mode should be used. It means that specific template will be loaded and specific cart will be used. </dd> <dt>string `k_appointment`</dt> <dd>Appointment key to pay for. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_id`</dt> <dd>Item key within <var>id_table</var> table that Quick Buy performed for.</dd> <dt>string `k_visit`</dt> <dd>Visit key to pay for. <i>Primary key in {@link \RsVisitSql}.</i></dd> <dt>string `uid`</dt> <dd>User that Quick Buy performed for.</dd> </dl>'
                id_source:
                  type: number
                  format: int32
                  description: 'Response source. One of {@link SourceSid} constants.'
                is_draft:
                  type: boolean
                  description: '`true` if need to save response in draft mode, `false` otherwise.'
                is_skip:
                  type: boolean
                  description: 'Whether the quiz needs to be skipped.'
                is_validate_only:
                  type: boolean
                  description: '`true` if need make only validation, `false` need make validation and saving response.'
                json_element:
                  type: string
                  description: 'List of quiz questions with responses in JSON format.'
                k_quiz_login:
                  type: string
                  description: 'Quiz login key.'
                s_secret:
                  type: string
                  description: 'Secret key for access to quiz request.'
                text_appointment_wizard_id:
                  type: [string, "null"]
                  description: 'Booking appointment wizard ID. Not `null` if quiz response was sent during booking appointment process.'
                text_class_wizard_id:
                  type: [string, "null"]
                  description: 'Booking class wizard ID. Not `null` if quiz response was sent during booking class process.'
                uid_response:
                  type: [string, "null"]
                  description: 'UID of the user who makes response for quiz.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz_response:
                        type: [string, "null"]
                        description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
      tags:
        - Response-Response65
    put:
      summary: 'Validates, updates and reindex response information for associated user. Used to amend an already-submitted response, for example when a business allows clients to edit their quiz answers after submission. Re-links the response to its owner and triggers downstream reindexing so search and reporting stay consistent.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
        - name: uid_link
          in: query
          description: 'User''s key for the response association.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_quiz_response_key:
                  type: string
                  description: 'Quiz response key list.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response65
  '/Wl/Report/Customization/CustomizationForm.json':
    get:
      summary: 'Loads customization data of the customization form that corresponds to specified report / report page. Populates {@link CustomizationFormApi::$a_customization_form} with the customization data of the requested report or report page, optionally converted to the reports listed in {@link CustomizationFormApi::$s_report}.'
      parameters:
        - name: cid_page
          in: query
          description: 'Report page CID.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'Report CID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business primary key.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Primary key of a saved report. `null` means that report is not saved.'
          schema:
            type: [string, "null"]
        - name: s_report
          in: query
          description: 'Report CID list to that page customization form must be converted. String separated with `,`. May be specified only if {@link CustomizationFormApi::$cid_page} in not empty.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Current user''s primary key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_customization_form:
                        type: array
                        description: 'Customization form data keyed by report or page CID. Each value has the following structure: <dl> <dt>array `a_element`</dt> <dd> Form elements keyed by element name. Each element has the following structure: <dl> <dt>array `a_field`</dt> <dd> Element fields keyed by field alias. Each field has the following structure: <dl> <dt>string `text_title`</dt> <dd>Localized field title.</dd> <dt>string `x_value`</dt> <dd>Current field value.</dd> </dl> </dd> <dt>int[] `a_report`</dt> <dd>Report CID list the element applies to.</dd> <dt>string `html_title`</dt> <dd>Localized element title.</dd> <dt>string `html_title_tooltip`</dt> <dd>Localized element tooltip.</dd> </dl> </dd> <dt>string `s_form`</dt> <dd>Form class identifier.</dd> </dl>'
      tags:
        - Customization-CustomizationForm
    post:
      summary: 'Saves given data of a customization form into database. Stores the customization data supplied in {@link CustomizationFormApi::$a_customization_form} for the requested report or report page and resets the related report configuration cache.'
      parameters:
        - name: cid_page
          in: query
          description: 'Report page CID.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'Report CID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business primary key.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Primary key of a saved report. `null` means that report is not saved.'
          schema:
            type: [string, "null"]
        - name: uid_actor
          in: query
          description: 'Current user''s primary key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_customization_form:
                  type: array
                  description: 'Customization form data keyed by report or page CID. Each value has the following structure: <dl> <dt>array `a_element`</dt> <dd> Form elements keyed by element name. Each element has the following structure: <dl> <dt>array `a_field`</dt> <dd> Element fields keyed by field alias. Each field has the following structure: <dl> <dt>string `text_title`</dt> <dd>Localized field title.</dd> <dt>string `x_value`</dt> <dd>Current field value.</dd> </dl> </dd> <dt>int[] `a_report`</dt> <dd>Report CID list the element applies to.</dd> <dt>string `html_title`</dt> <dd>Localized element title.</dd> <dt>string `html_title_tooltip`</dt> <dd>Localized element tooltip.</dd> </dl> </dd> <dt>string `s_form`</dt> <dd>Form class identifier.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Customization-CustomizationForm
  '/Wl/Report/Dashboard/ReportDashboard.json':
    post:
      summary: 'Adds new report to a dashboard. Creates a saved report from the specified title, description, filters and view widget settings and attaches it as a widget to the dashboard identified by {@link ReportDashboardApi::$k_report_dashboard}.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the report.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: object
                  description: 'Filter raw data of the report. Keys are filter field names, values are the raw filter values. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: object
                  description: 'View widget settings raw data of the saved report. Keys are widget setting names, values are the raw setting values.'
                cid_controller:
                  type: number
                  format: int32
                  description: 'CID of the controller class. Subclass of {@link ReportViewControllerAbstract}.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@link ReportDashboardApi::$a_filter}. In this array, key is name of a filter field. <i> A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array. </i>'
                k_report_dashboard:
                  type: string
                  description: 'Report dashboard key to which report should be added.'
                text_description:
                  type: string
                  description: 'Description of the report.'
                text_title:
                  type: string
                  description: 'Title of the report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dashboard-ReportDashboard
    put:
      summary: 'Updates added report to a dashboard. Rewrites the title, description, filters and view widget settings of the dashboard report identified by {@link ReportDashboardApi::$k_report_save} within the current business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the report.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: object
                  description: 'Filter raw data of the report. Keys are filter field names, values are the raw filter values. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: object
                  description: 'View widget settings raw data of the saved report. Keys are widget setting names, values are the raw setting values.'
                cid_controller:
                  type: number
                  format: int32
                  description: 'CID of the controller class. Subclass of {@link ReportViewControllerAbstract}.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@link ReportDashboardApi::$a_filter}. In this array, key is name of a filter field. <i> A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array. </i>'
                k_report_dashboard:
                  type: string
                  description: 'Report dashboard key to which report should be added.'
                text_description:
                  type: string
                  description: 'Description of the report.'
                text_title:
                  type: string
                  description: 'Title of the report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dashboard-ReportDashboard
  '/Wl/Report/Favorite/ReportFavorite.json':
    delete:
      summary: 'Removes report controller from favorites. Deletes the favorite record for the current user that matches either the specified saved report or the report controller within the current business. See {@link \RsReportFavorite::check()}.'
      parameters:
        - name: cid_controller
          in: query
          description: 'CID of the controller.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which request is performed.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Saved report key to manage.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Favorite-ReportFavorite
    get:
      summary: 'Returns information whether passed report controller (saved report controller) is favorite for the specified user within specified business. Populates {@link ReportFavoriteApi::$is_favorite} with the current favorite state for the specified saved report or report controller. See {@link \RsReportFavorite::check()}.'
      parameters:
        - name: cid_controller
          in: query
          description: 'CID of the controller.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which request is performed.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Saved report key to manage.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_favorite:
                        type: boolean
                        description: 'Whether report is favorite.'
      tags:
        - Favorite-ReportFavorite
    post:
      summary: 'Adds report controller to favorites. Creates a favorite record for the current user that references either the specified saved report or the report controller within the current business. See {@link \RsReportFavorite::check()}.'
      parameters:
        - name: cid_controller
          in: query
          description: 'CID of the controller.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which request is performed.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Saved report key to manage.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Favorite-ReportFavorite
  '/Wl/Report/Save/ReportSave.json':
    get:
      summary: 'Returns information about saved report. Loads the title, description, category and stored filters of the saved report identified by {@link ReportSaveApi::$k_report_save} within the current business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the saved report.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_filter:
                        type: object
                        description: 'Filter raw data of the saved report. Keys are filter field names, values are the raw filter values. This variable do not get empty values of filters from HTML forms.'
                      id_report_category:
                        type: number
                        format: int32
                        description: 'Report category of the saved report. One of the {@link \RsReportCategorySid} constants.'
                      text_description:
                        type: string
                        description: 'Description of the saved report.'
                      text_title:
                        type: string
                        description: 'Title of the saved report.'
      tags:
        - Save-ReportSave
    post:
      summary: 'Saves new saved report. Creates a new saved report for the current business from the specified title, description, category, filters and view widget settings, and stores the related customization for the report controller.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the saved report.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: object
                  description: 'Filter raw data of the saved report. Keys are filter field names, values are the raw filter values. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: object
                  description: 'View widget settings raw data of the saved report. Keys are widget setting names, values are the raw setting values.'
                cid_controller:
                  type: number
                  format: int32
                  description: 'CID of the controller class.'
                id_report_category:
                  type: number
                  format: int32
                  description: 'Report category of the saved report. One of the {@link \RsReportCategorySid} constants.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@link \Wl\Report\Save\ReportSaveApi::$a_filter}. In this array, key is name of a filter field. <i> A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array. </i>'
                k_report_config:
                  type: string
                  description: 'Report save key used as a base configuration when saving a new report.'
                text_description:
                  type: string
                  description: 'Description of the saved report.'
                text_title:
                  type: string
                  description: 'Title of the saved report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Save-ReportSave
    put:
      summary: 'Updates existing saved report. Rewrites the title, description, category, filters and view widget settings of the saved report identified by {@link ReportSaveApi::$k_report_save} within the current business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the saved report.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: object
                  description: 'Filter raw data of the saved report. Keys are filter field names, values are the raw filter values. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: object
                  description: 'View widget settings raw data of the saved report. Keys are widget setting names, values are the raw setting values.'
                cid_controller:
                  type: number
                  format: int32
                  description: 'CID of the controller class.'
                id_report_category:
                  type: number
                  format: int32
                  description: 'Report category of the saved report. One of the {@link \RsReportCategorySid} constants.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@link \Wl\Report\Save\ReportSaveApi::$a_filter}. In this array, key is name of a filter field. <i> A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array. </i>'
                text_description:
                  type: string
                  description: 'Description of the saved report.'
                text_title:
                  type: string
                  description: 'Title of the saved report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Save-ReportSave
  '/Wl/Promotion/Index/PromotionIndex.json':
    get:
      summary: 'Gets a list of packages/passes/memberships. Returns introductory promotion offers available at the specified location, optionally filtered by program type, including pricing, duration, visit limits, and access information for each item.'
      parameters:
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: id_program_type
          in: query
          description: 'The program type ID, which will be one of the {@link \RsProgramTypeSid} constants. `0` to not filter Purchase Options with type of the Purchase Option.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'If `true`, purchase options are loaded for backend mode. Otherwise, this will be `false` if purchase options are loaded for frontend mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. `null` to get a business from location.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'A list of introductory promotion offers available at the location. Every element is an array with the following keys: <dl> <dt>array `a_access`</dt> <dd> Information about services that can be attended with this pass or membership. <dl> <dt>array[] `a_class`</dt> <dd> A list of available classes. <dl> <dt>string `k_class`</dt> <dd>Key of the class.</dd> </dl> </dd> <dt>array[] `a_event`</dt> <dd> A list of available events. <dl> <dt>string `k_class`</dt> <dd>Key of the event.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of available assets. <dl> <dt>string `k_resource`</dt> <dd>Key of the asset.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> A list of available appointment types. <dl> <dt>string `k_service`</dt> <dd>Key of the appointment type.</dd> </dl> </dd> <dt>bool `is_class_all`</dt> <dd> If `true`, any class in the business can be visited with this Purchase Option. If `false`, only selected classes can be visited. Selected classes can be found in the list of available classes and events. </dd> <dt>bool `is_event_all`</dt> <dd> If `true`, if any event in the business can be visited with this Purchase Option. If `false`, if only selected events can be visited. Selected events can be found in the list of available classes and events. </dd> </dl> </dd> <dt>array[] `a_component`</dt> <dd> A list of components included in the Purchase Option if this Purchase Option is a package. If it''s not a package, the list will be always empty. If it''s a package, the list can still be empty if all its components were deactivated. This means that this array can''t be used to determine whether this is a package or not (check `id_program` or `id_program_type` value instead). There can be three types of components: events, Purchase Options, and products. Some fields can be different depending on type of the component, but each element of the array includes: <dl> <dt>int `id_purchase_item`</dt> <dd> The type of the component. One of {@link \RsPurchaseItemSid} constants. This can only be: <ul> <li>{@link \RsPurchaseItemSid::ENROLLMENT}</li> <li>{@link \RsPurchaseItemSid::PROMOTION}</li> <li>{@link \RsPurchaseItemSid::PRODUCT}</li> </ul> </dd> <dt>string `k_id`</dt> <dd> The primary key of the component in the related table. This depends on the type of the component. The key of the event for {@link \RsPurchaseItemSid::ENROLLMENT}, the key of the Purchase Option for the {@link \RsPurchaseItemSid::PROMOTION}, the key of the product option for the {@link \RsPurchaseItemSid::PRODUCT}. Full information about events can be taken from the {@link \Wl\Event\EventListApi}. Purchase Options from the {@link \Wl\Promotion\Index\PromotionIndexApi}. Full information about products are not available at this moment though API. </dd> <dt>int `i_quantity`</dt> <dd>The quantity. If empty, it means the quantity is 1.</dd> <dt>string `text_title`</dt> <dd>The name of the component.</dd> </dl> </dd> <dt>array `a_image`</dt> <dd> This will be an empty array if the Purchase Option doesn''t have image. Otherwise, this will display the following information about the Purchase Option image: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>string `url-thumbnail`</dt> <dd>The link to the image.</dd> </dl> </dd> <dt>array `a_visit_limit`</dt> <dd> Attendance restrictions, if available. If unavailable, this will be an empty array. Every element has a key, which is a type of the time period {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}, {@link \ADurationSid::YEAR}. The values are: <dl> <dt>int `i_limit`</dt> <dd>The quantity of sessions every <var>i_period</var>.</dd> <dt>int `i_period`</dt> <dd>The duration of the time period. This depends on a key of <var>a_visit_limit</var> array.</dd> <dt>int `i_roll_over_cap`</dt> <dd>The limit number of rollover sessions.</dd> <dt>int `i_roll_over_expire`</dt> <dd>The duration of the time period after which rolled over session will expire.</dd> <dt>int `id_roll_over_expire`</dt> <dd> The type of <var>i_roll_over_expire</var>. One of {@link \ADurationSid} contantant. This can only be: <ul> <li>{@link \ADurationSid::DAY}</li> <li>{@link \ADurationSid::WEEK}</li> <li>{@link \ADurationSid::MONTH}</li> <li>{@link \ADurationSid::YEAR}</li> </ul> </dd> <dt>int `id_limit_cycle`</dt> <dd>The type of the limit cycle {@link \Wl\Promotion\Edit\Limit\Cycle\Sid}.</dd> <dt>bool `is_reconcile_visit`</dt> <dd>Determines whether to reconcile unpaid sessions on restrictions reset.</dd> <dt>bool `is_roll_over_expire`</dt> <dd>Determines if the rolled over session is expired.</dd> <dt>bool `is_roll_over_renew`</dt> <dd>Determines whether to rollover sessions upon auto-renew.</dd> <dt>bool `is_rollup`</dt> <dd>Determines Whether to rollover sessions.</dd> <dt>bool `is_visit_conversion`</dt> <dd>Determines whether to convert remaining visits.</dd> </dl> </dd> <dt>string `dl_expire`</dt> <dd> The local expiration date. This is the last day of the Purchase Option''s active status. This is used only for the ''Expires on a certain date'' duration type. Otherwise, this should be ignored. </dd> <dt>int `i_duration`</dt> <dd> The number of periods for the "Period" duration type. For example, if the duration of the Purchase Option is 12 months, this field will be 12. </dd> <dt>int `i_limit`</dt> <dd> The number of visits that the owner of the Purchase Option can use. This will be `0` if the Purchase Option has unlimited visits. </dd> <dt>int `i_limit_duration`</dt> <dd> The maximum number of minutes or hours depending on <var>id_limit_duration</var> that the current promotion can be used in. </dd> <dt>int `i_payment_period`</dt> <dd>The number of `id_duration` periods between payments for memberships.</dd> <dt>int `id_limit_duration`</dt> <dd> The type of <var>i_limit_duration</var>. One of {@link \ADurationSid} contantant. This can only be: <ul> <li>{@link \ADurationSid::MINUTE}</li> <li>{@link \ADurationSid::HOUR}</li> <li>{@link \ADurationSid::MONTH}</li> <li>{@link \ADurationSid::YEAR}</li> </ul> </dd> <dt>int `id_duration`</dt> <dd> The type of periods for the duration type "Period". For example, if the duration of the Purchase Option is 12 months, this field will be the ID of the month. See all the possible options here: {@link \ADurationSid}. </dd> <dt>int `id_duration_type`</dt> <dd> The type of the duration. This can be "Without End", "Expires on a certain date", "Period". See more information here: {@link \RsDurationTypeSid}. </dd> <dt>int `id_program`</dt> <dd> The type of the Purchase Option. The <var>id_program</var> relates to only one <var>id_program_type</var> and one <var>id_program_category</var>. See more information here: {@link \RsProgramSid}. </dd> <dt>int `id_program_category`</dt> <dd> The category of the program for promotions. The <var>id_program_category</var> relates to more than one <var>id_program</var>. See more information here: {@link \RsProgramCategorySid}. </dd> <dt>int `id_program_type`</dt> <dd> The ID of the promotion program type. The <var>id_program_type</var> relates to more than one <var>id_program</var>. See more information here: {@link \RsProgramTypeSid}. </dd> <dt>bool `is_direct_buy_only`</dt> <dd> If `true`, the pricing option can be sold only by direct link. This means it shouldn''t be shown in the store or explorer. Otherwise, this will be `false`. </dd> <dt>bool `is_introductory`</dt> <dd>If `true`, this promotion is for introductory clients. Otherwise, this will be `false`.</dd> <dt>bool `is_online`</dt> <dd>If `true` this promotion is available for online purchase. Otherwise, this will be `false`.</dd> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The price.</dd> <dt>string `text_description`</dt> <dd>The description.</dd> <dt>string `text_program`</dt> <dd>The localized promotion program name corresponding to the value of <var>id_program</var>.</dd> <dt>string `text_title`</dt> <dd>The title.</dd> <dt>string `url_buy`</dt> <dd>The direct URL to the promotion purchase page.</dd> </dl>'
      tags:
        - Index-PromotionIndex
  '/Wl/Login/Promotion/PromotionPayPause.json':
    delete:
      summary: 'Deletes specified promotion payment pause. Validates access and then permanently removes the hold period identified by `k_promotion_pay_pause`, also cancelling any associated expiry reminder notification task.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-PromotionPayPause
    get:
      summary: 'Returns promotion payment pause data: all hold periods when {@link PromotionPayPauseApi::$is_list} is `true`, the specified hold period when {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided, or the currently active hold period otherwise. Also returns notification settings (email, push, SMS flags and email pattern key) and the date the last notification was sent for the hold period, if a hold notification template is configured for the business.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: dt_start
          in: query
          description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: is_list
          in: query
          description: 'Whether need to get all pause periods for the login promotion. If <tt>true</tt> then {@link PromotionPayPauseApi::$a_pay_pause_list} will be returned. If <tt>false</tt> then information about specified {@link PromotionPayPauseApi::$k_promotion_pay_pause} or currently active pause period will be returned ({@link PromotionPayPauseApi::$dt_start}, {@link PromotionPayPauseApi::$dt_end} and {@link PromotionPayPauseApi::$text_note}).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_pause_list:
                        type: [array, "null"]
                        description: 'List of all promotion payment pause periods. Each element has next structure: <tt>null</tt> if {@link PromotionPayPauseApi::$is_list} is false. <dl> <dt>string|null `dl_create`</dt> <dd>Date when this hold period was created. <tt>null</tt> for old records.</dd> <dt>string `dl_end`</dt> <dd> Ending date of the pause (inclusively - this date is paused). This field contains zero date for promotions placed on hold indefinitely. </dd> <dt>string `dl_start`</dt> <dd>Starting date of the pause (inclusively - this date is paused).</dd> <dt>int|null `i_hold_day`</dt> <dd>Duration of the hold in days. <tt>null</tt> if the hold is ongoing.</dd> <dt>bool `is_past`</dt> <dd>Whether the hold is in past.</dd> <dt>string `k_promotion_pay_pause`</dt> <dd>Key of the hold period. <i>Primary key in the {@link \RsPromotionPayPauseSql} table.</i></dd> <dt>string|null `text_note`</dt> <dd>Additional notes.</dd> <dt>string|null `text_user_create`</dt> <dd>Full name of a user that has created hold period. <tt>null</tt> for old records.</dd> <dt>string|null `uid_create`</dt> <dd>Key of a user that has created hold period. <tt>null</tt> for old records.</dd> </dl>'
                      a_payment_schedule:
                        type: array
                        description: 'List of all upcoming payments for the give pricing option. Just dates in local timezone.'
                      dt_end:
                        type: [string, "null"]
                        description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
                      dt_start:
                        type: [string, "null"]
                        description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
                      dtu_date_notification:
                        type: [string, "null"]
                        description: 'The date when the email notification was sent. `null` if it shouldn''t be updated.'
                      is_mail:
                        type: boolean
                        description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                      is_push:
                        type: boolean
                        description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                      is_sms:
                        type: boolean
                        description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
                      k_mail_pattern:
                        type: [string, "null"]
                        description: 'Key of the email pattern.'
                      k_promotion_pay_pause:
                        type: [string, "null"]
                        description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
                      text_note:
                        type: [string, "null"]
                        description: 'Additional notes for the promotion payment pause period. Leave this field as `null` if the note shouldn''t be updated. `null` if it shouldn''t be updated.'
      tags:
        - Promotion-PromotionPayPause
    post:
      summary: 'Adds or updates a payment pause period for promotion. Creates a new hold period for the purchased promotion if no `k_promotion_pay_pause` is provided, or updates an existing one. Optionally schedules or sends a hold notification via email, push, or SMS based on the provided flags and the business notification template.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: dt_start
          in: query
          description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_mail:
                  type: boolean
                  description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                is_push:
                  type: boolean
                  description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                is_sms:
                  type: boolean
                  description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                text_note:
                  type: [string, "null"]
                  description: 'Additional notes for the promotion payment pause period. Leave this field as `null` if the note shouldn''t be updated. `null` if it shouldn''t be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_promotion_pay_pause:
                        type: [string, "null"]
                        description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
      tags:
        - Promotion-PromotionPayPause
    put:
      summary: 'Updates a promotion payment pause period. Requires an existing `k_promotion_pay_pause` key and delegates to {@link PromotionPayPauseApi::post()} to apply the updated start date, end date, note, and notification settings. Returns an error if no existing hold key is provided.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: dt_start
          in: query
          description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_mail:
                  type: boolean
                  description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                is_push:
                  type: boolean
                  description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                is_sms:
                  type: boolean
                  description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                text_note:
                  type: [string, "null"]
                  description: 'Additional notes for the promotion payment pause period. Leave this field as `null` if the note shouldn''t be updated. `null` if it shouldn''t be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_promotion_pay_pause:
                        type: [string, "null"]
                        description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
      tags:
        - Promotion-PromotionPayPause
  '/Wl/Login/Coupon/Coupon.json':
    get:
      summary: 'Retrieves the key and balance of a gift card by its code for the specified business. Validates the gift card code against the specified business, enforces a per-IP rate limit, and checks that the card is active, not already redeemed, not expired, and in the correct currency before returning its key and remaining balance.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: s_code
          in: query
          description: 'The gift card.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_coupon:
                        type: string
                        description: 'The gift card reference number for this specific user. WellnessLiving uses this reference number for managing gift cards in the system.'
                      m_amount:
                        type: string
                        description: 'The gift card amount.'
      tags:
        - Coupon-Coupon
  '/Wl/Login/Mail/MailUse.json':
    get:
      summary: 'Checks if specified user exists in specified business. Accepts a business key and an email address, validates both, and returns `true` if a user with that email is an active member of the business. Requests are rate-limited per IP unless the caller has the required privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business for which the email address search is being performed.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The email address to check for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exists:
                        type: boolean
                        description: 'If <tt>true</tt>, the user with the specified email address exists in specified business. Otherwise, this will be <tt>false</tt>.'
                      uid_use:
                        type: [string, "null"]
                        description: 'Key of the user who using email within the business.'
      tags:
        - Mail-MailUse
  '/Wl/Login/Member/Member.json':
    get:
      summary: 'Returns the list of businesses where the specified user is an active member. Accepts a user key and returns all active businesses where that user has a membership, including franchise relationship flags, business title, and whether the user is allowed to sign in to each business. Forwards the request to other data centers in multi-region deployments and merges the results.'
      parameters:
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: array
                        description: 'A list of businesses where the client is present. Every element is an array with the following keys: <dl> <dt>bool `can_enter`</dt> <dd> <tt>true</tt> if the user is allowed to sign in into this business; <tt>false</tt> otherwise. <i>This value is based on {@link \Wl\Login\Disable\LoginDisableSql} table.</i> </dd> <dt>bool `is_franchisee`</dt> <dd>If `true`, then the business is a franchisee. Otherwise, this will be `false`.</dd> <dt>bool `is_franchisor`</dt> <dd>If `true`, then the business is a franchisor. Otherwise, this will be `false`.</dd> <dt>string `k_business`</dt> <dd>The business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_business_franchisor`</dt> <dd> The business key of the Enterprise Headquarters account (if applicable). <i>Primary key in {@link \RsBusinessSql} table.</i> </dd> <dt>string `text_title`</dt> <dd>The title of the business.</dd> </dl>'
      tags:
        - Member-Member
    post:
      summary: 'Adds a user into a business. Validates that the current user has profile access, then registers the specified user as a member of the given business without sending a registration email.'
      parameters:
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Primary key of the business to add the user into.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Member-Member
  '/Wl/Login/Member/MemberValidate63.json':
    get:
      summary: 'Checks whether anything prevents the user from using the business and returns details about missing required fields. Extends the base validation by additionally returning separate lists of missing profile fields grouped by their required context: booking and purchase, self-registration, or general requirement.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_empty_fields_booking:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for booking. <dl> <dt>int `id_field_general`</dt> <dd>The general field ID. One of {@link \RsFieldGeneralSid} constants.</dd> <dt>string `k_field`</dt> <dd>Profile field key. <i>Primary key in the {@link \RsFieldSql} table.</i></dd> <dt>string `text_field_title`</dt> <dd>Human-readable field title describing what value is missing.</dd> </dl>'
                      a_empty_fields_registration:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for registration. <dl> <dt>int `id_field_general`</dt> <dd>The general field ID. One of {@link \RsFieldGeneralSid} constants.</dd> <dt>string `k_field`</dt> <dd>Profile field key. <i>Primary key in the {@link \RsFieldSql} table.</i></dd> <dt>string `text_field_title`</dt> <dd>Human-readable field title describing what value is missing.</dd> </dl>'
                      a_empty_fields_required:
                        type: array
                        description: 'List of profile fields that are required but empty for this user. <dl> <dt>int `id_field_general`</dt> <dd>The general field ID. One of {@link \RsFieldGeneralSid} constants.</dd> <dt>string `k_field`</dt> <dd>Profile field key. <i>Primary key in the {@link \RsFieldSql} table.</i></dd> <dt>string `text_field_title`</dt> <dd>Human-readable field title describing what value is missing.</dd> </dl>'
                      has_credit_card:
                        type: boolean
                        description: '`true` If the user has credit cards on profile, otherwise `false`.'
                      has_outstanding_contract:
                        type: boolean
                        description: '`true` if the user has an outstanding contract, otherwise `false`.'
                      has_outstanding_waiver:
                        type: boolean
                        description: '`true` If the user has an outstanding waiver for the business, otherwise `false`.'
                      has_pending_quizzes:
                        type: boolean
                        description: '`true` If the user has pending registration quizzes to complete, otherwise `false`.'
                      is_booking_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details before booking, otherwise `false`.'
                      is_register_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details to finish their registration, otherwise `false`.'
                      k_location:
                        type: [string, "null"]
                        description: 'Home user`s location. `null` if user has not home location.'
      tags:
        - Member-MemberValidate63
  '/Wl/Login/Member/MemberValidate.json':
    get:
      summary: 'Checks whether anything prevents the user from using the business and returns any blocking conditions found. Returns flags indicating whether the user has an outstanding waiver, an unconfirmed contract, pending registration quizzes, a required credit card on file, and lists of missing required profile fields for booking and registration.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_empty_fields_booking:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for booking.'
                      a_empty_fields_registration:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for registration.'
                      has_credit_card:
                        type: boolean
                        description: '`true` If the user has credit cards on profile, otherwise `false`.'
                      has_outstanding_contract:
                        type: boolean
                        description: '`true` if the user has an outstanding contract, otherwise `false`.'
                      has_outstanding_waiver:
                        type: boolean
                        description: '`true` If the user has an outstanding waiver for the business, otherwise `false`.'
                      has_pending_quizzes:
                        type: boolean
                        description: '`true` If the user has pending registration quizzes to complete, otherwise `false`.'
                      is_booking_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details before booking, otherwise `false`.'
                      is_register_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details to finish their registration, otherwise `false`.'
                      k_location:
                        type: [string, "null"]
                        description: 'Home user`s location. `null` if user has not home location.'
      tags:
        - Member-MemberValidate
  '/Wl/Login/Member/LoginMember.json':
    get:
      summary: 'Gets client ID for a specific client in a business. Validates the business key and user key, checks profile access, and returns the member ID string assigned to the client in the specified business. Returns an empty string if the user has no member ID set.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_member:
                        type: string
                        description: 'The user''s ID to get/set.'
      tags:
        - Member-LoginMember
    post:
      summary: 'Updates client ID for a specific client in a business. Validates and saves the new member ID for the given client in the specified business, reindexes the user for search, and returns the date the user originally became a member.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: s_member
          in: query
          description: 'The user''s ID to get/set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dt_member:
                        type: string
                        description: 'The date when client became a member.'
      tags:
        - Member-LoginMember
  '/Wl/Login/Member/LoginMemberListAll.json':
    get:
      summary: 'Returns a paginated list of active member user keys for the specified business. Accepts a business key, an optional page size, and an optional last-seen user key for cursor-based pagination, then returns an ordered array of user keys for all active members of that business.'
      parameters:
        - name: i_page_size
          in: query
          description: 'The maximum number of members to return in the list. Use with `uid_last` to get the next page of results.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to get a list of members for.'
          schema:
            type: string
        - name: uid_last
          in: query
          description: 'The last member UID from the previous page. Use with `i_page_size` to get the next page of results. If empty, the first page of results is returned.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid:
                        type: array
                        description: 'List of UIDs for all active clients that belong to the business.'
      tags:
        - Member-LoginMemberListAll
  '/Wl/Login/Add/MailUseOk.json':
    post:
      summary: 'Checks required profile fields and, if complete, registers the existing user in the specified business. Validates the user''s profile for any missing required fields and, if all fields are complete, adds the user as a member of the business. Returns a status code and a list of any fields that still need to be filled in.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_lead:
                  type: boolean
                  description: 'This value is `true` if this user is a lead. `false` if otherwise.'
                k_business:
                  type: string
                  description: 'The business key used internally by WellnessLiving.'
                k_skin:
                  type: string
                  description: 'The Lead Capture Widget skin key. This is used only if a lead was added.'
                uid:
                  type: string
                  description: 'The user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: array
                        description: 'The list of fields with missing information. Each element is an array containing the following data: <dl> <dt>string `html_message`</dt> <dd>The error message.</dd> <dt>string `s_field`</dt> <dd>The name of the field missing information. Given in the format `k_field.name`.</dd> </dl>'
                      s_code:
                        type: string
                        description: 'The result code of the request.'
                      text_message:
                        type: string
                        description: 'The result message of the request.'
      tags:
        - Add-MailUseOk
  '/Wl/Login/Attendance/AttendanceInfo.json':
    get:
      summary: 'Returns detailed information about a single class period, appointment, or asset session. Accepts either a class period key with a local date or an appointment key, validates access, and returns scheduling details including title, start and end times, location, staff, assets, service type, and default purchase option information.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'Start date of the class in MySQL format in local time.'
          schema:
            type: string
        - name: k_appointment
          in: query
          description: 'ID of appointment to get information for.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business to get information for.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'ID of class period to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_visit_info:
                        type: array
                        description: 'Additional visit information about this appointment. Empty array if it''s not an appointment: <dl> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl>'
                      a_logo:
                        type: array
                        description: 'Service logo information: <dl> <dt>bool `is_empty`</dt> <dd>Whether service image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl>'
                      a_purchase_option_default:
                        type: array
                        description: 'Default purchase option information. <dl> <dt>int|null `id_sale`</dt> <dd> The sale item type, one of the {@link \RsSaleSid} constants. This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>string|null `k_id`</dt> <dd> The default Purchase Option key. <i>Primary key in {@link \RsPromotionSql} table.</i> This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>bool `is_single_default`</dt> <dd>If the default Purchase Option is set to "Drop-in rate" then the value will be `true`, `false` otherwise.</dd> </dl>'
                      a_resource:
                        type: array
                        description: 'Assets which are bound to this session.'
                      a_resource_layout:
                        type: array
                        description: 'Asset layouts of session: <dl> <dt>array `a_client`</dt> <dd> List of clients who occupy assets of class. It is a double nesting array. Keys - <i>primary keys of assets. See {@link \RsResourceSql} table.</i> Sub keys - asset index. Values - sub array with keys: <dl> <dt>string `text_client`</dt> <dd>User''s name.</dd> <dt>string `uid`</dt> <dd>User''s primary key.</dd> </dl> </dd> <dt>string[] `a_resource_available`</dt> <dd>Key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_layout`</dt> <dd>Key of layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `text_resource_type`</dt> <dd>Title of asset category.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'List of staff members who provide service: <dl> <dt>array `a_logo`</dt> <dd> Data of staff member''s photo. Empty if staff has no photo. Otherwise contains next keys: <dl> <dt>int `i_height`</dt> <dd>Height in pixels.</dd> <dt>int `i_width`</dt> <dd>Width in pixels.</dd> <dt>string `url_logo`</dt> <dd>Image URL.</dd> </dl> </dd> <dt>string `k_staff`</dt> <dd>deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `html_firstname`</dt> <dd>Staff member''s first name.</dd> <dt>string `html_lastname`</dt> <dd>Staff member''s last name.</dd> </dl>'
                      dt_confirm:
                        type: string
                        description: 'Confirmation date+time of appointment in MySQL format. If client never confirmed, will be zero date + time.'
                      dt_date_global:
                        type: string
                        description: 'Start date of the session in MySQL format in GMT.'
                      dtl_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in local timezone.'
                      dtu_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in GMT.'
                      dtu_wait_promote:
                        type: string
                        description: 'Date and time in UTC when the visit is promoted from wait list to active list. Not empty for appointments.'
                      has_note:
                        type: boolean
                        description: 'Whether notes added to visit.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'Duration of the session in minutes.'
                      id_note:
                        type: [number, object]
                        description: 'Type of note. One of {@link \Wl\Visit\Note\Sid\NoteSid} constants. <tt>false</tt> if notes not allowed.'
                      id_service:
                        type: number
                        format: int32
                        description: 'Service type, one of {@link \RsServiceSid}.'
                      is_start_virtual_service:
                        type: boolean
                        description: 'Whether this service be carried out in Zoom. <tt>true</tt> - If the service can be carried out in Zoom. <tt>false</tt> - otherwise.'
                      k_class:
                        type: string
                        description: 'Class identifier. Not empty if service is class or event reservation.'
                      k_location:
                        type: string
                        description: 'Location identifier.'
                      k_resource:
                        type: string
                        description: 'Resource identifier. Not empty if service is asset reservation.'
                      k_service:
                        type: string
                        description: 'Service identifier. Not empty if service is appointment reservation.'
                      text_location:
                        type: string
                        description: 'Location name.'
                      text_time_end:
                        type: string
                        description: 'Time when service starts in format <tt>8:00AM</tt>.'
                      text_time_start:
                        type: string
                        description: 'Start time of the session in format ''9:30AM''.'
                      text_title:
                        type: string
                        description: 'Title of the appointment.'
                      url_booking:
                        type: string
                        description: 'URL that leads directly to the class/event booking page in the Client Web App. Empty string if the service is an appointment reservation.'
      tags:
        - Attendance-AttendanceInfo
  '/Wl/Login/Attendance/AttendanceInfoByToken.json':
    get:
      summary: 'Returns detailed information about a single class period, appointment, or asset session. Accepts either a class period key with a local date or an appointment key, validates access, and returns scheduling details including title, start and end times, location, staff, assets, service type, and default purchase option information.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'Start date of the class in MySQL format in local time.'
          schema:
            type: string
        - name: k_appointment
          in: query
          description: 'ID of appointment to get information for.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business to get information for.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'ID of class period to get information for.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_visit_info:
                        type: array
                        description: 'Additional visit information about this appointment. Empty array if it''s not an appointment: <dl> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl>'
                      a_logo:
                        type: array
                        description: 'Service logo information: <dl> <dt>bool `is_empty`</dt> <dd>Whether service image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl>'
                      a_purchase_option_default:
                        type: array
                        description: 'Default purchase option information. <dl> <dt>int|null `id_sale`</dt> <dd> The sale item type, one of the {@link \RsSaleSid} constants. This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>string|null `k_id`</dt> <dd> The default Purchase Option key. <i>Primary key in {@link \RsPromotionSql} table.</i> This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>bool `is_single_default`</dt> <dd>If the default Purchase Option is set to "Drop-in rate" then the value will be `true`, `false` otherwise.</dd> </dl>'
                      a_resource:
                        type: array
                        description: 'Assets which are bound to this session.'
                      a_resource_layout:
                        type: array
                        description: 'Asset layouts of session: <dl> <dt>array `a_client`</dt> <dd> List of clients who occupy assets of class. It is a double nesting array. Keys - <i>primary keys of assets. See {@link \RsResourceSql} table.</i> Sub keys - asset index. Values - sub array with keys: <dl> <dt>string `text_client`</dt> <dd>User''s name.</dd> <dt>string `uid`</dt> <dd>User''s primary key.</dd> </dl> </dd> <dt>string[] `a_resource_available`</dt> <dd>Key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_layout`</dt> <dd>Key of layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `text_resource_type`</dt> <dd>Title of asset category.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'List of staff members who provide service: <dl> <dt>array `a_logo`</dt> <dd> Data of staff member''s photo. Empty if staff has no photo. Otherwise contains next keys: <dl> <dt>int `i_height`</dt> <dd>Height in pixels.</dd> <dt>int `i_width`</dt> <dd>Width in pixels.</dd> <dt>string `url_logo`</dt> <dd>Image URL.</dd> </dl> </dd> <dt>string `k_staff`</dt> <dd>deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `html_firstname`</dt> <dd>Staff member''s first name.</dd> <dt>string `html_lastname`</dt> <dd>Staff member''s last name.</dd> </dl>'
                      dt_confirm:
                        type: string
                        description: 'Confirmation date+time of appointment in MySQL format. If client never confirmed, will be zero date + time.'
                      dt_date_global:
                        type: string
                        description: 'Start date of the session in MySQL format in GMT.'
                      dtl_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in local timezone.'
                      dtu_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in GMT.'
                      dtu_wait_promote:
                        type: string
                        description: 'Date and time in UTC when the visit is promoted from wait list to active list. Not empty for appointments.'
                      has_note:
                        type: boolean
                        description: 'Whether notes added to visit.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'Duration of the session in minutes.'
                      id_note:
                        type: [number, object]
                        description: 'Type of note. One of {@link \Wl\Visit\Note\Sid\NoteSid} constants. <tt>false</tt> if notes not allowed.'
                      id_service:
                        type: number
                        format: int32
                        description: 'Service type, one of {@link \RsServiceSid}.'
                      is_start_virtual_service:
                        type: boolean
                        description: 'Whether this service be carried out in Zoom. <tt>true</tt> - If the service can be carried out in Zoom. <tt>false</tt> - otherwise.'
                      k_class:
                        type: string
                        description: 'Class identifier. Not empty if service is class or event reservation.'
                      k_location:
                        type: string
                        description: 'Location identifier.'
                      k_resource:
                        type: string
                        description: 'Resource identifier. Not empty if service is asset reservation.'
                      k_service:
                        type: string
                        description: 'Service identifier. Not empty if service is appointment reservation.'
                      text_location:
                        type: string
                        description: 'Location name.'
                      text_time_end:
                        type: string
                        description: 'Time when service starts in format <tt>8:00AM</tt>.'
                      text_time_start:
                        type: string
                        description: 'Start time of the session in format ''9:30AM''.'
                      text_title:
                        type: string
                        description: 'Title of the appointment.'
                      url_booking:
                        type: string
                        description: 'URL that leads directly to the class/event booking page in the Client Web App. Empty string if the service is an appointment reservation.'
      tags:
        - Attendance-AttendanceInfoByToken
  '/Wl/Login/Attendance/AttendanceList.json':
    get:
      summary: 'Returns the attendance list for a class period or appointment session. Accepts either a class period key with a local date or an appointment key, validates access and date, and returns the active list, wait list, and confirmed list of attending clients, along with session capacity, wait list limit, and per-client details such as purchase option, visit status, wearables, and quiz responses.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'The local date of the class or event session.'
          schema:
            type: string
        - name: is_purchase_info_return
          in: query
          description: 'If `true`, then return the purchase used to pay for session. Otherwise, do not return any purchase information.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. Not used if requesting information for a class or event session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. Not used if requesting information for an appointment.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list_active:
                        type: array
                        description: 'The list of clients in the active attendance list who haven''t confirmed or canceled. Each element is an array with the following fields: <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_confirm:
                        type: array
                        description: 'The list of clients who have confirmed their attendance. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_wait:
                        type: array
                        description: 'The list of clients who are on the wait list. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      i_capacity:
                        type: number
                        format: int32
                        description: 'The maximum capacity of the class or event session.'
                      i_client:
                        type: number
                        format: int32
                        description: 'Count client on the attendance.'
                      i_wait_list_limit:
                        type: number
                        format: int32
                        description: 'The maximum number of clients on wait list of the class or event session. `0` for appointments, use {@link AppointmentWaitListApi} instead.'
                      is_wait_list_limit:
                        type: boolean
                        description: '`true` to use class/event specific wait list limit, `false` to use the limit from default policies. `false` for appointments. Use {@link AppointmentWaitListApi} instead.'
                      k_location:
                        type: string
                        description: 'The Location key.'
      tags:
        - Attendance-AttendanceList
  '/Wl/Login/Attendance/AttendanceListByToken.json':
    get:
      summary: 'Returns the attendance list for a class period or appointment session. Accepts either a class period key with a local date or an appointment key, validates access and date, and returns the active list, wait list, and confirmed list of attending clients, along with session capacity, wait list limit, and per-client details such as purchase option, visit status, wearables, and quiz responses.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'The local date of the class or event session.'
          schema:
            type: string
        - name: is_purchase_info_return
          in: query
          description: 'If `true`, then return the purchase used to pay for session. Otherwise, do not return any purchase information.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. Not used if requesting information for a class or event session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. Not used if requesting information for an appointment.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list_active:
                        type: array
                        description: 'The list of clients in the active attendance list who haven''t confirmed or canceled. Each element is an array with the following fields: <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_confirm:
                        type: array
                        description: 'The list of clients who have confirmed their attendance. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_wait:
                        type: array
                        description: 'The list of clients who are on the wait list. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      i_capacity:
                        type: number
                        format: int32
                        description: 'The maximum capacity of the class or event session.'
                      i_client:
                        type: number
                        format: int32
                        description: 'Count client on the attendance.'
                      i_wait_list_limit:
                        type: number
                        format: int32
                        description: 'The maximum number of clients on wait list of the class or event session. `0` for appointments, use {@link AppointmentWaitListApi} instead.'
                      is_wait_list_limit:
                        type: boolean
                        description: '`true` to use class/event specific wait list limit, `false` to use the limit from default policies. `false` for appointments. Use {@link AppointmentWaitListApi} instead.'
                      k_location:
                        type: string
                        description: 'The Location key.'
      tags:
        - Attendance-AttendanceListByToken
  '/Wl/Login/Type/LoginType.json':
    get:
      summary: 'Gets a login types list of a business. Returns all client types configured for the specified business, each with its key, title, client type ID, and a deprecated membership flag. If `is_franchisor` is `true`, returns the combined login types of all franchisees under the franchisor business.'
      parameters:
        - name: is_franchisor
          in: query
          description: 'If `true`, this `k_business` is a franchisor, and login types of all franchisees should be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key used internally by WellnessLiving.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_type_list:
                        type: array
                        description: 'A list of login types, keys, and information. Each element is an array with the following information: <dl> <dt>int `id_client_type`</dt> <dd>Client Type ID. One of {@link \Wl\Login\Type\ClientTypeSid} constants.</dd> <dt>bool `is_member`</dt> <dd> Deprecated Use <var>id_client_type</var> instead. It will be `1` if the record contains a type of member. It will be `0` if record contains some other type of client. It will be `null` if the record contains some type of prospect. </dd> <dt>string `k_login_type`</dt> <dd>The login type key.</dd> <dt>string `s_title`</dt> <dd>The name of the login type.</dd> <dt>string `text_title`</dt> <dd>Same as <var>s_title</var> offset.</dd> </dl>'
      tags:
        - Type-LoginType
  '/Wl/Login/Rank/LoginRankElement.json':
    delete:
      summary: 'Deletes a rank record for a user. Removes the rank record from the database and logs the action in the belt history.'
      parameters:
        - name: is_rank_category
          in: query
          description: 'To delete entire rank category from this user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_rank
          in: query
          description: 'Login rank key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Rank-LoginRankElement
  '/Wl/Login/Search/Concerto.json':
    get:
      summary: 'Finds a user by their email or phone within the specified business. Accepts exactly one of `text_mail` or `text_phone`, validates the value, and returns the user key if a matching active member of the business is found. Returns an error if the user is not found or if multiple users share the given phone number.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to search user in.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Email to search.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'Phone to search.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'User''s primary key.'
      tags:
        - Search-Concerto
  '/Wl/Login/Product/Product.json':
    get:
      summary: 'Returns information about products purchased by client. Accepts filters for business, user, location, payment method, product option, price range, and date range, then returns a paginated list of product purchases with quantity, unit price, total amount, product name, and purchase location.'
      parameters:
        - name: dtu_end
          in: query
          description: 'Maximal date and time of purchase in UTC. Empty string means no filter by maximal date.'
          schema:
            type: string
        - name: dtu_start
          in: query
          description: 'Minimal date and time of purchase in UTC. Empty string means no filter by minimal date.'
          schema:
            type: string
        - name: i_page
          in: query
          description: 'Number of the page to get.'
          schema:
            type: number
            format: int32
        - name: i_page_size
          in: query
          description: 'Page size.'
          schema:
            type: number
            format: int32
        - name: id_pay_method
          in: query
          description: 'ID of the payment method. One of the {@link \RsPayMethodSid} constants. Zero means no filter by payment method.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Empty string means no filter by location.'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'Product option key. Empty string means no filter by product option.'
          schema:
            type: string
        - name: m_price_max
          in: query
          description: 'Maximum price of the product. 0 means no filter by maximum price.'
          schema:
            type: string
        - name: m_price_min
          in: query
          description: 'Minimum price of the product.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Empty string means return products purchased by walk-in.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_product:
                        type: array
                        description: 'List of purchased products: <dl> <dt>string `dtu_purchase`</dt> <dd>Date and time of purchase in UTC.</dd> <dt>int `i_quantity`</dt> <dd>Quantity of the product purchased.</dd> <dt>string `k_shop_product_option`</dt> <dd>Key of product. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> <dt>string `m_price`</dt> <dd>Price per unit of the product.</dd> <dt>string `m_total_amount`</dt> <dd>Total amount paid for the product.</dd> <dt>string `text_location`</dt> <dd>Location where the purchase was made.</dd> <dt>string `text_product`</dt> <dd>Name of the purchased product.</dd> </dl>'
      tags:
        - Product-Product
  '/Wl/Login/Permission/Permission.json':
    post:
      summary: 'Saves the auto-renew setting for a purchased promotion. Validates the purchased promotion, checks profile access, and sets the auto-renew flag to the value of `is_renew`. Returns an error if the promotion does not support auto-renewal or if the client is a debtor.'
      parameters:
        - name: k_login_promotion
          in: query
          description: 'The key of the purchased promotion.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_renew:
                  type: boolean
                  description: 'If `true`, the purchased promotion has been made auto-renewable. Otherwise, this will be `false`.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Permission-Permission
  '/Wl/Login/Agree/Agree.json':
    get:
      summary: 'Returns text of business liability release if business has liability release and if user did not agree to this liability release. Accepts the business key and the user key, validates both, and returns the rendered HTML of the liability release contract if one is configured and the user has not yet agreed to it. If the user has already agreed, the response will have an empty `html_contract` field.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_contract:
                        type: string
                        description: 'Formatted text of business liability release. Not empty if the business has a liability release and if the user did not agree to this liability release.'
      tags:
        - Agree-Agree
    post:
      summary: 'Saves the user''s agreement to the online waiver. Accepts the user''s base64-encoded signature image and an optional electronic-signature consent flag, validates both, records the agreement date and signature in the database, and sends a waiver confirmation notification.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_agree:
                  type: [boolean, "null"]
                  description: '<tt>false</tt> if the user has not agreed to use Electronic Signatures, <tt>true</tt> if the user has agreed to use Electronic Signatures, <tt>null</tt> otherwise.'
                s_signature:
                  type: string
                  description: 'The base64-encoded user''s signature image from the signature pad as a PNG file.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Agree-Agree
  '/Wl/Appointment/Edit/AddonUpdate.json':
    get:
      summary: 'Return data about appointment''s add-ons. Returns the current list of add-ons attached to the specified appointment along with the full catalog of available add-ons for the service. The caller must have view access to the appointment.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to get add-ons for. Not empty only when getting add-ons for new appointment ({@link AddonUpdateApi::$k_appointment} is null). User key ''-1'' means walk-in, user key ''0'' means new user (user will be created together with appointment).'
          schema:
            type: array
        - name: k_appointment
          in: query
          description: 'The appointment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key. This will be an empty string if not set yet.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Not empty only when getting add-ons for new appointment ({@link AddonUpdateApi::$k_appointment} is null).'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service key. Not empty only when getting add-ons for new appointment ({@link AddonUpdateApi::$k_appointment} is null).'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_addon_data:
                        type: array
                        description: 'Data to show appointment add-ons: <dl> <dt>array[] `a_addon`</dt> <dd> Data about appointment add-ons. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>array[] `a_addon_buy`</dt> <dd> Add-ons available for purchase: zero quantity, zero usage quantity, and zero banked quantity. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>array[] `a_addon_own`</dt> <dd> Add-ons already owned but not selected: zero quantity, zero usage quantity, non-zero banked quantity. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>array[] `a_addon_select`</dt> <dd> Add-ons selected for this appointment: non-zero quantity or non-zero usage quantity. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>bool `is_addon_banking`</dt> <dd>Whether at least one of appointment add-ons is bankable.</dd> <dt>bool `is_all_addon_selected`</dt> <dd>Whether all appointment add-ons have non-zero quantity or non-zero usage quantity.</dd> <dt>bool `is_search`</dt> <dd>Determines whether the add-on search field needs to be shown.</dd> </dl>'
      tags:
        - Edit-AddonUpdate
    put:
      summary: 'Replaces the add-ons for the appointment with the provided list, optionally updating the appointment duration. Removes all existing add-ons from the appointment and attaches the provided set in their place. When add-ons with a duration are included, the appointment end time is recalculated accordingly. The operation runs inside a database transaction to ensure consistency.'
      parameters:
        - name: k_appointment
          in: query
          description: 'The appointment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key. This will be an empty string if not set yet.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_addon:
                  type: [array]
                  description: 'The appointment addon-ons. Old format - an array where each value is key of the add-on. New format - each element is an array: <dl> <dt>int `i_product`</dt> <dd>The add-on count. Max value is 255.</dd> <dt>int `i_use`</dt> <dd>The add-on use count. Max value is 255. Not set means same value as add-on count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The add-on key. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl>'
                is_duration_update:
                  type: boolean
                  description: 'Determines whether the appointment duration needs to be updated. <i>When called from {@link rs_appointment_edit_save_change_trx()} duration already updated.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-AddonUpdate
  '/Wl/Appointment/Info/Info.json':
    get:
      summary: 'Gets information about appointment. Returns detailed information about the specified appointment, including service details, staff member, date and time in the location''s timezone, booking status, client information, and any associated assets or add-ons. Access is validated against the current user''s permissions.'
      parameters:
        - name: k_appointment
          in: query
          description: 'Appointment key to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_next:
                        type: array
                        description: 'Next appointment data, or empty array if there are no appointments in the future: <dl> <dt>string `dt_date_local`</dt> <dd>Start date and time of the next appointment in local time in MySQL format.</dd> <dt>bool `has_note`</dt> <dd>`true` if visit has a note, `false` otherwise.</dd> <dt>int `i_duration`</dt> <dd>Duration of the next appointment in minutes.</dd> <dt>bool `can_view`</dt> <dd>`true` if employees (staff) can view this appointment, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Next appointment key. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_visit`</dt> <dd>Visit key of next appointment.</dd> <dt>string `text_appointment_title`</dt> <dd>Title of next appointment.</dd> <dt>string `text_staff_name`</dt> <dd>Full staff name or empty if no staff assigned.</dd> </dl>'
                      a_previous:
                        type: array
                        description: 'Previous appointment data, or empty array if there are no appointments in the past: <dl> <dt>string `dt_date_local`</dt> <dd>Start date and time of the previous appointment in local time in MySQL format.</dd> <dt>bool `has_note`</dt> <dd>`true` if visit has a note, `false` otherwise.</dd> <dt>int `i_duration`</dt> <dd>Duration of the previous appointment in minutes.</dd> <dt>bool `can_view`</dt> <dd>`true` if employees (staff) can view this appointment, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Previous appointment key. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_visit`</dt> <dd>Visit key of previous appointment.</dd> <dt>string `text_appointment_title`</dt> <dd>Title of previous appointment.</dd> <dt>string `text_staff_name`</dt> <dd>Full staff name or empty if no staff assigned.</dd> </dl>'
                      a_question:
                        type: array
                        description: 'List of questions and answers: <dl> <dt>int `i_size`</dt> <dd>Size of rows for answer.</dd> <dt>bool `is_multiple`</dt> <dd>`true` if `i_size` greater than 1, `false` otherwise. Can be empty if answer is loaded.</dd> <dt>string `s_answer`</dt> <dd>Answer for `s_question`.</dd> <dt>string `s_key`</dt> <dd>Answer key.</dd> <dt>string `s_question`</dt> <dd>Question.</dd> </dl>'
                      a_repeat:
                        type: array
                        description: 'Repeat settings for appointment reschedule. Empty array for non-recurring appointment. Has next keys: <dl> <dt>int[] `a_week`</dt> <dd>Days of week to repeat appointment. Constants from {@link \ADateWeekSid}.</dd> <dt>string `dl_edit_from`</dt> <dd>Start date for range edit in location timezone. This is a current appointment date.</dd> <dt>string `dl_edit_to`</dt> <dd> End date for range edit in location timezone. This is a date of last created appointment in repeatable group. </dd> <dt>string `dl_end`</dt> <dd> Date when the repeat cycle stops, in location timezone. Empty if the repeat cycle does not stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> Number of occurrences after that the repeat cycle stops. `0` if the repeat cycle does not stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>Frequency of the repeats. For example, `2` for every second week.</dd> <dt>int `id_end`</dt> <dd>Type of repeat cycle end. One of {@link \RsRepeatEndSid} constants.</dd> <dt>int `id_period`</dt> <dd>Measurement unit of `i_period`. One of {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> `true` if the appointment repeats monthly at the same date. `false` if the appointment repeats monthly at the same week day or does not repeat monthly. </dd> <dt>string `s_time`</dt> <dd> Current appointment local start time in MySQL time format (`HH:MM:SS`). <p><b>WARNING!</b></p> <p>If the session you are starting the reschedule with was previously changed, its time may NOT match the time of the remaining sessions.</p> </dd> </dl>'
                      a_resource:
                        type: array
                        description: 'List of assets used by this appointment. Each element contains: <dl> <dt>int `i_color_background`</dt> <dd>Background color of the asset as an integer (RGB).</dd> <dt>int `i_color_border`</dt> <dd>Border color of the asset as an integer (RGB).</dd> <dt>int `i_index`</dt> <dd>Index of the booked asset slot.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the asset was removed from the booking, `false` otherwise.</dd> <dt>string `k_resource`</dt> <dd>Asset key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_type`</dt> <dd>Asset category key. <i>Primary key in the {@link \RsResourceTypeSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>Asset name.</dd> <dt>string `s_resource_type`</dt> <dd>Asset category name.</dd> <dt>string `text_resource_alias`</dt> <dd>Display alias for the asset slot, if configured.</dd> </dl>'
                      a_shop_product_option:
                        type: array
                        description: 'List of appointment add-ons. Every element has next keys: <dl> <dt>string[] `a_login_product`</dt> <dd> List of purchased product keys. Empty if no products were purchased. Each element: <dl> <dt>string `k_login_product`</dt> <dd>Purchased product key. <i>Primary key in the {@link \RsLoginProductSql} table.</i></dd> </dl> </dd> <dt>string `k_login_product`</dt> <dd>Deprecated, always `null`.</dd> <dt>string `k_shop_product`</dt> <dd>Primary key of add-on.</dd> <dt>string `k_shop_product_option`</dt> <dd>Add-on option. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> <dt>string `m_amount`</dt> <dd>Price that it adds to an appointment.</dd> </dl>'
                      dt_date_local:
                        type: string
                        description: 'Date/time of appointment in location timezone.'
                      i_duration:
                        type: [number, "null"]
                        description: 'Appointment duration (in minutes).'
                      i_index:
                        type: [number, "null"]
                        description: 'Index of booked asset.'
                      id_appointment_pay:
                        type: number
                        format: int32
                        description: 'Status of appointment payment. One of {@link \RsAppointmentPaySid} constants.'
                      k_location:
                        type: string
                        description: 'Location key.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'Purchased promotion which provides this appointment.'
                      k_resource:
                        type: [string, "null"]
                        description: 'Asset key.'
                      k_resource_type:
                        type: [string, "null"]
                        description: 'Asset category key.'
                      k_service:
                        type: [string, "null"]
                        description: 'Service key.'
                      k_service_category:
                        type: [string, "null"]
                        description: 'Service category key.'
                      k_session_pass:
                        type: [string, "null"]
                        description: 'Purchased drop-in which provides this appointment.'
                      k_staff:
                        type: string
                        description: 'Staff member who conducts this appointment. Deprecated: returned only for a limited list of third-party apps to keep backward compatibility. Use `uid_staff` instead.'
                      text_title:
                        type: string
                        description: 'Title of the appointment.'
                      uid_appointment:
                        type: string
                        description: 'User for whom this appointment was booked.'
                      uid_staff:
                        type: [string, "null"]
                        description: 'Staff member who conducts this appointment.'
      tags:
        - Info-Info
  '/Wl/Appointment/Recent/RecentService.json':
    get:
      summary: 'Gets list of client''s last booked services. Returns the most recently booked unique services for the given client at the given business, filtered by service type (appointment or bookable asset). Duplicate services are collapsed so only the most recent booking per service is included, up to {@link RecentServiceApi::MAX_SERVICE_COUNT}.'
      parameters:
        - name: i_visit
          in: query
          description: 'Count of last booked services to return. Default value is 5.'
          schema:
            type: number
            format: int32
        - name: id_service
          in: query
          description: 'Type of service to return. One of {@link ServiceSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service_last:
                        type: array
                        description: 'List of last booked services.'
      tags:
        - Recent-RecentService
  '/Wl/Passport/Login/Info.json':
    get:
      summary: 'Returns the current user''s login information and password reset URL for the specified business. Validates the business key, applies any business-specific redemption settings, then delegates to the parent implementation to return the current user ID and password reset URL.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: [string, "null"]
                        description: 'The current user key. This will be `null` if the user isn''t signed in.'
                      url_password_change:
                        type: string
                        description: 'A URL that a user can visit to reset their password.'
                      url_register:
                        type: string
                        description: 'The URL to the registration page.'
      tags:
        - Login-Info
  '/Wl/Discount/Code/DiscountCode.json':
    get:
      summary: 'Returns discount codes of the specified business. Used in the backend discount management UI to show the full list of discount codes for a business so the staff member can select one to view or edit.'
      parameters:
        - name: a_event_filter
          in: query
          description: 'List of event keys to filter the discount codes by. A discount code is returned if it applies to any of the given events, to any of the memberships in {@link DiscountCodeApi::$a_promotion_filter}, or to all purchase items. If both {@link DiscountCodeApi::$a_event_filter} and {@link DiscountCodeApi::$a_promotion_filter} are empty, discount codes are not filtered by applicability.'
          schema:
            type: array
        - name: a_promotion_filter
          in: query
          description: 'List of membership and passes keys to filter the discount codes by. A discount code is returned if it applies to any of the given memberships, to any of the events in {@link DiscountCodeApi::$a_event_filter}, or to all purchase items. If both {@link DiscountCodeApi::$a_event_filter} and {@link DiscountCodeApi::$a_promotion_filter} are empty, discount codes are not filtered by applicability.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key of the discount codes.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'Discount codes list. Each element has the following structure: <dl> <dt>bool `is_active`</dt> <dd>`true` if the discount code is active, `false` otherwise.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the discount code is removed, `false` otherwise.</dd> <dt>string `k_discount_code`</dt> <dd>Discount code key. <i>Primary key in the {@link \Wl\Discount\Code\Sql} table.</i></dd> <dt>string `s_code`</dt> <dd>Actual discount code string.</dd> <dt>string `text_discount_code`</dt> <dd>Discount code title.</dd> </dl>'
      tags:
        - Code-DiscountCode
  '/Wl/Business/Account/BusinessAccount.json':
    get:
      summary: 'Generates list of active business keys for the same region as the requesting user (proper permissions required). Used internally by WellnessLiving operations tools to enumerate all customer businesses in the current region. Returns only active, non-test businesses; set `is_prospects` to also include prospect businesses that have not yet churned. Requires the `rs.business.view` privilege.'
      parameters:
        - name: is_prospects
          in: query
          description: '`true` returns WellnessLiving customers. `false` returns prospects. Test and churned businesses are never returned.'
          schema:
            type: boolean
        - name: is_published
          in: query
          description: 'Specifies if only businesses having published locations should be returned.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_keys:
                        type: array
                        description: 'A list of business keys.'
      tags:
        - Account-BusinessAccount
  '/Wl/Business/Config/BusinessConfig.json':
    get:
      summary: 'Gets information about a business config. Used by the booking widget and checkout flow to know the rules governing client interactions: which family member relations are allowed, whether clients or staff must select a location at checkout, what cancellation penalties apply, and whether white-label mode is active.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_policy:
                        type: array
                        description: 'All business policies connected to clients and bookings. <dl> <dt>int[] `a_payment_reattempt_not_decline_reason`</dt> <dd> List of not allowed decline reasons to payment reattempt. Each element is one of {@link \Thoth\WlPay\PayExceptionSid} constants. </dd> <dt>bool[] `a_wait_service`</dt> <dd>Keys are list of IDs from {@link \Wl\Service\ServiceSid}, and values are flags whether wait list is allowed.</dd> <dt>int `i_book_before`</dt> <dd>Minimum hours|days|months before class should be booked.</dd> <dt>int `i_book_future`</dt> <dd>Maximum hours|days|months after class can be booked.</dd> <dt>int `i_cancel`</dt> <dd>Minimum hours|days|months before class should be canceled without penalty.</dd> <dt>int `i_promote`</dt> <dd>Minimum hours|days|months before class should be promoted from wait list.</dd> <dt>int `i_promote_fastest_response`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation required should be sent to all clients on the wait list at the same time up to the cut off time. </dd> <dt>int `i_promote_require_confirm`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation must confirm their a promote from the waiting list to the active list. </dd> <dt>int `i_reattempt_count`</dt> <dd>Number of failed auto-payments reattempts.</dd> <dt>int `id_book_before`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_book_future`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_cancel`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_fastest_response`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_require_confirm`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>bool `is_book_inside_active_pay_period`</dt> <dd> if `true` - clients with purchase options are only allowed to book sessions within their current paid period, `false` - during purchase option''s duration. </dd> <dt>bool `is_disable_promotion`</dt> <dd> 1 if a client''s automatic payment fails, their account should not be debited and their purchase option becomes inactive, 0 - otherwise. Default 0. </dd> <dt>bool `is_enable_payment_penalty`</dt> <dd>Whether to charge penalty after final auto-payment attempt.</dd> <dt>bool `is_enable_payment_reattempt`</dt> <dd>Whether to reattempt failed auto-payments.</dd> <dt>bool `is_enable_staff_ip_restriction`</dt> <dd>Whether to restrict which IP addresses staff can login from.</dd> <dt>bool `is_prevent_booking`</dt> <dd>1 if booking for a client with negative balance is disabled, 0 - otherwise. Default 0.</dd> <dt>bool `is_staff_restrict`</dt> <dd>If true, client can not choose provider while appointment wizard.</dd> <dt>bool `is_wait`</dt> <dd>Enable\disable wait list.</dd> <dt>string `k_currency`</dt> <dd>Currency from rs.currency table.</dd> <dt>string `k_timezone`</dt> <dd>Timezone from get.timezone table.</dd> <dt>string `m_payment_penalty`</dt> <dd>Penalty amount to charge after final auto-payment attempt.</dd> <dt>string `url_custom`</dt> <dd>Custom Url of a business</dd> <dt>string[] `a_family_relation`</dt> <dd> List of allowed relation types specific to a given business. Key is ID. One of {@link \RsFamilyRelationSid} constant. Value is SID. <i>Result of {@link \RsFamilyRelationSid::allByBusiness()} method.</i> </dd> <dt>int[] `a_family_relation_login_allow`</dt> <dd> List of allowed relation types specific to a given business. Each value constant from {@link \RsFamilyRelationSid} class. </dd> </dl>'
                      a_penalty:
                        type: array
                        description: 'A list of business penalties. <dl> <dt>string[] `a_class_period`</dt> <dd>List of class period keys. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string[] `a_login_type`</dt> <dd> List of client type keys. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i> Empty array means all active login types at concrete business. </dd> <dt>string[] `a_resource`</dt> <dd>List of resources keys. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_service`</dt> <dd>List of services keys. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>int `i_blame`</dt> <dd>Number of blamed visits.</dd> <dt>int `i_cancel_period`</dt> <dd>Count of days/weeks/months.</dd> <dt>int `i_charge_measure`</dt> <dd>Count of applied penalty.</dd> <dt>int `id_blame`</dt> <dd>One of {@link \Wl\Business\Policy\BlameSid} constants.</dd> <dt>int `id_cancel_period`</dt> <dd>Duration ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_charge`</dt> <dd>One of {@link \Wl\Business\Policy\ChargeSid} constants.</dd> <dt>bool `is_appointment_all`</dt> <dd>`1` if all classes are selected, `0` - otherwise.</dd> <dt>bool `is_charge_measure_money`</dt> <dd>`true` for a fixed money amount, `false` for a percentage.</dd> <dt>bool `is_class_all`</dt> <dd>`1` if all classes are selected, `0` - otherwise.</dd> <dt>bool `is_enable_cancel_charge`</dt> <dd>`true` if promotion penalties is enable, `false` otherwise.</dd> <dt>bool `is_event_all`</dt> <dd>`1` if all events are selected, `0` - otherwise.</dd> <dt>bool `is_login_type_all`</dt> <dd>`1` if all login types are selected, `0` - otherwise.</dd> <dt>bool `is_resource_all`</dt> <dd>`1` if all resources are selected, `0` - otherwise.</dd> <dt>bool `is_service_all`</dt> <dd>`1` if all services are selected, `0` - otherwise.</dd> </dl>'
                      is_location_client_select:
                        type: boolean
                        description: 'Whether client must select a location at checkout.'
                      is_location_select:
                        type: boolean
                        description: 'Determines whether staff members should select a location at checkout. If `true`, staff members should select a location at checkout. Otherwise, this will be `false`.'
                      is_white_label:
                        type: boolean
                        description: 'Determines whether the business has white label setting enabled in the admin settings. If `true`, admin settings are enabled. Otherwise, this will be `false`.'
      tags:
        - Config-BusinessConfig
  '/Wl/Business/Waiver/Waiver.json':
    get:
      summary: 'Returns the business waiver text rendered as HTML with user-specific variables substituted. Used to display the waiver agreement to a client before they complete a purchase or check in. Renders the waiver template with the client''s name filled in. If the business has no waiver configured, `has_waiver` is `false` and the waiver modal should not be shown.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of business to get waiver for.'
          schema:
            type: string
        - name: text_fullname
          in: query
          description: 'Full username. Used as a variable in a contract. It may be set if user does not exist.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key for which the waiver is shown. Used to fill the variables in the contract Not necessarily if the user does not already exist. In this case, you need to set {@link WaiverApi::$text_fullname}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_waiver:
                        type: boolean
                        description: 'Does the business have a waiver or not?'
                      html_waiver:
                        type: string
                        description: 'Text of the current waiver with the substituted variables.'
                      url_waiver:
                        type: string
                        description: 'URL to the waiver page. This page is only available to logged-in clients.'
      tags:
        - Waiver-Waiver
  '/Wl/Business/Partner/PartnerSettings.json':
    get:
      summary: 'Returns partner settings for the business identified by the given partner code. Used when rendering the Partner Program landing page. Resolves the partner code to a business and returns the business key and the home tour type so the landing page can show the appropriate demo tour. A daily failed-request limit per IP prevents brute-force enumeration of partner codes.'
      parameters:
        - name: text_code
          in: query
          description: 'The unique code that a business can provide other businesses to tell them about system.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_business_tour:
                        type: number
                        format: int32
                        description: 'The homepage tour of the business, which depends on the type.'
                      k_business:
                        type: string
                        description: 'The business key.'
                      text_name_first:
                        type: string
                        description: 'The first name of the business representative.'
                      text_name_last:
                        type: string
                        description: 'The last name of the business representative.'
                      text_position:
                        type: string
                        description: 'The job title of the business representative.'
                      text_review:
                        type: string
                        description: 'The text of the review about the WellnessLiving system.'
                      url_photo:
                        type: [string, "null"]
                        description: 'A link to the photo of the business representative. `null` in case when image is not uploaded.'
      tags:
        - Partner-PartnerSettings
  '/Wl/Business/Partner/PartnerCode.json':
    get:
      summary: 'Returns the partner URL for the specified business. Used to display or share the business''s unique Partner Program landing page link. Partner URLs are public information, so no access check is performed.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_partner:
                        type: string
                        description: 'Link to the landing campaign'
      tags:
        - Partner-PartnerCode
    post:
      summary: 'Updates partner url for a business that takes part in the partner program. Allows a Partner Program member to set a memorable custom vanity code for their referral landing page. The code must be unique across all businesses and contain only letters, digits, and underscores.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_code:
                  type: string
                  description: 'Unique code that business can give other businesses to tell them about system.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Partner-PartnerCode
  '/Wl/Business/Search/BusinessSearchUser.json':
    get:
      summary: 'Searches for businesses by name and email address and returns matching business keys. Used to locate a specific business for a given client, for example during a password reset or membership lookup. Returns only businesses where the email belongs to a registered member, so a match confirms the client actually has an account at that business.'
      parameters:
        - name: is_customer
          in: query
          description: 'If `true`, the return only active customers, `false` - all business.'
          schema:
            type: boolean
        - name: text_mail
          in: query
          description: 'User email to search business keys.'
          schema:
            type: string
        - name: text_name
          in: query
          description: 'Given business name to search by.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Array with business keys that contain given name in their title.'
      tags:
        - Search-BusinessSearchUser
  '/Wl/Business/Search/BusinessSearch.json':
    get:
      summary: 'Searches for businesses by name and returns matching business keys. Used in internal tools and admin pages to find a business by name or key when only partial information is known. Protected by a captcha to prevent automated enumeration. In strict mode, the title must match exactly; otherwise partial word matches are returned sorted by relevance.'
      parameters:
        - name: is_customer
          in: query
          description: 'If `true`, the return only active customers, `false` - all business.'
          schema:
            type: boolean
        - name: is_strict
          in: query
          description: 'If `true`, returns only active customer businesses, if `false`, returns all businesses.'
          schema:
            type: boolean
        - name: text_name
          in: query
          description: 'Given business name to search by.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Array with business keys that contain given name in their title.'
      tags:
        - Search-BusinessSearch
  '/Wl/Business/Design/BusinessDesign.json':
    get:
      summary: 'API method to get business design data. Used to render the client-facing schedule and booking widget with the business''s custom branding. Returns the color theme, header layout, logo style, capacity display preferences, and analytics tracking IDs (Google Analytics, Google Tag Manager, Facebook Pixel). Pass no business key to get the system default design.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to get design data. May be <tt>null</tt>. In this case default business design data is returned.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'Design data for a business. Contains the following keys: <dl> <dt>bool `hide_past_days`</dt> <dd>Clients only see today''s and upcoming sessions.</dd> <dt>int `id_layout`</dt> <dd>Layout ID. For more details, see {@link \RsPageFrontendHeaderLayoutSid}.</dd> <dt>int `id_logo_position`</dt> <dd>Logo position ID. For more details, see {@link \RsBusinessDesignLogoPositionSid}.</dd> <dt>int `id_logo_style`</dt> <dd>Logo style ID. For more details, see {@link \RsBusinessDesignLogoStyleSid}.</dd> <dt>bool `is_attend_self`</dt> <dd>If <tt>true</tt> clients can check-in using the Client Web App and Achieve Web App.</dd> <dt>bool `is_appointment_capacity`</dt> <dd>If <tt>true</tt> the capacity and number of clients signed up to the appointment will be shown.</dd> <dt>bool `is_class_capacity`</dt> <dd>If <tt>true</tt> the capacity and number of clients signed up to the class will be shown.</dd> <dt>bool `is_class_quick`</dt> <dd>If <tt>true</tt>, the quick class filter will be shown.</dd> <dt>bool `is_class_spot_only`</dt> <dd> `true` to display only available spots (e.g., "3 spots left"); `false` to display both available spots and total capacity (e.g., "3 spots left of 10"). </dd> <dt>bool `is_show_name`</dt> <dd>If <tt>true</tt>, the business name will be shown.</dd> <dt>string `s_color_background`</dt> <dd>Background color of frontend.</dd> <dt>string `s_color_header`</dt> <dd>Header color of frontend menu.</dd> <dt>string `s_color_menu_border`</dt> <dd>Border color of frontend menu.</dd> <dt>string `s_color_menu_element`</dt> <dd>Menu element color</dd> <dt>string `s_color_menu_hover`</dt> <dd>Hover colour of front end menu.</dd> <dt>string `s_color_menu_hover_background`</dt> <dd>Background hover color of front end menu.</dd> <dt>string `s_color_menu_press`</dt> <dd>Press colour of front end menu.</dd> <dt>string `s_color_submenu`</dt> <dd>Submenu colour of front end menu.</dd> <dt>string `s_color_submenu_element`</dt> <dd>Submenu element color of front end menu.</dd> <dt>string `s_color_submenu_hover`</dt> <dd>Hover color of submenu of front end menu</dd> <dt>string `s_color_submenu_press`</dt> <dd>Press color of submenu in front end menu.</dd> <dt>string `s_fb_pixel_id`</dt> <dd>Facebook Pixel ID. Used for Facebook analytics tracking. Empty string if tracking is disabled.</dd> <dt>string `s_ga_tracking_id`</dt> <dd>Google Analytics Tracking ID. Used for Google Analytics tracking. Empty string if tracking is disabled.</dd> <dt>string `s_gtm_container_id`</dt> <dd>Google Tag Manager Container ID. Used for Google Analytics tracking. Empty string if tracking is disabled.</dd> <dt>string `s_url_background`</dt> <dd>Path to the background image.</dd> </dl>'
      tags:
        - Design-BusinessDesign
  '/Wl/Business/Type/BusinessTypeList.json':
    get:
      summary: 'Returns the list of available business types with their categories and images. Used during the new business signup flow so the prospective client can select the type of business they are opening (e.g., yoga studio, gym, spa). The list drives both the type picker UI and the selection of the appropriate demo tour to show on the marketing site.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_type:
                        type: array
                        description: 'A list of business types. Each element has the next structure: <dl> <dt>array `a_image_list`</dt> <dd> Images list for business types. Each element has the next structure: <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is_resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to `false`, value returned in <var>url_thumbnail</var> equals value in <var>url_view</var>. </dd> <dt>string `url_thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger than specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise, link to original image is returned here. </dd> <dt>string `url_view`</dt> <dd>Url to original image in file storage.</dd> </dl> </dd> <dt>int `id_business_category`</dt> <dd>The business category. One of the {@link \RsBusinessCategorySid} constants.</dd> <dt>int `id_tour`</dt> <dd> Type of the demo tour on the sales site connected to the business type. More about tours: {@link \RsHomeTourSid}. </dd> <dt>string `k_business_type`</dt> <dd>The key of business type. <i>Primary key in the {@link \RsBusinessTypeSql} table.</i></dd> <dt>string `text_category`</dt> <dd>The title of the business category.</dd> <dt>string `text_category_description`</dt> <dd>The description of the business category.</dd> <dt>string `text_category_icon`</dt> <dd>The icon of the business category.</dd> <dt>string `text_file`</dt> <dd>System name of the business type.</dd> <dt>string `text_title`</dt> <dd>The title of business type.</dd> </dl>'
      tags:
        - Type-BusinessTypeList
  '/Wl/Business/AmazonRegion/AmazonRegion.json':
    get:
      summary: 'Returns the Amazon region ID for each of the requested business keys. Used to determine which data center stores the data for a given set of businesses. Knowing the region is required before routing API calls across data centers. Returns `0` for businesses whose region is not set. All requested business keys must exist.'
      parameters:
        - name: a_business
          in: query
          description: 'List of business key.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_region:
                        type: array
                        description: 'List of region IDs for {@link AmazonRegionApi::$a_business}. Key - is business key. Value - is region ID. One of {@link AmazonRegionSid} constants. `0` if this business is from an unknown region.'
      tags:
        - AmazonRegion-AmazonRegion
  '/Wl/Business/AuthorizePartner/AuthorizePartner.json':
    get:
      summary: 'Grants or denies access to business location for a partner. Allows a franchisor staff member to temporarily let another user into a franchisee location. Requires the "Manage Franchise Location" privilege. Granted access expires after 24 hours; revoking schedules removal within 15 minutes.'
      parameters:
        - name: is_grant
          in: query
          description: 'Determines whether the user will be granted access or if access will be revoked. If `true`, access will be granted. If `false`, access will be revoked. Revoking access is a scheduled task set to be run within the next 15 minutes.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'The key of the franchisee location to enter.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user who will be granted access.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AuthorizePartner-AuthorizePartner
  '/Wl/Business/Claim/BusinessClaim.json':
    get:
      summary: 'Checks if the email or phone number has already been used for a trial account or not. Called during the self-onboarding flow before creating a new business to detect duplicate registrations early. Checks across all data center regions. Rate-limited per IP to prevent abuse.'
      parameters:
        - name: text_mail
          in: query
          description: 'The email address of the location. Urlencoded email address is expected.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'The phone number of the business, staff and location. Urlencoded phone number is expected.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claim-BusinessClaim
    post:
      summary: 'Completes the verification process of the trial business. Used by WellnessLiving staff to confirm a new trial business after reviewing the signup. Marks the business as verified by the approving user. Returns the location microsite URL so the staff member can share it with the new client.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of the business to be verified.'
                k_location:
                  type: string
                  description: 'The key of the location to be verified.'
                uid_verified:
                  type: string
                  description: 'The key of the user who verified the business.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_microsite:
                        type: [string, "null"]
                        description: 'Location microsite URL. <i>Result of {@link rs_microsite_url()} function.</i>'
      tags:
        - Claim-BusinessClaim
    put:
      summary: 'Saves the Self-Setup wizard form data in the business claim log. Called at the end of the Self-Setup wizard to store the wizard input for internal auditing. Can only be called once per business after it has been claimed.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_form:
                  type: array
                  description: 'The Self-Setup wizard form data to be saved in the business claim log. The structure is conditionally arbitrary and is used only for logging purposes. No specific keys are required or validated.'
                k_business:
                  type: string
                  description: 'The key of the business to be verified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claim-BusinessClaim
  '/Wl/Business/Lead/BusinessLead.json':
    post:
      summary: 'Saves a new business lead referral record submitted through the Partner Program. Used when a Partner Program member refers a new prospective client to WellnessLiving. Stores the lead''s contact details linked to the referring partner so that sales can follow up and attribute the conversion to the correct partner.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business_partner:
                  type: string
                  description: 'The business''s partner key.'
                text_business_name:
                  type: string
                  description: 'The business''s name.'
                text_client_name:
                  type: string
                  description: 'The client''s full name.'
                text_email:
                  type: string
                  description: 'The client''s email.'
                text_phone:
                  type: string
                  description: 'The client''s phone number.'
                text_promo_code:
                  type: string
                  description: 'The business''s partner promo code.'
                url_website:
                  type: string
                  description: 'The website URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Lead-BusinessLead
  '/Wl/Business/Phone/Phone.json':
    delete:
      summary: 'Deletes business phone number from the system. Used by WellnessLiving admins to decommission a business''s SMS messaging service. After deletion, the business can no longer receive inbound SMS notifications. Admin privileges are required.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: text_phone
          in: query
          description: 'Business phone number(in locale format). Used to receive SMS notifications from clients. Can be `null` during bundle SID saving.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-Phone
    get:
      summary: 'Returns phone number(in locale format) of the business, if phone number does not exist returns empty line. Used in the SMS settings UI to display the currently registered business phone number and its input mask. The locale is derived from the business''s office country so the number is formatted correctly for that region.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_locale:
                        type: number
                        format: int32
                        description: 'Locale corresponding to the business'' address country. One of {@link \Core\Locale\LocaleSid} constants. Note that this may not be the same as the business'' locale, if the business is misconfigured and has an address country that is outside its locale.'
                      text_phone:
                        type: [string, "null"]
                        description: 'Business phone number(in locale format). Used to receive SMS notifications from clients. Can be `null` during bundle SID saving.'
                      text_phone_mask:
                        type: string
                        description: 'Business phone number mask.'
      tags:
        - Phone-Phone
    post:
      summary: 'Registers business phone number in system. Used when a business enables SMS messaging for the first time or replaces an existing number. Provisions a messaging service with the SMS provider so the business can receive inbound client messages. If the same number is already registered for this business, the call is a no-op.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: text_phone
          in: query
          description: 'Business phone number(in locale format). Used to receive SMS notifications from clients. Can be `null` during bundle SID saving.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-Phone
  '/Wl/Business/Select/BusinessSelect.json':
    get:
      summary: 'Performs checks and generates a list of businesses according to a given configuration. Populates the business-select dropdown widget shown in the backend. Used wherever staff need to switch between businesses they have access to. The returned structure drives the widget directly and includes selection state, visibility flags, and display configuration.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration data used to determine the list of businesses returned. This array has the following keys: <dl> <dt>bool `is_role`</dt> <dd> This will be `true` if we are checking for businesses where the user is a staff member. This will be `false` if we are checking for businesses where the user is a client. </dd> <dt>string `k_business`</dt> <dd>The business key.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business in which a list of business is requested.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User who is requesting the list of businesses.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_select:
                        type: array
                        description: 'Business list with additional parameters for a business select HTML component. This array contains the following keys and values. <dl> <dt>array `a_business`</dt> <dd> List of businesses the user can access. It is an array, each value is an array with the following keys: <dl> <dt>string `k_business`</dt> <dd>The key of the business.</dd> <dt>string `s_title`</dt> <dd>The name of the business.</dd> <dt>bool `selected`</dt> <dd>This will be true for the current business.</dd> </dl> </dd> <dt>bool `is_select`</dt> <dd>This will be `true` if one of the listed businesses is selected.</dd> <dt>string `name`</dt> <dd>Information for the widget, the name of the key in the list. This should always be `k_business`.</dd> <dt>string `onchange`</dt> <dd>Information for the widget, what the widget will do on selection.</dd> <dt>string `s_id`</dt> <dd>Information for the widget, the widget instance ID.</dd> <dt>string `s_style`</dt> <dd>Information for the widget, any additional CSS to apply.</dd> </dl>'
      tags:
        - Select-BusinessSelect
  '/Wl/Integration/Saml/SamlUserDeactivation.json':
    post:
      summary: 'Gets status of the user in business for given list of identifiers. Accepts a list of SAML identifier strings and returns a map of each identifier to a boolean indicating whether the corresponding user is active in their associated business. Requests with too many invalid identifiers are rate-limited with a penalty block mechanism.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_id:
                  type: array
                  description: 'A list of SAML identifiers.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'The key of the SAML identifier. This value is `true` if an active user is found in the business, `false` if otherwise.'
      tags:
        - Saml-SamlUserDeactivation
  '/Wl/Integration/Autymate/Report.json':
    get:
      summary: 'Gets the daily transaction data. Authenticates the request using the business GUID, runs the All Transactions report for the specified date, and returns paginated rows augmented with Autymate-specific columns such as tax details, location address, batch number, and payment method information.'
      parameters:
        - name: a_pay_method_remove
          in: query
          description: 'List of payment methods to filter out in the report. Each element is one of the {@link \RsPayMethodSid} constants.'
          schema:
            type: array
        - name: dl_date
          in: query
          description: 'The date in local time to retrieve transactions for.'
          schema:
            type: string
        - name: i_page
          in: query
          description: 'The page of the report, starting from 0. Each page will contain a maximum of {@link \Wl\Integration\Autymate\ReportApi::LIMIT} rows.'
          schema:
            type: number
            format: int32
        - name: is_refresh
          in: query
          description: 'Determines whether this report should be refreshed. `true` to refresh this report if it''s already generated. Refreshing of the report may not be queried while report is being generated. `false` to only return contents of the report. If report isn''t yet generated, it automatically starts the generation in the background.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business for which the report must be generated.'
          schema:
            type: string
        - name: s_guid
          in: query
          description: 'The randomly generated 32 character string used to authenticate requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field:
                        type: array
                        description: 'The list of fields in this report. This array is effectively a title row for table that is returned in {@link ReportApi::$a_row}.'
                      a_row:
                        type: object
                        description: 'The report data. This is an indexed array in which one row is an indexed array also. Indexes of the columns correspond to columns in {@link \Wl\Integration\Autymate\ReportApi::$a_field}.'
                      a_warning:
                        type: array
                        description: 'The warning list of the report, if applicable.'
                      dtu_complete:
                        type: [string, "null"]
                        description: 'The date and time when this report has completed generation. `null` if generation of this report isn''t completed.'
                      dtu_queue:
                        type: [string, "null"]
                        description: 'The date and time when this report was put in the generation queue. Effectively, this is the time when a user clicked to view this report or the report for this day was first called.'
                      dtu_start:
                        type: [string, "null"]
                        description: 'The date and time when generation of this report was started. `null` if generation of this report hasn''t started.'
                      id_report_status:
                        type: number
                        format: int32
                        description: 'The status of the report. One of the {@link \Thoth\ReportCore\Generator\ReportGeneratorStatusSid} constants.'
                      is_more:
                        type: boolean
                        description: 'If <tt>true</tt> then there are more report rows to get. Otherwise, <tt>false</tt> if all rows have been sent.'
                      is_report_complete:
                        type: boolean
                        description: 'Determines whether this report is complete. If this report is accessed on the current day, or is returning a result that was cached on the current day it could be incomplete as not all the transactions for the day are present. If `true` then this report will be complete. If `false` then this report could be incomplete.'
      tags:
        - Autymate-Report
  '/Wl/Integration/Autymate/AutymateActivate.json':
    get:
      summary: 'Returns or changes the Autymate enrollment status for the specified business. Autymate specifically requested this method be a get. Authenticates the request using the business GUID, then either reads or updates the enrollment status depending on the mode and the supplied `id_status` value. When activating enrollment, also provisions a Professional subscription for the business if one is not already active.'
      parameters:
        - name: id_mode
          in: query
          description: 'The mode of the request. One of {@link \Wl\Integration\Autymate\AutymateAccessModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_status
          in: query
          description: 'The new status of the enrollment. If `0`, the current status is returned. One of {@link \Wl\Integration\Autymate\AutymateStatusSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: s_guid
          in: query
          description: 'The randomly generated 32 character string used to authenticate Autymate requests for the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_status:
                        type: number
                        format: int32
                        description: 'The new status of the enrollment. If `0`, the current status is returned. One of {@link \Wl\Integration\Autymate\AutymateStatusSid} constants.'
      tags:
        - Autymate-AutymateActivate
  '/Wl/Integration/Curves/CurvesFranchiseLocation.json':
    get:
      summary: 'Returns country, region, state, city and location lists of the franchisor. This method returns all the information from the parent {@link \Wl\Business\Franchise\Location\BusinessFranchiseLocationApi::get()} and the Curves territory ID. Extends the parent location list with the Curves-specific territory ID (`k_territory_id`) for each franchisee location by joining against the Curves franchisee integration table.'
      parameters:
        - name: id_business_franchise_location
          in: query
          description: 'Determines which locations should be returned. If `null`, {@link BusinessFranchiseLocationSid::ALL} is used.'
          schema:
            type: number
            format: int32
        - name: is_include_churn
          in: query
          description: 'Determines whether to include churned/removed locations. If `true`, all locations are listed (regardless of their status).'
          schema:
            type: boolean
        - name: is_include_non_api
          in: query
          description: 'Determines whether to include locations marked to not be displayed on franchisor website. If `true`, all locations are listed (regardless of this setting).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_city_list:
                        type: array
                        description: 'The city list. Each element has next structure: <dl> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>City title.</dd> </dl>'
                      a_country_list:
                        type: array
                        description: 'The country list. Each element has next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Country title.</dd> </dl>'
                      a_location_list:
                        type: array
                        description: 'The location list. Each element has the next structure: <dl> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl>'
                      a_region_list:
                        type: array
                        description: 'The region list. Each element has the next structure: <dl> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Franchise region title.</dd> </dl>'
                      a_state_list:
                        type: array
                        description: 'The state list. Each element has the next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>State title.</dd> </dl>'
      tags:
        - Curves-CurvesFranchiseLocation
  '/Wl/Integration/DragonFly/Access.json':
    get:
      summary: 'Checks if the user can physically access the location. There are 3 scenarios where a user can be given access. Scenario 1 - If a valid member ID used, for instance from a bar code scanner, then the user may have access. Scenario 2 - If a valid uid is used and the user is a staff member, then the user may have access. Scenario 3 - If a valid uid is used and the client has a session or gym access at this location, then the user may have access.'
      parameters:
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: s_member
          in: query
          description: 'The member ID. Member ID is intended to work with barcode scanners. If this is specified the user key does not need to be set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This should be specified if the member ID is not set or not known.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_access:
                        type: boolean
                        description: 'Whether the specified user can access the location. `true` if the specified member can access the location. `false` if they can not.'
      tags:
        - DragonFly-Access
  '/Wl/Lead/Stage/LeadStageList.json':
    get:
      summary: 'Gets a list of lead stages of the business. Returns all lead stages configured for the specified business, both system-defined and custom ones, with their order, name and icon. In a case {@link LeadStageListApi::$is_statistic} is `true`, the number of clients of every stage is returned too.'
      parameters:
        - name: is_statistic
          in: query
          description: 'Determines whether statistics of the stages must be returned. `true` to return the number of clients of every stage in `i_user`, `false` to not return the statistics.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_lead_stage:
                        type: array
                        description: 'List of lead stages of the business. Ordered by `i_order`. <dl> <dt>int `i_order`</dt> <dd>Sequence number of the stage in the list.</dd> <dt>int `i_automation`</dt> <dd> Number of automations used this stage. Only set in a case {@link \Wl\Lead\Stage\LeadStageListApi::$is_statistic} is `true`. </dd> <dt>int `i_member_group`</dt> <dd> Number of client groups which convert their clients into this stage<i>, see {@link \Wl\Member\Group\Group::conversionCount()}</i>. Such a group depends on the stage: the stage may only be deleted with a replacement stage for the groups. Only set in a case {@link \Wl\Lead\Stage\LeadStageListApi::$is_statistic} is `true`. </dd> <dt>int `i_user`</dt> <dd> Number of clients who are in this stage. Only set in a case {@link \Wl\Lead\Stage\LeadStageListApi::$is_statistic} is `true`. </dd> <dt>int `id_lead_stage_shape`</dt> <dd>Shape of the stage icon. One of {@link \Wl\Lead\Stage\LeadStageShapeSid} constants.</dd> <dt>int|null `id_lead_stage_system`</dt> <dd> System-defined lead stage. One of {@link \Wl\Lead\Stage\LeadStageSystemSid} constants. `null` for a custom stage created by the business. </dd> <dt>int `id_lead_stage_type`</dt> <dd> Type of the stage. One of {@link \Wl\Lead\Stage\LeadStageTypeSid} constants. The type is set when the stage is created and can not be changed afterwards. </dd> <dt>string `k_lead_stage`</dt> <dd>Key of the stage. <i>Primary key in the {@link \Wl\Lead\Stage\LeadStageSql} table.</i></dd> <dt>string `s_color_background`</dt> <dd>Background color of the icon. Hexadecimal color.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of characters on the icon. Hexadecimal color.</dd> <dt>string `s_icon`</dt> <dd>Characters on the icon.</dd> <dt>string `text_title`</dt> <dd>Name of the stage.</dd> <dt>string `url_automation_list`</dt> <dd> URL of the list of automations which use this stage. Only set in a case {@link \Wl\Lead\Stage\LeadStageListApi::$is_statistic} is `true` and the stage is used in automations. </dd> <dt>string `url_member_report`</dt> <dd> URL of the report of clients who are in this stage. Only set in a case {@link \Wl\Lead\Stage\LeadStageListApi::$is_statistic} is `true` and the stage contains clients. </dd> </dl>'
      tags:
        - Stage-LeadStageList
  '/Wl/Lead/Stage/LeadStageElement.json':
    delete:
      summary: 'Deletes a lead stage. The last remaining stage of a type ({@link LeadStageTypeSid}) can not be deleted - a business must always have at least one stage of every type. If the stage has leads or clients assigned, {@link LeadStageElementApi::$k_lead_stage_replace} must be given - they are moved to the replacement stage, which must be of the same type. A system stage of a conversion type can also be used by the `Won` / `Lost` client groups of the business. Such a stage requires a replacement stage too, even if it has no clients: the system ID of the stage moves to the replacement one, and the groups convert their clients into it from now on.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_lead_stage
          in: query
          description: 'Key of the lead stage.'
          schema:
            type: string
        - name: k_lead_stage_replace
          in: query
          description: 'Key of the lead stage to move leads and clients of the deleted stage to. Required if the stage being deleted has leads or clients assigned to it. Ignored otherwise. The replacement stage must be of the same {@link LeadStageTypeSid} as the deleted one.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stage-LeadStageElement
    get:
      summary: 'Returns information about a lead stage. The information includes the name, icon and type of the lead stage.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_lead_stage
          in: query
          description: 'Key of the lead stage.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_lead_stage_shape:
                        type: number
                        format: int32
                        description: 'Shape of the stage icon. One of {@link LeadStageShapeSid} constants.'
                      id_lead_stage_type:
                        type: number
                        format: int32
                        description: 'Type of the stage. One of {@link LeadStageTypeSid} constants. Only used to create a stage. Type of an existing stage can not be changed.'
                      s_color_background:
                        type: string
                        description: 'Background color of the icon. Hexadecimal color.'
                      s_color_foreground:
                        type: string
                        description: 'Color of characters on the icon. Hexadecimal color.'
                      s_icon:
                        type: string
                        description: 'Characters on the icon. Allowed length depends on {@link LeadStageElementApi::$id_lead_stage_shape}, see {@link LeadStageShapeSid::character()}.'
                      text_title:
                        type: string
                        description: 'Name of the stage.'
      tags:
        - Stage-LeadStageElement
    post:
      summary: 'Edits name and icon of a lead stage. Type of the stage ({@link LeadStageTypeSid}) is read-only and can not be changed.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_lead_stage
          in: query
          description: 'Key of the lead stage.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_lead_stage_shape:
                  type: number
                  format: int32
                  description: 'Shape of the stage icon. One of {@link LeadStageShapeSid} constants.'
                s_color_background:
                  type: string
                  description: 'Background color of the icon. Hexadecimal color.'
                s_color_foreground:
                  type: string
                  description: 'Color of characters on the icon. Hexadecimal color.'
                s_icon:
                  type: string
                  description: 'Characters on the icon. Allowed length depends on {@link LeadStageElementApi::$id_lead_stage_shape}, see {@link LeadStageShapeSid::character()}.'
                text_title:
                  type: string
                  description: 'Name of the stage.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stage-LeadStageElement
    put:
      summary: 'Creates a new custom lead stage. The name must be unique within the business and no longer than {@link LeadStageElementApi::TITLE_LENGTH_MAX} characters. {@link LeadStageElementApi::$id_lead_stage_type} is required and can not be changed afterwards. A business may have no more than {@link LeadStageElementApi::STAGE_LIMIT} stages.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_lead_stage_shape:
                  type: number
                  format: int32
                  description: 'Shape of the stage icon. One of {@link LeadStageShapeSid} constants.'
                id_lead_stage_type:
                  type: number
                  format: int32
                  description: 'Type of the stage. One of {@link LeadStageTypeSid} constants. Only used to create a stage. Type of an existing stage can not be changed.'
                s_color_background:
                  type: string
                  description: 'Background color of the icon. Hexadecimal color.'
                s_color_foreground:
                  type: string
                  description: 'Color of characters on the icon. Hexadecimal color.'
                s_icon:
                  type: string
                  description: 'Characters on the icon. Allowed length depends on {@link LeadStageElementApi::$id_lead_stage_shape}, see {@link LeadStageShapeSid::character()}.'
                text_title:
                  type: string
                  description: 'Name of the stage.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_lead_stage:
                        type: string
                        description: 'Key of the lead stage.'
      tags:
        - Stage-LeadStageElement
  '/Wl/Lead/Stage/LeadStageUser.json':
    post:
      summary: 'Sets the lead stage of the client. If the client is already in this stage, nothing is changed.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the client whose stage is set.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_lead_stage:
                  type: string
                  description: 'Key of the lead stage to move the client into.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stage-LeadStageUser
  '/Wl/Lead/Source/LeadSourceElement.json':
    delete:
      summary: 'Deletes custom source lead. Removes the specified custom lead source from the business. If a replacement lead source key is provided, all users currently assigned to the deleted source are reassigned to it before deletion.'
      parameters:
        - name: k_lead_source_replace
          in: query
          description: 'Key of the lead source.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Source-LeadSourceElement
    post:
      summary: 'Creates or edits a custom source lead. Accepts a list of lead source records and applies bulk create or update operations: new entries without a key are inserted, and existing entries are updated with a new sort order or title.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_lead_source:
                  type: array
                  description: 'List of Lead Sources. <dl> <dt>int `i_sort`</dt> <dd>Sorting order.</dd> <dt>int|null `id_lead_source`</dt> <dd> System-defined Lead Source ID. One of {@link \Wl\Mode\ModeSid} constants. `null` if it is a custom Lead Source. </dd> <dt>string `k_lead_source`</dt> <dd>Key of the lead source. <i>Primary key in {@link \Wl\Lead\Source\LeadSourceSql} table.</i></dd> <dt>string `k_skin`</dt> <dd> Key of the lead skin. <i>Primary key in {@link \RsSkinSql} table.</i> Will be set only in case if <var>id_lead_source</var> is {@link \Wl\Mode\ModeSid::WIDGET} and widget can''t be used for "Add Lead" form. `false` - otherwise. </dd> <dt>string `text_title`</dt> <dd>Name of the Lead Source.</dd> </dl>'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Source-LeadSourceElement
    put:
      summary: 'Adds one lead source. Creates a single new custom lead source with the given title for the specified business, assigns it the next available sort position, and returns the generated lead source key.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                text_lead_source:
                  type: string
                  description: 'Name of new lead source item.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_lead_source:
                        type: string
                        description: 'Key of the lead source.'
      tags:
        - Source-LeadSourceElement
  '/Wl/Lead/Source/LeadSourceList.json':
    get:
      summary: 'Gets list of Lead Sources. Returns all lead sources configured for the specified business, including system-defined and custom sources, with sort order, title, associated skin, and flags indicating whether each source is currently in use. This is public endpoint, which is available for non-signed-in users. But if the user is not signed in, the list will be filtered to contain only those sources that are available for the user and with limited information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_lead_source:
                        type: array
                        description: 'List of Lead Sources. <dl> <dt>int|null `i_sort`</dt> <dd> Sorting order. Only used in the Lead Source widget option. `null` is a temporary value that exists for a short time during the process of inserting of a new row. </dd> <dt>int|null `id_lead_source`</dt> <dd> System-defined Lead Source ID. One of the {@link \Wl\Mode\ModeSid} constants. `null` if it is a custom Lead Source. </dd> <dt>bool `is_add_lead`</dt> <dd> `true` if this skin will be used for "Add Lead" form or `false` otherwise. Only one skin in the business can be used for "Add Lead" form. Not available if current user is not a staff member or admin. </dd> <dt>bool `is_use`</dt> <dd> Determines whether Lead Source is being used. `true` if Lead Source is being used. `false` - otherwise. Not available if current user is not a staff member or admin. </dd> <dt>string `k_lead_source`</dt> <dd>Key of the lead source. <i>Primary key in {@link \Wl\Lead\Source\LeadSourceSql} table.</i></dd> <dt>string `k_skin`</dt> <dd> Key of the lead skin. <i>Primary key in {@link \RsSkinSql} table.</i> Not available if current user is not a staff member or admin. </dd> <dt>string `text_title`</dt> <dd>Name of the Lead Source.</dd> </dl>'
      tags:
        - Source-LeadSourceList
  '/Wl/Notification/Send/NotificationInfo.json':
    get:
      summary: 'Returns configuration information about the specified notification for the given business. Validates the business key and notification ID, then returns the notification configuration including mail template settings and channel availability for the given business. Requires the `rs.profile` privilege.'
      parameters:
        - name: id_notification
          in: query
          description: 'ID of the notification. See {@link \RsMailSid}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business where information about notification should be retrieved.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: array
                        description: 'Information about mail.'
      tags:
        - Send-NotificationInfo
  '/Wl/Notification/Send/NotificationSend.json':
    post:
      summary: 'Schedules or immediately sends a notification to clients of the specified business or location. Validates the business, optional location, and notification ID, then either dispatches the notification immediately or schedules it at the UTC date and time provided in `$dtu_send` (must be within 7 days). Optionally restricts recipients to specific UIDs via `$s_uid`. Requires the `rs.profile` privilege.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Custom array with information which can be used to generate notification. Use {@link NotificationInfoApi} for more information about available fields.'
                dtu_send:
                  type: string
                  description: 'Date and time in UTC, when notification should be sent. Empty string means to send immediately. If date and time are set, it should be reasonable. If it''s too far in the future, API returns an error.'
                id_notification:
                  type: number
                  format: int32
                  description: 'ID of the notification. See {@link \RsMailSid}. Contact with WellnessLiving support to get the list of available notifications.'
                k_business:
                  type: string
                  description: 'Key of the business where notification should be sent.'
                k_location:
                  type: [string, "null"]
                  description: 'Key of the location. If it''s not empty, only clients from this location will get notification. `null` to not limit recipients with a certain location.'
                s_uid:
                  type: string
                  description: 'List of UIDs joined with comma, if notification should be sent to certain recipients. Empty string means to send to all clients of the business or location with proper subscription level.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Send-NotificationSend
  '/Wl/Notification/Otp/PhoneOtp.json':
    get:
      summary: 'Sends an OTP code to the specified phone number for verification. Checks that the rate limit for OTP generation has not been exceeded, generates a new code, and dispatches it to the given phone number via SMS. Requires a special privilege to use this endpoint.'
      parameters:
        - name: text_phone
          in: query
          description: 'Phone number to be validated.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Otp-PhoneOtp
    post:
      summary: 'Verifies the OTP code submitted by the user for the specified phone number. Checks that the submitted `$s_otp_code` matches the code previously generated for `$text_phone` and has not expired. Returns a validation error if the code is absent, incorrect, or expired.'
      parameters:
        - name: text_phone
          in: query
          description: 'Phone number to be validated.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp_code:
                  type: string
                  description: 'Otp code integer that user entered on the form. Will be compared with otp code, which was sent previously.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Otp-PhoneOtp
  '/Wl/Purchase/Receipt/PurchaseReceipt.json':
    get:
      summary: 'Gets purchase information. Returns full receipt data for the specified purchase, including business details, customer information, itemized purchase items, payment methods, pricing summary, and print URLs.'
      parameters:
        - name: is_url_public
          in: query
          description: 'Whether {@link PurchaseReceiptApi::$url_print} and {@link PurchaseReceiptApi::$url_print_receipt} require authentication. If `true`, the URL contains a token that allows temporary access to the page without logging in. If ''false'', the page requires authentication.'
          schema:
            type: boolean
        - name: k_purchase
          in: query
          description: 'The key of the purchase.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_account_rest:
                        type: array
                        description: 'Information about the account balance for a user''s account after payment for the purchase. Every element has the following keys: <dl> <dt>string `m_amount`</dt> <dd>The account balance on the user''s account.</dd> <dt>string `text_currency`</dt> <dd>The currency of the amount.</dd> <dt>string `text_method`</dt> <dd>The payment method title.</dd> </dl>'
                      a_business:
                        type: array
                        description: 'Information about the business. <dl> <dt>array `a_logo`</dt> <dd> The business logo. <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if the image is empty. Otherwise, this will be `false`.</dd> <dt>string `text_url`</dt> <dd>The URL of the image.</dd> </dl> </dd> <dt>string `text_address`</dt> <dd>The business''s address.</dd> <dt>string `text_mail`</dt> <dd>The business''s email address.</dd> <dt>string `text_phone`</dt> <dd>The business''s phone number.</dd> <dt>string `text_title`</dt> <dd>The business name.</dd> </dl>'
                      a_card:
                        type: array
                        description: 'Payment transaction information. Every element has the following keys: <dl> <dt>string `text_card_number`</dt> <dd>The card or account number.</dd> <dt>string `text_card_system`</dt> <dd>The card system name from {@link \ACardSystemSid}.</dd> <dt>string `text_title`</dt> <dd>The payment account title. This will be ''Account number'' for ACH, ''Card'' for all other cases.</dd> </dl>'
                      a_customer:
                        type: array
                        description: 'Information about the customer. <dl> <dt>string `text_address`</dt> <dd>The customer''s address.</dd> <dt>string `text_mail`</dt> <dd>The customer''s email address.</dd> <dt>string `text_name`</dt> <dd>The customer''s name.</dd> <dt>string `text_phone`</dt> <dd>The customer''s phone number.</dd> </dl>'
                      a_pay_method:
                        type: array
                        description: 'A list of payment methods for the current purchase. Every element has the following keys: <dl> <dt>string `m_amount`</dt> <dd>The amount of the payment.</dd> <dt>string `text_currency`</dt> <dd>The currency of the amount.</dd> <dt>string `text_pay_method`</dt> <dd>The payment method title.</dd> </dl>'
                      a_price:
                        type: array
                        description: 'Complete information about price information for the purchase. <dl> <dt>string `m_discount`</dt> <dd>The discount amount.</dd> <dt>string `m_discount_code`</dt> <dd>The discount amount for a discount code.</dd> <dt>string `m_sum`</dt> <dd>The subtotal amount (the sum of the prices).</dd> <dt>string `m_tax`</dt> <dd>The amount of taxes.</dd> <dt>string `m_tip`</dt> <dd>The amount of tips.</dd> <dt>string `m_total`</dt> <dd>The total amount.</dd> <dt>string `text_currency`</dt> <dd>The currency of the purchase.</dd> <dt>string `text_discount_code`</dt> <dd>The discount code applied to the purchase.</dd> </dl>'
                      a_purchase_item:
                        type: array
                        description: 'A list of purchase items. Every element has the following keys: <dl> <dt>array `a_logo`</dt> <dd> The purchase item logo. <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>bool `is_empty`</dt> <dd>Determines whether the image is empty.</dd> <dt>string `text_url`</dt> <dd>he URL of the image.</dd> </dl> </dd> <dt>string[] `a_visit`</dt> <dd> List of visit keys. Only available if the purchase item is a class period, enrollment, enrollment deposit, or enrollment discount. Each element is a string visit key. <i>Primary keys in {@link \RsVisitSql} table.</i> </dd> <dt>int `i_count`</dt> <dd>The number of items in the purchase.</dd> <dt>int|null `id_program`</dt> <dd> Program ID. One of {@link \RsProgramSid} constants. Only applies to promotions. `null` if the purchase item is not a promotion. </dd> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>int `id_sale`</dt> <dd>Sale ID. One of {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The identifier of the item.</dd> <dt>string `k_purchase_item`</dt> <dd>The key of the purchase item.</dd> <dt>string `k_shop_product`</dt> <dd> Product key. Set only if `id_purchase_item`is {@link \RsPurchaseItemSid::PRODUCT}. <i>Primary key in {@link \RsShopProductSql}.</i> </dd> <dt>string `m_price`</dt> <dd>The price of the single item.</dd> <dt>string `m_price_total`</dt> <dd>The price of all items.</dd> <dt>string `text_category`</dt> <dd>The type of the item (for example, a pass or membership).</dd> <dt>string `text_currency`</dt> <dd>The currency of the price.</dd> <dt>string `text_info`</dt> <dd>Additional information about the purchase item.</dd> <dt>string `text_title`</dt> <dd>The name of the purchase item.</dd> <dt>string `text_description`</dt> <dd>The description of the purchase item.</dd> <dt>string `url_print`</dt> <dd>The URL to print. Only available if the purchase item is a coupon (gift card).</dd> </dl>'
                      dtl_purchase:
                        type: string
                        description: 'The local date of the purchase in MySQL format.'
                      has_signature:
                        type: boolean
                        description: 'Determines whether the payment contained a signature.'
                      html_receipt:
                        type: string
                        description: 'HTML representation of the purchase receipt.'
                      text_purchase_id:
                        type: string
                        description: 'The normalized purchase ID.'
                      text_receipt:
                        type: string
                        description: 'The receipt text set in the store settings.'
                      url_print:
                        type: string
                        description: 'The URL for printing on a normal printer.'
                      url_print_receipt:
                        type: string
                        description: 'The URL for printing on a receipt printer.'
      tags:
        - Receipt-PurchaseReceipt
  '/Wl/Purchase/Mail/PurchaseMail.json':
    post:
      summary: 'Sends mail message with list of purchased items for specified purchase. Validates the caller''s access to the purchase and sends a receipt email to the appropriate recipient (staff member or client), recording the action in the mail history log.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_send_force:
                  type: boolean
                  description: 'Determines whether mail sending should be forced.'
                k_purchase:
                  type: string
                  description: 'The key of the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mail-PurchaseMail
  '/Wl/Purchase/Share/PurchaseShare.json':
    post:
      summary: 'Records the purchase share action and returns the social network sharing URL. Validates that the caller owns the purchase, prevents duplicate share actions, saves the share record, and returns the social network URL for sharing.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_social:
                  type: number
                  format: int32
                  description: 'The social network. One of the {@link \ASocialSid} constants.'
                k_purchase:
                  type: string
                  description: 'The key of the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_share:
                        type: string
                        description: 'The URL to the sharing page.'
      tags:
        - Share-PurchaseShare
  '/Wl/Widget/Analytics/WidgetAnalyticsEvent.json':
    post:
      summary: 'Accepts a Widget analytics event. Validates the event envelope and payload. `begin_checkout` and `abandoned_checkout` events are stored and scheduled for asynchronous processing. A `purchase` event marks pending checkout events for the same client and checkout type as checkout-complete, so the "Abandoned checkout" trigger stops enrolling the client for them.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_payload:
                  type: array
                  description: 'Event-specific payload. <dl> <dt>array[] `a_item`</dt> <dd> Selected checkout items. <dl> <dt>string|null `dtu_session`</dt> <dd>Selected booking session datetime. `null` if not available.</dd> <dt>int `i_order`</dt> <dd>Zero-based item order in the checkout list.</dd> <dt>int `i_quantity`</dt> <dd>Quantity of selected items.</dd> <dt>int|null `id_purchase_item`</dt> <dd> Purchase item ID. See {@link \RsPurchaseItemSid}. Required when the parent checkout type is {@link \Wl\Widget\Analytics\WidgetAnalyticsCheckoutTypeSid::STORE_PURCHASE}; `null` for {@link \Wl\Widget\Analytics\WidgetAnalyticsCheckoutTypeSid::BOOKING}. </dd> <dt>int|null `id_service`</dt> <dd> Service ID. See {@link \Wl\Service\ServiceSid}. Required when checkout type is {@link \Wl\Widget\Analytics\WidgetAnalyticsCheckoutTypeSid::BOOKING}; `null` for {@link \Wl\Widget\Analytics\WidgetAnalyticsCheckoutTypeSid::STORE_PURCHASE}. </dd> <dt>string|null `k_class_period`</dt> <dd> Selected class period key for classes/events booking items. `null` if class period is not specified or not applicable. </dd> <dt>string `k_item`</dt> <dd> Selected item key. When {@link \Wl\Widget\Analytics\CheckoutSnapshotItemEntity::$id_purchase_item} is: - {@link \RsPurchaseItemSid::COUPON}, this is a coupon key. - {@link \RsPurchaseItemSid::ENROLLMENT}, this is an event class key. - {@link \RsPurchaseItemSid::PRODUCT}, this is a product option key. - {@link \RsPurchaseItemSid::PROMOTION}, this is a Purchase Option key. When {@link \Wl\Widget\Analytics\CheckoutSnapshotItemEntity::$id_service} is: - {@link \Wl\Service\ServiceSid::APPOINTMENT}, this is a service key. - {@link \Wl\Service\ServiceSid::BOOKABLE_ASSET}, this is an asset key. - {@link \Wl\Service\ServiceSid::CLASSES} or {@link \Wl\Service\ServiceSid::EVENT}, this is a class key. </dd> <dt>string|null `m_price`</dt> <dd>Checkout item price in the location currency. `null` if not available.</dd> <dt>string `text_service_name`</dt> <dd>Item name snapshot from checkout.</dd> <dt>string `uid_staff`</dt> <dd>Selected staff user key. Empty if staff was not selected.</dd> </dl> </dd> <dt>int `id_checkout_type`</dt> <dd>Checkout type.</dd> <dt>string `k_location`</dt> <dd>Location key.</dd> <dt>string `m_total`</dt> <dd>Checkout total in the location currency.</dd> <dt>string `s_funnel_step`</dt> <dd>Last funnel step reached by the client. Empty if it is unavailable.</dd> <dt>string `s_session_id`</dt> <dd>Widget checkout session identifier. Used for diagnostics and continuation context, not for idempotency.</dd> <dt>string `s_utm_campaign`</dt> <dd>UTM campaign value. Empty if it is unavailable.</dd> <dt>string `s_utm_medium`</dt> <dd>UTM medium value. Empty if it is unavailable.</dd> <dt>string `s_utm_source`</dt> <dd>UTM source value. Empty if it is unavailable.</dd> <dt>string `uid`</dt> <dd>User key for whom checkout was abandoned.</dd> </dl>'
                id_event_name:
                  type: number
                  format: int32
                  description: 'Event name.'
                id_event_version:
                  type: number
                  format: int32
                  description: 'Event schema version. Currently only {@link WidgetAnalyticsEventVersionSid::V1} is supported.'
                k_business:
                  type: string
                  description: 'Business key used for shard and datacenter routing.'
                s_event_id:
                  type: string
                  description: 'Client-generated idempotency identifier. A retry of the same logical event must use the same value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Analytics-WidgetAnalyticsEvent
  '/Wl/AiAgent/Phone/CallReceived.json':
    post:
      summary: '{@inheritdoc} Sends the "Phone Agent Call Received" staff notification to every active staff member of the business who has the {@link PrivilegeSid::NOTIFICATION_PHONE_AGENT_CALL_RECEIVED} permission. Requires the business to have an active Phone Agent subscription tier.'
      parameters:
        - name: id_call_outcome
          in: query
          description: 'How the call ended. Required.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Required.'
          schema:
            type: string
        - name: text_call_summary
          in: query
          description: 'Short summary of the call generated by CAASI. Used in the email notification only. Optional.'
          schema:
            type: string
        - name: text_caller_name
          in: query
          description: 'The caller''s name, if identified. Optional.'
          schema:
            type: string
        - name: text_caller_phone
          in: query
          description: 'The caller''s phone number. Optional.'
          schema:
            type: string
        - name: url_view_conversation_link
          in: query
          description: 'URL to view the conversation between the caller and CAASI (for email). Optional.'
          schema:
            type: string
        - name: url_view_conversation_link_sms
          in: query
          description: 'URL to view the conversation, in an SMS-safe format. Optional.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-CallReceived
  '/Wl/AiAgent/Link/SendMail.json':
    post:
      summary: 'Sends an AI agent link action email (booking or purchase) to the specified user. Triggers the email that the AI agent sends when a client clicks a booking or purchase link generated by the AI agent chat. Requires the business to have an active AI agent subscription; the action type determines which notification template is used and which service or purchase item is referenced in the email body.'
      parameters:
        - name: id_purchase_item
          in: query
          description: 'Purchase item ID. Required if {@link SendMailApi::$text_action} is ''purchase''. One of the {@link \RsPurchaseItemSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_service
          in: query
          description: 'Service ID. Required if {@link SendMailApi::$text_action} is ''booking''.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Required.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Unique identifier for the link.'
          schema:
            type: string
        - name: text_action
          in: query
          description: 'Action type. Must be ''purchase'' or ''booking''.'
          schema:
            type: string
        - name: text_url_suffix
          in: query
          description: 'URL suffix for the link. Optional.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User ID. Required.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Link-SendMail
  '/Wl/AiAgent/Alert/Alert.json':
    post:
      summary: 'Fires an in-app alert for the specified event. Triggers the alert that appears in the business `Alert Center` for staff members with appropriate access. Requires the business to have an active `AI Agent` subscription.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_alert:
                  type: number
                  format: int32
                  description: 'CID of the alert to be fired.'
                url_conversation:
                  type: string
                  description: 'Conversation link assigned by the `AI Agent`.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Alert-Alert
  '/Wl/Catalog/Cart/Cart.json':
    get:
      summary: 'Gets purchase cost data. Used in the client-facing checkout flow to show a live price summary before the client submits payment. Calculates the full breakdown - subtotal, taxes, applied discounts, reward redemptions, and tips - so the client can review the total before confirming.'
      parameters:
        - name: a_item
          in: query
          description: 'The list of items in the cart. <dl> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date. This will be `null` in cases where the client prorate date hasn''t passed.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type, one of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd> The sale item key. Note: <i>For products, this will be the product key from {@link \RsShopProductSql}.</i> You need to specify the product option key in the `k_shop_product_option` field or the first available option will be used automatically. </dd> <dt>array `a_config`</dt> <dd> Additional configuration. Used only for `id_sale` = {@link \RsSaleSid::TUITION}. <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events. Each entry has the next structure: <dl> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> <dt>array `a_registration_fee_list`</dt> <dd> Registration fees for tuition participants. Keys are participant keys. <i>Primary key in {@link \PassportLoginSql}.</i> Each value has the next structure: <dl> <dt>string `m_amount`</dt> <dd>Registration fee amount for the tuition participant.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd>The total amount that will be charged for the tuition item when during purchase.</dd> <dt>string `m_deferred`</dt> <dd> The part of the tuition cost that is not charged during purchase. <i>See {@link \Wl\Purchase\Item\Tuition::checkoutConfig()}.</i> </dd> </dl> </dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize used on item. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_shop_product_option`</dt> <dd> <i>The shop product option key from {@link \RsShopProductOptionSql}.</i> Note: Used for products only `id_sale` = {@link \RsSaleSid::PRODUCT}. If empty the first available option will be used automatically. </dd> </dl>'
          schema:
            type: array
        - name: is_auto_apply_prize
          in: query
          description: 'Whether selected login prize should be auto applied to first applicable item.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_login_prize
          in: query
          description: 'The login prize key.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code. If `null`, the discount code has been reset.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. This will be `0` for guests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount_item:
                        type: array
                        description: 'The list of discounts for each item. Keys refer to unique item keys, and values refer to discount amounts for the item.'
                      a_item:
                        type: array
                        description: 'The list of items in the cart. <dl> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date. This will be `null` in cases where the client prorate date hasn''t passed.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type, one of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd> The sale item key. Note: <i>For products, this will be the product key from {@link \RsShopProductSql}.</i> You need to specify the product option key in the `k_shop_product_option` field or the first available option will be used automatically. </dd> <dt>array `a_config`</dt> <dd> Additional configuration. Used only for `id_sale` = {@link \RsSaleSid::TUITION}. <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events. Each entry has the next structure: <dl> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> <dt>array `a_registration_fee_list`</dt> <dd> Registration fees for tuition participants. Keys are participant keys. <i>Primary key in {@link \PassportLoginSql}.</i> Each value has the next structure: <dl> <dt>string `m_amount`</dt> <dd>Registration fee amount for the tuition participant.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd>The total amount that will be charged for the tuition item when during purchase.</dd> <dt>string `m_deferred`</dt> <dd> The part of the tuition cost that is not charged during purchase. <i>See {@link \Wl\Purchase\Item\Tuition::checkoutConfig()}.</i> </dd> </dl> </dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize used on item. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_shop_product_option`</dt> <dd> <i>The shop product option key from {@link \RsShopProductOptionSql}.</i> Note: Used for products only `id_sale` = {@link \RsSaleSid::PRODUCT}. If empty the first available option will be used automatically. </dd> </dl>'
                      a_prize_propose:
                        type: array
                        description: 'List of prizes that can be redeemed and applied to items in the cart. <dl> <dt>int `i_score`</dt> <dd>Amount of points needed to redeem the prize.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key. <i>Primary key in {@link \RsRewardPrizeSql} table.</i></dd> <dt>string `text_discount`</dt> <dd>Reward prize discount, e.g. ''.00 off'', ''10% off''.</dd> <dt>string `text_title`</dt> <dd> Reward prize title, e.g. ''on any item'', ''on Water bottle'', ''on any Appointment Pass or Membership Purchase Option''. </dd> </dl>'
                      a_reward_item:
                        type: array
                        description: 'List of cart items to which the selected prize can be applied <dl> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type, one of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_login_prize_used`</dt> <dd>Whether selected login prize discount applied to item.</dd> <dt>string `k_id`</dt> <dd>The sale item key.</dd> <dt>string `k_shop_product_option`</dt> <dd>The shop product option. If <tt>null</tt>, the sale item has no options.</dd> </dl>'
                      a_reward_propose:
                        type: array
                        description: 'List of login prizes that can be applied to items in the cart. <dl> <dt>string `k_login_prize`</dt> <dd>Login prize key. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `text_discount`</dt> <dd>Reward prize discount, e.g. ''.00 off'', ''10% off''.</dd> <dt>string `text_discount_applied`</dt> <dd>Applied login prize discount title, e.g. ''10% discount''.</dd> <dt>string `text_discount_purpose`</dt> <dd>Purpose login prize discount title, e.g. ''Apply 10% off''.</dd> <dt>string `text_title`</dt> <dd> Reward prize title, e.g. ''on any item'', ''on Water bottle'', ''on any Appointment Pass or Membership Purchase Option''. </dd> </dl>'
                      a_tax_list:
                        type: array
                        description: 'Values derived for individual tax rates. Keys refer to tax keys, values refer to the amount of tax in the cart.'
                      i_score:
                        type: [number, "null"]
                        description: 'Amount of client''s reward points.'
                      m_checkout:
                        type: string
                        description: 'The amount that has to be charged right now for the cart. Equals {@link CartApi::$m_total} for everything that is paid for in full at once. A tuition defers a part of its cost to an installment plan or to a membership schedule, and `m_total` covers the full cost regardless, so this is the amount to put into the payment form. It is `m_total` minus `a_config.m_deferred` of every tuition item in {@link CartApi::$a_item}.'
                      m_discount:
                        type: [string, "null"]
                        description: 'The full discount of the cart. If `null`, the discount isn''t calculated yet.'
                      m_discount_total:
                        type: string
                        description: 'The total discount amount.'
                      m_subtotal:
                        type: [string, "null"]
                        description: 'The total amount in the catalog cart without tax. If `null`, the total amount isn''t set yet.'
                      m_tax:
                        type: [string, "null"]
                        description: 'The tax amount. If `null`, the tax amount isn''t set yet.'
                      m_tip_purchase:
                        type: [string, "null"]
                        description: 'The amount of appointment tips.'
                      m_total:
                        type: [string, "null"]
                        description: 'The total amount in the catalog cart. If `null`, the total amount isn''t set yet.'
      tags:
        - Cart-Cart
  '/Wl/Catalog/Cart/LimitQuantity.json':
    get:
      summary: 'Checks limit quantity and whether a promotion can be added to the cart. Used in the checkout flow to prevent a client from purchasing more passes than the promotion allows. Returns the limit, how many the client has already bought, and how many are currently in the cart so the UI can display a clear message if the limit would be exceeded.'
      parameters:
        - name: a_item
          in: query
          description: 'The cart items list with the next structure: <dl> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The sale item ID.</dd> <dt>string `k_shop_product_option`</dt> <dd>The shop product option. <tt>null</tt> if the sale item has no options.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The business in which the purchase will be made.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The promotion key to add to the cart.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_purchase_already:
                        type: [number, "null"]
                        description: 'The quantity if promotions have already been purchased by the current user. If `true`, the quantity exceeded. Otherwise, this will be `false`. This will be `null` if not set yet, there''s no limit quantity, or the limit quantity isn''t exceeded.'
                      i_purchase_current:
                        type: [number, "null"]
                        description: 'The current quantity of a promotion with the quantity limit in the cart. This will be `null` if not set yet, there''s no limit quantity, or the limit quantity isn''t exceeded.'
                      i_quantity_limit:
                        type: [number, "null"]
                        description: 'The quantity limit of the promotion. This will be `null` if not set yet or if there''s no limit quantity.'
                      is_limit_exceeded:
                        type: [boolean, "null"]
                        description: 'Determines whether the limit quantity has been exceeded. If `true`, the limit quantity has been exceeded. Otherwise, this will be `false`. This will be `null` if not set yet.'
      tags:
        - Cart-LimitQuantity
  '/Wl/Catalog/Payment/Payment.json':
    post:
      summary: 'Allows to pay items for the client. The checkout endpoint that finalizes a purchase in the client-facing store. Charges the client''s selected payment method, applies any discounts and tips, and creates the purchase record. Returns the resulting purchase key so the frontend can redirect to the confirmation page.'
      parameters:
        - name: a_commission
          in: query
          description: 'The staff commission earned for this purchase. If this isn''t empty, it has the next fields: <dl> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key of {@link \PassportLoginSql} table.</i></dd> <dt>string `k_staff`</dt> <dd><b>Deprecated!</b> Staff key. <i>Primary key of {@link \RsStaffSql} table.</i></dd> <dt>string `k_staff_pay`</dt> <dd>The payment schema key.</dd> </dl>'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The WellnessLiving mode type (required). One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_guest
          in: query
          description: 'Determines if the payment owner is an anonymous user (optional).'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: 'Specify this if operations are performed by the staff member (optional).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key (required).'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key (required).'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key (required).'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_item:
                  type: array
                  description: 'The list of items in the cart. This parameter is required. Every element must have the following keys: <dl> <dt>array `a_config`</dt> <dd> Additional configuration information. This may contain the next keys: <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events. Used only for {@link \RsSaleSid::TUITION}. Each entry has the next structure: <dl> <dt>array `a_discount`</dt> <dd> Discount applied to the event price. Staff-only: ignored when sent by a client. <dl> <dt>string `m_discount`</dt> <dd>Discount amount.</dd> <dt>string `text_discount`</dt> <dd>Discount title.</dd> </dl> </dd> <dt>array `a_tax`</dt> <dd> List of taxes to be applied to the event price. Keys are tax keys. <i>Primary key in {@link \RsTaxSql}.</i> Values are tax amounts. If not specified, taxes are calculated based on class setup. Staff-only: ignored when sent by a client. </dd> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `m_checkout`</dt> <dd> The amount charged for this event at checkout. Staff-only: ignored when sent by a client. When provided for all events in the list, per-event amounts are used as-is without aggregate redistribution, allowing a non-uniform distribution across events (Mode 2). If omitted for any event, falls back to item-level `m_checkout` redistribution. </dd> <dt>string `m_price`</dt> <dd> Price of the event for the tuition participant. If not specified, price is calculated based on class setup. Staff-only: ignored when sent by a client. </dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> <dt>array `a_quick_gift`</dt> <dd> A list of components to be added to the client. For quick gift cards only: <dl> <dt>int `i_count`</dt> <dd>The quantity of elements.</dd> <dt>int `id_purchase_item`</dt> <dd>The purchase type of the element. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd> The primary key of the element, depending on the element type. Pay attention that if you add a product into gift card, there must be specified not product key but product option key. Do not specify separate field `k_shop_product_option`. </dd> </dl> </dd> <dt>array `a_registration_fee_list`</dt> <dd> Registration fees for tuition participants. Used only for `id_sale` = {@link \RsSaleSid::TUITION}. Keys are participant keys. <i>Primary key in {@link \PassportLoginSql}.</i> Staff-only: ignored when sent by a client. Each value has the next structure: <dl> <dt>array `a_discount`</dt> <dd> Manual discount applied to the registration fee. <dl> <dt>string `m_discount`</dt> <dd>Discount amount.</dd> <dt>string `text_discount`</dt> <dd>Discount title.</dd> </dl> </dd> <dt>array `a_tax`</dt> <dd> List of taxes to be applied to the registration fee. Keys are tax keys. <i>Primary key in {@link \RsTaxSql}.</i> Values are tax amounts. </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the tuition participant.</dd> </dl> </dd> <dt>string[] `a_uid_share`</dt> <dd>An array of UIDs representing the client''s relatives that share the Purchase Option.</dd> <dt>array `a_wellness_program`</dt> <dd> "Wellness Program" fields. <var>k_wellness_program</var> must be passed along with this array. See the description below. It''s recommended to validate the fields using the POST method of the {@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi} model. <dl> <dt>array `a_account`</dt> <dd> The list of reimbursement account field values. Corresponds to the `a_account` list in {@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi::$a_field_list}. Each element: <dl> <dt>string `k_field`</dt> <dd>The field key identifying the account field.</dd> <dt>string `text_value`</dt> <dd>The submitted value for the field.</dd> </dl> </dd> <dt>array `a_field`</dt> <dd> The list of wellness program field values. Corresponds to the `a_field` list in {@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi::$a_field_list}. Each element: <dl> <dt>string `k_field`</dt> <dd>The field key identifying the program field.</dd> <dt>string `text_value`</dt> <dd>The submitted value for the field.</dd> </dl> </dd> </dl> </dd> <dt>string `dt_prorate`</dt> <dd>The prorate date, used only for memberships.</dd> <dt>string `dt_send_local`</dt> <dd>The date when the gift card will be sent. This is only used for gift cards.</dd> <dt>string `dt_start`</dt> <dd>The start date, used only for memberships.</dd> <dt>bool `is_pay_when_start`</dt> <dd> If `true`, the client won''t be charged for this item until its start date. Otherwise, this will be `false`. The Purchase Option must have a specified start date. </dd> <dt>bool `is_prorate`</dt> <dd> If this is set to `1`, this purchase will use the prorate rule. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>bool `is_prorate_fix`</dt> <dd> If this is set to `1`, the custom prorate amount will be used. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>bool `is_prorate_only`</dt> <dd> If this is set to `1`, this purchase is prorate only. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>bool `is_renew`</dt> <dd> If this is set to `1`, auto-renew will be enabled for the item. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>string `k_appointment`</dt> <dd>The appointment key, used only for appointment add-ons.</dd> <dt>string `k_coupon_amount`</dt> <dd>The key of the gift card amount, used only for gift cards.</dd> <dt>string `k_staff`</dt> <dd> <b>Deprecated!</b> The staff member key, used only for appointment tips. <i>Primary key of staff member in {@link \RsStaffSql} table.</i> </dd> <dt>string `k_wellness_program`</dt> <dd> The "Wellness Program" key. Set this for the insurance membership promotion. <var>a_wellness_program</var> array must be passed along with the key. See the array description above. <p>Use the following models to work with this type of promotion:</p> <ul> <li>{@link \Wl\Insurance\Catalog\ProgramListApi} to obtain list of active programs.</li> <li>{@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi} to get and validate fields for a given program.</li> </ul> </dd> <dt>string `m_checkout`</dt> <dd> Staff-only: ignored when sent by a client. Total amount to charge for the tuition item at checkout (Mode 1). Distributed proportionally across `a_event_list` entries based on their own final cost. Ignored when all entries in `a_event_list` already contain `m_checkout` values (Mode 2). <i>See {@link \Wl\Purchase\Item\Tuition::prorateManualDistribute()}.</i> </dd> <dt>string `m_prorate_custom`</dt> <dd> The custom prorate price, used only for memberships. This is only used if <var>is_prorate_fix</var> is <tt>true</tt>. </dd> <dt>string `s_code`</dt> <dd>The gift card code. This is required for gift cards.</dd> <dt>string `s_image`</dt> <dd> The key of the image for the gift card. If empty, the business''s first gift card image will be used. Specify this only for gift cards. </dd> <dt>string `s_mail`</dt> <dd>The gift card receiver email. This is required for gift cards.</dd> <dt>string `s_recipient`</dt> <dd>The gift card receiver name. This is required for gift cards.</dd> <dt>string `s_sender`</dt> <dd>The gift card sender name. This is required for gift cards.</dd> <dt>string `uid_staff`</dt> <dd>The staff member user ID, used only for appointment tips.</dd> <dt>string `uid_to`</dt> <dd>Specifies the recipient of a transfer Purchase Option.</dd> </dl> </dd> <dt>array `a_signature`</dt> <dd>Signatures for an item that requires agreement to several distinct contracts at once.</dd> <dt>array[] `a_tax_custom`</dt> <dd> Customer taxes (optional). Every element must contain the following keys: <dl> <dt>string `f_tax`</dt> <dd>The tax amount.</dd> <dt>string `k_tax`</dt> <dd>The tax key.</dd> </dl> </dd> <dt>int `i_quantity`</dt> <dd>The item quantity.</dd> <dt>int `id_sale`</dt> <dd>The item type ID. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the login prize to apply a login prize discount.</dd> <dt>string `k_shop_product_option`</dt> <dd>The product option key, required only for products.</dd> <dt>string `m_price_custom`</dt> <dd>The custom price (optional).</dd> <dt>string `s_signature`</dt> <dd>The client signature, used for an item that requires agreement to a single contract.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'The list of quiz response keys. Keys refer to quiz keys. <i>Primary key from {@link \Core\Quiz\QuizSql} table.</i> And values refer to responses. <i>Primary key from {@link \Core\Quiz\Response\ResponseSql}.</i> Or special values from the {@link QuizResponse::RESPONSE_SKIP} constant.'
                f_discount_percent:
                  type: number
                  format: float
                  description: 'The percentage discount (optional).'
                k_pay_installment_template:
                  type: string
                  description: 'The installment template key (optional). This will be `null` if the installment plan doesn''t exist or isn''t set for the purchased item.'
                k_visit:
                  type: string
                  description: 'The visit key to be paid (optional).'
                m_discount_flat:
                  type: string
                  description: 'The amount to discount (optional).'
                m_surcharge:
                  type: string
                  description: 'The manual surcharge amount. An empty string represents an automatic surcharge amount.'
                m_tip:
                  type: string
                  description: 'The tip amount (optional).'
                text_discount_code:
                  type: string
                  description: 'The discount code (optional).'
                text_receipt_note:
                  type: string
                  description: 'The custom receipt note.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_activity:
                        type: [string, "null"]
                        description: 'The key of login activity. This will be `null` if not set yet.'
                      k_purchase:
                        type: string
                        description: 'The purchase key created during payment.'
      tags:
        - Payment-Payment
  '/Wl/Catalog/CatalogList/Element.json':
    get:
      summary: 'Retrieves an information about current sale item. Used to render the detail view of a single store item (promotion, product, event, or coupon) in the client-facing catalog. Returns everything needed to display the item: price, taxes, images, description, booking restrictions, and available purchase options.'
      parameters:
        - name: a_discount_code
          in: query
          description: 'Information about the discount code: <dl> <dt>string `f_amount`</dt> <dd>The fixed amount of the discount.</dd> <dt>float `f_percent`</dt> <dd>The percentage amount of the discount.</dd> <dt>int `i_limit`</dt> <dd>Limitation.</dd> <dt>string `k_discount_code`</dt> <dd>The discount code key.</dd> <dt>string `s_discount_code`</dt> <dd>The discount code value.</dd> </dl>'
          schema:
            type: array
        - name: a_sale_id_group
          in: query
          description: 'The list of items grouped by sale categories on the store page. Keys refer to sale IDs from {@link \RsSaleSid}, and values refer to data to identify an item: <dl> <dt>int `id_sale`</dt> <dd>The item category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The primary key of item.</dd> <dt>string `k_shop_product_option`</dt> <dd> The product option or <tt>0</tt> for any other cases. <i>Primary key in {@link \RsShopProductOptionSql} table.</i> </dd> </dl>'
          schema:
            type: array
        - name: dl_client_prorate
          in: query
          description: 'The client prorate date. This will be `null` in cases where the client prorate date hasn''t passed.'
          schema:
            type: [string, "null"]
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_promotion_image_height
          in: query
          description: 'The promotion image height in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_promotion_image_width
          in: query
          description: 'The promotion image width in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: id_sale
          in: query
          description: 'The ID of item category. One of the {@link \RsSaleSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'Determines whether the API is called in the backend mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The item key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'The product option key. This will be `null` if not set yet.'
          schema:
            type: [string, "null"]
        - name: text_item
          in: query
          description: 'A list of goods to get information for. Every element must contain the next keys: Note that this must be serialized via JSON. If this field is specified, don''t specify any of the following fields: * {@link ElementApi::$id_sale} * {@link ElementApi::$k_id} * {@link ElementApi::$k_shop_product_option} This will be `null` to get information for only one item. <dl> <dt>int `id_sale`</dt> <dd>The ID of the item category. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_shop_product_option`</dt> <dd>The product option key. This will be <tt>0</tt> if the item isn''t a product.</dd> </dl>'
          schema:
            type: [string, "null"]
        - name: uid_customer
          in: query
          description: 'The UID of a customer (user) for whom the purchase is made. This is used in the backend to calculate discounts.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_age_restriction:
                        type: array
                        description: 'The age restriction configuration. Age restrictions for an item apply when they''re configured for a specific item and the API is requested from the backend or when age restriction are public. <dl> <dt>int `i_age_from`</dt> <dd>Minimum age for service (years part).</dd> <dt>int `i_age_from_month`</dt> <dd>Minimum age for service (months part).</dd> <dt>int `i_age_from_year`</dt> <dd>Minimum age for service (years part).</dd> <dt>int `i_age_to`</dt> <dd>Maximum age for service (years part).</dd> <dt>int `i_age_to_month`</dt> <dd>Maximum age for service (months part).</dd> <dt>int `i_age_to_year`</dt> <dd>Maximum age for service (years part).</dd> <dt>bool `is_age_public`</dt> <dd> Is service public even if user does not meet age requirements or not? `true` - to show service to everyone. `false` - to show service only to users who meet age requirements. </dd> <dt>bool `is_month_enabled`</dt> <dd> Whether months are enabled for age restrictions. `true` - age restrictions can include number of months. `false` - age restrictions can include only years. </dd> </dl>'
                      a_data:
                        type: array
                        description: 'Additional information specific for the item. The structure may be different depending on the item category. Consider the following examples: * For a product, this contains inventory information. * For a gift card, this contains possible amounts. * For a session pass/membership/package, this contains information about start and stop dates. <dl> <dt>int[] `a_service_access`</dt> <dd> Access to services for a purchase option. Keys are one of the {@link \Wl\Service\ServiceSid} constants, values are one of the {@link \AFlagSid} constants. Set only for relevant purchase option service category. {@link \AFlagSid::ON} access to some services. {@link \AFlagSid::OFF} no access to services. It can be set only for classes and events. {@link \AFlagSid::ALL} access to all services. It can be set only for classes and events. For purchase options with appointments and assets service category status is always {@link \AFlagSid::ON}. </dd> <dt>bool `is_renew_public`</dt> <dd> This applies only for promotions. `true` - clients can set promotion auto-renew. `false` - clients can''t set promotion auto-renew. </dd> <dt>array[] `a_component`</dt> <dd> This applies only for coupons. Coupon components information. Each element will contain the following keys: <dl> <dt>int `id_program`</dt> <dd>Program ID. One of {@link \RsProgramSid} ID''s. Only applies to promotions.</dd> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. One of {@link \RsPurchaseItemSid} ID''s.</dd> <dt>int `id_sale`</dt> <dd>Sale ID. One of {@link \RsSaleSid} ID''s.</dd> <dt>string `k_id`</dt> <dd>The identifier of the item.</dd> <dt>string `text_title`</dt> <dd>The title of the item.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> This applies to enrollment/event items. Staff list for class periods. Each element contains: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. <i>Primary key in {@link \RsStaffSql}.</i> Deprecated, use `uid_staff`.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `text_family`</dt> <dd>Staff last name.</dd> <dt>string `text_staff`</dt> <dd>Staff display name.</dd> </dl> </dd> <dt>string `dl_expire`</dt> <dd>Date of expiration of coupon, local date in MySQL format.</dd> <dt>string `dl_now`</dt> <dd>Current date, local date in MySQL format.</dd> <dt>string `dl_start`</dt> <dd> Date to activate the coupon on, local date in MySQL format. When `id_activation`=FIXED, this field contains a custom date to activate the coupon on, local date in MySQL format. </dd> <dt>int `i_duration`</dt> <dd>Number of periods the coupon is active. Type of a period is specified by `id_duration`.</dd> <dt>int `id_activation`</dt> <dd>Type of a coupon activation date specification. One of {@link \Wl\Coupon\Edit\ActivationSid} constants.</dd> <dt>int `id_duration`</dt> <dd>Duration of a period. A constant from {@link \ADurationSid}.</dd> <dt>int `id_duration_type`</dt> <dd>A way to specify a duration. One of {@link \Wl\Coupon\Edit\DurationTypeSid} constants.</dd> </dl>'
                      a_image:
                        type: array
                        description: 'Image information: <dl> <dt>int `i_height`</dt> <dd>The height in pixels.</dd> <dt>int `i_width`</dt> <dd>The width in pixels.</dd> <dt>bool `is_empty`</dt> <dd> `true` - the item has no image (in this case, ignore the other keys of this array). `false` - the item has an image. </dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl> Use {@link ElementApi::$a_image_list} instead.'
                      a_image_list:
                        type: array
                        description: 'List of images. Keys are index and value is below information: <dl> <dt>int `i_height`</dt> <dd>The height in pixels.</dd> <dt>int `i_width`</dt> <dd>The width in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` - item has no image (in this case ignore other keys of this array). `false` - item has an image.</dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl>'
                      a_installment_template:
                        type: array
                        description: 'A list of installment plans. Each element has the following next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by <var>id_period</var> between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency Key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql}.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl>'
                      a_item:
                        type: array
                        description: 'The list of information pertaining to the specified item. <dl> <dt>array `a_data`</dt> <dd> Contains additional data for the sale item. For Package, it contains also the following key: The same structure as {@link \Wl\Catalog\CatalogList\ElementApi::$a_data} has. <dl> <dt>bool `is_price_breakdown`</dt> <dd> Whether to display individual prices for each item in the package. `true` display individual prices for each item in the package, `false` display a single total price for the package. </dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Contains information about one image connected to a sale item. <dl> <dt>array `a_image`</dt> <dd> <dl> <dt>int `i_height`</dt> <dd>Height of the variant image.</dd> <dt>int `i_height_src`</dt> <dd>Height of the original image.</dd> <dt>int `i_rotate`</dt> <dd>Rotate.</dd> <dt>int `i_width`</dt> <dd>Width of the variant image.</dd> <dt>int `i_width_src`</dt> <dd>Width of the original image.</dd> <dt>int `id_type_src`</dt> <dd>Type ID of the image. Constant from {@link \Core\Drive\DriveTypeSid}.</dd> <dt>bool `is-resize`</dt> <dd>`true` if the variant differs from the original, `false` - otherwise.</dd> <dt>string `url-thumbnail`</dt> <dd>URL to download variant (thumbnail) file.</dd> <dt>string `url-view`</dt> <dd>URL to download original file.</dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of the variant image.</dd> <dt>int `i_width`</dt> <dd>Width of the variant image.</dd> <dt>bool `is_empty`</dt> <dd>`true` if the image file does not exist, `false` - otherwise.</dd> <dt>string `s_url`</dt> <dd>Link to the variant file.</dd> </dl> </dd> <dt>array[] `a_tax`</dt> <dd> Contains information about taxes. <i>The structure of this array is described in {@link \RsTax::$a_tax}.</i> <dl> <dt>float `f_tax`</dt> <dd>The calculated tax amount applied by this rule.</dd> <dt>string `f_tax_discount`</dt> <dd>The tax amount after applying all discounts.</dd> <dt>string `f_tax_discount_login`</dt> <dd>The tax amount after applying the client type discount only.</dd> <dt>float `f_value`</dt> <dd>The tax rate. Its meaning depends on `id_tax`.</dd> <dt>int `id_tax`</dt> <dd>The tax type. One of {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key. <i>Primary key in the {@link \RsTaxSql} table.</i></dd> <dt>string `s_tax`</dt> <dd>The tax name.</dd> </dl> </dd> <dt>int `id_purchase_option_view`</dt> <dd>The Purchase Option view type. One of the {@link \Wl\Catalog\PurchaseOptionViewSid} constants.</dd> <dt>string `m_discount_code`</dt> <dd>The discount code amount.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type.</dd> <dt>string `s_comment`</dt> <dd>Additional information about the sale item. For example, information about ''introductory offer''.</dd> <dt>string `s_price`</dt> <dd>The price of the sale item in a human-readable format.</dd> <dt>string `s_sale`</dt> <dd>The category title of the sale item.</dd> <dt>string `s_title`</dt> <dd>The title of the sale item.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'A list of the item''s taxes. Keys refer tax keys, and values refer to the amount of tax. <dl> <dt>float `f_tax`</dt> <dd>The calculated tax amount applied by this rule.</dd> <dt>string `f_tax_discount`</dt> <dd>The tax amount after applying all discounts.</dd> <dt>string `f_tax_discount_login`</dt> <dd>The tax amount after applying the client type discount only.</dd> <dt>float `f_value`</dt> <dd>The tax rate. Its meaning depends on `id_tax`.</dd> <dt>int `id_tax`</dt> <dd>The tax type. One of {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key. <i>Primary key in the {@link \RsTaxSql} table.</i></dd> <dt>string `s_tax`</dt> <dd>The tax name.</dd> </dl>'
                      f_price:
                        type: [string, "null"]
                        description: 'The price of the sale item.'
                      f_price_include:
                        type: [string, "null"]
                        description: 'The price of the sale item, including tax.'
                      f_price_retail_product:
                        type: string
                        description: 'The retail price of the product. This will be empty if this isn''t a product.'
                      f_price_total_enrollment:
                        type: string
                        description: 'Full price of event. This will be empty if this isn''t an event.'
                      f_tax:
                        type: [string, "null"]
                        description: 'The tax amount.'
                      html_description:
                        type: [string, "null"]
                        description: 'The sale item description.'
                      html_special:
                        type: [string, "null"]
                        description: 'Special instructions for the sale item.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'The purchase item category ID. One of the {@link \RsPurchaseItemSid} constants.'
                      id_purchase_option_view:
                        type: number
                        format: int32
                        description: 'The ID of the item view category. One of the {@link PurchaseOptionViewSid} constants.'
                      id_sale:
                        type: number
                        format: int32
                        description: 'The ID of item category. One of the {@link \RsSaleSid} constants.'
                      is_contract:
                        type: boolean
                        description: 'If `true`, the item requires a contract. Otherwise, this will be `false`.'
                      k_id:
                        type: string
                        description: 'The item key.'
                      k_shop_product_option:
                        type: [string, "null"]
                        description: 'The product option key. This will be `null` if not set yet.'
                      m_discount_code:
                        type: string
                        description: 'The discount amount for a discount code.'
                      m_discount_login:
                        type: string
                        description: 'The discount amount for a user''s type.'
                      m_price:
                        type: string
                        description: 'The price on the price tag.'
                      m_price_include:
                        type: string
                        description: 'The price, including taxes.'
                      m_tax:
                        type: string
                        description: 'The tax amount.'
                      s_comment:
                        type: string
                        description: 'Additional comment(s). For example, information about ''introductory offer''.'
                      s_price:
                        type: [string, "null"]
                        description: 'The price of the sale item in a human-readable format.'
                      s_sale:
                        type: [string, "null"]
                        description: 'The category title of the sale item.'
                      s_title:
                        type: [string, "null"]
                        description: 'The sale item title.'
                      text_price:
                        type: string
                        description: 'The price on the price tag, with the currency sign.'
                      text_sale:
                        type: string
                        description: 'The title of the item category.'
                      text_title:
                        type: string
                        description: 'The item title.'
                      xml_description:
                        type: [string, "null"]
                        description: 'A detailed description.'
                      xml_special:
                        type: [string, "null"]
                        description: 'Special instructions.'
      tags:
        - CatalogList-Element
  '/Wl/Catalog/CatalogList/List.json':
    get:
      summary: 'Retrieves a list of all sale items. Used to render the full client-facing store catalog for a business. Returns all purchasable items - promotions, products, events, and coupons - merged across the business''s categories. The separate de-duplicated list and category list support both the flat and category-tabbed views.'
      parameters:
        - name: a_direct_link
          in: query
          description: 'Arguments from direct purchase link, which can give additional access to products, which are available only by such link. <dl> <dt>int `id_sale`</dt> <dd>Type of the item from {@link \RsSaleSid}.</dd> <dt>bool `is_product`</dt> <dd> `true` if `k_id` value is product key, `false` if `k_id` value is product option key. Optional. Default value is `false`. Used for `id_sale` === {@link \RsSaleSid::PRODUCT} only. </dd> <dt>string `k_id`</dt> <dd>Key of item.</dd> </dl>'
          schema:
            type: array
        - name: is_credit_card_check
          in: query
          description: '`true` to consider the requirement to have a credit card for booking (this requirement can be set in business settings) `false` to ignore it.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of a location. If `0`, all products in the business are retrieved.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'The list of all sale items (de-duplicated). Each element has the following keys: <dl> <dt>string[] `a_location`</dt> <dd> The list of location keys where the sale item is available. <i>Primary keys in the {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_shop_category`</dt> <dd>Shop category keys the item belongs to. <i>Primary keys in the {@link \RsShopCategorySql} table.</i></dd> <dt>string `dtu_create`</dt> <dd>UTC creation date of the item in MySQL format.</dd> <dt>string `f_price`</dt> <dd>The item price.</dd> <dt>bool `hide_application`</dt> <dd>`true` if the item should be hidden from the application, `false` otherwise.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_direct`</dt> <dd>`true` if the item was accessed via a direct purchase link, `false` otherwise.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if the item is an introductory offer, `false` otherwise.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_shop_category`</dt> <dd> The primary shop category key for this item entry. <i>Primary key in the {@link \RsShopCategorySql} table.</i> </dd> <dt>string `s_key`</dt> <dd>The unique string key identifying the item (product key combined with its DB SID).</dd> <dt>string `s_title`</dt> <dd>The item title.</dd> <dt>string `url_catalog`</dt> <dd>Direct URL to the catalog view page for this item.</dd> </dl>'
                      a_product_duplicate:
                        type: array
                        description: 'The list of products to show with duplicates. Products have their own order within every shop category, and they can be related to several categories. As a design decision, products are sorted in the backend using duplicates for every category. <dl> <dt>string[] `a_location`</dt> <dd> The list of location keys where the sale item is available. <i>Primary keys in the {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_shop_category`</dt> <dd>Shop category keys the item belongs to. <i>Primary keys in the {@link \RsShopCategorySql} table.</i></dd> <dt>string `dtu_create`</dt> <dd>UTC creation date of the item in MySQL format.</dd> <dt>string `f_price`</dt> <dd>The item price.</dd> <dt>bool `hide_application`</dt> <dd>`true` if the item should be hidden from the application, `false` otherwise.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_direct`</dt> <dd>`true` if the item was accessed via a direct purchase link, `false` otherwise.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if the item is an introductory offer, `false` otherwise.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_shop_category`</dt> <dd> The primary shop category key for this item entry. <i>Primary key in the {@link \RsShopCategorySql} table.</i> </dd> <dt>string `s_key`</dt> <dd>The unique string key identifying the item (product key combined with its DB SID).</dd> <dt>string `s_title`</dt> <dd>The item title.</dd> <dt>string `url_catalog`</dt> <dd>Direct URL to the catalog view page for this item.</dd> </dl>'
      tags:
        - CatalogList-List
  '/Wl/Catalog/CatalogList/CatalogProduct.json':
    get:
      summary: 'Gets store products by shop category. Used to populate a specific product category tab in the client-facing store. Supports pagination so large categories can be loaded incrementally. Returns the products together with the sort order configured for the category and a cache key for client-side caching.'
      parameters:
        - name: a_filter
          in: query
          description: 'Additional data to filter products. <dl> <dt>array `a_quick_config`</dt> <dd> Additional data for cases where the catalog list is in Quick Buy mode. Contains: <dl> <dt>string `k_visit`</dt> <dd>The visit key used to filter quick-buy items.</dd> </dl> </dd> <dt>int[] `a_sale_id`</dt> <dd>The list of special categories for the catalogue. Each value is a constant of {@link \RsSaleSid}.</dd> <dt>string[] `a_shop_category`</dt> <dd>Shop category keys to filter by. <i>Primary keys in the {@link \RsShopCategorySql} table.</i></dd> <dt>int `i_last`</dt> <dd>The last shown product index for pagination.</dd> <dt>int `id_order`</dt> <dd>The sort order ID. One of the {@link \Core\Sid\SortOrderSid} constants.</dd> <dt>int `id_sort`</dt> <dd>The sort field ID. One of the {@link \Wl\Shop\Category\ShopCategorySortSid} constants.</dd> <dt>string `k_business`</dt> <dd>The business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `s_title`</dt> <dd>A title fragment to filter products by name.</dd> <dt>string `uid`</dt> <dd>The user key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: i_last
          in: query
          description: 'The currently shown element.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key to get products for.'
          schema:
            type: string
        - name: k_shop_category
          in: query
          description: 'The selected shop category. This will be `0` for the system''s Unassigned category or `null` if not set yet.'
          schema:
            type: string
        - name: s_cache_key
          in: query
          description: 'The cache key used to get products.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category_sort:
                        type: array
                        description: 'Categories with sort settings. Keys refer to shop category keys. Values refer to sort settings. Contains the following structure: <dl> <dt>int[] `a_order`</dt> <dd> The order of the products by category if {@link \Wl\Shop\Category\ShopCategorySortSid::CUSTOM} is selected. Each element is a product key string, ordered as desired. </dd> <dt>int `id_order`</dt> <dd>The sort order. One of the {@link \Core\Sid\SortOrderSid} constants.</dd> <dt>int `id_sort`</dt> <dd>The sort field ID. One of the {@link \Wl\Shop\Category\ShopCategorySortSid} constants.</dd> </dl>'
                      a_product:
                        type: array
                        description: 'The list of products. Each element has the following keys: <dl> <dt>string `html_price`</dt> <dd>HTML-escaped formatted price of the product.</dd> <dt>string `html_price_early`</dt> <dd>HTML-escaped formatted early-bird price of the product. Present only when an early price applies.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The product key.</dd> <dt>string `k_shop_category`</dt> <dd>The shop category key. <i>Primary key in the {@link \RsShopCategorySql} table.</i></dd> <dt>string `s_title`</dt> <dd>The product title.</dd> <dt>string `url_catalog`</dt> <dd>Direct link to the catalog view page for this product.</dd> </dl>'
                      i_last:
                        type: number
                        format: int32
                        description: 'The currently shown element.'
                      is_load_more:
                        type: boolean
                        description: 'Determines whether more products can be loaded.'
                      s_cache_key:
                        type: string
                        description: 'The cache key used to get products.'
      tags:
        - CatalogList-CatalogProduct
  '/Wl/Catalog/Quiz/Quiz.json':
    get:
      summary: 'Gets quizzes for the selected purchase options. Resolves the quizzes required or available for the services behind the given purchase items, taking into account quizzes already completed by the specified user, and returns them so the client can collect answers before booking is finished.'
      parameters:
        - name: a_purchase_item
          in: query
          description: 'List of purchase items. Each element has format <tt>[id_purchase_item]::[k_id]</tt>, where Empty if no purchases are made for booking. <dl> <dt>int `id_purchase_item`</dt> <dd>ID of the purchase item. One of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>Key of the item. Depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Key of a business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user who is making a purchase. `null` in case when quizzes requested for guest.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: object
                        description: 'List of quizzes. Each element has next structure: <dl> <dt>bool `is_require`</dt> <dd>Whether the quiz is required and can not be skipped.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key from {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> is_require: bool, k_quiz: string, text_title: string }'
      tags:
        - Quiz-Quiz
  '/Wl/Reward/Action/Action.json':
    get:
      summary: 'Retrieves a list of available reward actions. Returns all reward actions visible to the specified user in the given business, optionally filtered by category, including the score value and request status for each action.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_reward_action_category
          in: query
          description: 'Key of reward action category.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward_action:
                        type: array
                        description: 'A list reward actions. Every element has next keys: <dl> <dt>int `id_reward_action_category`</dt> <dd> ID of reward actions category type. One of {@link \RsRewardActionCategorySid} constants. <tt>false</tt> for custom reward actions categories. </dd> <dt>string `k_reward_action`</dt> <dd>ID of reward action. <i>Primary key in {@link \RsRewardActionSql} table.</i></dd> <dt>string `k_reward_action_category`</dt> <dd>ID of reward action category in database. <i>Primary key in {@link \RsRewardActionCategorySql} table.</i></dd> </dl>'
      tags:
        - Action-Action
  '/Wl/Reward/Action/ActionType.json':
    get:
      summary: 'Retrieves information about reward actions. Returns all reward action records of the specified score type for the business, including the point value configured for each action.'
      parameters:
        - name: id_reward_score
          in: query
          description: 'ID of type of reward action. One of {@link \RsRewardScoreSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of a business to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward_action:
                        type: array
                        description: 'Information about reward actions. Every element has next keys: <dl> <dt>int `i_score`</dt> <dd>Number of points for this reward action.</dd> </dl>'
      tags:
        - Action-ActionType
  '/Wl/Reward/Action/Element.json':
    get:
      summary: 'Retrieves information about reward action item. Returns the point value, score type, description, and whether the user has already submitted a points request for the specified reward action.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_action
          in: query
          description: 'Reward action to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Points amount that will be added to user account after successful accomplishment of reward action.'
                      id_reward_score:
                        type: [number, "null"]
                        description: 'Reward score type ID.'
                      is_request:
                        type: boolean
                        description: '<tt>true</tt> if user has requested points for action accomplishment, <tt>false</tt> otherwise.'
                      s_description:
                        type: string
                        description: 'User friendly reward action description.'
      tags:
        - Action-Element
    post:
      summary: 'Requests points earning. Submits a custom reward points request for the specified action on behalf of the given user and records the pending approval request.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_action
          in: query
          description: 'Reward action to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Element
  '/Wl/Reward/Action/ActionWrite.json':
    post:
      summary: 'Updates configuration fields for the specified reward action. Accepts changes to fields such as auto-renewal flag, point cap, point value, and linked promotions, and persists only the provided fields while logging the changes.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_promotion:
                  type: [array, "null"]
                  description: 'Promotions'' list. <tt>null</tt> if not set yet.'
                i_cap:
                  type: [number, "null"]
                  description: 'Maximum count of score. <tt>null</tt> if not set yet.'
                i_count:
                  type: [number, "null"]
                  description: 'Count of the duration period. <tt>null</tt> if not set yet.'
                i_score:
                  type: [number, "null"]
                  description: 'Score of the reward. <tt>null</tt> if not set yet.'
                id_cap:
                  type: [number, "null"]
                  description: 'Period to apply maximum count of score. <tt>null</tt> if not set yet.'
                id_duration:
                  type: [number, "null"]
                  description: 'Type of a period. <tt>null</tt> if not set yet.'
                is_account_credit:
                  type: [boolean, "null"]
                  description: 'Flag to define points type (account credits or points) in the ''Refer-a-Friend'' section on the ''Enable Points'' page. <tt>true</tt>, if these are account credits. <tt>false</tt>, if these are points. <tt>null</tt> if field left unchanged.'
                is_auto_renewal:
                  type: [boolean, "null"]
                  description: 'Whether auto-renewal is enabled for this reward action.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. <tt>null</tt> if not set yet.'
                k_class:
                  type: [string, "null"]
                  description: 'Class ID. <tt>null</tt> if not set yet.'
                k_promotion:
                  type: [string, "null"]
                  description: 'Promotion key. <tt>null</tt> if not set yet.'
                k_reward_action:
                  type: [string, "null"]
                  description: 'Reward action ID. <tt>null</tt> if not set yet.'
                k_service:
                  type: [string, "null"]
                  description: 'Service key. <tt>null</tt> if not set yet.'
                k_shop_product:
                  type: [string, "null"]
                  description: 'Shop product ID. <tt>null</tt> if not set yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-ActionWrite
  '/Wl/Reward/Prize/Element.json':
    get:
      summary: 'Retrieves information about redeemable prize item. Returns the point cost and description for the specified redeemable prize in the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_prize
          in: query
          description: 'Key of redeemable prize.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Prize price in wellnessliving points.'
                      s_description:
                        type: string
                        description: 'User friendly prize description.'
      tags:
        - Prize-Element
    post:
      summary: 'Redeems selected prize. Deducts the required points from the user''s reward balance and records the redemption, returning the key of the created login prize record.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_prize
          in: query
          description: 'Key of redeemable prize.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_prize:
                        type: string
                        description: 'Key of login prize.'
      tags:
        - Prize-Element
  '/Wl/Reward/Prize/Prize.json':
    get:
      summary: 'Retrieves a list of redeemable prizes. Returns all active redeemable prizes for the specified business, ordered by point cost, including the prize key and required score for each item.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_prize:
                        type: array
                        description: 'Array of prize keys.'
                      a_score:
                        type: array
                        description: 'Array of prize scores.'
      tags:
        - Prize-Prize
  '/Wl/Reward/Board/Element.json':
    get:
      summary: 'Retrieves information about reward board item. In single-user mode returns the user''s score, name, and avatar on the specified board; in multi-user mode accepts a list of UIDs and returns an array of score, name, and avatar entries for all of them.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of users to get information for. <tt>null</tt> for mode of single user.'
          schema:
            type: [array, "null"]
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_board
          in: query
          description: 'Reward board to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward:
                        type: [array, "null"]
                        description: 'List of reward board elements. `null` if not loaded. Each element: <dl> <dt>int `i_score`</dt> <dd>Score in points.</dd> <dt>string `text_name`</dt> <dd>Reward name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `url_logo`</dt> <dd>URL to the user logo image.</dd> </dl>'
                      i_score:
                        type: number
                        format: int32
                        description: 'Score in points.'
                      s_name:
                        type: string
                        description: 'User name.'
                      url_logo:
                        type: string
                        description: 'User logo.'
      tags:
        - Board-Element
  '/Wl/Reward/Board/List.json':
    get:
      summary: 'Retrieves a list of reward board items to show. Returns an ordered list of user IDs ranked by points for the specified reward board, up to the configured maximum number of entries.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for.'
          schema:
            type: string
        - name: k_reward_board
          in: query
          description: 'ID of reward board to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid:
                        type: array
                        description: 'A list of UID items.'
      tags:
        - Board-List
  '/Wl/Reward/Score/Score.json':
    get:
      summary: 'Returns the total reward points for the specified activity list or user. If `a_login_activity` is provided, returns the total points earned across those activities; otherwise returns the current point balance for the specified user in the given business.'
      parameters:
        - name: a_login_activity
          in: query
          description: 'List of login activity keys for which points should be added or returned. NOTE: Login activity might belong to different users. It is possible for ex if booking performed for several users (usually family members) and required to retrieve sum of all scores for preformed activities.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Depending on arguments specified during API request might be: * Total number of points which were earned for each activity in a list provided in {@link ScoreApi::$a_login_activity} in a case if it was provided. * Total number of points user currently own in a cast if {@link ScoreApi::$uid} specified.'
      tags:
        - Score-Score
    post:
      summary: 'Gives user reward points for sharing of certain activity into Facebook. Validates the activity list, determines the appropriate reward score type per activity category, and schedules reward point additions for each shareable activity.'
      parameters:
        - name: a_login_activity
          in: query
          description: 'List of login activity keys for which points should be added or returned. NOTE: Login activity might belong to different users. It is possible for ex if booking performed for several users (usually family members) and required to retrieve sum of all scores for preformed activities.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Score-Score
    put:
      summary: 'Manually adjusts the reward points balance for the specified user. Requires the reward point reset privilege, creates a manual activity log entry, and applies the signed point adjustment to the user''s reward balance within a transaction.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_score:
                  type: number
                  format: int32
                  description: 'Depending on arguments specified during API request might be: * Total number of points which were earned for each activity in a list provided in {@link ScoreApi::$a_login_activity} in a case if it was provided. * Total number of points user currently own in a cast if {@link ScoreApi::$uid} specified.'
                k_business:
                  type: string
                  description: 'Business key within which score should be managed.'
                text_comment:
                  type: string
                  description: 'String to be added as a comment to manual reward points changes.'
                uid:
                  type: string
                  description: 'User key for which score should be managed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Score-Score
  '/Wl/Shop/Category/Category.json':
    get:
      summary: 'Returns the list of shop categories available for the given business. Returns all store categories that the current user is allowed to access, including their titles, descriptions, and display order. Guest and client users see only public categories; staff and admin users may see additional categories based on their access level.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get shop categories for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_shop_category:
                        type: array
                        description: 'An array containing information about all store categories. Each array element is an array containing the following fields: <dl> <dt>bool `is_default`</dt> <dd>Whether category is selected as default.</dd> <dt>int `i_order`</dt> <dd>The display order for the category.</dd> <dt>string `k_shop_category`</dt> <dd>The shop category key. <i>Primary key in {@link \RsShopCategorySql} table.</i></dd> <dt>string `text_description`</dt> <dd>The category description.</dd> <dt>string `text_title`</dt> <dd>The category name.</dd> </dl>'
      tags:
        - Category-Category
    post:
      summary: 'Creates new shop category. Creates a new product category in the business store with the given title. Requires backend access with the appropriate store management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get shop categories for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'Title of new category.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_shop_category:
                        type: string
                        description: 'Key of the created shop category.'
      tags:
        - Category-Category
  '/Wl/Video/Level/Level.json':
    delete:
      summary: 'Deletes the specified video level. Permanently removes the video level and unassigns it from all videos. If any videos are currently assigned to this level, a confirmation flag must be set; otherwise the API throws a confirmation-required error so the caller can prompt the user before proceeding.'
      parameters:
        - name: is_delete_confirm
          in: query
          description: 'If `true`, confirmation is required to delete videos. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_level
          in: query
          description: 'The video level key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-Level
    post:
      summary: 'Creates a new video level. Creates a difficulty level entry for the business video library. The new level is appended at the end of the current sort order and can be reordered afterwards using {@link LevelListApi::put()}. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_level
          in: query
          description: 'The video level key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video level title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_level:
                        type: string
                        description: 'The video level key.'
      tags:
        - Level-Level
    put:
      summary: 'Updates the specified video level. Renames an existing difficulty level in the business video library. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_level
          in: query
          description: 'The video level key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video level title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-Level
  '/Wl/Video/Level/LevelList.json':
    get:
      summary: 'Returns the list of video levels for the business. Returns all difficulty levels configured for the business video library, sorted by their current display order. Used to populate level pickers when creating or editing videos.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_level_list:
                        type: array
                        description: 'A list of video levels with the following structure: <dl> <dt>string `k_video_level`</dt> <dd>The video level key.</dd> <dt>string `text_title`</dt> <dd>The video level title.</dd> </dl>'
      tags:
        - Level-LevelList
    put:
      summary: 'Updates the order of video levels. Reorders the difficulty levels for the business video library according to the provided list. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of video level keys in the order to be saved.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-LevelList
  '/Wl/Video/Category/CategoryElement.json':
    delete:
      summary: 'Deletes the specified video category. Permanently removes the video category and its localized CMS records. Cannot be deleted if the category is currently configured as the cloud recording destination for the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_category
          in: query
          description: 'The category key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Category-CategoryElement
    get:
      summary: 'Returns the data for the specified video category. Returns the title, sort order, access restriction flags (by login type and member group), and whether this category is designated as the cloud recording destination for the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_category
          in: query
          description: 'The category key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_type:
                        type: array
                        description: 'A list of client and member types who can access videos from the category.'
                      a_member_group:
                        type: array
                        description: 'A list of member groups who can access videos from the category.'
                      i_order:
                        type: number
                        format: int32
                        description: 'The category''s placement in the business''s list of categories.'
                      is_cloud_recording:
                        type: boolean
                        description: 'This will be `true` if the video category is for cloud session recordings. Otherwise, this will be `false`.'
                      is_login_type:
                        type: boolean
                        description: 'This will be `true` if some client or member types can grant access to the video category. Otherwise, this will be `false`.'
                      is_member_group:
                        type: boolean
                        description: 'This will be `true` if some member groups can grant access to the video category. Otherwise, this will be `false` if no member groups can grant access to the video category.'
                      k_video_category:
                        type: string
                        description: 'The category key.'
                      text_title:
                        type: string
                        description: 'The category name.'
      tags:
        - Category-CategoryElement
    put:
      summary: 'Creates or updates a video category. Saves the category title and access restriction settings. When no category key is supplied, a new category is created; when an existing key is supplied, that category is updated. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_category
          in: query
          description: 'The category key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_type:
                  type: array
                  description: 'A list of client and member types who can access videos from the category.'
                a_member_group:
                  type: array
                  description: 'A list of member groups who can access videos from the category.'
                is_cloud_recording:
                  type: boolean
                  description: 'This will be `true` if the video category is for cloud session recordings. Otherwise, this will be `false`.'
                is_login_type:
                  type: boolean
                  description: 'This will be `true` if some client or member types can grant access to the video category. Otherwise, this will be `false`.'
                is_member_group:
                  type: boolean
                  description: 'This will be `true` if some member groups can grant access to the video category. Otherwise, this will be `false` if no member groups can grant access to the video category.'
                text_title:
                  type: string
                  description: 'The category name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_category:
                        type: string
                        description: 'The category key.'
      tags:
        - Category-CategoryElement
  '/Wl/Video/Category/CategoryList.json':
    get:
      summary: 'Returns the list of video categories for the business. Returns all video categories for the business with their titles, video counts, and cloud recording assignment. In frontend mode, only categories accessible to the current user are included. Results can be filtered by name and optionally limited to non-empty categories.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: is_skip_empty_group
          in: query
          description: 'If `true`, groups that are missing videos won''t be displayed. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: text_filter
          in: query
          description: 'The filter phrase used to filter categories by name.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_video_category:
                        type: array
                        description: 'The business video library categories as found in {@link CategoryListApi::$k_business}. Each element has the following structure: <dl> <dt>bool `can_delete`</dt> <dd>Determines whether the video category can be deleted.</dd> <dt>int `i_video`</dt> <dd>The number of videos.</dd> <dt>string `k_video_category`</dt> <dd>The key of the video category.</dd> <dt>string `k_video_category_full`</dt> <dd>The string key of the video category.</dd> <dt>string `text_title`</dt> <dd>The title of the category.</dd> </dl>'
      tags:
        - Category-CategoryList
    put:
      summary: 'Updates the order of video categories. Reorders the video library categories for the business according to the provided list. Requires backend access with the video library management privilege.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of shared video category keys displayed in the order to be saved. Values can be: * <b>[Deprecated]</b> String keys in old format. <i>See {@link DbShardKey}.</i> * String keys in new format. <i>See {@link DbKey}.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Category-CategoryList
  '/Wl/Video/Watch/Watch.json':
    post:
      summary: 'Records the start of a video watch session for the current user. Creates a new watch record for the user and video, verifying that the user has access to the video under their current membership. Returns the watch key that the client must use for subsequent progress updates via {@link WatchApi::put()}. Admin users are silently skipped - no record is created for them.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_current_time:
                  type: number
                  format: int32
                  description: 'The current time in seconds the user is at in the video.'
                id_source:
                  type: number
                  format: int32
                  description: 'The source ID.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_video:
                  type: string
                  description: 'The video string key: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbKey::generate()} or {@link DbShardKey::keyBinary()}.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_watch:
                        type: string
                        description: 'The video watch key.'
      tags:
        - Watch-Watch
    put:
      summary: 'Updates the watch progress (current position and total watched time) for an existing watch record. Advances the stored playback position and accumulated watch time for the given watch record. The total watched time can only increase; updates that report a smaller value than what is already stored are silently ignored to handle out-of-order requests.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_current_time:
                  type: number
                  format: int32
                  description: 'The current time in seconds the user is at in the video.'
                i_watched:
                  type: number
                  format: int32
                  description: 'The duration in seconds the user has watched the video for.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_video_watch:
                  type: string
                  description: 'The video watch key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Watch-Watch
  '/Wl/Video/Tag/Tag.json':
    delete:
      summary: 'Deletes the specified video tag. Permanently removes the video tag from the business library. If any videos are currently assigned to this tag, a confirmation flag must be set; otherwise the API throws a confirmation-required error so the caller can prompt the user before proceeding.'
      parameters:
        - name: is_delete_confirm
          in: query
          description: 'If `true`, confirmation is required to delete videos. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_tag
          in: query
          description: 'The video tag key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tag-Tag
    post:
      summary: 'Creates a new video tag. Adds a new content tag to the business video library for use when categorizing videos. Requires backend access and an active video subscription with at least the basic plan.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_tag
          in: query
          description: 'The video tag key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video tag title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_tag:
                        type: string
                        description: 'The video tag key.'
      tags:
        - Tag-Tag
    put:
      summary: 'Updates the specified video tag. Renames an existing content tag in the business video library. Requires backend access and an active video subscription with at least the basic plan.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_tag
          in: query
          description: 'The video tag key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video tag title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tag-Tag
  '/Wl/Video/Tag/TagList.json':
    get:
      summary: 'Returns the list of video tags for the business. Returns all content tags configured for the business video library. Used to populate tag pickers when creating or editing videos. Requires an active video subscription.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tag_list:
                        type: array
                        description: 'A list of video tags with the following structure: <dl> <dt>string `k_video_tag`</dt> <dd>The video tag key.</dd> <dt>string `text_title`</dt> <dd>The video tag title.</dd> </dl>'
      tags:
        - Tag-TagList
  '/Wl/Member/Purchase/MemberByPromotion.json':
    get:
      summary: 'Returns active clients holding at least one active Purchase Option from the specified list. Accepts a comma-separated list of Purchase Option keys, validates them against the business, and returns all active clients who hold at least one non-expired, non-limited Purchase Option from that list. Clients to which the currently signed-in user has no profile access are excluded from the result.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business for which to get a list of clients.'
          schema:
            type: string
        - name: s_promotion_keys
          in: query
          description: 'The comma-separated list of the Purchase Option keys. Only active clients with at least one active Purchase Option from this list will be returned. For example: ''324,123,11,556'''
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_clients:
                        type: array
                        description: 'The list of active clients with the given Purchase Options. <dl> <dt>array[] `a_purchase_options`</dt> <dd> The list of active Purchase Options. Each element has: <dl> <dt>string|null `dl_end`</dt> <dd> The date in the location time zone when the Purchase Option expires. This will be `null` if the Purchase Option doesn''t expire. </dd> <dt>string `dtu_purchase`</dt> <dd>The global date and time when the Purchase Option was sold.</dd> <dt>string|null `dl_start`</dt> <dd> The date in location''s time zone when the Purchase Option starts. This will be `null` if not defined yet. For example, if the starting date is the date of the first visit and there haven''t been any visits yet. </dd> <dt>string|null `dl_terminate`</dt> <dd> The date in location time zone when the Purchase Option will be terminated. This will be `null` if the Purchase Option isn''t scheduled to be terminated. </dd> <dt>string `k_promotion`</dt> <dd>The key of the Purchase Option.</dd> </dl> </dd> <dt>string `uid`</dt> <dd>The key of the client.</dd> </dl>'
      tags:
        - Purchase-MemberByPromotion
  '/Wl/Member/Info/Info.json':
    get:
      summary: 'Returns information about specified member. Supports both single-user and batch modes: when `$a_uid` is provided, returns a keyed list of user data in `$a_result_list`; otherwise returns data for the single user identified by `$uid`. When `$is_full` is `true`, additional profile details, group membership, visit history, and lifetime value are included.'
      parameters:
        - name: a_uid
          in: query
          description: 'Primary keys of users whose information must be returned. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: a_uid_date
          in: query
          description: 'List of dates for load additional information about users. Key is UID of user. Value is date. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'Date of the session, if we show it on the appointment info window or on the attendance list.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: '`true` - if API is being used from backend, `false` - otherwise.'
          schema:
            type: boolean
        - name: is_full
          in: query
          description: 'If you need to return additional information set to `true` or `false` if not.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business ID required to access client information. Specify this as `0` to retrieve the system-wide version of the information.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'ID of the visit, if we show icons on the attendance list and information that depends on visit is required.'
          schema:
            type: string
        - name: s_show
          in: query
          description: 'A list of icons with additional information about the business member. If empty, all available icons will be displayed. Comma separated values from {@link MemberInfoSid::idSid()} method.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to retrieve member information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: [array, "null"]
                        description: 'Additional member data or `null` if any data can be shown. Will be filled if {@link InfoApi::$is_full} flag is set. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl>'
                      a_result_list:
                        type: [array, "null"]
                        description: 'List of users data. Keys - users primary keys. Values - users data. <i>See for {@link InfoApi::_get()} details.</i> Keys refer to clients'' primary keys, and values refer to clients'' data. If `null`, data for a single client is being requested. <dl> <dt>array `a_info`</dt> <dd> Additional user''s information. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>bool `is_traveller`</dt> <dd>`true` - user is traveller; `false` - not traveller.</dd> <dt>string `s_member`</dt> <dd>User''s member number.</dd> <dt>string `text_first_name`</dt> <dd>User''s first name.</dd> <dt>string `text_fullname`</dt> <dd>User''s name.</dd> <dt>string `url_barcode`</dt> <dd>Link to barcode image to scan member number.</dd> </dl>'
                      a_visit_last:
                        type: array
                        description: 'Information about last visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      a_visit_next:
                        type: array
                        description: 'Information about next visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      i_lifetime_visit:
                        type: number
                        format: int32
                        description: 'Count attend visits for one client.'
                      is_traveller:
                        type: [boolean, "null"]
                        description: 'If `true`, the client is a traveler. Otherwise, this will be `false`. This will be `null` in cases when the client is a walk-in. Or when {@link InfoApi::$is_full} is `false`.'
                      m_lifetime_value:
                        type: string
                        description: 'The member''s lifetime value.'
                      s_member:
                        type: [string, "null"]
                        description: 'The member ID. If `null`, the specified client isn''t a member of the specified business.'
                      text_first_name:
                        type: string
                        description: 'First user''s name.'
                      text_fullname:
                        type: string
                        description: 'Full user''s name.'
                      url_barcode:
                        type: string
                        description: 'URL to barcode image.'
                      url_email:
                        type: string
                        description: 'URL to email.'
      tags:
        - Info-Info
  '/Wl/Tuition/Enrollment/TuitionEnrollmentList.json':
    get:
      summary: 'Returns list of enrollments for the tuition. Get a full list of users who purchased the tuition. For each enrollment, returns the list of events and the users have been enrolled in.'
      parameters:
        - name: a_filter
          in: query
          description: 'Filters. List includes enrollments to the tuition. And enrollments to the tuition include enrollment to the event. If any part of the enrollment to the tuition is valid, according to the given filter, full enrollment is included. For example. Client A is payer for two tuition enrollments. One is for client B and events E1, E2. Another for clients C and D. Both for E1. And Client W pays for one tuition enrollment - for client X. If we filter list by enrolled client D, for example. List will return two tuition enrollments. Both paid with Client A. Because we think that these enrollments are combined in single family and should both be returned. All filters are optional. And added as AND. If you send certain events and certain statues, both conditions should be met. <dl> <dt>string[] `a_event`</dt> <dd>Leave only enrollments with the given events enrolled.</dd> <dt>int[] `a_statuses`</dt> <dd>List of tuition statuses.</dd> <dt>string[] `a_purchase_item_tuition`</dt> <dd>List of tuition enrollments ids to leave only.</dd> <dt>string[] `a_uid_any`</dt> <dd>Leave only enrollments with the given payers or enrolled clients.</dd> <dt>string[] `a_uid_enrolled`</dt> <dd>Leave only enrollments with the given enrolled clients.</dd> <dt>string[] `a_uid_payer`</dt> <dd>Leave only enrollments with the given payers.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_tuition_id
          in: query
          description: 'Key of the tuition in tuition microservice.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_enrollment:
                        type: array
                        description: 'List of tuition enrollments. <dl> <dt>array[] `a_events`</dt> <dd> Events for this enrollment. <dl> <dt>int|null `id_enrollment_status`</dt> <dd> Enrollment status. Active user is enrolled into the event, and there are upcoming sessions. Canceled, if enrollment is canceled. Completed, if all event sessions are in the past and enrollment is not canceled. </dd> <dt>int|null `id_membership_status`</dt> <dd>Membership status. `null` if tuition paid in full with installment.</dd> <dt>bool `is_cancelled`</dt> <dd>Whether this enrollment is cancelled or not.</dd> <dt>string `k_class`</dt> <dd>Event ID.</dd> <dt>string `m_event_discount`</dt> <dd>Login type and shopping cart discounts.</dd> <dt>string `m_event_subtotal`</dt> <dd>Price of the event before login type and shopping cart discounts.</dd> <dt>string `m_event_tax`</dt> <dd>Taxes amount.</dd> <dt>string `uid`</dt> <dd>Enrolled client.</dd> </dl> </dd> <dt>string|null `dl_next`</dt> <dd> Next payment date in local business timezone and MySQL format. It will be the closest date to the current date for membership tuition. `null` if we cannot define next payment date. </dd> <dt>string `dtl_enrollment`</dt> <dd>Date and time of the enrollment in local business timezone.</dd> <dt>int `i_payments_done`</dt> <dd>Number of payments done.</dd> <dt>int|null `i_payments_left`</dt> <dd>Number of payments left. `null` for membership-based tuition.</dd> <dt>int `i_payments_total`</dt> <dd>Number of payments total at the moment of enrollment.</dd> <dt>int|null `id_installment_status`</dt> <dd>Installment plan status. `null` if tuition paid in full without installment.</dd> <dt>string `k_purchase_item_tuition`</dt> <dd> Key of the tuition purchase item. This is enrollment key, which can be used to modify and cancel the enrollment. </dd> <dt>string `m_paid`</dt> <dd>Rest amount has been already paid.</dd> <dt>string `m_payment`</dt> <dd>One payment amount.</dd> <dt>string|null `m_rest`</dt> <dd>Rest amount to be paid. `null` for membership-based tuition.</dd> <dt>string|null `m_total`</dt> <dd>Total initial amount to be paid. `null` for membership-based tuition.</dd> <dt>string `uid_payer`</dt> <dd>Payer for this enrollment. This user can be enrolled or not, but he pays for the entire enrollment.</dd> </dl>'
                      a_enrollment_fee:
                        type: array
                        description: 'List of users who has paid or are scheduled to pay tuition fee. Keys are user ids. Values are payment information. <dl> <dt>bool `is_paid`</dt> <dd> If `true`, user has paid tuition fee. If `false`, user is scheduled to pay tuition fee. If a user did not pay and does not have a scheduled payment, the key is not present. </dd> </dl>'
      tags:
        - Enrollment-TuitionEnrollmentList
  '/Wl/Tuition/Enrollment/TuitionEnrollmentCancel.json':
    put:
      summary: 'Allows canceling certain event enrollments within tuition. Send all users and events, which should be canceled to reduce tuition payment, cancel visits, and deactivate even pass.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_event_cancel:
                  type: array
                  description: 'List of users and events to be canceled. Each element has two keys: `uid` and `k_class`. <dl> <dt>string `k_class`</dt> <dd>Enrolled event ID.</dd> <dt>string `uid`</dt> <dd>Enrolled user ID.</dd> </dl>'
                is_cancel_visits:
                  type: boolean
                  description: 'Whether upcoming visits should be canceled or not.'
                is_pass_deactivate:
                  type: boolean
                  description: 'Whether even pass should be deactivated or not.'
                k_business:
                  type: string
                  description: 'Key of the business.'
                k_purchase_item_tuition:
                  type: string
                  description: 'Key of the tuition purchase item. This is enrollment key, which can be got from {@link TuitionEnrollmentListApi}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Enrollment-TuitionEnrollmentCancel
  '/Wl/Tuition/Enrollment/TuitionClientsSummary.json':
    get:
      summary: 'Returns summary of clients and events enrolled for the given tuitions. For each requested tuition, returns the number of unique clients enrolled in at least one not cancelled event, the number of unique events with at least one not cancelled enrollment, and the total number of not cancelled event enrollments.'
      parameters:
        - name: a_tuition_id
          in: query
          description: 'Keys of the tuitions in the tuition microservice to get summary for.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_summary:
                        type: array
                        description: 'Summary of clients and events enrolled, per tuition. Keys - tuition IDs from {@link \Wl\Tuition\Enrollment\TuitionClientsSummaryApi::$a_tuition_id}. Values - summary information: <dl> <dt>int `i_clients_enrolled`</dt> <dd>Number of unique clients having at least one not cancelled enrolled event.</dd> <dt>int `i_enrollments_active`</dt> <dd> Total number of group enrollments with at least one not cancelled enrolled client in the group and an active payment plan - `id_installment_status` equal to {@link \Wl\Pay\Installment\InstallmentStatusSid::ACTIVE} for installment-based tuition plans, or at least one `id_promotion_status` equal to {@link \Wl\Login\Promotion\LoginPromotionStatusSid::ACTIVE} among the group''s memberships for membership-based tuitions. </dd> <dt>int `i_enrollments_total`</dt> <dd>Total number of group enrollments.</dd> <dt>int `i_enrolled_total`</dt> <dd>Total number of not cancelled event enrollments.</dd> <dt>int `i_events_unique`</dt> <dd>Number of unique events having at least one not cancelled enrollment.</dd> </dl>'
      tags:
        - Enrollment-TuitionClientsSummary
  '/Wl/Family/Relation/Relation72.json':
    delete:
      summary: 'Deletes family relation between 2 users. Removes the relationship between the user identified by `uid` and the user identified by `uid_delete` within the given business, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
        - name: uid_delete
          in: query
          description: 'The key of the related user who {@link \Wl\Family\Relation\RelationApi::$uid} must be removed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation72
    get:
      summary: 'Gets relative list. Returns the list of all family relationships for the specified user within the given business, including relationship type, reverse relationship type, name, and photo information for each related user.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation72
    post:
      summary: 'Adds to user {@link \Wl\Family\Relation\RelationApi::$uid} relative {@link \Wl\Family\Relation\RelationApi::$a_new}. Creates a bidirectional family relationship between the user identified by `uid` and the user specified in `a_new`, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_new:
                  type: array
                  description: 'The new relationship to be added to user, specified with {@link \Wl\Family\Relation\RelationApi::$uid}: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `uid`</dt> <dd>The related user''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation72
  '/Wl/Family/Relation/Relation.json':
    delete:
      summary: 'Deletes family relation between 2 users. Removes the relationship between the user identified by `uid` and the user identified by `uid_delete` within the given business, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
        - name: uid_delete
          in: query
          description: 'The key of the related user who {@link \Wl\Family\Relation\RelationApi::$uid} must be removed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation
    get:
      summary: 'Gets relative list. Returns the list of all family relationships for the specified user within the given business, including relationship type, reverse relationship type, name, and photo information for each related user.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation
    post:
      summary: 'Adds to user {@link \Wl\Family\Relation\RelationApi::$uid} relative {@link \Wl\Family\Relation\RelationApi::$a_new}. Creates a bidirectional family relationship between the user identified by `uid` and the user specified in `a_new`, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_new:
                  type: array
                  description: 'The new relationship to be added to user, specified with {@link \Wl\Family\Relation\RelationApi::$uid}: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `uid`</dt> <dd>The related user''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation
  '/Wl/Family/Relation/FamilyRelation.json':
    get:
      summary: 'Gets relationships list. Returns all relationship types enabled for the given business, or all system-defined relationship types if no business key is provided.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_relationships:
                        type: array
                        description: 'The relationship types in the business. Key is ID, value is SID.'
      tags:
        - Relation-FamilyRelation
  '/Wl/Classes/ClassList/List.json':
    get:
      summary: 'Returns list of classes and events in the business. Used by import and integration tools to enumerate all classes and events offered by a business. In franchise mode, classes from all franchisee locations are included. Results are sorted by title and start date.'
      parameters:
        - name: a_class_tab
          in: query
          description: 'List of tabs keys. Filtering by Book Now Tab is not supported if {@link ListApi::$is_franchise} is `true`. `null` if no filtering by Book Now Tab is required.'
          schema:
            type: [array, "null"]
        - name: id_class_tab
          in: query
          description: 'ID of book now tab. One of {@link TabSid} constants. Only {@link TabSid::TRAINING} and {@link TabSid::ENROLLMENT} book now tab are supported. Filtering by Book Now Tab is not supported if {@link ListApi::$is_franchise} is `true`. `null` if no filtering by Book Now Tab is required.'
          schema:
            type: [number, "null"]
        - name: is_enrollment_block_all
          in: query
          description: 'Whether all events should be returned from same enrollment block. * <tt>true</tt> to return all events from same enrollment block. * <tt>false</tt> to return only one event from each enrollment block. Only published and non-empty events will be returned for the client. To return empty events, use {@link ListApi::$is_enrollment_block_empty}. For the staff, filtering by publication or emptiness is not applied.'
          schema:
            type: boolean
        - name: is_enrollment_block_empty
          in: query
          description: 'Whether to include events without sessions. An empty event is neither published nor unpublished. It simply has no sessions. Such events can be useful for list filters. Affects clients only. Does not affect staff. Staff always see empty events. Makes sense in conjunction with the flag {@link ListApi::$is_enrollment_block_all}. * <tt>true</tt> to include events without sessions. * <tt>false</tt> to exclude events without sessions.'
          schema:
            type: boolean
        - name: is_event_include
          in: query
          description: 'Whether to include events in the result. true` to include both classes and events. false` to include only classes.'
          schema:
            type: boolean
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created classes (if business is franchisor). <tt>true</tt> to include franchisee-created classes.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'List of classes and events. The result may depend on the following flags: * {@link ListApi::$is_enrollment_block_all} * {@link ListApi::$is_enrollment_block_empty} Each element has the following structure: <dl> <dt>string[] `a_class_tab`</dt> <dd> List of class book now tabs, where this class is presented. Each element is <i>primary key from {@link \Wl\Classes\Tab\Sql\ClassTab\Sql}</i> or `null` if class is presented in default system tab Classes. Events can also be added in class tabs. </dd> <dt>string[] `a_event_tab`</dt> <dd> List of event book now tabs, where this event is presented. Each element is <i>primary key from {@link \Wl\Classes\Tab\Sql\ClassTab\Sql}</i> or `null` if event is presented in default system tab Enrollments. Classes cannot be added in event tabs. </dd> <dt>string `dl_end`</dt> <dd>The ending date of the class/event. Maybe a zero date if the class is ongoing (has no end date).</dd> <dt>string `dl_start`</dt> <dd>The starting date of the class/event.</dd> <dt>bool `is_active`</dt> <dd>Whether class/event is active.</dd> <dt>bool `is_event`</dt> <dd>Define if current element is event.<tt>true</tt> - event, <tt>false</tt> - class.</dd> <dt>bool `is_remove`</dt> <dd>Whether class/event is removed.</dd> <dt>string `k_class`</dt> <dd>Class/event identifier. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `k_enrollment_block`</dt> <dd>Event Block ID. <i>Primary key in {@link \RsEnrollmentBlockSql}.</i></dd> <dt>string `text_title`</dt> <dd>Class/event name.</dd> </dl>'
      tags:
        - ClassList-List
  '/Wl/Classes/ClassList/BookList.json':
    get:
      summary: 'Gets a list of classes which take place in the specified location. Used to build the booking page for a location, displaying all classes that clients can sign up for. Returns the full class details needed for display: schedules, assigned staff, booking links, pricing, and category tabs.'
      parameters:
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'The class list. Every element has the following structure: <dl> <dt>string[] `a_class_tab`</dt> <dd> The list of class tab keys for the class. <i>Primary keys in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd>List of login types for current class. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i></dd> <dt>string[] `a_member_group`</dt> <dd>List of member groups for current class. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i></dd> <dt>string[] `a_url_book`</dt> <dd>The list of URL to book this class.</dd> <dt>string `dtu_book_date`</dt> <dd>The book date of an event in UTC date/time format. Can be empty string if user cannot book the event.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `id_bookable`</dt> <dd>Bookable type ID. One of {@link \Wl\Service\BookableSid} constants.</dd> <dt>bool `is_active`</dt> <dd>This will be `true` if the class is active. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>This will be `true` for events. Otherwise, this will be `false` for classes.</dd> <dt>bool `is_online_private`</dt> <dd> `true` means to show class only for clients who can book online, `false` means to show class for all clients. </dd> <dt>string `k_class`</dt> <dd>The class key. <i>The primary key in {@link \RsClassSql} table.</i></dd> <dt>string `k_enrollment_block`</dt> <dd>The enrollment block key. <i>The primary key in {@link \RsEnrollmentBlockSql} table.</i></dd> <dt>string `m_price_total`</dt> <dd>The total price for an event.</dd> <dt>string `text_description`</dt> <dd>The class description.</dd> <dt>string `text_title`</dt> <dd>The class name.</dd> </dl>'
      tags:
        - ClassList-BookList
  '/Wl/Classes/Info/Info.json':
    get:
      summary: 'Gets information about class. Returns the class title, service logo, and whether the class is actually an event, based on the given business and class keys.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business in which the class resides.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'Class identifier to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_logo:
                        type: array
                        description: 'Service logo information: <dl> <dt>bool `is_empty`</dt> <dd>Whether service image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl>'
                      is_event:
                        type: boolean
                        description: '<tt>true</tt> means event, <tt>false</tt> means class.'
                      k_enrollment_block:
                        type: string
                        description: 'Key of the group of events, which are different instances of the same event. Is always <tt>0</tt> for classes.'
                      text_title:
                        type: string
                        description: 'Title of the class.'
      tags:
        - Info-Info
  '/Wl/Classes/ClassView/Element.json':
    get:
      summary: 'Returns class information including schedules, images, and booking settings for the specified business. Used by import tools to read the full class catalog for a business. Returns a map of all classes (or a single class) with the information needed to replicate class data in an external system: schedules, images, booking constraints, and descriptions.'
      parameters:
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'The class key used to get information for a specific class. An empty value returns information for all classes of the business.'
          schema:
            type: string
        - name: show_cancelled
          in: query
          description: 'Defines if canceled schedules should be included in the result. If `true`, canceled schedules will be shown. Otherwise, this will be `false`.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_list:
                        type: array
                        description: 'Displays information about the class schedule(s). Each element has the next structure: <dl> <dt>array `a_schedule`</dt> <dd> A list of class schedules. Each element has the next structure: <dl> <dt>array `a_repeat`</dt> <dd> Schedule repeat options: <dl> <dt>int `i_repeat`</dt> <dd>A count of the periods specified in <var>id_repeat</var>.</dd> <dt>int `id_repeat`</dt> <dd>The measuring unit of <var>i_repeat</var>. One of {@link \ADurationSid} constants.</dd> </dl> </dd> <dt>string[] `a_staff_key`</dt> <dd>A list of staff member keys that provide sessions for this schedule.</dd> <dt>string[] `a_uid_staff_key`</dt> <dd>A list of staff member keys UID that provide sessions for this schedule.</dd> <dt>string `dl_end`</dt> <dd> The end date of the schedule in the location''s time zone. This will be empty or zero if the schedule is ongoing. </dd> <dt>string `dl_start`</dt> <dd>The start date of the schedule in location''s time zone.</dd> <dt>int `i_day`</dt> <dd> The day of the week, where the class schedule exists, starting with 1 for Monday and ending with 7 for Sunday. </dd> <dt>int `i_duration`</dt> <dd>The duration of the schedule sessions in minutes.</dd> <dt>bool `is_cancel`</dt> <dd>Defines whether the schedule canceled or not.</dd> <dt>string `k_location`</dt> <dd>The location key of the schedule.</dd> <dt>string `m_price`</dt> <dd>The price of a single session of the schedule.</dd> <dt>string `text_room`</dt> <dd>The room of the class period.</dd> <dt>string `text_time`</dt> <dd>The time of the session in the following format: `[start_time] - [end_time]` in the locale''s time zone.</dd> </dl> </dd> <dt>array|null `a_config`</dt> <dd> Class-specific business policies overriding the business defaults. Has the same structure as {@link \Wl\Business\Config\BusinessConfigApi::$a_business_policy}. `null` when the class uses the default business policy without overrides. <dl> <dt>string[] `a_family_relation`</dt> <dd> List of allowed relation types specific to a given business. Key is ID. One of {@link \RsFamilyRelationSid} constant. Value is SID. <i>Result of {@link \RsFamilyRelationSid::allByBusiness()} method.</i> </dd> <dt>int[] `a_family_relation_login_allow`</dt> <dd> List of allowed relation types specific to a given business. Each value constant from {@link \RsFamilyRelationSid} class. </dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> Tags for quick search. <i>Result of {@link \Wl\Classes\ClassesInfo::$a_search_tag} property.</i> <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Name of the tag.</dd> </dl> </dd> <dt>array `a_visits_required`</dt> <dd> Information about visits that should be reviewed prior to visiting this class/event. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::visitsRequiredInfo()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl> </dd> <dt>bool|null `has_own_image`</dt> <dd> This field will be `true` if the image used for the class is an image uploaded in the class setup. If an image isn''t uploaded in the class setup, but there''s at least one image in Setup->Locations image slider, this field will be `false`. This field will be `null` if the class image isn''t uploaded, and there are no images in location slider. In such cases, an empty image is used. </dd> <dt>string `html_description`</dt> <dd>The browser-safe description.</dd> <dt>string `html_special_instruction`</dt> <dd>Special instructions safe to be inserted to a browser.</dd> <dt>int `i_age_from`</dt> <dd>The minimum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_from_month`</dt> <dd>The minimum age restriction (months).</dd> <dt>int `i_age_from_year`</dt> <dd>The minimum age restriction (years).</dd> <dt>int `i_age_to`</dt> <dd>The maximum age restriction. Deprecated and is left only for back compatibility.</dd> <dt>int `i_age_to_month`</dt> <dd>The maximum age restriction (months).</dd> <dt>int `i_age_to_year`</dt> <dd>The maximum age restriction (years).</dd> <dt>bool `is_age_public`</dt> <dd>Determines whether age restriction exist and should be taken into account prior to booking this class/event.</dd> <dt>bool `is_bookable`</dt> <dd>Determines whether clients can book the class or event online.</dd> <dt>bool `is_online_private`</dt> <dd>`true` - show classes only for clients who can book online. `false` - show classes for all clients.</dd> <dt>bool `is_promotion_client`</dt> <dd>Determines whether clients of the business can see a list of applicable Purchase Options.</dd> <dt>bool `is_promotion_only`</dt> <dd>Determines whether the item can be booked only using promotions.</dd> <dt>bool `is_promotion_staff`</dt> <dd>Determines whether staff members of the business can see a list of applicable Purchase Options.</dd> <dt>bool `is_single_buy`</dt> <dd> Determines whether the item can be paid with Drop In or not. If it can be paid with Drop In, then `m_price` shows the price for one visit. If this is `0`, it means the class is free. </dd> <dt>bool `is_virtual`</dt> <dd>Determines whether the item is virtual.</dd> <dt>bool `is_event`</dt> <dd> Determines whether the item is an event or class instance. If `true`, the item is an event instance. Otherwise, this will be `false`. </dd> <dt>string `k_class`</dt> <dd>The class key.</dd> <dt>string `k_promotion_default`</dt> <dd>Promotion key of the default promotion to use when paying for the class. `null` if no promotion is selected.</dd> <dt>string `m_price`</dt> <dd>Drop In price, if the class allows payment for one visit.</dd> <dt>bool `show_special_instructions`</dt> <dd> This will be `true` if special instructions can be public. Otherwise, this will be `false` if they should only be shown to the clients who booked the class. </dd> <dt>string `xml_description`</dt> <dd>The item description.</dd> <dt>string `xml_special_instruction`</dt> <dd>The item''s special instructions.</dd> <dt>string `url_image`</dt> <dd>The URL link to the item image.</dd> </dl>'
      tags:
        - ClassView-Element
  '/Wl/Classes/Promotion/ClassPromotion.json':
    get:
      summary: 'Returns list of promotions that can be used to pay for the class / event. Used in the booking flow to show clients which of their existing passes or memberships cover the selected class. Also returns the default promotion to pre-select so the client does not have to choose manually when there is an obvious match.'
      parameters:
        - name: is_event
          in: query
          description: 'Determines whether the class is an event or not.'
          schema:
            type: boolean
        - name: is_login_type_ignore
          in: query
          description: '`true` - the login type or group restrictions are ignored and all pricing options will be returned. This will require staff or admin access level. `false` - the API should return only pricing options, which are available for the current user.'
          schema:
            type: boolean
        - name: is_related_only
          in: query
          description: '`true` - promotions should only be returned if they''re related to the given class or event. `false` - all promotions should be returned, even if they aren''t related to the given class or event.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'The class key. This refers to the class or event with which you want to connect the promotions. This will be `null` or `0` if you need to return a list of promotions for an uncreated class or event.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data with the following structure:. <dl> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The title of the promotion.</dd> <dt>int `id_program`</dt> <dd>One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if the promotion is related to the class or event, <tt>false</tt> otherwise.</dd> </dl>'
                      k_promotion_default:
                        type: [string, "null"]
                        description: 'The default promotion key. This will be `null` if `k_class` wasn''t provided, or if the class has no default promotion.'
      tags:
        - Promotion-ClassPromotion
  '/Wl/Reception/Application/ReceptionSchedule.json':
    get:
      summary: 'Returns the schedule and HTML for the Self Check-In Web App for the given user. Returns the rendered schedule HTML, a structured list of upcoming sessions, and a map of service type HTML classes for the authenticated user at the given location.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'The key of the Self Check-In Web App. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show the schedule for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'A list of sessions to display with the following fields: <dl> <dt>array `a_resources_shared`</dt> <dd> A list of shared resources containing: <dl> <dt>string `k_resource`</dt> <dd>The resource key.</dd> <dt>string `s_title`</dt> <dd>The resource title.</dd> </dl> </dd> <dt>array `a_staff_all`</dt> <dd> A list of staff members instructing the service, containing: <dl> <dt>string `s_name_full`</dt> <dd>The staff member''s full name.</dd> </dl> </dd> <dt>string `dtu_date`</dt> <dd>The session date.</dd> <dt>string `html_class_js`</dt> <dd>The name of the HTML class that JavaScript uses to display the icon, depending on the service type.</dd> <dt>int `i_duration`</dt> <dd>The session duration in minutes.</dd> <dt>int `id_service`</dt> <dd>Service ID. One of the {@link \Wl\Service\ServiceSid} constants.</dd> <dt>bool `is_auto`</dt> <dd>If `true`, the session can be checked in automatically. Otherwise, this will be `false`.</dd> <dt>bool `is_book`</dt> <dd>If `true`, the client should be notified that the visit is booked. Otherwise, this will be `false`.</dd> <dt>bool `is_wait`</dt> <dd>If `true`, the client should be notified that they''re on the wait list. Otherwise, this will be `false`.</dd> <dt>string|null `k_appointment`</dt> <dd>The appointment key. If `null`, this isn''t an appointment.</dd> <dt>string|null `k_class_period`</dt> <dd>The class period key. If `null`, this isn''t a class or event.</dd> <dt>string|null `k_visit`</dt> <dd>The visit key if the session has been checked in. Otherwise, this will be `null`.</dd> <dt>string `s_class`</dt> <dd>The service name.</dd> <dt>string `s_duration`</dt> <dd>The session duration, formatted by the business configuration.</dd> <dt>string `s_time`</dt> <dd>The time the session takes place.</dd> </dl>'
                      a_schedule_class_all:
                        type: array
                        description: 'All types of services that appear in the schedule. Keys are constants from {@link ServiceSid}. Values are the HTML classes associated with that service.'
                      html_schedule:
                        type: string
                        description: 'The schedule to be shown in the Self Check-In Web App for the selected user.'
      tags:
        - Application-ReceptionSchedule
    post:
      summary: 'Performs check-in for the given user and returns the confirmation HTML and data. Books or checks the user into the specified class period or appointment, then returns confirmation HTML and structured data including payment info, assigned assets, and visit counts.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'The key of the Self Check-In Web App. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show the schedule for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dtu_date:
                  type: string
                  description: 'The date and time of the visit in UTC and MySQL format.'
                k_appointment:
                  type: [string, "null"]
                  description: 'The key of the appointment to check in. If `null`, this is class visit or gym visit.'
                k_class_period:
                  type: [string, "null"]
                  description: 'The key of the class schedule to check in. If `null`, this is an appointment visit or gym visit.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_confirmation_data:
                        type: array
                        description: 'Data for the confirmation screen with the following fields: <dl> <dt>array `a_payment`</dt> <dd> Data for the promotion payment informational card. Each element contains: <dl> <dt>array `a_restrict`</dt> <dd> Calendar restrictions. A duration pass will only have elements if a restriction has been met. This contains: <dl> <dt>int `i_limit`</dt> <dd>The count of possible visits.</dd> <dt>int `i_remain`</dt> <dd>The count of remaining visits.</dd> <dt>string `s_date`</dt> <dd>The calendar period name.</dd> </dl> </dd> <dt>bool `has_visit_past`</dt> <dd>If `true`, the promotion was renewed in the past. Otherwise, this will be `false`.</dd> <dt>bool `hide_visit_additional_info`</dt> <dd>If `true`, additional info should be excluded. Otherwise, this will be `false`.</dd> <dt>int `i_book`</dt> <dd>The count of upcoming uses of the promotion.</dd> <dt>int `i_book_duration`</dt> <dd> Number of minutes used for upcoming visits. Has sense only for promotions of program type {@link \RsProgramTypeSid::DURATION}. </dd> <dt>int `i_limit`</dt> <dd>The promotion''s visit count limit.</dd> <dt>int `i_remain`</dt> <dd>The remaining count of visits.</dd> <dt>int `i_remain_duration`</dt> <dd> Number of minutes left in this promotion. Has sense only for promotions of program type {@link \RsProgramTypeSid::DURATION}. </dd> <dt>int `i_use`</dt> <dd>Count of held uses in the pass.</dd> <dt>int `i_use_duration`</dt> <dd> Number of minutes used for past visits by this promotion. Has sense only for promotions of program type {@link \RsProgramTypeSid::DURATION}. </dd> <dt>int `i_visit_past`</dt> <dd> The count of attended sessions before the last renewal. If `0`, there were no sessions before last renewal or the promotion doesn''t auto-renew. </dd> <dt>int `id_program_type`</dt> <dd>The promotion''s program type ID. One of {@link \RsProgramTypeSid} constants.</dd> <dt>bool `is_last_use`</dt> <dd>If `true`, the promotion has a usage limit and no remaining visits. Otherwise, this will be `false`.</dd> <dt>bool `is_package`</dt> <dd>If `true`, the promotion is a package. Otherwise, this will be `false`.</dd> <dt>bool `is_restrict_multiply`</dt> <dd>If `true`, there''s more than one calendar restriction on the pass. Otherwise, this will be `false`.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key from {@link \RsLoginPromotionSql}.</i></dd> <dt>string `s_remain_measure`</dt> <dd>The formatted remaining duration on the promotion.</dd> <dt>bool `show_remain`</dt> <dd>If `true`, there''s a rollover on the pass with calendar restrictions. Otherwise, this will be `false`.</dd> <dt>string `sid_program_type`</dt> <dd>The promotion''s program type SID.</dd> </dl> </dd> <dt>array `a_resources_not_shared`</dt> <dd> The list of assets reserved individually at the time of booking containing: <dl> <dt>int `i_index`</dt> <dd>The busy resource index.</dd> <dt>string `s_title`</dt> <dd>The resource name.</dd> </dl> </dd> <dt>string `dl_renew`</dt> <dd>The promotion renewal date in local time.</dd> <dt>int `i_visits`</dt> <dd>The total workouts attended.</dd> <dt>string `s_expire`</dt> <dd>The short format of the promotion expiration date.</dd> <dt>string `s_payment`</dt> <dd>The payment method used to book (free, unpaid, single buy, or promotion name).</dd> <dt>string `s_renew`</dt> <dd>The short format of the promotion renewal date.</dd> <dt>bool `show_visits`</dt> <dd>Determines whether to show the client''s total workouts attended on the confirmation screen.</dd> </dl>'
                      html_confirmation:
                        type: string
                        description: 'The confirmation template to be shown in the Self Check-In Web App for the selected user.'
                      k_visit:
                        type: string
                        description: 'The visit key, which was added or checked in.'
      tags:
        - Application-ReceptionSchedule
  '/Wl/Reception/Application/MemberInfo.json':
    get:
      summary: 'Returns member information and notification items for the Self Check-In Web App. Returns the user''s profile data along with any notification items (such as expiring memberships, required waivers, or outstanding balances) that should be displayed during the self check-in flow.'
      parameters:
        - name: a_uid
          in: query
          description: 'Primary keys of users whose information must be returned. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: a_uid_date
          in: query
          description: 'List of dates for load additional information about users. Key is UID of user. Value is date. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'Date of the session, if we show it on the appointment info window or on the attendance list.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: '`true` - if API is being used from backend, `false` - otherwise.'
          schema:
            type: boolean
        - name: is_full
          in: query
          description: 'If you need to return additional information set to `true` or `false` if not.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business ID required to access client information. Specify this as `0` to retrieve the system-wide version of the information.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location where the Self Check-In Web App is running.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'ID of the visit, if we show icons on the attendance list and information that depends on visit is required.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Key of the Check In application. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
        - name: s_show
          in: query
          description: 'A list of icons with additional information about the business member. If empty, all available icons will be displayed. Comma separated values from {@link MemberInfoSid::idSid()} method.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to retrieve member information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: [array, "null"]
                        description: 'Additional member data or `null` if any data can be shown. Will be filled if {@link InfoApi::$is_full} flag is set. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl>'
                      a_items:
                        type: array
                        description: 'The options presented in the web app. The items are generated this way due to JavaScript limitations around translated messages. <dl> <dt>int `id_color`</dt> <dd> ID of color the notice is displayed in. One of {@link \Wl\Reception\Application\MemberInfoColorSid} constants. </dd> <dt>int `id_icon`</dt> <dd> ID of icon that accompanies the message. One of {@link \Wl\Reception\Application\MemberInfoIconSid} constants. </dd> <dt>string `text_class`</dt> <dd>Name of the class for styles.</dd> <dt>string `text_message`</dt> <dd>Plain message for info box.</dd> <dt>string `html_message`</dt> <dd>Message for info box.</dd> </dl>'
                      a_result_list:
                        type: [array, "null"]
                        description: 'List of users data. Keys - users primary keys. Values - users data. <i>See for {@link InfoApi::_get()} details.</i> Keys refer to clients'' primary keys, and values refer to clients'' data. If `null`, data for a single client is being requested. <dl> <dt>array `a_info`</dt> <dd> Additional user''s information. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>bool `is_traveller`</dt> <dd>`true` - user is traveller; `false` - not traveller.</dd> <dt>string `s_member`</dt> <dd>User''s member number.</dd> <dt>string `text_first_name`</dt> <dd>User''s first name.</dd> <dt>string `text_fullname`</dt> <dd>User''s name.</dd> <dt>string `url_barcode`</dt> <dd>Link to barcode image to scan member number.</dd> </dl>'
                      a_visit_last:
                        type: array
                        description: 'Information about last visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      a_visit_next:
                        type: array
                        description: 'Information about next visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      i_lifetime_visit:
                        type: number
                        format: int32
                        description: 'Count attend visits for one client.'
                      is_traveller:
                        type: [boolean, "null"]
                        description: 'If `true`, the client is a traveler. Otherwise, this will be `false`. This will be `null` in cases when the client is a walk-in. Or when {@link MemberInfoApi::$is_full} is `false`.'
                      m_lifetime_value:
                        type: string
                        description: 'The member''s lifetime value.'
                      s_member:
                        type: [string, "null"]
                        description: 'The member ID. If `null`, the specified client isn''t a member of the specified business.'
                      text_first_name:
                        type: string
                        description: 'First user''s name.'
                      text_fullname:
                        type: string
                        description: 'Full user''s name.'
                      url_barcode:
                        type: string
                        description: 'URL to barcode image.'
                      url_email:
                        type: string
                        description: 'URL to email.'
      tags:
        - Application-MemberInfo
  '/Wl/Reception/Application/ReceptionAuthorize.json':
    post:
      summary: 'Performs authorization based on the given authorization value and business settings. Looks up clients by member ID, email address, or phone number and returns matching user records for selection in the Self Check-In Web App.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where Check In application is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location, where Check In application is started.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Key of the Check In application. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_authorize:
                  type: string
                  description: 'Authorization value - it''s a value, which client entered on authorization form. It can be client ID, email or phone number. Depends on the business settings.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_select:
                        type: array
                        description: 'List of the users, which can be authorized. Can be set only this field or {@link ReceptionAuthorizeApi::$uid}. It depends, whether we found one user or multiple. <dl> <dt>string `text_name_first`</dt> <dd>First name of the user.</dd> <dt>string `text_name_last`</dt> <dd>Last name of the user.</dd> <dt>string `uid`</dt> <dd>Key of the user.</dd> <dt>string `url_image`</dt> <dd>Link to the profile image.</dd> </dl>'
                      uid:
                        type: string
                        description: 'Key of the authorized user. Can be set only this field or {@link ReceptionAuthorizeApi::$a_select}. It depends, whether we found one user or multiple.'
      tags:
        - Application-ReceptionAuthorize
  '/Wl/Reception/Design/ReceptionDesign.json':
    get:
      summary: 'Returns information about settings for Check In Web Application. This method does not require any access checks, because this is public information.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where application is run.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location, where application is run.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reception_logo:
                        type: array
                        description: 'Array of image information for Self Check-In logo. Empty if {@link \Wl\Reception\Design\ReceptionDesignApi::$k_location} is not provided. Will contain the following keys: If image for self check in app is empty, will attempt to obtain business logo instead. If business logo is empty, will attempt to obtain location logo. <dl> <dt>string `url_view`</dt> <dd>url for full image.</dd> <dt>string `url_thumbnail`</dt> <dd>url for thumbnail version of image.</dd> </dl>'
                      hide_profile_images:
                        type: boolean
                        description: '`true` - client profile images will be hidden after the client signs in on the Check-in Kiosk; `false` - otherwise.'
                      i_book_open:
                        type: number
                        format: int32
                        description: 'Number of minutes for the client check-in window after session has started.'
                      i_confirm_delay:
                        type: number
                        format: int32
                        description: 'Delay in seconds on Check-in Confirmation Screen before redirect to Login screen.'
                      i_schedule_delay:
                        type: number
                        format: int32
                        description: 'Delay in seconds on Schedule Screen before redirect to Login screen.'
                      id_failed_sound:
                        type: number
                        format: int32
                        description: 'ID of the sound for failed check in.'
                      id_success_sound:
                        type: number
                        format: int32
                        description: 'ID of the sound for successful check in.'
                      is_attend_free:
                        type: boolean
                        description: '`true` - allow client to check-in unpaid; `false` - otherwise.'
                      is_auto_check_in:
                        type: boolean
                        description: 'If only one service available with the look ahead window the client will: `true` - automatically check in to it; `false` - have to select the service.'
                      is_book_open:
                        type: boolean
                        description: '`true` - allow clients to check-in to sessions in progress; `false` - otherwise.'
                      is_book_optional:
                        type: boolean
                        description: '`true` - allow client check-in without booking prior; `false` - otherwise.'
                      is_client_id:
                        type: boolean
                        description: '`true` - enable check-in by client id; `false` - otherwise.'
                      is_mail:
                        type: boolean
                        description: '`true` - enable check-in by email; `false` - otherwise.'
                      is_password:
                        type: boolean
                        description: '`true` - require password while check-in; `false` - otherwise.'
                      is_phone:
                        type: boolean
                        description: '`true` - enable check-in by phone; `false` - otherwise.'
                      is_reconcile_auto:
                        type: boolean
                        description: '`true` - automatically reconcile unpaid visits upon check in; `false` - otherwise.'
                      show_business_name:
                        type: boolean
                        description: '`true` - show business name on client self check-in page; `false` - otherwise.'
                      show_confirm_screen:
                        type: boolean
                        description: '`true` - show confirm screen after client self check-in; `false` - otherwise.'
                      text_business_name:
                        type: string
                        description: 'Name of the business to display on the client self check-in page.'
      tags:
        - Design-ReceptionDesign
  '/Wl/Reception/Roster/AttendanceListAttend.json':
    post:
      summary: 'Books a class for the client and marks the visit as attended via the Attendance Kiosk. Validates the Attendance Kiosk secret, books the specified class period for the client, and immediately marks the resulting visit as attended.'
      parameters:
        - name: dtu_date
          in: query
          description: 'The UTC datetime for the class in MySQL format.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'The secret for authenticating the attendance kiosk.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The client''s user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_visit:
                        type: string
                        description: 'The visit key.'
                      text_message:
                        type: string
                        description: 'The status message displayed on a successful check-in.'
      tags:
        - Roster-AttendanceListAttend
  '/Wl/Reception/Roster/AttendanceListBook.json':
    post:
      summary: 'Books a class for the client via the Attendance Kiosk and returns whether the client was placed on the class list or waitlist. Validates the Attendance Kiosk secret and books the client into the specified class period, returning a flag indicating whether the client was placed on the class list or the waitlist.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dtu_date:
                  type: string
                  description: 'The UTC datetime for the class in MySQL format.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_class_period:
                  type: string
                  description: 'The class period key.'
                s_secret:
                  type: string
                  description: 'The secret for authenticating the attendance kiosk.'
                uid:
                  type: string
                  description: 'The client''s user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_wait:
                        type: boolean
                        description: '`true` if the client is on the waitlist for the class, `false` if they are on the class list.'
                      text_message:
                        type: string
                        description: 'The status message displayed on a successful booking. Describes whether client was put in class list or waitlist.'
      tags:
        - Roster-AttendanceListBook
  '/Wl/Reception/Roster/AttendanceListNotAttend.json':
    post:
      summary: 'Marks the visit as not attended via the Attendance Kiosk. Validates the Attendance Kiosk secret and updates the specified visit record to reflect that the client did not attend the session.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The business key.'
                k_visit:
                  type: string
                  description: 'The visit key.'
                s_secret:
                  type: string
                  description: 'The secret for authenticating the attendance kiosk.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Roster-AttendanceListNotAttend
  '/Wl/Reception/Roster/AttendanceSecret.json':
    get:
      summary: 'Gets the secret string for Attendance Kiosk requests. Requires backend access and validates that the specified location belongs to the given business, then returns the secret token used to authenticate Attendance Kiosk API requests.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_secret:
                        type: string
                        description: 'Secret string.'
      tags:
        - Roster-AttendanceSecret
  '/Wl/Review/ReviewList/ReviewList.json':
    get:
      summary: 'Retrieves a list of reviews. Returns reviews for the specified location, or all reviews for the business if no location is given, with support for filtering by user, ordering, and pagination.'
      parameters:
        - name: i_page
          in: query
          description: 'If not specified, this request will return all review keys. If specified, this request will return detailed reviews (10 per page). This will be `null` if you only need to load the keys of the review.'
          schema:
            type: [number, "null"]
        - name: id_order
          in: query
          description: 'The order in which the review should be arranged. One of the {@link \Wl\Review\ReviewList\ReviewOrderSid} constants. If not passed use default order {@link \Wl\Review\ReviewList\ReviewOrderSid::LATEST}.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. If not specified, location key needs to be specified.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to show reviews for. If not specified, business key should be specified.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key. WellnessLiving allows staff to check low-rated reviews before posting them. Staff members can see all reviews. Clients can only see checked reviews.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_review:
                        type: array
                        description: 'List of reviews. If passed {@link \Wl\Review\ReviewList\ReviewListApi::$i_page} then the result will be full, otherwise in result will be keys: <tt>k_review</tt>, <tt>uid</tt>. <dl> <dt>bool `can_reply`</dt> <dd><tt>true</tt> if can reply to review, <tt>false</tt> otherwise.</dd> <dt>string `dl_reply`</dt> <dd>Date when staff reply to review. Can be empty string if no one replied.</dd> <dt>string `dt_add`</dt> <dd>Date when review added.</dd> <dt>float `f_rate`</dt> <dd>Rate of review.</dd> <dt>bool `is_featured`</dt> <dd><tt>true</tt> if review is featured, <tt>false</tt> otherwise.</dd> <dt>bool `is_verify`</dt> <dd><tt>true</tt> if review is verify, <tt>false</tt> otherwise.</dd> <dt>string `k_location`</dt> <dd>The location key for where the review was left/assigned. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_review`</dt> <dd>Review key. <i>Primary key in {@link \RsReviewSql} table.</i></dd> <dt>string `s_firstname`</dt> <dd>First name of user who wrote review.</dd> <dt>string `s_lastname`</dt> <dd>Last name of user who wrote review.</dd> <dt>string `s_reply`</dt> <dd>Reply for review.</dd> <dt>string `s_text`</dt> <dd>Review text.</dd> <dt>string `text_city`</dt> <dd>City from the profile of the user, who left review.</dd> <dt>string `text_reply_first`</dt> <dd>First name of staff who replied of review. Can be empty string if no one replied.</dd> <dt>string `text_reply_last`</dt> <dd>Last name of staff who replied of review. Can be empty string if no one replied.</dd> <dt>string `text_role`</dt> <dd>Staff role who replied of review. Can be empty string if no one replied.</dd> <dt>string `uid`</dt> <dd>User key who wrote review.</dd> <dt>string `url_image`</dt> <dd>Link to the image of the user who wrote review.</dd> <dt>string `url_reply_image`</dt> <dd>Link to the image of the user who replied of review. Can be empty string if no one replied.</dd> </dl>'
      tags:
        - ReviewList-ReviewList
  '/Wl/Review/ReviewList/ReviewElement.json':
    delete:
      summary: 'Deletes a review. Checks that the caller has the required permission, removes the review, and recomputes the best reviews for the associated business location.'
      parameters:
        - name: k_review
          in: query
          description: 'The review key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ReviewList-ReviewElement
    get:
      summary: 'Retrieves information about review item. Returns the review rating, text, date, author information, reply, and featured status for the specified review.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to which the review belongs.'
          schema:
            type: string
        - name: k_review
          in: query
          description: 'The review key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. May be empty in the case of a guest.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_review:
                        type: array
                        description: 'Review data: <dl> <dt>string `dt_add`</dt> <dd>Date when review was added by user.</dd> <dt>float `f_rate`</dt> <dd>Review rate.</dd> <dt>int `id_review_status`</dt> <dd>Status of the Review. One of {@link \RsReviewStatusSid} constants.</dd> <dt>bool `is_featured`</dt> <dd>If `true`, the review is featured. Otherwise, this will be `false`.</dd> <dt>bool `is_verify`</dt> <dd><tt>true</tt> if review is verified, <tt>false</tt> otherwise.</dd> <dt>string `text_firstname`</dt> <dd>Client''s first name who added review.</dd> <dt>string `text_lastname`</dt> <dd>Client''s last name who added review.</dd> <dt>string `text_reply`</dt> <dd>Staff reply. Can be empty.</dd> <dt>string `text_review`</dt> <dd>Review text.</dd> <dt>string `text_business_name`</dt> <dd>Business name</dd> <dt>string `text_response`</dt> <dd>Word "response".</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url_logo`</dt> <dd>User logo.</dd> </dl>'
      tags:
        - ReviewList-ReviewElement
  '/Wl/Service/ServiceList/List75.json':
    get:
      summary: 'Returns list of appointment type in the business. Gets key of the business and returns all available appointment types with their names and categories.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created appointment types (if business is franchisor). <tt>true</tt> to include franchisee-created appointment types.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key, primary key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'Appointment types list: <dl> <dt>bool `hide_application`</dt> <dd> Whether service will be hidden in the White Label mobile application. <tt>true</tt> means that service will not be displayed, <tt>false</tt> otherwise. </dd> <dt>bool `is_active`</dt> <dd>Whether appointment type is active.</dd> <dt>string `k_service`</dt> <dd>Appointment type identifier, primary key.</dd> <dt>string `k_service_category`</dt> <dd>Appointment type category identifier, primary key.</dd> <dt>string `text_category`</dt> <dd>Appointment type category name.</dd> <dt>string `text_service`</dt> <dd>Appointment type name.</dd> </dl>'
      tags:
        - ServiceList-List75
  '/Wl/Service/ServiceList/List.json':
    get:
      summary: 'Returns list of appointment type in the business. Gets key of the business and returns all available appointment types with their names and categories.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created appointment types (if business is franchisor). <tt>true</tt> to include franchisee-created appointment types.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key, primary key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'Appointment types list: <dl> <dt>bool `hide_application`</dt> <dd> Whether service will be hidden in the White Label mobile application. <tt>true</tt> means that service will not be displayed, <tt>false</tt> otherwise. </dd> <dt>string `k_service`</dt> <dd>Appointment type identifier, primary key.</dd> <dt>string `k_service_category`</dt> <dd>Appointment type category identifier, primary key.</dd> <dt>string `text_category`</dt> <dd>Appointment type category name.</dd> <dt>string `text_service`</dt> <dd>Appointment type name.</dd> </dl>'
      tags:
        - ServiceList-List
  '/Wl/Staff/StaffView/StaffView74.json':
    get:
      summary: 'Retrieves information about staff. This method can accept or one staff key {@link StaffViewApi::$k_staff} or staff list {@link StaffViewApi::$a_staff_list} but not both (exception would be thrown).'
      parameters:
        - name: a_uid_staff_list
          in: query
          description: 'A list of staff user IDs.'
          schema:
            type: [array, "null"]
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff member user ID. A staff member can work for more than one business. This key can be found using the {@link StaffListApi} endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_day:
                        type: array
                        description: 'An array containing information about the classes this staff member is running. Each element contains another array with three elements: <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl>'
                      a_result_list:
                        type: array
                        description: 'An array listing the class sessions the staff member provides at each location. <dl> <dt>array[] `a_class_day`</dt> <dd> Contains a schedule of classes per day. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_class_day} property. <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff information. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_staff} property. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: array
                        description: 'An array containing information about the staff member. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl>'
      tags:
        - StaffView-StaffView74
  '/Wl/Staff/StaffView/StaffView.json':
    get:
      summary: 'Retrieves information about staff. This method can accept or one staff key {@link StaffViewApi::$k_staff} or staff list {@link StaffViewApi::$a_staff_list} but not both (exception would be thrown).'
      parameters:
        - name: a_staff_list
          in: query
          description: 'A list of staff keys.'
          schema:
            type: [array, "null"]
        - name: a_uid_staff_list
          in: query
          description: 'A list of staff user IDs.'
          schema:
            type: [array, "null"]
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'The staff member key. A staff member can work for more than one business. This key can be found using the {@link StaffListApi} endpoint.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff member user ID. A staff member can work for more than one business. This key can be found using the {@link StaffListApi} endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_day:
                        type: array
                        description: 'An array containing information about the classes this staff member is running. Each element contains another array with three elements: <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl>'
                      a_result_list:
                        type: array
                        description: 'An array listing the class sessions the staff member provides at each location. <dl> <dt>array[] `a_class_day`</dt> <dd> Contains a schedule of classes per day. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_class_day} property. <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff information. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_staff} property. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: array
                        description: 'An array containing information about the staff member. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl>'
      tags:
        - StaffView-StaffView
  '/Wl/Staff/Privilege/PrivilegeList.json':
    get:
      summary: 'Returns the list of privileges for the current user in the given business. Returns the complete access profile of the signed-in user for the given business: staff privileges by ID, named admin privileges, and a flag indicating super-admin status. Can only be called for the currently authenticated user, not on behalf of another user.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to get privileges for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to get privileges for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_privilege_passport:
                        type: object
                        description: 'List of privileges, if user is administrator. There is no certain list of admin privileges, as they are name-based. If you need to check access to certain place contact WellnessLiving integration team.'
                      a_privilege_staff:
                        type: array
                        description: 'List of privileges, if the given user is a staff member in the give business.'
                      is_admin:
                        type: boolean
                        description: 'Whether this user is a super-administrator because he is a studio staff member. `true`, if this user is a super administrator because he is a studio staff member. Super administrators have all privileges. `false` if this is an ordinary user.'
      tags:
        - Privilege-PrivilegeList
  '/Wl/Staff/StaffList/StaffList.json':
    get:
      summary: 'Returns the list of staff members for the given business. Returns all active (or optionally inactive) staff members for the business, including their name, role, assigned services, contact details, and location assignments. Can be filtered to only staff who have a specific privilege, and whether access-level checks should be applied when building the result.'
      parameters:
        - name: a_privilege
          in: query
          description: 'A list of privileges to filter staff members by. This property acts as a filter. Only staff members possessing all the privileges specified in this list will be included in the result. Each element in the array represents a privilege ID, and should be one of {@link PrivilegeSid} constants.'
          schema:
            type: array
        - name: is_check_staff_access
          in: query
          description: 'Determines that only staff members which the current user has access to should be retrieved. `true` if only staff members which the current user has access to should be retrieved. `false` otherwise.'
          schema:
            type: boolean
        - name: is_staff_inactive
          in: query
          description: 'Whether inactive and removed staff members are available. If `true` inactive and removed staff members are available. `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'Information about staff members. Each array index is the staff member keys. Each array element is an array containing the following fields: <dl> <dt>string[] `a_pay_rate`</dt> <dd>A list of internal pay rate keys applicable to the staff member.</dd> <dt>array `a_staff_service`</dt> <dd> A list of all services, provided by the staff member. Each element is an array with structure: <dl> <dt>string `k_service`</dt> <dd>Service key. <i>Primary in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff_pay`</dt> <dd>Staff pay key for this service. <i>Primary in {@link \RsStaffPaySql} table.</i></dd> </dl> </dd> <dt>string `html_name`</dt> <dd>Staff name.</dd> <dt>int `i_order`</dt> <dd>Order for sorting.</dd> <dt>bool `is_appointment`</dt> <dd>Whether this staff provides appointments service.</dd> <dt>bool `is_class`</dt> <dd>Whether this staff provides class service.</dd> <dt>bool `is_event`</dt> <dd>Whether this staff provides events service.</dd> <dt>string `k_staff`</dt> <dd> The staff key. <i>Primary key in {@link \RsStaffSql} table.</i> deprecated Use `uid_staff` instead. Returned only for backward-compatible applications. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `s_surname`</dt> <dd> The first letter of the staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string|null `s_surname_full`</dt> <dd> The entire surname of the staff member. This will be `null` if private staff member information isn''t accessible. </dd> <dt>string `text_name_full`</dt> <dd>The full name of the staff member.</dd> <dt>string `uid`</dt> <dd> The user key. Each staff member in WellnessLiving can also access the system as a client of their business. This is the key used to represent the staff member as a client. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> <dt>string `uid_staff`</dt> <dd>The staff member user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_image`</dt> <dd>Url link to user photo, or empty string, if photo is not loaded.</dd> </dl>'
      tags:
        - StaffList-StaffList
  '/Wl/Sms/Phone/BusinessPhone.json':
    get:
      summary: 'Gets the phone number associated with a specific business. Returns the dedicated sender phone number configured for the business''s 2-Way SMS feature. Requires the `wl.business.phone` API privilege. Returns `null` or an empty value if the business has not configured a 2-Way SMS number.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_phone_sender:
                        type: [string, "null"]
                        description: 'Phone number of the business, which is added as sender for 2-Way SMS feature. `null` or an empty value if this business does not use this feature.'
      tags:
        - Phone-BusinessPhone
  '/Wl/Location/View/View.json':
    get:
      summary: 'Returns detailed information about the specified location. Returns the full profile of a location, including address, coordinates, timezone, contact information, business hours, logo, slide images, amenities, social media links, and a description.'
      parameters:
        - name: i_logo_height
          in: query
          description: 'Maximum location image height.'
          schema:
            type: number
            format: int32
        - name: i_logo_width
          in: query
          description: 'Maximum location image width.'
          schema:
            type: number
            format: int32
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_age:
                        type: array
                        description: 'A list of ages that are permitted for visiting this location.'
                      a_amenities:
                        type: array
                        description: 'A list of facilities that are available in this location.'
                      a_level:
                        type: array
                        description: 'A list of levels that are suitable for visiting this location.'
                      a_logo:
                        type: array
                        description: 'Information about the location logo used in WellnessLiving: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl>'
                      a_slide:
                        type: array
                        description: 'A list of the location images. Every element has the following keys: <dl> <dt>int `i_height`</dt> <dd>Height.</dd> <dt>int `i_width`</dt> <dd>Width.</dd> <dt>string `url_preview`</dt> <dd>URL to image preview.</dd> <dt>string `url_slide`</dt> <dd>URL to full image.</dd> </dl>'
                      a_work:
                        type: array
                        description: 'The hours of operation for the location. Fields are numbers representing specific days (1 is Monday, 7 is Sunday). Values are objects with the next fields: <dl> <dt>string `s_end`</dt> <dd>The time of day end in MySQL format.</dd> <dt>string `s_start`</dt> <dd>The time of day start in MySQL format.</dd> </dl>'
                      f_latitude:
                        type: number
                        format: float
                        description: 'The latitude coordinate of the location.'
                      f_longitude:
                        type: number
                        format: float
                        description: 'The longitude coordinate of the location.'
                      html_description_full:
                        type: string
                        description: 'The full description of the location.'
                      html_description_preview:
                        type: string
                        description: 'A shorter description of the location. A preview of {@link ViewApi::$html_description_full}.'
                      id_industry:
                        type: [number, "null"]
                        description: 'The industry of the business. `null` in case when industry didn''t set for chosen business type, or for business types where set value which is absent.'
                      is_phone:
                        type: boolean
                        description: '`true` if to display phone number on location page. `false` otherwise.'
                      is_top_choice:
                        type: boolean
                        description: '`true` if WellnessLiving identifies this is a top choice location, `false` otherwise.'
                      k_business:
                        type: string
                        description: 'The key of the business this location belongs to.'
                      k_business_type:
                        type: string
                        description: 'The key of the business type this location belongs to.'
                      k_timezone:
                        type: string
                        description: 'The timezone.'
                      s_address:
                        type: string
                        description: 'The physical address of the location.'
                      s_map:
                        type: string
                        description: 'A string that can be used in navigator programs.'
                      s_phone:
                        type: string
                        description: 'The phone number for the location.'
                      s_timezone:
                        type: string
                        description: 'The system name of the time zone.'
                      s_title:
                        type: string
                        description: 'The title of the location.'
                      text_address_individual:
                        type: string
                        description: 'The address of the location.'
                      text_alias:
                        type: string
                        description: 'The special system name, which can be used in custom location links. This is usually based on name of the location, with extra characters removed. However, it can also can be fully customized.'
                      text_business_type:
                        type: string
                        description: 'The name of the business type.'
                      text_city:
                        type: string
                        description: 'The city name of the location.'
                      text_country:
                        type: string
                        description: 'The country name of the location.'
                      text_industry:
                        type: string
                        description: 'The name of the industry of the business.'
                      text_mail:
                        type: string
                        description: 'The email address.'
                      text_postal:
                        type: string
                        description: 'Postal code of the location.'
                      text_region:
                        type: string
                        description: 'The region name of the location.'
                      text_region_code:
                        type: string
                        description: 'Region 2 or 3 letters abbreviation of the location. Can be empty, if abbreviation for region is not set.'
                      url_facebook:
                        type: string
                        description: 'The Facebook URL of the location.'
                      url_instagram:
                        type: string
                        description: 'The Instagram URL of the location.'
                      url_linkedin:
                        type: string
                        description: 'The Linked In URL of the location.'
                      url_map:
                        type: string
                        description: 'The URL of the location in Google Maps.'
                      url_microsite:
                        type: string
                        description: 'The URL to the location entry in the WellnessLiving Explorer listing.'
                      url_site:
                        type: string
                        description: 'The website URL of the location.'
                      url_twitter:
                        type: string
                        description: 'The URL of the location''s Twitter account.'
                      url_web:
                        type: string
                        description: 'The URL to the business''s Client Web App.'
                      url_youtube:
                        type: string
                        description: 'The YouTube URL of the location.'
      tags:
        - View-View
  '/Wl/Location/LocationRate/LocationRate.json':
    get:
      summary: 'Retrieves information about location rate. Returns the average rating and review count for the location, along with whether rating is enabled for the business type the location belongs to.'
      parameters:
        - name: k_location
          in: query
          description: 'ID of a location to show rate for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_rate:
                        type: number
                        format: float
                        description: 'Location rate.'
                      i_review:
                        type: number
                        format: int32
                        description: 'Review count.'
                      is_rate:
                        type: boolean
                        description: 'Determines that the rate type exists in the current business type.'
      tags:
        - LocationRate-LocationRate
  '/Wl/Location/Facility/FacilitySid.json':
    get:
      summary: 'Returns the list of all items for the given Sid class. Populates enumeration dropdowns and lookup tables on the frontend. <i> Given any class that extends {@link CoreSid}, returns every defined constant with its numeric ID, string identifier, and human-readable title so the client does not need to hard-code enum values. </i>'
      parameters:
        - name: s_class_name
          in: query
          description: 'Name of the Sid class to get list from. Should be a fully qualified class name. <i>Class should be a child of {@link CoreSid}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of items. Keys are IDs, values are arrays with additional information: <dl> <dt>int `sid`</dt> <dd>String ID.</dd> <dt>string `text_title`</dt> <dd>Title of the ID.</dd> </dl>'
      tags:
        - Facility-FacilitySid
  '/Wl/Location/Flag/Flag.json':
    get:
      summary: 'Checks if user {@link FlagApi::$uid} is flagged in location {@link FlagApi::$k_location} or each of users {@link FlagApi::$a_uid} is flagged in location {@link FlagApi::$k_location}. Accepts either a single user key (`uid`) or an array of user keys (`a_uid`) and returns the flag status for each, including whether the flagged user is restricted from booking or purchasing at the location.'
      parameters:
        - name: a_uid
          in: query
          description: 'User keys. `null` if not set.'
          schema:
            type: [array, "null"]
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_flag:
                        type: array
                        description: 'Array with structure: <tt>null</tt> until loaded or when <var>a_uid</var> was not set. <dl> <dt>string `uid`</dt> <dd>Key is passport login ID. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>bool `is_flag`</dt> <dd>Value: <tt>true</tt> if user is flagged; <tt>false</tt> otherwise.</dd> </dl>'
                      a_restrictions_multiple:
                        type: [array, "null"]
                        description: 'Array, where keys are UIDs to be checked and values are same as {@link FlagApi::$a_restrictions_single}. `null` if user is not flagged in the location. This field is set, if API gets {@link FlagApi::$a_uid} and not {@link FlagApi::$uid} properties. Keys are user keys. <i>Primary keys in the {@link \PassportLoginSql} table.</i> Values are flag restriction arrays: <dl> <dt>bool `is_book`</dt> <dd>`true` if the user cannot make new reservations.</dd> <dt>bool `is_purchase`</dt> <dd>`true` if the user cannot make new purchases.</dd> </dl>'
                      a_restrictions_single:
                        type: [array, "null"]
                        description: '<tt>null</tt> if user is not flagged in the location. This field is set, if API gets {@link FlagApi::$uid} and not {@link FlagApi::$a_uid} properties. <dl> <dt>bool `is_book`</dt> <dd>`true` if client cannot make new reservations.</dd> <dt>bool `is_purchase`</dt> <dd>`true` if client cannot make new purchases.</dd> </dl>'
                      is_flag:
                        type: boolean
                        description: '<tt>true</tt> if the user is flagged and can make purchases, but cannot make new reservations, <tt>false</tt> if otherwise. <tt>null</tt> until loaded or when {@link FlagApi::$a_uid} was not set.'
      tags:
        - Flag-Flag
  '/Wl/Location/WorkTime/LocationWorkTime.json':
    post:
      summary: 'Saves working hours of the location. Accepts a list of day-and-time-range entries representing the weekly schedule and replaces all existing working hours for the location. Days not present in the list are treated as non-working days.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_work_time:
                  type: array
                  description: 'The list of the location''s business hours. Each element has the next structure: Note that the days of the week that aren''t represented in this list will be saved as non-working days. <dl> <dt>int `i_day`</dt> <dd>The day of the week, from Monday (1) to Sunday (7). One of the{@link \ADateWeekSid} constants.</dd> <dt>int `t_end`</dt> <dd>The end time of work, formatted as HH:MM.</dd> <dt>int `t_start`</dt> <dd>The start time of work, formatted as HH:MM</dd> </dl>'
                k_business:
                  type: string
                  description: 'The business key.'
                k_location:
                  type: string
                  description: 'The location key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - WorkTime-LocationWorkTime
  '/Wl/Resource/ResourceList/List.json':
    get:
      summary: 'Returns assets list in the business. Returns all assets if <tt>$id_category</tt> is not specified or only certain category assets. Includes main information about assets.'
      parameters:
        - name: id_category
          in: query
          description: 'Type of the resource. 1 - if this is a bookable asset. 2 - if this is an off-site location with its own address.'
          schema:
            type: number
            format: int32
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created resources (if business is franchisor). <tt>true</tt> to include franchisee-created resources.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource:
                        type: array
                        description: 'Resources list: <dl> <dt>bool `hide_application`</dt> <dd> Whether resource will be hidden in the White Label mobile application. <tt>true</tt> means that resource will not be displayed, <tt>false</tt> otherwise. </dd> <dt>bool `is_active`</dt> <dd>Whether resource is active.</dd> <dt>bool `is_book`</dt> <dd>Whether resource is bookable.</dd> <dt>string `k_city`</dt> <dd>Key of the city for off-site locations.}. <tt>null</tt> if the resource is not an off-site location.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `k_resource_layout`</dt> <dd>Asset layout key.</dd> <dt>string `k_resource_type`</dt> <dd>Resource type key.</dd> <dt>string `text_address`</dt> <dd> Street address of the resource for off-site locations. Empty string if the resource does not have an off-site address. </dd> <dt>string `text_city`</dt> <dd> Name of the city with region and country for off-site locations. Empty string if the resource does not have an off-site address. </dd> <dt>string `text_guide`</dt> <dd>Additional directions or access tips for reaching an off-site location. Empty string if not specified.</dd> <dt>string `text_postal`</dt> <dd> Postal code of the resource for off-site locations. Empty string if the resource does not have an off-site address. </dd> <dt>string `text_title`</dt> <dd>Resource name.</dd> </dl>'
      tags:
        - ResourceList-List
  '/Wl/Resource/Layout/Layout.json':
    get:
      summary: 'Returns information about the specified asset layout, including assets and custom shapes. Returns the full layout configuration including the asset list with positions and images, custom shapes with coordinates and colors, and display settings such as grid dimensions and number visibility.'
      parameters:
        - name: k_resource_layout
          in: query
          description: 'The key of the layout.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource:
                        type: array
                        description: 'The list of assets. Every element contains the following keys: <dl> <dt>array `a_image`</dt> <dd> The asset''s appearance information. <i>See {@link \RsResourceImage::data()} for details.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See more {@link \Core\Drive\DriveImage::image()}.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Is set only if `sid_image` equals to `shape`.</dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `sid_image`</dt> <dd>Image kind. String representation of one of {@link \Wl\Resource\Image\ImageSid} constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. String representation of one of {@link \Wl\Resource\Image\ImageIconSid} constants. Is set only if `sid_image` equals to `image`. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. String representation of one of {@link \Wl\Resource\Image\ImageShapeSid} constants. Is set only if `sid_image` equals to `shape`. </dd> <dt>string `url`</dt> <dd>Path to image. <i>See {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> </dl> </dd> <dt>int `i_cell_x`</dt> <dd>The vertical cell number. Not empty if assets are snapped to grid.</dd> <dt>int `i_cell_y`</dt> <dd>The horizontal cell number. Not empty if assets are snapped to grid.</dd> <dt>int `i_left`</dt> <dd>The horizontal offset in pixels. Not empty if assets aren''t snapped to grid.</dd> <dt>int `i_top`</dt> <dd>The vertical offset in pixels. Not empty if assets aren''t snapped to grid.</dd> <dt>int `i_index`</dt> <dd>Asset number.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `s_name`</dt> <dd>Asset name.</dd> <dt>string `s_title`</dt> <dd>The asset title.</dd> </dl>'
                      a_shape_custom:
                        type: array
                        description: 'A list of custom shapes. Every element is an array with the following keys: <dl> <dt>float `f_height`</dt> <dd>The height for the shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::RECTANGLE}. Empty for other shapes.</dd> <dt>float `f_width`</dt> <dd>The width for the shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::RECTANGLE}. Empty for other shapes.</dd> <dt>int `i_degree_from`</dt> <dd>The start angle for the shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::PIE}. Empty for other shapes.</dd> <dt>int `i_degree_to`</dt> <dd>The start angle for shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::PIE}. Empty for other shapes.</dd> <dt>int `i_left`</dt> <dd>The position of the shape by horizontal axis.</dd> <dt>int `i_radius`</dt> <dd> The radius for shapes {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::PIE} and {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::CIRCLE}. Empty for other shapes. </dd> <dt>int `i_top`</dt> <dd>The position of the shape by vertical axis.</dd> <dt>int `id_resource_layout_shape`</dt> <dd>The shape type ID. One of the {@link \Wl\Resource\Layout\Shape\LayoutShapeSid} constants.</dd> <dt>string `k_resource_layout_shape`</dt> <dd>The shape key. <i>Primary key in {@link \Wl\Resource\Layout\Shape\Sql} table.</i></dd> <dt>string `s_color_background`</dt> <dd>The shape''s background color.</dd> <dt>string `s_color_foreground`</dt> <dd>The shape''s foreground color.</dd> <dt>string `s_text`</dt> <dd>The shape''s title.</dd> </dl>'
                      a_shape_icon:
                        type: array
                        description: 'A list of shapes and icons. Every element is an array with the following keys: <dl> <dt>int `i_cell_x`</dt> <dd>The cell number, positioned by the horizontal axis. Empty if grid is turned off.</dd> <dt>int `i_cell_y`</dt> <dd>The cell number, positioned by the vertical axis. Empty if grid is turned off.</dd> <dt>int `i_left`</dt> <dd>The horizontal position in pixels. Empty if grid is turned on.</dd> <dt>int `i_top`</dt> <dd>The vertical position in pixels. Empty if grid is turned on.</dd> <dt>int `id_shape_icon`</dt> <dd>The icon ID. One of the {@link \Wl\Resource\Image\ImageIconSid} constants.</dd> </dl>'
                      i_grid:
                        type: number
                        format: int32
                        description: 'The grid size.'
                      is_grid:
                        type: boolean
                        description: 'This will be `true` if snap to grid is enabled. Otherwise, this will be `false`.'
                      k_resource_type:
                        type: string
                        description: 'The key of the asset category.'
                      s_color_active:
                        type: string
                        description: 'The color for active assets. Hex encoding with prefix `#`.'
                      show_name:
                        type: boolean
                        description: 'This will be `true` if asset names are displayed. Otherwise, this will be `false`.'
                      show_number:
                        type: boolean
                        description: 'This will be `true` if asset numbers are displayed. Otherwise, this will be `false`.'
      tags:
        - Layout-Layout
  '/Wl/Skin/Application/Skin.json':
    delete:
      summary: 'Reset customisation form of client application. Resets the Achieve app customization skin to default values for the given business. If billing is required for the reset, a confirmation flag must be provided, and a customization fee may be scheduled.'
      parameters:
        - name: is_billing_confirm
          in: query
          description: 'Whether customization billing is confirmed. <tt>true</tt> to finish delete process even if customization fee will be billed. <tt>false</tt> if customization fee should be required for confirmation.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_link:
                        type: string
                        description: 'Name of the link to default application skin.'
      tags:
        - Application-Skin
    get:
      summary: 'Returns the application customisation skin data for the given business. Returns the full set of White Label Achieve app customization settings (colors, logos, fonts, and other branding options) for the given business, along with a flag indicating whether the business account has an active paid customization subscription.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_skin:
                        type: array
                        description: 'Skin information.'
                      has_pay:
                        type: boolean
                        description: 'Determines current business account contains amount for the application customisation.'
                      s_link:
                        type: string
                        description: 'Name of the link to default application skin.'
      tags:
        - Application-Skin
  '/Wl/Skin/Widget/SkinWidgetList.json':
    get:
      summary: 'Returns widget skins grouped by widget type for the given business. Returns all configured widget skins for the business, organized by widget type. Used to populate the widget management list so administrators can select and edit existing skins.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_widget_skin:
                        type: array
                        description: 'List of Widget skins grouped by widget type. Skin type, one of {@link \RsSkinSid} constants. The array structure: Keys - Skin type, one of {@link \RsSkinSid} constants. Values - arrays with next keys: <dl> <dt>string `k_skin`</dt> <dd>Skin key. <i>Primary key from {@link \RsSkinSql} table.</i></dd> <dt>string `text_widget_name`</dt> <dd>Widget name.</dd> </dl>'
      tags:
        - Widget-SkinWidgetList
  '/Wl/User/Info/UserIntegration.json':
    get:
      summary: 'Retrieves information about the user belongs to certain integrations. Returns which third-party booking integrations (Classpass, Gympass, Reserve With Google) the user is currently connected to within the given business context. Returns `null` when the user has no active integration memberships.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_integration:
                        type: [array, "null"]
                        description: 'Information about the integrations the user is connected to. The information returned has the following structure: `null` if the user does not belong to any integration. <dl> <dt>bool `is_classpass`</dt> <dd>`true` if user use the Classpass integration, `false` - otherwise.</dd> <dt>bool `is_gympass`</dt> <dd>`true` if user use the Gympass integration, `false` - otherwise.</dd> <dt>bool `is_reserve_with_google`</dt> <dd>`true` if user use the Reserve With Google integration, `false` - otherwise.</dd> </dl>'
      tags:
        - Info-UserIntegration
  '/Wl/User/Info/UserInfo.json':
    get:
      summary: 'Retrieves information about user. Returns profile data for a WellnessLiving user, including name, email, phone, photo, gender, login type, custom profile fields, member groups, and calendar integration status. Supports single-user mode and batch mode for loading multiple profiles in one request.'
      parameters:
        - name: a_user_list
          in: query
          description: 'A list of user keys.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The key of the business. This may be empty if system-wide information is needed.'
          schema:
            type: string
        - name: not_cached
          in: query
          description: '`true` to ignore cache and load information from the database directly. `false` - otherwise.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The key of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of the custom user fields. Each value is: <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Name of the field. Will be <tt>null</tt> for general fields. Their titles are static.</dd> <dt>string `text_value`</dt> <dd>Value of the field. This is the key of the selected item for `select` and `radio` elements.</dd> <dt>string `text_value_title`</dt> <dd>Title of the selected option for `select` and `radio` elements. Unused for other elements.</dd> </dl>'
                      a_member_group:
                        type: array
                        description: 'List of member groups that the user belongs to.'
                      a_photo:
                        type: array
                        description: 'Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>string `url_photo`</dt> <dd>The URL of the photo.</dd> </dl>'
                      a_result_list:
                        type: array
                        description: 'List of user''s data. <dl> <dt>array `a_custom_field`</dt> <dd> List of the custom user fields. Each value is: <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Name of the field. Will be <tt>null</tt> for general fields. Their titles are static.</dd> <dt>string `text_value`</dt> <dd>Value of the field.</dd> </dl> </dd> <dt>string[] `a_member_group`</dt> <dd> List of member groups that the user belongs to. <i>Primary keys from {@link \Wl\Member\Group\Sql} table.</i> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>string `url_photo`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>string `dt_add`</dt> <dd>The date the user was added, given in UTC time.</dd> <dt>string `dt_birth`</dt> <dd>The user''s birthday. This will be `null` if the birthday isn''t set yet.</dd> <dt>int `id_gender`</dt> <dd>The ID of the user''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_customer_new`</dt> <dd> This will be `true` if the user has never made purchases or reservations in this business. Otherwise, this will be `false`. </dd> <dt>bool `is_traveller`</dt> <dd> This will be `true` if the user is a traveler. A traveler is someone whose home location isn''t the current location in the Enterprise business. </dd> <dt>string `k_business`</dt> <dd>The key of the business. This may be empty if system-wide information is needed.</dd> <dt>string `k_login_type`</dt> <dd>The key of the login type. The login type describes the user''s client type in this business.</dd> <dt>string `s_first_name`</dt> <dd>The user''s first name.</dd> <dt>string `s_last_name`</dt> <dd>The user''s last name.</dd> <dt>string `s_mail`</dt> <dd>The user''s email address.</dd> <dt>string `s_member`</dt> <dd> The user''s member ID in the business. Also referred to as the client ID in the client''s profile. This value is set by the business and separate from the <var>uid</var> value. </dd> <dt>string `s_phone`</dt> <dd>The user''s phone number.</dd> <dt>string `s_phone_home`</dt> <dd>The user''s home phone number.</dd> <dt>string `s_phone_work`</dt> <dd>The user''s work phone number.</dd> <dt>string `uid`</dt> <dd>The key of the user.</dd> <dt>string `url_photo`</dt> <dd>The URL for the user''s photo.</dd> </dl>'
                      can_introductory:
                        type: boolean
                        description: 'Whether the user can purchase introductory offers. `true` if user can purchase introductory offers, `false` otherwise.'
                      dt_add:
                        type: string
                        description: 'The date the user was added, given in UTC time.'
                      dt_birth:
                        type: string
                        description: 'The user''s birthday. This will be `null` if the birthday isn''t set yet.'
                      has_discount:
                        type: [boolean, "null"]
                        description: 'Whether client''s login type has a discount. `true` - login type has a discount, `false` - otherwise. This will be `null` if a client has no assigned login type.'
                      id_gender:
                        type: number
                        format: int32
                        description: 'The ID of the user''s gender. One of the {@link \AGenderSid} constants. This will be `null` if the gender isn''t set yet.'
                      is_calendar_google:
                        type: boolean
                        description: 'This will be `true` if the user has Google Calendar linked to their account; otherwise, `false`.'
                      is_calendar_microsoft:
                        type: boolean
                        description: 'This will be `true` if the user has Microsoft Calendar linked to their account; otherwise, `false`.'
                      is_customer_new:
                        type: boolean
                        description: 'This will be `true` if the user has never made purchases or reservations in this business. Otherwise, this will be `false`.'
                      is_traveller:
                        type: boolean
                        description: 'This will be `true` if the user is a traveler. A traveler is someone whose home location isn''t the current location in the Enterprise business.'
                      k_city:
                        type: [string, "null"]
                        description: 'City key. `null` if "address" field is disabled in the business.'
                      k_login_type:
                        type: string
                        description: 'The key of the login type. The login type describes the user''s client type in this business.'
                      s_first_name:
                        type: string
                        description: 'The user''s first name.'
                      s_last_name:
                        type: string
                        description: 'The user''s last name.'
                      s_mail:
                        type: string
                        description: 'The user''s email address.'
                      s_member:
                        type: string
                        description: 'The user''s member ID in the business. Also referred to as the client ID in the client''s profile. This value is set by the business and separate from the <var>uid</var> value.'
                      s_phone:
                        type: string
                        description: 'The user''s phone number.'
                      s_phone_home:
                        type: string
                        description: 'The user''s home phone number.'
                      s_phone_work:
                        type: string
                        description: 'The user''s work phone number.'
                      text_address:
                        type: [string, "null"]
                        description: 'Address inside a city. `null` if "address" field is disabled in the business.'
                      text_city:
                        type: [string, "null"]
                        description: 'City name. `null` if "address" field is disabled in the business.'
                      text_login_type:
                        type: string
                        description: 'Login type title. Empty if a client has no client type assigned.'
                      text_postal:
                        type: [string, "null"]
                        description: 'Postal code. `null` if "address" field is disabled in the business.'
                      uid:
                        type: string
                        description: 'The key of the user.'
                      url_photo:
                        type: string
                        description: 'The URL for the user''s photo.'
      tags:
        - Info-UserInfo
  '/Wl/User/Referrer/ReferralInfo.json':
    get:
      summary: 'Returns referral count, total referral points, and shareable referral link for the given user. Computes result fields for the referrer identified: - number of invited referrals; - reward points earned for referral registrations; - the shareable invite link with the referrer''s encrypted user key.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key of the referrer whose statistics are being requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_point:
                        type: number
                        format: int32
                        description: 'Total reward points earned by this user for referral registration ({@link \RsRewardScoreSid::REFER_REGISTER}).'
                      i_referral:
                        type: number
                        format: int32
                        description: 'Number of invited referrals.'
                      url_referral:
                        type: string
                        description: 'Shareable invite link for the referrer. Navigates to the business registration page with the referrer''s encrypted user key as the `s_referral` parameter. When a new client registers through this link, the "Referred By" field is pre-filled with this user.'
      tags:
        - Referrer-ReferralInfo
  '/Wl/User/Referrer/Referrer.json':
    get:
      summary: 'Searches for a referrer by the given search string and returns their profile information. Searches for a matching client by member ID, email address, phone number, or encrypted user key, and returns their name, email, phone, and photo. Returns empty or `null` fields when no matching client is found. Search is rate-limited for guest and client callers.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the current business.'
          schema:
            type: string
        - name: s_search
          in: query
          description: 'The string to be used for searching for a referrer.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_photo:
                        type: array
                        description: 'Information about the referrer''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>string `url_photo`</dt> <dd>The URL of the photo.</dd> </dl>'
                      s_email:
                        type: string
                        description: 'The email address of the referrer.'
                      s_member:
                        type: string
                        description: 'The business client ID of the referrer.'
                      s_name_first:
                        type: string
                        description: 'The first name of the referrer.'
                      s_name_last:
                        type: string
                        description: 'The last name of the referrer.'
                      s_phone:
                        type: string
                        description: 'The phone number of the referrer.'
                      text_name_public:
                        type: [string, "null"]
                        description: 'Composes name of the referrer for public usage. `null` if the referrer is not found.'
                      uid_referrer:
                        type: string
                        description: 'The referrer''s user key.'
      tags:
        - Referrer-Referrer
  '/Wl/Coupon/CouponList/List.json':
    get:
      summary: 'Gets list of coupons. Used to populate the gift card picker in the store and booking flows. Returns all gift card types offered by the business. In franchise mode, gift cards from all franchise locations are included.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created coupons (if business is franchisor). <tt>true</tt> to include franchisee-created coupons.'
          schema:
            type: boolean
        - name: is_frontend
          in: query
          description: '`true` to get only gift cards available for current user; `false` to get all gift cards.'
          schema:
            type: boolean
        - name: is_inactive_include
          in: query
          description: 'A flag to include disabled items in the query result'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key to retrieve a list of all the gift cards in a business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_coupon:
                        type: array
                        description: 'A list of gift cards. Every element has the following keys: <dl> <dt>int `id_type`</dt> <dd>The type of coupon. One of {@link \Wl\Coupon\TypeSid} constants.</dd> <dt>string `k_coupon`</dt> <dd>The gift card key.</dd> <dt>string `text_title`</dt> <dd>The title of the gift card.</dd> </dl>'
      tags:
        - CouponList-List
  '/Wl/Search/Tag/SearchTagList.json':
    get:
      summary: 'Returns list of search tags. This is public information and method does not require any level of privileges. Returns the complete list of system-wide search tags used for filtering businesses and services in directory searches. No authentication or input parameters are required.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_search_tag:
                        type: array
                        description: 'A list of all the search tags. Each element is an array with the following keys: <dl> <dt>int `id_business_category`</dt> <dd> The business category that can use this search tag. One of the {@link \RsBusinessCategorySid} constants. This can be found in the business information found in {@link \Wl\Business\DataApi::$id_category}. </dd> <dt>string `k_search_tag`</dt> <dd>The key of the tag. The primary key in the table of tags.</dd> <dt>string `text_title`</dt> <dd>The name of the tag.</dd> </dl>'
      tags:
        - Tag-SearchTagList
  '/Wl/Insurance/Catalog/ProgramList.json':
    get:
      summary: 'Returns a list of active insurance programs for the specified promotion. Requires backend access and an active Wellness Program feature for the business. Returns all available insurance programs with pricing, organization name, partner name, and a link to the insurance detail page.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the franchisee business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_wellness_program:
                        type: array
                        description: 'A List of active programs. Every element has next keys: <dl> <dt>string `k_wellness_program`</dt> <dd>The insurance key.</dd> <dt>string `m_price`</dt> <dd>The Purchase Option price.</dd> <dt>string `text_insurance_organization`</dt> <dd>The insurance organization name.</dd> <dt>string `text_partner`</dt> <dd>The insurance partner name.</dd> <dt>string `text_program`</dt> <dd>The insurance name.</dd> <dt>string `url_insurance_detail`</dt> <dd>The insurance organization URL.</dd> </dl>'
      tags:
        - Catalog-ProgramList
  '/Wl/Gym/Visit/VisitAdd.json':
    post:
      summary: 'Records a gym visit for the specified client at the given date and time. Validates access and input, creates a visit record for the client at the specified location, optionally applies a membership promotion if payment is required, and returns the catalog URL if no promotion was found and payment was requested.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user making the gym visit.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_time:
                  type: array
                  description: 'The time of the visit, in the location''s timezone. An array with the following fields: <dl> <dt>int `i_hour`</dt> <dd>The hour component.</dd> <dt>int `i_minute`</dt> <dd>The minute component.</dd> <dt>bool `is_am`</dt> <dd>If `true` the visit is in the AM. If `false` it is in the PM.</dd> </dl>'
                dt_date:
                  type: string
                  description: 'The date of visit, without a time component in MySQL format. Date is based on the timezone of the location.'
                is_pay:
                  type: boolean
                  description: 'Determines whether payment is required for the visit. `true` if payment is needed for the visit. `false` if visit should be not pay.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_catalog:
                        type: string
                        description: 'Url to the business catalog with items that can be used to pay for the visit.'
      tags:
        - Visit-VisitAdd
  '/Wl/Microsoft/Login/MicrosoftLogin.json':
    delete:
      summary: 'Removes the association between a website client and a Microsoft account. Accepts the user''s UID, verifies that the caller is the account owner, and unlinks the Microsoft account from the user''s profile.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-MicrosoftLogin
    get:
      summary: 'Collects data for the Microsoft login button. Called when rendering the "Sign in with Microsoft" button. Generates the OAuth 2.0 authorization URL the button must link to. When a UID is provided, also reports whether that user already has a Microsoft account linked, so the frontend can show "Link" or "Unlink" instead of the default sign-in label.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exists:
                        type: boolean
                        description: 'If `true`, the user has a bound Microsoft account. Otherwise, this will be `false`.'
                      url_login:
                        type: string
                        description: 'The Microsoft OAuth 2.0 authorization link.'
      tags:
        - Login-MicrosoftLogin
    post:
      summary: 'Authenticates a user via Microsoft OAuth for the specified business. Validates the business key, sets it as the current frontend business context, and then delegates to the parent Microsoft OAuth flow to complete sign-in.'
      parameters:
        - name: is_external
          in: query
          description: 'If authorization is performed in a third-party application, set this flag in case of authorization errors.'
          schema:
            type: boolean
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business in which authorization is performed.'
                s_code:
                  type: string
                  description: 'The authorization code that the app requested.'
                s_state:
                  type: string
                  description: 'If a state parameter is included in the request, the same value should appear in the response. The app should verify that the state values in the request and response are identical.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-MicrosoftLogin
  '/Wl/Facebook/Login/FacebookLogin.json':
    post:
      summary: 'Performs Facebook authorization within the context of the specified business. Validates that the given business is active, sets it as the current frontend context, and then delegates to the parent Facebook login flow to authenticate the user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business in which authorization is performed.'
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link FacebookCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link FacebookCredential::CID}. When application passed, the credential loaded by application id.'
                s_token:
                  type: string
                  description: 'The Facebook token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-FacebookLogin
  '/Wl/Google/Login/GoogleLogin.json':
    post:
      summary: 'Performs Google authorization within the context of the specified business. Validates that the given business is active, sets it as the current frontend context, and then delegates to the parent Google login flow to authenticate the user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business in which authorization is performed.'
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link GoogleCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link GoogleCredential::CID}. When application passed, the credential loaded by application id.'
                s_code:
                  type: string
                  description: 'The Google server authorization code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-GoogleLogin
  '/Wl/Social/Share/SocialShare.json':
    post:
      summary: 'Saves the share post data and returns the secret key for the shared object. Records that the user has shared the specified objects (purchases, bookings, locations, or reviews) to a social network, and returns a secret token that can be appended to the destination URL to deep-link directly to the shared items.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_key:
                  type: array
                  description: 'The primary keys of the shared objects. Depends on {@link SocialShareApi::$id_share_object}. If {@link SocialShareApi::$id_share_object} is: * {@link ShareObjectSid::BOOK} - each value is key of the visit. <i>Primary key in {@link \RsVisitSql} table.</i> * {@link ShareObjectSid::LOCATION} - each value is key of the location. <i>Primary key in {@link \RsLocationSql} table.</i> * {@link ShareObjectSid::PURCHASE} - each value is key of the purchase. <i>Primary key in {@link \RsPurchaseSql} table.</i> * {@link ShareObjectSid::REVIEW} - each value is key of the review. <i>Primary key in {@link \RsReviewSql} table.</i>'
                id_share_destination:
                  type: number
                  format: int32
                  description: 'The id of the social network.'
                id_share_object:
                  type: number
                  format: int32
                  description: 'The id of type object for share post to social network.'
                k_business:
                  type: string
                  description: 'Business key.'
                uid:
                  type: string
                  description: 'User key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_secret:
                        type: string
                        description: 'Secret key for access shared object.'
      tags:
        - Share-SocialShare
  '/Thoth/WlPay/Account/Charge/Charge.json':
    post:
      summary: 'Refills the user account balance by the specified payment amount or adjusts it manually. Accepts the payment amount, account key or user-and-business identifiers, charge mode, and payment form data. Processes the payment through the configured payment environment and returns the purchase key when a new purchase is created (for the automatic charge mode).'
      parameters:
        - name: id_pay_account_charge
          in: query
          description: 'The account charge mode. One of the {@link \RsPayAccountChargeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_staff
          in: query
          description: 'If `true`, the account is filled by a staff member in the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The ID of the business the user account belongs to. This shouldn''t be passed if a user account has already been created. In such cases, {@link ChargeApi::$k_pay_account} should be passed instead. If both the business ID and account ID passed, the system checks if the given business is the owner of the specified account.'
          schema:
            type: string
        - name: k_pay_account
          in: query
          description: 'The ID of the user account to refill. This may be 0 if a user account hasn''t been created yet. In such cases, {@link ChargeApi::$k_business} and {@link ChargeApi::$uid} should be passed instead. If not passed, the currency of account equals the default business currency.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The ID of the user whose account is being refilled. This shouldn''t be passed if a user account has already been created. In such cases, {@link ChargeApi::$k_pay_account} should be passed instead. If both the user ID and account ID passed, the system checks if the given user is the owner of the specified account.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'The source mode key. One of the {@link \Wl\Mode\ModeSid} constants.'
                m_amount:
                  type: string
                  description: 'The payment amount.'
                s_comment:
                  type: string
                  description: 'The transaction comment.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_purchase:
                        type: string
                        description: 'The ID of the purchase that was created during payment. This value is only returned in cases where a purchase was created. A new purchase is created when {@link ChargeApi::$id_pay_account_charge} equals {@link \RsPayAccountChargeSid::AUTO}.'
      tags:
        - Charge-Charge
  '/Thoth/WlPay/Bank/Card/List.json':
    get:
      summary: 'Retrieves information about user''s bank cards. Returns the list of saved payment cards for the specified user and business, including card number fragment, expiry date, card system, holder name, and default status. Also returns whether new cards can be added.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of current business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show information for. <tt>0</tt> to use user''s home location.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_bank_card:
                        type: array
                        description: 'A list of bank cards. The array keys are the card `k_pay_bank` IDs. Each element has the following keys: <dl> <dt>int `i_month`</dt> <dd>The month when the payment card expires, represented by a number (1=January and 12=December).</dd> <dt>int `i_year`</dt> <dd>The last two digits of the year when the payment card expires.</dd> <dt>int `id_card_system`</dt> <dd>The card system ID. This is one of the {@link \ACardSystemSid} constants.</dd> <dt>bool `is_autopay`</dt> <dd> If `true`, this card backs at least one active autopay membership or package. If `false`, no active autopay membership or package is pinned to this card. </dd> <dt>bool `is_default`</dt> <dd>If `true`, then this card is the user default card. If `false`, then this isn''t the user default card.</dd> <dt>string `k_pay_address`</dt> <dd>The payment address ID. This refers to a physical address associated with a payment card.</dd> <dt>string `k_pay_bank`</dt> <dd>The payment method ID. Each payment card for each user will have its own ID.</dd> <dt>string `text_name_card`</dt> <dd> The payment card descriptor. This is typically the company name and the last 4 digits of the card (for example, visa-5903). </dd> <dt>string `text_name_holder`</dt> <dd>The name of the card owner as it appears on the card.</dd> <dt>string `text_number`</dt> <dd> A portion of the payment card number, used to identify the card. This is typically the last 4 digits of the card number. </dd> </dl>'
                      a_list:
                        type: array
                        description: 'List of bank cards. Duplicate of {@link \Thoth\WlPay\Bank\Card\ListApi::$a_bank_card} as a flat indexed array. <dl> <dt>int `i_month`</dt> <dd>The month when the payment card expires, represented by a number (1=January and 12=December).</dd> <dt>int `i_year`</dt> <dd>The last two digits of the year when the payment card expires.</dd> <dt>int `id_card_system`</dt> <dd>The card system ID. This is one of the {@link \ACardSystemSid} constants.</dd> <dt>bool `is_autopay`</dt> <dd> If `true`, this card backs at least one active autopay membership or package. If `false`, no active autopay membership or package is pinned to this card. </dd> <dt>bool `is_default`</dt> <dd>If `true`, then this card is the user default card. If `false`, then this isn''t the user default card.</dd> <dt>string `k_pay_address`</dt> <dd>The payment address ID. This refers to a physical address associated with a payment card.</dd> <dt>string `k_pay_bank`</dt> <dd>The payment method ID. Each payment card for each user will have its own ID.</dd> <dt>string `text_name_card`</dt> <dd> The payment card descriptor. This is typically the company name and the last 4 digits of the card (for example, visa-5903). </dd> <dt>string `text_name_holder`</dt> <dd>The name of the card owner as it appears on the card.</dd> <dt>string `text_number`</dt> <dd> A portion of the payment card number, used to identify the card. This is typically the last 4 digits of the card number. </dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'Whether new card can be added. `true` if new cards can be added. `false` if new cards can not be added.'
                      can_remove_self:
                        type: boolean
                        description: 'Whether the client is allowed to remove their own saved credit cards. `true` if the client can remove their own saved credit cards. `false` if the client must contact the business to remove a saved credit card.'
                      has_remove_self:
                        type: boolean
                        description: 'Whether the feature "client is allowed to remove their own saved credit cards" has been released. `true` if the feature has been released. `false` if the feature has not been released.'
      tags:
        - Card-List
  '/Thoth/WlPay/Bank/Ach/List.json':
    get:
      summary: 'Retrieves information about user''s ACH accounts. Validates the user, business, and location, then retrieves all saved ACH accounts for the resolved merchant. Also reports whether the current owner is allowed to add a new ACH account.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of current business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show information for. <tt>0</tt> to use user''s home location.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of ACH accounts: <dl> <dt>int `id_pay_bank_ach_holder`</dt> <dd>Type of account holder. One of {@link \RsPayBankAchHolderSid} constants.</dd> <dt>int `id_pay_bank_ach_type`</dt> <dd>Type of account. One of {@link \RsPayBankAchTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd><tt>true</tt> - this account is default payment method; <tt>false</tt> - otherwise.</dd> <dt>string `k_pay_bank`</dt> <dd>ID of bank account. <i>Primary key in {@link \RsPayBankSql}.</i></dd> <dt>string `text_name_account`</dt> <dd>Account name.</dd> <dt>string `text_name_holder`</dt> <dd>Account holder name.</dd> <dt>string `text_number`</dt> <dd>ACH account number.</dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'Whether new ACH account can be added. `true` if new ACH account can be added. `false` if new ACH account can not be added.'
      tags:
        - Ach-List
  '/Thoth/WlPay/Transaction/Report/TransactionAllPayment.json':
    get:
      summary: 'Returns All Transactions Report data for the specified date range. Provides access to the All Transactions Report used for revenue reconciliation and export. The report is generated asynchronously and cached; check `$id_report_status` to determine whether generation is still in progress. Set `$is_refresh` to request regeneration and use `$i_page` to paginate through up to {@link TransactionAllPaymentApi::LIMIT} rows per request.'
      parameters:
        - name: dl_date_end
          in: query
          description: 'The end date in local time to retrieve transactions for.'
          schema:
            type: string
        - name: dl_date_start
          in: query
          description: 'The end date in local time to retrieve transactions for.'
          schema:
            type: string
        - name: i_page
          in: query
          description: 'The page of the report, starting from 0. Each page will contain a maximum of {@link TransactionAllPaymentApi::LIMIT} rows.'
          schema:
            type: number
            format: int32
        - name: is_refresh
          in: query
          description: 'Determines whether the report should be refreshed. If `true`, this report should be refreshed. A report refresh can''t be requested while the report is being generated. Otherwise, `false` to only return contents of the report. If the report hasn''t yet been generated, it will automatically start generating in the background.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business for which report should be generated.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field:
                        type: array
                        description: 'A list of fields in the report. This array is effectively a title row for the table returned in {@link TransactionAllPaymentApi::$a_row}.'
                      a_row:
                        type: array
                        description: 'The report data. This is an indexed array where each element is also an indexed array representing one report row. Column positions in each row correspond to the field names in {@link TransactionAllPaymentApi::$a_field}. Known columns (use {@link TransactionAllPaymentApi::$a_field} to resolve their positional indexes): <dl> <dt>array `a_pay_transaction`</dt> <dd> List of pay transactions associated with this row. Each element: <dl> <dt>int `id_pay_method`</dt> <dd>Payment method ID. One of {@link \RsPayMethodSid} constants.</dd> <dt>string `k_pay_transaction`</dt> <dd>Pay transaction key. <i>Primary key in the {@link \RsPayTransactionSql} table.</i></dd> <dt>string `m_amount`</dt> <dd>Amount paid.</dd> <dt>string `m_surcharge`</dt> <dd>Surcharge amount from store settings.</dd> <dt>string `s_batch_number`</dt> <dd>Batch number for this transaction.</dd> </dl> </dd> <dt>string `dtu_purchase_start`</dt> <dd>Start date and time of the purchase in UTC.</dd> <dt>string `k_purchase`</dt> <dd>Purchase key. <i>Primary key in the {@link \RsPurchaseSql} table.</i></dd> <dt>string `m_net_sale`</dt> <dd>Net sales amount.</dd> <dt>string `m_total_tax`</dt> <dd>Total tax amount.</dd> <dt>string `o_client.text_first`</dt> <dd>First name of the client.</dd> <dt>string `o_client.text_last`</dt> <dd>Last name of the client.</dd> <dt>string `o_client.text_name`</dt> <dd>Full name of the client.</dd> <dt>string `o_location.k_location`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `s_batch_number`</dt> <dd>Batch number of the first transaction in the row.</dd> <dt>string `text_location_address`</dt> <dd>Street address of the location.</dd> <dt>string `text_location_city`</dt> <dd>Name of the location''s city.</dd> <dt>string `text_location_country`</dt> <dd>Name of the location''s country.</dd> <dt>string `text_location_postal`</dt> <dd>Postal code of the location.</dd> <dt>string `text_location_region`</dt> <dd>Name of the location''s region.</dd> <dt>string `text_payment_method`</dt> <dd>Name of the payment method used.</dd> <dt>string `text_payment_method_base`</dt> <dd>Name of the base payment method. If a custom method was used, this is the method it is based on.</dd> <dt>string `text_tax_title`</dt> <dd>Comma-separated list of tax names applied to the purchase.</dd> <dt>string `text_tax_value`</dt> <dd> Comma-separated list of tax values. Percentage-based taxes have `%` appended; fixed-amount taxes have `$` appended. </dd> </dl>'
                      a_warning:
                        type: array
                        description: 'The warning list of the report.'
                      dtu_complete:
                        type: [string, "null"]
                        description: 'The date and time if the report has completed generation. Otherwise, this will be `null`.'
                      dtu_queue:
                        type: [string, "null"]
                        description: 'The date and time if this report has been put in the generation queue. Otherwise, this will be `null`.'
                      dtu_start:
                        type: [string, "null"]
                        description: 'The date and time if generation of this report has started. Otherwise, this will be `null`.'
                      id_report_status:
                        type: number
                        format: int32
                        description: 'The report status.'
                      is_more:
                        type: boolean
                        description: 'Determines whether to show more rows in the report. If `true`, there are more report rows to get. Otherwise, `false` to indicate that all rows in the report have already been retrieved.'
                      is_report_complete:
                        type: boolean
                        description: 'Determines whether the report is complete. If the report is accessed on the current day, or is returning a result that was cached on the current day, it could be incomplete as not all the transactions for the day are present. If `true`, the report has been completed. Otherwise, `false`.'
      tags:
        - Report-TransactionAllPayment
  '/Thoth/WlPay/Address/Widget/WidgetEdit.json':
    get:
      summary: 'Gets data for "edit payment address" widget. Returns the list of geographic regions and phone number format masks configured for the given business locale. When no business key is provided, system-level defaults are returned.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_geo:
                        type: array
                        description: 'List of countries with their regions, keyed by country geo key. Each element: <dl> <dt>array[] `a_region`</dt> <dd> List of geographic regions within the country. Each element: <dl> <dt>bool `is_select`</dt> <dd>`true` if this region is currently selected; `false` otherwise.</dd> <dt>string `k_geo`</dt> <dd>Region key.</dd> <dt>string `s_title`</dt> <dd>Region title.</dd> <dt>string `text_abbr`</dt> <dd>Region abbreviation.</dd> </dl> </dd> <dt>bool `is_select`</dt> <dd>`true` if this country is currently selected; `false` otherwise.</dd> <dt>string `k_geo`</dt> <dd>Country key.</dd> <dt>string `s_title`</dt> <dd>Country title.</dd> <dt>string `text_abbr`</dt> <dd>Country abbreviation.</dd> </dl>'
                      html_phone_mask:
                        type: string
                        description: 'Mask for phone entering (ready for output to the page).'
                      text_phone_mask:
                        type: string
                        description: 'Mask for phone entering.'
      tags:
        - Widget-WidgetEdit
  '/Core/Passport/Login/Register/Register.json':
    post:
      summary: 'Validates the new user''s data and sends a confirmation email to complete registration. Accepts the new user''s name, email, and password, validates each field, stores the pending registration, and sends a confirmation email with a link to complete registration via {@link RegisterConfirmApi}. An optional application ID and custom confirmation URL may be provided.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'The additional data about new users. <dl> <dt>string `k_business`</dt> <dd> The key of the business for the WellnessLiving project to register the user in. The behavior may vary for different applications. If the application is connected to a given business, this property can always be empty (all clients will be registered in the connected business). If the business is set for such an application and is different from the business from the application settings, an error will occur. If the application isn''t connected to the business, any business key can be set. This doesn''t require any special privileges, as registration is a publicly available action. </dd> </dl>'
                id_mode:
                  type: [number, "null"]
                  description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants. `null` if not initialized or the api is not called in the Wellnessliving project.'
                s_application:
                  type: string
                  description: 'The application ID. This is a business specific ID required to register clients.'
                s_mail:
                  type: string
                  description: 'The new client email address.'
                s_name_first:
                  type: string
                  description: 'The new client given name.'
                s_name_last:
                  type: string
                  description: 'The new client surname.'
                s_password:
                  type: string
                  description: 'The new client password.'
                url_confirm:
                  type: string
                  description: 'The URL to the confirmation page. This link is used in a confirmation email. If empty, URL to default page is used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_confirm_config:
                        type: string
                        description: 'JSON configuration for confirmation email.'
                      url_confirm:
                        type: string
                        description: 'The URL to the confirmation page. This link is used in a confirmation email. If empty, URL to default page is used.'
      tags:
        - Register-Register
  '/Core/Passport/Login/Register/RegisterConfirm.json':
    post:
      summary: 'Confirms email of a new user and completes registration. Accepts the email, login, and verification code obtained from the confirmation link sent by {@link RegisterApi}, validates them against the pending registration record, creates the user account, and returns the new user''s UID.'
      parameters:
        - name: text_code
          in: query
          description: 'The unique registration code.'
          schema:
            type: string
        - name: text_login
          in: query
          description: 'The user''s login.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The user''s email.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'The key of the new registered user.'
      tags:
        - Register-RegisterConfirm
  '/Core/Passport/Login/Enter/Enter.json':
    post:
      summary: 'Signs the user in using their login and hashed password. Accepts the user login, a password hash derived using the notepad obtained from {@link NotepadApi}, and an optional remember preference. Validates credentials, enforces CAPTCHA when too many failed attempts have occurred, starts a session for the user, and returns a redirect URL if applicable.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_data:
                  type: string
                  description: 'Additional data for user authorization.'
                s_captcha:
                  type: string
                  description: 'The answer to the captcha, if needed.'
                s_login:
                  type: string
                  description: 'The user''s login.'
                s_notepad:
                  type: string
                  description: 'A copy of the notepad that was used to hash the user password. See {@link \Core\Passport\Login\Enter\EnterApi::$s_password} for details.'
                s_password:
                  type: string
                  description: 'The hash of the user password. Use `Core_Passport_Login_Enter_NotepadModel.hash()` to evaluate the password hash.'
                s_remember:
                  type: string
                  description: 'Determines whether the user login and password should be remembered, and how they should be remembered. The accepted values are: <ul> <li>`''''` Empty line (default value) if you do not want to remember anything.</li> <li>`''login''` Remember only user login.</li> <li>`''password''` Remember user login and password.</li> </ul>'
                url_return:
                  type: string
                  description: 'Url of previous page if the user was redirected to login.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'An optional URL for redirection after the user has signed in to the web application.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      url_captcha:
                        type: string
                        description: 'Optional URL to get captcha image. This field is filled in a case when a captcha code is required to sign in.'
      tags:
        - Enter-Enter
  '/Core/Passport/Login/Enter/Notepad.json':
    get:
      summary: 'Generates notepad for user sign in form. Generates a cryptographic nonce (one-time random string) and stores it in the session so that the client can hash the user''s password with it before sending it to {@link EnterApi}. This prevents replay attacks and avoids transmitting passwords in plain text.'
      parameters:
        - name: s_login
          in: query
          description: 'User login. If specified, additional information about user may be returned such as datacenter of the user. This value is only considered when {@link AmazonRegionSid::currentId()} is not empty, and {@link AmazonRegionSid::isRegionFixed()} returns `false`. In this case, regional cookie may be set, and the request may be forwarded internally to a different datacenter. `null` if additional information is not needed.'
          schema:
            type: [string, "null"]
        - name: s_type
          in: query
          description: 'The session type to store the notepad in. This will be an empty string for a temporary session. Otherwise, you can use `system` for a system session.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_region:
                        type: [number, "null"]
                        description: 'ID of the datacenter which is a preferred datacenter for the user specified in {@link NotepadApi::$s_login}. One of {@link AmazonRegionSid} constants. If ID of the datacenter returned in this property differs from the current datacenter, this means that this API request was forwarded to the datacenter returned here, and the notepad was created there. So that {@link EnterApi} must always go to the datacenter specified in this property. `null` if datacenter preference was not evaluated, or it is not known. In this case, the notepad is created in datacenter where the API request was initially sent.'
                      s_hash:
                        type: string
                        description: 'The hash type.'
                      s_notepad:
                        type: string
                        description: 'The notepad value, which is used to hash the user''s password. The password length is {@link NotepadApi::NONCE_LENGTH} characters.'
      tags:
        - Enter-Notepad
  '/Core/Passport/Login/SignOut/SignOut.json':
    post:
      summary: 'Signs user out. Requires the user to be signed in, fires a before-logout event allowing listeners to intercept or handle the logout, then clears the current session. Throws an error if the user is authenticated via SAML SSO, as API-based logout is not supported in that case.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - SignOut-SignOut
  '/Core/Passport/User/Email/EmailExist.json':
    get:
      summary: 'Checks if email address exists. Used in registration and "forgot password" flows to give immediate feedback before the user submits the full form. Returns `true` if a user account with the given email already exists, so the frontend can prompt to sign in instead of registering.'
      parameters:
        - name: text_email
          in: query
          description: 'The email address.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exist:
                        type: boolean
                        description: 'Determines whether the email address exists.'
      tags:
        - Email-EmailExist
  '/Core/Passport/Enter/Jwt/JwtToken.json':
    get:
      summary: 'Returns a jwt token that can be used to log user. Requires the user to be signed in. Generates a signed JWT token tied to the current authorization header and user ID that can be passed to other services to authenticate the user without sharing session cookies.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token:
                        type: string
                        description: 'Jwt token that allows to authenticate user.'
      tags:
        - Jwt-JwtToken
  '/Core/Request/Api/Application/Origin.json':
    delete:
      summary: 'Restricts access to API for all sites, which are given in the list. Accepts a list of origin URLs (with optional API domain overrides), validates each URL, and removes the matching entries from the allowed origins for the current application, then clears the origin cache.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Origin
    get:
      summary: 'Gets list of all sites, where usage of the API is allowed for the current application. Returns the list of allowed CORS origins for the current API application, where each key is an origin URL and each value is an optional API domain used to proxy requests from that origin.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: [array, "null"]
                        description: 'A list of origins. An associative array where the key is the origin URL of the site where API calls can be made, and the value is the additional API domain used to make API requests to the WellnessLiving server. `null` if not yet initialized.'
      tags:
        - Application-Origin
    put:
      summary: 'Allows access to API for all sites, which are given in the list. Accepts a list of origin URLs (with optional API domain overrides), validates each URL and domain, inserts or updates the entries in the allowed origins for the current application, then clears the origin cache.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_list:
                  type: [array, "null"]
                  description: 'A list of origins. An associative array where the key is the origin URL of the site where API calls can be made, and the value is the additional API domain used to make API requests to the WellnessLiving server. `null` if not yet initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Origin
  '/Wl/Schedule/ScheduleList/StaffApp/ScheduleList.json':
    get:
      summary: 'Gets schedule of business {@link ScheduleListApi::$k_business} for day {@link ScheduleListApi::$dt_date}. Returns all classes and appointments scheduled for the given business on the specified date, sorted chronologically. Supports both single-day and date-range modes, and includes full session details such as staff, visit counts, assets, and class images.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration options for schedule. Key is option name, value is boolean. May contain the following keys: <dl> <dt>bool `is_appointment_cancel_recurring`</dt> <dd>If needed to show recurring canceled appointments on schedule.</dd> <dt>bool `is_appointment_cancel_single`</dt> <dd>If needed to show single canceled appointments on schedule.</dd> <dt>bool `is_class_cancel`</dt> <dd>If needed to show canceled classes on schedule.</dd> </dl>'
          schema:
            type: array
        - name: dl_end
          in: query
          description: 'The end date of the range from which the list of schedule sessions should be retrieved. This will be `null` if the range has no end date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'The start date of the range from which the list of scheduled sessions should be retrieved. This will be `null` if the range has no start date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dt_date
          in: query
          description: 'The date of the sessions in Coordinated Universal Time (UTC) and MySQL format. If this value is used, then {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_end} and {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_start} should not be set.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_schedule:
                        type: array
                        description: 'The sessions present on the business schedule. These are sorted chronologically in ascending order. Every element has the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes.</dd> <dt>array[]|null `a_quiz`</dt> <dd> List of quizzes. Has next structure: `null` if no quizzes for schedule item. <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key, <i>primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key, <i>primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key, <i>primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array `a_appointment_visit_info`</dt> <dd> Additional visit information about this appointment. Empty array if it''s a class. <dl> <dt>int `id_visit`</dt> <dd>Visit id. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_confirmed`</dt> <dd>`true` means that appointment was requested and confirmed by the staff.</dd> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl> </dd> <dt>string[] `a_resource`</dt> <dd>A list of assets involved in the session. <i>Primary keys in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_resource_removed`</dt> <dd>List of removed assets involved in the session. <i>Primary keys in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_staff`</dt> <dd> A list of staff members who will conduct the session. <i>Primary keys in {@link \RsStaffSql} table.</i> Deprecated. Returned only for applications in the `APPS_USE_OLD_K_STAFF` allow-list. New applications should use `a_uid_staff` instead. </dd> <dt>string[] `a_uid_staff`</dt> <dd> A list of staff members who will conduct the session. <i>Primary keys in {@link \PassportLoginSql} table.</i> </dd> <dt>array `a_staff_info`</dt> <dd> Information about staff members who conduct this session. For applications in the `APPS_USE_OLD_K_STAFF` allow-list, keys are <i>primary keys in {@link \RsStaffSql} table</i>. For other applications, keys are <i>primary keys in {@link \PassportLoginSql} table</i>. Values are array with data: <dl> <dt>string `text_staff`</dt> <dd>Staff full name.</dd> <dt>bool `is_staff_change`</dt> <dd>`true` means staff is substituted, `false` simple staff member.</dd> <dt>string `k_staff`</dt> <dd> Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> Returned only for applications in the `APPS_USE_OLD_K_STAFF` allow-list. </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string[] `a_user`</dt> <dd>For appointments, this is a list of the names of users who are scheduled to attend the session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. Each value is <i>primary key in {@link \RsLocationSql} table</i>.</dd> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `dt_date_cancel`</dt> <dd>The date/time when the session was canceled in UTC. Only used for appointments.</dd> <dt>string `dt_date_local`</dt> <dd>The date/time of the session in local time.</dd> <dt>bool `has_quiz_uncompleted`</dt> <dd>`true` if has uncompleted quizzes, `false` otherwise.</dd> <dt>int `i_addon`</dt> <dd>The number of addons for the current appointment. Set only if the current session is an appointment.</dd> <dt>int `i_book`</dt> <dd>The number of clients booked into the session.</dd> <dt>int `i_capacity`</dt> <dd>The maximum capacity of the session.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_padding_after`</dt> <dd>The padding time after the session in minutes. Only used for appointments.</dd> <dt>int `i_padding_before`</dt> <dd>The padding time before the session in minutes. Only used for appointments.</dd> <dt>int `i_start`</dt> <dd> The start time in minutes after midnight. For example, a class starting at 10:30 in the morning local time will have an `i_start` value of 630. </dd> <dt>int `i_wait`</dt> <dd>Count clients on waitlist.</dd> <dt>int `id_option`</dt> <dd> Appointment title display style. Set only for appointments, for others it will be equal to 0. Constants from {@link \Wl\Schedule\Design\OptionSid}. </dd> <dt>int `id_service`</dt> <dd>The ID of the service type. One of {@link \RsServiceSid} constants.</dd> <dt>bool `is_arrive`</dt> <dd> For appointments: <tt>true</tt> if user has checked-in; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_class_view`</dt> <dd> `true` if this class or event session is visible only through the "View other staff schedules" permission and must be shown read-only (no attendance roster, no booked client identities, and no edit, cancel, reschedule, or booking actions); `false` otherwise. </dd> <dt>bool `is_pay`</dt> <dd> For appointments: <tt>true</tt> if appointment is paid; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_repeat`</dt> <dd> For appointments: <tt>true</tt> if appointment is recurring; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>string `k_appointment`</dt> <dd> The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i> If the session isn''t an appointment, this will be `0`. </dd> <dt>string `dt_confirm`</dt> <dd> Confirmation date for appointment in MySQL format. Will be zero date + time in case appointment is not yet confirmed by client. </dd> <dt>string `k_class`</dt> <dd> The class key. <i>Primary key in {@link \RsClassSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_class_period`</dt> <dd> The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_location`</dt> <dd>The location key for where the session takes place. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_service`</dt> <dd> This is the key of the appointment type, while `k_appointment` is the specific instance. <i>Primary key in {@link \RsServiceSql} table.</i> For other cases, this will be `0`. </dd> <dt>string `s_title`</dt> <dd>The name of the session.</dd> <dt>bool `show_quiz_icon`</dt> <dd>`true` when need show quiz icon, `false` otherwise.</dd> <dt>int `t_business_start`</dt> <dd>The earliest time the business where the session takes place, opens. In Unix format.</dd> <dt>int `t_business_end`</dt> <dd>The latest time the business where the session takes place, closes. In Unix format.</dd> <dt>string `text_alert`</dt> <dd>Alert message.</dd> <dt>string `text_color_background`</dt> <dd>The background color in hex representation as used on WellnessLiving.</dd> <dt>string `text_color_border`</dt> <dd>The border color in hex representation as used on WellnessLiving.</dd> <dt>string `text_quiz_title`</dt> <dd>Title for quiz icon.</dd> <dt>bool `is_virtual_service`</dt> <dd><tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.</dd> <dt>string `url_image`</dt> <dd>URL to image. Empty if image not exist.</dd> </dl>'
                      is_virtual_service:
                        type: boolean
                        description: '<tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.'
      tags:
        - StaffApp-ScheduleList
  '/Wl/Schedule/ScheduleList/StaffApp/ScheduleListByToken.json':
    get:
      summary: 'Gets schedule of business {@link ScheduleListApi::$k_business} for day {@link ScheduleListApi::$dt_date}. Returns all classes and appointments scheduled for the given business on the specified date, sorted chronologically. Supports both single-day and date-range modes, and includes full session details such as staff, visit counts, assets, and class images.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration options for schedule. Key is option name, value is boolean. May contain the following keys: <dl> <dt>bool `is_appointment_cancel_recurring`</dt> <dd>If needed to show recurring canceled appointments on schedule.</dd> <dt>bool `is_appointment_cancel_single`</dt> <dd>If needed to show single canceled appointments on schedule.</dd> <dt>bool `is_class_cancel`</dt> <dd>If needed to show canceled classes on schedule.</dd> </dl>'
          schema:
            type: array
        - name: dl_end
          in: query
          description: 'The end date of the range from which the list of schedule sessions should be retrieved. This will be `null` if the range has no end date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'The start date of the range from which the list of scheduled sessions should be retrieved. This will be `null` if the range has no start date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dt_date
          in: query
          description: 'The date of the sessions in Coordinated Universal Time (UTC) and MySQL format. If this value is used, then {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_end} and {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_start} should not be set.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_schedule:
                        type: array
                        description: 'The sessions present on the business schedule. These are sorted chronologically in ascending order. Every element has the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes.</dd> <dt>array `a_appointment_visit_info`</dt> <dd> Additional visit information about this appointment. Empty array if it''s a class. <dl> <dt>int `id_visit`</dt> <dd>Visit id. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_confirmed`</dt> <dd>`true` means that appointment was requested and confirmed by the staff.</dd> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl> </dd> <dt>string[] `a_resource`</dt> <dd>A list of assets involved in the session. <i>Primary keys in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_staff`</dt> <dd> A list of staff members who will conduct the session. <i>Primary keys in {@link \RsStaffSql} table.</i> Deprecated, use <var>a_staff_list</var> instead. </dd> <dt>array `a_staff_info`</dt> <dd> Information about staff members who conduct this session. The keys are <i>primary keys in {@link \RsStaffSql} table</i>. Values are array with data: <dl> <dt>string `text_staff`</dt> <dd>Staff full name.</dd> <dt>bool `is_staff_change`</dt> <dd><tt>true</tt> means staff is substituted, <tt>false</tt> simple staff member.</dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary keys in {@link \RsStaffSql} table.</i></dd> </dl> </dd> <dt>string[] `a_user`</dt> <dd>For appointments, this is a list of the names of users who are scheduled to attend the session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. Each value is <i>primary key in {@link \RsLocationSql} table</i>.</dd> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `dt_date_cancel`</dt> <dd>The date/time when the session was canceled in UTC. Only used for appointments.</dd> <dt>string `dt_date_local`</dt> <dd>The date/time of the session in local time.</dd> <dt>int `i_book`</dt> <dd>The number of clients booked into the session.</dd> <dt>int `i_capacity`</dt> <dd>The maximum capacity of the session.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_padding_after`</dt> <dd>The padding time after the session in minutes. Only used for appointments.</dd> <dt>int `i_padding_before`</dt> <dd>The padding time before the session in minutes. Only used for appointments.</dd> <dt>int `i_start`</dt> <dd> The start time in minutes after midnight. For example, a class starting at 10:30 in the morning local time will have an `i_start` value of 630. </dd> <dt>int `i_wait`</dt> <dd>Count clients on waitlist.</dd> <dt>int `id_option`</dt> <dd> Appointment title display style. Set only for appointments, for others it will be equal to 0. Constants from {@link \Wl\Schedule\Design\OptionSid}. </dd> <dt>int `id_service`</dt> <dd>The ID of the service type. One of {@link \RsServiceSid} constants.</dd> <dt>bool `is_arrive`</dt> <dd> For appointments: <tt>true</tt> if user has checked-in; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_pay`</dt> <dd> For appointments: <tt>true</tt> if appointment is paid; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_repeat`</dt> <dd> For appointments: <tt>true</tt> if appointment is recurring; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>string `k_appointment`</dt> <dd> The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i> If the session isn''t an appointment, this will be `0`. </dd> <dt>string `dt_confirm`</dt> <dd> Confirmation date for appointment in MySQL format. Will be zero date + time in case appointment is not yet confirmed by client. </dd> <dt>string `k_class`</dt> <dd> The class key. <i>Primary key in {@link \RsClassSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_class_period`</dt> <dd> The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_location`</dt> <dd>The location key for where the session takes place. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_service`</dt> <dd> This is the key of the appointment type, while `k_appointment` is the specific instance. <i>Primary key in {@link \RsServiceSql} table.</i> For other cases, this will be `0`. </dd> <dt>string `s_title`</dt> <dd>The name of the session.</dd> <dt>string `text_alert`</dt> <dd>Alert message.</dd> <dt>string `text_color_background`</dt> <dd>The background color in hex representation as used on WellnessLiving.</dd> <dt>string `text_color_border`</dt> <dd>The border color in hex representation as used on WellnessLiving.</dd> <dt>bool `is_virtual_service`</dt> <dd><tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.</dd> <dt>string `url_image`</dt> <dd>URL to image. Empty if image not exist.</dd> </dl>'
                      is_virtual_service:
                        type: boolean
                        description: '<tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.'
      tags:
        - StaffApp-ScheduleListByToken
  '/Wl/Book/Process/Store/Store.json':
    post:
      summary: 'Processes the "Purchase Options" step of the booking wizard, validates selections, and books the session when possible. Stores the selected purchase option (existing login promotion or new purchasable item) and session data in the booking process context, validates availability and eligibility, and attempts to complete the booking immediately when no payment or quiz step is needed. Returns visit keys, activity keys, and a flag indicating whether further wizard steps are required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_purchase_item_check:
                  type: array
                  description: 'The selected purchase item. This new purchase item should be checked to determine if it can be applied to the current class or event before being purchased. <dl> <dt>array `a_config`</dt> <dd> Additional configuration. Used only for `id_purchase_item` = {@link \RsPurchaseItemSid::TUITION}. <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events. Each entry has the next structure: <dl> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> </dl> </dd> <dt>int `i_session`</dt> <dd> The number of sessions that this item can cover. This only applies to items of type {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>string `s_value`</dt> <dd> The unique identifier of the item being checked. This corresponds to one of the following values: <ul> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_purchase}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_reward_prize}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_login_prize}`["s_value"]`</li> </ul> </dd> </dl>'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Each element has the following keys: <dl> <dt>int `i_index`</dt> <dd>The order number of the asset (from 1 to the asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: object
                  description: 'The selected sessions for an event. Keys are class period keys, values are indexed arrays of dates and times when the session occurred (in MySQL format, UTC).'
                a_session_wait_list_unpaid:
                  type: object
                  description: 'The selected sessions for an event that are on the wait list and unpaid. Keys are class period keys, values are indexed arrays of dates and times when the session occurred (in MySQL format, UTC).'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys for the user''s activities. This will be populated upon completion of the booking process.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings that have been made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      is_next:
                        type: boolean
                        description: 'If `true`, the next steps of the booking wizard are required to purchase an item or book the selected session. If `false`, no further steps in the booking wizard are required.'
      tags:
        - Store-Store
  '/Wl/Book/Process/Store/StoreGroup.json':
    post:
      summary: 'Validates and distributes selected purchase options across all clients in the booking group. For each client in the group, validates session selection, resource requirements, recurring booking configuration, and purchase item eligibility. When all clients pass validation, attempts to assign or share promotions across the group and returns per-client distribution results with any error details in `a_purchase_item_distribute`.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_promotion:
                  type: array
                  description: 'A list of existing login promotions which are selected by a group of clients. This login promotions will be checked to determine if it can be applied to the current class or event. Keys are UIDs of clients, values are login promotion keys.'
                a_login_promotion_guest_pass:
                  type: array
                  description: 'A list of host guest passes which are selected by a group of clients to pay for the guests visits. These guest passes will be checked to determine if they can be applied to the current class or event. Keys are UIDs of clients, values are host login promotion keys that grant the guest passes.'
                a_purchase_item_check:
                  type: array
                  description: 'A list of new purchase items which are selected by a group of clients. This new purchase items will be checked to determine if it can be applied to the current class or event before being purchased. Keys are UIDs of clients, values are arrays with the following structure: <dl> <dt>int `i_session`</dt> <dd> The number of sessions that this item can cover. This only applies to items of type {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>string `s_value`</dt> <dd> The unique identifier of the item being checked. This corresponds to one of the following values: <ul> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_purchase}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_reward_prize}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_login_prize}`["s_value"]`</li> </ul> </dd> </dl>'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking for each client in the group. Keys are UIDs of clients, values are arrays with information about the recurring booking: Should be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets which are selected by a group of clients. Keys are UIDs of clients, values are arrays with the following structure: <dl> <dt>int `i_index`</dt> <dd>The order number of the asset (from 1 to the asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_pass:
                  type: array
                  description: 'A list of existing session passes (drop-in, full-event) which are selected by a group of clients. This session passes will be checked to determine if it can be applied to the current class or event. Keys are UIDs of clients, values are session pass keys.'
                a_session_select:
                  type: object
                  description: 'A list of sessions which are selected by a group of clients. Keys are UIDs of clients, values are arrays of class period keys mapped to index arrays of dates/times when the session occurred (MySQL format; UTC).'
                a_session_wait_list_unpaid:
                  type: object
                  description: 'A list of wait list (unpaid) sessions which are selected by a group of clients. Keys are UIDs of clients, values are arrays of class period keys mapped to index arrays of dates/times when the session occurred (MySQL format; UTC).'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item_distribute:
                        type: array
                        description: 'A list of distributed new shared purchase items which are selected by a group of clients. Keys are UIDs of clients, values are arrays with the following structure: <dl> <dt>string[] `a_owner`</dt> <dd> List of UIDs of owners who will share this promotion with this client. If empty, this client is the owner of the promotion. This only applies to the new promotions. <i>Each UID is a primary key in the {@link \PassportLoginSql} table.</i> </dd> <dt>int `i_session`</dt> <dd> The number of sessions that this item can cover. The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_purchase_item_check}. `0` if not applicable. </dd> <dt>string|null `k_login_promotion`</dt> <dd> Login promotion key. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i> The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_login_promotion}. `null` if not applicable. </dd> <dt>string|null `k_login_promotion_guest_pass`</dt> <dd> Host login promotion key that grants the guest pass. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i> `null` if not applicable. </dd> <dt>string|null `k_session_pass`</dt> <dd> Session pass key. <i>Primary key in the {@link \Wl\Session\Pass\Sql} table.</i> The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_session_pass}. `null` if not applicable. </dd> <dt>string|null `s_value`</dt> <dd> The unique identifier of the purchase item. The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_purchase_item_check}. `null` if not applicable. </dd> <dt>string `text_error`</dt> <dd>Error text if the purchase item cannot be applied to the current class or event for this client.</dd> <dt>string `text_error_code`</dt> <dd>Error code if the purchase item cannot be applied to the current class or event for this client.</dd> </dl>'
      tags:
        - Store-StoreGroup
  '/Wl/Book/Process/Purchase/PurchaseElement.json':
    get:
      summary: 'Builds a single-item purchase list from the scalar input fields and delegates to the parent for price calculation. Constructs a one-element `a_purchase_item` array from the individual scalar GET fields (purchase item type, item key, session count, prize keys, and installment template), then delegates to {@link PurchaseElementGroupApi::get()} to compute the price breakdown for that item.'
      parameters:
        - name: a_config
          in: query
          description: 'Additional configuration for the purchase item. Depending on purchase item type different configuration parameters can be passed. For purchase items with {@link \RsPurchaseItemSid::TUITION} type only `a_event_list` is expected in the request, and only `k_class` and `uid` are accepted in every its entry. Prices, discounts, and taxes can not be overridden here: this booking flow is never authenticated as a staff member, so such fields are stripped from the input. In the response this is not an echo: `a_event_list` comes back recomputed, and `a_registration_fee_list`, `m_checkout`, and `m_deferred` are added. <dl> <dt>array[] `a_event_list`</dt> <dd> List of tuition events, one entry per participant and event class. Each entry has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the event, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[]|null `a_tax`</dt> <dd> Taxes of the event. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this event right now, including tax. `0.00` when every installment payment is still ahead. `null` when the amount is not calculated yet. </dd> <dt>string `m_deferred`</dt> <dd> The part of the event cost that is not charged right now, including tax. Goes to the installment plan or to the membership schedule, depending on the tuition billing mode. </dd> <dt>string `m_discount`</dt> <dd>Total discount amount applied to the event, `0.00` if there is none.</dd> <dt>string|null `m_price`</dt> <dd>Price of the event within the tuition, before discount and tax.</dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>array[] `a_registration_fee_list`</dt> <dd> Registration fees, keyed by participant key. <i>Primary key in {@link \PassportLoginSql} table.</i> Filled by the server, never accepted from the caller in this flow. Each value has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the fee, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[] `a_tax`</dt> <dd> Taxes of the fee. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the participant, before discount and tax.</dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this fee right now, including tax. A fee is either charged in full or deferred entirely, so `null` means the whole fee is deferred. </dd> <dt>string `m_deferred`</dt> <dd>The whole fee amount if the fee is deferred, `0.00` if it is charged right now.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd> The total amount charged for the tuition right now, including tax. The sum of `m_checkout` of every event and every registration fee. </dd> <dt>string `m_deferred`</dt> <dd> The total amount that is not charged right now, including tax. Together with `m_checkout` it adds up to the full cost of the tuition. </dd> </dl>'
          schema:
            type: array
        - name: dtu_date
          in: query
          description: 'Date/time of session is booking. `null` until initialized.'
          schema:
            type: [string, "null"]
        - name: i_session
          in: query
          description: 'The number of sessions which are booked simultaneously. Required when {@link PurchaseElementApi::$id_purchase_item} = {@link \RsPurchaseItemSid::CLASS_PERIOD}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The ID of the purchase item type. One of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: k_class_period
          in: query
          description: 'The key of the session to check for booking availability.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The key of the purchase item in the database.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location in which the purchase is made. This is also the booking process location.'
          schema:
            type: string
        - name: k_login_prize
          in: query
          description: 'The key of the user''s prize. Not empty only if the user wants to make a free visit from the prize.'
          schema:
            type: string
        - name: k_pay_installment_template
          in: query
          description: 'Installment template key. This property is optional. * can only be set for the purchase option which supports installment plan. <i>See {@link PurchaseItemAbstract::INSTALLMENT_ALLOW_USER} property.</i> * `null` if installment plan doesn''t exist for bought item; * `0` if installment plan doesn''t selected for bought item from the list of installment plans. NOTE: * Calculations of discounts and taxes for installment plans are for demonstration purposes only! * Installment is not an independent purchase item and has no discounts or taxes. * Installment is a division of the final amount (with taxes and discounts), of some purchase option, into N parts.'
          schema:
            type: [string, "null"]
        - name: k_reward_prize
          in: query
          description: 'The key of the reward prize. Not empty only if the user wants to redeem prize and use it to pay for a visit.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_config:
                        type: array
                        description: 'Additional configuration for the purchase item. Depending on purchase item type different configuration parameters can be passed. For purchase items with {@link \RsPurchaseItemSid::TUITION} type only `a_event_list` is expected in the request, and only `k_class` and `uid` are accepted in every its entry. Prices, discounts, and taxes can not be overridden here: this booking flow is never authenticated as a staff member, so such fields are stripped from the input. In the response this is not an echo: `a_event_list` comes back recomputed, and `a_registration_fee_list`, `m_checkout`, and `m_deferred` are added. <dl> <dt>array[] `a_event_list`</dt> <dd> List of tuition events, one entry per participant and event class. Each entry has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the event, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[]|null `a_tax`</dt> <dd> Taxes of the event. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this event right now, including tax. `0.00` when every installment payment is still ahead. `null` when the amount is not calculated yet. </dd> <dt>string `m_deferred`</dt> <dd> The part of the event cost that is not charged right now, including tax. Goes to the installment plan or to the membership schedule, depending on the tuition billing mode. </dd> <dt>string `m_discount`</dt> <dd>Total discount amount applied to the event, `0.00` if there is none.</dd> <dt>string|null `m_price`</dt> <dd>Price of the event within the tuition, before discount and tax.</dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>array[] `a_registration_fee_list`</dt> <dd> Registration fees, keyed by participant key. <i>Primary key in {@link \PassportLoginSql} table.</i> Filled by the server, never accepted from the caller in this flow. Each value has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the fee, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[] `a_tax`</dt> <dd> Taxes of the fee. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the participant, before discount and tax.</dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this fee right now, including tax. A fee is either charged in full or deferred entirely, so `null` means the whole fee is deferred. </dd> <dt>string `m_deferred`</dt> <dd>The whole fee amount if the fee is deferred, `0.00` if it is charged right now.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd> The total amount charged for the tuition right now, including tax. The sum of `m_checkout` of every event and every registration fee. </dd> <dt>string `m_deferred`</dt> <dd> The total amount that is not charged right now, including tax. Together with `m_checkout` it adds up to the full cost of the tuition. </dd> </dl>'
                      a_tax:
                        type: array
                        description: 'A list of taxes for the given purchase options. Keys - tax keys, values - tax amounts.'
                      m_checkout:
                        type: string
                        description: 'The amount that has to be charged right now for the given purchase options. Equals {@link PurchaseElementGroupApi::$m_cost} for everything that is paid for in full at once. A tuition defers a part of its cost to an installment plan or to a membership schedule, and `m_cost` covers the full cost regardless, so this is the amount to put into the payment form. It is `m_cost` minus `a_config.m_deferred` of every tuition item in {@link PurchaseElementGroupApi::$a_purchase_item}.'
                      m_checkout_tax:
                        type: string
                        description: 'The tax portion of {@link PurchaseElementGroupApi::$m_checkout}. Equals {@link PurchaseElementGroupApi::$m_tax} for everything that is paid for in full at once. A tuition defers a part of its tax to an installment plan along with the rest of its cost, so this is the tax on the amount actually charged right now, not on the full cost.'
                      m_cost:
                        type: string
                        description: 'The total cost of the given purchase options. For a tuition this is the full cost, including whatever is deferred to an installment plan or to a membership schedule. <i>See {@link PurchaseElementGroupApi::$m_checkout} for the amount that is charged right now.</i>'
                      m_discount:
                        type: string
                        description: 'The amount of the whole discount for the given purchase options.'
                      m_discount_code:
                        type: string
                        description: 'The discount amount provided by a discount code for the given purchase options.'
                      m_discount_login:
                        type: string
                        description: 'The discount amount for the client type for the given purchase options.'
                      m_price:
                        type: string
                        description: 'The price of the given purchase options (with or without taxes, depending on regional standards).'
                      m_subtotal:
                        type: string
                        description: 'The amount of subtotal for the given purchase options.'
                      m_tax:
                        type: string
                        description: 'The amount of taxes for the given purchase options.'
      tags:
        - Purchase-PurchaseElement
  '/Wl/Book/Process/Purchase/PurchaseElementGroup.json':
    get:
      summary: 'Returns the pricing breakdown (totals, taxes, and discounts) for the given list of purchase items. Validates each item in `a_purchase_item` (type, key, installment eligibility, and prize applicability), applies discount codes, login-type discounts, and installment adjustments, then accumulates price, subtotal, discount, tax, and cost totals across all items and returns them as result fields. For tuition items the totals cover the full cost, so the amount that has to be charged right now, and the tax portion of it, are reported separately in {@link PurchaseElementGroupApi::$m_checkout} and {@link PurchaseElementGroupApi::$m_checkout_tax}, and the per-component split is written back into `a_config` of the item it belongs to.'
      parameters:
        - name: a_purchase_item
          in: query
          description: 'A list of purchase items. Each item is an associative array with the following keys: <dl> <dt>array `a_config`</dt> <dd> Additional item configurations. <i>Used only for {@link \RsPurchaseItemSid::TUITION}.</i> Only `a_event_list` is expected in the request, and only `k_class` and `uid` are accepted in every its entry. Prices, discounts, and taxes can not be overridden here: this booking flow is never authenticated as a staff member, so such fields are stripped from the input. In the response `a_event_list` comes back recomputed, and `a_registration_fee_list`, `m_checkout`, and `m_deferred` are added. <dl> <dt>array[] `a_event_list`</dt> <dd> List of tuition events, one entry per participant and event class. Each entry has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the event, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[]|null `a_tax`</dt> <dd> Taxes of the event. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this event right now, including tax. `0.00` when every installment payment is still ahead. `null` when it is not calculated yet. </dd> <dt>string `m_deferred`</dt> <dd> The part of the event cost that is not charged right now, including tax. Goes to the installment plan or to the membership schedule, depending on the tuition billing mode. </dd> <dt>string `m_discount`</dt> <dd>Total discount amount applied to the event, `0.00` if there is none.</dd> <dt>string|null `m_price`</dt> <dd>Price of the event within the tuition, before discount and tax.</dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>array[] `a_registration_fee_list`</dt> <dd> Registration fees, keyed by participant key. <i>Primary key in {@link \PassportLoginSql} table.</i> Filled by the server, never accepted from the caller in this flow. Each value has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the fee, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[] `a_tax`</dt> <dd> Taxes of the fee. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the participant, before discount and tax.</dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this fee right now, including tax. A fee is either charged in full or deferred entirely, so `null` means it is fully deferred. </dd> <dt>string `m_deferred`</dt> <dd>The whole fee amount if the fee is deferred, `0.00` if it is charged now.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd> The amount charged for this tuition right now, including tax. The sum of `m_checkout` of every its event and registration fee. <i>See {@link \Wl\Book\Process\Purchase\PurchaseElementGroupApi::$m_checkout} for the amount to charge for the whole request.</i> </dd> <dt>string `m_deferred`</dt> <dd> The amount that is not charged right now, including tax. Together with `m_checkout` it adds up to the full cost of this tuition. </dd> </dl> </dd> <dt>int `i_session`</dt> <dd> Number of sessions which are booked simultaneously. Make sense only when `id_purchase_item` = {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item type. One of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize. Not empty only if the user wants to make a free visit from the prize.</dd> <dt>string|null `k_pay_installment_template`</dt> <dd> Installment template key. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> This property is optional. * can only be set for the purchase option which supports installment plan. <i>See {@link \Wl\Purchase\Item\PurchaseItemAbstract::INSTALLMENT_ALLOW_USER} property.</i> * `null` if installment plan doesn''t exist for bought item; * `0` if installment plan doesn''t selected for bought item from the list of installment plans. NOTE: * Calculations of discounts and taxes for installment plans are for demonstration purposes only! * Installment is not an independent purchase item and has no discounts or taxes. * Installment is a division of the final amount (with taxes and discounts), of some purchase option, into N parts. </dd> <dt>string `k_reward_prize`</dt> <dd> The key of the reward prize. Not empty only if the user wants to redeem prize and use it to pay for a visit. </dd> <dt>string `uid`</dt> <dd>The key of the user for whom the purchase item is being bought.</dd> </dl>'
          schema:
            type: array
        - name: dtu_date
          in: query
          description: 'Date/time of session is booking. `null` until initialized.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The key of the session to check for booking availability.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location in which the purchase is made. This is also the booking process location.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item:
                        type: array
                        description: 'A list of purchase items. Each item is an associative array with the following keys: <dl> <dt>array `a_config`</dt> <dd> Additional item configurations. <i>Used only for {@link \RsPurchaseItemSid::TUITION}.</i> Only `a_event_list` is expected in the request, and only `k_class` and `uid` are accepted in every its entry. Prices, discounts, and taxes can not be overridden here: this booking flow is never authenticated as a staff member, so such fields are stripped from the input. In the response `a_event_list` comes back recomputed, and `a_registration_fee_list`, `m_checkout`, and `m_deferred` are added. <dl> <dt>array[] `a_event_list`</dt> <dd> List of tuition events, one entry per participant and event class. Each entry has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the event, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[]|null `a_tax`</dt> <dd> Taxes of the event. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this event right now, including tax. `0.00` when every installment payment is still ahead. `null` when it is not calculated yet. </dd> <dt>string `m_deferred`</dt> <dd> The part of the event cost that is not charged right now, including tax. Goes to the installment plan or to the membership schedule, depending on the tuition billing mode. </dd> <dt>string `m_discount`</dt> <dd>Total discount amount applied to the event, `0.00` if there is none.</dd> <dt>string|null `m_price`</dt> <dd>Price of the event within the tuition, before discount and tax.</dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>array[] `a_registration_fee_list`</dt> <dd> Registration fees, keyed by participant key. <i>Primary key in {@link \PassportLoginSql} table.</i> Filled by the server, never accepted from the caller in this flow. Each value has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the fee, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[] `a_tax`</dt> <dd> Taxes of the fee. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the participant, before discount and tax.</dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this fee right now, including tax. A fee is either charged in full or deferred entirely, so `null` means it is fully deferred. </dd> <dt>string `m_deferred`</dt> <dd>The whole fee amount if the fee is deferred, `0.00` if it is charged now.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd> The amount charged for this tuition right now, including tax. The sum of `m_checkout` of every its event and registration fee. <i>See {@link \Wl\Book\Process\Purchase\PurchaseElementGroupApi::$m_checkout} for the amount to charge for the whole request.</i> </dd> <dt>string `m_deferred`</dt> <dd> The amount that is not charged right now, including tax. Together with `m_checkout` it adds up to the full cost of this tuition. </dd> </dl> </dd> <dt>int `i_session`</dt> <dd> Number of sessions which are booked simultaneously. Make sense only when `id_purchase_item` = {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item type. One of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize. Not empty only if the user wants to make a free visit from the prize.</dd> <dt>string|null `k_pay_installment_template`</dt> <dd> Installment template key. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> This property is optional. * can only be set for the purchase option which supports installment plan. <i>See {@link \Wl\Purchase\Item\PurchaseItemAbstract::INSTALLMENT_ALLOW_USER} property.</i> * `null` if installment plan doesn''t exist for bought item; * `0` if installment plan doesn''t selected for bought item from the list of installment plans. NOTE: * Calculations of discounts and taxes for installment plans are for demonstration purposes only! * Installment is not an independent purchase item and has no discounts or taxes. * Installment is a division of the final amount (with taxes and discounts), of some purchase option, into N parts. </dd> <dt>string `k_reward_prize`</dt> <dd> The key of the reward prize. Not empty only if the user wants to redeem prize and use it to pay for a visit. </dd> <dt>string `uid`</dt> <dd>The key of the user for whom the purchase item is being bought.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'A list of taxes for the given purchase options. Keys - tax keys, values - tax amounts.'
                      m_checkout:
                        type: string
                        description: 'The amount that has to be charged right now for the given purchase options. Equals {@link PurchaseElementGroupApi::$m_cost} for everything that is paid for in full at once. A tuition defers a part of its cost to an installment plan or to a membership schedule, and `m_cost` covers the full cost regardless, so this is the amount to put into the payment form. It is `m_cost` minus `a_config.m_deferred` of every tuition item in {@link PurchaseElementGroupApi::$a_purchase_item}.'
                      m_checkout_tax:
                        type: string
                        description: 'The tax portion of {@link PurchaseElementGroupApi::$m_checkout}. Equals {@link PurchaseElementGroupApi::$m_tax} for everything that is paid for in full at once. A tuition defers a part of its tax to an installment plan along with the rest of its cost, so this is the tax on the amount actually charged right now, not on the full cost.'
                      m_cost:
                        type: string
                        description: 'The total cost of the given purchase options. For a tuition this is the full cost, including whatever is deferred to an installment plan or to a membership schedule. <i>See {@link PurchaseElementGroupApi::$m_checkout} for the amount that is charged right now.</i>'
                      m_discount:
                        type: string
                        description: 'The amount of the whole discount for the given purchase options.'
                      m_discount_code:
                        type: string
                        description: 'The discount amount provided by a discount code for the given purchase options.'
                      m_discount_login:
                        type: string
                        description: 'The discount amount for the client type for the given purchase options.'
                      m_price:
                        type: string
                        description: 'The price of the given purchase options (with or without taxes, depending on regional standards).'
                      m_subtotal:
                        type: string
                        description: 'The amount of subtotal for the given purchase options.'
                      m_tax:
                        type: string
                        description: 'The amount of taxes for the given purchase options.'
      tags:
        - Purchase-PurchaseElementGroup
  '/Wl/Book/Process/Purchase/PurchaseElementList.json':
    get:
      summary: 'Returns the pricing breakdown for a list of purchase items, applying applicable discounts and taxes. Validates the business, location, and user, then for each item in `a_purchase_item_request` computes the price, applicable discount code reduction, login-type discount, and taxes, and returns per-item cost, discount, price, tax, and subtotal amounts in `a_purchase_item_result`. For a tuition the amounts cover the full cost, and the split between what is charged right now and what is deferred is added to the same row.'
      parameters:
        - name: a_purchase_item_request
          in: query
          description: 'A list of purchase items to get information for. Every element has the next keys: <dl> <dt>array `a_config`</dt> <dd> Additional configuration. <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events. <i>Used only for {@link \RsPurchaseItemSid::TUITION}.</i> Each entry has the next structure: <dl> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> </dl> </dd> <dt>int `i_session`</dt> <dd>The number of sessions booked simultaneously.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd> The key of the purchase item in the database. The name of the table in the database depends on <var>id_purchase_item</var> </dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The key of the business in which the purchase is made.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location in which the purchase is made. This is also the booking process location. This will be `null` if not set yet or if the location can''t be defined for some reason.'
          schema:
            type: [string, "null"]
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user making the purchase.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item_result:
                        type: array
                        description: 'Detailed information about the amounts for the purchase item list. Every element has the next keys: <dl> <dt>array[] `a_event_list`</dt> <dd> Tuition events with calculated amounts. <i>Only for {@link \RsPurchaseItemSid::TUITION}.</i> Each entry has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the event, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[]|null `a_tax`</dt> <dd> Taxes of the event. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this event right now, including tax. `0.00` when every installment payment is still ahead. `null` when the amount is not calculated yet. </dd> <dt>string `m_deferred`</dt> <dd> The part of the event cost that is not charged right now, including tax. Goes to the installment plan or to the membership schedule, depending on the tuition billing mode. </dd> <dt>string `m_discount`</dt> <dd>Total discount amount applied to the event, `0.00` if there is none.</dd> <dt>string|null `m_price`</dt> <dd>Price of the event within the tuition, before discount and tax.</dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>array[] `a_registration_fee_list`</dt> <dd> Registration fees with calculated amounts, keyed by participant key. <i>Primary key in {@link \PassportLoginSql} table.</i> <i>Only for {@link \RsPurchaseItemSid::TUITION}.</i> Each value has the next structure: <dl> <dt>array[]|null `a_discount`</dt> <dd> Discounts applied to the fee, `null` if there are none. Every row has the next keys: <dl> <dt>int `id_discount_rule`</dt> <dd>Discount rule type. One of {@link \Wl\Discount\DiscountRuleSid} constants.</dd> <dt>string `m_discount`</dt> <dd>Discount amount of this rule.</dd> <dt>string `text_discount`</dt> <dd>Discount title. Only for {@link \Wl\Discount\DiscountRuleSid::MANUAL}.</dd> </dl> </dd> <dt>string[] `a_tax`</dt> <dd> Taxes of the fee. Keys are tax keys, values are tax amounts. <i>Primary key in {@link \RsTaxSql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the participant, before discount and tax.</dd> <dt>string|null `m_checkout`</dt> <dd> The amount charged for this fee right now, including tax. A fee is either charged in full or deferred entirely, so `null` means the whole fee is deferred. </dd> <dt>string `m_deferred`</dt> <dd>The whole fee amount if the fee is deferred, `0.00` if it is charged right now.</dd> </dl> </dd> <dt>string[] `a_tax`</dt> <dd>Information about taxes. The key refers to the tax key, and the value refers to the tax amount.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of purchase item type. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `m_checkout`</dt> <dd> The amount that has to be charged for the tuition right now, including tax. The other amounts of this row cover the full cost, including whatever is deferred to an installment plan or to a membership schedule. <i>Only for {@link \RsPurchaseItemSid::TUITION}.</i> </dd> <dt>string `m_deferred`</dt> <dd> The part of the tuition cost that is not charged right now, including tax. Equals `m_cost` minus `m_checkout`. Rows for everything else are always paid for in full at once, so the amount to charge for the whole list is the sum of `m_cost` minus the sum of `m_deferred`. <i>Only for {@link \RsPurchaseItemSid::TUITION}.</i> </dd> <dt>string `m_cost`</dt> <dd>The cost of the purchase item (with taxes).</dd> <dt>string `m_discount`</dt> <dd>The amount of the whole discount.</dd> <dt>string `m_discount_login`</dt> <dd>The amount of the discount for the client type.</dd> <dt>string `m_price`</dt> <dd>The price of the purchase item (with or without taxes, depending on regional standards).</dd> <dt>string `m_tax`</dt> <dd>The amount of taxes for the purchase item.</dd> </dl>'
      tags:
        - Purchase-PurchaseElementList
  '/Wl/Book/Process/Purchase/Purchase.json':
    get:
      summary: 'Returns available purchase options, existing client promotions, session passes, and prizes for the specified session. Loads all purchase options that can be used to book the specified class period for the given user, including existing login promotions with remaining session counts, new purchasable options, reward prizes, and login prizes. Returns pricing, image, program category, and availability data for each option.'
      parameters:
        - name: a_login_promotion_group
          in: query
          description: 'A list of existing purchase options that were selected for previous clients (group). Note: * It makes sense if for all clients the list is loaded within the same pair {@link PurchaseApi::$dt_date_gmt} and {@link PurchaseApi::$k_class_period}. * If promotions are shared, the system will try to determine if there are enough sessions left for the next client who has the same promotion. * A very simple check is carried out based on a comparison of the remaining sessions for the promotion with the number of times it was selected. * Can affect the list of available login promotions {@link PurchaseApi::$a_login_promotion}. If a given client is eligible for such a promotion, but the remaining sessions (minus those previously selected) in it do not allow it to be applied to the selected session, then such promotion will simply not be returned for the client. * The order of clients across all APIs must be the same to guarantee their results. Each element has the following structure: <dl> <dt>int `i_session`</dt> <dd> Number of paid sessions of the same class|event that were selected for the previous user. This number should not include free or waitlist unpaid sessions. </dd> <dt>string `k_login_promotion`</dt> <dd>Selected purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `uid`</dt> <dd>UID of the previous user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: a_session
          in: query
          description: 'The list of sessions being booked. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: a_session_wait_list_unpaid
          in: query
          description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_card_authorize
          in: query
          description: 'Determines if the client must authorize the credit card.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. `null` if business key was not passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. Each element has the following fields: <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option with the following information: <dl> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The number of the remaining visits the Purchase Option has left.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in the Purchase Option.</dd> </dl> </dd> <dt>string[] `a_uid_share`</dt> <dd> List of UIDs of users who share this promotion. List of those passed in the {@link \Wl\Book\Process\Purchase\PurchaseApi::$a_login_promotion_group} array. </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the promotion (for example, 4 per week).</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The number of future sessions paid using this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The Purchase Option''s usage count.</dd> <dt>int `i_visit_past`</dt> <dd> The number of attended sessions before the last renewal. This will be `0` if there aren''t any sessions before the last renewal or the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for Purchase Options. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the promotion converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_shared`</dt> <dd>`true` if the promotion is shared with the client, `false` if the client is owner of the promotion.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new promotion instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package, this field will contain a list of Purchase Options contained in the package.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'A list of Purchase Options that are available for the session(s) being booked. Keys refer to unique string IDs, and values refer arrays with the next fields: <dl> <dt>array[] `a_installment_template`</dt> <dd> A list of installment plans. Every element has the next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by <var>id_period</var> between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl> </dd> <dt>array[] `a_visit_limit`</dt> <dd> This is only set for Purchase Options. A list of limits on booking by the Purchase Option. Every element has the next keys: <dl> <dt>string `s_title`</dt> <dd>The limit description.</dd> </dl> </dd> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date.</dd> <dt>string `f_price`</dt> <dd>The price.</dd> <dt>string `f_price_early`</dt> <dd>The price for early bookings.</dd> <dt>string `html_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var> in short form. </dd> <dt>string `html_description`</dt> <dd>The description, ready to paste in a browser.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked by Purchase Options.</dd> <dt>int `i_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The duration of the regular payment interval. </dd> <dt>int `i_session`</dt> <dd>This is only set for purchases of single sessions. The number of sessions booked simultaneously.</dd> <dt>int `id_program_category`</dt> <dd> This is only set for promotions. The ID of the promotion program category. One of the {@link \RsProgramCategorySid} constants. </dd> <dt>int `id_program_type`</dt> <dd> This is only set for promotions. The ID of the promotion program type. One of the {@link \RsProgramTypeSid} constants. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_contract`</dt> <dd>If `true`, the Purchase Option requires a contract assignment. Otherwise, this will be `false`.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the Purchase Option converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_renew`</dt> <dd>If `true`, the Purchase Option is renewable. Otherwise, this will be `false`.</dd> <dt>bool `is_renew_check`</dt> <dd> If `true`, the Purchase Option is renewable and the "auto-renew" option is turned on by default. Otherwise, this will be `false`. </dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `k_reward_prize`</dt> <dd>The key of the reward prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `m_prorate`</dt> <dd>Payment for membership prorate. Not empty only if prorate payment is required.</dd> <dt>string `s_contract`</dt> <dd>The contract of the Purchase Option. This is only set if <var>is_contract</var> is `true`.</dd> <dt>string `s_payment_duration`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var>. </dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new Purchase Option instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The title.</dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. Each element has the following fields: <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'The list of session passes that might be used in booking process. Each element has the following fields: <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if the drop-in rate should be the default promotion.'
                      k_promotion_default:
                        type: string
                        description: 'The default Purchase Option key. This will be empty if the class has no default Purchase Option.'
      tags:
        - Purchase-Purchase
  '/Wl/Book/Process/Purchase/Purchase56.json':
    get:
      summary: 'Returns available purchase options for the specified session, decoding JSON-encoded group promotion and session inputs. Deserializes the JSON-encoded group login-promotion list and session selection, then delegates to the parent to return the purchase options available for the current client and session.'
      parameters:
        - name: a_login_promotion_group
          in: query
          description: 'A list of existing purchase options that were selected for previous clients (group). Note: * It makes sense if for all clients the list is loaded within the same pair {@link PurchaseApi::$dt_date_gmt} and {@link PurchaseApi::$k_class_period}. * If promotions are shared, the system will try to determine if there are enough sessions left for the next client who has the same promotion. * A very simple check is carried out based on a comparison of the remaining sessions for the promotion with the number of times it was selected. * Can affect the list of available login promotions {@link PurchaseApi::$a_login_promotion}. If a given client is eligible for such a promotion, but the remaining sessions (minus those previously selected) in it do not allow it to be applied to the selected session, then such promotion will simply not be returned for the client. * The order of clients across all APIs must be the same to guarantee their results. Each element has the following structure: <dl> <dt>int `i_session`</dt> <dd> Number of paid sessions of the same class|event that were selected for the previous user. This number should not include free or waitlist unpaid sessions. </dd> <dt>string `k_login_promotion`</dt> <dd>Selected purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `uid`</dt> <dd>UID of the previous user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: a_session
          in: query
          description: 'The list of sessions being booked. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: a_session_wait_list_unpaid
          in: query
          description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_card_authorize
          in: query
          description: 'Determines if the client must authorize the credit card.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: json_login_promotion_group
          in: query
          description: 'A list of existing purchase options that were selected for previous clients (group). Note: * It makes sense if for all clients the list is loaded within the same pair {@link Purchase56Api::$dt_date_gmt} and {@link Purchase56Api::$k_class_period}. * If promotions are shared, the system will try to determine if there are enough sessions left for the next client who has the same promotion. * A very simple check is made based on a comparison of the remaining sessions for the promotion with the number of times it was selected. * Can affect the list of available login promotions {@link Purchase56Api::$a_login_promotion}. If a given client is eligible for such a promotion, but the remaining sessions (minus those previously selected) in it do not allow it to be applied to the selected session, then such promotion will simply not be returned for the client. * The order of clients across all APIs must be the same to guarantee their results. Serialized with JSON. Each element has the following structure: <dl> <dt>int `i_session`</dt> <dd> Number of paid sessions of the same class|event that were selected for the previous user. This number should not include free or waitlist unpaid sessions. </dd> <dt>string `k_login_promotion`</dt> <dd>Selected purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `uid`</dt> <dd>UID of the previous user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: json_session
          in: query
          description: 'The selected sessions. This won''t be empty for session mode only. Fields refer to IDs of sessions in the database. Values refer to arrays of dates/times when session occurred, returned in MySQL format and in UTC. Serialized with JSON.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key. `null` if business key was not passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. Each element has the following fields: <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option with the following information: <dl> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The number of the remaining visits the Purchase Option has left.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in the Purchase Option.</dd> </dl> </dd> <dt>string[] `a_uid_share`</dt> <dd> List of UIDs of users who share this promotion. List of those passed in the {@link \Wl\Book\Process\Purchase\PurchaseApi::$a_login_promotion_group} array. </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the promotion (for example, 4 per week).</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The number of future sessions paid using this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The Purchase Option''s usage count.</dd> <dt>int `i_visit_past`</dt> <dd> The number of attended sessions before the last renewal. This will be `0` if there aren''t any sessions before the last renewal or the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for Purchase Options. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the promotion converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_shared`</dt> <dd>`true` if the promotion is shared with the client, `false` if the client is owner of the promotion.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new promotion instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package, this field will contain a list of Purchase Options contained in the package.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'A list of Purchase Options that are available for the session(s) being booked. Keys refer to unique string IDs, and values refer arrays with the next fields: <dl> <dt>array[] `a_installment_template`</dt> <dd> A list of installment plans. Every element has the next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by <var>id_period</var> between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl> </dd> <dt>array[] `a_visit_limit`</dt> <dd> This is only set for Purchase Options. A list of limits on booking by the Purchase Option. Every element has the next keys: <dl> <dt>string `s_title`</dt> <dd>The limit description.</dd> </dl> </dd> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date.</dd> <dt>string `f_price`</dt> <dd>The price.</dd> <dt>string `f_price_early`</dt> <dd>The price for early bookings.</dd> <dt>string `html_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var> in short form. </dd> <dt>string `html_description`</dt> <dd>The description, ready to paste in a browser.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked by Purchase Options.</dd> <dt>int `i_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The duration of the regular payment interval. </dd> <dt>int `i_session`</dt> <dd>This is only set for purchases of single sessions. The number of sessions booked simultaneously.</dd> <dt>int `id_program_category`</dt> <dd> This is only set for promotions. The ID of the promotion program category. One of the {@link \RsProgramCategorySid} constants. </dd> <dt>int `id_program_type`</dt> <dd> This is only set for promotions. The ID of the promotion program type. One of the {@link \RsProgramTypeSid} constants. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_contract`</dt> <dd>If `true`, the Purchase Option requires a contract assignment. Otherwise, this will be `false`.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the Purchase Option converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_renew`</dt> <dd>If `true`, the Purchase Option is renewable. Otherwise, this will be `false`.</dd> <dt>bool `is_renew_check`</dt> <dd> If `true`, the Purchase Option is renewable and the "auto-renew" option is turned on by default. Otherwise, this will be `false`. </dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `k_reward_prize`</dt> <dd>The key of the reward prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `m_prorate`</dt> <dd>Payment for membership prorate. Not empty only if prorate payment is required.</dd> <dt>string `s_contract`</dt> <dd>The contract of the Purchase Option. This is only set if <var>is_contract</var> is `true`.</dd> <dt>string `s_payment_duration`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var>. </dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new Purchase Option instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The title.</dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. Each element has the following fields: <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'The list of session passes that might be used in booking process. Each element has the following fields: <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if the drop-in rate should be the default promotion.'
                      k_promotion_default:
                        type: string
                        description: 'The default Purchase Option key. This will be empty if the class has no default Purchase Option.'
      tags:
        - Purchase-Purchase56
  '/Wl/Book/Process/Info/Info.json':
    get:
      summary: 'Returns class session details and booking information for the "Class and Location" step of the booking wizard. Loads session data, staff, location, contract, capacity, wait-list state, pricing, special instructions, and recurring booking availability for the given class period and date. For events it also returns the list of individually selectable sessions together with their free-session eligibility.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_class_capacity
          in: query
          description: '`true` if class capacity should be shown, `false` to use business setting. <i>See {@link \RsBusinessDesign::data()}`[is_class_capacity]`.</i>'
          schema:
            type: boolean
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_day_available:
                        type: [array, "null"]
                        description: 'Week days available for recurring booking. Constants of {@link \ADateWeekSid} class. `null` if recurring booking is not available.'
                      a_session_all:
                        type: array
                        description: 'A list of all class sessions that can be booked together. Every element has the next structure: <dl> <dt>string[] `a_staff`</dt> <dd>List of staff names that are leading this session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations.</dd> <dt>string `dt_date`</dt> <dd>The date/time when session starts in MySQL format and in GMT.</dd> <dt>int `i_active`</dt> <dd>Total number of clients on the active list.</dd> <dt>int `i_active_limit`</dt> <dd>Total capacity of the active list.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_wait`</dt> <dd>Total number of clients on the wait list.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if this session should be selected when page is initialized; <tt>false</tt> if otherwise.</dd> <dt>bool `is_wait`</dt> <dd>`true` if client is added to a wait list, `false` - to an active list.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>Allow clients to join the wait list unpaid.</dd> <dt>string `k_class_period`</dt> <dd>The key of the session. <i>Primary keys in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `m_price`</dt> <dd>Price of the session.</dd> <dt>string `s_location`</dt> <dd>The name of the location where the session occurred.</dd> <dt>string `s_start`</dt> <dd>The date/time when the session starts in human-readable format. Returned in the time zone of the location.</dd> <dt>string `text_duration`</dt> <dd> String representation of session duration. <i>Duration formatting method {@link \Wl\Book\Process\Info\InfoApi::_classDurationFormat()}.</i> </dd> </dl>'
                      a_session_free:
                        type: array
                        description: 'List of sessions that can be paid without new purchases. Such as previously prepaid or free sessions. <i><A result of the {@link \RsBookProcess::sessionFreeGet()} method execution.</i> Each its item has the key of following format: `dt_date::k_class_period` and the value of following structure: <dl> <dt>string `dt_date`</dt> <dd>Session date.</dd> <dt>string `k_class_period`</dt> <dd>Class period key for the session.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'The staff member conducting the session. Every element has the next structure: <dl> <dt>array `a_logo`</dt> <dd> The staff member photo: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl> </dd> <dt>string `s_family`</dt> <dd>The first letter of staff member surname.</dd> <dt>string `s_staff`</dt> <dd>The staff member''s name.</dd> <dt>string `uid_staff`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `uid`</dt> <dd>Alias of `uid_staff` for backward compatibility.</dd> </dl>'
                      dl_end:
                        type: string
                        description: 'Date when this class session occurrences stop.'
                      dt_date_local:
                        type: string
                        description: 'The date/time of the session the user is booking in MySQL format in the location''s timezone.'
                      hide_price:
                        type: boolean
                        description: '`true` if price for the individual session should be hidden, if client has applicable pricing option to pay for this booking. `false` if price should be shown always.'
                      html_contract:
                        type: string
                        description: 'The text of the contract to which the user must agree before book this session. Not empty if business has contract and if user did not agree to this contract.'
                      html_duration:
                        type: string
                        description: 'Class duration in human-readable format.'
                      html_special:
                        type: string
                        description: 'The special instructions for the class.'
                      html_special_preview:
                        type: string
                        description: 'Special instructions preview for class.'
                      i_available:
                        type: [number, "null"]
                        description: 'Number of available spots. `null` if this information is not available.'
                      i_book:
                        type: [number, "null"]
                        description: 'Number of booked spots. `null` if this information is not available.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The duration of the session in minutes.'
                      i_session_all:
                        type: number
                        format: int32
                        description: 'Total number of sessions. For classes this value is always `1`. For events this value includes both past and future sessions.'
                      i_wait:
                        type: number
                        format: int32
                        description: 'Total number of clients on the wait list.'
                      i_wait_limit:
                        type: [number, "null"]
                        description: 'Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'Estimated place of reservation on the waiting list.'
                      is_book_repeat_client:
                        type: boolean
                        description: '`true` if recurring booking is available, `false` otherwise.'
                      is_book_repeat_no_end_date:
                        type: boolean
                        description: '`true` if the setting for frequency during class recurring booking will be `never end` by default, `false` otherwise.'
                      is_event_session:
                        type: boolean
                        description: 'Can client chooses several session per booking.'
                      is_location_phone:
                        type: boolean
                        description: '`true` if need to display location phone number, `false` otherwise.'
                      is_promotion_only:
                        type: boolean
                        description: '`true` if event can be paid with pricing option only. `false` if full event purchase or single session purchase are allowed.'
                      is_single_buy:
                        type: boolean
                        description: 'Whether the class can be paid with single session.'
                      is_special_preview:
                        type: boolean
                        description: 'Whether the full text of the special instructions fits within the preview length or not.'
                      is_virtual:
                        type: boolean
                        description: '`true` if class is virtual, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'Class period location key.'
                      m_price:
                        type: string
                        description: 'Price of the session.'
                      m_price_total:
                        type: string
                        description: 'Whole event cost.'
                      m_price_total_early:
                        type: string
                        description: 'Event price at an early discount. An empty string if there is no discount.'
                      s_class:
                        type: string
                        description: 'The class title.'
                      s_location_address:
                        type: string
                        description: 'The location address.'
                      s_location_title:
                        type: string
                        description: 'The location title.'
                      s_time:
                        type: string
                        description: 'The time when the session takes place in the location''s time zone. In format `hh:mm`.'
                      text_location_phone:
                        type: string
                        description: 'Location phone number.'
                      text_room:
                        type: string
                        description: 'Room where session takes place.'
                      text_staff:
                        type: string
                        description: 'Text representation of the list of staffs. List of staff see {@link InfoApi::$a_staff}.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
      tags:
        - Info-Info
    post:
      summary: 'Processes the "Class and Location" step of the booking wizard, optionally recording the waiver agreement and booking the session. Accepts the selected sessions, asset assignments, recurring configuration, and waiver signature, then attempts to book the session immediately when no further payment or resource steps are needed. Returns booking keys, activity keys, and flags indicating whether additional wizard steps are required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Every element has the next structure: <dl> <dt>int `i_index`</dt> <dd>Order number of the asset (maybe from 1 to asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: array
                  description: 'The selected sessions. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'Selected sessions on the waiting list without pay. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of dates/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_agree:
                  type: boolean
                  description: 'Determines if the user has agreed to the liability release. `true` - if  the user has agreed. Otherwise, this will be. `false` - if the user hasn''t agreed or the agreement isn''t required.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
                s_signature:
                  type: string
                  description: 'User signature.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of users'' activity.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      is_card_authorize:
                        type: boolean
                        description: 'If client must authorize credit card.'
                      is_force_book:
                        type: boolean
                        description: 'Can the class/event be booked immediately or not. The verification is based on the search for client''s promotions and other features of the class/event. But it does not take into account the presence of other mandatory steps. Their presence will be indicated by the {@link InfoApi::$is_next} flag.'
                      is_next:
                        type: boolean
                        description: '`true` - next steps of the wizard are needed (for example, to purchase something to book the selected session). `false` - no need for next steps (all that''s needed has already been purchased).'
      tags:
        - Info-Info
  '/Wl/Book/Process/Info/Info54.json':
    get:
      summary: 'Returns class and location booking information, enriched with localized date and time for each session. Delegates to the parent implementation and then appends a localized `html_date_time` string to each available session using the business locale and either the client''s profile timezone or the location timezone.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_class_capacity
          in: query
          description: '`true` if class capacity should be shown, `false` to use business setting. <i>See {@link \RsBusinessDesign::data()}`[is_class_capacity]`.</i>'
          schema:
            type: boolean
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_day_available:
                        type: [array, "null"]
                        description: 'Week days available for recurring booking. Constants of {@link \ADateWeekSid} class. `null` if recurring booking is not available.'
                      a_session_all:
                        type: array
                        description: 'A list of all class sessions that can be booked together. Every element has the next structure: <dl> <dt>string[] `a_staff`</dt> <dd>List of staff names that are leading this session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations.</dd> <dt>string `dt_date`</dt> <dd>The date/time when session starts in MySQL format and in GMT.</dd> <dt>int `i_active`</dt> <dd>Total number of clients on the active list.</dd> <dt>int `i_active_limit`</dt> <dd>Total capacity of the active list.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_wait`</dt> <dd>Total number of clients on the wait list.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if this session should be selected when page is initialized; <tt>false</tt> if otherwise.</dd> <dt>bool `is_wait`</dt> <dd>`true` if client is added to a wait list, `false` - to an active list.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>Allow clients to join the wait list unpaid.</dd> <dt>string `k_class_period`</dt> <dd>The key of the session. <i>Primary keys in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `m_price`</dt> <dd>Price of the session.</dd> <dt>string `s_location`</dt> <dd>The name of the location where the session occurred.</dd> <dt>string `s_start`</dt> <dd>The date/time when the session starts in human-readable format. Returned in the time zone of the location.</dd> <dt>string `text_duration`</dt> <dd> String representation of session duration. <i>Duration formatting method {@link \Wl\Book\Process\Info\InfoApi::_classDurationFormat()}.</i> </dd> </dl>'
                      a_session_free:
                        type: array
                        description: 'List of sessions that can be paid without new purchases. Such as previously prepaid or free sessions. <i><A result of the {@link \RsBookProcess::sessionFreeGet()} method execution.</i> Each its item has the key of following format: `dt_date::k_class_period` and the value of following structure: <dl> <dt>string `dt_date`</dt> <dd>Session date.</dd> <dt>string `k_class_period`</dt> <dd>Class period key for the session.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'The staff member conducting the session. Every element has the next structure: <dl> <dt>array `a_logo`</dt> <dd> The staff member photo: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl> </dd> <dt>string `s_family`</dt> <dd>The first letter of staff member surname.</dd> <dt>string `s_staff`</dt> <dd>The staff member''s name.</dd> <dt>string `uid_staff`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `uid`</dt> <dd>Alias of `uid_staff` for backward compatibility.</dd> </dl>'
                      dl_end:
                        type: string
                        description: 'Date when this class session occurrences stop.'
                      dt_date_local:
                        type: string
                        description: 'The date/time of the session the user is booking in MySQL format in the location''s timezone.'
                      hide_price:
                        type: boolean
                        description: '`true` if price for the individual session should be hidden, if client has applicable pricing option to pay for this booking. `false` if price should be shown always.'
                      html_contract:
                        type: string
                        description: 'The text of the contract to which the user must agree before book this session. Not empty if business has contract and if user did not agree to this contract.'
                      html_duration:
                        type: string
                        description: 'Class duration in human-readable format.'
                      html_special:
                        type: string
                        description: 'The special instructions for the class.'
                      html_special_preview:
                        type: string
                        description: 'Special instructions preview for class.'
                      i_available:
                        type: [number, "null"]
                        description: 'Number of available spots. `null` if this information is not available.'
                      i_book:
                        type: [number, "null"]
                        description: 'Number of booked spots. `null` if this information is not available.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The duration of the session in minutes.'
                      i_session_all:
                        type: number
                        format: int32
                        description: 'Total number of sessions. For classes this value is always `1`. For events this value includes both past and future sessions.'
                      i_wait:
                        type: number
                        format: int32
                        description: 'Total number of clients on the wait list.'
                      i_wait_limit:
                        type: [number, "null"]
                        description: 'Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'Estimated place of reservation on the waiting list.'
                      is_book_repeat_client:
                        type: boolean
                        description: '`true` if recurring booking is available, `false` otherwise.'
                      is_book_repeat_no_end_date:
                        type: boolean
                        description: '`true` if the setting for frequency during class recurring booking will be `never end` by default, `false` otherwise.'
                      is_event_session:
                        type: boolean
                        description: 'Can client chooses several session per booking.'
                      is_location_phone:
                        type: boolean
                        description: '`true` if need to display location phone number, `false` otherwise.'
                      is_promotion_only:
                        type: boolean
                        description: '`true` if event can be paid with pricing option only. `false` if full event purchase or single session purchase are allowed.'
                      is_single_buy:
                        type: boolean
                        description: 'Whether the class can be paid with single session.'
                      is_special_preview:
                        type: boolean
                        description: 'Whether the full text of the special instructions fits within the preview length or not.'
                      is_virtual:
                        type: boolean
                        description: '`true` if class is virtual, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'Class period location key.'
                      m_price:
                        type: string
                        description: 'Price of the session.'
                      m_price_total:
                        type: string
                        description: 'Whole event cost.'
                      m_price_total_early:
                        type: string
                        description: 'Event price at an early discount. An empty string if there is no discount.'
                      s_class:
                        type: string
                        description: 'The class title.'
                      s_location_address:
                        type: string
                        description: 'The location address.'
                      s_location_title:
                        type: string
                        description: 'The location title.'
                      s_time:
                        type: string
                        description: 'The time when the session takes place in the location''s time zone. In format `hh:mm`.'
                      text_location_phone:
                        type: string
                        description: 'Location phone number.'
                      text_room:
                        type: string
                        description: 'Room where session takes place.'
                      text_staff:
                        type: string
                        description: 'Text representation of the list of staffs. List of staff see {@link InfoApi::$a_staff}.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
      tags:
        - Info-Info54
    post:
      summary: 'Processes the "Class and Location" step of the booking wizard, optionally recording the waiver agreement and booking the session. Accepts the selected sessions, asset assignments, recurring configuration, and waiver signature, then attempts to book the session immediately when no further payment or resource steps are needed. Returns booking keys, activity keys, and flags indicating whether additional wizard steps are required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Every element has the next structure: <dl> <dt>int `i_index`</dt> <dd>Order number of the asset (maybe from 1 to asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: array
                  description: 'The selected sessions. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'Selected sessions on the waiting list without pay. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of dates/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_agree:
                  type: boolean
                  description: 'Determines if the user has agreed to the liability release. `true` - if  the user has agreed. Otherwise, this will be. `false` - if the user hasn''t agreed or the agreement isn''t required.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
                s_signature:
                  type: string
                  description: 'User signature.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of users'' activity.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      is_card_authorize:
                        type: boolean
                        description: 'If client must authorize credit card.'
                      is_force_book:
                        type: boolean
                        description: 'Can the class/event be booked immediately or not. The verification is based on the search for client''s promotions and other features of the class/event. But it does not take into account the presence of other mandatory steps. Their presence will be indicated by the {@link InfoApi::$is_next} flag.'
                      is_next:
                        type: boolean
                        description: '`true` - next steps of the wizard are needed (for example, to purchase something to book the selected session). `false` - no need for next steps (all that''s needed has already been purchased).'
      tags:
        - Info-Info54
  '/Wl/Book/Process/Info/InfoCanComplete.json':
    get:
      summary: 'Checks whether the user can complete booking wizard without additional steps. Copies the GET session selection into the POST field and delegates to {@link InfoCanCompleteApi::post()}. Prefer the POST variant when the session list may be large, as the query string has length limitations. query string length limitation.'
      parameters:
        - name: a_session_select
          in: query
          description: 'A list of sessions of an event that can be booked together. This is the same as {@link InfoCanCompleteApi::$a_session_select_post}, but limited by query string length and might fail if the user is booking more than 50 sessions at once. Therefore, using the POST method is preferable. <dl> <dt>string `dt_date`</dt> <dd>The date and time in MySQL format in GMT when the session starts.</dd> <dt>bool `is_select`</dt> <dd>`true` if this session should be booked, `false` otherwise.</dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'The date and time of the session that the user is booking, returned in MySQL format and in GMT.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the booking is performed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period ID that the user started to book.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique booking process key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user who is making the booking. Empty if user is a guest.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_complete:
                        type: boolean
                        description: 'Determines whether users can complete the booking process from the info step.'
                      hide_price:
                        type: boolean
                        description: '`true` if price for the individual session should be hidden, if client has applicable pricing option to pay for this booking. `false` if price should be shown always.'
      tags:
        - Info-InfoCanComplete
    post:
      summary: 'Checks whether the user can complete booking wizard without additional steps. Performs a dry-run booking attempt for the given class period, date, and session selection to determine whether the client can finalize the booking directly from the info step without proceeding to the payment or purchase-option steps.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'The date and time of the session that the user is booking, returned in MySQL format and in GMT.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the booking is performed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period ID that the user started to book.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique booking process key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user who is making the booking. Empty if user is a guest.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_session_select_post:
                  type: array
                  description: 'A list of sessions of an event that can be booked together. Every element has the next structure: <dl> <dt>string `dt_date`</dt> <dd>The date and time in MySQL format in GMT when the session starts.</dd> <dt>bool `is_select`</dt> <dd>`true` if this session should be booked, `false` otherwise.</dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_complete:
                        type: boolean
                        description: 'Determines whether users can complete the booking process from the info step.'
      tags:
        - Info-InfoCanComplete
  '/Wl/Book/Process/Payment/Payment.json':
    post:
      summary: 'Processes purchases and books sessions on the "Pay/Billing info" step of the booking wizard. Accepts the selected purchase items, payment form, optional discount code, and installment template, then collects payment, applies discounts and prizes, and creates the booking records. Returns visit keys, activity keys, and purchase activity key upon success.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_item:
                  type: array
                  description: 'A list of items to be bought. Every element has the next keys: <dl> <dt>array `a_config`</dt> <dd> Additional configuration. Used only for `id_purchase_item` = {@link \RsPurchaseItemSid::TUITION}. <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events. Each entry has the next structure: <dl> <dt>string `k_class`</dt> <dd>Key of the event class. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `uid`</dt> <dd>Key of the tuition participant. <i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of purchase item type. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_renew`</dt> <dd> `true` if the item should be set to auto-renew; `false` otherwise. If not set yet, use the default option for this item. </dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `k_login_prize`</dt> <dd> Key of user''s prize. <i>Primary key in table {@link \RsLoginPrizeSql}.</i> Not `0` only if user is paying book by prize. </dd> <dt>string `k_reward_prize`</dt> <dd> Key of reward prize. <i>Primary key in table {@link \RsRewardPrizeSql}.</i> Not `0` only if user wants to redeem prize and use it to pay for visit. </dd> <dt>string `s_signature`</dt> <dd> The signature of the Purchase Option contract. This won''t be set if the Purchase Option doesn''t require a contract assignment. </dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary key from the {@link \Core\Quiz\QuizSql} table.</i> Values are response keys. <i>Primary key from the {@link \Core\Quiz\Response\ResponseSql} table.</i> Or the {@link \Wl\Quiz\Response\QuizResponse::RESPONSE_SKIP} to skip the quiz.'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The number of asset(s). The actual number is returned for assets with a quantity greater than <tt>1</tt>.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session:
                  type: array
                  description: 'A list of sessions being booked. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'Selected sessions on the waiting list without pay. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_card_authorize:
                  type: boolean
                  description: 'If client must authorize credit card.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_pay_installment_template:
                  type: [string, "null"]
                  description: 'The installment template key. This property is optional, and it will be `null` if an installment plan doesn''t exist for the purchased item. This will be `0` if an installment plan isn''t selected for the purchased item from the list of installment plans.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
                text_discount_code:
                  type: string
                  description: 'The discount code to be applied to the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity_book:
                        type: array
                        description: 'Keys of the user''s activity which correspond to bookings made. Not empty when the booking process is finished.'
                      a_visit:
                        type: array
                        description: 'The keys of bookings made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      k_login_activity_purchase:
                        type: string
                        description: 'The keys of the user''s activity corresponding to the purchase made. This won''t be empty when the booking process is finished.'
      tags:
        - Payment-Payment
  '/Wl/Book/Process/Frequency/Repeat.json':
    get:
      summary: 'Returns the list of visits to be created for the given recurring booking settings. Accepts the class period, date, user, recurrence configuration (interval, end condition, and days of week), and returns a list of individual visit slots with availability and alert information for each occurrence, together with the computed date range and visit count.'
      parameters:
        - name: a_day
          in: query
          description: 'List of days of the week to create visits. Each value is a {@link \ADateWeekSid} constant.'
          schema:
            type: array
        - name: a_visit_ignore
          in: query
          description: 'List of visits to be ignored. Each value is a string consisting of a class period key and a visit date and time in location''s timezone, concatenated by two colons. Empty array if no visits should be ignored. Example: [''70::2024-11-05 18:00:00''].'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date and time of the class, when recurring booking was called, in UTC timezone.'
          schema:
            type: string
        - name: dt_from
          in: query
          description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: dt_to
          in: query
          description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: i_count
          in: query
          description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
          schema:
            type: number
            format: int32
        - name: i_duration
          in: query
          description: 'Count of days\weeks\months between recurring bookings.'
          schema:
            type: number
            format: int32
        - name: id_duration
          in: query
          description: 'Recurring booking interval, one of {@link \ADurationSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'WellnessLiving mode, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_repeat_end
          in: query
          description: 'Type of repeating, one of {@link \RsRepeatEndSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_cancel
          in: query
          description: '`true` when cancelling booking, `false` otherwise.'
          schema:
            type: boolean
        - name: is_new_user
          in: query
          description: '`true` if current user is not created yet, `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key.'
          schema:
            type: string
        - name: s_uid
          in: query
          description: 'Encoded list of user keys, who will attend visits.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of user, who will attend visits.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Key of user, who perform booking.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit:
                        type: array
                        description: 'List of visits to be created for the given settings: <dl> <dt>string `dt_date`</dt> <dd>Visit date and time in UTC.</dd> <dt>string `dt_local`</dt> <dd>Visit date and time in location''s time zone.</dd> <dt>string `html_short_name_list`</dt> <dd>List of names of the staff who provide this class.</dd> <dt>string `html_timezone`</dt> <dd>Location''s time zone abbreviation.</dd> <dt>int `i_spot`</dt> <dd>Number of open spots remaining for this session. `0` if the session is full.</dd> <dt>bool `is_disable`</dt> <dd>Whether booking is not available for this day.</dd> <dt>bool `is_free`</dt> <dd>Whether booking is free for this day.</dd> <dt>bool `is_ignore`</dt> <dd>Whether visit is from ignore list.</dd> <dt>bool `is_selected_series`</dt> <dd> `true` if this visit belongs to the originally selected class period''s series (parent and its reschedules). `false` if it belongs to a parallel series running at the same time and location. </dd> <dt>bool `is_wait`</dt> <dd>Whether booking can be only to wait list.</dd> <dt>string `k_class_period`</dt> <dd>Class period key.</dd> <dt>string `s_alert`</dt> <dd>Staff name if booking available, warning about wait list or disabled booking otherwise.</dd> <dt>string `s_date`</dt> <dd>Visit date and time in location''s time zone in human-readable format.</dd> </dl>'
                      dt_from:
                        type: string
                        description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      dt_to:
                        type: string
                        description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      i_count:
                        type: number
                        format: int32
                        description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
                      text_date_from:
                        type: string
                        description: 'Start date of repeatable period in human-readable format.'
                      text_date_to:
                        type: string
                        description: 'End date of repeatable period in human-readable format.'
      tags:
        - Frequency-Repeat
  '/Wl/Book/Process/Frequency/RepeatParallel.json':
    get:
      summary: 'Returns the list of visits to be created for the given recurring booking settings. Accepts the class period, date, user, recurrence configuration (interval, end condition, and days of week), and returns a list of individual visit slots with availability and alert information for each occurrence, together with the computed date range and visit count.'
      parameters:
        - name: a_day
          in: query
          description: 'List of days of the week to create visits. Each value is a {@link \ADateWeekSid} constant.'
          schema:
            type: array
        - name: a_visit_ignore
          in: query
          description: 'List of visits to be ignored. Each value is a string consisting of a class period key and a visit date and time in location''s timezone, concatenated by two colons. Empty array if no visits should be ignored. Example: [''70::2024-11-05 18:00:00''].'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date and time of the class, when recurring booking was called, in UTC timezone.'
          schema:
            type: string
        - name: dt_from
          in: query
          description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: dt_to
          in: query
          description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: i_count
          in: query
          description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
          schema:
            type: number
            format: int32
        - name: i_duration
          in: query
          description: 'Count of days\weeks\months between recurring bookings.'
          schema:
            type: number
            format: int32
        - name: id_duration
          in: query
          description: 'Recurring booking interval, one of {@link \ADurationSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'WellnessLiving mode, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_repeat_end
          in: query
          description: 'Type of repeating, one of {@link \RsRepeatEndSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_cancel
          in: query
          description: '`true` when cancelling booking, `false` otherwise.'
          schema:
            type: boolean
        - name: is_include_parallel
          in: query
          description: '`true` to also include sessions running in parallel at the same time and location (other class period series); `false` to return only the selected series (parent period and its reschedules). Controlled by the "Also include other sessions happening at the same time" toggle on the form. Defaults to `false`.'
          schema:
            type: boolean
        - name: is_new_user
          in: query
          description: '`true` if current user is not created yet, `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key.'
          schema:
            type: string
        - name: s_uid
          in: query
          description: 'Encoded list of user keys, who will attend visits.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of user, who will attend visits.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Key of user, who perform booking.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit:
                        type: array
                        description: 'List of visits to be created for the given settings: <dl> <dt>string `dt_date`</dt> <dd>Visit date and time in UTC.</dd> <dt>string `dt_local`</dt> <dd>Visit date and time in location''s time zone.</dd> <dt>string `html_short_name_list`</dt> <dd>List of names of the staff who provide this class.</dd> <dt>string `html_timezone`</dt> <dd>Location''s time zone abbreviation.</dd> <dt>int `i_spot`</dt> <dd>Number of open spots remaining for this session. `0` if the session is full.</dd> <dt>bool `is_disable`</dt> <dd>Whether booking is not available for this day.</dd> <dt>bool `is_free`</dt> <dd>Whether booking is free for this day.</dd> <dt>bool `is_ignore`</dt> <dd>Whether visit is from ignore list.</dd> <dt>bool `is_selected_series`</dt> <dd> `true` if this visit belongs to the originally selected class period''s series (parent and its reschedules). `false` if it belongs to a parallel series running at the same time and location. </dd> <dt>bool `is_wait`</dt> <dd>Whether booking can be only to wait list.</dd> <dt>string `k_class_period`</dt> <dd>Class period key.</dd> <dt>string `s_alert`</dt> <dd>Staff name if booking available, warning about wait list or disabled booking otherwise.</dd> <dt>string `s_date`</dt> <dd>Visit date and time in location''s time zone in human-readable format.</dd> </dl>'
                      dt_from:
                        type: string
                        description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      dt_to:
                        type: string
                        description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      i_count:
                        type: number
                        format: int32
                        description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
                      text_date_from:
                        type: string
                        description: 'Start date of repeatable period in human-readable format.'
                      text_date_to:
                        type: string
                        description: 'End date of repeatable period in human-readable format.'
      tags:
        - Frequency-RepeatParallel
  '/Wl/Book/Process/Quiz/Quiz.json':
    get:
      summary: 'Defines list of required quizzes. Merges the quizzes required by the booking itself with quizzes tied to the selected purchase options, filters out internal (staff-only) quizzes for non-backend requests, and returns the unified list in `a_quiz`.'
      parameters:
        - name: a_purchase_item
          in: query
          description: 'The list of purchase items. Each element has the format <tt>[id_purchase_item]::[k_id]</tt>, where: This will be empty if no purchases are made for the booking. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: array
                        description: 'The list of quizzes. Each element has the next structure: <dl> <dt>bool `is_require`</dt> <dd>Determines whether the quiz is required.</dd> <dt>string `k_quiz`</dt> <dd>The quiz key. <i>Primary key from {@link \Core\Quiz\QuizSql} table.</i></dd> </dl>'
      tags:
        - Quiz-Quiz
    post:
      summary: 'Finished booking process and save quiz responses (if quiz step is the last in booking wizard). Validates session selection and asset requirements, stores quiz responses in the booking process context, and attempts to complete the booking without payment when no payment step is needed. Returns visit keys, activity keys, and a flag indicating whether additional wizard steps are still required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_quiz_response:
                  type: array
                  description: 'The list of quiz response keys. Keys are quiz keys. <i>Primary key from the {@link \Core\Quiz\QuizSql} table.</i> Values are response keys. <i>Primary key from the {@link \Core\Quiz\Response\ResponseSql} table.</i> Or the {@link \Wl\Quiz\Response\QuizResponse::RESPONSE_SKIP} to skip the quiz.'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'The selected assets. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The order number of the asset (could be from 1 to the asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: array
                  description: 'The selected sessions. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The user''s activity keys.'
                      a_visit:
                        type: array
                        description: 'The keys the bookings that have been made.'
                      is_next:
                        type: boolean
                        description: 'If `true`, the next steps of the booking wizard are required for the purchase or booking. Otherwise, this will be `false`.'
      tags:
        - Quiz-Quiz
  '/Wl/Book/Process/Relation/Relation.json':
    get:
      summary: 'Checks if user can book specified session. Validates the date, business, class period, and user, verifies the session is not already booked, then runs the full booking eligibility check (credit card, waiver, age, overlap, outstanding balance) and throws a descriptive user-facing error if any requirement is not met.'
      parameters:
        - name: a_check_ignore
          in: query
          description: 'List of check that must be skipped. Each element must be a member of {@link ProcessCheckSid}.'
          schema:
            type: array
        - name: dtu_date
          in: query
          description: 'The date/time of the session to check for booking availability.'
          schema:
            type: string
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag and the check will still be performed.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business where <var>uid_from</var> creates the new relative.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The key of the session to check for booking availability.'
          schema:
            type: string
        - name: uid_from
          in: query
          description: 'The user who''s adding the new relative.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Relation-Relation
    post:
      summary: 'Added new relative. Creates a new family member profile for the specified business and links it as a relative of `uid_from`, applying birthday validation, email-inheritance rules, and payment responsibility settings. Returns the UID of the newly created user in `uid_create`.'
      parameters:
        - name: k_business
          in: query
          description: 'The business where <var>uid_from</var> creates the new relative.'
          schema:
            type: string
        - name: uid_from
          in: query
          description: 'The user who''s adding the new relative.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_day:
                  type: [number, "null"]
                  description: 'Day of birthday. `null` if birthday is not entered.'
                i_month:
                  type: [number, "null"]
                  description: 'Month of birthday. `null` if birthday is not entered.'
                i_year:
                  type: [number, "null"]
                  description: 'Year of birthday. `null` if birthday is not entered.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The mode type. One of the {@link ModeSid} constants.'
                id_relation:
                  type: number
                  format: int32
                  description: 'The relation type. One of the {@link \RsFamilyRelationSid} constants.'
                is_mail_inherit:
                  type: boolean
                  description: '`true` - the new relative uses the email from <var>uid_from</var>. `false` - the new relative has their own email.'
                is_pay_self:
                  type: boolean
                  description: '`true` - the new relative pays for themselves. `false` - <var>uid_from</var> pays for the new relative.'
                text_mail:
                  type: string
                  description: 'The new relative''s email.'
                text_name_first:
                  type: string
                  description: 'The new relative''s first name.'
                text_name_last:
                  type: string
                  description: 'The new relative''s last name.'
                uid_existed:
                  type: string
                  description: 'UID of already existed in another business user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid_create:
                        type: string
                        description: 'The newly added relative.'
      tags:
        - Relation-Relation
  '/Wl/Book/Process/Resource/Resource54.json':
    get:
      summary: 'Returns available assets for the booking, decoding the JSON-encoded session input before delegating to the parent. Deserializes the JSON-encoded session selection into `a_session` and then delegates to {@link ResourceApi::get()} to return the available asset categories and items for the booking.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: json_session
          in: query
          description: 'Selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Fields - IDs of sessions in database. Values - arrays of date/time when session is occurred. In MySQL format. In GMT. Serialized with JSON.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_all:
                        type: array
                        description: 'A list of asset categories which are available for specified session. Every element has next keys: <dl> <dt>bool[][] `a_client`</dt> <dd> A list of clients who have already occupied assets for this session. 1st level keys are asset keys. <i>Primary key from the {@link \RsResourceSql} table.</i> 2nd level keys are asset index numbers (starting from 1). Values are `true` if the asset spot is occupied. For example, to check if asset spot 10 with key ''125'' is occupied, check `a_client[''125''][''10'']`. </dd> <dt>array[] `a_resource_list`</dt> <dd> A list of available assets. Every element has next keys: <dl> <dt>int[][][] `a_class_period`</dt> <dd> List of resources available for booking sessions. The field structure is `[k_class_period][dtu_session][''a_available'']`. Contains indexes of resource available for each session. </dd> <dt>array `a_image`</dt> <dd> Asset image data. <i>Result of the {@link \RsResourceImage::data()} method.</i> <dl> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Set only when `sid_image` equals ''shape''.</dd> <dt>int `i_height`</dt> <dd>Height of the image in pixels.</dd> <dt>int `i_width`</dt> <dd>Width of the image in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` if no image was uploaded, `false` otherwise.</dd> <dt>string `sid_image`</dt> <dd>Image kind. One of {@link \Wl\Resource\Image\ImageSid} string constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. One of {@link \Wl\Resource\Image\ImageIconSid} string constants. Set only when `sid_image` equals ''icon''. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. One of {@link \Wl\Resource\Image\ImageShapeSid} string constants. Set only when `sid_image` equals ''shape''. </dd> <dt>string `url`</dt> <dd>Thumbnail image URL.</dd> </dl> </dd> <dt>int `i_index`</dt> <dd>The asset number. Actual for assets with a quantity more than <tt>1</tt>.</dd> <dt>int `i_quantity`</dt> <dd>Total number of the asset spots.</dd> <dt>int `i_use`</dt> <dd>Number of already used asset units.</dd> <dt>bool `is_current`</dt> <dd>City for the off-site location.</dd> <dt>string `k_city`</dt> <dd><tt>true</tt> means that this asset is selected by client, <tt>false</tt> - otherwise.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset in database. <i>Primary key {@link \RsResourceSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>The title of the asset.</dd> <dt>string `text_address`</dt> <dd>Address for the off-site location.</dd> <dt>string `text_guide`</dt> <dd>Additional address tips for the off-site location.</dd> <dt>string `text_postal`</dt> <dd>Postal code for the off-site location.</dd> </dl> </dd> <dt>bool `has_current`</dt> <dd><tt>true</tt> - has current resource in the list of available assets; <tt>false</tt> - otherwise.</dd> <dt>int `id_category`</dt> <dd>Type of the asset category. One of {@link \Wl\Resource\ResourceCategoryEnum} constants.</dd> <dt>bool `is_client_select`</dt> <dd><tt>true</tt> - the client selected the resource from the current group; <tt>false</tt> otherwise.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> - has selected resources; <tt>false</tt> - otherwise.</dd> <dt>bool `is_share`</dt> <dd> <tt>true</tt> resources in this category don''t belong to certain users, but to the entire session. <tt>false</tt> belong to specific users. </dd> <dt>string `k_resource_layout`</dt> <dd>The key of the asset layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `k_resource_type`</dt> <dd>The key of the asset category. <i>Primary key in {@link \RsResourceTypeSql} table.</i></dd> <dt>string `s_resource_type`</dt> <dd>The title of the asset category.</dd> </dl>'
      tags:
        - Resource-Resource54
    post:
      summary: 'Processes the asset selection step of the booking wizard, decoding the JSON-encoded session input before delegating to the parent. Deserializes the JSON-encoded session selection into `a_session` and then delegates to {@link ResourceApi::post()} to save the selected assets and advance the booking wizard.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: json_session
          in: query
          description: 'Selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Fields - IDs of sessions in database. Values - arrays of date/time when session is occurred. In MySQL format. In GMT. Serialized with JSON.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource_select:
                  type: array
                  description: 'The selected assets. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The asset number. Applies only for assets with a quantity greater than <tt>1</tt>.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of a user''s activity. This won''t be empty only if the session(s) was booked at this step.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings that have been made. Not empty only if session(s) was booked on this step.'
                      is_next:
                        type: boolean
                        description: '`true` - the next steps of the booking wizard are required (for example, to purchase something to book the selected session). `false` - no further booking steps are required.'
      tags:
        - Resource-Resource54
  '/Wl/Book/Process/Resource/Resource.json':
    get:
      summary: 'Returns the list of available assets for the booking session, grouped by asset category. Loads all asset categories and their items available for the given class period or selected sessions, strips shared resources that clients should not choose individually, attaches image data and occupancy information, and returns the result as `a_resource_all`.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_all:
                        type: array
                        description: 'A list of asset categories which are available for specified session. Every element has next keys: <dl> <dt>bool[][] `a_client`</dt> <dd> A list of clients who have already occupied assets for this session. 1st level keys are asset keys. <i>Primary key from the {@link \RsResourceSql} table.</i> 2nd level keys are asset index numbers (starting from 1). Values are `true` if the asset spot is occupied. For example, to check if asset spot 10 with key ''125'' is occupied, check `a_client[''125''][''10'']`. </dd> <dt>array[] `a_resource_list`</dt> <dd> A list of available assets. Every element has next keys: <dl> <dt>int[][][] `a_class_period`</dt> <dd> List of resources available for booking sessions. The field structure is `[k_class_period][dtu_session][''a_available'']`. Contains indexes of resource available for each session. </dd> <dt>array `a_image`</dt> <dd> Asset image data. <i>Result of the {@link \RsResourceImage::data()} method.</i> <dl> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Set only when `sid_image` equals ''shape''.</dd> <dt>int `i_height`</dt> <dd>Height of the image in pixels.</dd> <dt>int `i_width`</dt> <dd>Width of the image in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` if no image was uploaded, `false` otherwise.</dd> <dt>string `sid_image`</dt> <dd>Image kind. One of {@link \Wl\Resource\Image\ImageSid} string constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. One of {@link \Wl\Resource\Image\ImageIconSid} string constants. Set only when `sid_image` equals ''icon''. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. One of {@link \Wl\Resource\Image\ImageShapeSid} string constants. Set only when `sid_image` equals ''shape''. </dd> <dt>string `url`</dt> <dd>Thumbnail image URL.</dd> </dl> </dd> <dt>int `i_index`</dt> <dd>The asset number. Actual for assets with a quantity more than <tt>1</tt>.</dd> <dt>int `i_quantity`</dt> <dd>Total number of the asset spots.</dd> <dt>int `i_use`</dt> <dd>Number of already used asset units.</dd> <dt>bool `is_current`</dt> <dd>City for the off-site location.</dd> <dt>string `k_city`</dt> <dd><tt>true</tt> means that this asset is selected by client, <tt>false</tt> - otherwise.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset in database. <i>Primary key {@link \RsResourceSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>The title of the asset.</dd> <dt>string `text_address`</dt> <dd>Address for the off-site location.</dd> <dt>string `text_guide`</dt> <dd>Additional address tips for the off-site location.</dd> <dt>string `text_postal`</dt> <dd>Postal code for the off-site location.</dd> </dl> </dd> <dt>bool `has_current`</dt> <dd><tt>true</tt> - has current resource in the list of available assets; <tt>false</tt> - otherwise.</dd> <dt>int `id_category`</dt> <dd>Type of the asset category. One of {@link \Wl\Resource\ResourceCategoryEnum} constants.</dd> <dt>bool `is_client_select`</dt> <dd><tt>true</tt> - the client selected the resource from the current group; <tt>false</tt> otherwise.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> - has selected resources; <tt>false</tt> - otherwise.</dd> <dt>bool `is_share`</dt> <dd> <tt>true</tt> resources in this category don''t belong to certain users, but to the entire session. <tt>false</tt> belong to specific users. </dd> <dt>string `k_resource_layout`</dt> <dd>The key of the asset layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `k_resource_type`</dt> <dd>The key of the asset category. <i>Primary key in {@link \RsResourceTypeSql} table.</i></dd> <dt>string `s_resource_type`</dt> <dd>The title of the asset category.</dd> </dl>'
      tags:
        - Resource-Resource
    post:
      summary: 'Saves the asset selection for the booking and attempts to book the session if no further steps are required. Validates and stores the selected assets and session data in the booking process context, then, when no quiz step is needed and payment is not required, attempts to complete the booking immediately. Returns visit keys, activity keys, and a flag indicating whether further wizard steps are needed.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource_select:
                  type: array
                  description: 'The selected assets. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The asset number. Applies only for assets with a quantity greater than <tt>1</tt>.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of a user''s activity. This won''t be empty only if the session(s) was booked at this step.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings that have been made. Not empty only if session(s) was booked on this step.'
                      is_next:
                        type: boolean
                        description: '`true` - the next steps of the booking wizard are required (for example, to purchase something to book the selected session). `false` - no further booking steps are required.'
      tags:
        - Resource-Resource
  '/Wl/Book/Process/Guest/GuestProfile.json':
    get:
      summary: 'Check if user exists. Looks up a guest by email within the specified business and service context. Returns the user key if an existing member is found whose email, birthday (when required), and location eligibility all pass validation.'
      parameters:
        - name: dl_birthday
          in: query
          description: 'Guest''s birthday in MySQL format. Empty if service not restricted by age.'
          schema:
            type: string
        - name: id_service
          in: query
          description: 'Type of the service to book.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Key of service to book. Depending on {@link GuestProfileApi::$id_service} value:, <ul> <li>{@link ServiceSid::CLASSES} - class key. <i>Primary key in {@link \RsClassSql} table.</i></li> <li>{@link ServiceSid::APPOINTMENT} - service key.<i>Primary key in {@link \RsServiceSql} table.</i></li> <li>{@link ServiceSid::BOOKABLE_ASSET} - resource key.<i>Primary key in {@link \RsResourceSql} table.</i></li> </ul>'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: text_first_name
          in: query
          description: 'Guest''s first name.'
          schema:
            type: string
        - name: text_last_name
          in: query
          description: 'Guest''s last name.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Guest''s email.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'UID of found or created user.'
      tags:
        - Guest-GuestProfile
    post:
      summary: 'Creates new user. Creates a new guest profile (or reuses an existing non-member account) for the specified business and service, applying birthday and virtual-account rules, and returns the UID of the created or matched user.'
      parameters:
        - name: dl_birthday
          in: query
          description: 'Guest''s birthday in MySQL format. Empty if service not restricted by age.'
          schema:
            type: string
        - name: id_service
          in: query
          description: 'Type of the service to book.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Key of service to book. Depending on {@link GuestProfileApi::$id_service} value:, <ul> <li>{@link ServiceSid::CLASSES} - class key. <i>Primary key in {@link \RsClassSql} table.</i></li> <li>{@link ServiceSid::APPOINTMENT} - service key.<i>Primary key in {@link \RsServiceSql} table.</i></li> <li>{@link ServiceSid::BOOKABLE_ASSET} - resource key.<i>Primary key in {@link \RsResourceSql} table.</i></li> </ul>'
          schema:
            type: string
        - name: text_first_name
          in: query
          description: 'Guest''s first name.'
          schema:
            type: string
        - name: text_last_name
          in: query
          description: 'Guest''s last name.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Guest''s email.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: number
                  format: int32
                  description: 'The mode type used to determine the Lead Source for the created guest. One of the {@link ModeSid} constants. Default is {@link ModeSid::API}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'UID of found or created user.'
      tags:
        - Guest-GuestProfile
  '/Wl/Event/Book/EventView/Element.json':
    get:
      summary: 'Retrieves information about event item. Used to render the event detail page or event card in the booking flow. Returns everything needed to display the event to a prospective client: schedule, pricing, booking availability, assigned staff with photos, and available installment payment options.'
      parameters:
        - name: dl_book_available_end
          in: query
          description: 'The last available date for booking. If this is set and {@link ElementApi::$dl_book_available_start} is a set list of sessions available for booking, {@link ElementApi::$a_book_available} should match given date range.'
          schema:
            type: [string, "null"]
        - name: dl_book_available_start
          in: query
          description: 'The first available date for booking. If this is set and {@link ElementApi::$dl_book_available_end} is a set list of sessions available for booking {@link ElementApi::$a_book_available} should match given date range.'
          schema:
            type: [string, "null"]
        - name: dtu_session
          in: query
          description: 'Datetime of the session. Is not `null` only if we need to get information for an event with a specific class period and datetime. If this is `null`, then the {@link ElementApi::$k_class_period} should be `null` too.'
          schema:
            type: [string, "null"]
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_staff_image_height
          in: query
          description: 'Staff image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_staff_image_width
          in: query
          description: 'Staff image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: is_schedule_group
          in: query
          description: '`true` if schedule should be grouped by location time and staff, `false` if all schedule elements should be independent.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of a business to which the requested event(s) belong.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of a class period to show information for. Is not `null` only if we need to get information for an event with a specific class period and datetime. If this is `null`, then the {@link ElementApi::$dtu_session} should be `null` too.'
          schema:
            type: [string, "null"]
        - name: k_event
          in: query
          description: 'The event key. You can specify {@link ElementApi::$s_event} instead to get information for a large number of events.'
          schema:
            type: string
        - name: s_event
          in: query
          description: 'A list of event keys serialized with JSON. Specify instead of {@link ElementApi::$k_event} to get information for a large number of events.'
          schema:
            type: string
        - name: show_unpublished
          in: query
          description: '`true` to show schedule, which is not published yet. `false` to show only published schedule.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_age_restrictions:
                        type: array
                        description: 'Displays information about age restrictions for this event. Will be empty array if there are no age restrictions. <dl> <dt>int `i_age_from`</dt> <dd>Minimum age for service (years part).</dd> <dt>int `i_age_from_month`</dt> <dd>Minimum age for service (months part).</dd> <dt>int `i_age_from_year`</dt> <dd>Minimum age for service (years part).</dd> <dt>int `i_age_to`</dt> <dd>Maximum age for service (years part).</dd> <dt>int `i_age_to_month`</dt> <dd>Maximum age for service (months part).</dd> <dt>int `i_age_to_year`</dt> <dd>Maximum age for service (years part).</dd> <dt>bool `is_age_public`</dt> <dd> Is service public even if user does not meet age requirements or not? `true` - to show service to everyone. `false` - to show service only to users who meet age requirements. </dd> <dt>bool `is_month_enabled`</dt> <dd> Whether months are enabled for age restrictions. `true` - age restrictions can include number of months. `false` - age restrictions can include only years. </dd> </dl>'
                      a_book_available:
                        type: array
                        description: 'Retrieves information about an event item. Received only if {@link ElementApi::$k_event} has been specified. In this case, other fields aren''t receivers. <dl> <dt>string `dt_date`</dt> <dd>Date/time when the session starts. In UTC.</dd> <dt>string `k_class_period`</dt> <dd>Class session primary keys. <i>Primary keys in table {@link \RsClassPeriodSql}.</i></dd> </dl>'
                      a_business_policy:
                        type: array
                        description: 'Business policies connected to clients and bookings. Contains the same structure as {@link BusinessConfigApi::$a_business_policy}. If the event has custom overrides, those policies are used; otherwise, the default business policies are returned. <dl> <dt>int[] `a_payment_reattempt_not_decline_reason`</dt> <dd> List of not allowed decline reasons to payment reattempt. Each element is one of {@link \Thoth\WlPay\PayExceptionSid} constants. </dd> <dt>bool[] `a_wait_service`</dt> <dd>Keys are list of IDs from {@link \Wl\Service\ServiceSid}, and values are flags whether wait list is allowed.</dd> <dt>int `i_book_before`</dt> <dd>Minimum hours|days|months before class should be booked.</dd> <dt>int `i_book_future`</dt> <dd>Maximum hours|days|months after class can be booked.</dd> <dt>int `i_cancel`</dt> <dd>Minimum hours|days|months before class should be canceled without penalty.</dd> <dt>int `i_promote`</dt> <dd>Minimum hours|days|months before class should be promoted from wait list.</dd> <dt>int `i_promote_fastest_response`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation required should be sent to all clients on the wait list at the same time up to the cut off time. </dd> <dt>int `i_promote_require_confirm`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation must confirm their a promote from the waiting list to the active list. </dd> <dt>int `i_reattempt_count`</dt> <dd>Number of failed auto-payments reattempts.</dd> <dt>int `id_book_before`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_book_future`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_cancel`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_fastest_response`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_require_confirm`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>bool `is_book_inside_active_pay_period`</dt> <dd> if `true` - clients with purchase options are only allowed to book sessions within their current paid period, `false` - during purchase option''s duration. </dd> <dt>bool `is_disable_promotion`</dt> <dd> 1 if a client''s automatic payment fails, their account should not be debited and their purchase option becomes inactive, 0 - otherwise. Default 0. </dd> <dt>bool `is_enable_payment_penalty`</dt> <dd>Whether to charge penalty after final auto-payment attempt.</dd> <dt>bool `is_enable_payment_reattempt`</dt> <dd>Whether to reattempt failed auto-payments.</dd> <dt>bool `is_enable_staff_ip_restriction`</dt> <dd>Whether to restrict which IP addresses staff can login from.</dd> <dt>bool `is_prevent_booking`</dt> <dd>1 if booking for a client with negative balance is disabled, 0 - otherwise. Default 0.</dd> <dt>bool `is_staff_restrict`</dt> <dd>If true, client can not choose provider while appointment wizard.</dd> <dt>bool `is_wait`</dt> <dd>Enable\disable wait list.</dd> <dt>string `k_currency`</dt> <dd>Currency from rs.currency table.</dd> <dt>string `k_timezone`</dt> <dd>Timezone from get.timezone table.</dd> <dt>string `m_payment_penalty`</dt> <dd>Penalty amount to charge after final auto-payment attempt.</dd> <dt>string `url_custom`</dt> <dd>Custom Url of a business</dd> </dl>'
                      a_class_logo:
                        type: array
                        description: 'The logo of event. <dl> <dt>int `i_height`</dt> <dd>Is returned only if staff has a photo. Image height.</dd> <dt>int `i_width`</dt> <dd>Is returned only if staff has a photo. Image width.</dd> <dt>int `id_gender`</dt> <dd>Is returned only if staff does not have a photo. ID of staff gender. One of {@link \AGenderSid} constants.</dd> <dt>string `url_logo`</dt> <dd>Is returned only if staff has a photo. URL to image.</dd> </dl>'
                      a_class_tab:
                        type: array
                        description: 'The list of keys from class tab.'
                      a_event:
                        type: array
                        description: 'Information for a large number of events. Received only if {@link ElementApi::$s_event} has been specified. In this case, other fields are not populated. Key is the event class key. <i>Primary key in the {@link \RsClassSql} table.</i> <i>Value has the same structure as the return value of {@link ElementApi::_get()}.</i> <dl> <dt>array[] `a_book_available`</dt> <dd> List of sessions available for booking. See {@link \Wl\Event\Book\EventView\ElementApi::$a_book_available}. <dl> <dt>string `dt_date`</dt> <dd>Date/time when the session starts. In UTC.</dd> <dt>string `k_class_period`</dt> <dd>Class session primary keys. <i>Primary keys in table {@link \RsClassPeriodSql}.</i></dd> </dl> </dd> <dt>array `a_class_logo`</dt> <dd> Image of event. See {@link \Wl\Event\Book\EventView\ElementApi::$a_class_logo}. <dl> <dt>int `i_height`</dt> <dd>Is returned only if staff has a photo. Image height.</dd> <dt>int `i_width`</dt> <dd>Is returned only if staff has a photo. Image width.</dd> <dt>int `id_gender`</dt> <dd> Is returned only if staff does not have a photo. ID of staff gender. One of {@link \AGenderSid} constants. </dd> <dt>string `url_logo`</dt> <dd>Is returned only if staff has a photo. URL to image.</dd> </dl> </dd> <dt>string[] `a_class_tab`</dt> <dd>Class tab keys. See {@link \Wl\Event\Book\EventView\ElementApi::$a_class_tab}.</dd> <dt>array[] `a_installment_template`</dt> <dd> List of installment plans. See {@link \Wl\Event\Book\EventView\ElementApi::$a_installment_template}. <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by `id_period` between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency Key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql}.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl> </dd> <dt>array[] `a_makeup_class`</dt> <dd> Classes selected for make-up sessions. Every element has the following keys: <dl> <dt>string `k_class`</dt> <dd>Class key. <i>Primary key in table {@link \RsClassSql}.</i></dd> <dt>string|null `s_title`</dt> <dd>Class title. `null` if title is unavailable for the selected language.</dd> </dl> </dd> <dt>array[] `a_schedule`</dt> <dd> Schedule of event sessions. See {@link \Wl\Event\Book\EventView\ElementApi::$a_schedule}. <dl> <dt>true[] `a_day`</dt> <dd> Days of the week when the session occurs. Keys are weekday numbers (1 = Monday, 7 = Sunday), values are always `true`. </dd> <dt>array `a_repeat`</dt> <dd> Repeat periodicity instructions. <dl> <dt>int `i_repeat`</dt> <dd>Count of the periods which specified in `id_repeat`.</dd> <dt>int `id_repeat`</dt> <dd>Measuring unit of `i_repeat` (week, month, year). One of {@link \ADurationSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> A list of staff members who conduct the session. Every element has the following next keys: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `s_name`</dt> <dd>The staff member name.</dd> <dt>string `s_surname`</dt> <dd>The first letter of staff member''s surname.</dd> <dt>string `uid_staff`</dt> <dd>The user key of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>string `dt_end`</dt> <dd>The end date of the session. The local date without time.</dd> <dt>string `dt_start`</dt> <dd>The start date of the session. The local date without time.</dd> <dt>bool `hide_location`</dt> <dd> `true` if the location should be hidden in the event details. Hide if the event is virtual or if the business only has one location. `false` otherwise. </dd> <dt>int `i_capacity`</dt> <dd>The class capacity.</dd> <dt>int `i_duration`</dt> <dd>The duration of the class in seconds.</dd> <dt>bool `is_virtual`</dt> <dd> This will be `true` if the session is not held in person but offered remotely. It will be `false` otherwise. </dd> <dt>string `f_price`</dt> <dd>The price of the session, if it can be purchased separately.</dd> <dt>string `k_class_period`</dt> <dd>The key of the class period.</dd> <dt>string `k_location`</dt> <dd>The key of the location where the session is held.</dd> <dt>string `k_resource_location`</dt> <dd>Structured off-site location data used by the calendar attachment.</dd> <dt>string `s_location`</dt> <dd>The location title.</dd> <dt>string `s_time`</dt> <dd> The time when session occurred. A textual representation of the start and end time of a session. Example: `10:00 am - 11:00 am` </dd> <dt>string `s_timezone`</dt> <dd>The name of the timezone in which the session is held.</dd> <dt>string `text_room`</dt> <dd>The room of the event.</dd> </dl> </dd> <dt>string[] `a_shop_category`</dt> <dd>IDs of online store category. <i>Primary keys in {@link \RsShopCategorySql} table.</i></dd> <dt>array `a_staff_logo`</dt> <dd> Photos of staff. See {@link \Wl\Event\Book\EventView\ElementApi::$a_staff_logo}. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `uid`</dt> <dd>Key of the user. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> <dt>string `url_logo`</dt> <dd>URL to image.</dd> </dl> </dd> <dt>string `dt_book_date`</dt> <dd>Date/time of first event session.</dd> <dt>string `dt_early`</dt> <dd>Early date of event purchase.</dd> <dt>string `dt_end`</dt> <dd>End date of the event instance.</dd> <dt>string `dt_start`</dt> <dd>Date of first event session.</dd> <dt>string `html_end`</dt> <dd>Html End date of the event instance.</dd> <dt>string `html_start`</dt> <dd>Html Date of first event session.</dd> <dt>bool `hide_application`</dt> <dd>Whether event will be hidden in the White Label mobile application.</dd> <dt>string `html_special`</dt> <dd>Special instruction for event.</dd> <dt>int `i_makeup_cap`</dt> <dd> Number of allowed make-up sessions for event. `0` if make-up sessions are disabled for event or all missed sessions are available for make-up. </dd> <dt>int `i_session`</dt> <dd>Session count in event.</dd> <dt>int `i_session_all`</dt> <dd>Total number of sessions including both past and future sessions.</dd> <dt>int `i_session_remain`</dt> <dd>Remaining session count in event.</dd> <dt>bool `is_availability_checked`</dt> <dd>Whether event availability was checked.</dd> <dt>bool `is_book`</dt> <dd>Whether event is booked already</dd> <dt>bool `is_full`</dt> <dd>`true` if there are no free spots in the event and booking is available only into wait list.</dd> <dt>bool `is_makeup`</dt> <dd>`true` if the selected session can be a make up session; `false` otherwise.</dd> <dt>bool `is_past`</dt> <dd>`true` if the event session has already started or ended and is not available to book.</dd> <dt>bool `is_policy_custom`</dt> <dd> `true` {@link \Wl\Event\Book\EventView\ElementApi::$a_business_policy} contains the custom policies from the event; `false` otherwise. </dd> <dt>bool `is_virtual`</dt> <dd>`true` if event is virtual; `false` otherwise.</dd> <dt>string `k_book_class_period`</dt> <dd>Key of first event session. <i>Primary key in table {@link \RsClassPeriodSql}.</i></dd> <dt>string `m_price`</dt> <dd>Price of the event session.</dd> <dt>string `m_price_total`</dt> <dd>Price of the full event.</dd> <dt>string `m_price_total_early`</dt> <dd>Price of the full event, should be used as full price while `dt_early` is actual.</dd> <dt>string `s_deny_reason`</dt> <dd>Reason of booking restriction. For example if exception has been thrown.</dd> <dt>string `s_title`</dt> <dd>Event title.</dd> <dt>string `xml_description`</dt> <dd>Description of event. Ready to put into browser.</dd> </dl>'
                      a_installment_template:
                        type: array
                        description: 'A list of installment plans. Each element has the following next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by `id_period` between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency Key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql}.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl>'
                      a_makeup_class:
                        type: array
                        description: 'Class selected for make-up sessions. Every element has the following keys: <dl> <dt>string `k_class`</dt> <dd>Class key. <i>Primary key in table {@link \RsClassSql}.</i></dd> <dt>string|null `s_title`</dt> <dd>Class title. `null` if title is unavailable for the selected language.</dd> </dl>'
                      a_schedule:
                        type: array
                        description: 'A list of event sessions. Every element has the following next keys: <dl> <dt>true[] `a_day`</dt> <dd> Days of the week when the session occurs. Keys are weekday numbers (1 = Monday, 7 = Sunday), values are always `true`. </dd> <dt>array `a_repeat`</dt> <dd> Repeat periodicity instructions. <dl> <dt>int `i_repeat`</dt> <dd>Count of the periods which specified in `id_repeat`.</dd> <dt>int `id_repeat`</dt> <dd>Measuring unit of `i_repeat` (week, month, year). One of {@link \ADurationSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> A list of staff members who conduct the session. Every element has the following next keys: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `s_name`</dt> <dd>The staff member name.</dd> <dt>string `s_surname`</dt> <dd>The first letter of staff member''s surname.</dd> <dt>string `uid_staff`</dt> <dd>The user key of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>string `dt_end`</dt> <dd>The end date of the session. The local date without time.</dd> <dt>string `dt_start`</dt> <dd>The start date of the session. The local date without time.</dd> <dt>bool `hide_location`</dt> <dd> `true` if the location should be hidden in the event details. Hide if the event is virtual or if the business only has one location. `false` otherwise. </dd> <dt>int `i_capacity`</dt> <dd>The class capacity.</dd> <dt>int `i_duration`</dt> <dd>The duration of the class in seconds.</dd> <dt>bool `is_virtual`</dt> <dd>This will be `true` if the session is not held in person but offered remotely. It will be `false` otherwise.</dd> <dt>string `f_price`</dt> <dd>The price of the session, if it can be purchased separately.</dd> <dt>string `k_class_period`</dt> <dd>The key of the class period.</dd> <dt>string `k_location`</dt> <dd>The key of the location where the session is held.</dd> <dt>string `k_resource_location`</dt> <dd>Structured off-site location data used by the calendar attachment.</dd> <dt>string `s_location`</dt> <dd>The location title.</dd> <dt>string `s_time`</dt> <dd> The time when session occurred. A textual representation of the start and end time of a session. Example: `10:00 am - 11:00 am` </dd> <dt>string `s_timezone`</dt> <dd>The name of the timezone in which the session is held.</dd> <dt>string `text_room`</dt> <dd>The room of the event.</dd> </dl>'
                      a_shop_category:
                        type: array
                        description: 'IDs of online store category.'
                      a_staff_logo:
                        type: array
                        description: 'Photos of staff members. Keys are the keys of staff members. The values are the following: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `uid`</dt> <dd>Key of the user. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> <dt>string `url_logo`</dt> <dd>URL to image.</dd> </dl>'
                      a_timezone_info:
                        type: array
                        description: 'Timezone information for all timezones used in the event schedule. Key is the timezone key. Primary key in the `a_geo_timezone` table. Value contains timezone information from the geo timezone registry: <dl> <dt>int `i_shift`</dt> <dd>UTC offset in hours for this timezone.</dd> <dt>string `s_file`</dt> <dd>Timezone identifier string (e.g. `America/New_York`).</dd> <dt>string|null `text_abbr`</dt> <dd>Timezone abbreviation (e.g. `EST`). `null` if not set.</dd> </dl>'
                      a_visits_required:
                        type: array
                        description: 'A list of classes and events that clients should attend before this one. <dl> <dt>int `i_count`</dt> <dd>The number of visits required.</dd> <dt>int `i_has`</dt> <dd>The number of visits the client has already attended.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>The key of the class or event.</dd> <dt>string `text_title`</dt> <dd>The name of the class or event.</dd> </dl>'
                      dt_book_date:
                        type: [string, "null"]
                        description: 'That date that should be used to go to the booking wizard.'
                      dt_early:
                        type: [string, "null"]
                        description: 'The early registration date of the event.'
                      dt_end:
                        type: string
                        description: 'The end date of the event.'
                      dt_start:
                        type: string
                        description: 'The start date of the event.'
                      hide_application:
                        type: boolean
                        description: 'Whether event will be hidden in the White Label mobile application. `true` means that event will not be displayed, `false` otherwise.'
                      html_description:
                        type: [string, "null"]
                        description: 'The description of the event.'
                      html_special:
                        type: string
                        description: 'Special instruction for the event.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Class capacity. `null` if no set.'
                      i_makeup_cap:
                        type: number
                        format: int32
                        description: 'Number of allowed make-up sessions for event. `0` if make-up sessions are disabled for event or all missed sessions are available for make-up.'
                      i_session:
                        type: number
                        format: int32
                        description: 'The session count.'
                      i_session_all:
                        type: number
                        format: int32
                        description: 'Total number of sessions including both past and future sessions.'
                      i_session_remain:
                        type: number
                        format: int32
                        description: 'The remaining session count.'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      id_pay_require_option:
                        type: [number, "null"]
                        description: 'Default required value for {@link RequirePaySid::ADVANCE} payment mode, one of {@link RequirePaySid::ONLINE} or {@link RequirePaySid::VISIT}. `null` means default value not selected.'
                      id_virtual_provider:
                        type: [number, "null"]
                        description: 'The virtual provider ID. One of the {@link VirtualProviderSid} constants. `null` if an in-person event.'
                      is_age_restrict:
                        type: boolean
                        description: 'Whether the event is age restricted. `true` if the event is age restricted, `false` if the event is not.'
                      is_availability_checked:
                        type: boolean
                        description: '`true` if the event availability was checked; `false` if the event has too many sessions, and calculating the availability of each one takes a lot of time.'
                      is_book:
                        type: boolean
                        description: '`true` if the event is already booked; `false` if the event isn''t booked.'
                      is_bookable:
                        type: boolean
                        description: '`true` If the event is bookable; `false` if the event isn''t bookable.'
                      is_full:
                        type: boolean
                        description: '`true` if there are no free spots in the event; booking is available only into the wait list. `false` otherwise.'
                      is_makeup:
                        type: boolean
                        description: '`true` if the selected session can be a make up session. `false` otherwise.'
                      is_past:
                        type: boolean
                        description: '`true` if the selected session has already started and do not available to book. `false` otherwise.'
                      is_policy_custom:
                        type: boolean
                        description: '`true` if the {@link ElementApi::$a_business_policy} contains the custom policies from the event. `false` otherwise.'
                      is_promotion_only:
                        type: boolean
                        description: '`true` if the event can be paid with a Purchase Option only. `false` if full event purchases or single session purchases are permitted.'
                      is_prorate:
                        type: boolean
                        description: '`true` if clients, when they book event, which has been started, can pay only for the sessions in the future. `false` if clients, when they book event, have to pay for the entire event and the number of lefts in the future sessions does not matter.'
                      is_single_session_buy:
                        type: boolean
                        description: 'Determines whether this event allows paying for a single session.'
                      is_virtual:
                        type: boolean
                        description: '`true` if event is virtual; `false` otherwise.'
                      k_book_class_period:
                        type: string
                        description: 'The ID of the session which should be used to go to the booking wizard.'
                      m_price:
                        type: string
                        description: 'The price of a single session of the event.'
                      m_price_total:
                        type: string
                        description: 'The price of the all sessions of the event.'
                      m_price_total_early:
                        type: [string, "null"]
                        description: 'Price of the full event should be used as full price while {@link ElementApi::$dt_early} is actual.'
                      s_deny_reason:
                        type: string
                        description: 'The reason why the event can''t be booked. Empty if {@link ElementApi::$k_book_class_period} isn''t empty.'
                      s_title:
                        type: [string, "null"]
                        description: 'The title of the event.'
                      show_special_instructions:
                        type: boolean
                        description: '`true` if special instructions can be public, `false` if they should be shown only to the clients, who booked the class.'
                      text_end:
                        type: string
                        description: 'End date of the event in user-friendly format.'
                      text_start:
                        type: string
                        description: 'Start date of the event in user-friendly format.'
                      url_book:
                        type: [string, "null"]
                        description: 'Link to the start of the booking wizard to book the closed session from this event or the entire event. Can be `null` if there is no available for booking sessions.'
                      xml_description:
                        type: [string, "null"]
                        description: 'The description of the event.'
      tags:
        - EventView-Element
  '/Wl/Event/Book/EventList/List.json':
    get:
      summary: 'Retrieves a list of events. Used to populate the events tab on the client-facing booking page. Returns all events offered in the business (or filtered to a specific tab), together with availability flags so the UI can show which events still have open spots.'
      parameters:
        - name: id_status
          in: query
          description: 'Defines how the event availability flag filter should be applied. One of {@link \AFlagSid} constants. * {@link \AFlagSid::ON} to show only available events. * {@link \AFlagSid::OFF} to show only unavailable events. * {@link \AFlagSid::ALL} to show all events (available and unavailable).'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'The key of the category tab. If empty, select only elements with not specified book tab.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_event:
                        type: array
                        description: 'A list of event identifiers. The order of items in this array matches the order in which elements should be displayed.'
                      a_event_available:
                        type: array
                        description: 'Event availability map. Unavailable events are those that cannot be booked, but they can be shown to the client (for example, under the "not available" filter). Key is the event class key. <i>Primary key in the {@link \RsClassSql} table.</i> Value is `true` if the event is available for booking, `false` if it is unavailable.'
                      is_virtual_service:
                        type: boolean
                        description: '<tt>true</tt> if exist at least one virtual event by specific {@link ListApi::$k_business} and {@link ListApi::$k_class_tab}, <tt>false</tt> otherwise.'
      tags:
        - EventList-List
  '/Wl/Profile/Attendance/Schedule/PaymentMultiple.json':
    get:
      summary: 'Loads unpaid appointments data for the multiple payment panel. Loads unpaid appointments together with their addons, applicable purchase options, and purchase options already owned by the client. Appointments are loaded either for a client and their relatives on a specific day, or for an explicit list of appointments specified in {@link PaymentMultipleApi::$a_appointment}.'
      parameters:
        - name: a_appointment
          in: query
          description: 'List of appointment keys for which to load unpaid data. When specified, appointments are looked up directly by these keys, instead of by all unpaid appointments booked for {@link PaymentMultipleApi::$uid} on the day specified in {@link PaymentMultipleApi::$dtl_date}.'
          schema:
            type: array
        - name: dtl_date
          in: query
          description: 'Local date and time for which visit is booked in MySQL format. Can be `null` in case when {@link PaymentMultipleApi::$a_appointment} passed.'
          schema:
            type: [string, "null"]
        - name: is_relationship
          in: query
          description: '`true` to all appointments are related to the same user, `false` if appointments can be related to different users.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'When set to `true` it''s mean that need load full information about unpaid visits: * List of available/existing POs. * List of unpaid addons. When set to `false` loaded only general information about visits on passed day.'
          schema:
            type: boolean
        - name: is_unpaid
          in: query
          description: '`true` to get only unpaid appointments, `false` to get all appointments.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key. Can be `null` in case when {@link PaymentMultipleApi::$a_appointment} passed.'
          schema:
            type: [string, "null"]
        - name: k_visit
          in: query
          description: 'Last booked visit key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key. Can be empty when the payment is for one or more walk-in visits, in which case {@link PaymentMultipleApi::$a_appointment} must be passed instead.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_client:
                        type: array
                        description: 'Clients'' data. Clients with unpaid appointments data, each element has next structure: <dl> <dt>array|null `a_relation`</dt> <dd> Information about relation. `null` if there is no relation, in particular when appointments are loaded by {@link \Wl\Profile\Attendance\Schedule\PaymentMultipleApi::$a_appointment}. <dl> <dt>int `id_family_relation`</dt> <dd>Family relation ID. One of {@link \RsFamilyRelationSid} constants.</dd> <dt>string `uid_payer`</dt> <dd>Payer UID.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> Unpaid appointments data. <i>Result returned {@link \Wl\Profile\Attendance\Schedule\PaymentMultipleApi::_getServices()} method.</i> <dl> <dt>array `a_addon`</dt> <dd> List of add-ons: <dl> <dt>int `i_count`</dt> <dd>Number of the product.</dd> <dt>bool `is_paid`</dt> <dd>`true` if the product is paid, otherwise `false`.</dd> <dt>string `html_price`</dt> <dd>Formatted product price</dd> <dt>string `m_price`</dt> <dd>Product price.</dd> <dt>string `text_title`</dt> <dd>Product title.</dd> </dl> </dd> <dt>array `a_promotion_applicable`</dt> <dd> Promotions that can be used to pay for appointment: <dl> <dt>string `m_price`</dt> <dd>Promotion price.</dd> <dt>int `i_order`</dt> <dd>Sort order.</dd> <dt>string `text_key`</dt> <dd>Promotion key.</dd> <dt>string `text_price`</dt> <dd>Promotion price.</dd> <dt>string `text_title`</dt> <dd>Promotion title.</dd> </dl> </dd> <dt>array `a_promotion_owned`</dt> <dd> Promotions owned by the client, which can be used to pay for appointment: <dl> <dt>int `i_limit`</dt> <dd>Limit of visits.</dd> <dt>int `i_order`</dt> <dd>Sort order.</dd> <dt>int `i_remain`</dt> <dd>The number of the remaining visits.</dd> <dt>string `text_key`</dt> <dd>Promotion key.</dd> <dt>string `text_price`</dt> <dd>Promotion price.</dd> <dt>string `text_title`</dt> <dd>Promotion title.</dd> <dt>string `text_title_remain`</dt> <dd>Promotion title with number of remaining sessions.</dd> </dl> </dd> <dt>bool `has_unpaid_addon`</dt> <dd>`true` if appointment has unpaid add-ons, otherwise `false`.</dd> <dt>bool `is_pay_need`</dt> <dd>`true` if the appointment is not free and not paid, otherwise `false`.</dd> <dt>bool `is_required`</dt> <dd>`true` if the payment is required, otherwise `false`.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> <dt>string `text_date`</dt> <dd>Appointment date.</dd> <dt>string `text_service`</dt> <dd>Appointment title.</dd> <dt>string|null `text_staff`</dt> <dd>Name of the staff leading the appointment. `null` for asset.</dd> <dt>string `text_time`</dt> <dd>Appointment time.</dd> <dt>string `text_timezone_abbr`</dt> <dd>Appointment timezone abbreviation.</dd> <dt>string `uid_staff`</dt> <dd>Appointment user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string `uid`</dt> <dd>The user for whom the appointment is booked. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                      a_staff_list:
                        type: array
                        description: 'List of available staff members for tips. <dl> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary keys in {@link \RsStaffSql} table.</i></dd> <dt>string `text_name`</dt> <dd>Name of the staff.</dd> </dl> <dl> <dt>string `text_name`</dt> <dd>Staff full name.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                      i_unpaid_number:
                        type: number
                        format: int32
                        description: 'Total number of unpaid appointments.'
                      is_tip:
                        type: boolean
                        description: 'Whether tips are accepted.'
      tags:
        - Schedule-PaymentMultiple
    post:
      summary: 'Applies existing purchase options for appointments pay and generates a link for payment in the store. Applies purchase options and session passes already owned by the client to the selected visits. Redirects to the store cart or checkout page so the client can complete payment for any remaining unpaid addons or newly purchased purchase options.'
      parameters:
        - name: a_visit_pay
          in: query
          description: 'Visits payment data. Visits payment data to apply: <dl> <dt>string[] `a_shop_product_option`</dt> <dd>Selected for payment products options keys. <i>Primary keys in {@link \RsShopProductOptionSql} table.</i></dd> <dt>string `k_visit`</dt> <dd>The visit key. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>string `text_key`</dt> <dd> Selected pay option to apply. The key has structure {@link \Wl\Visit\Pay\PayChangeApi::$text_key}, plus an extra `k_promotion:0` (Drop-in) and `k_appointment_deposit:[k_appointment]` (deposit-only). </dd> <dt>string `uid`</dt> <dd>The user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: is_checkout
          in: query
          description: 'Determines for which store page the redirection url should be generated. `true` if to the checkout page. `false` if to the cart page.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key. Can be empty when the payment is for one or more walk-in visits, in which case {@link PaymentMultipleApi::$a_appointment} must be passed instead.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'Url for redirect after applying existing purchase options.'
      tags:
        - Schedule-PaymentMultiple
  '/Wl/Profile/Account/Select/Select.json':
    get:
      summary: 'Retrieves information about user specified in {@link SelectApi::$uid} and his relationship with sub accounts. Returns all user''s relatives and type of relationship, which are allowed to sign in with. So, you can use this list to sign in user as hist relative, but this is not the best way to get full family. Business can set that parent can sign in as his child, but child cannot sign in as his parent. So, this method will return children for parent, but will not return parents for child.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to retrieve relationship information. <i>Primary key in {@link \RsBusinessSql}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve relationship information. <i>Primary key in {@link \PassportLoginSql}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_user:
                        type: array
                        description: 'Array with information about current user and his relationship with sub accounts. <dl> <dt>int `id_family_relation`</dt> <dd>ID of relationship between current user and sub account from {@link \RsFamilyRelationSid}.</dd> <dt>string `s_name`</dt> <dd>Name of sub account.</dd> <dt>string `uid`</dt> <dd>UID of sub account.</dd> </dl>'
      tags:
        - Select-Select
    post:
      summary: 'Signs in user specified in {@link SelectApi::$uid_in}. Returns an error if you try to sign in user not according to the business permissions.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to retrieve relationship information. <i>Primary key in {@link \RsBusinessSql}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve relationship information. <i>Primary key in {@link \PassportLoginSql}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                uid_in:
                  type: string
                  description: 'ID of relative to sign in. <i>Primary key in {@link \PassportLoginSql}.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Select-Select
  '/Wl/Profile/Form/Response/ResponseList.json':
    get:
      summary: 'Returns the lists of completed and pending quiz responses for the specified user and business. Returns the client''s completed, draft, and pending form responses for the business, sorted newest first. Also provides permission flags that control which actions the current user may perform on each response.'
      parameters:
        - name: hide_completed
          in: query
          description: 'Defines whether completed forms should not be included in result list of forms. `true` to exclude completed forms from result. `false` to include completed forms to result.'
          schema:
            type: boolean
        - name: hide_optional
          in: query
          description: 'Defines whether optional uncompleted forms should not be included in result list of forms. `true` to exclude optional uncompleted forms from result. `false` to include optional uncompleted forms to result.'
          schema:
            type: boolean
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz_login:
                        type: array
                        description: 'The list of uncompleted quiz responses. Each element has the next structure: <dl> <dt>string[] `a_visit`</dt> <dd> List of visit keys associated with uncompleted response. Key is visit key. <i>Primary key in {@link \RsVisitSql} table.</i> Value is date of visit. </dd> <dt>string `dtl_date`</dt> <dd>The date of the request to fill out a quiz form.</dd> <dt>int `id_source`</dt> <dd> The place where the request to fill out a quiz form occurred. One of the {@link \Wl\Quiz\Response\SourceSid} constants. </dd> <dt>bool `is_private`</dt> <dd> Determines whether the form can be viewed by staff member only after confirmation. `true` - the form can be viewed only after confirmation. `false` - the form can always be viewed. </dd> <dt>string `k_quiz`</dt> <dd>The quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>The key of the request. <i>Primary key in the {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string|null `k_quiz_response`</dt> <dd> Key of the response. <i>Primary key in the {@link \Core\Quiz\Response\ResponseSql} table.</i> Not `null` in case when response in draft status. </dd> <dt>string `text_title`</dt> <dd>The quiz title.</dd> </dl>'
                      a_quiz_response:
                        type: array
                        description: 'The list of completed quiz responses. Each element has the next structure: <dl> <dt>string[] `a_visit`</dt> <dd> List of visit keys associated with uncompleted response. Key is visit key. <i>Primary key in {@link \RsVisitSql} table.</i> Value is date of visit. </dd> <dt>string `dtl_date`</dt> <dd>The date of the request to fill out a quiz form.</dd> <dt>int `id_source`</dt> <dd> The place where the request to fill out the quiz form occurred. One of the {@link \Wl\Quiz\Response\SourceSid} constants. </dd> <dt>int `id_status`</dt> <dd>Status of the response. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_private`</dt> <dd> Determines whether the form can be viewed by staff member only after confirmation. `true` - the form can be viewed only after confirmation. `false` - the form can always be viewed. </dd> <dt>string `k_quiz`</dt> <dd>The quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>The quiz response key. <i>Primary key in {@link \Core\Quiz\Response\ResponseSql} table.</i></dd> <dt>string|null `text_amend_date`</dt> <dd>Date when response amended. `null` if response was not edited.</dd> <dt>string|null `text_amend_user`</dt> <dd>Name of the user who amend the response. `null` if response was not edited.</dd> <dt>string `text_title`</dt> <dd>The quiz title.</dd> <dt>string `text_user`</dt> <dd>Name of user who complete response or start a draft.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether response can be amended by current user.'
                      can_fill:
                        type: boolean
                        description: 'Whether response can be filled by current user.'
                      can_remove:
                        type: boolean
                        description: 'Whether response can be removed by current user.'
                      can_view:
                        type: boolean
                        description: 'Whether response can be viewed by current user.'
      tags:
        - Response-ResponseList
  '/Wl/Profile/Edit/Email/EditEmail67.json':
    get:
      summary: 'Checks if specified email address is busy. Looks up whether the given email is already registered and returns user info, membership status, staff flags, and password status if found; guest requests are subject to a per-IP rate limit.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The email address to be checked.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_member_key:
                        type: array
                        description: 'List of business keys where a user is already a member.'
                      a_user:
                        type: array
                        description: 'Information about the user who occupies the specified email. This will be empty if the email is free or if the rate limit has been reached. Otherwise, has next keys: <dl> <dt>string `text_firstname`</dt> <dd>First name.</dd> <dt>string `text_lastname`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> <dt>string `url_photo`</dt> <dd>URL to photo.</dd> </dl>'
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
                      is_current_member:
                        type: boolean
                        description: 'If `true`, user is already a member of current business, `false` - otherwise.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the number of requests has exceeded the rate limit. Otherwise, this will be `false`.'
                      is_password_set:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, has password set, `false` - otherwise.'
                      is_staff:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in current business, `false` - otherwise.'
                      is_staff_any_business:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in any business, `false` - otherwise.'
                      is_use:
                        type: boolean
                        description: 'If `true`, then the specified email is in use. Otherwise, this will be `false`.'
                      uid_result:
                        type: string
                        description: 'The key of user who has the specified email. This will be `0` if the email is free or if the rate limit has been reached.'
      tags:
        - Email-EditEmail67
    post:
      summary: 'Adds user to business. Registers the found user as a member of the specified business. If required profile fields are missing, grants temporary access until the profile is completed. Sends a welcome notification once the user is fully registered.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
      tags:
        - Email-EditEmail67
  '/Wl/Profile/Edit/Email/EditEmail.json':
    get:
      summary: 'Checks if specified email address is busy. Looks up whether the given email is already registered and returns user info, membership status, staff flags, and password status if found; guest requests are subject to a per-IP rate limit.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The email address to be checked.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_member_key:
                        type: array
                        description: 'List of business keys where a user is already a member.'
                      a_user:
                        type: array
                        description: 'Information about the user who occupies the specified email. This will be empty if the email is free or if the rate limit has been reached. Otherwise, has next keys: <dl> <dt>string `text_firstname`</dt> <dd>First name.</dd> <dt>string `text_lastname`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> <dt>string `url_photo`</dt> <dd>URL to photo.</dd> </dl>'
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
                      is_current_member:
                        type: boolean
                        description: 'If `true`, user is already a member of current business, `false` - otherwise.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the number of requests has exceeded the rate limit. Otherwise, this will be `false`.'
                      is_password_set:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, has password set, `false` - otherwise.'
                      is_staff:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in current business, `false` - otherwise.'
                      is_staff_any_business:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in any business, `false` - otherwise.'
                      is_use:
                        type: boolean
                        description: 'If `true`, then the specified email is in use. Otherwise, this will be `false`.'
                      uid_result:
                        type: string
                        description: 'The key of user who has the specified email. This will be `0` if the email is free or if the rate limit has been reached.'
      tags:
        - Email-EditEmail
    post:
      summary: 'Adds user to business. Registers the found user as a member of the specified business. If required profile fields are missing, grants temporary access until the profile is completed. Sends a welcome notification once the user is fully registered.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
      tags:
        - Email-EditEmail
  '/Wl/Profile/Contract/ContractAll/ContractAll.json':
    get:
      summary: 'Gets list of contracts. Returns every contract the user has subscribed to or is required to agree to for the given business, including the agreement date, signature link, and contract text.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to get information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_contract:
                        type: array
                        description: 'List of contacts. Every element has next keys: <dl> <dt>string `dt_agree_local`</dt> <dd>Agreement date in business timezone.</dd> <dt>string `dt_agree_utc`</dt> <dd>Agreement date in UTC.</dd> <dt>string `html_contract`</dt> <dd>Contract text.</dd> <dt>string `html_title`</dt> <dd>Purchase item title.</dd> <dt>string `ip_agree`</dt> <dd>IP address from which agreement was done.</dd> <dt>string `k_purchase_item`</dt> <dd>Purchase item key. <i>Primary key in {@link \RsPurchaseItemSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Purchase item title.</dd> <dt>string `url_signature`</dt> <dd>URL to agreement signature.</dd> </dl>'
      tags:
        - ContractAll-ContractAll
  '/Wl/Login/Promotion/GuestPass/GuestPassList.json':
    get:
      summary: 'Retrieves guest passes for a specific client. Returns all guest passes assigned to the given user, optionally filtered by class and location. Guest passes are not applicable to events, so an empty list is returned when the specified class is an event.'
      parameters:
        - name: dl_date
          in: query
          description: 'Local calendar date for which guest pass availability should be calculated.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'Class key to filter guest passes by. `null` if guest passes should not be filtered by class.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location key to filter guest passes by. `null` if guest passes should not be filtered by location.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of client''s guest passes. <dl> <dt>array `a_image`</dt> <dd> Thumbnail image data for the guest promotion. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `url-thumbnail`</dt> <dd>Thumbnail url.</dd> </dl> </dd> <dt>array|null `a_period_dl`</dt> <dd> Start and end dates of the current reset period. `null` if there is no reset period or the reset date is unavailable. <dl> <dt>string `dl_end`</dt> <dd>Period end date in MySQL date format.</dd> <dt>string `dl_start`</dt> <dd>Period start date in MySQL date format.</dd> </dl> </dd> <dt>array `a_settings`</dt> <dd> Guest pass settings. Empty array when the guest pass has no settings (class-type passes). <dl> <dt>int `i_claim_day`</dt> <dd> Count of days for accept guest invite. If the invitation is not accepted within this time, it will be canceled. </dd> <dt>int `i_limit`</dt> <dd>Times that member can invite the same guest.</dd> <dt>int `i_limit_duration`</dt> <dd> The time during which a member can invite a guest {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit} times. </dd> <dt>int `id_limit_duration`</dt> <dd> Type of the duration of {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit_duration}. One of {@link \ADurationSid} constants. </dd> <dt>bool `is_checkin`</dt> <dd>Whether guests can only enter the gym when the inviting member is checked in.</dd> <dt>bool `is_limit`</dt> <dd>Whether there are limits for a guest promotion.</dd> </dl> </dd> <dt>bool `can_invite`</dt> <dd>`true` if the guest pass is invite-type (the member sends invitations to guests).</dd> <dt>bool `can_send`</dt> <dd>`true` if the member can currently send a guest pass (eligible and within quota).</dd> <dt>string|null `dl_reset`</dt> <dd>Date on which the pass resets or expires, in MySQL date format. `null` if no expiry date is determined.</dd> <dt>bool `has_service`</dt> <dd>`true` if the guest pass is service-type (not invite-type). Inverse of `can_invite`.</dd> <dt>bool `is_regional`</dt> <dd> `true` if regional access is enabled for the guest pass, i.e. it may be redeemed at other businesses within the franchise region, not only the business that granted it. </dd> <dt>int|null `i_cap_day`</dt> <dd>Maximum guest passes that can be sent per day. `null` if there is no daily cap.</dd> <dt>int|null `i_limit`</dt> <dd>Total number of guest passes initially granted. `null` if the supply is unlimited.</dd> <dt>int|null `i_period`</dt> <dd> Numeric length of the reset period (for example `1` for a one-month period). `null` if the promotion has no reset period. </dd> <dt>int|null `i_remain`</dt> <dd>Number of guest passes remaining in the current period. `null` if the supply is unlimited.</dd> <dt>int|null `i_remain_day`</dt> <dd>Number of guest passes remaining today per the daily cap. `null` if there is no daily cap.</dd> <dt>int `i_use`</dt> <dd>Number of accepted invitations for this guest pass.</dd> <dt>int|null `id_period`</dt> <dd> Unit of the reset period. One of {@link \ADurationSid} constants. `null` if the promotion has no reset period. </dd> <dt>int `id_program_guest`</dt> <dd>Guest pass program ID. One of {@link \RsProgramSid} constants.</dd> <dt>int `id_reset_type_guest`</dt> <dd>Reset type of the host promotion. One of {@link \Wl\Promotion\Guest\Pass\GuestPassResetTypeSid} constants.</dd> <dt>bool `is_expire_note`</dt> <dd>`true` if the pass is close enough to its reset or expiry date that the UI should display a warning.</dd> <dt>bool `is_reset`</dt> <dd>`true` if the remaining count resets on `dl_reset`; `false` if the pass expires on that date.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key from {@link \RsBusinessSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key of the host membership. <i>Primary key from {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Guest promotion key. <i>Primary key from {@link \RsPromotionSql} table.</i></dd> <dt>string `text_location`</dt> <dd> Comma-separated list of location titles where the promotion is valid. Empty when the promotion is business-wide. </dd> <dt>string `text_owner`</dt> <dd>Full name of the membership owner.</dd> <dt>string `text_period`</dt> <dd>Human-readable reset period label (e.g. "1 month"). Empty when there is no reset period.</dd> <dt>string `text_period_date`</dt> <dd> Human-readable date range of the current period (e.g. "Jan 1 - Jan 31"). Empty when there is no reset period or the reset date is unavailable. </dd> <dt>string `text_promotion_grant`</dt> <dd>Title of the host (granting) promotion.</dd> <dt>string `text_promotion_guest`</dt> <dd>Title of the guest promotion.</dd> <dt>string `uid_owner`</dt> <dd>User key of the membership owner. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - GuestPass-GuestPassList
  '/Wl/Login/Promotion/GuestPass/GuestPass.json':
    get:
      summary: 'Retrieves guest pass information for a client''s membership. Returns the guest pass configuration and remaining usage count for the specified login promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Login promotion key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_guest_pass:
                        type: array
                        description: 'Guest pass information. <dl> <dt>array `a_image`</dt> <dd> Thumbnail image data for the guest promotion. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `url-thumbnail`</dt> <dd>Thumbnail url.</dd> </dl> </dd> <dt>array|null `a_period_dl`</dt> <dd> Start and end dates of the current reset period. `null` if there is no reset period or the reset date is unavailable. <dl> <dt>string `dl_end`</dt> <dd>Period end date in MySQL date format.</dd> <dt>string `dl_start`</dt> <dd>Period start date in MySQL date format.</dd> </dl> </dd> <dt>array `a_settings`</dt> <dd> Guest pass settings. Empty array when the guest pass has no settings (class-type passes). <dl> <dt>int `i_claim_day`</dt> <dd> Count of days for accept guest invite. If the invitation is not accepted within this time, it will be canceled. </dd> <dt>int `i_limit`</dt> <dd>Times that member can invite the same guest.</dd> <dt>int `i_limit_duration`</dt> <dd> The time during which a member can invite a guest {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit} times. </dd> <dt>int `id_limit_duration`</dt> <dd> Type of the duration of {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit_duration}. One of {@link \ADurationSid} constants. </dd> <dt>bool `is_checkin`</dt> <dd>Whether guests can only enter the gym when the inviting member is checked in.</dd> <dt>bool `is_limit`</dt> <dd>Whether there are limits for a guest promotion.</dd> </dl> </dd> <dt>bool `can_invite`</dt> <dd>`true` if the guest pass is invite-type (the member sends invitations to guests).</dd> <dt>bool `can_send`</dt> <dd>`true` if the member can currently send a guest pass (eligible and within quota).</dd> <dt>string|null `dl_reset`</dt> <dd>Date on which the pass resets or expires, in MySQL date format. `null` if no expiry date is determined.</dd> <dt>bool `has_service`</dt> <dd>`true` if the guest pass is service-type (not invite-type). Inverse of `can_invite`.</dd> <dt>bool `is_regional`</dt> <dd> `true` if regional access is enabled for the guest pass, i.e. it may be redeemed at other businesses within the franchise region, not only the business that granted it. </dd> <dt>int|null `i_cap_day`</dt> <dd>Maximum guest passes that can be sent per day. `null` if there is no daily cap.</dd> <dt>int|null `i_limit`</dt> <dd>Total number of guest passes initially granted. `null` if the supply is unlimited.</dd> <dt>int|null `i_period`</dt> <dd> Numeric length of the reset period (for example `1` for a one-month period). `null` if the promotion has no reset period. </dd> <dt>int|null `i_remain`</dt> <dd>Number of guest passes remaining in the current period. `null` if the supply is unlimited.</dd> <dt>int|null `i_remain_day`</dt> <dd>Number of guest passes remaining today per the daily cap. `null` if there is no daily cap.</dd> <dt>int `i_use`</dt> <dd>Number of accepted invitations for this guest pass.</dd> <dt>int|null `id_period`</dt> <dd> Unit of the reset period. One of {@link \ADurationSid} constants. `null` if the promotion has no reset period. </dd> <dt>int `id_program_guest`</dt> <dd>Guest pass program ID. One of {@link \RsProgramSid} constants.</dd> <dt>int `id_reset_type_guest`</dt> <dd>Reset type of the host promotion. One of {@link \Wl\Promotion\Guest\Pass\GuestPassResetTypeSid} constants.</dd> <dt>bool `is_expire_note`</dt> <dd>`true` if the pass is close enough to its reset or expiry date that the UI should display a warning.</dd> <dt>bool `is_reset`</dt> <dd>`true` if the remaining count resets on `dl_reset`; `false` if the pass expires on that date.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key from {@link \RsBusinessSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key of the host membership. <i>Primary key from {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Guest promotion key. <i>Primary key from {@link \RsPromotionSql} table.</i></dd> <dt>string `text_location`</dt> <dd> Comma-separated list of location titles where the promotion is valid. Empty when the promotion is business-wide. </dd> <dt>string `text_owner`</dt> <dd>Full name of the membership owner.</dd> <dt>string `text_period`</dt> <dd>Human-readable reset period label (e.g. "1 month"). Empty when there is no reset period.</dd> <dt>string `text_period_date`</dt> <dd> Human-readable date range of the current period (e.g. "Jan 1 - Jan 31"). Empty when there is no reset period or the reset date is unavailable. </dd> <dt>string `text_promotion_grant`</dt> <dd>Title of the host (granting) promotion.</dd> <dt>string `text_promotion_guest`</dt> <dd>Title of the guest promotion.</dd> <dt>string `uid_owner`</dt> <dd>User key of the membership owner. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - GuestPass-GuestPass
    put:
      summary: 'Updates the guest pass remaining usages for a client''s membership. Adjusts the remaining guest pass count to the specified value and logs the change. Requires staff-level edit access for the login promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Login promotion key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_adjust:
                  type: number
                  format: int32
                  description: 'Number of guest passes remaining for the current period.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GuestPass-GuestPass
  '/Wl/Login/Promotion/Convert/Convert.json':
    delete:
      summary: 'Removes conversion and reset the after expiration setting to previous state. Validates access, removes the scheduled conversion record for the given purchased promotion, restores the previous auto-renew state, and reschedules the payment if the promotion is a membership type.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Convert-Convert
    get:
      summary: 'Returns the current conversion configuration and the list of promotions available to convert to. Returns the current conversion type, scheduled date, target promotion, hold period dates, next payment date, expiration date, and the list of promotions available as conversion targets for the given purchased promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data containing the following structure:. <dl> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The title of the promotion.</dd> <dt>int `id_program`</dt> <dd>One of {@link \RsProgramSid} constants.</dd> <dt>bool `is_select`</dt> <dd>If <tt>true</tt>, the promotion is related to the service. Otherwise, this will be <tt>false</tt>.</dd> </dl>'
                      dl_convert_max:
                        type: string
                        description: 'The last date on what conversion can be scheduled.'
                      dl_convert_min:
                        type: string
                        description: 'The first date on what conversion can be scheduled.'
                      dl_hold_end:
                        type: string
                        description: 'Local date, when hold ends, if PO is on hold right now. Empty if PO is not on hold, or hold is endless.'
                      dl_hold_start:
                        type: string
                        description: 'Local date, when hold starts, if PO is on hold right now. Empty if PO is not on hold.'
                      id_convert:
                        type: string
                        description: 'The conversion ID. One of the {@link PromotionConvertSid} constants.'
                      id_convert_when:
                        type: [number, "null"]
                        description: 'When conversion should be done. One of the {@link ConvertWhenSid} constants. `null` if it''s not set yet.'
                      is_edit:
                        type: boolean
                        description: 'Determines whether the conversion request is new or editing an existing conversion. If `true`, the conversion exists. Otherwise, this will be `false`.'
                      is_renew:
                        type: boolean
                        description: '`true` if PO is going to be renewed and not converted. `false` if PO is going to expire or to convert.'
                      s_date_convert:
                        type: string
                        description: 'The existing conversion date, returned as a string for the datepicker.'
                      s_date_now:
                        type: string
                        description: 'The current date, returned as a string.'
                      s_title:
                        type: string
                        description: 'The Purchase Option title.'
                      text_date_expire:
                        type: string
                        description: 'Expiration date in string user-friendly format.'
                      text_date_payment:
                        type: string
                        description: 'Next payment date in string user-friendly format.'
                      text_note:
                        type: [string, "null"]
                        description: 'The note for the promotion conversion.'
      tags:
        - Convert-Convert
    post:
      summary: 'Creates or updates conversion form data for the login promotion. Performs all necessary checks and apply changes. Validates access, the target promotion key, conversion type, conversion timing, and the optional scheduled date, then saves the conversion settings. Returns an error if the user is a debtor.'
      parameters:
        - name: dl_convert
          in: query
          description: 'The conversion date, in the local time zone.'
          schema:
            type: string
        - name: id_convert
          in: query
          description: 'The conversion ID. One of the {@link PromotionConvertSid} constants.'
          schema:
            type: string
        - name: id_convert_when
          in: query
          description: 'When conversion should be done. One of the {@link ConvertWhenSid} constants. `null` if it''s not set yet.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
        - name: k_promotion_to
          in: query
          description: 'The promotion key the given promotion will be converted to.'
          schema:
            type: string
        - name: text_note
          in: query
          description: 'The note for the promotion conversion.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Convert-Convert
  '/Wl/Login/Member/DynamicId/DynamicId.json':
    get:
      summary: 'Returns the member''s barcode and its expiry time for the specified business and user. If the business uses dynamic barcodes, generates or refreshes a time-limited barcode and returns its value, expiry countdown, and an image URL. If the business uses static barcodes, returns the member''s static ID with a zero expiry. If the business uses QR code as the scan format ({@link DynamicIdApi::$is_qr}), also returns the issue timestamp ({@link DynamicIdApi::$tu_qr_issued}) that the Achieve app uses to build and locally refresh the QR code payload, and does not generate a barcode image URL.'
      parameters:
        - name: is_refresh
          in: query
          description: '`true` if a new dynamic ID should be generated. `false` if the existing dynamic ID can be used, if it has not expired. If expired, a new dynamic ID will be generated anyway.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User unique identifier.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_expire:
                        type: number
                        format: int32
                        description: 'Number of seconds left until the dynamic ID expires. Zero means that business uses static barcode.'
                      is_qr:
                        type: boolean
                        description: '`true` if the business uses QR code as the client check-in scan format, `false` if it uses the classic barcode.'
                      text_barcode:
                        type: string
                        description: 'Barcode of the member. Dynamic, if business uses barcode as dynamic ID. Or static, if business uses static ID. In this case {@link DynamicIdApi::$i_expire} will be zero.'
                      tu_qr_issued:
                        type: number
                        format: int32
                        description: 'Unix time in seconds at which {@link DynamicIdApi::$text_barcode} was issued. Only meaningful when {@link DynamicIdApi::$is_qr} is `true`. The Achieve app combines this value with {@link DynamicIdApi::$text_barcode} and a hardcoded 30-second validity window to build the QR code payload, and increments it locally every 30 seconds to refresh the QR code without an additional request to the server.'
                      url_barcode:
                        type: string
                        description: 'URL of the barcode image.'
      tags:
        - DynamicId-DynamicId
  '/Wl/Login/Attendance/Add/Add.json':
    get:
      summary: 'Gets information about ability of user to pay for given session in any ways. Returns available Purchase Options and session passes that can be used to pay for the specified class session, along with the session price, the client''s account balance, and whether the session is free.'
      parameters:
        - name: dt_date_global
          in: query
          description: 'The start date and time of the class in GMT and MySQL format.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the request must be processed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'The client user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_promotion:
                        type: array
                        description: 'Any of the client memberships that can be used to pay for the session. Every element is an array with the following keys: <dl> <dt>string `k_login_promotion`</dt> <dd> The login promotion key, available to pay for the session. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `text_title`</dt> <dd>The title of the login promotion.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'Any user''s session passes that can be used to pay for the session. Every element is an array with the following keys: <dl> <dt>string `k_session_pass`</dt> <dd> The session pass key, available to pay for the session. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `text_title`</dt> <dd>The title of the session pass.</dd> </dl>'
                      is_free:
                        type: boolean
                        description: 'If `true`, the session is free with no methods of payment. If `false`, the session can be paid for.'
                      k_login_promotion:
                        type: string
                        description: 'The key of the user''s promotion to be used for booking. If empty, use any suitable user''s promotion.'
                      k_session_pass:
                        type: string
                        description: 'The key of a session pass that can be used for a single session payment. If no such payment is available, `0` will be returned.'
                      m_account:
                        type: string
                        description: 'Account balance.'
                      m_price:
                        type: [string, "null"]
                        description: 'The price of the session, including any taxes and discounts. If `null`, the price of the session hasn''t been loaded yet.'
                      m_rest:
                        type: [string, "null"]
                        description: 'The user''s account balance if they were charged the {@link AddApi::$m_price} amount.'
      tags:
        - Add-Add
    post:
      summary: 'Adds client to attendance list. Books the specified client into the given class session using the chosen payment option (debit, Purchase Option, session pass, or unpaid), and returns the resulting visit key, visit status, and a store URL if payment is still required.'
      parameters:
        - name: dt_date_global
          in: query
          description: 'The start date and time of the class in GMT and MySQL format.'
          schema:
            type: string
        - name: is_event_single
          in: query
          description: 'Defines whether only single session can be booked for block event. <tt>true</tt> Only current session of the block event will be booked in a case if staff event has appropriate setting to do this action. In this case this session will be considered as session out of event block. <tt>false</tt> all available event sessions will be booked. In this case session will be considered as part of event block.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of the business in which the request must be processed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'The client user key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_add_option:
                  type: number
                  format: int32
                  description: 'Determines how the payment was handled for the session. One of the {@link AddOptionSid} constants.'
                id_mode:
                  type: number
                  format: int32
                  description: 'Determines how the session was booked. One of the {@link ModeSid} constants. We recommend using the `WEB_BACKEND` value.'
                k_login_promotion:
                  type: string
                  description: 'The key of the user''s promotion to be used for booking. If empty, use any suitable user''s promotion.'
                k_session_pass:
                  type: string
                  description: 'The key of a session pass that can be used for a single session payment. If no such payment is available, `0` will be returned.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_visit:
                        type: number
                        format: int32
                        description: 'The status of the visit. One of the {@link VisitSid} constants.'
                      is_paid:
                        type: boolean
                        description: 'If `true`, the visit was automatically paid for in any available way during the booking. If `false`, the visit wasn''t automatically paid for.'
                      k_visit:
                        type: string
                        description: 'The key of the booked visit. This will be set on success. This value will be needed if the session still needs to be paid for.'
                      url_store:
                        type: string
                        description: 'The URL link to the store to allow for the payment of the visit. This link is for web only.'
      tags:
        - Add-Add
  '/Wl/Login/Search/StaffApp/List.json':
    get:
      summary: 'Performs access checks and returns a list of users, depending on the search query. When a search string is provided, returns clients whose name or email matches the query within the specified business. When the search string is empty, returns recently viewed clients. Each result includes the client''s name, email, phone, member ID, and user key.'
      parameters:
        - name: is_franchisee_search_wide
          in: query
          description: '<tt>true</tt> - search among whole franchise; <tt>false</tt> - search in specified business only.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'The search string. Clients can be matched by name or email.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of users matching the search string. The list depends on the search query. If the query is empty, recent users will be returned. Each element is an array with the following keys: <dl> <dt>string `text_mail`</dt> <dd>Client''s email.</dd> <dt>string `text_name_first`</dt> <dd>Client''s first name.</dd> <dt>string `text_name_last`</dt> <dd>Client''s last name.</dd> <dt>string `text_phone`</dt> <dd>Client''s phone.</dd> <dt>string `text_title`</dt> <dd>The client name.</dd> <dt>string `uid`</dt> <dd>The client key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'If `true`, then this user can add other users via the Add Client page.'
      tags:
        - StaffApp-List
  '/Wl/Appointment/Book/Purchase/Purchase.json':
    get:
      summary: 'Retrieves a list of information about available purchase options. Returns all Purchase Options (memberships, packages, passes) the given client can use to pay for the specified appointment at the given location, including pricing details and eligibility status. Also returns options that can be purchased on the spot during booking.'
      parameters:
        - name: a_service
          in: query
          description: 'List of selected services without current {@link PurchaseApi::$k_service}. The list of these services directly affects the list of selected promotions. Depending on the number and order of services, there may be different results. The current {@link PurchaseApi::$k_service} will be added to the end of this list. It is worth considering this list as a list of previously selected services. Each element has the following structure: <dl> <dt>array `a_purchase`</dt> <dd> List of purchase options selected for the service. Should be set if a new purchase option is selected for this service. <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. Constant from {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>Purchase item key.</dd> <dt>bool `is_purchase_previous`</dt> <dd> Should be `true` if this promotion is selected again, i.e. one that has already been applied to another appointment. This is needed to correctly calculate the remaining quantity of promotions with limited uses. </dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>Local date/time to check purchase options expiration.</dd> <dt>string|null `k_login_prize`</dt> <dd> Login prize key. <i>Primary key from {@link \RsLoginPrizeSql}.</i> `null` if no login prize used to pay for this service. </dd> <dt>string|null `k_login_promotion`</dt> <dd>Login promotion key. Should be set if login promotion selected for this service.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>See table {@link \RsServiceSql}.</i></dd> <dt>string `k_timezone`</dt> <dd> The timezone key for `dt_date` field. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link \Wl\Profile\Timezone\ProfileTimezone::createInBusiness()}.</i> In any case, the timezone will be used if the business allows client timezones. </dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to use to check for expiration of Purchase Options.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The asset booking duration.'
          schema:
            type: number
            format: int32
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` - get all Purchase Options suitable for appointment. `false` - get only Purchase Options available for the client.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'The resource key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used to select available Purchase Options. If multiple services are selected, they should be specified in {@link PurchaseApi::$a_service} array.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'The timezone key for {@link PurchaseApi::$dt_date}. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link ProfileTimezone::createInBusiness()}</i> In any case, the timezone will be used if the business allows client timezones.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option. It contains the following information: <dl> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The count of the remaining visits.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in this Purchase Option.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the Purchase Option. For example, four per week.</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period. For example "this week" or "for a four-day period".</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods. Given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The count of future sessions that are paid with this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The usage count of the Purchase Option.</dd> <dt>int `i_visit_past`</dt> <dd> The count of attended sessions before the last renewal. This will be ''0'' if no sessions before the last renewal or if the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period. For example, "this week" or "for a four-day period".</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for promotions. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_share`</dt> <dd> `true` if this purchase option is shared from another user. `false` if this purchase option belongs to the user for whom the request is made. </dd> <dt>string `k_login_promotion`</dt> <dd>The Purchase Option login key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd> If this Purchase Option is a package, then this field contains a list of Purchase Options contained in the package. </dd> <dt>string `uid_owner`</dt> <dd> The user key of the owner of the promotion. Will be different from the user for whom the request is made if the promotion is shared. </dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'An array with information about available Purchase Options. <dl> <dt>array `a_image`</dt> <dd> Information describing the logo of the purchase option. This value can be false if there is no logo described. Image information will have the following fields: <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to `false` value returned in `url-thumbnail` equals value in `url-view`. </dd> <dt>string `url-view`</dt> <dd>Url to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger than specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise, link to original image is returned here. </dd> </dl> </dd> <dt>array `a_payment`</dt> <dd> The set of calculated values for payment: <dl> <dt>string `m_discount`</dt> <dd>The amount of the whole discount of one purchase item.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>A list of calendar restrictions of the Purchase Option in a human-readable format, for example: ''4 per week''.</dd> <dt>string `dt_expire`</dt> <dd>Date, when promotion expires.</dd> <dt>string `dt_start`</dt> <dd>Date, when promotion starts.</dd> <dt>string `f_price`</dt> <dd>The price of the Purchase Option.</dd> <dt>int `i`</dt> <dd>Order number of the purchase option in the list.</dd> <dt>int `i_limit`</dt> <dd>Count of visits that purchase option allows to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>Maximum number of minutes that current promotion can be used.</dd> <dt>int `i_payment_period`</dt> <dd>Count of calendar periods (weeks, months, years) between payment for membership.</dd> <dt>int `id_duration`</dt> <dd>Duration ID. Constant from {@link \ADurationSid}.</dd> <dt>int `id_program`</dt> <dd>Program ID for promotions from {@link \RsProgramSid}.</dd> <dt>int `id_program_type`</dt> <dd>Program type ID. Constant from {@link \RsProgramTypeSid}.</dd> <dt>int `id_promotion_price`</dt> <dd>How the Purchase Item price is specified. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>int `id_purchase_item`</dt> <dd>ID of the purchase item from {@link \RsPurchaseItemSid}</dd> <dt>bool `is_contract`</dt> <dd>This will be `true` if the Purchase Option is a contract. It will `false` otherwise.</dd> <dt>bool `is_description`</dt> <dd>`true` if purchase option has description.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if promotion is introductory offer, `false` otherwise.</dd> <dt>bool `is_renew`</dt> <dd>This will be `true` if the Purchase Option will auto-renew. It will be `false` otherwise.</dd> <dt>bool `is_renew_check`</dt> <dd>Whether Purchase Option is renewable and the "auto-renew" option should be turned on by default.</dd> <dt>bool `is_start`</dt> <dd> This will be `true` if the Purchase Option has a duration that begins on purchase. It will be `false` otherwise. </dd> <dt>string `k_id`</dt> <dd>Primary ID of the element in it''s table.</dd> <dt>string|null `m_price_old`</dt> <dd> Price of single session purchase before online discount. `null` if service does not have online discount. Is set only if this purchase option is purchase of single visit. </dd> <dt>string `s_activation`</dt> <dd>Activation settings of the promotion.</dd> <dt>string `s_class`</dt> <dd>Class for designer to mark purchase options with different icons.</dd> <dt>string `s_class_include`</dt> <dd>List of services included in the promotion.</dd> <dt>string `s_description`</dt> <dd>Description of the purchase option.</dd> <dt>string `s_duration`</dt> <dd>Duration of the promotion.</dd> <dt>string `s_payment_duration`</dt> <dd>Period between payments for memberships.</dd> <dt>string `sid_program_category`</dt> <dd>Category of the program for promotions from {@link \RsProgramCategorySid}.</dd> <dt>string `s_title`</dt> <dd>Name of the purchase option.</dd> <dt>string `s_value`</dt> <dd>Key of the purchase option in the format [`purchase_item_id`]::[`k_id`]</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package. This field contains list of promotions contained in the package.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. <dl> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'Session pass information in a case if user books same appointment second time and already has Drop-in. <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if drop-in rate should be the default purchase option.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
                      k_promotion_default:
                        type: string
                        description: 'Default promotion key. Empty if the appointment has no default promotion.'
                      text_login_promotion:
                        type: string
                        description: 'Login promotion title suitable to pay for the services.'
      tags:
        - Purchase-Purchase
  '/Wl/Appointment/Book/Purchase/Purchase72.json':
    get:
      summary: 'Retrieves a list of information about available purchase options. Extends {@link PurchaseApi::get()} to also support rescheduling an existing appointment by accepting an optional appointment key. When a key is provided, the existing appointment is validated and its current Purchase Option usage is taken into account when calculating eligibility for available options.'
      parameters:
        - name: a_service
          in: query
          description: 'List of selected services without current {@link PurchaseApi::$k_service}. The list of these services directly affects the list of selected promotions. Depending on the number and order of services, there may be different results. The current {@link PurchaseApi::$k_service} will be added to the end of this list. It is worth considering this list as a list of previously selected services. Each element has the following structure: <dl> <dt>array `a_purchase`</dt> <dd> List of purchase options selected for the service. Should be set if a new purchase option is selected for this service. <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. Constant from {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>Purchase item key.</dd> <dt>bool `is_purchase_previous`</dt> <dd> Should be `true` if this promotion is selected again, i.e. one that has already been applied to another appointment. This is needed to correctly calculate the remaining quantity of promotions with limited uses. </dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>Local date/time to check purchase options expiration.</dd> <dt>string|null `k_login_prize`</dt> <dd> Login prize key. <i>Primary key from {@link \RsLoginPrizeSql}.</i> `null` if no login prize used to pay for this service. </dd> <dt>string|null `k_login_promotion`</dt> <dd>Login promotion key. Should be set if login promotion selected for this service.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>See table {@link \RsServiceSql}.</i></dd> <dt>string `k_timezone`</dt> <dd> The timezone key for `dt_date` field. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link \Wl\Profile\Timezone\ProfileTimezone::createInBusiness()}.</i> In any case, the timezone will be used if the business allows client timezones. </dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to use to check for expiration of Purchase Options.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The asset booking duration.'
          schema:
            type: number
            format: int32
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` - get all Purchase Options suitable for appointment. `false` - get only Purchase Options available for the client.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i> Not empty in case when we return payment options for rescheduling existing appointment.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'The resource key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used to select available Purchase Options. If multiple services are selected, they should be specified in {@link PurchaseApi::$a_service} array.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'The timezone key for {@link PurchaseApi::$dt_date}. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link ProfileTimezone::createInBusiness()}</i> In any case, the timezone will be used if the business allows client timezones.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option. It contains the following information: <dl> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The count of the remaining visits.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in this Purchase Option.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the Purchase Option. For example, four per week.</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period. For example "this week" or "for a four-day period".</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods. Given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The count of future sessions that are paid with this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The usage count of the Purchase Option.</dd> <dt>int `i_visit_past`</dt> <dd> The count of attended sessions before the last renewal. This will be ''0'' if no sessions before the last renewal or if the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period. For example, "this week" or "for a four-day period".</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for promotions. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_share`</dt> <dd> `true` if this purchase option is shared from another user. `false` if this purchase option belongs to the user for whom the request is made. </dd> <dt>string `k_login_promotion`</dt> <dd>The Purchase Option login key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd> If this Purchase Option is a package, then this field contains a list of Purchase Options contained in the package. </dd> <dt>string `uid_owner`</dt> <dd> The user key of the owner of the promotion. Will be different from the user for whom the request is made if the promotion is shared. </dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'An array with information about available Purchase Options. <dl> <dt>array `a_image`</dt> <dd> Information describing the logo of the purchase option. This value can be false if there is no logo described. Image information will have the following fields: <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to `false` value returned in `url-thumbnail` equals value in `url-view`. </dd> <dt>string `url-view`</dt> <dd>Url to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger than specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise, link to original image is returned here. </dd> </dl> </dd> <dt>array `a_payment`</dt> <dd> The set of calculated values for payment: <dl> <dt>string `m_discount`</dt> <dd>The amount of the whole discount of one purchase item.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>A list of calendar restrictions of the Purchase Option in a human-readable format, for example: ''4 per week''.</dd> <dt>string `dt_expire`</dt> <dd>Date, when promotion expires.</dd> <dt>string `dt_start`</dt> <dd>Date, when promotion starts.</dd> <dt>string `f_price`</dt> <dd>The price of the Purchase Option.</dd> <dt>int `i`</dt> <dd>Order number of the purchase option in the list.</dd> <dt>int `i_limit`</dt> <dd>Count of visits that purchase option allows to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>Maximum number of minutes that current promotion can be used.</dd> <dt>int `i_payment_period`</dt> <dd>Count of calendar periods (weeks, months, years) between payment for membership.</dd> <dt>int `id_duration`</dt> <dd>Duration ID. Constant from {@link \ADurationSid}.</dd> <dt>int `id_program`</dt> <dd>Program ID for promotions from {@link \RsProgramSid}.</dd> <dt>int `id_program_type`</dt> <dd>Program type ID. Constant from {@link \RsProgramTypeSid}.</dd> <dt>int `id_promotion_price`</dt> <dd>How the Purchase Item price is specified. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>int `id_purchase_item`</dt> <dd>ID of the purchase item from {@link \RsPurchaseItemSid}</dd> <dt>bool `is_contract`</dt> <dd>This will be `true` if the Purchase Option is a contract. It will `false` otherwise.</dd> <dt>bool `is_description`</dt> <dd>`true` if purchase option has description.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if promotion is introductory offer, `false` otherwise.</dd> <dt>bool `is_renew`</dt> <dd>This will be `true` if the Purchase Option will auto-renew. It will be `false` otherwise.</dd> <dt>bool `is_renew_check`</dt> <dd>Whether Purchase Option is renewable and the "auto-renew" option should be turned on by default.</dd> <dt>bool `is_start`</dt> <dd> This will be `true` if the Purchase Option has a duration that begins on purchase. It will be `false` otherwise. </dd> <dt>string `k_id`</dt> <dd>Primary ID of the element in it''s table.</dd> <dt>string|null `m_price_old`</dt> <dd> Price of single session purchase before online discount. `null` if service does not have online discount. Is set only if this purchase option is purchase of single visit. </dd> <dt>string `s_activation`</dt> <dd>Activation settings of the promotion.</dd> <dt>string `s_class`</dt> <dd>Class for designer to mark purchase options with different icons.</dd> <dt>string `s_class_include`</dt> <dd>List of services included in the promotion.</dd> <dt>string `s_description`</dt> <dd>Description of the purchase option.</dd> <dt>string `s_duration`</dt> <dd>Duration of the promotion.</dd> <dt>string `s_payment_duration`</dt> <dd>Period between payments for memberships.</dd> <dt>string `sid_program_category`</dt> <dd>Category of the program for promotions from {@link \RsProgramCategorySid}.</dd> <dt>string `s_title`</dt> <dd>Name of the purchase option.</dd> <dt>string `s_value`</dt> <dd>Key of the purchase option in the format [`purchase_item_id`]::[`k_id`]</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package. This field contains list of promotions contained in the package.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. <dl> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'Session pass information in a case if user books same appointment second time and already has Drop-in. <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if drop-in rate should be the default purchase option.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
                      k_promotion_default:
                        type: string
                        description: 'Default promotion key. Empty if the appointment has no default promotion.'
                      text_login_promotion:
                        type: string
                        description: 'Login promotion title suitable to pay for the services.'
      tags:
        - Purchase-Purchase72
  '/Wl/Appointment/Book/Service/ServiceList52.json':
    get:
      summary: 'Retrieves information about services in the current service category. Returns all services available for booking at the given location, optionally filtered by service category, book now tab, and client. Each service entry includes title, description, image, pricing, duration, staff availability, and age restriction details. Supports both frontend and backend modes.'
      parameters:
        - name: a_class_tab
          in: query
          description: 'The class tab key to use to filter services. If empty, this can be found on the standard book tab. If multiple tabs are sent, appointment types, which are in at least in one of the tabs, will be in the result.'
          schema:
            type: array
        - name: a_service_filter
          in: query
          description: 'List of services to filter a result. Each element is a service key<i>, a primary key in {@link \RsServiceSql}</i>. Empty if the filtering is not necessary.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'List of staff members to filter a result. Empty if the filtering is not necessary.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: '`true` - return all active services for a certain location ignoring user age and other restrictions. `false` - return only services that are allowed for the user.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only on the selected book now tab.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_service_category
          in: query
          description: 'The key of a service category to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'A list of services with information about them. <b>Key</b> - the service key. <b>Value</b> - an array, with every element consisting of the next keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>The list of tab keys for the service.</dd> <dt>array[] `a_direct_link`</dt> <dd> A list of links to start booking from a direct link. This can''t be one link, as the same appointment can be available in several booking tabs. Therefore, each booking tab has its own direct booking link. Each element has two values: <dl> <dt>string `k_class_tab`</dt> <dd>The key of the book now tab.</dd> <dt>string `url_tab`</dt> <dd>The booking URL. This will open the booking wizard under the related booking tab.</dd> </dl> </dd> <dt>array|null `a_config`</dt> <dd> Appointment-specific business policy overrides. `null` when the service uses the general business policy. When not `null`, contains a subset of business config fields, including: <dl> <dt>bool `is_back_to_back`</dt> <dd>`true` if this service supports back-to-back booking.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book this service on a recurring basis.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings for this service default to weekly recurring with no end date.</dd> <dt>bool `is_multiple_booking`</dt> <dd>`true` if multiple bookings are allowed for this service.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if clients on the wait list do not have to pay upfront.</dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Information about the appointment image. An array with the following values: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if the image is not set.</dd> <dt>string `k_service`</dt> <dd>The service key.</dd> <dt>string `s_service`</dt> <dd>The name of the service.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl> </dd> <dt>string[] `a_login_type_restriction`</dt> <dd> Keys are login type keys. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i> Values - List of login types'' titles for current service. Clients that have one of these types can book service. </dd> <dt>string[] `a_member_group_restriction`</dt> <dd> Keys are member group keys. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i> Values - list of member groups'' titles for current service. Clients that belongs to these groups can book service. </dd> <dt>string `f_deposit`</dt> <dd>The amount of deposit required.</dd> <dt>string `f_offline_max`</dt> <dd>The maximum offline price.</dd> <dt>string `f_offline_min`</dt> <dd>The minimum offline price.</dd> <dt>string `f_online`</dt> <dd>The online price.</dd> <dt>bool `hide_application`</dt> <dd> Determines whether the service will be hidden in the White Label mobile application. `true` means that service won''t be displayed. Otherwise, this will be `false`. </dd> <dt>string `html_deny_reason`</dt> <dd>Human-readable reason why the client cannot book this service. Empty string if there is no deny reason.</dd> <dt>int `i_age_from`</dt> <dd>The required minimum client age to book an appointment (years part).</dd> <dt>int `i_age_from_month`</dt> <dd>The required minimum client age to book an appointment (months part).</dd> <dt>int `i_age_to`</dt> <dd>The required maximum client age to book an appointment (years part).</dd> <dt>int `i_age_to_month`</dt> <dd>The required maximum client age to book an appointment (months part).</dd> <dt>int `i_price`</dt> <dd>The price type ID. One of {@link \RsServicePriceSid} constants.</dd> <dt>int `i_duration`</dt> <dd>The appointment duration in minutes.</dd> <dt>int `i_padding_after`</dt> <dd> Padding time after the end of the appointment, in minutes. Used to detect when a staff member is busy after this appointment when booking back-to-back appointments. </dd> <dt>int `i_padding_before`</dt> <dd> Padding time before the beginning of the appointment, in minutes. Used to detect when a staff member is busy before this appointment when booking back-to-back appointments. </dd> <dt>int `id_book_flow`</dt> <dd>The type of client booking flow. One of {@link \Wl\Service\ServiceBookFlowSid} constants.</dd> <dt>int|null `id_deny_reason`</dt> <dd> The ID of the reason why the client cannot book this service. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. `null` if there is no deny reason. </dd> <dt>int `id_service_require`</dt> <dd>The required payment type ID. One of {@link \RsServiceRequireSid} constants.</dd> <dt>int|null `id_virtual_provider`</dt> <dd> Virtual provider ID. One of {@link \Wl\Virtual\VirtualProviderSid} constants. `null` for non-virtual services. </dd> <dt>bool `is_age_public`</dt> <dd>`true` if age restrictions are public. Otherwise, `false` if they should be hidden from clients.</dd> <dt>bool `is_age_restricted`</dt> <dd>Determines whether this service can''t be booked due to age restrictions.</dd> <dt>bool `is_back_to_back`</dt> <dd>Determines whether this service supports back-to-back booking.</dd> <dt>bool `is_bookable`</dt> <dd>Whether this appointment can be booked online.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book classes and appointments on a recurring basis. Otherwise, this `false`.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings default to weekly recurring with no end date, `false` otherwise.</dd> <dt>bool `is_deposit_percent`</dt> <dd>`true` if `f_deposit` is a percentage. `false` if `f_deposit` is an amount of money.</dd> <dt>bool `is_gender_select`</dt> <dd>`true` if clients can select the staff member''s gender. Otherwise, this will be `false`.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if clients can buy this appointment. `false` if only staff members can sell it.</dd> <dt>bool `is_resource_type`</dt> <dd>`true` if the service requires assets. Otherwise, this will be `false`.</dd> <dt>bool `is_single_buy`</dt> <dd> `true` if the appointment can be booked without a Purchase Option. `false` if it''s necessary to buy a Purchase Option. </dd> <dt>bool `is_staff_confirm`</dt> <dd>`true` if the appointment must be confirmed by a staff member after booking. Otherwise, `false`.</dd> <dt>bool `is_staff_skip`</dt> <dd>`true` if clients can select a staff member for the appointment. Otherwise, `false`.</dd> <dt>bool `is_question`</dt> <dd>Determines whether the service will ask for questions or not.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if the service is virtual. Otherwise, this will be `false`.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location if service requires such type of asset.</dd> <dt>string `k_service`</dt> <dd>The appointment primary key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_service_category`</dt> <dd>The service category primary key. <i>Primary key in {@link \RsServiceCategorySql} table.</i></dd> <dt>string `s_duration`</dt> <dd>The appointment duration in a human-readable format.</dd> <dt>string `s_service`</dt> <dd>The appointment title.</dd> <dt>string|null `sid_deny_reason`</dt> <dd>String representation of the deny reason. `null` if no deny reason.</dd> <dt>string `text_age_restriction`</dt> <dd>Age restriction header.</dd> <dt>string `xml_description`</dt> <dd>Appointment description (deprecated, use `html_description`).</dd> <dt>string `xml_description_short`</dt> <dd>Appointment short description (deprecated, use `html_description_short`).</dd> <dt>string `xml_special`</dt> <dd>Special instructions (deprecated, use `html_special`).</dd> </dl>'
                      is_multiple_booking:
                        type: boolean
                        description: 'Whether services allow multiple appointment booking.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Service-ServiceList52
  '/Wl/Appointment/Book/Service/ServiceCheck.json':
    get:
      summary: 'Checks whether a service can be booked by the given client at the given date, throwing an exception if booking is restricted. Validates the client''s age against the service''s age restrictions and checks the booking date against the service''s booking window. Returns without data on success; throws exception with a descriptive message when booking is not allowed.'
      parameters:
        - name: dt_date
          in: query
          description: 'Date of booking in MySQL format.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-ServiceCheck
  '/Wl/Appointment/Book/Service/Category.json':
    get:
      summary: 'Retrieves a list of information about service categories on the appointment booking page. Returns all service categories available at the given location, optionally filtered by book now tab. Each category includes its title and the list of available services. Supports both frontend and backend modes; in backend mode access is verified against the business.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: is_backend
          in: query
          description: '`true` - return all service categories for a certain location. `false` - return only service categories that are associated with a book tab and with staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_flag_ignore
          in: query
          description: 'Whether a flagged client is allowed to retrieve categories. `true` - allowed, `false` - not allowed.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected book tab.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'The class tab key used to filter services.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category:
                        type: array
                        description: 'A list of information about service categories. <dl> <dt>bool `hide_application`</dt> <dd> <tt>true</tt> - all services are hidden in this category for White Label mobile application. <tt>false</tt> - otherwise. </dd> <dt>int `i_sort`</dt> <dd>Sort key for category. Used to sort categories on category list page.</dd> <dt>string `k_service_category`</dt> <dd>Service category key. <i>Primary key in {@link \RsServiceCategorySql} table.</i></dd> <dt>string `s_title`</dt> <dd>Service category title.</dd> </dl>'
                      is_client_flag:
                        type: boolean
                        description: '<tt>true</tt> - if client has a flag, <tt>false</tt> - otherwise.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Service-Category
  '/Wl/Appointment/Book/Service/ServiceList.json':
    get:
      summary: 'Retrieves information about services in the current service category. Returns all services available for booking at the given location, optionally filtered by service category, book now tab, and client. Each service entry includes title, description, image, pricing, duration, staff availability, and age restriction details. Supports both frontend and backend modes.'
      parameters:
        - name: a_class_tab
          in: query
          description: 'The class tab key to use to filter services. If empty, this can be found on the standard book tab. If multiple tabs are sent, appointment types, which are in at least in one of the tabs, will be in the result.'
          schema:
            type: array
        - name: a_service_filter
          in: query
          description: 'List of services to filter a result. Each element is a service key<i>, a primary key in {@link \RsServiceSql}</i>. Empty if the filtering is not necessary.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'List of staff members to filter a result. Empty if the filtering is not necessary.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: '`true` - return all active services for a certain location ignoring user age and other restrictions. `false` - return only services that are allowed for the user.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only on the selected book now tab.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'The class tab key to filter services. If empty or `0`, this can be found on the standard book now tab.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_service_category
          in: query
          description: 'The key of a service category to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'A list of services with information about them. <b>Key</b> - the service key. <b>Value</b> - an array, with every element consisting of the next keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>The list of tab keys for the service.</dd> <dt>array[] `a_direct_link`</dt> <dd> A list of links to start booking from a direct link. This can''t be one link, as the same appointment can be available in several booking tabs. Therefore, each booking tab has its own direct booking link. Each element has two values: <dl> <dt>string `k_class_tab`</dt> <dd>The key of the book now tab.</dd> <dt>string `url_tab`</dt> <dd>The booking URL. This will open the booking wizard under the related booking tab.</dd> </dl> </dd> <dt>array|null `a_config`</dt> <dd> Appointment-specific business policy overrides. `null` when the service uses the general business policy. When not `null`, contains a subset of business config fields, including: <dl> <dt>bool `is_back_to_back`</dt> <dd>`true` if this service supports back-to-back booking.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book this service on a recurring basis.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings for this service default to weekly recurring with no end date.</dd> <dt>bool `is_multiple_booking`</dt> <dd>`true` if multiple bookings are allowed for this service.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if clients on the wait list do not have to pay upfront.</dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Information about the appointment image. An array with the following values: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if the image is not set.</dd> <dt>string `k_service`</dt> <dd>The service key.</dd> <dt>string `s_service`</dt> <dd>The name of the service.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl> </dd> <dt>string[] `a_login_type_restriction`</dt> <dd> Keys are login type keys. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i> Values - List of login types'' titles for current service. Clients that have one of these types can book service. </dd> <dt>string[] `a_member_group_restriction`</dt> <dd> Keys are member group keys. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i> Values - list of member groups'' titles for current service. Clients that belongs to these groups can book service. </dd> <dt>string `f_deposit`</dt> <dd>The amount of deposit required.</dd> <dt>string `f_offline_max`</dt> <dd>The maximum offline price.</dd> <dt>string `f_offline_min`</dt> <dd>The minimum offline price.</dd> <dt>string `f_online`</dt> <dd>The online price.</dd> <dt>bool `hide_application`</dt> <dd> Determines whether the service will be hidden in the White Label mobile application. `true` means that service won''t be displayed. Otherwise, this will be `false`. </dd> <dt>string `html_deny_reason`</dt> <dd>Human-readable reason why the client cannot book this service. Empty string if there is no deny reason.</dd> <dt>int `i_age_from`</dt> <dd>The required minimum client age to book an appointment (years part).</dd> <dt>int `i_age_from_month`</dt> <dd>The required minimum client age to book an appointment (months part).</dd> <dt>int `i_age_to`</dt> <dd>The required maximum client age to book an appointment (years part).</dd> <dt>int `i_age_to_month`</dt> <dd>The required maximum client age to book an appointment (months part).</dd> <dt>int `i_price`</dt> <dd>The price type ID. One of {@link \RsServicePriceSid} constants.</dd> <dt>int `i_duration`</dt> <dd>The appointment duration in minutes.</dd> <dt>int `i_padding_after`</dt> <dd> Padding time after the end of the appointment, in minutes. Used to detect when a staff member is busy after this appointment when booking back-to-back appointments. </dd> <dt>int `i_padding_before`</dt> <dd> Padding time before the beginning of the appointment, in minutes. Used to detect when a staff member is busy before this appointment when booking back-to-back appointments. </dd> <dt>int `id_book_flow`</dt> <dd>The type of client booking flow. One of {@link \Wl\Service\ServiceBookFlowSid} constants.</dd> <dt>int|null `id_deny_reason`</dt> <dd> The ID of the reason why the client cannot book this service. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. `null` if there is no deny reason. </dd> <dt>int `id_service_require`</dt> <dd>The required payment type ID. One of {@link \RsServiceRequireSid} constants.</dd> <dt>int|null `id_virtual_provider`</dt> <dd> Virtual provider ID. One of {@link \Wl\Virtual\VirtualProviderSid} constants. `null` for non-virtual services. </dd> <dt>bool `is_age_public`</dt> <dd>`true` if age restrictions are public. Otherwise, `false` if they should be hidden from clients.</dd> <dt>bool `is_age_restricted`</dt> <dd>Determines whether this service can''t be booked due to age restrictions.</dd> <dt>bool `is_back_to_back`</dt> <dd>Determines whether this service supports back-to-back booking.</dd> <dt>bool `is_bookable`</dt> <dd>Whether this appointment can be booked online.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book classes and appointments on a recurring basis. Otherwise, this `false`.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings default to weekly recurring with no end date, `false` otherwise.</dd> <dt>bool `is_deposit_percent`</dt> <dd>`true` if `f_deposit` is a percentage. `false` if `f_deposit` is an amount of money.</dd> <dt>bool `is_gender_select`</dt> <dd>`true` if clients can select the staff member''s gender. Otherwise, this will be `false`.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if clients can buy this appointment. `false` if only staff members can sell it.</dd> <dt>bool `is_resource_type`</dt> <dd>`true` if the service requires assets. Otherwise, this will be `false`.</dd> <dt>bool `is_single_buy`</dt> <dd> `true` if the appointment can be booked without a Purchase Option. `false` if it''s necessary to buy a Purchase Option. </dd> <dt>bool `is_staff_confirm`</dt> <dd>`true` if the appointment must be confirmed by a staff member after booking. Otherwise, `false`.</dd> <dt>bool `is_staff_skip`</dt> <dd>`true` if clients can select a staff member for the appointment. Otherwise, `false`.</dd> <dt>bool `is_question`</dt> <dd>Determines whether the service will ask for questions or not.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if the service is virtual. Otherwise, this will be `false`.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location if service requires such type of asset.</dd> <dt>string `k_service`</dt> <dd>The appointment primary key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_service_category`</dt> <dd>The service category primary key. <i>Primary key in {@link \RsServiceCategorySql} table.</i></dd> <dt>string `s_duration`</dt> <dd>The appointment duration in a human-readable format.</dd> <dt>string `s_service`</dt> <dd>The appointment title.</dd> <dt>string|null `sid_deny_reason`</dt> <dd>String representation of the deny reason. `null` if no deny reason.</dd> <dt>string `text_age_restriction`</dt> <dd>Age restriction header.</dd> <dt>string `xml_description`</dt> <dd>Appointment description (deprecated, use `html_description`).</dd> <dt>string `xml_description_short`</dt> <dd>Appointment short description (deprecated, use `html_description_short`).</dd> <dt>string `xml_special`</dt> <dd>Special instructions (deprecated, use `html_special`).</dd> </dl>'
                      is_multiple_booking:
                        type: boolean
                        description: 'Whether services allow multiple appointment booking.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Service-ServiceList
  '/Wl/Appointment/Book/Staff/List.json':
    get:
      summary: 'Retrieves an information about staff members for the current service. Returns the list of staff members who can perform the specified service at the given location on the specified date and time. Each entry includes the staff member''s name, image, gender, and availability. An ''any staff'' option is included when the service allows random staff assignment.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date/time of the appointment selected by user, in the location''s time zone.'
          schema:
            type: string
        - name: i_duration_custom
          in: query
          description: 'Custom appointment duration in minutes. `null` if not set.'
          schema:
            type: [number, "null"]
        - name: id_role
          in: query
          description: 'User role by whom this api called. For different roles different results might be generated.'
          schema:
            type: number
            format: int32
        - name: is_unavailable
          in: query
          description: '`true` - returns service categories that have no staff members available to conduct them. `false` - returns only service categories that have staff members available to conduct them.'
          schema:
            type: boolean
        - name: k_appointment_ignore
          in: query
          description: 'Key of appointment which must be ignored when searches available staff.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The key of a service for which to show information.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'User''s timezone. `null` until initialized or to use location timezone.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key for whom the service is booking. `null` when not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'A list of staff members with information about them. <dl> <dt>int `id_gender`</dt> <dd>Staff member''s gender. One of {@link \AGenderSid} constants.</dd> <dt>int|null `i_free_spot`</dt> <dd> How many clients can still be booked with the staff member at the requested time. <p>If {@link \Wl\Appointment\Book\Staff\ListApi::$dt_date} is not passed, this value is `null`.</p> <p>If {@link \Wl\Appointment\Book\Staff\ListApi::$dt_date} is passed, this is calculated for the specific staff member at that time.</p> </dd> <dt>bool `is_available`</dt> <dd> Whether staff member is available for booking. Note, if staff member reached daily limits, this field will be different for client and staff booking flows. If client books, such staff member is not available and this field is `false`. If staff member books, such staff member is available. </dd> <dt>bool `is_daily_limit`</dt> <dd>Whether staff member reached daily limits on number or total duration of the appointments for one day.</dd> <dt>bool `is_wait_list`</dt> <dd>Whether staff member available only for wait list booking.</dd> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. <i>Primary key in {@link \RsStaffSql}.</i> Returned only for allow-listed apps.</dd> <dt>string `s_position`</dt> <dd>Position of the staff member in the business.</dd> <dt>string `s_staff`</dt> <dd>Name of the staff member.</dd> <dt>string `uid`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `xml_biography`</dt> <dd>Biography of the staff member.</dd> </dl>'
                      can_book_unavailable_staff:
                        type: boolean
                        description: 'Can staff booked unavailable staff.'
                      has_gender:
                        type: boolean
                        description: 'Determines whether to select the staff member''s gender for the appointment.'
                      has_staff:
                        type: boolean
                        description: 'Determines whether to select staff member(s) for the appointment.'
                      is_gender_different:
                        type: boolean
                        description: 'Determines if the staff list has male and female members.'
      tags:
        - Staff-List
  '/Wl/Appointment/Book/Staff/StaffList.json':
    get:
      summary: 'Retrieves an information about staff members for the current service. Returns the list of staff members who can perform the specified service at the given location on the specified date and time. Each entry includes the staff member''s name, image, gender, and availability. An ''any staff'' option is included when the service allows random staff assignment.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date/time of the appointment selected by user, in the location''s time zone.'
          schema:
            type: string
        - name: i_client
          in: query
          description: 'Count of clients on the appointment. Default value is 1.'
          schema:
            type: number
            format: int32
        - name: i_duration_custom
          in: query
          description: 'Custom appointment duration in minutes. `null` if not set.'
          schema:
            type: [number, "null"]
        - name: id_role
          in: query
          description: 'User role by whom this api called. For different roles different results might be generated.'
          schema:
            type: number
            format: int32
        - name: is_unavailable
          in: query
          description: '`true` - returns service categories that have no staff members available to conduct them. `false` - returns only service categories that have staff members available to conduct them.'
          schema:
            type: boolean
        - name: k_appointment_ignore
          in: query
          description: 'Key of appointment which must be ignored when searches available staff.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The key of a service for which to show information.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'User''s timezone. `null` until initialized or to use location timezone.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key for whom the service is booking. `null` when not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'A list of staff members with information about them. <dl> <dt>int `id_gender`</dt> <dd>Staff member''s gender. One of {@link \AGenderSid} constants.</dd> <dt>int|null `i_free_spot`</dt> <dd> How many clients can still be booked with the staff member at the requested time. <p>If {@link \Wl\Appointment\Book\Staff\ListApi::$dt_date} is not passed, this value is `null`.</p> <p>If {@link \Wl\Appointment\Book\Staff\ListApi::$dt_date} is passed, this is calculated for the specific staff member at that time.</p> </dd> <dt>bool `is_available`</dt> <dd> Whether staff member is available for booking. Note, if staff member reached daily limits, this field will be different for client and staff booking flows. If client books, such staff member is not available and this field is `false`. If staff member books, such staff member is available. </dd> <dt>bool `is_daily_limit`</dt> <dd>Whether staff member reached daily limits on number or total duration of the appointments for one day.</dd> <dt>bool `is_wait_list`</dt> <dd>Whether staff member available only for wait list booking.</dd> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. <i>Primary key in {@link \RsStaffSql}.</i> Returned only for allow-listed apps.</dd> <dt>string `s_position`</dt> <dd>Position of the staff member in the business.</dd> <dt>string `s_staff`</dt> <dd>Name of the staff member.</dd> <dt>string `uid`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `xml_biography`</dt> <dd>Biography of the staff member.</dd> </dl>'
                      can_book_unavailable_staff:
                        type: boolean
                        description: 'Can staff booked unavailable staff.'
                      has_gender:
                        type: boolean
                        description: 'Determines whether to select the staff member''s gender for the appointment.'
                      has_staff:
                        type: boolean
                        description: 'Determines whether to select staff member(s) for the appointment.'
                      is_gender_different:
                        type: boolean
                        description: 'Determines if the staff list has male and female members.'
      tags:
        - Staff-StaffList
  '/Wl/Appointment/Book/Finish/Finish47.json':
    get:
      summary: 'Loads data to prepare client side to complete booking. Returns notification settings (email, push, SMS) for the appointment creation confirmation so the client side can display the appropriate notification options before finalizing the booking.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_notification:
                        type: array
                        description: 'Information for sending an appointment notification. <dl> <dt>bool `is_attach`</dt> <dd>`true` to attach calendar file, `false` to not attach calendar file.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `true` if yes, `false` if no.</dd> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Finish-Finish47
    post:
      summary: 'Completes the appointment booking and logs variable counts for diagnostic purposes. Delegates to the parent {@link FinishApi::post()} implementation after logging the total variable counts from GET, POST, and the booking data array. The logging is temporary and intended to diagnose "Too many variables" errors in production.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: a_user
          in: query
          description: 'Data to create new user. Specify this if <var>uid</var> is empty. Must contain the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes to add to user.</dd> <dt>string `text_mail`</dt> <dd>Mail.</dd> <dt>string `text_name_first`</dt> <dd>First name.</dd> <dt>string `text_name_last`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> </dl>'
          schema:
            type: array
        - name: id_pay
          in: query
          description: 'The payment type ID for the service. One of the {@link \RsAppointmentPaySid} constants. The field is left for compatibility with old code and to control the new algorithm.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This should be set if you''re rebooking an existing appointment. Otherwise, use `0` to book a new appointment.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_answer:
                  type: array
                  description: 'A list of answers for the questions from {@link \Wl\Appointment\Book\Question\QuestionApi::$a_question}. Keys refer to hashes of the questions. Values refer to answers for the questions.'
                a_book_data:
                  type: array
                  description: 'The documentation is the same as in {@link FinishApi::$a_book_data}. <dl> <dt>array `a_conflict`</dt> <dd> Information about booking conflicts. Keys are bookings dates/times in MySQL format in UTC. Values are arrays with next keys: <dl> <dt>string `dt_date_local`</dt> <dd>New appointment date/time in MySQL in locale timezone.</dd> <dt>int `i_duration`</dt> <dd>New asset booking duration.</dd> <dt>int `i_index`</dt> <dd>New asset index.</dd> <dt>int `id_conflict`</dt> <dd>Solution type. One of {@link \RsAppointmentEditConflictSid} constants.</dd> <dt>string `k_resource`</dt> <dd>New asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string|null `k_staff`</dt> <dd>New staff member. <i>Primary key in {@link \RsStaffSql} table.</i> `null` in a case of asset booking.</dd> </dl> </dd> <dt>array `a_product`</dt> <dd> Add-ons to the appointment. Specify this for appointment bookings only. Old format: array keys refer to primary keys. <i>See table {@link \RsShopProductOptionSql}.</i> New format: each element is an array: <dl> <dt>int `i_count`</dt> <dd>The add-on count</dd> <dt>int `i_count_use`</dt> <dd>The add-on use count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The key of add-on. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl> </dd> <dt>array `a_repeat`</dt> <dd> Recurring booking information: This will be empty if the appointment isn''t recurring. <dl> <dt>int[] `a_week`</dt> <dd> The days of week when appointment repeats. One of the {@link \ADateWeekSid} constants. This will be empty if the appointment doesn''t repeat weekly. </dd> <dt>string `dl_end`</dt> <dd> The date when the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle must stop. This will be empty if the repeat cycle doesn''t stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>The frequency of the appointment''s repeat cycle.</dd> <dt>int `id_period`</dt> <dd>The measurement unit of <tt>i_period</tt>. One of the {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> <tt>true</tt> if the appointment repeats monthly on the same date. <tt>false</tt> if the appointment repeats monthly on the same day of the week. <tt>null</tt> if the appointment doesn''t repeat monthly. </dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> A list of assets for the appointment booking. Keys refer to asset categories. <i>Primary keys in {@link \RsResourceTypeSql} table.</i> Values refer to arrays with the next keys: Specify this only for the appointment booking. <dl> <dt>int `i_index`</dt> <dd>The asset index on the layout. Specify this only if the asset category has a layout.</dd> <dt>string `k_resource`</dt> <dd>The asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>The date/time for the booking in MySQL format in the location''s time zone.</dd> <dt>int `i_duration`</dt> <dd>The duration of asset booking in minutes. Specify this for separate asset bookings only.</dd> <dt>int `i_index`</dt> <dd> The asset index on the layout. Specify this for separate asset bookings only and for cases when the asset category only has a layout. </dd> <dt>int `id_class_tab`</dt> <dd>The booking service type. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>int `id_gender_staff`</dt> <dd> The gender of the staff member conducting the appointment. One of the {@link \AGenderSid} constants. Specify this for appointment bookings only. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>If `true`, appointment waits unpaid.</dd> <dt>string `k_login_prize`</dt> <dd>The user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd> The user''s pass (for example, a membership or a package). Specify this if you want to set the pass to use to pay for the booking. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_resource`</dt> <dd> The asset booking. Specify this for separate asset bookings only. <i>Primary key in {@link \RsResourceSql} table.</i> </dd> <dt>string `k_service`</dt> <dd> The appointment booking. Specify this for appointment bookings only. <i>Primary key in {@link \RsServiceSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd>The user''s pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `k_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `k_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this an <var>k_staff</var> is that this value must be set only in cases when you want to add a customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> </dl>'
                a_notification:
                  type: array
                  description: 'Information for sending an appointment notification. <dl> <dt>bool `is_attach`</dt> <dd>`true` to attach calendar file, `false` to not attach calendar file.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `true` if yes, `false` if no.</dd> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'Payment is not processed by this API. Use the following APIs for payment: * {@link \Wl\Appointment\Book\Payment\PaymentApi} * {@link \Wl\Appointment\Book\Payment\PaymentPostApi} * {@link \Wl\Appointment\Book\Payment\PaymentMultipleApi}'
                a_payment_data:
                  type: array
                  description: 'Data required for payment. Has next structure: <dl> <dt>int `id_purchase_item`</dt> <dd>Type of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> </dl>'
                a_purchase_item:
                  type: array
                  description: 'The purchase item keys. Empty if no purchases are made for the appointment booking.'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Key is quiz key. Value is quiz response key.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The booking mode ID. One of the {@link ModeSid} constants.'
                is_back_to_back:
                  type: boolean
                  description: 'Determines whether multiple appointments are booked in back-to-back mode.'
                is_unpaid_force:
                  type: boolean
                  description: 'If `true`, the appointment is booked as unpaid. Otherwise, this will be `false` to select an available Purchase Option.'
                m_pay:
                  type: string
                  description: 'The sum paid without tax. Only used for the following types of purchases: * {@link \RsPurchaseItemSid::SERVICE} * {@link \RsPurchaseItemSid::RESOURCE} * {@link \RsPurchaseItemSid::RESOURCE_DEPOSIT} * {@link \RsPurchaseItemSid::APPOINTMENT_DEPOSIT} The field is left for compatibility with old code and to control the new algorithm.'
                s_id:
                  type: string
                  description: 'Unique identifier of the wizard.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'The keys of the booked appointments. Every element has key: <dl> <dt>string `k_appointment`</dt> <dd>The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> </dl>'
                      a_login_activity_visit:
                        type: array
                        description: 'The activity keys of the bookings that were made.'
                      a_visit:
                        type: array
                        description: 'The keys of visits.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
      tags:
        - Finish-Finish47
  '/Wl/Appointment/Book/Finish/Finish.json':
    get:
      summary: 'Loads data to prepare client side to complete booking. Returns notification settings (email, push, SMS) for the appointment creation confirmation so the client side can display the appropriate notification options before finalizing the booking.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_notification:
                        type: array
                        description: 'Information for sending an appointment notification. <dl> <dt>bool `is_attach`</dt> <dd>`true` to attach calendar file, `false` to not attach calendar file.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `true` if yes, `false` if no.</dd> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Finish-Finish
    post:
      summary: 'Completes the appointment booking and processes payment for the client. Delegates to {@link FinishMultipleApi::post()} to handle a single appointment booking, mapping the legacy single-provider request fields to the multi-provider format. Creates an appointment record, applies the selected Purchase Option, and sends the booking confirmation notification.'
      parameters:
        - name: a_book_data
          in: query
          description: 'All data from the provider model <tt>Wl_Appointment_Book_ProviderModel</tt>: <dl> <dt>array `a_conflict`</dt> <dd> Information about booking conflicts. Keys are bookings dates/times in MySQL format in UTC. Values are arrays with next keys: <dl> <dt>string `dt_date_local`</dt> <dd>New appointment date/time in MySQL in locale timezone.</dd> <dt>int `i_duration`</dt> <dd>New asset booking duration.</dd> <dt>int `i_index`</dt> <dd>New asset index.</dd> <dt>int `id_conflict`</dt> <dd>Solution type. One of {@link \RsAppointmentEditConflictSid} constants.</dd> <dt>string `k_resource`</dt> <dd>New asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string|null `k_staff`</dt> <dd>New staff member. <i>Primary key in {@link \RsStaffSql} table.</i> `null` in a case of asset booking.</dd> </dl> </dd> <dt>array `a_product`</dt> <dd> Add-ons to the appointment. Specify this for appointment bookings only. Old format: array keys refer to primary keys. <i>See table {@link \RsShopProductOptionSql}.</i> New format: each element is an array: <dl> <dt>int `i_count`</dt> <dd>The add-on count</dd> <dt>int `i_count_use`</dt> <dd>The add-on use count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The key of add-on. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl> </dd> <dt>array `a_repeat`</dt> <dd> Recurring booking information: This will be empty if the appointment isn''t recurring. <dl> <dt>int[] `a_week`</dt> <dd> The days of week when appointment repeats. One of the {@link \ADateWeekSid} constants. This will be empty if the appointment doesn''t repeat weekly. </dd> <dt>string `dl_end`</dt> <dd> The date when the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle must stop. This will be empty if the repeat cycle doesn''t stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>The frequency of the appointment''s repeat cycle.</dd> <dt>int `id_period`</dt> <dd>The measurement unit of <tt>i_period</tt>. One of the {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> <tt>true</tt> if the appointment repeats monthly on the same date. <tt>false</tt> if the appointment repeats monthly on the same day of the week. <tt>null</tt> if the appointment doesn''t repeat monthly. </dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> A list of assets for the appointment booking. Keys refer to asset categories. <i>Primary keys in {@link \RsResourceTypeSql} table.</i> Values refer to arrays with the next keys: Specify this only for the appointment booking. <dl> <dt>int `i_index`</dt> <dd>The asset index on the layout. Specify this only if the asset category has a layout.</dd> <dt>string `k_resource`</dt> <dd>The asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>The date/time for the booking in MySQL format in the location''s time zone.</dd> <dt>int `i_duration`</dt> <dd>The duration of asset booking in minutes. Specify this for separate asset bookings only.</dd> <dt>int `i_index`</dt> <dd> The asset index on the layout. Specify this for separate asset bookings only and for cases when the asset category only has a layout. </dd> <dt>int `id_class_tab`</dt> <dd>The booking service type. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>int `id_gender_staff`</dt> <dd> The gender of the staff member conducting the appointment. One of the {@link \AGenderSid} constants. Specify this for appointment bookings only. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>If `true`, appointment waits unpaid.</dd> <dt>string `k_login_prize`</dt> <dd>The user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd> The user''s pass (for example, a membership or a package). Specify this if you want to set the pass to use to pay for the booking. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_resource`</dt> <dd> The asset booking. Specify this for separate asset bookings only. <i>Primary key in {@link \RsResourceSql} table.</i> </dd> <dt>string `k_service`</dt> <dd> The appointment booking. Specify this for appointment bookings only. <i>Primary key in {@link \RsServiceSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd>The user''s pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `k_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `k_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this an <var>k_staff</var> is that this value must be set only in cases when you want to add a customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: a_user
          in: query
          description: 'Data to create new user. Specify this if <var>uid</var> is empty. Must contain the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes to add to user.</dd> <dt>string `text_mail`</dt> <dd>Mail.</dd> <dt>string `text_name_first`</dt> <dd>First name.</dd> <dt>string `text_name_last`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> </dl>'
          schema:
            type: array
        - name: id_pay
          in: query
          description: 'The payment type ID for the service. One of the {@link \RsAppointmentPaySid} constants. The field is left for compatibility with old code and to control the new algorithm.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This should be set if you''re rebooking an existing appointment. Otherwise, use `0` to book a new appointment.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_answer:
                  type: array
                  description: 'A list of answers for the questions from {@link \Wl\Appointment\Book\Question\QuestionApi::$a_question}. Keys refer to hashes of the questions. Values refer to answers for the questions.'
                a_notification:
                  type: array
                  description: 'Information for sending an appointment notification. <dl> <dt>bool `is_attach`</dt> <dd>`true` to attach calendar file, `false` to not attach calendar file.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `true` if yes, `false` if no.</dd> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'Payment is not processed by this API. Use the following APIs for payment: * {@link \Wl\Appointment\Book\Payment\PaymentApi} * {@link \Wl\Appointment\Book\Payment\PaymentPostApi} * {@link \Wl\Appointment\Book\Payment\PaymentMultipleApi}'
                a_payment_data:
                  type: array
                  description: 'Data required for payment. Has next structure: <dl> <dt>int `id_purchase_item`</dt> <dd>Type of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> </dl>'
                a_purchase_item:
                  type: array
                  description: 'The purchase item keys. Empty if no purchases are made for the appointment booking.'
                a_quiz_response:
                  type: array
                  description: 'A list of quiz response keys. Key is quiz key. Value is quiz response key.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The booking mode ID. One of the {@link ModeSid} constants.'
                is_back_to_back:
                  type: boolean
                  description: 'Determines whether multiple appointments are booked in back-to-back mode.'
                is_unpaid_force:
                  type: boolean
                  description: 'If `true`, the appointment is booked as unpaid. Otherwise, this will be `false` to select an available Purchase Option.'
                m_pay:
                  type: string
                  description: 'The sum paid without tax. Only used for the following types of purchases: * {@link \RsPurchaseItemSid::SERVICE} * {@link \RsPurchaseItemSid::RESOURCE} * {@link \RsPurchaseItemSid::RESOURCE_DEPOSIT} * {@link \RsPurchaseItemSid::APPOINTMENT_DEPOSIT} The field is left for compatibility with old code and to control the new algorithm.'
                s_id:
                  type: string
                  description: 'Unique identifier of the wizard.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'The keys of the booked appointments. Every element has key: <dl> <dt>string `k_appointment`</dt> <dd>The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> </dl>'
                      a_login_activity_visit:
                        type: array
                        description: 'The activity keys of the bookings that were made.'
                      a_visit:
                        type: array
                        description: 'The keys of visits.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
      tags:
        - Finish-Finish
  '/Wl/Appointment/Book/Finish/FinishMultiple.json':
    post:
      summary: 'Completes the appointment booking for one or more providers, optionally creating a new client. Accepts booking details for one or more providers in {@link FinishMultipleApi::$a_book_data}, processes payment using the selected Purchase Option, creates appointment records, and sends booking confirmation notifications. A new client account can be created by supplying user details in {@link FinishMultipleApi::$a_user} when no UID is provided.'
      parameters:
        - name: a_pay
          in: query
          description: 'The payment type ID for each provider. * Keys refer to provider indexes. * Values are one of the {@link \RsAppointmentPaySid} constants. This is a multi-provider equivalent of {@link FinishApi::$id_pay}. The field is left for compatibility with old code and to control the new algorithm.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: a_user
          in: query
          description: 'Data to create new users. Specify this if <var>$uid</var> is empty. The data must contain the next keys: <dl> <dt>string|string[] `a_note`</dt> <dd>The note or list of notes to add to the new user''s profile.</dd> <dt>string `text_mail`</dt> <dd>The new user''s email address.</dd> <dt>string `text_name_first`</dt> <dd>The new user''s first name.</dd> <dt>string `text_name_last`</dt> <dd>The new user''s last name.</dd> <dt>string `text_phone`</dt> <dd>The new user''s mobile phone number.</dd> </dl>'
          schema:
            type: array
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. Specify this to reschedule a certain appointment.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_answer:
                  type: object
                  description: 'A list of answers for the questions from {@link \Wl\Appointment\Book\Question\QuestionApi::$a_question}. 1st dimension - provider index. 2nd dimension - keys refer to hashes of the questions. Values refer to answers for the questions.'
                a_book_data:
                  type: array
                  description: 'All data required to book an appointment. <dl> <dt>int `id_class_tab`</dt> <dd>The booking service type. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>array[] `a_provider`</dt> <dd> A list of providers and their booking details. Every element has next keys: <dl> <dt>array[] `a_conflict`</dt> <dd> Information about booking conflicts. Keys are bookings dates/times in MySQL format in UTC. Values are arrays with next keys: <dl> <dt>string `dt_date_local`</dt> <dd>New appointment date/time in MySQL in locale timezone.</dd> <dt>int `i_duration`</dt> <dd>New asset booking duration.</dd> <dt>int `i_index`</dt> <dd>New asset index.</dd> <dt>int `id_conflict`</dt> <dd>Solution type. One of {@link \RsAppointmentEditConflictSid} constants.</dd> <dt>string `k_resource`</dt> <dd>New asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string|null `k_staff`</dt> <dd>New staff member. <i>Primary key in {@link \RsStaffSql} table.</i> `null` in a case of asset booking.</dd> <dt>string|null `uid_staff`</dt> <dd> New staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> `null` in a case of asset booking. </dd> </dl> </dd> <dt>array `a_product`</dt> <dd> Add-ons to the appointment. Specified for appointment bookings only. The old format used array keys. <i>Primary keys in {@link \RsShopProductOptionSql} table.</i> While the new format has each element as an array: <dl> <dt>int `i_count`</dt> <dd>The add-on buy count.</dd> <dt>int `i_count_use`</dt> <dd>The add-on use count. If not set, then use count is equals to buy count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The add-on key. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl> </dd> <dt>array `a_repeat`</dt> <dd> Information for the recurring booking: This will be empty if the appointment isn''t booked recurringly. <dl> <dt>int[] `a_week`</dt> <dd> The days of the week when the appointment repeats. One of the constants of the {@link \ADateWeekSid} class. This will be empty if the appointment doesn''t repeat weekly. </dd> <dt>string `dl_end`</dt> <dd> The date when the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>The frequency at which the appointment repeats.</dd> <dt>int `id_period`</dt> <dd>The measurement unit of `i_period`. One of the {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> <tt>true</tt> if the appointment repeats monthly on the same date. <tt>false</tt> if the appointment repeats monthly on the same day of the week. <tt>null</tt> if the appointment doesn''t repeat monthly. </dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> The list of assets for the appointment booking. Keys refer to asset categories. Values are arrays with the next keys: Specify this only for an appointment booking. <dl> <dt>int `i_index`</dt> <dd>The asset index on the layout. This is only specified if the asset category has a layout.</dd> <dt>string `k_resource`</dt> <dd>The asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>The date/time for the booking in MySQL format in the location''s time zone.</dd> <dt>int `i_duration`</dt> <dd>The duration for the asset booking in minutes. Specify this for separate asset bookings only.</dd> <dt>int `i_index`</dt> <dd> The asset index on the layout. Specify this for separate asset bookings only and for cases when the asset category only has the layout. </dd> <dt>int `id_gender_staff`</dt> <dd> The gender of the staff member conducting the appointment. One of the {@link \AGenderSid} constants. Specify this for appointment bookings only. </dd> <dt>int `id_purchase_item`</dt> <dd>Type of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_unpaid_force`</dt> <dd> If `true`, the appointment is booked as unpaid. Otherwise, this will be `false` to select an available Purchase Option. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>If `true`, appointment waits unpaid.</dd> <dt>string `k_login_prize`</dt> <dd>The user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd> The user''s Purchase Option. Specify this if you want to use a specific Purchase Option to pay for the booking. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_resource`</dt> <dd> The asset booking. Specify this for separate asset bookings only. <i>Primary key in {@link \RsResourceSql} table.</i> </dd> <dt>string `k_service`</dt> <dd> The appointment booking. Specify this for appointment bookings only. <i>Primary key in {@link \RsServiceSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd> The user''s pass (for example, a membership or a package). Specify this if you want to set the pass to use to pay for the booking. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `k_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. deprecated Use <var>uid_staff</var>. Available for legacy allow-list only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `uid_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> <dt>string `k_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this and <var>k_staff</var> is that this value must be set only in cases when you want to add customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `uid_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this and <var>uid_staff</var> is that this value must be set only in cases when you want to add customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> <dt>string `k_timezone`</dt> <dd>The time zone key. This will be ''null'' if the time zone used matches the time zone of the location.</dd> <dt>string `uid`</dt> <dd> User key. <i>Primary key in {@link \PassportLoginSql} table.</i> Specify only in a case of booking for a lof of different users. </dd> </dl> </dd> </dl>'
                a_notification:
                  type: array
                  description: 'Information for sending an appointment notification. <dl> <dt>bool `is_attach`</dt> <dd>`true` to attach calendar file, `false` to not attach calendar file.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `true` if yes, `false` if no.</dd> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
                a_paid:
                  type: array
                  description: 'The sum paid without tax for each provider. * Keys refer to provider indexes. * Values are the paid amounts without tax. Only used for the following types of purchases: * {@link \RsPurchaseItemSid::SERVICE} * {@link \RsPurchaseItemSid::RESOURCE} * {@link \RsPurchaseItemSid::RESOURCE_DEPOSIT} * {@link \RsPurchaseItemSid::APPOINTMENT_DEPOSIT} This is a multi-provider equivalent of {@link FinishApi::$m_pay}. The field is left for compatibility with old code and to control the new algorithm.'
                a_pay_form:
                  type: array
                  description: 'Payment is not processed by this API. Use the following APIs for payment: * {@link \Wl\Appointment\Book\Payment\PaymentApi} * {@link \Wl\Appointment\Book\Payment\PaymentPostApi} * {@link \Wl\Appointment\Book\Payment\PaymentMultipleApi}'
                a_payment_data:
                  type: array
                  description: 'Payment is not processed by this API. Use the following APIs for payment: * {@link \Wl\Appointment\Book\Payment\PaymentApi} * {@link \Wl\Appointment\Book\Payment\PaymentPostApi} * {@link \Wl\Appointment\Book\Payment\PaymentMultipleApi}'
                a_purchase_item:
                  type: object
                  description: 'The keys of purchased items. The first level of the array is the list of providers from the batch. The second level of the array is the list of purchased items for this booking. If a purchased item was transferred, the transferred purchase item key is returned instead of the original one.'
                a_quiz_response:
                  type: array
                  description: 'The list of quiz response keys. Key is quiz key. Value is quiz response key.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The mode type. One of the {@link ModeSid} constants.'
                is_back_to_back:
                  type: boolean
                  description: 'Determines whether multiple appointments have been booked in back-to-back mode.'
                is_try:
                  type: boolean
                  description: 'This will be `true` when trying to make a test booking and rollback should be applied. Otherwise, this will be `false`. If the flag is set to `true`, credit card requirement will be ignored during this check.'
                s_id:
                  type: string
                  description: 'Unique identifier of the wizard.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'The booked appointments. Every element has the key: <dl> <dt>string `k_appointment`</dt> <dd>The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> </dl>'
                      a_login_activity_visit:
                        type: array
                        description: 'The activity IDs of bookings that have been made.'
                      a_visit:
                        type: array
                        description: 'The visit IDs.'
                      a_visit_provider:
                        type: object
                        description: 'Keys of booked visits. Structured into a two-dimensional array. 1st dimension - providers; 2nd dimension - visit keys inside a provider.'
      tags:
        - Finish-FinishMultiple
  '/Wl/Appointment/Book/Schedule/DayTime.json':
    get:
      summary: 'Retrieves a list of available appointment booking schedule. Returns available time slots for the specified service, location, staff member, and date range. Each slot includes the start time, duration, available capacity, and staff information. Supports both frontend and backend modes, as well as asset bookings and back-to-back scheduling.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to show the available appointment booking schedule.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_unavailable
          in: query
          description: '`true` - return service categories that have no staff members able to conduct them. `false` - return only service categories that have staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'The staff member key used for showing the available appointment booking schedule. In case of back-to-back booking - staff key of first appointment. `0` means any available staff. <i>TODO: Remove this property when constant {@link DayTimeApi::APPS_USE_OLD_K_STAFF} is empty.</i>'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. <dl> <dt>string `dt_date`</dt> <dd>Date of the calendar.</dd> <dt>string `dtu_date`</dt> <dd>Same moment as `dt_date`, converted to real UTC date and time, in MySQL format.</dd> <dt>int `i_count`</dt> <dd>The count of clients that have already booked this appointment.</dd> <dt>int `i_time`</dt> <dd>Integer representation of appointment schedule time.</dd> <dt>int `i_wait`</dt> <dd>The count of clients on the waiting list for this appointment.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether the appointment can be booked only in a wait list.</dd> <dt>string `k_staff`</dt> <dd> this key contains key of staff member. <i>Primary key in {@link \RsStaffSql} table.</i> Otherwise - `0`. </dd> <dt>string `uid_staff`</dt> <dd> If this time is already occupied by any client and staff member (but service capacity is not exhausted), this key contains user key of staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> Otherwise - `0`. </dd> <dt>string `s_title`</dt> <dd>String representation of appointment schedule time.</dd> </dl>'
                      dt_date:
                        type: string
                        description: 'The date to show the available appointment booking schedule.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-DayTime
  '/Wl/Appointment/Book/Schedule/ServiceAvailability.json':
    get:
      summary: 'Retrieves a list of available appointment booking schedule. Returns list of staff that can provide selected appointment. Each staff data contains list of dates from selected period when appointment can be booked. Each date contains list of available appointment start times.'
      parameters:
        - name: dl_end
          in: query
          description: 'End date of search period in MySQL format, in location time zone.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Start date of search period in MySQL format, in location time zone.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. Each record contains availability data of one staff and have next structure: <dl> <dt>string[][] `a_availability`</dt> <dd>Availability data of the staff. Keys are dates and values are lists of available appointment start times.</dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key.</dd> </dl>'
      tags:
        - Schedule-ServiceAvailability
  '/Wl/Appointment/Book/Schedule/Calendar.json':
    get:
      summary: 'Retrieves a list with all calendar days in specified period with available and unavailable appointment booking schedule. Returns each day in the requested month marked as available or unavailable for booking the given service at the given location. Availability is determined by the business schedule, holidays, and staff availability for the selected date range.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to show the available appointment booking schedule.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_month_view
          in: query
          description: 'If calendar should be displayed in month view mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_unavailable
          in: query
          description: '`true` - return service categories that have no staff members able to conduct them. `false` - return only service categories that have staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'The staff member key used for showing the available appointment booking schedule. In case of back-to-back booking - staff key of first appointment. `0` means any available staff. <i>TODO: Remove this property when constant {@link DayTimeApi::APPS_USE_OLD_K_STAFF} is empty.</i>'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set to use client''s profile timezone.'
          schema:
            type: [string, "null"]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: array
                        description: 'A list with all calendar days in the specified month with available and unavailable appointment bookings in the schedule. <dl> <dt>string `dt_date`</dt> <dd>Date item of the calendar.</dd> <dt>int `i_week`</dt> <dd>Number of day in week.</dd> <dt>bool `is_available`</dt> <dd>Whether booking is available for this day.</dd> <dt>bool `is_current`</dt> <dd>Whether date is current.</dd> <dt>bool `is_out`</dt> <dd>Whether date is out of current month or it''s business/location closed date.</dd> <dt>bool `is_waitlist_only`</dt> <dd>Whether booking for this day available only in wait list.</dd> <dt>bool `is_week_end`</dt> <dd>Whether date is last day of the week.</dd> <dt>bool `is_week_start`</dt> <dd>Whether date is first day of the week.</dd> <dt>string `s_day`</dt> <dd>String representation of day number with leading zeroes.</dd> <dt>string `s_week`</dt> <dd>String representation of week day (one letter, i.e. "F").</dd> </dl>'
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. <dl> <dt>string `dt_date`</dt> <dd>Date of the calendar.</dd> <dt>string `dtu_date`</dt> <dd>Same moment as `dt_date`, converted to real UTC date and time, in MySQL format.</dd> <dt>int `i_count`</dt> <dd>The count of clients that have already booked this appointment.</dd> <dt>int `i_time`</dt> <dd>Integer representation of appointment schedule time.</dd> <dt>int `i_wait`</dt> <dd>The count of clients on the waiting list for this appointment.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether the appointment can be booked only in a wait list.</dd> <dt>string `k_staff`</dt> <dd> this key contains key of staff member. <i>Primary key in {@link \RsStaffSql} table.</i> Otherwise - `0`. </dd> <dt>string `uid_staff`</dt> <dd> If this time is already occupied by any client and staff member (but service capacity is not exhausted), this key contains user key of staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> Otherwise - `0`. </dd> <dt>string `s_title`</dt> <dd>String representation of appointment schedule time.</dd> </dl>'
                      a_timezone_data:
                        type: array
                        description: 'Information about timezone. <dl> <dt>array|null `a_timezone`</dt> <dd> `null` if business settings doesn''t allow client to adjust timezone, otherwise list of timezones: <dl> <dt>int `i_order`</dt> <dd>Timezone order.</dd> <dt>int `i_shift`</dt> <dd>Timezone shift from UTC in hours.</dd> <dt>bool `is_select`</dt> <dd> `true` for selected timezone - from {@link \Wl\Appointment\Book\Schedule\CalendarApi::$k_timezone} param or client''s default timezone when param not set. </dd> <dt>string `k_timezone`</dt> <dd>Timezone key.</dd> <dt>string `s_title`</dt> <dd>Timezone name.</dd> <dt>string `text_abbr`</dt> <dd>Timezone abbreviation.</dd> </dl> </dd> <dt>string|null `name`</dt> <dd>`null` if business settings doesn''t allow client to adjust timezone, otherwise timezone input name.</dd> </dl>'
                      a_week_name:
                        type: array
                        description: 'Array with short week day''s names (2 letters, i.e. ''Fr'') for calendar month view. Week days order according to business''s settings. <dl> <dt>int `i_day`</dt> <dd>Week day, one of the {@link \ADateWeekSid} constants.</dd> <dt>string `html_week_day`</dt> <dd>Short week day''s name (2 letters, i.e. ''Fr'').</dd> </dl>'
                      can_backwards:
                        type: boolean
                        description: 'Whether previous calendar period can be shown (start of shown period later than current date).'
                      dt_date:
                        type: string
                        description: 'The date to show the available appointment booking schedule.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      i_week_end:
                        type: number
                        format: int32
                        description: 'Last day of the week. One of {@link \ADateWeekSid} constants.'
                      i_week_start:
                        type: number
                        format: int32
                        description: 'First day of the week. One of {@link \ADateWeekSid} constants.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-Calendar
  '/Wl/Appointment/Book/Schedule/NextAvailableDay.json':
    get:
      summary: 'Finds and returns the next available date for appointment booking starting from the given date. Searches forward from the specified start date until it finds a day with at least one available time slot for the given service, location, and optional staff member. Returns the first available date so the client side can pre-select it in the booking calendar. This request may take longer than usual on production and uses an extended execution time limit.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Start date to search next available date in MySQL format. If empty, the search will start from the current date.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dt_date:
                        type: string
                        description: 'Start date to search next available date in MySQL format. If empty, the search will start from the current date.'
                      dt_next:
                        type: string
                        description: 'Next available date in MySQL format.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-NextAvailableDay
  '/Wl/Appointment/Book/Payment/Payment.json':
    get:
      summary: 'Calculates pricing information for an appointment booking, including taxes, discounts, surcharges, and totals. Returns the full breakdown of amounts owed for the booking, including available Purchase Options, applicable discount codes, surcharge amounts, and the final total. Intended to be called before submitting payment so the client side can display a pricing summary to the user.'
      parameters:
        - name: a_book_data
          in: query
          description: 'Information detailing an appointment booking: <dl> <dt>int[] `a_product`</dt> <dd>Add-on list. Keys are add-on option keys, values are quantities.</dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>bool `is_pay_later`</dt> <dd>`true` if the client wants to pay upon their visit, `false` if paying now.</dd> <dt>bool `is_purchase_previous`</dt> <dd>`true` if a previously purchased option is used for this booking.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if the client is placed on a wait list without paying.</dd> <dt>int `i_duration`</dt> <dd>Asset booking duration in minutes. Used only for asset bookings.</dd> <dt>string `k_login_prize`</dt> <dd>Login prize key used to pay for the booking. Empty if no prize is used.</dd> <dt>string `k_login_promotion`</dt> <dd>Key of an already-purchased promotion used for this booking. Empty if not applicable.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Used only for asset bookings.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key used to redeem a free item. Empty if not applicable.</dd> <dt>string `k_service`</dt> <dd>Service key. Used only for appointment bookings.</dd> <dt>string `k_session_pass`</dt> <dd>Drop-in key. Used when booking via a drop-in pass.</dd> <dt>string `m_tip_appointment`</dt> <dd>Tip amount for the appointment.</dd> <dt>string `s_signature`</dt> <dd>Client signature for a contract. Required when the purchase option has a contract.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. Currently used only with {@link PaymentApi::$k_session_pass} variable.'
          schema:
            type: [string, "null"]
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'Session pass key.'
          schema:
            type: string
        - name: m_variable_price
          in: query
          description: 'Variable price. Is set only during booking an appointment with variable type of the price {@link \RsServicePriceSid::VARIES} from spa backend {@link ModeSid::SPA_BACKEND}.'
          schema:
            type: string
        - name: s_unique_key
          in: query
          description: 'Service unique key. Used for model cache.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion_data:
                        type: array
                        description: 'Information about selected Purchase Options. <dl> <dt>int `i_limit`</dt> <dd>The limit of total visits.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits.</dd> <dt>string `s_expire`</dt> <dd>The expiration date.</dd> <dt>string `s_title`</dt> <dd>The title of the Purchase Option.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'Information about selected purchase items. Indexes are strings in the format `id_purchase_item-k_id`. Values are an array with the following structure: <dl> <dt>array `a_tax`</dt> <dd> Contains information about taxes in the following format. A list of taxes to apply. The array keys are `k_tax` keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. A constant of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> <dt>string `m_pay`</dt> <dd>The payment for the Purchase Option or single visit without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the Purchase Option or single visit.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      m_coupon:
                        type: string
                        description: 'Gift card amount.'
                      m_discount:
                        type: string
                        description: 'Discount amount.'
                      m_surcharge:
                        type: string
                        description: 'Surcharge amount calculated for credit cards (Virtual Terminal and Card Swiper).'
                      m_surcharge_ach:
                        type: string
                        description: 'Surcharge amount calculated for money transfers from account: ACH, Direct Entry.'
                      m_tax:
                        type: string
                        description: 'The tax of service.'
                      m_total:
                        type: string
                        description: 'The total cost of the purchase.'
      tags:
        - Payment-Payment
    post:
      summary: 'Allows to pay an appointment or appointment purchase option for the client. Accepts payment method and booking data, charges the client for the appointment or the selected Purchase Option, and records the transaction. Requires the client to be authenticated and the booking data to include a valid service or asset key.'
      parameters:
        - name: a_book_data
          in: query
          description: 'Information detailing an appointment booking: <dl> <dt>int[] `a_product`</dt> <dd>Add-on list. Keys are add-on option keys, values are quantities.</dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>bool `is_pay_later`</dt> <dd>`true` if the client wants to pay upon their visit, `false` if paying now.</dd> <dt>bool `is_purchase_previous`</dt> <dd>`true` if a previously purchased option is used for this booking.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if the client is placed on a wait list without paying.</dd> <dt>int `i_duration`</dt> <dd>Asset booking duration in minutes. Used only for asset bookings.</dd> <dt>string `k_login_prize`</dt> <dd>Login prize key used to pay for the booking. Empty if no prize is used.</dd> <dt>string `k_login_promotion`</dt> <dd>Key of an already-purchased promotion used for this booking. Empty if not applicable.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Used only for asset bookings.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key used to redeem a free item. Empty if not applicable.</dd> <dt>string `k_service`</dt> <dd>Service key. Used only for appointment bookings.</dd> <dt>string `k_session_pass`</dt> <dd>Drop-in key. Used when booking via a drop-in pass.</dd> <dt>string `m_tip_appointment`</dt> <dd>Tip amount for the appointment.</dd> <dt>string `s_signature`</dt> <dd>Client signature for a contract. Required when the purchase option has a contract.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary keys in the {@link \Core\Quiz\QuizSql} table.</i> Values are quiz response keys. <i>Primary keys in the {@link \Core\Quiz\Response\ResponseSql} table.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item:
                        type: [array, "null"]
                        description: 'The purchase item keys from the database. This will be `null` if not set yet.'
                      id_pay:
                        type: number
                        format: int32
                        description: 'The payment type for the appointment. A constant of {@link \RsAppointmentPaySid}.'
                      k_login_activity_purchase:
                        type: string
                        description: 'The key of activity of the purchase made. Empty if no purchase has been made.'
                      k_login_prize:
                        type: string
                        description: 'Login prize key. In case when appointment paid by reward prize, there is the key of redeemed login prize. Empty otherwise.'
      tags:
        - Payment-Payment
  '/Wl/Appointment/Book/Payment/PaymentMultiple.json':
    get:
      summary: 'Calculates pricing information for a batch of appointment bookings, including taxes, discounts, surcharges, and per-provider totals. Returns the full pricing breakdown for all providers specified in the booking data, including available Purchase Options, applicable discount codes, surcharge amounts, and the final total for each provider. Intended to be called before submitting payment so the client side can display a pricing summary.'
      parameters:
        - name: a_book_data
          in: query
          description: 'The booking process information: Set this field value for GET requests. <dl> <dt>array[] `a_provider`</dt> <dd> The batch of appointments to be booked. Each element has the next values: <dl> <dt>array `a_product`</dt> <dd> The add-on list. Keys are add-on option keys. <i>Primary keys in the {@link \RsShopProductOptionSql} table</i> Values are quantities: <dl> <dt>string `k_shop_product_option`</dt> <dd> Add-on option key (used as array key). <i>Primary key in the {@link \RsShopProductOptionSql} table.</i> </dd> <dt>int `i_count`</dt> <dd>Quantity of the add-on to purchase (used as array value).</dd> </dl> </dd> <dt>int `i_duration`</dt> <dd>The asset duration in minutes. This won''t be empty for asset bookings.</dd> <dt>int `id_purchase_item`</dt> <dd> The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants. This won''t be empty for new options purchased. </dd> <dt>bool `is_pay_later`</dt> <dd> This will be <tt>true</tt> if the customer wants to pay upon their visit. Otherwise, this will be <tt>false</tt> if the user wants to pay now. </dd> <dt>bool `is_purchase_previous`</dt> <dd> This will be <tt>true</tt> if the Purchase Option that was selected for another appointment from the batch must be used for this appointment. Otherwise, this will be <tt>false</tt>. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd> This will be <tt>true</tt> if the customer is booked into the wait list and doesn''t have to pay. Otherwise, this will be <tt>false</tt> if the customer is booking to an active list (or if the wait list booking should be paid now). </dd> <dt>string `k_id`</dt> <dd>The key of the purchase item. This won''t be empty for new options purchased.</dd> <dt>string `k_login_prize`</dt> <dd> The key of the customer''s prize used to pay for the booking. This won''t be empty for bookings made using prizes. </dd> <dt>string `k_login_promotion`</dt> <dd> The key of the Purchase Option. This won''t be empty when using a Purchase Option that''s already been purchased. </dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset booking. This is only used for asset bookings.</dd> <dt>string `k_service`</dt> <dd>The key of the appointment booking service. This is only used for appointment bookings.</dd> <dt>string `s_signature`</dt> <dd> The signature for the Purchase Option contract. The data is returned from the canvas HTML element or the signature pad. This won''t be empty if the Purchase Option requires contract signing. </dd> <dt>string `uid`</dt> <dd> User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i> Specify only in a case of booking for a lof of different users. </dd> </dl> </dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>string `m_tip_appointment`</dt> <dd>The tips amount.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The ID of the source mode. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'The gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion_data:
                        type: array
                        description: 'Information about the selected login promotion. <dl> <dt>int `i_limit`</dt> <dd>The visit count limit of the promotion.</dd> <dt>int `i_remain`</dt> <dd>The count of remaining visits.</dd> <dt>string `s_expire`</dt> <dd>The expiration date.</dd> <dt>string `s_title`</dt> <dd>The title of the promotion.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'List of purchase options to be purchased. <b>Warning:</b> * This field contains incorrect data for multiple bookings. Use {@link PaymentMultipleApi::$a_purchase_provider} instead. * This field can be safely used for a single booking. * This field can contain valid data for the {@link \RsPurchaseItemSid::APPOINTMENT_TIP} purchase that does not belong to any provider. Array structure: * Keys refer to strings in the format `id_purchase_item-k_id`. * Values refer to an array with the next stricture: <dl> <dt>array `a_tax`</dt> <dd> A list of taxes to apply containing information about taxes. The array keys are <tt>k_tax</tt> keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_pay`</dt> <dd>The payment for the promotion (or single visit) without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the promotion (or single visit).</dd> </dl> Use {@link PaymentMultipleApi::$a_purchase_provider} instead. However, this field can still return valid data for an Appointment Tip purchase that does not belong to any provider, and the field can still be used for a single booking.'
                      a_purchase_provider:
                        type: array
                        description: 'A list of purchase options grouped by provider. * The first level keys are provider indexes from {@link PaymentMultipleApi::$a_book_data} field. * The second level keys are strings in the format `id_purchase_item-k_id`. * Each value has the same structure as {@link PaymentMultipleApi::$a_purchase}. <dl> <dt>array `a_tax`</dt> <dd> A list of taxes to apply containing information about taxes. The array keys are <tt>k_tax</tt> keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_pay`</dt> <dd>The payment for the promotion (or single visit) without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the promotion (or single visit).</dd> </dl>'
                      a_total:
                        type: array
                        description: 'The list of amounts to pay for appointments from the batch, with taxes and without surcharges. * Keys are provider indexes from {@link PaymentMultipleApi::$a_book_data}. * Values are the total amount for the corresponding provider, or `0.00` if the provider has nothing to pay.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      m_coupon:
                        type: string
                        description: 'The gift card amount.'
                      m_discount:
                        type: string
                        description: 'The discount amount.'
                      m_surcharge:
                        type: string
                        description: 'Surcharge amount calculated for credit cards (Virtual Terminal and Card Swiper).'
                      m_surcharge_ach:
                        type: string
                        description: 'Surcharge amount calculated for money transfers from account: ACH, Direct Entry.'
                      m_tax:
                        type: string
                        description: 'The amount of tax to pay.'
                      m_total:
                        type: string
                        description: 'The price of the service, with taxes and without surcharges.'
      tags:
        - Payment-PaymentMultiple
    post:
      summary: 'Allows to pay an appointment or appointment purchase option for the client. Accepts payment method and multi-provider booking data, charges the client for all appointments or selected Purchase Options in the batch, and records the transactions. Requires the client to be authenticated and each provider entry to include a valid service or asset key with date.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The ID of the source mode. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'The gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_book_data_post:
                  type: array
                  description: 'A copy of {@link PaymentMultipleApi::$a_book_data}. Has the same structure. Set this field value for POST requests. <dl> <dt>array[] `a_provider`</dt> <dd> The batch of appointments to be booked. Each element has the next values: <dl> <dt>array `a_product`</dt> <dd> The add-on list. Keys are add-on option keys. <i>Primary keys in the {@link \RsShopProductOptionSql} table</i> Values are quantities: <dl> <dt>string `k_shop_product_option`</dt> <dd> Add-on option key (used as array key). <i>Primary key in the {@link \RsShopProductOptionSql} table.</i> </dd> <dt>int `i_count`</dt> <dd>Quantity of the add-on to purchase (used as array value).</dd> </dl> </dd> <dt>int `i_duration`</dt> <dd>The asset duration in minutes. This won''t be empty for asset bookings.</dd> <dt>int `id_purchase_item`</dt> <dd> The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants. This won''t be empty for new options purchased. </dd> <dt>bool `is_pay_later`</dt> <dd> This will be <tt>true</tt> if the customer wants to pay upon their visit. Otherwise, this will be <tt>false</tt> if the user wants to pay now. </dd> <dt>bool `is_purchase_previous`</dt> <dd> This will be <tt>true</tt> if the Purchase Option that was selected for another appointment from the batch must be used for this appointment. Otherwise, this will be <tt>false</tt>. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd> This will be <tt>true</tt> if the customer is booked into the wait list and doesn''t have to pay. Otherwise, this will be <tt>false</tt> if the customer is booking to an active list (or if the wait list booking should be paid now). </dd> <dt>string `k_id`</dt> <dd>The key of the purchase item. This won''t be empty for new options purchased.</dd> <dt>string `k_login_prize`</dt> <dd> The key of the customer''s prize used to pay for the booking. This won''t be empty for bookings made using prizes. </dd> <dt>string `k_login_promotion`</dt> <dd> The key of the Purchase Option. This won''t be empty when using a Purchase Option that''s already been purchased. </dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset booking. This is only used for asset bookings.</dd> <dt>string `k_service`</dt> <dd>The key of the appointment booking service. This is only used for appointment bookings.</dd> <dt>string `s_signature`</dt> <dd> The signature for the Purchase Option contract. The data is returned from the canvas HTML element or the signature pad. This won''t be empty if the Purchase Option requires contract signing. </dd> <dt>string `uid`</dt> <dd> User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i> Specify only in a case of booking for a lof of different users. </dd> </dl> </dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>string `m_tip_appointment`</dt> <dd>The tips amount.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary keys in the {@link \Core\Quiz\QuizSql} table.</i> Values are quiz response keys. <i>Primary keys in the {@link \Core\Quiz\Response\ResponseSql} table.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'The list of redeemed prizes.'
                      a_pay:
                        type: array
                        description: 'The payment type for the appointment. One of the {@link \RsAppointmentPaySid} constants.'
                      a_purchase_item:
                        type: [object, "null"]
                        description: 'The keys of purchased items. The first level of the array is the list of appointments from the batch. The second level of the array is the list of items purchased for this appointment. If a purchased item was transferred, the key of the transferred purchase item will be returned instead of the key of the purchased one. Otherwise the original purchase item is kept.'
                      k_login_activity_purchase:
                        type: string
                        description: 'Key of the activity for the purchase made. This will be empty if no purchase has been made.'
      tags:
        - Payment-PaymentMultiple
  '/Wl/Appointment/Book/Payment/PaymentPost.json':
    get:
      summary: 'Calculates pricing information for an appointment booking, including taxes, discounts, surcharges, and totals. Returns the full breakdown of amounts owed for the booking, including available Purchase Options, applicable discount codes, surcharge amounts, and the final total. Intended to be called before submitting payment so the client side can display a pricing summary to the user.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. Currently used only with {@link PaymentApi::$k_session_pass} variable.'
          schema:
            type: [string, "null"]
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'Session pass key.'
          schema:
            type: string
        - name: m_variable_price
          in: query
          description: 'Variable price. Is set only during booking an appointment with variable type of the price {@link \RsServicePriceSid::VARIES} from spa backend {@link ModeSid::SPA_BACKEND}.'
          schema:
            type: string
        - name: s_unique_key
          in: query
          description: 'Service unique key. Used for model cache.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion_data:
                        type: array
                        description: 'Information about selected Purchase Options. <dl> <dt>int `i_limit`</dt> <dd>The limit of total visits.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits.</dd> <dt>string `s_expire`</dt> <dd>The expiration date.</dd> <dt>string `s_title`</dt> <dd>The title of the Purchase Option.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'Information about selected purchase items. Indexes are strings in the format `id_purchase_item-k_id`. Values are an array with the following structure: <dl> <dt>array `a_tax`</dt> <dd> Contains information about taxes in the following format. A list of taxes to apply. The array keys are `k_tax` keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. A constant of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> <dt>string `m_pay`</dt> <dd>The payment for the Purchase Option or single visit without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the Purchase Option or single visit.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      m_coupon:
                        type: string
                        description: 'Gift card amount.'
                      m_discount:
                        type: string
                        description: 'Discount amount.'
                      m_surcharge:
                        type: string
                        description: 'Surcharge amount calculated for credit cards (Virtual Terminal and Card Swiper).'
                      m_surcharge_ach:
                        type: string
                        description: 'Surcharge amount calculated for money transfers from account: ACH, Direct Entry.'
                      m_tax:
                        type: string
                        description: 'The tax of service.'
                      m_total:
                        type: string
                        description: 'The total cost of the purchase.'
      tags:
        - Payment-PaymentPost
    post:
      summary: 'Allows to pay an appointment or appointment purchase option for the client. Accepts payment method and booking data, charges the client for the appointment or the selected Purchase Option, and records the transaction. Requires the client to be authenticated and the booking data to include a valid service or asset key.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_book_data:
                  type: array
                  description: 'Information detailing an appointment booking. Has the same structure as {@link PaymentApi::$a_book_data}. <dl> <dt>int[] `a_product`</dt> <dd>Add-on list. Keys are add-on option keys, values are quantities.</dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>bool `is_pay_later`</dt> <dd>`true` if the client wants to pay upon their visit, `false` if paying now.</dd> <dt>bool `is_purchase_previous`</dt> <dd>`true` if a previously purchased option is used for this booking.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if the client is placed on a wait list without paying.</dd> <dt>int `i_duration`</dt> <dd>Asset booking duration in minutes. Used only for asset bookings.</dd> <dt>string `k_login_prize`</dt> <dd>Login prize key used to pay for the booking. Empty if no prize is used.</dd> <dt>string `k_login_promotion`</dt> <dd>Key of an already-purchased promotion used for this booking. Empty if not applicable.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Used only for asset bookings.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key used to redeem a free item. Empty if not applicable.</dd> <dt>string `k_service`</dt> <dd>Service key. Used only for appointment bookings.</dd> <dt>string `k_session_pass`</dt> <dd>Drop-in key. Used when booking via a drop-in pass.</dd> <dt>string `m_tip_appointment`</dt> <dd>Tip amount for the appointment.</dd> <dt>string `s_signature`</dt> <dd>Client signature for a contract. Required when the purchase option has a contract.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary keys in the {@link \Core\Quiz\QuizSql} table.</i> Values are quiz response keys. <i>Primary keys in the {@link \Core\Quiz\Response\ResponseSql} table.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item:
                        type: [array, "null"]
                        description: 'The purchase item keys from the database. This will be `null` if not set yet.'
                      id_pay:
                        type: number
                        format: int32
                        description: 'The payment type for the appointment. A constant of {@link \RsAppointmentPaySid}.'
                      k_login_activity_purchase:
                        type: string
                        description: 'The key of activity of the purchase made. Empty if no purchase has been made.'
                      k_login_prize:
                        type: string
                        description: 'Login prize key. In case when appointment paid by reward prize, there is the key of redeemed login prize. Empty otherwise.'
      tags:
        - Payment-PaymentPost
  '/Wl/Appointment/Book/Quiz/Quiz.json':
    get:
      summary: 'Gets a list of required quizzes. Gets a list of required quizzes which associated with requested service/resourse.'
      parameters:
        - name: id_purchase_item
          in: query
          description: 'Purchase item ID.'
          schema:
            type: [number, "null"]
        - name: is_all
          in: query
          description: '`true` to return both optional and required forms; `false` to return only required forms.'
          schema:
            type: boolean
        - name: json_shop_product_option
          in: query
          description: 'List of add-ons keys. <i>Each element is primary key in {@link \RsShopProductSql} table.</i>'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Promotion key or appointment key. Depends on {@link QuizApi::$id_purchase_item}.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'Resource key.'
          schema:
            type: [string, "null"]
        - name: k_service
          in: query
          description: 'Service key.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. Empty if guest.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: array
                        description: 'List of required quizzes. <dl> <dt>bool `is_require`</dt> <dd>Whether the quiz is required.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key from {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl>'
      tags:
        - Quiz-Quiz
  '/Wl/Appointment/Book/Asset/AssetList.json':
    get:
      summary: 'Retrieves information about assets in the current asset category. Returns the list of bookable assets at the given location, optionally filtered by category and book now tab. When a date and time are provided, only assets available at that time are included and busy asset slots are returned in {@link AssetListApi::$a_asset_busy}. Supports both frontend and backend modes.'
      parameters:
        - name: dtl_date
          in: query
          description: 'The selected date and time of the asset booking. It is used in cases when the business booking policy allows clients to select a date and time, and then the available asset.'
          schema:
            type: string
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'Mode type, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'This is `true` if asset categories are loaded for back-end mode. Otherwise, this will be `false` for front-end mode.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected book tab.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Key of the appointment, if we reschedule existing appointment. It should be sent to ignore it when we get availability hours for the asset.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. If not set, location''s business will be used.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'The class tab key used to filter assets. This will be `null` if not set yet or if elements with no specified class tab are selected.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_resource_category
          in: query
          description: 'The asset category key to show information for.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone of date and time of asset booking. Empty if {@link AssetListApi::$dtl_date} not set or client can''t change in which timezone dates should be shown.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Client to get information for. If client not set, returns full asset list without client restrictions.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_asset:
                        type: array
                        description: 'A list of information about assets: <dl> <dt>array `a_age_restrictions`</dt> <dd> Information about age restrictions for this event. This will be an empty array if there aren''t any age restrictions. <dl> <dt>int|null `i_age_from`</dt> <dd> The minimum age permitted for the event (years part). This will be `null` if a minimum age isn''t set or available. </dd> <dt>int|null `i_age_from_month`</dt> <dd> The minimum age permitted for the event (month part). This will be `null` if a minimum age isn''t set or available. </dd> <dt>int|null `i_age_to`</dt> <dd> The maximum age permitted for the event (years part). This will be `null` if a maximum age isn''t set or available. </dd> <dt>int|null `i_age_to_month`</dt> <dd> The maximum age permitted for the event (month part). This will be `null` if a maximum age isn''t set or available. </dd> <dt>bool `is_age_public`</dt> <dd> This will be `true` if age restrictions are public and available. Otherwise, this will be `false` if they''re hidden. When restrictions are hidden and current user isn''t a staff member, the age range will be empty. </dd> </dl> </dd> <dt>string[] `a_class_tab`</dt> <dd> The key of service. Keys are service key. <i>Primary key in {@link \RsServiceSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i> </dd> <dt>array[] `a_direct_link`</dt> <dd> A list of links to create a booking from a direct link (direct booking URL). The system needs to know what tab is associated with the booking. Therefore, there needs to be one link per tab. Each element has two values: <dl> <dt>string `k_class_tab`</dt> <dd>The key of the book now tab.</dd> <dt>string `url_tab`</dt> <dd>The direct booking URL. This will open the booking wizard under the related booking tab.</dd> </dl> </dd> <dt>array[] `a_image`</dt> <dd> Information about the asset logo: <dl> <dt>int `i_angle`</dt> <dd>The angle of the shape rotation. This is set only if the image is one of the default shapes.</dd> <dt>bool `is_empty`</dt> <dd>Determines if the asset logo is empty.</dd> <dt>string `sid_image_icon`</dt> <dd> The icon name. String representation of one of the {@link \Wl\Resource\Image\ImageIconSid} constants. This is only set if the image kind equals to `image`. </dd> <dt>string `sid_image_shape`</dt> <dd> The shape name. String representation of one of the {@link \Wl\Resource\Image\ImageShapeSid} constants. This is set only if the image kind equals to `shape`. </dd> <dt>string `url`</dt> <dd>The asset logo URL.</dd> </dl> </dd> <dt>array[] `a_period`</dt> <dd> A list of asset periods with the following information: <dl> <dt>string `html_duration`</dt> <dd>The HTML code used to display the asset duration.</dd> <dt>string `html_price`</dt> <dd>The HTML code used to display the formatted price.</dd> <dt>int `i_duration`</dt> <dd>The asset duration in minutes.</dd> <dt>int `id_price`</dt> <dd>The asset period price type. One of {@link \RsServicePriceSid} constants.</dd> <dt>string `m_price`</dt> <dd>The asset period price.</dd> </dl> </dd> <dt>string[] `a_search_tag`</dt> <dd>QUICK Search tag keys.</dd> <dt>bool `hide_application`</dt> <dd> Determines whether the asset will be hidden in the White Label mobile apps. If `true`, the asset won''t be displayed. Otherwise, this will be `false`. </dd> <dt>string `html_age_restriction`</dt> <dd>The resource age restriction</dd> <dt>string `html_deny_reason`</dt> <dd>Human-readable reason why the client cannot book this asset. Empty string if there is no deny reason.</dd> <dt>string `html_title`</dt> <dd>The resource name.</dd> <dt>int|null `id_deny_reason`</dt> <dd> The ID of the reason why the client cannot book this asset. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. `null` if there is no deny reason. </dd> <dt>int `id_service_require`</dt> <dd>The purchase rule. One of the {@link \RsServiceRequireSid} constants.</dd> <dt>bool `is_age_restricted`</dt> <dd>Determines whether this service can''t be booked due to age restrictions.</dd> <dt>string `k_class_tab`</dt> <dd>Quick book tab key. <i>Primary key in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i></dd> <dt>string `k_resource`</dt> <dd>The resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_category`</dt> <dd>The resource category key. <i>Primary key in {@link \RsResourceTypeSql} table.</i></dd> <dt>string|null `sid_deny_reason`</dt> <dd>String representation of the deny reason. `null` if no deny reason.</dd> </dl>'
                      a_asset_busy:
                        type: object
                        description: 'A list of reserved assets. 1st level keys refer to asset keys. 2nd level keys refer to asset numbers. Values are keys of appointment bookings that reserve the asset, or `true` if the asset is reserved by a class or event. For example, if you want to check if the 10th asset with the key of ''15'' is reserved, you can check if `a_resource_busy[''15''][''10'']` is free. If you''re rebooking an appointment, check the value of `a_resource_busy[''15''][''10'']`. If it''s equal to the key of your current appointment booking, you can assume the asset is available.'
                      k_resource_layout:
                        type: string
                        description: 'The asset layout key. May be empty if asset category has no layout.'
      tags:
        - Asset-AssetList
  '/Wl/Appointment/Book/Asset/Category.json':
    get:
      summary: 'Retrieves a list of information about asset categories for the appointment booking page. Returns all asset categories available at the given location, optionally filtered by book now tab. Each category includes its title and a flag indicating whether all its assets are hidden in the White Label mobile application. Supports both frontend and backend modes.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, asset categories are loaded for backend mode. Otherwise, this will be `false` if asset categories are loaded for frontend mode.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected book tab.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'The class tab key to use for filtering services.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category:
                        type: array
                        description: 'A list of information about asset categories. <dl> <dt>bool `hide_application`</dt> <dd> <tt>true</tt> - all resources are hidden in this category for White Label mobile application. <tt>false</tt> - otherwise. </dd> <dt>string `k_resource_type`</dt> <dd>Resource type ID.</dd> <dt>string `html_title`</dt> <dd>Title of resource type.</dd> <dt>string `text_title`</dt> <dd>Title of resource type as text.</dd> </dl>'
      tags:
        - Asset-Category
  '/Wl/Appointment/Book/Product/Product62.json':
    get:
      summary: 'Retrieves list of available service add-ons. Returns active add-on products for the specified service and location, filtered by the given client''s login type when a UID is provided. Products with a purchase restriction that does not match the client type are excluded from the result.'
      parameters:
        - name: k_location
          in: query
          description: 'The key of a location where appointment is going to be booked.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The key of a service to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user to show information for. Products with a {@link PurchaseRestrictionSid::TYPE} restriction will be filtered out. Only those products that match the client type or those that have no restrictions will be shown. `null` if the client is not logged in. If you don''t need to filter products by client type/group, use {@link ProductApi}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'A list service add-ons. This will be `null` if not set yet. <dl> <dt>array `a_logo`</dt> <dd> Information about first image connected to the product. <i>Result from {@link \RsShopProductImageList::imageOne()}.</i> <dl> <dt>int `i_height`</dt> <dd>Height of the image.</dd> <dt>int `i_width`</dt> <dd>Width of the image.</dd> <dt>bool `is_empty`</dt> <dd>`true` mean mock instead of real image.</dd> <dt>string `s_url`</dt> <dd>URL on the image.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Description of the product as prepared html code.</dd> <dt>int `i_duration`</dt> <dd>Duration that will be added to the appointment with this addon as count of minutes.</dd> <dt>bool `is_client_duration`</dt> <dd>`true` means that the duration information will be displayed for the client.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if client can purchase product, `false` if only staff can perform purchase.</dd> <dt>bool `is_amount`</dt> <dd>`true` means that addon increases price of the appointment.</dd> <dt>string `k_currency`</dt> <dd>ID of the currency.</dd> <dt>string `k_shop_product`</dt> <dd>ID of the product.</dd> <dt>string `k_shop_product_option`</dt> <dd> ID of the product option. Zero means that product has multiple options and option should be selected by client. </dd> <dt>string `m_amount`</dt> <dd>Amount that will be added to the appointment price with this addon.</dd> <dt>string `text_description`</dt> <dd>Description of the product.</dd> <dt>string `text_duration`</dt> <dd>Duration that will be added to the appointment with this addon as a formatted string.</dd> <dt>string `text_title`</dt> <dd>Name of the product.</dd> </dl>'
      tags:
        - Product-Product62
  '/Wl/Appointment/Book/Product/Product.json':
    get:
      summary: 'Retrieves list of available service add-ons. Returns all active add-on products available for the specified service, including title, image, price, and quantity information. Results are not filtered by client type; use {@link Product62Api} when filtering by client login type is required.'
      parameters:
        - name: k_service
          in: query
          description: 'The key of a service to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'A list service add-ons. This will be `null` if not set yet. <dl> <dt>array `a_logo`</dt> <dd> Information about first image connected to the product. <i>Result from {@link \RsShopProductImageList::imageOne()}.</i> <dl> <dt>int `i_height`</dt> <dd>Height of the image.</dd> <dt>int `i_width`</dt> <dd>Width of the image.</dd> <dt>bool `is_empty`</dt> <dd>`true` mean mock instead of real image.</dd> <dt>string `s_url`</dt> <dd>URL on the image.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Description of the product as prepared html code.</dd> <dt>int `i_duration`</dt> <dd>Duration that will be added to the appointment with this addon as count of minutes.</dd> <dt>bool `is_client_duration`</dt> <dd>`true` means that the duration information will be displayed for the client.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if client can purchase product, `false` if only staff can perform purchase.</dd> <dt>bool `is_amount`</dt> <dd>`true` means that addon increases price of the appointment.</dd> <dt>string `k_currency`</dt> <dd>ID of the currency.</dd> <dt>string `k_shop_product`</dt> <dd>ID of the product.</dd> <dt>string `k_shop_product_option`</dt> <dd> ID of the product option. Zero means that product has multiple options and option should be selected by client. </dd> <dt>string `m_amount`</dt> <dd>Amount that will be added to the appointment price with this addon.</dd> <dt>string `text_description`</dt> <dd>Description of the product.</dd> <dt>string `text_duration`</dt> <dd>Duration that will be added to the appointment with this addon as a formatted string.</dd> <dt>string `text_title`</dt> <dd>Name of the product.</dd> </dl>'
      tags:
        - Product-Product
  '/Wl/Appointment/Book/Question/Question.json':
    get:
      summary: 'Retrieves questions for the current service. Returns the list of intake questions defined for the specified service that the client must answer before completing an appointment booking. Each question includes its text, a stable hash key for matching answers, and input size metadata.'
      parameters:
        - name: k_service
          in: query
          description: 'The service key used for retrieving questions.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_question:
                        type: array
                        description: 'A list of questions for the service. Each element contains: <dl> <dt>int `i_size`</dt> <dd>Number of text rows for the answer input. Values greater than 1 indicate a multi-line answer.</dd> <dt>bool `is_multiple`</dt> <dd>`true` if `i_size` is greater than 1, `false` otherwise.</dd> <dt>string `s_key`</dt> <dd>SHA1 hash of the question text, used as a unique identifier for the question.</dd> <dt>string `s_question`</dt> <dd>Question text.</dd> </dl>'
      tags:
        - Question-Question
  '/Wl/Passport/Login/Enter/PassportOtp.json':
    get:
      summary: 'Sends an OTP code to the user''s email or phone number to initiate authorization. Checks the OTP rate limit, generates a new code for the given user, and dispatches it according to the chosen delivery strategy. The user must not be already signed in and must not be an admin. There are two ways to specify the delivery channel: **1. Broadcast strategy** (default) Set {@link PassportOtpApi::$id_delivery_strategy} to `null` or {@link OtpDeliveryStrategyEnum::BROADCAST} (or omit it entirely). Use {@link PassportOtpApi::$is_mail} and/or {@link PassportOtpApi::$is_phone} to specify which channels to use. At least one of them must be `true`; the OTP is sent to every channel that is enabled. The user must have a corresponding contact — a phone number when {@link PassportOtpApi::$is_phone} is `true`, an email when {@link PassportOtpApi::$is_mail} is `true` — otherwise an error is thrown. Example: set {@link PassportOtpApi::$is_mail} to `true` and {@link PassportOtpApi::$is_phone} to `true` to send OTP via both email and SMS simultaneously. **2. Priority strategy** Set {@link PassportOtpApi::$id_delivery_strategy} to {@link OtpDeliveryStrategyEnum::PRIORITY}. Provide {@link PassportOtpApi::$text_delivery_priority} — a comma-separated list of `sms` and `email` values in the preferred order, e.g. `"sms,email"`. The system picks the first channel from the list for which the user has valid contact data and sends the OTP only to that one channel. The selected channel is returned in {@link PassportOtpApi::$text_delivery_selected}; when SMS is selected, the last four digits of the phone are returned in {@link PassportOtpApi::$text_phone_masked}. If none of the listed channels can be used, an error is thrown. Example: set {@link PassportOtpApi::$text_delivery_priority} set to `sms,email` to prefer SMS but fall back to email if the user has no phone number.'
      parameters:
        - name: id_delivery_strategy
          in: query
          description: 'Type of delivery strategy from {@link OtpDeliveryStrategyEnum}.'
          schema:
            type: [number, "null"]
        - name: is_mail
          in: query
          description: 'Whether OTP code will be sending to user via email. `true` if OTP code is sending through email, `false` if OTP code is sending through phone number.'
          schema:
            type: boolean
        - name: is_phone
          in: query
          description: 'Whether OTP code will be sending to user via email. `true` if OTP code is sending through phone number, `false` if OTP code is sending through email.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_delivery_priority
          in: query
          description: 'Priority of delivery. Fixed values `sms` and `email` are supported. Should be given in priority order comma-separated.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_delivery_selected:
                        type: string
                        description: 'Delivery channel that was selected based on the given priorities and user data. Fixed values `sms` and `email` are supported. Only one value is returned.'
                      text_phone_masked:
                        type: string
                        description: 'Phone number masked with `*` symbols in case if we have priority sending and sms sending was selected.'
      tags:
        - Enter-PassportOtp
    post:
      summary: 'Verifies the submitted OTP code and establishes an authorized session for the user. Validates the OTP code for the given user, signs in the session, fires the post-login event, completes business registration if applicable, and returns the redirect URL for the business frontend.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp_code:
                  type: string
                  description: 'Otp code integer that was random generated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_attempt_left:
                        type: number
                        format: int32
                        description: 'Number of attempts left to submit the correct otp code.'
                      url_redirect:
                        type: string
                        description: 'Redirect url after successful authorization.'
      tags:
        - Enter-PassportOtp
  '/Wl/Passport/Login/Register/RegisterOtpJwtPublicKey.json':
    get:
      summary: 'Gets the public key material for OTP registration JWT verification. The public key is used to verify a JWT token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_keys:
                        type: array
                        description: 'Public key in JWK format. Each array item is one JWK with the following structure: <dl> <dt>string `alg`</dt> <dd>Signing algorithm. Always ''RS256''.</dd> <dt>string `e`</dt> <dd>RSA public exponent in base64url format.</dd> <dt>string `kid`</dt> <dd>Key identifier for JWT header matching.</dd> <dt>string `kty`</dt> <dd>Key type. Always ''RSA''.</dd> <dt>string `n`</dt> <dd>RSA modulus in base64url format.</dd> <dt>string `use`</dt> <dd>Public key usage. Always ''sig''.</dd> </dl>'
                      s_public_key:
                        type: string
                        description: 'Public key in PEM format.'
      tags:
        - Register-RegisterOtpJwtPublicKey
  '/Wl/Passport/Login/Register/RegisterOtp.json':
    get:
      summary: 'Sends an OTP code to the user''s email or phone number to initiate authorization. Checks the OTP rate limit, generates a new code for the given user, and dispatches it via email, SMS, or both depending on `$is_mail` and `$is_phone`. The user must not be already signed in and must not be an admin.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Email address OTP code will be sent to.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'Phone number OTP code will be sent to.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Register-RegisterOtp
    post:
      summary: 'Verifies the submitted OTP code and establishes an authorized session for the user. Validates the OTP code for the given user, signs in the session, fires the post-login event, completes business registration if applicable, and returns the redirect URL for the business frontend.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp_code:
                  type: string
                  description: 'Otp code integer that was random generated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_attempt_left:
                        type: number
                        format: int32
                        description: 'Number of attempts left to submit the correct otp code.'
                      s_jwt_token:
                        type: string
                        description: 'JWT token generated after successful otp validation.'
                      uid:
                        type: string
                        description: 'Key of the user, which was signed in after successful otp verification.'
                      url_redirect:
                        type: string
                        description: 'Redirect url after successful authorization.'
      tags:
        - Register-RegisterOtp
  '/Wl/Discount/Code/Edit/DiscountCodeEdit.json':
    get:
      summary: 'Gets information about discount. Used to load the discount code editor form. Returns the full configuration of an existing discount code so the staff member can review or modify it: value, date range, usage limit, which items it applies to, client type restrictions, and whether it is currently active.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_discount_code
          in: query
          description: 'Key of the discount code. Empty, if this is creation of a new code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_component:
                        type: array
                        description: 'List of components that are affected by this discount code. Each element is an array containing: <dl> <dt>int `id_purchase_item`</dt> <dd>Type ID of the purchase item. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd> Primary key of the component. <p>Special Notes:</p> <ul> <li>Zero means that all elements of this type are available.</li> <li>For `id_purchase_item`={@link \RsPurchaseItemSid::CLASS_PERIOD} we store <i>{@link \RsClassSql}</i>.`k_class` in the `k_id` field.</li> </ul> </dd> <dt>string `text_title`</dt> <dd>Title of the component.</dd> </dl>'
                      a_login_type:
                        type: array
                        description: 'List of client types.'
                      a_member_group:
                        type: array
                        description: 'List of client groups.'
                      dl_end:
                        type: [string, "null"]
                        description: 'Expiration date. <tt>null</tt> if discount code is never expires.'
                      dl_start:
                        type: string
                        description: 'Activation date.'
                      f_value:
                        type: string
                        description: 'Value of the percentage or fixed discount.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'Count of calendar periods from <var>id_duration</var> before expiration.'
                      i_limit:
                        type: number
                        format: int32
                        description: 'Maximum count of usage. Zero means unlimited usage.'
                      id_duration:
                        type: number
                        format: int32
                        description: 'Calendar periods for discount code duration from {@link \ADurationSid}. Zero if discount code is never expires.'
                      id_duration_type:
                        type: number
                        format: int32
                        description: 'Type of the discount code duration from {@link \RsDurationTypeSid}.'
                      is_active:
                        type: boolean
                        description: '<tt>true</tt> means discount code is active. <tt>false</tt> means discount code is inactive.'
                      is_auto_renew:
                        type: boolean
                        description: '<tt>true</tt> means membership will auto-renew at discounted rate. <tt>false</tt> means membership will auto-renew at full rate.'
                      is_coupon:
                        type: boolean
                        description: '<tt>true</tt> means discount code can reduce price of the gift cards. <tt>false</tt> means discount code cannot reduce price of the gift cards. If {@link DiscountCodeEditApi::$is_purchase_all} is <tt>true</tt> this fields does not matter.'
                      is_first_payment:
                        type: boolean
                        description: '<tt>true</tt> means discount code can be applied to first membership payment only. <tt>false</tt> means discount code can be applied to all membership payments.'
                      is_login_type:
                        type: boolean
                        description: '<tt>true</tt> means that only selected client types or groups can use this discount code. <tt>false</tt> means that all client types and groups can use this discount code.'
                      is_online:
                        type: boolean
                        description: '<tt>true</tt> if clients can use this discount code in online store, <tt>false</tt> if discount code can be used only in point of sale.'
                      is_percent:
                        type: boolean
                        description: '<tt>true</tt> means discount value is percent from the purchase amount. <tt>false</tt> means discount value is fixed sum from the purchase amount.'
                      is_purchase_all:
                        type: boolean
                        description: '<tt>true</tt> means discount can be applied to all products and purchase options. <tt>false</tt> means discount can be applied only to the selected product and purchase options.'
                      s_component:
                        type: string
                        description: 'List of product and purchase options that are effected with this discount code. Each element has the next format: <var>id_purchase_item</var>:<var>k_id</var>, where <tt>id_purchase_item</tt> - one of {@link \RsPurchaseItemSid}, <tt>k_id</tt> is key of certain purchase item. <i>Processed by function {@link \Core\Tool\UrlEncode\UrlEncode::encode()}.</i>'
                      text_code:
                        type: string
                        description: 'Discount code that can be used during purchase.'
                      text_description:
                        type: string
                        description: 'Description of the discount code.'
                      text_title:
                        type: string
                        description: 'Name of the discount code.'
      tags:
        - Edit-DiscountCodeEdit
    post:
      summary: 'Saves information about discount code or create new. Used to create a new discount code or update an existing one from the backend editor. Pass no discount code key to create; pass an existing key to update. Returns the key of the saved code.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_discount_code
          in: query
          description: 'Key of the discount code. Empty, if this is creation of a new code.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_type:
                  type: array
                  description: 'List of client types.'
                a_member_group:
                  type: array
                  description: 'List of client groups.'
                dl_end:
                  type: [string, "null"]
                  description: 'Expiration date. <tt>null</tt> if discount code is never expires.'
                dl_start:
                  type: string
                  description: 'Activation date.'
                f_value:
                  type: string
                  description: 'Value of the percentage or fixed discount.'
                i_duration:
                  type: number
                  format: int32
                  description: 'Count of calendar periods from <var>id_duration</var> before expiration.'
                i_limit:
                  type: number
                  format: int32
                  description: 'Maximum count of usage. Zero means unlimited usage.'
                id_duration:
                  type: number
                  format: int32
                  description: 'Calendar periods for discount code duration from {@link \ADurationSid}. Zero if discount code is never expires.'
                id_duration_type:
                  type: number
                  format: int32
                  description: 'Type of the discount code duration from {@link \RsDurationTypeSid}.'
                is_active:
                  type: boolean
                  description: '<tt>true</tt> means discount code is active. <tt>false</tt> means discount code is inactive.'
                is_auto_renew:
                  type: boolean
                  description: '<tt>true</tt> means membership will auto-renew at discounted rate. <tt>false</tt> means membership will auto-renew at full rate.'
                is_coupon:
                  type: boolean
                  description: '<tt>true</tt> means discount code can reduce price of the gift cards. <tt>false</tt> means discount code cannot reduce price of the gift cards. If {@link DiscountCodeEditApi::$is_purchase_all} is <tt>true</tt> this fields does not matter.'
                is_first_payment:
                  type: boolean
                  description: '<tt>true</tt> means discount code can be applied to first membership payment only. <tt>false</tt> means discount code can be applied to all membership payments.'
                is_login_type:
                  type: boolean
                  description: '<tt>true</tt> means that only selected client types or groups can use this discount code. <tt>false</tt> means that all client types and groups can use this discount code.'
                is_online:
                  type: boolean
                  description: '<tt>true</tt> if clients can use this discount code in online store, <tt>false</tt> if discount code can be used only in point of sale.'
                is_percent:
                  type: boolean
                  description: '<tt>true</tt> means discount value is percent from the purchase amount. <tt>false</tt> means discount value is fixed sum from the purchase amount.'
                is_purchase_all:
                  type: boolean
                  description: '<tt>true</tt> means discount can be applied to all products and purchase options. <tt>false</tt> means discount can be applied only to the selected product and purchase options.'
                s_component:
                  type: string
                  description: 'List of product and purchase options that are effected with this discount code. Each element has the next format: <var>id_purchase_item</var>:<var>k_id</var>, where <tt>id_purchase_item</tt> - one of {@link \RsPurchaseItemSid}, <tt>k_id</tt> is key of certain purchase item. <i>Processed by function {@link \Core\Tool\UrlEncode\UrlEncode::encode()}.</i>'
                text_code:
                  type: string
                  description: 'Discount code that can be used during purchase.'
                text_description:
                  type: string
                  description: 'Description of the discount code.'
                text_title:
                  type: string
                  description: 'Name of the discount code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_discount_code:
                        type: string
                        description: 'Key of the discount code. Empty, if this is creation of a new code.'
      tags:
        - Edit-DiscountCodeEdit
    put:
      summary: 'Duplicates an existing discount code. Used in the backend when a staff member wants to create a similar discount code without re-entering all settings manually. Creates an exact copy with a new unique code string.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_discount_code
          in: query
          description: 'Key of the discount code. Empty, if this is creation of a new code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_discount_code:
                        type: string
                        description: 'Key of the discount code. Empty, if this is creation of a new code.'
      tags:
        - Edit-DiscountCodeEdit
  '/Wl/Business/Account/Subscription/SubscriptionInfo.json':
    get:
      summary: 'Gets information about subscription. Used in the backend settings UI to read the current state of a business subscription (e.g., Achieve or another product). Returns whether the subscription is active, the current plan tier, and the business locale. Requires backend access to the business.'
      parameters:
        - name: cid_subscription
          in: query
          description: 'CID of the subscription information of which is requested.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key for which subscription information is requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_locale:
                        type: number
                        format: int32
                        description: 'Locale ID of the business which subscription information is requested for.'
                      id_plan:
                        type: number
                        format: int32
                        description: 'Currently active plan ID for requested subscription. Depending on subscription type constant from one of {@link PlanSid} inheritors. Plans for `2331`: <dl> <dt>int `id_plan`</dt> <dd> QuickBooks Online subscription plan ID. One of {@link \Wl\Business\Account\Subscription\QuickBooks\QuickBooksSubscriptionSid} constants. </dd> </dl> Plans for `1868`: <dl> <dt>int `id_plan`</dt> <dd> CAASI subscription plan ID. One of {@link \Wl\Business\Account\Subscription\AiAgent\AiAgentSubscriptionSid} constants. </dd> </dl> Plans for `1250`: <dl> <dt>int `id_plan`</dt> <dd> Collections subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Collections\CollectionsSubscriptionSid} constants. </dd> </dl> Plans for `1965`: <dl> <dt>int `id_plan`</dt> <dd> Mailchimp / Constant Contact subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Email\EmailSubscriptionSid} constants. </dd> </dl> Plans for `2166`: <dl> <dt>int `id_plan`</dt> <dd> Constant Contact Integration subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Eml\ConstantContactSubscriptionSid} constants. </dd> </dl> Plans for `2165`: <dl> <dt>int `id_plan`</dt> <dd> Mailchimp Integration subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Eml\MailchimpSubscriptionSid} constants. </dd> </dl> Plans for `1838`: <dl> <dt>int `id_plan`</dt> <dd> Marketing Suite subscription plan ID. One of {@link \Wl\Business\Account\Subscription\MarketingSuite\MarketingSuiteSubscriptionSid} constants. </dd> </dl> Plans for `857`: <dl> <dt>int `id_plan`</dt> <dd> Message Center subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Sms\SmsSubscriptionSid} constants. </dd> </dl> Plans for `858`: <dl> <dt>int `id_plan`</dt> <dd> Achieve White Label App subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Achieve\AchieveSubscriptionSid} constants. </dd> </dl> Plans for `861`: <dl> <dt>int `id_plan`</dt> <dd> Subscription Plan subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Base\BaseSubscriptionSid} constants. </dd> </dl> Plans for `1225`: <dl> <dt>int `id_plan`</dt> <dd> Finance subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Finance\FinanceSubscriptionSid} constants. </dd> </dl> Plans for `1964`: <dl> <dt>int `id_plan`</dt> <dd> Go High Level subscription plan ID. One of {@link \Wl\Business\Account\Subscription\GoHighLevel\GoHighLevelSubscriptionSid} constants. </dd> </dl> Plans for `1349`: <dl> <dt>int `id_plan`</dt> <dd> Zapier Integration subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Zapier\ZapierSubscriptionSid} constants. </dd> </dl> Plans for `986`: <dl> <dt>int `id_plan`</dt> <dd> FitLIVE subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitlive\FitliveSubscriptionSid} constants. </dd> </dl> Plans for `862`: <dl> <dt>int `id_plan`</dt> <dd> Direct Mail subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Postcard\PostcardSubscriptionSid} constants. </dd> </dl> Plans for `2225`: <dl> <dt>int `id_plan`</dt> <dd> Business Success Coaching subscription plan ID. One of {@link \Wl\Business\Account\Subscription\BusinessCoach\BusinessCoachSubscriptionSid} constants. </dd> </dl> Plans for `1370`: <dl> <dt>int `id_plan`</dt> <dd> Door Access subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Door\DoorSubscriptionSid} constants. </dd> </dl> Plans for `883`: <dl> <dt>int `id_plan`</dt> <dd> FitBUILDER subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitbuilder\FitbuilderSubscriptionSid} constants. </dd> </dl> Plans for `886`: <dl> <dt>int `id_plan`</dt> <dd> Rewards & Loyalty subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Reward\RewardSubscriptionSid} constants. </dd> </dl> Plans for `1266`: <dl> <dt>int `id_plan`</dt> <dd> API Access subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Api\ApiSubscriptionSid} constants. </dd> </dl> Plans for `875`: <dl> <dt>int `id_plan`</dt> <dd> FitVID on Demand subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitvid\FitvidSubscriptionSid} constants. </dd> </dl> Plans for `1133`: <dl> <dt>int `id_plan`</dt> <dd> Forms subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Quiz\QuizSubscriptionSid} constants. </dd> </dl> Plans for `863`: <dl> <dt>int `id_plan`</dt> <dd> Presence subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Website\WebsiteSubscriptionSid} constants. </dd> </dl> Plans for `864`: <dl> <dt>int `id_plan`</dt> <dd> Zoom subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Zoom\ZoomSubscriptionSid} constants. </dd> </dl> Plans for `884`: <dl> <dt>int `id_plan`</dt> <dd> FitZONE subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitzone\FitzoneSubscriptionSid} constants. </dd> </dl> Plans for `885`: <dl> <dt>int `id_plan`</dt> <dd> Customer Review Manager subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Review\ReviewSubscriptionSid} constants. </dd> </dl> Plans for `882`: <dl> <dt>int `id_plan`</dt> <dd> Book-a-Spot subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Asset\AssetSubscriptionSid} constants. </dd> </dl>'
                      is_active:
                        type: boolean
                        description: 'Whether subscription is active. `true` if subscription is active, `false` otherwise.'
      tags:
        - Subscription-SubscriptionInfo
  '/Wl/Business/Franchise/Location/BusinessFranchiseLocation.json':
    get:
      summary: 'Returns country, region, state, city and location lists of the franchisor. Used on the franchisor''s website location finder to display all franchisee locations on a map or list, grouped by country, state, and city. Results can be filtered to only locations assigned to a specific franchise region or to exclude churned locations. Results are cached for 3 minutes.'
      parameters:
        - name: id_business_franchise_location
          in: query
          description: 'Determines which locations should be returned. If `null`, {@link BusinessFranchiseLocationSid::ALL} is used.'
          schema:
            type: number
            format: int32
        - name: is_include_churn
          in: query
          description: 'Determines whether to include churned/removed locations. If `true`, all locations are listed (regardless of their status).'
          schema:
            type: boolean
        - name: is_include_non_api
          in: query
          description: 'Determines whether to include locations marked to not be displayed on franchisor website. If `true`, all locations are listed (regardless of this setting).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_city_list:
                        type: array
                        description: 'The city list. Each element has next structure: <dl> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>City title.</dd> </dl>'
                      a_country_list:
                        type: array
                        description: 'The country list. Each element has next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Country title.</dd> </dl>'
                      a_location_list:
                        type: array
                        description: 'The location list. Each element has the next structure: <dl> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl>'
                      a_region_list:
                        type: array
                        description: 'The region list. Each element has the next structure: <dl> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Franchise region title.</dd> </dl>'
                      a_state_list:
                        type: array
                        description: 'The state list. Each element has the next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>State title.</dd> </dl>'
      tags:
        - Location-BusinessFranchiseLocation
  '/Wl/Business/Config/Option/BusinessConfigOption.json':
    get:
      summary: 'Returns config option values for specified business. Loads every registered business configuration option for the given business and returns its current value, keyed by the config option class CID.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_option:
                        type: object
                        description: 'List of config option values. Key - config option class CID. Values - config option value.'
      tags:
        - Option-BusinessConfigOption
  '/Wl/Business/AuthorizeSupport/Response/Response.json':
    get:
      summary: 'Grants or denies access to business location for staff member. Used to respond to a support access request: a business owner accepts or rejects temporary entry for a WellnessLiving support agent. Requires the Manage Business permission. Granting access triggers an email notification and expires after 24 hours; the result is broadcast in real time to the requesting staff member.'
      parameters:
        - name: is_grant
          in: query
          description: 'Determines whether the user will be granted access or if access will be revoked. If <tt>true</tt> - then grant access; If <tt>false</tt> - then revoke access. Revoking access is a scheduled task set to be run within the next 15 minutes.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'The key of the location to access.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user who will be granted access.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response
  '/Wl/Business/Reward/Config/RewardConfig.json':
    get:
      summary: 'Returns the reward program configuration for the specified business. Used in the backend settings editor to show the current enabled or disabled state of the reward program before the staff member makes a change. Always reads live data, bypassing the cache.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_disabled_reward_program:
                        type: boolean
                        description: '<tt>true</tt> Reward program is disabled, <tt>false</tt> - otherwise.'
      tags:
        - Config-RewardConfig
    post:
      summary: 'Updates the reward program enabled or disabled state for the specified business. Used in the backend settings editor when a staff member turns the reward program on or off. The change is recorded in the reward audit log.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_disabled_reward_program:
                  type: boolean
                  description: '<tt>true</tt> Reward program is disabled, <tt>false</tt> - otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Config-RewardConfig
  '/Wl/Business/User/Subscribe/Subscribe.json':
    get:
      summary: 'Retrieves information about if user is subscribed on specified business or not. Used to pre-populate the notification preferences toggle in a client''s profile page. Shows whether the client has opted in to email and SMS communications from the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key used for users to subscribe, unsubscribe, and receive information about the status of the subscription.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose subscription status needs to be checked or switched to subscribed/unsubscribed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_subscribe:
                        type: [boolean, "null"]
                        description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an email subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false` unsubscribes the user in the business. This will be `null` if not set yet.'
                      is_subscribe_sms:
                        type: [boolean, "null"]
                        description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an SMS subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false1 unsubscribes the user in the business. This will be `null` if not set yet.'
      tags:
        - Subscribe-Subscribe
    put:
      summary: 'Subscribes or unsubscribes user on specified business. Called when a client changes their notification preferences. Controls whether the business can contact the client by email and by SMS.'
      parameters:
        - name: is_subscribe
          in: query
          description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an email subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false` unsubscribes the user in the business. This will be `null` if not set yet.'
          schema:
            type: [boolean, "null"]
        - name: is_subscribe_sms
          in: query
          description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an SMS subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false1 unsubscribes the user in the business. This will be `null` if not set yet.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key used for users to subscribe, unsubscribe, and receive information about the status of the subscription.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose subscription status needs to be checked or switched to subscribed/unsubscribed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscribe-Subscribe
  '/Wl/Catalog/StaffApp/CatalogCart/CatalogCart.json':
    get:
      summary: 'Calculates amount of cart. Used in the staff app checkout flow to show a live price summary before the staff member processes a client''s payment. Returns the full breakdown so the staff member can confirm the total with the client before charging.'
      parameters:
        - name: a_item
          in: query
          description: 'The list of cart items with the next structure: <dl> <dt>array `a_config`</dt> <dd> The list of purchase item additional options: <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events for tuition participant. Each record has the next structure: <dl> <dt>array `a_discount`</dt> <dd> Discount applied to the event price: <dl> <dt>string `m_discount`</dt> <dd>Discount amount.</dd> <dt>string `text_discount`</dt> <dd>Discount title.</dd> </dl> </dd> <dt>string `k_class`</dt> <dd>Key of the event class within the tuition which should be granted to the client.</dd> <dt>string `m_checkout`</dt> <dd> The amount that will be charged for this event when the tuition is purchased. Calculated based on the payment schedule (full amount or the prorated part due now). Can be overridden manually. If not specified or `null`, the amount will be calculated automatically. </dd> <dt>string `m_price`</dt> <dd> Price of the event for the tuition participant. If not specified or `null`, price will be calculated based on the class setup. </dd> <dt>string `uid`</dt> <dd>Key of the tuition participant.<i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> <dt>array `a_registration_fee_list`</dt> <dd> Registration fees for tuition participants. Keys are participant keys. <i>Primary key in {@link \PassportLoginSql}.</i> Each value has the next structure: <dl> <dt>array `a_discount`</dt> <dd> Manual discount applied to the registration fee: <dl> <dt>string `m_discount`</dt> <dd>Discount amount.</dd> <dt>string `text_discount`</dt> <dd>Discount title.</dd> </dl> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the tuition participant.</dd> <dt>string `m_discount`</dt> <dd>Total discount amount actually applied to the registration fee.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd>The total amount that will be charged for the tuition item during purchase.</dd> <dt>string `m_deferred`</dt> <dd> The part of the tuition cost that is not charged during purchase. <i>See {@link \Wl\Purchase\Item\Tuition::checkoutConfig()}.</i> </dd> <dt>string `f_price`</dt> <dd>The custom price.</dd> <dt>string `dt_prorate`</dt> <dd>The prorate date. This should be passed when `is_prorate`=`true`.</dd> <dt>string `dt_start`</dt> <dd>The promotion start date.</dd> <dt>bool `is_prorate`</dt> <dd>Determines whether to prorate the first payment.</dd> <dt>bool `is_prorate_fix`</dt> <dd>Determines if the client should pay for the first period now.</dd> <dt>bool `is_prorate_only`</dt> <dd>Determines whether the selected option ''pay prorate amount only'' should only include the prorate amount.</dd> <dt>string `m_custom`</dt> <dd>The custom price of the gift card.</dd> <dt>string `dt_send_local`</dt> <dd>The date when the gift card email must be sent.</dd> <dt>bool `is_mail`</dt> <dd>If `true`, the gift card will be sent via email. Otherwise, `false` if the gift card will be printed.</dd> <dt>string `s_mail`</dt> <dd>The recipient''s email.</dd> <dt>string `s_message`</dt> <dd>The message.</dd> <dt>string `s_recipient`</dt> <dd>The recipient''s name.</dd> <dt>string `s_sender`</dt> <dd>The sender''s name.</dd> <dt>string `m_prorate_custom`</dt> <dd>The amount of money for the prorate period. This should only be passed in the case of manual entry.</dd> <dt>string `k_coupon`</dt> <dd>The coupon key.</dd> <dt>string `k_coupon_amount`</dt> <dd>The coupon amount key.</dd> </dl> </dd> <dt>array `a_tax_custom`</dt> <dd> Information about taxes. If not passed, no custom taxes have been applied to the sale item. If a record is present, the tax is custom using the next structure: <dl> <dt>string `f_tax`</dt> <dd>The tax amount.</dd> <dt>string `k_tax`</dt> <dd>The tax keys. <i>Primary key in {@link \RsTaxSql} table.</i></dd> </dl> </dd> <dt>float `f_discount_percent`</dt> <dd>The discount percentage, applied to the current item.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The sale item key.</dd> <dt>string `k_shop_product_option`</dt> <dd> The store product option key. This will be `null` if the sale item has no options. <i>Primary key in {@link \RsShopProductOptionSql} table.</i> </dd> <dt>string `m_discount_fix`</dt> <dd>The fixed price discount, applied to the current item.</dd> <dt>string `m_price_custom`</dt> <dd>The custom price of the sale item. If not passed, no custom price has been applied to the sale item.</dd> </dl>'
          schema:
            type: array
        - name: f_discount_percent
          in: query
          description: 'The discount as a percentage.'
          schema:
            type: number
            format: float
        - name: is_check_cart_item
          in: query
          description: 'If `true`, every item is checked at the cart. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. This field isn''t used directly, but described for correct JavaScript auto-generation.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The business location key.'
          schema:
            type: string
        - name: m_discount_flat
          in: query
          description: 'The manual discount amount in dollars, excluding tax. Staff members can set this amount when making a sale.'
          schema:
            type: string
        - name: m_tip
          in: query
          description: 'The amount of tips.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code. This will be `null` if not set yet.'
          schema:
            type: [string, "null"]
        - name: uid_current
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
        - name: uid_customer
          in: query
          description: 'The key of the user who performed the purchase.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount_code:
                        type: array
                        description: 'A list of available discount codes with the next structure: <dl> <dt>bool `is_select`</dt> <dd>`true` if this code is selected currently, `false` otherwise.</dd> <dt>string `k_discount_code`</dt> <dd>Discount code key. <i>Primary key from the {@link \Wl\Discount\Code\Sql} table.</i></dd> <dt>string `text_discount_code`</dt> <dd>Value of the code that can be used to get discount.</dd> <dt>string `text_title`</dt> <dd>Name of the code.</dd> </dl>'
                      a_item:
                        type: array
                        description: 'The list of cart items with the next structure: <dl> <dt>array `a_config`</dt> <dd> The list of purchase item additional options: <dl> <dt>array `a_event_list`</dt> <dd> List of tuition events for tuition participant. Each record has the next structure: <dl> <dt>array `a_discount`</dt> <dd> Discount applied to the event price: <dl> <dt>string `m_discount`</dt> <dd>Discount amount.</dd> <dt>string `text_discount`</dt> <dd>Discount title.</dd> </dl> </dd> <dt>string `k_class`</dt> <dd>Key of the event class within the tuition which should be granted to the client.</dd> <dt>string `m_checkout`</dt> <dd> The amount that will be charged for this event when the tuition is purchased. Calculated based on the payment schedule (full amount or the prorated part due now). Can be overridden manually. If not specified or `null`, the amount will be calculated automatically. </dd> <dt>string `m_price`</dt> <dd> Price of the event for the tuition participant. If not specified or `null`, price will be calculated based on the class setup. </dd> <dt>string `uid`</dt> <dd>Key of the tuition participant.<i>Primary key in {@link \PassportLoginSql}.</i></dd> </dl> </dd> <dt>array `a_registration_fee_list`</dt> <dd> Registration fees for tuition participants. Keys are participant keys. <i>Primary key in {@link \PassportLoginSql}.</i> Each value has the next structure: <dl> <dt>array `a_discount`</dt> <dd> Manual discount applied to the registration fee: <dl> <dt>string `m_discount`</dt> <dd>Discount amount.</dd> <dt>string `text_discount`</dt> <dd>Discount title.</dd> </dl> </dd> <dt>string `m_amount`</dt> <dd>Registration fee amount for the tuition participant.</dd> <dt>string `m_discount`</dt> <dd>Total discount amount actually applied to the registration fee.</dd> </dl> </dd> <dt>string `m_checkout`</dt> <dd>The total amount that will be charged for the tuition item during purchase.</dd> <dt>string `m_deferred`</dt> <dd> The part of the tuition cost that is not charged during purchase. <i>See {@link \Wl\Purchase\Item\Tuition::checkoutConfig()}.</i> </dd> <dt>string `f_price`</dt> <dd>The custom price.</dd> <dt>string `dt_prorate`</dt> <dd>The prorate date. This should be passed when `is_prorate`=`true`.</dd> <dt>string `dt_start`</dt> <dd>The promotion start date.</dd> <dt>bool `is_prorate`</dt> <dd>Determines whether to prorate the first payment.</dd> <dt>bool `is_prorate_fix`</dt> <dd>Determines if the client should pay for the first period now.</dd> <dt>bool `is_prorate_only`</dt> <dd>Determines whether the selected option ''pay prorate amount only'' should only include the prorate amount.</dd> <dt>string `m_custom`</dt> <dd>The custom price of the gift card.</dd> <dt>string `dt_send_local`</dt> <dd>The date when the gift card email must be sent.</dd> <dt>bool `is_mail`</dt> <dd>If `true`, the gift card will be sent via email. Otherwise, `false` if the gift card will be printed.</dd> <dt>string `s_mail`</dt> <dd>The recipient''s email.</dd> <dt>string `s_message`</dt> <dd>The message.</dd> <dt>string `s_recipient`</dt> <dd>The recipient''s name.</dd> <dt>string `s_sender`</dt> <dd>The sender''s name.</dd> <dt>string `m_prorate_custom`</dt> <dd>The amount of money for the prorate period. This should only be passed in the case of manual entry.</dd> <dt>string `k_coupon`</dt> <dd>The coupon key.</dd> <dt>string `k_coupon_amount`</dt> <dd>The coupon amount key.</dd> </dl> </dd> <dt>array `a_tax_custom`</dt> <dd> Information about taxes. If not passed, no custom taxes have been applied to the sale item. If a record is present, the tax is custom using the next structure: <dl> <dt>string `f_tax`</dt> <dd>The tax amount.</dd> <dt>string `k_tax`</dt> <dd>The tax keys. <i>Primary key in {@link \RsTaxSql} table.</i></dd> </dl> </dd> <dt>float `f_discount_percent`</dt> <dd>The discount percentage, applied to the current item.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The sale item key.</dd> <dt>string `k_shop_product_option`</dt> <dd> The store product option key. This will be `null` if the sale item has no options. <i>Primary key in {@link \RsShopProductOptionSql} table.</i> </dd> <dt>string `m_discount_fix`</dt> <dd>The fixed price discount, applied to the current item.</dd> <dt>string `m_price_custom`</dt> <dd>The custom price of the sale item. If not passed, no custom price has been applied to the sale item.</dd> </dl>'
                      is_commission:
                        type: boolean
                        description: 'Determines whether the business applied a commission at checkout.'
                      is_discount_code_mode_select:
                        type: boolean
                        description: 'Determines, how staff sees discount codes in Store. `true` as select field, `false` as input field.'
                      is_receipt_note:
                        type: boolean
                        description: 'Determines whether to display custom receipt notes at checkout.'
                      m_checkout:
                        type: string
                        description: 'The amount that has to be charged right now for the cart. Equals {@link CatalogCartApi::$m_total} for everything that is paid for in full at once. A tuition defers a part of its cost to an installment plan or to a membership schedule, and `m_total` covers the full cost regardless, so this is the amount to put into the payment form. It is `m_total` minus `a_config.m_deferred` of every tuition item in {@link CatalogCartApi::$a_item}.'
                      m_checkout_before_discount:
                        type: string
                        description: 'The subtotal amount that has to be charged right now for the cart, before discount. Equals {@link CatalogCartApi::$m_subtotal} for everything that is paid for in full at once. See {@link CatalogCartApi::$m_checkout} for how a tuition''s deferred cost is excluded.'
                      m_checkout_before_tax:
                        type: string
                        description: 'The amount that has to be charged right now for the cart, after discount but before tax. Equals {@link CatalogCartApi::$m_checkout_before_discount} minus {@link CatalogCartApi::$m_checkout_discount}.'
                      m_checkout_discount:
                        type: string
                        description: 'The discount amount that applies to the part of the cart charged right now, excluding tax. Equals {@link CatalogCartApi::$m_discount_total} for everything that is paid for in full at once. See {@link CatalogCartApi::$m_checkout} for how a tuition''s deferred cost is excluded.'
                      m_checkout_tax:
                        type: string
                        description: 'The amount of tax that applies to the part of the cart charged right now. Equals {@link CatalogCartApi::$m_tax} for everything that is paid for in full at once. See {@link CatalogCartApi::$m_checkout} for how a tuition''s deferred cost is excluded.'
                      m_discount:
                        type: string
                        description: 'The discount amount in dollars, excluding tax.'
                      m_discount_total:
                        type: string
                        description: 'The discount amount applied to the cart''s total amount, including taxes.'
                      m_subtotal:
                        type: string
                        description: 'The subtotal amount.'
                      m_tax:
                        type: string
                        description: 'The amount of tax.'
                      m_tip_purchase:
                        type: string
                        description: 'The amount of appointment tips.'
                      m_total:
                        type: string
                        description: 'The cart''s total amount.'
                      text_error_code:
                        type: [string, "null"]
                        description: 'The discount code''s error code. `null` if no error.'
                      text_error_message:
                        type: [string, "null"]
                        description: 'The discount code''s error message. `null` if no error.'
                      text_receipt_note:
                        type: string
                        description: 'The custom receipt note''s text.'
      tags:
        - CatalogCart-CatalogCart
  '/Wl/Catalog/StaffApp/CatalogList/CatalogList.json':
    get:
      summary: 'Returns all sale items available in the business for staff, optionally filtered by location and visit. Used in the staff app to populate the add-item picker during checkout. Returns the products and services the staff member is allowed to sell at the current location. When a visit key is provided, the list is filtered to items that are relevant to that visit.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get categories for.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key. This can affect the list of displayed products.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'The visit key to pay for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_shop_product:
                        type: array
                        description: 'Products in the online store category. Every element has the following fields: <dl> <dt>string[] `a_location`</dt> <dd> The list of location keys where the current sale item is available. Each value is a location key. <i>Primary key in the {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd> The list of login types available to purchase the sale item. Empty list means that the sale item is available to all login types. </dd> <dt>string[] `a_member_group`</dt> <dd> The list of member groups available to purchase the sale item. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i> Empty list means that the sale item is available to all member groups. </dd> <dt>string[] `a_shop_category`</dt> <dd>A list of online store category keys. <i>Primary keys in {@link \RsShopCategorySql} table.</i></dd> <dt>int `id_program`</dt> <dd> The program ID, set for promotions. One of the {@link \RsProgramSid} constants. <p>If `id_program` is {@link \RsProgramSid::INSURANCE_MEMBERSHIP}, then use:</p> <ul> <li>{@link \Wl\Insurance\Catalog\ProgramListApi} to obtain a list of active programs.</li> <li>{@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi} to get and validate fields for a given program.</li> <li>{@link \Wl\Catalog\Payment\PaymentApi} for a program purchase.</li> </ul> </dd> <dt>int `id_restriction`</dt> <dd>The restriction ID. One of the {@link \Wl\Shop\Product\PurchaseRestrictionSid} constants.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_online_sell`</dt> <dd>Determines whether the sale item can be purchased by the client.</dd> <dt>bool `is_visit`</dt> <dd> This will be <tt>true</tt> if this Purchase Option is suitable to pay for the visit {@link \Wl\Catalog\StaffApp\CatalogList\CatalogListApi::$k_visit}. Otherwise, this will be <tt>false</tt>. If {@link \Wl\Catalog\StaffApp\CatalogList\CatalogListApi::$k_visit} is empty, this will always be `false`. </dd> <dt>string `k_id`</dt> <dd>The sale item ID.</dd> <dt>string `text_title`</dt> <dd>The category title.</dd> <dt>string `url_catalog`</dt> <dd>Direct link to catalog view page of this purchase item.</dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'If `true`, the current user is able to add the purchased item to the account.'
      tags:
        - CatalogList-CatalogList
  '/Wl/Catalog/StaffApp/CatalogView/CatalogView.json':
    get:
      summary: 'Calculates price information about sale item. Used in the staff app checkout to show the real-time price of a single line item as the staff member configures it (adjusting quantity, custom price, or proration). Returns the full tax breakdown so the staff member sees exactly what the client will be charged.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration information about the item, which can specify prorated amounts. If set, these values will change the values returned by the endpoint call. <dl> <dt>float `f_promote`</dt> <dd>The amount to prorate the item by.</dd> <dt>bool `is_prorate`</dt> <dd>If <tt>true</tt>, this item is prorated.</dd> <dt>string `m_prorate_custom`</dt> <dd>The custom prorate amount.</dd> </dl>'
          schema:
            type: array
        - name: a_tax
          in: query
          description: 'Contains information about edited taxes. This will be `null` if not set yet or default taxes are used. The key is the tax identifier, where the value is: <dl> <dt>string `k_tax`</dt> <dd>The tax ID.</dd> <dt>string `m_tax_custom`</dt> <dd>The amount of custom tax.</dd> </dl>'
          schema:
            type: array
        - name: i_quantity
          in: query
          description: 'The quantity of items.'
          schema:
            type: number
            format: int32
        - name: id_sale
          in: query
          description: 'The ID of the sale category. One of the {@link \RsSaleSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key. This will be `null` for the system business.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The key of the sale item. This can be retrieved with the {@link CatalogListApi} endpoint (see the <var>a_shop_product</var> parameter).'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'The product option key.'
          schema:
            type: string
        - name: m_price
          in: query
          description: 'The custom price of the sale item.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The ID of the user who performed the actions.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tax_data:
                        type: array
                        description: 'Contains information about calculated taxes. <dl> <dt>float `f_tax`</dt> <dd>The calculated tax amount applied by this rule.</dd> <dt>string `f_tax_discount`</dt> <dd>The tax amount after applying all discounts.</dd> <dt>string `f_tax_discount_login`</dt> <dd>The tax amount after applying the client type discount only.</dd> <dt>float `f_value`</dt> <dd>The tax rate. Its meaning depends on `id_tax`.</dd> <dt>int `id_tax`</dt> <dd>The tax type. One of {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key. <i>Primary key in the {@link \RsTaxSql} table.</i></dd> <dt>string `s_tax`</dt> <dd>The tax name.</dd> </dl>'
                      m_prorate:
                        type: string
                        description: 'The prorated amount.'
                      m_subtotal:
                        type: string
                        description: 'The amount of the sale item, excluding taxes.'
                      m_tax:
                        type: string
                        description: 'The calculated amount of tax.'
                      m_total:
                        type: string
                        description: 'The calculated amount of the sale item, including taxes.'
      tags:
        - CatalogView-CatalogView
  '/Wl/Reward/Action/CategoryList/CategoryList.json':
    get:
      summary: 'Retrieves all reward action categories for business specified in {@link \Wl\Reward\Action\CategoryList\CategoryListApi::$k_business}. Returns the list of reward action categories including category type ID, database key, and title for the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category:
                        type: array
                        description: '<dl> <dt>int|null `id_reward_action_category`</dt> <dd> ID of reward actions category type. One of {@link \RsRewardActionCategorySid} constants. <tt>null</tt> for custom reward actions categories. </dd> <dt>string `k_reward_action_category`</dt> <dd>ID of reward actions category in database. <i>Primary key in {@link \RsRewardActionCategorySql} table.</i></dd> <dt>string `s_title`</dt> <dd>Title of the reward action.</dd> </dl>'
      tags:
        - CategoryList-CategoryList
  '/Wl/Reward/Board/BoardList/List.json':
    get:
      summary: 'Retrieves all reward boards for business specified in {@link \Wl\Reward\Board\BoardList\ListApi::$k_business}. Returns the list of reward boards available to the given user in the business, including board key and title, filtered to exclude boards the user is not eligible to view.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward_board:
                        type: [array, "null"]
                        description: 'A list of reward boards. `null` if not loaded. Each element: <dl> <dt>string `k_reward_board`</dt> <dd>Reward board key. `0` for the all-time system board. <i>Primary key in {@link \RsRewardBoardSql}.</i></dd> <dt>string `s_title`</dt> <dd>Board title.</dd> </dl>'
      tags:
        - BoardList-List
  '/Wl/Reward/Score/Current/Current.json':
    get:
      summary: 'Retrieves information about current score in wellnessliving points. Returns the user''s current reward point balance, their rank in the first available leaderboard, and the leaderboard title for the specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_board_rank:
                        type: number
                        format: int32
                        description: 'Current user''s rank in the first leaderboard.'
                      i_score:
                        type: number
                        format: int32
                        description: 'Current score in wellnessliving points.'
                      s_board_title:
                        type: string
                        description: 'Title of the first leaderboard.'
      tags:
        - Current-Current
  '/Wl/Member/Progress/Log/Verify.json':
    put:
      summary: 'Verifies client''s progress log data. Marks all unverified progress log entries for the specified user and date as verified by the currently signed-in staff member, then triggers a search index reindex for the user.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date without time of the progress log.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Log-Verify
  '/Wl/Member/Progress/Field/State.json':
    put:
      summary: 'Changes states of field. Updates one or more state flags (`is_active`, `is_public`, `is_require`, `is_require_staff`) for the specified progress field and returns the new values. At least one flag must be provided; the field must be active to change its required state.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: [boolean, "null"]
                  description: 'Whether field is active and should be displayed on page.'
                is_public:
                  type: [boolean, "null"]
                  description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                is_require:
                  type: [boolean, "null"]
                  description: 'Whether field value is required for clients.'
                is_require_staff:
                  type: [boolean, "null"]
                  description: 'Whether field value is required for staffs.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_field:
                        type: [number, "null"]
                        description: 'Field ID. One of the {@link ProgressFieldSid} constant.'
                      is_active:
                        type: [boolean, "null"]
                        description: 'Whether field is active and should be displayed on page.'
                      is_public:
                        type: [boolean, "null"]
                        description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                      is_require:
                        type: [boolean, "null"]
                        description: 'Whether field value is required for clients.'
                      is_require_staff:
                        type: [boolean, "null"]
                        description: 'Whether field value is required for staffs.'
                      text_title:
                        type: [string, "null"]
                        description: 'Field title.'
      tags:
        - Field-State
  '/Wl/Member/Group/User/UserGroup.json':
    delete:
      summary: 'Deletes the user from the group. Removes the specified user from the given member group within the business after verifying access rights and that the group key is valid.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: [string, "null"]
        - name: uid_user
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-UserGroup
    get:
      summary: 'Gets information about all groups to which the specified user belongs. Returns the list of member group keys for the given user within the specified business, populating `$a_group_info` with those keys after access validation.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: [string, "null"]
        - name: uid_user
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_group_info:
                        type: array
                        description: 'All groups to which the specified user belongs.'
                      is_quick_group:
                        type: boolean
                        description: 'Whether the user is in the quick group or not.'
      tags:
        - User-UserGroup
    post:
      summary: 'Adds a user to a group. Assigns the specified user to the given member group within the business after verifying access rights and that the group key is valid.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: [string, "null"]
        - name: uid_user
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_member_group:
                  type: [string, "null"]
                  description: 'Member group key. Use special value ''quick'' to add/remove the user to/from the quick group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-UserGroup
  '/Wl/Member/Group/Edit/Edit.json':
    get:
      summary: 'Returns data for group edit form. Gets full information about a client group.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_conversion_type:
                        type: [number, "null"]
                        description: 'Lead conversion type. `null` if this group is not used for to track lead conversion.'
                      id_member_group_shape:
                        type: number
                        format: int32
                        description: 'Shape of icon. One of {@link \Wl\Member\Group\ShapeSid} constants.'
                      is_brivo_active:
                        type: boolean
                        description: 'Whether Facility Access enabled for group.'
                      is_brivo_checkin_active:
                        type: boolean
                        description: 'Whether automatic check-in on Brivo access granted is enabled for the group. When enabled, a successful Brivo door access reported for a client of this group triggers an automatic check-in attempt in WellnessLiving. When disabled, Brivo access events for these clients are ignored.'
                      is_brivo_invitation_active:
                        type: boolean
                        description: 'Whether Brivo invitation feature enabled for the group.'
                      is_icon:
                        type: boolean
                        description: '`true` to enable group icon. `false` to disable.'
                      is_update:
                        type: boolean
                        description: 'Whether auto-update enabled for group.'
                      k_search_template:
                        type: string
                        description: 'Key of existing template. Can be empty if template needs to be created.'
                      s_color_background:
                        type: string
                        description: 'Hexadecimal color of icon background.'
                      s_color_foreground:
                        type: string
                        description: 'Hexadecimal color of icon foreground.'
                      s_icon:
                        type: string
                        description: 'Characters on icon.'
                      text_title:
                        type: string
                        description: 'Title for a client group.'
      tags:
        - Edit-Edit
    post:
      summary: 'Adds or changes a client group. Use to update existing client group or create a new one.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_criteria:
                  type: array
                  description: 'Entity in array format. <dl> <dt>int `cid_search_criteria`</dt> <dd>Search criteria CID. Constant from {@link \Wl\Search\Criteria\SearchCriteriaAbstract} subclasses.</dd> <dt>int `id_operation`</dt> <dd>ID of selected operation. Constant from {@link \Wl\Search\SearchOperationSid}.</dd> <dt>string `s_id`</dt> <dd> Unique criteria key. <i>Detailed description see {@link \Wl\Search\Criteria\SearchCriteriaAbstract::__construct()}.</i> </dd> <dt>mixed `x_value`</dt> <dd>Operation data value.</dd> </dl> can be transferred to server via HTTP.'
                a_logic:
                  type: [object, "null"]
                  description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                a_search_entity:
                  type: array
                  description: 'Search entity CID list. Constants from {@link SearchEntityAbstract} subclasses.'
                a_staff_role_selected:
                  type: array
                  description: 'Selected staff roles of the search template. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. <i>Primary key in {@link \RsBusinessRoleSql} table.</i></dd> </dl>'
                id_conversion_type:
                  type: [number, "null"]
                  description: 'Lead conversion type. `null` if this group is not used for to track lead conversion.'
                id_member_group_shape:
                  type: number
                  format: int32
                  description: 'Shape of icon. One of {@link \Wl\Member\Group\ShapeSid} constants.'
                id_share:
                  type: string
                  description: 'Type of share option. One of {@link ShareSid} constants.'
                is_brivo_active:
                  type: boolean
                  description: 'Whether Facility Access enabled for group.'
                is_brivo_checkin_active:
                  type: boolean
                  description: 'Whether automatic check-in on Brivo access granted is enabled for the group. When enabled, a successful Brivo door access reported for a client of this group triggers an automatic check-in attempt in WellnessLiving. When disabled, Brivo access events for these clients are ignored.'
                is_brivo_invitation_active:
                  type: boolean
                  description: 'Whether Brivo invitation feature enabled for the group.'
                is_icon:
                  type: boolean
                  description: '`true` to enable group icon. `false` to disable.'
                is_update:
                  type: boolean
                  description: 'Whether auto-update enabled for group.'
                json_criteria:
                  type: string
                  description: 'Entity in array format. Alternative of {@link SearchTemplatePanelApiTrait::$a_criteria} to transfer data via HTTP as one variable. <i>For more information about the format, see {@link SearchCriteriaList::createArray()} method.</i>'
                s_color_background:
                  type: string
                  description: 'Hexadecimal color of icon background.'
                s_color_foreground:
                  type: string
                  description: 'Hexadecimal color of icon foreground.'
                s_icon:
                  type: string
                  description: 'Characters on icon.'
                text_title:
                  type: string
                  description: 'Title for a client group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_member_group:
                        type: string
                        description: 'Member group key.'
                      k_search_template:
                        type: string
                        description: 'Key of existing template. Can be empty if template needs to be created.'
                      text_warning:
                        type: [string, "null"]
                        description: 'Additional warning message if there were some minor issues with request.'
      tags:
        - Edit-Edit
    put:
      summary: 'Adds or edit client group Query. Use to update existing client group search query or create a new one. Member group should be already created.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_criteria:
                  type: array
                  description: 'Entity in array format. <dl> <dt>int `cid_search_criteria`</dt> <dd>Search criteria CID. Constant from {@link \Wl\Search\Criteria\SearchCriteriaAbstract} subclasses.</dd> <dt>int `id_operation`</dt> <dd>ID of selected operation. Constant from {@link \Wl\Search\SearchOperationSid}.</dd> <dt>string `s_id`</dt> <dd> Unique criteria key. <i>Detailed description see {@link \Wl\Search\Criteria\SearchCriteriaAbstract::__construct()}.</i> </dd> <dt>mixed `x_value`</dt> <dd>Operation data value.</dd> </dl> can be transferred to server via HTTP.'
                a_logic:
                  type: [object, "null"]
                  description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                a_search_entity:
                  type: array
                  description: 'Search entity CID list. Constants from {@link SearchEntityAbstract} subclasses.'
                a_staff_role_selected:
                  type: array
                  description: 'Selected staff roles of the search template. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. <i>Primary key in {@link \RsBusinessRoleSql} table.</i></dd> </dl>'
                id_member_group_shape:
                  type: number
                  format: int32
                  description: 'Shape of icon. One of {@link \Wl\Member\Group\ShapeSid} constants.'
                id_share:
                  type: string
                  description: 'Type of share option. One of {@link ShareSid} constants.'
                is_icon:
                  type: boolean
                  description: '`true` to enable group icon. `false` to disable.'
                json_criteria:
                  type: string
                  description: 'Entity in array format. Alternative of {@link SearchTemplatePanelApiTrait::$a_criteria} to transfer data via HTTP as one variable. <i>For more information about the format, see {@link SearchCriteriaList::createArray()} method.</i>'
                s_color_background:
                  type: string
                  description: 'Hexadecimal color of icon background.'
                s_color_foreground:
                  type: string
                  description: 'Hexadecimal color of icon foreground.'
                s_icon:
                  type: string
                  description: 'Characters on icon.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_warning:
                        type: [string, "null"]
                        description: 'Additional warning message if there were some minor issues with request.'
      tags:
        - Edit-Edit
  '/Wl/Member/Group/GroupList/List.json':
    delete:
      summary: 'Removes groups. Deletes specified groups and associated search templates.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GroupList-List
    get:
      summary: 'Returns all member groups list in the business if <var>$a_member_group_select</var> is empty, otherwise filters result according to <var>$a_member_group_select</var>. If <var>is_return_members</var> is `true` includes in the result list of members of each groups.'
      parameters:
        - name: a_member_group_select
          in: query
          description: 'List of groups for filtering groups of business. `null` in the case when need to return all groups of business.'
          schema:
            type: [array, "null"]
        - name: is_churn_risk
          in: query
          description: 'Whether include "Isaac Churn Risk" group.'
          schema:
            type: boolean
        - name: is_return_members
          in: query
          description: 'Whether include a list of members of groups.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_member_group:
                        type: array
                        description: 'Member groups list: <dl> <dt>string `k_member_group`</dt> <dd>The key of the member group. <i>Primary key in {@link \Wl\Member\Group\Sql}.</i></dd> <dt>string `text_title`</dt> <dd>The name of the member group.</dd> </dl>'
      tags:
        - GroupList-List
    put:
      summary: 'Updates the order of groups in a list. Saves the display order of member groups for the business using the positions supplied in `$a_member_group_order`, verifying that all specified group keys belong to the business before writing.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_member_group_order:
                  type: array
                  description: 'List of groups in the order in which they will be saved.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GroupList-List
  '/Wl/Classes/Period/Modify/Modify.json':
    get:
      summary: 'Gets data for step in wizard. Used by the class modification wizard (edit, cancel, reschedule) to load the data for a specific wizard step. Each step has a different data shape driven by `id_step`; the response feeds directly into the step''s form.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: dt_start
          in: query
          description: 'The start date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: id_class_modify_action
          in: query
          description: 'The class modify step type. One of the {@link \RsClassModifyActionSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_class_modify_mode
          in: query
          description: 'The modify mode type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_step
          in: query
          description: 'The class modify action type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_back
          in: query
          description: 'The step direction. `true` - step forward. `false` - step back. After each step, this variable sets to the default position. By default, the step direction is forward.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be empty if not set yet.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique hash string. <i> This hash string is a hash of the {@link \RsClassModify} object. It needs to work with the same {@link \RsClassModify} object on each step. </i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_get:
                        type: array
                        description: 'Step information, depending on the steps.'
      tags:
        - Modify-Modify
    post:
      summary: 'Makes step in wizard. Used by the class modification wizard to submit a completed step and advance to the next. The wizard tracks state server-side; this call validates the step input, persists it, and returns the next step the UI should render.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: dt_start
          in: query
          description: 'The start date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: id_class_modify_action
          in: query
          description: 'The class modify step type. One of the {@link \RsClassModifyActionSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_class_modify_mode
          in: query
          description: 'The modify mode type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_step
          in: query
          description: 'The class modify action type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_back
          in: query
          description: 'The step direction. `true` - step forward. `false` - step back. After each step, this variable sets to the default position. By default, the step direction is forward.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be empty if not set yet.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique hash string. <i> This hash string is a hash of the {@link \RsClassModify} object. It needs to work with the same {@link \RsClassModify} object on each step. </i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_set:
                  type: array
                  description: 'The list of data to save. Data depends on current {@link ModifyApi::$id_step}: <dl> <dt>bool `is_forward`</dt> <dd>Modify all future sessions. Used in {@link \RsClassModifyStepSid::PERIOD}.</dd> <dt>string[] `a_period`</dt> <dd>Selected session keys (<i>{@link \RsClassPeriodSql}</i>). Used in {@link \RsClassModifyStepSid::ITEM}.</dd> <dt>string[] `a_edit`</dt> <dd> Edited fields flags. Used in {@link \RsClassModifyStepSid::FORM}. Keys are field names (e.g. `a_staff`, `dt_time`); each value is `''1''` to indicate the field was changed. </dd> <dt>array[] `a_staff`</dt> <dd> Selected instructors. Used in {@link \RsClassModifyStepSid::FORM}: <dl> <dt>bool `is_quick_substitute`</dt> <dd>Whether this staff member is a quick substitute. Optional.</dd> <dt>string `k_staff`</dt> <dd><b>Deprecated.</b> Staff key. <i>Primary key in {@link \RsStaffSql}.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i> Optional.</dd> </dl> </dd> <dt>bool `is_inform_mail`</dt> <dd>Send email notification to clients. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_inform_sms`</dt> <dd>Send SMS notification to clients. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_inform_mail_staff`</dt> <dd>Send email notification to staff. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_inform_sms_staff`</dt> <dd>Send SMS notification to staff. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_cancel_resource`</dt> <dd>Cancel resources for modified classes. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `show_canceled_class`</dt> <dd>Show canceled classes in the result list. Used in {@link \RsClassModifyStepSid::CONFIRM}.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_step:
                        type: number
                        format: int32
                        description: 'The class modify action type. One of the {@link \RsClassModifyModeSid} constants.'
                      is_back:
                        type: [boolean, "null"]
                        description: 'The step direction. `true` - step forward. `false` - step back. After each step, this variable sets to the default position. By default, the step direction is forward.'
                      k_class:
                        type: [string, "null"]
                        description: 'The class ID.'
                      s_id:
                        type: string
                        description: 'The unique hash string. <i> This hash string is a hash of the {@link \RsClassModify} object. It needs to work with the same {@link \RsClassModify} object on each step. </i>'
      tags:
        - Modify-Modify
  '/Wl/Reception/Roster/Design/ReceptionRosterDesign.json':
    get:
      summary: 'Returns configuration for the Attendance Kiosk. Returns kiosk display settings including custom image, logo, direction mode, background color, and business name for the specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image data for image which will be displayed in attendance web app page. <dl> <dt>int `i_height`</dt> <dd>Requested image height.</dd> <dt>int `i_height_src`</dt> <dd>Original image height.</dd> <dt>int `i_width`</dt> <dd>Requested image width.</dd> <dt>int `i_width_src`</dt> <dd>Original image width.</dd> <dt>int `id_type_src`</dt> <dd> Image type ID. One of {@link \Core\Drive\DriveTypeSid} constants. Will be present only for \Core\Drive\Drive images. </dd> <dt>bool `is-resize`</dt> <dd>Whether image was resized to satisfy specified <var>i_width</var> and <var>i_height</var>.</dd> <dt>string `url-thumbnail`</dt> <dd>Url to requested image.</dd> <dt>string `url-view`</dt> <dd>Url to original image.</dd> </dl>'
                      hide_profile_images:
                        type: boolean
                        description: 'Whether to hide client profile images. `true` if client profile images should be hidden, `false` otherwise.'
                      i_attendance_direct_delay:
                        type: number
                        format: int32
                        description: 'Number of seconds of inactivity before automatic redirect.'
                      i_book_open:
                        type: number
                        format: int32
                        description: 'Number of minutes for the client check-in window after session has started.'
                      i_book_quick_app:
                        type: number
                        format: int32
                        description: 'Units of time for the look ahead window for attendance app.'
                      i_confirm_delay:
                        type: number
                        format: int32
                        description: 'Delay in seconds on attendance web app confirmation screen before redirect to login screen.'
                      id_attendance_direct:
                        type: number
                        format: int32
                        description: 'ID of the action to take when automatically redirecting. One of {@link DirectSid} constants.'
                      id_book_quick_app:
                        type: number
                        format: int32
                        description: 'Unit of time for the look ahead window for attendance app.'
                      is_attend_free:
                        type: boolean
                        description: '`true` if clients are allowed to check-in unpaid, `false` otherwise.'
                      is_book_open:
                        type: boolean
                        description: 'Whether to allow sign-ins to classes in progress.'
                      is_book_optional:
                        type: boolean
                        description: '`true` if clients are allowed to check-in without booking prior, `false` otherwise.'
                      is_book_quick_app:
                        type: boolean
                        description: '`true` if clients are allowed to sign in before session is started., `false` otherwise.'
                      show_business_name:
                        type: boolean
                        description: '`true` - show business name on attendance web app; `false` - do not show.'
                      show_confirm_screen:
                        type: boolean
                        description: '`true` - show confirm screen on attendance web app; `false` - do not show.'
                      text_business_name:
                        type: string
                        description: 'Name of the business to display in the attendance web app.'
      tags:
        - Design-ReceptionRosterDesign
  '/Wl/Skin/Application/Resource/ApplicationResourceMaterial.json':
    post:
      summary: 'Saves text metadata for the given business application. Persists all text fields of the Client App Materials form (store listing copy, contact details, privacy policy URL, etc.) for the specified business. File assets such as icons and screenshots are submitted separately via {@link ApplicationResourceUploadApi}. Requires the App Build Automation edit privilege. the category does not exist, or the update type is invalid.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_version:
                  type: number
                  format: int32
                  description: 'Code version.'
                id_category:
                  type: number
                  format: int32
                  description: 'Application category in the store. One of {@link ApplicationCategorySid} constants. `0` if no category is selected.'
                id_version_change:
                  type: number
                  format: int32
                  description: 'Type of application update. One of {@link AppUpdateTypeEnum} constants.'
                k_business:
                  type: string
                  description: 'Business key.'
                s_firebase_ios:
                  type: string
                  description: 'Reversed client ID of the iOS application on Firebase.'
                s_splash_color_android:
                  type: string
                  description: 'Android splash screen background color in hex format.'
                s_version_name:
                  type: string
                  description: 'Version name for the App Store and Google Play builds.'
                text_annotation:
                  type: string
                  description: 'Short application description in the store (up to 80 characters).'
                text_country:
                  type: string
                  description: 'Available countries for application distribution (comma-separated list).'
                text_description:
                  type: string
                  description: 'Full application description in the store.'
                text_description_google:
                  type: string
                  description: 'Full application description specific to Google Play. If empty, `text_description` is used for Google Play as well.'
                text_domain:
                  type: string
                  description: 'Bundle ID / package name (e.g. `com.fitness.studio`).'
                text_email:
                  type: [string, "null"]
                  description: 'Support email address shown in the store listing. `null` to not change exists email.'
                text_information:
                  type: string
                  description: 'Additional information about the application build.'
                text_keyword:
                  type: string
                  description: 'App Store search keywords (comma-separated).'
                text_name:
                  type: string
                  description: 'Application name shown on the device home screen.'
                text_phone:
                  type: string
                  description: 'Support phone number shown in the store listing.'
                text_title:
                  type: string
                  description: 'Application title in the App Store listing.'
                text_website:
                  type: string
                  description: 'Support website URL shown in the store listing.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Resource-ApplicationResourceMaterial
  '/Wl/Skin/Application/Resource/ApplicationResourceUpload.json':
    post:
      summary: 'Uploads image and file assets for the given business application. Each image must be submitted as `a_image_upload[{key}]`, where `{key}` is the image ID. The `google-services.json` file must be submitted as `google_services_json`.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_image_upload:
                  type: object
                  description: 'Image assets keyed by image ID: Each value contains array with keys that are the sanitized asset file paths (e.g. `icon_ios_Icon_60_3x_png`). <dl> <dt>int[] `error`</dt> <dd>An error code representing the success or failure status.</dd> <dt>string[] `tmp_name`</dt> <dd>The temporary filename of the file in which the uploaded file was stored on the server.</dd> </dl>'
                f_google_services_json:
                  type: object
                  description: 'Firebase Android config file (`google-services.json`).'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Resource-ApplicationResourceUpload
  '/Wl/Skin/Application/Resource/ApplicationResource.json':
    get:
      summary: 'Gets application resources. Returns image resource groups, version information, and Google OAuth credentials for one or all White Label mobile applications. Called by the build pipeline to gather the data needed to generate application source bundles. Pass a business key to restrict results to a single app.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key. Empty to get data for all businesses.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_application:
                        type: array
                        description: 'The application data. Key is the business key. <i>Primary key in {@link \RsBusinessSql} table.</i> Value is application resource data for that business. Each element: <dl> <dt>array `a_resource`</dt> <dd> List of resource groups for image generation. <i>See {@link \Wl\Skin\Application\Resource\EditController::resourceStructure()}.</i> <dl> <dt>array[] `a_group`</dt> <dd> Groups of images, such as icons, loading screens or screenshots. One element of this array is an array that contains: <dl> <dt>array[] `a_image`</dt> <dd> A list of images in the group. One element contains parameters of an image: <dl> <dt>string[] `a_file`</dt> <dd>A list of fully qualified file names. This name is used to save files in final archive.</dd> <dt>bool `can_transparent`</dt> <dd> Specified if this image can have transparent pixels. <tt>true</tt> if it is allowed that there be transparent pixels. <tt>false</tt> if it is required that all pixels be opaque. </dd> <dt>int `i_height`</dt> <dd>Required image height.</dd> <dt>int `i_width`</dt> <dd>Required image width.</dd> <dt>bool `is_require`</dt> <dd>Whether this image is required.</dd> </dl> </dd> <dt>string `s_class`</dt> <dd>CSS class for this images block.</dd> <dt>string `text_comment`</dt> <dd>Comment for images block.</dd> <dt>string `text_subtitle`</dt> <dd>Subtitle for images block.</dd> <dt>string `text_title`</dt> <dd>Title for images block.</dd> </dl> </dd> <dt>string `text_group`</dt> <dd>Title of the group.</dd> </dl> </dd> <dt>int `i_version`</dt> <dd>Application version number.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql}.</i></dd> <dt>string `s_google_id`</dt> <dd>Google OAuth client ID for this application.</dd> <dt>string `s_google_reverse`</dt> <dd>Reverse Google OAuth client ID used for deep linking.</dd> <dt>string `s_splash_color_android`</dt> <dd>Android splash screen background color in hex format.</dd> <dt>string `s_version_name`</dt> <dd>Application version name string (for example, ''2.0.1'').</dd> <dt>string `text_annotation`</dt> <dd>Short app store description.</dd> <dt>string `text_description`</dt> <dd>Full app store description for iOS App Store.</dd> <dt>string `text_description_google`</dt> <dd>Full app store description for Google Play.</dd> <dt>string `text_domain`</dt> <dd>Application domain.</dd> <dt>string `text_keyword`</dt> <dd>App store search keywords.</dd> <dt>string `text_name`</dt> <dd>Application internal name.</dd> <dt>string `text_title`</dt> <dd>Application display title shown in app stores.</dd> </dl>'
      tags:
        - Resource-ApplicationResource
  '/Wl/Skin/Application/Connect/ApplicationConnect.json':
    post:
      summary: 'Creates or updates the integration credentials for the given business application. If a record already exists for `k_business`, it is updated in place. Otherwise, a new record is created and linked to the business. does not exist, belongs to a different datacenter, or lacks an active Mobile App subscription; the domain is already used by another application; or iOS key fields are incomplete.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_apple:
                  type: boolean
                  description: '`true` to enable Sign In with Apple for this application, `false` to disable.'
                is_facebook:
                  type: boolean
                  description: '`true` to enable Sign In with Facebook (iOS) for this application, `false` to disable.'
                is_facebook_android:
                  type: boolean
                  description: '`true` to enable Sign In with Facebook (Android) for this application, `false` to disable.'
                k_business:
                  type: string
                  description: 'Business key.'
                s_firebase_key:
                  type: string
                  description: 'Firebase service account private key (JSON content of the `.p8`-equivalent key file).'
                s_firebase_project:
                  type: string
                  description: 'Firebase project ID.'
                s_google_id:
                  type: string
                  description: 'Google OAuth client ID.'
                s_google_reverse:
                  type: string
                  description: 'Reversed Google OAuth client ID (iOS URL scheme).'
                s_google_secret:
                  type: string
                  description: 'Google OAuth client secret.'
                text_cert_ios_login:
                  type: string
                  description: 'Sign In with Apple key (`.p8` file content).'
                text_cert_ios_push:
                  type: string
                  description: 'APNs push notification auth key (`.p8` file content).'
                text_domain:
                  type: string
                  description: 'Bundle ID used as the application identifier.'
                text_key_login_id:
                  type: string
                  description: 'Sign In with Apple key ID.'
                text_key_push_id:
                  type: string
                  description: 'APNs push notification key ID.'
                text_name:
                  type: string
                  description: 'Application display name.'
                text_team_id:
                  type: string
                  description: 'Apple Developer Team ID.'
                url_google_redirect:
                  type: string
                  description: 'Google OAuth redirect URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_spa_application:
                        type: string
                        description: 'Key of the created or updated application. Set in the response after a successful `post()` call.'
      tags:
        - Connect-ApplicationConnect
  '/Wl/Insurance/Enrollment/Field/EnrollmentFieldList.json':
    get:
      summary: 'Returns the list of required partner fields for the specified wellness program. Returns enrollment fields defined by the insurance partner along with reimbursement account fields if the program requires reimbursement. Each field includes validation rules, display labels, and configuration options for rendering the enrollment form.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'The partner field list: <dl> <dt>array[] `a_account`</dt> <dd> The list of reimbursement account fields. If the program doesn''t require reimbursement, this list will be empty. Each element has the same structure as elements of `a_field`: <dl> <dt>string[] `a_config`</dt> <dd> Additional configuration for the field. For the account type field, keys are account type titles and values are {@link \RsPayBankAchTypeSid} constants. Empty array for other fields. </dd> <dt>int|null `i_length`</dt> <dd>Maximum length of the field value. `null` if no limit.</dd> <dt>bool `is_disable`</dt> <dd>`true` if the field is disabled and should not be shown.</dd> <dt>bool `is_required`</dt> <dd>`true` if the field is required.</dd> <dt>string `k_field`</dt> <dd>The field identifier key.</dd> <dt>string `s_regular`</dt> <dd>Regular expression for client-side validation. Empty string if no validation pattern.</dd> <dt>string `text_display_name`</dt> <dd>The label text for the field displayed to the user.</dd> <dt>string `text_error_message`</dt> <dd>The error message to display if the field value is invalid.</dd> <dt>string `text_field_name`</dt> <dd>The internal name of the field.</dd> </dl> </dd> <dt>array[] `a_field`</dt> <dd> The list of partner enrollment fields. Each element: <dl> <dt>string[] `a_config`</dt> <dd> Additional configuration for the field: <dl> <dt>string[] `a_option`</dt> <dd> Options for dropdown fields. Keys are display titles, values are option values. Empty array if the field is not a dropdown. </dd> <dt>string `text_type`</dt> <dd>The type of text input. Present only for text-type fields.</dd> </dl> </dd> <dt>int|null `i_length`</dt> <dd>Maximum length of the field value. `null` if no limit.</dd> <dt>bool `is_disable`</dt> <dd>`true` if the field is disabled and should not be shown.</dd> <dt>bool `is_required`</dt> <dd>`true` if the field is required.</dd> <dt>string `k_field`</dt> <dd>The field key. <i>Primary key in the {@link \Wl\Integration\Curves\PartnerFieldSql} table.</i></dd> <dt>string `s_regular`</dt> <dd>Regular expression for client-side validation. Empty string if no validation pattern.</dd> <dt>string `text_display_name`</dt> <dd>The label text for the field displayed to the user.</dd> <dt>string `text_error_message`</dt> <dd>The error message to display if the field value is invalid.</dd> <dt>string `text_field_name`</dt> <dd>The internal name of the field.</dd> </dl> </dd> </dl>'
      tags:
        - Field-EnrollmentFieldList
    post:
      summary: 'Validates the list of fields filled in by the user for enrollment. Accepts the values submitted by the user for the wellness program enrollment form and validates them against the partner''s field rules before passing them to the payment API.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_account:
                  type: array
                  description: 'Reimbursement account information. Keys - field name. Values - value entered by user. <i>Validation is performed by {@link EnrollmentFieldList::setAccountForm()} method.</i>'
                a_field:
                  type: array
                  description: 'List of fields that the user has filled in for enrollment. Keys refer specifically to field keys. <i>Primary keys in {@link \Wl\Integration\Curves\PartnerFieldSql} table.</i> Values refer specifically to values entered by the user. <i>Validation is performed by {@link EnrollmentFieldList::setFieldForm()} method.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Field-EnrollmentFieldList
  '/Thoth/ReportCore/QueryEngine/Report/Customization/ReportQueryCustomizationForm.json':
    get:
      summary: 'Loads the customization form scoped to {@link ReportQueryCustomizationFormApi::$k_report_query} when set, falling back to the generic shared row when no per-query record exists.'
      parameters:
        - name: cid_page
          in: query
          description: 'Report page CID.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'Report CID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business primary key.'
          schema:
            type: string
        - name: k_report_query
          in: query
          description: 'SQL query primary key. Primary key in {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql}. Scopes the customization record to a specific SQL query. `null` loads the generic shared row as a backward-compatible fallback.'
          schema:
            type: [string, "null"]
        - name: k_report_save
          in: query
          description: 'Primary key of a saved report. `null` means that report is not saved.'
          schema:
            type: [string, "null"]
        - name: s_report
          in: query
          description: 'Report CID list to that page customization form must be converted. String separated with `,`. May be specified only if {@link CustomizationFormApi::$cid_page} in not empty.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Current user''s primary key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_customization_form:
                        type: array
                        description: 'Customization form data keyed by report or page CID. Each value has the following structure: <dl> <dt>array `a_element`</dt> <dd> Form elements keyed by element name. Each element has the following structure: <dl> <dt>array `a_field`</dt> <dd> Element fields keyed by field alias. Each field has the following structure: <dl> <dt>string `text_title`</dt> <dd>Localized field title.</dd> <dt>string `x_value`</dt> <dd>Current field value.</dd> </dl> </dd> <dt>int[] `a_report`</dt> <dd>Report CID list the element applies to.</dd> <dt>string `html_title`</dt> <dd>Localized element title.</dd> <dt>string `html_title_tooltip`</dt> <dd>Localized element tooltip.</dd> </dl> </dd> <dt>string `s_form`</dt> <dd>Form class identifier.</dd> </dl>'
      tags:
        - Customization-ReportQueryCustomizationForm
    post:
      summary: 'Saves the customization form scoped to {@link ReportQueryCustomizationFormApi::$k_report_query} when set.'
      parameters:
        - name: cid_page
          in: query
          description: 'Report page CID.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'Report CID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business primary key.'
          schema:
            type: string
        - name: k_report_query
          in: query
          description: 'SQL query primary key. Primary key in {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql}. Scopes the customization record to a specific SQL query. `null` loads the generic shared row as a backward-compatible fallback.'
          schema:
            type: [string, "null"]
        - name: k_report_save
          in: query
          description: 'Primary key of a saved report. `null` means that report is not saved.'
          schema:
            type: [string, "null"]
        - name: uid_actor
          in: query
          description: 'Current user''s primary key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_customization_form:
                  type: array
                  description: 'Customization form data keyed by report or page CID. Each value has the following structure: <dl> <dt>array `a_element`</dt> <dd> Form elements keyed by element name. Each element has the following structure: <dl> <dt>array `a_field`</dt> <dd> Element fields keyed by field alias. Each field has the following structure: <dl> <dt>string `text_title`</dt> <dd>Localized field title.</dd> <dt>string `x_value`</dt> <dd>Current field value.</dd> </dl> </dd> <dt>int[] `a_report`</dt> <dd>Report CID list the element applies to.</dd> <dt>string `html_title`</dt> <dd>Localized element title.</dd> <dt>string `html_title_tooltip`</dt> <dd>Localized element tooltip.</dd> </dl> </dd> <dt>string `s_form`</dt> <dd>Form class identifier.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Customization-ReportQueryCustomizationForm
  '/Thoth/WlPay/Bank/Card/Add/Add.json':
    delete:
      summary: 'Deletes saved card. Removes the payment card identified by `$k_pay_bank` from the specified business, permanently deleting the stored card record.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: k_pay_bank
          in: query
          description: 'Pay bank key to delete.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Add-Add
    get:
      summary: 'Gets code of bank card widget. Returns an HTML snippet containing the card entry form rendered by the server-side widget for the given payment owner. This method is deprecated; prefer building the card widget on the client side.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'The payment owner ID. This is different from the user ID. It can be found with the {@link \Thoth\WlPay\Owner\OwnerApi}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_widget:
                        type: string
                        description: 'The HTML form containing the fields required to add a card.'
      tags:
        - Add-Add
    post:
      summary: 'Saves new bank card. Validates the payment owner and business, then saves the new card details provided in `$a_card_detail` and optionally sets the card as the default recurring payment source.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'The payment owner ID. This is different from the user ID. It can be found with the {@link \Thoth\WlPay\Owner\OwnerApi}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card_detail:
                  type: array
                  description: 'An array containing payment card information with the following fields: <dl> <dt>array `a_pay_address`</dt> <dd> Address information: <dl> <dt>bool `is_new`</dt> <dd>`true` - add new address. `false` - use existing address.</dd> <dt>string `k_pay_address`</dt> <dd>The chosen payment address ID. THis will be set even if the user decided to add a new address.</dd> <dt>string `k_geo_country`</dt> <dd>The country key.</dd> <dt>string `k_geo_region`</dt> <dd>The region key.</dd> <dt>string `s_city`</dt> <dd>The city name.</dd> <dt>string `s_name`</dt> <dd>The user''s name.</dd> <dt>string `s_street1`</dt> <dd>The first address line.</dd> <dt>string `s_street2`</dt> <dd>The second address line.</dd> <dt>string `s_phone`</dt> <dd>The phone number.</dd> <dt>string `s_postal`</dt> <dd>The postal code.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The payment card security code, also known as the CVC or CVV.</dd> <dt>int `i_month`</dt> <dd>The number of the month when the payment card expires. 1=January and 12=December.</dd> <dt>int `i_year`</dt> <dd>The last two digits of the year when the payment card expires.</dd> <dt>int `id_pay_actor`</dt> <dd>Pay actor id. One of {@link \RsPayActorSid} constants.</dd> <dt>string `k_pay_bank`</dt> <dd> Key of existing payment source in case of editing. <i>Primary key from {@link \RsPayBankSql} table.</i> Empty if new pay source is being added. </dd> <dt>string `s_name`</dt> <dd>The name as it appears on the payment card.</dd> <dt>string `s_number`</dt> <dd>The payment card number with no spaces or dashes.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Add-Add
  '/Thoth/WlPay/Bank/Card/Widget/WidgetSelect.json':
    get:
      summary: 'Gets a list of saved bank cards. Validates the payment method, currency, business, location, and pay owner type, then loads the configured merchant and returns the list of saved cards available to the given owner. For business owners, a system-wide merchant must be configured; for users, a business-specific merchant is required.'
      parameters:
        - name: id_pay_method
          in: query
          description: 'Payment method.'
          schema:
            type: number
            format: int32
        - name: id_pay_mode
          in: query
          description: 'Payment mode.'
          schema:
            type: number
            format: int32
        - name: id_pay_owner
          in: query
          description: 'Payment owner kind. Must be {@link \RsPayOwnerSid::BUSINESS} or {@link \RsPayOwnerSid::USER}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. `null` if use system business.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'Currency key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Payment owner. Must be primary key of a business or a user.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. `null` to use system-wide merchant.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_card:
                        type: array
                        description: 'List of saved bank cards. <i>See {@link \RsPayBankCardSelectWidget::additional_data()} for details.</i> <dl> <dt>\Thoth\WlPay\Bank\Card\CardTypeEnum|null `eid_card_type`</dt> <dd>Payment card type enum case. `null` if card type is not set.</dd> <dt>int `i_month`</dt> <dd>Card expiration month.</dd> <dt>int `i_year`</dt> <dd>Card expiration year.</dd> <dt>int `id_card_system`</dt> <dd>Card system ID. One of {@link \Wl\Card\CardSystemSid} constants.</dd> <dt>int `id_card_type`</dt> <dd>Payment card type ID. One of {@link \Thoth\WlPay\Bank\Card\CardTypeEnum} constants as int.</dd> <dt>int `id_pay_processor`</dt> <dd>Payment processor ID. One of {@link \Thoth\PayProcessor\PayProcessorSid} constants.</dd> <dt>bool `is_default`</dt> <dd>`true` if this card is the default payment method; `false` otherwise.</dd> <dt>string `k_pay_address`</dt> <dd>Billing address key.</dd> <dt>string `k_pay_bank`</dt> <dd>Payment card key.</dd> <dt>string `k_pay_recurrent`</dt> <dd>Recurrent payment token key.</dd> <dt>string `k_region`</dt> <dd>Billing region key.</dd> <dt>string `s_name`</dt> <dd>Card nickname. Auto-generated from card system and masked number if not explicitly set.</dd> <dt>string `s_number`</dt> <dd>Partial card number.</dd> <dt>string `sid_card_system`</dt> <dd>Card system SID. One of {@link \Wl\Card\CardSystemSid} constants as string.</dd> <dt>string `text_city`</dt> <dd>Billing city.</dd> <dt>string `text_country`</dt> <dd>Country name resolved from the region''s parent geo entity. Empty if region has no parent.</dd> <dt>string `text_holder`</dt> <dd>Cardholder name.</dd> <dt>string `text_postal`</dt> <dd>Billing postal code.</dd> <dt>string `text_public_token`</dt> <dd>Public representation of the recurrent payment token, safe for browser output.</dd> <dt>string `text_region`</dt> <dd>Region name resolved from the region key. Empty if no region is set.</dd> <dt>string `text_street_1`</dt> <dd>Billing street address line 1.</dd> <dt>string `text_street_2`</dt> <dd>Billing street address line 2.</dd> </dl>'
      tags:
        - Widget-WidgetSelect
  '/Thoth/WlPay/Bank/Ach/Add/Add.json':
    delete:
      summary: 'Deletes saved ACH. Removes the ACH bank account identified by `k_pay_bank` from the pay owner''s saved payment methods for the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_pay_bank
          in: query
          description: 'Pay bank key to delete.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Add-Add
    get:
      summary: 'Gets widget for ACH account add. Validates the locale, business, and pay owner, then determines whether to use Direct Entry (Australia and New Zealand) or ACH as the payment method. Returns the rendered widget HTML and the processor ID for the resolved merchant.'
      parameters:
        - name: id_locale
          in: query
          description: 'Locale ID. `null` - if it is not set.'
          schema:
            type: [number, "null"]
        - name: is_new
          in: query
          description: 'Determines if the set of configs of the new payment form design is used. `true` - if it is the new payment form design. `false` - if it is the old payment form design.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. If empty, user''s home location will be used.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'Pay owner key. <i>Copy of result of {@link PayOwner::ownerMoney()}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_widget:
                        type: string
                        description: 'The HTML form containing the fields required to add a card. <i>Result of {@link \RsPayWidgetInterface::widget()} method.</i>'
                      id_pay_processor:
                        type: number
                        format: int32
                        description: 'Transaction processor ID.'
      tags:
        - Add-Add
    post:
      summary: 'Saves new ACH pay method. Processes the submitted ACH widget data, saves the bank account under the given pay owner and business, and optionally marks it as the default payment method. Returns the saved account details including billing address information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. If empty, user''s home location will be used.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'Pay owner key. <i>Copy of result of {@link PayOwner::ownerMoney()}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card_detail:
                  type: array
                  description: 'Array with ACH details from ACH widget. <dl> <dt>array `a_pay_address`</dt> <dd> Payment address: <dl> <dt>bool `is_new`</dt> <dd><tt>true</tt> - add new address; <tt>false</tt> - use existing address.</dd> <dt>string `k_geo_country`</dt> <dd>Country ID.</dd> <dt>string `k_geo_region`</dt> <dd>Region ID.</dd> <dt>string `k_pay_address`</dt> <dd>Chosen payment address ID. It will be set even if user decided to add new address.</dd> <dt>string `s_city`</dt> <dd>City name.</dd> <dt>string `s_name`</dt> <dd>User name.</dd> <dt>string `s_phone`</dt> <dd>Phone number.</dd> <dt>string `s_postal`</dt> <dd>Postal code.</dd> <dt>string `s_street1`</dt> <dd>First address line.</dd> <dt>string `s_street2`</dt> <dd>Second address line.</dd> </dl> </dd> <dt>int `id_pay_actor`</dt> <dd>Pay actor id. One of {@link \RsPayActorSid} constants.</dd> <dt>int `id_pay_bank_ach_holder`</dt> <dd>Account holder type. One of {@link \RsPayBankAchHolderSid} constants.</dd> <dt>int `id_pay_bank_ach_sec`</dt> <dd>SEC code. One of {@link \RsPayBankAchSecSid} constants.</dd> <dt>int `id_pay_bank_ach_type`</dt> <dd>Account type. One of {@link \RsPayBankAchTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd><tt>true</tt> - if a payment method is set as default, <tt>false</tt> - otherwise.</dd> <dt>string `k_pay_bank`</dt> <dd> Key of existing payment source in case of editing. <i>Primary key from {@link \RsPayBankSql} table.</i> Empty if new pay source is being added. </dd> <dt>string `s_aban`</dt> <dd>ABA number.</dd> <dt>string `s_account`</dt> <dd>Account number.</dd> <dt>string `s_name`</dt> <dd>Bank account nickname.</dd> <dt>string `s_number`</dt> <dd>Part of bank account number.</dd> <dt>string `s_owner`</dt> <dd>Name of bank account owner.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_bank:
                        type: array
                        description: 'ACH account information: <dl> <dt>bool `can_remove`</dt> <dd> Whether current user can remove payment method. `true` if user can remove payment methods. `false` if user cannot remove payment methods. </dd> <dt>int `id_pay_bank`</dt> <dd>Type of recurrent payment token. One of {@link \RsPayBankSid} constants.</dd> <dt>int `id_pay_bank_ach_holder`</dt> <dd>Type of account holder. One of {@link \RsPayBankAchHolderSid} constants.</dd> <dt>int `id_pay_bank_ach_type`</dt> <dd>Type of account. One of {@link \RsPayBankAchTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd>`true` - this account is default payment method; `false` - otherwise.</dd> <dt>string `k_pay_address`</dt> <dd>Billing address. <i>Primary key in {@link \RsPayAddressSql}.</i></dd> <dt>string `k_pay_bank`</dt> <dd>ID of bank account. <i>Primary key in {@link \RsPayBankSql}.</i></dd> <dt>string `k_region`</dt> <dd>Region ID. <i>Primary key in {@link \AGeoSql} table.</i></dd> <dt>string `text_city`</dt> <dd>Name of city.</dd> <dt>string `text_country`</dt> <dd>Name of country.</dd> <dt>string `text_name`</dt> <dd>Account nickname.</dd> <dt>string `text_name_account`</dt> <dd>Account name.</dd> <dt>string `text_name_holder`</dt> <dd>Account holder name.</dd> <dt>string `text_number`</dt> <dd>ACH account number.</dd> <dt>string `text_phone`</dt> <dd>Phone number.</dd> <dt>string `text_postal`</dt> <dd>Postal code.</dd> <dt>string `text_region`</dt> <dd>Name of region.</dd> <dt>string `text_street1`</dt> <dd>Street address line 1.</dd> <dt>string `text_street2`</dt> <dd>Street address line 2.</dd> </dl>'
      tags:
        - Add-Add
  '/Core/Request/Api/Application/Credential/Credential.json':
    delete:
      summary: 'Deletes the specified credential for the application. Accepts a credential CID identifying the credential type (such as Google or Facebook), resolves the corresponding credential for the current application, and removes it from the database.'
      parameters:
        - name: cid_credential
          in: query
          description: 'The CID of the credential. <i> Accepted values: <ul> <li> {@link GoogleCredential::CID} - The Google credential. Use the {@link GoogleCredentialData} object to send the credential. </li> <li> {@link FacebookCredential::CID} - The Facebook credential. Use the {@link GoogleCredentialData} object to send the credential. </li> </ul> </i>'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Credential-Credential
    get:
      summary: 'Returns the credential data for the application. Accepts a credential CID identifying the credential type, loads the credential stored for the current application, and returns it as a JSON-encoded object containing only public fields.'
      parameters:
        - name: cid_credential
          in: query
          description: 'The CID of the credential. <i> Accepted values: <ul> <li> {@link GoogleCredential::CID} - The Google credential. Use the {@link GoogleCredentialData} object to send the credential. </li> <li> {@link FacebookCredential::CID} - The Facebook credential. Use the {@link GoogleCredentialData} object to send the credential. </li> </ul> </i>'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_credential:
                        type: string
                        description: 'An array with the credential.'
      tags:
        - Credential-Credential
    put:
      summary: 'Saves the credential for the application. Accepts a credential CID and a JSON-encoded object with credential fields, validates the data, and stores or replaces the credential for the current application.'
      parameters:
        - name: cid_credential
          in: query
          description: 'The CID of the credential. <i> Accepted values: <ul> <li> {@link GoogleCredential::CID} - The Google credential. Use the {@link GoogleCredentialData} object to send the credential. </li> <li> {@link FacebookCredential::CID} - The Facebook credential. Use the {@link GoogleCredentialData} object to send the credential. </li> </ul> </i>'
          schema:
            type: number
            format: int32
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_credential:
                  type: string
                  description: 'An array with the credential.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Credential-Credential
  '/Wl/Profile/Attendance/Schedule/Frontend/LifetimeTotals.json':
    get:
      summary: 'Retrieves lifetime visit totals. Returns aggregated lifetime attendance statistics for the specified user in the given business. Used to display summary visit counts and related metrics on the client profile schedule page.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_total:
                        type: array
                        description: 'Report totals. A set of key-value pairs. Each element has followed structure: <dl> <dt>string `text_title`</dt> <dd>Total title.</dd> <dt>string `s_value`</dt> <dd>Total value.</dd> </dl>'
      tags:
        - Frontend-LifetimeTotals
  '/Wl/Login/Promotion/GuestPass/Invite/Invite.json':
    delete:
      summary: 'Revokes a guest pass invitation. Marks the invitation as rejected and sends an expiry notification to the guest.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret of the invitation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Invite-Invite
    put:
      summary: 'Sends a reminder notification for a guest pass invitation. Resends the invitation notification to the guest using the reminder email or SMS template.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret of the invitation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Invite-Invite
  '/Wl/Login/Promotion/GuestPass/Invite/InviteList.json':
    get:
      summary: 'Returns a list of guest pass invitations for the given membership or user. Supports filtering by host membership, guest user, host user, date range, and whether expired or revoked invitations are included.'
      parameters:
        - name: dl_end
          in: query
          description: 'End of the guest pass activity period. Used to filter invitations whose activity moment is on or before this local date. `null` to not filter by period end.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Start of the guest pass activity period. Used to filter invitations whose activity moment is on or after this local date. `null` to not filter by period start.'
          schema:
            type: [string, "null"]
        - name: is_include_expire
          in: query
          description: 'Whether to include invitations that have already expired or were revoked.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of the business within which guest pass invitations are managed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of the login promotion which provides the guest pass invitation. > * Used for `GET` to get list of initial invitations for the specific login promotion. > * Used for `POST` to send invitation to the specific user for the specific login promotion.'
          schema:
            type: [string, "null"]
        - name: uid_guest
          in: query
          description: 'Key of the invited user. > * Used for `GET` to get list of incoming invitations for the specific user. > * Used for `POST` to send invitation to the specific user.'
          schema:
            type: [string, "null"]
        - name: uid_host
          in: query
          description: 'Key of the host user who sent the invitation. > Used only for `GET` request to get list of outgoing invitations from the specific user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of guest pass invitations suitable for the specific request parameters. <dl> <dt>array `a_guest`</dt> <dd> Guest user identity data. <dl> <dt>string `text_mail`</dt> <dd>Guest email address.</dd> <dt>string `text_name_first`</dt> <dd>Guest first name.</dd> <dt>string `text_name_last`</dt> <dd>Guest last name.</dd> <dt>string|null `text_phone`</dt> <dd>Guest phone number, or `null` if not available.</dd> <dt>string|null `uid`</dt> <dd> Guest user key. <i>Primary key from {@link \PassportLoginSql} table.</i> `null` if the guest did not have an account when the invitation was sent. </dd> </dl> </dd> <dt>string|null `dtl_accept`</dt> <dd> Acceptance date and time in business timezone in MySQL datetime format. `null` if the invitation has not been accepted. </dd> <dt>string|null `dtl_expire`</dt> <dd> Expiry date and time in business timezone in MySQL datetime format. `null` if the invitation has no expiry date. </dd> <dt>string `dtl_invite`</dt> <dd>Date and time the invitation was sent, in business timezone in MySQL datetime format.</dd> <dt>string|null `dtl_redeem`</dt> <dd> Redemption date and time in business timezone in MySQL datetime format. `null` if the invitation has not been redeemed. </dd> <dt>string|null `dtl_reject`</dt> <dd> Rejection date and time in business timezone in MySQL datetime format. `null` if the invitation has not been rejected. </dd> <dt>string|null `dtl_visit`</dt> <dd>Visit date and time in MySQL datetime format. `null` if no visit is associated.</dd> <dt>string|null `dtu_accept`</dt> <dd>Acceptance date and time in UTC in MySQL datetime format. `null` if the invitation has not been accepted.</dd> <dt>string|null `dtu_expire`</dt> <dd>Expiry date and time in UTC in MySQL datetime format. `null` if the invitation has  no expiry date.</dd> <dt>string `dtu_invite`</dt> <dd>Date and time the invitation was sent, in UTC in MySQL datetime format.</dd> <dt>string|null `dtu_redeem`</dt> <dd> Redemption date and time in UTC in MySQL datetime format. `null` if the invitation  has not been redeemed. </dd> <dt>string|null `dtu_reject`</dt> <dd>Rejection date and time in UTC in MySQL datetime format. `null` if the invitation has not been rejected.</dd> <dt>int `id_status`</dt> <dd>Invitation lifecycle status. One of {@link \Wl\Login\Promotion\GuestPass\Invite\InviteStatusEnum} values.</dd> <dt>bool|null `is_checkin`</dt> <dd> `true` if the guest may only enter when the inviting member is checked in. `null` if no guest pass settings are configured for the promotion. </dd> <dt>bool `is_visit_business_other`</dt> <dd> `true` if the visit was redeemed at a sibling franchisee business rather than the business that issued the guest pass (regional redemption). `false` if no visit is associated or it was redeemed at the issuing business. </dd> <dt>bool `is_visit_future`</dt> <dd>`true` if associated visit in future, `false` otherwise.</dd> <dt>string|null `k_login_promotion_guest`</dt> <dd> Login promotion key granted to the guest upon acceptance. <i>Primary key from {@link \RsLoginPromotionSql} table.</i> `null` if the invitation has not been accepted. </dd> <dt>string `k_login_promotion_host`</dt> <dd>Login promotion key of the host membership. <i>Primary key from {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Guest promotion key. <i>Primary key from {@link \RsPromotionSql} table.</i></dd> <dt>string|null `k_visit`</dt> <dd> Visit key redeemed with this guest pass. <i>Primary key from {@link \RsVisitSql} table.</i> `null` if no visit is associated. </dd> <dt>string `s_secret`</dt> <dd>Secret token used to accept or reject the invitation via a direct link.</dd> <dt>string|null `text_host_name`</dt> <dd>Full name of the host user who sent the invitation. `null` if the host could not be resolved.</dd> <dt>string `text_promotion_guest`</dt> <dd>Title of the guest promotion.</dd> <dt>string `text_promotion_host`</dt> <dd>Title of the host promotion.</dd> <dt>string|null `text_location`</dt> <dd> Title of the location the visit was redeemed at - may be a sibling franchisee''s location when the guest pass has regional access. `null` if no visit is associated. </dd> <dt>string|null `text_service_title`</dt> <dd>Title of the service booked with the guest pass. `null` if no visit is associated.</dd> <dt>string `text_timezone`</dt> <dd>Name of the business timezone used for all `dtl_*` fields (e.g. "America/New_York").</dd> <dt>string|null `uid_host`</dt> <dd> User key of the host who sent the invitation. <i>Primary key from {@link \PassportLoginSql} table.</i> `null` if the host could not be resolved. </dd> <dt>string `url_service`</dt> <dd> URL to the attendance page for the associated class period. Empty string if no visit is associated or the visit is not a class. </dd> </dl>'
      tags:
        - Invite-InviteList
    post:
      summary: 'Sends a guest pass invitation to a user. Creates a new invitation for the specified guest pass and sends a notification. The guest can be identified by user key or by contact details when no account exists.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business within which guest pass invitations are managed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of the login promotion which provides the guest pass invitation. > * Used for `GET` to get list of initial invitations for the specific login promotion. > * Used for `POST` to send invitation to the specific user for the specific login promotion.'
          schema:
            type: [string, "null"]
        - name: uid_guest
          in: query
          description: 'Key of the invited user. > * Used for `GET` to get list of incoming invitations for the specific user. > * Used for `POST` to send invitation to the specific user.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_guest:
                  type: array
                  description: 'Guest information. > Used only for `POST` request to send invitation to the guest without account. Required fields: <dl> <dt>string `text_mail`</dt> <dd>Email address to which the invitation will be sent.</dd> <dt>string `text_phone`</dt> <dd>Phone number to which the invitation SMS will be sent.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Invite-InviteList
  '/Wl/Login/Promotion/GuestPass/Claim/Claim.json':
    post:
      summary: 'Claims the guest pass invitation for the current user and accepts it. Validates the invitation secret, verifies the current user is the intended recipient, and marks the invitation as accepted. current user is not allowed to claim it.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business within which the invitation was issued.'
                s_secret:
                  type: string
                  description: 'Secret token identifying the guest pass invitation to claim.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claim-Claim
  '/Wl/Login/Promotion/GuestPass/Apply/Apply.json':
    post:
      summary: 'Applies the guest pass of the specified login promotion to the attendee''s visit. If the visit is already paid (with a regular session pass or an existing guest pass), the previous payment is unwound before the new guest pass is applied.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_login_promotion:
                  type: string
                  description: 'Login promotion key of the host member''s class membership whose guest pass balance will be used.'
                k_visit:
                  type: string
                  description: 'Visit key of the attendee to whom the guest pass is applied.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Apply-Apply
  '/Wl/Appointment/Book/Asset/Service/Service.json':
    get:
      summary: 'Retrieves assets required for a service booking at the given date and time, including availability and busy status. Returns the asset categories and individual assets linked to the service at the given location. Each asset includes its availability flag for the requested time slot. The response also includes {@link ServiceApi::$a_resource_busy} with currently reserved asset slots and a flag indicating whether the current user is allowed to book unavailable assets.'
      parameters:
        - name: dt_start
          in: query
          description: 'The appointment booking date selected by the user.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'Mode type, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'If `true`, back-end mode is selected. In this mode, the list of asset categories must be returned even if asset selection is disabled for clients. If `false` for a case of front-end mode. In this mode, the list of asset categories won''t be returned if asset selection is disabled for clients.'
          schema:
            type: boolean
        - name: is_grid_any
          in: query
          description: 'Whether to show both grid layouts and custom layouts. `true` if custom layout should be included in the result. `false` if only grid layout can be used.'
          schema:
            type: boolean
        - name: is_show_unavailable_assets
          in: query
          description: 'Whether unavailable assets should be included.'
          schema:
            type: boolean
        - name: k_appointment_ignore
          in: query
          description: 'The appointment booking key to ignore when {@link ServiceApi::$a_resource_busy} is derived. `null` if no appointment booking must be ignored.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'The selected location key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The selected service''s key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone of date and time of service start. Empty if client can''t change in which timezone dates should be shown.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_busy:
                        type: object
                        description: 'A list of reserved assets. 1st level keys refer to asset keys. 2nd level keys refer to asset numbers. Values refer to keys of appointment bookings that reserve the asset(s). For example, if you want to check if the 10th asset with the key of ''15'' is reserved, you can check if `a_resource_busy[''15''][''10'']` is free. If you''re rebooking an appointment, check the value of `a_resource_busy[''15''][''10'']`. If it''s equal to the key of your current appointment booking, you can assume the asset is available.'
                      a_resource_type:
                        type: array
                        description: 'A list of assets required for the service booking. Keys refer the asset category key(s). Values refer to sub-arrays with next key(s). <dl> <dt>array[] `a_resource`</dt> <dd> A list of resources. Every element has the following keys: <dl> <dt>array `a_image`</dt> <dd> The asset''s image data. <i>See {@link \RsResourceImage::data()} for details.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See more {@link \Core\Drive\DriveImage::image()}.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Is set only if `sid_image` equals to `shape`.</dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `sid_image`</dt> <dd>Image kind. String representation of one of {@link \Wl\Resource\Image\ImageSid} constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. String representation of one of {@link \Wl\Resource\Image\ImageIconSid} constants. Is set only if `sid_image` equals to `image`. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. String representation of one of {@link \Wl\Resource\Image\ImageShapeSid} constants. Is set only if `sid_image` equals to `shape`. </dd> <dt>string `url`</dt> <dd>Path to image. <i>See {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> </dl> </dd> <dt>int `i_quantity`</dt> <dd>Asset quantity.</dd> <dt>bool `is_available`</dt> <dd>Whether this asset has at least one free unit.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>The asset title.</dd> </dl> </dd> <dt>string|null `k_resource_layout`</dt> <dd> The asset''s layout key. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i> This will be `null` if the asset category has no layout. </dd> <dt>string `s_resource_type`</dt> <dd>The title of asset category.</dd> </dl>'
                      can_book_unavailable_assets:
                        type: boolean
                        description: 'Can the staff members book reserved assets.'
      tags:
        - Service-Service
  '/Wl/Business/Account/Subscription/Achieve/AchieveSubscription.json':
    get:
      summary: 'Returns the Achieve subscription plan flags (free and white-label) for the given business. Used to determine which features and branding options are available in the Achieve mobile app for a business. For franchisees, the franchisor''s subscription is used. Call this before rendering any Achieve-specific UI that depends on the plan tier.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to check Achieve app subscription plan.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_free:
                        type: boolean
                        description: 'If Achieve app has free subscription plan is `true`, otherwise (white label, professional) - `false`.'
                      is_white_label:
                        type: boolean
                        description: 'If Achieve app has white label subscription plan is `true`, otherwise (free, professional) - `false`.'
      tags:
        - Achieve-AchieveSubscription
  '/Wl/Mail/Pattern/AutomatedMarketing/CustomTemplate/Pattern.json':
    get:
      summary: 'Gets custom pattern data. Loads the saved notification template for the given business and mail type together with the default template, the reply-to business contact data and the list of variables available for substitution. When a list is requested, also loads all custom templates for the mail form.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of the notification. One of {@link \RsMailSid} constants. `0` for empty template.'
          schema:
            type: number
            format: int32
        - name: is_custom_list
          in: query
          description: 'Whether to get a list of custom template data. `true` - to get a list, `false` - otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern
          in: query
          description: 'Mail pattern key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Key of the service being booked. Used to prefer a service-specific custom notification template, when one is configured, over the business-level template. `null` when no single service is in context (e.g. several services are selected at once), in which case the business-level template is returned.'
          schema:
            type: [string, "null"]
        - name: sid_mail_form
          in: query
          description: 'SID of the mail form. String representation of one from {@link \RsMailFormSid} class constants.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_data:
                        type: array
                        description: 'Business data. <dl> <dt>string|null `text_business_reply_mail`</dt> <dd>Business email address to `Reply TO`. `null` for system business.</dd> <dt>string|null `text_business_reply_name`</dt> <dd>Business name to `Reply TO`. `null` for system business.</dd> </dl>'
                      a_help_data:
                        type: [array, "null"]
                        description: 'Help data for variables. `null` for pushes. <dl> <dt>array[] `a_help_global`</dt> <dd> An array with help data for global variables: <dl> <dt>string `text_description`</dt> <dd>A variable description.</dd> <dt>string `text_field`</dt> <dd>Field name.</dd> <dt>string `text_parameter`</dt> <dd>A variable name.</dd> </dl> </dd> <dt>array[] `a_help`</dt> <dd> An array with help data for template-specific variables: <dl> <dt>string `text_description`</dt> <dd>A variable description.</dd> <dt>string `text_field`</dt> <dd>Field name.</dd> <dt>string `text_parameter`</dt> <dd>A variable name.</dd> </dl> </dd> </dl>'
                      a_pattern_load:
                        type: array
                        description: 'Mail pattern data to load. <dl> <dt>bool `is_mail`</dt> <dd>Is email enabled.</dd> <dt>bool `is_push`</dt> <dd>Is push notification enabled.</dd> <dt>bool `is_sms`</dt> <dd>Is SMS enabled.</dd> <dt>int `id_mail`</dt> <dd>ID of the notification. One of {@link \RsMailSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. <i>Primary key in {@link \RsMailPatternSql} table.</i></dd> <dt>string `text_date_edit`</dt> <dd>Data of the last template save in human-readable format.</dd> <dt>string `text_html`</dt> <dd>HTML content of the email.</dd> <dt>string `text_json`</dt> <dd>JSON content of the email for Unlayer editor.</dd> <dt>string `text_preview`</dt> <dd>>Preview text of the email.</dd> <dt>string `text_push`</dt> <dd>Content of the push notification.</dd> <dt>string `text_sms`</dt> <dd>Content of the SMS.</dd> <dt>string `text_template_description`</dt> <dd>Template description.</dd> <dt>string `text_template_title`</dt> <dd>Template title.</dd> <dt>string `text_text`</dt> <dd>Text content of the email.</dd> <dt>int `tl_last_save`</dt> <dd>Time in seconds of the last template save.</dd> </dl>'
                      a_pattern_load_default:
                        type: array
                        description: 'Default mail pattern data to load. <dl> <dt>bool `is_mail`</dt> <dd>Is email enabled.</dd> <dt>bool `is_push`</dt> <dd>Is push notification enabled.</dd> <dt>bool `is_sms`</dt> <dd>Is SMS enabled.</dd> <dt>int `id_mail`</dt> <dd>ID of the notification. One of {@link \RsMailSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. <i>Primary key in {@link \RsMailPatternSql} table.</i></dd> <dt>string `text_date_edit`</dt> <dd>Data of the last template save in human-readable format.</dd> <dt>string `text_html`</dt> <dd>HTML content of the email.</dd> <dt>string `text_json`</dt> <dd>JSON content of the email for Unlayer editor.</dd> <dt>string `text_preview`</dt> <dd>>Preview text of the email.</dd> <dt>string `text_push`</dt> <dd>Content of the push notification.</dd> <dt>string `text_sms`</dt> <dd>Content of the SMS.</dd> <dt>string `text_template_description`</dt> <dd>Template description.</dd> <dt>string `text_template_title`</dt> <dd>Template title.</dd> <dt>string `text_text`</dt> <dd>Text content of the email.</dd> <dt>int `tl_last_save`</dt> <dd>Time in seconds of the last template save.</dd> </dl>'
                      json_pattern_list_load:
                        type: string
                        description: 'List of mail patterns data to load. Each element has the structure, described in {@link PatternApi::$a_pattern_load}.'
                      text_phone_formatted:
                        type: string
                        description: 'Business sender phone formatted.'
      tags:
        - CustomTemplate-Pattern
    post:
      summary: 'Saves custom pattern. Validates and stores the submitted notification template (email, push or SMS) for the given business. A system template edited under a business is copied into that business instead of modifying the original. The saved mail pattern key is returned in {@link PatternApi::$k_mail_pattern}.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of the notification. One of {@link \RsMailSid} constants. `0` for empty template.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern
          in: query
          description: 'Mail pattern key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pattern_save:
                  type: array
                  description: 'Mail pattern data to save. <dl> <dt>bool `is_mail`</dt> <dd>Is email enabled.</dd> <dt>bool `is_push`</dt> <dd>Is push notification enabled.</dd> <dt>bool `is_sms`</dt> <dd>Is SMS enabled.</dd> <dt>int `id_mail`</dt> <dd>ID of the notification. One of {@link \RsMailSid} constants.</dd> <dt>string|null `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. <i>Primary key in {@link \RsMailPatternSql} table.</i></dd> <dt>string `text_html`</dt> <dd>HTML content of the email.</dd> <dt>string `text_json`</dt> <dd>JSON content of the email for Unlayer editor.</dd> <dt>string `text_preview`</dt> <dd>Preview text of the email.</dd> <dt>string `text_push`</dt> <dd>Content of the push notification.</dd> <dt>string `text_sms`</dt> <dd>Content of the SMS.</dd> <dt>string `text_subject`</dt> <dd>Subject of the email.</dd> <dt>string `text_template_description`</dt> <dd>Template description.</dd> <dt>string `text_template_title`</dt> <dd>Template title.</dd> <dt>string `text_text`</dt> <dd>Text content of the email.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_mail_pattern:
                        type: string
                        description: 'Mail pattern key.'
      tags:
        - CustomTemplate-Pattern
  '/Wl/Member/Progress/Log/Edit/Log.json':
    get:
      summary: 'Returns progress log data. Loads field values recorded for the specified user on the given date (defaulting to the business''s current local date), and also returns the last date the user entered progress data and whether the viewer is staff.'
      parameters:
        - name: dt_date
          in: query
          description: 'Progress log local date. If <tt>null</tt> use the current date.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
                      dt_last:
                        type: [string, "null"]
                        description: 'Last local date when the user entered progress values. <tt>null</tt> if the user had not entered progress data before the {@link \Wl\Member\Progress\Log\Edit\LogApi::$dt_date} date.'
                      is_staff:
                        type: boolean
                        description: 'How we want to see this page. As a staff or as a user.'
      tags:
        - Edit-Log
    post:
      summary: 'Saves progress fields log. Validates and persists the field values for the specified user and date, enforcing access rules (clients cannot edit verified entries), and logs the change when data is modified.'
      parameters:
        - name: dt_date
          in: query
          description: 'Progress log local date. If <tt>null</tt> use the current date.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_log:
                  type: array
                  description: 'List of fields. Keys are field IDs ({@link \Wl\Member\Progress\Field\ProgressFieldSid} constants). Values are new field values. Structure of value completely defined by individual fields.'
                is_weight_only:
                  type: boolean
                  description: 'Whether field is only weight should be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
      tags:
        - Edit-Log
  '/Wl/Member/Progress/Log/Image/Image.json':
    delete:
      summary: 'Deletes progress image. Removes the progress picture for the specified user and date from storage and deletes the corresponding progress log record from the database, then logs the change.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date of the progress picture. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Image-Image
    get:
      summary: 'Returns progress picture data. Loads the progress picture for the specified user and date, returning image storage data, thumbnail dimensions, the upload URL, and a direct link to the image.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date of the progress picture. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image data: <dl> <dt>array `a_image`</dt> <dd> Image storage data. <i>Structure of this array corresponds result of {@link \Wl\Member\Progress\Log\Image\ProgressImage::load()} method.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i-size`</dt> <dd>Max image size.</dd> <dt>int `i-thumbnail-height`</dt> <dd>Image thumbnail height.</dd> <dt>int `i-thumbnail-width`</dt> <dd>Image thumbnail width.</dd> <dt>string `s_link`</dt> <dd>Image link.</dd> <dt>string `url-action`</dt> <dd>Image upload url.</dd> </dl>'
      tags:
        - Image-Image
    post:
      summary: 'Saves progress picture. Applies the image command from `$s_command` to the progress picture for the specified user and date, persists the result in storage, and writes or updates the corresponding progress log record in the database.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date of the progress picture. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_command:
                  type: [string, "null"]
                  description: 'Save image command. <tt>null</tt> if not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Image-Image
  '/Wl/Member/Progress/Log/Compare/Compare.json':
    get:
      summary: 'Returns list of dates where client logged his progress. Retrieves all distinct dates on which the given user recorded progress entries for the business, excluding the most recent date (since it cannot be compared with itself). Returns an empty array when fewer than two entries exist.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to get information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to search dates for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: array
                        description: 'List of local dates of the progress log.'
      tags:
        - Compare-Compare
  '/Wl/Member/Progress/Field/Edit/Field.json':
    get:
      summary: 'Returns field data. Loads the configuration of the specified progress field for the given business, including its type, measurement unit, visibility settings, and title, and populates the result properties accordingly.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_measurement:
                        type: array
                        description: 'A list of field measurement. See {@link \Wl\Member\Progress\Field\MeasurementSid}.'
                      a_type:
                        type: array
                        description: 'A list of field type. See {@link \Wl\Member\Progress\Field\TypeSid}.'
                      id_field:
                        type: [number, "null"]
                        description: 'Field ID. One of the {@link ProgressFieldSid} constant.'
                      id_measurement_unit:
                        type: ["null", number]
                        description: 'Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.'
                      id_type:
                        type: number
                        format: int32
                        description: 'Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.'
                      is_active:
                        type: boolean
                        description: 'Whether field is active and should be displayed on page.'
                      is_public:
                        type: boolean
                        description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                      is_require:
                        type: boolean
                        description: 'Whether field value is required for clients.'
                      is_require_staff:
                        type: boolean
                        description: 'Whether field value is required for staffs.'
                      text_title:
                        type: [string, "null"]
                        description: 'Field title. Only titles of the custom progress fields can be changed. <tt>null</tt> If default title should be used.'
      tags:
        - Edit-Field
    post:
      summary: 'Saves field data. Updates the configuration of the specified progress field, validating measurement unit, type, and title constraints before persisting the changes. Logs the change when the field data is modified.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_measurement_unit:
                  type: ["null", number]
                  description: 'Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.'
                id_type:
                  type: number
                  format: int32
                  description: 'Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.'
                is_active:
                  type: boolean
                  description: 'Whether field is active and should be displayed on page.'
                is_public:
                  type: boolean
                  description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                is_require:
                  type: boolean
                  description: 'Whether field value is required for clients.'
                is_require_staff:
                  type: boolean
                  description: 'Whether field value is required for staffs.'
                text_title:
                  type: [string, "null"]
                  description: 'Field title. Only titles of the custom progress fields can be changed. <tt>null</tt> If default title should be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_title:
                        type: [string, "null"]
                        description: 'Field title. Only titles of the custom progress fields can be changed. <tt>null</tt> If default title should be used.'
      tags:
        - Edit-Field
  '/Wl/Member/Progress/Goal/Edit/Goal.json':
    get:
      summary: 'Returns progress goal log data. Loads the current goal values for all configured progress fields for the specified user and business, and also returns whether the current viewer is a staff member.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
                      is_staff:
                        type: boolean
                        description: 'How we want to see this page. As a staff or as a user.'
      tags:
        - Edit-Goal
    post:
      summary: 'Saves progress goal fields log. Validates and persists the goal values provided in `$a_field_goal` for the specified user and business, then returns the updated field list in `$a_field_list`.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_goal:
                  type: array
                  description: 'List of fields. Keys are field IDs ({@link \Wl\Member\Progress\Field\ProgressFieldSid} constants). Values are new field values. Structure of value completely defined by individual fields.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
      tags:
        - Edit-Goal
  '/Wl/Shop/Product/Option/Inventory/Count/InventoryCount.json':
    get:
      summary: 'Returns information about product options. Works in two modes: review and search. In review mode, the method returns products with changed inventory quantities and notes. In search mode, the method returns one product and its options (the result of searching by product barcode). Returns product option details including the current inventory quantity, last change date, and notes. In barcode search mode, returns matching products; in review mode, returns only options whose inventory or notes have been modified via the pending quantity or note maps.'
      parameters:
        - name: a_note
          in: query
          description: 'The list of notes for product options in the store. Keys refer to product options in the store. Values refer to text notes.'
          schema:
            type: object
        - name: a_quantity
          in: query
          description: 'The list of product option quantities in the store. Keys refer to product options in the store. Values refer to product counts.'
          schema:
            type: object
        - name: k_business
          in: query
          description: 'The current business.'
          schema:
            type: string
        - name: text_barcode
          in: query
          description: 'The product barcode, used for the search.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product_option:
                        type: array
                        description: 'Information about product options for review or search. Each element contains: <dl> <dt>array `a_image`</dt> <dd> Information about the product option image: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `url`</dt> <dd>The image URL.</dd> </dl> </dd> <dt>string `dl_change`</dt> <dd>The date of the last product inventory change.</dd> <dt>int `i_count`</dt> <dd>The product option quantity.</dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_shop_product_option`</dt> <dd>The key of the product option in the store.</dd> <dt>string `k_shop_product_option_inventory`</dt> <dd>The key of the product option inventory in the store.</dd> <dt>string `text_category`</dt> <dd>The title of the store category.</dd> <dt>string `text_location`</dt> <dd>The location title.</dd> <dt>string `text_note`</dt> <dd>The product option inventory note.</dd> <dt>string `text_option`</dt> <dd>The product option title.</dd> <dt>string `text_product`</dt> <dd>The product option title.</dd> </dl>'
                      text_user_name:
                        type: string
                        description: 'The current username.'
      tags:
        - Count-InventoryCount
    post:
      summary: 'Saves changes of product inventory quantities and notes. Persists updated inventory counts and per-option notes for the specified product options. Requires backend access with the store management privilege. Creates an inventory transaction record for audit trail purposes.'
      parameters:
        - name: a_note
          in: query
          description: 'The list of notes for product options in the store. Keys refer to product options in the store. Values refer to text notes.'
          schema:
            type: object
        - name: a_quantity
          in: query
          description: 'The list of product option quantities in the store. Keys refer to product options in the store. Values refer to product counts.'
          schema:
            type: object
        - name: k_business
          in: query
          description: 'The current business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Count-InventoryCount
tags:
  -
    name: Account-Account
    description: 'Displays information about user''s accounts.'
  -
    name: Account-BusinessAccount
    description: 'Gets a list of active businesses. To run this endpoint, a user must be authorized with the `rs.business.view` privilege.'
  -
    name: Ach-List
    description: 'List of user''s ACH accounts.'
  -
    name: Achieve-AchieveSubscription
    description: 'Entry point to get information about business Achieve subscription.'
  -
    name: Action-Action
    description: 'Retrieves a list of available reward actions.'
  -
    name: Action-ActionType
    description: 'Information about reward actions of given type in business.'
  -
    name: Action-ActionWrite
    description: 'Point for modify the reward action.'
  -
    name: Action-Element
    description: 'Retrieves information about reward action item.'
  -
    name: Activity-Element
    description: 'Returns detailed information about a single client activity.'
  -
    name: Activity-List
    description: 'Returns a list of client activities.'
  -
    name: Add-Add
    description: 'Entry point to save ACH pay method. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Add-MailUseOk
    description: 'Registers an existing user in the specified Enterprise Headquarters account. This endpoint isn''t applicable for users who are already members of the business. After a profile is edited, there can be a delay while the data replicates to system caches. As a result, you may see a ''profile-field-empty'' status code. In most cases, this delay is less than 20 seconds. For Enterprise Cloud in WellnessLiving, clients can only be members in one Enterprise Location. In all other Enterprise Locations, they''ll be treated as travelers in the system.'
  -
    name: Address-Address
    description: 'Gets information about a user''s payment addresses.'
  -
    name: Address-Profile
    description: 'Retrieves a client’s physical address from their profile.'
  -
    name: Agree-Agree
    description: 'Saves the user''s agreement to the online waiver.'
  -
    name: Alert-Alert
    description: 'Api to fire an in-app alert for an event triggered by the AI Agent. This endpoint is not public and can be used only by the AI agent.'
  -
    name: Alert-AlertEdit
    description: 'Allows to create, read, update and delete login notes.'
  -
    name: AmazonRegion-AmazonRegion
    description: 'An endpoint for retrieve region ID for businesses.'
  -
    name: Analytics-WidgetAnalyticsEvent
    description: 'Receives versioned analytics events from React Widgets and SDK applications.'
  -
    name: Announcement-AnnouncementList
    description: 'Retrieves a list of announcements.'
  -
    name: Api-DriveCopy
    description: 'Copy file between two links.'
  -
    name: Api-DriveCopyInstance
    description: 'Copies all files from one instance to another.'
  -
    name: Api-DriveDelete
    description: 'Deletes a link to a physical file from the database.'
  -
    name: Api-DriveDownload
    description: 'Returns contents of a specified file.'
  -
    name: Api-DriveFile
    description: 'Returns contents of a specified file.'
  -
    name: Api-DriveGenericInfo
    description: 'Returns contents of a specified file.'
  -
    name: Api-DriveRegisterVariants
    description: 'Registers variants.'
  -
    name: Api-DriveSave
    description: 'Saves the uploaded file.'
  -
    name: Api-DriveSave74
    description: 'Saves the uploaded file.'
  -
    name: Api-DriveUrlSign
    description: 'Returns a pre-signed URL to download a file with restricted access rights.'
  -
    name: Api-KeySecret
    description: 'Gets a secret key for request signing. This is required for a CORS request. A request to this point must be signed as an ordinary request from the SDK using transient and persistent cookies. There must be no CORS (don''t send from a browser).'
  -
    name: Application-MemberInfo
    description: 'Returns information about user for self check in application.'
  -
    name: Application-Origin
    description: 'Gets, deletes, and adds origins for the application. Origins are links on the sites where the API can be used sign CORS authorizations from a client''s browser. If the site isn''t in the list, you can only use the API for requests between two servers. Requests directly from a client''s browser are restricted. Origins should appear as a full link in the same manner they appear on the site. It''s important to understand that the application can add allowed sites only for itself and can''t add sites for another application. This means that you need to call this endpoint using only the application that you''re using on the sites.'
  -
    name: Application-ReceptionAuthorize
    description: 'Performs authorization from the Check In web application. This is a pretty special thing, because it''s not really authorize user, it''s just selects it for Check In application only and allows only to check in to the location. That is why it can not require password. But also can require - this depends on the location settings. It also requires special secret string, which connects request to the certain Check In web application session.'
  -
    name: Application-ReceptionSchedule
    description: 'Shows the schedule for the check-in process. Returns the HTML code that is normally presented for the check-in process.'
  -
    name: Application-Skin
    description: 'Retrieves customized skin settings for the client view.'
  -
    name: Application-SpaApplication
    description: 'Information of application.'
  -
    name: Apply-Apply
    description: 'Endpoint designed to pay for a visit with guest pass options attached to specific promotion. If visit is paid already then old payment option will be revoked without penalties.'
  -
    name: Asset-AssetList
    description: 'Retrieves information about assets in the current asset category.'
  -
    name: Asset-Category
    description: 'Retrieves information about asset categories.'
  -
    name: Attach-AttachElement
    description: 'Endpoint for upload and manage attachments.'
  -
    name: Attach-AttachList
    description: 'Manages client attachment list.'
  -
    name: Attendance-AttendanceInfo
    description: 'Attendance list information.'
  -
    name: Attendance-AttendanceInfoByToken
    description: 'Displays information for certain sessions. Version of {@link \Wl\Login\Attendance\AttendanceInfoApi} for access validation by security token.'
  -
    name: Attendance-AttendanceList
    description: 'Retrieves information about clients attending a class, appointment, or event session.'
  -
    name: Attendance-AttendanceListByToken
    description: 'An endpoint that displays attendance list data. Version of {@link AttendanceListApi} for access validation by security token.'
  -
    name: Attendance-AttendanceOverlap
    description: 'Retrieves a list of visits that overlap with the specified service/class/resource/time data.'
  -
    name: Attendance-AttendanceOverlapList
    description: 'Checks a batch of candidate visits for booking overlaps and reports the result per date.'
  -
    name: AuthorizePartner-AuthorizePartner
    description: 'Gives members of a franchisor access to a franchisee. To grant a user access, the "Manage Franchise Location" staff permission is required. Access will be granted for 24 hours.'
  -
    name: Automation-CacheFlush
    description: 'Automation test API endpoint that flushes all application caches.'
  -
    name: Autymate-AutymateActivate
    description: 'Checks or changes the status of Autymate enrollments.'
  -
    name: Autymate-Report
    description: 'Retrieves all daily transactions for a business using the All Transactions report endpoint. See {@link \Thoth\ReportCore\Generator\QueryApi}. This endpoint is a streamlined interface and adds additional columns to the report for Autymate. Autymate will have one user to access all businesses, this user will require the wl.integration.autymate.report privilege.'
  -
    name: Board-Element
    description: 'Retrieves information about reward board item.'
  -
    name: Board-List
    description: 'Retrieves a list of items to show in user profile.'
  -
    name: BoardList-List
    description: 'Retrieves a list with information about reward board.'
  -
    name: Bot-Access
    description: 'Allows to grant/revoke access from a bot to a service.'
  -
    name: Bot-Edit
    description: 'Allows to create, edit and delete bots.'
  -
    name: Branch-Branch
    description: 'Runs a build for creation a new branch or delete an existing branch for the specified task.'
  -
    name: Branch-BranchBuild
    description: 'Changes branch information during build. This API is designed to use only from builds (`BranchCreate`, `BranchDelete`) and is not expected that this API is called from other places.'
  -
    name: Branch-BranchList
    description: 'Runs a build for creation a new branch or delete an existing branch for the specified task.'
  -
    name: Bulk-BulkBilling
    description: 'Manages a bulk billing that charges the same set of purchase items to many clients at once. Covers the whole life cycle of a scheduled bulk billing: <ul> <li> Schedules the billing, either immediately or on a chosen date and time. Call {@link PurchaseItemListApi} first to review the clients and items and to obtain the review id, then schedule the billing with that review id. The set of clients and items reviewed earlier is billed without having to send it again. </li> <li>Reschedules a still-scheduled billing to a new date and time.</li> <li>Cancels a still-scheduled billing so that it is never billed.</li> </ul> Only a billing that has not started yet can be rescheduled or cancelled.'
  -
    name: Bulk-BulkBillingUser
    description: 'Removes a single client from a bulk billing before they are billed. Only a client that has not been billed yet can be removed; a client that was already charged or failed keeps their result. The client is not deleted - they are marked cancelled and skipped when the billing runs.'
  -
    name: Bulk-PurchaseItemList
    description: 'Lists the promotions and products available for bulk billing, and reviews a bulk billing before it is scheduled. Use the GET request to list the promotions and products of a business available at a location, with their prices. Products are expanded into their options, each with its own price. Use the POST request to review a bulk billing: it returns the per-client totals, the clients that will be billed, and the clients excluded because a selected item is not available to them. The review also returns an id that is passed to {@link BulkBillingApi} to schedule the billing without sending the clients and items again.'
  -
    name: Business-Business
    description: 'Creates a new business.'
  -
    name: Business-BusinessAccess
    description: 'Returns information about a given staff member''s business.'
  -
    name: Business-Data
    description: 'Displays information for a specified business.'
  -
    name: Cancel-CancelCan
    description: 'Returns information about whether the given user can cancel an online booking and what consequences the cancellation would have. This API performs a dry run only — no actual cancellation is performed.'
  -
    name: Captcha-CaptchaRequire
    description: 'Checks if a CAPTCHA is required.'
  -
    name: Captcha-CaptchaScore
    description: 'Endpoint for validating and overriding the score for {@link CaptchaVersionSid::V3} captcha.'
  -
    name: Captcha-GoogleCaptcha
    description: 'Stores the user token CAPTCHA.'
  -
    name: Card-List
    description: 'Gets information about any payment cards belonging to a user.'
  -
    name: Cart-Cart
    description: 'Displays information about the cart in the catalog.'
  -
    name: Cart-LimitQuantity
    description: 'Displays information about the quantity limit when a promotion (Purchase Option) is added to the cart.'
  -
    name: Catalog-ProgramList
    description: 'Retrieves a list of programs'' information.'
  -
    name: CatalogCart-CatalogCart
    description: 'Calculates price data for a sale item. Note that the terms "promotion" and "Purchase Option" refer to the same thing.'
  -
    name: CatalogList-CatalogList
    description: 'Returns a list of all products available from a business’s shop. If a location is specified, only products available at that location are included.'
  -
    name: CatalogList-CatalogProduct
    description: 'Returns a list of products.'
  -
    name: CatalogList-Element
    description: 'Displays information about a certain item in the store. When using this endpoint, note that the terms "promotion" and "Purchase Option" refer to the same thing.'
  -
    name: CatalogList-List
    description: 'Displays information about store items.'
  -
    name: CatalogView-CatalogView
    description: 'Returns information about an item’s price after taxes.'
  -
    name: Category-Category
    description: 'Returns a list of all categories in a business’s store.'
  -
    name: Category-CategoryElement
    description: 'Saves video categories.'
  -
    name: Category-CategoryList
    description: 'Returns a list of video categories.'
  -
    name: CategoryList-CategoryList
    description: 'Retrieves a list with reward action categories.'
  -
    name: ChangePassword-ChangePasswordApply
    description: 'Finishes the password change procedure for a user. This endpoint advances the {@link ChangePasswordBeginApi} endpoint. Set your desired password in the {@link ChangePasswordBeginApi::$url_reset} field, where you use the "reset password" link via email to get the user''s new password for this endpoint. When the user goes to your page, get <tt>text_mail</tt> and <tt>text_code</tt> from the URL, then post this model to set a password for user. Use <tt>text_mail</tt> as the value for {@link ChangePasswordApplyApi::$text_login} and {@link ChangePasswordApplyApi::$text_mail}, and <tt>text_code</tt> as the value for {@link ChangePasswordApplyApi::$text_code}.'
  -
    name: ChangePassword-ChangePasswordBegin
    description: 'Starts the password change procedure for a user. Post this to send the user the "reset password" email.'
  -
    name: Charge-Charge
    description: 'Fills a user account with a specified payment. This endpoint uses a CAPTCHA check. To pass the CAPTCHA, review the CAPTCHA API doumentation. where you''ll find that you must send a CAPTCHA for a specific action. For this endpoint, the action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Cid-Info
    description: 'Allows to retrieve information about a CID class. This API endpoint is only available for Studio personnel and bots.'
  -
    name: Cid-Register
    description: 'Allows to register a new CID class. This API endpoint is only available for Studio personnel and bots.'
  -
    name: Cid-Rename
    description: 'Allows to rename a class associated with a CID. This API endpoint is only available for Studio personnel and bots.'
  -
    name: Claim-BusinessClaim
    description: 'Business trial verification API.'
  -
    name: Claim-Claim
    description: 'API to claim a guest pass invitation on behalf of the currently signed-in user.'
  -
    name: ClassList-BookList
    description: 'An endpoint to get all the classes for a location.'
  -
    name: ClassList-ClassList
    description: 'Retrieves a list of classes and class information for a Class Tab.'
  -
    name: ClassList-ClassList68
    description: 'Retrieves a list of classes and class information for a Class Tab.'
  -
    name: ClassList-List
    description: 'List of classes and events.'
  -
    name: ClassSession-ClassSessionSearch
    description: 'Handles searching of class sessions.'
  -
    name: ClassView-ClassView
    description: 'Gets information about a specified class session. A class listing can be retrieved from the {@link ClassListApi} endpoint.'
  -
    name: ClassView-Element
    description: 'Retrieves information about a class element.'
  -
    name: Cluster-Cluster
    description: 'Manage `Studio Deploy Cluster`.'
  -
    name: Code-DiscountCode
    description: 'Discount codes API point.'
  -
    name: Collector-DebtList
    description: 'Returns list of debts added within the previous month.'
  -
    name: Collector-DebtPay
    description: 'Registers a debt payment that was performed beyond Wellnessliving, with a specified credit amount applied to a client''s account balance.'
  -
    name: Collector-DebtTransaction
    description: 'Returns a list of payments for debts added within the previous day.'
  -
    name: Compare-Compare
    description: 'Member progress log compare data.'
  -
    name: Config-BusinessConfig
    description: 'Manages business configurations for clients, bookings, payments, and related things.'
  -
    name: Config-RewardConfig
    description: 'Retrieves information about reward config of the business.'
  -
    name: Connect-ApplicationConnect
    description: 'Creates or updates third-party integration credentials for a white-label application. Used by the App Build Automation (n8n) to programmatically submit the Client App Connect form. Automatically finds the existing record for the given business or creates a new one if none exists.'
  -
    name: Contract-Contract
    description: 'Gets information about a session pass or membership with a contract. The POST method will complete a sale of a Purchase Option requiring a contract. The method that WellnessLiving uses to encode a signature into a string isn''t currently available in the SDK.'
  -
    name: ContractAll-ContractAll
    description: 'Entry point to get list of subscribed or required contracts of user.'
  -
    name: Convert-Convert
    description: 'Receives convertable Purchase Option data and promotion conversions. The DELETE method will remove the conversion and reset the date. The GET method will return a list of promotions available at the business. The POST method can be used to change what the Purchase Option should convert to and when to convert it.'
  -
    name: Count-InventoryCount
    description: 'Used to retrieve and change inventory quantities and notes for product options in the store.'
  -
    name: Coupon-Coupon
    description: 'Retrieves information about a gift card (also referred to as the coupon code).'
  -
    name: CouponList-List
    description: 'Retrieves a list of gift cards.'
  -
    name: Credential-Credential
    description: 'Manages credentials for the application.'
  -
    name: Currency-Currency
    description: 'Retrieves information about all currencies in the system.'
  -
    name: Current-Current
    description: 'Retrieves information about current score.'
  -
    name: Curves-CurvesFranchiseLocation
    description: 'Gets information for enterprise locations.'
  -
    name: CustomTemplate-Pattern
    description: 'Gets and saves custom template pattern data.'
  -
    name: Customization-CustomizationForm
    description: 'API endpoint to load and save data for a report customization form.'
  -
    name: Customization-ReportQueryCustomizationForm
    description: 'Loads and saves customization form data scoped to a specific SQL query report. Extends {@link CustomizationFormApi} to add {@link ReportQueryCustomizationFormApi::$k_report_query}, which isolates customization settings (visible buttons, date range, report generation mode) per SQL query instead of sharing them across all SQL reports for the same business and user. When {@link ReportQueryCustomizationFormApi::$k_report_query} is set, the per-query customization row is loaded first; if no per-query row exists, the generic shared row is used as a fallback so existing settings are preserved after the first deployment.'
  -
    name: CyberSource-CsCaptureContext
    description: 'Generate a capture context data and key ID to encrypt the card number in the follow-on Tokenize Card request.'
  -
    name: CyberSource-CsPaEnrollment
    description: 'Allows to Check Payer Authentication Enrollment request.'
  -
    name: CyberSource-CsPaSetup
    description: 'Allows to perform Payer Authentication Setup request.'
  -
    name: CyberSource-CsPaValidate
    description: 'This call retrieves and validates the authentication results from issuer and allows the merchant to proceed with processing the payment'
  -
    name: Dashboard-ReportDashboard
    description: 'API endpoint to manage reports added to a dashboard. <b>Note</b>. Old context and context from {@link ReportSaveApi} is used here because: - reason to use ''rs.report.save'' context: messages should be the same in both implementations (old and new); - reason to use context from {@link ReportSaveApi}: saving and adding to a dashboard in the old implementation uses the same module.'
  -
    name: DebugToken-DebugTokenService
    description: 'Allows checking the validity of a debug token.'
  -
    name: Design-BusinessDesign
    description: 'Provides business design information.'
  -
    name: Design-ReceptionDesign
    description: 'Api endpoint to get information about Self Check-In Kiosk settings.'
  -
    name: Design-ReceptionRosterDesign
    description: 'Api endpoint to get information about Attendance Kiosk settings.'
  -
    name: DragonFly-Access
    description: 'Checks if a user can access a physical location.'
  -
    name: Drive-ProductImageUpload
    description: 'Overwrites core functionality of the {@link ImageUploadApi} based on the product specification.'
  -
    name: DynamicId-DynamicId
    description: 'API to get member dynamic ID information.'
  -
    name: Edit-AddonUpdate
    description: 'Updates add-ons for an appointment. This endpoint can be used to update add-ons for existing appointments.'
  -
    name: Edit-Create
    description: 'Creates user account for the given email address if email address does not already exist. checks if a user''s email address is already used or changes a user’s login email address.'
  -
    name: Edit-DiscountCodeEdit
    description: 'API point for read, create and edit existing discount code.'
  -
    name: Edit-Edit
    description: 'Point to add or change a client group.'
  -
    name: Edit-EditByToken
    description: 'An endpoint that displays client profile information. Version of {@link \Wl\Profile\Edit\EditApi} for access validation by security token.'
  -
    name: Edit-EditPassword
    description: 'Model to change user''s password.'
  -
    name: Edit-Field
    description: 'Manages progress field data.'
  -
    name: Edit-Goal
    description: 'Progress goal api.'
  -
    name: Edit-Log
    description: 'Progress log api.'
  -
    name: Editor-List
    description: 'Returns a list of microservices.'
  -
    name: Email-EditEmail
    description: 'Checks if a user''s email address is already used or changes a user’s login email address. This endpoint is rate limited. If the rate limit is exceeded, it won''t return user information. While `get` request allows to check, whether user with the given email exists on the site, `post` allows to add it to the certain business, if it''s not added yet. If client has all required by this business fields filled, it will be added right away. Otherwise it will add temporary access to this client, and you need to send all necessary fields with {@link EditApi} endpoint.'
  -
    name: Email-EditEmail67
    description: 'Checks if a user''s email address is already used or changes a user’s login email address. This endpoint is rate limited. If the rate limit is exceeded, it won''t return user information. While `get` request allows to check whether a user with the given email exists on the site, `post` allows adding it to the certain business, if it''s not added yet. If client has all required by this business fields filled, it will be added right away. Otherwise, it will add temporary access to this client, and you need to send all necessary fields with {@link EditApi} endpoint.'
  -
    name: Email-EmailExist
    description: 'Checks if the email address exists in the system.'
  -
    name: Enrollment-TuitionClientsSummary
    description: 'Model to get summary of clients and events enrolled for the given tuitions from the microservice.'
  -
    name: Enrollment-TuitionEnrollmentCancel
    description: 'Model to get list of tuition, which gives access to a certain event from the microservice.'
  -
    name: Enrollment-TuitionEnrollmentList
    description: 'Model to get list of tuition, which gives access to a certain event from the microservice.'
  -
    name: Enter-Enter
    description: 'Signs a user in.'
  -
    name: Enter-Notepad
    description: 'An endpoint that retrieves the notepad value from the server that is used to sign someone in to the SDK.'
  -
    name: Enter-PassportOtp
    description: 'Entry point to verify user authorization by email and phone number via sending and checking otp code.'
  -
    name: Event-EventCancelWhole
    description: 'Cancels a whole event booked by a client.'
  -
    name: Event-EventEnrollment
    description: 'Retrieves a list of clients enrolled in the specified event instances.'
  -
    name: Event-EventList
    description: 'Retrieves a list of events that fit the given filter parameters.'
  -
    name: EventList-List
    description: 'Retrieves a list of events for a given class tab.'
  -
    name: EventView-Element
    description: 'Retrieves information about an event element.'
  -
    name: Facility-FacilitySid
    description: 'API to get list of additional information about IDs from {@link \RsFacilitySid} class. Property {@link FacilitySidApi::$s_class_name} can be left empty in this class. It will use predefined class name.'
  -
    name: Favorite-ReportFavorite
    description: 'API endpoint to manage favorite reports.'
  -
    name: Field-EnrollmentFieldList
    description: 'Retrieves and validates a list of insurance partner fields. Use the GET method to load a list of required fields. Use the POST method to validate the fields before sending them to the payment API.'
  -
    name: Field-State
    description: 'Performs ajax requests for progress fields edit page.'
  -
    name: Finish-Finish
    description: 'Completes the booking process for a service. This endpoint can be used with an existing client by specifying their UID. It can also be used to create a new client by specifying user details in {@link FinishApi::$a_user} and omitting any UID. This endpoint can be accessed anonymously without authentication, but only when creating clients.'
  -
    name: Finish-Finish47
    description: 'Completes an appointment booking.'
  -
    name: Finish-FinishMultiple
    description: 'Pays for an appointment or appointment Purchase Option for a client.'
  -
    name: Fitbuilder-Message
    description: 'Point to get messenger information from Fitbuilder.'
  -
    name: Flag-Flag
    description: 'Retrieves information if a user is flagged in a location.'
  -
    name: Footer-Footer
    description: 'Entry point to get information required to render the site footer.'
  -
    name: Form-Environment
    description: 'Gets information about payment environments.'
  -
    name: Form-EnvironmentUser
    description: 'Gets information about payment environments for the user.'
  -
    name: Frequency-Repeat
    description: 'For recurrent class booking returns list of visits to be created for the given settings.'
  -
    name: Frequency-RepeatParallel
    description: 'Extension of {@link RepeatApi} that adds control over whether parallel class period series (other sessions running at the same time and location) are included in the returned visit list. When `is_include_parallel` is `false` (default), only the originally selected class period''s series (parent and its reschedules) is returned. When `true`, all parallel series are also included and can be reviewed or individually ignored by the client.'
  -
    name: Frontend-LifetimeTotals
    description: 'Returns a list with totals of lifetime visits.'
  -
    name: Generator-Query
    description: 'Returns content of a report as a table. If report is not yet generated, it gets generated. If the report is being generated now, partial content may be returned.'
  -
    name: Geo-Combobox
    description: 'Retrieves a list of cities matching the search string for a given location.'
  -
    name: Google-GoogleCaptcha
    description: 'Stores the user token CAPTCHA.'
  -
    name: GroupList-List
    description: 'Member groups list.'
  -
    name: Guest-GuestProfile
    description: 'Endpoint for guest account creation during "Book on behalf of guest" process.'
  -
    name: GuestPass-GuestPass
    description: 'API for managing guest passes.'
  -
    name: GuestPass-GuestPassList
    description: 'API for managing guest passes of specific client.'
  -
    name: Holiday-BulkBusinessHoliday
    description: 'An endpoint displaying information about all location/business holidays for a given business.'
  -
    name: Holiday-Holiday
    description: 'An endpoint displaying information about locations'' business holidays.'
  -
    name: Image-Image
    description: 'Progress image api.'
  -
    name: ImageUpload-ImageUpload
    description: 'An endpoint that performs the final action to an uploaded image. Images are uploaded using {@link \Core\Drive\ImageUpload\ImageUploadTemporaryApi}.'
  -
    name: ImageUpload-ImageUploadTemporary
    description: 'Uploads a raw image to a server. You must save an image by using {@link ImageUploadApi}.'
  -
    name: Index-PromotionIndex
    description: 'Gets a list of introductory promotion offers of a specified type available at a given location.'
  -
    name: Info-Info
    description: 'Offers functionality for the class booking wizard on the "Class and Location" page.'
  -
    name: Info-Info54
    description: 'Offers functionality for the class booking wizard on the "Class and Location" page. When using this endpoint, take into account the {@link ProcessSpaSid::QUIZ} step.'
  -
    name: Info-InfoCanComplete
    description: 'Checks for `next` and `complete` buttons in the booking process.'
  -
    name: Info-UserInfo
    description: 'Retrieves information about a WellnessLiving user.'
  -
    name: Info-UserIntegration
    description: 'An endpoint that return the information about the integrations the user is connected to.'
  -
    name: Instance-Instance
    description: 'Manage `Studio Deploy Instance`.'
  -
    name: Invite-Invite
    description: 'API to manage a single guest pass invitation.'
  -
    name: Invite-InviteList
    description: 'API to manage guest pass invitations within a client''s membership or client''s profile.'
  -
    name: Jwt-JwtToken
    description: 'Endpoint to get JWT token.'
  -
    name: Language-LanguageSwitch
    description: 'Switches the language of the user who makes this request. The choice is remembered for this user in the context this user is currently in, which is the currently selected business in WellnessLiving environment, see {@link LanguageManager::setLanguage()}. It is also stored in the session, which is the only place a user who is not signed in has, and which is where the language of the next request is taken from, see {@link \CmsLanguage::session_get()}. This class does not redirect anywhere. The language is not a part of the URL of a page, so the caller only has to reload the page it is on for the new language to take effect.'
  -
    name: Layout-Layout
    description: 'Displays information about an asset layout.'
  -
    name: Lead-BusinessLead
    description: 'Creates a new business lead request within the Partner Program.'
  -
    name: Lead-Lead
    description: 'Gets information from the Lead Capture widget and saves a new user’s information. A user can be added to a second business by adding them first as a lead. If your business uses Enterprise Cloud, there may be a restriction where clients can only be members in one enterprise location (travellers in all other enterprise locations). This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link LeadCaptcha::CID}.'
  -
    name: Lead-LeadCheck
    description: 'Checks if user with specified email already belongs to specified business.'
  -
    name: Level-Level
    description: 'Retrieves, changes, or deletes a video level.'
  -
    name: Level-LevelList
    description: 'Changes the order of video levels.'
  -
    name: Link-SendMail
    description: 'Model to send email based on AI agent link action. This endpoint is not public and can be used only by the AI agent.'
  -
    name: Location-BusinessFranchiseLocation
    description: 'Manages franchisor locations.'
  -
    name: Location-List
    description: 'Returns information for all locations belonging to a business.'
  -
    name: Location-ListBulk
    description: 'Gets all locations for a list of businesses. This can be used to get all locations from all businesses. When using this endpoint, note that it will return a list of all locations in the system, potentially using substantial system resources. Using this endpoint can potentially result in larger API calls than average calls made for other endpoints.'
  -
    name: Location-Location
    description: 'Creates a new location in the business or edits a specified location. Access to this endpoint is logged with specified permissions. It can also be accessed by guests during the registration process for new businesses. See {@link \Wl\Business\BusinessApi::sessionKey()}. Field <tt>k_business</tt> is required. Other fields are optionally if the location should be edited. This means if a field is not specified it is not be changed. If <tt>k_location</tt> is not specified that new location will be created. In this case <tt>k_city</tt>, <tt>text_address</tt> and <tt>text_title</tt> are required.'
  -
    name: LocationRate-LocationRate
    description: 'Retrieves information about location rate.'
  -
    name: Log-Verify
    description: 'Performs ajax requests for verify client''s progress log.'
  -
    name: LogTriage-ConnectionCheck
    description: 'Checks that Studio can connect to the monolith and returns selected findings.'
  -
    name: Login-AppleLogin
    description: 'Authorizes a user with Apple.'
  -
    name: Login-FacebookLogin
    description: 'An endpoint that performs authorization actions with Facebook.'
  -
    name: Login-GoogleLogin
    description: 'An endpoint that performs authorization actions with Google.'
  -
    name: Login-Info
    description: 'Gets the user ID for the current user and a password reset URL.'
  -
    name: Login-Login
    description: 'Authorizes Facebook users.'
  -
    name: Login-MicrosoftLogin
    description: 'An endpoint that performs authorization actions with Microsoft.'
  -
    name: Mail-MailUse
    description: 'An endpoint to check if an email address is used by member of a business.'
  -
    name: Mail-PurchaseMail
    description: 'Emails a receipt for a purchase.'
  -
    name: Mail-SendMail
    description: 'Can be used to send email messages.'
  -
    name: Member-LoginMember
    description: 'An endpoint that manages client ID.'
  -
    name: Member-LoginMemberListAll
    description: 'Displays list of active members in a business.'
  -
    name: Member-Member
    description: 'Displays information about members of businesses.'
  -
    name: Member-MemberValidate
    description: 'Checking whether there is something that prevents the user from using the business.'
  -
    name: Member-MemberValidate63
    description: 'Checking whether there is something that prevents the user from using the business.'
  -
    name: Method-List
    description: 'An endpoint containing a model to get the list of payment methods.                                                        .'
  -
    name: Microsoft-Login
    description: 'Performs preliminary authorization actions with Microsoft.'
  -
    name: Modify-Modify
    description: 'Modifies the master model.'
  -
    name: Nuvei-NuveiOpenOrder
    description: 'Authenticates `Nuvei` merchant credentials and sets up an order in the `Nuvei` system.'
  -
    name: Nuvei-NuveiTransactionCancel
    description: 'Cancels `Nuvei` transaction.'
  -
    name: Nuvei-NuveiTransactionOnAuthSuccess
    description: 'Updates `Nuvei` transaction with the gateway transaction ID.'
  -
    name: Nuvei-NuveiUpdateOrder
    description: 'Allow modifying the amount of a previously opened order {@link NuveiOpenOrderApi}.'
  -
    name: Option-BusinessConfigOption
    description: 'Retrieves business config options.'
  -
    name: Otp-PhoneOtp
    description: 'Entry point to verify access to the phone number via sending and checking otp code. This endpoint is available only for user with a special privilege. If you want to use this endpoint, contact with WellnessLiving support first.'
  -
    name: Owner-Owner
    description: 'Gets the payment owner ID.'
  -
    name: Page-PageElement
    description: 'Returns information about a specified visit.'
  -
    name: Page-PageList
    description: 'Returns a list of either previous or upcoming visits for a specific user. A visit can be for an appointment, class, or an event.'
  -
    name: Partner-PartnerCode
    description: 'Works with partner url for a business that takes part in the partner program.'
  -
    name: Partner-PartnerSettings
    description: 'Works with partner settings of the business. This information is entered by a staff member when a client enrolls into Partner Program and uses a custom landing page, which is available by special business URL.'
  -
    name: Payment-Payment
    description: 'An endpoint that displays information about payments for an appointment. The POST method for this endpoint is implemented as a separate endpoint (see {@link PaymentPostApi}). This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Payment-PaymentMultiple
    description: 'Completes the payment for an appointment or appointment Purchase Option for a client. The only difference between this endpoint and {@link PaymentApi} is that this endpoint is capable of paying for multiple appointments at the same time. This endpoint uses a CAPTCHA check. To pass the CAPTCHA, you''ll need to study the documentation of the CAPTCHA base class. In the documentation, you''ll find that you need to send a CAPTCHA for a specific action. For this API, the action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Payment-PaymentPost
    description: 'Sends payments for an appointment booking. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Permission-Permission
    description: 'Makes a purchased promotion auto-renewable. The POST method makes the promotion auto-renewable based on the {@link PermissionApi::$is_renew} parameter.'
  -
    name: Phone-BusinessPhone
    description: 'API endpoint to get phone number of a business.'
  -
    name: Phone-CallReceived
    description: 'Public endpoint used by the CAASI Phone Agent backend to trigger the "Phone Agent Call Received" staff notification, immediately after a Phone Agent call concludes, regardless of outcome (resolved by CAASI, transferred live, or callback requested). Access is restricted to applications registered under {@link ApiApplicationGroup::WL_AIAGENT_MAIL}.'
  -
    name: Phone-Phone
    description: 'Manages business phone number. It is used to register business phone number or add bundle SID, or to get phone number of some business.'
  -
    name: Plus-Login
    description: 'Authorizes a user with Google.'
  -
    name: Privilege-PrivilegeList
    description: 'Can return list of admin or staff privileges of the current user. Cannot be used to check privileges of another user.'
  -
    name: Prize-Element
    description: 'Retrieves information about redeemable prize.'
  -
    name: Prize-Prize
    description: 'Retrieves a list of available redeemable prizes.'
  -
    name: Process-Process
    description: 'Performs the booking wizard steps.'
  -
    name: Process-Process54
    description: 'Performs the booking wizard steps. Include the {@link ProcessSpaSid::QUIZ} step, if needed.'
  -
    name: Process-Process59
    description: 'Performs the booking wizard steps. This endpoint includes changes to the list of steps if the service is already booked for the currently selected client, and if {@link ProcessApi::$is_family_relation_book} is set to `true`.'
  -
    name: Process-ProcessGroup
    description: 'API for group booking process. This API allows to book a class/event for multiple clients at once.'
  -
    name: Product-Product
    description: 'Returns information about service add-ons. If you need to filter products by client type/group, use {@link Product62Api}.'
  -
    name: Product-Product62
    description: 'Returns information about service add-ons.'
  -
    name: Profile-ProfileCreate
    description: 'Creates user profile with minimum fields.'
  -
    name: Promotion-ClassPromotion
    description: 'Returns a list of promotions that can pay for a business''s classes or events.'
  -
    name: Promotion-Promotion
    description: 'Gets promotions for the specified business and promotion keys.'
  -
    name: Promotion-PromotionList
    description: 'Gets a promotion list of the specified business.'
  -
    name: Promotion-PromotionPayPause
    description: 'Manages or retrieves information about holds on Purchase Options. The DELETE method can remove a hold. The GET method only returns information about active holds. The POST method can create or edit a hold. The PUT method can edit a hold.'
  -
    name: Purchase-MemberByPromotion
    description: 'Returns a list of active clients with the given Purchase Options. This endpoint returns only active clients using active Purchase Options from the given list. Does not return clients, to which currently signed-in user does not have access to.'
  -
    name: Purchase-Purchase
    description: 'Retrieves information about Purchase Options that can be used to pay for an appointment.'
  -
    name: Purchase-Purchase56
    description: 'Displays information about Purchase Options that can book specified session(s). Note that the terms "Purchase Option" and "promotion" are used interchangeably.'
  -
    name: Purchase-Purchase72
    description: 'Retrieves information about Purchase Options that can be used to pay for an appointment during creating new or rescheduling existing appointment.'
  -
    name: Purchase-PurchaseElement
    description: 'A model of the purchase item.'
  -
    name: Purchase-PurchaseElementGroup
    description: 'API point to get information about a list of purchase options wile booking process.'
  -
    name: Purchase-PurchaseElementList
    description: 'Displays a list of purchase items.'
  -
    name: Purchase-PurchaseList
    description: 'An endpoint that gets a list of purchases for a user.'
  -
    name: PurchaseList-PurchaseList
    description: 'Gets a list of purchases for a user.'
  -
    name: PurchaseList-PurchaseListElement
    description: 'Gets the details of a specified purchase. This endpoint may require different IDs to be specified for different types of purchases. The IDs required for a specific item will be the same IDs returned from the {@link PurchaseListApi} endpoint.'
  -
    name: Question-Question
    description: 'Retrieves questions for a selected service.'
  -
    name: Quiz-Quiz
    description: 'Manages quizzes for the service or the asset and selected purchase option during booking process.'
  -
    name: Quiz-QuizElement
    description: 'Manages a single quiz.'
  -
    name: Quiz-QuizElement72
    description: 'Manages a single quiz.'
  -
    name: Rank-LoginRankElement
    description: 'API to delete a rank record for a user.'
  -
    name: Rank-Rank
    description: 'Retrieves information about belts in a business. This information can be used to figure out totals in the Belts Report.'
  -
    name: Receipt-PurchaseReceipt
    description: 'Returns receipt data of certain purchase.'
  -
    name: Recent-RecentService
    description: 'Returns list of client''s last booked services. Only unique services are returned, i.e. if a client booked the same service several times, only last of these bookings is returned in the list.'
  -
    name: Referrer-ReferralInfo
    description: 'Returns referral statistics for a user within a business. Provides the count of referred users, total reward points earned from referrals, and a shareable invite link. The link encodes the referrer''s user key so that when a new client registers via the link, the "Referred By" field is pre-filled.'
  -
    name: Referrer-Referrer
    description: 'Searches for a referrer using a string.'
  -
    name: Region-Region
    description: 'Gets a list of regions for a specified country or all countries. A region refers to a jurisdiction like a state, province, or territory.'
  -
    name: Register-Register
    description: 'Allows new clients to be registered. The {@link LeadApi} endpoint is recommended for registering new users. However, this endpoint isn''t enabled by default. To enable this endpoint, use the contact form in the developer portal.'
  -
    name: Register-RegisterConfirm
    description: 'Confirm the user''s email and completes the registration process.'
  -
    name: Register-RegisterOtp
    description: 'Entry point to verify user authorization by email and phone number via sending and checking otp code.'
  -
    name: Register-RegisterOtpJwtPublicKey
    description: 'Returns a public key that verifies JWT from {@link RegisterOtpApi}.'
  -
    name: Relation-FamilyRelation
    description: 'Returns business'' enabled relationship types.'
  -
    name: Relation-Relation
    description: 'Adds a relative during the booking process.'
  -
    name: Relation-Relation72
    description: 'Returns data about a user''s relationships.'
  -
    name: Report-Access
    description: 'Checks access of the current user to the certain report int the given business.'
  -
    name: Report-Data
    description: 'Returns information from a WellnessLiving report. There are two All Clients Reports that can be used to find user IDs. The results from each report can be filtered by different fields. The two reports are similar, but not exactly the same. The `LOGIN_LIST` (ID 22) report requires filters to be set to retrieve information. The `LOGIN_LIST_ALL` (ID 33) doesn’t require any filters to be set when specifying a date range.'
  -
    name: Report-PageData
    description: 'Returns collection of the reports.'
  -
    name: Report-TransactionAllPayment
    description: 'Can retrieve information from the All Transactions Report.'
  -
    name: Request-Example
    description: 'Example API endpoint. Designed to debug issues with API.'
  -
    name: Resource-ApplicationResource
    description: 'Returns information used to generate application sources.'
  -
    name: Resource-ApplicationResourceMaterial
    description: 'Saves text metadata for a white-label application build. Used by the App Build Automation (n8n) to programmatically submit the Client App Materials form (text fields only). File assets are submitted separately via {@link ApplicationResourceUploadApi}.'
  -
    name: Resource-ApplicationResourceUpload
    description: 'Uploads image and file assets for a white-label application build. Accepts multipart/form-data with: - `a_image_upload[{key}]` file fields for image assets, where `{key}` is the image ID produced by sanitizing the asset file path (non-alphanumeric characters replaced with `_`). - A `google_services_json` file field for the Firebase Android config file. Text metadata is submitted separately via {@link ApplicationResourceMaterialApi}.'
  -
    name: Resource-Resource
    description: 'Selects assets for making a booking.'
  -
    name: Resource-Resource54
    description: 'Selects assets when making a booking. Take note of the {@link ProcessSpaSid::QUIZ} step.'
  -
    name: ResourceList-List
    description: 'Asset list by business.'
  -
    name: Response-Response
    description: 'An endpoint to request entrance into a business location. This endpoint is similar to {@link AuthorizePartnerApi} but not identical. To grant a user access the SDK sign-in account, the Manage Business permission is required. The associated accounts will receive an email regarding when they are given access and when that access expires. Access will be granted for 24 hours.'
  -
    name: Response-Response65
    description: 'Endpoint to work with quiz responses.'
  -
    name: Response-ResponseList
    description: 'Retrieves a list of a user''s quiz responses.'
  -
    name: Review-Review
    description: 'Adds a review for a location. The review consists of a rating from 1 to 5 stars and text. The review will be attributed to the user who is signed in to the API. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link ReviewCaptcha::CID}.'
  -
    name: Review-ReviewFeature
    description: 'Changes the status of a featured review.'
  -
    name: Review-ReviewReply
    description: 'Adds a review reply.'
  -
    name: ReviewList-ReviewElement
    description: 'Returns information about a review.'
  -
    name: ReviewList-ReviewList
    description: 'Returns a list of review IDs for all reviews for a location. If location is not specified, returns all reviews for all locations in the specified business. Reviews in WellnessLiving apply to specific locations. This endpoint can be used to get the IDs for all reviews or to get a listing that includes all the review data if the `i_page parameter` is set.'
  -
    name: Roster-AttendanceListAttend
    description: 'Book a class for a client and mark as attended using the Attendance Kiosk.'
  -
    name: Roster-AttendanceListBook
    description: 'Book a class for a client using the Attendance Kiosk.'
  -
    name: Roster-AttendanceListNotAttend
    description: 'Marks the visit as not attended using the Attendance Kiosk.'
  -
    name: Roster-AttendanceSecret
    description: 'Api endpoint to get the secret string for Attendance Kiosk requests.'
  -
    name: Saml-SamlUserDeactivation
    description: 'Retrieves user statuses in the business.'
  -
    name: Save-ReportSave
    description: 'API endpoint to manage saved reports.'
  -
    name: Schedule-Calendar
    description: 'Retrieves a list of all calendar days in a specified month with available and unavailable appointment bookings in the schedule.'
  -
    name: Schedule-Cancel
    description: 'Cancels either an appointment, class, or event session for the client.'
  -
    name: Schedule-DayTime
    description: 'Retrieves a list of information about the booking schedule for available appointments.'
  -
    name: Schedule-NextAvailableDay
    description: 'Returns next day with available.'
  -
    name: Schedule-PaymentMultiple
    description: 'Manages multiple payments for unpaid appointments on the same calendar date, or for a specific list of appointments.'
  -
    name: Schedule-ScheduleAvailableDate
    description: 'Retrieves the nearest class session available for booking.'
  -
    name: Schedule-ServiceAvailability
    description: 'Retrieves a list of information about the booking schedule for appointment.'
  -
    name: Score-Score
    description: 'API endpoint designed to manage points which are earned for certain activity.'
  -
    name: Search-BusinessSearch
    description: 'API to search business keys by given name. This endpoint uses captcha check. To pass a captcha you need to study the captcha API documentation, there you will find that you need to send a captcha for a specific action. In this API the action is {@link BusinessSearchCaptcha::CID}.'
  -
    name: Search-BusinessSearchUser
    description: 'API to search business keys by given name and given user email. This endpoint uses captcha check. To pass a captcha you need to study the captcha API documentation, there you will find that you need to send a captcha for a specific action. In this API the action is {@link BusinessSearchCaptcha::CID}.'
  -
    name: Search-Concerto
    description: 'Point for business Concerto to search a user by mail or phone.'
  -
    name: Select-BusinessSelect
    description: 'Provides the list of business that a staff member may access.'
  -
    name: Select-Select
    description: 'Retrieves information about current user and his relationship with sub accounts.'
  -
    name: Send-NotificationInfo
    description: 'An endpoint that allows to send notifications to users of the certain business or location. This endpoint is available only for user with a special privilege. If you want to use this endpoint, contact with WellnessLiving support first.'
  -
    name: Send-NotificationSend
    description: 'An endpoint that allows to send notifications to users of the certain business or location. This endpoint is available only for user with a special privilege. If you want to use this endpoint, contact with WellnessLiving support first.'
  -
    name: Service-Category
    description: 'Retrieves information about service categories.'
  -
    name: Service-Edit
    description: 'Allows to create, edit and delete IP services.'
  -
    name: Service-InstanceActiveCheck
    description: 'Checks instance activity.'
  -
    name: Service-Service
    description: 'Retrieves assets that are required for a service booking.'
  -
    name: Service-ServiceCheck
    description: 'Checks if service can be booked by client. Throws exception if booking is restricted.'
  -
    name: Service-ServiceList
    description: 'Retrieves information about services in the current service category.'
  -
    name: Service-ServiceList52
    description: 'Retrieves information about services in the current service category. This is a new version of the {@link ServiceListApi} endpoint. It allows for filtering a list of services by multiple book now tabs.'
  -
    name: ServiceList-List
    description: 'List of appointment types of the business.'
  -
    name: ServiceList-List75
    description: 'Versioned list of appointment types with support for loading all (active and inactive) services.'
  -
    name: Session-Environment
    description: 'Returns information about current session environment (business ID, user information, etc.).'
  -
    name: Setting-Setting
    description: 'Retrieves the user’s notification settings. Alternatively, this endpoint can be used to change a user’s notification settings.'
  -
    name: Share-PurchaseShare
    description: 'Shares a purchase to a specified social network.'
  -
    name: Share-SocialShare
    description: 'Saves data about share post to database. Api usage example: <code> $o_api = new SocialShareApi(); $o_api->id_share_object = \Wl\Social\Share\ShareObjectSid::PURCHASE; // Key of the purchase because id_share_object is \Wl\Social\Share\ShareObjectSid::PURCHASE $o_api->a_key = [$k_purchase_a, $k_purchase_b]; $o_api->id_share_destination = \ASocialSid::FACEBOOK; $o_api->k_business = $k_business; $o_api->uid = $uid; $o_api->post(); $url_link = ''''; // Example: Wl_Profile_Activity_ElementModel.url_link // s_secret points specifically to your object that you want to share, so add it to the url. // In this case url_link stores all purchases for the business, // and by using s_secret we will get a link to only those purchases we want to share. $url_facebook_share = ''https://www.facebook.com/sharer/sharer.php?u='' . urlencode($url_link . ''&s_secret='' . $o_api->s_secret); </code>'
  -
    name: Sid-CoreSid
    description: 'API to get list of additional information about IDs from any Sid class. This API can be used itself - in this case you need to send correct name of the class. Or you can use one of the inherited classes with predefined class name, if it exists.'
  -
    name: SignOut-SignOut
    description: 'Signs user out.'
  -
    name: Skin-Skin
    description: 'Retrieves skin settings for customisation of client application.'
  -
    name: Skin-SkinForeign
    description: 'Allows to create, update and remove foreign widgets. Requires a special privilege for the application. Contact WellnessLiving support to get it.'
  -
    name: Source-LeadSourceElement
    description: 'Retrieves Lead Source element.'
  -
    name: Source-LeadSourceList
    description: 'Retrieves a list of Lead Sources.'
  -
    name: Staff-List
    description: 'Retrieves information about staff members for the current service.'
  -
    name: Staff-StaffElement
    description: 'Can create or edit a staff member in a business. You can also use this endpoint to get information about a staff member''s activity in another business when using the {@link BusinessApi} endpoint.'
  -
    name: Staff-StaffList
    description: 'Retrieves information about staff members for the current service.'
  -
    name: StaffApp-List
    description: 'Gets a list of clients whose names match a search string.'
  -
    name: StaffApp-ScheduleList
    description: 'Gets information about sessions (both classes and appointments) at a business on a given day.'
  -
    name: StaffApp-ScheduleListByToken
    description: 'An endpoint that returns information about a business schedule for a specified date. Version of {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi} for access validation by security token.'
  -
    name: StaffList-StaffList
    description: 'Returns a list of all staff members working for a business.'
  -
    name: StaffView-StaffView
    description: 'Returns information about a specified staff member. This method can accept one staff key {@link StaffViewApi::$k_staff} or a staff list {@link StaffViewApi::$a_staff_list} but not both (an exception would be thrown).'
  -
    name: StaffView-StaffView74
    description: 'Returns information about a specified staff member. This method can accept one staff UID {@link StaffView74Api::$uid_staff} or a staff UID list {@link StaffView74Api::$a_uid_staff_list} but not both an exception would be thrown.'
  -
    name: Stage-LeadStageElement
    description: 'Creates, edits and deletes a Lead Stage.'
  -
    name: Stage-LeadStageList
    description: 'Retrieves a list of lead stages of a business.'
  -
    name: Stage-LeadStageUser
    description: 'Sets the lead stage of a client.'
  -
    name: Stand-Stand
    description: 'Prepare creation a new stand.'
  -
    name: Store-Store
    description: 'Manages the "Purchase Options" page of the booking wizard.'
  -
    name: Store-StoreGroup
    description: 'Performs preliminary checks and distribution of selected purchase options between the client who is currently logged in and his relatives.'
  -
    name: StripeCom-PaymentIntentUpdate
    description: 'Updates created Payment Intent. WARNING: Can be used for Stripe Payment Element only. Currently this API is used to update Payment Intent, that is created to show Stripe Payment Element form, in case of cart changes. To use for other cases, please consider extending code of this API.'
  -
    name: StripeCom-StripeComKey
    description: 'Get Stripe public key for a merchant.'
  -
    name: StripeCom-StripeComPayInit
    description: 'Creates not captured Payment Intent, returns its status and secret key to use on frontend.'
  -
    name: StripeCom-StripeComPaymentIntentCancel
    description: 'Cancels existing Payment Intent.'
  -
    name: Subscribe-Subscribe
    description: 'Manages a user`s subscription in a business.'
  -
    name: Subscription-SubscriptionInfo
    description: 'Entry point to retrieve information about business subscription.'
  -
    name: Tab-Tab
    description: 'Retrieves a list of tabs for bookable services specific to the user and the location. The names and contents of these tabs can be customized by the business. For example, a business may have tabs named ‘Appointments’, ‘Group Appointments’, and ‘Room Rentals’.'
  -
    name: Tag-SearchTagList
    description: 'Returns a list of all existing search tags in the system. Search tags can be used for quick searches between businesses and services (mainly in the directories).'
  -
    name: Tag-Tag
    description: 'Retrieves, changes, or deletes a video tag.'
  -
    name: Tag-TagList
    description: 'Retrieves video tags.'
  -
    name: Tax-Tax
    description: 'Returns tax information for a business.'
  -
    name: Tax-TaxList
    description: 'Retrieves a list of taxes for a business.'
  -
    name: Term-Term
    description: 'Displays information about online waiver.'
  -
    name: Timezone-ProfileTimezone
    description: 'Allows to get and update timezone in user''s profile.'
  -
    name: Token-TokenPin
    description: 'Creates a one-time PIN code. This code must be used for generating a security token.'
  -
    name: Type-BusinessTypeList
    description: 'Retrieves a list of business types.'
  -
    name: Type-LoginType
    description: 'Retrieves information about login types. A login type is basically a client type. This information can be used to filter clients by their client type in the All Clients Report.'
  -
    name: User-UserGroup
    description: 'API to see client''s groups and add/remove the client to the group.'
  -
    name: Video-VideoElement
    description: 'Returns a list of video categories.'
  -
    name: Video-VideoList
    description: 'Returns a list of videos.'
  -
    name: View-View
    description: 'Returns information on a specified location.'
  -
    name: Visit-VisitAdd
    description: 'An endpoint for adding a gym visit.'
  -
    name: Visit-VisitStatus
    description: 'Views or changes the visit status.'
  -
    name: Visit-VisitTotalCount
    description: 'Returns the total number of visits for a user at a business. Total is the sum of attended visits in {@link \RsVisitSql} and historical visits in {@link VisitHistoricalCountSql}.'
  -
    name: Waiver-Waiver
    description: 'Information about business waiver.'
  -
    name: Watch-Watch
    description: 'Saves information about a watched video.'
  -
    name: WebSocket-Subscribe
    description: 'Checks whether a listener can subscribe to specified channel. If yes, subscribes it.'
  -
    name: Widget-SkinWidgetList
    description: 'Lists widget skins grouped by widget type for a business.'
  -
    name: Widget-WidgetEdit
    description: 'Point to get data for "edit payment address" widget.'
  -
    name: Widget-WidgetSelect
    description: 'Point to get saved bank cards of a user or a business.'
  -
    name: WorkTime-LocationWorkTime
    description: 'Saves working hours for a specified location. Note that to run this endpoint, an authenticated user with specified permissions is required. Otherwise, use a guest account during the new business registration process (see {@link \Wl\Business\BusinessApi::sessionKey()}).'