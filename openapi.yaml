openapi: 3.1.0
info:
  title: 'WellnessLiving public API'
  version: 1.1.20260626171900
  description: |
    WellnessLiving API is a RESTful way to access and manage your business data.
    Last updated at: 2026-06-26 17:19:00
servers:
  - url: https://us.wellnessliving.com
    description: 'WellnessLiving server for businesses served from the United States data center'
  - url: https://au.wellnessliving.com
    description: 'WellnessLiving server for businesses served from the Australia data center'
paths:
  '/Thoth/PayHardware/HardwareStaffPreference.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_type:
                  type: [string, "null"]
                  description: 'Terminal type. One of {@link \Thoth\PayProcessor\Terminal\TerminalTypeSid} constants. `null` in case when not initialized yet or when `k_terminal` is set.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of business to save preferred terminal. `null` in case when not initialized yet.'
                k_location:
                  type: [string, "null"]
                  description: 'Key of the location to save preferred terminal. `null` in case when not initialized yet.'
                k_terminal:
                  type: [string, "null"]
                  description: 'Terminal key. `null` in case when not initialized yet or when `id_type` is set.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PayHardware-HardwareStaffPreference
  '/Studio/Build/BuildRevision.json':
    get:
      summary: ''
      parameters:
        - name: dtu_min
          in: query
          description: 'The minimum date/time of the revision in MySQL format.'
          schema:
            type: string
        - name: k_deploy_instance
          in: query
          description: 'Key of deployment instance. Primary key in {@link \StudioDeployInstanceSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_build_revision:
                        type: array
                        description: 'The list of build revision. <dl> <dt>string `dtu_date`</dt> <dd>Date/time of revision in Mysql format.</dd> <dt>int `i_build`</dt> <dd>The number of build.</dd> <dt>string `k_build`</dt> <dd>The key of build. Primary key in {@link \StudioBuildSql}.</dd> <dt>string `s_revision`</dt> <dd>Number of revision.</dd> </dl>'
      tags:
        - Build-BuildRevision
  '/Studio/Build/BuildAction.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_action:
                  type: number
                  format: int32
                  description: 'Action Id. One of {@link BuildActionSid} constants.'
                k_build:
                  type: string
                  description: 'Key of the build. Primary key from {@link \StudioBuildSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Build-BuildAction
  '/Studio/Tests/Event.json':
    get:
      summary: 'Gets information about specific test event.'
      parameters:
        - name: k_test_file
          in: query
          description: 'Test file key.'
          schema:
            type: string
        - name: k_test_run
          in: query
          description: 'Test run key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_log:
                        type: string
                        description: 'Prepared log of the test.'
      tags:
        - Tests-Event
  '/Studio/Task/TaskInfo.json':
    get:
      summary: 'Retrieves information about studio task.'
      parameters:
        - name: k_studio_task
          in: query
          description: 'Key of the studio task. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_studio_sprint:
                        type: [string, "null"]
                        description: 'Key of the studio sprint. Primary key in {@link \Studio\Sprint\SprintSql} table, `null` - if not set.'
                      text_jira_key_parent:
                        type: string
                        description: 'Key of the parent jira task connected to studio task.'
      tags:
        - Task-TaskInfo
  '/Studio/Sprint/Sprint.json':
    get:
      summary: ''
      parameters:
        - name: k_project
          in: query
          description: 'Key of the studio project. Primary key in {@link \StudioProjectSql} table, <tt>null</tt> - if not set.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Key of the studio task. Primary key in {@link \StudioTaskSql} table, <tt>null</tt> - if not set.'
          schema:
            type: [string, "null"]
        - name: k_task_parent
          in: query
          description: 'Key of the studio parent task. Primary key in {@link \StudioTaskSql} table, `null` - if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_sprint:
                        type: array
                        description: 'A list of options of sprint: <dl> <dt>bool `is_selected`</dt> <dd><tt>true</tt> - if this sprint will be selected in the drop-down list, <tt>false</tt> - otherwise.</dd> <dt>string `k_sprint`</dt> <dd>Sprint key. Primary key {@link \Studio\Sprint\SprintSql} table.</dd> <dt>string `text_title`</dt> <dd>Name of the sprint.</dd> </dl>'
      tags:
        - Sprint-Sprint
  '/Studio/Sprint/SprintElement.json':
    delete:
      summary: ''
      parameters:
        - name: k_sprint
          in: query
          description: 'Sprint key. Primary key in {@link \Studio\Sprint\SprintSql} table.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Sprint-SprintElement
    get:
      summary: ''
      parameters:
        - name: k_sprint
          in: query
          description: 'Sprint key. Primary key in {@link \Studio\Sprint\SprintSql} table.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_state:
                        type: number
                        format: int32
                        description: 'State of the sprint. One of {@link \Studio\Sprint\State\SprintStateSid} constants.'
                      k_project:
                        type: [string, "null"]
                        description: 'Project key. Primary key in {@link \StudioProjectSql} table.'
                      text_title:
                        type: string
                        description: 'Name of the sprint.'
      tags:
        - Sprint-SprintElement
    post:
      summary: ''
      parameters:
        - name: k_sprint
          in: query
          description: 'Sprint key. Primary key in {@link \Studio\Sprint\SprintSql} table.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_state:
                  type: number
                  format: int32
                  description: 'State of the sprint. One of {@link \Studio\Sprint\State\SprintStateSid} constants.'
                k_project:
                  type: [string, "null"]
                  description: 'Project key. Primary key in {@link \StudioProjectSql} table.'
                text_title:
                  type: string
                  description: 'Name of the sprint.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Sprint-SprintElement
  '/Studio/Sprint/SprintList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of studio sprints.'
      tags:
        - Sprint-SprintList
  '/Studio/Personnel/LoginCheck.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_login:
                        type: [string, "null"]
                        description: 'User login. `null` - user not logged in.'
                      s_password:
                        type: string
                        description: 'One time password.'
      tags:
        - Personnel-LoginCheck
  '/Studio/Personnel/Key.json':
    post:
      summary: 'Gets repository key for user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_login:
                  type: string
                  description: 'User login.'
                s_repository:
                  type: string
                  description: 'Repository name.'
                s_user_password:
                  type: string
                  description: 'One time password for user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_password:
                        type: string
                        description: 'Password for repository key.'
                      s_private:
                        type: string
                        description: 'Private repository key.'
      tags:
        - Personnel-Key
  '/Studio/Personnel/Password.json':
    put:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_password:
                  type: string
                  description: 'Temporary password.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Personnel-Password
  '/Studio/Personnel/Personnel.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_personnel:
                        type: array
                        description: 'A list of studio''s personnel, where key is key of the personnel and value is name of the personnel.'
      tags:
        - Personnel-Personnel
  '/Studio/SeleniumCluster/SeleniumCluster.json':
    get:
      summary: 'Gets a list of clusters and servers.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_cluster:
                        type: [array, "null"]
                        description: 'Information about clusters and count of reserved browsers for each cluster. <dl> <dt>int `i_server_count`</dt> <dd>Number of reserved browsers.</dd> <dt>string `text_testing_cluster`</dt> <dd>Test cluster identifier.</dd> </dl>'
                      a_server:
                        type: [array, "null"]
                        description: 'Information about servers. <dl> <dt>int `i_count_active`</dt> <dd>Number of active browsers.</dd> <dt>int `i_count_all`</dt> <dd>Number of active+passive browsers.</dd> <dt>int `i_count_failed`</dt> <dd>Number of failed browsers.</dd> <dt>int `i_count_ready`</dt> <dd>Number of passive browsers.</dd> <dt>int `i_count_starting`</dt> <dd>Number of starting browsers.</dd> <dt>int `id_state`</dt> <dd> Selenium instance state. One of the constants of the {@link \Core\Testing\Selenium\WebEmulator\WebEmulatorServerStateSid} class. </dd> <dt>string|null `ip_private`</dt> <dd> Private IP address of the Selenium server. <tt>null</tt> if <var>id_state</var> field is equal to {@link \Core\Testing\Selenium\WebEmulator\WebEmulatorServerStateSid::STARTING}. </dd> <dt>string|null `ip_public`</dt> <dd> Public IP address of the Selenium server. <tt>null</tt> if <var>id_state</var> field is equal to {@link \Core\Testing\Selenium\WebEmulator\WebEmulatorServerStateSid::STARTING}. </dd> <dt>string `k_web_emulator_server`</dt> <dd> Key of selenium server. Primary key in {@link \Core\Testing\Selenium\WebEmulator\WebEmulatorServerSql} table. </dd> <dt>string `s_server`</dt> <dd>The Amazon EC2 instance ID.</dd> <dt>string `text_state`</dt> <dd> Selenium instance state title. The title of one of the constants of the {@link \Core\Testing\Selenium\WebEmulator\WebEmulatorServerStateSid} class. </dd> </dl>'
      tags:
        - SeleniumCluster-SeleniumCluster
    post:
      summary: 'Actions with the servers.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_action:
                  type: number
                  format: int32
                  description: 'Server action id. One of {@link SeleniumClusterActionSid} constants.'
                is_auto_disable:
                  type: [boolean, "null"]
                  description: 'Is automatic server management disabled. When `true` - automatic server management will be disabled. When `false` - automatic server management will be enabled.'
                s_server:
                  type: [string, "null"]
                  description: 'The Amazon EC2 instance ID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - SeleniumCluster-SeleniumCluster
  '/Studio/Ip/IpGet.json':
    get:
      summary: 'Get current IP.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      ip_v4:
                        type: string
                        description: 'Current IPv4.'
                      ip_v6:
                        type: string
                        description: 'Current IPv6.'
      tags:
        - Ip-IpGet
  '/Studio/Ip/AccessAcquire.json':
    put:
      summary: 'Acquires access from current IP address of a staff member to specified service.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_ip_service:
                  type: string
                  description: 'Key of the service to manage. Primary key in {@link \Studio\Ip\Service\ServiceSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Ip-AccessAcquire
  '/Studio/Ip/AccessRevoke.json':
    put:
      summary: 'Revokes access from current IP address of a staff member to specified service.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_ip_service:
                  type: string
                  description: 'Key of the service to manage. Primary key in {@link \Studio\Ip\Service\ServiceSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Ip-AccessRevoke
  '/Studio/Ip/AccessType.json':
    put:
      summary: 'Saves information about access type of current staff member to specified service.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_access:
                  type: number
                  format: int32
                  description: 'Type of access of current staff member to specified service. One of {@link \Studio\Ip\AccessSid} constants.'
                k_ip_service:
                  type: string
                  description: 'Key of the service to manage.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Ip-AccessType
  '/Studio/Ip/Info.json':
    put:
      summary: 'Saves information about an IP address.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_timeout:
                  type: number
                  format: int32
                  description: 'Timeout of IP address. One of {@link \Studio\Ip\TimeoutSid} constants. <tt>null</tt> to leave this field unchanged.'
                is_office:
                  type: [boolean, "null"]
                  description: 'Whether this IP address is used in the office. <tt>null</tt> to leave this field unchanged.'
                text_comment:
                  type: [string, "null"]
                  description: 'Comment on IP address. <tt>null</tt> if comment should not be changed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Ip-Info
  '/Studio/Ip/PersonalSetting.json':
    put:
      summary: 'Update personal settings.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_favorite:
                  type: boolean
                  description: 'Whether the service is marked as favorite. `true` means marked as favorite, `false` otherwise.'
                k_ip_service:
                  type: [string, "null"]
                  description: 'The service for which the settings will be updated. Primary key in {@link Service\ServiceSql}. `null` if the service is not specified.'
                text_comment:
                  type: string
                  description: 'The comment on the service.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Ip-PersonalSetting
  '/Studio/Stand/Stand.json':
    delete:
      summary: 'Marks a stand as deleted. Renames the stand record with a removal timestamp suffix to prevent name conflicts, and sets the removal date. Called by the build server after stand servers are removed.'
      parameters:
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stand-Stand
    get:
      summary: 'Returns stand data by name. Retrieves stand status and deserialized stand data for the specified stand name. Verifies that the current user has access to the stand''s project.'
      parameters:
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_stand_data:
                        type: [array, string, "null"]
                        description: 'Information about entities necessary for the functioning of the stand. <i>Structure of an array is identical to the `z_stand_data` field of the {@link StandSql} table.</i> `string` if it needs to set an empty value. `null` will not change.'
                      id_stand_status:
                        type: [number, "null"]
                        description: 'Stand status, one of {@link StandStatusSid}. `null` if not initialized and will be ignored.'
      tags:
        - Stand-Stand
    post:
      summary: 'Registers a new stand and generates a URL for its creation. Validates the microservice environment configuration, creates a stand record, and returns a build URL to trigger stand creation.'
      parameters:
        - name: k_microservice_primary
          in: query
          description: 'Primary microservice for the stand. The main microservice from which the stand is created; other microservices can be added if they are compatible with the primary one, see {@link StandApi::$json_stand_environment}. `null` if value is not defined. If the information is not provided as a request parameter, the stand will not be created.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Task key within which a stand should be created. `null` if not defined, in this case the stand name must be specified manually in {@link StandApi::$s_stand}.'
          schema:
            type: [string, "null"]
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_stand_data:
                  type: [array, string, "null"]
                  description: 'Information about entities necessary for the functioning of the stand. <i>Structure of an array is identical to the `z_stand_data` field of the {@link StandSql} table.</i> `string` if it needs to set an empty value. `null` will not change.'
                json_stand_environment:
                  type: [string, "null"]
                  description: 'A microservice environment for create stand. Each element of the array contains `null` if the value is not defined. If the information is not provided as a request parameter, the stand will not be created. <dl> <dt>string `k_microservice`</dt> <dd>Microservice key. Primary key in {@link \Studio\Microservice\MicroserviceSql} table.</dd> <dt>string `s_environment`</dt> <dd>Environment name. See {@link \Studio\Microservice\MicroserviceSql}.`json_path`.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'Link to redirect for stand creation.'
      tags:
        - Stand-Stand
    put:
      summary: 'Updates stand status and stand data. Updates `id_stand_status` or `a_stand_data` for the specified stand. When the stand becomes ready and both data centers are configured, triggers builds on each.'
      parameters:
        - name: id_stand_status
          in: query
          description: 'Stand status, one of {@link StandStatusSid}. `null` if not initialized and will be ignored.'
          schema:
            type: [number, "null"]
        - name: s_stand
          in: query
          description: 'Stand name. If a task is specified when creating a stand (the {@link StandApi::$k_task} field), then the name of the new stand is determined from the branch of this task and is linked to this task. In all other cases, the stand name must be specified explicitly. `null` if the value is not defined. If the stand name is not specified, stand management is not possible. When creating a stand, the stand name can also be obtained from the task key {@link StandApi::$k_task}. If neither the stand name nor the task key is specified, the stand cannot be created.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_stand_data:
                  type: [array, string, "null"]
                  description: 'Information about entities necessary for the functioning of the stand. <i>Structure of an array is identical to the `z_stand_data` field of the {@link StandSql} table.</i> `string` if it needs to set an empty value. `null` will not change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Stand-Stand
  '/Core/All/Version.json':
    get:
      summary: 'Retrieves current application version number.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_version:
                        type: string
                        description: 'Current application version number.'
      tags:
        - All-Version
  '/Core/Prg/PrgKeyword.json':
    get:
      summary: ''
      parameters:
        - name: id_mode
          in: query
          description: 'Mode for load keyword. One of {@link \Core\Prg\PrgKeywordModeSid} constants.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_keyword:
                        type: array
                        description: 'List of keywords.'
      tags:
        - Prg-PrgKeyword
  '/Core/Request/RequestForward.json':
    get:
      summary: 'Forwards this API request to all datacenters. Multiple datacenters are not configured on testing servers.'
      parameters:
        - name: s_forward
          in: query
          description: 'Example string which may be used to see if data is correctly forwarded.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_forward:
                        type: array
                        description: 'Copy of {@link RequestForwardApi::$s_forward}. Key is datacenter ID. One of {@link AmazonRegionSid} constants. Values are copies of {@link RequestForwardApi::$s_forward} as seen on that datacenter. Value is not set if {@link RequestForwardApi::$s_forward} is `null`.'
                      a_region:
                        type: array
                        description: 'A list of datacenters to which the request was forwarded. One element is region ID, one of {@link AmazonRegionSid} constants.'
                      a_uid:
                        type: array
                        description: 'Keys users signed in. Key is datacenter ID. One of {@link AmazonRegionSid} constants. Values are copies of results of {@link passport_uid()} on that datacenter.'
      tags:
        - Request-RequestForward
  '/Core/Request/Example.json':
    get:
      summary: 'Returns the example result for debugging. A diagnostic endpoint used to verify that a given API connection is working correctly. Increments the input argument by one (confirming round-trip data flow), records a debug log entry with the current session and authentication context, and returns the log key so the caller can inspect it.'
      parameters:
        - name: i_argument
          in: query
          description: 'Example argument.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_result:
                        type: number
                        format: int32
                        description: 'Example result.'
                      s_log:
                        type: string
                        description: 'Key of the log record written by the API controller.'
      tags:
        - Request-Example
  '/Core/Passport/UidUuid.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_uid:
                  type: array
                  description: 'Indexed array of user UIDs.'
                a_uuid:
                  type: array
                  description: 'Indexed array of UUIDs.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid_uuid:
                        type: array
                        description: 'Associative array where key is UID (as string) and value is UUID (UUIDv8 string). Contains merged and converted data from both input arrays.'
      tags:
        - Passport-UidUuid
  '/Core/Studio/KeySecret.json':
    get:
      summary: 'Gets a secret key for signing.'
      parameters:
        - name: s_csrf
          in: query
          description: 'The CSRF code from the client side.'
          schema:
            type: string
        - name: s_key_session
          in: query
          description: 'The session key.'
          schema:
            type: string
        - name: url_origin
          in: query
          description: 'Origin for client requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_key_secret:
                        type: string
                        description: 'The secret key for the request signing.'
      tags:
        - Studio-KeySecret
  '/Core/Geo/GeoAddress.json':
    get:
      summary: 'Gets name of city and keys of GEO country and region.'
      parameters:
        - name: k_geo_city
          in: query
          description: 'Geo city key. Primary key in the {@link \AGeoSql} table.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_geo_country:
                        type: [string, "null"]
                        description: 'Geo country key. Primary key in the {@link \AGeoSql} table.'
                      k_geo_region:
                        type: [string, "null"]
                        description: 'Geo region key. Primary key in the {@link \AGeoSql} table.'
                      text_city:
                        type: [string, "null"]
                        description: 'Name of the city.'
      tags:
        - Geo-GeoAddress
  '/Core/Geo/Combobox.json':
    get:
      summary: 'Returns a list of cities to show in combobox list. Powers city autocomplete dropdowns in address forms. Returns up to 100 cities whose names start with the given fragment, each labelled as "City, Region, Country". Pass a locale to restrict results to a single country, or omit it to search worldwide.'
      parameters:
        - name: id_locale
          in: query
          description: 'The locale ID used as a filter. The locale is generally a country. One of the {@link LocaleSid} constants. This will be `0` if all locations are searched.'
          schema:
            type: number
            format: int32
        - name: s_value
          in: query
          description: 'The city name (or a fragment of the city name) used for the search.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of items to show in the combobox list. Each element of the array is a city matching the string with the following structure: <dl> <dt>string `s_value`</dt> <dd>The human-readable name of the city, including the state/province and country.</dd> <dt>string `s_key`</dt> <dd>The <var>k_city</var> value for the city.</dd> </dl>'
      tags:
        - Geo-Combobox
  '/Core/Geo/GeoTimezone.json':
    get:
      summary: 'Gets timezone by time offset.'
      parameters:
        - name: i_timezone_offset_minutes
          in: query
          description: 'Time offset in minutes.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_timezone:
                        type: [string, "null"]
                        description: 'Timezone key. <tt>null</tt> if can''t determine.'
      tags:
        - Geo-GeoTimezone
  '/Core/Geo/GeoPosition.json':
    get:
      summary: 'Gets current geo position.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_latitude:
                        type: number
                        format: float
                        description: 'Latitude coordinate.'
                      f_longitude:
                        type: number
                        format: float
                        description: 'Longitude coordinate.'
                      k_city:
                        type: string
                        description: 'City primary key in {@link \AGeoSql} table.'
      tags:
        - Geo-GeoPosition
  '/Core/Sid/CoreSid.json':
    get:
      summary: 'Returns the list of all items for the given Sid class. Populates enumeration dropdowns and lookup tables on the frontend. <i> Given any class that extends {@link CoreSid}, returns every defined constant with its numeric ID, string identifier, and human-readable title so the client does not need to hard-code enum values. </i>'
      parameters:
        - name: s_class_name
          in: query
          description: 'Name of the Sid class to get list from. Should be a fully qualified class name. <i>Class should be a child of {@link CoreSid}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of items. Keys are IDs, values are arrays with additional information: <dl> <dt>int `sid`</dt> <dd>String ID.</dd> <dt>string `text_title`</dt> <dd>Title of the ID.</dd> </dl>'
      tags:
        - Sid-CoreSid
  '/Core/WebSocket/Subscribe.json':
    post:
      summary: 'Checks whether a listener can subscribe to specified channel. Subscribes in a case of positive result. Called by the WebSocket client immediately after a connection is established, to register interest in a specific channel (identified by CID and key fields). If the current user is not permitted to receive events on that channel, the subscription is rejected. On success, any messages already queued for the channel are returned so the client does not miss events that arrived before the subscription was set up.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_key:
                  type: array
                  description: 'Keys of the channel. Key is name of a key field in a channel controller. Value is its value. Subscriber will only receive notifications about events in which all these values equal values specified here. Parameters for `60`: <dl> <dt>string `k_personnel`</dt> <dd>Key of a staff member.</dd> <dt>string `kip`</dt> <dd>Key of IP address from which this staff member accesses the system.</dd> </dl> Parameters for `1921`: <dl> <dt>int `id_visit`</dt> <dd>Visit status ID. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>string `k_business`</dt> <dd>Key of a business.</dd> <dt>string `uid`</dt> <dd>Key of the user.</dd> </dl> Parameters for `1583`: <dl> <dt>string `k_channel`</dt> <dd>Messenger channel key.</dd> </dl> Parameters for `1342`: <dl> <dt>string `k_business`</dt> <dd>Key of a business.</dd> <dt>string `uid_receiver`</dt> <dd>Key of the user.</dd> </dl> Parameters for `688`: <dl> <dt>string `dtu_class_period`</dt> <dd>Class schedule session date.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key.</dd> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `k_class_period`</dt> <dd>Class schedule key.</dd> <dt>string `k_visit`</dt> <dd>Visit key.</dd> <dt>string `s_secret`</dt> <dd>Visit secret key.</dd> </dl> Parameters for `1869`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid_receiver`</dt> <dd>User key who will receive message.</dd> </dl> Parameters for `735`: <dl> <dt>string|null `k_business`</dt> <dd>Key of a business to which report to subscribe. `null` for system-wide businesses.</dd> <dt>string `s_report`</dt> <dd> Key of a report to subscribe to. <i>Hex-encoded value of {@link \Thoth\ReportCore\Generator\ReportStorageListSql}.<tt>z_report</tt>.</i> </dd> </dl> Parameters for `1005`: <dl> <dt>string|null `k_microservice`</dt> <dd>Microservice key. `null` if the value is not set</dd> </dl> Parameters for `1105`: <dl> <dt>string `uid`</dt> <dd>Current user key.</dd> </dl> Parameters for `19`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1850`: <dl> <dt>int `id_import_custom`</dt> <dd>ID of custom import process. One of {@link \Wl\Import\Custom\CustomSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key within which import is progressing.</dd> </dl> Parameters for `298`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `305`: <dl> <dt>string `k_location`</dt> <dd>Location where session booked or cancelled.</dd> </dl> Parameters for `1660`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> Parameters for `510`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> Parameters for `842`: <dl> <dt>string `k_business`</dt> <dd>Business in which the report was generated.</dd> <dt>string `uid`</dt> <dd>User who requested the report generation.</dd> </dl> Parameters for `1543`: <dl> <dt>int `id_report`</dt> <dd>ID of the report.</dd> <dt>string `k_business`</dt> <dd>Business key in which the report was monitoring.</dd> </dl> Parameters for `1163`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid_actor`</dt> <dd>Key of the actor.</dd> </dl> Parameters for `16`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `977`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid`</dt> <dd>UID of the client for which failed to send SMS.</dd> </dl> Parameters for `677`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1834`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1820`: <dl> <dt>string `k_business`</dt> <dd>Key of a business.</dd> </dl> Parameters for `1657`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> </dl> Parameters for `1421`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant.</dd> <dt>string `k_pay_transaction`</dt> <dd>Pay transaction key, which was used to create ticket.</dd> <dt>string `s_ticket_id`</dt> <dd>Gateway Ticket ID.</dd> </dl> Parameters for `523`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> Parameters for `960`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid_actor`</dt> <dd>Key of the actor. <tt>admin</tt> - for WebSocket channel for which admins subscribed.</dd> </dl> Parameters for `41`: <dl> <dt>string `k_location`</dt> <dd>Location key.</dd> <dt>string `uid`</dt> <dd>User-receiver of response.</dd> </dl> Parameters for `34`: <dl> <dt>string `uid_response`</dt> <dd>Key of user who can grant access to location.</dd> </dl> Parameters for `57`: <dl> <dt>string `k_location`</dt> <dd>Key of location access to which was requested.</dd> <dt>string `uid_request`</dt> <dd>Key of user who requested access to location.</dd> </dl> Parameters for `828`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `s_id`</dt> <dd>Key of the task where addresses should be verified.</dd> </dl> Parameters for `1736`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant.</dd> <dt>string `k_pay_transaction`</dt> <dd>Pay transaction key, which was used to create ticket.</dd> <dt>string `s_exchange_identification`</dt> <dd>Gateway exchange identification.</dd> </dl> Parameters for `549`: <dl> <dt>string `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `952`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid`</dt> <dd>UID of the client.</dd> </dl> Parameters for `963`: <dl> <dt>string `k_business`</dt> <dd>Key of the business.</dd> <dt>string `uid_receiver`</dt> <dd>Key of the user.</dd> </dl>'
                cid_channel:
                  type: number
                  format: int32
                  description: 'Channel controller CID.'
                s_password:
                  type: string
                  description: 'Subscriber password.'
                s_token:
                  type: string
                  description: 'Request token. This token is only used for asynchronous functions, and identifies a specific request.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_message_broadcast:
                        type: array
                        description: 'All messages in queue. Key is a message key. Value is message data. When a client is initially subscribed for a WebSocket channel, entire message queue is sent to the client. This is needed to send message that were generated before a client has subscribed. Parameters for `60`: <dl> <dt>string|null `dt_remove`</dt> <dd>Date/time when access to specified service will be revoked. <tt>null</tt> if access is revoked.</dd> <dt>string `k_ip_service`</dt> <dd>Key of a service that is changed.</dd> </dl> Parameters for `1921`: <dl> <dt>string `k_visit`</dt> <dd>Key of a book/visit.</dd> </dl> Parameters for `1583`: <dl> <dt>array `a_data`</dt> <dd> New information for messenger. <dl> <dt>array `message`</dt> <dd> Message information: <dl> <dt>int `id`</dt> <dd>Message key.</dd> <dt>string `message`</dt> <dd>Message text.</dd> <dt>string `updated_at`</dt> <dd>Date/time when the message has been posted.</dd> </dl> </dd> <dt>array `user_profile`</dt> <dd> User''s information: <dl> <dt>int `id_gender`</dt> <dd>User gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>string `s_first_name`</dt> <dd>User first name.</dd> <dt>string `s_last_name`</dt> <dd>User last name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_photo`</dt> <dd>URL of the user''s profile photo.</dd> </dl> </dd> </dl> </dd> </dl> Parameters for `1342`: <dl> <dt>int `i_unread_alert`</dt> <dd>The count with unread alert for the user.</dd> <dt>bool `is_active`</dt> <dd>Whether message is active. This is needed to avoid showing previous messages on page reload.</dd> <dt>bool `is_message`</dt> <dd>Whether this is an alert for an SMS message</dd> <dt>bool `is_sound`</dt> <dd>Whether the user should hear a sound when a notification is sent.</dd> <dt>string|null `k_alert`</dt> <dd>Key of the alert that was just sent. <tt>null</tt> means alert was read.</dd> </dl> Parameters for `688`: <dl> <dt>string `url_redirect`</dt> <dd>Url link to redirect to join virtual meeting.</dd> </dl> Parameters for `733`: <dl> <dt>string|null `k_business`</dt> <dd>Business key.</dd> </dl> Parameters for `1869`: <dl> <dt>int `i_overdue`</dt> <dd>Number of overdue tasks.</dd> <dt>int `id_task_status`</dt> <dd>New task status of the changed task. One of {@link \Wl\Task\TaskStatusSid}.</dd> <dt>string `k_task`</dt> <dd>Key of the changed task.</dd> </dl> Parameters for `735`: <dl> <dt>int `i_cas_change`</dt> <dd> A CAS (compare-and-swap) number that allows to track changes in the report storage. This number is changed every time content of the report gets updated. If this number is not changed, the content is not updated. <i>Copy of {@link \Thoth\ReportCore\Generator\ReportStorageListSql}.<tt>i_cas_change</tt>.</i> </dd> <dt>int `id_report_status`</dt> <dd>Status of this report. One of {@link \Thoth\ReportCore\Generator\ReportGeneratorStatusSid} constants.</dd> </dl> Parameters for `1005`: <dl> <dt>string[] `a_op`</dt> <dd> Names of operation that were executed. Operation names are defined by <tt>OP_*</tt> constants of {@link \Studio\Sprint\Promoting\PromotingChannel}. </dd> <dt>string|null `s_step`</dt> <dd>Name of the step that was set. `null` if step has not changed.</dd> <dt>string|null `text_log`</dt> <dd> A new log item was added during execution of a script. Text of the log item that was just added. `null` if this message does not contain information about newly added log item. </dd> </dl> Parameters for `1105`: <dl> <dt>string `s_code`</dt> <dd>Code for authorization.</dd> <dt>int `text_time`</dt> <dd>Unique string for get code.</dd> </dl> Parameters for `19`: <dl> <dt>string `text_title`</dt> <dd>New title of the business.</dd> </dl> Parameters for `1850`: <dl> <dt>int `i_complete`</dt> <dd>Number of successfully processed items.</dd> <dt>int `i_fail`</dt> <dd>Number of failed items.</dd> <dt>int `i_left`</dt> <dd>Number of items left to process.</dd> <dt>string `k_import`</dt> <dd>Key of the import being processing.</dd> <dt>string `url_log`</dt> <dd>Url link to download log file with import progress.</dd> </dl> Parameters for `298`: <dl> <dt>string `dt_end_local`</dt> <dd>End of change interval. Empty string if change interval has no end.</dd> <dt>string `dt_start_local`</dt> <dd>Start of change interval.</dd> <dt>string|null `k_class_tab_new`</dt> <dd> New "Book now" tab primary key in {@link \Wl\Classes\Tab\TabSid} table. <tt>null</tt> means system default tab. </dd> <dt>string|null `k_class_tab_old`</dt> <dd> Old "Book now" tab primary key in {@link \Wl\Classes\Tab\TabSid} table. <tt>null</tt> means system default tab. </dd> </dl> Parameters for `305`: <dl> <dt>string `dtl_end`</dt> <dd>Session end date/time.</dd> <dt>string `dtl_start`</dt> <dd>Session start date/time.</dd> <dt>string|null `k_appointment`</dt> <dd> Key of the changed appointment. Is set only if changed session is appointment. `null` if changed session is not an appointment. </dd> <dt>string|null `k_class_period`</dt> <dd> Key of the changed class session. Is set only if changed session is class. `null` if changed session is not a class. </dd> </dl> Parameters for `510`: <dl> <dt>array `a_visit`</dt> <dd> New data of changes schedule item: <dl> <dt>string `dtl_end`</dt> <dd>Date/time of end.</dd> <dt>string `dtl_start`</dt> <dd>Date/time of start.</dd> <dt>bool `is_cancel`</dt> <dd><tt>true</tt> if book was canceled; <tt>false</tt> otherwise.</dd> <dt>string `s_key`</dt> <dd>Book key. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of location.</dd> <dt>string `text_location`</dt> <dd>Title of location.</dd> <dt>string `text_note`</dt> <dd>Description of event.</dd> <dt>string `text_title`</dt> <dd>Title of event.</dd> <dt>string `url`</dt> <dd>URL of virtual service. Empty if the visit is not virtual.</dd> </dl> </dd> </dl> Parameters for `842`: <dl> <dt>int `i_generation`</dt> <dd>Duration of the generation of the report in seconds.</dd> <dt>int `id_report`</dt> <dd>ID of the report that was generated. One of the {@link \RsReportSid} constants.</dd> <dt>bool `is_need_show`</dt> <dd>Whether need to display a message about report generation, regardless of the generation time.</dd> <dt>string `k_report_accumulation`</dt> <dd>Report accumulation.</dd> <dt>string `text_report`</dt> <dd>Title of the report that was generated.</dd> </dl> Parameters for `1163`: <dl> <dt>bool `is_active`</dt> <dd>Whether message is active. This is needed to avoid showing previous messages on page reload.</dd> <dt>bool|null `is_booking_in_progress`</dt> <dd> Whether booking is in progress. <i>This property will contain value from {@link \Wl\Book\Multiple\MultipleTaskSession::verify()} method, can be null.</i> </dd> </dl> Parameters for `16`: <dl> <dt>string `text_title`</dt> <dd>New title of the business.</dd> </dl> Parameters for `977`: <dl> <dt>string `html_error_message`</dt> <dd>The text of the error.</dd> <dt>string `s_key`</dt> <dd>The unique key of the sent SMS.</dd> <dt>int `tl_send`</dt> <dd>The time in UNIX format when socket message was sent.</dd> </dl> Parameters for `677`: <dl> <dt>int `id_mail_verify_status`</dt> <dd> Status of mail verification. One of the {@link \Wl\Mail\Verify\MailVerifyStatusSid} constants. Or {@link \Wl\Business\Config\Option\MailVerifyStatusOption::DEFAULT_VALUE} if option "Enable Custom Reply To Emails" in Business -> Feature is disabled. </dd> <dt>string `text_email`</dt> <dd>Email address to check.</dd> </dl> Parameters for `1834`: <dl> <dt>array `a_domain_token`</dt> <dd> List of email domain tokens: Empty array if the business does not have a mail domain set up. <dl> <dt>int `id_status`</dt> <dd>Status of the token. One of {@link \Wl\Mail\Domain\DomainVerifyStatusSid} constants.</dd> <dt>string `text_host`</dt> <dd>Host part of the token, which should be added to the DNS records.</dd> <dt>string `text_icon_class`</dt> <dd>CSS class for the icon representing the token status.</dd> <dt>string `text_status`</dt> <dd>Text representation of the token status.</dd> <dt>string `text_type`</dt> <dd>Type of the token. One of {@link \Wl\Mail\Domain\DomainTokenTypeSid} constants in text representation.</dd> <dt>string `text_value`</dt> <dd>Value of the token, which should be added to the DNS records.</dd> </dl> </dd> <dt>int|null `id_domain_status`</dt> <dd>Email domain status. `null` if the business does not have a mail domain set up.</dd> <dt>string `text_domain_status_icon`</dt> <dd> CSS class for the icon representing the email domain status. Empty string if the business does not have a mail domain set up. </dd> </dl> Parameters for `1820`: <dl> <dt>string|null `k_login_type`</dt> <dd>Key of the client type. `null` if client type is not set.</dd> <dt>string `uid`</dt> <dd>Key of the user.</dd> </dl> Parameters for `1421`: <dl> <dt>int|null `id_card_system`</dt> <dd> Card type ID. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>int|null `id_pay_exception`</dt> <dd>Transaction exception ID. `null` if no errors occurred.</dd> <dt>int|null `id_ticket_status`</dt> <dd>Ticket status. `null` if not set yet.</dd> <dt>string `m_tip`</dt> <dd>Amount of tips entered by customer on terminal.</dd> <dt>string|null `s_card_holder`</dt> <dd> Cardholder name. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>string|null `s_expire`</dt> <dd> Card expiration date. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>string|null `s_last4`</dt> <dd> Last four digits of the card number. Not `null` when {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusChannel::$id_ticket_status} is equal {@link \Thoth\PayProcessor\DirectConnect\Ticket\DirectConnectTicketStatusSid::COMPLETE}. </dd> <dt>string|null `text_message`</dt> <dd>Text message to notify the client. `null` until initialized.</dd> </dl> Parameters for `523`: <dl> <dt>array `a_visit`</dt> <dd> New data of changes schedule item: <dl> <dt>string `dtl_end`</dt> <dd>Date/time of end.</dd> <dt>string `dtl_start`</dt> <dd>Date/time of start.</dd> <dt>bool `is_cancel`</dt> <dd><tt>true</tt> if book was canceled; <tt>false</tt> otherwise.</dd> <dt>string `s_key`</dt> <dd>Book key. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>string `text_location`</dt> <dd>Title of location.</dd> <dt>string `text_note`</dt> <dd>Description of event.</dd> <dt>string `text_title`</dt> <dd>Title of event.</dd> <dt>string `url`</dt> <dd>URL of virtual service. Empty if the visit is not virtual.</dd> </dl> </dd> </dl> Parameters for `960`: <dl> <dt>int `i_unread_sms`</dt> <dd>The count with unread SMS in the business.</dd> </dl> Parameters for `41`: <dl> <dt>bool `is_grant`</dt> <dd><tt>true</tt> - access is granted; <tt>false</tt> - denied.</dd> <dt>string `text_full_name`</dt> <dd>Full name of user-receiver of response.</dd> <dt>float `tu_send`</dt> <dd>The time in seconds with fractional part in UNIX format when socket message was sent.</dd> </dl> Parameters for `34`: <dl> <dt>bool `is_close`</dt> <dd> `true` if the request has been already processed (question window must not be shown); `false` otherwise (question window must be shown). </dd> <dt>string `k_location`</dt> <dd>Location key.</dd> <dt>string `text_full_name`</dt> <dd>Name of the user requesting access.</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `uid_request`</dt> <dd>Key of user who request access to location.</dd> </dl> Parameters for `57`: <dl> <dt>bool `is_inactive`</dt> <dd> `true` if a new assess request has been sent (question window must be not closed); `false` otherwise (question window must be closed). </dd> </dl> Parameters for `828`: <dl> <dt>array `a_invalid`</dt> <dd> List of users for which address verification failed. <dl> <dt>array `a_address`</dt> <dd> Address information. <i>See {@link \Core\Geo\AddressEntity::toArray()} for details.</i> <dl> <dt>string `text_address`</dt> <dd>Address string.</dd> <dt>string `text_city`</dt> <dd>City title.</dd> <dt>string `text_country`</dt> <dd>Country title.</dd> <dt>string `text_postal`</dt> <dd>Postal address.</dd> <dt>string `text_state`</dt> <dd>State.</dd> </dl> </dd> <dt>bool `is_address`</dt> <dd>Whether address string is specified.</dd> <dt>bool `is_checked`</dt> <dd>`true` if address check, `false` otherwise.</dd> <dt>string `text_email`</dt> <dd>User email address.</dd> <dt>string `text_name`</dt> <dd>User name.</dd> <dt>string `text_phone`</dt> <dd>User phone number.</dd> <dt>string `text_note`</dt> <dd>Note.</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url`</dt> <dd>Link to user profile.</dd> <dt>string `url_image`</dt> <dd>Link to user image.</dd> </dl> </dd> <dt>array `a_valid`</dt> <dd> List of users for which address verification succeed. <dl> <dt>array `a_address`</dt> <dd> Address information. <i>See {@link \Core\Geo\AddressEntity::toArray()} for details.</i> <dl> <dt>string `text_address`</dt> <dd>Address string.</dd> <dt>string `text_city`</dt> <dd>City title.</dd> <dt>string `text_country`</dt> <dd>Country title.</dd> <dt>string `text_postal`</dt> <dd>Postal address.</dd> <dt>string `text_state`</dt> <dd>State.</dd> </dl> </dd> <dt>bool `is_address`</dt> <dd>Whether address string is specified.</dd> <dt>bool `is_checked`</dt> <dd>`true` if address check, `false` otherwise.</dd> <dt>string `text_email`</dt> <dd>User email address.</dd> <dt>string `text_name`</dt> <dd>User name.</dd> <dt>string `text_phone`</dt> <dd>User phone number.</dd> <dt>string `text_note`</dt> <dd>Note.</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url`</dt> <dd>Link to user profile.</dd> <dt>string `url_image`</dt> <dd>Link to user image.</dd> </dl> </dd> </dl> Parameters for `1736`: <dl> <dt>int|null `id_card_system`</dt> <dd> Card type ID. Not `null` when {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\PaymentStatusChannel::$id_payment_status} is equal {@link \Thoth\PayProcessor\Nuvei\Code\CSResponseSid::APPROVED}. </dd> <dt>int|null `id_card_type`</dt> <dd> Card type ID. `null` If were unable to determine the card type. See {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\Entity\OCservice\PaymentResponseEntity::$id_card_type}. </dd> <dt>int|null `id_pay_exception`</dt> <dd>Transaction exception ID. `null` if no errors occurred.</dd> <dt>int|null `id_payment_status`</dt> <dd>Payment status. `null` if not set yet.</dd> <dt>string|null `m_tip`</dt> <dd>Tip amount entered by the customer at the terminal. `null` if not initialized yet.</dd> <dt>string|null `s_last4`</dt> <dd> Last four digits of the card number. Not `null` when {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\PaymentStatusChannel::$id_payment_status} is equal {@link \Thoth\PayProcessor\Nuvei\Code\CSResponseSid::APPROVED}. </dd> <dt>string|null `text_card_holder`</dt> <dd> Cardholder name. Not `null` when {@link \Thoth\PayProcessor\Nuvei\Terminal\OMNIChannelApi\PaymentStatusChannel::$id_payment_status} is equal {@link \Thoth\PayProcessor\Nuvei\Code\CSResponseSid::APPROVED}. </dd> <dt>string|null `text_message`</dt> <dd>Text message to notify the client. `null` until initialized.</dd> </dl> Parameters for `549`: <dl> <dt>float `f_progress`</dt> <dd>Accumulation process progress.</dd> </dl> Parameters for `952`: <dl> <dt>array `a_info`</dt> <dd> An array with information about SMS: <dl> <dt>string `dtl_message`</dt> <dd>Date and time, when SMS messages were sent, in MySQL format.</dd> <dt>bool `is_automated`</dt> <dd><tt>true</tt> if SMS was sent from task, <tt>false</tt> - if the staff member sent SMS.</dd> <dt>bool `is_outbound`</dt> <dd><tt>true</tt> if staff member sent SMS, <tt>false</tt> - if the client sent the SMS.</dd> <dt>string `k_sms_history`</dt> <dd>The key of SMS history. <i>Primary key in {@link \ASmsHistorySql} table.</i></dd> <dt>string `sid_sms_status`</dt> <dd>Sid of the SMS status.</dd> <dt>string `text_date`</dt> <dd>The day when SMS messages were sent.</dd> <dt>string `text_sender_name`</dt> <dd>The sender''s name.</dd> <dt>string `text_sms_body`</dt> <dd>The body of the SMS.</dd> <dt>string `text_sms_status`</dt> <dd>The title of the SMS status.</dd> <dt>string `text_sms_type`</dt> <dd>The title of the mail type.</dd> <dt>string `text_time`</dt> <dd>The time when SMS was sent.</dd> <dt>string|null `uid_sender`</dt> <dd>The UID of the sender, <tt>null</tt> - if SMS sent from task.</dd> </dl> </dd> <dt>string `s_key`</dt> <dd> The unique key of sent SMS from 2-way SMS chat. The key need to update SMS information in the chat after sent SMS to client. </dd> </dl> Parameters for `963`: <dl> <dt>array `a_info`</dt> <dd> An array with information about dialog: <dl> <dt>string `dtl_last_activity`</dt> <dd>The date and time of the last SMS in the dialog.</dd> <dt>int `i_unread_sms`</dt> <dd>A count of unread messages.</dd> <dt>bool `is_inbound`</dt> <dd>Whether the last SMS is inbound or not.</dd> <dt>string `text_last_sms`</dt> <dd>The text of last sms.</dd> <dt>string `uid`</dt> <dd>The UID of the client. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>int `tl_send`</dt> <dd>The time in UNIX format when socket message was sent.</dd> </dl>'
      tags:
        - WebSocket-Subscribe
  '/Core/Push/Push.json':
    post:
      summary: 'Adds new device ID for push notifications. Does nothing if user {@link PushApi::$uid} already has devise ID {@link PushApi::$s_id}.'
      parameters:
        - name: s_application_id
          in: query
          description: 'Application ID. For example ''com.wellnessliving.com''.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'Device ID.'
          schema:
            type: string
        - name: sid_system
          in: query
          description: 'OS name. String version of {@link \APushOsSid} constants.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Push-Push
  '/Core/Captcha/CaptchaRequire.json':
    get:
      summary: 'Checks if a CAPTCHA is required for the given captcha type. Used before rendering a form that may include a CAPTCHA widget. The caller supplies the captcha type (identified by CID) and any constructor arguments it needs; the response tells the frontend whether to show the challenge at all and which reCAPTCHA version is active.'
      parameters:
        - name: a_arguments
          in: query
          description: 'Arguments for creating CAPTCHA object. <i>Structure can be missing in case when no configured captcha in environment.</i> Parameters for `1064`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if system business.</dd> </dl> Parameters for `1066`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if is system business.</dd> </dl> Parameters for `1072`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if is system business.</dd> </dl> Parameters for `1139`: <dl> <dt>string|null `k_business`</dt> <dd>Business key. `null` if is system business.</dd> </dl> Parameters for `1508`: <dl> <dt>bool `is_search`</dt> <dd> Flag to determine is it a search action or no. If search action is `true` it increments {@link \Wl\Business\Search\BusinessSearchCaptcha::$ip_actor}. Otherwise {@link \Wl\Business\Search\BusinessSearchCaptcha::$s_back_action}. </dd> </dl>'
          schema:
            type: array
        - name: cid_captcha
          in: query
          description: 'The CID of the CAPTCHA.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_enable_v3:
                        type: boolean
                        description: '`true` if enabled V3 captcha enabled. `false` if only V2 captcha enable.'
                      is_require:
                        type: boolean
                        description: 'This will be `true` if a CAPTCHA is required. Otherwise, this will be `false`.'
      tags:
        - Captcha-CaptchaRequire
  '/Core/Debug/Error.json':
    post:
      summary: 'Logs new error occurred in browser.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_error_list:
                  type: [array, string]
                  description: 'List of errors. For structure see {@link Core_Debug_ErrorModel.a_error_list}. JSON-encoded array mary arrive as a `string`.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Debug-Error
  '/Core/Google/GoogleCoordinate.json':
    get:
      summary: 'Gets geo coordinates from address using Google Maps.'
      parameters:
        - name: s_address
          in: query
          description: 'Address to get geo position for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_latitude:
                        type: number
                        format: float
                        description: 'Latitude coordinate.'
                      f_longitude:
                        type: number
                        format: float
                        description: 'Longitude coordinate.'
      tags:
        - Google-GoogleCoordinate
  '/Core/Google/LoginLink.json':
    get:
      summary: 'Returns a link for a user to sign in with Google+.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'A link for a user to sign in with Google+.'
      tags:
        - Google-LoginLink
  '/Core/Google/GoogleCaptcha.json':
    put:
      summary: 'Saves the user CAPTCHA token for the current session. Accepts the CAPTCHA version, the action name, and the user token obtained from the Google reCAPTCHA widget, and stores them in the session so that subsequent API requests requiring CAPTCHA verification can use them.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_version:
                  type: number
                  format: int32
                  description: 'Captcha version ID.'
                text_action:
                  type: string
                  description: 'The action name. Used to determine the place where the CAPTCHA is needed in documentation for endpoints that used the CAPTCHA.'
                text_token:
                  type: string
                  description: 'The user token CAPTCHA.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Google-GoogleCaptcha
  '/Core/Cms/TranslateState.json':
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Cms-TranslateState
  '/Social/Microsoft/Login.json':
    delete:
      summary: 'Removes the association between a website client and a Microsoft account. Accepts the user''s UID, verifies that the caller is the account owner, and unlinks the Microsoft account from the user''s profile.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Microsoft-Login
    get:
      summary: 'Collects data for the Microsoft login button. Called when rendering the "Sign in with Microsoft" button. Generates the OAuth 2.0 authorization URL the button must link to. When a UID is provided, also reports whether that user already has a Microsoft account linked, so the frontend can show "Link" or "Unlink" instead of the default sign-in label.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exists:
                        type: boolean
                        description: 'If `true`, the user has a bound Microsoft account. Otherwise, this will be `false`.'
                      url_login:
                        type: string
                        description: 'The Microsoft OAuth 2.0 authorization link.'
      tags:
        - Microsoft-Login
    post:
      summary: 'Signs a user in with Microsoft. Accepts the Microsoft authorization code, an optional state parameter for CSRF verification, and an optional redirect URI. Validates the state, exchanges the code for user identity, and signs the user in or creates a new account.'
      parameters:
        - name: is_external
          in: query
          description: 'If authorization is performed in a third-party application, set this flag in case of authorization errors.'
          schema:
            type: boolean
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_code:
                  type: string
                  description: 'The authorization code that the app requested.'
                s_state:
                  type: string
                  description: 'If a state parameter is included in the request, the same value should appear in the response. The app should verify that the state values in the request and response are identical.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Microsoft-Login
  '/Wl/Visit/VisitStatus.json':
    get:
      summary: 'Gets visit status. Returns full details for the specified visit, including date, duration, staff, location, virtual service join URL, assigned resources, downloadable calendar data, and whether the visit can still be cancelled. Handles class, appointment, and gym visit types. Used to render the visit detail view and action buttons in the client portal.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'The time zone key. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: k_visit
          in: query
          description: 'The visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_cancel:
                        type: array
                        description: 'Information about whether the given user can cancel an online booking and what consequences the cancellation would have: <dl> <dt>array|null `a_penalty`</dt> <dd> `null` if penalty must be not applied. <dl> <dt>bool `is_flat`</dt> <dd>`true` in a case of flat penalty type; `false` in a case of percentage penalty type.</dd> <dt>string `k_currency`</dt> <dd>Currency <i>primary key in {@link \RsCurrencySql} table</i>.</dd> <dt>string `m_amount`</dt> <dd>Penalty amount.</dd> </dl> </dd> <dt>bool `can_cancel`</dt> <dd>`true` if the booking can be canceled online by the specified user, `false` otherwise.</dd> <dt>bool `is_flag`</dt> <dd>`true` if the client''s account will be flagged instead of charging a monetary fee, `false` otherwise.</dd> <dt>bool `is_late`</dt> <dd>`true` if the cancellation would be considered a late cancel, `false` otherwise.</dd> <dt>bool `is_refund`</dt> <dd> `true` if the visit credit (from the purchase option used to book) will be returned to the user''s profile after cancellation, `false` otherwise. </dd> </dl>'
                      a_resource:
                        type: [array, "null"]
                        description: 'An array of service resources. The key refers to the `k_resource_type`. <i>See {@link \RsResourceTypeSql}.</i> The value is an array with the following key: `k_resource`. <i>See {@link \RsResourceSql}</i>. The array element contains a nested array with `i_index` and `i_quantity`. <i>See {@link \RsResourceBusySql}</i>. This will be empty if not set yet.'
                      a_resource_alias:
                        type: [array, "null"]
                        description: 'An array of service resources. Contains an extended data set, as well as a different format than {@link VisitStatusApi::$a_resource}. Each element contains the following set of data: <dl> <dt>string `k_resource`</dt> <dd>Resource <i>primary key in {@link \RsResourceSql} table</i>.</dd> <dt>string `k_resource_type`</dt> <dd>Resource type <i>primary key in {@link \RsResourceTypeSql} table</i>.</dd> <dt>int `i_index`</dt> <dd>Index of the resource on the layout.</dd> <dt>int `i_quantity`</dt> <dd>Quantity of the resource on the layout.</dd> <dt>string `text_alias`</dt> <dd>Resource''s custom name (alias) on the layout.</dd> <dt>string `text_title`</dt> <dd>Resource''s title.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'The list of keys of staff members that conduct the class.'
                      a_uid_staff:
                        type: array
                        description: 'The list of user IDs of staff members that conduct the class.'
                      dt_date:
                        type: string
                        description: 'The visit date and time in UTC and in MySQL format.'
                      dtl_date:
                        type: string
                        description: 'The visit date in the location''s time zone and in MySQL format.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The service duration (in minutes).'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'The client''s place in a waiting list.'
                      id_mode:
                        type: number
                        format: int32
                        description: 'The source of the visit or the visit change. One of the {@link ModeSid} constants. If you''re unsure about the value to use, keep the default value.'
                      id_visit:
                        type: number
                        format: int32
                        description: 'The status of the visit. One of the {@link VisitSid} constants.'
                      is_event:
                        type: boolean
                        description: 'Determines whether the visit is from an event.'
                      is_request:
                        type: boolean
                        description: 'Whether this visit is requested and requires staff confirmation. * `true` - visit is requested. * `false` - visit is confirmed or denied or this is a system request.'
                      k_class:
                        type: string
                        description: 'The class key.'
                      k_class_period:
                        type: string
                        description: 'The class period key.'
                      k_location:
                        type: string
                        description: 'The key of the location where visit provides.'
                      k_service:
                        type: [string, "null"]
                        description: 'The service key. If ''null'', the visit isn''t from an appointment.'
                      k_staff:
                        type: [string, "null"]
                        description: 'The key of the staff providing the appointment. If `null`, the visit isn''t from an appointment (for example, the visit is from an asset).'
                      s_calendar_file_content:
                        type: string
                        description: 'The .ics file for adding the service to a phone calendar.'
                      text_abbr_timezone:
                        type: string
                        description: 'The text abbreviation of the time zone.'
                      text_location:
                        type: string
                        description: 'The full address of the location for the visit (not the name of the location).'
                      text_staff:
                        type: string
                        description: 'The full name of the staff member who conducts this visit. If there are several staff members conducting the visit, their names will all be listed and separated by commas.'
                      text_title:
                        type: string
                        description: 'The service title.'
                      uid:
                        type: [string, "null"]
                        description: 'User key who made a visit. `null` for a guest visit.'
                      uid_staff:
                        type: [string, "null"]
                        description: 'The ID of the user who is the staff providing the appointment. If `null`, the visit isn''t from an appointment (for example, the visit is from an asset).'
                      url_book_referral:
                        type: [string, "null"]
                        description: 'The direct link to start class/event booking on the WellnessLiving website. `null` for appointments/events/gym visits.'
                      url_book_referral_short:
                        type: [string, "null"]
                        description: 'The shortened direct link to start class/event booking on the WellnessLiving website. `null` for appointments/events/gym visits.'
                      url_virtual_service:
                        type: string
                        description: 'URL of virtual service. Empty if the visit is not virtual.'
      tags:
        - Visit-VisitStatus
    post:
      summary: 'Changes visit status. Applies a status transition to the specified visit (for example, check-in, no-show, or cancellation). Supports optimistic concurrency via an expected-from status, optional late-cancel fee charging, and client notification flags. Requires backend access or appropriate staff privileges.'
      parameters:
        - name: is_charge_fee
          in: query
          description: 'The staff decision to charge (or not charge) a penalty when a client meets late cancel/no-show requirements. If `true`, a late cancel fee should be charged. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'The email pattern key. If `null`, the live email pattern shouldn''t be used.'
          schema:
            type: [string, "null"]
        - name: k_visit
          in: query
          description: 'The visit key.'
          schema:
            type: string
        - name: text_reason
          in: query
          description: 'The reason the visit was canceled.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: number
                  format: int32
                  description: 'The source of the visit or the visit change. One of the {@link ModeSid} constants. If you''re unsure about the value to use, keep the default value.'
                id_visit:
                  type: number
                  format: int32
                  description: 'The status of the visit. One of the {@link VisitSid} constants.'
                id_visit_from:
                  type: [number, "null"]
                  description: 'The status of the visit from which the transition is made. One of the {@link VisitSid} constants. If the visit status is passed, it will be used to check with the actual status in the database. If `null`, the visit hasn''t yet passed. If the status of this parameter is out of date, the API call will refresh it.'
                is_mail:
                  type: boolean
                  description: 'Whether to send email notification. `true` - email notification will be sent. `false` - email notification will not be sent.'
                is_push:
                  type: boolean
                  description: 'Whether to send push notification. `true` - push notification will be sent. `false` - push notification will not be sent.'
                is_sms:
                  type: boolean
                  description: 'Whether to send sms notification. `true` - sms notification will be sent. `false` - sms notification will not be sent.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      id_visit_from:
                        type: [number, "null"]
                        description: 'The status of the visit from which the transition is made. One of the {@link VisitSid} constants. If the visit status is passed, it will be used to check with the actual status in the database. If `null`, the visit hasn''t yet passed. If the status of this parameter is out of date, the API call will refresh it.'
      tags:
        - Visit-VisitStatus
  '/Wl/Visit/VisitTotalCount.json':
    get:
      summary: 'Returns the total visit count for the user at the business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_visit_total:
                        type: number
                        format: int32
                        description: 'Total number of visits. Sum of attended visits in {@link \RsVisitSql} and historical visits in {@link VisitHistoricalCountSql}.'
      tags:
        - Visit-VisitTotalCount
  '/Wl/ClassPass/BusinessList.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessList
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pagination:
                        type: array
                        description: 'See {@link BusinessListData::toClassPass()} for documentation.'
                      a_partner_list:
                        type: array
                        description: 'See {@link BusinessListData::toClassPass()} for documentation. Be attentive this argument used as intermediate when request is forwarded and as final for direct request. Logic of filling this array described in `namespace.Wl/ClassPass/doc/BusinessListApi-get.en.md`.'
      tags:
        - ClassPass-BusinessList
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessList
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessList
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessList
  '/Wl/ClassPass/LocationSchedule.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationSchedule
    get:
      summary: ''
      parameters:
        - name: s_partner
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pagination:
                        type: array
                        description: 'See {@link \Wl\ClassPass\LocationScheduleListData::toClassPass()} for documentation.'
                      a_schedule:
                        type: array
                        description: 'See {@link \Wl\ClassPass\LocationScheduleListData::toClassPass()} for documentation.'
      tags:
        - ClassPass-LocationSchedule
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationSchedule
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationSchedule
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationSchedule
  '/Wl/ClassPass/BusinessData.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessData
    get:
      summary: ''
      parameters:
        - name: s_partner
          in: query
          description: 'Partner ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_partner:
                        type: array
                        description: 'Information about partner'
      tags:
        - ClassPass-BusinessData
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessData
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessData
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BusinessData
  '/Wl/ClassPass/LocationData.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationData
    get:
      summary: ''
      parameters:
        - name: s_partner
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_venue:
                        type: array
                        description: 'See {@link \Wl\ClassPass\LocationData::toClassPass()} for documentation.'
      tags:
        - ClassPass-LocationData
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationData
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationData
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationData
  '/Wl/ClassPass/LocationList.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationList
    get:
      summary: ''
      parameters:
        - name: s_partner
          in: query
          description: 'Partner ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pagination:
                        type: object
                        description: 'See {@link \Wl\ClassPass\LocationListData::toClassPass()} for documentation.'
                      a_venue_list:
                        type: array
                        description: 'See {@link \Wl\ClassPass\LocationListData::toClassPass()} for documentation.'
      tags:
        - ClassPass-LocationList
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationList
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationList
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-LocationList
  '/Wl/ClassPass/ReservationList.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationList
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationList
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationList
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_spot_label:
                        type: [string, "null"]
                        description: 'See {@link \Wl\ClassPass\ReservationResultData::$text_spot_label} for documentation.'
      tags:
        - ClassPass-ReservationList
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationList
  '/Wl/ClassPass/ScheduleAttendance.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ScheduleAttendance
    get:
      summary: ''
      parameters:
        - name: s_partner
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_schedule
          in: query
          description: 'Schedule ID.'
          schema:
            type: string
        - name: s_venue
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_attendance:
                        type: array
                        description: 'See {@link \Wl\ClassPass\ScheduleAttendanceListData::toClassPass()} for documentation.'
                      a_pagination:
                        type: array
                        description: 'See {@link \Wl\ClassPass\ScheduleAttendanceListData::toClassPass()} for documentation.'
      tags:
        - ClassPass-ScheduleAttendance
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ScheduleAttendance
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ScheduleAttendance
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ScheduleAttendance
  '/Wl/ClassPass/ReservationData.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationData
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationData
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_late_cancel:
                        type: boolean
                        description: 'See {@link \Wl\ClassPass\ReservationCancel::$is_late_cancel} for documentation. `null` if not defined yet.'
      tags:
        - ClassPass-ReservationData
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationData
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ReservationData
  '/Wl/ClassPass/AppointmentAttendance.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentAttendance
    get:
      summary: 'Gets appointment reservation state.'
      parameters:
        - name: s_appointment_id
          in: query
          description: 'Appointment reservation identifier generated by <tt>Classpass</tt>.'
          schema:
            type: string
        - name: s_partner_id
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue_id
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_attendance:
                        type: object
                        description: 'List of appointment attendees. _Basically here will be only one attendee because <tt>Wellnessliving</tt> do not support multiple attendees for one appointment ID._'
      tags:
        - ClassPass-AppointmentAttendance
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentAttendance
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentAttendance
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentAttendance
  '/Wl/ClassPass/AppointmentEdit.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentEdit
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentEdit
    patch:
      summary: 'Cancels appointment reservation.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_status:
                  type: string
                  description: 'Visit status.'
                text_note:
                  type: string
                  description: 'Some custom string for any additional information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentEdit
    post:
      summary: 'Makes appointment reservation.'
      parameters:
        - name: s_partner_id
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue_id
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_book:
                  type: object
                  description: 'List of bookable slots.'
                dtl_date:
                  type: string
                  description: 'Date and time of the reservation in venue locale. This is an ISO format of the local date: _[date]T[time]_'
                o_user:
                  type: object
                  description: 'User for which reservation should be performed.'
                s_service_id:
                  type: string
                  description: 'Service ID to be reserved.'
                text_note:
                  type: string
                  description: 'Some custom string for any additional information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_appointment_id:
                        type: string
                        description: 'Appointment reservation ID.'
      tags:
        - ClassPass-AppointmentEdit
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AppointmentEdit
  '/Wl/ClassPass/AvailabilityList.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AvailabilityList
    get:
      summary: 'Retrieves list of all services.'
      parameters:
        - name: dl_end
          in: query
          description: 'End date of the range to search availability.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Start date of the range to search availability.'
          schema:
            type: string
        - name: s_partner_id
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue_id
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_availability:
                        type: object
                        description: 'List of availabilities.'
                      a_pagination:
                        type: array
                        description: 'Pagination information. See {@link \Wl\ClassPass\Pagination}.'
      tags:
        - ClassPass-AvailabilityList
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AvailabilityList
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AvailabilityList
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-AvailabilityList
  '/Wl/ClassPass/BookableList.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BookableList
    get:
      summary: 'Retrieves list of all available bookables.'
      parameters:
        - name: s_partner_id
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue_id
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_bookable:
                        type: object
                        description: 'List of bookable items.'
                      a_pagination:
                        type: array
                        description: 'Pagination information. See {@link \Wl\ClassPass\Pagination}.'
      tags:
        - ClassPass-BookableList
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BookableList
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BookableList
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-BookableList
  '/Wl/ClassPass/ServiceList.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ServiceList
    get:
      summary: 'Retrieves list of all services.'
      parameters:
        - name: s_partner_id
          in: query
          description: 'Partner ID.'
          schema:
            type: string
        - name: s_venue_id
          in: query
          description: 'Venue ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pagination:
                        type: array
                        description: 'Pagination information. See {@link \Wl\ClassPass\Pagination}.'
                      a_service:
                        type: object
                        description: 'List of services.'
      tags:
        - ClassPass-ServiceList
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ServiceList
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ServiceList
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-ServiceList
  '/Wl/ClassPass/Nx.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-Nx
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-Nx
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-Nx
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-Nx
    put:
      summary: 'This method is called to process <tt>PUT</tt> query.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClassPass-Nx
  '/Wl/Event/EventCancelWhole.json':
    post:
      summary: 'Cancels book of event {@link EventCancelWholeApi::$k_class}. Used when a client wants to drop out of a multi-session event entirely. Cancels all remaining upcoming sessions at once, including any waitlisted spots, without requiring the client to cancel each session individually.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business in which the class resides.'
                k_class:
                  type: string
                  description: 'An event key.'
                uid:
                  type: string
                  description: 'A user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Event-EventCancelWhole
  '/Wl/Event/EventList.json':
    get:
      summary: 'Returns the list of events matching the specified filter parameters. Used to display the event schedule on business and location pages. Supports filtering by location, date range, staff, and booking status. Results are cached per business and user context; the cache is invalidated automatically when events are modified.'
      parameters:
        - name: a_class_filter
          in: query
          description: 'List of class keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_day
          in: query
          description: 'List of day the week applied by filter {@link \ADateWeekSid}.'
          schema:
            type: [array, "null"]
        - name: a_enrollment_block_filter
          in: query
          description: 'List of enrollment blocks keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_location
          in: query
          description: 'List of location keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_staff
          in: query
          description: 'List of staff keys applied by filter.'
          schema:
            type: [array, "null"]
        - name: a_time
          in: query
          description: 'List of time day applied by filter {@link \RsScheduleTimeSid}.'
          schema:
            type: [array, "null"]
        - name: a_virtual
          in: query
          description: 'List of IDs to include/exclude virtual events. If the only ID is {@link YesNoSid::YES}, only virtual events are included. If the only ID is {@link YesNoSid::NO}, only in-person events are included. Otherwise, no filtering is done.'
          schema:
            type: [array, "null"]
        - name: dl_end
          in: query
          description: 'The end date of the range from which a list of events should be retrieved. <tt>null</tt> if the range has no end date.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'The start date of the range from which a list of events should be retrieved. <tt>null</tt> if the range has no start date.'
          schema:
            type: [string, "null"]
        - name: id_flag
          in: query
          description: 'Defines how the event availability flag filter should be applied. One of {@link \AFlagSid} constants. * {@link \AFlagSid::ON} to show only available events. * {@link \AFlagSid::OFF} to show only unavailable events. * {@link \AFlagSid::ALL} to show all events (available and unavailable).'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'Determines whether the endpoint is used for backend mode.'
          schema:
            type: boolean
        - name: is_ignore_requirement
          in: query
          description: '`true` to show even event restricted by booking policies; `false` to show available events only.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: 'Determines whether you need to retrieve a list of event sessions regardless of the tab specified in {@link EventListApi::$k_class_tab}. * <tt>true</tt> - retrieves a list regardless of the specified tab. * <tt>false</tt> - retrieves a list only for the specific tab.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The event business key to retrieve a list of all event sessions in business. `null` to retrieve events from {@link EventListApi::$k_location}.'
          schema:
            type: [string, "null"]
        - name: k_class
          in: query
          description: 'The event class key to retrieve a list of all event sessions of a specific class. `null` to retrieve a list of event sessions of all classes.'
          schema:
            type: [string, "null"]
        - name: k_class_tab
          in: query
          description: 'The class tab key to retrieve a list of event sessions from a specific tab only. An empty value to retrieve a list of event sessions that don''t belong to any tab. Will be ignored in next cases: * {@link EventListApi::$k_skin} specified. * {@link EventListApi::$is_tab_all} is <tt>true</tt>.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The event location key to retrieve a list of all event sessions in a specific location. Required if {@link EventListApi::$k_business} isn''t specified. `null` if you need to retrieve a list of event sessions in all locations of {@link EventListApi::$k_business}.'
          schema:
            type: [string, "null"]
        - name: k_skin
          in: query
          description: 'The skin key if an event list is used for widget mode. {@link EventListApi::$k_class_tab} will be ignored for widget mode.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Search string to filter events by name.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. Required to apply specific user rules such as age restrictions.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_enrollment_block_list:
                        type: array
                        description: 'List of available enrollment blocks matching the requested parameters. Key is the enrollment block key. <i>Primary key in the {@link \RsEnrollmentBlockSql} table.</i> Value is the title of the first event found in that enrollment block.'
                      a_event_list:
                        type: array
                        description: 'A list of events corresponding to requested parameters.'
      tags:
        - Event-EventList
    put:
      summary: 'Resets the event list cache for the specified business and user. Call this after modifying event data to force the next request to rebuild the schedule from the database. Can also conditionally reset without parameters if the caller determines a reset is needed.'
      parameters:
        - name: k_business
          in: query
          description: 'The event business key to retrieve a list of all event sessions in business. `null` to retrieve events from {@link EventListApi::$k_location}.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. Required to apply specific user rules such as age restrictions.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_cache_reset:
                  type: boolean
                  description: 'Model cache reset flag.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Event-EventList
  '/Wl/Event/EventCancel.json':
    post:
      summary: 'Cancels book of session {@link EventCancelApi::$k_visit}.'
      parameters:
        - name: is_late_cancel
          in: query
          description: '<tt>true</tt> is late cancel, <tt>false</tt> reservation is not late cancel. This is required to enable late cancel even if the user is staff.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of the business to which the visit belongs.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Book ID. Primary key in table {@link \RsVisitSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: number
                  format: int32
                  description: 'Visit source. One of {@link \Wl\Mode\ModeSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Event-EventCancel
  '/Wl/Event/EventReorganize.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_class_period:
                  type: array
                  description: 'List of the class periods'' keys which should be reorganized.'
                k_class:
                  type: string
                  description: 'Class key. Destination class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Event-EventReorganize
  '/Wl/Holiday/Holiday.json':
    get:
      summary: 'Returns information about holiday day of business/locations. For the specified business and date, returns whether that date is a business-wide closed day, the closed day title if applicable, and a map of location keys to their closed day titles for any locations also closed on that date.'
      parameters:
        - name: dl_work
          in: query
          description: 'The date working hours are required for. Set in the business''s timezone.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location_holiday:
                        type: array
                        description: 'A list of the location''s closed day titles by location keys on the date {@link \Wl\Holiday\HolidayApi::$dl_work}. Keys are location keys. Values are holiday title strings.'
                      is_business_holiday:
                        type: boolean
                        description: '<tt>true</tt> if the business has a closed day on the date {@link \Wl\Holiday\HolidayApi::$dl_work}, <tt>false</tt> if otherwise.'
                      text_business_title:
                        type: string
                        description: 'The message used for the business''s closed day on the date {@link \Wl\Holiday\HolidayApi::$dl_work}.'
      tags:
        - Holiday-Holiday
  '/Wl/Holiday/BulkBusinessHoliday.json':
    get:
      summary: 'Returns all holiday records for all locations of the specified business. Retrieves the complete list of holidays across all date ranges for every location belonging to the given business, including both business-level and location-level closed days.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_holidays:
                        type: array
                        description: 'An array consisting of the business''s closed day data for all locations by holiday keys {@link \Wl\Holiday\HolidayApi::$dl_work}. List of holidays. <dl> <dt>true[] `a_class`</dt> <dd> List of effected classes, keys are class keys. Values always `true`. <i>Primary keys from the {@link \RsClassSql} table.</i> </dd> <dt>true[] `a_class_only`</dt> <dd>List of effected classes, keys are class keys. Values always `true`. Used in schedule widget.</dd> <dt>true[] `a_event_only`</dt> <dd>List of effected events, keys are class keys. Values always `true`. Used in schedule widget.</dd> <dt>string `dt_end`</dt> <dd>Date of the end of the holiday.</dd> <dt>string `dt_start`</dt> <dd>Date of the start of the holiday.</dd> <dt>bool `is_class_all`</dt> <dd>`1` if all classes are selected to cancel, `0` - otherwise.</dd> <dt>bool `is_event_all`</dt> <dd>`1` if all events are selected to cancel, `0` - otherwise.</dd> <dt>bool `is_service_all`</dt> <dd>`1` if all services are selected to cancel, `0` - otherwise.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key from the {@link \RsBusinessSql} table.</i></dd> <dt>string `k_holiday`</dt> <dd>Holiday key. <i>Primary key from the {@link \RsHolidaySql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from the {@link \RsLocationSql} table.</i></dd> <dt>string `k_market`</dt> <dd>The market key. <i>Primary key from the {@link \RsMarketSql} table.</i></dd> <dt>string `s_title`</dt> <dd>Name of the holiday.</dd> </dl>'
      tags:
        - Holiday-BulkBusinessHoliday
  '/Wl/Log/LogBusinessData.json':
    get:
      summary: 'Retrieves log data in html format.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_log
          in: query
          description: 'Log key. Primary key from {@link \Core\Log\DataSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_template:
                        type: array
                        description: '<dl> <dt>string `html_template`</dt> <dd>Log data template.</dd> </dl>'
      tags:
        - Log-LogBusinessData
  '/Wl/Rank/Rank.json':
    get:
      summary: 'Gets belts list of a business. Returns all belts for the specified business, optionally filtered by belt category keys, including each belt''s title and its parent category title.'
      parameters:
        - name: a_rank_category
          in: query
          description: 'Rank category keys. Used to filter belts by belt categories.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_rank_list:
                        type: array
                        description: 'A list of belts, keys, and information. Each element is an array with the following information: <dl> <dt>string `k_rank`</dt> <dd>A belt key. <i>Primary key in {@link \RsRankSql} table.</i></dd> <dt>string `k_rank_category`</dt> <dd>A belt category key. <i>Primary key in {@link \RsRankCategorySql} table.</i></dd> <dt>string `text_rank`</dt> <dd>Belt title.</dd> <dt>string `text_rank_category`</dt> <dd>Belt category title.</dd> </dl>'
      tags:
        - Rank-Rank
  '/Wl/Rank/SearchData.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_rank_category_list:
                        type: array
                        description: 'Belt rank categories.'
      tags:
        - Rank-SearchData
  '/Wl/Schedule/Schedule.json':
    get:
      summary: 'Returns parsed template for the schedule page.'
      parameters:
        - name: is_backend
          in: query
          description: 'Whether API is called in the backend mode.'
          schema:
            type: boolean
        - name: is_widget
          in: query
          description: 'Whether the schedule is shown in the widget.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key for which schedule should be got. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: s_data
          in: query
          description: 'Parameters and filters of the schedule encoded by Javascript analogue of the {@link \Core\Tool\UrlEncode\UrlEncode::encode()} method.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key who get the schedule.'
          schema:
            type: string
        - name: url_page
          in: query
          description: 'URL to the controller which were used before API to show the schedule. Used to create URL for printing schedule.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: [array, "null"]
                        description: 'Schedule info. Prepares only in the backend mode.'
                      a_schedule:
                        type: array
                        description: 'Schedule session data.'
                      html_template:
                        type: string
                        description: 'Parsed template of the schedule.'
      tags:
        - Schedule-Schedule
  '/Wl/Schedule/Cancel.json':
    get:
      summary: 'Cancels session for the client. Cancels the specified appointment or class period for the given user. Staff and admin users may cancel on behalf of any client; regular clients may only cancel their own bookings if the visit is still in a cancellable state. Clears cached schedule data after a successful class period cancellation.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date of the session in UTC.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the back end. Otherwise, this will be `false`. Here, the back end refers to either a staff member or admin from the side of the business.'
          schema:
            type: boolean
        - name: is_late_cancel
          in: query
          description: '<tt>true</tt> is late cancel, <tt>false</tt> reservation is not late cancel. This is required to enable late cancel even if the user is staff.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This will be `null` if not set yet or if a class or event is canceled.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of the business within which the action is performed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be `null` if not set yet or if an appointment is canceled.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Schedule-Cancel
    post:
      summary: 'Cancels session for the client. This method is an alias for partners using the API or SDK. Identical in behavior to {@link CancelApi::get()}; exists as a POST alias for partner integrations that cannot issue GET requests.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date of the session in UTC.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the back end. Otherwise, this will be `false`. Here, the back end refers to either a staff member or admin from the side of the business.'
          schema:
            type: boolean
        - name: is_late_cancel
          in: query
          description: '<tt>true</tt> is late cancel, <tt>false</tt> reservation is not late cancel. This is required to enable late cancel even if the user is staff.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This will be `null` if not set yet or if a class or event is canceled.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of the business within which the action is performed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be `null` if not set yet or if an appointment is canceled.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Schedule-Cancel
  '/Wl/Schedule/ScheduleAvailableDate.json':
    get:
      summary: 'Finds the nearest class session that can be booked by the current user. Looks no further than {@link ScheduleAvailableDateApi::PERIOD_LIMIT} seconds ahead from the start date.'
      parameters:
        - name: a_class
          in: query
          description: 'Class keys to filter. Empty to search for all classes.'
          schema:
            type: array
        - name: a_day
          in: query
          description: 'IDs of week days from {@link \ADateWeekSid} class. Empty to search for all week days.'
          schema:
            type: array
        - name: a_event
          in: query
          description: 'Event keys to filter. Empty to search for all events.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'Location keys to filter. Empty to search in all locations.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'Staff member keys to filter. Empty to search for all staff members.'
          schema:
            type: array
        - name: a_time
          in: query
          description: 'Time interval: Empty to search for all time. <dl> <dt>int `tl_end`</dt> <dd>End time.</dd> <dt>int `tl_start`</dt> <dd>Start time.</dd> </dl>'
          schema:
            type: array
        - name: dtu_start
          in: query
          description: 'The date/time to start from in UTC.'
          schema:
            type: string
        - name: id_class_tab
          in: query
          description: '"Book now" tab ID. One of {@link TabSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_class
          in: query
          description: '`true` to include classes; `false` to exclude.'
          schema:
            type: boolean
        - name: is_event
          in: query
          description: '`true` to include events; `false` to exclude.'
          schema:
            type: boolean
        - name: is_virtual
          in: query
          description: '`true` to include only virtual classes; `false` to include only in-person; `null` to no filtering.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dl_next_available:
                        type: [string, "null"]
                        description: 'Nearest session date available for booking in user''s or business timezone. `null` if there is no suitable session found.'
      tags:
        - Schedule-ScheduleAvailableDate
  '/Wl/Schedule/SchedulePostBased.json':
    get:
      summary: 'Returns parsed template for the schedule page.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Schedule-SchedulePostBased
    post:
      summary: 'Processes POST request to get the schedule for printing. Delegates to {@link ScheduleApi::get()} to avoid code duplication. Using POST avoids 414 (Request-URI Too Large) errors when {@link SchedulePostBasedApi::$s_data} is large.'
      parameters:
        - name: is_backend
          in: query
          description: 'Whether API is called in the backend mode.'
          schema:
            type: boolean
        - name: is_widget
          in: query
          description: 'Whether the schedule is shown in the widget.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key for which schedule should be got. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key who get the schedule.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_data:
                  type: [string, "null"]
                  description: 'Parameters and filters of the schedule encoded by Javascript analogue of the {@link \Core\Tool\UrlEncode\UrlEncode::encode()} method. Sent in the POST body to avoid 414 errors with large filter sets.'
                url_page:
                  type: string
                  description: 'URL to the controller which were used before API to show the schedule. Used to create URL for printing schedule. Sent in the POST body to avoid 414 errors because this URL includes {@link SchedulePostBasedApi::$s_data}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: [array, "null"]
                        description: 'Schedule info. Prepares only in the backend mode.'
                      a_schedule:
                        type: array
                        description: 'Schedule session data.'
                      html_template:
                        type: string
                        description: 'Parsed template of the schedule.'
      tags:
        - Schedule-SchedulePostBased
  '/Wl/Collector/DebtList.json':
    get:
      summary: 'Returns a list of client debts for the specified business within the given date range. Used by the Collections module to display outstanding debts to the collector. Requires an active Collections subscription and either the business privilege or emulation access. The default date range covers the previous month relative to the business timezone.'
      parameters:
        - name: dl_end
          in: query
          description: 'Date before which debts should be returned. If set, this is the end of the date window. Only debts added before or on this date will be shown. If left `null` and {@link DebtListApi::$dl_start} has been specified, only debts added after the start date will be returned. If left `null` and {@link DebtListApi::$dl_start} is also `null`, this will return debts from the previous month.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Date since which debts should be returned. If set, this is the start of the date window. Only debts added on or after this date will be shown. If left `null` and {@link DebtListApi::$dl_end} has been specified will return debts added since the beginning of time. If left `null` and {@link DebtListApi::$dl_end} is also `null`, this will return debts from the previous month.'
          schema:
            type: [string, "null"]
        - name: is_request_debt
          in: query
          description: 'Defines whether new debts should be returned or only previously sent debts. If `true` then return new debts only. If `false` then return previously sent debts only.'
          schema:
            type: boolean
        - name: is_test
          in: query
          description: 'Defines whether debts for test or real business should be returned. If `true`, debts from test businesses will be returned. Otherwise, this will be `false` if only debts from real businesses will be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key for which debts should be returned. Use `null` if debt payments from all businesses should be returned.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_debt:
                        type: array
                        description: 'A list of debts for the given business added within the previous month. Each value is an array with the next structure: <dl> <dt>string `dl_client_birth`</dt> <dd>The birth date of the debtor client.</dd> <dt>string `dtu_client_since`</dt> <dd>The date and time in UTC from which the debtor client is a member of the business.</dd> <dt>string `dtu_due`</dt> <dd>The date and time in UTC of the last payment on the debtor client''s account.</dd> <dt>string `dtu_add`</dt> <dd>The date and time in UTC when the client was added to the list of debtors.</dd> <dt>string `dtu_cease`</dt> <dd>The date and time in UTC when the debt was fully ceased.</dd> <dt>int `id_currency`</dt> <dd>The debt currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_business`</dt> <dd>The business key where the debt occurred.</dd> <dt>string `k_collector_debt`</dt> <dd>The debt key, which should be used for debt payment.</dd> <dt>string `k_pay_transaction_debt`</dt> <dd>The transaction key after which the debt occurred.</dd> <dt>string `k_pay_transaction_last`</dt> <dd>The last transaction key on account after the debt occurred.</dd> <dt>string `m_amount_debt`</dt> <dd>The initial amount of the debt.</dd> <dt>string `m_amount_paid`</dt> <dd>The amount paid for debt.</dd> <dt>string `text_business_title`</dt> <dd>The title of the business.</dd> <dt>string `text_client_name`</dt> <dd>The full name of the debtor client.</dd> <dt>string `text_client_address`</dt> <dd>The address of the debtor client.</dd> <dt>string `text_client_mail`</dt> <dd>The email address of the debtor client.</dd> <dt>string `text_client_phone`</dt> <dd>The phone number of the debtor client.</dd> <dt>string `text_client_zip`</dt> <dd>The ZIP code of the debtor client.</dd> <dt>string `text_skip`</dt> <dd>The notification message sent when the debt payment is missed.</dd> <dt>string `uid`</dt> <dd>The user key of the debt owner.</dd> </dl>'
      tags:
        - Collector-DebtList
  '/Wl/Collector/DebtActivate.json':
    post:
      summary: ''
      parameters:
        - name: id_currency
          in: query
          description: 'Currency of the payment. One of {@link CurrencySid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key of the debt. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_collector_debt:
                  type: array
                  description: 'List of debts that should be activated. Keys are user keys. Primary key in {@link \PassportLoginSql} Values are debt keys. Primary key in {@link CollectorDebtSql}.'
                text_reason:
                  type: string
                  description: 'Text to be shown as reason of the skip.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Collector-DebtActivate
  '/Wl/Collector/DebtTransaction.json':
    get:
      summary: 'Returns a list of debt payment transactions for the specified business within the given date range. Used by the Collections module to audit payments made against debts. The default date range covers the previous day relative to the business timezone. Requires an active Collections subscription and either the business privilege or emulation access.'
      parameters:
        - name: dl_end
          in: query
          description: 'If set, this is the end of the date window. Only debt payments added before or on this date will be shown. If left `null` and {@link DebtTransactionApi::$dl_start} has been specified only debt payments added after the start date will be returned. If left `null` and {@link DebtTransactionApi::$dl_start} is also `null`, this will return debt payments from the previous month.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'If set, this is the start of the date window. Only debt payments added on or after this date will be shown. If left `null` and {@link DebtTransactionApi::$dl_end} has been specified, this will return debt payments since the beginning of time. If left `null` and {@link DebtTransactionApi::$dl_end} is also `null`, this will return debt payments from the previous month.'
          schema:
            type: [string, "null"]
        - name: is_test
          in: query
          description: 'If `true`, debt payments from test businesses will be returned. Otherwise, this will be `false` if only debt payments from real businesses will be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key for which debt payments should be returned. Use `null` if debt payments from all businesses should be returned.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_transaction:
                        type: array
                        description: 'A list of debt payments for a given business added within the previous day. Each value is an array with next structure: <dl> <dt>string `dtu_pay`</dt> <dd>The date and time in UTC of the payment transaction.</dd> <dt>string `m_pay_amount`</dt> <dd>The amount of the payment''s transaction.</dd> <dt>int `id_pay_method`</dt> <dd>The payment method ID that was used for the payment. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_pay_collections`</dt> <dd> Defines whether the payment was performed via a third party service. `true` if the payment was performed via a third party service and this transaction is responsible for an account credit transaction for that payment. See {@link \Wl\Collector\DebtPayApi}. `false` if the payment was performed within the Wellnessliving system. </dd> <dt>bool `is_debt_paid`</dt> <dd> Defines whether the initial debt to which this related transaction is fully paid. `true` if the debt is fully paid. `false` if the debt is partially paid. </dd> <dt>string `text_client_name`</dt> <dd>The full name of the debtor client for whom the payment transaction was performed.</dd> <dt>string `uid`</dt> <dd>The UID of the debtor client for whom the payment transaction was performed.</dd> </dl>'
      tags:
        - Collector-DebtTransaction
  '/Wl/Collector/DebtPay.json':
    post:
      summary: 'Registers a debt payment made outside WellnessLiving and applies a credit to the client''s account balance. Used by collectors to record cash or external payments against outstanding debts. The specified amount is credited to the client''s account and the debt status is updated accordingly. Requires an active Collections subscription and the business privilege or emulation access.'
      parameters:
        - name: id_currency
          in: query
          description: 'The currency of the payment. One of {@link CurrencySid} constants. Important! The currency must coincide with currency of the debt that was sent to collections.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business from which the debt originates.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user with the debt.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_method:
                  type: [number, "null"]
                  description: 'The payment method ID. One of {@link \RsPayMethodSid} constants. `null` if the payment method is unknown. In such cases, {@link \RsPayMethodSid::CASH} would be used.'
                k_collector_debt:
                  type: string
                  description: 'The debt key towards which the payment was performed.'
                m_amount:
                  type: string
                  description: 'The amount of money that''s been paid towards the debt.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: string
                        description: 'The transaction key generated to register the payment.'
      tags:
        - Collector-DebtPay
  '/Wl/Collector/DebtSkip.json':
    post:
      summary: ''
      parameters:
        - name: id_currency
          in: query
          description: 'Currency of the payment. One of {@link CurrencySid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key of the debt. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Business key of the debt. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_collector_debt:
                  type: array
                  description: 'List of debts that should be skipped. Keys are user keys. Primary key in {@link \PassportLoginSql} Values are debt keys. Primary key in {@link CollectorDebtSql}.'
                k_collector_debt:
                  type: string
                  description: 'Debt key towards which skip should be performed. Primary key in {@link CollectorDebtSql}.'
                text_reason:
                  type: string
                  description: 'Text to be shown as reason of the skip.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Collector-DebtSkip
  '/Wl/Profile/ProfileCreate.json':
    post:
      summary: 'Creates a new client profile with the provided personal details in the specified business. Creates or retrieves a user account by email or phone, saves personal details such as name, address, phones, birthday, gender, and vaccination status, registers the user in the business, and optionally adds them to the lead report and sets intents.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                dt_birthday:
                  type: string
                  description: 'Date of the user''s birthday in MySQL format.'
                id_gender:
                  type: number
                  format: int32
                  description: 'Gender ID. One of the {@link GenderSid} constants. `0` if not specified.'
                id_lead_source:
                  type: number
                  format: int32
                  description: 'Lead source ID. One of the {@link ModeSid} constants. `0` if not specified.'
                id_vaccination_status:
                  type: number
                  format: int32
                  description: 'Vaccination status ID. One of the {@link VaccinationStatusSid} constants. `0` if not specified.'
                is_lead:
                  type: boolean
                  description: '`true` means to add user to the legacy lead report. `false` means to not add user to the legacy lead report. Note, that this setting does not impact new Lead Management report, which will always include new user. Also lead capture marketing notification will never be triggered with this endpoint.'
                k_business:
                  type: string
                  description: 'The key of the business.'
                k_lead_source:
                  type: string
                  description: 'The key of the lead source. Empty string if not specified.'
                k_location_home:
                  type: string
                  description: 'The key of the home location.'
                text_address:
                  type: string
                  description: 'The address of the user.'
                text_firstname:
                  type: string
                  description: 'First name of the user.'
                text_lastname:
                  type: string
                  description: 'Last name of the user.'
                text_mail:
                  type: string
                  description: 'Email of the user. Required if {@link ProfileCreateApi::$text_phone} not provided.'
                text_phone:
                  type: string
                  description: 'Phone of the user. Required if {@link ProfileCreateApi::$text_mail} not provided.'
                text_phone_home:
                  type: string
                  description: 'Home phone of the user.'
                text_phone_work:
                  type: string
                  description: 'Work phone of the user.'
                uid_referrer:
                  type: string
                  description: 'Referrer user key. Empty string if not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'The key of the user.'
      tags:
        - Profile-ProfileCreate
  '/Wl/Report/Data.json':
    get:
      summary: 'Gets data of required report. Loads the specified report for the given business, applying filter, sort, and pagination parameters, and returns the report rows and totals.'
      parameters:
        - name: i_page
          in: query
          description: 'The page of results to show, starting at zero. The API will return 256 results per page.'
          schema:
            type: number
            format: int32
        - name: id_report
          in: query
          description: 'The report ID. The following reports are currently available via the API: <ul> <li>{@link \RsReportSid::LOGIN_LIST} - All Clients Report (Reports tab, features date search)</li> <li>{@link \RsReportSid::LOGIN_LIST_ALL} - All Clients Report (Clients tab) Batch Report</li> <li>{@link \RsReportSid::PAY_TRANSACTION_BATCH} - Batch Report</li> <li>{@link \RsReportSid::PURCHASE_ITEM_LIST_USER} - Sales per Client Report</li> <li>{@link \RsReportSid::PURCHASE_ITEM_ACCRUAL_CASH} - All Sales Report</li> </ul>'
          schema:
            type: number
            format: int32
        - name: id_report_group
          in: query
          description: 'The report group ID.'
          schema:
            type: number
            format: int32
        - name: id_report_page
          in: query
          description: 'The report page ID. One of the {@link \RsReportPageSid} constants. If set, the collection of that page will be used, otherwise a collection of single report will be used.'
          schema:
            type: [number, "null"]
        - name: id_report_view
          in: query
          description: 'The report view ID. One of the {@link \RsReportChartViewSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of business for which the report must be generated.'
          schema:
            type: string
        - name: s_filter
          in: query
          description: 'Filter settings in encoded format. <i>May be decoded by {@link \Core\Tool\UrlEncode\UrlDecode::decode()}.</i>'
          schema:
            type: string
        - name: s_sort
          in: query
          description: 'The field to use for sorting report data.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'The report contents. Contents of this array can vary based on the report that''s loaded.'
                      a_total:
                        type: array
                        description: 'The report total. Contents of this array can vary based on the report that''s loaded.'
      tags:
        - Report-Data
  '/Wl/Report/PageData.json':
    get:
      summary: 'Gets data of required report collection. Loads all reports belonging to the specified report page collection for the given business, applying shared filter, sort, group, and view parameters, and returns the data for each report keyed by report ID.'
      parameters:
        - name: a_page
          in: query
          description: 'The page of results to show for each report in collection, starting at zero (set by default). The API will return 256 results per page. Key is report id from {@link \RsReportSid}, value is the page of results.'
          schema:
            type: array
        - name: id_report_group
          in: query
          description: 'The report group ID. One of the {@link \RsReportGroupSid} constants that describes the time period (day, week, month, or year) for the report to cover.'
          schema:
            type: number
            format: int32
        - name: id_report_page
          in: query
          description: 'The report page ID. One of the {@link \RsReportPageSid} constants. The collection of that page will be used to get list of reports. The following report collections are currently available via the API: * PROFILE_PURCHASE (ID 11) Collection with reports from client profile: memberships, packages, session/duration passes, single sessions, events, gift cards and groupon.'
          schema:
            type: number
            format: int32
        - name: id_report_view
          in: query
          description: 'The report view ID. One of the {@link \RsReportChartViewSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of business for which the report collection must be generated.'
          schema:
            type: string
        - name: s_filter
          in: query
          description: 'Filter settings in encoded format. <i>May be decoded by {@link \Core\Tool\UrlEncode\UrlDecode::decode()}.</i>'
          schema:
            type: string
        - name: s_sort
          in: query
          description: 'The field to use for sorting report data.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'List of the reports contents. Key is the report id from {@link \RsReportSid}, value is report contents. Contents can vary based on the report that''s loaded.'
      tags:
        - Report-PageData
  '/Wl/Report/Access.json':
    get:
      summary: 'Checks access to given report. Accepts either {@link AccessApi::$id_report} (first-generation reports) or {@link AccessApi::$cid_report} (second-generation reports), but not both, and returns `has_access` indicating whether the current user may view the report in the given business.'
      parameters:
        - name: cid_report
          in: query
          description: 'Report CID. It''s used mostly for second generation reports. For first generation reports use {@link AccessApi::$id_report} instead. Only one of these two fields should be sent, but not both.'
          schema:
            type: number
            format: int32
        - name: id_report
          in: query
          description: 'Report ID. It''s used mostly for first generation reports. For second generation reports use {@link AccessApi::$cid_report} instead. Only one of these two fields should be sent, but not both.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'ID of business for which access must be checked.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_access:
                        type: boolean
                        description: '<tt>true</tt> - access is granted; <tt>false</tt> - access is denied.'
      tags:
        - Report-Access
  '/Wl/Report/Info.json':
    get:
      summary: 'Gets report type data.'
      parameters:
        - name: id_report
          in: query
          description: 'Report ID. One of {@link \RsReportSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'ID of business for which report must be generated. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_report_type:
                        type: number
                        format: int32
                        description: 'Report type ID. One of {@link \RsReportTypeSid} constants.'
      tags:
        - Report-Info
  '/Wl/Resource/Resource.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business. Primary key from {@link \RsBusinessSql} table. <tt>null</tt> - if not set.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Key of the location. Primary key from {@link \RsLocationSql} table. <tt>null</tt> - if not set.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'Key of the resource. Primary key from {@link \RsResourceSql} table. <tt>null</tt> - if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_category:
                        type: number
                        format: int32
                        description: 'Type of the resource. 1 - if this is a bookable asset. 2 - if this is an off-site location with its own address.'
                      id_service_require:
                        type: number
                        format: int32
                        description: 'Mode to require amount while booking a resource. * One of the {@link \RsServiceRequireSid} constants. * Copy of the {@link \RsResourceSql}.`id_service_require`.'
                      is_staff_confirm:
                        type: boolean
                        description: 'Whether booking of the resource required confirmation from staff member. * Copy of {@link \RsResourceSql}.`is_staff_confirm`.'
                      k_location:
                        type: [string, "null"]
                        description: 'Key of the location. Primary key from {@link \RsLocationSql} table. <tt>null</tt> - if not set.'
                      text_title:
                        type: string
                        description: 'Title of the resource.'
      tags:
        - Resource-Resource
  '/Wl/Book/BookUser.json':
    post:
      summary: 'Saves a list of client who a class session must be booked for into session.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_uid:
                  type: array
                  description: 'List of clients to save. Each value is a primary key in {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_id:
                        type: string
                        description: 'Key in the storage to get a list of saved clients.'
      tags:
        - Book-BookUser
  '/Wl/Business/BusinessAccess.json':
    get:
      summary: 'Gets information about businesses where given user is a staff member. Used during staff login to determine which businesses the user can access. Accepts identity via UID or any supported social login identifier, then returns the matching list of businesses to display on the business selection screen. In backend mode, franchisee locations and IP access restrictions are also evaluated.'
      parameters:
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: s_facebook_id
          in: query
          description: 'The staff member''s Facebook ID. This won''t be empty if the staff member is logging in with Facebook. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: s_microsoft_id
          in: query
          description: 'The staff member''s Microsoft key. This won''t be empty if the staff member is logging in with Microsoft.'
          schema:
            type: string
        - name: text_authorization_apple
          in: query
          description: 'The staff member''s Apple authorization code. This won''t be empty if the staff member is logging in with Apple. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: text_google_plus
          in: query
          description: 'The Google Plus user ID. This won''t be empty if the staff member is logging in with Google. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The staff member''s email to determine their UID. This won''t be empty if the staff member is logging in with email or with Google. This will be empty if the UID is already known or the staff member is logging in another way.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Empty if user is not logged in, but their authorization data is known.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: array
                        description: 'The businesses the staff member belongs to. Note that this field is here for backwards compatibility.'
                      a_business_data:
                        type: array
                        description: 'The list of accessible businesses with their corresponding data. Each value is an array with the next structure: <dl> <dt>int `id_region`</dt> <dd> The business region. One of the {@link \Core\Amazon\Region\AmazonRegionSid} constants. This will be `null` if no regions are currently set. </dd> <dt>string `k_business`</dt> <dd>The business key. <i>Primary keys in table {@link \RsBusinessSql}.</i></dd> <dt>string `text_office_address`</dt> <dd>The business address.</dd> <dt>string `text_title`</dt> <dd>The business title.</dd> <dt>string `url_logo`</dt> <dd>The business logo URL. This will be empty if the business hasn''t added a logo.</dd> </dl>'
                      uid_mail:
                        type: string
                        description: 'The staff member key, determined by their email. This will be empty if the UID isn''t empty.'
      tags:
        - Business-BusinessAccess
  '/Wl/Business/Business.json':
    post:
      summary: 'Creates new business. Used during the self-onboarding flow when a new client signs up for WellnessLiving. Creates the business with its default integrations, optional first location, default service categories, and an optional owner staff account - all in a single transaction. Rate-limited per IP and globally to prevent abuse.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_member:
                  type: array
                  description: 'Empty array means to not create a staff member. Otherwise, information about staff member to be created in the business. <dl> <dt>bool `is_enter`</dt> <dd>Should created staff member be automatically signed in or not.</dd> <dt>string `text_mail`</dt> <dd>Email address to be used to sign in.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the staff member.</dd> <dt>string `text_name_last`</dt> <dd>The last name of the staff member.</dd> </dl>'
                id_locale:
                  type: number
                  format: int32
                  description: 'The business locale.'
                is_location_create:
                  type: boolean
                  description: '`true` if location should be also created for the business. `false` if otherwise.'
                is_mail_send:
                  type: boolean
                  description: '`true` to send {@link \RsMailSid::BUSINESS_CREATE} mail when creating a staff member. Otherwise send {@link \RsMailSid::LOGIN_WELCOME} mail.'
                k_business_type:
                  type: string
                  description: 'The key of the business type.'
                k_office_city:
                  type: string
                  description: 'The city key of the business. Optional.'
                s_clarity_session_id:
                  type: string
                  description: 'Microsoft Clarity session ID associated with the business claim session.'
                text_mail:
                  type: string
                  description: 'The email address of the location. Optional.'
                text_office_address:
                  type: string
                  description: 'The address of the business. Optional.'
                text_office_postal:
                  type: string
                  description: 'The postal code of the business. Optional.'
                text_phone:
                  type: string
                  description: 'The phone number of the business, staff and location.'
                text_title:
                  type: string
                  description: 'The title of the business.'
                url_site:
                  type: string
                  description: 'The URL of the business website. Optional.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_business:
                        type: string
                        description: 'The key of the business. When using {@link BusinessApi::post()} returned key of the created business. When using {@link BusinessApi::put()} used for update business.'
                      k_location:
                        type: [string, "null"]
                        description: 'The key of the location for update or after creating. `null` if location is not created.'
                      uid:
                        type: [string, "null"]
                        description: 'The UID of the created staff member or `null` if no staff member should be created.'
                      url_password_change:
                        type: [string, "null"]
                        description: 'Url for password change page. Filled only when creating staff member, otherwise `null`.'
      tags:
        - Business-Business
    put:
      summary: 'Claims business. Used when a prospective client claims an existing unclaimed business record during onboarding. Activates trial features, updates the location contact details, and optionally creates an owner staff account. The business must not already be claimed and the location must belong to it.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_member:
                  type: array
                  description: 'Empty array means to not create a staff member. Otherwise, information about staff member to be created in the business. <dl> <dt>bool `is_enter`</dt> <dd>Should created staff member be automatically signed in or not.</dd> <dt>string `text_mail`</dt> <dd>Email address to be used to sign in.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the staff member.</dd> <dt>string `text_name_last`</dt> <dd>The last name of the staff member.</dd> </dl>'
                is_mail_send:
                  type: boolean
                  description: '`true` to send {@link \RsMailSid::BUSINESS_CREATE} mail when creating a staff member. Otherwise send {@link \RsMailSid::LOGIN_WELCOME} mail.'
                k_business:
                  type: string
                  description: 'The key of the business. When using {@link BusinessApi::post()} returned key of the created business. When using {@link BusinessApi::put()} used for update business.'
                k_location:
                  type: [string, "null"]
                  description: 'The key of the location for update or after creating. `null` if location is not created.'
                s_clarity_session_id:
                  type: string
                  description: 'Microsoft Clarity session ID associated with the business claim session.'
                text_mail:
                  type: string
                  description: 'The email address of the location. Optional.'
                text_phone:
                  type: string
                  description: 'The phone number of the business, staff and location.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_business:
                        type: string
                        description: 'The key of the business. When using {@link BusinessApi::post()} returned key of the created business. When using {@link BusinessApi::put()} used for update business.'
                      k_location:
                        type: [string, "null"]
                        description: 'The key of the location for update or after creating. `null` if location is not created.'
                      uid:
                        type: [string, "null"]
                        description: 'The UID of the created staff member or `null` if no staff member should be created.'
                      url_password_change:
                        type: [string, "null"]
                        description: 'Url for password change page. Filled only when creating staff member, otherwise `null`.'
      tags:
        - Business-Business
  '/Wl/Business/Data.json':
    get:
      summary: 'Returns detailed information about the specified business, including locale, franchise status, services, tips, social URLs, and logo. Used to bootstrap client-facing pages and widgets that need to know the full business profile: which services are enabled, tip and progress log settings, franchise relationship, social media links, and deep links to the schedule, catalog, and sign-up pages. Accepts either a business key or a location request token for widget contexts where the key is not available.'
      parameters:
        - name: i_logo_height
          in: query
          description: 'The maximum height of the business image (in pixels).'
          schema:
            type: number
            format: int32
        - name: i_logo_width
          in: query
          description: 'The maximum width of the business image (in pixels).'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The authorization token. This may be used instead of {@link DataApi::$k_business} to identify a business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service_list:
                        type: array
                        description: 'A list of all business services and their availability data. This is presented as an array, where keys are SIDs from {@link ServiceSid} and values are Boolean. If `true`, at least one service is enabled in the business. Otherwise, this will be `false`.'
                      a_tip_predefine:
                        type: array
                        description: 'The list of predefined tips in percentages.'
                      id_category:
                        type: number
                        format: int32
                        description: 'The business category ID of the business.'
                      id_claim_status:
                        type: number
                        format: int32
                        description: 'Business status for managing claim request behavior. Constant from {@link BusinessClaimStatusSid}.'
                      id_currency:
                        type: number
                        format: int32
                        description: 'The currency ID of the given business (or the system currency ID if the business didn''t pass).'
                      id_locale:
                        type: number
                        format: int32
                        description: 'The Locale ID, used to search geo items.'
                      id_rank_type:
                        type: [number, "null"]
                        description: 'The rank type ID of the business. One of the {@link \RsRankTypeSid} constants. This will be `null` if the business doesn''t have a rank type.'
                      id_region:
                        type: number
                        format: int32
                        description: 'The region ID. This indicates the data center where the information about the business is stored. One of the {@link AmazonRegionSid} constants. Requests made to different regions can lead to known issues such as responses indicating that the business (or its elements) doesn''t exist. This is because databases on different data centers are independent. For example, performing a request to the US cluster for a list of classes for an AU cluster business will return an empty list.'
                      is_apply_surcharge:
                        type: boolean
                        description: 'Determines whether surcharges to client payments are enabled in the business.'
                      is_franchise:
                        type: boolean
                        description: '`true` if business is a franchisor or franchisee.'
                      is_location_multiple:
                        type: boolean
                        description: 'Determines whether the business has multiple locations (including inactive locations).'
                      is_profile_timezone:
                        type: boolean
                        description: '`true` - clients of the business can select a custom time zone in their profile. `false` - the location or business time zone is used.'
                      is_progress_client:
                        type: boolean
                        description: 'If `true`, clients can enter the progress log. Otherwise, this will be `false`.'
                      is_progress_verify:
                        type: boolean
                        description: 'If `true`, verification of the progress log by a staff member is required. Otherwise, this will be `false`.'
                      is_quiz_available:
                        type: boolean
                        description: 'Determines whether quizzes are available in the business.'
                      is_test:
                        type: boolean
                        description: 'Determines if the business operates in testing mode.'
                      is_tip:
                        type: boolean
                        description: 'If `true`, tips are available in the business. Otherwise, this will be `false`.'
                      is_tip_deny:
                        type: boolean
                        description: 'If `true`, the business has the "No tip" option displayed. Otherwise, this will be `false`.'
                      is_tip_sign:
                        type: boolean
                        description: 'If `true`, the client must sign after selecting the tip. Otherwise, this will be `false`.'
                      k_business_franchisor:
                        type: string
                        description: 'The franchisor business key. This will be empty if this business is the franchisor or not in a franchise.'
                      k_business_token:
                        type: string
                        description: 'The business key obtained by the security token {@link DataApi::$text_token}. This can be used on the client side if authorization token should be used instead of the business key (the business key isn''t available).'
                      k_business_type:
                        type: string
                        description: 'Key of the business type.'
                      k_currency:
                        type: string
                        description: 'The currency key of the given business, or the system currency if the business didn''t pass.'
                      s_reply_mail:
                        type: string
                        description: 'The reply-to email address.'
                      s_reply_name:
                        type: string
                        description: 'The reply-to business name.'
                      text_office_address:
                        type: string
                        description: 'The business address.'
                      text_title:
                        type: string
                        description: 'The business title.'
                      url_catalog:
                        type: string
                        description: 'The URL for the online store.'
                      url_enter:
                        type: string
                        description: 'The URL for client sign in.'
                      url_facebook:
                        type: string
                        description: 'The Facebook page.'
                      url_google:
                        type: string
                        description: 'The Google+ page.'
                      url_home_tour:
                        type: string
                        description: 'URL of the home tour page related to the business type.'
                      url_instagram:
                        type: string
                        description: 'The Instagram page.'
                      url_linkedin:
                        type: string
                        description: 'The `LinkedIn` profile.'
                      url_logo:
                        type: string
                        description: 'The logo URL.'
                      url_logo_empty:
                        type: string
                        description: 'The image stub (in cases where the business logo isn''t loaded).'
                      url_redeem:
                        type: string
                        description: 'The URL to redeem gift card.'
                      url_review:
                        type: string
                        description: 'The URL for the client review page.'
                      url_schedule:
                        type: string
                        description: 'The URL for the client web app (schedule).'
                      url_signup:
                        type: string
                        description: 'The URL for client sign up.'
                      url_twitter:
                        type: string
                        description: 'The Twitter page.'
                      url_website:
                        type: string
                        description: 'The business website.'
                      url_youtube:
                        type: string
                        description: 'The YouTube website.'
      tags:
        - Business-Data
  '/Wl/Business/DateLocal.json':
    get:
      summary: 'Gets information about a business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_date:
                        type: string
                        description: 'Business local date in mysql format.'
      tags:
        - Business-DateLocal
  '/Wl/Business/EnterForm.json':
    get:
      summary: ''
      parameters:
        - name: i_logo_height
          in: query
          description: 'Height of the logo.'
          schema:
            type: number
            format: int32
        - name: i_logo_width
          in: query
          description: 'Width of the logo.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business to which the application belongs. Can be empty, in this case current business from the session will be used. Primary key of the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: s_application
          in: query
          description: 'Unique ID of application.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_apple:
                        type: boolean
                        description: '`true` - enable for this application sign in with Apple; `false` - disable.'
                      is_facebook:
                        type: boolean
                        description: '`true` - enable for this application sign in with Facebook; `false` - disable.'
                      is_facebook_android:
                        type: boolean
                        description: '`true` - enable for this application sign in with Facebook for Android; `false` - disable.'
                      is_google:
                        type: boolean
                        description: '`true` - enable for this application sign in with Google; `false` - disable.'
                      is_microsoft:
                        type: boolean
                        description: '`true` - enable for this application sign in with Microsoft; `false` - disable.'
                      is_register_deny:
                        type: boolean
                        description: '`true` - registration of new user is denied in application; `false` - registration of new user is allowed.'
                      is_social:
                        type: boolean
                        description: '`true` if sign in via social networks is allowed; `false` otherwise.'
                      is_tjx:
                        type: boolean
                        description: '`true` if sign in via TJX Azure is allowed; `false` otherwise.'
                      text_tjx_button_name:
                        type: string
                        description: 'Tjx button name.'
                      url_facebook:
                        type: string
                        description: 'Facebook authorization link.'
                      url_logo:
                        type: string
                        description: 'Link to the logo of the business.'
                      url_tjx:
                        type: string
                        description: 'Tjx link.'
      tags:
        - Business-EnterForm
  '/Wl/GymPass/BusinessDisconnect.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Business key for which integration should be disabled. `null` in a case value is not specified.'
                k_location:
                  type: [string, "null"]
                  description: 'Location key  for which integration should be disabled. `null` in a case value is not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GymPass-BusinessDisconnect
  '/Wl/GymPass/BusinessConnect.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Business key. `null` in a case value is not initialized yet.'
                k_location:
                  type: [string, "null"]
                  description: 'Location key. `null` in a case value is not initialized yet.'
                s_gym_id:
                  type: [string, "null"]
                  description: 'Gym ID. `null` in a case value is not initialized yet.'
                s_product_id:
                  type: [string, "null"]
                  description: 'Product ID. `null` in a case value is not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GymPass-BusinessConnect
  '/Wl/Login/Login.json':
    get:
      summary: 'Retrieves information about user. Returns name, gender, photo URL, email, and staff details for the specified user within the given business. Public staff information is returned even without profile access; full client details require access to the user''s profile.'
      parameters:
        - name: i_photo_height
          in: query
          description: 'The height of the requested photo.'
          schema:
            type: number
            format: int32
        - name: i_photo_width
          in: query
          description: 'The width of the requested photo.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business. Users can be in multiple businesses. This can be left as `null` to retrieve system-wide information.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user. `null` on case when is walk-in client.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_postcard:
                        type: boolean
                        description: 'Whether this user can send postcards.'
                      can_send_message:
                        type: boolean
                        description: 'Whether this user can send SMS. If `true` - user can send SMS, otherwise - `false`.'
                      id_gender:
                        type: number
                        format: int32
                        description: 'The gender ID. It will be one of the {@link \AGenderSid} constants.'
                      is_photo_empty:
                        type: boolean
                        description: 'Whether photo is uploaded. `true` if photo is not uploaded, `false` otherwise.'
                      k_staff:
                        type: string
                        description: 'The user''s staff key for the specified business.'
                      s_first_name:
                        type: string
                        description: 'The first name of the user.'
                      s_last_name:
                        type: string
                        description: 'The surname of the user.'
                      text_mail_client:
                        type: string
                        description: 'The client`s mailing address.'
                      text_mail_staff:
                        type: string
                        description: 'The staff member''s mailing address. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      text_name_first_staff:
                        type: string
                        description: 'The staff member''s first name. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      text_name_full_client:
                        type: string
                        description: 'The user''s login name. This is returned in cases when neither the first name nor the last name have been specified. An empty string is returned in cases where neither the first name, last name, nor login have been specified.'
                      text_name_full_staff:
                        type: string
                        description: 'The staff member''s full name. The user login is returned in cases where neither the first name nor the last name have been specified. An empty string is returned in cases where neither the first name, last name, nor login have been specified. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      text_name_last_staff:
                        type: string
                        description: 'The staff member''s last name. This will be set if the user is a staff member ({@link LoginApi::$k_staff}).'
                      uid:
                        type: [string, "null"]
                        description: 'The key of the user. `null` on case when is walk-in client.'
                      uid_staff:
                        type: [string, "null"]
                        description: 'The user''s UID as a staff member for the specified business.'
                      url_photo:
                        type: string
                        description: 'The URL where the user photo can be retrieved.'
      tags:
        - Login-Login
    post:
      summary: 'Retrieves information about a list of users. This is done via "post" method because only "post" allows large requests. Accepts a JSON-encoded array of user keys, validates each one, resolves staff and client roles, and returns name, gender, photo URL, email, and staff details for every user in the list, respecting per-user profile access rules.'
      parameters:
        - name: i_photo_height
          in: query
          description: 'The height of the requested photo.'
          schema:
            type: number
            format: int32
        - name: i_photo_width
          in: query
          description: 'The width of the requested photo.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business. Users can be in multiple businesses. This can be left as `null` to retrieve system-wide information.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_uid:
                  type: [string, "null"]
                  description: 'List of users to get information for serialized as JSON string. `null` for mode of single user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login:
                        type: array
                        description: 'List of information about users: <dl> <dt>int `id_gender`</dt> <dd>User''s gender. One of {@link \AGenderSid} constants.</dd> <dt>string `k_staff`</dt> <dd>User''s key as staff member. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `s_first_name`</dt> <dd>User first name.</dd> <dt>string `s_last_name`</dt> <dd>First letter of user last name.</dd> <dt>string `text_mail_client`</dt> <dd>Client`s mail.</dd> <dt>string `text_mail_staff`</dt> <dd>Staff`s mail.</dd> <dt>string `text_name_first_staff`</dt> <dd>Staff''s first name.</dd> <dt>string `text_name_full_client`</dt> <dd> Full client name. User login is returned in a case neither first name, nor last name specified. An empty string is returned in a case neither first name, nor last name specified, nor login. <i>See description of the {@link \Wl\User\Info\UserInfo::nameFullText()} method.</i> </dd> <dt>string `text_name_full_staff`</dt> <dd> Full staff name. User login is returned in a case neither first name, nor last name specified. An empty string is returned in a case neither first name, nor last name specified, nor login. </dd> <dt>string `text_name_last_staff`</dt> <dd>Staff`s last name.</dd> <dt>string `uid_staff`</dt> <dd>The user''s UID as a staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_photo`</dt> <dd>User photo URL.</dd> </dl>'
      tags:
        - Login-Login
  '/Wl/Mail/SendMail.json':
    post:
      summary: 'Sends email. Validates the recipient address, business email, subject, and HTML content, then saves the email pattern and dispatches the message to the specified recipient. Rate limiting is applied per hour and per minute.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_business_mail:
                  type: string
                  description: 'The business''s mailing address.'
                s_business_name:
                  type: string
                  description: 'The business name.'
                s_business_reply:
                  type: string
                  description: 'The business''s reply.'
                s_campaign:
                  type: string
                  description: 'The campaign name.'
                s_html:
                  type: string
                  description: 'The message uncompressed content.'
                s_mail:
                  type: string
                  description: 'The message recipient''s address.'
                s_subject:
                  type: string
                  description: 'The message''s subject line.'
                z_html:
                  type: string
                  description: 'The message content.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mail-SendMail
  '/Wl/Postcard/PostcardAddress.json':
    get:
      summary: 'Retrieves status of the address validation.'
      parameters:
        - name: json_address
          in: query
          description: 'Address for validation.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_valid:
                        type: boolean
                        description: 'Whether address is verified and valid. <tt>true</tt> if address verified and valid. <tt>false</tt> if address is not verified yet or not valid.'
                      is_verified:
                        type: boolean
                        description: 'Whether address already verified.'
                      text_note:
                        type: string
                        description: 'Additional notes for address verification.'
      tags:
        - Postcard-PostcardAddress
    post:
      summary: 'Performs address validation.'
      parameters:
        - name: json_address
          in: query
          description: 'Address for validation.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_valid:
                        type: boolean
                        description: 'Whether address is verified and valid. <tt>true</tt> if address verified and valid. <tt>false</tt> if address is not verified yet or not valid.'
                      is_verified:
                        type: boolean
                        description: 'Whether address already verified.'
                      text_note:
                        type: string
                        description: 'Additional notes for address verification.'
      tags:
        - Postcard-PostcardAddress
  '/Wl/Quiz/QuizElement.json':
    delete:
      summary: 'Deletes the quiz with the given key. Permanently removes the quiz and all associated elements.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Quiz-QuizElement
    get:
      summary: 'Returns quiz element data including settings, elements, and access information for the given quiz. Loads the quiz configuration, element list, and access log for the specified business and user context, applying franchise and privilege checks before returning the result.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_builder
          in: query
          description: '`true` for request quiz from form builder page, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: json_purchase_item
          in: query
          description: 'List of purchase items for which this form is loaded in JSON format. This variable supports two structures: New structure: Each element key has the format `[id_purchase_item]::[k_id]`, where: Old structure: Each element is a string in the format `[id_purchase_item]::[k_id]`. Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl> <dl> <dt>string|null `dl_start`</dt> <dd>Overridden start date of the purchase option.</dd> <dt>string|null `f_price`</dt> <dd>Overridden price of the purchase option.</dd> <dt>int `i_duration`</dt> <dd>Duration of the purchase option.</dd> <dt>int `i_payment_period`</dt> <dd>Payment period for the purchase option.</dd> <dt>int `id_duration`</dt> <dd>Duration type, one of {@link \RsDurationTypeSid} constants.</dd> </dl>'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'UID of the client for which quiz requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether user has privileges to amend form.'
                      i_responses:
                        type: number
                        format: int32
                        description: 'Number of responses for specific quiz.'
                      is_active:
                        type: boolean
                        description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                      is_imported:
                        type: boolean
                        description: '`true` if quiz is imported, `false` otherwise.'
                      is_prevent_franchisee:
                        type: boolean
                        description: 'Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise.'
                      k_business_type:
                        type: [string, "null"]
                        description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_title:
                        type: string
                        description: 'Quiz form title.'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement
    post:
      summary: 'Creates or updates a quiz with the given elements and settings. When no quiz key is provided, a new quiz is created; when a key is given, the existing quiz is updated in place. Element list and configuration are saved in a single transaction.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_setting:
                  type: array
                  description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                json_element:
                  type: string
                  description: 'List of quiz elements in json format. Order of the element in array corresponds to order of elements on the form.'
                k_business_type:
                  type: [string, "null"]
                  description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                show_numbering:
                  type: boolean
                  description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                text_title:
                  type: string
                  description: 'Quiz form title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz:
                        type: string
                        description: 'Quiz key.'
      tags:
        - Quiz-QuizElement
    put:
      summary: 'Updates the active status of the given quiz. Toggles whether the quiz is available for respondents to fill out. Inactive quizzes are hidden from the booking and profile flows but their existing responses are preserved.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement
  '/Wl/Quiz/QuizList.json':
    get:
      summary: ''
      parameters:
        - name: a_active_id
          in: query
          description: 'Whether to show active or inactive quizzes. Each element should be one of {@link YesNoSid} constants.'
          schema:
            type: array
        - name: is_register_before
          in: query
          description: 'Shows which register quizzes must be returned. `true` if return quizzes before registration, `false` otherwise. `null` if return all quizzes for registration.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Filter phrase to filter quizzes by name.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of available quizzes within quiz holder.'
                      a_quiz_register:
                        type: array
                        description: 'List of quiz keys, needed for registration. Each element is primary key in {@link \Wl\Quiz\QuizSql} table.'
      tags:
        - Quiz-QuizList
  '/Wl/Quiz/QuizElementClone.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_quiz_clone:
                  type: string
                  description: 'Key of quiz to copy.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Quiz-QuizElementClone
  '/Wl/Quiz/ServiceList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'List of services.'
      tags:
        - Quiz-ServiceList
  '/Wl/Quiz/QuizElement72.json':
    delete:
      summary: 'Deletes the quiz with the given key. Permanently removes the quiz and all associated elements.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Quiz-QuizElement72
    get:
      summary: 'Returns quiz element data including settings, elements, and access information for the given quiz. Loads the quiz configuration, element list, and access log for the specified business and user context, applying franchise and privilege checks before returning the result.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_builder
          in: query
          description: '`true` for request quiz from form builder page, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: json_purchase_item
          in: query
          description: 'List of purchase items for which this form is loaded in JSON format. This variable supports two structures: New structure: Each element key has the format `[id_purchase_item]::[k_id]`, where: Old structure: Each element is a string in the format `[id_purchase_item]::[k_id]`. Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl> <dl> <dt>string|null `dl_start`</dt> <dd>Overridden start date of the purchase option.</dd> <dt>string|null `f_price`</dt> <dd>Overridden price of the purchase option.</dd> <dt>int `i_duration`</dt> <dd>Duration of the purchase option.</dd> <dt>int `i_payment_period`</dt> <dd>Payment period for the purchase option.</dd> <dt>int `id_duration`</dt> <dd>Duration type, one of {@link \RsDurationTypeSid} constants.</dd> </dl>'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'UID of the client for which quiz requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether user has privileges to amend form.'
                      i_responses:
                        type: number
                        format: int32
                        description: 'Number of responses for specific quiz.'
                      is_active:
                        type: boolean
                        description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                      is_imported:
                        type: boolean
                        description: '`true` if quiz is imported, `false` otherwise.'
                      is_prevent_franchisee:
                        type: boolean
                        description: 'Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise.'
                      k_business_type:
                        type: [string, "null"]
                        description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_title:
                        type: string
                        description: 'Quiz form title.'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement72
    post:
      summary: 'Creates or updates a quiz with the given elements and settings. When no quiz key is provided, a new quiz is created; when a key is given, the existing quiz is updated in place. Element list and configuration are saved in a single transaction.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz elements. Each element responsible for one quiz element and structure of each value depend on type of element and contains public arguments of responsible class element. Order of the elements in array corresponds to order of elements on the form. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_setting:
                  type: array
                  description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
                json_element:
                  type: string
                  description: 'List of quiz elements in json format. Order of the element in array corresponds to order of elements on the form.'
                k_business_type:
                  type: [string, "null"]
                  description: 'Business type key. Used only for forms in the system business. `null` if not initialized.'
                show_numbering:
                  type: boolean
                  description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                text_title:
                  type: string
                  description: 'Quiz form title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz:
                        type: string
                        description: 'Quiz key.'
      tags:
        - Quiz-QuizElement72
    put:
      summary: 'Updates the active status of the given quiz. Toggles whether the quiz is available for respondents to fill out. Inactive quizzes are hidden from the booking and profile flows but their existing responses are preserved.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Quiz active status. `true` if quiz is active. `false` if quiz is not active.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_setting:
                        type: array
                        description: 'Quiz settings. <i>Array representation of {@link QuizElementApi::$o_setting}.</i> <dl> <dt>string[] `a_notify_additional`</dt> <dd> List of additional email addresses which should receive email notification after quiz is submitted. Considered only if {@link \Wl\Quiz\QuizSetting::$is_notify_additional} is `true`. </dd> <dt>string[][] `a_service`</dt> <dd> List of services grouped by service ID. Key is service ID. One of {@link \Wl\Quiz\Service\ServiceSid} constants. Values is list of services. </dd> <dt>bool `hide_frontend`</dt> <dd>Whether completed forms should be hidden for client in frontend.</dd> <dt>int|string `i_notify_automated`</dt> <dd> Number of periods email reminders should be sent for incomplete forms after. Type of a period is specified by {@link \Wl\Quiz\QuizSetting::$id_notify_automated}. <i> The field value comes from the form and can be an empty string. Validated in the {@link \Wl\Quiz\QuizSetting::validate()} method. </i> </dd> <dt>int `id_book_request_type`</dt> <dd> Whether response is required every time they book service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>int `id_notify_automated`</dt> <dd>Time interval ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_purchase_request_type`</dt> <dd> Whether response is required every time they purchase service. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_amend_support`</dt> <dd>Whether form support amending responses.</dd> <dt>bool `is_book`</dt> <dd> Whether quiz is required during/after book service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. * Related to options: * * {@link \Wl\Quiz\QuizSetting::$is_service_all} * * {@link \Wl\Quiz\QuizSetting::$is_book_before} * * {@link \Wl\Quiz\QuizSetting::$id_book_request_type} </dd> <dt>bool `is_book_before`</dt> <dd> Whether quiz is required during booking process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_book} is `true`. </dd> <dt>bool `is_internal`</dt> <dd> Whether form can be required for clients. Internal forms can be only added to client profiles or filled by direct link, but they don''t prevent clients to perform any actions until form filled. `true` if form can be visible to clients `false` otherwise </dd> <dt>bool `is_notify_additional`</dt> <dd> Whether email notification should be sent to specific email addresses after quiz is submitted. List of email addresses stored in {@link \Wl\Quiz\QuizSetting::$a_notify_additional}. </dd> <dt>bool `is_notify_automated`</dt> <dd>Whether email reminders should be sent for incomplete forms.</dd> <dt>bool `is_notify_client`</dt> <dd>Whether email notification should be sent to client after quiz submitted.</dd> <dt>bool `is_notify_staff`</dt> <dd>Whether email notification should be sent to staff members after quiz is submitted.</dd> <dt>bool `is_prevent_franchisee`</dt> <dd> Whether form can be edited by franchisee. `true` prevent franchisees from editing this form, `false` - otherwise. </dd> <dt>bool `is_private`</dt> <dd> Whether form can be viewed by staff member only after confirmation. `true` if form can be viewed only after confirmation `false` if form can be viewed always </dd> <dt>bool `is_purchase`</dt> <dd> Whether quiz is required during/after purchase service selected in {@link \Wl\Quiz\QuizSetting::$a_service} list. Related to options: * {@link \Wl\Quiz\QuizSetting::$is_purchase_all} * {@link \Wl\Quiz\QuizSetting::$is_purchase_before} * {@link \Wl\Quiz\QuizSetting::$id_purchase_request_type} </dd> <dt>bool `is_purchase_all`</dt> <dd>Whether all services for purchase should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> <dt>bool `is_purchase_before`</dt> <dd> Whether quiz is required during purchase process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_purchase} is `true`. </dd> <dt>bool `is_register_after`</dt> <dd>Whether quiz is required after registration process.</dd> <dt>bool `is_register_before`</dt> <dd> Whether quiz is required during registration process. Considered only if {@link \Wl\Quiz\QuizSetting::$is_register_after} is `true`. </dd> <dt>bool `is_require_user`</dt> <dd>Whether client should be redirected to sign-in page before completing the form.</dd> <dt>bool `is_service_all`</dt> <dd>Whether all services for booking should be added in {@link \Wl\Quiz\QuizSetting::$a_service}.</dd> </dl>'
                      url_quiz:
                        type: string
                        description: 'Direct URL to quiz.'
                      url_quiz_kiosk:
                        type: string
                        description: 'Kiosk direct URL to quiz.'
      tags:
        - Quiz-QuizElement72
  '/Wl/Review/Review.json':
    post:
      summary: 'Submits user''s review. Validates the captcha, saves the review rating and text for the specified location, and returns reward score information if the business awards points for leaving a review.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_rate:
                  type: [number, "null"]
                  description: 'The rating given to the location (1 to 5 stars). This will be `null` if not set yet.'
                k_location:
                  type: [string, "null"]
                  description: 'The key of a location. This will be `null` if not loaded yet.'
                k_visit:
                  type: [string, "null"]
                  description: 'Visit key. Can be `null` if the review is not connected to a visit.'
                s_text:
                  type: [string, "null"]
                  description: 'The text of the review. This will be `null` if not set yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Reward score for leaving a review.'
                      i_score_facebook:
                        type: number
                        format: int32
                        description: 'Reward score for sharing a review on Facebook.'
                      i_score_twitter:
                        type: number
                        format: int32
                        description: 'Reward score for sharing a review on Twitter.'
                      is_score:
                        type: boolean
                        description: 'If a reward score for leaving a review exists.'
                      is_score_facebook:
                        type: boolean
                        description: 'If a reward score for sharing a review on Facebook exists.'
                      is_score_twitter:
                        type: boolean
                        description: 'If a reward score for sharing a review on Twitter exists.'
                      is_share_points:
                        type: boolean
                        description: 'If a reward score for sharing exists.'
                      is_share_points_none:
                        type: boolean
                        description: 'If a reward score does not exist for leaving a review or sharing the review.'
                      k_login_activity:
                        type: string
                        description: 'The key of the review writing activity. This will be empty if the review was saved but not published.'
                      k_review:
                        type: string
                        description: 'Review key.'
                      uid:
                        type: string
                        description: 'The UID of client who leaves review.'
                      url_share:
                        type: string
                        description: 'The sharing url of the review.'
      tags:
        - Review-Review
  '/Wl/Review/ReviewFeature.json':
    post:
      summary: 'Updates the featured status of the given review for the business. If featuring the review, unfeatures the currently featured review for the business location and marks the specified review as featured; if unfeaturing, clears the featured flag on the given review.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_featured:
                  type: boolean
                  description: 'Determines if the review is featured.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_review:
                  type: string
                  description: 'The review key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Review-ReviewFeature
  '/Wl/Review/ReviewReply.json':
    post:
      summary: 'Saves the reply text and optional status update for the given review. Validates edit access for the current user, persists the reply text and optional review status change, and records the replying staff or admin user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_review_status:
                  type: [number, "null"]
                  description: 'Status of the Review. One of {@link \RsReviewStatusSid} constants. `null` until passed to the api when admin replies to the review.'
                k_business:
                  type: string
                  description: 'Key of the business to which the review belongs. Empty string to defined business automatically, based on value of <tt>k_review</tt>. Although this value may be empty, this behavior is deprecated and will be removed in the future. You MUST pass key of the business always.'
                k_review:
                  type: string
                  description: 'The review key.'
                text_reply:
                  type: string
                  description: 'The reply text for review.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Review-ReviewReply
  '/Wl/Service/Service.json':
    get:
      summary: 'Loads appointment data.'
      parameters:
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Appointment primary key in {@link \RsServiceSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'Locations where an appointment may be booked. Primary keys in {@link \RsServiceSql} table.'
                      id_service_price:
                        type: number
                        format: int32
                        description: 'Service price mode. * One of the {@link \RsServicePriceSid} constants. * Copy of the {@link \RsServiceSql}.`id_service_price`.'
                      id_service_require:
                        type: number
                        format: int32
                        description: 'Mode to require amount while booking a resource. * One of the {@link \RsServiceRequireSid} constants. * Copy of the {@link \RsServiceSql}.`id_service_require`.'
                      is_staff_confirm:
                        type: boolean
                        description: 'Whether booking of the service required confirmation from staff member. * Copy of the {@link \RsServiceSql}.`is_staff_confirm`.'
      tags:
        - Service-Service
  '/Wl/Service/ServiceSort.json':
    post:
      summary: ''
      parameters:
        - name: id_order
          in: query
          description: 'Sort order ID. One of {@link \Core\Sid\SortOrderSid} constants. `null` if custom sort order is selected. See {@link \Wl\Backend\Setup\Services\ServicesSortSid::CUSTOM}.'
          schema:
            type: number
            format: int32
        - name: id_sort
          in: query
          description: 'Sort options ID, one of {@link \Wl\Backend\Setup\Services\ServicesSortSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-ServiceSort
  '/Wl/Skin/Skin.json':
    get:
      summary: 'Gets skin data.'
      parameters:
        - name: k_skin
          in: query
          description: 'Skin key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_skin:
                        type: array
                        description: 'Skin data. Result of the {@link \RsSkinData::skin()} method.'
                      id_skin:
                        type: number
                        format: int32
                        description: 'Skin type, one of {@link \RsSkinSid} constants.'
                      url_skin:
                        type: string
                        description: 'URL to the main page of the widget.'
      tags:
        - Skin-Skin
    post:
      summary: 'Saves the specified skin fields.'
      parameters:
        - name: k_skin
          in: query
          description: 'Skin key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field:
                  type: array
                  description: 'Skin fields to save. Key is full name of the skin field, it is separated string containing field name and element name from {@link \Wl\Skin\Application\Application::field_list()}. Value is new field value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Skin-Skin
  '/Wl/Skin/WidgetLeadSkin.json':
    get:
      summary: 'Gets list of Lead Widgets.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_lead_skin:
                        type: array
                        description: 'Lead skin list with the next structure: <dl> <dt>string `k_skin`</dt> <dd>Key of the skin. Primary key in {@link \RsSkinSql} table.</dd> <dt>string `text_title`</dt> <dd>Name of the skin.</dd> </dl>'
      tags:
        - Skin-WidgetLeadSkin
  '/Wl/Skin/SkinForeign.json':
    delete:
      summary: 'Deletes the existing widget. Permanently removes a widget skin and its foreign ID mapping from the business. Used by external integrators to clean up a widget that is no longer needed.'
      parameters:
        - name: id_skin
          in: query
          description: 'Skin type, one of {@link \RsSkinSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: s_foreign_id
          in: query
          description: 'Foreign skin key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_skin:
                        type: string
                        description: 'Skin key.'
      tags:
        - Skin-SkinForeign
    post:
      summary: 'Creates a new widget. Creates a new widget skin for the business and links it to an external identifier provided by the integrator. The foreign ID must be unique within the business; attempting to create a duplicate triggers an error directing the caller to use PUT instead.'
      parameters:
        - name: id_skin
          in: query
          description: 'Skin type, one of {@link \RsSkinSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: s_foreign_id
          in: query
          description: 'Foreign skin key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data_specific:
                  type: array
                  description: 'Data specific for the skin type. <i>It is passed to {@link \RsSkinData::data_set()} method.</i> All fields are optional and may be omitted if not needed. For Lead Capture widget it may contain: <dl> <dt>bool `is_add_lead`</dt> <dd>`true` if it can be used in staff back office to add leads by staff member.</dd> <dt>bool `is_welcome_email`</dt> <dd>`true` if welcome email should be sent to each added lead.</dd> <dt>string `k_promotion`</dt> <dd> Key of the purchase option to be granted to each added lead for free. <i>Primary key in the {@link \RsPromotionSql} table.</i> </dd> <dt>bool `is_redirect_self`</dt> <dd>`true` if redirection after lead is captured should be done in the same tab, `false` - in a new tab.</dd> <dt>string `url_redirect`</dt> <dd>URL for redirection after lead is captured.</dd> <dt>string `text_confirmation`</dt> <dd>Confirmation text to be shown after lead is added.</dd> </dl>'
                a_fields:
                  type: array
                  description: 'Profile fields, which should be shown on the widget and if they are required or optional. <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>bool `is_required`</dt> <dd>`true` if field should be required, `false` - optional.</dd> </dl>'
                text_name:
                  type: string
                  description: 'Name of the widget.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_skin:
                        type: string
                        description: 'Skin key.'
      tags:
        - Skin-SkinForeign
    put:
      summary: 'Updates the existing widget. Updates the configuration of an existing widget skin identified by the business and external foreign ID. Throws an error if no matching foreign ID is found, directing the caller to use POST instead.'
      parameters:
        - name: id_skin
          in: query
          description: 'Skin type, one of {@link \RsSkinSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: s_foreign_id
          in: query
          description: 'Foreign skin key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data_specific:
                  type: array
                  description: 'Data specific for the skin type. <i>It is passed to {@link \RsSkinData::data_set()} method.</i> All fields are optional and may be omitted if not needed. For Lead Capture widget it may contain: <dl> <dt>bool `is_add_lead`</dt> <dd>`true` if it can be used in staff back office to add leads by staff member.</dd> <dt>bool `is_welcome_email`</dt> <dd>`true` if welcome email should be sent to each added lead.</dd> <dt>string `k_promotion`</dt> <dd> Key of the purchase option to be granted to each added lead for free. <i>Primary key in the {@link \RsPromotionSql} table.</i> </dd> <dt>bool `is_redirect_self`</dt> <dd>`true` if redirection after lead is captured should be done in the same tab, `false` - in a new tab.</dd> <dt>string `url_redirect`</dt> <dd>URL for redirection after lead is captured.</dd> <dt>string `text_confirmation`</dt> <dd>Confirmation text to be shown after lead is added.</dd> </dl>'
                a_fields:
                  type: array
                  description: 'Profile fields, which should be shown on the widget and if they are required or optional. <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>bool `is_required`</dt> <dd>`true` if field should be required, `false` - optional.</dd> </dl>'
                text_name:
                  type: string
                  description: 'Name of the widget.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_skin:
                        type: string
                        description: 'Skin key.'
      tags:
        - Skin-SkinForeign
  '/Wl/Sms/SmsStatus.json':
    get:
      summary: 'Returns information about SMS settings.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_sms_number:
                        type: boolean
                        description: 'Indicates if business phone number configured.'
                      is_a2p_registration_due:
                        type: boolean
                        description: 'Indicates if the A2P10DLC registration for this business needs to be completed now.'
                      is_regulatory_bundle_required:
                        type: boolean
                        description: 'Indicates if a regulatory bundle submission is required for this business. `true` if the business locale requires a regulatory bundle (UK/Australia) and it has not been submitted yet.'
                      is_sms_enable:
                        type: boolean
                        description: 'Indicates if SMS is enabled for this business. Determines if SMS features are available.'
                      is_sms_notification_enable:
                        type: boolean
                        description: 'Indicates if SMS notifications are enabled for this business.'
                      text_a2p_registration_due_message:
                        type: [string, "null"]
                        description: 'Returns the message to be displayed when A2P10DLC registration is due for a business and the user attempts to access an SMS feature. `null` if registration is not due.'
      tags:
        - Sms-SmsStatus
  '/Wl/Staff/StaffPastWorkTimeOption.json':
    post:
      summary: 'Updates the state of "Show past dates" toggle in staff''s profile''s working hours tab.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Business key within which setting is managed.'
                show_work_time_past:
                  type: [string, "null"]
                  description: 'Option value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Staff-StaffPastWorkTimeOption
  '/Wl/Staff/StaffElement.json':
    post:
      summary: 'Update or create staff. Creates a new staff member or updates an existing one in the business. When creating, a new user account is provisioned if no matching email exists. When updating, the target staff member must already belong to the business. Role, location, contact details, employment dates, and custom profile fields can all be set in a single call.'
      parameters:
        - name: k_staff
          in: query
          description: 'The key of the staff member resolved and used internally by this API. This property is populated from {@link StaffElementApi::$uid_staff} in {@link StaffElementApi::post()} and returned in API result for compatibility. Passing `k_staff` in request payload is not supported and causes an error. <i>See {@link StaffElementApi::_normalizeStaffKey()} method.</i>'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: [array, "null"]
                  description: 'The list of locations where the staff member works. `null` means to not change the current value of the field.'
                dl_end:
                  type: [string, "null"]
                  description: 'Employment end date. `null` means to not change the current value of the field.'
                dl_start:
                  type: [string, "null"]
                  description: 'Employment start date. `null` means to not change the current value of the field.'
                id_gender:
                  type: [number, "null"]
                  description: 'Gender of staff member. One of {@link \AGenderSid} constants. `null` means to not change the current value of the field or set gender by default for new staff.'
                id_role:
                  type: [number, "null"]
                  description: 'ID of the default system role. One of {@link \RsPrivilegeRoleSid}. `null` means to not change the current value of the field.'
                is_employ:
                  type: [boolean, "null"]
                  description: 'Whether the staff is currently employed. `null` means to not change the current value of the field.'
                is_microsite:
                  type: [boolean, "null"]
                  description: 'Determines whether the staff member be shown on the directory site of the business. If `null`, the current value of the field shouldn''t be changed.'
                is_password_set_disable:
                  type: boolean
                  description: 'Whether password set is disabled. `true` - if the email is associated to a client profile which has a password set, `false` - otherwise.'
                is_uid:
                  type: boolean
                  description: 'Whether the staff member can to sign in.'
                k_business:
                  type: string
                  description: 'The key of the business in which the staff member is being created or edited. This field is required.'
                k_business_role:
                  type: [string, "null"]
                  description: 'Business role key. `null` if not set.'
                k_city:
                  type: [string, "null"]
                  description: 'City key. `null` means to not change the current value of the field.'
                k_location:
                  type: [string, "null"]
                  description: 'Home location key. `null` if not set.'
                text_address:
                  type: [string, "null"]
                  description: 'Address. `null` means to not change the current value of the field.'
                text_biography:
                  type: [string, "null"]
                  description: 'Staff biography. `null` means to not change the current value of the field.'
                text_city:
                  type: [string, "null"]
                  description: 'Custom city title. `null` means to not change the current value of the field or <var>k_city</var> is specified.'
                text_email:
                  type: [string, "null"]
                  description: 'The staff member''s email address. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_first_name:
                  type: [string, "null"]
                  description: 'The staff member''s first name. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_last_name:
                  type: [string, "null"]
                  description: 'The staff member''s last name. If `null`, the current value of the field shouldn''t be changed.'
                text_password:
                  type: [string, "null"]
                  description: 'The password. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_password_confirm:
                  type: [string, "null"]
                  description: 'Confirmation of the password. This field is required when creating a new staff member. If `null`, the current value of the field shouldn''t be changed.'
                text_phone_home:
                  type: [string, "null"]
                  description: 'Home phone. `null` means to not change the current value of the field.'
                text_phone_mobile:
                  type: [string, "null"]
                  description: 'Cell phone. `null` means to not change the current value of the field.'
                text_position:
                  type: [string, "null"]
                  description: 'The staff member''s job title. If `null`, the current value of the field shouldn''t be changed.'
                text_postal:
                  type: [string, "null"]
                  description: 'The staff member''s job title. If `null`, the current value of the field shouldn''t be changed.'
                uid_staff:
                  type: [string, "null"]
                  description: 'User key of a staff member. This is the supported request identifier for staff update operations. Value is normalized into {@link StaffElementApi::$k_staff} in {@link StaffElementApi::post()}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_staff:
                        type: [string, "null"]
                        description: 'The key of the staff member resolved and used internally by this API. This property is populated from {@link StaffElementApi::$uid_staff} in {@link StaffElementApi::post()} and returned in API result for compatibility. Passing `k_staff` in request payload is not supported and causes an error. <i>See {@link StaffElementApi::_normalizeStaffKey()} method.</i>'
      tags:
        - Staff-StaffElement
  '/Wl/Staff/StaffSort.json':
    post:
      summary: ''
      parameters:
        - name: id_order
          in: query
          description: 'Sort order ID. One of {@link \Core\Sid\SortOrderSid} constants. `null` if custom sort order is selected. See {@link \Wl\Backend\Setup\Staffs\StaffsSortSid::CUSTOM}.'
          schema:
            type: number
            format: int32
        - name: id_sort
          in: query
          description: 'Sort options ID, one of {@link \Wl\Backend\Setup\Staffs\StaffsSortSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_sort_changed
          in: query
          description: 'Whether sorting order is changed. `true` if changed, `false` - otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Staff-StaffSort
  '/Wl/Video/VideoElement.json':
    delete:
      summary: 'Deletes the specified video. Permanently removes the video, its localized CMS records, and all category associations from the business library. Also deletes the video file and thumbnail from storage. Requires backend access with the video library management privilege.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video
          in: query
          description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Video-VideoElement
    get:
      summary: 'Returns the data for the specified video. Returns detailed information about a single video, including title, description, duration, categories, tags, difficulty level, assigned staff, virtual session settings, and playback progress for the current user. In frontend mode, access is restricted to videos the current user is allowed to watch based on their membership and login type.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video
          in: query
          description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'The keys of the locations where this video is available.'
                      a_staff:
                        type: array
                        description: 'The keys of the user staff members who are on the video. <i>TODO wl-76380: remove this property when external developers confirm that they do not use it.</i>'
                      a_staff_info:
                        type: array
                        description: 'A list of staff members associated with the video. Every item has the following structure: <dl> <dt>string `k_staff`</dt> <dd><b>Deprecated</b> The staff member key. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `text_name`</dt> <dd>The staff member''s full name.</dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql}.`uid`.</i></dd> </dl>'
                      a_staff_uid:
                        type: array
                        description: 'The user IDs of the staff members who are on the video (authoritative list for who is assigned to the video).'
                      a_video_category:
                        type: array
                        description: 'The video category keys where this video can be found.'
                      a_video_tag:
                        type: array
                        description: 'The video tag keys.'
                      dtl_publish:
                        type: [string, "null"]
                        description: 'The date and time when the video was published. This will be `null` if the published date is not set yet.'
                      dtl_unpublish:
                        type: [string, "null"]
                        description: 'The date and time when the video was unpublished. This will be `null` if the unpublished date is not set yet.'
                      dtl_upload:
                        type: string
                        description: 'The date when the video was uploaded.'
                      html_embed:
                        type: string
                        description: 'The video embed code.'
                      i_calorie:
                        type: number
                        format: int32
                        description: 'The count of burned calories associated with the video.'
                      i_current_percent:
                        type: number
                        format: int32
                        description: 'The current time as a percentage of the video duration at which the user is at in the video.'
                      i_current_time:
                        type: number
                        format: int32
                        description: 'The current time in seconds at which the user is at in the video.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The video duration in seconds.'
                      i_watch:
                        type: number
                        format: int32
                        description: 'The number of video views.'
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      id_location_select:
                        type: number
                        format: int32
                        description: '{@link YesNoSid::NO} if the video is available in all locations. {@link YesNoSid::YES} if the video is available only in certain locations.'
                      id_source:
                        type: number
                        format: int32
                        description: 'Source of the video. One of {@link VideoSourceSid} constants.'
                      is_calorie:
                        type: boolean
                        description: 'If `true`, the calorie count will be displayed on the video. If `false`, the calorie count will be hidden on the video.'
                      is_converted:
                        type: boolean
                        description: 'If `true`, the video is converted. If `false`, the video isn''t converted.'
                      is_published:
                        type: boolean
                        description: 'If `true`, the video is published. Otherwise, this will be `false`.'
                      is_video_level:
                        type: boolean
                        description: 'If `true`, the video level for the video is enabled. If `false`, the video level for the video is disabled.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
                      k_video_binary:
                        type: string
                        description: 'The binary string video key.'
                      k_video_category_primary:
                        type: string
                        description: 'The video category primary key.'
                      k_video_level:
                        type: string
                        description: 'The video level key. This will be `0` if `All Levels` is selected.'
                      s_preview_video_key:
                        type: [string, "null"]
                        description: 'The string representation of the video''s key. This will be `null` if the video doesn''t have a thumbnail image.'
                      show_calorie:
                        type: boolean
                        description: 'If `false`, the calorie count for videos is hidden. Otherwise, this will be `true`.'
                      show_level:
                        type: boolean
                        description: 'If `false`, video levels are hidden. Otherwise, this will be `true`.'
                      show_view:
                        type: boolean
                        description: 'If `false`, the number of views for videos is hidden. Otherwise, this will be `true`.'
                      text_level_title:
                        type: string
                        description: 'The video level name.'
                      text_title:
                        type: string
                        description: 'The video name.'
                      url_thumbnail:
                        type: string
                        description: 'The URL of the video thumbnail.'
                      url_video:
                        type: [string, "null"]
                        description: 'The URL of the video file. This URL return with domain which you use when you call this API. In case when you make request to local domain and video is {@link VideoElementApi::$is_converted} links in file will have global domain and for correct work you must replace global domain to local. Example for `videojs` player you can override method `beforeRequest`: <code> videojs.Vhs.xhr.beforeRequest = function(options) { if (options.uri.startsWith(''https://www.'')) { options.uri = options.uri.replace(''https://www.'', ''https://us.''); } return options; } </code>'
                      url_video_direct:
                        type: string
                        description: 'The direct URL of the video.'
                      url_video_frame:
                        type: string
                        description: 'The direct URL of the video for iframe.'
                      xml_description:
                        type: string
                        description: 'The video description.'
      tags:
        - Video-VideoElement
    post:
      summary: 'Creates or updates a video for the specified business. Saves a video record with its title, description, categories, tags, difficulty level, staff assignments, and access settings. When no video key is provided, a new record is created; otherwise the existing record is updated. Requires backend access with the video library management privilege and an active video subscription.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: is_converted
          in: query
          description: 'If `true`, the video is converted. If `false`, the video isn''t converted.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video
          in: query
          description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: array
                  description: 'The keys of the locations where this video is available.'
                a_staff:
                  type: array
                  description: 'The keys of the user staff members who are on the video. <i>TODO wl-76380: remove this property when external developers confirm that they do not use it.</i>'
                a_staff_uid:
                  type: array
                  description: 'The user IDs of the staff members who are on the video (authoritative list for who is assigned to the video).'
                a_video_category:
                  type: array
                  description: 'The video category keys where this video can be found.'
                a_video_tag:
                  type: array
                  description: 'The video tag keys.'
                dtl_publish:
                  type: [string, "null"]
                  description: 'The date and time when the video was published. This will be `null` if the published date is not set yet.'
                dtl_unpublish:
                  type: [string, "null"]
                  description: 'The date and time when the video was unpublished. This will be `null` if the unpublished date is not set yet.'
                file_video:
                  type: array
                  description: 'Video file.'
                html_embed:
                  type: string
                  description: 'The video embed code.'
                i_calorie:
                  type: number
                  format: int32
                  description: 'The count of burned calories associated with the video.'
                i_duration:
                  type: number
                  format: int32
                  description: 'The video duration in seconds.'
                i_file_upload_size:
                  type: number
                  format: int32
                  description: 'The size of the uploaded file. If the file will be uploaded in parts, its size will be set. <i> In this case, the file will not be uploaded to this API. Method {@link VideoElementApi::_fileVideoGet()} will not return the result. </i>'
                id_location_select:
                  type: number
                  format: int32
                  description: '{@link YesNoSid::NO} if the video is available in all locations. {@link YesNoSid::YES} if the video is available only in certain locations.'
                id_source:
                  type: number
                  format: int32
                  description: 'Source of the video. One of {@link VideoSourceSid} constants.'
                is_calorie:
                  type: boolean
                  description: 'If `true`, the calorie count will be displayed on the video. If `false`, the calorie count will be hidden on the video.'
                is_video_level:
                  type: boolean
                  description: 'If `true`, the video level for the video is enabled. If `false`, the video level for the video is disabled.'
                k_video_category_primary:
                  type: string
                  description: 'The video category primary key.'
                k_video_level:
                  type: string
                  description: 'The video level key. This will be `0` if `All Levels` is selected.'
                s_command:
                  type: string
                  description: 'The action that must be performed to the video thumbnail image.'
                s_file_upload_id:
                  type: string
                  description: 'ID of the uploaded file. If the file will be uploaded in parts, upload ID will be set. In this case, the file will not be uploaded to this API. <i>Method {@link VideoElementApi::_fileVideoGet()} will not return the result.</i>'
                s_file_upload_name:
                  type: string
                  description: 'The real name of the uploaded file. If the file will be uploaded in parts, its name will be set. In this case, the file won''t be uploaded to this API and the POST method won''t return the result.'
                s_thumbnail:
                  type: string
                  description: 'The video thumbnail.'
                text_title:
                  type: string
                  description: 'The video name.'
                xml_description:
                  type: string
                  description: 'The video description.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video:
                        type: string
                        description: 'The video key. *Be careful, when use this property in code, use {@link VideoElementApi::$k_video_binary} instead.* In this property can be key in next format: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbShardKey::keyBinary()} or {@link DbKey::generate()}.</i>'
      tags:
        - Video-VideoElement
    put:
      summary: 'Validates video embed code. Checks that the provided embed snippet conforms to the supported embed code format. Throws if the snippet is malformed or uses an unsupported source. Call this before saving to give the user an immediate validation error without persisting the video record.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                html_embed:
                  type: string
                  description: 'The video embed code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
      tags:
        - Video-VideoElement
  '/Wl/Video/VideoList.json':
    get:
      summary: 'Returns the list of videos for the specified business. Returns videos for the business video library with optional filtering by category, tag, difficulty level, staff, location, calorie range, and duration. Supports sorting and pagination for large libraries. In frontend mode, results are restricted to videos the current user is allowed to watch based on their membership and login type.'
      parameters:
        - name: a_calorie
          in: query
          description: 'The calorie range ascribed to the video to use for the search. <dl> <dt>int `i_from`</dt> <dd>The minimum number of calories.</dd> <dt>int `i_to`</dt> <dd>The maximum number of calories.</dd> </dl>'
          schema:
            type: array
        - name: a_duration
          in: query
          description: 'The duration range ascribed to the video to use for the search. The interval is set in seconds. <dl> <dt>int `i_from`</dt> <dd>The minimum duration.</dd> <dt>int `i_to`</dt> <dd>The maximum duration.</dd> </dl>'
          schema:
            type: array
        - name: a_level
          in: query
          description: 'A list of levels to show videos for. A level can refer to the difficulty ascribed to the video.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'A list of locations to show videos from.'
          schema:
            type: array
        - name: a_source
          in: query
          description: 'A list of video sources.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'A list of staff members who appear in videos. <i>TODO wl-72248: remove this property when external developers confirm that they do not use it.</i>'
          schema:
            type: array
        - name: a_staff_uid
          in: query
          description: 'A list of staff members who appear in videos.'
          schema:
            type: array
        - name: a_video_category
          in: query
          description: 'A list of video categories to show videos from.'
          schema:
            type: array
        - name: a_video_tag
          in: query
          description: 'A list of video tags to show videos for.'
          schema:
            type: array
        - name: i_page
          in: query
          description: 'Page to return. Pagination are ignored when count of videos less than {@link VideoListApi::VIDEO_COUNT_PAGINATION}. `null` if you need to return all the videos.'
          schema:
            type: [number, "null"]
        - name: id_order
          in: query
          description: 'Sort order ID. One of {@link \Core\Sid\SortOrderSid} constants. `null` or 0 if order is undefined and depends on sort order.'
          schema:
            type: [number, "null"]
        - name: id_sort
          in: query
          description: 'The sorting type. A constant from {@link \Wl\Video\Catalog\Filter\Sort\FilterSortSid}. `null` or 0 if sort is undefined and custom sort order is used.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'The filter phrase to filter videos by name.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of the client who request list of videos. `null` if user is not signed in. NOTE: Not used directly in API, needed for {@link VideoListApi::KEY} constant.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of videos. Each element: <dl> <dt>array[] `a_staff`</dt> <dd> List of staff members associated with the video. <dl> <dt>string `uid_staff`</dt> <dd>UID of the staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `text_name`</dt> <dd>Full name of the staff.</dd> <dt>string `k_staff`</dt> <dd>Staff key. Only for legacy apps. <i>Primary key in {@link \RsStaffSql} table.</i></dd> </dl> </dd> <dt>array[] `a_video_category`</dt> <dd> List of video categories. <dl> <dt>string `k_video_category`</dt> <dd>Video category key. <i>Primary key in {@link \Wl\Video\Category\VideoCategorySql} table.</i></dd> <dt>string `text_title`</dt> <dd>Category name.</dd> </dl> </dd> <dt>array[] `a_video_tag`</dt> <dd> List of video tags. <dl> <dt>string `k_video_tag`</dt> <dd>Video tag key. <i>Primary key in {@link \Wl\Video\Tag\TagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Tag name.</dd> </dl> </dd> <dt>string|null `dtl_publish`</dt> <dd>Date and time when the video becomes published, in the business time zone. `null` if not set.</dd> <dt>string|null `dtl_unpublish`</dt> <dd>Date and time when the video becomes unpublished, in the business time zone. `null` if not set.</dd> <dt>string `dtl_upload`</dt> <dd>Date and time when the video was uploaded, in the business time zone.</dd> <dt>string `html_description`</dt> <dd>HTML description of the video parsed from `xml_description`.</dd> <dt>int `i_calorie`</dt> <dd>Number of calories burned during the video session.</dd> <dt>int `i_current_percent`</dt> <dd>Percentage of the video watched by the current user.</dd> <dt>int `i_current_time`</dt> <dd>Current watch position in seconds for the current user.</dd> <dt>int `i_duration`</dt> <dd>Video duration in seconds.</dd> <dt>int `i_watch`</dt> <dd>Total number of views.</dd> <dt>int `id_source`</dt> <dd>Video source. One of {@link \Wl\Video\VideoSourceSid} constants.</dd> <dt>bool `is_calorie`</dt> <dd>`true` if calorie information is enabled for this video; `false` otherwise.</dd> <dt>bool `is_converted`</dt> <dd>`true` if the video has been converted to HLS format; `false` otherwise.</dd> <dt>bool `is_published`</dt> <dd>`true` if the video is currently published; `false` otherwise.</dd> <dt>bool `is_video_level`</dt> <dd>`true` if a difficulty level is assigned to this video; `false` otherwise.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql}.</i></dd> <dt>string `k_video`</dt> <dd>Video key. <i>Primary key in {@link \Wl\Video\VideoSql}.</i></dd> <dt>string `k_video_binary`</dt> <dd>Binary video key. <i>See {@link \Core\Db\Shard\DbShardKey::keyBinary()}.</i></dd> <dt>string|null `k_video_level`</dt> <dd>Video level key. `null` if no level assigned. <i>Primary key in {@link \Wl\Video\Level\LevelSql} table.</i></dd> <dt>bool `show_calorie`</dt> <dd>`true` if calorie count should be displayed; `false` otherwise.</dd> <dt>bool `show_level`</dt> <dd>`true` if difficulty level should be displayed; `false` otherwise.</dd> <dt>bool `show_view`</dt> <dd>`true` if view count should be displayed; `false` otherwise.</dd> <dt>string `text_level_title`</dt> <dd>Difficulty level title.</dd> <dt>string `text_title`</dt> <dd>Video title.</dd> <dt>string|null `url_thumbnail`</dt> <dd>URL of the video thumbnail image.</dd> <dt>string|null `url_video`</dt> <dd> URL of the video file (HLS stream). `null` if not available. See {@link \Wl\Video\VideoElementApi::$url_video} for details about the video URL. </dd> </dl>'
                      a_page:
                        type: array
                        description: 'Pagination data. Empty array if pagination is not active (total video count is within {@link VideoListApi::VIDEO_COUNT_PAGINATION}). Otherwise contains a list of page entries as: <dl> <dt>bool `is-next`</dt> <dd>`true` for the "next page" navigation entry. Only present on the next-page entry.</dd> <dt>bool `is-previous`</dt> <dd>`true` for the "previous page" navigation entry. Only present on the previous-page entry.</dd> <dt>bool `is-selected`</dt> <dd>`true` if this entry represents the current page. Only present on the selected page entry.</dd> <dt>int `page`</dt> <dd>Page number. `0` represents a skipped range (ellipsis) between non-adjacent pages.</dd> <dt>bool `skip`</dt> <dd>`true` if this entry represents a skipped page range (ellipsis). Only present on skip entries.</dd> </dl>'
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      id_order:
                        type: [number, "null"]
                        description: 'Sort order ID. One of {@link \Core\Sid\SortOrderSid} constants. `null` or 0 if order is undefined and depends on sort order.'
                      id_sort:
                        type: [number, "null"]
                        description: 'The sorting type. A constant from {@link \Wl\Video\Catalog\Filter\Sort\FilterSortSid}. `null` or 0 if sort is undefined and custom sort order is used.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
      tags:
        - Video-VideoList
    put:
      summary: 'Updates the custom sort order of videos for the specified business. Reorders the video library according to the provided list. Only takes effect when the library is configured to use the custom sort order. Requires backend access with the video library management privilege.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of shared video keys in their saved order.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_embed_source:
                        type: [number, "null"]
                        description: 'Embedded video source. One of {@link VideoEmbedSourceSid} constants. `null` if video is uploaded.'
                      json_setup:
                        type: string
                        description: 'Video.js media player initialization parameters in JSON format. Usage example: <script> videojs($(''video''), { "techOrder": ["Vimeo"], "sources": [ {"type": "video/vimeo", "src": "https://vimeo.com/123456789"} ] }); </script>'
      tags:
        - Video-VideoList
  '/Wl/Video/VideoLimit.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_limit:
                        type: boolean
                        description: 'Whether business has reach limits for video upload.'
                      i_limit:
                        type: [number, string]
                        description: 'Maximum video size in bytes.'
                      id_upgrade_plan:
                        type: [number, "null"]
                        description: 'Subscription plan proposed for upgrade if limit reached and this subscription plan can avoid this limit. One of {@link FitvidSubscriptionSid} constants. <tt>null</tt> if plan can not be upgraded.'
                      m_upgrade_price:
                        type: [string, "null"]
                        description: 'Monthly payment amount for subscription plan proposed for upgrade if limit reached and this subscription plan can avoid this limit. <tt>null</tt> if plan can not be upgraded.'
      tags:
        - Video-VideoLimit
  '/Wl/Zoom/ZoomRecordingComplete.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event:
                  type: number
                  format: int32
                  description: 'Event type.'
                o_payload:
                  type: object
                  description: 'Recording payload information.'
                s_download_token:
                  type: string
                  description: 'Token string that should be added to video download link like parameter <tt>access_token</tt> to be able to download video without password validation.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_encrypted:
                        type: string
                        description: 'Encrypted token string.'
                      s_token_plain:
                        type: string
                        description: 'Plain token string.'
      tags:
        - Zoom-ZoomRecordingComplete
  '/Wl/Zoom/ZoomUserList.json':
    get:
      summary: ''
      parameters:
        - name: k_business_owner
          in: query
          description: 'Business key which currently owns zoom user. <tt>null</tt> in a case of system business.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_license_list:
                        type: array
                        description: 'List of all licensed for given business owner.'
                      f_license_use:
                        type: string
                        description: 'Percent of license in use.'
      tags:
        - Zoom-ZoomUserList
  '/Wl/Zoom/ZoomUserSync.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event:
                  type: number
                  format: int32
                  description: 'Event type.'
                o_payload:
                  type: object
                  description: 'User payload information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_encrypted:
                        type: string
                        description: 'Encrypted token string.'
                      s_token_plain:
                        type: string
                        description: 'Plain token string.'
      tags:
        - Zoom-ZoomUserSync
  '/Wl/Zoom/ZoomRecordingPause.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event:
                  type: number
                  format: int32
                  description: 'Event type.'
                o_payload:
                  type: object
                  description: 'Recording payload information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_encrypted:
                        type: string
                        description: 'Encrypted token string.'
                      s_token_plain:
                        type: string
                        description: 'Plain token string.'
      tags:
        - Zoom-ZoomRecordingPause
  '/Wl/Zoom/ZoomMeetingEnd.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event:
                  type: number
                  format: int32
                  description: 'Event type.'
                o_payload:
                  type: object
                  description: 'Meeting payload information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_encrypted:
                        type: string
                        description: 'Encrypted token string.'
                      s_token_plain:
                        type: string
                        description: 'Plain token string.'
      tags:
        - Zoom-ZoomMeetingEnd
  '/Wl/Zoom/ZoomUserRelease.json':
    put:
      summary: ''
      parameters:
        - name: k_business_use
          in: query
          description: 'Business key which currently use zoom user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_user_id:
                  type: string
                  description: 'Zoom user ID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zoom-ZoomUserRelease
  '/Wl/Zoom/ZoomAccountSync.json':
    post:
      summary: ''
      parameters:
        - name: k_business_owner
          in: query
          description: 'Business key which currently use zoom user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zoom-ZoomAccountSync
  '/Wl/Location/LocationActiveLog.json':
    post:
      summary: 'Edits history of location activation change.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_operation:
                  type: string
                  description: 'Local date of operation.'
                k_business:
                  type: string
                  description: 'Primary key of business in {@link \RsBusinessSql} table.'
                k_location:
                  type: string
                  description: 'Primary key of location in {@link \RsLocationSql} table.'
                k_location_active_log:
                  type: string
                  description: 'Primary key of location active log in {@link \Wl\Location\LocationActiveLogSql} table.'
                text_reason:
                  type: string
                  description: 'Reason of the change of location status.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Location-LocationActiveLog
  '/Wl/Location/List.json':
    get:
      summary: 'Retrieves a list of locations for the given business. Accepts a single business key, a JSON-encoded list of business keys, or an array of business keys, and returns location details including coordinates, address, timezone, directories, and logo URLs. For franchisor businesses, automatically expands to include all franchisee locations.'
      parameters:
        - name: a_business
          in: query
          description: 'A list of business keys. You can specify this instead of {@link ListApi::$k_business} to load data for many businesses in one call.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The business key used internally by WellnessLiving. In case when passed regular or franchisee business returns only locations for requested business. In case when passed franchisor business returns locations for all franchisees of this franchisor.'
          schema:
            type: string
        - name: s_business
          in: query
          description: 'The primary keys of the selected businesses. You can specify this instead of {@link ListApi::$k_business} to load data for many businesses in one call. This is the same as <var>$a_business</var>, but serialized with JSON (to make the URL shorter).'
          schema:
            type: string
        - name: show_remove
          in: query
          description: 'Determines whether removed locations should be returned.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'Information about the business''s location(s). If you''ve specified multiple businesses for this endpoint, this will return location information for multiple businesses. Keys refer to location keys. Values refer to nested arrays with the next keys: <dl> <dt>int[] `a_directories`</dt> <dd>List of directories from {@link \RsProjectSid}, where location is published.</dd> <dt>array `a_timezone`</dt> <dd> Information about location timezone: <dl> <dt>string `k_timezone`</dt> <dd>Timezone key. <i>Primary key in the {@link \AGeoTimezoneSql} table.</i></dd> <dt>string `text_abbr`</dt> <dd>User-friendly short timezone abbreviation, for example ''EST'' or ''PST''.</dd> <dt>string `text_name`</dt> <dd>Full timezone name, for example ''America/New_York''.</dd> </dl> </dd> <dt>float `f_latitude`</dt> <dd>The location latitude.</dd> <dt>float `f_longitude`</dt> <dd>The location longitude.</dd> <dt>int `i_shift`</dt> <dd>Timezone shift in hours.</dd> <dt>string `k_business`</dt> <dd>The business key.</dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_region`</dt> <dd>Region key. <i>Primary key in {@link \AGeoSql} table.</i></dd> <dt>string `s_mail`</dt> <dd>The location email address.</dd> <dt>string `s_phone`</dt> <dd>The location phone number.</dd> <dt>string `s_title`</dt> <dd>The location title.</dd> <dt>string `text_address`</dt> <dd>The location''s full address.</dd> <dt>string `url_logo`</dt> <dd> The location logo''s URL. This will only be set if the location has a logo and contains small thumbnail of the image. </dd> <dt>string `url_logo_source`</dt> <dd>The location logo''s URL. This will only be set if the location has a logo and contains full uploaded image.</dd> </dl>'
      tags:
        - Location-List
  '/Wl/Location/Location.json':
    post:
      summary: 'Creates new location or edits the existing location. When {@link LocationApi::$k_location} is omitted, creates a new location requiring {@link LocationApi::$k_city}, {@link LocationApi::$text_address}, and {@link LocationApi::$text_title}. When {@link LocationApi::$k_location} is provided, updates only the supplied fields of the existing location. Returns the key of the created or updated location.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of the business. This field is required.'
                k_city:
                  type: [string, "null"]
                  description: 'The key of the city. This field is required for creating a new location.'
                k_location:
                  type: [string, "null"]
                  description: 'The key of the location. This will be `null` if it''s being used to create a new location.'
                k_timezone:
                  type: [string, "null"]
                  description: 'The key of the time zone. This field is optional.'
                text_address:
                  type: [string, "null"]
                  description: 'The address of the location. This field is required for creating a new location.'
                text_email:
                  type: [string, "null"]
                  description: 'The mail address.'
                text_phone:
                  type: [string, "null"]
                  description: 'The phone number.'
                text_postal:
                  type: [string, "null"]
                  description: 'The postal code.'
                text_title:
                  type: [string, "null"]
                  description: 'The title of the location. This field is required for creating a new location.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_location:
                        type: [string, "null"]
                        description: 'The key of the location. This will be `null` if it''s being used to create a new location.'
      tags:
        - Location-Location
  '/Wl/Location/LocationOption.json':
    post:
      summary: 'Updates location customer status, count of customers locations and adds log.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_start:
                  type: string
                  description: 'Local date of operation.'
                is_customer:
                  type: boolean
                  description: 'Whether location is fee.'
                k_business:
                  type: string
                  description: 'Key of business. Primary key in {@link \RsBusinessSql} table.'
                k_location:
                  type: string
                  description: 'Key of location. Primary key in {@link \RsLocationSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Location-LocationOption
  '/Wl/Location/ListBulk.json':
    get:
      summary: 'Gets location lists for a bulk of businesses. Accepts a JSON-encoded list of business keys, a JSON-encoded list of location keys, or both, and returns short-form location data alongside full location detail objects. Optionally filters results to only locations that have a specific directory integration enabled.'
      parameters:
        - name: id_directory
          in: query
          description: 'The ID of the directory if locations should be filtered by enabling directory integration. `0` if a directory filter isn''t required.'
          schema:
            type: number
            format: int32
        - name: s_business
          in: query
          description: 'A list of businesses. Business primary keys are serialized with JSON. Empty string if you need all locations in the system.'
          schema:
            type: string
        - name: s_location
          in: query
          description: 'A list of locations. Location primary keys are serialized with JSON.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'Short-form information about locations. Keys refer to location primary keys. Values refer to sub-arrays with the next keys: <dl> <dt>float `f_latitude`</dt> <dd>The latitude coordinate.</dd> <dt>float `f_longitude`</dt> <dd>The longitude coordinate.</dd> <dt>float `f_rate`</dt> <dd>The location rate.</dd> <dt>int `i_order`</dt> <dd>The order number.</dd> <dt>string `k_business`</dt> <dd>The business''s primary key.</dd> <dt>string `k_location`</dt> <dd>The location''s primary key.</dd> <dt>string `s_title`</dt> <dd>The location title.</dd> <dt>string `text_address`</dt> <dd>The full location address.</dd> <dt>string `url_logo`</dt> <dd>The location logo.</dd> </dl>'
                      a_location_full:
                        type: array
                        description: 'A list of models with full information about each location. <dl> <dt>int[] `a_age`</dt> <dd>A list of ages that are permitted for visiting this location.</dd> <dt>int[] `a_amenities`</dt> <dd>A list of facilities that are available in this location.</dd> <dt>string[] `a_level`</dt> <dd>A list of levels that are suitable for visiting this location.</dd> <dt>array `a_logo`</dt> <dd> Information about the location logo used in WellnessLiving: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl> </dd> <dt>array `a_slide`</dt> <dd> A list of the location images. Every element has the following keys: <dl> <dt>int `i_height`</dt> <dd>Height.</dd> <dt>int `i_width`</dt> <dd>Width.</dd> <dt>string `url_preview`</dt> <dd>URL to image preview.</dd> <dt>string `url_slide`</dt> <dd>URL to full image.</dd> </dl> </dd> <dt>array `a_work`</dt> <dd> The hours of operation for the location. Fields are numbers representing specific days (1 is Monday, 7 is Sunday). Values are objects with the next fields: <dl> <dt>string `s_end`</dt> <dd>The time of day end in MySQL format.</dd> <dt>string `s_start`</dt> <dd>The time of day start in MySQL format.</dd> </dl> </dd> <dt>string|null `dtu_check`</dt> <dd> The date when the location actuality was last checked. `null` if the location has never been checked. Set only in {@link \Wl\Location\ListBulkApi}, otherwise always `null`. </dd> <dt>string `dtu_create`</dt> <dd>The date when the location was created.</dd> <dt>float `f_latitude`</dt> <dd>The latitude coordinate of the location.</dd> <dt>float `f_longitude`</dt> <dd>The longitude coordinate of the location.</dd> <dt>string `html_description_full`</dt> <dd>The full description of the location.</dd> <dt>string `html_description_preview`</dt> <dd> A shorter description of the location. A preview of {@link \Wl\Location\View\ViewApi::$html_description_full}. </dd> <dt>int `i_logo_height`</dt> <dd>Maximum location image height.</dd> <dt>int `i_logo_width`</dt> <dd>Maximum location image width.</dd> <dt>int|null `id_industry`</dt> <dd> The industry of the business. `null` in case when industry didn''t set for chosen business type, or for business types where set value which is absent. </dd> <dt>bool `is_phone`</dt> <dd>`true` if to display phone number on location page. `false` otherwise.</dd> <dt>bool `is_top_choice`</dt> <dd>`true` if WellnessLiving identifies this is a top choice location, `false` otherwise.</dd> <dt>string `k_business`</dt> <dd>The key of the business this location belongs to.</dd> <dt>string `k_business_type`</dt> <dd>The key of the business type this location belongs to.</dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_timezone`</dt> <dd>The timezone.</dd> <dt>string `s_address`</dt> <dd>The physical address of the location.</dd> <dt>string `s_map`</dt> <dd>A string that can be used in navigator programs.</dd> <dt>string `s_phone`</dt> <dd>The phone number for the location.</dd> <dt>string `s_timezone`</dt> <dd>The system name of the time zone.</dd> <dt>string `s_title`</dt> <dd>The title of the location.</dd> <dt>string `text_address_individual`</dt> <dd>The address of the location.</dd> <dt>string `text_alias`</dt> <dd> The special system name, which can be used in custom location links. This is usually based on name of the location, with extra characters removed. However, it can also can be fully customized. </dd> <dt>string `text_business_type`</dt> <dd>The name of the business type.</dd> <dt>string `text_city`</dt> <dd>The city name of the location.</dd> <dt>string `text_country`</dt> <dd>The country name of the location.</dd> <dt>string `text_industry`</dt> <dd>The name of the industry of the business.</dd> <dt>string `text_mail`</dt> <dd>The email address.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the location.</dd> <dt>string `text_region`</dt> <dd>The region name of the location.</dd> <dt>string `text_region_code`</dt> <dd>Region 2 or 3 letters abbreviation of the location. Can be empty, if abbreviation for region is not set.</dd> <dt>string `url_facebook`</dt> <dd>The Facebook URL of the location.</dd> <dt>string `url_instagram`</dt> <dd>The Instagram URL of the location.</dd> <dt>string `url_linkedin`</dt> <dd>The Linked In URL of the location.</dd> <dt>string `url_map`</dt> <dd>The URL of the location in Google Maps.</dd> <dt>string `url_microsite`</dt> <dd>The URL to the location entry in the WellnessLiving Explorer listing.</dd> <dt>string `url_site`</dt> <dd>The website URL of the location.</dd> <dt>string `url_twitter`</dt> <dd>The URL of the location''s Twitter account.</dd> <dt>string `url_web`</dt> <dd>The URL to the business''s Client Web App.</dd> <dt>string `url_youtube`</dt> <dd>The YouTube URL of the location.</dd> </dl>'
      tags:
        - Location-ListBulk
  '/Wl/Location/LocationActiveEdit.json':
    delete:
      summary: 'Deletes location active log.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Primary key of location in {@link \RsLocationSql} table.'
          schema:
            type: string
        - name: k_location_active_log
          in: query
          description: 'Key of the location active log.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Location-LocationActiveEdit
    get:
      summary: 'Returns confirmation message that should be shown during the edit process of the location status.'
      parameters:
        - name: is_churn
          in: query
          description: 'Whether type of operation is the churn, otherwise - reactivation.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Primary key of location in {@link \RsLocationSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_confirm:
                        type: string
                        description: 'Confirmation message.'
      tags:
        - Location-LocationActiveEdit
    post:
      summary: 'Edits location active status.'
      parameters:
        - name: dl_operation
          in: query
          description: 'Local date of operation.'
          schema:
            type: string
        - name: is_churn
          in: query
          description: 'Whether type of operation is the churn, otherwise - reactivation.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Primary key of location in {@link \RsLocationSql} table.'
          schema:
            type: string
        - name: text_reason
          in: query
          description: 'Reason of the change of location status.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Location-LocationActiveEdit
  '/Wl/Location/LocationAddPermission.json':
    get:
      summary: 'Checks if the user has the privilege to add locations.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_add_permission:
                        type: boolean
                        description: '`true` if the user has the privilege to add locations, `false` - otherwise.'
      tags:
        - Location-LocationAddPermission
  '/Wl/Location/List73.json':
    get:
      summary: 'Retrieves a list of locations for the given business. Accepts a single business key, a JSON-encoded list of business keys, or an array of business keys, and returns location details including coordinates, address, timezone, directories, and logo URLs. For franchisor businesses, automatically expands to include all franchisee locations.'
      parameters:
        - name: a_business
          in: query
          description: 'A list of business keys. You can specify this instead of {@link ListApi::$k_business} to load data for many businesses in one call.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The business key used internally by WellnessLiving. In case when passed regular or franchisee business returns only locations for requested business. In case when passed franchisor business returns locations for all franchisees of this franchisor.'
          schema:
            type: string
        - name: s_business
          in: query
          description: 'The primary keys of the selected businesses. You can specify this instead of {@link ListApi::$k_business} to load data for many businesses in one call. This is the same as <var>$a_business</var>, but serialized with JSON (to make the URL shorter).'
          schema:
            type: string
        - name: show_remove
          in: query
          description: 'Determines whether removed locations should be returned.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'Information about the business''s location(s). If you''ve specified multiple businesses for this endpoint, this will return location information for multiple businesses. Keys refer to location keys. Values refer to nested arrays with the next keys: <dl> <dt>int[] `a_directories`</dt> <dd>List of directories from {@link \RsProjectSid}, where location is published.</dd> <dt>array `a_timezone`</dt> <dd> Information about location timezone: <dl> <dt>string `k_timezone`</dt> <dd>Timezone key. <i>Primary key in the {@link \AGeoTimezoneSql} table.</i></dd> <dt>string `text_abbr`</dt> <dd>User-friendly short timezone abbreviation, for example ''EST'' or ''PST''.</dd> <dt>string `text_name`</dt> <dd>Full timezone name, for example ''America/New_York''.</dd> </dl> </dd> <dt>float `f_latitude`</dt> <dd>The location latitude.</dd> <dt>float `f_longitude`</dt> <dd>The location longitude.</dd> <dt>int `i_shift`</dt> <dd>Timezone shift in hours.</dd> <dt>string `k_business`</dt> <dd>The business key.</dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_region`</dt> <dd>Region key. <i>Primary key in {@link \AGeoSql} table.</i></dd> <dt>string `s_mail`</dt> <dd>The location email address.</dd> <dt>string `s_phone`</dt> <dd>The location phone number.</dd> <dt>string `s_title`</dt> <dd>The location title.</dd> <dt>string `text_address`</dt> <dd>The location''s full address.</dd> <dt>string `url_logo`</dt> <dd> The location logo''s URL. This will only be set if the location has a logo and contains small thumbnail of the image. </dd> <dt>string `url_logo_source`</dt> <dd>The location logo''s URL. This will only be set if the location has a logo and contains full uploaded image.</dd> </dl>'
      tags:
        - Location-List73
  '/Wl/Fitbuilder/Message.json':
    post:
      summary: 'Notifies messengers with new information. Accepts a channel key and a data payload, then dispatches a notification to the Fitbuilder messenger channel associated with that key. Requires the `notify` API privilege.'
      parameters:
        - name: k_channel
          in: query
          description: 'Messenger channel key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Messenger information: <dl> <dt>array `message`</dt> <dd> Message information: <dl> <dt>int `id`</dt> <dd>Message key.</dd> <dt>string `message`</dt> <dd>Message text.</dd> <dt>string `updated_at`</dt> <dd>Date/time when the message has been posted.</dd> </dl> </dd> <dt>array `user_profile`</dt> <dd> User''s information: <dl> <dt>int `id_gender`</dt> <dd>User gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>string `s_first_name`</dt> <dd>User first name.</dd> <dt>string `s_last_name`</dt> <dd>User last name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_photo`</dt> <dd>URL of the user''s profile photo.</dd> </dl> </dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Fitbuilder-Message
  '/Wl/Promotion/PromotionList.json':
    get:
      summary: 'Returns promotion list of the specified business. Returns all active promotions for the given business, with optional inclusion of Enterprise Location promotions when the business is a franchisor and `is_franchise` is set to `true`.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Determines whether to return promotions created by Enterprise Locations (for Enterprise Cloud businesses only). If `true`, promotions created by Enterprise Locations are included. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key used to get the promotions.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'A list of promotions. <dl> <dt>int `id_program`</dt> <dd>The promotion program. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_program_type`</dt> <dd>The type of the promotion program. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether the promotion is active.</dd> <dt>string `k_promotion`</dt> <dd>The key of the promotion. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The title of the promotion.</dd> </dl>'
      tags:
        - Promotion-PromotionList
  '/Wl/Promotion/Promotion.json':
    delete:
      summary: 'Soft-deletes the specified promotion if it has no associated sales. Validates staff privileges and checks that the promotion has not been sold before marking it as removed.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-Promotion
    get:
      summary: 'Returns promotion by the specified business and promotion keys. Returns full promotion details including access restrictions, image, pricing, duration, tags, locations, and guest pass settings if applicable. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion information. <dl> <dt>array `a_access`</dt> <dd> Information about services that can be attended with this pass or membership. <dl> <dt>array[] `a_class`</dt> <dd> A list of available classes. <dl> <dt>string `k_class`</dt> <dd>Key of the class.</dd> </dl> </dd> <dt>array[] `a_event`</dt> <dd> A list of available events. <dl> <dt>string `k_class`</dt> <dd>Key of the event.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of available assets. <dl> <dt>string `k_resource`</dt> <dd>Key of the asset.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> A list of available appointment types. <dl> <dt>string `k_service`</dt> <dd>Key of the appointment type.</dd> </dl> </dd> <dt>bool `is_class_all`</dt> <dd> This will be `true` if any class in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected classes can be visited with this Purchase Option. Selected classes can be found in the list of available classes and events. </dd> <dt>bool `is_event_all`</dt> <dd> This will be `true` if any event in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected events can be visited with this Purchase Option. Selected events can be found in the list of available classes and events. </dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Information about Purchase Option image. This will be an empty array if the Purchase Option doesn''t have an image. <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>string `url-thumbnail`</dt> <dd>The link to the image.</dd> </dl> </dd> <dt>string[] `a_location`</dt> <dd> A list of locations where this promotion is available. <i>Each value is primary key in {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Login\Type\Sql}. `k_login_type` which is a primary key in {@link \Wl\Login\Type\Sql}. </i> </dd> <dt>string[] `a_member_group`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Member\Group\PromotionMemberGroupSql}. `k_member_group` which is a primary key in {@link \Wl\Member\Group\Sql}. </i> </dd> <dt>string[] `a_tag`</dt> <dd>List of selected tags. <i>Each value is a primary key in {@link \RsTagSql} table.</i></dd> <dt>string `dl_expire`</dt> <dd> The local expiration date. This is the last day when the Purchase Option is active. This is used only when the ''Expires on a certain date'' duration type is set. </dd> <dt>string `dl_terminate`</dt> <dd>Date when promotion should be terminated. `null` if there is no terminate date.</dd> <dt>string `f_revenue_single`</dt> <dd>Approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `f_revenue_single`.</i></dd> <dt>int `i_duration`</dt> <dd> The number of periods for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will be 12. </dd> <dt>int `i_limit`</dt> <dd> The number of visits that the owner can use for the Purchase Option. This will be `0` if the Purchase Option allows for unlimited visits. </dd> <dt>int `id_duration`</dt> <dd> The type of period for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will display the ID of the month. For all possible options, see {@link \ADurationSid}. </dd> <dt>int `id_duration_type`</dt> <dd> The duration type. Possible duration types can be ''Without End'', ''Expires on a certain date'', ''Period''. For more information, see {@link \RsDurationTypeSid}. </dd> <dt>int `id_program`</dt> <dd>The Purchase Option type. For more information, see {@link \RsProgramSid}.</dd> <dt>int `id_restriction`</dt> <dd>Purchase restriction for this product, one of {@link \Wl\Promotion\PurchaseRestrictionSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether this promotion is active. <i>Copy of {@link \RsPromotionSql} `is_active`.</i></dd> <dt>bool `is_business_wide`</dt> <dd> Whether this promotion is available for all locations of the business. <i>Copy of {@link \RsPromotionSql} `is_business_wide`.</i> </dd> <dt>bool `is_introductory`</dt> <dd>If `true`, this promotion is for introductory clients. Otherwise, this will be `false`.</dd> <dt>bool `is_online`</dt> <dd>If `true`, this promotion is available for online purchase. Otherwise, this will be `false`.</dd> <dt>bool `is_payroll`</dt> <dd> Whether clients who purchase this item excluded from payroll calculations. <i>Copy of {@link \RsPromotionSql} `is_payroll`.</i> </dd> <dt>bool `is_revenue_single`</dt> <dd> If `true` need to add approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `is_revenue_single`.</i> </dd> <dt>bool `is_use`</dt> <dd>Whether promotion can be used for booking. `null` if there is no termination.</dd> <dt>string `k_promotion`</dt> <dd>The key of the promotion. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_tag_primary`</dt> <dd>Primary tag. <i>Primary key in {@link \RsTagSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The price.</dd> <dt>string `text_description`</dt> <dd>The description of the Purchase Option.</dd> <dt>string `text_title`</dt> <dd>The title of the Purchase Option.</dd> <dt>string `url_buy`</dt> <dd>The direct URL to the promotion purchase page.</dd> <dt>string `url_catalog`</dt> <dd>The direct URL to the promotion details page in the online store.</dd> </dl>'
                      o_guest_settings:
                        type: [object, array, "null"]
                        description: 'Guest passes settings for promotion. This will be `null` if there are no guest pass settings for the promotion.'
      tags:
        - Promotion-Promotion
    post:
      summary: 'Saves promotion data. Accepts the full promotion configuration including title, description, access rules, duration, tags, and guest pass settings, then validates and persists the changes.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_promotion:
                  type: array
                  description: 'Promotion information. <dl> <dt>array `a_access`</dt> <dd> Information about services that can be attended with this pass or membership. <dl> <dt>array[] `a_class`</dt> <dd> A list of available classes. <dl> <dt>string `k_class`</dt> <dd>Key of the class.</dd> </dl> </dd> <dt>array[] `a_event`</dt> <dd> A list of available events. <dl> <dt>string `k_class`</dt> <dd>Key of the event.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of available assets. <dl> <dt>string `k_resource`</dt> <dd>Key of the asset.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> A list of available appointment types. <dl> <dt>string `k_service`</dt> <dd>Key of the appointment type.</dd> </dl> </dd> <dt>bool `is_class_all`</dt> <dd> This will be `true` if any class in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected classes can be visited with this Purchase Option. Selected classes can be found in the list of available classes and events. </dd> <dt>bool `is_event_all`</dt> <dd> This will be `true` if any event in the business can be visited with this Purchase Option. Otherwise, this will be `false` if only selected events can be visited with this Purchase Option. Selected events can be found in the list of available classes and events. </dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Information about Purchase Option image. This will be an empty array if the Purchase Option doesn''t have an image. <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>string `url-thumbnail`</dt> <dd>The link to the image.</dd> </dl> </dd> <dt>string[] `a_location`</dt> <dd> A list of locations where this promotion is available. <i>Each value is primary key in {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Login\Type\Sql}. `k_login_type` which is a primary key in {@link \Wl\Login\Type\Sql}. </i> </dd> <dt>string[] `a_member_group`</dt> <dd> A list of login types linked with this promotion. <i> Value is a copy of {@link \Wl\Promotion\Member\Group\PromotionMemberGroupSql}. `k_member_group` which is a primary key in {@link \Wl\Member\Group\Sql}. </i> </dd> <dt>string[] `a_tag`</dt> <dd>List of selected tags. <i>Each value is a primary key in {@link \RsTagSql} table.</i></dd> <dt>string `dl_expire`</dt> <dd> The local expiration date. This is the last day when the Purchase Option is active. This is used only when the ''Expires on a certain date'' duration type is set. </dd> <dt>string `dl_terminate`</dt> <dd>Date when promotion should be terminated. `null` if there is no terminate date.</dd> <dt>string `f_revenue_single`</dt> <dd>Approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `f_revenue_single`.</i></dd> <dt>int `i_duration`</dt> <dd> The number of periods for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will be 12. </dd> <dt>int `i_limit`</dt> <dd> The number of visits that the owner can use for the Purchase Option. This will be `0` if the Purchase Option allows for unlimited visits. </dd> <dt>int `id_duration`</dt> <dd> The type of period for the ''Period'' duration type. For example, if the duration of the Purchase Option is 12 months, this field will display the ID of the month. For all possible options, see {@link \ADurationSid}. </dd> <dt>int `id_duration_type`</dt> <dd> The duration type. Possible duration types can be ''Without End'', ''Expires on a certain date'', ''Period''. For more information, see {@link \RsDurationTypeSid}. </dd> <dt>int `id_program`</dt> <dd>The Purchase Option type. For more information, see {@link \RsProgramSid}.</dd> <dt>int `id_restriction`</dt> <dd>Purchase restriction for this product, one of {@link \Wl\Promotion\PurchaseRestrictionSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether this promotion is active. <i>Copy of {@link \RsPromotionSql} `is_active`.</i></dd> <dt>bool `is_business_wide`</dt> <dd> Whether this promotion is available for all locations of the business. <i>Copy of {@link \RsPromotionSql} `is_business_wide`.</i> </dd> <dt>bool `is_introductory`</dt> <dd>If `true`, this promotion is for introductory clients. Otherwise, this will be `false`.</dd> <dt>bool `is_online`</dt> <dd>If `true`, this promotion is available for online purchase. Otherwise, this will be `false`.</dd> <dt>bool `is_payroll`</dt> <dd> Whether clients who purchase this item excluded from payroll calculations. <i>Copy of {@link \RsPromotionSql} `is_payroll`.</i> </dd> <dt>bool `is_revenue_single`</dt> <dd> If `true` need to add approximate revenue per session value. <i>Copy of {@link \RsPromotionSql} `is_revenue_single`.</i> </dd> <dt>bool `is_use`</dt> <dd>Whether promotion can be used for booking. `null` if there is no termination.</dd> <dt>string `k_promotion`</dt> <dd>The key of the promotion. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_tag_primary`</dt> <dd>Primary tag. <i>Primary key in {@link \RsTagSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The price.</dd> <dt>string `text_description`</dt> <dd>The description of the Purchase Option.</dd> <dt>string `text_title`</dt> <dd>The title of the Purchase Option.</dd> <dt>string `url_buy`</dt> <dd>The direct URL to the promotion purchase page.</dd> <dt>string `url_catalog`</dt> <dd>The direct URL to the promotion details page in the online store.</dd> </dl>'
                o_guest_settings:
                  type: [object, array, "null"]
                  description: 'Guest passes settings for promotion. This will be `null` if there are no guest pass settings for the promotion.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-Promotion
  '/Wl/Lead/LeadCheck.json':
    post:
      summary: 'Checks if user with specified email already registered for specified business.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of business to which the new user must be captured.'
                s_mail:
                  type: string
                  description: 'User email.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_lead:
                        type: boolean
                        description: '`true` if user with specified email is lead of a specified business, `false` - otherwise.'
                      is_member:
                        type: boolean
                        description: '`true` if user with specified email is a member of a specified business, `false` - otherwise.'
      tags:
        - Lead-LeadCheck
  '/Wl/Lead/Lead.json':
    get:
      summary: 'Gets information necessary to display "Lead capture" widget. Returns the list of profile fields configured for the widget, skin styling data, captcha URL if required, and whether a free promotion can be applied when a new lead is created.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of business to which the new user must be captured.'
          schema:
            type: string
        - name: k_skin
          in: query
          description: 'The key of the widget skin. If left empty, then the default skin is used. This will be the Lead Capture widget skin with "Use this widget for the Add Lead form" selected. If your business doesn''t have a skin selected, this endpoint will default to the system-wide default, which may lack fields your business requires when adding a lead or a client.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'A list of profile fields in the business. Every element has the following keys: <dl> <dt>array[] `a_item`</dt> <dd> A list of possible options for an HTML select field. This value is only used if this field is an HTML select. Every element has the following keys: <dl> <dt>string `s_id`</dt> <dd>The option ID.</dd> <dt>string `text_title`</dt> <dd>The option title.</dd> </dl> </dd> <dt>int `id_field_general`</dt> <dd> The type of the general field. This is one of the {@link \RsFieldGeneralSid}. This value is only set if the field is one of the general fields. </dd> <dt>int `id_field_type`</dt> <dd>The field type. This is one of the {@link \RsFieldTypeSid} constants.</dd> <dt>bool `is_require`</dt> <dd>If `true`, then the field is mandatory. If `false`, then the field isn''t mandatory.</dd> <dt>string `k_field`</dt> <dd>The field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>string `text_field`</dt> <dd>The field title.</dd> </dl>'
                      a_skin:
                        type: array
                        description: 'The skin configuration: <dl> <dt>array[] `a_style`</dt> <dd> List of compiled CSS style blocks. Each element: <dl> <dt>string `s_style`</dt> <dd>Compiled CSS style block string.</dd> <dt>string `s_value_name`</dt> <dd>Name of the skin value this style block belongs to.</dd> </dl> </dd> <dt>array `background`</dt> <dd> Background styling for the widget container: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `field-font`</dt> <dd> Font styling for form fields: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `header-text`</dt> <dd> Header section settings: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> <dt>string `text`</dt> <dd>Header text content.</dd> </dl> </dd> <dt>array `info-show`</dt> <dd> Visibility settings for information elements: <dl> <dt>bool[] `a_require`</dt> <dd>Map of required element names. Keys are element names, values are `true`.</dd> <dt>bool[] `a_show`</dt> <dd>Map of visible element names. Keys are element names, values are `true`.</dd> <dt>bool `empty`</dt> <dd>`true` if no elements are visible.</dd> </dl> </dd> <dt>array `submit-background`</dt> <dd> Background styling for the submit button: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `submit-font`</dt> <dd> Font styling for the submit button: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> </dl> </dd> <dt>array `submit-text`</dt> <dd> Text settings for the submit button: <dl> <dt>string `class`</dt> <dd>CSS class names to apply to the element.</dd> <dt>string `css`</dt> <dd>Inline CSS style string to apply to the element.</dd> <dt>string `text`</dt> <dd>Submit button text content.</dd> </dl> </dd> </dl>'
                      can_use_free_purchase:
                        type: [boolean, "null"]
                        description: 'Whether it is possible to give free promotion when adding a user (only if free promotion is configured in the widget). `true` or `null` if it is possible, `false` if not. `null` used for backward compatibility.'
                      url_captcha:
                        type: string
                        description: 'The URL to load the image with a captcha test. This string is empty if it''s not necessary to pass a captcha test.'
      tags:
        - Lead-Lead
    post:
      summary: 'Saves new user via "Lead capture". Creates a new user account from the submitted lead capture form data, optionally signs in the new user, and associates the lead with a lead source. Returns an error code in `text_sign_in_error` if sign-in was requested but could not be completed.'
      parameters:
        - name: can_use_free_purchase
          in: query
          description: 'Whether it is possible to give free promotion when adding a user (only if free promotion is configured in the widget). `true` or `null` if it is possible, `false` if not. `null` used for backward compatibility.'
          schema:
            type: [boolean, "null"]
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of business to which the new user must be captured.'
          schema:
            type: string
        - name: k_skin
          in: query
          description: 'The key of the widget skin. If left empty, then the default skin is used. This will be the Lead Capture widget skin with "Use this widget for the Add Lead form" selected. If your business doesn''t have a skin selected, this endpoint will default to the system-wide default, which may lack fields your business requires when adding a lead or a client.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_data:
                  type: array
                  description: 'A list of fields containing the lead information. The keys are the field keys and values are field values. If field key is `Address` field key, value may be an array or string. Can be a string if only address field is used. Will be an array if city and postal code are used, with the following keys: <dl> <dt>string `text_address`</dt> <dd>Address.</dd> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in {@link \AGeoSql}.</i></dd> <dt>string `text_postal`</dt> <dd>Postal code.</dd> </dl>'
                is_sing_in:
                  type: boolean
                  description: '`true` if newly created lead should be automatically signed in, `false` otherwise. Lead will not be signed in if: - email is used already for another existing user; - different user is signed in already (can be changed with {@link \Wl\Lead\LeadApi::$is_sing_in_force}). If lead is not signed in, then {@link LeadApi::$text_sign_in_error} will contain an error message.'
                is_sing_in_force:
                  type: boolean
                  description: '`true` if newly created lead should be automatically signed in instead of the currently signed-in user, `false` if currently singed-in user should not be signed out. If lead is not signed in, then {@link LeadApi::$text_sign_in_error} will contain an error message.'
                k_lead_source:
                  type: [string, "null"]
                  description: 'Key of the lead source. Must be `null` if <var>text_lead_source</var> is set. If both parameters are empty, the {@link ModeSid::API} lead source will be used. {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                s_captcha:
                  type: string
                  description: 'The characters entered by the lead for the captcha test. This isn''t necessary if the GET method returned an empty {@link LeadApi::$url_captcha}. This field isn''t necessary if the GET method returned an empty {@link LeadApi::$url_captcha}.'
                text_lead_source:
                  type: [string, "null"]
                  description: 'Lead source title. A new lead source will be created if it does not exist. Must be `null` if <var>k_lead_source</var> is set. If both parameters are empty, the {@link ModeSid::API} lead source will be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_lead_source:
                        type: [string, "null"]
                        description: 'Key of the lead source. Must be `null` if <var>text_lead_source</var> is set. If both parameters are empty, the {@link ModeSid::API} lead source will be used. {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                      text_sign_in_error:
                        type: string
                        description: 'An error code if the lead is not signed in after creation. This field is filled in the POST method. Possible values: - `email-exists` - the email is already used by another lead; - `different-user` - another user is signed in already.'
                      uid:
                        type: string
                        description: 'The key of the new user. Typing is not added because the variable is an integer. Specifying typing may break third party integration.'
      tags:
        - Lead-Lead
  '/Wl/Lead/LeadStatus.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_lead_status:
                  type: string
                  description: 'Status of the lead record. One of {@link \RsLeadStatusSid} constants.'
                is_lead_create_nx:
                  type: boolean
                  description: 'Whether to create lead if it does not exist. If `true` and lead does not exist, create it, otherwise throw an error.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
                uid:
                  type: string
                  description: 'User key. Primary key in {@link \PassportLoginSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Lead-LeadStatus
  '/Wl/Lead/LeadPage.json':
    post:
      summary: 'Creates or edits a custom source lead.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_lead_field:
                  type: array
                  description: 'List of lead fields. <dl> <dt>int `i_sort`</dt> <dd>Field position on the lead page.</dd> <dt>bool `is_active`</dt> <dd>`true` if the field is active and should be displayed on the lead form, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>`true` if the field is required, `false` otherwise.</dd> <dt>string `k_field`</dt> <dd>Field key. Primary key in {@link \RsFieldSql} table.</dd> </dl>'
                a_lead_source:
                  type: array
                  description: 'List of Lead Sources. <dl> <dt>int `i_sort`</dt> <dd>Sorting order.</dd> <dt>int|null `id_lead_source`</dt> <dd> System-defined Lead Source ID. One of the {@link \Wl\Lead\Source\LeadSourceSql} constants. `null` if it is a custom Lead Source. </dd> <dt>string `k_lead_source`</dt> <dd>Key of the lead source. Primary key in {@link \Wl\Lead\Source\LeadSourceSql} table.</dd> <dt>string `k_skin`</dt> <dd> Key of the lead skin. Primary key in {@link \RsSkinSql} table. Will be set only in case if <var>id_lead_source</var> is {@link \Wl\Mode\ModeSid::WIDGET} and widget can''t be used for "Add Lead" form. `false` - otherwise. </dd> <dt>string `text_title`</dt> <dd>Name of the Lead Source.</dd> </dl>'
                k_business:
                  type: string
                  description: 'Business key.'
                o_lead_setting:
                  type: [object, array, "null"]
                  description: 'Settings for added leads.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Lead-LeadPage
  '/Wl/Session/Environment.json':
    get:
      summary: 'Returns current session environment.'
      parameters:
        - name: s_application_id
          in: query
          description: 'Application ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: [array, "null"]
                        description: 'List of businesses which are available in given application. Primary keys in {@link \RsBusinessSql} table. <tt>null</tt> for default application.'
                      a_business_franchise_all:
                        type: [array, "null"]
                        description: 'List of businesses keys, which are connected to franchise. Primary keys in {@link \RsBusinessSql} table. <tt>null</tt> if current business is not part of franchise.'
                      a_business_franchisee:
                        type: [array, "null"]
                        description: 'List of business franchisee keys. Primary keys in {@link \RsBusinessSql} table. Can be limited by levels to which a user can view/access in frontend. See {@link FranchiseSwitchSid}. <tt>null</tt> if current business is not part of franchise or if ''Allow clients to switch between franchise locations'' is OFF.'
                      a_payment_alert:
                        type: [array, "null"]
                        description: 'Payment alert data. `null` if user is not logged or none businesses are available for user, or if it''s not backend. Otherwise, contains data: <dl> <dt>int `i_duration`</dt> <dd> Time left in seconds. <var>null</var> if business account is not created or if service interruption dates aren''t specified. </dd> <dt>bool `is_alert`</dt> <dd>Determines whether to show an alert. Alert should be shown only once per session.</dd> <dt>bool `is_churn`</dt> <dd>Determines whether the business is churned.</dd> <dt>string `url_billing`</dt> <dd>Link to business billing page on the web.</dd> </dl>'
                      a_splash_screen:
                        type: [array, "null"]
                        description: 'Settings of splash screen customization for business {@link Wl_Session_EnvironmentModel.k_business}: <tt>null</tt> if splash screen in not customized for business. <dl> <dt>array `a_background`</dt> <dd>Background image.</dd> <dt>array `a_gradient`</dt> <dd>Background color.</dd> <dt>array `a_image`</dt> <dd>Logo image.</dd> </dl>'
                      dtl_now:
                        type: string
                        description: 'Local date with time now in current location {@link EnvironmentApi::$k_location}.'
                      has_form_quiz:
                        type: boolean
                        description: 'Whether exists form to confirm during registration process.'
                      has_merchant:
                        type: boolean
                        description: 'Whether the merchant is configured for the client''s home location or for business {@link EnvironmentApi::$k_business}.'
                      id_currency:
                        type: string
                        description: 'Currency ID. One of {@link \RsCurrencySid} constants.'
                      id_locale:
                        type: number
                        format: int32
                        description: 'Business locale. One of {@link LocaleSid} constants.'
                      id_place:
                        type: number
                        format: int32
                        description: 'Current place ID. One of {@link \RsPlaceSid} constants. For the Application is determined by the template. For the Web version of the site is determined by the current session.'
                      id_plan_achieve:
                        type: number
                        format: int32
                        description: 'Achieve subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Achieve\AchieveSubscriptionSid} constants.'
                      id_plan_base:
                        type: number
                        format: int32
                        description: 'Base subscription plan ID.'
                      id_plan_marketing:
                        type: number
                        format: int32
                        description: 'Marketing subscription plan ID.'
                      id_travel_mode:
                        type: [number, "null"]
                        description: 'Current travel mode ID in business. `null` if travel mode is disabled.'
                      is_aiagent_chat:
                        type: boolean
                        description: 'Whether AI agent chat (CAASI) is available for current business. * `true` if chat is available * `false` if chat is not available'
                      is_application_custom:
                        type: boolean
                        description: 'Determines current business has custom application. If <tt>true</tt> application has custom application, <tt>false</tt> otherwise.'
                      is_billing_required:
                        type: boolean
                        description: '<tt>true</tt> if user must be redirected to billing page to enter credit card; <tt>false</tt> otherwise.'
                      is_booking_appointment_authorize:
                        type: boolean
                        description: '<tt>true</tt> if clients must sign in to book an appointment; <tt>false</tt> otherwise.'
                      is_booking_required_billing:
                        type: boolean
                        description: '<tt>true</tt> if user must be redirected from booking page to billing page to enter credit card; <tt>false</tt> otherwise.'
                      is_business_location_multiple:
                        type: boolean
                        description: 'Determines if a business has multiple locations. See {@link \rs_business_location_multiple()}.'
                      is_client_profile_share:
                        type: [boolean, "null"]
                        description: '`true` id profile details can be shared with the new location while adding a client that already has an existing profile in franchise, `false` - otherwise. * `null` if not franchise.'
                      is_client_select_date_asset:
                        type: boolean
                        description: 'If clients are allowed to select a date and time, then the available asset while asset booking.'
                      is_contract:
                        type: boolean
                        description: '<tt>true</tt> - user has unassigned contract(s); <tt>false</tt> - otherwise.'
                      is_family_relation_display_client:
                        type: boolean
                        description: '`true` is allowed relationship to display, `false` - otherwise.'
                      is_franchisor:
                        type: boolean
                        description: '<tt>true</tt> - if business is franchisor; <tt>false</tt> - otherwise.'
                      is_location_geo:
                        type: boolean
                        description: '<tt>true</tt> to use page of geographical search of locations (possible for franchise case only); <tt>false</tt> to use simple select of locations from list.'
                      is_location_redirect:
                        type: boolean
                        description: '<tt>true</tt> - in backend application user must be redirected to location selection; <tt>false</tt> - otherwise.'
                      is_profile_required:
                        type: boolean
                        description: '<tt>true</tt> if user must be redirected to profile page to enter mandatory fields; <tt>false</tt> otherwise.'
                      is_request_required_information:
                        type: boolean
                        description: 'Indicates whether to obtain customer required information such as required profile data and waiver. Mostly used for selenium tests running purposes to avoid creating profile filled with required information. If <tt>true</tt> all required information must be obtained before client will be allowed to use application, <tt>false</tt> otherwise.'
                      is_traveller:
                        type: boolean
                        description: '<tt>true</tt> if user is traveler; <tt>false</tt> otherwise.'
                      is_waiver_required:
                        type: boolean
                        description: '<tt>true</tt> if it is required to sign online waiver, <tt>false</tt> otherwise.'
                      k_business:
                        type: string
                        description: 'Key of business to be set as current. Primary key in table {@link \RsBusinessSql}.'
                      k_business_franchisor:
                        type: [string, "null"]
                        description: 'Not <tt>null</tt> for franchise application only.'
                      k_currency:
                        type: string
                        description: 'Currency key. Primary key in the {@link \RsCurrencySql} table.'
                      k_location:
                        type: string
                        description: 'Key of location to be set as current. Primary key in table {@link \RsLocationSql}.'
                      text_country_code:
                        type: string
                        description: 'Country code for business locale {@link LocaleSid} constants.'
                      uid:
                        type: [string, "null"]
                        description: 'Current user key. <tt>null</tt> if user is not signed in.'
                      url_login:
                        type: [string, "null"]
                        description: 'Business login URL. `null` if user is not logged in or none businesses are available for user.'
      tags:
        - Session-Environment
    put:
      summary: 'Saves current location of business for current user.'
      parameters:
        - name: s_application_id
          in: query
          description: 'Application ID.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_staff:
                  type: boolean
                  description: '`true` if you want the API to switch to staff back office; `false` otherwise.'
                k_business:
                  type: string
                  description: 'Key of business to be set as current. Primary key in table {@link \RsBusinessSql}.'
                k_location:
                  type: string
                  description: 'Key of location to be set as current. Primary key in table {@link \RsLocationSql}.'
                s_business_change:
                  type: string
                  description: 'Key to check session in a case of business change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Session-Environment
  '/Wl/Session/RedirectUrl.json':
    get:
      summary: 'Encodes URL such that it can be used for redirect.'
      parameters:
        - name: ip
          in: query
          description: 'IP address of remote user.'
          schema:
            type: string
        - name: url
          in: query
          description: 'URL to encode.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url:
                        type: string
                        description: 'URL to encode.'
      tags:
        - Session-RedirectUrl
  '/Wl/Session/Location.json':
    delete:
      summary: 'Deletes selected by guest location from session.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Session-Location
    post:
      summary: 'Saves into session location which was selected by guest when application was started.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_location:
                  type: string
                  description: 'Location selected when application was started. Primary key in {@link \RsLocationSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Session-Location
  '/Wl/Tag/TagList.json':
    get:
      summary: 'Returns tags of the specified business. Returns all client tags configured for the business in display order, along with flags indicating whether the business has configured a penalty fee for failed automatic payments and whether surcharges are enabled. Used to populate tag pickers and client profile forms.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'The tag list. Each element has the next structure: <dl> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `k_tag`</dt> <dd>The tag key. <i>Primary key in the {@link \RsTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
                      has_fee:
                        type: boolean
                        description: 'Whether a business did set up a penalty fee for failed automatic payments.'
                      has_surcharge:
                        type: boolean
                        description: 'Whether a business did set up surcharges.'
      tags:
        - Tag-TagList
    post:
      summary: 'Saves the list of tags. Can be used to create new tags or update existing ones. Persists the given set of client tags for the business. Tags without a key are created; tags with an existing key are updated with the new title. Requires backend access.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_list:
                  type: array
                  description: 'The tag list. Each element has the next structure: <dl> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `k_tag`</dt> <dd>The tag key. <i>Primary key in the {@link \RsTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
                json_list:
                  type: [string, "null"]
                  description: 'List of tags in json format. See {@link TagListApi::$a_list} for the structure of each tag. `null` to use {@link TagListApi::$a_list} for getting the tag list.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'The tag list. Each element has the next structure: <dl> <dt>int `i_sort`</dt> <dd>The sort order of the tag.</dd> <dt>string `k_tag`</dt> <dd>The tag key. <i>Primary key in the {@link \RsTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The tag title.</dd> </dl>'
      tags:
        - Tag-TagList
  '/Wl/Tag/Tag.json':
    delete:
      summary: 'Removes the tag.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key of the tags.'
          schema:
            type: string
        - name: k_tag
          in: query
          description: 'The tag key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tag-Tag
  '/Wl/Alert/AlertList.json':
    get:
      summary: 'Returns all alerts for the user.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_alert_list:
                        type: array
                        description: 'List of alerts. For details see {@link \Wl\Alert\Alert::getList()}.'
      tags:
        - Alert-AlertList
    post:
      summary: 'Sets alerts as already read.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_alert_read:
                  type: [array, "null"]
                  description: 'List of alert keys to mark as read. `null` to set all alerts as read.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Alert-AlertList
  '/Wl/Alert/AlertSetting.json':
    post:
      summary: 'Save alert settings for the user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_alert:
                  type: number
                  format: int32
                  description: 'CID of alert. One of {@link AlertAbstract} subclasses.'
                id_receive:
                  type: number
                  format: int32
                  description: 'Determines whether the user wish to see all alerts, only their alerts, or none. One of {@link AlertReceiveSid} constants.'
                is_sound:
                  type: boolean
                  description: 'Determines whether the user wish to hear a sound when a new alert is added.'
                k_business:
                  type: string
                  description: 'Key of the business to save alert settings in.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Alert-AlertSetting
  '/Wl/Virtual/VirtualSchedule.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dtu_class_period:
                  type: string
                  description: 'Class schedule session date.'
                k_appointment:
                  type: string
                  description: 'Appointment key.'
                k_class_period:
                  type: string
                  description: 'Class schedule key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_delay:
                        type: [number, "null"]
                        description: 'Number of seconds after which need to repeat request. <tt>null</tt> if request should not be repeated.'
                      k_business:
                        type: string
                        description: 'Business key.'
                      url_redirect:
                        type: string
                        description: 'Link to created meeting.'
      tags:
        - Virtual-VirtualSchedule
  '/Wl/Virtual/MeetingList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_meeting_list:
                        type: array
                        description: 'Active meeting list. Each element has next structure: <dl> <dt>array[] `a_staff`</dt> <dd> Information about staff. Each element has next structure: <dl> <dt>string `text_staff`</dt> <dd>Full name of the staff.</dd> <dt>string `url_photo`</dt> <dd>Url photo of the staff.</dd> <dt>string `url_staff`</dt> <dd>Url for view information about the staff.</dd> </dl> </dd> <dt>string `text_service`</dt> <dd>Name of the class or service.</dd> <dt>string `text_date`</dt> <dd>Date of the session.</dd> <dt>string `text_time`</dt> <dd>Time of the session.</dd> <dt>string `s_meeting_id`</dt> <dd>Meeting id.</dd> </dl>'
                      can_add_license:
                        type: boolean
                        description: '<tt>true</tt> if can add FitLIVE licenses, <tt>false</tt> otherwise.'
                      i_license:
                        type: number
                        format: int32
                        description: 'Number of paid FitLIVE licenses.'
                      is_fitlive:
                        type: boolean
                        description: '<tt>true</tt> if business use FitLIVE, <tt>false</tt> otherwise.'
      tags:
        - Virtual-MeetingList
  '/Wl/Tax/TaxList.json':
    get:
      summary: 'Returns taxes of the specified business. Returns all active taxes configured for the business, including each tax''s title, rate, and type. Used to populate tax pickers when creating products, promotions, or invoices.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business for which to get a list of taxes.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of taxes. Each element has the following structure: <dl> <dt>string[] `a_location`</dt> <dd>The locations where the tax is applicable.</dd> <dt>float `f_value`</dt> <dd>The amount of the tax.</dd> <dt>string `k_tax`</dt> <dd>The type of the tax, one of the {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl>'
      tags:
        - Tax-TaxList
  '/Wl/Tax/Tax.json':
    get:
      summary: 'Returns the title for the specified tax. Returns the human-readable display name for the given tax record. Throws if the tax key is invalid or if the tax has been marked as removed.'
      parameters:
        - name: k_tax
          in: query
          description: 'The tax key to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_tax:
                        type: string
                        description: 'The tax title.'
      tags:
        - Tax-Tax
  '/Wl/Drive/ProductImageUpload.json':
    get:
      summary: 'Returns information about the image. Used to initialize an image upload widget for an existing entity. Returns thumbnail and full-size URLs, upload endpoint, dimension constraints, and whether deletion is allowed, so the frontend can render the current image and offer upload or delete actions.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - Drive-ProductImageUpload
    post:
      summary: 'Loads image information for a list of IDs. The POST method is used instead of the GET method because the maximum permitted URI length is restricted. Bulk variant of the GET method for pages that must display many images at once. Accepts a JSON-encoded list of entity IDs and returns the same metadata as the single-item GET, loading all images in one round-trip to avoid N+1 requests.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_id:
                  type: [string, "null"]
                  description: 'A list of image IDs set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}, serialized using JSON. This will be `null` if there''s no data being sent by the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image information for every ID. <i>Every element contains values such as the {@link ImageUploadApi::_get()} result.</i> <dl> <dt>array `a_text_empty`</dt> <dd> Information about the text on the empty upload image. <i>See: {@link \AImageUpload::textEmpty()}.</i> <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl> </dd> <dt>int `i_height_max`</dt> <dd>The maximum height of the image.</dd> <dt>int `i_height_min`</dt> <dd>The minimum height of the image.</dd> <dt>int `i_thumbnail_height`</dt> <dd>The height of the thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_HEIGHT}.</i></dd> <dt>int `i_thumbnail_width`</dt> <dd>The width of thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_WIDTH}.</i></dd> <dt>int `i_width_max`</dt> <dd>The maximum width of image.</dd> <dt>int `i_width_min`</dt> <dd>The minimum width of image.</dd> <dt>bool `is_circular`</dt> <dd><tt>true</tt> if image is treated as circular; <tt>false</tt> otherwise.</dd> <dt>bool `is_delete_allow`</dt> <dd><tt>true</tt> if image deleting is allowed; <tt>false</tt> otherwise.</dd> <dt>string `s_link`</dt> <dd>The image link.</dd> <dt>string `url_empty`</dt> <dd>The URL of the image that should be shown in a case image is not uploaded.</dd> <dt>string|null `url_thumbnail`</dt> <dd>The URL of thumbnail of the image. <tt>null</tt> if image is not uploaded.</dd> <dt>string `url_upload`</dt> <dd>The URL the script where new image should be uploaded.</dd> <dt>string|null `url_view`</dt> <dd>The URL of the full image. <tt>null</tt> if image is not uploaded.</dd> </dl>'
      tags:
        - Drive-ProductImageUpload
    put:
      summary: 'Updates the image. Applies a command (such as delete or replace) to a previously uploaded image, then returns the refreshed image metadata so the frontend can update the widget state without a separate GET call.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_command:
                  type: string
                  description: 'The action that must be performed to the image.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - Drive-ProductImageUpload
  '/Wl/Drive/ProductImageCopyTemporary.json':
    post:
      summary: 'Makes a temporary copy of the specified image.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: s_link
          in: query
          description: 'Image link.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_crop:
                        type: boolean
                        description: '<tt>true</tt> - crop is used. <tt>false</tt> - crop is not used (the image is saved in the original size).'
                      i_height:
                        type: number
                        format: int32
                        description: 'Actual height of thumbnail image.'
                      i_height_src:
                        type: number
                        format: int32
                        description: 'Height of original image.'
                      i_rotate:
                        type: number
                        format: int32
                        description: 'Angle on which image was rotated compared to the original.'
                      i_width:
                        type: number
                        format: int32
                        description: 'Actual width of thumbnail image.'
                      i_width_src:
                        type: number
                        format: int32
                        description: 'Width of original image.'
                      id_type_src:
                        type: number
                        format: int32
                        description: 'Image type ID. One of {@link \Core\Drive\DriveTypeSid} constants.'
                      is_resize:
                        type: boolean
                        description: '<tt>true</tt> if thumbnail is a resized variant of original image.'
                      s_link:
                        type: string
                        description: 'Image link.'
                      url_thumbnail:
                        type: string
                        description: 'URL to resized and rotated image in file storage.'
                      url_view:
                        type: string
                        description: 'URL to original image in file storage.'
      tags:
        - Drive-ProductImageCopyTemporary
  '/Wl/Eml/EmlEdit.json':
    put:
      summary: ''
      parameters:
        - name: id_eml
          in: query
          description: 'Eml system ID. One of the {@link \RsEmlSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key for which eml settings are edited. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_list_id:
                  type: string
                  description: 'Eml list ID to be set as active.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Eml-EmlEdit
  '/Wl/Announcement/AnnouncementList.json':
    get:
      summary: 'Returns a list of announcements for the given business and location. Retrieves published announcements visible to the current user, optionally filtered to a single location. In backend mode the caller can also sort the results by column and direction, and the chosen sort preference is persisted per user so the backend grid remembers it across requests. Access is validated against announcement editor permissions for the business.'
      parameters:
        - name: id_order
          in: query
          description: 'Order ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default order.'
          schema:
            type: [number, "null"]
        - name: id_sort_field
          in: query
          description: 'Sort field ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default field.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key for which need show announcement. `null` in case when need show location for all locations in business.'
          schema:
            type: [string, "null"]
        - name: text_search
          in: query
          description: 'The filter phrase to filter announcements by name.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of announcements. Each element has the following keys: <dl> <dt>string[] `a_location`</dt> <dd>Location keys where the announcement is published. <i>Primary keys in the {@link \RsLocationSql} table.</i></dd> <dt>string `dtl_create`</dt> <dd>Creation date in local MySQL format.</dd> <dt>string|null `dtl_publish`</dt> <dd>Local date and time the announcement is published in MySQL format. `null` if not set.</dd> <dt>string|null `dtl_unpublish`</dt> <dd>Local date and time the announcement is unpublished in MySQL format. `null` if not set.</dd> <dt>string `html_description`</dt> <dd>Announcement description as HTML.</dd> <dt>string `html_description_clear`</dt> <dd>Announcement description text stripped of HTML tags.</dd> <dt>int|null `i_order`</dt> <dd>Sort order. `null` if the announcement is not pinned.</dd> <dt>int `id_status`</dt> <dd>Announcement status ID. One of {@link \Wl\Announcement\AnnouncementStatusEnum} constants.</dd> <dt>bool `is_pin`</dt> <dd>`true` if the announcement is pinned, `false` otherwise.</dd> <dt>string `k_announcement`</dt> <dd>Announcement key. <i>Primary key in the {@link \Wl\Announcement\AnnouncementSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string `text_create_date`</dt> <dd>Formatted creation date.</dd> <dt>string `text_publish_date`</dt> <dd>Formatted publish date.</dd> <dt>string `text_publish_date_short`</dt> <dd>Formatted publish date with month name but without day of week.</dd> <dt>string `text_title`</dt> <dd>Announcement title.</dd> <dt>string `text_unpublish_date`</dt> <dd>Formatted unpublish date.</dd> <dt>string `uid_create`</dt> <dd>Key of the user who created the announcement. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_detail`</dt> <dd>URL of the announcement detail page.</dd> <dt>string `url_image`</dt> <dd>URL of the announcement image.</dd> <dt>string `xml_description`</dt> <dd>Announcement description as XML.</dd> </dl>'
                      id_order:
                        type: [number, "null"]
                        description: 'Order ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default order.'
                      id_sort_field:
                        type: [number, "null"]
                        description: 'Sort field ID for list of announcements. Used only when {@link AnnouncementListApi::$is_backend} is `true`. `null` in case when use default field.'
      tags:
        - Announcement-AnnouncementList
  '/Wl/Announcement/AnnouncementElement.json':
    delete:
      summary: 'Deletes announcement. {@inheritdoc}'
      parameters:
        - name: k_announcement
          in: query
          description: 'Announcement key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Announcement-AnnouncementElement
    get:
      summary: 'Returns information about the announcement. {@inheritdoc}'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_announcement
          in: query
          description: 'Announcement key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'List of locations keys.'
                      dtl_publish:
                        type: [string, "null"]
                        description: 'The date and time when the announcement was published. This will be `null` if the published date is not set yet.'
                      dtl_unpublish:
                        type: [string, "null"]
                        description: 'The date and time when the announcement was unpublished. This will be `null` if the unpublished date is not set yet.'
                      html_description:
                        type: string
                        description: 'Announcement description.'
                      i_order:
                        type: [number, "null"]
                        description: 'Order to sort announcements. This will be `null` if sort order is not set yet (announcement is not pinned).'
                      id_status:
                        type: number
                        format: int32
                        description: 'Announcement status ID.'
                      k_announcement:
                        type: string
                        description: 'Announcement key.'
                      k_announcement_image:
                        type: [string, "null"]
                        description: 'The string representation of the announcement''s image key. `null` if the announcement doesn''t have an image.'
                      text_publish_date:
                        type: string
                        description: 'The date and time when the announcement was published in human format.'
                      text_publish_date_short:
                        type: string
                        description: 'The date when the announcement was published in human format.'
                      text_title:
                        type: string
                        description: 'Title of announcement.'
                      url_image:
                        type: string
                        description: 'Announcement image url.'
                      xml_description:
                        type: string
                        description: 'Description of announcement.'
      tags:
        - Announcement-AnnouncementElement
    post:
      summary: 'Creates/updates announcement. {@inheritdoc}'
      parameters:
        - name: k_announcement
          in: query
          description: 'Announcement key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: array
                  description: 'List of locations keys.'
                dtl_publish:
                  type: [string, "null"]
                  description: 'The date and time when the announcement was published. This will be `null` if the published date is not set yet.'
                dtl_unpublish:
                  type: [string, "null"]
                  description: 'The date and time when the announcement was unpublished. This will be `null` if the unpublished date is not set yet.'
                s_command:
                  type: string
                  description: 'The action that must be performed to the announcement image.'
                text_title:
                  type: string
                  description: 'Title of announcement.'
                xml_description:
                  type: string
                  description: 'Description of announcement.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_announcement:
                        type: string
                        description: 'Announcement key.'
      tags:
        - Announcement-AnnouncementElement
    put:
      summary: 'Sets announcement ''pinned'' status. Only one announcement can be pinned for business. {@inheritdoc}'
      parameters:
        - name: k_announcement
          in: query
          description: 'Announcement key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_pin:
                  type: boolean
                  description: '`true` to pin the announcement, `false` to unpin.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Announcement-AnnouncementElement
  '/Wl/Unlayer/AdvancedEditor.json':
    get:
      summary: 'Gets email variables for merge-tags feature of Unlayer editor. {@link https://docs.unlayer.com/docs/merge-tags}'
      parameters:
        - name: id_mail
          in: query
          description: 'Mail identifier, one of {@link \RsMailSid}.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern
          in: query
          description: 'Key of mail pattern. Primary key in {@link \RsMailPatternSql} table.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_mail_variables:
                        type: array
                        description: 'Array with all mail variables. An array with help data for global and template-specific variables: <dl> <dt>array[] `a_help_global`</dt> <dd> An array with help data for global variables {@link \Wl\Mail\Pattern\MailPattern::variablesHelpData()}: <dl> <dt>string `text_description`</dt> <dd>A variable description.</dd> <dt>string `text_parameter`</dt> <dd>A variable name.</dd> </dl> </dd> <dt>array[] `a_help`</dt> <dd> An array with help data for template-specific variables {@link \Wl\Mail\Pattern\MailPattern::variablesHelpData()}: <dl> <dt>string `text_description`</dt> <dd>A variable description.</dd> <dt>string `text_parameter`</dt> <dd>A variable name.</dd> </dl> </dd> </dl>'
      tags:
        - Unlayer-AdvancedEditor
  '/Wl/Hint/Hint.json':
    put:
      summary: 'Stores that user has seen the hint and returns a flag allowing to show a hint if it has not yet been shown earlier.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_template:
                  type: array
                  description: 'A list of hints that should be displayed sequentially. Template should be placed in <tt>Wl\Hint\Xml</tt> as a single xml file.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business to show the hint in. `null` means to show hint in the system business.'
                uid:
                  type: string
                  description: 'The user who will be shown a hint.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_template_show:
                        type: boolean
                        description: 'Name of the hint`s xml to be shown. An empty string if none of the hints should be shown.'
      tags:
        - Hint-Hint
  '/Wl/Currency/Currency.json':
    get:
      summary: 'Retrieves information about currencies.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_currency:
                        type: array
                        description: 'Keys - currency keys. Values - information about currency: <dl> <dt>bool `is_before`</dt> <dd><tt>true</tt> - locate sign before amount; <tt>false</tt> - locate sign after amount.</dd> <dt>string `s_sign`</dt> <dd>Sign of currency.</dd> <dt>string `sid_currency`</dt> <dd>International code of the currency.</dd> </dl>'
      tags:
        - Currency-Currency
  '/Wl/Currency/BusinessCurrency.json':
    get:
      summary: 'Retrieves information about currencies.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of franchisees'' currencies.'
      tags:
        - Currency-BusinessCurrency
  '/Wl/Presence/PresenceActivate.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where client want to activate website.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_website:
                  type: number
                  format: int32
                  description: 'ID of the plan to be activated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_prospect:
                        type: boolean
                        description: 'Whether business does not have a subscription.'
                      is_sent:
                        type: boolean
                        description: 'Whether email has been sent in 24 hours.'
      tags:
        - Presence-PresenceActivate
  '/Wl/Oauth/OauthMe.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_mail:
                        type: string
                        description: 'Email of the current user.'
                      text_name:
                        type: string
                        description: 'Name of the current user.'
      tags:
        - Oauth-OauthMe
  '/Wl/Oauth/OauthToken.json':
    get:
      summary: 'Generated new token base on refresh token.'
      parameters:
        - name: s_token_refresh
          in: query
          description: 'Generated refresh token.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_access:
                        type: string
                        description: 'Generated live token.'
                      s_token_refresh:
                        type: string
                        description: 'Generated refresh token.'
      tags:
        - Oauth-OauthToken
    post:
      summary: 'Generates new token or returns already generated and not expired token.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_client_id:
                  type: string
                  description: 'Application client id.'
                s_client_secret:
                  type: string
                  description: 'Application client secret.'
                s_code:
                  type: string
                  description: 'Randomly generated code required for query verification.'
                url_redirect:
                  type: string
                  description: 'Url link to redirect user to after token successfully generated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_access:
                        type: string
                        description: 'Generated live token.'
                      s_token_refresh:
                        type: string
                        description: 'Generated refresh token.'
      tags:
        - Oauth-OauthToken
  '/Wl/Timezone/TimezoneCurrentTime.json':
    get:
      summary: 'Gets current date time in the timezone.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_current:
                        type: string
                        description: 'Current date time in the timezone.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
                      text_timezone_name:
                        type: string
                        description: 'Timezone title.'
      tags:
        - Timezone-TimezoneCurrentTime
  '/Wl/Toast/Toast.json':
    get:
      summary: 'Get all toasts by uid of current user.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Primary key of registered user in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_toast:
                        type: array
                        description: 'Array of result toasts.'
      tags:
        - Toast-Toast
    put:
      summary: 'Update one or many toast.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Primary key of registered user in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_toast_list:
                  type: array
                  description: 'Array of primary keys of toast messages in {@link ToastSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Toast-Toast
  '/Wl/Captcha/BusinessCaptcha.json':
    put:
      summary: '{@inheritdoc} Saves the business CAPTCHA enabled setting.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_captcha_enable:
                  type: boolean
                  description: '`true` if captcha is enabled for the business, `false` otherwise.'
                k_business:
                  type: string
                  description: 'Business key. <i>Primary key from {@link \RsBusinessSql}.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Captcha-BusinessCaptcha
  '/Wl/Spivi/SpiviConnect.json':
    delete:
      summary: 'Disconnecting with Spivi, remove a token.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Spivi-SpiviConnect
    get:
      summary: 'Retrieves Spivi account information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_facility_id:
                        type: string
                        description: 'Facility ID.'
                      s_username:
                        type: string
                        description: 'Username.'
      tags:
        - Spivi-SpiviConnect
    post:
      summary: 'Connecting with Spivi, getting a token.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_facility_id:
                  type: string
                  description: 'Facility ID.'
                s_password:
                  type: string
                  description: 'Password.'
                s_username:
                  type: string
                  description: 'Username.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Spivi-SpiviConnect
  '/Wl/Spivi/SpiviClientMetric.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_widget:
                        type: string
                        description: 'URL to the Spivi widget for the client.'
      tags:
        - Spivi-SpiviClientMetric
  '/Wl/Widget/WidgetReact.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                url_frontend:
                  type: [string, "null"]
                  description: 'New url for the widget frontend. Empty string for reset to default.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_frontend:
                        type: [string, "null"]
                        description: 'New url for the widget frontend. Empty string for reset to default.'
      tags:
        - Widget-WidgetReact
  '/Wl/Feedback/Feedback.json':
    post:
      summary: 'Sends a feedback email to the support team.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_screen_height:
                  type: number
                  format: int32
                  description: 'Screen height in pixels.'
                i_screen_width:
                  type: number
                  format: int32
                  description: 'Screen width in pixels.'
                is_mail_location:
                  type: boolean
                  description: '`true` to send feedback email to location email; `false` to send the email to support team.'
                is_user_initialized:
                  type: boolean
                  description: '`true` of a user initiated the feedback; `false` otherwise.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in {@link \RsBusinessSql}.'
                k_location:
                  type: string
                  description: 'Location key. Primary key in {@link \RsBusinessSql}.'
                text_app_id:
                  type: string
                  description: 'Application ID.'
                text_app_page:
                  type: string
                  description: 'App page where feedback was submitted.'
                text_device_model:
                  type: string
                  description: 'Device model.'
                text_device_os:
                  type: string
                  description: 'Device operating system.'
                text_feedback:
                  type: string
                  description: 'Feedback text.'
                text_prompt_shown:
                  type: string
                  description: 'The text that shows up when the user submits feedback.'
                text_version:
                  type: string
                  description: 'Application version.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Feedback-Feedback
  '/Wl/Feedback/FeedbackLog.json':
    post:
      summary: 'Logs feedback from the mobile application.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_screen_height:
                  type: number
                  format: int32
                  description: 'Screen height in pixels.'
                i_screen_width:
                  type: number
                  format: int32
                  description: 'Screen width in pixels.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The source of the action. A constant of {@link ModeSid}. Is optional. The default value is the current mode.'
                is_satisfied:
                  type: boolean
                  description: '`true` of a user is satisfied by application; `false` otherwise.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in {@link \RsBusinessSql}.'
                k_location:
                  type: string
                  description: 'Location key. Primary key in {@link \RsBusinessSql}.'
                text_device_model:
                  type: string
                  description: 'Device model.'
                text_device_os:
                  type: string
                  description: 'Device operating system.'
                text_prompt_shown:
                  type: string
                  description: 'The text that shows up when the user submits feedback.'
                text_version:
                  type: string
                  description: 'Application version.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Feedback-FeedbackLog
  '/Wl/BalanceSummary/BalanceSummary.json':
    get:
      summary: ''
      parameters:
        - name: dl_end
          in: query
          description: 'End date exclusively to retrieve account balance values. `null` if no need to get opening balance.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'The key of the business. `null` if not set.'
          schema:
            type: ["null", string]
        - name: uid
          in: query
          description: 'Key of the user. `null` if not set.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of account custom. Each element has the following structure: <dl> <dt>string `k_pay_method`</dt> <dd>The pay method key.</dd> <dt>string `text_title`</dt> <dd>The name of the account method.</dd> <dt>string `m_opening_balance`</dt> <dd>Opening account balance. Before the start of the report filter period.</dd> <dt>string `f_total`</dt> <dd>Total account balance.</dd> </dl>'
      tags:
        - BalanceSummary-BalanceSummary
  '/Wl/QuickStart/QuickStartConfig.json':
    get:
      summary: 'Gets stored quick start configuration.'
      parameters:
        - name: cid
          in: query
          description: 'Quick Start class CID.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to bind configuration to a specific user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_config:
                        type: [array, "null"]
                        description: 'Last configuration for the quick start state. `null` if not initialized yet. `array` for compatibility with JavaScript. Has the following structure: <dl> <dt>bool `is_expand`</dt> <dd>Whether the Quick Start is expanded.</dd> <dt>bool `is_hide`</dt> <dd>Whether the Quick Start is hidden.</dd> <dt>array `a_category_collapsed`</dt> <dd> Array of booleans with category numbers as keys and values indicating whether the category is collapsed. </dd> </dl>'
      tags:
        - QuickStart-QuickStartConfig
    post:
      summary: 'Updates stored quick start configuration.'
      parameters:
        - name: cid
          in: query
          description: 'Quick Start class CID.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to bind configuration to a specific user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_config:
                  type: [array, "null"]
                  description: 'Last configuration for the quick start state. `null` if not initialized yet. `array` for compatibility with JavaScript. Has the following structure: <dl> <dt>bool `is_expand`</dt> <dd>Whether the Quick Start is expanded.</dd> <dt>bool `is_hide`</dt> <dd>Whether the Quick Start is hidden.</dd> <dt>array `a_category_collapsed`</dt> <dd> Array of booleans with category numbers as keys and values indicating whether the category is collapsed. </dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - QuickStart-QuickStartConfig
  '/Wl/Tuition/TuitionListByEvent.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_event
          in: query
          description: 'Key of the event to get list of tuition for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tuition_list:
                        type: array
                        description: 'List of tuition. <dl> <dt>string `k_tuition`</dt> <dd>Key of the tuition.</dd> <dt>string `text_title`</dt> <dd>Title of the tuition.</dd> </dl>'
      tags:
        - Tuition-TuitionListByEvent
  '/Thoth/ExplorerSearch/ClassSession/ClassSessionSearch.json':
    get:
      summary: 'Gets list of class sessions based on search criteria. Returns sessions matching the given geographic area, date range, and optional filters for business, location, staff, experience type, home tour type, price, and location rating. Only sessions within their current booking window are included in the result.'
      parameters:
        - name: a_business
          in: query
          description: 'List of business keys to search by. Empty array to not filter by business keys.'
          schema:
            type: array
        - name: a_experience_type
          in: query
          description: 'List of experience types to search by. Each value is one of {@link ClassSessionExperienceTypeEnum} case values. Empty array to not filter by experience type.'
          schema:
            type: array
        - name: a_home_tour
          in: query
          description: 'List of home tour activity types to search by. Each value is from {@link \RsHomeTourSid}. Empty array to not filter by home tour type.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'List of location keys to search by. Empty array to not filter by location keys.'
          schema:
            type: array
        - name: a_location_rating
          in: query
          description: 'List of location ratings to search by. Values are integers from 1 to 5, or `null`/`0` for unrated locations. Empty array to not filter by location rating.'
          schema:
            type: [array, object]
        - name: a_staff_uid
          in: query
          description: 'List of staff user keys to search by. Each value is a user key (uid) of a staff member. Empty array to not filter by staff.'
          schema:
            type: array
        - name: dl_begin
          in: query
          description: 'Start date of the session search date range in MySQL format (`Y-m-d`). Required. `null` if not set by request.'
          schema:
            type: [string, "null"]
        - name: dl_end
          in: query
          description: 'End date of the session search date range in MySQL format (`Y-m-d`). Required. `null` if not set by request.'
          schema:
            type: [string, "null"]
        - name: f_latitude
          in: query
          description: 'Latitude coordinate for search. Required. `null` if not set by request.'
          schema:
            type: [number, "null"]
        - name: f_longitude
          in: query
          description: 'Longitude coordinate for search. Required. `null` if not set by request.'
          schema:
            type: [number, "null"]
        - name: f_radius
          in: query
          description: 'Search radius in kilometers. Required. `null` if not set by request.'
          schema:
            type: [number, "null"]
        - name: m_price_max
          in: query
          description: 'Maximum price to search by (inclusive). Decimal string in dollars (e.g. `"100.00"`). `null` to not limit by maximum price.'
          schema:
            type: [string, "null"]
        - name: m_price_min
          in: query
          description: 'Minimum price to search by (inclusive). Decimal string in dollars (e.g. `"0.00"`). `null` to not limit by minimum price.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_session:
                        type: array
                        description: 'List of found class sessions. Each item is an associative array with the following structure: <dl> <dt>string|null `dtu_book_begin`</dt> <dd>Date and time when booking for this session starts in UTC. `null` if there is no "too early" limitation.</dd> <dt>string|null `dtu_book_end`</dt> <dd>Date and time when booking for this session ends in UTC. `null` if there is no "too late" limitation.</dd> <dt>string `dtu_session`</dt> <dd>Date and time of the session in UTC.</dd> <dt>string `k_class`</dt> <dd>Class key. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `k_class_period`</dt> <dd>Class period key. <i>Primary key in {@link \RsClassPeriodSql}.</i></dd> <dt>string `k_class_period_session`</dt> <dd>Class session key. <i>Primary key in {@link \Thoth\ExplorerSearch\ClassSession\ClassPeriodSessionSql}.</i></dd> </dl>'
      tags:
        - ClassSession-ClassSessionSearch
  '/Thoth/LayoutBe/Login/LoginLogo.json':
    get:
      summary: ''
      parameters:
        - name: i_height
          in: query
          description: 'Logo height.'
          schema:
            type: number
            format: int32
        - name: i_width
          in: query
          description: 'Logo width.'
          schema:
            type: number
            format: int32
        - name: not_staff
          in: query
          description: 'Whether staff photo should be excluded. `true` - staff photo should be excluded, `false` - otherwise.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_height:
                        type: number
                        format: int32
                        description: 'Logo height.'
                      i_width:
                        type: number
                        format: int32
                        description: 'Logo width.'
                      url_logo:
                        type: string
                        description: 'Logo URL.'
      tags:
        - Login-LoginLogo
  '/Thoth/PayHardware/Nuvei/NuveiHardwareElement.json':
    delete:
      summary: 'Removes terminal.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Nuvei-NuveiHardwareElement
    get:
      summary: 'Fetch terminal information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_model:
                        type: number
                        format: int32
                        description: 'Hardware model ID.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Status ID.'
                      ip_terminal:
                        type: ["null", string]
                        description: 'IP address of terminal. Maybe an empty string as it is not set for all terminals.'
                      k_location:
                        type: string
                        description: 'Location key.'
                      text_label:
                        type: string
                        description: 'Label of the terminal.'
                      text_location:
                        type: [string, "null"]
                        description: 'Location name. `null` in case when not initialized yet.'
      tags:
        - Nuvei-NuveiHardwareElement
    post:
      summary: 'Creates terminal.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_model:
                  type: number
                  format: int32
                  description: 'Hardware model ID.'
                id_status:
                  type: number
                  format: int32
                  description: 'Status ID.'
                k_location:
                  type: string
                  description: 'Location key.'
                s_auth_key:
                  type: [string, "null"]
                  description: 'Authorization Key. `null` if not set.'
                s_register_id:
                  type: [string, "null"]
                  description: 'Register id, received from Nuvei on frontend. `null` if not set.'
                s_terminal_id:
                  type: [string, "null"]
                  description: 'Terminal id, received from Nuvei on frontend. `null` if not set.'
                text_label:
                  type: string
                  description: 'Label of the terminal.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_terminal:
                        type: [string, "null"]
                        description: 'Terminal key. `null` in case when create new terminal.'
      tags:
        - Nuvei-NuveiHardwareElement
  '/Thoth/PayHardware/Nuvei/NuveiHardwareList.json':
    get:
      summary: ''
      parameters:
        - name: id_status
          in: query
          description: 'Reader status ID for filter.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Text filter.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of available hardware.'
      tags:
        - Nuvei-NuveiHardwareList
  '/Thoth/PayHardware/DirectConnect/DirectConnectHardwareElement.json':
    delete:
      summary: 'Removes terminal.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - DirectConnect-DirectConnectHardwareElement
    get:
      summary: 'Fetch terminal information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_model:
                        type: number
                        format: int32
                        description: 'Hardware model ID.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Status ID.'
                      ip_terminal:
                        type: ["null", string]
                        description: 'IP address of terminal. Maybe an empty string as it is not set for all terminals.'
                      k_location:
                        type: string
                        description: 'Location key.'
                      s_terminal_id:
                        type: string
                        description: 'Terminal id which is added in the paragon.'
                      text_label:
                        type: string
                        description: 'Label of the terminal.'
                      text_location:
                        type: [string, "null"]
                        description: 'Location name. `null` in case when not initialized yet.'
      tags:
        - DirectConnect-DirectConnectHardwareElement
    post:
      summary: 'Creates or updates terminal.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_model:
                  type: number
                  format: int32
                  description: 'Hardware model ID.'
                id_status:
                  type: number
                  format: int32
                  description: 'Status ID.'
                k_location:
                  type: string
                  description: 'Location key.'
                s_terminal_id:
                  type: string
                  description: 'Terminal id which is added in the paragon.'
                text_label:
                  type: string
                  description: 'Label of the terminal.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_terminal:
                        type: [string, "null"]
                        description: 'Terminal key. `null` in case when create new terminal.'
      tags:
        - DirectConnect-DirectConnectHardwareElement
  '/Thoth/PayHardware/DirectConnect/DirectConnectHardwareList.json':
    get:
      summary: ''
      parameters:
        - name: id_status
          in: query
          description: 'Reader status ID for filter.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Text filter.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of available hardware.'
      tags:
        - DirectConnect-DirectConnectHardwareList
  '/Thoth/PayHardware/StripeCom/StripeComHardwareElement.json':
    delete:
      summary: 'Removes terminal.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - StripeCom-StripeComHardwareElement
    get:
      summary: 'Fetch terminal information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_model:
                        type: number
                        format: int32
                        description: 'Hardware model ID.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Status ID.'
                      ip_terminal:
                        type: ["null", string]
                        description: 'IP address of terminal. Maybe an empty string as it is not set for all terminals.'
                      k_location:
                        type: string
                        description: 'Location key.'
                      text_label:
                        type: string
                        description: 'Label of the terminal.'
                      text_location:
                        type: [string, "null"]
                        description: 'Location name. `null` in case when not initialized yet.'
      tags:
        - StripeCom-StripeComHardwareElement
    post:
      summary: 'Creates terminal.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_terminal
          in: query
          description: 'Terminal key. `null` in case when create new terminal.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_model:
                  type: number
                  format: int32
                  description: 'Hardware model ID.'
                id_status:
                  type: number
                  format: int32
                  description: 'Status ID.'
                k_location:
                  type: string
                  description: 'Location key.'
                s_registration_code:
                  type: [string, "null"]
                  description: 'Registration code. `null` in case when add terminal is not ethernet model. We do not save this field into database as of now.'
                s_serial_number:
                  type: [string, "null"]
                  description: 'Serial number. `null` in case when add terminal is ethernet model.'
                s_terminal_id:
                  type: [string, "null"]
                  description: 'Terminal id, received from Stripe on frontend. Not null only when the request is from the staff application and only for BT readers.'
                text_label:
                  type: string
                  description: 'Label of the terminal.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_terminal:
                        type: [string, "null"]
                        description: 'Terminal key. `null` in case when create new terminal.'
      tags:
        - StripeCom-StripeComHardwareElement
  '/Thoth/PayHardware/StripeCom/StripeComHardwareList.json':
    get:
      summary: ''
      parameters:
        - name: id_status
          in: query
          description: 'Reader status ID for filter.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Text filter.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of available hardware.'
      tags:
        - StripeCom-StripeComHardwareList
  '/Thoth/PayProcessor/Nuvei/NuveiTransactionStart.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card:
                  type: [array, "null"]
                  description: 'Credit card data. Contents of the payment source as it presents on the payment form. This data is passed into {@link \RsPayBankCardSelectWidget::data_set()}. Supplied in case payment source is card. `null` if payment source is other than card or payment is initialized before card data is known. <dl> <dt>array `a_pay_address`</dt> <dd> Payment address for address edit widget. <dl> <dt>bool `is_new`</dt> <dd>`true` if user has chosen to add new address, `false` otherwise.</dd> <dt>string `k_geo_country`</dt> <dd>Key of the country. Primary key in {@link \AGeoSql}. Required for newly added billing address.</dd> <dt>string `k_geo_region`</dt> <dd>Key of the region. Primary key in {@link \AGeoSql}. Required for newly added billing address.</dd> <dt>string `s_street1`</dt> <dd>First address line. Required for newly added billing address.</dd> <dt>string `s_street2`</dt> <dd>Second address line. Required for newly added billing address.</dd> <dt>string `s_city`</dt> <dd>City name. Required for newly added billing address.</dd> <dt>string `s_phone`</dt> <dd>Phone number. Required for newly added billing address.</dd> <dt>string `s_postal`</dt> <dd>Postal code. Required for newly added billing address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>Card CSC code. Required for newly added card.</dd> <dt>int `i_month`</dt> <dd>Card expiration month. Required for newly added card.</dd> <dt>int `i_year`</dt> <dd>Card expiration year. Required for newly added card.</dd> <dt>bool `is_default`</dt> <dd>If the card should be made the default payment method. Required for newly added card.</dd> <dt>bool `is_new`</dt> <dd>Whenever card is new.</dd> <dt>bool `is_save`</dt> <dd>If the card information should be saved for future transactions. Required for newly added card.</dd> <dt>string `k_pay_bank`</dt> <dd>Key of existing payment source in case of editing. Primary key from {@link \RsPayBankSql} table.</dd> <dt>string `s_name`</dt> <dd>Card nickname. Required for newly added card.</dd> <dt>string `s_number`</dt> <dd>Card number. Required for newly added card.</dd> </dl>'
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                is_save:
                  type: boolean
                  description: 'Whether the card information should be saved for future transactions.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_card_type:
                        type: [object, "null"]
                        description: 'Credit card type. `null` in case card type can not be determined.'
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Nuvei-NuveiTransactionStart
  '/Thoth/PayProcessor/Nuvei/NuveiOpenOrder.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      s_nuvei_order:
                        type: [string, "null"]
                        description: 'The order ID provided by Nuvei. `null` in case of errors.'
                      s_nuvei_session:
                        type: [string, "null"]
                        description: 'The session identifier to be used by the request that processes the newly opened order. `null` in case of errors.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Nuvei-NuveiOpenOrder
  '/Thoth/PayProcessor/Nuvei/NuveiTransactionOnAuthSuccess.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_bank:
                  type: [string, "null"]
                  description: 'Key of a selected payment source. `null` in case when an existed payment source is not used.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Key of payment transaction that was previously created with {@link \Thoth\PayProcessor\Nuvei\NuveiOpenOrderApi}. `null` in case when not initialized yet.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_encrypt:
                  type: [string, "null"]
                  description: 'Contents of the encrypted token. `null` in case when not initialized yet.'
                s_nuvei_order:
                  type: [string, "null"]
                  description: 'The order ID provided by Nuvei. `null` in case when not initialized yet.'
                s_nuvei_session:
                  type: [string, "null"]
                  description: 'The session identifier to be used by the request that processes the newly opened order. `null` in case when not initialized yet.'
                s_response:
                  type: [string, "null"]
                  description: 'Payment gateway-specific response object.'
                s_transaction:
                  type: [string, "null"]
                  description: 'The Gateway transaction ID.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Nuvei-NuveiTransactionOnAuthSuccess
  '/Thoth/PayProcessor/Nuvei/NuveiTransactionCancel.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Key of payment transaction that was created in {@link NuveiOpenOrderApi}. This will be `null` if not loaded yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Nuvei-NuveiTransactionCancel
  '/Thoth/PayProcessor/Nuvei/NuveiUpdateOrder.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Key of payment transaction that was created in {@link NuveiOpenOrderApi}. This will be `null` if not loaded yet.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_nuvei_order:
                  type: [string, "null"]
                  description: 'The order ID that was created in {@link NuveiOpenOrderApi}. This will be `null` if not loaded yet.'
                s_nuvei_session:
                  type: [string, "null"]
                  description: 'The session identifier in which order was created. This will be `null` if not loaded yet.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Nuvei-NuveiUpdateOrder
  '/Thoth/PayProcessor/StripeCom/PaymentIntentUpdate.json':
    post:
      summary: ''
      parameters:
        - name: id_pay_actor
          in: query
          description: 'ID of the actor. One of {@link \RsPayActorSid} constants. Required.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business to update Payment Intent for. Primary key in {@link \RsBusinessSql}. Required.'
          schema:
            type: string
        - name: k_business_merchant
          in: query
          description: 'Key of the business merchant to update Payment Intent for. Primary key in {@link \RsBusinessMerchantSql}. Required.'
          schema:
            type: string
        - name: k_pay_transaction
          in: query
          description: 'Key of the payment transaction to update. Required.'
          schema:
            type: string
        - name: s_payment_intent
          in: query
          description: 'Payment intent ID to update. Required.'
          schema:
            type: string
        - name: uid_purchase
          in: query
          description: 'Payment owner user key. `null` if pay owner is anonymous.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: number
                  format: int32
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. Required.'
                m_amount:
                  type: string
                  description: 'Payment amount to set. Required.'
                m_surcharge:
                  type: string
                  description: 'Surcharge amount to set. Required.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - StripeCom-PaymentIntentUpdate
  '/Thoth/PayProcessor/StripeCom/StripeComCharge.json':
    get:
      summary: ''
      parameters:
        - name: id_currency
          in: query
          description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants.'
          schema:
            type: number
            format: int32
        - name: id_pay_actor
          in: query
          description: 'ID of the actor. One of {@link \RsPayActorSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_business_merchant
          in: query
          description: 'Key of the business merchant to get the public key for. Primary key in {@link \RsBusinessMerchantSql}.'
          schema:
            type: string
        - name: s_charge
          in: query
          description: 'Charge id to get data for.'
          schema:
            type: string
        - name: uid_purchase
          in: query
          description: 'Payment owner user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      o_payment_method_details:
                        type: array
                        description: 'Details about the payment method at the time of the transaction: <dl> <dt>string `s_generated_card_id`</dt> <dd>ID of a card payment method generated.</dd> <dt>string `text_brand`</dt> <dd>Card brand, like visa or mastercard.</dd> <dt>string `text_cardholder_name`</dt> <dd>The cardholder name as read from the card.</dd> <dt>string `text_exp_month`</dt> <dd>Two-digit number representing the card''s expiration month.</dd> <dt>string `text_exp_year`</dt> <dd>Four-digit number representing the card''s expiration year.</dd> <dt>string `text_last4`</dt> <dd>Last four digits of the card number.</dd> </dl>'
                      s_payment_method:
                        type: string
                        description: 'ID of the payment method used in this charge.'
      tags:
        - StripeCom-StripeComCharge
  '/Thoth/PayProcessor/StripeCom/StripeComConnectionToken.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: number
                  format: int32
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants.'
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants.'
                k_business:
                  type: string
                  description: 'Key of the business. Primary key in {@link \RsBusinessSql}.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant to get the public key for. Primary key in {@link \RsBusinessMerchantSql}.'
                k_location:
                  type: [string, "null"]
                  description: 'Location key that this connection token is scoped to. If you provide a location, the connection token is only usable with smart readers assigned to that location. If you do not provide a location, the connection token is usable with all readers. For Bluetooth readers, the location of a connection token has no effect. This ensures that Bluetooth readers near you are always discoverable. Primary key in the {@link \RsLocationSql}. `null` if the connection token should be usable with all readers.'
                uid_purchase:
                  type: string
                  description: 'Payment owner user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_secret:
                        type: string
                        description: 'Connection token secret key.'
      tags:
        - StripeCom-StripeComConnectionToken
  '/Thoth/PayProcessor/StripeCom/StripeComKey.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants.'
                k_business:
                  type: string
                  description: 'Key of the business. Primary key in {@link \RsBusinessSql}.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant to get the public key for. Primary key in {@link \RsBusinessMerchantSql}.'
                uid_purchase:
                  type: string
                  description: 'Payment owner user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_public:
                        type: [string, "null"]
                        description: 'Merchant public key. `null` before the key is set. It is set by {@link \Thoth\PayProcessor\StripeCom\StripeComKeyApi::post()}.'
      tags:
        - StripeCom-StripeComKey
  '/Thoth/PayProcessor/StripeCom/StripeComPayInit.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card:
                  type: [array, "null"]
                  description: 'Credit card data. Contents of the payment source as it presents on the payment form. This data is passed into {@link \RsPayBankCardSelectWidget::data_set()}. Supplied in case payment source is card. `null` if payment source is other than card or payment is initialized before card data is known.'
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_pay_method:
                  type: [number, "null"]
                  description: 'ID of pay method. One of {@link \RsPayMethodSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                is_pos:
                  type: boolean
                  description: '`true` if card swiper is used, `false` otherwise.'
                is_stripe_elements:
                  type: boolean
                  description: '`true` if Stripe Payment Element form (@link https://stripe.com/docs/payments/elements) is used, `false` otherwise.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_payment_method:
                  type: [string, "null"]
                  description: 'Payment method ID. `null` if Payment intent must be created without existing Payment method. This is the case for card swiper or Stripe Payment Element ({@link https://stripe.com/docs/payments/elements}) - create payment intent first, then create payment method. And vice versa in case of virtual terminal.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_status:
                        type: number
                        format: int32
                        description: 'Status of this Payment Intent. One of {@link PaymentIntentStatusSid} constants.'
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      s_client_secret:
                        type: string
                        description: 'Payment Intent client secret key.'
                      s_payment_intent:
                        type: string
                        description: 'Payment intent ID.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - StripeCom-StripeComPayInit
  '/Thoth/PayProcessor/StripeCom/StripeComPaymentIntentCancel.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants.'
                k_business:
                  type: string
                  description: 'Key of the business. Primary key in {@link \RsBusinessSql}.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant, that processes payment. Primary key in {@link \RsBusinessMerchantSql}.'
                k_pay_transaction:
                  type: string
                  description: 'Pay transaction key, which was used to create payment intent.'
                s_payment_intent:
                  type: string
                  description: 'Payment intent ID.'
                uid_purchase:
                  type: string
                  description: 'Payment owner user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - StripeCom-StripeComPaymentIntentCancel
  '/Thoth/PayProcessor/CyberSource/CsCaptureContext.json':
    post:
      summary: '1) it requires interaction with the user in the browser; 2) Cyber Source requires that we have `localhost` as a domain which is incompatible with our testing cluster configuration.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant that should be used to perform the request. Primary key in {@link \RsBusinessMerchantSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_client_library_integrity:
                        type: [string, "null"]
                        description: 'Microform Integration JavaScript library integrity property value. `null` until generated.'
                      s_key:
                        type: [string, "null"]
                        description: 'Contents of the key. `null` until generated.'
                      s_key_id:
                        type: [string, "null"]
                        description: 'ID of the key. `null` until generated.'
                      url_client_library:
                        type: [string, "null"]
                        description: 'Microform Integration JavaScript library url. `null` until generated.'
      tags:
        - CyberSource-CsCaptureContext
  '/Thoth/PayProcessor/CyberSource/CsPaEnrollment.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_device_info:
                  type: array
                  description: 'User device info: <dl> <dt>int `i_browser_screen_height`</dt> <dd>User browser screen height.</dd> <dt>int `i_browser_screen_width`</dt> <dd>User browser screen width.</dd> </dl>'
                k_pay_transaction:
                  type: string
                  description: 'Key of payment transaction that was previously created with {@link \Thoth\PayProcessor\CyberSource\CsPaSetupApi}. Primary key in {@link \RsPayTransactionSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_jwt:
                        type: [string, "null"]
                        description: 'JSON Web Token to include into the request to the second frame.'
                      url_interact:
                        type: [string, "null"]
                        description: 'URL for frontend interaction. This normally means that payer needs to make further steps for the authentication. `null` if no interaction required, and we should proceed with the withdrawal. This normally means that payer is authenticated. If payer is not authenticated, or there is an error occurred during check of the enrollment, this API returns an exception.'
      tags:
        - CyberSource-CsPaEnrollment
  '/Thoth/PayProcessor/CyberSource/CsPaSetup.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card:
                  type: array
                  description: 'Credit card data. Contents of the payment source as it presents on the payment form. This data is passed into {@link \RsPayBankCardSelectWidget::data_set()}.'
                id_pay_actor:
                  type: number
                  format: int32
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization and void immediately. `false` (or anything that is empty, or do not pass this value) to capture the payment amount.'
                k_business:
                  type: string
                  description: 'Key of the business which receives the payment. Primary key in {@link \RsBusinessSql}.'
                k_business_merchant:
                  type: string
                  description: 'Key of the business merchant that should be used to perform the request. Primary key in {@link \RsBusinessMerchantSql}.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. Primary key in {@link MethodSql}. `null` or an empty string if payment is performed with a standard payment method. Only payment methods based on {@link \RsPayMethodSid::ECOMMERCE} are accepted.'
                m_amount:
                  type: string
                  description: 'Payment amount.'
                m_surcharge:
                  type: string
                  description: 'Surcharge amount. Empty string or null for zero surcharge amount.'
                uid_pay_owner:
                  type: string
                  description: 'UID of the payment owner.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: string
                        description: 'Key of payment transaction that was created. Primary key in {@link \RsPayTransactionSql}.'
                      s_jwt:
                        type: string
                        description: 'JWT token, as returned by Cyber Source.'
                      s_reference:
                        type: string
                        description: 'Reference ID, as returned by Cyber Source.'
                      url_collection:
                        type: string
                        description: 'Device collection URL, as returned by Cyber Source.'
      tags:
        - CyberSource-CsPaSetup
  '/Thoth/PayProcessor/CyberSource/CsPaValidate.json':
    post:
      summary: '1) it requires interaction with the user in the browser; 2) Cyber Source requires that we have `localhost` as a domain which is incompatible with out testing cluster configuration.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_pay_transaction:
                  type: string
                  description: 'Key of payment transaction that was previously created with {@link \Thoth\PayProcessor\CyberSource\CsPaSetupApi}. Primary key in {@link \RsPayTransactionSql}.'
                s_transaction:
                  type: string
                  description: 'Payer authentication transaction ID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CyberSource-CsPaValidate
  '/Thoth/ProgressLog/Report/ProgressLogReportInfo.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Key of the user, primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'Key of the user, whose progress log profile page to render. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_chart_metric:
                        type: array
                        description: 'List of available progress log chart metric fields. Each item is result of {@link Field::getField()} with additional information. Complete structure of each item: <dl> <dt>array `a_measurement_unit`</dt> <dd> Measurement unit short form list. See {@link \Wl\Member\Progress\Field\MeasurementSid::getShortFormList()} for more details. </dd> <dt>array `a_measurement_unit_order`</dt> <dd> Order of measurement units. Each value is one from {@link \Wl\Member\Progress\Field\MeasurementSid} constants, each key is order position. This is needed to fix problem that order of JSON object keys is not preserved. </dd> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid} constants.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>bool `is_active`</dt> <dd>Whether field is active and should be displayed on page.</dd> <dt>bool `is_combined`</dt> <dd>Whether measurement combines several measurement units and field needs several inputs.</dd> <dt>bool `is_public`</dt> <dd>Whether this field is public. If this field is set to 0, this field is not visible to clients.</dd> <dt>bool `is_require`</dt> <dd>Whether field value is required for clients.</dd> <dt>bool `is_require_staff`</dt> <dd>Whether field value is required for staffs.</dd> <dt>string `k_business`</dt> <dd>Business Key. Primary key in the {@link \RsBusinessSql} table.</dd> <dt>string `k_field`</dt> <dd>Field key. Primary key it the {@link \Wl\Member\Progress\Field\ProgressFieldSql} table.</dd> <dt>string `s_column_name`</dt> <dd>Field name of this metric within {@link \Thoth\ProgressLog\Report\ProgressLogListReport} report row.</dd> <dt>string `text_title`</dt> <dd>Progress field title.</dd> </dl>'
                      can_add_measurement:
                        type: boolean
                        description: 'Whether specified actor can add a new transformation log measurement for the specified client. `true` if they can, `false` otherwise.'
      tags:
        - Report-ProgressLogReportInfo
  '/Thoth/ProgressLog/ProfilePage/ProgressLogProfilePageView.json':
    get:
      summary: ''
      parameters:
        - name: dl_compare
          in: query
          description: 'Date to compare progress log with. `null` to only display latest values without comparison.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of the business, in which profile page is rendered.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Key of the user who is viewing the profile page.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'Key of the user, whose progress log profile page to render.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_view:
                        type: string
                        description: 'Rendered progress log profile page.'
      tags:
        - ProfilePage-ProgressLogProfilePageView
  '/Thoth/ReportCore/Generator/Filter.json':
    get:
      summary: 'Gets last configuration of the report filter.'
      parameters:
        - name: cid_page
          in: query
          description: 'CID of a report page. One of {@link ReportPageAbstract} subclasses. Empty in a case of a single report.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'CID of a report. One of {@link ReportGeneratorReportAbstract} subclasses. Empty in a case of a report page.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business that report is shown. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Key of the saved report. Primary key in {@link \RsReportSaveSql}.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Actor user key. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_filter:
                        type: string
                        description: 'Filter that is applied to the report. `array` temporary solution.'
      tags:
        - Generator-Filter
    post:
      summary: 'Saves last configuration of the report filter.'
      parameters:
        - name: cid_page
          in: query
          description: 'CID of a report page. One of {@link ReportPageAbstract} subclasses. Empty in a case of a single report.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'CID of a report. One of {@link ReportGeneratorReportAbstract} subclasses. Empty in a case of a report page.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business that report is shown. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Key of the saved report. Primary key in {@link \RsReportSaveSql}.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Actor user key. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_filter:
                  type: string
                  description: 'Filter that is applied to the report. `array` temporary solution.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Generator-Filter
  '/Thoth/ReportCore/Generator/FilterInfo.json':
    post:
      summary: 'Returns information about filters.'
      parameters:
        - name: cid_report
          in: query
          description: 'CID of the report which filter information should be retrieved. One of {@link ReportGeneratorReportAbstract} subclasses.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business which report should be shown. Primary key in {@link \RsBusinessSql}. `0` or an empty string for system-wide reports.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Actor user. Primary key in {@link \PassportLoginSql}. `0` or an empty string for guests.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_filter:
                  type: string
                  description: 'Filters that should be applied to the report. In this array, key is name of a filter field. A filter field is a property of a {@link ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: object
                        description: 'Additional filter field data. This array is gathered with data returned by {@link \Thoth\ReportCore\Generator\ReportFilterFieldAbstract::data()}.'
                      a_dynamic:
                        type: [array, object]
                        description: 'A list of dynamic filter fields in this report. Copy of result of {@link \Thoth\ReportCore\Generator\ReportGeneratorReportAbstract::filterDynamic()}.'
                      a_hide:
                        type: array
                        description: 'A list of hidden filter fields in this report. Each element is a {@link ReportFilterFieldInfo::$s_name filter field name}.'
                      a_message:
                        type: array
                        description: 'Warning list of the report.'
      tags:
        - Generator-FilterInfo
  '/Thoth/ReportCore/Generator/Query.json':
    post:
      summary: 'Returns contents of a report as a table. If the report is not yet generated, it gets generated. If the report is being generated now, partial content may be returned.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_report:
                  type: number
                  format: int32
                  description: 'CID of the report to show. One of {@link ReportGeneratorReportAbstract} subclasses.'
                i_limit:
                  type: number
                  format: int32
                  description: 'How many rows of the report to return. `0` to not to return content at all. This may be useful to get metadata of the report.'
                i_offset:
                  type: number
                  format: int32
                  description: 'Offset. How many rows of the report to skip at the beginning of the list. Only return rows after the last skipped row. `0` to return contents from the very beginning.'
                is_actual:
                  type: boolean
                  description: 'Defines whether actual or already generated report should be returned. `true` to not attempt search generated report and generate it again. `false` to attempt search generated report.'
                is_backend:
                  type: boolean
                  description: 'Whether report is generating for the backend.'
                is_export:
                  type: boolean
                  description: 'Whether the timeout check should be enforced during cell loading. Set to `true` by the export controller to abort cell loading with a user-facing error when the remaining execution time drops below {@link QueryApi::TIME_LIMIT_BUFFER_SECOND}.'
                is_refresh:
                  type: boolean
                  description: 'Whether this report should be refreshed. `true` to refresh this report if it is already generated. Refreshing of the report may not be queried while report is being generated. `false` to only return contents of the report. If report is not yet generated, it automatically starts the generation in the background.'
                json_filter:
                  type: string
                  description: 'Filters that should be applied to the report. In this array, key is name of a filter field. <i> A filter field is a property of a {@link ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array. </i>'
                k_business:
                  type: string
                  description: 'Key of the business which report should be shown. `0` or an empty string for system-wide reports.'
                s_sort:
                  type: string
                  description: 'Sorting order. A comma-separated list of fields to sort by. Name of a field may be prepended with a `+` or `-` sign to sort in ascending or descending order. Ascending order is the default. You may sort reports by report fields and by cell fields. To sort by a report field, just specify name of that field. To sort by a cell field, specify name of a report field and name of the cell field. Separate with a dot. For example: <tt>o_account.m_amount</tt>. <i> You may specify a field which contains objects of {@link ReportGeneratorCellAbstract} without specification of a name of a cell field. In this case sorting specified by {@link ReportGeneratorCellAbstract::SORT} will be applied. </i> Example value: <tt>s_first,+s_last,-o_account.m_amount,o_address</tt>. Means the following: * sort by <tt>s_first</tt> in ascending order (which is the default). * sort by <tt>s_last</tt> in ascending order (which is specified explicitly). * sort by <tt>m_amount</tt> field of <tt>o_account</tt> cell in descending order. * sort by default field (i.e. the one marked with &#64;`sort` tag) of <tt>o_address</tt> cell in ascending order. Note that you may not sort by fields that are marked with &#64;`store-no` tag.'
                s_sql:
                  type: string
                  description: 'An SQL query that should be executed on report table. See <tt>namespace.Wl/Report/Generator/doc/report-query-sql.md</tt> for details. Empty string to select all columns of the report table.'
                uid_actor:
                  type: string
                  description: 'Actor user. `0` or an empty string for guests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_dynamic:
                        type: object
                        description: 'A list of dynamic fields in this report. Copy of result of {@link ReportGeneratorReportAbstract::generatorDynamic()}.'
                      a_field:
                        type: array
                        description: 'A list of fields in this report. This array is effectively a title row for table that is returned in {@link QueryApi::$a_row}.'
                      a_row:
                        type: object
                        description: 'Report data. This is an indexed array in which one row is an indexed array also. Indexes of the columns correspond columns in {@link QueryApi::$a_field}.'
                      a_stale:
                        type: array
                        description: 'A list of stale rows. This array is only filled in when report is being updated now, or due to some other reasons contains mixed version data (some data from the latest generation, and other from one of previous generations). Value is index in {@link QueryApi::$a_row}. If a row is not listed here, it is of the latest version.'
                      a_warning:
                        type: array
                        description: 'Warning list of the report.'
                      dtu_complete:
                        type: string
                        description: 'Date and time when this report has completed generation. `null` if generation of this report is not completed.'
                      dtu_queue:
                        type: string
                        description: 'Date and time when this report was put on generation queue. Effectively, this is the time when a user clicked to view this report.'
                      dtu_start:
                        type: string
                        description: 'Date and time when generation of this report has started. `null` if generation of this report is not started.'
                      i_cas_change:
                        type: number
                        format: int32
                        description: 'A CAS (compare-and-swap) number that allows to track changes in the report storage. This number is changed every time content of the report gets updated. If this number is not changed, the content is not updated.'
                      id_report_status:
                        type: number
                        format: int32
                        description: 'Status of the report. One of {@link ReportGeneratorStatusSid} constants.'
                      s_report:
                        type: string
                        description: 'Key of this report. This key may be used to subscribe to report changes with {@link UpdateChannel}.'
                      text_error:
                        type: string
                        description: 'Text of an error message that occurred during generation of the report. An empty string in no error messages have occurred.'
      tags:
        - Generator-Query
  '/Thoth/WlPay/Form/Environment.json':
    get:
      summary: 'Returns information about payment environment. Called before rendering a payment form to determine which payment methods and card types are available for a given business and location, what surcharges apply, and how the form should behave (tip prompt, optional card save). This endpoint is deprecated; use {@link \Thoth\WlPay\Form\EnvironmentUserApi} for new integrations.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to retrieve payment information for. This will be `0` if not set yet. An empty string or `null` if payments are performed with the WellnessLiving system merchant.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'The key of the currency to retrieve payment information for.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to retrieve payment information for. This will be `0` if not set yet.'
          schema:
            type: string
        - name: uid_owner
          in: query
          description: 'The user ID to retrieve payment information for. `null` if not defined.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_card_system:
                        type: array
                        description: 'A list of supported bank card systems. Keys refer to card types based on card numbers (see {@link \ACardSystemSid}), and values are IDs of card systems (one of {@link \ACardSystemSid} constants).'
                      a_method_staff:
                        type: array
                        description: 'A list of payment methods enabled for staff members. The ID is one of {@link \RsPayMethodSid} constants. The value is always `true`.'
                      a_method_support:
                        type: array
                        description: 'A list of all payment methods that can be used within this business. This array is sorted in the order in which payment methods should be shown to the user. Each element of the array has the following structure: <dl> <dt>int `id_pay_method`</dt> <dd>The ID of type of payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_client`</dt> <dd> Determines whether this method is available for clients. This field is only returned for custom payment methods. </dd> <dt>string|null `k_pay_method`</dt> <dd> The key of the custom payment method. This will be `null` if this payment method isn''t customized. <i>Primary key in {@link \Thoth\WlPay\Method\MethodSql} table.</i> </dd> <dt>string `s_method`</dt> <dd>The name of payment method. This field is only returned for custom payment methods.</dd> </dl>'
                      a_mobile_config:
                        type: [array, "null"]
                        description: 'The configuration array that''s sent to mobile card reader plugin. The structure of this array depends on the payment processor being used. `null` when mobile card readers are not supported, or when actor has no access to them.'
                      a_pay_processor:
                        type: [array, "null"]
                        description: 'Represents information about payment processors. Keys are payment methods IDs, one of {@link \RsPayMethodSid} constants. Value is the following array: `null` if no processors are set up. <dl> <dt>array|null `a_public_keys`</dt> <dd> Public keys configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicKeysInterface::publicKeys()}.</i> `null` if this payment processor does not support public keys. </dd> <dt>array|null `a_public_info`</dt> <dd> Public info configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicInfoInterface::publicInfo()}.</i> `null` if this payment processor does not support public info. </dd> <dt>bool `hide_save_source`</dt> <dd>Whether `save payment method` option should be hidden. `true` if hidden, `false` - otherwise.</dd> <dt>int `id_pay_processor`</dt> <dd>ID of the payment processor. One of {@link \Thoth\PayProcessor\PayProcessorSid} constants.</dd> <dt>null|bool `is_enabled_3ds`</dt> <dd> `true` if 3DS should be performed, `false` if 3DS should not be performed. `null` if this is not defined for payment processor. Only for {@link \Thoth\PayProcessor\PayProcessorSid::NUVEI} </dd> <dt>bool|null `is_test`</dt> <dd> `true` if the merchant is in a test mode, `false` otherwise. `null` if this is not defined for payment processor. </dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant. <i>Primary key in {@link \RsBusinessMerchantSql}.</i></dd> </dl>'
                      dl_now:
                        type: string
                        description: 'Current local date in current location {@link EnvironmentApi::$k_location} or business {@link EnvironmentApi::$k_business} if not set location.'
                      f_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      f_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      id_locale:
                        type: [number, "null"]
                        description: 'The locale ID of the business.'
                      is_save_optional:
                        type: boolean
                        description: '`true` if clients can choose whether their banking and credit card information is saved at checkout, `false` if this information is always saved.'
                      is_save_source:
                        type: boolean
                        description: 'Determines whether newly added payment sources should be saved. This will be `true` if payment sources should be saved, `false` if otherwise.'
                      is_tip:
                        type: boolean
                        description: 'Whether tips are accepted.'
                      m_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
                      m_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
      tags:
        - Form-Environment
  '/Thoth/WlPay/Form/EnvironmentUser.json':
    get:
      summary: 'Returns information about payment environment. Called before rendering a payment form to determine which payment methods and card types are available for a given business and location, what surcharges apply, and how the form should behave (tip prompt, optional card save). This endpoint is deprecated; use {@link \Thoth\WlPay\Form\EnvironmentUserApi} for new integrations.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to retrieve payment information for. This will be `0` if not set yet. An empty string or `null` if payments are performed with the WellnessLiving system merchant.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'The key of the currency to retrieve payment information for.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to retrieve payment information for. This will be `0` if not set yet.'
          schema:
            type: string
        - name: uid_owner
          in: query
          description: 'The user ID to retrieve payment information for. `null` if not defined.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_card_system:
                        type: array
                        description: 'A list of supported bank card systems. Keys refer to card types based on card numbers (see {@link \ACardSystemSid}), and values are IDs of card systems (one of {@link \ACardSystemSid} constants).'
                      a_method_staff:
                        type: array
                        description: 'A list of payment methods enabled for staff members. The ID is one of {@link \RsPayMethodSid} constants. The value is always `true`.'
                      a_method_support:
                        type: array
                        description: 'A list of all payment methods that can be used within this business. This array is sorted in the order in which payment methods should be shown to the user. Each element of the array has the following structure: <dl> <dt>int `id_pay_method`</dt> <dd>The ID of type of payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_client`</dt> <dd> Determines whether this method is available for clients. This field is only returned for custom payment methods. </dd> <dt>string|null `k_pay_method`</dt> <dd> The key of the custom payment method. This will be `null` if this payment method isn''t customized. <i>Primary key in {@link \Thoth\WlPay\Method\MethodSql} table.</i> </dd> <dt>string `s_method`</dt> <dd>The name of payment method. This field is only returned for custom payment methods.</dd> </dl>'
                      a_mobile_config:
                        type: [array, "null"]
                        description: 'The configuration array that''s sent to mobile card reader plugin. The structure of this array depends on the payment processor being used. `null` when mobile card readers are not supported, or when actor has no access to them.'
                      a_pay_processor:
                        type: [array, "null"]
                        description: 'Represents information about payment processors. Keys are payment methods IDs, one of {@link \RsPayMethodSid} constants. Value is the following array: `null` if no processors are set up. <dl> <dt>array|null `a_public_keys`</dt> <dd> Public keys configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicKeysInterface::publicKeys()}.</i> `null` if this payment processor does not support public keys. </dd> <dt>array|null `a_public_info`</dt> <dd> Public info configured for this payment processor. <i>Copy of result of {@link \Thoth\PayProcessor\ProcessorInterface\PayProcessorPublicInfoInterface::publicInfo()}.</i> `null` if this payment processor does not support public info. </dd> <dt>bool `hide_save_source`</dt> <dd>Whether `save payment method` option should be hidden. `true` if hidden, `false` - otherwise.</dd> <dt>int `id_pay_processor`</dt> <dd>ID of the payment processor. One of {@link \Thoth\PayProcessor\PayProcessorSid} constants.</dd> <dt>null|bool `is_enabled_3ds`</dt> <dd> `true` if 3DS should be performed, `false` if 3DS should not be performed. `null` if this is not defined for payment processor. Only for {@link \Thoth\PayProcessor\PayProcessorSid::NUVEI} </dd> <dt>bool|null `is_test`</dt> <dd> `true` if the merchant is in a test mode, `false` otherwise. `null` if this is not defined for payment processor. </dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant. <i>Primary key in {@link \RsBusinessMerchantSql}.</i></dd> </dl>'
                      dl_now:
                        type: string
                        description: 'Current local date in current location {@link EnvironmentApi::$k_location} or business {@link EnvironmentApi::$k_business} if not set location.'
                      f_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      f_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a percent of transaction amount. This will be `null` if the percentage surcharge amount shouldn''t be withdrawn.'
                      id_locale:
                        type: [number, "null"]
                        description: 'The locale ID of the business.'
                      is_save_optional:
                        type: boolean
                        description: '`true` if clients can choose whether their banking and credit card information is saved at checkout, `false` if this information is always saved.'
                      is_save_source:
                        type: boolean
                        description: 'Determines whether newly added payment sources should be saved. This will be `true` if payment sources should be saved, `false` if otherwise.'
                      is_tip:
                        type: boolean
                        description: 'Whether tips are accepted.'
                      m_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with card represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
                      m_surcharge_ach:
                        type: [string, "null"]
                        description: 'Surcharge amount for payment with ACH represented as a fixed amount. This will be `null` if the fixed surcharge amount shouldn''t be withdrawn.'
      tags:
        - Form-EnvironmentUser
  '/Thoth/WlPay/Form/PayLog.json':
    post:
      summary: 'Logs events occurred in the browser.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: [array, string]
                  description: 'Additional data attached to the log item. JSON-encoded array mary arrives as a `string`.'
                id_log:
                  type: number
                  format: int32
                  description: 'Type of log item.'
                k_business:
                  type: string
                  description: 'Business key.'
                text_message:
                  type: string
                  description: 'Message content.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Form-PayLog
  '/Thoth/WlPay/Bank/Primary.json':
    put:
      summary: 'Sets a specified payment method as default.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_pay_bank:
                  type: string
                  description: 'ID of pay bank.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bank-Primary
  '/Thoth/WlPay/Bank/Entity.json':
    post:
      summary: 'Creates a new payment source.'
      parameters:
        - name: id_currency
          in: query
          description: 'ID of the currency of this payment entity is managed.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business within which this payment entity is managed.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location within which this payment entity is managed. Empty string, zero should be passed here if a business-wide merchant should be used. In this case it is also allowed to not to pass this parameter.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Contains data associated with payment source. Structure of this array depends on type of the payment source.'
                id_pay_method:
                  type: number
                  format: int32
                  description: 'Type of payment source. One of {@link \RsPayMethodSid} constants.'
                k_pay_owner:
                  type: string
                  description: 'Key of payment owner.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bank-Entity
  '/Thoth/WlPay/Method/List.json':
    get:
      summary: 'Returns list of active payment methods data. Returns the system-level payment methods enabled for the business combined with any custom payment methods configured for the business and accessible to the given user based on their role. When `$is_active` is `false`, inactive custom methods are included as well.'
      parameters:
        - name: is_active
          in: query
          description: 'Whether only active payment methods should be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: show_manual
          in: query
          description: 'Whether payment method {@link \RsPayMethodSid::ACCOUNT_MANUAL} should be included in response.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_method:
                        type: array
                        description: 'A list of payment methods: <dl> <dt>int `id_pay_method`</dt> <dd>The payment method type ID. One of {@link \RsPayMethodSid} constants.</dd> <dt>string|null `k_pay_method`</dt> <dd> Payment method key. <i>Primary key int the {@link \Thoth\WlPay\Method\MethodSql} table.</i> May be <tt>null</tt> if it is a system payment method. </dd> <dt>string `s_title`</dt> <dd>The payment method name.</dd> </dl>'
      tags:
        - Method-List
  '/Thoth/WlPay/Transaction/Edit.json':
    put:
      summary: 'Changes payment transaction according to request.'
      parameters:
        - name: k_pay_transaction
          in: query
          description: 'ID of a payment transaction to change. Primary key from {@link \RsPayTransactionSql}. May be empty if {@link EditApi::$k_pay_transaction_process} is specified.'
          schema:
            type: string
        - name: k_pay_transaction_process
          in: query
          description: 'ID of a payment transaction process to change. Primary key from {@link \RsPayTransactionProcessSql}. May be empty if {@link EditApi::$k_pay_transaction} is specified.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_time:
                  type: array
                  description: 'New time of payment transaction: Not specified if should not be changed. <dl> <dt>int `i_hour`</dt> <dd>Number of hours.</dd> <dt>int `i_minute`</dt> <dd>Number of minutes.</dd> <dt>bool `is_am`</dt> <dd>Whether time in AM or PM.</dd> </dl>'
                dt_date_local:
                  type: string
                  description: 'New date without time of payment transaction process. Not specified if should not be changed.'
                id_pay_method:
                  type: number
                  format: int32
                  description: 'New base payment method of payment transaction. One of {@link \RsPayMethodSid} constants. Not specified if should not be changed.'
                k_pay_method:
                  type: string
                  description: 'New custom payment method of payment transaction. Primary key from {@link \Thoth\WlPay\Method\MethodSql}. Not specified if should not be changed or if base payment method should be used.'
                m_amount:
                  type: string
                  description: 'New amount of payment transaction process. Not specified if should not be changed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_amount:
                        type: string
                        description: 'Changed amount of the transaction.'
                      html_date:
                        type: string
                        description: 'Changed date of the transaction.'
      tags:
        - Transaction-Edit
  '/Thoth/WlPay/Transaction/Status.json':
    get:
      summary: 'Get a payment status for multiple transactions.'
      parameters:
        - name: a_pay_transaction
          in: query
          description: 'List of keys of transactions to get statuses for. Values are primary keys from the {@link \RsPayTransactionSql} table. `null` if not initialized yet. via POST. The annotation is kept for backward compatibility with any external GET callers (e.g. third-party integrations or SDK consumers) that may pass keys as standard `a_pay_transaction[]=123` query parameters. Safe to remove only after confirming no external callers exist.'
          schema:
            type: [array, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_transaction_data:
                        type: [array, "null"]
                        description: 'Loaded data about transactions. Values are array with the following properties: `null` if not initialized yet. <dl> <dt>int `id_pay_transaction_status`</dt> <dd>Transaction payment status. One of {@link \RsPayTransactionStatusSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key. Primary key from the {@link \RsBusinessSql} table.</dd> <dt>string `k_pay_transaction`</dt> <dd>Transaction key. Primary key from the {@link \RsPayTransactionSql} table.</dd> </dl>'
      tags:
        - Transaction-Status
    post:
      summary: 'Get a payment status for multiple transactions via POST body. This method exists to fix an HTTP 414 "URI Too Long" error that occurred when a report contained a large number of transactions (e.g. a full month of Batch Reconciliation). The JS caller `StatusReportCell.afterLoadTable()` passed all transaction keys as a JSON-encoded GET query parameter, causing the URL to exceed server limits. Switching to POST moves the payload to the request body, which has no length restriction.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_pay_transaction:
                  type: string
                  description: 'List of keys of transactions to get statuses for, JSON-encoded array of primary keys. Originally this field was a GET input with type `array`. For GET parameters the framework automatically json-decodes fields whose names start with `json_` when the declared type is `array`, so no manual decoding was needed. However, that auto-decode only applies to query string parameters - POST body data goes through a different code path and the auto-decode does not fire. As a result, a POST body value (a JSON string) could not be assigned to an `array` typed property and the field silently stayed empty. To fix HTTP 414 "URI Too Long" errors when the report contains many transactions, the JS caller `StatusReportCell.afterLoadTable()` was switched from GET to POST (see {@link StatusApi::post()}). The annotation was changed to a POST input and the type was changed from `array` to `string` so the raw JSON string from the POST body is received as-is; {@link StatusApi::post()} calls `json_decode()` explicitly before passing the keys to {@link StatusApi::_loadStatuses()}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_transaction_data:
                        type: [array, "null"]
                        description: 'Loaded data about transactions. Values are array with the following properties: `null` if not initialized yet. <dl> <dt>int `id_pay_transaction_status`</dt> <dd>Transaction payment status. One of {@link \RsPayTransactionStatusSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key. Primary key from the {@link \RsBusinessSql} table.</dd> <dt>string `k_pay_transaction`</dt> <dd>Transaction key. Primary key from the {@link \RsPayTransactionSql} table.</dd> </dl>'
      tags:
        - Transaction-Status
    put:
      summary: 'Changes status of payment transaction.'
      parameters:
        - name: k_pay_transaction
          in: query
          description: 'ID of a payment transaction to change. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_pay_transaction_status:
                  type: [number, "null"]
                  description: 'New status of payment transaction. One of {@link \RsPayTransactionStatusSid} constants. `null` if not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_pay_transaction_status:
                        type: [string, "null"]
                        description: 'HTML output of {@link PayTransactionStatus} based on <var>k_pay_transaction</var>. `null` if not initialized yet.'
      tags:
        - Transaction-Status
  '/Thoth/WlPay/Account/Account.json':
    get:
      summary: 'Retrieves information about accounts of given user in given business. Returns the list of existing accounts and accounts not yet created for the user within the specified business, including balance, currency, and payment method details. When {@link AccountApi::$is_owner} is `true`, resolves the money owner and includes the debtor status.'
      parameters:
        - name: is_owner
          in: query
          description: 'If `true`, information for the account''s owner is returned. Clients can be configured to pay for a relative''s expenses. For example, a parent can pay for their child. Otherwise, `false` to indicate information strictly for the specified user is returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_account:
                        type: array
                        description: 'A list of the user''s accounts. Keys are account keys. This could be `0` if the user has no accounts in the currency of the given business. Values are account data: <dl> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constant.</dd> <dt>string `k_currency`</dt> <dd>Key of account currency. <i>Primary key in {@link \RsCurrencySql} table.</i></dd> <dt>string|null `k_pay_account`</dt> <dd> ID of payment account. <i>Primary key in {@link \RsPayAccountSql} table.</i> <tt>null</tt> if this is a user account based on system payment method. </dd> <dt>string|null `k_pay_method`</dt> <dd> ID of custom payment method. <tt>null</tt> if this is a user account based on system payment method. <i>Primary key in {@link \Thoth\WlPay\Method\MethodSql}.</i> </dd> <dt>string `m_rest`</dt> <dd>Account balance.</dd> <dt>string|null `s_method`</dt> <dd>Name of a custom payment method. <tt>null</tt> if this is a user account based on system payment method.</dd> </dl>'
                      a_account_nx:
                        type: array
                        description: 'A list of accounts that have not been created for this user yet. Keys are composed of `{k_pay_method}:{k_currency}`. Each element: <dl> <dt>bool `can_negative`</dt> <dd>`true` if the account is allowed to have a negative balance, `false` otherwise.</dd> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_currency`</dt> <dd>Currency key. <i>Primary key in the {@link \RsCurrencySql} table.</i></dd> <dt>string|null `k_pay_account`</dt> <dd>Payment account key. `null` for accounts not yet created.</dd> <dt>string|null `k_pay_method`</dt> <dd> Custom payment method key. `null` for accounts based on the system payment method. <i>Primary key in the {@link \Thoth\WlPay\Method\MethodSql} table.</i> </dd> <dt>string `m_rest`</dt> <dd>Account balance. Always `''0.00''` for accounts not yet created.</dd> <dt>string|null `s_method`</dt> <dd>Name of the custom payment method. `null` for accounts based on the system payment method.</dd> </dl>'
                      is_debtor:
                        type: boolean
                        description: 'Determines whether the user is a debtor. If `true` - the owner of this account is a debtor. If `false` - the user is not a debtor or the information is not returned for the account owner ({@link AccountApi::$is_owner} is `false`).'
      tags:
        - Account-Account
  '/Thoth/WlPay/Address/Address.json':
    get:
      summary: 'Gets user''s payment addresses information. Returns the list of saved payment addresses for the specified owner type and key, including address fields, country and region details, and the currently selected address. Returns `null` if the current user cannot edit bank accounts for the owner.'
      parameters:
        - name: id_pay_owner
          in: query
          description: 'The ID of the payment owner type. One of {@link \RsPayOwnerSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key, where the payment is performed. <tt>null</tt> if pay owner is not user.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The primary key of a payment owner. This could be the business of the user depending on a {@link \Thoth\WlPay\Address\AddressApi::$id_pay_owner} value.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_address:
                        type: [array, "null"]
                        description: 'The payee''s address information. A list of payment addresses belonging to the owner. Each element: `null` if the current user has no access to the required information, or cannot edit the bank account. <dl> <dt>bool `is_selected`</dt> <dd>`true` if this address is currently selected, `false` otherwise.</dd> <dt>string|null `k_country`</dt> <dd>Country geo key. `null` if the country cannot be determined.</dd> <dt>string `k_pay_address`</dt> <dd>Payment address key. Primary key in the `rs_pay_address` table.</dd> <dt>string `k_region`</dt> <dd>Region geo key.</dd> <dt>string `s_city`</dt> <dd>City name.</dd> <dt>string|null `s_country`</dt> <dd>Country name. `null` if the country cannot be determined.</dd> <dt>string `s_country_code`</dt> <dd>Country abbreviation code, or an empty string if unavailable.</dd> <dt>string `s_name`</dt> <dd>Full name of the address owner.</dd> <dt>string `s_phone`</dt> <dd>Phone number.</dd> <dt>string `s_postal`</dt> <dd>Postal code.</dd> <dt>string|null `s_region`</dt> <dd>Region name.</dd> <dt>string|null `s_region_code`</dt> <dd>Region abbreviation code. `null` if the region cannot be determined.</dd> <dt>string `s_street1`</dt> <dd>First line of the street address.</dd> <dt>string `s_street2`</dt> <dd>Second line of the street address.</dd> <dt>string|null `text_mail`</dt> <dd>Email address of the owner. `null` if the owner is a business.</dd> </dl>'
      tags:
        - Address-Address
  '/Thoth/WlPay/Address/Profile.json':
    get:
      summary: 'Returns default payment address data that is retrieved from user profile. Loads the user''s profile for the given business and returns address fields (name, phone, street, city, postal code, country, region) to pre-populate a payment address form.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose data should be retrieved.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_geo_country:
                        type: [string, "null"]
                        description: 'The country key. This will be `null` if not set yet.'
                      k_geo_region:
                        type: [string, "null"]
                        description: 'The region ID. This will be `null` if not set yet.'
                      s_city:
                        type: [string, "null"]
                        description: 'The name of the city in the address. This will be `null` if not set yet.'
                      s_name:
                        type: [string, "null"]
                        description: 'The client name as listed in their address. This will be `null` if not set yet.'
                      s_phone:
                        type: [string, "null"]
                        description: 'The client phone number. This will be `null` if not set yet.'
                      s_postal:
                        type: [string, "null"]
                        description: 'The client postal or zip code. This will be `null` if not set yet.'
                      s_street1:
                        type: [string, "null"]
                        description: 'The first line of the client address. This will be `null` if not set yet.'
                      s_street2:
                        type: [string, "null"]
                        description: 'The second line of the client address. This will be `null` if not set yet.'
      tags:
        - Address-Profile
  '/Thoth/WlPay/Owner/Owner.json':
    get:
      summary: 'Returns information about payment owner. Must be called before initiating any payment on behalf of a user, to determine the correct payment owner keys to pass to the payment form. Also indicates whether a family-account relationship exists (for example, a parent paying for a child), which affects how the payment form is pre-populated.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` if not passed.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_pay_owner:
                        type: number
                        format: int32
                        description: 'The type of user for which transactions can be made (this property is optional). This is one of the {@link \RsPayOwnerSid} constants.'
                      is_pay_self_only:
                        type: boolean
                        description: 'Is client pay only for self. If parent pays for child this flag will be `false` for both. `true` if client pay only for self, `false` - otherwise.'
                      k_pay_owner:
                        type: string
                        description: 'The payment owner key. This is used for financial transactions.'
                      k_pay_owner_money:
                        type: string
                        description: 'Key of the money owner. <i>Copy of result of {@link PayOwner::ownerMoney()}.</i>'
      tags:
        - Owner-Owner
  '/Thoth/WlPay/Refund/Refund.json':
    get:
      summary: 'Returns form data for a page to perform refund operation.'
      parameters:
        - name: is_free
          in: query
          description: '`true` if need to refund a free purchase without any transactions, `false` if purchase has transactions. If it is a free purchase without any transaction, then we will not proceed the money refunds. For example, it may be used to return the inventory that was obtained through the free purchases.'
          schema:
            type: boolean
        - name: is_void
          in: query
          description: '`true` if transaction should be voided, `false` if transaction should be refunded.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business where to process purchase refund. `null` in case when not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_purchase
          in: query
          description: 'Purchase key. `null` in case when not initialized yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_transaction:
                        type: [array, "null"]
                        description: 'List of payments transactions in this purchase, where each item is an array: `null` in case when not initialized yet. <dl> <dt>array `a_pay_refund_method`</dt> <dd> A drop-down list data to select a refund method. See {@link \Thoth\WlPay\Refund\RefundApi::getRefundMethodOptions()}. </dd> <dt>bool `can_refund`</dt> <dd>`true` when payment transaction can be refunded.</dd> <dt>int `id_pay_method`</dt> <dd>Payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_wait`</dt> <dd>`true` when we are waiting when financial institution (e.g. bank) will process our transaction.</dd> <dt>string `f_amount_refunded`</dt> <dd>Amount of returned funds.</dd> <dt>string `f_amount_rest`</dt> <dd>Amount of the transaction after application of all refunds.</dd> <dt>string `k_pay_transaction`</dt> <dd>Transaction key. Primary key in {@link \RsPayTransactionSql}.</dd> <dt>string `text_card`</dt> <dd>Part of account number (Credit Card, bank account).</dd> <dt>string `text_info`</dt> <dd>Additional information attached to the transaction.</dd> <dt>string `text_pay_exception`</dt> <dd>Transaction exception source for a translated message</dd> <dt>string `text_pay_method`</dt> <dd>Payment method title.</dd> <dt>string `text_pay_transaction_status`</dt> <dd>Transaction status title.</dd> <dt>string `text_amount_refunded`</dt> <dd>Formatted amount of returned funds.</dd> <dt>string `text_amount_rest`</dt> <dd>Formatted amount of the transaction after application of all refunds.</dd> <dt>string `text_amount_start`</dt> <dd>Formatted amount of the transaction.</dd> <dt>string `sid_pay_transaction_status`</dt> <dd>Transaction status string identifier.</dd> <dt>string `url_transaction_detail`</dt> <dd>Transaction details page URL.</dd> </dl>'
                      a_purchase_item:
                        type: array
                        description: 'List or purchased items, where each item is an array: <dl> <dt>array `a_tax`</dt> <dd> Array of applied taxes. Each item is an array: <dl> <dt>string `f_pay_tax_refund`</dt> <dd>Tax refund amount.</dd> <dt>string `html_tax`</dt> <dd>Formatted tax amount.</dd> <dt>string `html_tax_refund`</dt> <dd>Formatted tax refund amount.</dd> <dt>string `html_title`</dt> <dd>Tax name.</dd> <dt>string `k_tax`</dt> <dd>Tax key. Primary key in {@link \RsTaxSql} table.</dd> <dt>string `k_purchase_item`</dt> <dd>Purchase item key. Primary key in the {@link \RsPurchaseItemSql} table.</dd> <dt>string `m_tax`</dt> <dd>Amount of tax for all units of a purchase item, after application discount.</dd> </dl> </dd> <dt>bool `can_change_count`</dt> <dd>`true` when quantity is 1 and can not be adjusted for refund.</dd> <dt>string `f_pay_price_refund`</dt> <dd>Purchase item price without taxes to refund.</dd> <dt>string `html_date_suspend_pay`</dt> <dd>Deferred payment date or <tt>null</tt> if the payment task has already been completed or never existed.</dd> <dt>string `html_price`</dt> <dd>Formatted purchase item price without taxes.</dd> <dt>string `html_price_refund`</dt> <dd>Formatted purchase item price without taxes to refund.</dd> <dt>string `html_suspend_pay`</dt> <dd>Deferred payment amount or <tt>null</tt> if the payment task has already been completed or never existed.</dd> <dt>string `html_title`</dt> <dd>Purchased item title.</dd> <dt>string `html_total`</dt> <dd>Purchased item total amount.</dd> <dt>int `i_count`</dt> <dd>Number of items initially bought.</dd> <dt>int `i_buy`</dt> <dd>Number of items that is not cancelled yet.</dd> <dt>int `id_purchase_item`</dt> <dd>ID of the purchased item. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_refund_full`</dt> <dd>`true` when a purchase item is completely refunded.</dd> <dt>bool `is_refund_partial`</dt> <dd>`true` when a purchase item is partially refunded.</dd> <dt>string `k_purchase_item`</dt> <dd>Purchase item key. Primary key in the {@link \RsPurchaseItemSql} table. `0` if the item is tips.</dd> <dt>string `m_price`</dt> <dd>Purchase item price without taxes.</dd> <dt>bool `show_deactivate`</dt> <dd>`true` when PO can be deactivated.</dd> <dt>bool `show_reflect_schedule`</dt> <dd>`true` when it can be reflected in payment schedule</dd> <dt>string `url_edit`</dt> <dd>URL to page when purchase item can be edited.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'List of applied taxes, where each element is an array: <dl> <dt>string `html_title`</dt> <dd>Tax name.</dd> <dt>string `k_tax`</dt> <dd>Tax key. Primary key in {@link \RsTaxSql} table.</dd> </dl>'
                      can_change_count:
                        type: boolean
                        description: '`true` if there are any purchase item where is possible to refund specific quantity.'
                      can_refund:
                        type: boolean
                        description: 'Whether transaction in a specified status can be refunded.'
                      has_installment:
                        type: boolean
                        description: 'Whether transaction has an installment payment.'
                      has_surcharge:
                        type: boolean
                        description: 'Whether transaction has a surcharge applied.'
                      html_pay_owner_money:
                        type: [string, "null"]
                        description: 'Payment owner name for this purchase. `null` if payment owner and purchase owner are the same or in case when not initialized yet.'
                      html_pay_owner_purchase:
                        type: [string, "null"]
                        description: 'Purchase owner name. `null` in case when not initialized yet.'
                      html_purchase_total:
                        type: [string, "null"]
                        description: 'Total purchase amount. `null` in case when not initialized yet.'
                      html_surcharge_total:
                        type: [string, "null"]
                        description: 'Total surcharge amount. `null` in case when not initialized yet.'
                      i_quantity_total:
                        type: number
                        format: int32
                        description: 'Total purchase quantity.'
                      is_allowed_refund:
                        type: boolean
                        description: 'Whether refund is allowed for this transaction.'
                      is_mail_refund:
                        type: boolean
                        description: '''true'' if enable mail `Purchase Refunded`, otherwise `false`.'
                      k_currency:
                        type: [string, "null"]
                        description: 'Currency key of purchase. `null` in case when not initialized yet.'
                      show_action_column:
                        type: boolean
                        description: 'Show action column.'
                      show_transaction_details:
                        type: boolean
                        description: 'Defines whether to show table column with links to transaction details.'
                      text_currency:
                        type: [string, "null"]
                        description: 'Currency sing of purchase. `null` in case when not initialized yet.'
                      text_note:
                        type: [string, "null"]
                        description: 'Purchase comments. `null` in case when not initialized yet.'
                      url_purchase:
                        type: [string, "null"]
                        description: 'URL to purchase edit page. `null` in case when not initialized yet.'
      tags:
        - Refund-Refund
    post:
      summary: 'Processes refund operation.'
      parameters:
        - name: is_free
          in: query
          description: '`true` if need to refund a free purchase without any transactions, `false` if purchase has transactions. If it is a free purchase without any transaction, then we will not proceed the money refunds. For example, it may be used to return the inventory that was obtained through the free purchases.'
          schema:
            type: boolean
        - name: is_mail_refund_send
          in: query
          description: '''true'' if need send `Purchase Refunded` mail, otherwise `false`.'
          schema:
            type: boolean
        - name: is_void
          in: query
          description: '`true` if transaction should be voided, `false` if transaction should be refunded.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business where to process purchase refund. `null` in case when not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_purchase
          in: query
          description: 'Purchase key. `null` in case when not initialized yet.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pay_refund_method:
                  type: [array, "null"]
                  description: 'Key is the transaction key. Primary key from {@link \RsPayTransactionProcessSql} table. Value is one of the {@link \RsPayRefundMethodSid} constant. See {@link \Thoth\WlPay\Refund\PayRefund::save()} for documentation. `null` in case when not initialized yet.'
                a_refund:
                  type: [array, "null"]
                  description: 'Key is the transaction key. Primary key from {@link \RsPayTransactionProcessSql} table. Value is an amount to refund. See {@link \Thoth\WlPay\Refund\PayRefund::save()} for documentation. `null` in case when not initialized yet.'
                a_refund_item:
                  type: [array, "null"]
                  description: 'Purchase items data to refund. Key is the purchase item key. Primary key from {@link \RsPurchaseItemSql} table. Note! Key "0" corresponds to the refund of the tips. Key "-1" corresponds to the refund of the surcharges. Key "-2" corresponds to the refund of the penalty fee. Key "-3" corresponds to the refund of the credit card surcharges. Key "-4" corresponds to the refund of the ach surcharges. Value of each element has next structure: See {@link \Thoth\WlPay\Refund\PayRefund::save()} for documentation. `null` in case when not initialized yet. <dl> <dt>string `m_price`</dt> <dd>Refund amount.</dd> <dt>array `a_tax`</dt> <dd> Refunds for each tax of item. Key is tax key. Primary key from {@link \RsTaxSql} table. Value is a tax refund amount. </dd> </dl>'
                text_note:
                  type: [string, "null"]
                  description: 'Purchase comments. `null` in case when not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Refund-Refund
  '/Thoth/DriveMs/Api/DriveCopy.json':
    get:
      summary: 'Copies a file from the source link to the destination link. Accepts the source and destination drive links and optional copy parameters, validates their lengths, and copies the physical file referenced by the source link to the destination link in the drive storage.'
      parameters:
        - name: a_param
          in: query
          description: 'Additional parameters for the copy operation. <dl> <dt>int `i_height_max`</dt> <dd> Checks if image''s height is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_height_min`</dt> <dd> Checks if image''s height is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_max`</dt> <dd> Checks if image''s width is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_min`</dt> <dd> Checks if image''s width is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>bool `is_load_image_info`</dt> <dd>Whether to load information about the image. Optional.</dd> <dt>bool `is_private`</dt> <dd>Whether the file is private. Optional.</dd> <dt>bool `require-image`</dt> <dd>Whether to throw an exception if the uploaded file is not an image. Optional.</dd> <dt>int `timeout`</dt> <dd>Remove the file after the specified number of seconds. `0` for persistent files. Optional.</dd> </dl>'
          schema:
            type: array
        - name: s_link_destination
          in: query
          description: 'Link to the destination file.'
          schema:
            type: string
        - name: s_link_source
          in: query
          description: 'Link to the source file.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveCopy
  '/Thoth/DriveMs/Api/DriveDelete.json':
    get:
      summary: 'Deletes the file identified by the given link. Accepts a drive link, validates its length, and removes the corresponding file record from the drive database, making the file unavailable for download.'
      parameters:
        - name: s_link
          in: query
          description: 'Link to the file.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveDelete
  '/Thoth/DriveMs/Api/DriveDownload.json':
    get:
      summary: 'Downloads the contents of the file identified by the given link. Accepts a drive link, retrieves the raw file content from storage, and returns it as a base64-encoded gzip-compressed string. Returns `false` if the file is not found.'
      parameters:
        - name: s_link
          in: query
          description: 'Link to the file.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_content:
                        type: [string, boolean]
                        description: 'The contents of a specified file. Encoded in base64 and compressed with gzip. `false` if a file is not found, otherwise - content file.'
      tags:
        - Api-DriveDownload
  '/Thoth/DriveMs/Api/DriveFile.json':
    post:
      summary: 'Returns file information for each of the given links. Batch lookup for file metadata by drive link. Used when a page or component needs to display details (such as name, dimensions, or type) for several files at once. The JSON input variant exists to avoid hitting POST body size limits when the list is large.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_link:
                  type: array
                  description: 'List of links to the files. It is preferable to use {@link DriveFileApi::$json_list} parameter instead of this.'
                is_cache:
                  type: boolean
                  description: 'Whether use cache. `true` to use cache, `false` otherwise.'
                json_list:
                  type: string
                  description: 'Link list to the file as JSON string. This parameter allows passing a large number of links to the controller without exceeding the maximum POST request size. The links should be provided as a JSON-encoded array of strings. It is preferable to use this parameter instead of {@link DriveFileApi::$a_link}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_file:
                        type: array
                        description: 'List of information about files. <dl> <dt>int `i_height`</dt> <dd>Height of image. Returns if file is image.</dd> <dt>int `i_width`</dt> <dd>Width of image. Returns if file is image.</dd> <dt>int|null `id_type`</dt> <dd>Image type ID. One of {@link \Core\Drive\DriveTypeSid} constants. `null` if file not image.</dd> <dt>bool `is_private`</dt> <dd>Whether link is private.</dd> <dt>bool `is_private_file`</dt> <dd>Whether file is private.</dd> <dt>string `s_link`</dt> <dd>File link.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
      tags:
        - Api-DriveFile
  '/Thoth/DriveMs/Api/DriveGenericInfo.json':
    post:
      summary: 'Returns generic metadata for each of the given file links. Low-level metadata fetch used by the drive client library before performing operations that depend on file attributes such as privacy flag, dimensions, or SHA1 hash. Results are returned as serialized rows to keep the payload compact. Use {@link DriveFileApi} for structured file info instead.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_link:
                  type: array
                  description: 'Link list to the file. This parameter was originally added, but it turned out that a large number of links could be sent to this controller, which led to exceeding the maximum POST request size. Therefore, the {@link DriveGenericInfoApi::$json_list} parameter was introduced, which allows passing the list of links in JSON format. The current parameter is kept for backward compatibility. The client library that sends requests to this controller has been updated and now uses only {@link DriveGenericInfoApi::$json_list}.'
                json_list:
                  type: string
                  description: 'Link list to the file as JSON string. This parameter allows passing a large number of links to the controller without exceeding the maximum POST request size. The links should be provided as a JSON-encoded array of strings. It is preferable to use this parameter instead of {@link DriveGenericInfoApi::$a_link}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: array
                        description: 'Information about files. Every element of array is string in base64.'
      tags:
        - Api-DriveGenericInfo
  '/Thoth/DriveMs/Api/DriveRegisterVariants.json':
    get:
      summary: 'Registers file variants for the given link (deprecated GET compatibility endpoint). Reads variant and link values from the raw GET parameters and delegates to the POST method. This endpoint exists only for backward compatibility; use {@link DriveRegisterVariantsApi::post()} instead. It is not possible to test it normally because model does not support sending necessary data via get method.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveRegisterVariants
    post:
      summary: 'Registers file variants for the given link. Accepts a drive link and a list of variant file paths keyed by descriptor (either as an array or as a JSON-encoded string), and associates the given variants with the specified link in the drive storage.'
      parameters:
        - name: s_link
          in: query
          description: 'Link to the file.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_variant:
                  type: array
                  description: 'List of variant file paths with key as descriptor.'
                json_variant:
                  type: string
                  description: 'List of variant file paths with key as descriptor in JSON format. This parameter allows passing a large number of files to the controller without exceeding the maximum POST request variable limit. The files should be provided as a JSON-encoded array of strings. It is preferable to use this parameter instead of {@link DriveRegisterVariantsApi::$a_variant}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveRegisterVariants
  '/Thoth/DriveMs/Api/DriveSave.json':
    post:
      summary: 'Saves the uploaded file content to the specified link. Bridges the legacy {@link DriveSaveApi::$s_content} field into the fields expected by the parent implementation: writes the raw content to a temporary file and sets {@link DriveSave74Api::$a_file_content} for regular uploads, or copies the value directly into {@link DriveSave74Api::$s_file_key} for multipart uploads.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_file_content:
                  type: [object, "null"]
                  description: 'The uploaded file data from the HTTP request. `null` if a multipart S3 upload is used instead.'
                a_param:
                  type: array
                  description: 'A set of parameters. If a file is uploaded using an S3 bucket, the array must contain the key `is_multipart`. <dl> <dt>int `i_height_max`</dt> <dd> Checks if image''s height is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_height_min`</dt> <dd> Checks if image''s height is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_max`</dt> <dd> Checks if image''s width is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_min`</dt> <dd> Checks if image''s width is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>bool `is_load_image_info`</dt> <dd>Whether to load information about the image. Optional.</dd> <dt>bool `is_private`</dt> <dd>Whether the file is private. Optional.</dd> <dt>bool `require-image`</dt> <dd>Whether to throw an exception if the uploaded file is not an image. Optional.</dd> <dt>int `timeout`</dt> <dd>Remove the file after the specified number of seconds. `0` for persistent files. Optional.</dd> </dl>'
                s_content:
                  type: string
                  description: 'The contents of a specified file if a regular file is saved. Or this is the key of the object in S3, if the key `is_multipart` exists in the array of parameters {@link DriveSaveApi::$a_param}. or {@link DriveSave74Api::$s_file_key} for multipart uploads instead.'
                s_file_key:
                  type: [string, "null"]
                  description: 'Key of the object in S3. Used when `is_multipart` is set in {@link DriveSave74Api::$a_param}. `null` if not a multipart upload.'
                s_link:
                  type: string
                  description: 'Link to the file.'
                s_name:
                  type: [string, "null"]
                  description: 'File name with extension. `null` if a file name is not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_file:
                        type: array
                        description: 'Information about saved a file. <dl> <dt>bool `is_new`</dt> <dd>`true` if a new file was saved, otherwise `false`.</dd> <dt>bool `is_private_file`</dt> <dd>`true` if the file is private, otherwise `false`.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
      tags:
        - Api-DriveSave
  '/Thoth/DriveMs/Api/DriveUrlSign.json':
    get:
      summary: 'Returns a pre-signed URL to download the specified private file. Accepts file metadata (including the file name and privacy flag) and an optional download name, and generates a time-limited signed URL that grants temporary access to a private file in the drive storage.'
      parameters:
        - name: a_file
          in: query
          description: 'File data. <dl> <dt>int `i_height`</dt> <dd>Height of the image. Only present if the file is an image.</dd> <dt>int `i_width`</dt> <dd>Width of the image. Only present if the file is an image.</dd> <dt>int `id_type`</dt> <dd>Image type code. Only present if the file is an image. One of {@link \Core\Drive\DriveTypeSid} constants.</dd> <dt>bool `is_private`</dt> <dd>Whether the link is private.</dd> <dt>bool `is_private_file`</dt> <dd>Whether the file is private.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
          schema:
            type: array
        - name: s_name
          in: query
          description: 'Name of the file to set on download. `null` if file name not specified.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url:
                        type: string
                        description: 'A pre-signed URL to download a file with restricted access rights.'
      tags:
        - Api-DriveUrlSign
  '/Thoth/DriveMs/Api/DriveCopyInstance.json':
    post:
      summary: 'Copies all files from the source instance to the destination instance. Used during server provisioning or data migration to replicate the entire drive file set from one instance to another, for example when cloning a staging environment or moving a tenant to a new server. All file records belonging to the source instance are duplicated under the destination.'
      parameters:
        - name: s_instance_destination
          in: query
          description: 'Instance ID from which links must be copied.'
          schema:
            type: string
        - name: s_instance_source
          in: query
          description: 'Instance ID from which links must be copied.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-DriveCopyInstance
  '/Thoth/DriveMs/Api/DriveSave74.json':
    post:
      summary: 'Saves the uploaded file content to the specified link. Accepts the raw file content (or an S3 multipart key when `is_multipart` is set in {@link DriveSaveApi::$a_param}), a drive link, and an optional file name. Writes the content to the drive storage and returns the saved file metadata.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_file_content:
                  type: [object, "null"]
                  description: 'The uploaded file data from the HTTP request. `null` if a multipart S3 upload is used instead.'
                a_param:
                  type: array
                  description: 'A set of parameters. If a file is uploaded using an S3 bucket, the array must contain the key `is_multipart`. <dl> <dt>int `i_height_max`</dt> <dd> Checks if image''s height is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_height_min`</dt> <dd> Checks if image''s height is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_max`</dt> <dd> Checks if image''s width is not greater than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>int `i_width_min`</dt> <dd> Checks if image''s width is not less than this value. Verified only in conjunction with `require-image`. Optional. </dd> <dt>bool `is_load_image_info`</dt> <dd>Whether to load information about the image. Optional.</dd> <dt>bool `is_private`</dt> <dd>Whether the file is private. Optional.</dd> <dt>bool `require-image`</dt> <dd>Whether to throw an exception if the uploaded file is not an image. Optional.</dd> <dt>int `timeout`</dt> <dd>Remove the file after the specified number of seconds. `0` for persistent files. Optional.</dd> </dl>'
                s_file_key:
                  type: [string, "null"]
                  description: 'Key of the object in S3. Used when `is_multipart` is set in {@link DriveSave74Api::$a_param}. `null` if not a multipart upload.'
                s_link:
                  type: string
                  description: 'Link to the file.'
                s_name:
                  type: [string, "null"]
                  description: 'File name with extension. `null` if a file name is not specified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_file:
                        type: array
                        description: 'Information about saved a file. <dl> <dt>bool `is_new`</dt> <dd>`true` if a new file was saved, otherwise `false`.</dd> <dt>bool `is_private_file`</dt> <dd>`true` if the file is private, otherwise `false`.</dd> <dt>string `s_name`</dt> <dd>The name of the physical file on the drive.</dd> </dl>'
      tags:
        - Api-DriveSave74
  '/Studio/Repository/Environment/EnvironmentElement.json':
    delete:
      summary: ''
      parameters:
        - name: k_environment
          in: query
          description: 'Environment key. Primary key in {@link \Studio\Repository\Environment\EnvironmentSql} table. <tt>null</tt> if a new studio environment should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Environment-EnvironmentElement
    get:
      summary: ''
      parameters:
        - name: k_environment
          in: query
          description: 'Environment key. Primary key in {@link \Studio\Repository\Environment\EnvironmentSql} table. <tt>null</tt> if a new studio environment should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_sprint_state:
                        type: array
                        description: 'A list of sprint conditions relative to the selected environment.'
                      text_path:
                        type: string
                        description: 'A list of allow/disallow environments.'
                      text_task_type:
                        type: string
                        description: 'A list of jira type task. The type separator is a comma.'
                      text_title:
                        type: string
                        description: 'Name of the environment.'
      tags:
        - Environment-EnvironmentElement
    post:
      summary: 'The method for create/update studio environment element.'
      parameters:
        - name: k_environment
          in: query
          description: 'Environment key. Primary key in {@link \Studio\Repository\Environment\EnvironmentSql} table. <tt>null</tt> if a new studio environment should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_sprint_state:
                  type: array
                  description: 'A list of sprint conditions relative to the selected environment.'
                text_path:
                  type: string
                  description: 'A list of allow/disallow environments.'
                text_task_type:
                  type: string
                  description: 'A list of jira type task. The type separator is a comma.'
                text_title:
                  type: string
                  description: 'Name of the environment.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Environment-EnvironmentElement
  '/Studio/Repository/Environment/EnvironmentList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of studio environments.'
      tags:
        - Environment-EnvironmentList
  '/Studio/Repository/Access/AccessPersonnel.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                has_access:
                  type: boolean
                  description: 'Whether given personnel has access to give rule.'
                k_access_rule:
                  type: string
                  description: 'Repository access rule key.'
                k_personnel:
                  type: string
                  description: 'Studio personnel key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Access-AccessPersonnel
  '/Studio/Microservice/Editor/List.json':
    get:
      summary: 'Gets a list of microservices. Returns microservices accessible to the current user based on project roles and privileges.'
      parameters:
        - name: id_remove
          in: query
          description: 'Specifies whether to show removed and/or not removed microservices. One of {@link \AFlagSid} constants. * {@link \AFlagSid::ON} to show only not removed microservices. * {@link \AFlagSid::OFF} to show only removed microservices. * {@link \AFlagSid::ALL} to show all microservices (removed and not removed).'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: object
                        description: 'A list of microservices. One element contains: <dl> <dt>bool `is_remove`</dt> <dd>Whether the microservice has been deleted. `true` is the microservice is removed, `false` otherwise.</dd> <dt>string `json_path`</dt> <dd> A list of repositories and paths of this microservice. <i>Copy of {@link \Studio\Microservice\MicroserviceSql}.<var>json_path</var> field.</i> </dd> <dt>string `k_project`</dt> <dd>Project key. <i>Primary key in {@link \StudioProjectSql} table.</i></dd> <dt>string `s_file`</dt> <dd>File name.</dd> <dt>string `s_group`</dt> <dd> The group to which the microservice belongs. <i>See details in the `s_group` field of {@link \Studio\Microservice\MicroserviceSql} table.</i> </dd> <dt>string `s_variable`</dt> <dd> Microservice-specific variables. A set of pairs key=value, separated by newlines. <i>The list of variables is described in class {@link \Studio\Sprint\Promoting\PromotingConfigValue}.</i> </dd> <dt>string `text_project`</dt> <dd>Title of the project.</dd> <dt>string `text_title`</dt> <dd>Title of the microservice.</dd> <dt>string `url_microservice_promoting`</dt> <dd>URL of the promoting page for this microservice.</dd> </dl>'
      tags:
        - Editor-List
  '/Studio/Microservice/Editor/Edit.json':
    delete:
      summary: 'Deletes a microservice.'
      parameters:
        - name: k_microservice
          in: query
          description: 'If a microservice is edited, its key is stored here. Primary key in {@link \Studio\Microservice\MicroserviceSql}. `null` if a new microservice should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Editor-Edit
    get:
      summary: 'Loads information about a microservice.'
      parameters:
        - name: k_microservice
          in: query
          description: 'If a microservice is edited, its key is stored here. Primary key in {@link \Studio\Microservice\MicroserviceSql}. `null` if a new microservice should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_project_role:
                        type: array
                        description: 'Project role keys. Primary key in {@link \StudioProjectRoleSql} table.'
                      is_remove:
                        type: boolean
                        description: 'Whether the microservice has been deleted. `true` is the microservice is removed, `false` otherwise.'
                      json_path:
                        type: string
                        description: 'A list of repositories and paths of this microservice.'
                      k_project:
                        type: string
                        description: 'Project key. Primary key in {@link \StudioProjectSql} table.'
                      s_file:
                        type: string
                        description: 'File name.'
                      s_group:
                        type: string
                        description: 'The group to which the microservice belongs. See details in the <var>s_group</var> field of {@link \Studio\Microservice\MicroserviceSql} table.'
                      s_variable:
                        type: string
                        description: 'Microservice-specific variables.'
                      text_title:
                        type: string
                        description: 'Name of the microservice.'
      tags:
        - Editor-Edit
    post:
      summary: 'Saves a microservice (create or update).'
      parameters:
        - name: k_microservice
          in: query
          description: 'If a microservice is edited, its key is stored here. Primary key in {@link \Studio\Microservice\MicroserviceSql}. `null` if a new microservice should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_project_role:
                  type: array
                  description: 'Project role keys. Primary key in {@link \StudioProjectRoleSql} table.'
                is_remove:
                  type: boolean
                  description: 'Whether the microservice has been deleted. `true` is the microservice is removed, `false` otherwise.'
                json_path:
                  type: string
                  description: 'A list of repositories and paths of this microservice.'
                k_project:
                  type: string
                  description: 'Project key. Primary key in {@link \StudioProjectSql} table.'
                s_file:
                  type: string
                  description: 'File name.'
                s_group:
                  type: string
                  description: 'The group to which the microservice belongs. See details in the <var>s_group</var> field of {@link \Studio\Microservice\MicroserviceSql} table.'
                s_variable:
                  type: string
                  description: 'Microservice-specific variables.'
                text_title:
                  type: string
                  description: 'Name of the microservice.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_microservice:
                        type: [string, "null"]
                        description: 'If a microservice is edited, its key is stored here. Primary key in {@link \Studio\Microservice\MicroserviceSql}. `null` if a new microservice should be created. This value is required for create and delete operations.'
      tags:
        - Editor-Edit
  '/Studio/Microservice/Service/InstanceActiveCheck.json':
    post:
      summary: 'Checks activity status of microservice instances. Queries DynamoDB for each provided instance ID and returns active, test, and existence flags for each instance.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_instance_check:
                  type: array
                  description: 'Instance ID list to check.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_instance:
                        type: array
                        description: 'List of instance IDs. Key is instance ID, value is an array with the following fields: <dl> <dt>bool `is_active`</dt> <dd> Whether the instance is active. `true` - the instance is active. `false` - the instance is not active. `null` - unknown, error occurred while checking instance activity. </dd> <dt>bool `is_error`</dt> <dd> Whether there was an error while checking the instance. `true` - an error occurred while checking the instance. `false` - the instance was checked successfully. </dd> <dt>bool `is_exists`</dt> <dd>Whether information about the instance was found. `true` - found, `false` - not found, `null` - unknown.</dd> <dt>bool `is_test`</dt> <dd>Whether the instance is a test instance. `true` - test, `false` - not a test, `null` - unknown.</dd> </dl>'
      tags:
        - Service-InstanceActiveCheck
  '/Studio/Tests/WebEmulator/WebEmulatorConfig.json':
    post:
      summary: 'Saves the number of active and passive browsers.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_count_active:
                  type: number
                  format: int32
                  description: 'Maximum number of active browsers on the server.'
                i_count_passive:
                  type: number
                  format: int32
                  description: 'Maximum number of active browsers on the server.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - WebEmulator-WebEmulatorConfig
  '/Studio/Task/Branch/BranchList.json':
    get:
      summary: 'Returns a list of branches for accessible projects. Lists all active branches in projects where the current user has the branch view privilege.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'List of branch. One element contains: <dl> <dt>string[] `a_path`</dt> <dd> Information about paths into which catalogs are cloned/copied. <i>The structure corresponds to the {@link \Studio\Task\Branch\BranchSql}.<var>z_path</var> field.</i> </dd> <dt>string `dtu_create`</dt> <dd>Date when branch was created.</dd> <dt>string `k_deploy_instance`</dt> <dd>Key of deployment instance. <i>Primary key in {@link \StudioDeployInstanceSql} table.</i></dd> <dt>string `k_task`</dt> <dd>Task key. <i>Primary key in {@link \StudioTaskSql} table.</i></dd> <dt>string `text_create`</dt> <dd>Formatted time interval when branch was created.</dd> <dt>string `text_fio`</dt> <dd>Task author name.</dd> <dt>string `text_title`</dt> <dd>Title of the task.</dd> <dt>string `url_task`</dt> <dd>URL of the task.</dd> </dl>'
      tags:
        - Branch-BranchList
  '/Studio/Task/Branch/Branch.json':
    delete:
      summary: 'Changes a status of the branch for the specified task in the database and runs build for deleting a branch. Sets branch status to deleting and starts the `BranchDelete` build on the deployment instance. Returns the build log URL on success.'
      parameters:
        - name: id_build_mode
          in: query
          description: 'Mode of the <tt>BranchDelete</tt> build.'
          schema:
            type: number
            format: int32
        - name: k_deploy_instance
          in: query
          description: 'Deployment instance key which creates/deletes a branch for the specified task.'
          schema:
            type: string
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_build:
                        type: string
                        description: 'Url to build page. Empty string means that build is not started.'
      tags:
        - Branch-Branch
    get:
      summary: 'Returns branch status and data for a task. Retrieves `id_branch_status`, `id_stand_status`, and additional branch data for the specified task.'
      parameters:
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_branch_data:
                        type: [array, string, "null"]
                        description: 'Addition information about branch and related entities. Has an arbitrary structure. `string` if it needs to set an empty value. `null` will not change.'
                      id_branch_status:
                        type: [number, "null"]
                        description: 'Branch status, one of {@link BranchStatusSid}.'
                      id_stand_status:
                        type: [number, "null"]
                        description: 'Stand status, one of {@link StandStatusSid}. `null` if not initialized and will be ignored.'
                      k_task:
                        type: string
                        description: 'Task key within which a branch should be created/deleted.'
      tags:
        - Branch-Branch
    post:
      summary: 'Registers a new branch for the specified task in the database and runs a build for creating a branch. Validates the task, deployment instance, and microservice, then creates a branch record and starts the `BranchCreate` build. Returns the build log URL on success.'
      parameters:
        - name: k_deploy_instance
          in: query
          description: 'Deployment instance key which creates/deletes a branch for the specified task.'
          schema:
            type: string
        - name: k_microservice
          in: query
          description: 'A microservice key.'
          schema:
            type: string
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
        - name: s_environment
          in: query
          description: 'A microservice environment for create branch.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_branch_data:
                  type: [array, string, "null"]
                  description: 'Addition information about branch and related entities. Has an arbitrary structure. `string` if it needs to set an empty value. `null` will not change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_build:
                        type: string
                        description: 'Url to build page. Empty string means that build is not started.'
      tags:
        - Branch-Branch
    put:
      summary: 'Updates branch data for a task. Saves additional branch data (`a_branch_data`) for the branch of the specified task.'
      parameters:
        - name: k_task
          in: query
          description: 'Task key within which a branch should be created/deleted.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_branch_data:
                  type: [array, string, "null"]
                  description: 'Addition information about branch and related entities. Has an arbitrary structure. `string` if it needs to set an empty value. `null` will not change.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-Branch
  '/Studio/Task/Branch/BranchBuild.json':
    delete:
      summary: 'Deletes a branch from the database. <tt>BranchDelete</tt> build calls at the end of the executing.'
      parameters:
        - name: s_task_link
          in: query
          description: 'Task link of the branch. <i>Copy of the {@link \StudioTaskSql}.<var>s_link</var> field.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchBuild
    post:
      summary: 'Sets build that creates a branch and information about paths into which catalogs are cloned. <tt>BranchCreate</tt> build calls at the start of the executing.'
      parameters:
        - name: a_path
          in: query
          description: 'Information about paths into which catalogs are cloned. Keys are aliases of the original repository catalogs. Values are paths into which original catalogs are cloned. Each element has the following structure: Set this property when a new branch is created. In other cases, an exception will be thrown. <dl> <dt>int `id_repository_server_type`</dt> <dd>Repository service type ID. One of {@link \StudioRepositoryServerTypeSid} constants.</dd> <dt>string `s_destination`</dt> <dd>Path into which original catalog is cloned/copied.</dd> <dt>string `s_repository`</dt> <dd>Repository name.</dd> <dt>string `s_source`</dt> <dd>Path of the original catalog.</dd> </dl>'
          schema:
            type: array
        - name: k_build
          in: query
          description: 'Build key that creates a branch for the specified task.'
          schema:
            type: string
        - name: s_task_link
          in: query
          description: 'Task link of the branch. <i>Copy of the {@link \StudioTaskSql}.<var>s_link</var> field.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchBuild
    put:
      summary: 'Changes branch status as {@link BranchStatusSid::READY}. <tt>BranchCreate</tt>> build calls at the end of the executing.'
      parameters:
        - name: k_build
          in: query
          description: 'Build key that creates a branch for the specified task.'
          schema:
            type: string
        - name: s_task_link
          in: query
          description: 'Task link of the branch. <i>Copy of the {@link \StudioTaskSql}.<var>s_link</var> field.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchBuild
  '/Studio/Task/Jira/Sync.json':
    post:
      summary: ''
      parameters:
        - name: text_jira_key
          in: query
          description: ''
          schema:
            type: [string, "null"]
        - name: text_jira_key_destination
          in: query
          description: 'Key of the destination jira task. * Specified when link changed. * `null` if task changed.'
          schema:
            type: [string, "null"]
        - name: text_jira_key_source
          in: query
          description: 'Key of the source jira task. Specified when link changed. `null` if task changed.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Jira-Sync
  '/Studio/Task/Jira/Link.json':
    post:
      summary: ''
      parameters:
        - name: k_studio_task
          in: query
          description: 'Key of the studio task. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_studio_task_jira:
                  type: [string, "null"]
                  description: 'Key of the jira task connection. Primary key from {@link \Studio\Task\Jira\JiraSql}. <tt>null</tt> if adding new link.'
                text_jira_key:
                  type: [string, "null"]
                  description: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_status_list:
                        type: array
                        description: 'List of statuses current jira task can be transferred to.'
                      k_studio_task_jira:
                        type: [string, "null"]
                        description: 'Key of the jira task connection. Primary key from {@link \Studio\Task\Jira\JiraSql}. <tt>null</tt> if adding new link.'
                      text_jira_key:
                        type: [string, "null"]
                        description: ''
                      text_jira_status:
                        type: string
                        description: 'Status of the task on jira.'
                      text_jira_title:
                        type: string
                        description: 'Title of the task on jira.'
                      text_type_name:
                        type: string
                        description: 'Name of the task type on Jira.'
                      url_jira:
                        type: string
                        description: 'Url link to the task on jira.'
                      url_type_icon:
                        type: string
                        description: 'Url to the icon of the task type on jira.'
      tags:
        - Jira-Link
  '/Studio/Task/Jira/Transition.json':
    post:
      summary: ''
      parameters:
        - name: k_studio_task
          in: query
          description: 'Key of the studio task. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_studio_task_jira:
                  type: string
                  description: 'Key of the jira task connection. Primary key from {@link \Studio\Task\Jira\JiraSql}.'
                text_jira_key:
                  type: [string, "null"]
                  description: 'Key of the jira task.'
                text_jira_status_id:
                  type: [string, "null"]
                  description: 'ID of the status to transit jira task to.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Jira-Transition
  '/Studio/Task/Edit/Edit.json':
    post:
      summary: 'Edits studio task.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_environment:
                  type: [string, "null"]
                  description: 'Studio environment key that should be edited. `null` if value should not be changed. zero - reset manually set environment or if an incorrect value was set.'
                k_studio_task:
                  type: string
                  description: 'Studio task key that should be edited.'
                k_task_priority:
                  type: [string, "null"]
                  description: 'Studio task priority key that should be edited. <tt>null</tt> if value should not be changed.'
                k_task_type:
                  type: [string, "null"]
                  description: 'Studio task type key that should be edited. <tt>null</tt> if value should not be changed.'
                text_acceptance:
                  type: [string, "null"]
                  description: 'Acceptance test of the studio task. <tt>null</tt> if value should not be changed.'
                text_description:
                  type: [string, "null"]
                  description: 'Description of the studio task. <tt>null</tt> if value should not be changed.'
                text_title:
                  type: [string, "null"]
                  description: 'Title of the studio task. <tt>null</tt> if value should not be changed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_environment:
                        type: [string, "null"]
                        description: 'Studio environment key that should be edited. `null` if value should not be changed. zero - reset manually set environment or if an incorrect value was set.'
      tags:
        - Edit-Edit
  '/Studio/Task/Edit/RetrieveJira.json':
    get:
      summary: 'Retrieves information about issue from jira.'
      parameters:
        - name: text_jira_key
          in: query
          description: 'Key of the jira task.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_personnel_assignee:
                        type: string
                        description: 'Key of the personnel in studio to assign task to.'
                      k_sprint:
                        type: string
                        description: 'Sprint key.'
                      text_jira_description:
                        type: string
                        description: 'Description of the task on jira.'
                      text_jira_key:
                        type: string
                        description: 'Key of the jira task.'
                      text_jira_title:
                        type: string
                        description: 'Title of the task on jira.'
                      url_jira:
                        type: string
                        description: 'Url link to the task on jira.'
      tags:
        - Edit-RetrieveJira
  '/Studio/Task/Edit/Prolong.json':
    post:
      summary: 'Prolongs studio task development cycle.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_studio_task:
                  type: string
                  description: 'Studio task key'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Prolong
  '/Studio/Task/Review/Review.json':
    delete:
      summary: 'Removes fisheye review from studio.'
      parameters:
        - name: k_task
          in: query
          description: 'Studio task ID. Obligatory field. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
        - name: k_task_review
          in: query
          description: 'Studio task revision key. Primary key from {@link \Studio\Task\Review\Sql}. `null` in a case of adding new revision.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Review-Review
    get:
      summary: 'Gets fisheye review information.'
      parameters:
        - name: k_task
          in: query
          description: 'Studio task ID. Obligatory field. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
        - name: k_task_review
          in: query
          description: 'Studio task revision key. Primary key from {@link \Studio\Task\Review\Sql}. `null` in a case of adding new revision.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reviewer:
                        type: array
                        description: 'List of personnel of the revision which has some activity in review.'
                      can_lock:
                        type: boolean
                        description: 'Whether revision lock status can be changed.'
                      is_lock:
                        type: boolean
                        description: 'Whether revision is locked for future commits.'
                      k_task_review:
                        type: [string, "null"]
                        description: 'Studio task revision key. Primary key from {@link \Studio\Task\Review\Sql}. `null` in a case of adding new revision.'
                      sid_status:
                        type: string
                        description: 'Status of the revision. One of the {@link ReviewStatusSid} string constants.'
                      text_comment:
                        type: string
                        description: 'Studio revision comment.'
                      text_review:
                        type: [string, "null"]
                        description: 'Fisheye revision key.'
                      text_task_title:
                        type: string
                        description: 'Task title to copy to clipboard.'
                      url_review:
                        type: string
                        description: 'Url link to the revision.'
      tags:
        - Review-Review
    post:
      summary: 'Updates fisheye review information.'
      parameters:
        - name: k_task
          in: query
          description: 'Studio task ID. Obligatory field. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
        - name: k_task_review
          in: query
          description: 'Studio task revision key. Primary key from {@link \Studio\Task\Review\Sql}. `null` in a case of adding new revision.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_review:
                  type: [string, "null"]
                  description: 'Fisheye revision key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reviewer:
                        type: array
                        description: 'List of personnel of the revision which has some activity in review.'
                      can_lock:
                        type: boolean
                        description: 'Whether revision lock status can be changed.'
                      is_lock:
                        type: boolean
                        description: 'Whether revision is locked for future commits.'
                      k_task_review:
                        type: [string, "null"]
                        description: 'Studio task revision key. Primary key from {@link \Studio\Task\Review\Sql}. `null` in a case of adding new revision.'
                      sid_status:
                        type: string
                        description: 'Status of the revision. One of the {@link ReviewStatusSid} string constants.'
                      text_comment:
                        type: string
                        description: 'Studio revision comment.'
                      text_review:
                        type: [string, "null"]
                        description: 'Fisheye revision key.'
                      text_task_title:
                        type: string
                        description: 'Task title to copy to clipboard.'
                      url_review:
                        type: string
                        description: 'Url link to the revision.'
      tags:
        - Review-Review
  '/Studio/Task/Review/Unfinished.json':
    get:
      summary: 'Retrieves information about unfinished reviews for reviewers of the task.'
      parameters:
        - name: k_task
          in: query
          description: 'Studio task ID. Primary key from {@link \StudioTaskSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_review:
                        type: array
                        description: 'List of unfinished reviews with information about reviewers.'
      tags:
        - Review-Unfinished
  '/Studio/Task/Backlight/Change.json':
    put:
      summary: 'Changes backlights for task on rapid board.'
      parameters:
        - name: k_task
          in: query
          description: 'Studio task ID. Primary key from {@link \StudioTaskSql}.'
          schema:
            type: string
        - name: k_task_color
          in: query
          description: 'Color ID. Primary key from {@Link \Studio\Task\Color\ColorSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_task_color:
                        type: string
                        description: 'Color ID. Primary key from {@Link \Studio\Task\Color\ColorSql}.'
                      s_color_background:
                        type: string
                        description: 'HEX value of the color of the background of the task.'
                      s_color_border:
                        type: string
                        description: 'HEX value of the color of the border of the task.'
                      s_color_link:
                        type: string
                        description: 'HEX value of the color of the link of the task.'
                      s_color_text:
                        type: string
                        description: 'HEX value of the color of the title of the task.'
      tags:
        - Backlight-Change
  '/Studio/Task/Color/Color.json':
    delete:
      summary: 'Deletes color by color ID for current user.'
      parameters:
        - name: k_task_color
          in: query
          description: 'Task color key. <tt>null</tt> if we are adding new color.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Color-Color
    get:
      summary: 'Get all colors for current user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_color:
                        type: array
                        description: 'List of all colors for current user.'
      tags:
        - Color-Color
    put:
      summary: 'Adds/updates color for current user.'
      parameters:
        - name: k_task_color
          in: query
          description: 'Task color key. <tt>null</tt> if we are adding new color.'
          schema:
            type: [string, "null"]
        - name: s_color_background
          in: query
          description: 'HEX value of the color of the background of the task.'
          schema:
            type: string
        - name: s_color_link
          in: query
          description: 'HEX value of the color of the link of the task.'
          schema:
            type: string
        - name: s_color_text
          in: query
          description: 'HEX value of the color of the title of the task.'
          schema:
            type: string
        - name: text_title
          in: query
          description: 'Description of the color.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_task_color:
                        type: [string, "null"]
                        description: 'Task color key. <tt>null</tt> if we are adding new color.'
                      s_color_background:
                        type: string
                        description: 'HEX value of the color of the background of the task.'
                      s_color_link:
                        type: string
                        description: 'HEX value of the color of the link of the task.'
                      s_color_text:
                        type: string
                        description: 'HEX value of the color of the title of the task.'
                      text_title:
                        type: string
                        description: 'Description of the color.'
      tags:
        - Color-Color
  '/Studio/Sprint/Promoting/ExecuteScript.json':
    post:
      summary: ''
      parameters:
        - name: k_microservice
          in: query
          description: 'Microservice key. Primary key in {@link MicroserviceSql} table. `null` if the value is not set'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_script:
                  type: string
                  description: 'Name of the script to execute. Scripts are defined by <tt>a_script</tt> variable in <tt>namespace.Studio/Sprint/Promoting/PromotingConfig.json</tt>.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promoting-ExecuteScript
  '/Studio/Sprint/Promoting/PromotingLog.json':
    get:
      summary: ''
      parameters:
        - name: k_microservice
          in: query
          description: 'Microservice key. Primary key in {@link MicroserviceSql} table. `null` if the value is not set'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_log:
                        type: array
                        description: 'Contents of the logs retrieved.'
      tags:
        - Promoting-PromotingLog
  '/Studio/Deploy/Cluster/Cluster.json':
    post:
      summary: 'Create new `Deploy Cluster`. Validates SSH keys, Amazon security group configuration, and repository paths. Creates a new cluster record linked to the specified deployment instance.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_port:
                  type: [number, "null"]
                  description: 'SSH port number that should be used by compilation server to connect instance servers.'
                is_active:
                  type: [boolean, "null"]
                  description: 'Whether this deployment cluster is active.'
                is_fingerprint:
                  type: boolean
                  description: 'Whether to update fingerprints. Additionally, this setting allows you to force the transfer of keys to the server.'
                k_deploy_cluster:
                  type: [string, "null"]
                  description: 'Key of the cluster being saved.'
                k_deploy_compile:
                  type: [string, "null"]
                  description: 'ID of deployment server that serves this cluster.'
                k_deploy_instance:
                  type: [string, "null"]
                  description: '`Deploy Instance` ID.'
                s_cluster:
                  type: [string, "null"]
                  description: 'Cluster description contains either amazon security group id.'
                s_deploy_cluster:
                  type: [string, "null"]
                  description: 'Name of this deployment cluster.'
                s_key_control:
                  type: [string, "null"]
                  description: 'Private key to access all instances of cluster over SSH under control user.'
                s_ssh:
                  type: [string, "null"]
                  description: 'Private key to access all instances of cluster over SSH under instance user.'
                s_user:
                  type: [string, "null"]
                  description: 'Name of instance user.'
                s_user_control:
                  type: [string, "null"]
                  description: 'Name of control user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_deploy_cluster:
                        type: [string, "null"]
                        description: 'Key of the cluster being saved.'
      tags:
        - Cluster-Cluster
  '/Studio/Deploy/Instance/Instance.json':
    delete:
      summary: 'Delete an instance from database. For now only `stand` instances can be deleted physically. Validates that the instance exists and is a stand instance before deleting the record.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Instance-Instance
    post:
      summary: 'Creates new `Deploy Instance` Validates repository paths, variables, project, and optional account, role, and tag assignments.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_role:
                  type: [array, "null"]
                  description: 'List of `Project Role`.'
                a_tag:
                  type: [array, "null"]
                  description: 'List of tags. Each is one of {@link Tag::TAG_LIST}.'
                is_button:
                  type: [boolean, "null"]
                  description: 'Allow users to start builds manually by clicking a button.'
                is_commit:
                  type: [boolean, "null"]
                  description: 'Run build automatically upon commit.'
                is_queue:
                  type: [boolean, "null"]
                  description: 'Allow multiple commits to wait in the build queue. If not allowed, then only a single (the last one) commit may wait in the queue.'
                is_remove:
                  type: [boolean, "null"]
                  description: 'Indicates if instance set as removed.'
                k_amazon_account:
                  type: [string, "null"]
                  description: 'The key of `Studio Amazon Account`.'
                k_deploy_instance:
                  type: [string, "null"]
                  description: 'The key of `Studio Deploy Instance`.'
                k_project:
                  type: [string, "null"]
                  description: 'The key of `Studio Project`'
                s_deploy_instance:
                  type: [string, "null"]
                  description: 'The name of `Studio Deploy Instance`.'
                s_path:
                  type: [string, "null"]
                  description: 'The value contains a list of repository path.'
                s_variable:
                  type: [string, "null"]
                  description: 'The value contains a list of variables.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_deploy_instance:
                        type: [string, "null"]
                        description: 'The key of `Studio Deploy Instance`.'
      tags:
        - Instance-Instance
  '/Studio/Deploy/Instance/Preliminary.json':
    put:
      summary: 'Sets preliminary mode.'
      parameters:
        - name: k_deploy_instance
          in: query
          description: 'Key of a deployment instance to change. Primary key in {@link \StudioDeployInstanceSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_preliminary:
                  type: [boolean, "null"]
                  description: 'Preliminary mode. <tt>true</tt> to perform deployment in preliminary mode. <tt>false</tt> to perform normal deployment. <tt>null</tt> if mode is undefined.'
                text_reason:
                  type: string
                  description: 'Reason for change locking mode.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_preliminary:
                        type: [boolean, "null"]
                        description: 'Preliminary mode. <tt>true</tt> to perform deployment in preliminary mode. <tt>false</tt> to perform normal deployment. <tt>null</tt> if mode is undefined.'
      tags:
        - Instance-Preliminary
  '/Studio/Deploy/Instance/Item.json':
    put:
      summary: 'Patches a deployment instance.'
      parameters:
        - name: k_deploy_instance
          in: query
          description: 'ID of deployment instance. <tt>null</tt> if a new instance is about to be created.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_lock:
                  type: [boolean, "null"]
                  description: 'Locking mode. <tt>true</tt> to allow locking of the servers. <tt>false</tt> to deny locking. <tt>null</tt> if locking mode should not be changed.'
                text_reason:
                  type: string
                  description: 'Reason for change locking mode.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_lock:
                        type: [boolean, "null"]
                        description: 'Locking mode. <tt>true</tt> to allow locking of the servers. <tt>false</tt> to deny locking. <tt>null</tt> if locking mode should not be changed.'
      tags:
        - Instance-Item
  '/Studio/Deploy/Instance/InstancePersonalSetting.json':
    put:
      summary: 'Update personal settings.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_deploy_instance:
                  type: string
                  description: 'The deployment instance for which the settings will be updated. Primary key in the {@link \StudioDeployInstanceSql} table.'
                text_comment:
                  type: string
                  description: 'The comment on the service.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Instance-InstancePersonalSetting
  '/Studio/Personnel/Expertise/ExpertiseList.json':
    get:
      summary: ''
      parameters:
        - name: k_personnel
          in: query
          description: 'Personnel key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of project''s components: <dl> <dt>string `k_component`</dt> <dd>Component key. Primary key in {@link \Studio\Project\Component\ComponentSql} table.</dd> <dt>string `k_personnel`</dt> <dd>Personnel key. Primary key {@link \StudioPersonnelSql} table.</dd> <dt>bool `show_delete_button`</dt> <dd><tt>true</tt> - if need to show delete button for the record, <tt>false</tt> - otherwise.</dd> <dt>bool `show_edit_button`</dt> <dd><tt>true</tt> - if need to show edit button for the record, <tt>false</tt> - otherwise.</dd> <dt>string `text_component`</dt> <dd>Component title.</dd> <dt>string `text_project`</dt> <dd>Project title.</dd> <dt>string|float `x_expertise`</dt> <dd> The number of personnel expertise by component. If a personnel in is expert in the component returns <tt>Expert</tt> string. </dd> </dl>'
                      show_modify_button:
                        type: boolean
                        description: 'Whether to show the create button. Default value <tt>true</tt>. <tt>true</tt> - is show, <tt>false</tt> - otherwise.'
      tags:
        - Expertise-ExpertiseList
  '/Studio/Personnel/Expertise/ExpertiseElement.json':
    delete:
      summary: ''
      parameters:
        - name: k_component
          in: query
          description: 'Component key.'
          schema:
            type: string
        - name: k_personnel
          in: query
          description: 'Personnel key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Expertise-ExpertiseElement
    get:
      summary: ''
      parameters:
        - name: k_component
          in: query
          description: 'Component key.'
          schema:
            type: string
        - name: k_personnel
          in: query
          description: 'Personnel key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_expertise:
                        type: number
                        format: float
                        description: 'The number of personnel expertise by a specific component.'
                      is_expert:
                        type: boolean
                        description: '<tt>true</tt> - if the personnel is expert in the component, <tt>false</tt> - otherwise.'
      tags:
        - Expertise-ExpertiseElement
    post:
      summary: ''
      parameters:
        - name: k_component
          in: query
          description: 'Component key.'
          schema:
            type: string
        - name: k_personnel
          in: query
          description: 'Personnel key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                f_expertise:
                  type: number
                  format: float
                  description: 'The number of personnel expertise by a specific component.'
                is_expert:
                  type: boolean
                  description: '<tt>true</tt> - if the personnel is expert in the component, <tt>false</tt> - otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Expertise-ExpertiseElement
  '/Studio/Personnel/DebugToken/DebugTokenService.json':
    post:
      summary: 'Return information is the debug token is valid. Validates token expiration, the requesting bot login, and personnel access before confirming.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_passport_bot_login:
                  type: string
                  description: 'Passport bot login that sends the request to check the token. <i>Copy of the {@link PASSPORT_BOT_LOGIN}.</i>'
                s_token:
                  type: string
                  description: 'Debug token to check.'
                url_request:
                  type: string
                  description: 'Full URL of the request which was made to get access with testing token, including protocol, host, and URI.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_valid:
                        type: boolean
                        description: 'Debug token to check and if valid to return `true` otherwise `false`.'
      tags:
        - DebugToken-DebugTokenService
  '/Studio/Personnel/DebugToken/DebugToken.json':
    put:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_application:
                  type: string
                  description: 'Name application that is being created token.'
                s_token:
                  type: string
                  description: 'Contain debug token for login to application.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token:
                        type: string
                        description: 'Contain debug token for login to application.'
      tags:
        - DebugToken-DebugToken
  '/Studio/Personnel/Foreign/PersonnelForeign.json':
    get:
      summary: ''
      parameters:
        - name: id_foreign
          in: query
          description: 'Id of the integration system. One of the {@link PersonnelForeignSid} constants.'
          schema:
            type: string
        - name: k_personnel
          in: query
          description: 'Key of the studio personnel {@link StudioPersonnelSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_alias:
                        type: string
                        description: 'Alias af the personnel within specific integrations system.'
      tags:
        - Foreign-PersonnelForeign
    post:
      summary: ''
      parameters:
        - name: id_foreign
          in: query
          description: 'Id of the integration system. One of the {@link PersonnelForeignSid} constants.'
          schema:
            type: string
        - name: k_personnel
          in: query
          description: 'Key of the studio personnel {@link StudioPersonnelSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_alias:
                  type: string
                  description: 'Alias af the personnel within specific integrations system.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_alias:
                        type: string
                        description: 'Alias af the personnel within specific integrations system.'
      tags:
        - Foreign-PersonnelForeign
  '/Studio/Personnel/Detail/Detail.json':
    post:
      summary: 'Gets information about user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_login:
                  type: string
                  description: 'User login.'
                s_user_password:
                  type: string
                  description: 'One time password for user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_bot_password:
                        type: string
                        description: 'Bot password of the user.'
                      text_mail:
                        type: string
                        description: 'Mail of the user.'
      tags:
        - Detail-Detail
  '/Studio/Personnel/Telegram/TelegramHook.json':
    post:
      summary: ''
      parameters:
        - name: s_token
          in: query
          description: 'Telegram bot token for additional check from telegram.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Telegram-TelegramHook
  '/Studio/Personnel/Project/ProjectList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of studio projects: <dl> <dt>string `k_project`</dt> <dd>Project key. Primary key in {@link \StudioProjectSql} table.</dd> <dt>string `k_project_status`</dt> <dd>Project status key. Primary key in {@link \StudioProjectStatusSql} table.</dd> <dt>string `text_title`</dt> <dd>Project title.</dd> </dl>'
      tags:
        - Project-ProjectList
  '/Studio/Personnel/Birthday/Birthday.json':
    post:
      summary: 'Sends a birthday wish to the specified personnel.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_personnel_to:
                  type: string
                  description: 'Key of the personnel to send a birthday wish to.'
                text_wish:
                  type: string
                  description: 'Text of the birthday wish to send.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_result:
                        type: string
                        description: 'Text of the birthday wish to send.'
      tags:
        - Birthday-Birthday
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Birthday-Birthday
  '/Studio/Schedule/Holiday/HolidayList.json':
    get:
      summary: ''
      parameters:
        - name: id_flag
          in: query
          description: 'State of the schedule holiday.'
          schema:
            type: number
            format: int32
        - name: k_personnel
          in: query
          description: 'Key of the studio personnel.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list with a schedule holiday of the personnel: <dl> <dt>string `dl_end`</dt> <dd>End date of the by specific holiday.</dd> <dt>string `dl_start`</dt> <dd>Start date of the by specific holiday.</dd> <dt>int `i_row`</dt> <dd>A number row.</dd> <dt>int `i_use`</dt> <dd>A count of used vocation days.</dd> <dt>bool `is_remove`</dt> <dd><tt>true</tt> - schedule holiday is remove, <tt>false</tt> - otherwise.</dd> <dt>string `k_schedule_holiday`</dt> <dd>Schedule holiday key. Primary key {@link \StudioScheduleHolidaySql} table.</dd> <dt>string `text_holiday`</dt> <dd>Name of the specific holiday.</dd> <dt>string `text_personnel`</dt> <dd>Name of the personnel.</dd> </dl>'
                      show_create_button:
                        type: boolean
                        description: 'Whether to show the create button. Default value <tt>true</tt>. <tt>true</tt> - is show, <tt>false</tt> - otherwise.'
                      show_filter:
                        type: boolean
                        description: 'Whether to show the edit button. Default value <tt>true</tt>. <tt>true</tt> - is show, <tt>false</tt> - otherwise.'
      tags:
        - Holiday-HolidayList
  '/Studio/Schedule/Holiday/HolidayTotal.json':
    get:
      summary: ''
      parameters:
        - name: k_personnel
          in: query
          description: 'Key of the personnel. Primary key in {@link StudioPersonnelSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_vacation:
                        type: array
                        description: 'A List of vacations for personnel. Each element has structure: <dl> <dt>string `dl_end`</dt> <dd>Date when vacation is end.</dd> <dt>string `dl_start`</dt> <dd>Date when vacation is start.</dd> <dt>int `i_available`</dt> <dd>Available vacation days. Calculated <tt><var>i_day</var>-<var>i_use</var></tt>.</dd> <dt>int `i_day`</dt> <dd>Count of vacation days.</dd> <dt>int `i_use`</dt> <dd>Count of used days in this vacation.</dd> <dt>string `k_personnel_vacation`</dt> <dd>Personnel vacation key. Primary key in {@link \Studio\Personnel\Vacation\PersonnelVacationSql} table.</dd> </dl>'
                      i_available:
                        type: number
                        format: int32
                        description: 'The number of remaining vacation days available this year.'
                      i_holiday:
                        type: number
                        format: int32
                        description: 'The number of vacation days available this year.'
      tags:
        - Holiday-HolidayTotal
  '/Studio/Schedule/Holiday/HolidayElement.json':
    get:
      summary: ''
      parameters:
        - name: k_schedule_holiday
          in: query
          description: 'Schedule holiday key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dl_end:
                        type: string
                        description: 'End date of the holiday.'
                      dl_start:
                        type: string
                        description: 'Start date of the holiday.'
                      is_remove:
                        type: boolean
                        description: 'Whether schedule holiday is remove. Default value <tt>false</tt>. <tt>true</tt> - schedule holiday is remove, <tt>false</tt> - otherwise.'
                      k_personnel:
                        type: [string, "null"]
                        description: 'Key of the studio personnel.'
                      text_holiday:
                        type: string
                        description: 'Name of the holiday.'
                      url_log:
                        type: string
                        description: 'Link to logs for the current editable holiday.'
      tags:
        - Holiday-HolidayElement
    post:
      summary: ''
      parameters:
        - name: k_schedule_holiday
          in: query
          description: 'Schedule holiday key.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_end:
                  type: string
                  description: 'End date of the holiday.'
                dl_start:
                  type: string
                  description: 'Start date of the holiday.'
                is_remove:
                  type: boolean
                  description: 'Whether schedule holiday is remove. Default value <tt>false</tt>. <tt>true</tt> - schedule holiday is remove, <tt>false</tt> - otherwise.'
                k_personnel:
                  type: [string, "null"]
                  description: 'Key of the studio personnel.'
                text_holiday:
                  type: string
                  description: 'Name of the holiday.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Holiday-HolidayElement
  '/Studio/Schedule/Holiday/HolidayCalendar.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_event:
                        type: string
                        description: 'A JSON array information about used holidays.'
      tags:
        - Holiday-HolidayCalendar
  '/Studio/Service/Cid/Info.json':
    get:
      summary: 'Returns information about a CID. Retrieves the class name registered for the given CID value.'
      parameters:
        - name: cid
          in: query
          description: 'CID of the class to retrieve information for.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_class:
                        type: string
                        description: 'Name of the class associated with specified CID.'
      tags:
        - Cid-Info
  '/Studio/Service/Cid/Register.json':
    post:
      summary: 'Registers a new CID. If class is registered already, returns its CID.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_class:
                  type: string
                  description: 'Name of the class to register.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      cid:
                        type: number
                        format: int32
                        description: 'CID of requested class.'
      tags:
        - Cid-Register
  '/Studio/Service/Cid/Rename.json':
    put:
      summary: 'Allows to rename a class associated with a CID. The CID must already be registered. Validates the new class name format before updating.'
      parameters:
        - name: cid
          in: query
          description: 'CID of the class to rename.'
          schema:
            type: number
            format: int32
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_class:
                  type: string
                  description: 'New name to assign to a CID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Cid-Rename
  '/Studio/Report/EstimateEffort/Effort.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_jira_key:
                  type: string
                  description: 'List of Jira keys to load.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Result of the effort estimation.'
      tags:
        - EstimateEffort-Effort
  '/Studio/Ip/Service/Edit.json':
    delete:
      summary: 'Deletes an IP service. Removes the service record from the database.'
      parameters:
        - name: k_ip_service
          in: query
          description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-Edit
    get:
      summary: 'Returns information about an IP service. Returns the service name, group, type, timeout, update rules, and auto-restriction flag.'
      parameters:
        - name: k_ip_service
          in: query
          description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_service_timeout:
                        type: number
                        format: int32
                        description: 'Timeout of the service. One of {@link \Studio\Ip\Service\ServiceTimeoutSid} constants.'
                      id_service_type:
                        type: number
                        format: int32
                        description: 'Type of the service. One of {@link \Studio\Ip\Service\ServiceTypeSid} constants.'
                      is_auto_restricted:
                        type: boolean
                        description: 'Indicates whether automatic session renewal for IP-based access is restricted. If set to `1`, access by IP is restricted and automatic session renewal is disabled if `0`, access by IP is allowed and session can be automatically renewed.'
                      text_group:
                        type: string
                        description: 'Group of services.'
                      text_title:
                        type: string
                        description: 'Name of the service.'
                      text_update:
                        type: string
                        description: 'Network update rules.'
      tags:
        - Service-Edit
    post:
      summary: 'Saves an IP service. Creates or updates a service record. When update rules are provided, the service IP list is updated.'
      parameters:
        - name: k_ip_service
          in: query
          description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_service_timeout:
                  type: number
                  format: int32
                  description: 'Timeout of the service. One of {@link \Studio\Ip\Service\ServiceTimeoutSid} constants.'
                id_service_type:
                  type: number
                  format: int32
                  description: 'Type of the service. One of {@link \Studio\Ip\Service\ServiceTypeSid} constants.'
                is_auto_restricted:
                  type: boolean
                  description: 'Indicates whether automatic session renewal for IP-based access is restricted. If set to `1`, access by IP is restricted and automatic session renewal is disabled if `0`, access by IP is allowed and session can be automatically renewed.'
                text_group:
                  type: string
                  description: 'Group of services.'
                text_title:
                  type: string
                  description: 'Name of the service.'
                text_update:
                  type: string
                  description: 'Network update rules.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_ip_service:
                        type: [string, "null"]
                        description: 'If a service is edited, its key is stored here. <tt>null</tt> if a new service should be created. This value is required for create and delete operations.'
      tags:
        - Service-Edit
  '/Studio/Ip/Service/List.json':
    get:
      summary: 'Returns a list of IP services.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of IP services.'
      tags:
        - Service-List
  '/Studio/Ip/Bot/Access.json':
    delete:
      summary: 'Revokes bot access from a service. Removes the access record and schedules a service update to apply the change.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'Key of a bot which access should be changed.'
          schema:
            type: string
        - name: k_ip_service
          in: query
          description: 'Key of a service which access should be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Access
    put:
      summary: 'Grants bot access to a service. Inserts an access record and schedules a service update to apply the change.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'Key of a bot which access should be changed.'
          schema:
            type: string
        - name: k_ip_service
          in: query
          description: 'Key of a service which access should be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Access
  '/Studio/Ip/Bot/Edit.json':
    delete:
      summary: 'Deletes a bot. Removes the bot record and schedules service updates for all services the bot had access to.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Edit
    get:
      summary: 'Returns information about a bot. Returns the bot name, network, password, update rules, and active status.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_active:
                        type: boolean
                        description: 'Whether this bot is active.'
                      text_bot:
                        type: string
                        description: 'Name of the bot.'
                      text_network:
                        type: string
                        description: 'Network of the bot. Note that value sent here is ignored in a case {@link EditApi::$text_update} is not empty.'
                      text_password:
                        type: string
                        description: 'Update password.'
                      text_update:
                        type: string
                        description: 'Network update rules.'
      tags:
        - Bot-Edit
    post:
      summary: 'Saves a bot. Creates or updates a bot record. When update rules are provided, the network is derived from them.'
      parameters:
        - name: k_ip_bot
          in: query
          description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Whether this bot is active.'
                text_bot:
                  type: string
                  description: 'Name of the bot.'
                text_network:
                  type: string
                  description: 'Network of the bot. Note that value sent here is ignored in a case {@link EditApi::$text_update} is not empty.'
                text_password:
                  type: string
                  description: 'Update password.'
                text_update:
                  type: string
                  description: 'Network update rules.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_ip_bot:
                        type: [string, "null"]
                        description: 'If a bot is edited, its key is stored here. <tt>null</tt> if a new bot should be created. This value is required for create and delete operations.'
      tags:
        - Bot-Edit
  '/Studio/Ip/Bot/List.json':
    get:
      summary: 'Returns a list of bots of IP services.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of bots of IP services.'
      tags:
        - Bot-List
  '/Studio/Ip/Bot/Update.json':
    get:
      summary: 'Updates network of the bot.'
      parameters:
        - name: s_password
          in: query
          description: 'Password to update the bot.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Bot-Update
  '/Studio/Ip/Personnel/Access.json':
    delete:
      summary: 'Revokes access.'
      parameters:
        - name: a_ip_service
          in: query
          description: 'A list of IP services to change.'
          schema:
            type: array
        - name: k_personnel
          in: query
          description: 'Key of a personnel member whose access should be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Personnel-Access
    put:
      summary: 'Grants access.'
      parameters:
        - name: a_ip_service
          in: query
          description: 'A list of IP services to change.'
          schema:
            type: array
        - name: k_personnel
          in: query
          description: 'Key of a personnel member whose access should be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Personnel-Access
  '/Studio/Tv/Example/ClassPageListGet.json':
    get:
      summary: 'Get a key list of view class that are available to this group.'
      parameters:
        - name: id_group
          in: query
          description: 'Group ID. One of {@link \Studio\Tv\TvGroupSid} constants.'
          schema:
            type: number
            format: int32
        - name: s_password
          in: query
          description: 'Authorization password.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_key:
                        type: array
                        description: 'A key page list of view class that are available to current group.'
      tags:
        - Example-ClassPageListGet
  '/Studio/Tv/Example/PageChart.json':
    get:
      summary: 'Get chart data.'
      parameters:
        - name: id_group
          in: query
          description: 'Group ID. One of {@link \Studio\Tv\TvGroupSid} constants.'
          schema:
            type: number
            format: int32
        - name: s_password
          in: query
          description: 'Authorization password.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: object
                        description: 'Chart data. Each element is an array of arrays of coordinates of points on a separate graph, where the zero element is the abscissa, and the first is the ordinate of the point.'
                      text_data:
                        type: string
                        description: 'Chart description.'
      tags:
        - Example-PageChart
  '/Studio/Tv/Example/PageTable.json':
    get:
      summary: 'Get text data.'
      parameters:
        - name: id_group
          in: query
          description: 'Group ID. One of {@link \Studio\Tv\TvGroupSid} constants.'
          schema:
            type: number
            format: int32
        - name: s_password
          in: query
          description: 'Authorization password.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: object
                        description: 'Table data.'
                      text_title:
                        type: string
                        description: 'Table description.'
      tags:
        - Example-PageTable
  '/Studio/Tv/Example/PageText.json':
    get:
      summary: 'Get text data.'
      parameters:
        - name: id_group
          in: query
          description: 'Group ID. One of {@link \Studio\Tv\TvGroupSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_page
          in: query
          description: 'Key page.'
          schema:
            type: string
        - name: s_password
          in: query
          description: 'Authorization password.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_data:
                        type: string
                        description: 'Backend text data.'
      tags:
        - Example-PageText
  '/Studio/Team/Manage/TeamEdit.json':
    delete:
      summary: 'Deletes a team.'
      parameters:
        - name: k_team
          in: query
          description: 'Key of the team. Primary key in {@link \Studio\Team\TeamSql} table. <tt>null</tt> if a new team should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Manage-TeamEdit
    get:
      summary: 'Returns information about a team.'
      parameters:
        - name: k_team
          in: query
          description: 'Key of the team. Primary key in {@link \Studio\Team\TeamSql} table. <tt>null</tt> if a new team should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_personnel_select:
                        type: string
                        description: 'Personnel list html.'
                      text_title:
                        type: string
                        description: 'Name of the team.'
      tags:
        - Manage-TeamEdit
    post:
      summary: 'Saves a team.'
      parameters:
        - name: k_team
          in: query
          description: 'Key of the team. Primary key in {@link \Studio\Team\TeamSql} table. <tt>null</tt> if a new team should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_personnel:
                  type: array
                  description: 'List of selected personnel.'
                a_team_role:
                  type: array
                  description: 'List of personnel roles. Key is personnel key. Value is team role.'
                text_title:
                  type: string
                  description: 'Name of the team.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_team:
                        type: [string, "null"]
                        description: 'Key of the team. Primary key in {@link \Studio\Team\TeamSql} table. <tt>null</tt> if a new team should be created. This value is required for edit and delete operations.'
      tags:
        - Manage-TeamEdit
  '/Studio/Team/Manage/TeamList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: [array, "null"]
                        description: 'A list of team.'
      tags:
        - Manage-TeamList
  '/Studio/Project/Role/RoleList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: object
                        description: 'A list of project roles. Each element of the array has a next structure: <dl> <dt>string `k_project_role`</dt> <dd>Project role key. Primary key in {@link \StudioProjectRoleSql} table.</dd> <dt>string `s_project_role`</dt> <dd>Project role title.</dd> </dl>'
      tags:
        - Role-RoleList
  '/Studio/Project/Component/ComponentList.json':
    get:
      summary: ''
      parameters:
        - name: k_project
          in: query
          description: 'Project key. <tt>null</tt> - when need to load all components for which the current personnel has access.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of project''s components: <dl> <dt>string `k_component`</dt> <dd>Component key. Primary key in {@link \Studio\Project\Component\ComponentSql} table.</dd> <dt>string `text_description`</dt> <dd>Component description.</dd> <dt>string `text_project`</dt> <dd>Project title.</dd> <dt>string `text_title`</dt> <dd>Component title.</dd> <dt>string `text_title_full`</dt> <dd>Component title with project title in the bracket.</dd> <dt>string `url_expert_list`</dt> <dd>A link to the page with experts list by the component.</dd> </dl>'
      tags:
        - Component-ComponentList
  '/Studio/Project/Component/ComponentElement.json':
    delete:
      summary: ''
      parameters:
        - name: k_component
          in: query
          description: 'Component key. <tt>null</tt> - if need to create new component.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Component-ComponentElement
    get:
      summary: ''
      parameters:
        - name: k_component
          in: query
          description: 'Component key. <tt>null</tt> - if need to create new component.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_project:
                        type: string
                        description: 'Project key.'
                      text_description:
                        type: string
                        description: 'Description of the component.'
                      text_title:
                        type: string
                        description: 'Title of the component.'
      tags:
        - Component-ComponentElement
    post:
      summary: ''
      parameters:
        - name: k_component
          in: query
          description: 'Component key. <tt>null</tt> - if need to create new component.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_project:
                  type: string
                  description: 'Project key.'
                text_description:
                  type: string
                  description: 'Description of the component.'
                text_title:
                  type: string
                  description: 'Title of the component.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Component-ComponentElement
  '/Studio/Integration/Fisheye/FisheyeReviewLock.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_lock:
                  type: string
                  description: 'Whether revision should be locked for future commits. `true` to lock revision `false` to unlock revision.'
                k_task:
                  type: [string, "null"]
                  description: 'Key of the studio task within which review status should be changed. This will influence list of reviewers to check whether current user has access to change status. `null` to ignore list of reviewers from Studio and only check reviewers on Fisheye.'
                s_review_key:
                  type: string
                  description: 'Fisheye revision key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Fisheye-FisheyeReviewLock
  '/Studio/Integration/Claude/ClaudePinecone.json':
    post:
      summary: ''
      parameters:
        - name: s_authorization_key
          in: query
          description: 'Secret key required to authorize and access this API endpoint. This value must be provided by the client in the request to successfully authenticate and use the endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claude-ClaudePinecone
  '/Studio/AI/Teams/MicrosoftBot.json':
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Teams-MicrosoftBot
  '/Core/Quiz/Element/QuizXMLParse.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_fields:
                  type: array
                  description: 'Fields to be XML parsed. Keys are field names that prefixed with xml_. Values are XML strings to be parsed and converted to HTML.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_results:
                        type: array
                        description: 'Fields that were XML parsed. Keys are field names that prefixed with html_. Values are HTML strings.'
      tags:
        - Element-QuizXMLParse
  '/Core/Locale/Message/ClearCache.json':
    post:
      summary: 'Clears all caches where messages can be cached. This includes compiled versions of templates.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Message-ClearCache
  '/Core/Prg/Branch/BranchSwitchLog.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_log:
                        type: array
                        description: 'Error logs on branch switching. Last {@link BranchSwitchLogApi::NUMBER_LINES} lines.'
                      a_log:
                        type: array
                        description: 'Logs on branch switching. Last {@link BranchSwitchLogApi::NUMBER_LINES} lines.'
                      a_repair_status:
                        type: object
                        description: 'List of repositories that failed the last switch and could not be auto-repaired. Each element has the same structure as a {@link BranchRepairTool::statusRead()} entry with {@link BranchRepairTool::STATUS_ERROR} status. Empty array if all repositories are healthy.'
                      is_logging_done:
                        type: boolean
                        description: 'Whether the branch switching operation is complete and no more logs are generated. `true` - switch done, `false` - the replacement of branches is still ongoing.'
                      is_switch_blocked:
                        type: boolean
                        description: 'Whether branch switching is currently blocked due to unresolved repair errors from a previous switch. `true` - blocked (at least one repository requires manual repair), `false` - all repositories are healthy.'
      tags:
        - Branch-BranchSwitchLog
  '/Core/Prg/Branch/BranchSwitch.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_switch:
                  type: object
                  description: 'List directories for switch. One element contains: <dl> <dt>string `s_path_local`</dt> <dd>Local directory which will be switched.</dd> <dt>string `url_server`</dt> <dd>Remote repository URI which will be installed.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchSwitch
  '/Core/Prg/Branch/BranchRepairVerify.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_path_local:
                  type: string
                  description: 'Local path to the SVN working copy to verify.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Branch-BranchRepairVerify
  '/Core/Prg/ClearPublic/ClearPublic.json':
    post:
      summary: 'Recursively deletes all files and subdirectories from public html except for the following: Keeps files in these directories: *  public html *  public html/a *  public html/static Does not recurse into these directories: *  public html/a/drive*'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClearPublic-ClearPublic
  '/Core/Prg/Editor/SqlKeyword.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_connection:
                        type: [object, array]
                        description: 'List of all MySQL connections. Key is name of a MySQL connection. Value is a list of tables in that connection. Alternatively, value may have name of another connection - in a case a list of tables in these two connections are equal.'
                      a_table:
                        type: array
                        description: 'List of information about tables. Key is name of a table. Value is an array with the following structure: <dl> <dt>string `s_type`</dt> <dd>Type of the column.</dd> <dt>string `s_table`</dt> <dd> Name of a table in which this column is a primary key. Not specified if this column is not a primary key. </dd> </dl>'
      tags:
        - Editor-SqlKeyword
  '/Core/Prg/Login/Enter.json':
    post:
      summary: 'Signs programmer in.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_login:
                  type: string
                  description: 'Programmer login.'
                s_notepad:
                  type: string
                  description: 'Notepad that is used to hash user password. See {@link \Core\Passport\Login\Enter\EnterApi::$s_password} about hashing of user password.'
                s_password:
                  type: string
                  description: 'User password (evaluated with <tt>Core_Passport_Login_Enter_NotepadModel.hash()</tt> function on javascript).'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-Enter
  '/Core/Prg/PgSql/QueryExecute.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_root:
                  type: boolean
                  description: '`true` if the query must be executed on behalf of the root, `false` if the query must be executed on behalf of a normal user with limited rights.'
                s_connection:
                  type: string
                  description: 'The connection key to execute PostgreSQL query. One of the result keys of the {@link QueryPgSqlExecute::connectionList()} method.'
                s_pgsql_query:
                  type: string
                  description: 'PostgreSQL query to execute.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_body:
                        type: string
                        description: 'Result of the executing query.'
      tags:
        - PgSql-QueryExecute
  '/Core/Prg/SmartCache/SmartCache.json':
    get:
      summary: ''
      parameters:
        - name: is_redirect
          in: query
          description: '<tt>true</tt> if use redirect, <tt>false</tt> otherwise.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_change_js:
                        type: boolean
                        description: 'See {@link SmartCache::$is_change_js} for documentation.'
                      is_change_less:
                        type: boolean
                        description: 'See {@link SmartCache::$is_change_less} for documentation.'
                      is_change_opcache:
                        type: boolean
                        description: 'See {@link SmartCache::$is_change_opcache} for documentation.'
      tags:
        - SmartCache-SmartCache
  '/Core/Prg/Task/QueueList.json':
    get:
      summary: 'Get data about task queue.'
      parameters:
        - name: a_prefix
          in: query
          description: 'List of prefix of the task class for task search. Empty array - all task select.'
          schema:
            type: array
        - name: is_erroneous
          in: query
          description: 'Whether queued or erroneous tasks must be returned. ''false'' - load of tasks in the queue, `true` - load of erroneous tasks.'
          schema:
            type: boolean
        - name: s_id
          in: query
          description: 'Prefix of task IDs for task search. Empty value - no ID search, selecting all tasks.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_queue:
                        type: object
                        description: 'List of queue. One element contains: <dl> <dt>int `cid_task`</dt> <dd>CID of the task class. One of {@link \Core\Task\CoreTaskAbstract} classes.</dd> <dt>string|null `dtu_queue`</dt> <dd>Task start time. `null` for erroneous tasks.</dd> <dt>bool `is_active`</dt> <dd>Whether the task is currently running. `true` if the task is currently running, `false` otherwise.</dd> <dt>bool `is_safe`</dt> <dd> Copy of {@link \Core\Task\CoreTaskAbstract::PRG_SAFE}. `true` if tasks of this class require special attention when removing, `false` if they can be removed safely. </dd> <dt>string `s_class`</dt> <dd>Class name. One of the {@link \Core\Task\CoreTaskAbstract} subclasses.</dd> <dt>string `s_id`</dt> <dd>ID of the task. Copy of {@link \CmsTaskQueueSql}.`s_id`.</dd> <dt>string `s_prefix`</dt> <dd>Task ID prefix. This can be a class name or an CID.</dd> <dt>string|null `s_queue`</dt> <dd> Time elapsed since the start of the task. Difference between <var>dtu_queue</var> field and current time formatted using the {@link \PrgCmsTaskThreadView::timeFormat()} method. `null` for erroneous tasks. </dd> </dl>'
      tags:
        - Task-QueueList
  '/Core/Prg/Task/RemoveAll.json':
    post:
      summary: 'Clear all tasks from task queue.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Task-RemoveAll
  '/Core/Prg/Task/RemoveTask.json':
    post:
      summary: 'Remove tasks.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_task:
                  type: array
                  description: 'List of task IDs (copy of {@link \CmsTaskQueueSql}.<tt>s_id</tt> values) of the queued task to remove.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Task-RemoveTask
  '/Core/Prg/Task/RestartTask.json':
    post:
      summary: 'Restart tasks.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_task:
                  type: array
                  description: 'List of task IDs (copy of  {@link \CmsTaskQueueSql}.<tt>s_id</tt> values) of the queued task to restart.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_task_fail:
                        type: array
                        description: 'List of failed task IDs (copy of {@link \CmsTaskQueueSql}.<tt>s_id</tt> values) of the queued task to be restarted.'
                      a_task_restarted:
                        type: array
                        description: 'List of task IDs (copy of {@link \CmsTaskQueueSql}.<tt>s_id</tt> values) which were restarted.'
      tags:
        - Task-RestartTask
  '/Core/Prg/Task/StatList.json':
    get:
      summary: 'Get statistic data about task queue.'
      parameters:
        - name: is_erroneous
          in: query
          description: 'Whether queued or erroneous tasks must be returned. ''false'' - load of tasks in the queue, `true` - load of erroneous tasks.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_queue:
                        type: object
                        description: 'List of queue. One element contains: <dl> <dt>int `cid_task`</dt> <dd>CID of the task class. One of {@link \Core\Task\CoreTaskAbstract} classes.</dd> <dt>string|null `dtu_queue`</dt> <dd>Task start time. `null` for erroneous tasks.</dd> <dt>bool `is_active`</dt> <dd>Whether the task is currently running. `true` if the task is currently running, `false` otherwise.</dd> <dt>bool `is_safe`</dt> <dd> Copy of {@link \Core\Task\CoreTaskAbstract::PRG_SAFE}. `true` if tasks of this class require special attention when removing, `false` if they can be removed safely. </dd> <dt>string `s_class`</dt> <dd>Class name. One of the {@link \Core\Task\CoreTaskAbstract} subclasses.</dd> <dt>string `s_id`</dt> <dd>ID of the task. Copy of {@link \CmsTaskQueueSql}.`s_id`.</dd> <dt>string `s_prefix`</dt> <dd>Task ID prefix. This can be a class name or an CID.</dd> <dt>string|null `s_queue`</dt> <dd> Time elapsed since the start of the task. Difference between <var>dtu_queue</var> field and current time formatted using the {@link \PrgCmsTaskThreadView::timeFormat()} method. `null` for erroneous tasks. </dd> </dl>'
                      a_queue_stat:
                        type: array
                        description: 'List of task queue statistic per class. This statistic includes all tasks that are due or erroneous tasks, while {@link QueueTrait::$a_queue} contains only the {@link \Core\Prg\Task\QueueListApi::DISPLAYED_QUEUE} of the oldest tasks for the queue or the {@link \Core\Prg\Task\QueueListApi::DISPLAYED_ERRONEOUS} of the erroneous tasks. One element contains: <dl> <dt>int `cid_task`</dt> <dd>CID of the task class. One of {@link \Core\Task\CoreTaskAbstract} classes.</dd> <dt>string|null `dtu_queue`</dt> <dd>The oldest tasks start time. `null` for erroneous tasks.</dd> <dt>int `i_active`</dt> <dd>The number of tasks that running at the moment.</dd> <dt>int `i_all`</dt> <dd>The number of tasks.</dd> <dt>string `s_class`</dt> <dd>Class name. One of the {@link \Core\Task\CoreTaskAbstract} subclasses.</dd> <dt>string `s_prefix`</dt> <dd>Task ID prefix. This can be a class name or an CID.</dd> <dt>string `s_queue`</dt> <dd> Time elapsed since the oldest task started. Difference between `dtu_queue` field and current time formatted using the {@link \PrgCmsTaskThreadView::timeFormat()} method. `null` for erroneous tasks. </dd> <dt>string `text_active`</dt> <dd> The numbers of active tasks to check, the first number is the count of active processes, the second number is the direct count of active tasks for a given class. </dd> </dl>'
      tags:
        - Task-StatList
  '/Core/Prg/Task/ThreadList.json':
    get:
      summary: 'Get data about task threads.'
      parameters:
        - name: is_cache
          in: query
          description: 'Determines where to get information from. `true` - from a cache (default), `no` - receiving current data from servers.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_server:
                        type: object
                        description: 'List of servers. One element contains: <dl> <dt>string `dtu_update`</dt> <dd>Time of last data update.</dd> <dt>string `ip_private`</dt> <dd>Private (local) IP.</dd> <dt>string `ip_public`</dt> <dd>Public IP.</dd> <dt>string `s_tag`</dt> <dd>EC2 instance tag name.</dd> <dt>string `text_update`</dt> <dd>Elapsed time since the last data update.</dd> </dl>'
                      a_server_error:
                        type: array
                        description: 'List of errors on servers. The key is the private IP address and the value is the error message.'
                      a_thread:
                        type: object
                        description: 'List of threads. One element contains: <dl> <dt>bool `can_remove`</dt> <dd>Is it possible to remove a thread. `true` is the thread can be remove, `false` otherwise.</dd> <dt>string `dtu_active`</dt> <dd>When the last action was performed by this thread.</dd> <dt>string `dtu_create`</dt> <dd>When this thread has started.</dd> <dt>string `dtu_current`</dt> <dd>When method that is being executed now has started.</dd> <dt>int `i_sort`</dt> <dd>Sort number.</dd> <dt>int `id_task_status`</dt> <dd>Status of the thread. One of {@link \CmsTaskStatusSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>Whether the thread has been deleted. `true` is the thread is removed, `false` otherwise.</dd> <dt>string `ip_server`</dt> <dd>IP address (local) of the server where the task thread takes place.</dd> <dt>string `s_method`</dt> <dd>Name of a method that is being executed now.</dd> <dt>string `s_task_status`</dt> <dd>Title of the thread status.</dd> <dt>string `s_thread`</dt> <dd>Key of this thread.</dd> <dt>string `s_thread_short`</dt> <dd>Short key of this thread.</dd> <dt>string `text_active`</dt> <dd>When the last action was performed by this thread.</dd> <dt>string `text_create`</dt> <dd>Lifetime of thread formatted using the {@link \PrgCmsTaskThreadView::timeFormat()} method.</dd> <dt>string `text_current`</dt> <dd>When method that is being executed now has started.</dd> <dt>string `text_info`</dt> <dd> Any additional info set at moment of {@link \Core\Task\ThreadInfo\ThreadInfo::$t_active}. Copy of argument of {@link \CmsTaskThread::active()}. </dd> <dt>string `text_remove`</dt> <dd>The comment that was set when this task thread is removed.</dd> </dl>'
      tags:
        - Task-ThreadList
  '/Core/Prg/XDebug/ProfilerMode.json':
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_enable:
                        type: boolean
                        description: '<tt>true</tt> if xdebug profiler enable, <tt>false</tt> otherwise.'
      tags:
        - XDebug-ProfilerMode
  '/Core/Prg/AsyncTask/AsyncThreadList.json':
    delete:
      summary: 'Delete asynchronous task.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AsyncTask-AsyncThreadList
    get:
      summary: 'Get information about asynchronous task.'
      parameters:
        - name: s_id
          in: query
          description: 'An identifier of asynchronous task. One identifier is a copy of result of {@link \Core\Async\CoreAsyncAbstract::id()}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: array
                        description: 'List of task representations displayed to customers. Each element contains rendered information about an individual task. The structure of each array element is: <dl> <dt>array `a_view`</dt> <dd> Contain log information about error that happened this task. Copy of result of {@link \Core\Log\TableView::load()}. </dd> <dt>string `html_task`</dt> <dd>Dumped variable of the task.</dd> </dl>'
      tags:
        - AsyncTask-AsyncThreadList
    put:
      summary: 'Run asynchronous task again.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_id:
                  type: array
                  description: 'A list of identifiers of asynchronous task. One identifier is a copy of result of {@link \Core\Async\CoreAsyncAbstract::id()}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AsyncTask-AsyncThreadList
  '/Core/Request/Api/KeySecret.json':
    get:
      summary: 'Gets a secret key for signing. Accepts a CSRF code, a session key, and the client origin URL, validates the session key against the current application, and returns a secret key that the client uses to sign subsequent CORS requests. This endpoint must not be called directly from a browser.'
      parameters:
        - name: s_csrf
          in: query
          description: 'The CSRF code from the client side.'
          schema:
            type: string
        - name: s_key_session
          in: query
          description: 'The session key.'
          schema:
            type: string
        - name: url_domain
          in: query
          description: 'Alias of {@link \Core\Request\Api\KeySecretApi::$url_origin}.'
          schema:
            type: string
        - name: url_origin
          in: query
          description: 'Origin for client requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_key_secret:
                        type: string
                        description: 'The secret key for the request signing.'
      tags:
        - Api-KeySecret
  '/Core/Request/Api/KeySession.json':
    get:
      summary: 'Gets a session key.'
      parameters:
        - name: s_application
          in: query
          description: 'Application ID.'
          schema:
            type: string
        - name: s_csrf
          in: query
          description: 'CSRF code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_key:
                        type: string
                        description: 'Session key.'
      tags:
        - Api-KeySession
  '/Core/Request/Api/Nx.json':
    delete:
      summary: 'Only throws exception.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-Nx
    get:
      summary: 'Only throws exception.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-Nx
    post:
      summary: 'Only throws exception.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-Nx
    put:
      summary: 'Only throws exception.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Api-Nx
  '/Core/Request/Token/TokenPin.json':
    get:
      summary: 'Gets new pin code. First step of push-notification-based security token generation. Issues a short-lived PIN code linked to a push recipient, which is then sent to the user''s device. The client must present this PIN before its expiry to receive the final security token.'
      parameters:
        - name: text_push
          in: query
          description: 'The ID to use to send a security code via a push notification.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtu_expire:
                        type: string
                        description: 'The expiration date/time of the PIN code.'
                      text_pin:
                        type: string
                        description: 'The PIN code.'
      tags:
        - Token-TokenPin
  '/Core/Testing/Postman/Hash.json':
    post:
      summary: 'Encode original string by algorithm that comes from the request. This method is used only for testing.'
      parameters:
        - name: s_algorithm
          in: query
          description: 'Algorithm for encoding string from {@link HashApi::$s_original}. Default is `sha3-512`.'
          schema:
            type: string
        - name: s_original
          in: query
          description: 'Original string'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_hash:
                        type: string
                        description: 'Return hash of the string'
      tags:
        - Postman-Hash
  '/Core/Spa/Model/ModelPool.json':
    post:
      summary: 'Processes the requested model. Finds the appropriate API class and executes the get method.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_request:
                  type: array
                  description: 'A set of model objects that need to be synchronized. Key - concatenate string(model class name, model registry key) Value - model data.'
                s_method:
                  type: string
                  description: 'The method (get, post, put, delete) that will be called on all given models. Currently only the "get" method is supported'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Model result.'
      tags:
        - Model-ModelPool
  '/Core/Spa/Application/SpaApplication.json':
    get:
      summary: 'Gets information of application.'
      parameters:
        - name: s_application
          in: query
          description: 'Unique ID of application.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_apple:
                        type: boolean
                        description: '`true` - enable for this application sign in with Apple; `false` - disable.'
                      is_facebook:
                        type: boolean
                        description: '`true` - enable for this application sign in with Facebook; `false` - disable.'
                      is_facebook_android:
                        type: boolean
                        description: '`true` - enable for this application sign in with Facebook for Android; `false` - disable.'
                      is_google:
                        type: boolean
                        description: '`true` - enable for this application sign in with Google; `false` - disable.'
                      is_microsoft:
                        type: boolean
                        description: '`true` - enable for this application sign in with Microsoft; `false` - disable.'
                      is_register_deny:
                        type: boolean
                        description: '`true` - registration of new user is denied in application; `false` - registration of new user is allowed.'
                      is_social:
                        type: boolean
                        description: '`true` if sign in via social networks is allowed; `false` otherwise.'
                      is_tjx:
                        type: boolean
                        description: '`true` if sign in via TJX Azure is allowed; `false` otherwise.'
                      text_tjx_button_name:
                        type: string
                        description: 'Tjx button name.'
                      url_facebook:
                        type: string
                        description: 'Facebook authorization link.'
                      url_tjx:
                        type: string
                        description: 'Tjx link.'
      tags:
        - Application-SpaApplication
  '/Core/Search/Autoscaling/AutoscalingSuspend.json':
    delete:
      summary: 'Unsuspend autoscaling activities of the PostgreSQL instances for search index.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Autoscaling-AutoscalingSuspend
    post:
      summary: 'Suspend autoscaling activities of the PostgreSQL instances for search index.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_table:
                  type: number
                  format: int32
                  description: 'CID of the search table class, one of {@link CloudSearchTable} subclasses.'
                s_provider:
                  type: string
                  description: 'Name of the search provider configuration. See {@link SearchConfigAbstract::$s_name}.'
                s_where:
                  type: string
                  description: 'Comment of suspension of autoscaling activities.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Autoscaling-AutoscalingSuspend
  '/Core/Search/Autoscaling/AutoscalingSize.json':
    post:
      summary: 'Set the number of PostgreSQL instances for search index.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_table:
                  type: number
                  format: int32
                  description: 'CID of the search table class, one of {@link CloudSearchTable} subclasses.'
                i_want:
                  type: number
                  format: int32
                  description: 'New number of instances.'
                s_provider:
                  type: string
                  description: 'Name of the search provider configuration. See {@link SearchConfigAbstract::$s_name}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Autoscaling-AutoscalingSize
  '/Core/Passport/Mfa/MfaActivate.json':
    post:
      summary: 'Activates an MFA device.'
      parameters:
        - name: k_mfa_device
          in: query
          description: 'Key of the MFA device to activate. Primary key in {@link MfaDeviceSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp:
                  type: [string, "null"]
                  description: 'The first OTP code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mfa-MfaActivate
  '/Core/Passport/Mfa/MfaEnter.json':
    post:
      summary: 'Checks the OTP code.'
      parameters:
        - name: k_mfa_device
          in: query
          description: 'Key of the MFA device to check OTP code with. Primary key in {@link MfaDeviceSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp:
                  type: [string, "null"]
                  description: 'Value of the OTP code to check.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mfa-MfaEnter
  '/Core/Passport/Login/Info.json':
    get:
      summary: 'Returns information about user that is currently signed in. Used to bootstrap the login widget: determines whether a session is active and provides the URLs needed to redirect an unauthenticated visitor to the password reset or registration flows without hard-coding those URLs on the frontend.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: [string, "null"]
                        description: 'The current user key. This will be `null` if the user isn''t signed in.'
                      url_password_change:
                        type: string
                        description: 'A URL that a user can visit to reset their password.'
                      url_register:
                        type: string
                        description: 'The URL to the registration page.'
      tags:
        - Login-Info
  '/Core/Passport/Login/PassportLoginTjx.json':
    post:
      summary: 'Signs in the user who corresponds to authorisation code.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_authorisation:
                  type: string
                  description: 'Authorisation code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-PassportLoginTjx
  '/Core/Passport/ChangePassword/ChangePasswordApply.json':
    post:
      summary: 'Saves for user new password. Accepts the login, email, verification code (from the reset password link), and the new password, validates each against the stored state, and updates the user''s password. If the user is not already signed in, a new session is started for them.'
      parameters:
        - name: text_code
          in: query
          description: 'The verification code, which can be obtained from the "reset password" email link. This is a required value.'
          schema:
            type: string
        - name: text_login
          in: query
          description: 'The user''s login, which can be obtained from the "reset password" email link. This is a required value.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The user''s email address, which can be obtained from the "reset password" email link. This is a required value.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_password:
                  type: string
                  description: 'The user''s new password. This is a required value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ChangePassword-ChangePasswordApply
  '/Core/Passport/ChangePassword/ChangePasswordBegin.json':
    post:
      summary: 'Sends to user "password recovery" mail. Accepts the user''s email address and an optional custom reset-page URL, validates the input, optionally checks a CAPTCHA, and sends a password reset email containing a link the user can follow to set a new password using {@link ChangePasswordApplyApi}.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_captcha:
                  type: string
                  description: 'The characters to pass captcha test. Specify this only if server requires captcha.'
                text_mail:
                  type: string
                  description: 'The user''s email. This is a required value.'
                url_reset:
                  type: string
                  description: 'The URL for the password reset page. This link will be used in a password reset email. Specify this only if you want to send the user to a custom password reset page. If empty, the default URL page will be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_error:
                        type: string
                        description: 'The error code. This will be an empty string if the email has been sent successfully.'
      tags:
        - ChangePassword-ChangePasswordBegin
  '/Core/Passport/Alive/PassportAlive.json':
    get:
      summary: 'Just refreshes session.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Alive-PassportAlive
  '/Core/Passport/FusionAuth/Connector.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_application_id:
                  type: string
                  description: 'The application ID. This is used to identify the application in `FusionAuth`.'
                s_login:
                  type: string
                  description: 'The user login.'
                s_password:
                  type: string
                  description: 'The user password.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_user:
                        type: array
                        description: 'The user data. The result is an array with all user fields and a registrations key containing an array of registration objects. Contains the result of merging two objects: <dl> <dt>array `user`</dt> <dd>Copy of result {@link \Core\Passport\FusionAuth\UserDto::asArray()}</dd> <dt>array `registrations`</dt> <dd>Registration is a copy of {@link \Core\Passport\FusionAuth\UserRegistrationDto::asArray()}</dd> </dl>'
      tags:
        - FusionAuth-Connector
  '/Core/Geo/City/City.json':
    delete:
      summary: ''
      parameters:
        - name: k_city
          in: query
          description: 'Key of the city.'
          schema:
            type: string
        - name: k_city_replace
          in: query
          description: 'Key of the city to use instead of removed city. Is used only, when you remove some record.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_removed:
                        type: object
                        description: '`true` if city was removed, `false` if there is no permission to remove city in this environment. See more {@link RemoveView::CAN_REMOVE}.'
      tags:
        - City-City
  '/Core/Geo/Region/Region.json':
    get:
      summary: 'Retrieves information about countries and regions. Populates country and region dropdowns in address forms. Returns one or all countries together with their subordinate regions (states, provinces, territories). Pass a locale to get only the country relevant to a given market, or set `$is_locale_all` to retrieve the full list for multi-country UIs.'
      parameters:
        - name: id_locale
          in: query
          description: 'The locale ID to find regions for. One of the {@link LocaleSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_locale_all
          in: query
          description: 'Determines whether to get regions for all locales. If `true`, this will get regions for all locales. Otherwise, this will be `false`.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_region:
                        type: array
                        description: 'A list of regions grouped by their country. <dl> <dt>string[] `a_region`</dt> <dd> A list of regions in the country. Every element has the next keys: <dl> <dt>string `k_geo`</dt> <dd>The region key.</dd> <dt>string `s_title`</dt> <dd>The name of the region.</dd> </dl> </dd> <dt>int `id_locale`</dt> <dd>The locale ID of the country. One of the {@link \Core\Locale\LocaleSid} constants.</dd> <dt>string `k_geo_country`</dt> <dd>The country key.</dd> <dt>string `s_abbr`</dt> <dd>The country abbreviation.</dd> <dt>string `s_title`</dt> <dd>The name of the country.</dd> </dl>'
      tags:
        - Region-Region
  '/Core/Tool/Combobox/ToolCombobox.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data_additional:
                  type: array
                  description: 'A data additional. Various parameters for comboboxes that depend on the specific combobox.'
                a_filter:
                  type: array
                  description: 'A filter. Various parameters for comboboxes that depend on the specific combobox.'
                cid:
                  type: [number, "null"]
                  description: 'CID of a combobox class. `null` until initialized. One of {@link ToolComboboxAbstract} subclasses.'
                s_id:
                  type: string
                  description: 'ID of the combobox.'
                s_name:
                  type: string
                  description: 'Combobox field name.'
                s_value:
                  type: string
                  description: 'Value to search.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'Search result. The structure of the result depends on the specific combobox.'
                      i_count:
                        type: number
                        format: int32
                        description: 'Count of non-service items in the list.'
                      s_comment_class:
                        type: string
                        description: 'CSS class for the comment.'
                      s_id:
                        type: string
                        description: 'ID of the combobox.'
      tags:
        - Combobox-ToolCombobox
  '/Core/Tool/Combobox/ToolComboboxService.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data_additional:
                  type: array
                  description: 'A data additional. Various parameters for comboboxes that depend on the specific combobox.'
                a_filter:
                  type: array
                  description: 'Filter data. Various parameters for comboboxes that depend on the specific combobox.'
                cid:
                  type: [number, "null"]
                  description: 'CID of a combobox class. `null` until initialized. One of {@link ToolComboboxAbstract} subclasses.'
                s_id:
                  type: string
                  description: 'ID of the combobox.'
                s_key:
                  type: string
                  description: 'Key of the combobox.'
                s_name:
                  type: string
                  description: 'The name of the combobox.'
                s_value:
                  type: string
                  description: 'Value of combobox.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_comment:
                        type: string
                        description: 'Title for input field.'
                      s_comment_class:
                        type: string
                        description: 'CSS class for the comment.'
                      s_key:
                        type: string
                        description: 'Key of the combobox.'
                      s_value:
                        type: string
                        description: 'Value of combobox.'
      tags:
        - Combobox-ToolComboboxService
  '/Core/Cache/CachePhp/Invalidate.json':
    get:
      summary: ''
      parameters:
        - name: cid_class
          in: query
          description: 'CID of a PHP cache file to invalidate. One of {@link CachePhpAbstract} subclasses.'
          schema:
            type: number
            format: int32
        - name: s_hash
          in: query
          description: 'Protection hash.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CachePhp-Invalidate
  '/Core/Sms/Status/SmsStatus.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_error_code:
                  type: [number, "null"]
                  description: 'Error code returned, if sending failed.'
                s_id_message:
                  type: string
                  description: 'Unique identifier of the message.'
                s_message_status:
                  type: string
                  description: 'The message status.'
                text_error:
                  type: [string, "null"]
                  description: 'Error message, if sending failed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Status-SmsStatus
  '/Core/Sms/Response/ResponseTwilio.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_id_message:
                  type: string
                  description: 'Unique identifier of the message.'
                text_body:
                  type: string
                  description: 'Body of the SMS.'
                text_phone_from:
                  type: string
                  description: 'The recipient phone number.'
                text_phone_to:
                  type: string
                  description: 'The sender phone number.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-ResponseTwilio
  '/Core/Drive/ImageUpload/ImageUpload.json':
    get:
      summary: 'Returns information about the image. Used to initialize an image upload widget for an existing entity. Returns thumbnail and full-size URLs, upload endpoint, dimension constraints, and whether deletion is allowed, so the frontend can render the current image and offer upload or delete actions.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - ImageUpload-ImageUpload
    post:
      summary: 'Loads image information for a list of IDs. The POST method is used instead of the GET method because the maximum permitted URI length is restricted. Bulk variant of the GET method for pages that must display many images at once. Accepts a JSON-encoded list of entity IDs and returns the same metadata as the single-item GET, loading all images in one round-trip to avoid N+1 requests.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_id:
                  type: [string, "null"]
                  description: 'A list of image IDs set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}, serialized using JSON. This will be `null` if there''s no data being sent by the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image information for every ID. <i>Every element contains values such as the {@link ImageUploadApi::_get()} result.</i> <dl> <dt>array `a_text_empty`</dt> <dd> Information about the text on the empty upload image. <i>See: {@link \AImageUpload::textEmpty()}.</i> <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl> </dd> <dt>int `i_height_max`</dt> <dd>The maximum height of the image.</dd> <dt>int `i_height_min`</dt> <dd>The minimum height of the image.</dd> <dt>int `i_thumbnail_height`</dt> <dd>The height of the thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_HEIGHT}.</i></dd> <dt>int `i_thumbnail_width`</dt> <dd>The width of thumbnail image. <i>A copy of {@link \AImageUpload::THUMBNAIL_WIDTH}.</i></dd> <dt>int `i_width_max`</dt> <dd>The maximum width of image.</dd> <dt>int `i_width_min`</dt> <dd>The minimum width of image.</dd> <dt>bool `is_circular`</dt> <dd><tt>true</tt> if image is treated as circular; <tt>false</tt> otherwise.</dd> <dt>bool `is_delete_allow`</dt> <dd><tt>true</tt> if image deleting is allowed; <tt>false</tt> otherwise.</dd> <dt>string `s_link`</dt> <dd>The image link.</dd> <dt>string `url_empty`</dt> <dd>The URL of the image that should be shown in a case image is not uploaded.</dd> <dt>string|null `url_thumbnail`</dt> <dd>The URL of thumbnail of the image. <tt>null</tt> if image is not uploaded.</dd> <dt>string `url_upload`</dt> <dd>The URL the script where new image should be uploaded.</dd> <dt>string|null `url_view`</dt> <dd>The URL of the full image. <tt>null</tt> if image is not uploaded.</dd> </dl>'
      tags:
        - ImageUpload-ImageUpload
    put:
      summary: 'Updates the image. Applies a command (such as delete or replace) to a previously uploaded image, then returns the refreshed image metadata so the frontend can update the widget state without a separate GET call.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: is_temporary
          in: query
          description: 'If `true`, the temporary image will be retrieved. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The image ID set in {@link \Core\Drive\ImageUpload\ImageUploadApi::$s_class}.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_command:
                  type: string
                  description: 'The action that must be performed to the image.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_text_empty:
                        type: [array, "null"]
                        description: 'Information about the text for an empty image upload. <dl> <dt>string `s_class`</dt> <dd>Class to change view of the upload form.</dd> <dt>string `s_text`</dt> <dd>Text to replacing.</dd> </dl>'
                      html_image_hint:
                        type: string
                        description: 'An HTML string to use for the image recommendation.'
                      i_height_max:
                        type: [number, "null"]
                        description: 'The maximum height of image.'
                      i_height_min:
                        type: [number, "null"]
                        description: 'The minimum height of image.'
                      i_thumbnail_height:
                        type: [number, "null"]
                        description: 'The height of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_thumbnail_width:
                        type: [number, "null"]
                        description: 'The width of the thumbnail image. This will be `null` until it''s loaded from the server.'
                      i_width_max:
                        type: [number, "null"]
                        description: 'The maximum width of the image.'
                      i_width_min:
                        type: [number, "null"]
                        description: 'The minimum width of the image.'
                      is_circular:
                        type: [boolean, "null"]
                        description: 'If `true`, the image is treated as circular. Otherwise, this will be `false`.'
                      is_delete_allow:
                        type: [boolean, "null"]
                        description: 'If `true`, image deletion is permitted. Otherwise, this will be `false`. This will be `null` until it''s loaded from the server.'
                      s_code:
                        type: [string, "null"]
                        description: 'The link protection code.'
                      s_link:
                        type: [string, "null"]
                        description: 'The image link.'
                      url_empty:
                        type: [string, "null"]
                        description: 'The image URL that should be displayed in cases where no image is uploaded. This will be `null` until it''s loaded from the server.'
                      url_thumbnail:
                        type: [string, "null"]
                        description: 'The thumbnail URL of the image. This will be `null` until it''s loaded from the server.'
                      url_upload:
                        type: [string, "null"]
                        description: 'The script URL where new image should be uploaded from.'
                      url_view:
                        type: [string, "null"]
                        description: 'The URL of the full image.'
      tags:
        - ImageUpload-ImageUpload
  '/Core/Drive/ImageUpload/Variant.json':
    get:
      summary: 'Returns information about image.'
      parameters:
        - name: i_rotate
          in: query
          description: 'Angle to rotate image. Positive - counterclockwise. Negative - clockwise.'
          schema:
            type: number
            format: int32
        - name: s_code
          in: query
          description: 'Protection string. Used to prevent custom link generation on client side.'
          schema:
            type: string
        - name: s_link
          in: query
          description: 'Image link.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Information about image. <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to <tt>false</tt>, value returned in <var>url-thumbnail</var> equals value in <var>url-view</var>. </dd> <dt>string `url-view`</dt> <dd>Url to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger then specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise link to original image is returned here. </dd> </dl>'
      tags:
        - ImageUpload-Variant
  '/Core/Drive/ImageUpload/ImageUploadTemporary.json':
    post:
      summary: 'Saves new uploaded image. First step of the two-step image upload flow. Stores the uploaded file in temporary drive storage and returns preview URLs and dimension metadata so the user can review or crop the image before it is permanently saved. Call {@link ImageUploadApi} to commit the temporary image to the entity.'
      parameters:
        - name: k_id
          in: query
          description: 'The key of the image within {@link ImageUploadTemporaryApi::$s_class}. For example, for a user''s photo, specify the user''s key here.'
          schema:
            type: string
        - name: s_class
          in: query
          description: 'The name of the class that manages this image. For example, for a user''s photo, specify the string `PassportLoginImage` here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                f_image:
                  type: object
                  description: 'The image to be uploaded.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_crop:
                        type: boolean
                        description: 'If `true`, a crop is used. Otherwise, this will be `false` if a crop isn''t used (the image is saved in its original size).'
                      i_height:
                        type: number
                        format: int32
                        description: 'The actual height of the thumbnail image.'
                      i_height_src:
                        type: number
                        format: int32
                        description: 'The height of the original image.'
                      i_rotate:
                        type: number
                        format: int32
                        description: 'The angle of the image rotation compared to the original.'
                      i_width:
                        type: number
                        format: int32
                        description: 'The actual width of the thumbnail image.'
                      i_width_src:
                        type: number
                        format: int32
                        description: 'The width of the original image.'
                      id_type_src:
                        type: number
                        format: int32
                        description: 'The image type ID. One of the {@link \Core\Drive\DriveTypeSid} constants.'
                      is_resize:
                        type: boolean
                        description: 'If `true`, the thumbnail is a resized variant of the original image.'
                      url_thumbnail:
                        type: string
                        description: 'The URL to the resized and rotated image in file storage.'
                      url_view:
                        type: string
                        description: 'The URL to the original image in file storage.'
      tags:
        - ImageUpload-ImageUploadTemporary
  '/Core/Drive/ImageUpload/ImageCopyTemporary.json':
    post:
      summary: 'Makes a temporary copy of the specified image.'
      parameters:
        - name: a_config
          in: query
          description: 'Allows to give custom parameters which can be required for different types of images.'
          schema:
            type: array
        - name: s_link
          in: query
          description: 'Image link.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_crop:
                        type: boolean
                        description: '<tt>true</tt> - crop is used. <tt>false</tt> - crop is not used (the image is saved in the original size).'
                      i_height:
                        type: number
                        format: int32
                        description: 'Actual height of thumbnail image.'
                      i_height_src:
                        type: number
                        format: int32
                        description: 'Height of original image.'
                      i_rotate:
                        type: number
                        format: int32
                        description: 'Angle on which image was rotated compared to the original.'
                      i_width:
                        type: number
                        format: int32
                        description: 'Actual width of thumbnail image.'
                      i_width_src:
                        type: number
                        format: int32
                        description: 'Width of original image.'
                      id_type_src:
                        type: number
                        format: int32
                        description: 'Image type ID. One of {@link \Core\Drive\DriveTypeSid} constants.'
                      is_resize:
                        type: boolean
                        description: '<tt>true</tt> if thumbnail is a resized variant of original image.'
                      s_link:
                        type: string
                        description: 'Image link.'
                      url_thumbnail:
                        type: string
                        description: 'URL to resized and rotated image in file storage.'
                      url_view:
                        type: string
                        description: 'URL to original image in file storage.'
      tags:
        - ImageUpload-ImageCopyTemporary
  '/Core/Session/Limit/SessionLimit.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Limit-SessionLimit
  '/Core/Async/QueueServer/StatWorker.json':
    get:
      summary: 'Returns complete list of all workers in the system.'
      parameters:
        - name: is_all_scopes
          in: query
          description: 'Whether show all scopes. `true` - all scopes are shown, and scope search is performed by string. `false` - only the current scope is shown.'
          schema:
            type: boolean
        - name: s_provider
          in: query
          description: 'Name of a provider which information should be shown.'
          schema:
            type: string
        - name: s_scope_search
          in: query
          description: 'Search substring for scopes. Empty string - without filtration.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_worker:
                        type: array
                        description: 'A list of workers. <dl> <dt>string[] `a_function`</dt> <dd>A list of functions that this worker listens to.</dd> <dt>int `id_region`</dt> <dd> Datacenter region ID to which the worker belongs. Example: If the full name is `prod#monolith#2/10.2.164.70/3`, the region is `2`. `0` if there is no division into regions. </dd> <dt>string `ip_queue`</dt> <dd>IP address of the queue server that has provided information about this worker.</dd> <dt>string `ip_worker`</dt> <dd>IP address of the worker server.</dd> <dt>string `s_id`</dt> <dd> ID of the worker. This ID uniquely identifies a running thread within specified queue server. Example: `prod#monolith#2/10.2.164.70/3`. </dd> <dt>string `s_id_short`</dt> <dd> ID of the worker without an scope. Example: If the full name is `prod#monolith#2/10.2.164.70/3`, the short name is `10.2.164.70/3`. Empty string if the full name does not contain a scope. </dd> <dt>string `s_microservice`</dt> <dd> Microservice to which worker belongs, one of {@link \Core\All\MicroserviceList} constant. Example: If the full name is `prod#monolith#2/10.2.164.70/3`, the microservice is `monolith`. Empty string if the full name does not contain a microservice. </dd> <dt>string `s_scope`</dt> <dd> Async task scope. See the documentation for details in the method {@link \Core\Async\Provider\AsyncProviderConfig::scopeGet()}. Example: If the full name is `prod#monolith#2/10.2.164.70/3`, the scope is `prod#monolith#2/`. Empty string if the full name does not contain a scope. </dd> <dt>string `s_world`</dt> <dd> World to which worker belongs. Example: If the full name is `prod#monolith#2/10.2.164.70/3`, the world is `prod`. Empty string if the full name does not contain a world. </dd> </dl>'
      tags:
        - QueueServer-StatWorker
  '/Core/Async/QueueServer/QueueServerList.json':
    get:
      summary: 'Returns a list of queue servers.'
      parameters:
        - name: s_provider
          in: query
          description: 'Name of a provider which information should be shown.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_queue:
                        type: array
                        description: 'A list of queue servers. <dl> <dt>int `i_count`</dt> <dd>Number of times this status was approved.</dd> <dt>int `id_status`</dt> <dd>Status of the queue server. One of {@link \Core\Async\QueueServer\QueueServerStatusSid} constants.</dd> <dt>string `ip`</dt> <dd>IP address of the server.</dd> </dl>'
      tags:
        - QueueServer-QueueServerList
  '/Core/Async/QueueServer/StatFunction.json':
    get:
      summary: 'Returns complete list of all functions in the system.'
      parameters:
        - name: is_all_scopes
          in: query
          description: 'Whether show all scopes. `true` - all scopes are shown, and scope search is performed by string. `false` - only the current scope is shown.'
          schema:
            type: boolean
        - name: s_provider
          in: query
          description: 'Name of a provider which information should be shown.'
          schema:
            type: string
        - name: s_scope_search
          in: query
          description: 'Search substring for scopes. Empty string - without filtration.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_function:
                        type: array
                        description: 'A list of functions. <dl> <dt>int `i_queue`</dt> <dd>Number of tasks in the queue.</dd> <dt>int `i_run`</dt> <dd>Number of tasks currently running.</dd> <dt>int `i_worker`</dt> <dd>Number of workers that listen for this function.</dd> <dt>string `ip_queue`</dt> <dd>IP address of the queue server that has provided information about this function.</dd> <dt>string `s_class`</dt> <dd>Class name of the function.</dd> <dt>string|null `s_class_type`</dt> <dd> Type of the async function. Possible values: {@link \Core\Async\QueueServer\StatFunctionApi::DETACHED} or {@link \Core\Async\QueueServer\StatFunctionApi::FOREGROUND}. `null` if type could not be determined. </dd> <dt>string `s_function`</dt> <dd> Fully qualified name of the function (as registered in Gearman). Example: `prod#monolith#2/127.1.1.0/1/643`. </dd> <dt>string `s_function_short`</dt> <dd> Name of the function without a scope. Example: If the full name is `prod#monolith#2/127.1.1.0/1/643`, the short name is `127.1.1.0/1/643`. </dd> <dt>string `s_scope`</dt> <dd> Async function scope. See the documentation for details in the method {@link \Core\Async\Provider\AsyncProviderConfig::scopeGet()}. Example: If the full name is `prod#monolith#2/127.1.1.0/1/643`, the scope is `prod#monolith#2/`. </dd> </dl>'
      tags:
        - QueueServer-StatFunction
  '/Core/Async/QueueServer/Disable.json':
    post:
      summary: 'Disables or activates a queue server.'
      parameters:
        - name: s_provider
          in: query
          description: 'Name of a provider which queue server should be disabled or activated.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                ip_queue:
                  type: string
                  description: 'IP address of a queue server that should be disabled or activated.'
                is_active:
                  type: boolean
                  description: 'Whether this queue server should be disabled or activated. <tt>true</tt> to activate this queue server. <tt>false</tt> to disable this queue server.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - QueueServer-Disable
  '/Core/Async/Service/Activate.json':
    post:
      summary: 'Activates or deactivates a worker server.'
      parameters:
        - name: s_provider
          in: query
          description: 'Name of a provider which worker server should be activated or deactivated.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_region:
                  type: [number, "null"]
                  description: 'Datacenter region ID to which the worker server belongs. One of the {@link AmazonRegionSid} constants. `null` if there is no division into regions or current region.'
                ip_worker:
                  type: string
                  description: 'IP address of a worker server that should be activated or deactivated.'
                is_active:
                  type: string
                  description: 'Whether this worker server should be activated or deactivated. <tt>true</tt> to activate this worker server. <tt>false</tt> to deactivate this worker server.'
                s_microservice:
                  type: [string, "null"]
                  description: 'Microservice to which worker server belongs, one of {@link MicroserviceList} constant. `null` if current microservice.'
                s_world:
                  type: [string, "null"]
                  description: 'World to which worker server belongs. See for details: {@link \Core\All\WorldList::currentWorld()}. `null` if current world.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-Activate
  '/Core/Async/Service/Delete.json':
    post:
      summary: 'Removes all tasks of specified function from task queue.'
      parameters:
        - name: s_provider
          in: query
          description: 'Name of a provider which queue should be purged.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                ip_queue:
                  type: string
                  description: 'IP address of a queue server at which queue should be purged. Empty string to purge all available queue servers.'
                s_function:
                  type: string
                  description: 'Name of a function to purge.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-Delete
  '/Core/Async/Service/Restart.json':
    post:
      summary: 'Restarts a thread or all threads at worker server or at all worker servers.'
      parameters:
        - name: s_provider
          in: query
          description: 'Name of a provider which thread(s) should be restarted.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_region:
                  type: [number, "null"]
                  description: 'Datacenter region ID to which the worker belongs. One of the {@link AmazonRegionSid} constants. `null` if there is no division into regions.'
                ip_queue:
                  type: string
                  description: 'IP address of a queue server which worker threads should be restarted. An empty string to restart worker threads at all queue servers.'
                ip_worker:
                  type: string
                  description: 'IP address of a worker at which all threads should be restarted. This variable is required if {@link \Core\Async\Service\RestartApi::$is_all} is <tt>false</tt> and {@link \Core\Async\Service\RestartApi::$s_thread} is not specified, otherwise this variable is not allowed.'
                is_all:
                  type: boolean
                  description: 'Whether all threads at all worker servers should be restarted. This variable should be set to `true` if neither {@link \Core\Async\Service\RestartApi::$ip_worker} nor {@link \Core\Async\Service\RestartApi::$s_thread} specified, otherwise this variable should be set to `false`.'
                s_microservice:
                  type: [string, "null"]
                  description: 'Microservice to which worker belongs, one of {@link MicroserviceList} constant. `null` if value is not defined.'
                s_thread:
                  type: string
                  description: 'ID of a thread to restart. This variable is required if {@link \Core\Async\Service\RestartApi::$is_all} is <tt>false</tt> and {@link \Core\Async\Service\RestartApi::$ip_worker} is not specified, otherwise this variable is not allowed.'
                s_world:
                  type: [string, "null"]
                  description: 'World to which worker belongs. See for details: {@link \Core\All\WorldList::currentWorld()}. `null` if search should be performed in all worlds.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-Restart
  '/Core/Debug/Run/Kill.json':
    delete:
      summary: 'Sends signal to exit to specified processes.'
      parameters:
        - name: a_id
          in: query
          description: 'A list of process IDs to kill.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Run-Kill
  '/Core/Docs/Search/DocsSearch.json':
    get:
      summary: 'Performs search and returns list of found records.'
      parameters:
        - name: i_limit
          in: query
          description: 'Max count of records to be returned. `null` means to use default limit.'
          schema:
            type: [number, "null"]
        - name: text_search
          in: query
          description: 'Search text.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_docs:
                        type: array
                        description: 'List of found docs.'
      tags:
        - Search-DocsSearch
  '/Core/Google/Captcha/GoogleCaptcha.json':
    put:
      summary: 'Saves the user CAPTCHA token for the current session. Accepts the CAPTCHA version, the action name, and the user token obtained from the Google reCAPTCHA widget, and stores them in the session so that subsequent API requests requiring CAPTCHA verification can use them.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_version:
                  type: number
                  format: int32
                  description: 'Captcha version ID.'
                text_action:
                  type: string
                  description: 'The action name. Used to determine the place where the CAPTCHA is needed in documentation for endpoints that used the CAPTCHA.'
                text_token:
                  type: string
                  description: 'The user token CAPTCHA.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Captcha-GoogleCaptcha
  '/Core/Google/Captcha/CaptchaScore.json':
    get:
      summary: 'Returns the overridden score for the reCAPTCHA v3. Returns the current session-level score override value that was set via the PUT method. Requires reCAPTCHA v3 to be enabled; throws an exception otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_score:
                        type: [number, "null"]
                        description: 'Overridden score value for V3 captcha. `null` to reset override.'
      tags:
        - Captcha-CaptchaScore
    post:
      summary: 'Validates the reCAPTCHA v3 token. Accepts a reCAPTCHA v3 user token, sends it to Google for verification, and returns a response ID indicating the outcome. Note that each token can only be validated once; a new token must be generated for subsequent requests.'
      parameters:
        - name: text_token
          in: query
          description: 'The user token CAPTCHA from {@link CaptchaVersionSid::V3} captcha. Be careful when use this endpoint for validate token, because token can be used only once, so if you validate token in this endpoint, you must generate new token for next requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_response:
                        type: number
                        format: int32
                        description: 'Captcha response ID.'
      tags:
        - Captcha-CaptchaScore
    put:
      summary: 'Overrides the score for the reCAPTCHA v3. Sets a custom score value in the range 0.0-1.0 to override the actual reCAPTCHA v3 score for the current session. Pass `null` to clear the override and restore the default behavior. Requires the score override feature to be enabled.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                f_score:
                  type: [number, "null"]
                  description: 'Overridden score value for V3 captcha. `null` to reset override.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Captcha-CaptchaScore
  '/Core/Server/ApcClear/ApcClear.json':
    post:
      summary: 'Deletes a key from APC cache.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_key:
                  type: array
                  description: 'A list of keys to delete from APC cache at all servers in the cluster. Note that values stored here are raw keys, i.e. they are copies of results of {@link \CmsCache::key()}, arguments of {@link \apcu_delete()}.'
                s_cache:
                  type: string
                  description: 'Type of the cache served by this API: * `apc` to delete from APC cache; * `yac` to delete from YAC cache. Copy of argument of {@link CoreCache::create()}.'
                s_hash:
                  type: string
                  description: 'Signature to ensure that the request originates from {@link ApcClearAsync::run()}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ApcClear-ApcClear
  '/Core/Server/ApcClear/ApcClearChannel.json':
    get:
      summary: 'Deletes a key from APC cache.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ApcClear-ApcClearChannel
  '/Core/Jira/AI/JiraTaskRequest.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_key:
                  type: string
                  description: 'The endpoint for Jira task requests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: string
                        description: 'The response message generated'
      tags:
        - AI-JiraTaskRequest
  '/Core/Jira/AI/JiraRequest.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_email:
                  type: string
                  description: 'The email address associated with the request'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: string
                        description: 'The response message generated'
      tags:
        - AI-JiraRequest
  '/Core/Jira/AI/JiraResultWork.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_date_resolved:
                  type: string
                  description: 'The starting data for the Jira result work.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: string
                        description: 'The response message generated'
      tags:
        - AI-JiraResultWork
  '/Social/Facebook/Login/Login.json':
    post:
      summary: 'Authorizes user with facebook. Accepts a Facebook access token and an optional application ID. If the user is already signed in, links the Facebook account to their existing account; otherwise, signs them in or creates a new account using the Facebook identity and the configured Facebook credentials.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link FacebookCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link FacebookCredential::CID}. When application passed, the credential loaded by application id.'
                s_token:
                  type: string
                  description: 'The Facebook token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-Login
  '/Social/Facebook/Login/LoginBind.json':
    delete:
      summary: 'Deletes link to Facebook profile.'
      parameters:
        - name: i_facebook
          in: query
          description: 'User ID in Facebook.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-LoginBind
    get:
      summary: 'Checks if user''s profile is bound to Facebook.'
      parameters:
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_facebook:
                        type: string
                        description: 'User ID in Facebook.'
                      is_exists:
                        type: boolean
                        description: '<tt>true</tt> - profile is bound to Facebook; <tt>false</tt> - otherwise.'
      tags:
        - Login-LoginBind
    post:
      summary: 'Binds user''s profile to Facebook.'
      parameters:
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_token:
                  type: string
                  description: 'Facebook token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-LoginBind
  '/Social/Apple/Login/AppleLogin.json':
    delete:
      summary: 'Removes the association between a website client and the Apple account. Invalidates the tokens and associated client authorizations. Requires the user to be signed in. Accepts the application ID, retrieves the Apple sign-in certificate for that application, and unlinks the Apple account from the current user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-AppleLogin
    post:
      summary: 'Signs user in with Apple. Accepts the application ID, the Apple authorization code, and optional first and last name values. If the user is already signed in, binds the Apple account to their existing account; otherwise, signs them in or creates a new account using the Apple identity.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_application:
                  type: string
                  description: 'The application ID.'
                text_authorization:
                  type: string
                  description: 'The authorization code.'
                text_name_first:
                  type: string
                  description: 'User''s first name.'
                text_name_last:
                  type: string
                  description: 'User''s last name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-AppleLogin
  '/Social/Google/Plus/Login.json':
    post:
      summary: 'Authorizes user with Google. Accepts a Google server authorization code and an optional application ID, loads the configured Google credential for the application, and completes the OAuth 2.0 flow to sign the user in or create a new account.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link GoogleCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link GoogleCredential::CID}. When application passed, the credential loaded by application id.'
                s_code:
                  type: string
                  description: 'The Google server authorization code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Plus-Login
  '/Social/Twitter/Login/Login.json':
    delete:
      summary: 'Deletes link to Twitter profile.'
      parameters:
        - name: i_twitter
          in: query
          description: 'User ID in Twitter.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-Login
    get:
      summary: 'Checks if user''s profile is linked to Twitter.'
      parameters:
        - name: uid
          in: query
          description: 'User''s primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_twitter:
                        type: string
                        description: 'User ID in Twitter.'
                      is_exists:
                        type: boolean
                        description: '<tt>true</tt> - profile is linked to Twitter; <tt>false</tt> - otherwise.'
      tags:
        - Login-Login
    post:
      summary: 'Authorizes user with twitter.'
      parameters:
        - name: i_twitter
          in: query
          description: 'User ID in Twitter.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_secret:
                  type: string
                  description: 'Twitter secret key.'
                s_token:
                  type: string
                  description: 'Twitter token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-Login
  '/Wl/Visit/Receipt/ReceiptMail.json':
    post:
      summary: 'Performs data checks, checks the availability of the mail, as well as the access policy. If the checks are successful, the mail will be sent, otherwise the client will see an error.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business in which the visit is located. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'The visit key for which the menu buttons will be generated. Primary key in the {@link \RsVisitSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Receipt-ReceiptMail
  '/Wl/Visit/Blame/Check.json':
    get:
      summary: 'Gets visit status.'
      parameters:
        - name: id_visit
          in: query
          description: 'Visit status. One of {@link VisitSid} constants.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit ID to get status for. Primary key in {@link \RsVisitSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_fee_amount:
                        type: [string, "null"]
                        description: 'Amount to be charged to the user. `null` if specified visit is not the blame, according business late cancel and no shows rules.'
                      is_blame:
                        type: boolean
                        description: 'Blame status of the visit. `true` if specified visit, according business late cancel and no shows rules, is blamed.'
                      show_confirm:
                        type: boolean
                        description: 'Whether display the confirmation modal. `true` the confirmation modal must be shown if specified visit, according business late cancel and no shows rules, is blamed and the current user has the privilege to decide whether to charge a penalty or not. `false` - do not show the confirmation modal.'
      tags:
        - Blame-Check
  '/Wl/Visit/Pay/PayChange.json':
    get:
      summary: 'Returns data to change visit pay option.'
      parameters:
        - name: is_pay_now
          in: query
          description: 'Defines whether ''pay now'' option should be present.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Empty value means not set.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Class period key. Empty value means not set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Current user ID. Empty value means not set.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of purchase options that can be applied to pay for visit: <dl> <dt>array `a_logo`</dt> <dd>Promotion logo, see result of {@link \RsPromotionImageLogo::data()}. Empty array for not paid option.</dd> <dt>bool `is_select`</dt> <dd>Whether current element selected in the list.</dd> <dt>string `s_key`</dt> <dd> <tt>0</tt> means ''Single buy'', <tt>-1</tt> means ''Not paid''. Otherwise key with next structure: <tt>k_login_promotion:[k_login_promotion]</tt> or <tt>k_session_pass:[k_session_pass]</tt>. </dd> <dt>string `text_title`</dt> <dd>Title of select option.</dd> </dl>'
      tags:
        - Pay-PayChange
    post:
      summary: 'Saves user''s promotion for certain attendance.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Empty value means not set.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Class period key. Empty value means not set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Current user ID. Empty value means not set.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_key:
                  type: string
                  description: 'Selected pay option to save. The value with one of next structures: * <tt>k_login_promotion</tt>:<tt>[k_login_promotion]</tt> * <tt>k_session_pass</tt>:<tt>[k_session_pass]</tt>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_change:
                        type: boolean
                        description: 'Whether changes applied to visit (if user selected the same promotion, we should do nothing).'
      tags:
        - Pay-PayChange
  '/Wl/Visit/VisitHistoricalCount/VisitHistoricalCount.json':
    post:
      summary: 'Change value of historical visit.'
      parameters:
        - name: i_visit
          in: query
          description: 'Count of visits.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - VisitHistoricalCount-VisitHistoricalCount
  '/Wl/Catalog/CatalogList/Element.json':
    get:
      summary: 'Retrieves an information about current sale item. Used to render the detail view of a single store item (promotion, product, event, or coupon) in the client-facing catalog. Returns everything needed to display the item: price, taxes, images, description, booking restrictions, and available purchase options.'
      parameters:
        - name: a_discount_code
          in: query
          description: 'Information about the discount code: <dl> <dt>string `f_amount`</dt> <dd>The fixed amount of the discount.</dd> <dt>float `f_percent`</dt> <dd>The percentage amount of the discount.</dd> <dt>int `i_limit`</dt> <dd>Limitation.</dd> <dt>string `k_discount_code`</dt> <dd>The discount code key.</dd> <dt>string `s_discount_code`</dt> <dd>The discount code value.</dd> </dl>'
          schema:
            type: array
        - name: a_sale_id_group
          in: query
          description: 'The list of items grouped by sale categories on the store page. Keys refer to sale IDs from {@link \RsSaleSid}, and values refer to data to identify an item: <dl> <dt>int `id_sale`</dt> <dd>The item category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The primary key of item.</dd> <dt>string `k_shop_product_option`</dt> <dd> The product option or <tt>0</tt> for any other cases. <i>Primary key in {@link \RsShopProductOptionSql} table.</i> </dd> </dl>'
          schema:
            type: array
        - name: dl_client_prorate
          in: query
          description: 'The client prorate date. This will be `null` in cases where the client prorate date hasn''t passed.'
          schema:
            type: [string, "null"]
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_promotion_image_height
          in: query
          description: 'The promotion image height in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_promotion_image_width
          in: query
          description: 'The promotion image width in pixels. Specify this value if you need the image to be returned in a specific size. The returned image will have default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: id_sale
          in: query
          description: 'The ID of item category. One of the {@link \RsSaleSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'Determines whether the API is called in the backend mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The item key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'The product option key. This will be `null` if not set yet.'
          schema:
            type: [string, "null"]
        - name: text_item
          in: query
          description: 'A list of goods to get information for. Every element must contain the next keys: Note that this must be serialized via JSON. If this field is specified, don''t specify any of the following fields: * {@link ElementApi::$id_sale} * {@link ElementApi::$k_id} * {@link ElementApi::$k_shop_product_option} This will be `null` to get information for only one item. <dl> <dt>int `id_sale`</dt> <dd>The ID of the item category. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_shop_product_option`</dt> <dd>The product option key. This will be <tt>0</tt> if the item isn''t a product.</dd> </dl>'
          schema:
            type: [string, "null"]
        - name: uid_customer
          in: query
          description: 'The UID of a customer (user) for whom the purchase is made. This is used in the backend to calculate discounts.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_age_restriction:
                        type: array
                        description: 'The age restriction configuration. Age restrictions for an item apply when they''re configured for a specific item and the API is requested from the backend or when age restriction are public. <dl> <dt>int|null `i_age_from`</dt> <dd>The minimum age permitted for the event. This will be `null` if a minimum age isn''t set or available.</dd> <dt>int|null `i_age_to`</dt> <dd>The maximum age permitted for the event. This will be `null` if a maximum age isn''t set or available.</dd> <dt>bool `is_age_public`</dt> <dd> If `true`, age restrictions are public and available. Otherwise, this will be `false` if they''re hidden. When restrictions are hidden and the current user isn''t a staff member, the age range will be empty. </dd> </dl>'
                      a_data:
                        type: array
                        description: 'Additional information specific for the item. The structure may be different depending on the item category. Consider the following examples: * For a product, this contains inventory information. * For a gift card, this contains possible amounts. * For a session pass/membership/package, this contains information about start and stop dates. <dl> <dt>int[] `a_service_access`</dt> <dd> Access to services for a purchase option. Keys are one of the {@link \Wl\Service\ServiceSid} constants, values are one of the {@link \AFlagSid} constants. Set only for relevant purchase option service category. {@link \AFlagSid::ON} access to some services. {@link \AFlagSid::OFF} no access to services. It can be set only for classes and events. {@link \AFlagSid::ALL} access to all services. It can be set only for classes and events. For purchase options with appointments and assets service category status is always {@link \AFlagSid::ON}. </dd> <dt>bool `is_renew_public`</dt> <dd> This applies only for promotions. `true` - clients can set promotion auto-renew. `false` - clients can''t set promotion auto-renew. </dd> <dt>array[] `a_component`</dt> <dd> This applies only for coupons. Coupon components information. Each element will contain the following keys: <dl> <dt>int `id_program`</dt> <dd>Program ID. One of {@link \RsProgramSid} ID''s. Only applies to promotions.</dd> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. One of {@link \RsPurchaseItemSid} ID''s.</dd> <dt>int `id_sale`</dt> <dd>Sale ID. One of {@link \RsSaleSid} ID''s.</dd> <dt>string `k_id`</dt> <dd>The identifier of the item.</dd> <dt>string `text_title`</dt> <dd>The title of the item.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> This applies to enrollment/event items. Staff list for class periods. Each element contains: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. <i>Primary key in {@link \RsStaffSql}.</i> Deprecated, use `uid_staff`.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `text_family`</dt> <dd>Staff last name.</dd> <dt>string `text_staff`</dt> <dd>Staff display name.</dd> </dl> </dd> <dt>string `dl_expire`</dt> <dd>Date of expiration of coupon, local date in MySQL format.</dd> <dt>string `dl_now`</dt> <dd>Current date, local date in MySQL format.</dd> <dt>string `dl_start`</dt> <dd> Date to activate the coupon on, local date in MySQL format. When `id_activation`=FIXED, this field contains a custom date to activate the coupon on, local date in MySQL format. </dd> <dt>int `i_duration`</dt> <dd>Number of periods the coupon is active. Type of a period is specified by `id_duration`.</dd> <dt>int `id_activation`</dt> <dd>Type of a coupon activation date specification. One of {@link \Wl\Coupon\Edit\ActivationSid} constants.</dd> <dt>int `id_duration`</dt> <dd>Duration of a period. A constant from {@link \ADurationSid}.</dd> <dt>int `id_duration_type`</dt> <dd>A way to specify a duration. One of {@link \Wl\Coupon\Edit\DurationTypeSid} constants.</dd> </dl>'
                      a_image:
                        type: array
                        description: 'Image information: <dl> <dt>int `i_height`</dt> <dd>The height in pixels.</dd> <dt>int `i_width`</dt> <dd>The width in pixels.</dd> <dt>bool `is_empty`</dt> <dd> `true` - the item has no image (in this case, ignore the other keys of this array). `false` - the item has an image. </dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl> Use {@link ElementApi::$a_image_list} instead.'
                      a_image_list:
                        type: array
                        description: 'List of images. Keys are index and value is below information: <dl> <dt>int `i_height`</dt> <dd>The height in pixels.</dd> <dt>int `i_width`</dt> <dd>The width in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` - item has no image (in this case ignore other keys of this array). `false` - item has an image.</dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl>'
                      a_installment_template:
                        type: array
                        description: 'A list of installment plans. Each element has the following next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by <var>id_period</var> between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency Key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql}.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl>'
                      a_item:
                        type: array
                        description: 'The list of information pertaining to the specified item. <dl> <dt>array `a_data`</dt> <dd> Contains additional data for the sale item. For Package, it contains also the following key: The same structure as {@link \Wl\Catalog\CatalogList\ElementApi::$a_data} has. <dl> <dt>bool `is_price_breakdown`</dt> <dd> Whether to display individual prices for each item in the package. `true` display individual prices for each item in the package, `false` display a single total price for the package. </dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Contains information about one image connected to a sale item. <dl> <dt>array `a_image`</dt> <dd> <dl> <dt>int `i_height`</dt> <dd>Height of the variant image.</dd> <dt>int `i_height_src`</dt> <dd>Height of the original image.</dd> <dt>int `i_rotate`</dt> <dd>Rotate.</dd> <dt>int `i_width`</dt> <dd>Width of the variant image.</dd> <dt>int `i_width_src`</dt> <dd>Width of the original image.</dd> <dt>int `id_type_src`</dt> <dd>Type ID of the image. Constant from {@link \Core\Drive\DriveTypeSid}.</dd> <dt>bool `is-resize`</dt> <dd>`true` if the variant differs from the original, `false` - otherwise.</dd> <dt>string `url-thumbnail`</dt> <dd>URL to download variant (thumbnail) file.</dd> <dt>string `url-view`</dt> <dd>URL to download original file.</dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of the variant image.</dd> <dt>int `i_width`</dt> <dd>Width of the variant image.</dd> <dt>bool `is_empty`</dt> <dd>`true` if the image file does not exist, `false` - otherwise.</dd> <dt>string `s_url`</dt> <dd>Link to the variant file.</dd> </dl> </dd> <dt>array[] `a_tax`</dt> <dd> Contains information about taxes. <i>The structure of this array is described in {@link \RsTax::$a_tax}.</i> <dl> <dt>float `f_tax`</dt> <dd>The calculated tax amount applied by this rule.</dd> <dt>string `f_tax_discount`</dt> <dd>The tax amount after applying all discounts.</dd> <dt>string `f_tax_discount_login`</dt> <dd>The tax amount after applying the client type discount only.</dd> <dt>float `f_value`</dt> <dd>The tax rate. Its meaning depends on `id_tax`.</dd> <dt>int `id_tax`</dt> <dd>The tax type. One of {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key. <i>Primary key in the {@link \RsTaxSql} table.</i></dd> <dt>string `s_tax`</dt> <dd>The tax name.</dd> </dl> </dd> <dt>int `id_purchase_option_view`</dt> <dd>The Purchase Option view type. One of the {@link \Wl\Catalog\PurchaseOptionViewSid} constants.</dd> <dt>string `m_discount_code`</dt> <dd>The discount code amount.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type.</dd> <dt>string `s_comment`</dt> <dd>Additional information about the sale item. For example, information about ''introductory offer''.</dd> <dt>string `s_price`</dt> <dd>The price of the sale item in a human-readable format.</dd> <dt>string `s_sale`</dt> <dd>The category title of the sale item.</dd> <dt>string `s_title`</dt> <dd>The title of the sale item.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'A list of the item''s taxes. Keys refer tax keys, and values refer to the amount of tax. <dl> <dt>float `f_tax`</dt> <dd>The calculated tax amount applied by this rule.</dd> <dt>string `f_tax_discount`</dt> <dd>The tax amount after applying all discounts.</dd> <dt>string `f_tax_discount_login`</dt> <dd>The tax amount after applying the client type discount only.</dd> <dt>float `f_value`</dt> <dd>The tax rate. Its meaning depends on `id_tax`.</dd> <dt>int `id_tax`</dt> <dd>The tax type. One of {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key. <i>Primary key in the {@link \RsTaxSql} table.</i></dd> <dt>string `s_tax`</dt> <dd>The tax name.</dd> </dl>'
                      f_price:
                        type: [string, "null"]
                        description: 'The price of the sale item.'
                      f_price_include:
                        type: [string, "null"]
                        description: 'The price of the sale item, including tax.'
                      f_price_retail_product:
                        type: string
                        description: 'The retail price of the product. This will be empty if this isn''t a product.'
                      f_price_total_enrollment:
                        type: string
                        description: 'Full price of event. This will be empty if this isn''t an event.'
                      f_tax:
                        type: [string, "null"]
                        description: 'The tax amount.'
                      html_description:
                        type: [string, "null"]
                        description: 'The sale item description.'
                      html_special:
                        type: [string, "null"]
                        description: 'Special instructions for the sale item.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'The purchase item category ID. One of the {@link \RsPurchaseItemSid} constants.'
                      id_purchase_option_view:
                        type: number
                        format: int32
                        description: 'The ID of the item view category. One of the {@link PurchaseOptionViewSid} constants.'
                      id_sale:
                        type: number
                        format: int32
                        description: 'The ID of item category. One of the {@link \RsSaleSid} constants.'
                      is_contract:
                        type: boolean
                        description: 'If `true`, the item requires a contract. Otherwise, this will be `false`.'
                      k_id:
                        type: string
                        description: 'The item key.'
                      k_shop_product_option:
                        type: [string, "null"]
                        description: 'The product option key. This will be `null` if not set yet.'
                      m_discount_code:
                        type: string
                        description: 'The discount amount for a discount code.'
                      m_discount_login:
                        type: string
                        description: 'The discount amount for a user''s type.'
                      m_price:
                        type: string
                        description: 'The price on the price tag.'
                      m_price_include:
                        type: string
                        description: 'The price, including taxes.'
                      m_tax:
                        type: string
                        description: 'The tax amount.'
                      s_comment:
                        type: string
                        description: 'Additional comment(s). For example, information about ''introductory offer''.'
                      s_price:
                        type: [string, "null"]
                        description: 'The price of the sale item in a human-readable format.'
                      s_sale:
                        type: [string, "null"]
                        description: 'The category title of the sale item.'
                      s_title:
                        type: [string, "null"]
                        description: 'The sale item title.'
                      text_price:
                        type: string
                        description: 'The price on the price tag, with the currency sign.'
                      text_sale:
                        type: string
                        description: 'The title of the item category.'
                      text_title:
                        type: string
                        description: 'The item title.'
                      xml_description:
                        type: [string, "null"]
                        description: 'A detailed description.'
                      xml_special:
                        type: [string, "null"]
                        description: 'Special instructions.'
      tags:
        - CatalogList-Element
  '/Wl/Catalog/CatalogList/List.json':
    get:
      summary: 'Retrieves a list of all sale items. Used to render the full client-facing store catalog for a business. Returns all purchasable items - promotions, products, events, and coupons - merged across the business''s categories. The separate de-duplicated list and category list support both the flat and category-tabbed views.'
      parameters:
        - name: a_direct_link
          in: query
          description: 'Arguments from direct purchase link, which can give additional access to products, which are available only by such link. <dl> <dt>int `id_sale`</dt> <dd>Type of the item from {@link \RsSaleSid}.</dd> <dt>bool `is_product`</dt> <dd> `true` if `k_id` value is product key, `false` if `k_id` value is product option key. Optional. Default value is `false`. Used for `id_sale` === {@link \RsSaleSid::PRODUCT} only. </dd> <dt>string `k_id`</dt> <dd>Key of item.</dd> </dl>'
          schema:
            type: array
        - name: is_credit_card_check
          in: query
          description: '`true` to consider the requirement to have a credit card for booking (this requirement can be set in business settings) `false` to ignore it.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of a location. If `0`, all products in the business are retrieved.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'The list of all sale items (de-duplicated). Each element has the following keys: <dl> <dt>string[] `a_location`</dt> <dd> The list of location keys where the sale item is available. <i>Primary keys in the {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_shop_category`</dt> <dd>Shop category keys the item belongs to. <i>Primary keys in the {@link \RsShopCategorySql} table.</i></dd> <dt>string `dtu_create`</dt> <dd>UTC creation date of the item in MySQL format.</dd> <dt>string `f_price`</dt> <dd>The item price.</dd> <dt>bool `hide_application`</dt> <dd>`true` if the item should be hidden from the application, `false` otherwise.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_direct`</dt> <dd>`true` if the item was accessed via a direct purchase link, `false` otherwise.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if the item is an introductory offer, `false` otherwise.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_shop_category`</dt> <dd> The primary shop category key for this item entry. <i>Primary key in the {@link \RsShopCategorySql} table.</i> </dd> <dt>string `s_key`</dt> <dd>The unique string key identifying the item (product key combined with its DB SID).</dd> <dt>string `s_title`</dt> <dd>The item title.</dd> <dt>string `url_catalog`</dt> <dd>Direct URL to the catalog view page for this item.</dd> </dl>'
                      a_product_duplicate:
                        type: array
                        description: 'The list of products to show with duplicates. Products have their own order within every shop category, and they can be related to several categories. As a design decision, products are sorted in the backend using duplicates for every category. <dl> <dt>string[] `a_location`</dt> <dd> The list of location keys where the sale item is available. <i>Primary keys in the {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_shop_category`</dt> <dd>Shop category keys the item belongs to. <i>Primary keys in the {@link \RsShopCategorySql} table.</i></dd> <dt>string `dtu_create`</dt> <dd>UTC creation date of the item in MySQL format.</dd> <dt>string `f_price`</dt> <dd>The item price.</dd> <dt>bool `hide_application`</dt> <dd>`true` if the item should be hidden from the application, `false` otherwise.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_direct`</dt> <dd>`true` if the item was accessed via a direct purchase link, `false` otherwise.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if the item is an introductory offer, `false` otherwise.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_shop_category`</dt> <dd> The primary shop category key for this item entry. <i>Primary key in the {@link \RsShopCategorySql} table.</i> </dd> <dt>string `s_key`</dt> <dd>The unique string key identifying the item (product key combined with its DB SID).</dd> <dt>string `s_title`</dt> <dd>The item title.</dd> <dt>string `url_catalog`</dt> <dd>Direct URL to the catalog view page for this item.</dd> </dl>'
      tags:
        - CatalogList-List
  '/Wl/Catalog/CatalogList/CatalogProduct.json':
    get:
      summary: 'Gets store products by shop category. Used to populate a specific product category tab in the client-facing store. Supports pagination so large categories can be loaded incrementally. Returns the products together with the sort order configured for the category and a cache key for client-side caching.'
      parameters:
        - name: a_filter
          in: query
          description: 'Additional data to filter products. <dl> <dt>array `a_quick_config`</dt> <dd> Additional data for cases where the catalog list is in Quick Buy mode. Contains: <dl> <dt>string `k_visit`</dt> <dd>The visit key used to filter quick-buy items.</dd> </dl> </dd> <dt>int[] `a_sale_id`</dt> <dd>The list of special categories for the catalogue. Each value is a constant of {@link \RsSaleSid}.</dd> <dt>string[] `a_shop_category`</dt> <dd>Shop category keys to filter by. <i>Primary keys in the {@link \RsShopCategorySql} table.</i></dd> <dt>int `i_last`</dt> <dd>The last shown product index for pagination.</dd> <dt>int `id_order`</dt> <dd>The sort order ID. One of the {@link \Core\Sid\SortOrderSid} constants.</dd> <dt>int `id_sort`</dt> <dd>The sort field ID. One of the {@link \Wl\Shop\Category\ShopCategorySortSid} constants.</dd> <dt>string `k_business`</dt> <dd>The business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `s_title`</dt> <dd>A title fragment to filter products by name.</dd> <dt>string `uid`</dt> <dd>The user key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: i_last
          in: query
          description: 'The currently shown element.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key to get products for.'
          schema:
            type: string
        - name: k_shop_category
          in: query
          description: 'The selected shop category. This will be `0` for the system''s Unassigned category or `null` if not set yet.'
          schema:
            type: string
        - name: s_cache_key
          in: query
          description: 'The cache key used to get products.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category_sort:
                        type: array
                        description: 'Categories with sort settings. Keys refer to shop category keys. Values refer to sort settings. Contains the following structure: <dl> <dt>int[] `a_order`</dt> <dd> The order of the products by category if {@link \Wl\Shop\Category\ShopCategorySortSid::CUSTOM} is selected. Each element is a product key string, ordered as desired. </dd> <dt>int `id_order`</dt> <dd>The sort order. One of the {@link \Core\Sid\SortOrderSid} constants.</dd> <dt>int `id_sort`</dt> <dd>The sort field ID. One of the {@link \Wl\Shop\Category\ShopCategorySortSid} constants.</dd> </dl>'
                      a_product:
                        type: array
                        description: 'The list of products. Each element has the following keys: <dl> <dt>string `html_price`</dt> <dd>HTML-escaped formatted price of the product.</dd> <dt>string `html_price_early`</dt> <dd>HTML-escaped formatted early-bird price of the product. Present only when an early price applies.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The product key.</dd> <dt>string `k_shop_category`</dt> <dd>The shop category key. <i>Primary key in the {@link \RsShopCategorySql} table.</i></dd> <dt>string `s_title`</dt> <dd>The product title.</dd> <dt>string `url_catalog`</dt> <dd>Direct link to the catalog view page for this product.</dd> </dl>'
                      i_last:
                        type: number
                        format: int32
                        description: 'The currently shown element.'
                      is_load_more:
                        type: boolean
                        description: 'Determines whether more products can be loaded.'
                      s_cache_key:
                        type: string
                        description: 'The cache key used to get products.'
      tags:
        - CatalogList-CatalogProduct
  '/Wl/Catalog/CatalogList/CatalogProductCheck.json':
    get:
      summary: 'Checking if client selected in a store when user in a backend'
      parameters:
        - name: a_data
          in: query
          description: 'Data array: <ul> <li>string <var>uid</var></li> <li>bool <var>is_backend</var></li> <li>string <var>k_business</var></li> </ul>'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_client_selected:
                        type: boolean
                        description: 'Whether more products can be loaded.'
                      s_message:
                        type: string
                        description: 'Message to show in a popup.'
      tags:
        - CatalogList-CatalogProductCheck
  '/Wl/Catalog/Cart/Cart.json':
    get:
      summary: 'Gets purchase cost data. Used in the client-facing checkout flow to show a live price summary before the client submits payment. Calculates the full breakdown - subtotal, taxes, applied discounts, reward redemptions, and tips - so the client can review the total before confirming.'
      parameters:
        - name: a_item
          in: query
          description: 'The list of items in the cart. <dl> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date. This will be `null` in cases where the client prorate date hasn''t passed.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type, one of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd> The sale item key. Note: <i>For products, this will be the product key from {@link \RsShopProductSql}.</i> You need to specify the product option key in the `k_shop_product_option` field or the first available option will be used automatically. </dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize used on item. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_shop_product_option`</dt> <dd> <i>The shop product option key from {@link \RsShopProductOptionSql}.</i> Note: Used for products only `id_sale` = {@link \RsSaleSid::PRODUCT}. If empty the first available option will be used automatically. </dd> </dl>'
          schema:
            type: array
        - name: is_auto_apply_prize
          in: query
          description: 'Whether selected login prize should be auto applied to first applicable item.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_login_prize
          in: query
          description: 'The login prize key.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code. If `null`, the discount code has been reset.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. This will be `0` for guests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount_item:
                        type: array
                        description: 'The list of discounts for each item. Keys refer to unique item keys, and values refer to discount amounts for the item.'
                      a_item:
                        type: array
                        description: 'The list of items in the cart. <dl> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date. This will be `null` in cases where the client prorate date hasn''t passed.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type, one of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd> The sale item key. Note: <i>For products, this will be the product key from {@link \RsShopProductSql}.</i> You need to specify the product option key in the `k_shop_product_option` field or the first available option will be used automatically. </dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize used on item. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_shop_product_option`</dt> <dd> <i>The shop product option key from {@link \RsShopProductOptionSql}.</i> Note: Used for products only `id_sale` = {@link \RsSaleSid::PRODUCT}. If empty the first available option will be used automatically. </dd> </dl>'
                      a_prize_propose:
                        type: array
                        description: 'List of prizes that can be redeemed and applied to items in the cart. <dl> <dt>int `i_score`</dt> <dd>Amount of points needed to redeem the prize.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key. <i>Primary key in {@link \RsRewardPrizeSql} table.</i></dd> <dt>string `text_discount`</dt> <dd>Reward prize discount, e.g. ''.00 off'', ''10% off''.</dd> <dt>string `text_title`</dt> <dd> Reward prize title, e.g. ''on any item'', ''on Water bottle'', ''on any Appointment Pass or Membership Purchase Option''. </dd> </dl>'
                      a_reward_item:
                        type: array
                        description: 'List of cart items to which the selected prize can be applied <dl> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type, one of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_login_prize_used`</dt> <dd>Whether selected login prize discount applied to item.</dd> <dt>string `k_id`</dt> <dd>The sale item key.</dd> <dt>string `k_shop_product_option`</dt> <dd>The shop product option. If <tt>null</tt>, the sale item has no options.</dd> </dl>'
                      a_reward_propose:
                        type: array
                        description: 'List of login prizes that can be applied to items in the cart. <dl> <dt>string `k_login_prize`</dt> <dd>Login prize key. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `text_discount`</dt> <dd>Reward prize discount, e.g. ''.00 off'', ''10% off''.</dd> <dt>string `text_discount_applied`</dt> <dd>Applied login prize discount title, e.g. ''10% discount''.</dd> <dt>string `text_discount_purpose`</dt> <dd>Purpose login prize discount title, e.g. ''Apply 10% off''.</dd> <dt>string `text_title`</dt> <dd> Reward prize title, e.g. ''on any item'', ''on Water bottle'', ''on any Appointment Pass or Membership Purchase Option''. </dd> </dl>'
                      a_tax_list:
                        type: array
                        description: 'Values derived for individual tax rates. Keys refer to tax keys, values refer to the amount of tax in the cart.'
                      i_score:
                        type: [number, "null"]
                        description: 'Amount of client''s reward points.'
                      m_discount:
                        type: [string, "null"]
                        description: 'The full discount of the cart. If `null`, the discount isn''t calculated yet.'
                      m_discount_total:
                        type: string
                        description: 'The total discount amount.'
                      m_subtotal:
                        type: [string, "null"]
                        description: 'The total amount in the catalog cart without tax. If `null`, the total amount isn''t set yet.'
                      m_tax:
                        type: [string, "null"]
                        description: 'The tax amount. If `null`, the tax amount isn''t set yet.'
                      m_tip_purchase:
                        type: [string, "null"]
                        description: 'The amount of appointment tips.'
                      m_total:
                        type: [string, "null"]
                        description: 'The total amount in the catalog cart. If `null`, the total amount isn''t set yet.'
      tags:
        - Cart-Cart
  '/Wl/Catalog/Cart/LimitQuantity.json':
    get:
      summary: 'Checks limit quantity and whether a promotion can be added to the cart. Used in the checkout flow to prevent a client from purchasing more passes than the promotion allows. Returns the limit, how many the client has already bought, and how many are currently in the cart so the UI can display a clear message if the limit would be exceeded.'
      parameters:
        - name: a_item
          in: query
          description: 'The cart items list with the next structure: <dl> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The sale item ID.</dd> <dt>string `k_shop_product_option`</dt> <dd>The shop product option. <tt>null</tt> if the sale item has no options.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The business in which the purchase will be made.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The promotion key to add to the cart.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_purchase_already:
                        type: [number, "null"]
                        description: 'The quantity if promotions have already been purchased by the current user. If `true`, the quantity exceeded. Otherwise, this will be `false`. This will be `null` if not set yet, there''s no limit quantity, or the limit quantity isn''t exceeded.'
                      i_purchase_current:
                        type: [number, "null"]
                        description: 'The current quantity of a promotion with the quantity limit in the cart. This will be `null` if not set yet, there''s no limit quantity, or the limit quantity isn''t exceeded.'
                      i_quantity_limit:
                        type: [number, "null"]
                        description: 'The quantity limit of the promotion. This will be `null` if not set yet or if there''s no limit quantity.'
                      is_limit_exceeded:
                        type: [boolean, "null"]
                        description: 'Determines whether the limit quantity has been exceeded. If `true`, the limit quantity has been exceeded. Otherwise, this will be `false`. This will be `null` if not set yet.'
      tags:
        - Cart-LimitQuantity
  '/Wl/Catalog/Cart/CheckCurrentStock.json':
    get:
      summary: 'Checks showing confirmation, when product''s inventory current count would be negative.'
      parameters:
        - name: a_item
          in: query
          description: 'Purchase items. Every element has next fields: <dl> <dt>int `i_count`</dt> <dd>Current stock quantity.</dd> <dt>int `id_purchase_item`</dt> <dd>Purchase Item ID, one of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Product option key, primary key in {@link \RsShopProductOptionSql} table.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key, primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key, primary key in {@link \RsLocationSql} table. <tt>null</tt> if not set.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_current:
                        type: array
                        description: 'Current stock counts for products. Every element has next fields: <dl> <dt>int `i_current`</dt> <dd>Product''s inventory count at current location.</dd> <dt>string `k_id`</dt> <dd>Product option key.</dd> <dt>string `html_title`</dt> <dd>Product title.</dd> </dl>'
                      has_access:
                        type: boolean
                        description: '<tt>true</tt> if user has access to the business, <tt>false</tt> otherwise.'
                      show_confirmation:
                        type: boolean
                        description: '<tt>true</tt> if show confirmation of negative current count, <tt>false</tt> otherwise.'
      tags:
        - Cart-CheckCurrentStock
    post:
      summary: 'Checks showing confirmation, when product''s inventory current count would be negative. Works exactly as {@link CheckCurrentStockApi::get()} method.'
      parameters:
        - name: a_item
          in: query
          description: 'Purchase items. Every element has next fields: <dl> <dt>int `i_count`</dt> <dd>Current stock quantity.</dd> <dt>int `id_purchase_item`</dt> <dd>Purchase Item ID, one of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Product option key, primary key in {@link \RsShopProductOptionSql} table.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key, primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key, primary key in {@link \RsLocationSql} table. <tt>null</tt> if not set.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_item_post:
                  type: array
                  description: 'Purchase items. This field is the same as {@link CheckCurrentStockApi::$a_item}, but for POST method. Currently, it''s prohibited to make API field working for both GET and POST, so it''s necessary to make another field for different method. Every element has next fields: <dl> <dt>int `i_count`</dt> <dd>Current stock quantity.</dd> <dt>int `id_purchase_item`</dt> <dd>Purchase Item ID, one of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Product option key, primary key in {@link \RsShopProductOptionSql} table.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_current:
                        type: array
                        description: 'Current stock counts for products. Every element has next fields: <dl> <dt>int `i_current`</dt> <dd>Product''s inventory count at current location.</dd> <dt>string `k_id`</dt> <dd>Product option key.</dd> <dt>string `html_title`</dt> <dd>Product title.</dd> </dl>'
                      has_access:
                        type: boolean
                        description: '<tt>true</tt> if user has access to the business, <tt>false</tt> otherwise.'
                      show_confirmation:
                        type: boolean
                        description: '<tt>true</tt> if show confirmation of negative current count, <tt>false</tt> otherwise.'
      tags:
        - Cart-CheckCurrentStock
  '/Wl/Catalog/Cart/DiscountCode.json':
    get:
      summary: 'Checks discount code and whether it can be applied to the elements of the purchase.'
      parameters:
        - name: a_item
          in: query
          description: 'Information about elements of purchase. <dl> <dt>int `id_sale`</dt> <dd>Purchase ite sale category. Constant from {@link \RsSaleSid}.</dd> <dt>string `k_id`</dt> <dd>Purchase item key.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business in which the purchase is made.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'Discount code value.'
          schema:
            type: string
        - name: uid_customer
          in: query
          description: 'Customer UID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount_code:
                        type: array
                        description: '<dl> <dt>string `f_amount`</dt> <dd>Fixed amount of discount. For 1 unit of purchase item.</dd> <dt>float `f_percent`</dt> <dd>Discount size in percents.</dd> <dt>int `i_limit`</dt> <dd>Maximum count of usage. Zero means unlimited usage.</dd> <dt>string `k_discount_code`</dt> <dd>Discount code key. See {@link \Wl\Discount\Code\Sql}.</dd> <dt>string `s_discount_code`</dt> <dd>Discount code value.</dd> </dl>'
                      text_error_code:
                        type: string
                        description: 'Error code. Empty string if no errors.'
      tags:
        - Cart-DiscountCode
  '/Wl/Catalog/Cart/Surcharge.json':
    post:
      summary: 'Sets surcharge fee amount to pay and returns updated data.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_quick_config:
                  type: array
                  description: 'Data for Quick Buy. Structure of this array is described in {@link \RsCatalogCart::__construct()}.'
                is_manual:
                  type: boolean
                  description: 'Whether surcharge was set manually. `false` if not, `true` if yes.'
                k_business:
                  type: string
                  description: 'Business key.'
                m_surcharge:
                  type: string
                  description: 'Surcharge amount to update the cart.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Cart-Surcharge
  '/Wl/Catalog/Payment/Payment.json':
    post:
      summary: 'Allows to pay items for the client. The checkout endpoint that finalizes a purchase in the client-facing store. Charges the client''s selected payment method, applies any discounts and tips, and creates the purchase record. Returns the resulting purchase key so the frontend can redirect to the confirmation page.'
      parameters:
        - name: a_commission
          in: query
          description: 'The staff commission earned for this purchase. If this isn''t empty, it has the next fields: <dl> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key of {@link \PassportLoginSql} table.</i></dd> <dt>string `k_staff`</dt> <dd><b>Deprecated!</b> Staff key. <i>Primary key of {@link \RsStaffSql} table.</i></dd> <dt>string `k_staff_pay`</dt> <dd>The payment schema key.</dd> </dl>'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The WellnessLiving mode type (required). One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_guest
          in: query
          description: 'Determines if the payment owner is an anonymous user (optional).'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: 'Specify this if operations are performed by the staff member (optional).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key (required).'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key (required).'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key (required).'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_item:
                  type: array
                  description: 'The list of items in the cart. This parameter is required. Every element must have the following keys: <dl> <dt>array `a_config`</dt> <dd> Additional configuration information. This may contain the next keys: <dl> <dt>array `a_quick_gift`</dt> <dd> A list of components to be added to the client. For quick gift cards only: <dl> <dt>int `i_count`</dt> <dd>The quantity of elements.</dd> <dt>int `id_purchase_item`</dt> <dd>The purchase type of the element. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd> The primary key of the element, depending on the element type. Pay attention that if you add a product into gift card, there must be specified not product key but product option key. Do not specify separate field `k_shop_product_option`. </dd> </dl> </dd> <dt>string[] `a_uid_share`</dt> <dd>An array of UIDs representing the client''s relatives that share the Purchase Option.</dd> <dt>array `a_wellness_program`</dt> <dd> "Wellness Program" fields. <var>k_wellness_program</var> must be passed along with this array. See the description below. It''s recommended to validate the fields using the POST method of the {@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi} model. <dl> <dt>array `a_account`</dt> <dd> The list of reimbursement account field values. Corresponds to the `a_account` list in {@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi::$a_field_list}. Each element: <dl> <dt>string `k_field`</dt> <dd>The field key identifying the account field.</dd> <dt>string `text_value`</dt> <dd>The submitted value for the field.</dd> </dl> </dd> <dt>array `a_field`</dt> <dd> The list of wellness program field values. Corresponds to the `a_field` list in {@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi::$a_field_list}. Each element: <dl> <dt>string `k_field`</dt> <dd>The field key identifying the program field.</dd> <dt>string `text_value`</dt> <dd>The submitted value for the field.</dd> </dl> </dd> </dl> </dd> <dt>string `dt_prorate`</dt> <dd>The prorate date, used only for memberships.</dd> <dt>string `dt_send_local`</dt> <dd>The date when the gift card will be sent. This is only used for gift cards.</dd> <dt>string `dt_start`</dt> <dd>The start date, used only for memberships.</dd> <dt>bool `is_pay_when_start`</dt> <dd> If `true`, the client won''t be charged for this item until its start date. Otherwise, this will be `false`. The Purchase Option must have a specified start date. </dd> <dt>bool `is_prorate`</dt> <dd> If this is set to `1`, this purchase will use the prorate rule. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>bool `is_prorate_fix`</dt> <dd> If this is set to `1`, the custom prorate amount will be used. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>bool `is_prorate_only`</dt> <dd> If this is set to `1`, this purchase is prorate only. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>bool `is_renew`</dt> <dd> If this is set to `1`, auto-renew will be enabled for the item. Otherwise, set this to `0`. This is only used for memberships. </dd> <dt>string `k_appointment`</dt> <dd>The appointment key, used only for appointment add-ons.</dd> <dt>string `k_coupon_amount`</dt> <dd>The key of the gift card amount, used only for gift cards.</dd> <dt>string `k_staff`</dt> <dd> <b>Deprecated!</b> The staff member key, used only for appointment tips. <i>Primary key of staff member in {@link \RsStaffSql} table.</i> </dd> <dt>string `k_wellness_program`</dt> <dd> The "Wellness Program" key. Set this for the insurance membership promotion. <var>a_wellness_program</var> array must be passed along with the key. See the array description above. <p>Use the following models to work with this type of promotion:</p> <ul> <li>{@link \Wl\Insurance\Catalog\ProgramListApi} to obtain list of active programs.</li> <li>{@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi} to get and validate fields for a given program.</li> </ul> </dd> <dt>string `m_prorate_custom`</dt> <dd> The custom prorate price, used only memberships only. This is only used if <var>is_prorate_fix</var> is <tt>true</tt>. </dd> <dt>string `s_code`</dt> <dd>The gift card code. This is required for gift cards.</dd> <dt>string `s_image`</dt> <dd> The key of the image for the gift card. If empty, the business''s first gift card image will be used. Specify this only for gift cards. </dd> <dt>string `s_mail`</dt> <dd>The gift card receiver email. This is required for gift cards.</dd> <dt>string `s_recipient`</dt> <dd>The gift card receiver name. This is required for gift cards.</dd> <dt>string `s_sender`</dt> <dd>The gift card sender name. This is required for gift cards.</dd> <dt>string `uid_staff`</dt> <dd>The staff member user ID, used only for appointment tips.</dd> <dt>string `uid_to`</dt> <dd>Specifies the recipient of a transfer Purchase Option.</dd> </dl> </dd> <dt>array[] `a_tax_custom`</dt> <dd> Customer taxes (optional). Every element must contain the following keys: <dl> <dt>string `f_tax`</dt> <dd>The tax amount.</dd> <dt>string `k_tax`</dt> <dd>The tax key.</dd> </dl> </dd> <dt>int `i_quantity`</dt> <dd>The item quantity.</dd> <dt>int `id_sale`</dt> <dd>The item type ID. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the login prize to apply a login prize discount.</dd> <dt>string `k_shop_product_option`</dt> <dd>The product option key, required only for products.</dd> <dt>string `m_price_custom`</dt> <dd>The custom price (optional).</dd> <dt>string `s_signature`</dt> <dd>The client signature, used for items that require a signed contract.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'The list of quiz response keys. Keys refer to quiz keys. <i>Primary key from {@link \Core\Quiz\QuizSql} table.</i> And values refer to responses. <i>Primary key from {@link \Core\Quiz\Response\ResponseSql}.</i> Or special values from the {@link QuizResponse::RESPONSE_SKIP} constant.'
                f_discount_percent:
                  type: number
                  format: float
                  description: 'The percentage discount (optional).'
                k_pay_installment_template:
                  type: string
                  description: 'The installment template key (optional). This will be `null` if the installment plan doesn''t exist or isn''t set for the purchased item.'
                k_visit:
                  type: string
                  description: 'The visit key to be paid (optional).'
                m_discount_flat:
                  type: string
                  description: 'The amount to discount (optional).'
                m_surcharge:
                  type: string
                  description: 'The manual surcharge amount. An empty string represents an automatic surcharge amount.'
                m_tip:
                  type: string
                  description: 'The tip amount (optional).'
                text_discount_code:
                  type: string
                  description: 'The discount code (optional).'
                text_receipt_note:
                  type: string
                  description: 'The custom receipt note.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_activity:
                        type: [string, "null"]
                        description: 'The key of login activity. This will be `null` if not set yet.'
                      k_purchase:
                        type: string
                        description: 'The purchase key created during payment.'
      tags:
        - Payment-Payment
  '/Wl/Catalog/Quiz/Quiz.json':
    get:
      summary: ''
      parameters:
        - name: a_purchase_item
          in: query
          description: 'List of purchase items. Each element has format <tt>[id_purchase_item]::[k_id]</tt>, where Empty if no purchases are made for booking. <dl> <dt>int `id_purchase_item`</dt> <dd>ID of the purchase item. One of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>Key of the item. Depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Key of a business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user who is making a purchase. `null` in case when quizzes requested for guest.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: object
                        description: 'List of quizzes. Each element has next structure: <dl> <dt>bool `is_require`</dt> <dd>Whether the quiz is required and can not be skipped.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. Primary key from {@link \Core\Quiz\QuizSql} table.</dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> is_require: bool, k_quiz: string, text_title: string }'
      tags:
        - Quiz-Quiz
  '/Wl/Catalog/Coupon/AmountCoupon.json':
    post:
      summary: 'Adds new amount coupon gift card. Note, that only one card of {@link TypeSid::AMOUNT} can be created in one business.'
      parameters:
        - name: id_place
          in: query
          description: 'From where request comes. One of {@link \RsPlaceSid}.'
          schema:
            type: number
            format: int32
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                k_coupon:
                  type: string
                  description: 'Coupon key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_cart_coupon:
                        type: [array, "null"]
                        description: 'Amount coupon information.'
      tags:
        - Coupon-AmountCoupon
  '/Wl/Catalog/Location/Location.json':
    get:
      summary: 'Retrieves a list of information about locations in the catalog.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of the business.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'A list of information about locations.'
                      k_location_active:
                        type: string
                        description: 'Location key that was selected by the client last time.'
                      k_location_home:
                        type: string
                        description: 'Home location ID.'
                      k_location_random:
                        type: string
                        description: 'Random location ID for the business.'
      tags:
        - Location-Location
  '/Wl/Classes/ClassList/BookList.json':
    get:
      summary: 'Gets a list of classes which take place in the specified location. Used to build the booking page for a location, displaying all classes that clients can sign up for. Returns the full class details needed for display: schedules, assigned staff, booking links, pricing, and category tabs.'
      parameters:
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'The class list. Every element has the following structure: <dl> <dt>string[] `a_class_tab`</dt> <dd> The list of class tab keys for the class. <i>Primary keys in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd>List of login types for current class. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i></dd> <dt>string[] `a_member_group`</dt> <dd>List of member groups for current class. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i></dd> <dt>string[] `a_url_book`</dt> <dd>The list of URL to book this class.</dd> <dt>string `dtu_book_date`</dt> <dd>The book date of an event in UTC date/time format. Can be empty string if user cannot book the event.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `id_bookable`</dt> <dd>Bookable type ID. One of {@link \Wl\Service\BookableSid} constants.</dd> <dt>bool `is_active`</dt> <dd>This will be `true` if the class is active. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>This will be `true` for events. Otherwise, this will be `false` for classes.</dd> <dt>bool `is_online_private`</dt> <dd> `true` means to show class only for clients who can book online, `false` means to show class for all clients. </dd> <dt>string `k_class`</dt> <dd>The class key. <i>The primary key in {@link \RsClassSql} table.</i></dd> <dt>string `k_enrollment_block`</dt> <dd>The enrollment block key. <i>The primary key in {@link \RsEnrollmentBlockSql} table.</i></dd> <dt>string `m_price_total`</dt> <dd>The total price for an event.</dd> <dt>string `text_description`</dt> <dd>The class description.</dd> <dt>string `text_title`</dt> <dd>The class name.</dd> </dl>'
      tags:
        - ClassList-BookList
  '/Wl/Classes/ClassList/List.json':
    get:
      summary: 'Returns list of classes and events in the business. Used by import and integration tools to enumerate all classes and events offered by a business. In franchise mode, classes from all franchisee locations are included. Results are sorted by title and start date.'
      parameters:
        - name: a_class_tab
          in: query
          description: 'List of tabs keys. Filtering by Book Now Tab is not supported if {@link ListApi::$is_franchise} is `true`. `null` if no filtering by Book Now Tab is required.'
          schema:
            type: [array, "null"]
        - name: id_class_tab
          in: query
          description: 'ID of book now tab. One of {@link TabSid} constants. Only {@link TabSid::TRAINING} and {@link TabSid::ENROLLMENT} book now tab are supported. Filtering by Book Now Tab is not supported if {@link ListApi::$is_franchise} is `true`. `null` if no filtering by Book Now Tab is required.'
          schema:
            type: [number, "null"]
        - name: is_enrollment_block_all
          in: query
          description: 'Whether all events should be returned from same enrollment block. * <tt>true</tt> to return all events from same enrollment block. * <tt>false</tt> to return only one event from each enrollment block. Only published and non-empty events will be returned for the client. To return empty events, use {@link ListApi::$is_enrollment_block_empty}. For the staff, filtering by publication or emptiness is not applied.'
          schema:
            type: boolean
        - name: is_enrollment_block_empty
          in: query
          description: 'Whether to include events without sessions. An empty event is neither published nor unpublished. It simply has no sessions. Such events can be useful for list filters. Affects clients only. Does not affect staff. Staff always see empty events. Makes sense in conjunction with the flag {@link ListApi::$is_enrollment_block_all}. * <tt>true</tt> to include events without sessions. * <tt>false</tt> to exclude events without sessions.'
          schema:
            type: boolean
        - name: is_event_include
          in: query
          description: 'Whether to include events in the result. true` to include both classes and events. false` to include only classes.'
          schema:
            type: boolean
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created classes (if business is franchisor). <tt>true</tt> to include franchisee-created classes.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'List of classes and events. The result may depend on the following flags: * {@link ListApi::$is_enrollment_block_all} * {@link ListApi::$is_enrollment_block_empty} Each element has the following structure: <dl> <dt>string[] `a_class_tab`</dt> <dd> List of class book now tabs, where this class is presented. Each element is <i>primary key from {@link \Wl\Classes\Tab\Sql\ClassTab\Sql}</i> or `null` if class is presented in default system tab Classes. Events can also be added in class tabs. </dd> <dt>string[] `a_event_tab`</dt> <dd> List of event book now tabs, where this event is presented. Each element is <i>primary key from {@link \Wl\Classes\Tab\Sql\ClassTab\Sql}</i> or `null` if event is presented in default system tab Enrollments. Classes cannot be added in event tabs. </dd> <dt>string `dl_end`</dt> <dd>The ending date of the class/event. Maybe a zero date if the class is ongoing (has no end date).</dd> <dt>string `dl_start`</dt> <dd>The starting date of the class/event.</dd> <dt>bool `is_active`</dt> <dd>Whether class/event is active.</dd> <dt>bool `is_event`</dt> <dd>Define if current element is event.<tt>true</tt> - event, <tt>false</tt> - class.</dd> <dt>bool `is_remove`</dt> <dd>Whether class/event is removed.</dd> <dt>string `k_class`</dt> <dd>Class/event identifier. <i>Primary key in {@link \RsClassSql}.</i></dd> <dt>string `k_enrollment_block`</dt> <dd>Event Block ID. <i>Primary key in {@link \RsEnrollmentBlockSql}.</i></dd> <dt>string `text_title`</dt> <dd>Class/event name.</dd> </dl>'
      tags:
        - ClassList-List
  '/Wl/Classes/Tab/ClassTab.json':
    delete:
      summary: 'Hides the tab.'
      parameters:
        - name: k_class_tab
          in: query
          description: 'Class tab key. Primary key in the {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table. <tt>null</tt> to create new class tab.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tab-ClassTab
    post:
      summary: 'Edits or creates new tab.'
      parameters:
        - name: k_class_tab
          in: query
          description: 'Class tab key. Primary key in the {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table. <tt>null</tt> to create new class tab.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_class_tab_object:
                  type: [number, "null"]
                  description: 'ID of class tab object. One of {@link \Wl\Classes\Tab\TabSid} constants. <tt>null</tt> to hide class tab.'
                id_class_tab_system:
                  type: [number, "null"]
                  description: 'Id of class tab type if this class tab is system default. One of {@link \Wl\Classes\Tab\TabSid} constants. <tt>null</tt> to hide class tab or if class tab is not system default.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. Primary key in the {@link \RsBusinessSql} table. <tt>null</tt> to hide class tab.'
                text_title:
                  type: [string, "null"]
                  description: 'Class tab title. <tt>null</tt> to hide class tab.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_class_tab:
                        type: [string, "null"]
                        description: 'Class tab key. Primary key in the {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table. <tt>null</tt> to create new class tab.'
      tags:
        - Tab-ClassTab
  '/Wl/Classes/ClassView/Element.json':
    get:
      summary: 'Returns class information including schedules, images, and booking settings for the specified business. Used by import tools to read the full class catalog for a business. Returns a map of all classes (or a single class) with the information needed to replicate class data in an external system: schedules, images, booking constraints, and descriptions.'
      parameters:
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need the image to be returned in a specific size. If this value isn''t specified, the returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'The class key used to get information for a specific class. An empty value returns information for all classes of the business.'
          schema:
            type: string
        - name: show_cancelled
          in: query
          description: 'Defines if canceled schedules should be included in the result. If `true`, canceled schedules will be shown. Otherwise, this will be `false`.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_list:
                        type: array
                        description: 'Displays information about the class schedule(s). Each element has the next structure: <dl> <dt>array `a_schedule`</dt> <dd> A list of class schedules. Each element has the next structure: <dl> <dt>array `a_repeat`</dt> <dd> Schedule repeat options: <dl> <dt>int `i_repeat`</dt> <dd>A count of the periods specified in <var>id_repeat</var>.</dd> <dt>int `id_repeat`</dt> <dd>The measuring unit of <var>i_repeat</var>. One of {@link \ADurationSid} constants.</dd> </dl> </dd> <dt>string[] `a_staff_key`</dt> <dd>A list of staff member keys that provide sessions for this schedule.</dd> <dt>string[] `a_uid_staff_key`</dt> <dd>A list of staff member keys UID that provide sessions for this schedule.</dd> <dt>string `dl_end`</dt> <dd> The end date of the schedule in the location''s time zone. This will be empty or zero if the schedule is ongoing. </dd> <dt>string `dl_start`</dt> <dd>The start date of the schedule in location''s time zone.</dd> <dt>int `i_day`</dt> <dd> The day of the week, where the class schedule exists, starting with 1 for Monday and ending with 7 for Sunday. </dd> <dt>int `i_duration`</dt> <dd>The duration of the schedule sessions in minutes.</dd> <dt>bool `is_cancel`</dt> <dd>Defines whether the schedule canceled or not.</dd> <dt>string `k_location`</dt> <dd>The location key of the schedule.</dd> <dt>string `m_price`</dt> <dd>The price of a single session of the schedule.</dd> <dt>string `text_room`</dt> <dd>The room of the class period.</dd> <dt>string `text_time`</dt> <dd>The time of the session in the following format: `[start_time] - [end_time]` in the locale''s time zone.</dd> </dl> </dd> <dt>array|null `a_config`</dt> <dd> Class-specific business policies overriding the business defaults. Has the same structure as {@link \Wl\Business\Config\BusinessConfigApi::$a_business_policy}. `null` when the class uses the default business policy without overrides. <dl> <dt>string[] `a_family_relation`</dt> <dd> List of allowed relation types specific to a given business. Key is ID. One of {@link \RsFamilyRelationSid} constant. Value is SID. <i>Result of {@link \RsFamilyRelationSid::allByBusiness()} method.</i> </dd> <dt>int[] `a_family_relation_login_allow`</dt> <dd> List of allowed relation types specific to a given business. Each value constant from {@link \RsFamilyRelationSid} class. </dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> Tags for quick search. <i>Result of {@link \Wl\Classes\ClassesInfo::$a_search_tag} property.</i> <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Name of the tag.</dd> </dl> </dd> <dt>array `a_visits_required`</dt> <dd> Information about visits that should be reviewed prior to visiting this class/event. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::visitsRequiredInfo()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl> </dd> <dt>bool|null `has_own_image`</dt> <dd> This field will be `true` if the image used for the class is an image uploaded in the class setup. If an image isn''t uploaded in the class setup, but there''s at least one image in Setup->Locations image slider, this field will be `false`. This field will be `null` if the class image isn''t uploaded, and there are no images in location slider. In such cases, an empty image is used. </dd> <dt>string `html_description`</dt> <dd>The browser-safe description.</dd> <dt>string `html_special_instruction`</dt> <dd>Special instructions safe to be inserted to a browser.</dd> <dt>int `i_age_from`</dt> <dd>The minimum age restriction.</dd> <dt>int `i_age_to`</dt> <dd>The maximum age restriction.</dd> <dt>bool `is_age_public`</dt> <dd>Determines whether age restriction exist and should be taken into account prior to booking this class/event.</dd> <dt>bool `is_bookable`</dt> <dd>Determines whether clients can book the class or event online.</dd> <dt>bool `is_online_private`</dt> <dd>`true` - show classes only for clients who can book online. `false` - show classes for all clients.</dd> <dt>bool `is_promotion_client`</dt> <dd>Determines whether clients of the business can see a list of applicable Purchase Options.</dd> <dt>bool `is_promotion_only`</dt> <dd>Determines whether the item can be booked only using promotions.</dd> <dt>bool `is_promotion_staff`</dt> <dd>Determines whether staff members of the business can see a list of applicable Purchase Options.</dd> <dt>bool `is_single_buy`</dt> <dd> Determines whether the item can be paid with Drop In or not. If it can be paid with Drop In, then `m_price` shows the price for one visit. If this is `0`, it means the class is free. </dd> <dt>bool `is_virtual`</dt> <dd>Determines whether the item is virtual.</dd> <dt>bool `is_event`</dt> <dd> Determines whether the item is an event or class instance. If `true`, the item is an event instance. Otherwise, this will be `false`. </dd> <dt>string `k_class`</dt> <dd>The class key.</dd> <dt>string `k_promotion_default`</dt> <dd>Promotion key of the default promotion to use when paying for the class. `null` if no promotion is selected.</dd> <dt>string `m_price`</dt> <dd>Drop In price, if the class allows payment for one visit.</dd> <dt>bool `show_special_instructions`</dt> <dd> This will be `true` if special instructions can be public. Otherwise, this will be `false` if they should only be shown to the clients who booked the class. </dd> <dt>string `xml_description`</dt> <dd>The item description.</dd> <dt>string `xml_special_instruction`</dt> <dd>The item''s special instructions.</dd> <dt>string `url_image`</dt> <dd>The URL link to the item image.</dd> </dl>'
      tags:
        - ClassView-Element
  '/Wl/Classes/Info/Info.json':
    get:
      summary: 'Gets information about class.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business in which the class resides.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'Class identifier to get information for. Primary key in {@link \RsClassSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_logo:
                        type: array
                        description: 'Service logo information: <dl> <dt>bool `is_empty`</dt> <dd>Whether service image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl>'
                      is_event:
                        type: boolean
                        description: '<tt>true</tt> means event, <tt>false</tt> means class.'
                      text_title:
                        type: string
                        description: 'Title of the class.'
      tags:
        - Info-Info
  '/Wl/Classes/Virtual/Notification.json':
    get:
      summary: 'Returns information about user that is currently signed in.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business to which the closed notification modal window belongs. Primary key of the {@link \RsBusinessSql} table. `null` if key of the business is not known (may only occur in legacy applications).'
          schema:
            type: [string, "null"]
        - name: k_visit
          in: query
          description: 'Key of the visit whose modal is being dismissed. Primary key of the {@link \RsVisitSql} table. `null` if this value did not arrive from the client application.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the user that is dismissing the modal. `null` if this value did not arrive from the client application.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Virtual-Notification
  '/Wl/Classes/Promotion/ClassPromotion.json':
    get:
      summary: 'Returns list of promotions that can be used to pay for the class / event. Used in the booking flow to show clients which of their existing passes or memberships cover the selected class. Also returns the default promotion to pre-select so the client does not have to choose manually when there is an obvious match.'
      parameters:
        - name: is_event
          in: query
          description: 'Determines whether the class is an event or not.'
          schema:
            type: boolean
        - name: is_login_type_ignore
          in: query
          description: '`true` - the login type or group restrictions are ignored and all pricing options will be returned. This will require staff or admin access level. `false` - the API should return only pricing options, which are available for the current user.'
          schema:
            type: boolean
        - name: is_related_only
          in: query
          description: '`true` - promotions should only be returned if they''re related to the given class or event. `false` - all promotions should be returned, even if they aren''t related to the given class or event.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'The class key. This refers to the class or event with which you want to connect the promotions. This will be `null` or `0` if you need to return a list of promotions for an uncreated class or event.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data with the following structure:. <dl> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The title of the promotion.</dd> <dt>int `id_program`</dt> <dd>One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if the promotion is related to the class or event, <tt>false</tt> otherwise.</dd> </dl>'
                      k_promotion_default:
                        type: [string, "null"]
                        description: 'The default promotion key. This will be `null` if `k_class` wasn''t provided, or if the class has no default promotion.'
      tags:
        - Promotion-ClassPromotion
  '/Wl/Discount/Code/DiscountCode.json':
    get:
      summary: 'Returns discount codes of the specified business. Used in the backend discount management UI to show the full list of discount codes for a business so the staff member can select one to view or edit.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the discount codes.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'Discount codes list. Each element has next structure: <dl> <dt>string `k_discount_code`</dt> <dd>Discount code key. <i>Primary key in the {@link \Wl\Discount\Code\Sql} table.</i></dd> <dt>string `text_discount_code`</dt> <dd>Discount code title.</dd> </dl>'
      tags:
        - Code-DiscountCode
  '/Wl/Event/Transfer/EventTransfer.json':
    get:
      summary: 'Checks whether users are related.'
      parameters:
        - name: k_purchase_item
          in: query
          description: 'Key of event purchase item to be transferred. Primary key in {@link \RsPurchaseItemSql} table.'
          schema:
            type: string
        - name: uid_from
          in: query
          description: 'User key to transfer purchased event from. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
        - name: uid_to
          in: query
          description: 'User key to transfer purchased event to. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about relation between users: <dl> <dt>string `html_relation`</dt> <dd>Name of relation type.</dd> <dt>string `html_user_name`</dt> <dd>User name with whom purchased event owner has relation.</dd> </dl>'
      tags:
        - Transfer-EventTransfer
    post:
      summary: 'Transfers purchased event to another user.'
      parameters:
        - name: id_mode
          in: query
          description: 'Key of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '<tt>true</tt> if API is being used from backend, <tt>false</tt> otherwise.'
          schema:
            type: boolean
        - name: k_purchase_item
          in: query
          description: 'Key of event purchase item to be transferred. Primary key in {@link \RsPurchaseItemSql} table.'
          schema:
            type: string
        - name: uid_from
          in: query
          description: 'User key to transfer purchased event from. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
        - name: uid_to
          in: query
          description: 'User key to transfer purchased event to. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Transfer-EventTransfer
  '/Wl/Import/Pay/Abort.json':
    post:
      summary: 'Aborts a running import.'
      parameters:
        - name: k_business_merchant
          in: query
          description: 'ID of a merchant for which import should be aborted.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Pay-Abort
  '/Wl/Import/Pay/Start.json':
    post:
      summary: 'Starts an import.'
      parameters:
        - name: k_business_merchant
          in: query
          description: 'ID of a merchant for which import should be started.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Pay-Start
  '/Wl/Import/Application/Attachment.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_attachment_list:
                  type: array
                  description: 'List of attachment for member.'
                k_business:
                  type: string
                  description: 'Business key.'
                s_member_id:
                  type: string
                  description: 'Member id.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_file_broken:
                        type: array
                        description: 'List of broken files.'
      tags:
        - Application-Attachment
  '/Wl/Import/Application/Check.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Check
  '/Wl/Import/Application/Photo.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_photo:
                  type: array
                  description: 'User photo.'
                k_business:
                  type: string
                  description: 'Business key.'
                s_member_id:
                  type: string
                  description: 'Member id.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Photo
  '/Wl/Import/Progress/Progress.json':
    get:
      summary: 'Designed to notify about import progress.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Progress-Progress
  '/Wl/Rank/Setting/RankSetting.json':
    post:
      summary: 'Save belts settings for the business.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_display_promotion_criteria:
                  type: boolean
                  description: 'Whether to display promotion criteria to clients in the Client Wep App. `true` - to display promotion criteria, `false` - otherwise.'
                is_display_promotion_status:
                  type: boolean
                  description: 'Whether to display promotion status to clients in the Client Wep App. `true` - to display promotion status, `false` - otherwise.'
                is_reset_class_attendance:
                  type: boolean
                  description: 'Whether to reset class attendance requirements after promotion. `true` - only classes which took place after the date of promotion should be counted for the current belt class attendance requirements, `false` - otherwise.'
                k_business:
                  type: string
                  description: 'Key of the business to save belts settings in.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Setting-RankSetting
  '/Wl/Rank/Category/RankCategory.json':
    get:
      summary: 'Gets belts categories list of a business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_rank_category_list:
                        type: array
                        description: 'A list of belts categories, keys, and information. Each element is an array with the following information: <dl> <dt>string `k_rank_category`</dt> <dd>A belt category key. Primary key in {@link \RsRankCategorySql} table.</dd> <dt>string `text_rank_category`</dt> <dd>Belt category title.</dd> </dl>'
      tags:
        - Category-RankCategory
  '/Wl/Reward/Action/Action.json':
    get:
      summary: 'Retrieves a list of available reward actions. Returns all reward actions visible to the specified user in the given business, optionally filtered by category, including the score value and request status for each action.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_reward_action_category
          in: query
          description: 'Key of reward action category.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward_action:
                        type: array
                        description: 'A list reward actions. Every element has next keys: <dl> <dt>int `id_reward_action_category`</dt> <dd> ID of reward actions category type. One of {@link \RsRewardActionCategorySid} constants. <tt>false</tt> for custom reward actions categories. </dd> <dt>string `k_reward_action`</dt> <dd>ID of reward action. <i>Primary key in {@link \RsRewardActionSql} table.</i></dd> <dt>string `k_reward_action_category`</dt> <dd>ID of reward action category in database. <i>Primary key in {@link \RsRewardActionCategorySql} table.</i></dd> </dl>'
      tags:
        - Action-Action
  '/Wl/Reward/Action/Element.json':
    get:
      summary: 'Retrieves information about reward action item. Returns the point value, score type, description, and whether the user has already submitted a points request for the specified reward action.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_action
          in: query
          description: 'Reward action to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Points amount that will be added to user account after successful accomplishment of reward action.'
                      id_reward_score:
                        type: [number, "null"]
                        description: 'Reward score type ID.'
                      is_request:
                        type: boolean
                        description: '<tt>true</tt> if user has requested points for action accomplishment, <tt>false</tt> otherwise.'
                      s_description:
                        type: string
                        description: 'User friendly reward action description.'
      tags:
        - Action-Element
    post:
      summary: 'Requests points earning. Submits a custom reward points request for the specified action on behalf of the given user and records the pending approval request.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_action
          in: query
          description: 'Reward action to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Element
  '/Wl/Reward/Action/ActionWrite.json':
    post:
      summary: 'Updates configuration fields for the specified reward action. Accepts changes to fields such as auto-renewal flag, point cap, point value, and linked promotions, and persists only the provided fields while logging the changes.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_promotion:
                  type: [array, "null"]
                  description: 'Promotions'' list. <tt>null</tt> if not set yet.'
                i_cap:
                  type: [number, "null"]
                  description: 'Maximum count of score. <tt>null</tt> if not set yet.'
                i_count:
                  type: [number, "null"]
                  description: 'Count of the duration period. <tt>null</tt> if not set yet.'
                i_score:
                  type: [number, "null"]
                  description: 'Score of the reward. <tt>null</tt> if not set yet.'
                id_cap:
                  type: [number, "null"]
                  description: 'Period to apply maximum count of score. <tt>null</tt> if not set yet.'
                id_duration:
                  type: [number, "null"]
                  description: 'Type of a period. <tt>null</tt> if not set yet.'
                is_account_credit:
                  type: [boolean, "null"]
                  description: 'Flag to define points type (account credits or points) in the ''Refer-a-Friend'' section on the ''Enable Points'' page. <tt>true</tt>, if these are account credits. <tt>false</tt>, if these are points. <tt>null</tt> if field left unchanged.'
                is_auto_renewal:
                  type: [boolean, "null"]
                  description: 'Whether auto-renewal is enabled for this reward action.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. <tt>null</tt> if not set yet.'
                k_class:
                  type: [string, "null"]
                  description: 'Class ID. <tt>null</tt> if not set yet.'
                k_promotion:
                  type: [string, "null"]
                  description: 'Promotion key. <tt>null</tt> if not set yet.'
                k_reward_action:
                  type: [string, "null"]
                  description: 'Reward action ID. <tt>null</tt> if not set yet.'
                k_service:
                  type: [string, "null"]
                  description: 'Service key. <tt>null</tt> if not set yet.'
                k_shop_product:
                  type: [string, "null"]
                  description: 'Shop product ID. <tt>null</tt> if not set yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-ActionWrite
  '/Wl/Reward/Action/ActionType.json':
    get:
      summary: 'Retrieves information about reward actions. Returns all reward action records of the specified score type for the business, including the point value configured for each action.'
      parameters:
        - name: id_reward_score
          in: query
          description: 'ID of type of reward action. One of {@link \RsRewardScoreSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of a business to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward_action:
                        type: array
                        description: 'Information about reward actions. Every element has next keys: <dl> <dt>int `i_score`</dt> <dd>Number of points for this reward action.</dd> </dl>'
      tags:
        - Action-ActionType
  '/Wl/Reward/Score/Score.json':
    get:
      summary: 'Returns the total reward points for the specified activity list or user. If `a_login_activity` is provided, returns the total points earned across those activities; otherwise returns the current point balance for the specified user in the given business.'
      parameters:
        - name: a_login_activity
          in: query
          description: 'List of login activity keys for which points should be added or returned. NOTE: Login activity might belong to different users. It is possible for ex if booking performed for several users (usually family members) and required to retrieve sum of all scores for preformed activities.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Depending on arguments specified during API request might be: * Total number of points which were earned for each activity in a list provided in {@link ScoreApi::$a_login_activity} in a case if it was provided. * Total number of points user currently own in a cast if {@link ScoreApi::$uid} specified.'
      tags:
        - Score-Score
    post:
      summary: 'Gives user reward points for sharing of certain activity into Facebook. Validates the activity list, determines the appropriate reward score type per activity category, and schedules reward point additions for each shareable activity.'
      parameters:
        - name: a_login_activity
          in: query
          description: 'List of login activity keys for which points should be added or returned. NOTE: Login activity might belong to different users. It is possible for ex if booking performed for several users (usually family members) and required to retrieve sum of all scores for preformed activities.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Score-Score
    put:
      summary: 'Manually adjusts the reward points balance for the specified user. Requires the reward point reset privilege, creates a manual activity log entry, and applies the signed point adjustment to the user''s reward balance within a transaction.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_score:
                  type: number
                  format: int32
                  description: 'Depending on arguments specified during API request might be: * Total number of points which were earned for each activity in a list provided in {@link ScoreApi::$a_login_activity} in a case if it was provided. * Total number of points user currently own in a cast if {@link ScoreApi::$uid} specified.'
                k_business:
                  type: string
                  description: 'Business key within which score should be managed.'
                text_comment:
                  type: string
                  description: 'String to be added as a comment to manual reward points changes.'
                uid:
                  type: string
                  description: 'User key for which score should be managed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Score-Score
  '/Wl/Reward/Board/Element.json':
    get:
      summary: 'Retrieves information about reward board item. In single-user mode returns the user''s score, name, and avatar on the specified board; in multi-user mode accepts a list of UIDs and returns an array of score, name, and avatar entries for all of them.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of users to get information for. <tt>null</tt> for mode of single user.'
          schema:
            type: [array, "null"]
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_board
          in: query
          description: 'Reward board to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward:
                        type: [array, "null"]
                        description: 'List of reward board elements. `null` if not loaded. Each element: <dl> <dt>int `i_score`</dt> <dd>Score in points.</dd> <dt>string `text_name`</dt> <dd>Reward name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `url_logo`</dt> <dd>URL to the user logo image.</dd> </dl>'
                      i_score:
                        type: number
                        format: int32
                        description: 'Score in points.'
                      s_name:
                        type: string
                        description: 'User name.'
                      url_logo:
                        type: string
                        description: 'User logo.'
      tags:
        - Board-Element
  '/Wl/Reward/Board/List.json':
    get:
      summary: 'Retrieves a list of reward board items to show. Returns an ordered list of user IDs ranked by points for the specified reward board, up to the configured maximum number of entries.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for.'
          schema:
            type: string
        - name: k_reward_board
          in: query
          description: 'ID of reward board to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid:
                        type: array
                        description: 'A list of UID items.'
      tags:
        - Board-List
  '/Wl/Reward/Prize/Element.json':
    get:
      summary: 'Retrieves information about redeemable prize item. Returns the point cost and description for the specified redeemable prize in the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_prize
          in: query
          description: 'Key of redeemable prize.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Prize price in wellnessliving points.'
                      s_description:
                        type: string
                        description: 'User friendly prize description.'
      tags:
        - Prize-Element
    post:
      summary: 'Redeems selected prize. Deducts the required points from the user''s reward balance and records the redemption, returning the key of the created login prize record.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: k_reward_prize
          in: query
          description: 'Key of redeemable prize.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_prize:
                        type: string
                        description: 'Key of login prize.'
      tags:
        - Prize-Element
  '/Wl/Reward/Prize/Prize.json':
    get:
      summary: 'Retrieves a list of redeemable prizes. Returns all active redeemable prizes for the specified business, ordered by point cost, including the prize key and required score for each item.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_prize:
                        type: array
                        description: 'Array of prize keys.'
                      a_score:
                        type: array
                        description: 'Array of prize scores.'
      tags:
        - Prize-Prize
  '/Wl/Schedule/ClassList/ClassList.json':
    get:
      summary: 'Retrieves a list of classes and class information for the given business and date range. Returns upcoming class sessions for a business, with optional filtering by location, day of week, time of day, staff member, and virtual or in-person mode. The result includes per-session details such as staff, capacity, booking counts, and virtual locations, as well as a calendar map indicating which dates have at least one scheduled session.'
      parameters:
        - name: a_class
          in: query
          description: 'The list of classes keys to filter. Return sessions with matching class IDs. If it''s empty, all classes/events will be returned.'
          schema:
            type: array
        - name: a_day
          in: query
          description: 'Class filter by day of the week. Array of number representing the days of the week. Return sessions matching the given weekdays. (7 = Sunday, 1 = Monday, ..., 6 = Saturday) Empty array means no filtering.'
          schema:
            type: array
        - name: a_location
          in: query
          description: 'The list of location keys to filter results. If it''s empty, schedule for all locations will be returned. All given locations should be from the same business, which is sent in {@link ClassListApi::$k_business}.'
          schema:
            type: array
        - name: a_time
          in: query
          description: 'Class filter by time of day. List of arrays with start and end time in "HH:MM" format (24h). Include sessions that start between the specified time range. Each value is array with time parameters: Empty array means no filtering. <dl> <dt>int `tl_start`</dt> <dd>Time when the session starts. Example: value `''06:00''`.</dd> <dt>int `tl_end`</dt> <dd>Time when the session ends. Example: value `''14:00''`.</dd> </dl>'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The list start date in UTC and in MySQL format.'
          schema:
            type: string
        - name: dt_end
          in: query
          description: 'The list end date in UTC and in MySQL format. <i> If left empty, the default duration is {@link ClassListApi::DEFAULT_PERIOD} days after {@link ClassListApi::$dt_date}. </i>'
          schema:
            type: string
        - name: is_response_short
          in: query
          description: '`true` means to not generate {@link ClassListApi::$a_session} result. Can be used, if you do not need full information about existing classes and result in {@link ClassListApi::$a_calendar} is enough.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: 'If `true`, sessions from every class tab are returned. If `false`, use the {@link ClassListApi::$k_class_tab} value.'
          schema:
            type: boolean
        - name: is_virtual
          in: query
          description: 'Class filter by type. The class is virtual. `true`: Only virtual classes. `false`: Only in-person. `null` or not set: No filtering.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'The category tab key. This will be `null` if not set yet. This will be ignored if {@link ClassListApi::$is_tab_all} is `true`.'
          schema:
            type: string
        - name: s_staff
          in: query
          description: 'The list of staff members to filter. A comma separated list of staff keys.'
          schema:
            type: string
        - name: s_staff_uid
          in: query
          description: 'The list of staff user keys to filter. A comma separated list of staff user keys.'
          schema:
            type: string
        - name: show_cancel
          in: query
          description: 'If `true`, canceled sessions will be returned. If `false`, canceled sessions won''t be returned.'
          schema:
            type: boolean
        - name: show_class
          in: query
          description: 'If `true`, classes will be included in the response. `false` - otherwise.'
          schema:
            type: boolean
        - name: show_event
          in: query
          description: 'If `true`, events are also returned. If `false`, only classes are returned.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_calendar:
                        type: object
                        description: 'Keys are dates of the days inside requested date range, when there is at least one class in the business. If locations are sent as a parameter, then at least one class must exist in the given locations. Each key is a date string in `YYYY-MM-DD` format (local date in the business time zone). Each value is an empty array reserved for future use.'
                      a_session:
                        type: array
                        description: 'A list of classes sessions starting with the date {@link ClassListApi::$dt_date} and in the 62 days ahead (or up to {@link ClassListApi::$dt_end}). Every element has the following keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>Keys of class tab. <i>Primary keys in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i></dd> <dt>string[] `a_image`</dt> <dd>The class image. Empty array if there is no image.</dd> <dt>string[] `a_search_tag`</dt> <dd>Tags associated with an individual class.</dd> <dt>string[] `a_staff`</dt> <dd> The list of staff keys for the staff member conducting the session. For legacy third-party apps listed in {@link \Wl\Schedule\ClassList\ClassListApi::APPS_USE_OLD_K_STAFF}, contains <i>primary keys in {@link \RsStaffSql} table</i> `k_staff` for backward compatibility. Empty for all other applications. Use `a_staff_uid` instead. </dd> <dt>string[] `a_staff_uid`</dt> <dd> The list of staff user keys for the staff member conducting the session. <i>Primary keys in {@link \PassportLoginSql} table.</i> </dd> <dt>string[] `a_virtual_location`</dt> <dd> The list of virtual locations keys. Each value is a location key. <i>Primary keys in {@link \RsLocationSql} table.</i> </dd> <dt>string `dt_date`</dt> <dd>The date/time of the session start in UTC.</dd> <dt>string `dt_time`</dt> <dd>The time of the session start in the local time zone.</dd> <dt>string `dtl_date`</dt> <dd>The date/time of session start in the location''s time zone.</dd> <dt>bool `hide_application`</dt> <dd> Specifies whether the class will be hidden in the White Label Achieve Client App. If `true`, it means that the class won''t be displayed. Otherwise, this will be `false` to indicate that the class will be displayed. </dd> <dt>string `html_description`</dt> <dd>The class description.</dd> <dt>int `i_book`</dt> <dd>Count of visits on this class.</dd> <dt>int|null `i_capacity`</dt> <dd>The capacity of the service. ''null'' indicates that the capacity is not set.</dd> <dt>int `i_day`</dt> <dd>The day of the week when session is occurred. Constant from {@link \ADateWeekSid}.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_wait`</dt> <dd>Number of clients in wait list.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>If `true`, this class period was canceled. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>If `true`, this is an event. Otherwise, this will be `false`.</dd> <dt>bool `is_virtual`</dt> <dd>If `true`, this class is virtual. Otherwise, this will be `false`.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>This will be `true` if user is only on the wait-list. Otherwise, this will be `false`.</dd> <dt>string `k_class`</dt> <dd>The class key. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The key of the session''s location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_title`</dt> <dd>The title of the session.</dd> <dt>string `url_book`</dt> <dd>The direct link to start booking on the WellnessLiving website.</dd> </dl>'
                      is_timezone_different:
                        type: boolean
                        description: 'If `true`, the list of sessions contains sessions from different time zones. Otherwise, this will be `false`.'
                      is_virtual_service:
                        type: boolean
                        description: 'If `true`, there exists at least one virtual service by a specified {@link ClassListApi::$k_business} and {@link ClassListApi::$k_class_tab}, Otherwise, this will be `false`.'
      tags:
        - ClassList-ClassList
  '/Wl/Schedule/ClassList/ClassList68.json':
    post:
      summary: 'This method is a modified Get method {@link ClassListApi::get()}. The difference is as follows: - Some data for filtering is now transmitted by the post method. Because the addition of the filters creates a scenario where we can easily reach the maximum URL length of GET requests and the browser refuse to send the request (situations with long class ID, event ID or staff ID lists). - Added generation of a separate ''Quick filter''. This generation is enabled using the flag {@link ClassList68Api::$show_quick_filter}. - Added a filter list of events {@link ClassList68Api::$a_event}. - Added a filter ID of tab {@link ClassList68Api::$id_class_tab}.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_class:
                  type: array
                  description: 'The list of classes keys to filter. Return sessions with matching class IDs. If it''s empty and {@link ClassList68Api::$show_class} is `true`, all classes will be returned.'
                a_class_tab:
                  type: [array, "null"]
                  description: 'List of tabs keys. This will be ignored if {@link ClassList68Api::$is_tab_all} is `true`. If list of tab keys is not empty, {@link ClassList68Api::$id_class_tab} is mandatory. `null` if no filtering by Book Now Tab is required.'
                a_day:
                  type: array
                  description: 'Class filter by day of the week. Array of number representing the days of the week. Return sessions matching the given weekdays. (7 = Sunday, 1 = Monday, ..., 6 = Saturday) Empty array means no filtering.'
                a_event:
                  type: array
                  description: 'The list of event keys to filter. Return sessions with matching event keys. If it''s empty and {@link ClassList68Api::$show_event} is `true`, all events will be returned.'
                a_location:
                  type: array
                  description: 'The list of location keys to filter results. If it''s empty, schedule for all locations will be returned. All given locations should be from the same business, which is sent in {@link ClassListApi::$k_business}.'
                a_time:
                  type: array
                  description: 'Class filter by time of day. List of arrays with start and end time in "HH:MM" format (24h). Include sessions that start between the specified time range. Each value is array with time parameters: Empty array means no filtering. <dl> <dt>int `tl_start`</dt> <dd>Time when the session starts. Example: value `''06:00''`.</dd> <dt>int `tl_end`</dt> <dd>Time when the session ends. Example: value `''14:00''`.</dd> </dl>'
                dt_date:
                  type: string
                  description: 'The list start date in MySQL format.'
                dt_end:
                  type: string
                  description: 'The list end date in MySQL format. <i> If left empty, the default duration is {@link ClassList68Api::DEFAULT_PERIOD} days after {@link ClassList68Api::$dt_date}. </i>'
                id_class_tab:
                  type: [number, "null"]
                  description: 'ID of tab. One of {@link TabSid} constants. This will be ignored if {@link ClassList68Api::$is_tab_all} is `true`. `null` if no filtering by tab is required.'
                is_response_short:
                  type: boolean
                  description: '`true` means to not generate {@link ClassList68Api::$a_session} result. Can be used, if you do not need full information about existing classes and result in {@link ClassList68Api::$a_calendar} is enough.'
                is_tab_all:
                  type: boolean
                  description: 'If `true`, sessions from every class tab are returned. If `false`, use the {@link ClassList68Api::$k_class_tab} or {@link ClassList68Api::$id_class_tab} to filter sessions by class tab.'
                is_virtual:
                  type: [boolean, "null"]
                  description: 'Class filter by type. The class is virtual. `true`: Only virtual classes. `false`: Only in-person. `null` or not set: No filtering.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_class_tab:
                  type: string
                  description: 'The tab key. This will be ignored if {@link ClassList68Api::$is_tab_all} is `true`.'
                s_staff:
                  type: string
                  description: 'The list of staff members to filter. A comma separated list of staff keys.'
                s_staff_uid:
                  type: string
                  description: 'The list of staff user keys to filter. A comma separated list of staff user keys.'
                show_cancel:
                  type: boolean
                  description: 'If `true`, canceled sessions will be returned. If `false`, canceled sessions won''t be returned.'
                show_class:
                  type: boolean
                  description: 'If `true`, classes will be included in the response. `false` - otherwise.'
                show_event:
                  type: boolean
                  description: 'If `true`, events are also returned. If `false`, only classes are returned.'
                show_quick_filter:
                  type: boolean
                  description: 'Whether to generate {@link ClassList68Api::$a_quick} a quick filter. If `true`, a quick filter will be generated. `false` otherwise.'
                uid:
                  type: string
                  description: 'The user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_calendar:
                        type: object
                        description: 'Keys are dates of the days inside requested date range, when there is at least one class in the business. If locations are sent as a parameter, then at least one class must exist in the given locations. Each key is a date string in `YYYY-MM-DD` format (local date in the business time zone). Each value is an empty array reserved for future use.'
                      a_quick:
                        type: array
                        description: 'Information about classes/events for quick filter. Every element has the following keys: For generating this filter, all filters are applied except: - {@link ClassList68Api::$a_class}; - {@link ClassList68Api::$a_event}; - {@link ClassList68Api::$show_class}; - {@link ClassList68Api::$show_event}. <dl> <dt>string `text_type`</dt> <dd>Type of class ("class" || "event")</dd> <dt>string `k_class`</dt> <dd>Class/event key. <i>Primary key from {@link \RsClassSql} table.</i></dd> <dt>string `s_class`</dt> <dd>Class/event title.</dd> <dt>int `i_class`</dt> <dd>Total sessions found.</dd> </dl>'
                      a_session:
                        type: array
                        description: 'A list of classes sessions starting with the date {@link ClassListApi::$dt_date} and in the 62 days ahead (or up to {@link ClassListApi::$dt_end}). Every element has the following keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>Keys of class tab. <i>Primary keys in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i></dd> <dt>string[] `a_image`</dt> <dd>The class image. Empty array if there is no image.</dd> <dt>string[] `a_search_tag`</dt> <dd>Tags associated with an individual class.</dd> <dt>string[] `a_staff`</dt> <dd> The list of staff keys for the staff member conducting the session. For legacy third-party apps listed in {@link \Wl\Schedule\ClassList\ClassListApi::APPS_USE_OLD_K_STAFF}, contains <i>primary keys in {@link \RsStaffSql} table</i> `k_staff` for backward compatibility. Empty for all other applications. Use `a_staff_uid` instead. </dd> <dt>string[] `a_staff_uid`</dt> <dd>The list of staff user keys for the staff member conducting the session.</dd> <dt>string[] `a_virtual_location`</dt> <dd> The list of virtual locations keys. Each value is a location key. <i>Primary keys in {@link \RsLocationSql} table.</i> </dd> <dt>string `dt_date`</dt> <dd>The date/time of the session start in UTC.</dd> <dt>string `dt_time`</dt> <dd>The time of the session start in the local time zone.</dd> <dt>string `dtl_date`</dt> <dd>The date/time of session start in the location''s time zone.</dd> <dt>bool `hide_application`</dt> <dd> Specifies whether the class will be hidden in the White Label Achieve Client App. If `true`, it means that the class won''t be displayed. Otherwise, this will be `false` to indicate that the class will be displayed. </dd> <dt>string `html_description`</dt> <dd>The class description.</dd> <dt>int `i_book`</dt> <dd>Count of visits on this class.</dd> <dt>int|null `i_capacity`</dt> <dd>The capacity of the service. ''null'' indicates that the capacity is not set.</dd> <dt>int `i_day`</dt> <dd>The day of the week when session is occurred. Constant from {@link \ADateWeekSid}.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_wait`</dt> <dd>Number of clients in wait list.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>If `true`, this class period was canceled. Otherwise, this will be `false`.</dd> <dt>bool `is_event`</dt> <dd>If `true`, this is an event. Otherwise, this will be `false`.</dd> <dt>bool `is_virtual`</dt> <dd>If `true`, this class is virtual. Otherwise, this will be `false`.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>This will be `true` if user is only on the wait-list. Otherwise, this will be `false`.</dd> <dt>string `k_class`</dt> <dd>The class key. <i>Primary key in {@link \RsClassSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>The key of the session''s location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `s_title`</dt> <dd>The title of the session.</dd> <dt>string `url_book`</dt> <dd>The direct link to start booking on the WellnessLiving website.</dd> </dl>'
                      is_timezone_different:
                        type: boolean
                        description: 'If `true`, the list of sessions contains sessions from different time zones. Otherwise, this will be `false`.'
                      is_virtual_service:
                        type: boolean
                        description: 'If `true`, there exists at least one virtual service by a specified {@link ClassList68Api::$k_business} and {@link ClassList68Api::$k_class_tab}, Otherwise, this will be `false`.'
      tags:
        - ClassList-ClassList68
  '/Wl/Schedule/Page/PageElement.json':
    get:
      summary: 'Retrieves information about one element of schedule. Returns full details for a single visit, including staff, location, timing, assigned assets, virtual join URL, and class-specific or appointment-specific information. Used to render the visit detail page in the client''s schedule view.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business to which the visit belongs.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_visit_info:
                        type: array
                        description: 'Additional visit information about this appointment. Empty array if it''s not an appointment. <dl> <dt>int `id_visit`</dt> <dd>Visit status. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_confirmed`</dt> <dd>`true` means that appointment was requested and confirmed by the staff.</dd> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl>'
                      a_asset:
                        type: array
                        description: 'List of assets: . <dl> <dt>string `s_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> </dl>'
                      a_class_info:
                        type: [array, "null"]
                        description: 'Class data: <tt>null</tt> if it isn''t class visit. <dl> <dt>int `i_book_active`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_wait`</dt> <dd>A total number of booked visits in the waitlist.</dd> </dl>'
                      a_resource_image:
                        type: [array, "null"]
                        description: 'Resource image data. `null` for all services except resources. <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See more {@link \Core\Drive\DriveImage::image()}.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Is set only if <var>sid_image</var> equals to <tt>shape</tt>.</dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `sid_image`</dt> <dd>Image kind. String representation of one of {@link \Wl\Resource\Image\ImageSid} constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name.String representation of one of {@link \Wl\Resource\Image\ImageIconSid} constants. Is set only if <var>sid_image</var> equals to <tt>image</tt>. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. String representation of one of {@link \Wl\Resource\Image\ImageShapeSid} constants. Is set only if <var>sid_image</var> equals to <tt>shape</tt>. </dd> <dt>string `url`</dt> <dd>Path to image. <i>See {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> </dl>'
                      a_staff:
                        type: array
                        description: 'A list of staff members involved in the visit. Each element is an array with the following elements: <dl> <dt>string `k_staff`</dt> <dd>The staff member key. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `s_family`</dt> <dd>The surname of the staff member.</dd> <dt>string `s_name`</dt> <dd>The first name of the staff member.</dd> </dl>'
                      dt_cancel:
                        type: string
                        description: 'The latest date and time for when the visit can be canceled without penalty.'
                      dt_date_global:
                        type: string
                        description: 'The date and time of the visit in UTC.'
                      dt_date_local:
                        type: string
                        description: 'The date and time of the visit in the local time zone.'
                      dtl_location:
                        type: string
                        description: 'Session date/time in timezone of the location where it takes place.'
                      html_description:
                        type: string
                        description: 'The description of the service.'
                      html_special:
                        type: string
                        description: 'The special instructions for the service.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Capacity of the service. <tt>null</tt> indicates that the capacity is not set.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The scheduled duration of the visit. <tt>0</tt> means that session duration is hidden.'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'Estimated place of reservation on the waiting list.'
                      id_note:
                        type: [number, "null"]
                        description: 'The note type ID. This will be set to `null` if notes aren''t allowed. This is one of the {@link \Wl\Visit\Note\Sid\NoteSid} constants. This will be `null` if notes aren''t allowed.'
                      id_virtual_provider:
                        type: [number, "null"]
                        description: 'The virtual provider ID. One of the {@link \Wl\Virtual\VirtualProviderSid} constants. This will be `null` for non-virtual services.'
                      id_visit:
                        type: number
                        format: int32
                        description: 'The visit type ID. One of the {@link VisitSid} constants. This will be `null` if not loaded yet.'
                      is_checkin:
                        type: boolean
                        description: 'If `true`, then this visit is ready to be checked in. If `false`, then this visit can''t be checked in.'
                      is_enable_client_cancel:
                        type: boolean
                        description: 'This will be `true` if clients can cancel the session. Otherwise, this will be `false`.'
                      is_event:
                        type: boolean
                        description: 'If `true`, then this visit is a part of a larger event. If `false`, then this visit is an individual session.'
                      is_in_progress:
                        type: boolean
                        description: '<tt>true</tt> - service is in progress; <tt>false</tt> - otherwise.'
                      is_virtual:
                        type: boolean
                        description: '<tt>true</tt> - service is virtual; <tt>false</tt> - otherwise.'
                      k_appointment:
                        type: [string, "null"]
                        description: 'The appointment key. This will be set only if the visit is an appointment. If the visit is a class or event, this will be `null`.'
                      k_class:
                        type: [string, "null"]
                        description: 'The class key. This will be set only if the visit is a class or an event. If the visit is an appointment, this will be `null`.'
                      k_class_period:
                        type: [string, "null"]
                        description: 'The class period key. This will be set only if the visit is a class or an event. If the visit is an appointment, this will be `null`.'
                      k_location:
                        type: string
                        description: 'The location key.'
                      k_resource:
                        type: [string, "null"]
                        description: 'Resource key.'
                      k_service:
                        type: [string, "null"]
                        description: 'Session key.'
                      s_title:
                        type: string
                        description: 'The name of class or service.'
                      text_location:
                        type: string
                        description: 'Location title.'
                      text_room:
                        type: string
                        description: 'Room where session takes place.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
                      uid:
                        type: string
                        description: 'The user key.'
                      url_image:
                        type: string
                        description: 'URL of the image representing the service or resource.'
                      url_virtual_join:
                        type: string
                        description: 'Url to wait page is the beginning of an online service.'
      tags:
        - Page-PageElement
  '/Wl/Schedule/Page/PageList.json':
    get:
      summary: 'Retrieves items of schedule for the client. Returns the client''s upcoming or past visits for a given business, ordered by date. Supports optional date range boundaries to retrieve visits within a specific window. Used to populate the schedule history and upcoming bookings pages in the client portal.'
      parameters:
        - name: dtu_end
          in: query
          description: 'If the date is set, a list of services before this date will be returned. The date and time is in UTC and in MySQL format. If left as `null`, then a list of services that aren''t limited to the end date will be returned.'
          schema:
            type: [string, "null"]
        - name: dtu_start
          in: query
          description: 'If the date is set, a list of services after this date will be returned. The date and time is in UTC and in MySQL format. If left as `null`, then a list of services that aren''t limited to the start date will be returned.'
          schema:
            type: [string, "null"]
        - name: is_past
          in: query
          description: 'If `true`, then all the client previous visits will be retrieved. If `false` or left as `null`, then all the client upcoming visits will be retrieved.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit:
                        type: array
                        description: 'Elements of user''s schedule. Every element has next keys: <dl> <dt>string `dtu_date`</dt> <dd>Date and time of the visit in UTC.</dd> <dt>string `k_business`</dt> <dd>Key of the business in which this visit was made. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_visit`</dt> <dd>Key of a book/visit. <i>Primary key in {@link \RsVisitSql} table.</i></dd> <dt>int `id_visit`</dt> <dd>Visit status ID. One of {@link \Wl\Visit\VisitSid} constants.</dd> </dl>'
      tags:
        - Page-PageList
  '/Wl/Schedule/Config/ConfigDefault.json':
    post:
      summary: ''
      parameters:
        - name: k_schedule_config
          in: query
          description: 'ID of the predefined set of filters on the schedule.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_default:
                  type: boolean
                  description: 'ID of the predefined set of filters on the schedule.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Config-ConfigDefault
  '/Wl/Schedule/ScheduleList/ScheduleList.json':
    get:
      summary: 'Retrieves schedule of sessions.'
      parameters:
        - name: dt_date
          in: query
          description: 'Date to show information for. In MySQL format, without time.'
          schema:
            type: string
        - name: id_class_tab
          in: query
          description: 'ID of group of category tab. One of {@link \Wl\Classes\Tab\TabSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_class_tab
          in: query
          description: 'ID of category tab in database.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'ID of business location to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of user to show information for. Primary key in table {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_schedule:
                        type: array
                        description: 'Schedule of sessions in location {@link \Wl\Schedule\ScheduleList\ScheduleListApi::$k_location} in day {@link \Wl\Schedule\ScheduleList\ScheduleListApi::$dt_date}. Every element has keys: <dl> <dt>array `a_staff`</dt> <dd> List of staff: <dl> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Determines current staff member as substituted.</dd> <dt>string `s_staff`</dt> <dd>Full name of staff.</dd> </dl> </dd> <dt>bool `can_book`</dt> <dd><tt>true</tt> - session can be booked. <tt>false</tt> - otherwise.</dd> <dt>string `dt_date_local`</dt> <dd>Date/time of session start (in local timezone).</dd> <dt>string `dt_date_gmt`</dt> <dd>Date/time of session start (in GMT).</dd> <dt>int `i_duration`</dt> <dd>Session duration (in minutes).</dd> <dt>bool `is_wait_list`</dt> <dd><tt>true</tt> if user can take place in wait list only; <tt>false</tt> otherwise.</dd> <dt>string `k_class_period`</dt> <dd>Session ID. Primary key in {@link \RsClassPeriodSql} table.</dd> <dt>string `s_title`</dt> <dd>Class title.</dd> </dl>'
                      is_classes_available:
                        type: boolean
                        description: 'Is there any classes available for booking.'
      tags:
        - ScheduleList-ScheduleList
  '/Wl/Schedule/ClassView/ClassView.json':
    get:
      summary: 'Retrieves information about class. Returns detailed information about one or more class sessions, including staff, location, assigned assets, virtual location links, visit counts, and booking availability. Supports single-session mode for one class period or multi-session mode for batch lookups.'
      parameters:
        - name: a_session_request
          in: query
          description: 'A list of sessions to get information for. Every element has the following keys: `null` if requesting a single session. <dl> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `k_class_period`</dt> <dd>The session key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'The date/time of the session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the action is performed. `null` if key of the business was not passed. Key of the business is required if {@link ClassViewApi} was passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_asset:
                        type: [array, "null"]
                        description: 'Asset list data. <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl>'
                      a_class:
                        type: [array, "null"]
                        description: 'Detailed information about the class. This will be `null` if data isn''t loaded yet. <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int|null `i_age_from`</dt> <dd>Age from that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int|null `i_age_to`</dt> <dd>Age to that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl>'
                      a_location:
                        type: [array, "null"]
                        description: 'Location data. This will be `null` if data isn''t loaded yet. <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl>'
                      a_session_result:
                        type: array
                        description: 'A list of sessions with information, received in a multiple session mode. <dl> <dt>array `a_asset`</dt> <dd> Array of asset. <i>Result of {@link \Wl\Service\Asset::assetsUsingResourceBusyList()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl> </dd> <dt>array `a_class`</dt> <dd> Class information. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_classInfo()}</i> <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int|null `i_age_from`</dt> <dd>Age from that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int|null `i_age_to`</dt> <dd>Age to that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl> </dd> <dt>array `a_location`</dt> <dd> Location info. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_locationInfo()}</i> <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff list. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_staffInfo()} method.</i> <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd> List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations. </dd> <dt>array[] `a_visits_required`</dt> <dd> List of classes and events, which client should visit before this one. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::visitsRequiredInfo()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: [array, "null"]
                        description: 'Staff member list data. This will be `null` if data isn''t loaded yet. <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl>'
                      a_virtual_location:
                        type: array
                        description: 'List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations.'
                      a_visits_required:
                        type: array
                        description: 'A list of classes and events that clients should visit before this one. <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl>'
      tags:
        - ClassView-ClassView
    post:
      summary: 'Retrieves information about classes. A POST-method alternative to {@link ClassViewApi::get()} for multi-session batch lookups where the session list is too long to encode in a GET query string. The session list is provided as a JSON-encoded string in the request body.'
      parameters:
        - name: a_session_request
          in: query
          description: 'A list of sessions to get information for. Every element has the following keys: `null` if requesting a single session. <dl> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `k_class_period`</dt> <dd>The session key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'The date/time of the session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the action is performed. `null` if key of the business was not passed. Key of the business is required if {@link ClassViewApi} was passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_session_request:
                  type: string
                  description: 'A list of sessions to get information for. A serialized array. See {@link ClassViewApi::$a_session_request} for the array structure. Serialization and sending by POST is necessary to send big lists.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_asset:
                        type: [array, "null"]
                        description: 'Asset list data. <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl>'
                      a_class:
                        type: [array, "null"]
                        description: 'Detailed information about the class. This will be `null` if data isn''t loaded yet. <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int|null `i_age_from`</dt> <dd>Age from that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int|null `i_age_to`</dt> <dd>Age to that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl>'
                      a_location:
                        type: [array, "null"]
                        description: 'Location data. This will be `null` if data isn''t loaded yet. <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl>'
                      a_session_result:
                        type: array
                        description: 'A list of sessions with information, received in a multiple session mode. <dl> <dt>array `a_asset`</dt> <dd> Array of asset. <i>Result of {@link \Wl\Service\Asset::assetsUsingResourceBusyList()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of sessions.</dd> <dt>int `i_index`</dt> <dd>Asset index.</dd> <dt>int `id_category`</dt> <dd>Type of the asset: Asset or Off-Site Location. One of {@link \Wl\Resource\ResourceCategoryEnum} constant.</dd> <dt>string `k_city`</dt> <dd>City of the asset, if this is Off-Site Location.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `text_address`</dt> <dd>Address of the asset, if this is Off-Site Location.</dd> <dt>string `text_guide`</dt> <dd>Additional address guidance, if this is Off-Site Location.</dd> <dt>string `text_index`</dt> <dd>Asset index with ''#'' prefix.</dd> <dt>string `text_postal`</dt> <dd>Postal code of the asset, if this is Off-Site Location.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> </dl> </dd> <dt>array `a_class`</dt> <dd> Class information. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_classInfo()}</i> <dl> <dt>string[] `a_class_tab`</dt> <dd> Keys are class key. <i>Primary key in {@link \RsClassSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\ClassTabAr} table.</i> </dd> <dt>array `a_image`</dt> <dd> Class image data: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>bool `is_empty`</dt> <dd>Whether current image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl> </dd> <dt>array[] `a_search_tag`</dt> <dd> List of search tags. <dl> <dt>string `k_search_tag`</dt> <dd>Search tag key. <i>Primary key in {@link \RsSearchTagSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Search tag name.</dd> </dl> </dd> <dt>int[] `a_tag`</dt> <dd>List of tags.</dd> <dt>bool `can_book`</dt> <dd>Whether current client can book class.</dd> <dt>string `dt_date_global`</dt> <dd>Session date/time in UTC.</dd> <dt>string `dt_date_local`</dt> <dd>Start date in local time.</dd> <dt>string `html_deny_reason`</dt> <dd>Reason why client can not book class. Not empty only if <var>can_book</var> is `false`.</dd> <dt>string `html_description`</dt> <dd>Class description.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>string `text_timezone`</dt> <dd>Timezone title.</dd> <dt>int|null `i_age_from`</dt> <dd>Age from that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int|null `i_age_to`</dt> <dd>Age to that class is allowed. <tt>null</tt> if information is not available.</dd> <dt>int `i_book`</dt> <dd>A total number of booked visits in the class, including all lists: active and waitlist.</dd> <dt>int `i_book_active`</dt> <dd>A total number of booked active visits in the class.</dd> <dt>int `i_capacity`</dt> <dd>Class capacity.</dd> <dt>int `i_duration`</dt> <dd>Class duration. In number of minutes.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Limit of wait list. <tt>null</tt> if limit is not set.</dd> <dt>int `id_deny_reason`</dt> <dd>ID of deny reason. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants.</dd> <dt>bool `is_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_book_for_guest`</dt> <dd> Allow clients to book on behalf of a guest. `true` if clients can book on behalf of a guest. `false` otherwise. </dd> <dt>bool `is_cancel`</dt> <dd>`true` if class period was cancelled; `false` otherwise.</dd> <dt>bool `is_event`</dt> <dd>`true` if it is event; `false` if it is class.</dd> <dt>bool `is_promotion_only`</dt> <dd>`true` if this class can be paid with promotion only; `false` otherwise.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if class is virtual, `false` otherwise.</dd> <dt>bool `is_wait_list`</dt> <dd>`true` if user can take place in wait list only; `false` otherwise.</dd> <dt>bool `is_wait_list_enabled`</dt> <dd>`true` if wait list is enabled for class; `false` otherwise.</dd> <dt>string `k_resource_location`</dt> <dd>Off-site location asset key. Empty if off-site location is not assigned to this class.</dd> <dt>string `m_price`</dt> <dd>Session price.</dd> <dt>string `s_duration`</dt> <dd>Class duration. In human readable format.</dd> <dt>string `s_title`</dt> <dd>Class name.</dd> <dt>string `text_room`</dt> <dd>Class room.</dd> </dl> </dd> <dt>array `a_location`</dt> <dd> Location info. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_locationInfo()}</i> <dl> <dt>float `f_latitude`</dt> <dd>Location latitude.</dd> <dt>float `f_longitude`</dt> <dd>Location longitude.</dd> <dt>float `f_rate`</dt> <dd>Location rating. From 1 to 5.</dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `s_address`</dt> <dd>Location address.</dd> <dt>string `s_map`</dt> <dd>Query to search location on "Google maps".</dd> <dt>string `s_phone`</dt> <dd>Location phone.</dd> <dt>string `s_title`</dt> <dd>Location name.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff list. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::_staffInfo()} method.</i> <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd> List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations. </dd> <dt>array[] `a_visits_required`</dt> <dd> List of classes and events, which client should visit before this one. <i>Result of {@link \Wl\Schedule\ClassView\ClassViewApi::visitsRequiredInfo()} method.</i> <dl> <dt>int `i_count`</dt> <dd>Number of visits.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>Key of the class or event.</dd> <dt>string `text_title`</dt> <dd>Name of the class or event.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: [array, "null"]
                        description: 'Staff member list data. This will be `null` if data isn''t loaded yet. <dl> <dt>array `a_logo`</dt> <dd> Information about staff photo: <dl> <dt>int `id_gender`</dt> <dd>ID of gender. One of {@link \Wl\Gender\GenderSid} constants.</dd> <dt>bool `is_empty`</dt> <dd>`true` - staff has photo; `false` - has no photo.</dd> <dt>string `s_url`</dt> <dd>URL to staff photo.</dd> </dl> </dd> <dt>bool `is_quick_substitute`</dt> <dd>Whether staff or pay rate changed due quick substitution.</dd> <dt>bool `is_substitute`</dt> <dd>Whether or not this staff member is a substitute.</dd> <dt>string `k_staff`</dt> <dd> <i>Staff primary key in {@link \RsStaffSql} table.</i> Deprecated use `uid_staff` instead. <i>Returned only for applications in {@link \Wl\Schedule\ClassView\ClassViewApi::APPS_USE_OLD_K_STAFF} allow-list.</i> </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `s_family`</dt> <dd>1st letter of surname of staff member.</dd> <dt>string `s_name`</dt> <dd>Staff name.</dd> </dl>'
                      a_virtual_location:
                        type: array
                        description: 'List of other locations where virtual class can be booked. Empty array if class isn''t virtual or can''t be booked in other locations.'
      tags:
        - ClassView-ClassView
  '/Wl/Schedule/Tab/Tab.json':
    get:
      summary: 'Gets information about tabs for page "Book now". Returns the set of service booking tabs configured by the business, filtered for the specified location and user. Used to populate the tab strip on the "Book Now" page or embedded widget. Tabs may represent classes, appointments, events, or other bookable services.'
      parameters:
        - name: is_widget
          in: query
          description: 'Whether we are inside the widget or not.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the current business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the current location.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user. This isn''t used on the back end as the back end uses the current user to build a list of tabs. Therefore, the model must depend on the current user ID. If changed to a relative (the current user is changed, but the application isn''t restarted), information about the booking tabs for this user''s relative will be requested. This will be `null` if not set yet.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tab:
                        type: array
                        description: 'An array containing information about tabs to present to the user. Each array index corresponds to a tab. Each array element is an array that contains the following fields: <dl> <dt>int `id_class_tab_object`</dt> <dd>The tab type ID which is one of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>int|null `id_class_tab_system`</dt> <dd> If this tab has redefined a default system Class Tab then it references a constant defined in {@link \Wl\Classes\Tab\TabSid}. Otherwise, this will be `null`. </dd> <dt>string|null `k_class_tab`</dt> <dd>The class tab key. This will be `null` if it''s a system tab.</dd> <dt>string `k_id`</dt> <dd>A unique identifier in the list.</dd> <dt>string `s_title`</dt> <dd>The tab title.</dd> </dl>'
      tags:
        - Tab-Tab
  '/Wl/Schedule/WorkingTime/StaffWorking.json':
    get:
      summary: 'Gets working hours for some staff.'
      parameters:
        - name: dl_end
          in: query
          description: 'End date of the staff working.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Start date of the staff working.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule. Primary key in {@link \RsLocationSql} table.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service primary key in {@link \RsServiceSql} table.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'Staff Key to show what days are available for appointment booking. Primary key in {@link \RsStaffSql} table.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_period:
                        type: array
                        description: 'Staff period.'
      tags:
        - WorkingTime-StaffWorking
  '/Wl/Schedule/HolidayView/HolidayView.json':
    get:
      summary: 'Retrieves information about holiday.'
      parameters:
        - name: a_holiday_request
          in: query
          description: 'List of request holiday data to get information for. Each element has next structure: <tt>null</tt> for get info about single holiday by {@link HolidayViewApi::$k_holiday}. <dl> <dt>string `dt_date`</dt> <dd>Holiday date.</dd> <dt>string `k_holiday`</dt> <dd>Primary key in the {@link \RsHolidaySql} table.</dd> </dl>'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'Holiday date to show. <tt>null</tt> for get info about single holiday by {@link HolidayViewApi::$a_holiday_request}.'
          schema:
            type: [string, "null"]
        - name: k_holiday
          in: query
          description: 'Holiday key. <tt>null</tt> for get info about single holiday by {@link HolidayViewApi::$a_holiday_request}.'
          schema:
            type: [string, "null"]
        - name: text_timezone
          in: query
          description: 'Client timezone name to show. <tt>null</tt> when client not selected timezone in the profile.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_holiday_info:
                        type: array
                        description: 'Holiday data. See {@link HolidayViewApi::_getHolidayInfo()} method.'
                      a_holiday_result:
                        type: array
                        description: 'List of data for requested holidays. See {@link HolidayViewApi::_getHolidayInfo()} method.'
      tags:
        - HolidayView-HolidayView
  '/Wl/Schedule/ScheduleDesign/ScheduleDesign.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_interval:
                        type: number
                        format: int32
                        description: 'Schedule time interval in minutes. One of {@link IntervalSid} constants. 0 if not set yet.'
                      id_cell:
                        type: number
                        format: int32
                        description: 'Cell size. One of {@link CellSid} constants. 0 if not set yet.'
                      id_option:
                        type: number
                        format: int32
                        description: 'Option of appointments display. One of {@link OptionSid} constants. 0 if not set yet.'
                      id_start_week:
                        type: number
                        format: int32
                        description: 'Day ID of the start week. Constant from {@link WeekDaySid}. 0 if not set yet.'
                      is_appointment_alert:
                        type: [boolean, "null"]
                        description: 'Determines whether to show the first profile alert on the schedule. `true` - to show the first profile alert on the schedule. `false` not to show. `null` if not set.'
                      is_appointment_cancel_recurring:
                        type: [boolean, "null"]
                        description: '`true` - If needed to show recurring canceled appointments on schedule. `false` - otherwise. Current setting from business schedule design. `null` if not set.'
                      is_appointment_cancel_single:
                        type: [boolean, "null"]
                        description: '`true` - If needed to show single canceled appointments on schedule. `false` - otherwise. Current setting from business schedule design. `null` if not set.'
                      is_class_cancel:
                        type: [boolean, "null"]
                        description: '`true` - If needed to show canceled classes on schedule. `false` - otherwise. Current setting from business schedule design. `null` if not set.'
                      is_diagonal_staff_busy:
                        type: [boolean, "null"]
                        description: '`true` If needed to show staff unavailable times with diagonal lines, `false` - otherwise. `null` if not set.'
                      is_drag_and_drop:
                        type: [boolean, "null"]
                        description: 'Whether sessions can be dragged and dropped on the schedule. `true` if sessions can be dragged and dropped, `false` otherwise. `null` if not set.'
                      is_form_icon:
                        type: [boolean, "null"]
                        description: 'Determines whether to show the forms icon on the schedule. `true` means to show the forms icon on the schedule, `false` not to show. `null` if not set.'
                      is_service_icon:
                        type: [boolean, "null"]
                        description: 'Determines whether to show icon in the corner. `true` means to show icon in the corner, `false` otherwise. `null` if not set.'
                      is_visit_note:
                        type: [boolean, "null"]
                        description: 'Determines whether to show QUICK and SOAP notes preview on the schedule. `true` means to show QUICK and SOAP notes preview on the schedule, `false` not to show. `null` if not set.'
                      is_work_note:
                        type: [boolean, "null"]
                        description: '`true` - if need to show work note, `false` - otherwise. `null` if not set.'
                      is_work_only:
                        type: [boolean, "null"]
                        description: 'Whether only business hours should be shown on schedule. `true` if only business hours should be shown on schedule. `false` otherwise. `null` if not set.'
                      s_color_staff_available:
                        type: [string, "null"]
                        description: 'Cell background color when staff member is available. `null` if not set.'
                      s_color_staff_busy:
                        type: [string, "null"]
                        description: 'Cell background color when staff member is not available. `null` if not set.'
                      s_color_staff_substitute:
                        type: [string, "null"]
                        description: 'Color of staff name when staff was substituted. `null` if not set.'
                      show_booking_after_book:
                        type: [boolean, "null"]
                        description: 'Whether to scroll schedule to last booked service. `true` to scroll, `false` otherwise. `null` if not set.'
      tags:
        - ScheduleDesign-ScheduleDesign
    post:
      summary: 'Saves schedule design settings including schedule config settings.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_interval:
                  type: number
                  format: int32
                  description: 'Schedule time interval in minutes. One of {@link IntervalSid} constants. 0 if not set yet.'
                id_cell:
                  type: number
                  format: int32
                  description: 'Cell size. One of {@link CellSid} constants. 0 if not set yet.'
                id_option:
                  type: number
                  format: int32
                  description: 'Option of appointments display. One of {@link OptionSid} constants. 0 if not set yet.'
                id_start_week:
                  type: number
                  format: int32
                  description: 'Day ID of the start week. Constant from {@link WeekDaySid}. 0 if not set yet.'
                is_appointment_alert:
                  type: [boolean, "null"]
                  description: 'Determines whether to show the first profile alert on the schedule. `true` - to show the first profile alert on the schedule. `false` not to show. `null` if not set.'
                is_appointment_cancel_recurring:
                  type: [boolean, "null"]
                  description: '`true` - If needed to show recurring canceled appointments on schedule. `false` - otherwise. Current setting from business schedule design. `null` if not set.'
                is_appointment_cancel_single:
                  type: [boolean, "null"]
                  description: '`true` - If needed to show single canceled appointments on schedule. `false` - otherwise. Current setting from business schedule design. `null` if not set.'
                is_class_cancel:
                  type: [boolean, "null"]
                  description: '`true` - If needed to show canceled classes on schedule. `false` - otherwise. Current setting from business schedule design. `null` if not set.'
                is_diagonal_staff_busy:
                  type: [boolean, "null"]
                  description: '`true` If needed to show staff unavailable times with diagonal lines, `false` - otherwise. `null` if not set.'
                is_drag_and_drop:
                  type: [boolean, "null"]
                  description: 'Whether sessions can be dragged and dropped on the schedule. `true` if sessions can be dragged and dropped, `false` otherwise. `null` if not set.'
                is_form_icon:
                  type: [boolean, "null"]
                  description: 'Determines whether to show the forms icon on the schedule. `true` means to show the forms icon on the schedule, `false` not to show. `null` if not set.'
                is_service_icon:
                  type: [boolean, "null"]
                  description: 'Determines whether to show icon in the corner. `true` means to show icon in the corner, `false` otherwise. `null` if not set.'
                is_visit_note:
                  type: [boolean, "null"]
                  description: 'Determines whether to show QUICK and SOAP notes preview on the schedule. `true` means to show QUICK and SOAP notes preview on the schedule, `false` not to show. `null` if not set.'
                is_work_note:
                  type: [boolean, "null"]
                  description: '`true` - if need to show work note, `false` - otherwise. `null` if not set.'
                is_work_only:
                  type: [boolean, "null"]
                  description: 'Whether only business hours should be shown on schedule. `true` if only business hours should be shown on schedule. `false` otherwise. `null` if not set.'
                s_color_staff_available:
                  type: [string, "null"]
                  description: 'Cell background color when staff member is available. `null` if not set.'
                s_color_staff_busy:
                  type: [string, "null"]
                  description: 'Cell background color when staff member is not available. `null` if not set.'
                s_color_staff_substitute:
                  type: [string, "null"]
                  description: 'Color of staff name when staff was substituted. `null` if not set.'
                show_booking_after_book:
                  type: [boolean, "null"]
                  description: 'Whether to scroll schedule to last booked service. `true` to scroll, `false` otherwise. `null` if not set.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ScheduleDesign-ScheduleDesign
  '/Wl/Shop/Category/Category.json':
    get:
      summary: 'Returns the list of shop categories available for the given business. Returns all store categories that the current user is allowed to access, including their titles, descriptions, and display order. Guest and client users see only public categories; staff and admin users may see additional categories based on their access level.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get shop categories for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_shop_category:
                        type: array
                        description: 'An array containing information about all store categories. Each array element is an array containing the following fields: <dl> <dt>bool `is_default`</dt> <dd>Whether category is selected as default.</dd> <dt>int `i_order`</dt> <dd>The display order for the category.</dd> <dt>string `k_shop_category`</dt> <dd>The shop category key. <i>Primary key in {@link \RsShopCategorySql} table.</i></dd> <dt>string `text_description`</dt> <dd>The category description.</dd> <dt>string `text_title`</dt> <dd>The category name.</dd> </dl>'
      tags:
        - Category-Category
    post:
      summary: 'Creates new shop category. Creates a new product category in the business store with the given title. Requires backend access with the appropriate store management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get shop categories for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'Title of new category.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_shop_category:
                        type: string
                        description: 'Key of the created shop category.'
      tags:
        - Category-Category
  '/Wl/Shop/Supplier/ShopSupplier.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'Suppliers info.'
      tags:
        - Supplier-ShopSupplier
  '/Wl/Collector/DebtStatus/DebtStatus.json':
    get:
      summary: ''
      parameters:
        - name: id_currency
          in: query
          description: 'The currency for which status should be retrieved. One of {@link CurrencySid} constants. `null` if status should be retrieved for current business currency.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The key of the business within which status of the user should be retrieved.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user for which status should be retrieved.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_sent:
                        type: [string, "null"]
                        description: 'Date and time when user sent to collection agency. `null` if user has no debts.'
                      dtl_since:
                        type: [string, "null"]
                        description: 'Date and time since user in given debt status. `null` if user has no debts.'
                      dtu_sent:
                        type: [string, "null"]
                        description: 'Date and time when user sent to collection agency. `null` if user has no debts.'
                      dtu_since:
                        type: [string, "null"]
                        description: 'Date and time since user in given debt status. `null` if user has no debts.'
                      i_day_due:
                        type: number
                        format: int32
                        description: 'Number of days past due.'
                      id_currency:
                        type: [number, "null"]
                        description: 'The currency for which status should be retrieved. One of {@link CurrencySid} constants. `null` if status should be retrieved for current business currency.'
                      id_status:
                        type: number
                        format: int32
                        description: 'The debt status ID of the client. One of {@link DebtStatusSid} constants. `0` if collections are not supported or status could not be defined.'
                      k_collector_debt:
                        type: [string, "null"]
                        description: 'Active debt key. Primary key in {@link \Wl\Collector\CollectorDebtSql}. `null` if user has no debts.'
                      m_debt_current:
                        type: [string, "null"]
                        description: 'Current debt amount. `null` if user has no debts.'
                      m_debt_future:
                        type: [string, "null"]
                        description: 'Future debt amount. `null` if user has no debts.'
                      text_comment:
                        type: [string, "null"]
                        description: 'Additional note to the debt. `null` if user has no debts.'
      tags:
        - DebtStatus-DebtStatus
  '/Wl/Profile/Alert/Alert.json':
    get:
      summary: 'Retrieves a list of notices to show in user''s profile. Returns `a_alert` (system alerts including unconfirmed contracts) and `a_warning` (login notes and flags with author and editor information) for the specified user in the given business.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_alert:
                        type: array
                        description: 'A list of alerts. Every element is an array with the following keys: <dl> <dt>string|null `dt_date`</dt> <dd> The date and time in MySQL format in local time when the alert was last modified. This will be `null` if the date isn''t available for this alert. </dd> <dt>bool `is_today`</dt> <dd>This will be `true` if the alert was added today. Otherwise, this will be `false`.</dd> <dt>int `id_profile_note`</dt> <dd>ID of alert type. One of {@link \RsProfileNoteSid} constants.</dd> <dt>string `k_pay_account`</dt> <dd>The key of the client''s account. <i>Primary key in {@link \RsPayAccountSql} table.</i></dd> <dt>string `k_purchase_item`</dt> <dd>The key of the purchase item. <i>Primary key in {@link \RsPurchaseItemSql} table.</i></dd> <dt>string `s_text`</dt> <dd>The alert''s text.</dd> </dl>'
                      a_warning:
                        type: array
                        description: 'A list of warnings. Every element is an array with the following keys: <dl> <dt>string[] `a_location_flag`</dt> <dd>The list of location keys where this note is flagged. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>bool `dt_create`</dt> <dd>The date and time in MySQL format in local time when the warning was created.</dd> <dt>bool `dt_date`</dt> <dd>The date and time in MySQL format in local time when the warning was last modified.</dd> <dt>bool `is_book_restricted`</dt> <dd>`true` means that client is restricted to make bookings, `false` - otherwise.</dd> <dt>bool `is_flag`</dt> <dd>This will be `true` if the client is flagged. Otherwise, this will be `false`.</dd> <dt>bool `is_purchase_restricted`</dt> <dd>`true` means that client is restricted to make purchases. Otherwise, this will be `false`.</dd> <dt>bool `is_today`</dt> <dd>This will be `true` if the warning was added today. Otherwise, this will be `false`.</dd> <dt>string `s_text`</dt> <dd>The text of the warning.</dd> <dt>string `text_author`</dt> <dd>Staff name, who created a note.</dd> <dt>string `text_editor`</dt> <dd>Staff name, who edited a note last time.</dd> </dl>'
      tags:
        - Alert-Alert
  '/Wl/Profile/Alert/AlertEdit.json':
    delete:
      summary: 'Deletes existing note. Permanently removes the login note specified by `k_login_note` from the business, requiring backend access for the current user.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_login_note
          in: query
          description: 'Login note key to edit or get info for. Empty if new entry is being added.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Alert-AlertEdit
    get:
      summary: 'Retrieves login note data. Loads a single login note for editing, including its text, access type, flag settings, booking and purchase restrictions, and location flags. Used to populate the edit form before saving changes.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_login_note
          in: query
          description: 'Login note key to edit or get info for. Empty if new entry is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information or post a note for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_note_data:
                        type: array
                        description: 'Login note information. <dl> <dt>array `a_location`</dt> <dd> List of locations the note applies to. Each element: <dl> <dt>string `html_title`</dt> <dd>HTML-escaped location title.</dd> <dt>bool `is_select`</dt> <dd>`true` if this location is selected for the flag; `false` otherwise.</dd> <dt>string `k_id`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl> </dd> <dt>bool `can_flag`</dt> <dd>`true` if the current user can flag the client; `false` otherwise.</dd> <dt>int `id_login_note_access`</dt> <dd>Login note access type. One of {@link \RsLoginNoteAccessSid} constants.</dd> <dt>int `id_login_note_flag`</dt> <dd>Login note flag type. One of {@link \RsLoginNoteFlagSid} constants.</dd> <dt>int|null `id_source`</dt> <dd> Flag source identifier. One of {@link \Wl\Location\Flag\FlagSourceEnum} constant. `null` if the flag was created manually. </dd> <dt>bool `is_automated`</dt> <dd>`true` if the note was created by an automated process; `false` if created manually.</dd> <dt>bool `is_book`</dt> <dd>`true` if the flagged client is allowed to book; `false` otherwise.</dd> <dt>bool `is_flag`</dt> <dd>`true` if the client is currently flagged; `false` otherwise.</dd> <dt>bool `is_icon`</dt> <dd>`true` if a flag icon should be displayed; `false` otherwise.</dd> <dt>bool `is_location_multiple`</dt> <dd>`true` if the business has multiple locations; `false` otherwise.</dd> <dt>bool `is_purchase`</dt> <dd>`true` if the flagged client is allowed to make purchases; `false` otherwise.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the note is marked for removal; `false` otherwise.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string `k_login_note`</dt> <dd>Login note key. <i>Primary key in the {@link \RsLoginNoteSql} table.</i></dd> <dt>string `s_text`</dt> <dd>Note text content.</dd> <dt>string `text_flag_description`</dt> <dd>Description of the flag source for display.</dd> <dt>string `text_flag_disable`</dt> <dd>Text explaining how to disable the automated flag. Empty string if the flag is manual.</dd> <dt>string `uid`</dt> <dd>User key of the flagged client. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>string `url_action`</dt> <dd>URL to submit the login note edit form.</dd> </dl>'
      tags:
        - Alert-AlertEdit
    post:
      summary: 'Creates new text note or updates the existing one. Creates or updates a login note on the client''s profile, setting its text, access restrictions, booking and purchase flags, and the locations where the note applies. Used by staff to attach internal notes or warnings that appear on check-in and booking flows.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: k_login_note
          in: query
          description: 'Login note key to edit or get info for. Empty if new entry is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information or post a note for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: array
                  description: 'The list of location keys to flag client in.'
                id_login_note_access:
                  type: number
                  format: int32
                  description: 'Login note access type ID.'
                is_book:
                  type: boolean
                  description: 'Whether flagged user can book. Does not matter if {@link AlertEditApi::$is_flag} is `false`.'
                is_flag:
                  type: boolean
                  description: 'Whether to flag the user.'
                is_purchase:
                  type: boolean
                  description: 'Whether flagged user can make purchases. Does not matter if {@link AlertEditApi::$is_flag} is `false`.'
                text_note:
                  type: string
                  description: 'Note text.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_note:
                        type: string
                        description: 'Login note key to edit or get info for. Empty if new entry is being added.'
      tags:
        - Alert-AlertEdit
  '/Wl/Profile/Attach/AttachElement.json':
    delete:
      summary: 'Deletes the attachment. Permanently removes the specified attachment from the client profile and logs the deletion action in the business audit trail.'
      parameters:
        - name: k_attach
          in: query
          description: 'Attachment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Attach-AttachElement
    get:
      summary: 'Gets the data for the attachment editing form. Returns attachment metadata including filename, description, filesize, file type, creation and edit timestamps, download URL, preview URL, and a flag indicating whether the current user has permission to delete the attachment.'
      parameters:
        - name: k_attach
          in: query
          description: 'Attachment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtu_create:
                        type: string
                        description: 'Date and time of the creation.'
                      dtu_edit:
                        type: string
                        description: 'Date and time of the last edit.'
                      s_show_delete:
                        type: string
                        description: 'Flag for showing a delete button.'
                      text_description:
                        type: [string, "null"]
                        description: 'Attachment description. `null` for not update description.'
                      text_filename:
                        type: [string, "null"]
                        description: 'Attachment file name. `null` for not update file name.'
                      text_filesize:
                        type: string
                        description: 'Attachment file size.'
                      text_filetype:
                        type: string
                        description: 'Attachment file type.'
                      url_file:
                        type: string
                        description: 'URL to get attachment file.'
                      url_preview:
                        type: [string, "null"]
                        description: 'URL to get preview attachment data. `null` in case when preview not available.'
      tags:
        - Attach-AttachElement
    post:
      summary: 'In case the attachment key is not specified, it adds a new attachment. In case the attachment key is specified, edits the attachment. When {@link AttachElementApi::$k_attach} is empty, uploads a new file to the client profile; when set, updates the attachment metadata, file content, or visibility flag depending on the provided fields.'
      parameters:
        - name: k_attach
          in: query
          description: 'Attachment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_file:
                  type: array
                  description: 'Uploaded file.'
                is_private:
                  type: [boolean, "null"]
                  description: '`true` if the attachment is private; `false` otherwise. `null` until initialized.'
                text_description:
                  type: [string, "null"]
                  description: 'Attachment description. `null` for not update description.'
                text_filename:
                  type: [string, "null"]
                  description: 'Attachment file name. `null` for not update file name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_attach:
                        type: string
                        description: 'Attachment key.'
      tags:
        - Attach-AttachElement
  '/Wl/Profile/Attach/AttachList.json':
    get:
      summary: 'Returns the list of file attachments for the specified client in the given business. Returns all file attachments uploaded to the client''s profile. In backend mode the result also includes private attachments that are hidden from the client-facing view, with additional metadata such as source, description, and a delete permission flag.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'The filter phrase to filter attach by name.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of client attachments. Each element: <dl> <dt>string `dtu_create`</dt> <dd>Date and time of creation in UTC MySQL format.</dd> <dt>string `dtu_edit`</dt> <dd>Date and time of the last edit in UTC MySQL format.</dd> <dt>int|null `i_height`</dt> <dd>Image height in pixels. `null` if not an image or dimensions are unavailable.</dd> <dt>int `i_show_delete`</dt> <dd> Flag indicating whether to show the delete button. `''1''` to show, `''0''` to hide. Only present in backend mode. </dd> <dt>int|null `i_width`</dt> <dd>Image width in pixels. `null` if not an image or dimensions are unavailable.</dd> <dt>int `id_preview`</dt> <dd>Preview type identifier. One of {@link \Wl\Profile\Attach\AttachPreviewSid} constants.</dd> <dt>bool `is_private`</dt> <dd> `true` if the attachment is private (not visible to the client); `false` otherwise. Only present in backend mode. </dd> <dt>string `k_attach`</dt> <dd>Attachment key. <i>Primary key in the {@link \AAttachSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in the {@link \RsBusinessSql} table.</i></dd> <dt>string|null `k_quiz_response`</dt> <dd> Quiz response key. `null` if the attachment is not from a quiz response. <i>Primary key in the {@link \Core\Quiz\Response\ResponseSql} table.</i> Only present in backend mode. </dd> <dt>string|null `text_description`</dt> <dd>Attachment description. `null` if no description. Only present in backend mode.</dd> <dt>string `text_filename`</dt> <dd>Attachment file name.</dd> <dt>string `text_filesize`</dt> <dd>Human-readable file size (for example, "1.2 MB").</dd> <dt>string `text_filetype`</dt> <dd>File extension in lowercase (for example, "pdf", "jpg").</dd> <dt>string `text_source`</dt> <dd>Source description (for example, quiz title or "Uploaded by staff"). Only present in backend mode.</dd> <dt>string `text_uploaded_by`</dt> <dd>Name of the user who uploaded the attachment. Empty string if unknown.</dd> <dt>string `text_uploaded_date`</dt> <dd>Formatted upload date in the business locale.</dd> <dt>string `url_file`</dt> <dd>URL to download the attachment file.</dd> <dt>string|null `url_preview`</dt> <dd>URL to a preview image (50x50). `null` if preview is not available for this file type.</dd> </dl>'
      tags:
        - Attach-AttachList
  '/Wl/Profile/Attach/ProfileAttach.json':
    get:
      summary: 'Gets user''s attached files.'
      parameters:
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Business primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_attach:
                        type: array
                        description: 'List of attaches.'
      tags:
        - Attach-ProfileAttach
    post:
      summary: 'Saves user''s attaches.'
      parameters:
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Business primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_attach_comment:
                  type: array
                  description: 'Comments for exists attaches. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
                a_attach_file:
                  type: array
                  description: 'New files for exists attaches. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
                a_attach_name:
                  type: array
                  description: 'New names for exists attaches. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
                a_attach_save:
                  type: array
                  description: 'Old attaches which must be kept. If this field does not contain an attach, it will be removed with POST. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
                a_new_attach_comment:
                  type: array
                  description: 'Comments for new attaches. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
                a_new_attach_file:
                  type: array
                  description: 'Files for new attaches. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
                a_new_attach_name:
                  type: array
                  description: 'Names for new attaches. Strange name for compatibility with old server side. <b>This field is not used directly and described for correct auto generation JavaScript.</b>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Attach-ProfileAttach
  '/Wl/Profile/Attendance/AttendanceOverlap.json':
    get:
      summary: 'Returns a list of visits that overlap with the specified service, class, resource, or time data. Checks whether the specified user has any existing bookings that overlap with a given time range or service. Used before scheduling to detect conflicts and prompt staff or the client with a warning.'
      parameters:
        - name: dtu_date
          in: query
          description: 'Date of a selected service.'
          schema:
            type: [string, "null"]
        - name: i_duration
          in: query
          description: 'Duration of a service.'
          schema:
            type: number
            format: int32
        - name: is_appointment
          in: query
          description: 'Whether an asset is booking. Needed in case when a client is allowed to select a date and time, then the available asset.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Primary key of the business to add the user into.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'Asset key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit_list:
                        type: array
                        description: 'List of visits that overlap with the specified data. <dl> <dt>string `dtu_date`</dt> <dd>Date and time of the visit.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_end`</dt> <dd>End datetime of the visit in unix format.</dd> <dt>int `i_end_local`</dt> <dd>Local end datetime of the visit in unix format.</dd> <dt>int `i_start`</dt> <dd>Start datetime of the visit in unix format.</dd> <dt>int `i_start_local`</dt> <dd>Local start datetime of the visit in unix format.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_class_period`</dt> <dd>Class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_enrollment_book`</dt> <dd>Enrollment book key. <i>Primary key in {@link \RsEnrollmentBookSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Title of a service</dd> </dl>'
      tags:
        - Attendance-AttendanceOverlap
  '/Wl/Profile/Contract/Contract.json':
    get:
      summary: 'Returns contract information for the specified purchase option. Renders the contract text for the specified purchase option, applying any applicable discounts, and returns the content needed to display the contract acceptance modal to the client.'
      parameters:
        - name: dt_start
          in: query
          description: 'The start date of the contract.'
          schema:
            type: string
        - name: f_manual_discount
          in: query
          description: 'The percentage discount for the item.'
          schema:
            type: number
            format: float
        - name: id_purchase_item
          in: query
          description: 'The type of purchase item. This is one of the {@link \RsPurchaseItemSid} constants. Optional if {@link \Wl\Profile\Contract\ContractApi::$k_purchase_item} is not empty.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The key of the purchase item in the database. The item key. Depends on {@link ContractApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the selected location.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the selected purchase item.'
          schema:
            type: string
        - name: m_discount_flat
          in: query
          description: 'Amount of a flat manual discount.'
          schema:
            type: string
        - name: m_price_custom
          in: query
          description: 'The custom price of the item.'
          schema:
            type: string
        - name: s_discount_code
          in: query
          description: 'The discount code used for the item.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_contract:
                        type: string
                        description: 'The text of the contract.'
                      i_minor_age:
                        type: number
                        format: int32
                        description: 'Age of minor which documents can be signed by parent or legal guardian.'
                      text_title:
                        type: string
                        description: 'Title of purchase option.'
      tags:
        - Contract-Contract
    post:
      summary: 'Completes a sale of a Purchase Option requiring a contract by submitting the signed contract. Accepts an encoded client signature and agreement flag, decodes the signature, and records the signed contract for the specified purchase item.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the selected purchase item.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_agree:
                  type: [boolean, "null"]
                  description: '<tt>false</tt> if user has not agreed to use Electronic Signatures, <tt>true</tt> if user has agreed to use Electronic Signatures, <tt>null</tt> otherwise.'
                s_signature:
                  type: string
                  description: 'An encoded version of the client signature. This is different from the signature needed to communicate with an endpoint.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Contract-Contract
  '/Wl/Profile/Overview/SendMail.json':
    post:
      summary: ''
      parameters:
        - name: id_mail
          in: query
          description: 'ID of the email.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user for whom an email should be sent. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Overview-SendMail
  '/Wl/Profile/Purchase/PurchaseElement.json':
    get:
      summary: 'Retrieves information about 1 purchase item. Loads the full details of a single purchase item for display on the client profile, including pricing, discounts, taxes, refunds, and current status. The item can be identified by purchase item key, redemption code, or login promotion key.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key. Currently used only with {@link \Wl\Profile\Purchase\PurchaseElementApi::$k_session_pass} variable.'
          schema:
            type: [string, "null"]
        - name: k_code
          in: query
          description: 'The key of the redemption code used to obtain some goods. This should be specified only for items obtained via redemption code (not for ordinary purchases or for purchased promotions without purchases). This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_enrollment_book
          in: query
          description: 'The key of an entirely booked event. This must be specified if the purchased item is a whole event and a component of a package.'
          schema:
            type: string
        - name: k_login_product
          in: query
          description: 'The key of a purchased product. This must be specified if the purchased item is a product and a component of a package.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The key of the user''s promotion. This should only be specified for promotions given without a purchase. This isn''t for ordinary purchases or for promotions obtained via redemption codes. This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the purchased item. This should be specified only for ordinary purchases, not for those obtained via redemption codes or as a result of an old import.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'The key of the make-up session used to attend an event.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_component:
                        type: array
                        description: 'List of components (not empty if this purchase element is a package). Every element has keys: <dl> <dt>string `k_enrollment_book`</dt> <dd>Key of enrollment book. <i>Primary key in {@link \RsEnrollmentBookSql} table.</i></dd> <dt>string `k_login_product`</dt> <dd>Key of product. <i>Primary key in {@link \RsLoginProductSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd>Key of promotion. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Session key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> </dl>'
                      a_logo:
                        type: array
                        description: 'An array containing information about the image of the purchased item. Every element has the following keys: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>If `true`, then the purchased item doesn''t have image. If `false`, then the item has an image.</dd> <dt>string `s_url`</dt> <dd>The URL for the image.</dd> </dl>'
                      a_restrict:
                        type: array
                        description: 'This field is used only for promotions. It contains restrictions that will apply to bookings made with this promotion. Every element has the following keys: <dl> <dt>int `i_limit`</dt> <dd>The number of sessions that may be booked during this period.</dd> <dt>int `i_remain`</dt> <dd>The number of sessions that remain available.</dd> <dt>string `s_date`</dt> <dd>The name of the period type.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'The list of taxes paid for the purchased item. Every element has the following fields: <dl> <dt>string `f_tax_discount`</dt> <dd>The amount of tax.</dd> <dt>string `s_tax`</dt> <dd>The name of the tax.</dd> </dl>'
                      can_renew:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion and the user can control the renewal of the promotion. Otherwise, this will be `false`.'
                      dl_purchase:
                        type: string
                        description: 'Local date of purchase in MySQL format.'
                      dl_start:
                        type: string
                        description: 'The start date of the promotion. This is used only if the purchased item is a promotion.'
                      dt_end:
                        type: string
                        description: 'The expiration date of the promotion. This is used only if the purchased item is a promotion.'
                      dt_hold_end:
                        type: string
                        description: 'The end date of the promotion hold. This is used only for promotions on hold. This may be empty if the hold is internal.'
                      dt_hold_start:
                        type: string
                        description: 'The start date of the promotion hold. This is used only for promotions on hold.'
                      dt_purchase:
                        type: string
                        description: 'The purchase date of the purchased item. This is given in GMT in MySQL format.'
                      dt_send:
                        type: string
                        description: 'The date for when the email regarding the gift card should be sent (or was sent) to the receiver. This is only used for gift cards.'
                      f_discount_login_type_percent:
                        type: number
                        format: float
                        description: 'The percentage value of the discount for the client type. This is empty if this discount wasn''t applied.'
                      html_description:
                        type: string
                        description: 'The description of the purchased item.'
                      i_book:
                        type: number
                        format: int32
                        description: 'The number of sessions remaining for a promotion. This is used only for promotions. This will be `0` if the purchased item isn''t a promotion or if there are no remaining sessions.'
                      i_buy:
                        type: number
                        format: int32
                        description: 'The number of purchased items bought at the time of purchase. This field will be `0` for purchase items obtained via a redemption code or as a result of an old import.'
                      i_discount_count:
                        type: number
                        format: int32
                        description: 'The percentage value of the discount given by a reward prize. This field will be `0` if a discount wasn''t applied.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'This field is only added for promotions. Duration of promotion. Measurement unit is <var>id_duration</var>.'
                      i_left:
                        type: number
                        format: int32
                        description: 'The number of bookings remaining in this promotion. This value is used only for non-sequential promotions.'
                      i_limit:
                        type: number
                        format: int32
                        description: 'The number of visits which can be made with this promotion. This is used only for promotions.'
                      i_payment_period:
                        type: number
                        format: int32
                        description: 'The duration of the regular payments interval. This is used only for "membership" type promotions.'
                      i_remain:
                        type: number
                        format: int32
                        description: 'The number of remaining bookings for the promotion. This is used only for promotions.'
                      i_remain_duration:
                        type: number
                        format: int32
                        description: 'The number of minutes remaining for the promotion. This is used only for promotions that work with a limited number of minutes.'
                      i_renew:
                        type: number
                        format: int32
                        description: 'Not <tt>0</tt> only for promotions. How many times promotion was updated.'
                      i_use:
                        type: number
                        format: int32
                        description: 'This is used only for promotions. This is the number of visits that were attended for the promotion.'
                      id_activation:
                        type: number
                        format: int32
                        description: 'Activation mode. One on {@link \RsActivationSid} constants. This field is only added for promotions.'
                      id_discount_commission_type:
                        type: number
                        format: int32
                        description: 'The type of discount given via a reward prize. One on the {@link \RsCommissionTypeSid} constants. This will be empty if this discount wasn''t applied.'
                      id_duration:
                        type: number
                        format: int32
                        description: 'The type of duration for the promotion. This is used only for "membership" type promotions. This determines whether the promotion lasts indefinitely, has a fixed duration, or ends on a specific date. One of the {@link \RsDurationTypeSid} constants.'
                      id_program_type:
                        type: number
                        format: int32
                        description: 'The ID of the promotion program. One of the {@link \RsProgramTypeSid} constants. This is used only for promotions.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'The ID of the purchase item type. One of the {@link \RsPurchaseItemSid} constants.'
                      id_sale:
                        type: number
                        format: int32
                        description: 'The ID of the sale category. One of the {@link \RsSaleSid} constants.'
                      is_active:
                        type: boolean
                        description: 'If `true`, then the purchase item is active. If `false`, then the purchase item isn''t active.'
                      is_component:
                        type: boolean
                        description: 'If `true`, then the purchased item is a component of another purchase item, as is the case of a package. If `false`, then the purchased item isn''t a component of another purchase item.'
                      is_convert:
                        type: boolean
                        description: 'If `true`, then this promotion will be converted to a different promotion after it expires. It `false`, then the promotion won''t be converted to a different promotion after it expires. This can only be true for certain promotions.'
                      is_expire:
                        type: boolean
                        description: 'This is `true` only if the purchased item is an expired promotion. Otherwise, this will be `false`.'
                      is_hold:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion placed on hold. Otherwise, this will be `false`.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the usage of this purchased item is disabled. If `false`, then the usage of this purchased item is enabled.'
                      is_login_type_member:
                        type: [boolean, "null"]
                        description: 'If `true`, then the discount was applied for the member type. If `false`, then the discount was applied for the pass holder type. If `null`, then the discount was applied for the prospect type (this won''t be `null` only if a discount based on a client type was applied).'
                      is_mail:
                        type: boolean
                        description: 'If `true`, then the gift card email notification will be sent to the receiver. This is `true` only if the purchased item is a gift card. Otherwise, this will be `false`.'
                      is_past:
                        type: boolean
                        description: 'If `true`, then the gift card email notification has been sent. This can only be `true` if the purchased item is a gift card. Otherwise, this will be `false`.'
                      is_redeem:
                        type: boolean
                        description: 'This is `true` if the gift card has been redeemed. This can only be true if the purchased item is a gift card. Otherwise, this will be `false`.'
                      is_renew:
                        type: boolean
                        description: 'This is `true` if the promotion is auto-renewable. This can only be true if the purchased item is a promotion. Otherwise, this will be `false`.'
                      is_transfer:
                        type: boolean
                        description: 'This is `true` if the promotion has been transferred. This can only be `true` if the purchased item is a promotion. This is `false` if the promotion hasn''t been transferred or in all other cases.'
                      k_code_result:
                        type: string
                        description: 'The key of the redemption code. This won''t be `0` if the purchased item is obtained via a redemption code or if it''s a component of a package which is obtained via a redemption code.'
                      k_currency:
                        type: string
                        description: 'The key of the currency used to make the purchase.'
                      k_login_promotion_result:
                        type: string
                        description: 'The key of the purchased promotion. This won''t be `0` only for promotions.'
                      k_purchase_result:
                        type: string
                        description: 'The key of the whole purchase which includes the given purchased item.'
                      m_cost_item:
                        type: string
                        description: 'The cost of the purchased item.'
                      m_cost_total:
                        type: string
                        description: 'The total cost of the purchase, including taxes and fees.'
                      m_discount_amount:
                        type: string
                        description: 'The amount of the discount given via a reward prize. This will be empty if the discount wasn''t applied.'
                      m_discount_login_type_amount:
                        type: string
                        description: 'The amount of the discount for the client type. This will be empty if the discount wasn''t applied.'
                      m_discount_manual:
                        type: string
                        description: 'Amount of manual discount. Empty if this discount was not applied.'
                      m_period_price:
                        type: string
                        description: 'The amount to pay for each payment period. This is used only for promotions with the "membership" program type.'
                      m_price:
                        type: string
                        description: 'Source price of item.'
                      m_refund:
                        type: string
                        description: 'The amount refunded. This will be `0` if there are no refunds.'
                      s_code:
                        type: string
                        description: 'The redemption code. This is used only if the purchase is bought using a redemption code.'
                      s_gift_code:
                        type: string
                        description: 'The code to redeem gift card. This won''t be empty only for gift cards. This can be auto-generated or custom (entered manually by the staff member during purchase). The max length is 20 characters, which can include both numbers and letters.'
                      s_gift_recipient:
                        type: string
                        description: 'Name of gift recipient. This won''t be empty only for gift cards.'
                      s_limit_actor:
                        type: string
                        description: 'The name of the staff member who disabled the usage of the purchased item. This will be empty if the purchased item is enabled.'
                      s_mail:
                        type: string
                        description: 'The email address the gift card was sent to. This is used only for gift cards.'
                      s_message:
                        type: string
                        description: 'The text of the message sent to the gift card recipient. This is used only for gift cards. This will be empty if an email notification shouldn''t be sent.'
                      s_payer:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_promotion_convert:
                        type: string
                        description: 'The title of the converted promotion. This is used only for promotions which must be converted to another promotion after expiration.'
                      s_purchase:
                        type: string
                        description: 'The purchase ID number as it appears on the receipt. This value will also include the leading 0s for the ID number.'
                      s_recipient:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_sender:
                        type: string
                        description: 'The name of the sender. This is used only for gift cards.'
                      s_title:
                        type: string
                        description: 'The name of the purchased item.'
                      sid_value:
                        type: string
                        description: 'The string ID of the purchased item. This ID may be used to change the view of the purchased item.'
                      uid_payer:
                        type: string
                        description: 'The key of the user who paid for this purchase.'
                      uid_recipient:
                        type: string
                        description: 'The key of the user who paid for this purchase.'
      tags:
        - Purchase-PurchaseElement
  '/Wl/Profile/Purchase/PurchaseItemCancel.json':
    post:
      summary: 'Changes state of the purchase option.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key specific purchase item belongs to.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'Purchase item key which should be cancelled.'
          schema:
            type: [string, "null"]
        - name: k_purchase_item
          in: query
          description: 'Purchase item key which should be cancelled.'
          schema:
            type: [string, "null"]
        - name: k_session_pass_event_component
          in: query
          description: 'Session pass key if an event in a package is being cancelled.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key of the purchase item owner.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Additional data. <dl> <dt>string `dl_deactivation`</dt> <dd>Date of a promotion cancellation.</dd> <dt>string `k_reason`</dt> <dd>Reason key. Primary key in {@link \Wl\Promotion\Terminate\Reason\PromotionTerminateReasonSql} table.</dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key. Primary key in {@link \RsLoginPromotionSql} table.</dd> <dt>string `m_fee`</dt> <dd>Cancellation fee.</dd> <dt>string `s_reason`</dt> <dd>Text not about cancellation reason.</dd> </dl>'
                is_past:
                  type: boolean
                  description: 'Whether cancellation should influence historical data. `true` to influence historical data. `false` otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Purchase-PurchaseItemCancel
  '/Wl/Profile/Purchase/PurchaseList.json':
    get:
      summary: 'Retrieves a list of user''s purchase items to show in user profile. Returns the complete purchase history for the client in the given business, covering all item types such as memberships, redemption codes, enrollments, products, appointments, and gift cards. Package components are resolved and included inline beside their parent item.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase:
                        type: array
                        description: 'A list of purchased items. Every element contains a sub-array with the following fields: <dl> <dt>bool[] `a_active`</dt> <dd> The value of <var>a_purchase[''is_active'']</var>. For packages, this also contains the values of <var>a_purchase[''is_active'']</var> of every component. </dd> <dt>int[] `a_sale`</dt> <dd> The value of <var>a_purchase[''id_sale'']</var>. For packages, this also contains the values of <var>a_purchase[''id_sale'']</var> of every component. </dd> <dt>string `dt_add`</dt> <dd>The date that the purchase was added.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase type. One of the constants {@link \RsPurchaseItemSid}.</dd> <dt>int `id_sale`</dt> <dd>The ID of the sale category in the store. One of the constants {@link \RsSaleSid}.</dd> <dt>bool `is_active`</dt> <dd>If `true`, then the purchase item is active. Otherwise, this will be `false`.</dd> <dt>bool `is_component`</dt> <dd>If `true`, then the purchase item is a package component. Otherwise, this will be `false`.</dd> <dt>bool `is_package`</dt> <dd>If `true`, then the purchase item is a package, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key from {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_code`</dt> <dd> The redemption code key that was used to make a purchase. This is used only if <var>k_login_promotion</var> and <var>k_purchase</var> are empty. </dd> <dt>string `k_enrollment_book`</dt> <dd> The key of an event that requires clients to book every session at once. This is used only if the event is part of a package. </dd> <dt>string `k_id`</dt> <dd> The key of the purchase type referring to different types of keys depending on the value of <var>id_sale</var>. </dd> <dt>string `k_login_product`</dt> <dd>The key of the purchased product. This is used only if the event is part of a package.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the user''s existing purchase options. This is used only if `k_code` and `k_purchase` are empty.</dd> <dt>string `k_purchase`</dt> <dd> The key of a purchase where no special case rules are in effect. This is used only used if `k_code` is empty. </dd> <dt>string `k_purchase_item`</dt> <dd> The key of a purchase item where no special case rules are in effect. This is used only if <var>k_code</var> is empty. </dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd> In certain cases a session can be canceled and makeup sessions can be granted to a client in lieu of other compensation. This is the key of one of the makeup sessions used to attend an event. This is also present for a repeat purchase of an event. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `s_title`</dt> <dd>The name of the purchase item.</dd> <dt>string `uid`</dt> <dd>The ID of the purchase owner. <i>Primary key {@link \PassportLoginSql} in table.</i></dd> </dl>'
      tags:
        - Purchase-PurchaseList
  '/Wl/Profile/Purchase/Mail.json':
    post:
      summary: ''
      parameters:
        - name: k_purchase
          in: query
          description: 'Purchase ID. Primary key in {@link \RsPurchaseSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of user for who receipt should be sent. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Purchase-Mail
  '/Wl/Profile/Term/Term.json':
    get:
      summary: 'Returns online waiver information for the specified user and business. Returns the rendered online waiver text for the business along with the client''s current agreement status. Used to display the waiver page and show whether the client has already signed, including their signature image and the confirmation timestamp.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the current business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dt_agree:
                        type: [string, "null"]
                        description: 'The date/time of the waiver confirmation. <tt>null</tt> if waiver is not confirmed.'
                      html_contract:
                        type: string
                        description: 'The text of the online waiver.'
                      i_minor_age:
                        type: number
                        format: int32
                        description: 'Age of minor which documents can be signed by parent or legal guardian.'
                      ip_agree:
                        type: [string, "null"]
                        description: 'The IP address from which the confirmation was carried out. <tt>null</tt> if waiver is not confirmed.'
                      is_agree:
                        type: boolean
                        description: 'Flag of successful saving agreement. <tt>true</tt> if agreement was sign up successful <tt>false</tt> - otherwise.'
                      s_name:
                        type: string
                        description: 'The user''s name.'
                      url_signature:
                        type: [string, object]
                        description: 'The URL to the image with the client''s signature. <tt>false</tt> if waiver is not confirmed.'
      tags:
        - Term-Term
  '/Wl/Profile/PurchaseList/PurchaseListElement.json':
    get:
      summary: 'Retrieves information about 1 purchase item. Loads the full details of a single purchase item for display on the client profile, including pricing, discounts, taxes, refunds, and current status. The item can be identified by purchase item key, redemption code, or login promotion key.'
      parameters:
        - name: i_image_height
          in: query
          description: 'Image Height in pixels. Please specify this value if you need purchase image to be returned in specific size. In case this value is not specified returned image will have default purchase size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image Width in pixels. Please specify this value if you need purchase image to be returned in specific size. In case this value is not specified returned image will have default purchase size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key. Used with {@link PurchaseListElementApi::$k_session_pass} variable and also with gift card components.'
          schema:
            type: [string, "null"]
        - name: k_code
          in: query
          description: 'The ID of the redemption code used to obtain the item. This should be specified only for items obtained via redemption code (not for ordinary purchases or for purchased promotions without purchases). This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_enrollment_book
          in: query
          description: 'The key of an entirely booked event. This must be specified if the purchased item is a whole event and part of a package.'
          schema:
            type: string
        - name: k_login_product
          in: query
          description: 'The key of a purchased product. This must be specified if the purchased item is a product and part of a package.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The key of the user''s promotion. This should only be specified for promotions given without a purchase. This isn''t for ordinary purchases or for promotions obtained via redemption codes. This will be `null` if not set yet.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'The key of the purchased item. This should be specified only for ordinary purchases and not for purchases obtained via redemption codes or as a result of an old import.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'The ID of the makeup session used to attend an event. This must be specified if the purchase item is a component of a package and a whole event booking. And only for items which aren''t components of package.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_component:
                        type: array
                        description: 'A list of components. This won''t be empty if this purchase element is a package or a coupon. In the case of a package, every element has keys: <dl> <dt>string `k_enrollment_book`</dt> <dd>The booking enrollment key.</dd> <dt>string `k_login_product`</dt> <dd>The key referencing the product a user purchased.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of a user''s Purchase Option.</dd> <dt>string `k_session_pass`</dt> <dd>The key of a user''s session pass.</dd> </dl> <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item type. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>ID of the item. Will be different for different {@link \RsPurchaseItemSid} constants.</dd> <dt>string `text_title`</dt> <dd>Title of the coupon component.</dd> </dl>'
                      a_logo:
                        type: [array, "null"]
                        description: 'An array containing information about the image of the purchased item. Every element has the following keys: `null` in a case when the item has no logo. <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>If `true`, then the purchased item doesn''t have an image. If `false`, then the item has an image.</dd> </dl>'
                      a_restrict:
                        type: array
                        description: 'This field is used only for promotions. It contains restrictions that will apply to bookings made with this promotion. Every element has the following keys: This will be an empty array for other purchased items. <dl> <dt>int `i_limit`</dt> <dd>The number of sessions that may be booked during this period.</dd> <dt>int `i_remain`</dt> <dd>The number of sessions that remain available.</dd> <dt>string `text_date`</dt> <dd>The name of the period type.</dd> </dl>'
                      a_tax:
                        type: array
                        description: 'The list of taxes paid for the purchased item. Every element has the following fields: <dl> <dt>string `f_tax_discount`</dt> <dd>The amount of tax.</dd> <dt>string `s_tax`</dt> <dd>The name of the tax.</dd> </dl>'
                      can_renew:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion and the user can control the renewal of the promotion. Otherwise, this will be `false`.'
                      dl_cancel:
                        type: string
                        description: 'The cancellation date of the promotion. Only available if the item is a promotion and has been canceled.'
                      dl_end:
                        type: string
                        description: 'The expiration date of the promotion. Only available if the item is a promotion.'
                      dl_purchase:
                        type: string
                        description: 'The local date of the purchase in MySQL format.'
                      dl_send:
                        type: string
                        description: 'The local date in MySQL format when mail about purchasing was sent (or will be sent) to the user. This won''t be empty only for gift cards.'
                      dl_start:
                        type: string
                        description: 'The start date of the promotion. Only available if the item is a promotion.'
                      dt_cancel:
                        type: string
                        description: 'The local date of cancellation in MySQL format. Only available if the item is a membership.'
                      dt_hold_end:
                        type: string
                        description: 'The end date of the promotion hold. This is used only for promotions on hold. This value may be empty if the hold is eternal.'
                      dt_hold_start:
                        type: string
                        description: 'The start date of the promotion hold. Only available if the item is a promotion and on hold.'
                      dt_purchase:
                        type: string
                        description: 'The purchase date of the purchased item. This is given in UTC in MySQL format.'
                      dt_redeem:
                        type: string
                        description: 'The date in MySQL format when gift card was redeemed. Only available if the item is a gift card.'
                      f_discount_login_type_percent:
                        type: number
                        format: float
                        description: 'The percentage value of the discount for the client type. This will be empty if a discount wasn''t applied.'
                      html_description:
                        type: string
                        description: 'The description of the purchased item.'
                      i_book:
                        type: number
                        format: int32
                        description: 'The number of sessions remaining for a promotion. This value is used only for promotions. This value will be `0` if the purchased item isn''t a promotion or if there are no remaining sessions.'
                      i_book_duration:
                        type: number
                        format: int32
                        description: 'Displays the number of minutes of sessions that were booked by this promotion. This isn''t `0` only for promotions.'
                      i_buy:
                        type: number
                        format: int32
                        description: 'The number of purchased items bought at the time of purchase. This will be `0` for purchase items obtained via a redemption code or as a result of an import.'
                      i_client_cancel_notice_days:
                        type: number
                        format: int32
                        description: 'The number of days notice before the client is able to set cancel date.'
                      i_discount_count:
                        type: number
                        format: int32
                        description: 'The percentage value of the discount given by a reward prize. This will be `0` if a discount wasn''t applied.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The duration of the promotion. The measurement unit is <var>id_duration</var>. This field is only added for promotions.'
                      i_left:
                        type: number
                        format: int32
                        description: 'The number of bookings left with this promotion. This value is used only for non-sequential promotions.'
                      i_limit:
                        type: number
                        format: int32
                        description: 'The number of visits that can be made with this promotion. This value is used only for promotions.'
                      i_payment_period:
                        type: number
                        format: int32
                        description: 'The interval duration of the regular payments. This value is used only for membership-type promotions.'
                      i_remain:
                        type: number
                        format: int32
                        description: 'The number of remaining bookings for the promotion. This is used only for promotions.'
                      i_remain_duration:
                        type: number
                        format: int32
                        description: 'The number of minutes remaining for the promotion. This is used only for promotions that work with a limited number of minutes.'
                      i_renew:
                        type: number
                        format: int32
                        description: 'Displays how many times promotion was updated. This isn''t `0` only for promotions.'
                      i_use:
                        type: number
                        format: int32
                        description: 'The number of visits that were attended for the promotion. This is used only for promotions.'
                      i_use_duration:
                        type: number
                        format: int32
                        description: 'The number of minutes the user has already used with this purchase of this promotion. This isn''t `0` only for promotions.'
                      id_activation:
                        type: number
                        format: int32
                        description: 'The activation mode. One of the {@link \RsActivationSid} constants. This field is only added for promotions.'
                      id_discount_commission_type:
                        type: number
                        format: int32
                        description: 'The type of discount given via a reward prize. One of the {@link \RsCommissionTypeSid} constants. This will be empty if this discount wasn''t applied.'
                      id_duration:
                        type: number
                        format: int32
                        description: 'The type of duration for the promotion. This is used only for membership-type promotions. This determines whether the promotion lasts indefinitely, has a fixed duration, or ends on a specific date. The measurement unit for key <var>i_payment_period</var>. One of the {@link \RsDurationTypeSid} constants.'
                      id_program_type:
                        type: number
                        format: int32
                        description: 'The ID of the promotion program. This is used only for promotions. The measurement unit for key <var>i_payment_period</var>. The ID of promotion program type. One of the {@link \RsProgramTypeSid} constants.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'The ID of the purchase item type. The measurement unit for key <var>i_payment_period</var>. The ID of purchase item type. One of the {@link \RsPurchaseItemSid} constants.'
                      id_sale:
                        type: number
                        format: int32
                        description: 'The ID of the sale category. One of the constants {@link \RsSaleSid}.'
                      is_active:
                        type: boolean
                        description: 'If `true`, then the purchase item is active. If `false`, then the purchase item isn''t active.'
                      is_asset:
                        type: boolean
                        description: 'Whether the purchase item is asset.'
                      is_cancel_pending:
                        type: boolean
                        description: 'If `true`, then a promotion is pending cancellation. Otherwise, this will be `false`.'
                      is_client_cancel_allow:
                        type: boolean
                        description: 'If `true`, then a promotion can be canceled by a client. Otherwise, this will be `false`.'
                      is_component:
                        type: boolean
                        description: 'If `true`, then the purchased item is a component of another purchase item, as is the case of a package. If `false`, then the purchased item isn''t a component of another purchase item.'
                      is_convert:
                        type: boolean
                        description: 'If `true`, then this promotion will be converted to a different promotion after it expires. If `false`, then the promotion won''t be converted to a different promotion after it expires. This can only be true for certain promotions.'
                      is_event:
                        type: boolean
                        description: '`true` if the purchase item is an event; `false` otherwise.'
                      is_expire:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion that has expired. Otherwise, this will be `false` in all other cases.'
                      is_hold:
                        type: boolean
                        description: 'This is `true` only if the purchased item is a promotion and has been placed on hold. Otherwise, this is `false` in all other cases.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the usage of this purchased item is disabled. If `false`, then the usage of this purchased item is enabled.'
                      is_login_type_member:
                        type: [boolean, "null"]
                        description: 'If `true`, then the discount was applied for the member type. If `false`, then the discount was applied for the pass holder type. If `null`, then the discount was applied for the prospect type. This won''t be `null` only if a discount based on a client type was applied.'
                      is_mail:
                        type: boolean
                        description: 'If `true`, then the gift card email notification will be sent to the receiver. This is `true` only if the purchased item is a gift card. Otherwise, this will be `false` in all other cases.'
                      is_past:
                        type: boolean
                        description: 'If `true`, then the gift card email notification has been sent. This can only be `true` if the purchased item is a gift card. Otherwise, this will be `false` in all other cases.'
                      is_redeem:
                        type: boolean
                        description: 'This is `true` if the gift card has been redeemed. This can only be `true` if the purchased item is a gift card. Otherwise, this will be `false` in all other cases.'
                      is_renew:
                        type: boolean
                        description: 'This is `true` if the promotion is auto-renewable. This can only be `true` if the purchased item is a promotion. Otherwise, this will be `false` in all other cases.'
                      is_transfer:
                        type: boolean
                        description: 'This is `true` if the promotion has been transferred. This can only be true if the purchased item is a promotion. This is `false` if the promotion hasn''t been transferred or in all other cases.'
                      k_code_result:
                        type: string
                        description: 'The key of the redemption code. This value will not be `0` if the purchased item is obtained via a redemption code or if it''s a component of a package obtained via a redemption code.'
                      k_currency:
                        type: string
                        description: 'The key of the currency used to make the purchase.'
                      k_id:
                        type: string
                        description: 'The key of the item. This can correspond to one of many values depending on {@link PurchaseListElementApi::$id_sale}.'
                      k_login_promotion_result:
                        type: string
                        description: 'The key of the purchased promotion. This won''t be `0` only for promotions.'
                      k_purchase_result:
                        type: string
                        description: 'The key of the whole purchase, including the given purchased item.'
                      m_cost_item:
                        type: string
                        description: 'The cost of the purchased item.'
                      m_cost_total:
                        type: string
                        description: 'The total cost of the purchase, including taxes and fees.'
                      m_discount_amount:
                        type: string
                        description: 'The amount of the discount via prize. This will be empty if this discount wasn''t applied.'
                      m_discount_login_type_amount:
                        type: string
                        description: 'The amount of the discount for the client type. This value will be empty if the discount wasn''t applied.'
                      m_discount_manual:
                        type: string
                        description: 'The amount of the manual discount. This will be empty if this discount wasn''t applied.'
                      m_period_price:
                        type: string
                        description: 'The amount to pay for each payment period. This is used only for promotions with the membership program type.'
                      m_price:
                        type: string
                        description: 'The source price of item.'
                      m_refund:
                        type: string
                        description: 'The amount refunded. This will be `0` if there are no refunds.'
                      m_terminate_fee:
                        type: [string, "null"]
                        description: 'Cancellation fee amount. `null` if no need charge fee.'
                      s_code:
                        type: string
                        description: 'The redemption code. This value is used only if the purchase is bought via a redemption code.'
                      s_gift_code:
                        type: string
                        description: 'The code to redeem the gift card. This isn''t empty only for gift cards. This can be auto-generated or custom (entered manually by the staff member during purchase). The max length is 20 characters, including numbers and letters.'
                      s_gift_recipient:
                        type: string
                        description: 'The name of gift recipient. This isn''t empty only for gift cards.'
                      s_limit_actor:
                        type: string
                        description: 'The name of the staff member who disabled the usage of the purchased item. This will be empty if the purchased item is enabled.'
                      s_mail:
                        type: string
                        description: 'The email address to which the gift card was sent. This is used only for gift cards.'
                      s_message:
                        type: string
                        description: 'The test of the message sent to the gift card recipient. This is used only for gift cards. This will be empty if an email notification shouldn''t be sent.'
                      s_payer:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_promotion_convert:
                        type: string
                        description: 'The title of the converted promotion. This value is used only for promotions that must be converted to another promotion after expiration.'
                      s_purchase:
                        type: string
                        description: 'The purchase ID number as it appears on the receipt. This value will also include the leading 0s for the ID number.'
                      s_recipient:
                        type: string
                        description: 'The name of the user who paid for this purchase.'
                      s_sender:
                        type: string
                        description: 'The name of the sender. This value is used only for gift cards.'
                      s_title:
                        type: string
                        description: 'The name of the purchased item.'
                      sid_value:
                        type: string
                        description: 'The string ID of the purchased item. This ID may be used to change the view of the purchased item.'
                      text_expire:
                        type: string
                        description: 'Expire conditions for the purchased item (only for promotion).'
                      text_guest_pass:
                        type: string
                        description: 'Text describing guest passes available for the purchased promotion.'
                      uid_payer:
                        type: [string, "null"]
                        description: 'The key of the user who paid for this purchase. `null` in a case when there was no money transaction.'
                      uid_recipient:
                        type: string
                        description: 'The key of the user who paid for this purchase.'
      tags:
        - PurchaseList-PurchaseListElement
  '/Wl/Profile/PurchaseList/PurchaseList.json':
    get:
      summary: 'Retrieves a list of user''s purchase items to show in user profile. Returns the complete purchase history for the client in the given business, covering all item types such as memberships, redemption codes, enrollments, products, appointments, and gift cards. Package components are resolved and included inline beside their parent item.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase:
                        type: array
                        description: 'A list of purchased items. Every element contains a sub-array with the following fields: <dl> <dt>bool[] `a_active`</dt> <dd> The value of <var>a_purchase[''is_active'']</var>. For packages, this also contains the values of <var>a_purchase[''is_active'']</var> of every component. </dd> <dt>int[] `a_sale`</dt> <dd> The value of <var>a_purchase[''id_sale'']</var>. For packages, this also contains the values of <var>a_purchase[''id_sale'']</var> of every component. </dd> <dt>string `dt_add`</dt> <dd>The date that the purchase was added.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase type. One of the constants {@link \RsPurchaseItemSid}.</dd> <dt>int `id_sale`</dt> <dd>The ID of the sale category in the store. One of the constants {@link \RsSaleSid}.</dd> <dt>bool `is_active`</dt> <dd>If `true`, then the purchase item is active. Otherwise, this will be `false`.</dd> <dt>bool `is_component`</dt> <dd>If `true`, then the purchase item is a package component. Otherwise, this will be `false`.</dd> <dt>bool `is_package`</dt> <dd>If `true`, then the purchase item is a package, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Appointment key. <i>Primary key from {@link \RsAppointmentSql} table.</i></dd> <dt>string `k_code`</dt> <dd> The redemption code key that was used to make a purchase. This is used only if <var>k_login_promotion</var> and <var>k_purchase</var> are empty. </dd> <dt>string `k_enrollment_book`</dt> <dd> The key of an event that requires clients to book every session at once. This is used only if the event is part of a package. </dd> <dt>string `k_id`</dt> <dd> The key of the purchase type referring to different types of keys depending on the value of <var>id_sale</var>. </dd> <dt>string `k_login_product`</dt> <dd>The key of the purchased product. This is used only if the event is part of a package.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the user''s existing purchase options. This is used only if `k_code` and `k_purchase` are empty.</dd> <dt>string `k_purchase`</dt> <dd> The key of a purchase where no special case rules are in effect. This is used only used if `k_code` is empty. </dd> <dt>string `k_purchase_item`</dt> <dd> The key of a purchase item where no special case rules are in effect. This is used only if <var>k_code</var> is empty. </dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd> In certain cases a session can be canceled and makeup sessions can be granted to a client in lieu of other compensation. This is the key of one of the makeup sessions used to attend an event. This is also present for a repeat purchase of an event. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `s_title`</dt> <dd>The name of the purchase item.</dd> <dt>string `uid`</dt> <dd>The ID of the purchase owner. <i>Primary key {@link \PassportLoginSql} in table.</i></dd> </dl>'
      tags:
        - PurchaseList-PurchaseList
  '/Wl/Profile/Relation/Inheritance.json':
    get:
      summary: 'Gets full list of ranks owned by the client.'
      parameters:
        - name: id_field_general
          in: query
          description: 'Type of the field. One of {@link \RsFieldGeneralSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if API is being used from backend, `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User''s key. `null` if not specified.'
          schema:
            type: [string, "null"]
        - name: uid_relative
          in: query
          description: 'Relative''s User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_address:
                        type: string
                        description: 'Text representation of relative''s address.'
                      text_cell_phone:
                        type: string
                        description: 'Text representation of relative''s cell phone number.'
                      text_city:
                        type: string
                        description: 'Text representation of relative''s city.'
                      text_city_key:
                        type: string
                        description: 'Text representation of relative''s city''s key for combobox.'
                      text_home_phone:
                        type: string
                        description: 'Text representation of relative''s home phone number.'
                      text_mail:
                        type: string
                        description: 'Text representation of relative''s email address.'
                      text_postal:
                        type: string
                        description: 'Text representation of relative''s postal code.'
                      text_work_phone:
                        type: string
                        description: 'Text representation of relative''s work phone number.'
      tags:
        - Relation-Inheritance
  '/Wl/Profile/Edit/Edit.json':
    get:
      summary: 'Returns current user profile information. Returns the profile field definitions and current values for the specified user, or an empty structure when creating a new account. Used to populate the profile edit form with the correct fields, validation rules, and inheritance options for the business.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      a_phone_inherit:
                        type: array
                        description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                      a_structure:
                        type: array
                        description: 'The values and structure of all fields. Array keys are field IDs (`k_field`). Array values are the field values. The array has the following structure: <dl> <dt>int `id_field_general`</dt> <dd> The ID of a system field. One of the {@link \RsFieldGeneralSid} constants. This value is only defined if <var>id_field_type</var>={@link \RsFieldTypeSid::GENERAL}. </dd> <dt>bool `is_require`</dt> <dd> Indicates whether the value of this field is required. This will be `1` if required or `0` if the field is optional. </dd> <dt>int `id_field_type`</dt> <dd>The type of field. One of the {@link \RsFieldTypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>The field ID (<var>k_field</var>). A copy of the key of this array element.</dd> <dt>string `s_title`</dt> <dd>The title of the field.</dd> <dt>mixed `x_value`</dt> <dd>The value of the field. This value is defined by individual fields.</dd> </dl>'
                      can_password_change:
                        type: boolean
                        description: 'Whether current user can change password of the given user or not. If client is part of multiple businesses, then staff of the business can only request reset of the password, but cannot change it.'
                      is_a2p:
                        type: boolean
                        description: '`true` if the A2P 10DLC registration feature is enabled for this business, `false` otherwise.'
                      is_address_inherit:
                        type: [boolean, "null"]
                        description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
                      is_short:
                        type: boolean
                        description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
                      k_lead_source:
                        type: [string, "null"]
                        description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid_inherit_address:
                        type: [string, "null"]
                        description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-Edit
    post:
      summary: 'Creates a new user. Registers a new user in the business using the submitted profile field values, enforces IP-based registration rate limiting, and handles family relations and phone or address inheritance. Returns the identifier of the newly created account.'
      parameters:
        - name: id_mode
          in: query
          description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: [number, "null"]
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_short
          in: query
          description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: text_business_uid_key
          in: query
          description: 'Compound key delimited with a colon. First part is the business key where the selected client exists. Second part is the uid of the already existing user we want to add. Empty if a non-existent client is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_existed
          in: query
          description: 'The UID of an existing user in another business to add to the current business.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
        - name: uid_relative_key
          in: query
          description: 'UID of the user, whose email was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose email is inherited.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_family_relation:
                  type: [array, "null"]
                  description: 'Family relation data for the newly created user. `null` if no family relation is required: <dl> <dt>int `id_family_relation`</dt> <dd>Relation type. One of {@link \RsFamilyRelationSid} constants.</dd> <dt>bool `is_relative_pay`</dt> <dd>`true` if the relative will pay for this user; `false` or absent otherwise.</dd> <dt>string `uid_from`</dt> <dd> User key of the relative (must be the currently logged-in user). <i>Primary key in the {@link \PassportLoginSql} table.</i> </dd> </dl>'
                a_image_upload:
                  type: array
                  description: 'Information for user''s photo. It is necessary to save simultaneously user and photo in staff member mode. Attention! Data from this field is taken directly from a POST somewhere in the depths of the photo upload.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_new:
                  type: array
                  description: 'A copy of the {@link EditApi::$a_change}. This is used for POST requests when creating a new user.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                is_exception_throw:
                  type: boolean
                  description: '`true` to throw an exception in a case of validation error; `false` to not throw.'
                is_sing_in:
                  type: boolean
                  description: 'This will be `true` to sign in a created user. Otherwise, this will be `false`.'
                is_sms_subscription_marketing:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive marketing SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                is_sms_subscription_presented:
                  type: boolean
                  description: '<tt>true</tt> - user was shown checkboxes to subscribe to marketing and transactional sms messages. This means we need to modify his subscription based on the values in these fields. <tt>false</tt> - otherwise. This means we should not change subscription settings.'
                is_sms_subscription_transactional:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive transactional SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                text_password:
                  type: string
                  description: 'The password to be set for a new user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      s_status:
                        type: [string, "null"]
                        description: 'Status of the request. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid:
                        type: string
                        description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-Edit
    put:
      summary: 'Updates values of profile fields. Saves the updated profile field values for the existing user, propagates phone and address inheritance changes, triggers relevant notifications, and re-indexes the user for search.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_change:
                  type: array
                  description: 'A list of fields to change. Values are the new field values. Specific values depend on an individual field type. Some fields may be strings while others are arrays. If a composite field is changed, all fields should be specified (for example, if you change your address, you''ll need to specify the city). Keys are the `k_field` values.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
  '/Wl/Profile/Edit/Create.json':
    post:
      summary: 'Creates a new user. Creates a new user account with the given email, first name, and last name in the specified business, registers the user as a member, and returns the `uid` of the created or existing user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of the business where you''re creating new user.'
                text_firstname:
                  type: string
                  description: 'First name of the user.'
                text_lastname:
                  type: string
                  description: 'Last name of the user.'
                text_mail:
                  type: string
                  description: 'Email of the user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'The key of the user added.'
      tags:
        - Edit-Create
  '/Wl/Profile/Edit/EditByToken.json':
    get:
      summary: 'Returns current user profile information. Returns the profile field definitions and current values for the specified user, or an empty structure when creating a new account. Used to populate the profile edit form with the correct fields, validation rules, and inheritance options for the business.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      a_phone_inherit:
                        type: array
                        description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                      a_structure:
                        type: array
                        description: 'The values and structure of all fields. Array keys are field IDs (`k_field`). Array values are the field values. The array has the following structure: <dl> <dt>int `id_field_general`</dt> <dd> The ID of a system field. One of the {@link \RsFieldGeneralSid} constants. This value is only defined if <var>id_field_type</var>={@link \RsFieldTypeSid::GENERAL}. </dd> <dt>bool `is_require`</dt> <dd> Indicates whether the value of this field is required. This will be `1` if required or `0` if the field is optional. </dd> <dt>int `id_field_type`</dt> <dd>The type of field. One of the {@link \RsFieldTypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>The field ID (<var>k_field</var>). A copy of the key of this array element.</dd> <dt>string `s_title`</dt> <dd>The title of the field.</dd> <dt>mixed `x_value`</dt> <dd>The value of the field. This value is defined by individual fields.</dd> </dl>'
                      can_password_change:
                        type: boolean
                        description: 'Whether current user can change password of the given user or not. If client is part of multiple businesses, then staff of the business can only request reset of the password, but cannot change it.'
                      is_a2p:
                        type: boolean
                        description: '`true` if the A2P 10DLC registration feature is enabled for this business, `false` otherwise.'
                      is_address_inherit:
                        type: [boolean, "null"]
                        description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
                      is_short:
                        type: boolean
                        description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
                      k_lead_source:
                        type: [string, "null"]
                        description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid_inherit_address:
                        type: [string, "null"]
                        description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-EditByToken
    post:
      summary: 'Creates a new user. Registers a new user in the business using the submitted profile field values, enforces IP-based registration rate limiting, and handles family relations and phone or address inheritance. Returns the identifier of the newly created account.'
      parameters:
        - name: id_mode
          in: query
          description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: [number, "null"]
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_short
          in: query
          description: 'Indicates whether to display the full profile edit form or the short version. This is assumed to be used only in user registration mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: text_business_uid_key
          in: query
          description: 'Compound key delimited with a colon. First part is the business key where the selected client exists. Second part is the uid of the already existing user we want to add. Empty if a non-existent client is being added.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_existed
          in: query
          description: 'The UID of an existing user in another business to add to the current business.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
        - name: uid_relative_key
          in: query
          description: 'UID of the user, whose email was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose email is inherited.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_family_relation:
                  type: [array, "null"]
                  description: 'Family relation data for the newly created user. `null` if no family relation is required: <dl> <dt>int `id_family_relation`</dt> <dd>Relation type. One of {@link \RsFamilyRelationSid} constants.</dd> <dt>bool `is_relative_pay`</dt> <dd>`true` if the relative will pay for this user; `false` or absent otherwise.</dd> <dt>string `uid_from`</dt> <dd> User key of the relative (must be the currently logged-in user). <i>Primary key in the {@link \PassportLoginSql} table.</i> </dd> </dl>'
                a_image_upload:
                  type: array
                  description: 'Information for user''s photo. It is necessary to save simultaneously user and photo in staff member mode. Attention! Data from this field is taken directly from a POST somewhere in the depths of the photo upload.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_new:
                  type: array
                  description: 'A copy of the {@link EditApi::$a_change}. This is used for POST requests when creating a new user.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
                is_exception_throw:
                  type: boolean
                  description: '`true` to throw an exception in a case of validation error; `false` to not throw.'
                is_sing_in:
                  type: boolean
                  description: 'This will be `true` to sign in a created user. Otherwise, this will be `false`.'
                is_sms_subscription_marketing:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive marketing SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                is_sms_subscription_presented:
                  type: boolean
                  description: '<tt>true</tt> - user was shown checkboxes to subscribe to marketing and transactional sms messages. This means we need to modify his subscription based on the values in these fields. <tt>false</tt> - otherwise. This means we should not change subscription settings.'
                is_sms_subscription_transactional:
                  type: boolean
                  description: '<tt>true</tt> - user agreed to receive transactional SMS; <tt>false</tt> - otherwise. Only matters if {@link EditApi::$is_sms_subscription_presented} is `true`.'
                text_password:
                  type: string
                  description: 'The password to be set for a new user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
                      s_class:
                        type: [string, "null"]
                        description: 'Exception class name. `null` if there was no mistake.'
                      s_code:
                        type: [string, "null"]
                        description: 'Code of the error. `null` if there was no mistake.'
                      s_status:
                        type: [string, "null"]
                        description: 'Status of the request. `null` if there was no mistake.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` if there was no mistake.'
                      uid:
                        type: string
                        description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      a_error_list:
                        type: [array, "null"]
                        description: 'List of validation errors. `null` if no error occurred. Each element: <dl> <dt>string `code`</dt> <dd>Error code identifying the type of validation failure.</dd> <dt>string `field`</dt> <dd>Field identifier that caused the error.</dd> <dt>string `message`</dt> <dd>Human-readable error message.</dd> </dl>'
      tags:
        - Edit-EditByToken
    put:
      summary: 'Updates values of profile fields. Saves the updated profile field values for the existing user, propagates phone and address inheritance changes, triggers relevant notifications, and re-indexes the user for search.'
      parameters:
        - name: id_register_source
          in: query
          description: 'Registration source ID. One of {@link RegisterSourceSid} constants. Used only to register new clients. * If the client is already authorized, the field value will not be used. * If the client is not authorized and no value is set, {@link RegisterSourceSid::SELF} will be used. <i> Use the {@link EditApi::_registerSourceGet()} method to get the value required for the field list object, for method {@link FieldList::registerSourceSet()}. </i>'
          schema:
            type: [number, "null"]
        - name: is_address_inherit
          in: query
          description: 'Whether the address be inherited. `true` if address inherit, `false` to remove inheritance. `null` if not explicitly set (PUT only: preserves the current state).'
          schema:
            type: [boolean, "null"]
        - name: is_staff
          in: query
          description: 'Indicates whether to display the form as a user or as a staff member. Staff members may have access to different fields than the user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business you''re editing. An empty value will return the system-wide fields.'
          schema:
            type: string
        - name: k_lead_source
          in: query
          description: 'Key of the lead source. `null` if not defined. When creating or editing a user: {@link LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The key of the user to edit. If empty, an empty form will be displayed to add a new user.'
          schema:
            type: string
        - name: uid_inherit_address
          in: query
          description: 'UID of the user, whose address was inherited by the existing client we want to add. Empty if non-existent user is being added or user to add is the one, whose address is inherited. `null` If the address is not inherited.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_change:
                  type: array
                  description: 'A list of fields to change. Values are the new field values. Specific values depend on an individual field type. Some fields may be strings while others are arrays. If a composite field is changed, all fields should be specified (for example, if you change your address, you''ll need to specify the city). Keys are the `k_field` values.'
                a_intents:
                  type: array
                  description: 'List of intent identifiers. Each element is one of {@link \Wl\Login\Member\Intents\MemberIntentsSid} constants. Available only for leads added by CAASI agent.'
                a_phone_inherit:
                  type: array
                  description: 'An array contained with information about phone inheritance. The array has the following structure: <dl> <dt>bool `is_phone_inherit`</dt> <dd> Indicates weather to inherit phone numbers from relative or not. `1` if phone inheritance is needed, ''0'' otherwise. </dd> <dt>string `text_relative`</dt> <dd>Relative''s name.</dd> <dt>string `uid_relative`</dt> <dd>User key of relative.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-EditByToken
  '/Wl/Profile/Edit/EditPassword.json':
    post:
      summary: 'Saves new password for user. Changes the user''s password after verifying the current one, enforcing complexity and length rules, and sends a password-change notification email. The old password check can be skipped by staff with the appropriate access level.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_old_password_required:
                  type: boolean
                  description: 'Checks if the current password is required or not to update the password.'
                k_business:
                  type: string
                  description: 'The key ID of business where actions take place. Business is necessary for mail sending only.'
                s_password_confirm:
                  type: string
                  description: 'The confirmation of new password.'
                s_password_new:
                  type: string
                  description: 'The new password.'
                s_password_old:
                  type: string
                  description: 'The old password.'
                uid:
                  type: string
                  description: 'The user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-EditPassword
  '/Wl/Profile/Prize/Element.json':
    get:
      summary: 'Retrieves information about prize item of user.'
      parameters:
        - name: k_login_prize
          in: query
          description: 'Key of redeemable prize.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_score:
                        type: number
                        format: int32
                        description: 'Prize price in wellnessliving points.'
                      s_date:
                        type: [string, "null"]
                        description: 'Date of redeem.'
                      s_description:
                        type: string
                        description: 'User friendly prize description.'
      tags:
        - Prize-Element
    post:
      summary: 'Sets number of remaining prize quantity to 0.'
      parameters:
        - name: k_login_prize
          in: query
          description: 'Key of redeemable prize.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Prize-Element
  '/Wl/Profile/Prize/Prize.json':
    get:
      summary: 'Retrieves a list of prizes redeemed by user.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of current business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Array of prize keys redeemed by user.'
      tags:
        - Prize-Prize
  '/Wl/Profile/Sensor/SensorSave.json':
    delete:
      summary: 'Deletes sensor.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_sensor
          in: query
          description: 'Sensor ID.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Sensor-SensorSave
    post:
      summary: 'Saves sensor info while adding and editing sensors. If deleting sensor, else sensor row data for template: <dl> <dt>array <var>a_class_menu</var></dt> <dd>Menu data with structure: <dl> <dt>string <var>s_id</var></dt> <dd>ID of the current button.</dd> <dt>string <var>s_class</var></dt> <dd>Class for current button.</dd> <dt>string <var>s_onclick</var></dt> <dd>Code to execute on button click.</dd> <dt>string <var>s_title</var></dt> <dd>Button title.</dd> </dl> </dd> <dt>string <var>s_sensor</var></dt> <dd>Sensor id.</dd> <dt>string <var>s_id_sensor</var></dt> <dd>Id name for sensor menu button.</dd> <dt>string <var>text_name</var></dt> <dd>Sensor name.</dd> </dl>'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_sensor
          in: query
          description: 'Sensor ID.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_sensor_type:
                  type: number
                  format: int32
                  description: 'Sensor type id, one of {@link SensorTypesSid} constants.'
                is_edit:
                  type: boolean
                  description: '<tt>true</tt> if editing sensor, <tt>false</tt> if adding.'
                s_old_sensor:
                  type: string
                  description: 'Old sensor ID before editing.'
                text_name:
                  type: string
                  description: 'Sensor name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_page:
                        type: number
                        format: int32
                        description: 'Page number.'
      tags:
        - Sensor-SensorSave
  '/Wl/Profile/Sensor/SensorList.json':
    get:
      summary: 'Returns data for given page to show in view.'
      parameters:
        - name: i_page
          in: query
          description: 'Page to display.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_page:
                        type: array
                        description: '<tt>false</tt> if no pages present or pagination data with structure: <dl> <dt>int `page`</dt> <dd>Page number.</dd> <dt>bool `is_selected`</dt> <dd>Whether the page is current.</dd> <dt>bool `is_previous`</dt> <dd>Whether the page is previous.</dd> <dt>bool `is_next`</dt> <dd>Whether the page is next.</dd> <dt>string `onclick`</dt> <dd>Code to execute after button is clicked.</dd> </dl>'
                      a_sensor:
                        type: array
                        description: 'Sensors data with structure: <dl> <dt>array `a_class_menu`</dt> <dd> Menu data with structure: <dl> <dt>string `s_id`</dt> <dd>ID of the current button.</dd> <dt>string `s_class`</dt> <dd>Class for current button.</dd> <dt>string `s_onclick`</dt> <dd>Code to execute on button click.</dd> <dt>string `s_title`</dt> <dd>Button title.</dd> </dl> </dd> <dt>string `s_sensor`</dt> <dd>Sensor id.</dd> <dt>string `s_id_sensor`</dt> <dd>Id name for sensor menu button.</dd> <dt>string `text_name`</dt> <dd>Sensor name.</dd> </dl>'
                      i_current_page:
                        type: number
                        format: int32
                        description: 'Current page number.'
                      k_business:
                        type: string
                        description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
                      uid:
                        type: string
                        description: 'User key. Primary key in {@link \PassportLoginSql} table.'
      tags:
        - Sensor-SensorList
  '/Wl/Profile/Merge/ProfileMerge.json':
    post:
      summary: 'Executes merging of clients.'
      parameters:
        - name: a_field_save
          in: query
          description: 'List of fields to save. Keys are keys of fields. Primary key in the {@link \RsFieldSql} table. Values are keys of users from whose profile field value is needed save. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to merge. One element is primary key in {@link \PassportLoginSql}.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Key of current business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid_main:
                        type: [string, "null"]
                        description: 'Key of main user. Primary key in {@link \PassportLoginSql} table. <tt>null</tt> if not set yet.'
      tags:
        - Merge-ProfileMerge
  '/Wl/Profile/Setting/Setting.json':
    get:
      summary: 'Retrieves a list of user settings and other additional information for the settings page. Returns the user''s current notification preferences for the business, covering account activity, news, and schedule reminders across email and SMS channels, along with the user''s current language setting.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_account_management_email:
                        type: boolean
                        description: 'Determines whether email notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                      is_account_management_sms:
                        type: boolean
                        description: 'Determines whether SMS notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                      is_news_and_updates_email:
                        type: boolean
                        description: 'Determines whether email notifications related to news and updates from the business regarding their services, availability, and promotions are enabled.'
                      is_news_and_updates_sms:
                        type: boolean
                        description: 'Whether sms notifications related to news and updates from the business regarding their services, availability and promotions are enabled.'
                      is_schedule_and_reminders_email:
                        type: boolean
                        description: 'Determines whether email notifications related to the services a client has booked are enabled.'
                      is_schedule_and_reminders_sms:
                        type: boolean
                        description: 'Determines whether SMS notifications related to the services a client has booked are enabled.'
                      text_language:
                        type: string
                        description: 'SID of language code.'
      tags:
        - Setting-Setting
    post:
      summary: 'Saves the user''s notification settings for the specified business. Saves the user''s notification preferences for the business, updating email and SMS opt-in settings for account activity, news, and schedule reminders.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_account_management_email:
                  type: boolean
                  description: 'Determines whether email notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                is_account_management_sms:
                  type: boolean
                  description: 'Determines whether SMS notifications related to purchases, contracts, and other activity in a client''s account are enabled.'
                is_news_and_updates_email:
                  type: boolean
                  description: 'Determines whether email notifications related to news and updates from the business regarding their services, availability, and promotions are enabled.'
                is_news_and_updates_sms:
                  type: boolean
                  description: 'Whether sms notifications related to news and updates from the business regarding their services, availability and promotions are enabled.'
                is_schedule_and_reminders_email:
                  type: boolean
                  description: 'Determines whether email notifications related to the services a client has booked are enabled.'
                is_schedule_and_reminders_sms:
                  type: boolean
                  description: 'Determines whether SMS notifications related to the services a client has booked are enabled.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Setting-Setting
  '/Wl/Profile/Rank/ProfileRank.json':
    get:
      summary: 'Gets promotion date on belts.'
      parameters:
        - name: k_rank
          in: query
          description: 'Rank key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of user promotion date on belts of whom to be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtu_promotion_date:
                        type: [string, "null"]
                        description: 'Promotion date on belts. <tt>null</tt> if not initialized yet.'
      tags:
        - Rank-ProfileRank
    post:
      summary: 'Sets promotion date on belts.'
      parameters:
        - name: dtu_promotion_date
          in: query
          description: 'Promotion date on belts. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_login_rank
          in: query
          description: 'Login rank key. Can be empty. In this case a promotion date will be set for currently promoted belt and belts in all franchisees a belt was pushed to.'
          schema:
            type: string
        - name: k_rank
          in: query
          description: 'Rank key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of user promotion date on belts of whom to be changed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Rank-ProfileRank
  '/Wl/Profile/Rank/Addon.json':
    get:
      summary: 'Gets full list of ranks owned by the client.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_rank_category
          in: query
          description: 'Rank Category key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User''s key in which profile should be shown addons.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_addon:
                        type: array
                        description: 'Full list of ranks owned by the client. The key of the array is a category rank key. Primary key in the {@link \RsRankCategorySql} table. <dl> <dt>string `dt_add`</dt> <dd>Date, when rank has been added.</dd> <dt>string `k_rank`</dt> <dd>Rank key. Primary key in the {@link \RsRankSql} table.</dd> <dt>string `k_rank_category`</dt> <dd>Category rank key. Primary key in the {@link \RsRankCategorySql} table.</dd> <dt>string `s_rank`</dt> <dd>Rank name.</dd> <dt>string `s_rank_category`</dt> <dd>Rank category name.</dd> <dt>string `uid`</dt> <dd>User''s key. Primary key in {@link \PassportLoginSql} table.</dd> <dt>string `z_addon`</dt> <dd>Compressed and serialised list of addons, that client has.</dd> <dt>string[] `a_addon`</dt> <dd>Colors for character and skill sections.</dd> <dt>string `s_date`</dt> <dd>Date, when rank has been added.</dd> <dt>int `i_days_left`</dt> <dd>The number of days that have passed since the start date of the set time period for class attendance.</dd> <dt>int `i_duration`</dt> <dd>The number of days/months/years that were set for attending the classes.</dd> <dt>string `s_duration`</dt> <dd>Tha name of the duration (day/month/year).</dd> </dl>'
                      a_addon_list:
                        type: array
                        description: 'HEX color codes for Character and Skill fields. <dl> <dt>array `a_addon`</dt> <dd>Array of addons.</dd> <dt>string `s_title`</dt> <dd>The title for the row (Character | Skill).</dd> </dl>'
      tags:
        - Rank-Addon
    post:
      summary: 'Sets addon ranks in Belt.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_rank_category
          in: query
          description: 'Rank Category key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User''s key in which profile should be shown addons.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_addon_new:
                  type: array
                  description: 'List of new addons for rank where each item is a selected HEX color code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Rank-Addon
  '/Wl/Profile/Activity/Element.json':
    get:
      summary: 'Retrieves information about activity item. Loads a single activity record by `k_login_activity`, validates access via profile privileges, and returns the activity message, icon, type, reward and credit scores, activity dates in UTC and local time, spend amount, and a share URL.'
      parameters:
        - name: k_login_activity
          in: query
          description: 'The key of the activity item represented by this endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_credit_score:
                        type: array
                        description: 'Messages with description what did user do to get account credits as reward points.'
                      a_reward_score:
                        type: array
                        description: 'Messages with description what did user do to get points.'
                      can_profile:
                        type: boolean
                        description: 'Verifies that current user can view the specified profile.'
                      dt_date_gmt:
                        type: string
                        description: 'The date of the activity in GMT.'
                      dt_date_local:
                        type: string
                        description: 'The date of the activity in the client''s time zone.'
                      html_message:
                        type: string
                        description: 'Description of the action, who and what did.'
                      i_credit_score:
                        type: number
                        format: int32
                        description: 'Total amount of account credits user got for {@link ElementApi::$k_login_activity}. <i>This field is a copy of result of {@link \RsLoginActivity::reward()}.</i>'
                      i_score:
                        type: number
                        format: int32
                        description: 'The total amount of rewards points the client received for the activity.'
                      i_spend:
                        type: number
                        format: int32
                        description: 'The rewards points used to redeem a prize.'
                      id_icon:
                        type: [number, "null"]
                        description: 'The ID of the icon that should be shown for this activity. One of {@link IconSid} constants. This will be `null` is no special icon for this activity.'
                      id_type:
                        type: number
                        format: int32
                        description: 'The ID of the activity item returned by this endpoint. One of {@link \RsLoginActivityTypeSid} constants.'
                      k_id:
                        type: string
                        description: 'Object ID, for example, class period ID for books and visits.'
                      s_message:
                        type: string
                        description: 'The description of the activity. This should include the nature of the activity and the people involved.'
                      url_link:
                        type: string
                        description: 'Link to share activity with social networks.'
      tags:
        - Activity-Element
  '/Wl/Profile/Activity/List.json':
    get:
      summary: 'Retrieves a list of activity items to show in user profile. Returns the client''s activity history for the specified business, filtered by an optional date range. Requires profile-view access and respects activity-type visibility rules for the requesting user.'
      parameters:
        - name: dl_end
          in: query
          description: 'Ending date of the date range. Optional. This is local business timezone date. This day is included in the result.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Starting date of the date range. Optional. This is local business timezone date. This day is included in the result.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the client to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_activity:
                        type: array
                        description: 'An array listing client activities, where each activity is provided as an ID number. The order of items in this array is the order in which the elements should be displayed.'
      tags:
        - Activity-List
  '/Wl/Profile/Timezone/ProfileTimezone.json':
    get:
      summary: 'Gets a list of timezones with currently selected user''s timezone and the business policy adjustment whether clients are allowed to adjust timezone.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, in which user selected timezone. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_timezone:
                        type: array
                        description: 'List of timezones. Keys - timezone keys; primary keys in {@link \AGeoTimezoneSql} table. Values - sub array with next keys: <dl> <dt>string `k_geo_timezone`</dt> <dd>Timezone key. Primary key in {@link \AGeoTimezoneSql} table.</dd> <dt>int `i_shift`</dt> <dd>Shift time zone.</dd> <dt>string `s_file`</dt> <dd>File name.</dd> <dt>int `i_order`</dt> <dd>Order.</dd> <dt>string `text_abbr`</dt> <dd>Timezone abbreviation.</dd> <dt>string `s_title`</dt> <dd>Timezone title.</dd> <dt>string `k_timezone`</dt> <dd>Timezone key.</dd> </dl>'
                      is_profile_timezone:
                        type: boolean
                        description: 'Whether clients are allowed to adjust timezone. `true` if allowed, `false` - otherwise.'
                      k_timezone_select:
                        type: string
                        description: 'Key of the timezone which is currently selected. Primary key in {@link \AGeoTimezoneSql}.'
      tags:
        - Timezone-ProfileTimezone
    put:
      summary: 'Updates selected timezone for the site visitor in the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, in which user selected timezone. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_deprecate_restrict:
                  type: boolean
                  description: 'If a timezone deprecation validation is needed. `true` if required, `false` - otherwise.'
                k_timezone:
                  type: string
                  description: 'Key of the timezone, which user selected to see site in. Primary key in {@link \AGeoTimezoneSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Timezone-ProfileTimezone
  '/Wl/Profile/Billing/Billing.json':
    get:
      summary: 'Retrieves information about billing page display properties.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve information about. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_show_account:
                        type: boolean
                        description: 'Whether to show account information.'
                      is_show_transaction:
                        type: boolean
                        description: 'Whether to show credit card and ACH account list.'
                      s_payer_name:
                        type: [string, "null"]
                        description: 'Payer name in case if user is not self paid, <tt>null</tt> otherwise.'
                      uid_payer:
                        type: string
                        description: 'ID of a payer user to show logo for.'
      tags:
        - Billing-Billing
  '/Wl/Profile/Notification/List.json':
    get:
      summary: 'Retrieves a list of push notification items to show in user profile.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to show information for. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to show information for. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_notification:
                        type: array
                        description: 'A list of notification items. Each value is a primary key in {@link \Core\Push\History\Sql}.'
      tags:
        - Notification-List
  '/Wl/Profile/Notification/Element.json':
    get:
      summary: 'Retrieves information about push notification item.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_push_history
          in: query
          description: 'Key of push notification to retrieve information about. Primary key in {@link \Core\Push\History\Sql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_send:
                        type: string
                        description: 'Date of push notification sending.'
                      text_message:
                        type: string
                        description: 'Push notification message.'
                      text_title:
                        type: string
                        description: 'Push notification title.'
      tags:
        - Notification-Element
  '/Wl/Profile/Waiver/Waiver.json':
    get:
      summary: 'Gets list of subscribed waivers.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to get information for. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to get information for. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_waiver:
                        type: array
                        description: 'List of subscribed waivers. Every element has next keys: <dl> <dt>string `dt_agree_local`</dt> <dd>Agreement date in business timezone.</dd> <dt>string `dt_agree_utc`</dt> <dd>Agreement date in UTC.</dd> <dt>string `html_contract`</dt> <dd>Contract text.</dd> <dt>string `ip_agree`</dt> <dd>IP address from which agreement was done.</dd> <dt>string `url_signature`</dt> <dd>URL to agreement signature.</dd> </dl>'
                      i_minor_age:
                        type: number
                        format: int32
                        description: 'Age of minor which documents can be signed by parent or legal guardian.'
      tags:
        - Waiver-Waiver
  '/Wl/Profile/Visit/Visit.json':
    post:
      summary: 'Registers a visit to user profile by current user. This is used to show a list of users that current user has recently visited in search results.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                uid_profile:
                  type: string
                  description: 'UID of the user that is visited by current user. Primary key in {@link \PassportLoginSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Visit-Visit
  '/Wl/Purchase/Item/PurchaseItemAmount.json':
    get:
      summary: 'Returns information about amounts of the purchase item.'
      parameters:
        - name: dl_client_prorate
          in: query
          description: 'Client prorate date.'
          schema:
            type: [string, "null"]
        - name: i_count
          in: query
          description: 'Number of units of the purchase item.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'ID of the purchase item. One of {@link \RsPurchaseItemSid} constants. Key of the appropriate item stored in {@link PurchaseItemAmountApi::$k_id}.'
          schema:
            type: number
            format: int32
        - name: is_new
          in: query
          description: 'Whether purchase item is new.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of the business that should be set to the purchase item. Primary key from {@link \RsBusinessSql}. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: k_id
          in: query
          description: 'Key of the item within appropriate table according to {@link PurchaseItemAmountApi::$id_purchase_item}. Can be <tt>null</tt> if it is new purchase item.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Key of the location purchase occur. Primary key from {@link \RsLocationSql}. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: k_reward_prize
          in: query
          description: 'Key of the reward prize that should be applied to the purchase item. Primary key from {@link \RsRewardPrizeSql}.'
          schema:
            type: string
        - name: m_price
          in: query
          description: 'Manual price that should be set to the purchase item. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: uid_purchase
          in: query
          description: 'Key of the user that performs purchase. Primary key from {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_out_of_stock:
                        type: boolean
                        description: 'Whether purchase item is out of stock.'
                      m_discount:
                        type: string
                        description: 'Amount of discounts applied to the purchase item.'
                      m_subtotal:
                        type: string
                        description: 'Amount of all units of the purchase item without taxes and discounts.'
                      m_tax:
                        type: string
                        description: 'Amount of all taxes applied to the purchase item.'
                      m_total:
                        type: string
                        description: 'Total amount of the purchase item.'
      tags:
        - Item-PurchaseItemAmount
  '/Wl/Purchase/Item/PurchaseItemList.json':
    get:
      summary: 'Gets information about purchase and it''s items.'
      parameters:
        - name: is_web
          in: query
          description: 'Define a different type result of API. For economy of traffic `false` it means in response will less data. Otherwise, more detailed data will be returned.'
          schema:
            type: boolean
        - name: k_purchase
          in: query
          description: 'Purchase ID. Primary key in {@link \RsPurchaseSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_additional_info:
                        type: array
                        description: 'Additional information. Uses only on desktop version.'
                      a_logo:
                        type: array
                        description: 'Logo details array.'
                      a_purchase_item:
                        type: array
                        description: 'List of purchase items: <dl> <dt>string `m_price`</dt> <dd>Item price. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `true`</dd> <dt>string `text_title`</dt> <dd>Item title. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `true`</dd> <dt>string `html_category`</dt> <dd> Item category. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> <dt>string `html_description`</dt> <dd> Item description. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> <dt>string `html_price`</dt> <dd>Item price. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false`</dd> <dt>string `html_title`</dt> <dd>Item title. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false`</dd> <dt>string `html_total`</dt> <dd> Item total price. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> <dt>int `i_count`</dt> <dd>Item count. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false`</dd> <dt>int `i_logo_height`</dt> <dd> Item picture height. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> <dt>int `i_logo_width`</dt> <dd> Item picture width. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> <dt>int `id_purchase_item`</dt> <dd>. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false`</dd> <dt>bool `is_logo_empty`</dt> <dd> Whether item picture exists. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> <dt>string `s_logo_url`</dt> <dd> Item picture URL. This key doesn''t returned when {@link \Wl\Purchase\Item\PurchaseItemListApi::$is_web} is `false` </dd> </dl>'
                      dt_date_local:
                        type: string
                        description: 'Purchase date in business timezone.'
                      k_business:
                        type: string
                        description: 'Business ID.'
                      k_currency:
                        type: string
                        description: 'Purchase currency. Primary key in {@link \RsCurrencySql} table.'
                      k_location:
                        type: string
                        description: 'Purchase location. Primary key in {@link \RsLocationSql} table.'
                      m_discount:
                        type: string
                        description: 'Discount amount.'
                      m_subtotal:
                        type: string
                        description: 'Subtotal amount.'
                      m_surcharge:
                        type: [string, "null"]
                        description: 'Surcharge amount. <tt>null</tt> - if purchase is made without surcharge and not is web.'
                      m_tax:
                        type: string
                        description: 'Tax amount.'
                      m_tip:
                        type: string
                        description: 'Tips amount.'
                      m_total:
                        type: string
                        description: 'Total amount.'
                      text_pay_method:
                        type: string
                        description: 'Human-readable description of payment method.'
                      uid:
                        type: string
                        description: 'Purchase owner. Primary key in {@link \PassportLoginSql} table.'
      tags:
        - Item-PurchaseItemList
  '/Wl/Purchase/Pool/PoolItem.json':
    get:
      summary: 'Checks if certain item exists in the active purchase pool.'
      parameters:
        - name: i_attempt
          in: query
          description: 'Include only purchase pools with `i_attempt` number of done attempts or more.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'Type of the item in the pool from {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business, where pool is created.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Key of the item, which depends on {@link PoolItemApi::$id_purchase_item}.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The key of the login promotion. If this is set, {@link PoolItemApi::$k_id} and {@link PoolItemApi::$id_purchase_item} are not used.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_purchase_pool:
                        type: string
                        description: 'Key of the active purchase pool, if it''s found. In theory, can be multiple pools for the same item, one random pool will be returned in this case.'
      tags:
        - Pool-PoolItem
  '/Wl/Purchase/Staff/PurchaseStaffCommission.json':
    get:
      summary: 'Gets information about staff commission for the purchase item.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_purchase
          in: query
          description: 'Purchase key.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'Purchase item key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_available_staff:
                        type: array
                        description: 'Information about all staff commission in the business which can be applied for purchase. Every element has next keys: <dl> <dt>array `a_staff_pay`</dt> <dd> List of staff pay rates. Every element has next information: <dl> <dt>bool `is_select`</dt> <dd><tt>true</tt> if pay rate is applied for purchase; <tt>false</tt> otherwise.</dd> <dt>string `k_staff_pay`</dt> <dd>Pay rate key, primary key in {@link \RsStaffPaySql}.</dd> <dt>string `text_pay_title`</dt> <dd>Pay rate title.</dd> </dl> </dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if staff pay rate is applied for purchase; <tt>false</tt> otherwise.</dd> <dt>string `text_staff_family`</dt> <dd>Staff surname.</dd> <dt>string `text_staff_name`</dt> <dd>Staff name.</dd> <dt>string `uid_staff`</dt> <dd>Staff uid key, primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                      a_commission_purchase:
                        type: array
                        description: 'Information about applied staff commission for purchase. Each element has next structure: <dl> <dt>string `k_staff_pay`</dt> <dd>Staff pay key, primary key in {@link \RsStaffPaySql} table.</dd> <dt>string `text_staff_family`</dt> <dd>Staff surname.</dd> <dt>string `text_staff_name`</dt> <dd>Staff name.</dd> <dt>string `text_pay_title`</dt> <dd>Pay rate title.</dd> <dt>string `uid_staff`</dt> <dd>Staff uid key, primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                      a_future_payments:
                        type: array
                        description: 'Settings of applied commission for future auto-payments: <dl> <dt>int|null `i_future_payments`</dt> <dd>Limit of future auto-payments to apply commission. `null`  if not limit.</dd> <dt>int|null `i_future_payments_left`</dt> <dd>Count of left of future auto-payments to apply commission. `null`  if not limit.</dd> <dt>bool `is_future_payments`</dt> <dd>`true` if commission is applied for future auto-payments, `false` otherwise.</dd> </dl>'
                      a_past_payments:
                        type: array
                        description: 'Information about past auto-payments for which the commission has been applied: <dl> <dt>int `i_payment`</dt> <dd>Number of auto-payments.</dd> <dt>string `url_report`</dt> <dd>Url to the staff commission report. See {@link \RsReportSid::STAFF_PAY_DETAIL}.Empty string if no payments.</dd> </dl>'
                      can_override:
                        type: boolean
                        description: 'Whether current user can override commission or not.'
                      is_recurring:
                        type: boolean
                        description: 'Whether the promotion, that was paid by this purchase, is renewable. This determines if the commission can be applied to future auto-payments.'
      tags:
        - Staff-PurchaseStaffCommission
    post:
      summary: 'Saves staff commission for the purchase items.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_purchase
          in: query
          description: 'Purchase key.'
          schema:
            type: string
        - name: k_purchase_item
          in: query
          description: 'Purchase item key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_commission_purchase:
                  type: array
                  description: 'Information about applied staff commission for purchase. Each element has next structure: <dl> <dt>string `k_staff_pay`</dt> <dd>Staff pay key, primary key in {@link \RsStaffPaySql} table.</dd> <dt>string `text_staff_family`</dt> <dd>Staff surname.</dd> <dt>string `text_staff_name`</dt> <dd>Staff name.</dd> <dt>string `text_pay_title`</dt> <dd>Pay rate title.</dd> <dt>string `uid_staff`</dt> <dd>Staff uid key, primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                a_future_payments:
                  type: array
                  description: 'Settings of applied commission for future auto-payments: <dl> <dt>int|null `i_future_payments`</dt> <dd>Limit of future auto-payments to apply commission. `null`  if not limit.</dd> <dt>int|null `i_future_payments_left`</dt> <dd>Count of left of future auto-payments to apply commission. `null`  if not limit.</dd> <dt>bool `is_future_payments`</dt> <dd>`true` if commission is applied for future auto-payments, `false` otherwise.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Staff-PurchaseStaffCommission
  '/Wl/Purchase/Receipt/PurchaseReceipt.json':
    get:
      summary: 'Gets purchase information. Returns full receipt data for the specified purchase, including business details, customer information, itemized purchase items, payment methods, pricing summary, and print URLs.'
      parameters:
        - name: is_url_public
          in: query
          description: 'Whether {@link PurchaseReceiptApi::$url_print} and {@link PurchaseReceiptApi::$url_print_receipt} require authentication. If `true`, the URL contains a token that allows temporary access to the page without logging in. If ''false'', the page requires authentication.'
          schema:
            type: boolean
        - name: k_purchase
          in: query
          description: 'The key of the purchase.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_account_rest:
                        type: array
                        description: 'Information about the account balance for a user''s account after payment for the purchase. Every element has the following keys: <dl> <dt>string `m_amount`</dt> <dd>The account balance on the user''s account.</dd> <dt>string `text_currency`</dt> <dd>The currency of the amount.</dd> <dt>string `text_method`</dt> <dd>The payment method title.</dd> </dl>'
                      a_business:
                        type: array
                        description: 'Information about the business. <dl> <dt>array `a_logo`</dt> <dd> The business logo. <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if the image is empty. Otherwise, this will be `false`.</dd> <dt>string `text_url`</dt> <dd>The URL of the image.</dd> </dl> </dd> <dt>string `text_address`</dt> <dd>The business''s address.</dd> <dt>string `text_mail`</dt> <dd>The business''s email address.</dd> <dt>string `text_phone`</dt> <dd>The business''s phone number.</dd> <dt>string `text_title`</dt> <dd>The business name.</dd> </dl>'
                      a_card:
                        type: array
                        description: 'Payment transaction information. Every element has the following keys: <dl> <dt>string `text_card_number`</dt> <dd>The card or account number.</dd> <dt>string `text_card_system`</dt> <dd>The card system name from {@link \ACardSystemSid}.</dd> <dt>string `text_title`</dt> <dd>The payment account title. This will be ''Account number'' for ACH, ''Card'' for all other cases.</dd> </dl>'
                      a_customer:
                        type: array
                        description: 'Information about the customer. <dl> <dt>string `text_address`</dt> <dd>The customer''s address.</dd> <dt>string `text_mail`</dt> <dd>The customer''s email address.</dd> <dt>string `text_name`</dt> <dd>The customer''s name.</dd> <dt>string `text_phone`</dt> <dd>The customer''s phone number.</dd> </dl>'
                      a_pay_method:
                        type: array
                        description: 'A list of payment methods for the current purchase. Every element has the following keys: <dl> <dt>string `m_amount`</dt> <dd>The amount of the payment.</dd> <dt>string `text_currency`</dt> <dd>The currency of the amount.</dd> <dt>string `text_pay_method`</dt> <dd>The payment method title.</dd> </dl>'
                      a_price:
                        type: array
                        description: 'Complete information about price information for the purchase. <dl> <dt>string `m_discount`</dt> <dd>The discount amount.</dd> <dt>string `m_discount_code`</dt> <dd>The discount amount for a discount code.</dd> <dt>string `m_sum`</dt> <dd>The subtotal amount (the sum of the prices).</dd> <dt>string `m_tax`</dt> <dd>The amount of taxes.</dd> <dt>string `m_tip`</dt> <dd>The amount of tips.</dd> <dt>string `m_total`</dt> <dd>The total amount.</dd> <dt>string `text_currency`</dt> <dd>The currency of the purchase.</dd> <dt>string `text_discount_code`</dt> <dd>The discount code applied to the purchase.</dd> </dl>'
                      a_purchase_item:
                        type: array
                        description: 'A list of purchase items. Every element has the following keys: <dl> <dt>array `a_logo`</dt> <dd> The purchase item logo. <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>bool `is_empty`</dt> <dd>Determines whether the image is empty.</dd> <dt>string `text_url`</dt> <dd>he URL of the image.</dd> </dl> </dd> <dt>string[] `a_visit`</dt> <dd> List of visit keys. Only available if the purchase item is a class period, enrollment, enrollment deposit, or enrollment discount. Each element is a string visit key. <i>Primary keys in {@link \RsVisitSql} table.</i> </dd> <dt>int `i_count`</dt> <dd>The number of items in the purchase.</dd> <dt>int|null `id_program`</dt> <dd> Program ID. One of {@link \RsProgramSid} constants. Only applies to promotions. `null` if the purchase item is not a promotion. </dd> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>int `id_sale`</dt> <dd>Sale ID. One of {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The identifier of the item.</dd> <dt>string `k_purchase_item`</dt> <dd>The key of the purchase item.</dd> <dt>string `k_shop_product`</dt> <dd> Product key. Set only if `id_purchase_item`is {@link \RsPurchaseItemSid::PRODUCT}. <i>Primary key in {@link \RsShopProductSql}.</i> </dd> <dt>string `m_price`</dt> <dd>The price of the single item.</dd> <dt>string `m_price_total`</dt> <dd>The price of all items.</dd> <dt>string `text_category`</dt> <dd>The type of the item (for example, a pass or membership).</dd> <dt>string `text_currency`</dt> <dd>The currency of the price.</dd> <dt>string `text_info`</dt> <dd>Additional information about the purchase item.</dd> <dt>string `text_title`</dt> <dd>The name of the purchase item.</dd> <dt>string `text_description`</dt> <dd>The description of the purchase item.</dd> <dt>string `url_print`</dt> <dd>The URL to print. Only available if the purchase item is a coupon (gift card).</dd> </dl>'
                      dtl_purchase:
                        type: string
                        description: 'The local date of the purchase in MySQL format.'
                      has_signature:
                        type: boolean
                        description: 'Determines whether the payment contained a signature.'
                      html_receipt:
                        type: string
                        description: 'HTML representation of the purchase receipt.'
                      text_purchase_id:
                        type: string
                        description: 'The normalized purchase ID.'
                      text_receipt:
                        type: string
                        description: 'The receipt text set in the store settings.'
                      url_print:
                        type: string
                        description: 'The URL for printing on a normal printer.'
                      url_print_receipt:
                        type: string
                        description: 'The URL for printing on a receipt printer.'
      tags:
        - Receipt-PurchaseReceipt
  '/Wl/Purchase/Mail/PurchaseMail.json':
    post:
      summary: 'Sends mail message with list of purchased items for specified purchase. Validates the caller''s access to the purchase and sends a receipt email to the appropriate recipient (staff member or client), recording the action in the mail history log.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_send_force:
                  type: boolean
                  description: 'Determines whether mail sending should be forced.'
                k_purchase:
                  type: string
                  description: 'The key of the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mail-PurchaseMail
  '/Wl/Purchase/Share/PurchaseShare.json':
    post:
      summary: 'Records the purchase share action and returns the social network sharing URL. Validates that the caller owns the purchase, prevents duplicate share actions, saves the share record, and returns the social network URL for sharing.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_social:
                  type: number
                  format: int32
                  description: 'The social network. One of the {@link \ASocialSid} constants.'
                k_purchase:
                  type: string
                  description: 'The key of the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_share:
                        type: string
                        description: 'The URL to the sharing page.'
      tags:
        - Share-PurchaseShare
  '/Wl/Report/Background/GenerateReport.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_report_accumulation
          in: query
          description: 'Key of the report file generation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Background-GenerateReport
    get:
      summary: 'Gets status of the report accumulation.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_report_accumulation
          in: query
          description: 'Key of the report file generation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_status:
                        type: number
                        format: int32
                        description: 'Status of the report background generation. One of {@link \Wl\Report\Accumulation\ReportAccumulationStatusSid} constants. <tt>0</tt> in case of report title updating.'
      tags:
        - Background-GenerateReport
    post:
      summary: 'Marks the report as needed for display in Generated reports.'
      parameters:
        - name: is_need_show
          in: query
          description: 'Whether the report should be displayed in Generated reports.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_report_accumulation
          in: query
          description: 'Key of the report file generation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Background-GenerateReport
    put:
      summary: ''
      parameters:
        - name: id_status
          in: query
          description: 'Status of the report background generation. One of {@link \Wl\Report\Accumulation\ReportAccumulationStatusSid} constants. <tt>0</tt> in case of report title updating.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_report_accumulation
          in: query
          description: 'Key of the report file generation.'
          schema:
            type: string
        - name: text_title
          in: query
          description: 'Title of generated report.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Background-GenerateReport
  '/Wl/Report/Customization/CustomizationForm.json':
    get:
      summary: 'Loads customization data of the customization form that corresponds to specified report / report page.'
      parameters:
        - name: cid_page
          in: query
          description: 'Report page CID.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'Page CID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Primary key of a saved report in {@link \RsReportSaveSql} table. <tt>null</tt> means that report is not saved.'
          schema:
            type: [string, "null"]
        - name: s_report
          in: query
          description: 'Report CID list to that page customization form must be converted. String separated with <tt>,</tt>. May be specified only if {@link CustomizationFormApi::$cid_page} in not empty.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Current user''s primary key in {@link \Core\Passport\Login\PassportLoginAr} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_customization_form:
                        type: array
                        description: 'Customization form data. See {@link CustomizationFormAbstract::toArray()} for details.'
      tags:
        - Customization-CustomizationForm
    post:
      summary: 'Saves given data of a customization form into database.'
      parameters:
        - name: cid_page
          in: query
          description: 'Report page CID.'
          schema:
            type: number
            format: int32
        - name: cid_report
          in: query
          description: 'Page CID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Primary key of a saved report in {@link \RsReportSaveSql} table. <tt>null</tt> means that report is not saved.'
          schema:
            type: [string, "null"]
        - name: uid_actor
          in: query
          description: 'Current user''s primary key in {@link \Core\Passport\Login\PassportLoginAr} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_customization_form:
                  type: array
                  description: 'Customization form data. See {@link CustomizationFormAbstract::toArray()} for details.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Customization-CustomizationForm
  '/Wl/Report/Dashboard/ReportDashboard.json':
    post:
      summary: 'Adds new report to a dashboard.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: array
                  description: 'Filter raw data of the report. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: array
                  description: 'View widget settings raw data of the saved report.'
                cid_controller:
                  type: [number, object]
                  description: 'CID of the controller class. Subclass of {@link ReportViewControllerAbstract}.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@link ReportDashboardApi::$a_filter}. In this array, key is name of a filter field. A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array.'
                k_report_dashboard:
                  type: string
                  description: 'Report dashboard key to which report should be added. Primary key in the {@link \RsReportDashboardSql} table.'
                text_description:
                  type: string
                  description: 'Description of the report.'
                text_title:
                  type: string
                  description: 'Title of the report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dashboard-ReportDashboard
    put:
      summary: 'Updates added report to a dashboard.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: array
                  description: 'Filter raw data of the report. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: array
                  description: 'View widget settings raw data of the saved report.'
                cid_controller:
                  type: [number, object]
                  description: 'CID of the controller class. Subclass of {@link ReportViewControllerAbstract}.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@link ReportDashboardApi::$a_filter}. In this array, key is name of a filter field. A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array.'
                k_report_dashboard:
                  type: string
                  description: 'Report dashboard key to which report should be added. Primary key in the {@link \RsReportDashboardSql} table.'
                text_description:
                  type: string
                  description: 'Description of the report.'
                text_title:
                  type: string
                  description: 'Title of the report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dashboard-ReportDashboard
  '/Wl/Report/Dashboard/Dashboard.json':
    get:
      summary: 'Loads dashboard data.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard
          in: query
          description: 'Report dashboard key to which report should be added. Primary key in the {@link \RsReportDashboardSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_dashboard_data:
                        type: array
                        description: 'Dashboard data.'
      tags:
        - Dashboard-Dashboard
  '/Wl/Report/Mail/SmsSummary.json':
    get:
      summary: 'Calculates available amount of messages.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_available:
                        type: number
                        format: int32
                        description: 'Available amount of messages.'
                      i_quantity:
                        type: number
                        format: int32
                        description: 'The monthly messages the business has sent to date.'
      tags:
        - Mail-SmsSummary
  '/Wl/Report/Mail/PostcardSummary.json':
    get:
      summary: 'Calculates summary information about mail campaign.'
      parameters:
        - name: k_business
          in: query
          description: 'Business in which clients must be searched. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'ID of the element with the list of receivers. See {@link \RsReportMail::list_set()}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_quantity:
                        type: number
                        format: int32
                        description: 'Quantity of recipients.'
                      k_currency:
                        type: string
                        description: 'Current currency of the business.'
                      m_cost:
                        type: string
                        description: 'Total cost of postcards.'
      tags:
        - Mail-PostcardSummary
  '/Wl/Report/Favorite/ReportFavorite.json':
    delete:
      summary: 'Removes report controller from favorites. See {@link \RsReportFavorite::check()}.'
      parameters:
        - name: cid_controller
          in: query
          description: 'CID of the controller. Subclass of the {@link ReportViewControllerAbstract}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which request is performed. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Saved report key to manage.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Favorite-ReportFavorite
    get:
      summary: 'Returns information whether passed report controller (saved report controller) is favorite for the specified user within specified business. See {@link \RsReportFavorite::check()}.'
      parameters:
        - name: cid_controller
          in: query
          description: 'CID of the controller. Subclass of the {@link ReportViewControllerAbstract}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which request is performed. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Saved report key to manage.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_favorite:
                        type: boolean
                        description: 'Whether report is favorite.'
      tags:
        - Favorite-ReportFavorite
    post:
      summary: 'Adds report controller to favorites. See {@link \RsReportFavorite::check()}.'
      parameters:
        - name: cid_controller
          in: query
          description: 'CID of the controller. Subclass of the {@link ReportViewControllerAbstract}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which request is performed. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Saved report key to manage.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Favorite-ReportFavorite
  '/Wl/Report/Save/ReportSave.json':
    get:
      summary: 'Returns information about saved report.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the saved report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_filter:
                        type: array
                        description: 'Filter raw data of the saved report. This variable do not get empty values of filters from HTML forms.'
                      id_report_category:
                        type: number
                        format: int32
                        description: 'Report category of the saved report. One of the {@link \RsReportCategorySid} constants.'
                      text_description:
                        type: string
                        description: 'Description of the saved report.'
                      text_title:
                        type: string
                        description: 'Title of the saved report.'
      tags:
        - Save-ReportSave
    post:
      summary: 'Saves new saved report.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the saved report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: array
                  description: 'Filter raw data of the saved report. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: array
                  description: 'View widget settings raw data of the saved report.'
                cid_controller:
                  type: [number, object]
                  description: 'CID of the controller class. Subclass of {@link ReportViewControllerAbstract}.'
                id_report_category:
                  type: number
                  format: int32
                  description: 'Report category of the saved report. One of the {@link \RsReportCategorySid} constants.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@lind \Wl\Report\Save\ReportSaveApi::$a_filter}. In this array, key is name of a filter field. A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array.'
                k_report_config:
                  type: string
                  description: 'Report save key. Primary key in the {@link \RsReportSaveSql} table.'
                text_description:
                  type: string
                  description: 'Description of the saved report.'
                text_title:
                  type: string
                  description: 'Title of the saved report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Save-ReportSave
    put:
      summary: 'Updates existing saved report.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key of the saved report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: array
                  description: 'Filter raw data of the saved report. This variable do not get empty values of filters from HTML forms.'
                a_settings:
                  type: array
                  description: 'View widget settings raw data of the saved report.'
                cid_controller:
                  type: [number, object]
                  description: 'CID of the controller class. Subclass of {@link ReportViewControllerAbstract}.'
                id_report_category:
                  type: number
                  format: int32
                  description: 'Report category of the saved report. One of the {@link \RsReportCategorySid} constants.'
                json_filter:
                  type: string
                  description: 'Filter raw data of the saved report. Uses to save empty filter values, actual when empty filter is the same as all values are chosen. Use this value instead of {@lind \Wl\Report\Save\ReportSaveApi::$a_filter}. In this array, key is name of a filter field. A filter field is a property of a {@link \Wl\Report\Generator\ReportFilterAbstract} subclass. Name of the property of that subclass corresponds key in this array.'
                text_description:
                  type: string
                  description: 'Description of the saved report.'
                text_title:
                  type: string
                  description: 'Title of the saved report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Save-ReportSave
  '/Wl/Report/Save/ReportDuplicate.json':
    post:
      summary: 'Duplicates the given saved report with new parameters: - filters; - sorting order.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which duplicate operation is performed. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key that should be duplicated. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_filter:
                  type: array
                  description: 'Filter raw data of the duplicated report.'
                a_settings:
                  type: array
                  description: 'View widget settings raw data of the saved report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_report_save:
                        type: string
                        description: 'Report save key that should be duplicated. Primary key in the {@link \RsReportSaveSql} table.'
      tags:
        - Save-ReportDuplicate
  '/Wl/Resource/Layout/Layout.json':
    get:
      summary: 'Returns information about the specified asset layout, including assets and custom shapes. Returns the full layout configuration including the asset list with positions and images, custom shapes with coordinates and colors, and display settings such as grid dimensions and number visibility.'
      parameters:
        - name: k_resource_layout
          in: query
          description: 'The key of the layout.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource:
                        type: array
                        description: 'The list of assets. Every element contains the following keys: <dl> <dt>array `a_image`</dt> <dd> The asset''s appearance information. <i>See {@link \RsResourceImage::data()} for details.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See more {@link \Core\Drive\DriveImage::image()}.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Is set only if <var>sid_image</var> equals to <tt>shape</tt>.</dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `sid_image`</dt> <dd>Image kind. String representation of one of {@link \Wl\Resource\Image\ImageSid} constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name.String representation of one of {@link \Wl\Resource\Image\ImageIconSid} constants. Is set only if <var>sid_image</var> equals to <tt>image</tt>. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. String representation of one of {@link \Wl\Resource\Image\ImageShapeSid} constants. Is set only if <var>sid_image</var> equals to <tt>shape</tt>. </dd> <dt>string `url`</dt> <dd>Path to image. <i>See {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> </dl> </dd> <dt>int `i_cell_x`</dt> <dd>The vertical cell number. Not empty if assets are snapped to grid.</dd> <dt>int `i_cell_y`</dt> <dd>The horizontal cell number. Not empty if assets are snapped to grid.</dd> <dt>int `i_left`</dt> <dd>The horizontal offset in pixels. Not empty if assets aren''t snapped to grid.</dd> <dt>int `i_top`</dt> <dd>The vertical offset in pixels. Not empty if assets aren''t snapped to grid.</dd> <dt>int `i_index`</dt> <dd>Asset number.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `s_name`</dt> <dd>Asset name.</dd> <dt>string `s_title`</dt> <dd>The asset title.</dd> </dl>'
                      a_shape_custom:
                        type: array
                        description: 'A list of custom shapes. Every element is an array with the following keys: <dl> <dt>float `f_height`</dt> <dd>The height for the shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::RECTANGLE}. Empty for other shapes.</dd> <dt>float `f_width`</dt> <dd>The width for the shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::RECTANGLE}. Empty for other shapes.</dd> <dt>int `i_degree_from`</dt> <dd>The start angle for the shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::PIE}. Empty for other shapes.</dd> <dt>int `i_degree_to`</dt> <dd>The start angle for shape {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::PIE}. Empty for other shapes.</dd> <dt>int `i_left`</dt> <dd>The position of the shape by horizontal axis.</dd> <dt>int `i_radius`</dt> <dd> The radius for shapes {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::PIE} and {@link \Wl\Resource\Layout\Shape\LayoutShapeSid::CIRCLE}. Empty for other shapes. </dd> <dt>int `i_top`</dt> <dd>The position of the shape by vertical axis.</dd> <dt>int `id_resource_layout_shape`</dt> <dd>The shape type ID. One of the {@link \Wl\Resource\Layout\Shape\LayoutShapeSid} constants.</dd> <dt>string `k_resource_layout_shape`</dt> <dd>The shape key. <i>Primary key in {@link \Wl\Resource\Layout\Shape\Sql} table.</i></dd> <dt>string `s_color_background`</dt> <dd>The shape''s background color.</dd> <dt>string `s_color_foreground`</dt> <dd>The shape''s foreground color.</dd> <dt>string `s_text`</dt> <dd>The shape''s title.</dd> </dl>'
                      a_shape_icon:
                        type: array
                        description: 'A list of shapes and icons. Every element is an array with the following keys: <dl> <dt>int `i_cell_x`</dt> <dd>The cell number, positioned by the horizontal axis. Empty if grid is turned off.</dd> <dt>int `i_cell_y`</dt> <dd>The cell number, positioned by the vertical axis. Empty if grid is turned off.</dd> <dt>int `i_left`</dt> <dd>The horizontal position in pixels. Empty if grid is turned on.</dd> <dt>int `i_top`</dt> <dd>The vertical position in pixels. Empty if grid is turned on.</dd> <dt>int `id_shape_icon`</dt> <dd>The icon ID. One of the {@link \Wl\Resource\Image\ImageIconSid} constants.</dd> </dl>'
                      i_grid:
                        type: number
                        format: int32
                        description: 'The grid size.'
                      is_grid:
                        type: boolean
                        description: 'This will be `true` if snap to grid is enabled. Otherwise, this will be `false`.'
                      k_resource_type:
                        type: string
                        description: 'The key of the asset category.'
                      s_color_active:
                        type: string
                        description: 'The color for active assets. Hex encoding with prefix `#`.'
                      show_name:
                        type: boolean
                        description: 'This will be `true` if asset names are displayed. Otherwise, this will be `false`.'
                      show_number:
                        type: boolean
                        description: 'This will be `true` if asset numbers are displayed. Otherwise, this will be `false`.'
      tags:
        - Layout-Layout
  '/Wl/Resource/Change/Change.json':
    put:
      summary: 'Changes an asset occupied by specified visit.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_index:
                  type: number
                  format: int32
                  description: 'Index of asset to be set.'
                k_resource:
                  type: string
                  description: 'Primary key in table {@link \RsResourceSql} of asset to be set.'
                k_visit:
                  type: string
                  description: 'Primary key in table {@link \RsVisitSql} of visit.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Change-Change
  '/Wl/Resource/ResourceList/List.json':
    get:
      summary: 'Returns assets list in the business. Returns all assets if <tt>$id_category</tt> is not specified or only certain category assets. Includes main information about assets.'
      parameters:
        - name: id_category
          in: query
          description: 'Type of the resource. 1 - if this is a bookable asset. 2 - if this is an off-site location with its own address.'
          schema:
            type: number
            format: int32
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created resources (if business is franchisor). <tt>true</tt> to include franchisee-created resources.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource:
                        type: array
                        description: 'Resources list: <dl> <dt>bool `hide_application`</dt> <dd> Whether resource will be hidden in the White Label mobile application. <tt>true</tt> means that resource will not be displayed, <tt>false</tt> otherwise. </dd> <dt>bool `is_active`</dt> <dd>Whether resource is active.</dd> <dt>bool `is_book`</dt> <dd>Whether resource is bookable.</dd> <dt>string `k_city`</dt> <dd>Key of the city for off-site locations.}. <tt>null</tt> if the resource is not an off-site location.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `k_resource_layout`</dt> <dd>Asset layout key.</dd> <dt>string `k_resource_type`</dt> <dd>Resource type key.</dd> <dt>string `text_address`</dt> <dd> Street address of the resource for off-site locations. Empty string if the resource does not have an off-site address. </dd> <dt>string `text_guide`</dt> <dd>Additional directions or access tips for reaching an off-site location. Empty string if not specified.</dd> <dt>string `text_postal`</dt> <dd> Postal code of the resource for off-site locations. Empty string if the resource does not have an off-site address. </dd> <dt>string `text_title`</dt> <dd>Resource name.</dd> </dl>'
      tags:
        - ResourceList-List
  '/Wl/Resource/Type/ResourceTypeList.json':
    get:
      summary: 'Returns list of resource types in the business.'
      parameters:
        - name: id_category
          in: query
          description: 'Type of the resource. 1 - if this is a bookable asset. 2 - if this is an off-site location with its own address.'
          schema:
            type: number
            format: int32
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created resource types (if business is franchisor). <tt>true</tt> to include franchisee-created resource types.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key, to load resource types for, primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_type:
                        type: array
                        description: 'Resource type list: <dl> <dt>int `id_category`</dt> <dd> Type of the resource. 1 - if this is a bookable asset. 2 - if this is an off-site location with its own address. </dd> <dt>bool `is_active`</dt> <dd>Whether resource type is active.</dd> <dt>string `k_resource_type`</dt> <dd>Resource type key, primary key in {@link \RsResourceTypeSql}.</dd> <dt>string `text_title`</dt> <dd>Resource type title.</dd> </dl>'
      tags:
        - Type-ResourceTypeList
  '/Wl/Resource/Select/Resource.json':
    get:
      summary: 'Returns resources list in the business.'
      parameters:
        - name: i_duration
          in: query
          description: 'Duration of the service in minutes. <tt>null</tt> if duration not set.'
          schema:
            type: [number, "null"]
        - name: id_purchase_item
          in: query
          description: 'ID of the purchase item. One of {@link \RsPurchaseItemSid} constants. <tt>null</tt> if purchase item id not set.'
          schema:
            type: number
            format: int32
        - name: is_book
          in: query
          description: 'If return only bookable resources. If <tt>false</tt> returns all resources.'
          schema:
            type: boolean
        - name: is_period_only
          in: query
          description: 'If need period only data.'
          schema:
            type: boolean
        - name: is_resource_period
          in: query
          description: 'Is resource period data needed. <tt>true</tt> if need resource period data. <tt>false</tt> if this data not need.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key, primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show resource. Primary key in {@link \RsLocationSql} table.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'Asset primary key in {@link \RsResourceSql} table.'
          schema:
            type: [string, "null"]
        - name: not_duration_empty
          in: query
          description: 'Whether duration is empty. <tt>true</tt> if duration not empty. <tt>false</tt> if duration empty.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource:
                        type: array
                        description: 'Resources list: <dl> <dt>bool `is_active`</dt> <dd>Whether resource is active.</dd> <dt>string `k_resource`</dt> <dd>Resource identifier, primary key in {@link \RsResourceSql}.</dd> <dt>string `text_title`</dt> <dd>Resource name.</dd> </dl>'
                      a_resource_period:
                        type: array
                        description: 'Resources period data list: <dl> <dt>string `html_title`</dt> <dd>Title of resources period.</dd> <dt>int `i_duration`</dt> <dd>Duration of asset.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if asset was selected. <tt>false</tt> in other cases.</dd> <dt>bool `is_visible`</dt> <dd><tt>true</tt> if asset are visible. <tt>false</tt> in other cases.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Primary key in {@link \RsResourceSql} table.</dd> <dt>string `k_resource_type`</dt> <dd>Key of asset category. Primary key in {@link \RsResourceTypeSql} table.</dd> </dl>'
                      html_period_one:
                        type: string
                        description: 'Resources period one template.'
      tags:
        - Select-Resource
  '/Wl/Resource/Promotion/ResourcePromotion.json':
    get:
      summary: 'Returns list of promotions that can be used to pay for the resource.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'Resource key. Primary key in {@link \RsResourceSql} table. The resource with which you want to connect the promotions. <tt>null</tt> or <tt>0</tt> if you need to return a list of promotions for an uncreated resource.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data with structure:. <dl> <dt>string `k_promotion`</dt> <dd>Promotion key. Primary key in {@link \RsPromotionSql} table.</dd> <dt>string `text_title`</dt> <dd>Title of promotion.</dd> <dt>int `id_program`</dt> <dd>One of {@link \RsProgramSid} constants.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if the promotion is related to the resource, <tt>false</tt> otherwise.</dd> </dl>'
                      k_promotion_default:
                        type: [string, "null"]
                        description: 'Default promotion key. Primary key in {@link \RsPromotionSql} table. `null` if `k_resource` was not provided, or if the resource has no default promotion.'
      tags:
        - Promotion-ResourcePromotion
  '/Wl/Appointment/Change/Duration.json':
    post:
      summary: 'Change appointment duration.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_mail_form_client:
                  type: array
                  description: 'Array with state of client mail form.'
                a_mail_form_staff:
                  type: array
                  description: 'Array with state of staff mail form.'
                i_duration_delta:
                  type: number
                  format: int32
                  description: 'Duration change value.'
                is_notify_client:
                  type: boolean
                  description: '<tt>true</tt> if it is required to send notification about appointment change to client, <tt>false</tt> otherwise.'
                is_notify_staff:
                  type: boolean
                  description: '<tt>true</tt> if it is required to send notification about appointment change to staff, <tt>false</tt> otherwise.'
                k_appointment:
                  type: string
                  description: 'Appointment key. Empty value means not set.'
                s_form_id:
                  type: string
                  description: 'ID form.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_reload:
                        type: boolean
                        description: 'Reload page.'
                      is_skip:
                        type: boolean
                        description: 'To do nothing, skip all changes.'
                      s_time_end:
                        type: string
                        description: 'New appointment end time'
      tags:
        - Change-Duration
  '/Wl/Appointment/Change/DurationConflict.json':
    get:
      summary: 'Gets booking conflicts.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: i_duration_new
          in: query
          description: 'Appointment duration. Must be specified for asset booking only.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Appointment primary key in {@link \RsAppointmentSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_conflict:
                        type: array
                        description: 'See for {@link \RsAppointmentEditConflict::checkAvailability()} details.'
                      can_book_anyway:
                        type: boolean
                        description: '<tt>true</tt> if staff can book appointment when conflict. <tt>false</tt> in other cases.'
                      id_staff_book_flow:
                        type: number
                        format: int32
                        description: 'Staff book flow id, one of {@link ServiceStaffBookFlowSid}.'
                      k_business:
                        type: string
                        description: 'Key of the business. Primary key from {@link \RsBusinessSql} table.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      k_service:
                        type: string
                        description: 'Service primary key in {@link \RsServiceSql} table.'
                      s_form_id:
                        type: string
                        description: 'ID form.'
                      uid_staff:
                        type: string
                        description: 'Selected user staff key.'
      tags:
        - Change-DurationConflict
  '/Wl/Appointment/Edit/AddonUpdate.json':
    get:
      summary: 'Return data about appointment''s add-ons. Returns the current list of add-ons attached to the specified appointment along with the full catalog of available add-ons for the service. The caller must have view access to the appointment.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to get add-ons for. Not empty only when getting add-ons for new appointment ({@link AddonUpdateApi::$k_appointment} is null). User key ''-1'' means walk-in, user key ''0'' means new user (user will be created together with appointment).'
          schema:
            type: array
        - name: k_appointment
          in: query
          description: 'The appointment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key. This will be an empty string if not set yet.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Not empty only when getting add-ons for new appointment ({@link AddonUpdateApi::$k_appointment} is null).'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service key. Not empty only when getting add-ons for new appointment ({@link AddonUpdateApi::$k_appointment} is null).'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_addon_data:
                        type: array
                        description: 'Data to show appointment add-ons: <dl> <dt>array[] `a_addon`</dt> <dd> Data about appointment add-ons. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>array[] `a_addon_buy`</dt> <dd> Add-ons available for purchase: zero quantity, zero usage quantity, and zero banked quantity. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>array[] `a_addon_own`</dt> <dd> Add-ons already owned but not selected: zero quantity, zero usage quantity, non-zero banked quantity. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>array[] `a_addon_select`</dt> <dd> Add-ons selected for this appointment: non-zero quantity or non-zero usage quantity. <i>For details see {@link \Wl\Appointment\Edit\AddonListView::addonInfo()}.</i> <dl> <dt>string `html_amount`</dt> <dd>Formatted HTML price of the addon.</dd> <dt>string `html_duration`</dt> <dd>HTML-escaped duration text.</dd> <dt>string `html_title`</dt> <dd>HTML-escaped addon title.</dd> <dt>int `i_count_banked`</dt> <dd>Pre-purchased units the client has; at least 0.</dd> <dt>int `i_count_paid`</dt> <dd>Paid units in the current appointment.</dd> <dt>int `i_count_unpaid`</dt> <dd>Unpaid units in the current appointment.</dd> <dt>int `i_count_use`</dt> <dd>Used units in the current appointment.</dd> <dt>int `i_product`</dt> <dd>Total product count for this appointment: `i_count_paid` plus `i_count_unpaid`.</dd> <dt>bool `is_disable`</dt> <dd>`true` when the addon has no products or is not active.</dd> </dl> </dd> <dt>bool `is_addon_banking`</dt> <dd>Whether at least one of appointment add-ons is bankable.</dd> <dt>bool `is_all_addon_selected`</dt> <dd>Whether all appointment add-ons have non-zero quantity or non-zero usage quantity.</dd> <dt>bool `is_search`</dt> <dd>Determines whether the add-on search field needs to be shown.</dd> </dl>'
      tags:
        - Edit-AddonUpdate
    put:
      summary: 'Replaces the add-ons for the appointment with the provided list, optionally updating the appointment duration. Removes all existing add-ons from the appointment and attaches the provided set in their place. When add-ons with a duration are included, the appointment end time is recalculated accordingly. The operation runs inside a database transaction to ensure consistency.'
      parameters:
        - name: k_appointment
          in: query
          description: 'The appointment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key. This will be an empty string if not set yet.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_addon:
                  type: [array]
                  description: 'The appointment addon-ons. Old format - an array where each value is key of the add-on. New format - each element is an array: <dl> <dt>int `i_product`</dt> <dd>The add-on count. Max value is 255.</dd> <dt>int `i_use`</dt> <dd>The add-on use count. Max value is 255. Not set means same value as add-on count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The add-on key. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl>'
                is_duration_update:
                  type: boolean
                  description: 'Determines whether the appointment duration needs to be updated. <i>When called from {@link rs_appointment_edit_save_change_trx()} duration already updated.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-AddonUpdate
  '/Wl/Appointment/View/ViewStore.json':
    get:
      summary: 'Loads information about amounts that must be paid for an appointment.'
      parameters:
        - name: k_appointment
          in: query
          description: 'Appointment primary key in {@link \RsAppointmentSql} table.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_item:
                        type: array
                        description: 'List of amounts that must be paid. Each element contains keys: <dl> <dt>string `m_price`</dt> <dd>Item price.</dd> <dt>string `text_title`</dt> <dd>Item title.</dd> </dl>'
      tags:
        - View-ViewStore
  '/Wl/Appointment/View/LinkJoin.json':
    post:
      summary: 'Changes url join for virtual appointment.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_appointment:
                  type: string
                  description: 'Key of non-integrated virtual appointment. Primary key from {@link \RsAppointmentSql}.'
                k_business:
                  type: string
                  description: 'Key of business for which appointment belongs. Primary key from {@link \RsBusinessSql}.'
                url_join:
                  type: string
                  description: 'Join link for non-integrated virtual appointment.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - View-LinkJoin
  '/Wl/Appointment/Cancel/Confirm.json':
    get:
      summary: 'Returns data for appointment cancellation confirm.'
      parameters:
        - name: dl_end
          in: query
          description: 'End date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Start date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
          schema:
            type: [string, "null"]
        - name: id_visit
          in: query
          description: 'Cancelling status. One of {@link VisitSid} constants. Zero when status is choosing on form (opening form from schedule list view).'
          schema:
            type: number
            format: int32
        - name: is_appointment_specific
          in: query
          description: 'Is it recurring appointment cancellation for specific appointments. `true` - for specific appointments; `false` - appointments going forward.'
          schema:
            type: boolean
        - name: is_recurring
          in: query
          description: 'Is it recurring appointment cancellation. `false` in case of cancellation of single appointment.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Key of the cancelling appointment. Primary key in {@link \RsAppointmentSql} table.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Key of the visit to cancel. Primary key in {@link \RsVisitSql} table. Empty when visit key not known (opening form from schedule list view).'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_list:
                        type: [array, "null"]
                        description: 'Information about the appointment. `null` in case of cancellation of single appointment.'
                      dl_end:
                        type: [string, "null"]
                        description: 'End date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
                      dl_start:
                        type: [string, "null"]
                        description: 'Start date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
                      dtl_max:
                        type: [string, "null"]
                        description: 'Max date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
                      dtl_min:
                        type: [string, "null"]
                        description: 'Min date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
                      html_fee_amount:
                        type: [string, "null"]
                        description: 'Amount to be charged for the user. `null` if specified visit is not the blame, according business late cancel and no shows rules.'
                      i_appointment_selected:
                        type: [number, "null"]
                        description: 'Count of selected appointments during recurring appointments cancellation. `null` in case of cancellation of single appointment.'
                      id_charge:
                        type: number
                        format: int32
                        description: 'Type of the blame, one of {@link \Wl\Business\Policy\ChargeSid} constants. `0` means blame is not configured.'
                      is_inform_mail:
                        type: boolean
                        description: 'Is mail notification should be sent by default.'
                      is_inform_push:
                        type: boolean
                        description: 'Is push notification should be sent by default.'
                      is_inform_sms:
                        type: boolean
                        description: 'Is SMS notification should be sent by default.'
                      k_location:
                        type: string
                        description: 'Location key of cancelling appointment.'
                      k_service:
                        type: [string, "null"]
                        description: 'Key of the service of appointment. Primary key in {@link \RsServiceSql} table. ''null'' when cancelling asset booking.'
                      k_visit:
                        type: string
                        description: 'Key of the visit to cancel. Primary key in {@link \RsVisitSql} table. Empty when visit key not known (opening form from schedule list view).'
                      m_fee_amount:
                        type: [string, "null"]
                        description: 'Amount to be charged for the user. `null` if specified visit is not the blame, according business late cancel and no shows rules.'
                      sid_mail:
                        type: string
                        description: 'Notification to be sent. One of {@link \RsMailSid} constants.'
                      text_client_name:
                        type: string
                        description: 'Name of client which appointment is cancelling.'
                      text_status:
                        type: string
                        description: 'Cancelling status name.'
                      text_title:
                        type: [string, "null"]
                        description: 'Appointment title. `null` in case of cancellation of single appointment.'
      tags:
        - Cancel-Confirm
    post:
      summary: 'Cancels appointment.'
      parameters:
        - name: a_appointment_list
          in: query
          description: 'Information about the appointment. `null` in case of cancellation of single appointment.'
          schema:
            type: [array, "null"]
        - name: dl_end
          in: query
          description: 'End date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Start date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
          schema:
            type: [string, "null"]
        - name: dtl_max
          in: query
          description: 'Max date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
          schema:
            type: [string, "null"]
        - name: dtl_min
          in: query
          description: 'Min date of period for appointments cancellation. `null` in case of cancellation of single appointment.'
          schema:
            type: [string, "null"]
        - name: id_visit
          in: query
          description: 'Cancelling status. One of {@link VisitSid} constants. Zero when status is choosing on form (opening form from schedule list view).'
          schema:
            type: number
            format: int32
        - name: is_appointment_specific
          in: query
          description: 'Is it recurring appointment cancellation for specific appointments. `true` - for specific appointments; `false` - appointments going forward.'
          schema:
            type: boolean
        - name: is_charge_fee
          in: query
          description: 'A staff decision to charge or not charge a penalty when a customer meets late cancel/no-show requirements.'
          schema:
            type: boolean
        - name: is_inform_mail
          in: query
          description: 'Is mail notification should be sent by default.'
          schema:
            type: boolean
        - name: is_inform_push
          in: query
          description: 'Is push notification should be sent by default.'
          schema:
            type: boolean
        - name: is_inform_sms
          in: query
          description: 'Is SMS notification should be sent by default.'
          schema:
            type: boolean
        - name: is_recurring
          in: query
          description: 'Is it recurring appointment cancellation. `false` in case of cancellation of single appointment.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Key of the cancelling appointment. Primary key in {@link \RsAppointmentSql} table.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'Key of the live mail pattern. Primary key in {@link \RsMailPatternLiveSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Key of the visit to cancel. Primary key in {@link \RsVisitSql} table. Empty when visit key not known (opening form from schedule list view).'
          schema:
            type: string
        - name: m_fee_amount
          in: query
          description: 'Amount to be charged for the user. `null` if specified visit is not the blame, according business late cancel and no shows rules.'
          schema:
            type: [string, "null"]
        - name: text_reason
          in: query
          description: 'Cancelling reason.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Cancel-Confirm
  '/Wl/Appointment/Repeat/Repeat.json':
    post:
      summary: 'Change recurring appointment booking settings.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_repeat_unpaid:
                  type: boolean
                  description: 'Whether future appointments should be booked unpaid even if client have applicable purchase option.'
                k_business:
                  type: string
                  description: 'Business key.'
                k_visit:
                  type: string
                  description: 'Visit key.'
                uid:
                  type: string
                  description: 'User key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Repeat-Repeat
  '/Wl/Appointment/Client/AppointmentEditClient.json':
    put:
      summary: 'Changes Appointment Client.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business where operation in performing.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Key of certain reservation to set client for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Client key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Client-AppointmentEditClient
  '/Wl/Appointment/Request/AppointmentRequest.json':
    post:
      summary: 'Confirms or denies the appointment.'
      parameters:
        - name: id_place
          in: query
          description: 'From where request comes. One of {@link \RsPlaceSid}.'
          schema:
            type: number
            format: int32
        - name: is_deny
          in: query
          description: 'Whether to deny the appointment. `true` to deny appointment or `false` to confirm.'
          schema:
            type: boolean
        - name: is_repeat
          in: query
          description: 'Whether all instances of recurring appointment should be denied or approved. `true` if all instances, `false` for only current appointment.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Appointment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Request-AppointmentRequest
  '/Wl/Appointment/Info/Info.json':
    get:
      summary: 'Gets information about appointment. Returns detailed information about the specified appointment, including service details, staff member, date and time in the location''s timezone, booking status, client information, and any associated assets or add-ons. Access is validated against the current user''s permissions.'
      parameters:
        - name: k_appointment
          in: query
          description: 'Class identifier to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_next:
                        type: array
                        description: 'Next appointment data, or empty array if there are no appointments in the future: <dl> <dt>string `dt_date_local`</dt> <dd>Start date and time of the next appointment in local time in MySQL format.</dd> <dt>bool `has_note`</dt> <dd>`true` if visit has a note, `false` otherwise.</dd> <dt>int `i_duration`</dt> <dd>Duration of the next appointment in minutes.</dd> <dt>bool `can_view`</dt> <dd>`true` if employees (staff) can view this appointment, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Next appointment key. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_visit`</dt> <dd>Visit key of next appointment.</dd> <dt>string `text_appointment_title`</dt> <dd>Title of next appointment.</dd> <dt>string `text_staff_name`</dt> <dd>Full staff name or empty if no staff assigned.</dd> </dl>'
                      a_previous:
                        type: array
                        description: 'Previous appointment data, or empty array if there are no appointments in the past: <dl> <dt>string `dt_date_local`</dt> <dd>Start date and time of the previous appointment in local time in MySQL format.</dd> <dt>bool `has_note`</dt> <dd>`true` if visit has a note, `false` otherwise.</dd> <dt>int `i_duration`</dt> <dd>Duration of the previous appointment in minutes.</dd> <dt>bool `can_view`</dt> <dd>`true` if employees (staff) can view this appointment, `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Previous appointment key. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_visit`</dt> <dd>Visit key of previous appointment.</dd> <dt>string `text_appointment_title`</dt> <dd>Title of previous appointment.</dd> <dt>string `text_staff_name`</dt> <dd>Full staff name or empty if no staff assigned.</dd> </dl>'
                      a_question:
                        type: array
                        description: 'List of questions and answers: <dl> <dt>int `i_size`</dt> <dd>Size of rows for answer.</dd> <dt>bool `is_multiple`</dt> <dd>`true` if `i_size` greater than 1, `false` otherwise. Can be empty if answer is loaded.</dd> <dt>string `s_answer`</dt> <dd>Answer for `s_question`.</dd> <dt>string `s_key`</dt> <dd>Answer key.</dd> <dt>string `s_question`</dt> <dd>Question.</dd> </dl>'
                      a_resource:
                        type: array
                        description: 'List of assets used by this appointment. Each element contains: <dl> <dt>int `i_color_background`</dt> <dd>Background color of the asset as an integer (RGB).</dd> <dt>int `i_color_border`</dt> <dd>Border color of the asset as an integer (RGB).</dd> <dt>int `i_index`</dt> <dd>Index of the booked asset slot.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the asset was removed from the booking, `false` otherwise.</dd> <dt>string `k_resource`</dt> <dd>Asset key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_type`</dt> <dd>Asset category key. <i>Primary key in the {@link \RsResourceTypeSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>Asset name.</dd> <dt>string `s_resource_type`</dt> <dd>Asset category name.</dd> <dt>string `text_resource_alias`</dt> <dd>Display alias for the asset slot, if configured.</dd> </dl>'
                      a_shop_product_option:
                        type: array
                        description: 'List of appointment add-ons. Every element has next keys: <dl> <dt>string[] `a_login_product`</dt> <dd> List of purchased product keys. Empty if no products were purchased. Each element: <dl> <dt>string `k_login_product`</dt> <dd>Purchased product key. <i>Primary key in the {@link \RsLoginProductSql} table.</i></dd> </dl> </dd> <dt>string `k_login_product`</dt> <dd>Deprecated, always `null`.</dd> <dt>string `k_shop_product`</dt> <dd>Primary key of add-on.</dd> <dt>string `k_shop_product_option`</dt> <dd>Add-on option. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> <dt>string `m_amount`</dt> <dd>Price that it adds to an appointment.</dd> </dl>'
                      dt_date_local:
                        type: string
                        description: 'Date/time of appointment in location timezone.'
                      i_duration:
                        type: [number, "null"]
                        description: 'Appointment duration (in minutes).'
                      i_index:
                        type: [number, "null"]
                        description: 'Index of booked asset.'
                      id_appointment_pay:
                        type: number
                        format: int32
                        description: 'Status of appointment payment. One of {@link \RsAppointmentPaySid} constants.'
                      k_location:
                        type: string
                        description: 'Location key.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'Purchased promotion which provides this appointment.'
                      k_resource:
                        type: [string, "null"]
                        description: 'Asset key.'
                      k_resource_type:
                        type: [string, "null"]
                        description: 'Asset category key.'
                      k_service:
                        type: [string, "null"]
                        description: 'Service key.'
                      k_service_category:
                        type: [string, "null"]
                        description: 'Service category key.'
                      k_session_pass:
                        type: [string, "null"]
                        description: 'Purchased drop-in which provides this appointment.'
                      k_staff:
                        type: string
                        description: 'Staff member who conducts this appointment. Deprecated: returned only for a limited list of third-party apps to keep backward compatibility. Use `uid_staff` instead.'
                      text_title:
                        type: string
                        description: 'Title of the appointment.'
                      uid_appointment:
                        type: string
                        description: 'User for whom this appointment was booked.'
                      uid_staff:
                        type: [string, "null"]
                        description: 'Staff member who conducts this appointment.'
      tags:
        - Info-Info
  '/Wl/Appointment/Mark/Mark.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_appointment:
                  type: string
                  description: 'Appointment key. Empty value means not set.'
                k_business:
                  type: string
                  description: 'Business key. Empty value means not set.'
                text_status:
                  type: string
                  description: 'Appointment status. One of ''attend'', ''book'', ''confirmed'', ''pending'' string values. Empty value means not set.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mark-Mark
  '/Wl/Appointment/WaitList/AppointmentWaitList.json':
    get:
      summary: 'Gets information about the appointment and wait list.'
      parameters:
        - name: k_appointment
          in: query
          description: 'Appointment key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'Information about the appointment. Has next structure: <dl> <dt>int `i_wait_list_limit`</dt> <dd>Maximum number of clients on wait list for the appointment.</dd> <dt>bool `is_service`</dt> <dd><tt>true</tt> if the appointment is reserved for service, <tt>false</tt> if for asset.</dd> <dt>bool `is_wait_list_limit`</dt> <dd>`true` to use service specific wait list limit, `false` to use the limit from default policies.</dd> <dt>string `dtl_session_end`</dt> <dd>Local end datetime of the appointment.</dd> <dt>string `dtl_session_start`</dt> <dd>Local start datetime of the appointment.</dd> <dt>string `text_appointment`</dt> <dd>Service or asset title</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string|null `text_staff`</dt> <dd>Staff name. <tt>null</tt> if the appointment is reserved for asset.</dd> </dl>'
                      a_wait:
                        type: array
                        description: 'List of visits in wait list. Each element has next structure: <dl> <dt>array `a_addon`</dt> <dd> A list of addons. Each element has next structure: <dl> <dt>string `k_shop_product_options`</dt> <dd>Key of the shop product option. Primary key from {@link \RsShopProductOptionSql} table.</dd> <dt>string `text_title`</dt> <dd>Title of the product.</dd> </dl> </dd> <dt>array `a_answer`</dt> <dd> List of answers for the appointment question. Each element has next structure: <dl> <dt>string `text_answer`</dt> <dd>Answer.</dd> <dt>string `text_question`</dt> <dd>Question.</dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> List of resources for booked service. Each element has next structure: <dl> <dt>string `text_resource`</dt> <dd>Title of the resource.</dd> <dt>string `text_resource_type`</dt> <dd>Title of the resource type.</dd> </dl> </dd> <dt>array `a_note`</dt> <dd> Information for visit note (SOAP and QUICK types): <dl> <dt>int `id_note`</dt> <dd>Type of note. One of {@link \Wl\Visit\Note\Sid\NoteSid} constants.</dd> <dt>string `sid_icon`</dt> <dd>Icon class for a note by note type.</dd> </dl> </dd> <dt>array `a_user`</dt> <dd> Information about user: <dl> <dt>string `text_mail`</dt> <dd>User email. Empty string if current user has no access to this information.</dd> <dt>string `text_name`</dt> <dd>User name.</dd> <dt>string `text_phone`</dt> <dd>User phone. Empty string if the user has no phone or current user has not access to this information.</dd> <dt>string `uid`</dt> <dd>User key. Primary key in the {@link \PassportLoginSql} table.</dd> <dt>string `url_logo`</dt> <dd>Link to user logo.</dd> <dt>string `url_notify_mail`</dt> <dd>Link to get form for send mail to user.</dd> <dt>string `url_notify_sms`</dt> <dd>Link to get form for send sms to user.</dd> <dt>string `url_profile`</dt> <dd>Link to user profile.</dd> </dl> </dd> <dt>string `dtl_book`</dt> <dd>Local datetime when the visit was booked (in MySQL format).</dd> <dt>bool `is_free`</dt> <dd>Whether the visit is free. Does not need a pay.</dd> <dt>bool `is_paid`</dt> <dd>Whether the visit is paid.</dd> <dt>bool `is_wait_confirm`</dt> <dd>Whether is awaiting for user confirmation.</dd> <dt>string `k_visit`</dt> <dd>Visit key. Primary key in the {@link \RsVisitSql} table.</dd> </dl>'
                      is_mail:
                        type: boolean
                        description: 'Whether or not to send email notification. `true` - enable check-in by email; `false` - otherwise.'
                      is_push:
                        type: boolean
                        description: 'Whether or not to send push notification. `true` - enable check-in by push; `false` - otherwise.'
                      is_sms:
                        type: boolean
                        description: 'Whether or not to send SMS notification. `true` - enable check-in by sms; `false` - otherwise.'
      tags:
        - WaitList-AppointmentWaitList
  '/Wl/Appointment/Recent/RecentService.json':
    get:
      summary: 'Gets list of client''s last booked services. Returns the most recently booked unique services for the given client at the given business, filtered by service type (appointment or bookable asset). Duplicate services are collapsed so only the most recent booking per service is included, up to {@link RecentServiceApi::MAX_SERVICE_COUNT}.'
      parameters:
        - name: i_visit
          in: query
          description: 'Count of last booked services to return. Default value is 5.'
          schema:
            type: number
            format: int32
        - name: id_service
          in: query
          description: 'Type of service to return. One of {@link ServiceSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service_last:
                        type: array
                        description: 'List of last booked services.'
      tags:
        - Recent-RecentService
  '/Wl/Book/Process/Process.json':
    get:
      summary: 'Returns the ordered list of booking wizard steps for the given session and client. Evaluates the session, user profile, payment requirements, asset selection, and subscription state to build an ordered list of steps the client must complete to finish the booking. The first step in the list is marked as current, and additional flags about event type, wait-list availability, and payment options are returned.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_family_relation_login_allow:
                        type: [array, "null"]
                        description: 'Relationships who clients are allowed to book for. One of the {@link \RsFamilyRelationSid} constants. This will be `null` if clients aren''t allowed to book for their relationships.'
                      a_path:
                        type: array
                        description: 'All the steps to be performed to make a booking. Every element has the next keys: <dl> <dt>int `id_book_process`</dt> <dd>The step ID. One of the {@link \Wl\Book\Process\ProcessSpaSid} constants.</dd> <dt>bool `is_current`</dt> <dd><tt>true</tt> - this item is current. <tt>false</tt> - this item isn''t current or not set yet.</dd> </dl>'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      is_age_require:
                        type: boolean
                        description: '`true` if this class has age restriction and requires user to specify age. `false` otherwise.'
                      is_card_authorize:
                        type: boolean
                        description: 'Determines if the client must authorize the credit card.'
                      is_event:
                        type: boolean
                        description: 'If `true`, the session being booked is an event. Otherwise, this will be `false`.'
                      is_family_relation_book:
                        type: boolean
                        description: 'Determines whether clients are allowed to book for their relationships without switching profiles.'
                      is_free:
                        type: boolean
                        description: 'Determines whether the class/event is free (price).'
                      is_have_ach:
                        type: boolean
                        description: '`true` if the client has an ach account, `false` otherwise.'
                      is_have_credit_card:
                        type: boolean
                        description: 'If `true`, the client has a credit card. Otherwise, this will be `false`.'
                      is_session:
                        type: boolean
                        description: '`true` - the client can select several sessions per booking. `false` - the client can''t select several sessions.'
                      is_wait:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list. Otherwise, this will be `false`.'
                      is_wait_list_unpaid:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list without payment, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'The key of the location where the session is booked.'
      tags:
        - Process-Process
  '/Wl/Book/Process/Process59.json':
    get:
      summary: 'Returns the booking wizard steps, adjusting the path when the service is already booked for a family member. Delegates to the parent implementation and then, when family-relation booking is enabled and the service is already booked for the selected relative, trims the wizard path down to only the relation and detail steps.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_family_relation_login_allow:
                        type: [array, "null"]
                        description: 'Relationships who clients are allowed to book for. One of the {@link \RsFamilyRelationSid} constants. This will be `null` if clients aren''t allowed to book for their relationships.'
                      a_path:
                        type: array
                        description: 'All the steps to be performed to make a booking. Every element has the next keys: <dl> <dt>int `id_book_process`</dt> <dd>The step ID. One of the {@link \Wl\Book\Process\ProcessSpaSid} constants.</dd> <dt>bool `is_current`</dt> <dd><tt>true</tt> - this item is current. <tt>false</tt> - this item isn''t current or not set yet.</dd> </dl>'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      is_age_require:
                        type: boolean
                        description: '`true` if this class has age restriction and requires user to specify age. `false` otherwise.'
                      is_card_authorize:
                        type: boolean
                        description: 'Determines if the client must authorize the credit card.'
                      is_event:
                        type: boolean
                        description: 'If `true`, the session being booked is an event. Otherwise, this will be `false`.'
                      is_family_relation_book:
                        type: boolean
                        description: 'Determines whether clients are allowed to book for their relationships without switching profiles.'
                      is_free:
                        type: boolean
                        description: 'Determines whether the class/event is free (price).'
                      is_have_ach:
                        type: boolean
                        description: '`true` if the client has an ach account, `false` otherwise.'
                      is_have_credit_card:
                        type: boolean
                        description: 'If `true`, the client has a credit card. Otherwise, this will be `false`.'
                      is_session:
                        type: boolean
                        description: '`true` - the client can select several sessions per booking. `false` - the client can''t select several sessions.'
                      is_wait:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list. Otherwise, this will be `false`.'
                      is_wait_list_unpaid:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list without payment, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'The key of the location where the session is booked.'
      tags:
        - Process-Process59
  '/Wl/Book/Process/Process54.json':
    get:
      summary: 'Returns the ordered list of booking wizard steps, including the quiz step when required. Delegates to the parent implementation and then appends the quiz (or document) step when the class or event requires it, adds the detail step for Client Web App flows, and re-sorts the path into the correct wizard order.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_family_relation_login_allow:
                        type: [array, "null"]
                        description: 'Relationships who clients are allowed to book for. One of the {@link \RsFamilyRelationSid} constants. This will be `null` if clients aren''t allowed to book for their relationships.'
                      a_path:
                        type: array
                        description: 'All the steps to be performed to make a booking. Every element has the next keys: <dl> <dt>int `id_book_process`</dt> <dd>The step ID. One of the {@link \Wl\Book\Process\ProcessSpaSid} constants.</dd> <dt>bool `is_current`</dt> <dd><tt>true</tt> - this item is current. <tt>false</tt> - this item isn''t current or not set yet.</dd> </dl>'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      is_age_require:
                        type: boolean
                        description: '`true` if this class has age restriction and requires user to specify age. `false` otherwise.'
                      is_card_authorize:
                        type: boolean
                        description: 'Determines if the client must authorize the credit card.'
                      is_event:
                        type: boolean
                        description: 'If `true`, the session being booked is an event. Otherwise, this will be `false`.'
                      is_family_relation_book:
                        type: boolean
                        description: 'Determines whether clients are allowed to book for their relationships without switching profiles.'
                      is_free:
                        type: boolean
                        description: 'Determines whether the class/event is free (price).'
                      is_have_ach:
                        type: boolean
                        description: '`true` if the client has an ach account, `false` otherwise.'
                      is_have_credit_card:
                        type: boolean
                        description: 'If `true`, the client has a credit card. Otherwise, this will be `false`.'
                      is_session:
                        type: boolean
                        description: '`true` - the client can select several sessions per booking. `false` - the client can''t select several sessions.'
                      is_wait:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list. Otherwise, this will be `false`.'
                      is_wait_list_unpaid:
                        type: boolean
                        description: 'If `true`, the user can be placed on a wait list without payment, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'The key of the location where the session is booked.'
      tags:
        - Process-Process54
  '/Wl/Book/Process/ProcessGroup.json':
    post:
      summary: 'Processes the group booking: validates input, collects payment, books sessions, and sends confirmation emails. Accepts a list of clients with their purchase items, resources, sessions, and quiz responses, then validates capacity and eligibility, processes payment for all clients at once, creates booking records, and dispatches confirmation emails. Returns visit keys, activity keys, and purchase activity key upon success. errors.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_client:
                  type: array
                  description: 'List of clients to book. Each value is an array with next keys: <dl> <dt>array[] `a_event_session`</dt> <dd> List of event sessions to book. Required for only for event that allows to select sessions to book. Each element has next keys: <dl> <dt>string `dtu_date`</dt> <dd>Session date/time.</dd> <dt>string `k_class_period`</dt> <dd>Session key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl> </dd> <dt>array[] `a_purchase_item`</dt> <dd> Purchase option information. Required if the service requires online payment. Elements have next keys: <dl> <dt>int `i_count`</dt> <dd>Number of options to purchase. Specify only if you want to pay a class booking by Drop-In.</dd> <dt>int `id_purchase_item`</dt> <dd> Kind of option to purchase. One of {@link \RsPurchaseItemSid} constants. Specify only if you want to purchase a new option. </dd> <dt>bool `is_renew`</dt> <dd> `true` if you want to enable auto-renewal for new purchase option. `false` otherwise. Specify only if you want to purchase a new option. </dd> <dt>bool `is_owner`</dt> <dd> `true` if client is owner of this purchase option. This means that this purchase option will be purchased for this client, even if another client can share a similar purchase option. </dd> <dt>string `k_id`</dt> <dd><i>Primary key of option to purchase.</i> Specify only if you want to purchase a new option.</dd> <dt>string `k_login_prize`</dt> <dd> <i>Primary key of user''s prize in {@link \RsLoginPrizeSql} table.</i> Specify if you want to pay by user''s prize. </dd> <dt>string `k_reward_prize`</dt> <dd> <i>Primary key of a prize to redeem in {@link \RsRewardPrizeSql} table.</i> Specify if you want to redeem a prize for payment. </dd> <dt>string `s_signature`</dt> <dd> Signature of the client in base64 format. Specify only if you want to buy a purchase option that requires signature. </dd> </dl> </dd> <dt>string[] `a_quiz_response`</dt> <dd> Answers for questions. Required if the service requires answers for questions. Keys are quiz keys. <i>Primary key from the {@link \Core\Quiz\QuizSql} table.</i> Values are response keys. <i>Primary key from the {@link \Core\Quiz\Response\ResponseSql} table.</i> Or the {@link \Wl\Quiz\Response\QuizResponse::RESPONSE_SKIP} to skip the quiz. </dd> <dt>array `a_repeat`</dt> <dd> Recurring booking configuration: You can test this key only in a case of `1` client. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the session repeats. One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_duration`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> Asset information. Required if the service requires asset booking. Each element has next keys: <dl> <dt>int `i_index`</dt> <dd>Order number of the asset.</dd> <dt>string `k_resource`</dt> <dd>Asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `k_login_promotion`</dt> <dd> Login promotion key. Specify only if you want to pay by already purchased option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_login_promotion_guest_pass`</dt> <dd> Guest pass login promotion key. Specify only if you want to pay with guest pass granted by this login promotion. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd>Session pass to be used to book a session. <i>Primary key from {@link \Wl\Session\Pass\Sql}.</i></dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_pay_installment_template:
                  type: [string, "null"]
                  description: 'The installment template primary key. `null` to not use installment template.'
                text_discount_code:
                  type: string
                  description: 'The discount code to be applied to the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_book_error:
                        type: array
                        description: 'List of errors that occurred during booking. Keys are user keys. <i>Primary key from the {@link \PassportLoginSql} table.</i> Each value has the next structure: <dl> <dt>string `text_code`</dt> <dd>Error code identifying the type of error that occurred.</dd> <dt>string `text_message`</dt> <dd>Human-readable error message describing the booking failure.</dd> </dl>'
                      a_login_activity_book:
                        type: array
                        description: 'Primary keys of users'' activity that correspond to bookings made.'
                      a_visit:
                        type: array
                        description: 'Primary keys of bookings made.'
                      k_login_activity_purchase:
                        type: [string, "null"]
                        description: 'The key of the user''s activity corresponding to the purchase made. `null` if no purchase was made.'
      tags:
        - Process-ProcessGroup
  '/Wl/Book/Promote/Promote.json':
    post:
      summary: 'Promote visit.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: [number, "null"]
                  description: 'ID of the source where a visit was promoted, one of {@link ModeSid} constants. `null` if not initialized yet.'
                k_business:
                  type: string
                  description: 'ID of business. Primary key in {@link \RsBusinessSql} table.'
                k_visit:
                  type: string
                  description: 'Visit ID to promote. Primary key in {@link \RsVisitSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: string
                        description: 'Message about successful promote.'
      tags:
        - Promote-Promote
  '/Wl/Book/Cancel/Cancel.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_visit:
                  type: array
                  description: 'Array of visit''s key. Each key is primary key in {@link \RsVisitSql} table.'
                k_business:
                  type: string
                  description: 'Key of the business within which the action is performed.'
                uid:
                  type: string
                  description: 'User key. Primary key in {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit_canceled:
                        type: array
                        description: 'Array of visit''s key which were canceled. These visits are in current location. Bookings made at other locations cannot be canceled, but they can be visible in clients Upcoming schedule in franchisee and can be selected for cancellation. Each key is primary key in {@link \RsVisitSql} table.'
      tags:
        - Cancel-Cancel
  '/Wl/Book/Cancel/CancelCan.json':
    get:
      summary: 'Returns information about whether the given user can cancel the booking and the expected consequences. Accepts a business key and a visit key, validates access, and performs a dry-run cancellation to determine whether the visit can be cancelled online, whether a late-cancel penalty applies, and whether the purchase option credit would be refunded.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business within which the action is performed.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_penalty:
                        type: [array, "null"]
                        description: 'Penalty data. `null` if penalty must be not applied. <dl> <dt>bool `is_flat`</dt> <dd>`true` in a case of flat penalty type; `false` in a case of percentage penalty type.</dd> <dt>string `k_currency`</dt> <dd>Currency key. <i>Primary key in {@link \RsCurrencySql} table.</i></dd> <dt>string `m_amount`</dt> <dd>Penalty amount.</dd> </dl>'
                      can_cancel:
                        type: boolean
                        description: '`true` if the booking can be canceled online by the specified user, `false` otherwise. Cancellation is possible only if the current visit status is {@link VisitSid::BOOK} or {@link VisitSid::WAIT}.'
                      is_flag:
                        type: boolean
                        description: '`true` if the client''s account will be flagged instead of charging a monetary fee, `false` otherwise. Meaningful only when {@link CancelCanApi::$is_late} is `true`.'
                      is_late:
                        type: boolean
                        description: '`true` if the cancellation would be considered a late cancel, `false` otherwise. Late cancel applies only to bookings with status {@link VisitSid::BOOK}. Wait-list bookings ({@link VisitSid::WAIT}) are never subject to late cancellation rules.'
                      is_refund:
                        type: boolean
                        description: '`true` if the visit credit (from the purchase option used to book) will be returned to the user''s profile after cancellation, `false` otherwise. For regular (non-late) cancellations, the credit is always returned when the booking was made with a purchase option. For late cancellations, return depends on the business''s Payment Return Policy.'
      tags:
        - Cancel-CancelCan
  '/Wl/Business/Account/BusinessAccountConfig.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_subscription:
                  type: array
                  description: 'List of subscription settings. Keys are CID of the subscription type and values are arrays of the payments for this subscription required for {@link \Wl\Business\Account\Subscription\SubscriptionWrite::gather()}'
                a_trial_detail:
                  type: array
                  description: 'Trial details to be modified. Array with next structure if trial details should be modified: Empty array if trial details should not be modified. <dl> <dt>string `dl_expire`</dt> <dd>New expiration date of the business trial period.</dd> <dt>int `eid_expire`</dt> <dd>Action which should be done after trial expiration. One of {@link \Wl\Business\Trial\TrialExpireEnum} values.</dd> </dl>'
                f_tax:
                  type: string
                  description: 'Additional tax amount value.'
                i_alert_fee:
                  type: number
                  format: int32
                  description: 'Number of days to show alert about failed payment.'
                i_alert_termination:
                  type: number
                  format: int32
                  description: 'Number of days after which business should be terminated.'
                is_alert_fee:
                  type: boolean
                  description: 'Whether alert about failed payment should be shown to business owner.'
                is_alert_termination:
                  type: boolean
                  description: 'Whether alert about business future termination should be shown to business owner.'
                is_currency_locale:
                  type: boolean
                  description: 'Whether business local currency should be used instead of system currency.'
                is_tax:
                  type: boolean
                  description: 'Whether additional taxes should be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_reject:
                        type: [string, "null"]
                        description: 'Reason why subscription changes are rejected. Uses to show to a user who operates with subscription settings. When it is set it means that subscription changes are rejected and was not saved. `null` if not initialized yet.'
      tags:
        - Account-BusinessAccountConfig
  '/Wl/Business/Account/BusinessAccount.json':
    get:
      summary: 'Generates list of active business keys for the same region as the requesting user (proper permissions required). Used internally by WellnessLiving operations tools to enumerate all customer businesses in the current region. Returns only active, non-test businesses; set `is_prospects` to also include prospect businesses that have not yet churned. Requires the `rs.business.view` privilege.'
      parameters:
        - name: is_prospects
          in: query
          description: '`true` returns WellnessLiving customers. `false` returns prospects. Test and churned businesses are never returned.'
          schema:
            type: boolean
        - name: is_published
          in: query
          description: 'Specifies if only businesses having published locations should be returned.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_keys:
                        type: array
                        description: 'A list of business keys.'
      tags:
        - Account-BusinessAccount
  '/Wl/Business/Account/BusinessAccountConfigClearAll.json':
    post:
      summary: 'Clears an existing payment and sets the payment to the status of ''Clear Balance''.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business account transaction key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Account-BusinessAccountConfigClearAll
  '/Wl/Business/Account/BusinessAccountConfigClear.json':
    post:
      summary: 'Clears an existing payment and sets the payment to the status of ''Clear Balance''. Clear balance is used when WellnessLiving wants to forgive outstanding payments for a business. ''Clear Balance'' is a type of payment method and corresponds to the {@link \RsPayMethodSid::CLEAR_BALANCE} constant.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                k_business_account_transaction:
                  type: string
                  description: 'Business account transaction key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Account-BusinessAccountConfigClear
  '/Wl/Business/Account/PayMethod.json':
    get:
      summary: 'Changes payment schedule according to request.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_recurrent:
                        type: [string, "null"]
                        description: 'Returns default recurrent payment source for business. <tt>null</tt> if source not managed and automatic payments for business are not available.'
      tags:
        - Account-PayMethod
  '/Wl/Business/Feature/FeatureInitTest.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Feature-FeatureInitTest
  '/Wl/Business/Franchise/Franchisee.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Franchisee business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_workout:
                  type: string
                  description: 'Final Reimbursement Date. Filled in franchisee local time. After this date no workouts should come in Reimbursement report that franchisor able to generate for this franchisee if <var>is_generate_workout</var> true. <var>dl_workout</var> field in the {@link \FranchiseListSql} table.'
                is_api_display:
                  type: boolean
                  description: 'API display. Influence on "start/stop showing the selected franchisee on Franchisor website". <var>is_api_display</var> field in the {@link \FranchiseListSql} table.'
                is_generate_workout:
                  type: boolean
                  description: 'Generate workout. If ON, franchisor will be able to generate Reimbursement reports for this franchisee. If OFF, reimbursement records for this franchisee will not appear in monthly reimbursement report. <var>is_generate_workout</var> field in the {@link \FranchiseListSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Franchise-Franchisee
  '/Wl/Business/Franchise/FranchiseGroup.json':
    get:
      summary: 'Gets franchise group for a given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to get data for. It may be both franchisor and franchisee. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: array
                        description: 'Keys of businesses of franchise group. Primary keys in {@link \RsBusinessSql} table.'
      tags:
        - Franchise-FranchiseGroup
  '/Wl/Business/Policy/CancelPolicyService.json':
    get:
      summary: 'Gets available services data of a business.'
      parameters:
        - name: i_index
          in: query
          description: 'Key of a business penalty policy.'
          schema:
            type: [number, "null"]
        - name: json_penalty_edit
          in: query
          description: 'Business penalty policy to edit in json format. <dl> <dt>array `a_class`</dt> <dd>Class keys. Primary key in {@link \RsClassSql} table.</dd> <dt>array `a_event`</dt> <dd>Class keys. Primary key in {@link \RsClassSql} table.</dd> <dt>array `a_login_type`</dt> <dd>Login types. Primary keys in {@link \Wl\Login\Type\Sql} table.</dd> <dt>array `a_resource`</dt> <dd>Resources keys. Primary keys in {@link \RsResourceSql} table.</dd> <dt>array `a_service`</dt> <dd>Service keys. Primary keys in {@link \RsServiceSql} table.</dd> <dt>int `i_blame`</dt> <dd>Quantity of penalty visits.</dd> <dt>int `i_cancel_period`</dt> <dd>Cancellation period.</dd> <dt>int `i_charge_measure`</dt> <dd>Charge measure.</dd> <dt>int `id_blame`</dt> <dd> Defines if client has "Late cancel" or "No shows" sessions. One of {@link \Wl\Business\Policy\BlameSid} constants. </dd> <dt>int `id_cancel_period`</dt> <dd>Duration ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_charge`</dt> <dd> Client''s charge if he has "Late cancel" or "No shows" sessions. One of {@link \Wl\Business\Policy\ChargeSid} constants. </dd> <dt>int `id_charge_measure`</dt> <dd>Percent or money sign.</dd> <dt>bool `is_appointment_all`</dt> <dd>`true` - all appointments are selected, `false` - otherwise.</dd> <dt>bool `is_class_all`</dt> <dd>`true` - all classes are selected, `false` - otherwise.</dd> <dt>bool `is_enable_cancel_charge`</dt> <dd>`true` - allow charge for "Late cancel" or "No shows" sessions, `false` - otherwise.</dd> <dt>bool `is_event_all`</dt> <dd>`true` - all events are selected, `false` - otherwise.</dd> <dt>bool `is_login_type_all`</dt> <dd>`true` - all login types are selected, `false` - otherwise.</dd> <dt>bool `is_resource_all`</dt> <dd>`true` - all assets are selected, `false` - otherwise.</dd> <dt>bool `is_service_all`</dt> <dd>`true` - all services are selected, `false` - otherwise.</dd> </dl>'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_all_service_selected:
                        type: [boolean, "null"]
                        description: 'Whether all services are selected. `null` if not initialized yet.'
                      json_service_all:
                        type: string
                        description: 'Services data. For details see {@link Policy::$a_service_all}.'
      tags:
        - Policy-CancelPolicyService
  '/Wl/Business/RegionTravel/BusinessRegionTravel.json':
    get:
      summary: 'Checks permission of user, and whether business can be transferred to target region.'
      parameters:
        - name: id_region_destination
          in: query
          description: 'ID of Amazon region that business is travelling to. `null` by default.'
          schema:
            type: ["null", number]
        - name: k_business
          in: query
          description: 'Key of business that is travelling. `null` by default.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_full_check_pass:
                        type: boolean
                        description: '`true` if business passed full check and can initiate travel. `false` otherwise.'
      tags:
        - RegionTravel-BusinessRegionTravel
    post:
      summary: 'Initiates business region travel.'
      parameters:
        - name: id_region_destination
          in: query
          description: 'ID of Amazon region that business is travelling to. `null` by default.'
          schema:
            type: ["null", number]
        - name: k_business
          in: query
          description: 'Key of business that is travelling. `null` by default.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - RegionTravel-BusinessRegionTravel
  '/Wl/Business/Report/DefaultReportConfig.json':
    post:
      summary: 'Saves default business report config, and overwrites all customized report configurations for the current user.'
      parameters:
        - name: a_button_hidden
          in: query
          description: 'Hidden feature button list. Each element is a constant of {@link ReportContainerControlButtonSid}. May be an empty array.'
          schema:
            type: array
        - name: a_button_visible
          in: query
          description: 'Visible feature button list. Each element is a constant of {@link ReportContainerControlButtonSid}. May be an empty array.'
          schema:
            type: array
        - name: id_report_date
          in: query
          description: 'Selected report date range. One of the {@link \RsReportDateSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_report_date_default
          in: query
          description: 'Does report use preselected date range. If not - open date range page before report generation.'
          schema:
            type: boolean
        - name: is_report_generate_last
          in: query
          description: 'Whether last generated range should be used instead of generating new report.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: show_button_selection
          in: query
          description: 'Whether buttons for date range selection should be shown nearby date filter.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'User''s UID key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Report-DefaultReportConfig
  '/Wl/Business/Report/ReportConfig.json':
    get:
      summary: 'Returns the specified report configuration for the specified user within the specified business.'
      parameters:
        - name: cid_report
          in: query
          description: 'The report CID. One of the {@link ReportGeneratorReportAbstract} subclasses.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'The key of the saved report. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User''s UID key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_button_hidden:
                        type: [array, "null"]
                        description: 'Hidden feature button list. Each element is a constant of {@link ReportContainerControlButtonSid}. May be an empty array. Order of items is relevant. <tt>null</tt> if configuration is not save.'
                      a_button_visible:
                        type: [array, "null"]
                        description: 'Visible feature button list. Each element is a constant of {@link ReportContainerControlButtonSid}. May be an empty array. Order of items is relevant. <tt>null</tt> if configuration is not save.'
                      a_cell_hidden:
                        type: array
                        description: 'Hidden cell list. See {@link ReportGeneratorConfig::$a_cell_hidden}.'
                      a_cell_visible:
                        type: array
                        description: 'Visible cell list. Order of items is relevant. See {@link ReportGeneratorConfig::$a_cell_visible}.'
                      id_report_date:
                        type: [number, "null"]
                        description: 'Selected report date range. One of the {@link \RsReportDateSid} constants. <tt>null</tt> if configuration is not save.'
                      show_client_details:
                        type: [boolean, "null"]
                        description: 'Whether to show extended client details in the client info cell. <tt>null</tt> if report doesn''t have a client cell.'
      tags:
        - Report-ReportConfig
    post:
      summary: 'Saves report configuration for specified user within specified business.'
      parameters:
        - name: cid_report
          in: query
          description: 'The report CID. One of the {@link ReportGeneratorReportAbstract} subclasses.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'The key of the saved report. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User''s UID key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_button_hidden:
                  type: [array, "null"]
                  description: 'Hidden feature button list. Each element is a constant of {@link ReportContainerControlButtonSid}. May be an empty array. Order of items is relevant. <tt>null</tt> if configuration is not save.'
                a_button_visible:
                  type: [array, "null"]
                  description: 'Visible feature button list. Each element is a constant of {@link ReportContainerControlButtonSid}. May be an empty array. Order of items is relevant. <tt>null</tt> if configuration is not save.'
                a_cell_hidden:
                  type: array
                  description: 'Hidden cell list. See {@link ReportGeneratorConfig::$a_cell_hidden}.'
                a_cell_visible:
                  type: array
                  description: 'Visible cell list. Order of items is relevant. See {@link ReportGeneratorConfig::$a_cell_visible}.'
                id_report_date:
                  type: [number, "null"]
                  description: 'Selected report date range. One of the {@link \RsReportDateSid} constants. <tt>null</tt> if configuration is not save.'
                show_client_details:
                  type: [boolean, "null"]
                  description: 'Whether to show extended client details in the client info cell. <tt>null</tt> if report doesn''t have a client cell.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Report-ReportConfig
  '/Wl/Business/Service/Service.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusiness} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_services_enabled:
                        type: array
                        description: 'Business service availability. Array where keys are service IDs (see {@link \RsServiceSid}) and values are `true` if the service is available for the business and `false` otherwise.'
      tags:
        - Service-Service
  '/Wl/Business/Skin/BusinessSkin.json':
    get:
      summary: 'Gets business design (skin) settings.'
      parameters:
        - name: id_place
          in: query
          description: 'Current place ID. Constant from {@link \RsPlaceSid}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}. If <tt>null</tt> you will receive data for the default skin.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_skin:
                        type: array
                        description: 'Full business skin data. See result of the {@link \rs_business_skin()} function.'
                      i_focus_delay:
                        type: number
                        format: int32
                        description: 'Period without any actions from owner after which we move focus on Client Search Field.'
                      id_focus_duration:
                        type: number
                        format: int32
                        description: 'Measurement unit of <var>i_focus_delay</var>. Member of {@link \ADurationSid}.'
                      s_background_skin:
                        type: string
                        description: 'Navigation menu background colors.'
                      s_left_theme:
                        type: string
                        description: 'Left panel theme.'
                      s_navmenu_theme:
                        type: string
                        description: 'Navigation menu theme.'
                      sid_header_button_size:
                        type: string
                        description: 'Size of icons in the top header panel for quick access.'
                      sid_header_button_type:
                        type: string
                        description: 'Type of icons in the top header panel for quick access.'
      tags:
        - Skin-BusinessSkin
  '/Wl/Business/Sms/SmsSendCheck.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
                uid:
                  type: string
                  description: 'User key for which need to send the SMS. Primary key in {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Sms-SmsSendCheck
  '/Wl/Business/Sms/SmsSend.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
                s_key:
                  type: string
                  description: 'The unique key of the sent SMS.'
                text_sms_body:
                  type: string
                  description: 'The body of the sms.'
                uid:
                  type: string
                  description: 'User key for which need to send the sms. Primary key in {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_send:
                        type: string
                        description: 'Local date with time when sms sent successfully (or failed) in MySQL format. If business is not specified, will not be returned.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      dtl_send:
                        type: string
                        description: 'Local date with time when sms sent successfully (or failed) in MySQL format. If business is not specified, will not be returned.'
      tags:
        - Sms-SmsSend
  '/Wl/Business/Sms/SmsUsage.json':
    get:
      summary: ''
      parameters:
        - name: a_member_group
          in: query
          description: 'Member group keys.'
          schema:
            type: array
        - name: i_sms_segments
          in: query
          description: 'Quantity of segments af a message to send. Depends on the length of a message.'
          schema:
            type: number
            format: int32
        - name: i_uid
          in: query
          description: 'Number of receivers to send SMS to.'
          schema:
            type: number
            format: int32
        - name: json_uid_select
          in: query
          description: 'UIDs of users to send sms to in JSON format. Is set if sms is sent from an attendance list, Marketing.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'Session key of selected users (in report).'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_usage_info:
                        type: array
                        description: 'SMS usage statistic. For details see return data of {@link BusinessSms::getUsageLimitInfo()} method.'
                      has_access:
                        type: boolean
                        description: 'Whether currently signed in staff has privilege to manage sms subscription.'
                      is_trial:
                        type: boolean
                        description: 'Whether current business is in SMS subscription trial period.'
                      is_usage_limit_ignore:
                        type: [boolean, "null"]
                        description: 'Whether sms usage limit message should be ignored. `null` if not set yet.'
                      text_trial_end:
                        type: string
                        description: 'Trial end date formatted according to business locale. Empty string if there is no active SMS subscription trial.'
                      url_subscription_plan:
                        type: string
                        description: 'Url to ''Message Center'' subscription plans.'
      tags:
        - Sms-SmsUsage
    post:
      summary: ''
      parameters:
        - name: i_limit
          in: query
          description: 'SMS usage limit.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_usage_limit_ignore:
                  type: [boolean, "null"]
                  description: 'Whether sms usage limit message should be ignored. `null` if not set yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Sms-SmsUsage
  '/Wl/Business/Config/BusinessConfig.json':
    get:
      summary: 'Gets information about a business config. Used by the booking widget and checkout flow to know the rules governing client interactions: which family member relations are allowed, whether clients or staff must select a location at checkout, what cancellation penalties apply, and whether white-label mode is active.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_policy:
                        type: array
                        description: 'All business policies connected to clients and bookings. <dl> <dt>int[] `a_payment_reattempt_not_decline_reason`</dt> <dd> List of not allowed decline reasons to payment reattempt. Each element is one of {@link \Thoth\WlPay\PayExceptionSid} constants. </dd> <dt>bool[] `a_wait_service`</dt> <dd>Keys are list of IDs from {@link \Wl\Service\ServiceSid}, and values are flags whether wait list is allowed.</dd> <dt>int `i_book_before`</dt> <dd>Minimum hours|days|months before class should be booked.</dd> <dt>int `i_book_future`</dt> <dd>Maximum hours|days|months after class can be booked.</dd> <dt>int `i_cancel`</dt> <dd>Minimum hours|days|months before class should be canceled without penalty.</dd> <dt>int `i_promote`</dt> <dd>Minimum hours|days|months before class should be promoted from wait list.</dd> <dt>int `i_promote_fastest_response`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation required should be sent to all clients on the wait list at the same time up to the cut off time. </dd> <dt>int `i_promote_require_confirm`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation must confirm their a promote from the waiting list to the active list. </dd> <dt>int `i_reattempt_count`</dt> <dd>Number of failed auto-payments reattempts.</dd> <dt>int `id_book_before`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_book_future`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_cancel`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_fastest_response`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_require_confirm`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>bool `is_book_inside_active_pay_period`</dt> <dd> if `true` - clients with purchase options are only allowed to book sessions within their current paid period, `false` - during purchase option''s duration. </dd> <dt>bool `is_disable_promotion`</dt> <dd> 1 if a client''s automatic payment fails, their account should not be debited and their purchase option becomes inactive, 0 - otherwise. Default 0. </dd> <dt>bool `is_enable_payment_penalty`</dt> <dd>Whether to charge penalty after final auto-payment attempt.</dd> <dt>bool `is_enable_payment_reattempt`</dt> <dd>Whether to reattempt failed auto-payments.</dd> <dt>bool `is_enable_staff_ip_restriction`</dt> <dd>Whether to restrict which IP addresses staff can login from.</dd> <dt>bool `is_prevent_booking`</dt> <dd>1 if booking for a client with negative balance is disabled, 0 - otherwise. Default 0.</dd> <dt>bool `is_staff_restrict`</dt> <dd>If true, client can not choose provider while appointment wizard.</dd> <dt>bool `is_wait`</dt> <dd>Enable\disable wait list.</dd> <dt>string `k_currency`</dt> <dd>Currency from rs.currency table.</dd> <dt>string `k_timezone`</dt> <dd>Timezone from get.timezone table.</dd> <dt>string `m_payment_penalty`</dt> <dd>Penalty amount to charge after final auto-payment attempt.</dd> <dt>string `url_custom`</dt> <dd>Custom Url of a business</dd> <dt>string[] `a_family_relation`</dt> <dd> List of allowed relation types specific to a given business. Key is ID. One of {@link \RsFamilyRelationSid} constant. Value is SID. <i>Result of {@link \RsFamilyRelationSid::allByBusiness()} method.</i> </dd> <dt>int[] `a_family_relation_login_allow`</dt> <dd> List of allowed relation types specific to a given business. Each value constant from {@link \RsFamilyRelationSid} class. </dd> </dl>'
                      a_penalty:
                        type: array
                        description: 'A list of business penalties. <dl> <dt>string[] `a_class_period`</dt> <dd>List of class period keys. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> <dt>string[] `a_login_type`</dt> <dd> List of client type keys. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i> Empty array means all active login types at concrete business. </dd> <dt>string[] `a_resource`</dt> <dd>List of resources keys. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_service`</dt> <dd>List of services keys. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>int `i_blame`</dt> <dd>Number of blamed visits.</dd> <dt>int `i_cancel_period`</dt> <dd>Count of days/weeks/months.</dd> <dt>int `i_charge_measure`</dt> <dd>Count of applied penalty.</dd> <dt>int `id_blame`</dt> <dd>One of {@link \Wl\Business\Policy\BlameSid} constants.</dd> <dt>int `id_cancel_period`</dt> <dd>Duration ID. One of {@link \ADurationSid} constants.</dd> <dt>int `id_charge`</dt> <dd>One of {@link \Wl\Business\Policy\ChargeSid} constants.</dd> <dt>int `id_charge_measure`</dt> <dd>Charge measure type. `0` for percent, `1` for money amount.</dd> <dt>bool `is_appointment_all`</dt> <dd>`1` if all classes are selected, `0` - otherwise.</dd> <dt>bool `is_class_all`</dt> <dd>`1` if all classes are selected, `0` - otherwise.</dd> <dt>bool `is_enable_cancel_charge`</dt> <dd>`true` if promotion penalties is enable, `false` otherwise.</dd> <dt>bool `is_event_all`</dt> <dd>`1` if all events are selected, `0` - otherwise.</dd> <dt>bool `is_login_type_all`</dt> <dd>`1` if all login types are selected, `0` - otherwise.</dd> <dt>bool `is_resource_all`</dt> <dd>`1` if all resources are selected, `0` - otherwise.</dd> <dt>bool `is_service_all`</dt> <dd>`1` if all services are selected, `0` - otherwise.</dd> </dl>'
                      is_location_client_select:
                        type: boolean
                        description: 'Whether client must select a location at checkout.'
                      is_location_select:
                        type: boolean
                        description: 'Determines whether staff members should select a location at checkout. If `true`, staff members should select a location at checkout. Otherwise, this will be `false`.'
                      is_white_label:
                        type: boolean
                        description: 'Determines whether the business has white label setting enabled in the admin settings. If `true`, admin settings are enabled. Otherwise, this will be `false`.'
      tags:
        - Config-BusinessConfig
  '/Wl/Business/AmazonRegion/AmazonRegion.json':
    get:
      summary: 'Returns the Amazon region ID for each of the requested business keys. Used to determine which data center stores the data for a given set of businesses. Knowing the region is required before routing API calls across data centers. Returns `0` for businesses whose region is not set. All requested business keys must exist.'
      parameters:
        - name: a_business
          in: query
          description: 'List of business key.'
          schema:
            type: array
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_region:
                        type: array
                        description: 'List of region IDs for {@link AmazonRegionApi::$a_business}. Key - is business key. Value - is region ID. One of {@link AmazonRegionSid} constants. `0` if this business is from an unknown region.'
      tags:
        - AmazonRegion-AmazonRegion
  '/Wl/Business/AuthorizeSupport/AuthorizeSupportEnter.json':
    get:
      summary: 'Logs current user into business backend.'
      parameters:
        - name: k_location
          in: query
          description: 'Location primary key in {@link \RsLocationSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AuthorizeSupport-AuthorizeSupportEnter
  '/Wl/Business/Partner/PartnerSettings.json':
    get:
      summary: 'Returns partner settings for the business identified by the given partner code. Used when rendering the Partner Program landing page. Resolves the partner code to a business and returns the business key and the home tour type so the landing page can show the appropriate demo tour. A daily failed-request limit per IP prevents brute-force enumeration of partner codes.'
      parameters:
        - name: text_code
          in: query
          description: 'The unique code that a business can provide other businesses to tell them about system.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_business_tour:
                        type: number
                        format: int32
                        description: 'The homepage tour of the business, which depends on the type.'
                      k_business:
                        type: string
                        description: 'The business key.'
                      text_name_first:
                        type: string
                        description: 'The first name of the business representative.'
                      text_name_last:
                        type: string
                        description: 'The last name of the business representative.'
                      text_position:
                        type: string
                        description: 'The job title of the business representative.'
                      text_review:
                        type: string
                        description: 'The text of the review about the WellnessLiving system.'
                      url_photo:
                        type: [string, "null"]
                        description: 'A link to the photo of the business representative. `null` in case when image is not uploaded.'
      tags:
        - Partner-PartnerSettings
  '/Wl/Business/Partner/PartnerCode.json':
    get:
      summary: 'Returns the partner URL for the specified business. Used to display or share the business''s unique Partner Program landing page link. Partner URLs are public information, so no access check is performed.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_partner:
                        type: string
                        description: 'Link to the landing campaign'
      tags:
        - Partner-PartnerCode
    post:
      summary: 'Updates partner url for a business that takes part in the partner program. Allows a Partner Program member to set a memorable custom vanity code for their referral landing page. The code must be unique across all businesses and contain only letters, digits, and underscores.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_code:
                  type: string
                  description: 'Unique code that business can give other businesses to tell them about system.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Partner-PartnerCode
  '/Wl/Business/Phone/Phone.json':
    delete:
      summary: 'Deletes business phone number from the system. Used by WellnessLiving admins to decommission a business''s SMS messaging service. After deletion, the business can no longer receive inbound SMS notifications. Admin privileges are required.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: text_phone
          in: query
          description: 'Business phone number(in locale format). Used to receive SMS notifications from clients. Can be `null` during bundle SID saving.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-Phone
    get:
      summary: 'Returns phone number(in locale format) of the business, if phone number does not exist returns empty line. Used in the SMS settings UI to display the currently registered business phone number and its input mask. The locale is derived from the business''s office country so the number is formatted correctly for that region.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_locale:
                        type: number
                        format: int32
                        description: 'Locale corresponding to the business'' address country. One of {@link \Core\Locale\LocaleSid} constants. Note that this may not be the same as the business'' locale, if the business is misconfigured and has an address country that is outside its locale.'
                      text_phone:
                        type: [string, "null"]
                        description: 'Business phone number(in locale format). Used to receive SMS notifications from clients. Can be `null` during bundle SID saving.'
                      text_phone_mask:
                        type: string
                        description: 'Business phone number mask.'
      tags:
        - Phone-Phone
    post:
      summary: 'Registers business phone number in system. Used when a business enables SMS messaging for the first time or replaces an existing number. Provisions a messaging service with the SMS provider so the business can receive inbound client messages. If the same number is already registered for this business, the call is a no-op.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: text_phone
          in: query
          description: 'Business phone number(in locale format). Used to receive SMS notifications from clients. Can be `null` during bundle SID saving.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-Phone
  '/Wl/Business/Phone/PhoneRegulatoryBundle.json':
    post:
      summary: 'Adds or changes regulatory bundle SID.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: [string, "null"]
        - name: s_id_regulatory_bundle
          in: query
          description: 'Regulatory bundle SID from Twilio (located in the Regulatory Bundle they have submitted). Used for australian businesses currently.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-PhoneRegulatoryBundle
  '/Wl/Business/Phone/Settings.json':
    post:
      summary: 'Updates SMS settings to define whether individual sms status is enabled or disabled for this business.'
      parameters:
        - name: is_individual_read
          in: query
          description: 'Is individual read.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Phone-Settings
  '/Wl/Business/Phone/PhoneGenerateByMask.json':
    get:
      summary: 'Gets similar phone numbers, depended on number, which failed verification.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: text_area_code_mask
          in: query
          description: 'Phone area code mask to use when searching for possible phone numbers.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_phone:
                        type: array
                        description: 'Array of phone numbers, which were got from provider. Amount of phone numbers is based on {@link PhoneSimilarApi::PHONE_AMOUNT}. Structure of this array is: <dl> <dt>string `text_phone`</dt> <dd>Phone number.</dd> <dt>string `text_phone_formatted`</dt> <dd>Phone number with mask applied.</dd> </dl>'
      tags:
        - Phone-PhoneGenerateByMask
  '/Wl/Business/Phone/PhoneSimilar.json':
    get:
      summary: 'Gets similar phone numbers, depended on number, which failed verification.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'Phone number, which was failed to verify. Used to get similar phone numbers from provider.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_phone:
                        type: array
                        description: 'Array of phone numbers, which were got from provider. Amount of phone numbers is based on {@link PhoneSimilarApi::PHONE_AMOUNT}. Structure of this array is: <dl> <dt>string `text_phone`</dt> <dd>Phone number.</dd> <dt>string `text_phone_mask`</dt> <dd>Phone number with mask applied.</dd> </dl>'
      tags:
        - Phone-PhoneSimilar
  '/Wl/Business/Phone/PhoneVerify.json':
    get:
      summary: 'Verifies that phone number is not registered in system yet.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}. It is necessary to get business locale settings and normalize phone number.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'Business phone number.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_new_number:
                        type: boolean
                        description: '<tt>true</tt> if phone number is not registered in system yet. <tt>false</tt> otherwise.'
      tags:
        - Phone-PhoneVerify
  '/Wl/Business/Select/BusinessSelect.json':
    get:
      summary: 'Performs checks and generates a list of businesses according to a given configuration. Populates the business-select dropdown widget shown in the backend. Used wherever staff need to switch between businesses they have access to. The returned structure drives the widget directly and includes selection state, visibility flags, and display configuration.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration data used to determine the list of businesses returned. This array has the following keys: <dl> <dt>bool `is_role`</dt> <dd> This will be `true` if we are checking for businesses where the user is a staff member. This will be `false` if we are checking for businesses where the user is a client. </dd> <dt>string `k_business`</dt> <dd>The business key.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business in which a list of business is requested.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User who is requesting the list of businesses.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_select:
                        type: array
                        description: 'Business list with additional parameters for a business select HTML component. This array contains the following keys and values. <dl> <dt>array `a_business`</dt> <dd> List of businesses the user can access. It is an array, each value is an array with the following keys: <dl> <dt>string `k_business`</dt> <dd>The key of the business.</dd> <dt>string `s_title`</dt> <dd>The name of the business.</dd> <dt>bool `selected`</dt> <dd>This will be true for the current business.</dd> </dl> </dd> <dt>bool `is_select`</dt> <dd>This will be `true` if one of the listed businesses is selected.</dd> <dt>string `name`</dt> <dd>Information for the widget, the name of the key in the list. This should always be `k_business`.</dd> <dt>string `onchange`</dt> <dd>Information for the widget, what the widget will do on selection.</dd> <dt>string `s_id`</dt> <dd>Information for the widget, the widget instance ID.</dd> <dt>string `s_style`</dt> <dd>Information for the widget, any additional CSS to apply.</dd> </dl>'
      tags:
        - Select-BusinessSelect
  '/Wl/Business/Churn/BusinessChurn.json':
    get:
      summary: 'Gets information about a business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_churn:
                        type: boolean
                        description: '<tt>true</tt> if business is a lost customer, <tt>false</tt> - otherwise.'
                      text_business_name:
                        type: string
                        description: 'Business name'
      tags:
        - Churn-BusinessChurn
    post:
      summary: 'Churns the business.'
      parameters:
        - name: dl_churn
          in: query
          description: 'Business churn date. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: text_churn_reason
          in: query
          description: 'Reason of a business churn.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Churn-BusinessChurn
  '/Wl/Business/Waiver/BusinessWaiver.json':
    get:
      summary: 'Gets waiver settings for the business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business Key. Primary key in table {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_contract_minor:
                        type: number
                        format: int32
                        description: 'If {@link \Wl\Business\Waiver\BusinessWaiverApi::$is_contract_minor} is set, this field contains the age of the minor. Default value is 18.'
                      is_contract_book_only:
                        type: boolean
                        description: 'Require clients to agree to the Liability Release only when booking services. `True` means to require. `False` otherwise.'
                      is_contract_minor:
                        type: boolean
                        description: 'Clients can sign the waiver on behalf of any minor of age {@link \Wl\Business\Waiver\BusinessWaiverApi::$i_contract_minor}'
                      is_contract_upon_register:
                        type: boolean
                        description: 'Require clients to agree to the Liability Release upon registration. `True` means to require. `False` otherwise.'
                      xml_contract:
                        type: string
                        description: 'Contents of the business waiver.'
                      xml_description:
                        type: string
                        description: 'Description of the business waiver.'
      tags:
        - Waiver-BusinessWaiver
  '/Wl/Business/Waiver/Waiver.json':
    get:
      summary: 'Returns the business waiver text rendered as HTML with user-specific variables substituted. Used to display the waiver agreement to a client before they complete a purchase or check in. Renders the waiver template with the client''s name filled in. If the business has no waiver configured, `has_waiver` is `false` and the waiver modal should not be shown.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of business to get waiver for.'
          schema:
            type: string
        - name: text_fullname
          in: query
          description: 'Full username. Used as a variable in a contract. It may be set if user does not exist.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key for which the waiver is shown. Used to fill the variables in the contract Not necessarily if the user does not already exist. In this case, you need to set {@link WaiverApi::$text_fullname}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_waiver:
                        type: boolean
                        description: 'Does the business have a waiver or not?'
                      html_waiver:
                        type: string
                        description: 'Text of the current waiver with the substituted variables.'
      tags:
        - Waiver-Waiver
  '/Wl/Business/Design/BusinessDesign.json':
    get:
      summary: 'API method to get business design data. Used to render the client-facing schedule and booking widget with the business''s custom branding. Returns the color theme, header layout, logo style, capacity display preferences, and analytics tracking IDs (Google Analytics, Google Tag Manager, Facebook Pixel). Pass no business key to get the system default design.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to get design data. May be <tt>null</tt>. In this case default business design data is returned.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data:
                        type: array
                        description: 'Design data for a business. Contains the following keys: <dl> <dt>bool `hide_past_days`</dt> <dd>Clients only see today''s and upcoming sessions.</dd> <dt>int `id_layout`</dt> <dd>Layout ID. For more details, see {@link \RsPageFrontendHeaderLayoutSid}.</dd> <dt>int `id_logo_position`</dt> <dd>Logo position ID. For more details, see {@link \RsBusinessDesignLogoPositionSid}.</dd> <dt>int `id_logo_style`</dt> <dd>Logo style ID. For more details, see {@link \RsBusinessDesignLogoStyleSid}.</dd> <dt>bool `is_attend_self`</dt> <dd>If <tt>true</tt> clients can check-in using the Client Web App and Achieve Web App.</dd> <dt>bool `is_appointment_capacity`</dt> <dd>If <tt>true</tt> the capacity and number of clients signed up to the appointment will be shown.</dd> <dt>bool `is_class_capacity`</dt> <dd>If <tt>true</tt> the capacity and number of clients signed up to the class will be shown.</dd> <dt>bool `is_class_quick`</dt> <dd>If <tt>true</tt>, the quick class filter will be shown.</dd> <dt>bool `is_class_spot_only`</dt> <dd> `true` to display only available spots (e.g., "3 spots left"); `false` to display both available spots and total capacity (e.g., "3 spots left of 10"). </dd> <dt>bool `is_show_name`</dt> <dd>If <tt>true</tt>, the business name will be shown.</dd> <dt>string `s_color_background`</dt> <dd>Background color of frontend.</dd> <dt>string `s_color_header`</dt> <dd>Header color of frontend menu.</dd> <dt>string `s_color_menu_border`</dt> <dd>Border color of frontend menu.</dd> <dt>string `s_color_menu_element`</dt> <dd>Menu element color</dd> <dt>string `s_color_menu_hover`</dt> <dd>Hover colour of front end menu.</dd> <dt>string `s_color_menu_hover_background`</dt> <dd>Background hover color of front end menu.</dd> <dt>string `s_color_menu_press`</dt> <dd>Press colour of front end menu.</dd> <dt>string `s_color_submenu`</dt> <dd>Submenu colour of front end menu.</dd> <dt>string `s_color_submenu_element`</dt> <dd>Submenu element color of front end menu.</dd> <dt>string `s_color_submenu_hover`</dt> <dd>Hover color of submenu of front end menu</dd> <dt>string `s_color_submenu_press`</dt> <dd>Press color of submenu in front end menu.</dd> <dt>string `s_fb_pixel_id`</dt> <dd>Facebook Pixel ID. Used for Facebook analytics tracking. Empty string if tracking is disabled.</dd> <dt>string `s_ga_tracking_id`</dt> <dd>Google Analytics Tracking ID. Used for Google Analytics tracking. Empty string if tracking is disabled.</dd> <dt>string `s_gtm_container_id`</dt> <dd>Google Tag Manager Container ID. Used for Google Analytics tracking. Empty string if tracking is disabled.</dd> <dt>string `s_url_background`</dt> <dd>Path to the background image.</dd> </dl>'
      tags:
        - Design-BusinessDesign
  '/Wl/Business/Lead/BusinessLead.json':
    post:
      summary: 'Saves a new business lead referral record submitted through the Partner Program. Used when a Partner Program member refers a new prospective client to WellnessLiving. Stores the lead''s contact details linked to the referring partner so that sales can follow up and attribute the conversion to the correct partner.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business_partner:
                  type: string
                  description: 'The business''s partner key.'
                text_business_name:
                  type: string
                  description: 'The business''s name.'
                text_client_name:
                  type: string
                  description: 'The client''s full name.'
                text_email:
                  type: string
                  description: 'The client''s email.'
                text_phone:
                  type: string
                  description: 'The client''s phone number.'
                text_promo_code:
                  type: string
                  description: 'The business''s partner promo code.'
                url_website:
                  type: string
                  description: 'The website URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Lead-BusinessLead
  '/Wl/Business/Lead/BusinessChange.json':
    post:
      summary: 'Saves changes to referrer or referral business for a business lead request within partner program.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_referral:
                  type: boolean
                  description: 'Defines whether {@link BusinessChangeApi::$k_business} should be associated with referral or with referrer business. <tt>true</tt> - to associate with referral business. <tt>false</tt> - to associate with referrer business.'
                k_business:
                  type: string
                  description: 'Depending on value {@link BusinessChangeApi::$is_referral} defines referrer or referral business ID. Empty value mean that business is not specified. Primary key from {@link \RsBusinessSql}.'
                k_business_lead:
                  type: string
                  description: 'ID of the business lead request within partner program. Primary key from {@link \Wl\Business\LeadSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Lead-BusinessChange
  '/Wl/Business/Type/BusinessTypeList.json':
    get:
      summary: 'Returns the list of available business types with their categories and images. Used during the new business signup flow so the prospective client can select the type of business they are opening (e.g., yoga studio, gym, spa). The list drives both the type picker UI and the selection of the appropriate demo tour to show on the marketing site.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_type:
                        type: array
                        description: 'A list of business types. Each element has the next structure: <dl> <dt>array `a_image_list`</dt> <dd> Images list for business types. Each element has the next structure: <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is_resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to `false`, value returned in <var>url_thumbnail</var> equals value in <var>url_view</var>. </dd> <dt>string `url_thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger than specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise, link to original image is returned here. </dd> <dt>string `url_view`</dt> <dd>Url to original image in file storage.</dd> </dl> </dd> <dt>int `id_business_category`</dt> <dd>The business category. One of the {@link \RsBusinessCategorySid} constants.</dd> <dt>int `id_tour`</dt> <dd> Type of the demo tour on the sales site connected to the business type. More about tours: {@link \RsHomeTourSid}. </dd> <dt>string `k_business_type`</dt> <dd>The key of business type. <i>Primary key in the {@link \RsBusinessTypeSql} table.</i></dd> <dt>string `text_category`</dt> <dd>The title of the business category.</dd> <dt>string `text_category_description`</dt> <dd>The description of the business category.</dd> <dt>string `text_category_icon`</dt> <dd>The icon of the business category.</dd> <dt>string `text_file`</dt> <dd>System name of the business type.</dd> <dt>string `text_title`</dt> <dd>The title of business type.</dd> </dl>'
      tags:
        - Type-BusinessTypeList
  '/Wl/Business/Application/Application.json':
    get:
      summary: 'Gets information about the business application.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of a business to show information for. Primary key from {@link \RsSkinSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_application_title:
                        type: string
                        description: 'Application title. Empty for default application.'
      tags:
        - Application-Application
  '/Wl/Business/Search/BusinessInfo.json':
    get:
      summary: ''
      parameters:
        - name: a_business
          in: query
          description: 'Array of business keys to retrieve the data. Primary keys in {@link \RsBusinessSql} table.'
          schema:
            type: array
        - name: f_latitude
          in: query
          description: 'User''s location latitude in degrees.'
          schema:
            type: number
            format: float
        - name: f_longitude
          in: query
          description: 'User''s location Longitude in degrees.'
          schema:
            type: number
            format: float
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: object
                        description: 'Array with needed info about the business. Values have the next structure: <dl> <dt>string `k_business`</dt> <dd>Business key. Primary key in {@link \RsBusinessSql} table.</dd> <dt>string `s_location`</dt> <dd>Business address.</dd> <dt>string `s_title`</dt> <dd>Business name.</dd> <dt>string `url_business`</dt> <dd>Business URL address to sign in.</dd> <dt>string `url_logo`</dt> <dd>Path to business logo.</dd> </dl>'
      tags:
        - Search-BusinessInfo
  '/Wl/Business/Search/BusinessSearch.json':
    get:
      summary: 'Searches for businesses by name and returns matching business keys. Used in internal tools and admin pages to find a business by name or key when only partial information is known. Protected by a captcha to prevent automated enumeration. In strict mode, the title must match exactly; otherwise partial word matches are returned sorted by relevance.'
      parameters:
        - name: is_customer
          in: query
          description: 'If `true`, the return only active customers, `false` - all business.'
          schema:
            type: boolean
        - name: is_strict
          in: query
          description: 'If `true`, returns only active customer businesses, if `false`, returns all businesses.'
          schema:
            type: boolean
        - name: text_name
          in: query
          description: 'Given business name to search by.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Array with business keys that contain given name in their title.'
      tags:
        - Search-BusinessSearch
  '/Wl/Business/Search/BusinessSearchUser.json':
    get:
      summary: 'Searches for businesses by name and email address and returns matching business keys. Used to locate a specific business for a given client, for example during a password reset or membership lookup. Returns only businesses where the email belongs to a registered member, so a match confirms the client actually has an account at that business.'
      parameters:
        - name: is_customer
          in: query
          description: 'If `true`, the return only active customers, `false` - all business.'
          schema:
            type: boolean
        - name: text_mail
          in: query
          description: 'User email to search business keys.'
          schema:
            type: string
        - name: text_name
          in: query
          description: 'Given business name to search by.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Array with business keys that contain given name in their title.'
      tags:
        - Search-BusinessSearchUser
  '/Wl/Business/AuthorizePartner/AuthorizePartner.json':
    get:
      summary: 'Grants or denies access to business location for a partner. Allows a franchisor staff member to temporarily let another user into a franchisee location. Requires the "Manage Franchise Location" privilege. Granted access expires after 24 hours; revoking schedules removal within 15 minutes.'
      parameters:
        - name: is_grant
          in: query
          description: 'Determines whether the user will be granted access or if access will be revoked. If `true`, access will be granted. If `false`, access will be revoked. Revoking access is a scheduled task set to be run within the next 15 minutes.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'The key of the franchisee location to enter.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user who will be granted access.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AuthorizePartner-AuthorizePartner
  '/Wl/Business/Header/Owner.json':
    get:
      summary: 'Performs all necessary checks, as well as prepares and returns data.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business for which you want to get header information. As a result, this field may be empty. In this case, the location key will be setted. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key for which information will be loaded. Primary key from {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_title:
                        type: string
                        description: 'Business or Location title. Which of the title will be returned depends on the current user and the features of the business.'
                      is_logo_exists:
                        type: boolean
                        description: 'Whether the business or location logo exists and should be displayed.'
                      k_business:
                        type: string
                        description: 'The key of the business for which you want to get header information. As a result, this field may be empty. In this case, the location key will be setted. Primary key from {@link \RsBusinessSql} table.'
                      k_location:
                        type: string
                        description: 'Key of current user location. Primary key from {@link \RsLocationSql} table.'
                      show_business_name:
                        type: boolean
                        description: 'Whether title will be displayed or not. Depends on business settings.'
                      url_microsite:
                        type: string
                        description: 'Microsite URL for user location.'
      tags:
        - Header-Owner
  '/Wl/Business/NameList/NameList.json':
    post:
      summary: 'Retrieves names for given business keys.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_business:
                  type: string
                  description: 'Array of business keys, encoded to string to get names for. Each element is a primary key in the {@link \RsBusinessSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_error:
                        type: array
                        description: 'List of business keys that are incorrect.'
                      a_business_name:
                        type: array
                        description: 'Each field is a business key, the primary key in the {@link \RsBusinessSql} table. Each value is the business name.'
      tags:
        - NameList-NameList
  '/Wl/Business/Trial/Trial.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key to remove trial from. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trial-Trial
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key to remove trial from. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_trial:
                        type: boolean
                        description: 'Whether business account has trial limitation. `true` if business account has trial limitation, `false` otherwise.'
      tags:
        - Trial-Trial
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key to remove trial from. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trial-Trial
  '/Wl/Business/Claim/BusinessClaim.json':
    get:
      summary: 'Checks if the email or phone number has already been used for a trial account or not. Called during the self-onboarding flow before creating a new business to detect duplicate registrations early. Checks across all data center regions. Rate-limited per IP to prevent abuse.'
      parameters:
        - name: text_mail
          in: query
          description: 'The email address of the location. Urlencoded email address is expected.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'The phone number of the business, staff and location. Urlencoded phone number is expected.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claim-BusinessClaim
    post:
      summary: 'Completes the verification process of the trial business. Used by WellnessLiving staff to confirm a new trial business after reviewing the signup. Marks the business as verified by the approving user. Returns the location microsite URL so the staff member can share it with the new client.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The key of the business to be verified.'
                k_location:
                  type: string
                  description: 'The key of the location to be verified.'
                uid_verified:
                  type: string
                  description: 'The key of the user who verified the business.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_microsite:
                        type: [string, "null"]
                        description: 'Location microsite URL. <i>Result of {@link rs_microsite_url()} function.</i>'
      tags:
        - Claim-BusinessClaim
    put:
      summary: 'Saves the Self-Setup wizard form data in the business claim log. Called at the end of the Self-Setup wizard to store the wizard input for internal auditing. Can only be called once per business after it has been claimed.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_form:
                  type: array
                  description: 'The Self-Setup wizard form data to be saved in the business claim log. The structure is conditionally arbitrary and is used only for logging purposes. No specific keys are required or validated.'
                k_business:
                  type: string
                  description: 'The key of the business to be verified.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claim-BusinessClaim
  '/Wl/Contact/Type/ContactTypeList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_contact_type:
                        type: array
                        description: 'List of contact types.'
      tags:
        - Type-ContactTypeList
  '/Wl/Coupon/Edit/CouponRemove.json':
    post:
      summary: 'Removes Gift Card from the business.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_coupon:
                  type: string
                  description: 'ID of the Gift Card that should be removed. Primary key from {@link \RsCouponSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-CouponRemove
  '/Wl/Coupon/CouponList/List.json':
    get:
      summary: 'Gets list of coupons. Used to populate the gift card picker in the store and booking flows. Returns all gift card types offered by the business. In franchise mode, gift cards from all franchise locations are included.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created coupons (if business is franchisor). <tt>true</tt> to include franchisee-created coupons.'
          schema:
            type: boolean
        - name: is_frontend
          in: query
          description: '`true` to get only gift cards available for current user; `false` to get all gift cards.'
          schema:
            type: boolean
        - name: is_inactive_include
          in: query
          description: 'A flag to include disabled items in the query result'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key to retrieve a list of all the gift cards in a business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_coupon:
                        type: array
                        description: 'A list of gift cards. Every element has the following keys: <dl> <dt>int `id_type`</dt> <dd>The type of coupon. One of {@link \Wl\Coupon\TypeSid} constants.</dd> <dt>string `k_coupon`</dt> <dd>The gift card key.</dd> <dt>string `text_title`</dt> <dd>The title of the gift card.</dd> </dl>'
      tags:
        - CouponList-List
  '/Wl/Franchise/Transfer/TransferAdd.json':
    post:
      summary: 'Creates transfer.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dt_end_local:
                  type: [string, "null"]
                  description: 'End date of temporary transfer. Set if {@link \Wl\Franchise\Transfer\TransferAddApi::$id_transfer_period} equals to {@link \Wl\Franchise\Transfer\TransferPeriodSid::RANGE} constant, otherwise <tt>null</tt>.'
                dt_start_local:
                  type: [string, "null"]
                  description: 'Start date of temporary transfer. Set if {@link \Wl\Franchise\Transfer\TransferAddApi::$id_transfer_period} equals to {@link \Wl\Franchise\Transfer\TransferPeriodSid::RANGE} constant, otherwise <tt>null</tt>.'
                i_day:
                  type: [number, "null"]
                  description: 'Number of days of temporary transfer. Set if {@link \Wl\Franchise\Transfer\TransferAddApi::$id_transfer_period} equals to {@link \Wl\Franchise\Transfer\TransferPeriodSid::DAY} constant, otherwise <tt>null</tt>.'
                id_transfer_direction:
                  type: number
                  format: int32
                  description: 'Transfer direction ID. One of {@link \Wl\Franchise\Transfer\TransferDirectionSid} constants.'
                id_transfer_period:
                  type: number
                  format: int32
                  description: 'Transfer period ID. One of {@link \Wl\Franchise\Transfer\TransferPeriodSid} constants.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in {@link \RsBusinessSql} table. This is business in which the transfer takes place.'
                k_location_to:
                  type: string
                  description: 'Location key. Primary key in {@link \RsLocationSql} table. This is a location where the transfer takes place.'
                uid_transfer:
                  type: string
                  description: 'Transferred user key. Primary key in the {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Transfer-TransferAdd
  '/Wl/Franchise/Transfer/TransferUser.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid_transfer
          in: query
          description: 'Transfer uid key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_name_full:
                        type: string
                        description: 'Full user''s name.'
                      url_photo:
                        type: string
                        description: 'User photo URL.'
      tags:
        - Transfer-TransferUser
  '/Wl/Franchise/Transfer/TransferStatus.json':
    put:
      summary: 'Changes transfer status.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_transfer_status:
                  type: number
                  format: int32
                  description: 'Transfer status ID. One of {@link \Wl\Franchise\Transfer\TransferStatusSid} constants.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
                k_franchise_transfer:
                  type: string
                  description: 'Franchise transfer key. Primary key in {@link \Wl\Franchise\Transfer\TransferSql} table.'
                text_reason:
                  type: string
                  description: 'Reason of transfer status.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Transfer-TransferStatus
  '/Wl/Franchise/Fee/FeeList.json':
    get:
      summary: ''
      parameters:
        - name: is_all
          in: query
          description: 'If `true` - show inactive franchise fee. If `false` show only active franchise fee.'
          schema:
            type: boolean
        - name: is_include_remove
          in: query
          description: 'If `true` - show removed franchise fee. `false` otherwise..'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: object
                        description: 'List of franchise fee.'
      tags:
        - Fee-FeeList
  '/Wl/Franchise/Fee/FeeElement.json':
    delete:
      summary: 'Deletes franchise fee. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Franchisor business key.'
          schema:
            type: string
        - name: k_franchise_fee
          in: query
          description: 'Franchise fee key. `null` when creating new fee.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Fee-FeeElement
    get:
      summary: 'Returns information about the franchise fee.'
      parameters:
        - name: k_business
          in: query
          description: 'Franchisor business key.'
          schema:
            type: string
        - name: k_franchise_fee
          in: query
          description: 'Franchise fee key. `null` when creating new fee.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_apply:
                        type: [array, "null"]
                        description: 'List of {@link FeeApplySid}. Used only when {@link FeeEntity::$id_fee_type} is {@link FeeTypeSid::PERCENTAGE}, otherwise `null`.'
                      a_location:
                        type: array
                        description: 'List of locations where fee is applied. Key is location key. Value is override of the fee rate(percent/flat). Can be empty in case when we load list of fees by location in {@link FranchiseFee::listGet()} method.'
                      dl_end:
                        type: [string, "null"]
                        description: 'End date of the fee. This is a date of last active day when fee can be applied. `null` in a case fee should be active infinitely.'
                      dl_start:
                        type: [string, "null"]
                        description: 'Start date of the fee. This is a date since which fees applied to franchisee purchases.'
                      f_fee:
                        type: [string, "null"]
                        description: 'Fee rate. Percent or flat rate. `null` if not initialized.'
                      i_day:
                        type: [number, "null"]
                        description: 'Specific day of the month when fee is applied. Specified when {@link FeeEntity::$id_frequency} is {@link \ADurationSid::MONTH}. And when {@link FeeEntity::$id_fee_schedule} is {@link FeeScheduleSid::SCHEDULE}. In others cases `null`.'
                      id_date_week:
                        type: [number, "null"]
                        description: 'Week day ID. Specified when {@link FeeEntity::$id_frequency} is: * {@link \ADurationSid::WEEK}. * {@link \ADurationSid::WEEK2}. And when {@link FeeEntity::$id_fee_schedule} is {@link FeeScheduleSid::SCHEDULE}. In others cases `null`.'
                      id_fee_schedule:
                        type: [number, "null"]
                        description: 'Schedule type ID. `null` if not initialized.'
                      id_fee_type:
                        type: [number, "null"]
                        description: 'Fee type ID.'
                      id_frequency:
                        type: [number, "null"]
                        description: 'Frequency ID. Used when {@link FeeEntity::$id_fee_schedule} is {@link FeeScheduleSid::SCHEDULE}. In others cases `null`.'
                      m_min:
                        type: [string, "null"]
                        description: 'Minimum amount of the fee. Set only when {@link FeeEntity::$id_fee_type} is {@link FeeTypeSid::PERCENTAGE}, otherwise `null`.'
                      text_title:
                        type: [string, "null"]
                        description: 'Name of the fee. Maximum length is {@link FeeEntity::FEE_TITLE_MAX}. `null` if not initialized.'
      tags:
        - Fee-FeeElement
    post:
      summary: 'Creates/updates franchise fee.'
      parameters:
        - name: k_business
          in: query
          description: 'Franchisor business key.'
          schema:
            type: string
        - name: k_franchise_fee
          in: query
          description: 'Franchise fee key. `null` when creating new fee.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_apply:
                  type: [array, "null"]
                  description: 'List of {@link FeeApplySid}. Used only when {@link FeeEntity::$id_fee_type} is {@link FeeTypeSid::PERCENTAGE}, otherwise `null`.'
                a_location:
                  type: array
                  description: 'List of locations where fee is applied. Key is location key. Value is override of the fee rate(percent/flat). Can be empty in case when we load list of fees by location in {@link FranchiseFee::listGet()} method.'
                dl_end:
                  type: [string, "null"]
                  description: 'End date of the fee. This is a date of last active day when fee can be applied. `null` in a case fee should be active infinitely.'
                dl_start:
                  type: [string, "null"]
                  description: 'Start date of the fee. This is a date since which fees applied to franchisee purchases.'
                f_fee:
                  type: [string, "null"]
                  description: 'Fee rate. Percent or flat rate. `null` if not initialized.'
                i_day:
                  type: [number, "null"]
                  description: 'Specific day of the month when fee is applied. Specified when {@link FeeEntity::$id_frequency} is {@link \ADurationSid::MONTH}. And when {@link FeeEntity::$id_fee_schedule} is {@link FeeScheduleSid::SCHEDULE}. In others cases `null`.'
                id_date_week:
                  type: [number, "null"]
                  description: 'Week day ID. Specified when {@link FeeEntity::$id_frequency} is: * {@link \ADurationSid::WEEK}. * {@link \ADurationSid::WEEK2}. And when {@link FeeEntity::$id_fee_schedule} is {@link FeeScheduleSid::SCHEDULE}. In others cases `null`.'
                id_fee_schedule:
                  type: [number, "null"]
                  description: 'Schedule type ID. `null` if not initialized.'
                id_fee_type:
                  type: [number, "null"]
                  description: 'Fee type ID.'
                id_frequency:
                  type: [number, "null"]
                  description: 'Frequency ID. Used when {@link FeeEntity::$id_fee_schedule} is {@link FeeScheduleSid::SCHEDULE}. In others cases `null`.'
                m_min:
                  type: [string, "null"]
                  description: 'Minimum amount of the fee. Set only when {@link FeeEntity::$id_fee_type} is {@link FeeTypeSid::PERCENTAGE}, otherwise `null`.'
                text_title:
                  type: [string, "null"]
                  description: 'Name of the fee. Maximum length is {@link FeeEntity::FEE_TITLE_MAX}. `null` if not initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_franchise_fee:
                        type: [string, "null"]
                        description: 'Franchise fee key. `null` when creating new fee.'
      tags:
        - Fee-FeeElement
  '/Wl/GymPass/Webhook/BusinessConnect.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event_type:
                  type: object
                  description: 'Event type.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. `null` in a case value is not initialized yet.'
                k_location:
                  type: [string, "null"]
                  description: 'Location key. `null` in a case value is not initialized yet.'
                o_event_data:
                  type: object
                  description: 'Event data.'
                s_gym_id:
                  type: [string, "null"]
                  description: 'Gym ID. `null` in a case value is not initialized yet.'
                s_product_id:
                  type: [string, "null"]
                  description: 'Product ID. `null` in a case value is not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Webhook-BusinessConnect
  '/Wl/GymPass/Webhook/BookingCheckin.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event_type:
                  type: object
                  description: 'Event type.'
                o_event_data:
                  type: object
                  description: 'Event data.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Webhook-BookingCheckin
  '/Wl/GymPass/Webhook/BookingCreate.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event_type:
                  type: object
                  description: 'Event type.'
                o_event_data:
                  type: object
                  description: 'Event data.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Webhook-BookingCreate
  '/Wl/GymPass/Webhook/BookingCancel.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event_type:
                  type: object
                  description: 'Event type.'
                o_event_data:
                  type: object
                  description: 'Event data.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Webhook-BookingCancel
  '/Wl/Html/Menu/Menu.json':
    get:
      summary: 'Checks the received data, loads the menu.'
      parameters:
        - name: id_place
          in: query
          description: 'Current place ID. Constant from {@link \RsPlaceSid}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business in which the user wants to see the menu.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user for which the menu should be displayed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_menu:
                        type: array
                        description: 'Menu list or an empty array if the menu for the specified unit could not be loaded or user does not have enough privileges.'
      tags:
        - Menu-Menu
  '/Wl/Insurance/Reimbursement/ReimbursementResubmit.json':
    get:
      summary: 'Triggers resubmission of the reimbursement record.'
      parameters:
        - name: k_insurance_reimbursement
          in: query
          description: 'Key of the reimbursement record to be resubmitted. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Reimbursement-ReimbursementResubmit
  '/Wl/Insurance/Enrollment/EnrollmentPanel.json':
    get:
      summary: 'Gets "Wellness Program" info and a list of required fields for enrollment. If we open the already purchased program, then the fields will be filled in with the entered data during enrollment.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Purchased program key. Primary key from {@link \RsLoginPromotionSql} table. If set, the program key (<tt>k_wellness_program</tt>) will be taken from the {@link \Wl\Login\Promotion\LoginPromotionInsuranceSql} table.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: array
                        description: '"Wellness Program" info. <dl> <dt>bool `is_reimbursement_information_required`</dt> <dd>Determines whether the requiring ACH account.</dd> <dt>string `s_account`</dt> <dd>Part of reimbursement account number.</dd> <dt>string `text_insurance_organization`</dt> <dd>Insurance organization name.</dd> </dl>'
                      html_field_list:
                        type: string
                        description: 'List of fields that must be filled for enrollment. Presented by rendered template.'
      tags:
        - Enrollment-EnrollmentPanel
    post:
      summary: 'Validates list of fields that the user has filled in for enrollment.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Purchased program key. Primary key from {@link \RsLoginPromotionSql} table. If set, the program key (<tt>k_wellness_program</tt>) will be taken from the {@link \Wl\Login\Promotion\LoginPromotionInsuranceSql} table.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_account:
                  type: array
                  description: 'Reimbursement account information. Keys - field name. Values - value entered by user. <i>Validation is performed by {@link EnrollmentFieldList::setAccountForm()} method.</i>'
                a_field:
                  type: array
                  description: 'List of fields that the user has filled in for enrollment. Keys refer specifically to field keys. <i>Primary keys in {@link \Wl\Integration\Curves\PartnerFieldSql} table.</i> Values refer specifically to values entered by the user. <i>Validation is performed by {@link EnrollmentFieldList::setFieldForm()} method.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Enrollment-EnrollmentPanel
    put:
      summary: 'Validates and modifies the fields of the purchased program.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Purchased program key. Primary key from {@link \RsLoginPromotionSql} table. If set, the program key (<tt>k_wellness_program</tt>) will be taken from the {@link \Wl\Login\Promotion\LoginPromotionInsuranceSql} table.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_account:
                  type: array
                  description: 'Reimbursement account information. Keys - field name. Values - value entered by user. <i>Validation is performed by {@link EnrollmentFieldList::setAccountForm()} method.</i>'
                a_field:
                  type: array
                  description: 'List of fields that the user has filled in for enrollment. Keys refer specifically to field keys. <i>Primary keys in {@link \Wl\Integration\Curves\PartnerFieldSql} table.</i> Values refer specifically to values entered by the user. <i>Validation is performed by {@link EnrollmentFieldList::setFieldForm()} method.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Enrollment-EnrollmentPanel
  '/Wl/Insurance/Profile/InsuranceCancel.json':
    delete:
      summary: 'Reactivates cancelled Wellness Program membership.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business where we need to cancel Wellness Program membership.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion to cancel. Primary key from {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to cancel Wellness Program membership.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Profile-InsuranceCancel
    put:
      summary: 'Api method to cancel client''s Wellness Program membership together with all upcoming visits related to it.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business where we need to cancel Wellness Program membership.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion to cancel. Primary key from {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to cancel Wellness Program membership.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_reason:
                  type: string
                  description: 'Reason to cancel Wellness Program membership.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Profile-InsuranceCancel
  '/Wl/Insurance/Profile/InsuranceHold.json':
    delete:
      summary: 'Stops (if start date is yesterday or earlier) or removes (if start date is today) currently active promotion payment pause.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to which currently handled login promotion belongs. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion to cancel. Primary key in {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Profile-InsuranceHold
    post:
      summary: 'Adds promotion payment pause with default settings, thus putting membership on hold.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to which currently handled login promotion belongs. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion to cancel. Primary key in {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_promotion_pay_pause:
                        type: string
                        description: 'Key of created promotion pause; table {@link \RsPromotionPayPauseSql}.'
      tags:
        - Profile-InsuranceHold
  '/Wl/Insurance/Catalog/ProgramList.json':
    get:
      summary: 'Returns a list of active insurance programs for the specified promotion. Requires backend access and an active Wellness Program feature for the business. Returns all available insurance programs with pricing, organization name, partner name, and a link to the insurance detail page.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the franchisee business.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'The key of the promotion.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_wellness_program:
                        type: array
                        description: 'A List of active programs. Every element has next keys: <dl> <dt>string `k_wellness_program`</dt> <dd>The insurance key.</dd> <dt>string `m_price`</dt> <dd>The Purchase Option price.</dd> <dt>string `text_insurance_organization`</dt> <dd>The insurance organization name.</dd> <dt>string `text_partner`</dt> <dd>The insurance partner name.</dd> <dt>string `text_program`</dt> <dd>The insurance name.</dd> <dt>string `url_insurance_detail`</dt> <dd>The insurance organization URL.</dd> </dl>'
      tags:
        - Catalog-ProgramList
  '/Wl/Integration/Curves/CurvesFranchiseLocation.json':
    get:
      summary: 'Returns country, region, state, city and location lists of the franchisor. This method returns all the information from the parent {@link \Wl\Business\Franchise\Location\BusinessFranchiseLocationApi::get()} and the Curves territory ID. Extends the parent location list with the Curves-specific territory ID (`k_territory_id`) for each franchisee location by joining against the Curves franchisee integration table.'
      parameters:
        - name: id_business_franchise_location
          in: query
          description: 'Determines which locations should be returned. One of the {@link BusinessFranchiseLocationSid} constants. If `null`, {@link BusinessFranchiseLocationSid::ALL} is used.'
          schema:
            type: number
            format: int32
        - name: is_include_churn
          in: query
          description: 'Determines whether to include churned/removed locations. If `true`, all locations are listed (regardless of their status).'
          schema:
            type: boolean
        - name: is_include_non_api
          in: query
          description: 'Determines whether to include locations marked to not be displayed on franchisor website. If `true`, all locations are listed (regardless of this setting).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_city_list:
                        type: array
                        description: 'The city list. Each element has next structure: <dl> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>City title.</dd> </dl>'
                      a_country_list:
                        type: array
                        description: 'The country list. Each element has next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Country title.</dd> </dl>'
                      a_location_list:
                        type: array
                        description: 'The location list. Each element has the next structure: <dl> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl>'
                      a_region_list:
                        type: array
                        description: 'The region list. Each element has the next structure: <dl> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Franchise region title.</dd> </dl>'
                      a_state_list:
                        type: array
                        description: 'The state list. Each element has the next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>State title.</dd> </dl>'
      tags:
        - Curves-CurvesFranchiseLocation
  '/Wl/Integration/Curves/Partner.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_partner:
                        type: [array, "null"]
                        description: 'A partner list. Where key is key partner, value is name of the partner.'
      tags:
        - Curves-Partner
  '/Wl/Integration/Autymate/Report.json':
    get:
      summary: 'Gets the daily transaction data. Authenticates the request using the business GUID, runs the All Transactions report for the specified date, and returns paginated rows augmented with Autymate-specific columns such as tax details, location address, batch number, and payment method information.'
      parameters:
        - name: a_pay_method_remove
          in: query
          description: 'List of payment methods to filter out in the report. Each element is one of the {@link \RsPayMethodSid} constants.'
          schema:
            type: array
        - name: dl_date
          in: query
          description: 'The date in local time to retrieve transactions for.'
          schema:
            type: string
        - name: i_page
          in: query
          description: 'The page of the report, starting from 0. Each page will contain a maximum of {@link \Wl\Integration\Autymate\ReportApi::LIMIT} rows.'
          schema:
            type: number
            format: int32
        - name: is_refresh
          in: query
          description: 'Determines whether this report should be refreshed. `true` to refresh this report if it''s already generated. Refreshing of the report may not be queried while report is being generated. `false` to only return contents of the report. If report isn''t yet generated, it automatically starts the generation in the background.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business for which the report must be generated.'
          schema:
            type: string
        - name: s_guid
          in: query
          description: 'The randomly generated 32 character string used to authenticate requests.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field:
                        type: array
                        description: 'The list of fields in this report. This array is effectively a title row for table that is returned in {@link ReportApi::$a_row}.'
                      a_row:
                        type: object
                        description: 'The report data. This is an indexed array in which one row is an indexed array also. Indexes of the columns correspond to columns in {@link \Wl\Integration\Autymate\ReportApi::$a_field}.'
                      a_warning:
                        type: array
                        description: 'The warning list of the report, if applicable.'
                      dtu_complete:
                        type: [string, "null"]
                        description: 'The date and time when this report has completed generation. `null` if generation of this report isn''t completed.'
                      dtu_queue:
                        type: [string, "null"]
                        description: 'The date and time when this report was put in the generation queue. Effectively, this is the time when a user clicked to view this report or the report for this day was first called.'
                      dtu_start:
                        type: [string, "null"]
                        description: 'The date and time when generation of this report was started. `null` if generation of this report hasn''t started.'
                      id_report_status:
                        type: number
                        format: int32
                        description: 'The status of the report. One of the {@link \Wl\Report\Generator\ReportGeneratorStatusSid} constants.'
                      is_more:
                        type: boolean
                        description: 'If <tt>true</tt> then there are more report rows to get. Otherwise, <tt>false</tt> if all rows have been sent.'
                      is_report_complete:
                        type: boolean
                        description: 'Determines whether this report is complete. If this report is accessed on the current day, or is returning a result that was cached on the current day it could be incomplete as not all the transactions for the day are present. If `true` then this report will be complete. If `false` then this report could be incomplete.'
      tags:
        - Autymate-Report
  '/Wl/Integration/Autymate/AutymateActivate.json':
    get:
      summary: 'Returns or changes the Autymate enrollment status for the specified business. Autymate specifically requested this method be a get. Authenticates the request using the business GUID, then either reads or updates the enrollment status depending on the mode and the supplied `id_status` value. When activating enrollment, also provisions a Professional subscription for the business if one is not already active.'
      parameters:
        - name: id_mode
          in: query
          description: 'The mode of the request. One of {@link \Wl\Integration\Autymate\AutymateAccessModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_status
          in: query
          description: 'The new status of the enrollment. If `0`, the current status is returned. One of {@link \Wl\Integration\Autymate\AutymateStatusSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: s_guid
          in: query
          description: 'The randomly generated 32 character string used to authenticate Autymate requests for the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_status:
                        type: number
                        format: int32
                        description: 'The new status of the enrollment. If `0`, the current status is returned. One of {@link \Wl\Integration\Autymate\AutymateStatusSid} constants.'
      tags:
        - Autymate-AutymateActivate
  '/Wl/Integration/Autymate/AutymateUrlIframe.json':
    get:
      summary: 'Constructs the URL to add to the iframe to allow the user to access Autymate''s platform.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_iframe:
                        type: string
                        description: 'URL to use in the Autymate iframe.'
      tags:
        - Autymate-AutymateUrlIframe
  '/Wl/Integration/Autymate/AutymateEnroll.json':
    get:
      summary: 'Creates an Autymate enrollment for a business. Once an enrollment is created the user can make the initial visit to Autymate to continue the sign up process on Autymate''s side.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_autymate_enroll:
                        type: string
                        description: 'Autymate enrollment key, primary key in the {@link \Wl\Integration\Autymate\AutymateEnrollSql} table.'
      tags:
        - Autymate-AutymateEnroll
  '/Wl/Integration/Centred/Centred.json':
    delete:
      summary: 'Removes records from the database.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: s_studio_id
          in: query
          description: 'Studio ID of the business. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Centred-Centred
    get:
      summary: 'Gets studio ID for business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: s_studio_id
          in: query
          description: 'Studio ID of the business. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Centred-Centred
    post:
      summary: 'On API request save value to database.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: s_studio_id
          in: query
          description: 'Studio ID of the business. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Centred-Centred
  '/Wl/Integration/Zoom/Connection.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zoom-Connection
    post:
      summary: 'Deauthorization endpoint.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_payload:
                  type: array
                  description: 'Event payload information.'
                s_event:
                  type: string
                  description: 'Event type.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zoom-Connection
  '/Wl/Integration/Zoom/Confirmation.json':
    post:
      summary: 'Sends email about paid account connection.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zoom-Confirmation
  '/Wl/Integration/Collection/CollectionActivate.json':
    post:
      summary: 'Changes the status of the Collections.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business. Primary key in {@link \RsBusinessSql} table.'
                uid:
                  type: string
                  description: 'User key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_test:
                        type: boolean
                        description: 'Defines whether business in testing mode. `true` if business in testing mode, `false` otherwise.'
      tags:
        - Collection-CollectionActivate
  '/Wl/Integration/DragonFly/Access.json':
    get:
      summary: 'Checks if the user can physically access the location. There are 3 scenarios where a user can be given access. Scenario 1 - If a valid member ID used, for instance from a bar code scanner, then the user may have access. Scenario 2 - If a valid uid is used and the user is a staff member, then the user may have access. Scenario 3 - If a valid uid is used and the client has a session or gym access at this location, then the user may have access.'
      parameters:
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: s_member
          in: query
          description: 'The member ID. Member ID is intended to work with barcode scanners. If this is specified the user key does not need to be set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This should be specified if the member ID is not set or not known.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_access:
                        type: boolean
                        description: 'Whether the specified user can access the location. `true` if the specified member can access the location. `false` if they can not.'
      tags:
        - DragonFly-Access
  '/Wl/Integration/Saml/SamlUserDeactivation.json':
    post:
      summary: 'Gets status of the user in business for given list of identifiers. Accepts a list of SAML identifier strings and returns a map of each identifier to a boolean indicating whether the corresponding user is active in their associated business. Requests with too many invalid identifiers are rate-limited with a penalty block mechanism.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_id:
                  type: array
                  description: 'A list of SAML identifiers.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'The key of the SAML identifier. This value is `true` if an active user is found in the business, `false` if otherwise.'
      tags:
        - Saml-SamlUserDeactivation
  '/Wl/Integration/Zapier/Upgrade.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zapier-Upgrade
  '/Wl/Integration/Ecommerce/OrderSync.json':
    post:
      summary: 'Creates a new order in WellnessLiving based on the provided data. Sets {@link OrderSyncApi::$k_purchase} on success.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_purchase_item:
                  type: array
                  description: 'List of items to add to the order. Each element has keys: <dl> <dt>array `a_config`</dt> <dd> Item specific configuration. <dl> <dt>array[] `a_quick_gift`</dt> <dd> Quick gift card component list. Each element has keys: Required for {@link \RsPurchaseItemSid::COUPON}. <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID from {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Purchase item key in the database.</dd> </dl> </dd> <dt>string `dt_send_local`</dt> <dd>Date of a gift card sending. Required for {@link \RsPurchaseItemSid::COUPON}.</dd> <dt>string `s_code`</dt> <dd>Code of a gift card. Required for {@link \RsPurchaseItemSid::COUPON}.</dd> <dt>string `k_staff`</dt> <dd> Staff member''s primary key in {@link \RsStaffSql} table. Required for {@link \RsPurchaseItemSid::APPOINTMENT_TIP}. </dd> </dl> </dd> <dt>array[] `a_tax`</dt> <dd> Custom taxes list. Each element has keys: <dl> <dt>string `k_tax`</dt> <dd>Tax primary key in {@link \RsTaxSql} table.</dd> <dt>string `m_tax`</dt> <dd>Tax amount.</dd> </dl> </dd> <dt>int `i_quantity`</dt> <dd>Item quantity.</dd> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID from {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Purchase item key in the database.</dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'The WellnessLiving mode type. One of the {@link ModeSid} constants.'
                k_business:
                  type: string
                  description: 'Business to add order for. Primary key in {@link \RsBusinessSql} table.'
                k_location:
                  type: string
                  description: 'Location to add order for. Primary key in {@link \RsLocationSql} table.'
                m_tip:
                  type: string
                  description: 'The tip amount (optional).'
                text_order:
                  type: string
                  description: 'Order key.'
                text_receipt_note:
                  type: string
                  description: 'The custom receipt note.'
                uid:
                  type: string
                  description: 'User to add order for. Primary key in {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_purchase:
                        type: string
                        description: 'Created purchase. Primary key in {@link \RsPurchaseSql} table.'
      tags:
        - Ecommerce-OrderSync
  '/Wl/Login/Add/MailUseOk.json':
    post:
      summary: 'Checks required profile fields and, if complete, registers the existing user in the specified business. Validates the user''s profile for any missing required fields and, if all fields are complete, adds the user as a member of the business. Returns a status code and a list of any fields that still need to be filled in.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_lead:
                  type: boolean
                  description: 'This value is `true` if this user is a lead. `false` if otherwise.'
                k_business:
                  type: string
                  description: 'The business key used internally by WellnessLiving.'
                k_skin:
                  type: string
                  description: 'The Lead Capture Widget skin key. This is used only if a lead was added.'
                uid:
                  type: string
                  description: 'The user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_list:
                        type: array
                        description: 'The list of fields with missing information. Each element is an array containing the following data: <dl> <dt>string `html_message`</dt> <dd>The error message.</dd> <dt>string `s_field`</dt> <dd>The name of the field missing information. Given in the format `k_field.name`.</dd> </dl>'
                      s_code:
                        type: string
                        description: 'The result code of the request.'
                      text_message:
                        type: string
                        description: 'The result message of the request.'
      tags:
        - Add-MailUseOk
  '/Wl/Login/Add/ProfilePurchaseOption.json':
    get:
      summary: 'Retrieves information about users by email.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_promotion_active:
                        type: string
                        description: 'User''s active purchase options data. For more details see {@link \RsProfilePassView::getPassData()}. Serialized with JSON.'
      tags:
        - Add-ProfilePurchaseOption
  '/Wl/Login/Attendance/AttendanceInfo.json':
    get:
      summary: 'Returns detailed information about a single class period, appointment, or asset session. Accepts either a class period key with a local date or an appointment key, validates access, and returns scheduling details including title, start and end times, location, staff, assets, service type, and default purchase option information.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'Start date of the class in MySQL format in local time.'
          schema:
            type: string
        - name: k_appointment
          in: query
          description: 'ID of appointment to get information for.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business to get information for.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'ID of class period to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_visit_info:
                        type: array
                        description: 'Additional visit information about this appointment. Empty array if it''s not an appointment: <dl> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl>'
                      a_logo:
                        type: array
                        description: 'Service logo information: <dl> <dt>bool `is_empty`</dt> <dd>Whether service image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl>'
                      a_purchase_option_default:
                        type: array
                        description: 'Default purchase option information. <dl> <dt>int|null `id_sale`</dt> <dd> The sale item type, one of the {@link \RsSaleSid} constants. This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>string|null `k_id`</dt> <dd> The default Purchase Option key. <i>Primary key in {@link \RsPromotionSql} table.</i> This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>bool `is_single_default`</dt> <dd>If the default Purchase Option is set to "Drop-in rate" then the value will be `true`, `false` otherwise.</dd> </dl>'
                      a_resource:
                        type: array
                        description: 'Assets which are bound to this session.'
                      a_resource_layout:
                        type: array
                        description: 'Asset layouts of session: <dl> <dt>array `a_client`</dt> <dd> List of clients who occupy assets of class. It is a double nesting array. Keys - <i>primary keys of assets. See {@link \RsResourceSql} table.</i> Sub keys - asset index. Values - sub array with keys: <dl> <dt>string `text_client`</dt> <dd>User''s name.</dd> <dt>string `uid`</dt> <dd>User''s primary key.</dd> </dl> </dd> <dt>string[] `a_resource_available`</dt> <dd>Key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_layout`</dt> <dd>Key of layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `text_resource_type`</dt> <dd>Title of asset category.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'List of staff members who provide service: <dl> <dt>array `a_logo`</dt> <dd> Data of staff member''s photo. Empty if staff has no photo. Otherwise contains next keys: <dl> <dt>int `i_height`</dt> <dd>Height in pixels.</dd> <dt>int `i_width`</dt> <dd>Width in pixels.</dd> <dt>string `url_logo`</dt> <dd>Image URL.</dd> </dl> </dd> <dt>string `k_staff`</dt> <dd>deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `html_firstname`</dt> <dd>Staff member''s first name.</dd> <dt>string `html_lastname`</dt> <dd>Staff member''s last name.</dd> </dl>'
                      dt_confirm:
                        type: string
                        description: 'Confirmation date+time of appointment in MySQL format. If client never confirmed, will be zero date + time.'
                      dt_date_global:
                        type: string
                        description: 'Start date of the session in MySQL format in GMT.'
                      dtl_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in local timezone.'
                      dtu_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in GMT.'
                      dtu_wait_promote:
                        type: string
                        description: 'Date and time in UTC when the visit is promoted from wait list to active list. Not empty for appointments.'
                      has_note:
                        type: boolean
                        description: 'Whether notes added to visit.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'Duration of the session in minutes.'
                      id_note:
                        type: [number, object]
                        description: 'Type of note. One of {@link \Wl\Visit\Note\Sid\NoteSid} constants. <tt>false</tt> if notes not allowed.'
                      id_service:
                        type: number
                        format: int32
                        description: 'Service type, one of {@link \RsServiceSid}.'
                      is_start_virtual_service:
                        type: boolean
                        description: 'Whether this service be carried out in Zoom. <tt>true</tt> - If the service can be carried out in Zoom. <tt>false</tt> - otherwise.'
                      k_class:
                        type: string
                        description: 'Class identifier. Not empty if service is class or event reservation.'
                      k_location:
                        type: string
                        description: 'Location identifier.'
                      k_resource:
                        type: string
                        description: 'Resource identifier. Not empty if service is asset reservation.'
                      k_service:
                        type: string
                        description: 'Service identifier. Not empty if service is appointment reservation.'
                      text_location:
                        type: string
                        description: 'Location name.'
                      text_time_end:
                        type: string
                        description: 'Time when service starts in format <tt>8:00AM</tt>.'
                      text_time_start:
                        type: string
                        description: 'Start time of the session in format ''9:30AM''.'
                      text_title:
                        type: string
                        description: 'Title of the appointment.'
                      url_booking:
                        type: string
                        description: 'URL that leads directly to the class/event booking page in the Client Web App. Empty string if the service is an appointment reservation.'
      tags:
        - Attendance-AttendanceInfo
  '/Wl/Login/Attendance/AttendanceList.json':
    get:
      summary: 'Returns the attendance list for a class period or appointment session. Accepts either a class period key with a local date or an appointment key, validates access and date, and returns the active list, wait list, and confirmed list of attending clients, along with session capacity, wait list limit, and per-client details such as purchase option, visit status, wearables, and quiz responses.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'The local date of the class or event session.'
          schema:
            type: string
        - name: is_purchase_info_return
          in: query
          description: 'If `true`, then return the purchase used to pay for session. Otherwise, do not return any purchase information.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. Not used if requesting information for a class or event session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. Not used if requesting information for an appointment.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list_active:
                        type: array
                        description: 'The list of clients in the active attendance list who haven''t confirmed or canceled. Each element is an array with the following fields: <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_confirm:
                        type: array
                        description: 'The list of clients who have confirmed their attendance. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_wait:
                        type: array
                        description: 'The list of clients who are on the wait list. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      i_capacity:
                        type: number
                        format: int32
                        description: 'The maximum capacity of the class or event session.'
                      i_client:
                        type: number
                        format: int32
                        description: 'Count client on the attendance.'
                      i_wait_list_limit:
                        type: number
                        format: int32
                        description: 'The maximum number of clients on wait list of the class or event session. `0` for appointments, use {@link AppointmentWaitListApi} instead.'
                      is_wait_list_limit:
                        type: boolean
                        description: '`true` to use class/event specific wait list limit, `false` to use the limit from default policies. `false` for appointments. Use {@link AppointmentWaitListApi} instead.'
                      k_location:
                        type: string
                        description: 'The Location key.'
      tags:
        - Attendance-AttendanceList
  '/Wl/Login/Attendance/AttendanceInfoByToken.json':
    get:
      summary: 'Returns detailed information about a single class period, appointment, or asset session. Accepts either a class period key with a local date or an appointment key, validates access, and returns scheduling details including title, start and end times, location, staff, assets, service type, and default purchase option information.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'Start date of the class in MySQL format in local time.'
          schema:
            type: string
        - name: k_appointment
          in: query
          description: 'ID of appointment to get information for.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business to get information for.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'ID of class period to get information for.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment_visit_info:
                        type: array
                        description: 'Additional visit information about this appointment. Empty array if it''s not an appointment: <dl> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl>'
                      a_logo:
                        type: array
                        description: 'Service logo information: <dl> <dt>bool `is_empty`</dt> <dd>Whether service image is empty.</dd> <dt>string `s_url`</dt> <dd>Url link to image.</dd> </dl>'
                      a_purchase_option_default:
                        type: array
                        description: 'Default purchase option information. <dl> <dt>int|null `id_sale`</dt> <dd> The sale item type, one of the {@link \RsSaleSid} constants. This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>string|null `k_id`</dt> <dd> The default Purchase Option key. <i>Primary key in {@link \RsPromotionSql} table.</i> This will be `null` if the class has no default Purchase Option, or it sets to "Drop-in rate". </dd> <dt>bool `is_single_default`</dt> <dd>If the default Purchase Option is set to "Drop-in rate" then the value will be `true`, `false` otherwise.</dd> </dl>'
                      a_resource:
                        type: array
                        description: 'Assets which are bound to this session.'
                      a_resource_layout:
                        type: array
                        description: 'Asset layouts of session: <dl> <dt>array `a_client`</dt> <dd> List of clients who occupy assets of class. It is a double nesting array. Keys - <i>primary keys of assets. See {@link \RsResourceSql} table.</i> Sub keys - asset index. Values - sub array with keys: <dl> <dt>string `text_client`</dt> <dd>User''s name.</dd> <dt>string `uid`</dt> <dd>User''s primary key.</dd> </dl> </dd> <dt>string[] `a_resource_available`</dt> <dd>Key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_layout`</dt> <dd>Key of layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `text_resource_type`</dt> <dd>Title of asset category.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'List of staff members who provide service: <dl> <dt>array `a_logo`</dt> <dd> Data of staff member''s photo. Empty if staff has no photo. Otherwise contains next keys: <dl> <dt>int `i_height`</dt> <dd>Height in pixels.</dd> <dt>int `i_width`</dt> <dd>Width in pixels.</dd> <dt>string `url_logo`</dt> <dd>Image URL.</dd> </dl> </dd> <dt>string `k_staff`</dt> <dd>deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `html_firstname`</dt> <dd>Staff member''s first name.</dd> <dt>string `html_lastname`</dt> <dd>Staff member''s last name.</dd> </dl>'
                      dt_confirm:
                        type: string
                        description: 'Confirmation date+time of appointment in MySQL format. If client never confirmed, will be zero date + time.'
                      dt_date_global:
                        type: string
                        description: 'Start date of the session in MySQL format in GMT.'
                      dtl_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in local timezone.'
                      dtu_end:
                        type: string
                        description: 'End date and time of the session in MySQL format in GMT.'
                      dtu_wait_promote:
                        type: string
                        description: 'Date and time in UTC when the visit is promoted from wait list to active list. Not empty for appointments.'
                      has_note:
                        type: boolean
                        description: 'Whether notes added to visit.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'Duration of the session in minutes.'
                      id_note:
                        type: [number, object]
                        description: 'Type of note. One of {@link \Wl\Visit\Note\Sid\NoteSid} constants. <tt>false</tt> if notes not allowed.'
                      id_service:
                        type: number
                        format: int32
                        description: 'Service type, one of {@link \RsServiceSid}.'
                      is_start_virtual_service:
                        type: boolean
                        description: 'Whether this service be carried out in Zoom. <tt>true</tt> - If the service can be carried out in Zoom. <tt>false</tt> - otherwise.'
                      k_class:
                        type: string
                        description: 'Class identifier. Not empty if service is class or event reservation.'
                      k_location:
                        type: string
                        description: 'Location identifier.'
                      k_resource:
                        type: string
                        description: 'Resource identifier. Not empty if service is asset reservation.'
                      k_service:
                        type: string
                        description: 'Service identifier. Not empty if service is appointment reservation.'
                      text_location:
                        type: string
                        description: 'Location name.'
                      text_time_end:
                        type: string
                        description: 'Time when service starts in format <tt>8:00AM</tt>.'
                      text_time_start:
                        type: string
                        description: 'Start time of the session in format ''9:30AM''.'
                      text_title:
                        type: string
                        description: 'Title of the appointment.'
                      url_booking:
                        type: string
                        description: 'URL that leads directly to the class/event booking page in the Client Web App. Empty string if the service is an appointment reservation.'
      tags:
        - Attendance-AttendanceInfoByToken
  '/Wl/Login/Attendance/AttendanceListByToken.json':
    get:
      summary: 'Returns the attendance list for a class period or appointment session. Accepts either a class period key with a local date or an appointment key, validates access and date, and returns the active list, wait list, and confirmed list of attending clients, along with session capacity, wait list limit, and per-client details such as purchase option, visit status, wearables, and quiz responses.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'The local date of the class or event session.'
          schema:
            type: string
        - name: is_purchase_info_return
          in: query
          description: 'If `true`, then return the purchase used to pay for session. Otherwise, do not return any purchase information.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. Not used if requesting information for a class or event session.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. Not used if requesting information for an appointment.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list_active:
                        type: array
                        description: 'The list of clients in the active attendance list who haven''t confirmed or canceled. Each element is an array with the following fields: <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_confirm:
                        type: array
                        description: 'The list of clients who have confirmed their attendance. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      a_list_wait:
                        type: array
                        description: 'The list of clients who are on the wait list. <dl> <dt>array `a_info`</dt> <dd> Information about member. <i>Result of {@link \Wl\Member\Info\MemberInfoView::dataPrepare()}.</i> <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if a photo hasn''t been set yet.</dd> <dt>string `s_url`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>array `a_progress`</dt> <dd> Information about a user''s current progress. By default, this information isn''t sent. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl> </dd> <dt>array[] `a_quiz`</dt> <dd> Quiz information that concerns current visit. <i>See {@link \Wl\Quiz\Response\QuizResponse::visitQuizGet()}.</i> <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. <i>Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key. <i>Primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of information for any associated resources for this visit. Each element: <dl> <dt>int `i_index`</dt> <dd>Index of the resource among multiple assets. `0` if the resource was booked from the wait list.</dd> <dt>string `k_resource`</dt> <dd>Resource key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> <dt>string `text_alias`</dt> <dd>Resource layout alias.</dd> <dt>string `text_title`</dt> <dd>Resource title.</dd> </dl> </dd> <dt>array `a_wait_confirm`</dt> <dd> The date and time when the client transitioned from the wait list to the active list, in human-readable format. Empty array if the visit is not awaiting confirmation. Each element: <dl> <dt>string `s_day`</dt> <dd>The formatted date string (for example, "Monday, Nov 08, 2021").</dd> <dt>string `s_time`</dt> <dd>The formatted time string (for example, "10:00 AM").</dd> </dl> </dd> <dt>array `a_wearable`</dt> <dd> A list of information pertaining to the client''s wearables. <dl> <dt>int `id_type`</dt> <dd>Device type ID. One of {@link \Wl\Profile\Sensor\SensorTypesSid} constants.</dd> <dt>bool `is_remove`</dt> <dd>`true` if the device has been removed.</dd> <dt>string `s_sensor`</dt> <dd>Sensor identifier. Also used as the array key.</dd> <dt>string `text_name`</dt> <dd>Device name.</dd> <dt>string `uid`</dt> <dd>User key. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>bool `can_profile`</dt> <dd>If `true`, the current user can access this client profile.</dd> <dt>string `dt_book`</dt> <dd>The date the session was booked, in UTC.</dd> <dt>string `dt_date`</dt> <dd>The date of the session, in UTC.</dd> <dt>string `dt_expire`</dt> <dd>The date the Purchase Option that was used will expire, in UTC.</dd> <dt>string `dt_register`</dt> <dd>The date the client checked in for the visit, in UTC.</dd> <dt>string `html_age`</dt> <dd>The client age.</dd> <dt>string `html_book_by`</dt> <dd>The name of the person who booked this visit.</dd> <dt>string `html_gender_class`</dt> <dd>The name of the icon to show under gender in the class attendance list.</dd> <dt>string `html_member`</dt> <dd>The client''s member ID, if set.</dd> <dt>string `html_tooltip_book_by`</dt> <dd>When and where this visit was booked, along with who booked it.</dd> <dt>int `i`</dt> <dd>The default place in the list to show this client. This is a deprecated copy of the `i_order` field.</dd> <dt>int `i_left`</dt> <dd>The number of visits left on this Purchase Option. This will be `NULL` if there''s no limit.</dd> <dt>int `i_order`</dt> <dd>The default place in the list to show this client.</dd> <dt>int `i_total`</dt> <dd>The total visits available from this Purchase Option.</dd> <dt>int `i_visit_return`</dt> <dd>Number of purchase option usages that was returned to user.</dd> <dt>int `id_gender`</dt> <dd>The gender ID of the client. One of the {@link \AGenderSid} constants.</dd> <dt>int `id_pass_prospect`</dt> <dd> If this visit was paid for by an external system, this ID will be set. Otherwise, it will be `0`. One of {@link \Wl\Visit\PassProspectSid} constants. </dd> <dt>int `id_program`</dt> <dd>The program ID of the Purchase Option. One of the {@link \RsProgramSid} constants.</dd> <dt>int `id_visit`</dt> <dd>The status of the visit. One of the {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_attend`</dt> <dd>This will be `true` if the client has attended the visit.</dd> <dt>bool `is_duration_pass`</dt> <dd>This will be `true` if the Purchase Option used is a duration pass.</dd> <dt>bool `is_deposit`</dt> <dd>This will be `true` if the client has paid a deposit for the visit.</dd> <dt>bool `is_early`</dt> <dd> This will be `true` if the client has canceled the visit early, incurring no penalty. This will be `false` if canceled late or if not canceled at all. </dd> <dt>bool `is_free`</dt> <dd>This will be `true` if the visit is free.</dd> <dt>bool `is_hidden`</dt> <dd>This will be `true` if details on this visit should be hidden from staff members.</dd> <dt>bool `is_penalty`</dt> <dd>This will be `true` if this visit was cancelled late and a penalty is applied.</dd> <dt>bool `is_promotion_first`</dt> <dd>This will be `true` if this was the first visit for the Purchase Option used.</dd> <dt>bool `is_promotion_last`</dt> <dd>This will be `true` if this was the latest visit for the Purchase Option used.</dd> <dt>bool `is_unlimited`</dt> <dd>This will be `true` if the Purchase Option used has no usage limits.</dd> <dt>bool `is_visit`</dt> <dd> This will be `true` if this visit is still considered valid. If visit was removed by the system or a staff member, this will be `false`. </dd> <dt>bool `is_wait`</dt> <dd>This will be `true` if the visit is on the waiting list.</dd> <dt>bool `is_wait_confirm`</dt> <dd>This will be `true` if the visit is awaiting confirmation.</dd> <dt>bool `is_wait_priority`</dt> <dd>This will be `true` if the visit has priority on the wait list.</dd> <dt>string `k_location`</dt> <dd>The key of the location.</dd> <dt>string `k_login_promotion`</dt> <dd>The key of the client''s Purchase Option.</dd> <dt>string `k_visit`</dt> <dd>The key of the visit.</dd> <dt>object|null `o_purchase_item`</dt> <dd> The purchase used to pay for the session. Instance of {@link \Wl\Profile\PurchaseList\PurchaseListElementApi}. `null` if the visit has no associated purchase or access is denied. Only present when `is_purchase_info_return` is `true`. </dd> <dt>string `s_expire`</dt> <dd>The expiry information for the Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_firstname`</dt> <dd>The client''s first name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_lastname`</dt> <dd>The client''s last name. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_note`</dt> <dd>Any notes tied to the client. This field is deprecated, use the text_ version instead.</dd> <dt>string `s_promotion`</dt> <dd>The description of the type of Purchase Option. This field is deprecated, use the text_ version instead.</dd> <dt>string `text_expire`</dt> <dd>The expiry information for the Purchase Option.</dd> <dt>string `text_firstname`</dt> <dd>The client first name.</dd> <dt>string `text_lastname`</dt> <dd>The client last name.</dd> <dt>string `text_note`</dt> <dd>Any notes tied to the client.</dd> <dt>string `text_promotion_return`</dt> <dd>Title of purchase option usages of that was returned to user.</dd> <dt>string `text_promotion`</dt> <dd>The description of the type of Purchase Option.</dd> <dt>string `text_restrict_title`</dt> <dd> The Purchase Option restriction description. It can be empty string when the Purchase Option has no restrictions. </dd> <dt>string `text_visit_status_class`</dt> <dd>The class visit status.</dd> <dt>string `text_visit_status_icon`</dt> <dd>The icon to use for the class visit status.</dd> <dt>string `text_wearable`</dt> <dd>The wearable device ID.</dd> <dt>string `uid`</dt> <dd>The client user key.</dd> <dt>string `uid_book`</dt> <dd> The key of the user who made the booking. If differing from `uid`, the booking could have been made by a staff or family member. </dd> <dt>string `url-cancel`</dt> <dd>The URL that can be used to cancel the visit.</dd> <dt>string `url-cancel-admin`</dt> <dd>The URL that can be used by an admin to cancel the visit.</dd> <dt>string `url-login-view`</dt> <dd>The URL that for the user''s page.</dd> <dt>string `url-mail`</dt> <dd>The URL to a page for sending an email to the client.</dd> <dt>string `url-profile`</dt> <dd>The URL that for the client''s profile.</dd> </dl>'
                      i_capacity:
                        type: number
                        format: int32
                        description: 'The maximum capacity of the class or event session.'
                      i_client:
                        type: number
                        format: int32
                        description: 'Count client on the attendance.'
                      i_wait_list_limit:
                        type: number
                        format: int32
                        description: 'The maximum number of clients on wait list of the class or event session. `0` for appointments, use {@link AppointmentWaitListApi} instead.'
                      is_wait_list_limit:
                        type: boolean
                        description: '`true` to use class/event specific wait list limit, `false` to use the limit from default policies. `false` for appointments. Use {@link AppointmentWaitListApi} instead.'
                      k_location:
                        type: string
                        description: 'The Location key.'
      tags:
        - Attendance-AttendanceListByToken
  '/Wl/Login/Member/MemberValidate63.json':
    get:
      summary: 'Checks whether anything prevents the user from using the business and returns details about missing required fields. Extends the base validation by additionally returning separate lists of missing profile fields grouped by their required context: booking and purchase, self-registration, or general requirement.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_empty_fields_booking:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for booking. <dl> <dt>int `id_field_general`</dt> <dd>The general field ID. One of {@link \RsFieldGeneralSid} constants.</dd> <dt>string `k_field`</dt> <dd>Profile field key. <i>Primary key in the {@link \RsFieldSql} table.</i></dd> <dt>string `text_field_title`</dt> <dd>Human-readable field title describing what value is missing.</dd> </dl>'
                      a_empty_fields_registration:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for registration. <dl> <dt>int `id_field_general`</dt> <dd>The general field ID. One of {@link \RsFieldGeneralSid} constants.</dd> <dt>string `k_field`</dt> <dd>Profile field key. <i>Primary key in the {@link \RsFieldSql} table.</i></dd> <dt>string `text_field_title`</dt> <dd>Human-readable field title describing what value is missing.</dd> </dl>'
                      a_empty_fields_required:
                        type: array
                        description: 'List of profile fields that are required but empty for this user. <dl> <dt>int `id_field_general`</dt> <dd>The general field ID. One of {@link \RsFieldGeneralSid} constants.</dd> <dt>string `k_field`</dt> <dd>Profile field key. <i>Primary key in the {@link \RsFieldSql} table.</i></dd> <dt>string `text_field_title`</dt> <dd>Human-readable field title describing what value is missing.</dd> </dl>'
                      has_credit_card:
                        type: boolean
                        description: '`true` If the user has credit cards on profile, otherwise `false`.'
                      has_outstanding_contract:
                        type: boolean
                        description: '`true` if the user has an outstanding contract, otherwise `false`.'
                      has_outstanding_waiver:
                        type: boolean
                        description: '`true` If the user has an outstanding waiver for the business, otherwise `false`.'
                      has_pending_quizzes:
                        type: boolean
                        description: '`true` If the user has pending registration quizzes to complete, otherwise `false`.'
                      is_booking_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details before booking, otherwise `false`.'
                      is_register_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details to finish their registration, otherwise `false`.'
                      k_location:
                        type: [string, "null"]
                        description: 'Home user`s location. `null` if user has not home location.'
      tags:
        - Member-MemberValidate63
  '/Wl/Login/Member/MemberValidate.json':
    get:
      summary: 'Checks whether anything prevents the user from using the business and returns any blocking conditions found. Returns flags indicating whether the user has an outstanding waiver, an unconfirmed contract, pending registration quizzes, a required credit card on file, and lists of missing required profile fields for booking and registration.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_empty_fields_booking:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for booking.'
                      a_empty_fields_registration:
                        type: array
                        description: 'List of fields if the user has empty profile fields, which are required for registration.'
                      has_credit_card:
                        type: boolean
                        description: '`true` If the user has credit cards on profile, otherwise `false`.'
                      has_outstanding_contract:
                        type: boolean
                        description: '`true` if the user has an outstanding contract, otherwise `false`.'
                      has_outstanding_waiver:
                        type: boolean
                        description: '`true` If the user has an outstanding waiver for the business, otherwise `false`.'
                      has_pending_quizzes:
                        type: boolean
                        description: '`true` If the user has pending registration quizzes to complete, otherwise `false`.'
                      is_booking_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details before booking, otherwise `false`.'
                      is_register_require_card:
                        type: boolean
                        description: '`true` If the user has to provide credit card details to finish their registration, otherwise `false`.'
                      k_location:
                        type: [string, "null"]
                        description: 'Home user`s location. `null` if user has not home location.'
      tags:
        - Member-MemberValidate
  '/Wl/Login/Member/Member.json':
    get:
      summary: 'Returns the list of businesses where the specified user is an active member. Accepts a user key and returns all active businesses where that user has a membership, including franchise relationship flags, business title, and whether the user is allowed to sign in to each business. Forwards the request to other data centers in multi-region deployments and merges the results.'
      parameters:
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: array
                        description: 'A list of businesses where the client is present. Every element is an array with the following keys: <dl> <dt>bool `can_enter`</dt> <dd> <tt>true</tt> if the user is allowed to sign in into this business; <tt>false</tt> otherwise. <i>This value is based on {@link \Wl\Login\Disable\LoginDisableSql} table.</i> </dd> <dt>bool `is_franchisee`</dt> <dd>If `true`, then the business is a franchisee. Otherwise, this will be `false`.</dd> <dt>bool `is_franchisor`</dt> <dd>If `true`, then the business is a franchisor. Otherwise, this will be `false`.</dd> <dt>string `k_business`</dt> <dd>The business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_business_franchisor`</dt> <dd> The business key of the Enterprise Headquarters account (if applicable). <i>Primary key in {@link \RsBusinessSql} table.</i> </dd> <dt>string `text_title`</dt> <dd>The title of the business.</dd> </dl>'
      tags:
        - Member-Member
    post:
      summary: 'Adds a user into a business. Validates that the current user has profile access, then registers the specified user as a member of the given business without sending a registration email.'
      parameters:
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Primary key of the business to add the user into.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Member-Member
  '/Wl/Login/Member/GenerateId.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_uid:
                  type: array
                  description: 'User''s ids.'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_amount:
                        type: number
                        format: int32
                        description: 'Amount of generated users ids.'
      tags:
        - Member-GenerateId
  '/Wl/Login/Member/LoginMember.json':
    get:
      summary: 'Gets client ID for a specific client in a business. Validates the business key and user key, checks profile access, and returns the member ID string assigned to the client in the specified business. Returns an empty string if the user has no member ID set.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_member:
                        type: string
                        description: 'The user''s ID to get/set.'
      tags:
        - Member-LoginMember
    post:
      summary: 'Updates client ID for a specific client in a business. Validates and saves the new member ID for the given client in the specified business, reindexes the user for search, and returns the date the user originally became a member.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: s_member
          in: query
          description: 'The user''s ID to get/set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dt_member:
                        type: string
                        description: 'The date when client became a member.'
      tags:
        - Member-LoginMember
  '/Wl/Login/Member/LoginMemberListAll.json':
    get:
      summary: 'Returns a paginated list of active member user keys for the specified business. Accepts a business key, an optional page size, and an optional last-seen user key for cursor-based pagination, then returns an ordered array of user keys for all active members of that business.'
      parameters:
        - name: i_page_size
          in: query
          description: 'The maximum number of members to return in the list. Use with `uid_last` to get the next page of results.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to get a list of members for.'
          schema:
            type: string
        - name: uid_last
          in: query
          description: 'The last member UID from the previous page. Use with `i_page_size` to get the next page of results. If empty, the first page of results is returned.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid:
                        type: array
                        description: 'List of UIDs for all active clients that belong to the business.'
      tags:
        - Member-LoginMemberListAll
  '/Wl/Login/Promotion/PromotionPayPause.json':
    delete:
      summary: 'Deletes specified promotion payment pause. Validates access and then permanently removes the hold period identified by `k_promotion_pay_pause`, also cancelling any associated expiry reminder notification task.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-PromotionPayPause
    get:
      summary: 'Returns promotion payment pause data: all hold periods when {@link PromotionPayPauseApi::$is_list} is `true`, the specified hold period when {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided, or the currently active hold period otherwise. Also returns notification settings (email, push, SMS flags and email pattern key) and the date the last notification was sent for the hold period, if a hold notification template is configured for the business.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: dt_start
          in: query
          description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: is_list
          in: query
          description: 'Whether need to get all pause periods for the login promotion. If <tt>true</tt> then {@link PromotionPayPauseApi::$a_pay_pause_list} will be returned. If <tt>false</tt> then information about specified {@link PromotionPayPauseApi::$k_promotion_pay_pause} or currently active pause period will be returned ({@link PromotionPayPauseApi::$dt_start}, {@link PromotionPayPauseApi::$dt_end} and {@link PromotionPayPauseApi::$text_note}).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_pause_list:
                        type: [array, "null"]
                        description: 'List of all promotion payment pause periods. Each element has next structure: <tt>null</tt> if {@link PromotionPayPauseApi::$is_list} is false. <dl> <dt>string|null `dl_create`</dt> <dd>Date when this hold period was created. <tt>null</tt> for old records.</dd> <dt>string `dl_end`</dt> <dd> Ending date of the pause (inclusively - this date is paused). This field contains zero date for promotions placed on hold indefinitely. </dd> <dt>string `dl_start`</dt> <dd>Starting date of the pause (inclusively - this date is paused).</dd> <dt>int|null `i_hold_day`</dt> <dd>Duration of the hold in days. <tt>null</tt> if the hold is ongoing.</dd> <dt>bool `is_past`</dt> <dd>Whether the hold is in past.</dd> <dt>string `k_promotion_pay_pause`</dt> <dd>Key of the hold period. <i>Primary key in the {@link \RsPromotionPayPauseSql} table.</i></dd> <dt>string|null `text_note`</dt> <dd>Additional notes.</dd> <dt>string|null `text_user_create`</dt> <dd>Full name of a user that has created hold period. <tt>null</tt> for old records.</dd> <dt>string|null `uid_create`</dt> <dd>Key of a user that has created hold period. <tt>null</tt> for old records.</dd> </dl>'
                      dt_end:
                        type: [string, "null"]
                        description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
                      dt_start:
                        type: [string, "null"]
                        description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
                      dtu_date_notification:
                        type: [string, "null"]
                        description: 'The date when the email notification was sent. `null` if it shouldn''t be updated.'
                      is_mail:
                        type: boolean
                        description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                      is_push:
                        type: boolean
                        description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                      is_sms:
                        type: boolean
                        description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
                      k_mail_pattern:
                        type: [string, "null"]
                        description: 'Key of the email pattern.'
                      k_promotion_pay_pause:
                        type: [string, "null"]
                        description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
                      text_note:
                        type: [string, "null"]
                        description: 'Additional notes for the promotion payment pause period. Leave this field as `null` if the note shouldn''t be updated. `null` if it shouldn''t be updated.'
      tags:
        - Promotion-PromotionPayPause
    post:
      summary: 'Adds or updates a payment pause period for promotion. Creates a new hold period for the purchased promotion if no `k_promotion_pay_pause` is provided, or updates an existing one. Optionally schedules or sends a hold notification via email, push, or SMS based on the provided flags and the business notification template.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: dt_start
          in: query
          description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option key. If this key is used, a new hold will be created. The endpoint will return a `start-cross` status code if a hold is already in place. Ignored if {@link PromotionPayPauseApi::$k_promotion_pay_pause} is provided. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_mail:
                  type: boolean
                  description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                is_push:
                  type: boolean
                  description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                is_sms:
                  type: boolean
                  description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                text_note:
                  type: [string, "null"]
                  description: 'Additional notes for the promotion payment pause period. Leave this field as `null` if the note shouldn''t be updated. `null` if it shouldn''t be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_promotion_pay_pause:
                        type: [string, "null"]
                        description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
      tags:
        - Promotion-PromotionPayPause
    put:
      summary: 'Updates a promotion payment pause period. Requires an existing `k_promotion_pay_pause` key and delegates to {@link PromotionPayPauseApi::post()} to apply the updated start date, end date, note, and notification settings. Returns an error if no existing hold key is provided.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date of the current hold, in the local time zone. This can be set to a special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: dt_start
          in: query
          description: 'The start date of the current hold, in the local time zone. `null` if it shouldn''t be updated.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of business to which currently handled pause period or login promotion belongs. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_promotion_pay_pause
          in: query
          description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_mail:
                  type: boolean
                  description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                is_push:
                  type: boolean
                  description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                is_sms:
                  type: boolean
                  description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                text_note:
                  type: [string, "null"]
                  description: 'Additional notes for the promotion payment pause period. Leave this field as `null` if the note shouldn''t be updated. `null` if it shouldn''t be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_promotion_pay_pause:
                        type: [string, "null"]
                        description: 'The promotion payment hold key. If this key is used, it will edit an existing hold. This key will be empty if there''s no active hold in place or if a scheduled hold isn''t in effect. `null` if not yet initialized or if the request is based on {@link PromotionPayPauseApi::$k_login_promotion}.'
      tags:
        - Promotion-PromotionPayPause
  '/Wl/Login/Promotion/PromotionCheckExtension.json':
    get:
      summary: ''
      parameters:
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion. Primary key of {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_extend:
                        type: number
                        format: int32
                        description: 'Number of days the contract will get extended'
                      is_expiration_date:
                        type: boolean
                        description: 'Whether to show warning upon disabling auto-renew `true` - login promotion has been applied to visit beyond its expiration date, `false` - otherwise.'
                      is_extend:
                        type: boolean
                        description: 'Whether login promotion will get extended upon disabling auto-renew. `true` - promotion will be extended, `false` - otherwise.'
      tags:
        - Promotion-PromotionCheckExtension
  '/Wl/Login/Promotion/PromotionPayCancel.json':
    put:
      summary: 'Updates a cancelled promotion''s options.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to which the promotion belongs. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion to update options for. Primary key in {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user performing the request. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_use:
                  type: boolean
                  description: 'Whether the client may use remaining visits after promotion cancellation.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Promotion-PromotionPayCancel
  '/Wl/Login/Search/Concerto.json':
    get:
      summary: 'Finds a user by their email or phone within the specified business. Accepts exactly one of `text_mail` or `text_phone`, validates the value, and returns the user key if a matching active member of the business is found. Returns an error if the user is not found or if multiple users share the given phone number.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to search user in.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Email to search.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'Phone to search.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'User''s primary key.'
      tags:
        - Search-Concerto
  '/Wl/Login/Search/SearchSettings.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_auto_check_in:
                        type: [boolean, "null"]
                        description: '`true` if found client should be automatically check-in if possible. `false` if not. `null` if setting should not be changed within the put request.'
                      is_franchisee_search_wide:
                        type: [boolean, "null"]
                        description: '`true` if enterprise clients (clients from different franchisees) should be included in the result. Can be applied only in the franchise businesses. `false`if not. `null` if setting should not be changed within the put request.'
                      is_inactive:
                        type: [boolean, "null"]
                        description: '`true` if inactive clients should be included in the result. `false`if not. `null` if setting should not be changed within the put request.'
                      is_show_client_id:
                        type: [boolean, "null"]
                        description: '`true` if member ID should be shown in the result. `false`if not. `null` if setting should not be changed within the put request.'
                      is_show_email:
                        type: [boolean, "null"]
                        description: '`true` if email should be shown in the result. `false` if not. `null` if setting should not be changed within the put request.'
                      is_show_phone:
                        type: [boolean, "null"]
                        description: '`true` if phone number should be shown in the result. `false` if not. `null` if setting should not be changed within the put request.'
      tags:
        - Search-SearchSettings
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_auto_check_in:
                  type: [boolean, "null"]
                  description: '`true` if found client should be automatically check-in if possible. `false` if not. `null` if setting should not be changed within the put request.'
                is_franchisee_search_wide:
                  type: [boolean, "null"]
                  description: '`true` if enterprise clients (clients from different franchisees) should be included in the result. Can be applied only in the franchise businesses. `false`if not. `null` if setting should not be changed within the put request.'
                is_inactive:
                  type: [boolean, "null"]
                  description: '`true` if inactive clients should be included in the result. `false`if not. `null` if setting should not be changed within the put request.'
                is_show_client_id:
                  type: [boolean, "null"]
                  description: '`true` if member ID should be shown in the result. `false`if not. `null` if setting should not be changed within the put request.'
                is_show_email:
                  type: [boolean, "null"]
                  description: '`true` if email should be shown in the result. `false` if not. `null` if setting should not be changed within the put request.'
                is_show_phone:
                  type: [boolean, "null"]
                  description: '`true` if phone number should be shown in the result. `false` if not. `null` if setting should not be changed within the put request.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Search-SearchSettings
  '/Wl/Login/Type/LoginType.json':
    get:
      summary: 'Gets a login types list of a business. Returns all client types configured for the specified business, each with its key, title, client type ID, and a deprecated membership flag. If `is_franchisor` is `true`, returns the combined login types of all franchisees under the franchisor business.'
      parameters:
        - name: is_franchisor
          in: query
          description: 'If `true`, this `k_business` is a franchisor, and login types of all franchisees should be returned.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key used internally by WellnessLiving.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_type_list:
                        type: array
                        description: 'A list of login types, keys, and information. Each element is an array with the following information: <dl> <dt>int `id_client_type`</dt> <dd>Client Type ID. One of {@link \Wl\Login\Type\ClientTypeSid} constants.</dd> <dt>bool `is_member`</dt> <dd> Deprecated Use <var>id_client_type</var> instead. It will be `1` if the record contains a type of member. It will be `0` if record contains some other type of client. It will be `null` if the record contains some type of prospect. </dd> <dt>string `k_login_type`</dt> <dd>The login type key.</dd> <dt>string `s_title`</dt> <dd>The name of the login type.</dd> <dt>string `text_title`</dt> <dd>Same as <var>s_title</var> offset.</dd> </dl>'
      tags:
        - Type-LoginType
  '/Wl/Login/Permission/Permission.json':
    post:
      summary: 'Saves the auto-renew setting for a purchased promotion. Validates the purchased promotion, checks profile access, and sets the auto-renew flag to the value of `is_renew`. Returns an error if the promotion does not support auto-renewal or if the client is a debtor.'
      parameters:
        - name: k_login_promotion
          in: query
          description: 'The key of the purchased promotion.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_renew:
                  type: boolean
                  description: 'If `true`, the purchased promotion has been made auto-renewable. Otherwise, this will be `false`.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Permission-Permission
  '/Wl/Login/Agree/Agree.json':
    get:
      summary: 'Returns text of business liability release if business has liability release and if user did not agree to this liability release. Accepts the business key and the user key, validates both, and returns the rendered HTML of the liability release contract if one is configured and the user has not yet agreed to it. If the user has already agreed, the response will have an empty `html_contract` field.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_contract:
                        type: string
                        description: 'Formatted text of business liability release. Not empty if the business has a liability release and if the user did not agree to this liability release.'
      tags:
        - Agree-Agree
    post:
      summary: 'Saves the user''s agreement to the online waiver. Accepts the user''s base64-encoded signature image and an optional electronic-signature consent flag, validates both, records the agreement date and signature in the database, and sends a waiver confirmation notification.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_agree:
                  type: [boolean, "null"]
                  description: '<tt>false</tt> if the user has not agreed to use Electronic Signatures, <tt>true</tt> if the user has agreed to use Electronic Signatures, <tt>null</tt> otherwise.'
                s_signature:
                  type: string
                  description: 'The base64-encoded user''s signature image from the signature pad as a PNG file.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Agree-Agree
  '/Wl/Login/Activity/Share.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_activity:
                  type: array
                  description: 'List of IDs of user''s activity. Primary keys in {@link \RsLoginActivitySql} table.'
                is_facebook:
                  type: boolean
                  description: '<tt>true</tt> - share activity on Facebook; <tt>false</tt> - do not share on Facebook.'
                is_twitter:
                  type: boolean
                  description: '<tt>true</tt> - share activity on Twitter; <tt>false</tt> - do not share on Twitter.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Activity-Share
  '/Wl/Login/Rank/LoginRankList.json':
    get:
      summary: 'Returns list of ranks for specified users or login ranks.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'Class key. Used to filter the list of ranks by their visit conditions. `null` if no need to filter by class.'
          schema:
            type: [string, "null"]
        - name: s_user_key
          in: query
          description: 'UIDs of users encoded by Javascript analogue of the {@link \Core\Tool\UrlEncode\UrlEncode::encode()} method.'
          schema:
            type: string
        - name: s_user_rank_category
          in: query
          description: 'Users with rank categories encoded by Javascript analogue of the {@link \Core\Tool\UrlEncode\UrlEncode::encode()} method. This logic was selected, because there is a possibility to promote belts of too many users. In this case URI is too long. <tt>null</tt> if not initialized.'
          schema:
            type: string
        - name: show_user_detail
          in: query
          description: 'Whether need to return user detail information (mail, phone).'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_user_list:
                        type: array
                        description: 'List of users with information about their ranks in the business.'
      tags:
        - Rank-LoginRankList
    post:
      summary: 'Saves ranks for specified users in the business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_user_rank_save:
                  type: string
                  description: 'Users with rank categories and ranks that need to be saved encoded by Javascript analogue of the {@link \Core\Tool\UrlEncode\UrlEncode::encode()} method. <tt>null</tt> if not initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Rank-LoginRankList
  '/Wl/Login/Rank/LoginRankElement.json':
    delete:
      summary: 'Deletes a rank record for a user.'
      parameters:
        - name: is_rank_category
          in: query
          description: 'To delete entire rank category from this user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_rank
          in: query
          description: 'Login rank key. Primary key in {@link \RsLoginRankSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Rank-LoginRankElement
  '/Wl/Login/Coupon/Coupon.json':
    get:
      summary: 'Retrieves the key and balance of a gift card by its code for the specified business. Validates the gift card code against the specified business, enforces a per-IP rate limit, and checks that the card is active, not already redeemed, not expired, and in the correct currency before returning its key and remaining balance.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: s_code
          in: query
          description: 'The gift card.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_login_coupon:
                        type: string
                        description: 'The gift card reference number for this specific user. WellnessLiving uses this reference number for managing gift cards in the system.'
                      m_amount:
                        type: string
                        description: 'The gift card amount.'
      tags:
        - Coupon-Coupon
  '/Wl/Login/Location/LoginLocation.json':
    get:
      summary: 'Gets locations ever visited by user.'
      parameters:
        - name: uid
          in: query
          description: 'Key of user to get data for. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'Keys of locations. Primary keys in {@link \RsLocationSql} table.'
      tags:
        - Location-LoginLocation
  '/Wl/Login/Code/Code.json':
    post:
      summary: 'Applies login code.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: number
                  format: int32
                  description: 'Key of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
                k_business:
                  type: string
                  description: 'Key of current business.'
                s_code:
                  type: string
                  description: 'Login code.'
                uid:
                  type: string
                  description: 'Key of a user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Code-Code
  '/Wl/Login/Mail/MailUse.json':
    get:
      summary: 'Checks if specified user exists in specified business. Accepts a business key and an email address, validates both, and returns `true` if a user with that email is an active member of the business. Requests are rate-limited per IP unless the caller has the required privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business for which the email address search is being performed.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The email address to check for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exists:
                        type: boolean
                        description: 'If <tt>true</tt>, the user with the specified email address exists in specified business. Otherwise, this will be <tt>false</tt>.'
                      uid_use:
                        type: [string, "null"]
                        description: 'Key of the user who using email within the business.'
      tags:
        - Mail-MailUse
  '/Wl/Login/Product/Product.json':
    get:
      summary: 'Returns information about products purchased by client. Accepts filters for business, user, location, payment method, product option, price range, and date range, then returns a paginated list of product purchases with quantity, unit price, total amount, product name, and purchase location.'
      parameters:
        - name: dtu_end
          in: query
          description: 'Maximal date and time of purchase in UTC. Empty string means no filter by maximal date.'
          schema:
            type: string
        - name: dtu_start
          in: query
          description: 'Minimal date and time of purchase in UTC. Empty string means no filter by minimal date.'
          schema:
            type: string
        - name: i_page
          in: query
          description: 'Number of the page to get.'
          schema:
            type: number
            format: int32
        - name: i_page_size
          in: query
          description: 'Page size.'
          schema:
            type: number
            format: int32
        - name: id_pay_method
          in: query
          description: 'ID of the payment method. One of the {@link \RsPayMethodSid} constants. Zero means no filter by payment method.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Empty string means no filter by location.'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'Product option key. Empty string means no filter by product option.'
          schema:
            type: string
        - name: m_price_max
          in: query
          description: 'Maximum price of the product. 0 means no filter by maximum price.'
          schema:
            type: string
        - name: m_price_min
          in: query
          description: 'Minimum price of the product.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Empty string means return products purchased by walk-in.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_product:
                        type: array
                        description: 'List of purchased products: <dl> <dt>string `dtu_purchase`</dt> <dd>Date and time of purchase in UTC.</dd> <dt>int `i_quantity`</dt> <dd>Quantity of the product purchased.</dd> <dt>string `k_shop_product_option`</dt> <dd>Key of product. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> <dt>string `m_price`</dt> <dd>Price per unit of the product.</dd> <dt>string `m_total_amount`</dt> <dd>Total amount paid for the product.</dd> <dt>string `text_location`</dt> <dd>Location where the purchase was made.</dd> <dt>string `text_product`</dt> <dd>Name of the purchased product.</dd> </dl>'
      tags:
        - Product-Product
  '/Wl/Mail/Pattern/PatternVariable.json':
    get:
      summary: 'Gets the list of available template variables.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of the notification.'
          schema:
            type: number
            format: int32
        - name: id_mail_form
          in: query
          description: 'SID of the mail form.'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_variable:
                        type: [object]
                        description: 'List of template variables. Each element is array: <dl> <dt>bool `is_global`</dt> <dd>Whether variable global or template specific.</dd> <dt>string `text_description`</dt> <dd>Variable description.</dd> <dt>string `text_field`</dt> <dd>Variable field name.</dd> <dt>string `text_parameter`</dt> <dd>Variable name.</dd> </dl>'
      tags:
        - Pattern-PatternVariable
  '/Wl/Mail/Group/MailGroupOpen.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mail_group:
                  type: number
                  format: int32
                  description: 'Mail group ID. One of {@link \RsMailGroupSid} constants.'
                is_remove:
                  type: boolean
                  description: '<tt>true</tt> If mail group is hide, <tt>false</tt> - otherwise.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
                uid:
                  type: string
                  description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Group-MailGroupOpen
  '/Wl/Mail/Verify/MailVerify.json':
    post:
      summary: 'Adds an email address to the list of identities for our Amazon SES account in the current AWS region and attempts to verify it. As a result of executing this operation, a verification email is sent to the specified address. If the operation to Amazon is successful this method changes status of the {@link MailVerifyStatusOption} option in {@link BusinessConfigOptionSql} table and adds task to check mail verification status.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                text_email:
                  type: string
                  description: 'Email address.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Verify-MailVerify
  '/Wl/Mail/Domain/DomainVerify.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                text_domain:
                  type: string
                  description: 'Domain name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Domain-DomainVerify
  '/Wl/Mail/Domain/DomainRefresh.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Domain-DomainRefresh
  '/Wl/Mail/Domain/DomainRemove.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Domain-DomainRemove
  '/Wl/Mail/Domain/MailDomainStatus.json':
    get:
      summary: 'Returns the domain verification status for the business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_domain_settings_access:
                        type: boolean
                        description: 'Indicates if the current user has access to Setup > Business > Email Settings. `true` if the user may follow the domain verification link. `false` if the user should be directed to ask an administrator instead.'
                      is_domain_verified:
                        type: boolean
                        description: 'Indicates if the business email domain is verified. `true` if the domain is configured and its status is {@link DomainVerifyStatusSid::SUCCESS}.'
      tags:
        - Domain-MailDomainStatus
  '/Wl/Member/Group/GroupTemporary.json':
    get:
      summary: 'Gets users in the quick group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_empty:
                        type: boolean
                        description: '<tt>true</tt> if there are no clients is the quick group; <tt>false</tt> otherwise.'
      tags:
        - Group-GroupTemporary
  '/Wl/Member/Info/Info.json':
    get:
      summary: 'Returns information about specified member. Supports both single-user and batch modes: when `$a_uid` is provided, returns a keyed list of user data in `$a_result_list`; otherwise returns data for the single user identified by `$uid`. When `$is_full` is `true`, additional profile details, group membership, visit history, and lifetime value are included.'
      parameters:
        - name: a_uid
          in: query
          description: 'Primary keys of users whose information must be returned. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: a_uid_date
          in: query
          description: 'List of dates for load additional information about users. Key is UID of user. Value is date. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'Date of the session, if we show it on the appointment info window or on the attendance list.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: '`true` - if API is being used from backend, `false` - otherwise.'
          schema:
            type: boolean
        - name: is_full
          in: query
          description: 'If you need to return additional information set to `true` or `false` if not.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business ID required to access client information. Specify this as `0` to retrieve the system-wide version of the information.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'ID of the visit, if we show icons on the attendance list and information that depends on visit is required.'
          schema:
            type: string
        - name: s_show
          in: query
          description: 'A list of icons with additional information about the business member. If empty, all available icons will be displayed. Comma separated values from {@link MemberInfoSid::idSid()} method.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to retrieve member information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: [array, "null"]
                        description: 'Additional member data or `null` if any data can be shown. Will be filled if {@link InfoApi::$is_full} flag is set. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl>'
                      a_result_list:
                        type: [array, "null"]
                        description: 'List of users data. Keys - users primary keys. Values - users data. <i>See for {@link InfoApi::_get()} details.</i> Keys refer to clients'' primary keys, and values refer to clients'' data. If `null`, data for a single client is being requested. <dl> <dt>array `a_info`</dt> <dd> Additional user''s information. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>bool `is_traveller`</dt> <dd>`true` - user is traveller; `false` - not traveller.</dd> <dt>string `s_member`</dt> <dd>User''s member number.</dd> <dt>string `text_first_name`</dt> <dd>User''s first name.</dd> <dt>string `text_fullname`</dt> <dd>User''s name.</dd> <dt>string `url_barcode`</dt> <dd>Link to barcode image to scan member number.</dd> </dl>'
                      a_visit_last:
                        type: array
                        description: 'Information about last visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      a_visit_next:
                        type: array
                        description: 'Information about next visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      i_lifetime_visit:
                        type: number
                        format: int32
                        description: 'Count attend visits for one client.'
                      is_traveller:
                        type: [boolean, "null"]
                        description: 'If `true`, the client is a traveler. Otherwise, this will be `false`. This will be `null` in cases when the client is a walk-in. Or when {@link InfoApi::$is_full} is `false`.'
                      m_lifetime_value:
                        type: string
                        description: 'The member''s lifetime value.'
                      s_member:
                        type: [string, "null"]
                        description: 'The member ID. If `null`, the specified client isn''t a member of the specified business.'
                      text_first_name:
                        type: string
                        description: 'First user''s name.'
                      text_fullname:
                        type: string
                        description: 'Full user''s name.'
                      url_barcode:
                        type: string
                        description: 'URL to barcode image.'
                      url_email:
                        type: string
                        description: 'URL to email.'
      tags:
        - Info-Info
  '/Wl/Member/Purchase/MemberByPromotion.json':
    get:
      summary: 'Returns active clients holding at least one active Purchase Option from the specified list. Accepts a comma-separated list of Purchase Option keys, validates them against the business, and returns all active clients who hold at least one non-expired, non-limited Purchase Option from that list. Clients to which the currently signed-in user has no profile access are excluded from the result.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business for which to get a list of clients.'
          schema:
            type: string
        - name: s_promotion_keys
          in: query
          description: 'The comma-separated list of the Purchase Option keys. Only active clients with at least one active Purchase Option from this list will be returned. For example: ''324,123,11,556'''
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_clients:
                        type: array
                        description: 'The list of active clients with the given Purchase Options. <dl> <dt>array[] `a_purchase_options`</dt> <dd> The list of active Purchase Options. Each element has: <dl> <dt>string|null `dl_end`</dt> <dd> The date in the location time zone when the Purchase Option expires. This will be `null` if the Purchase Option doesn''t expire. </dd> <dt>string `dtu_purchase`</dt> <dd>The global date and time when the Purchase Option was sold.</dd> <dt>string|null `dl_start`</dt> <dd> The date in location''s time zone when the Purchase Option starts. This will be `null` if not defined yet. For example, if the starting date is the date of the first visit and there haven''t been any visits yet. </dd> <dt>string|null `dl_terminate`</dt> <dd> The date in location time zone when the Purchase Option will be terminated. This will be `null` if the Purchase Option isn''t scheduled to be terminated. </dd> <dt>string `k_promotion`</dt> <dd>The key of the Purchase Option.</dd> </dl> </dd> <dt>string `uid`</dt> <dd>The key of the client.</dd> </dl>'
      tags:
        - Purchase-MemberByPromotion
  '/Wl/Notification/Banner/BannerList.json':
    get:
      summary: 'Loads all banners that should be shown to the client.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business where the list of available banners will be loaded. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User for which available banners will be loaded. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_banner_list:
                        type: array
                        description: 'List of banners.'
      tags:
        - Banner-BannerList
    put:
      summary: 'Stores that user closes the banner.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business where the list of available banners will be loaded. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User for which available banners will be loaded. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_notification_banner:
                  type: string
                  description: 'Key of the banner to be closed. Primary key in the {@link \Wl\Notification\Banner\BannerSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Banner-BannerList
  '/Wl/Notification/Send/NotificationSend.json':
    post:
      summary: 'Schedules or immediately sends a notification to clients of the specified business or location. Validates the business, optional location, and notification ID, then either dispatches the notification immediately or schedules it at the UTC date and time provided in `$dtu_send` (must be within 7 days). Optionally restricts recipients to specific UIDs via `$s_uid`. Requires the `rs.profile` privilege.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Custom array with information which can be used to generate notification. Use {@link NotificationInfoApi} for more information about available fields.'
                dtu_send:
                  type: string
                  description: 'Date and time in UTC, when notification should be sent. Empty string means to send immediately. If date and time are set, it should be reasonable. If it''s too far in the future, API returns an error.'
                id_notification:
                  type: number
                  format: int32
                  description: 'ID of the notification. See {@link \RsMailSid}. Contact with WellnessLiving support to get the list of available notifications.'
                k_business:
                  type: string
                  description: 'Key of the business where notification should be sent.'
                k_location:
                  type: [string, "null"]
                  description: 'Key of the location. If it''s not empty, only clients from this location will get notification. `null` to not limit recipients with a certain location.'
                s_uid:
                  type: string
                  description: 'List of UIDs joined with comma, if notification should be sent to certain recipients. Empty string means to send to all clients of the business or location with proper subscription level.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Send-NotificationSend
  '/Wl/Notification/Send/NotificationInfo.json':
    get:
      summary: 'Returns configuration information about the specified notification for the given business. Validates the business key and notification ID, then returns the notification configuration including mail template settings and channel availability for the given business. Requires the `rs.profile` privilege.'
      parameters:
        - name: id_notification
          in: query
          description: 'ID of the notification. See {@link \RsMailSid}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business where information about notification should be retrieved.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: array
                        description: 'Information about mail.'
      tags:
        - Send-NotificationInfo
  '/Wl/Notification/Send/MessageSend.json':
    get:
      summary: 'Checks user list, returns template variables and parameters.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of users for sent message to. Each value is a primary key from {@link \PassportLoginSql} table.'
          schema:
            type: array
        - name: id_mail
          in: query
          description: 'ID of the notification. One of {@link \RsMailSid} constants. `0` for empty template.'
          schema:
            type: number
            format: int32
        - name: id_mail_form
          in: query
          description: 'Type of the notification. One of {@link \RsMailFormSid} constants. `0` if not set.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business where notification should be sent. Primary key in {@link \RsBusinessSql} table. Empty string if not set.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid_checked:
                        type: array
                        description: 'List of users who can receive message. Each value is a primary key from {@link \PassportLoginSql} table.'
                      a_variable:
                        type: [object]
                        description: 'List of template variables. Each element is array: <dl> <dt>bool `is_global`</dt> <dd>Whether variable global or template specific.</dd> <dt>string `text_description`</dt> <dd>Variable description.</dd> <dt>string `text_parameter`</dt> <dd>Variable name.</dd> </dl>'
                      html_mail:
                        type: string
                        description: 'HTML version of the email pattern.'
                      id_mail_copy_business:
                        type: number
                        format: int32
                        description: 'Type if copy to business. One of {@link \RsMailCopySid} constants. `0` if not set.'
                      is_mail:
                        type: boolean
                        description: '`true` if email configured for notification, `false` otherwise.'
                      is_push:
                        type: boolean
                        description: '`true` if push configured for notification, `false` otherwise.'
                      is_sms:
                        type: boolean
                        description: '`true` if SMS configured for notification, `false` otherwise.'
                      k_mail_pattern:
                        type: string
                        description: 'Key of the mail pattern. Primary key in {@link \RsMailPatternSql} table.'
                      text_business_mail:
                        type: string
                        description: 'Business copy e-mail.'
                      text_business_name:
                        type: string
                        description: 'Business reply to name.'
                      text_business_reply:
                        type: string
                        description: 'Business reply to email.'
                      text_mail:
                        type: string
                        description: 'Text version of the email pattern.'
                      text_push:
                        type: string
                        description: 'Pattern for the push notification.'
                      text_sms:
                        type: string
                        description: 'Pattern for the SMS notification.'
                      text_subject:
                        type: string
                        description: 'Mail subject.'
      tags:
        - Send-MessageSend
    post:
      summary: 'Send message to users.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Send-MessageSend
  '/Wl/Notification/Otp/PhoneOtp.json':
    get:
      summary: 'Sends an OTP code to the specified phone number for verification. Checks that the rate limit for OTP generation has not been exceeded, generates a new code, and dispatches it to the given phone number via SMS. Requires a special privilege to use this endpoint.'
      parameters:
        - name: text_phone
          in: query
          description: 'Phone number to be validated.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Otp-PhoneOtp
    post:
      summary: 'Verifies the OTP code submitted by the user for the specified phone number. Checks that the submitted `$s_otp_code` matches the code previously generated for `$text_phone` and has not expired. Returns a validation error if the code is absent, incorrect, or expired.'
      parameters:
        - name: text_phone
          in: query
          description: 'Phone number to be validated.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp_code:
                  type: string
                  description: 'Otp code integer that user entered on the form. Will be compared with otp code, which was sent previously.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Otp-PhoneOtp
  '/Wl/Page/Backend/BackendConfig.json':
    get:
      summary: 'Returns backend settings for the given user inside given business. Also prepares and returns data sets for the left, right, and navigation panels.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key from {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_head_quick:
                        type: array
                        description: 'Data for the navigation banner template. Method {@link \Wl\Page\Backend\BackendConfigApi::headQuick()} is preparing the data.'
                      a_panel_left:
                        type: array
                        description: 'Data for the left panel template. Method {@link \Wl\Page\Backend\BackendConfigApi::panelLeft()} is preparing the data.'
                      can_partner:
                        type: boolean
                        description: 'Whether user has passed partner program manage privilege.'
                      can_search_franchise:
                        type: boolean
                        description: 'Whether current user can search for enterprise clients or not.'
                      can_search_uid:
                        type: boolean
                        description: 'Whether user can use smart search or not.'
                      is_franchisee_search_wide:
                        type: boolean
                        description: 'Whether enterprise clients should be included in the search or not.'
      tags:
        - Backend-BackendConfig
  '/Wl/Page/Backend/Config.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where we want to change staff back office settings.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_map_state:
                        type: [array, "null"]
                        description: 'Each element is about one panel in the left navigation. Any number of panels can be sent. All panels, which are not sent, will not be changed. `null` to not change current setting in put requests. <dl> <dt>int `id_map`</dt> <dd>Left navigation panel from {@link \Wl\Page\Map\MapSid}.</dd> <dt>bool `is_close`</dt> <dd>bool `true` if panel is minimized, `false` - otherwise.</dd> </dl>'
                      is_search_lock:
                        type: [boolean, "null"]
                        description: '`true` if search field is locked in opened mode. `false` if search field is closed until user clicks on it. `null` to not change current setting in put requests.'
      tags:
        - Backend-Config
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where we want to change staff back office settings.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_map_state:
                  type: [array, "null"]
                  description: 'Each element is about one panel in the left navigation. Any number of panels can be sent. All panels, which are not sent, will not be changed. `null` to not change current setting in put requests. <dl> <dt>int `id_map`</dt> <dd>Left navigation panel from {@link \Wl\Page\Map\MapSid}.</dd> <dt>bool `is_close`</dt> <dd>bool `true` if panel is minimized, `false` - otherwise.</dd> </dl>'
                is_search_lock:
                  type: [boolean, "null"]
                  description: '`true` if search field is locked in opened mode. `false` if search field is closed until user clicks on it. `null` to not change current setting in put requests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Backend-Config
  '/Wl/Page/BackAdmin/BackAdmin.json':
    post:
      summary: 'Switches administrator back to his administrator account when he signed in as a different user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'A link to redirect to an administrator account who is currently signed as a different user.'
      tags:
        - BackAdmin-BackAdmin
  '/Wl/Postcard/Campaign/Campaign.json':
    delete:
      summary: 'Deletes mail campaign.'
      parameters:
        - name: k_postcard_campaign
          in: query
          description: 'ID of mail campaign. Primary key in {@link \Wl\Postcard\Campaign\CampaignSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Campaign-Campaign
  '/Wl/Postcard/Campaign/CampaignDuplicate.json':
    post:
      summary: 'Duplicates mail campaign.'
      parameters:
        - name: k_postcard_campaign
          in: query
          description: 'ID of mail campaign. Primary key in {@link \Wl\Postcard\Campaign\CampaignSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Campaign-CampaignDuplicate
  '/Wl/Postcard/Campaign/CampaignSend.json':
    post:
      summary: 'Performs force sending of mail campaign.'
      parameters:
        - name: k_postcard_campaign
          in: query
          description: 'ID of mail campaign. Primary key in {@link \Wl\Postcard\Campaign\CampaignSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Campaign-CampaignSend
  '/Wl/Quiz/Element/QuizXMLParse.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_fields:
                  type: array
                  description: 'Fields to be XML parsed. Keys are field names that prefixed with xml_. Values are XML strings to be parsed and converted to HTML.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_results:
                        type: array
                        description: 'Fields that were XML parsed. Keys are field names that prefixed with html_. Values are HTML strings.'
      tags:
        - Element-QuizXMLParse
  '/Wl/Quiz/Response/Response.json':
    delete:
      summary: 'Deletes the specified quiz response or list of responses for the given business. Validates access privileges and removes the specified response records, updating any related search indexes and activity logs.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response
    get:
      summary: 'Returns quiz response data including element answers, dates, and access information. Loads the response for the specified quiz and user, resolving answers, formatted dates, and access flags such as amendment availability and PDF generation support.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_answer
          in: query
          description: '`true` for load answers for response, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_service_info:
                        type: array
                        description: 'Information about service if response connected to visit. Empty array if the response is not connected to a visit: <dl> <dt>string `text_date`</dt> <dd>Formatted visit date and time in the location''s time zone.</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `text_service`</dt> <dd>Service title.</dd> <dt>string `text_staff_member`</dt> <dd>Comma-separated list of full names of staff members conducting the visit.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether response can be amended by current user.'
                      dtu_response:
                        type: string
                        description: 'Date when response was submitted.'
                      id_source:
                        type: number
                        format: int32
                        description: 'Response source. One of {@link SourceSid} constants.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Response status ID.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_add_date:
                        type: string
                        description: 'Date when response added.'
                      text_amend_date:
                        type: string
                        description: 'Date when response amended.'
                      text_amend_user:
                        type: string
                        description: 'Name of the user who amend the response.'
                      text_complete_date:
                        type: string
                        description: 'Date when response completed.'
                      text_complete_user:
                        type: string
                        description: 'Name of the user who complete the response.'
                      text_response_by:
                        type: string
                        description: 'Name of the user who owned the response.'
                      text_title:
                        type: [string, "null"]
                        description: 'Title of the filled form. `null` in case when not filled yet.'
      tags:
        - Response-Response
    post:
      summary: 'Saves a quiz response with the given element answers. Validates the submitted answers and persists the response record in a transaction. If {@link \Core\Quiz\QuizResponseApi::$is_validate_only} is set, only validation runs and no record is created. Pass {@link \Core\Quiz\QuizResponseApi::$is_skip} to bypass validation for pre-confirmed responses.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_element_file:
                  type: array
                  description: 'Elements'' uploaded files.'
                a_purchase_item:
                  type: array
                  description: 'List of purchase items for which this form is loaded. The list of purchase items. Each element has the format `[id_purchase_item]::[k_id]`, where: Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
                a_quick_config:
                  type: array
                  description: 'Data for Quick Buy. Empty array if this is not a Quick Buy session. <dl> <dt>int `id_table`</dt> <dd>Quick purchase type. One of {@link \Wl\Catalog\QuickPurchaseTypeSid} constant.</dd> <dt>bool `is_quick_buy`</dt> <dd> Whether quick buy mode should be used. It means that specific template will be loaded and specific cart will be used. </dd> <dt>string `k_appointment`</dt> <dd>Appointment key to pay for. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_id`</dt> <dd>Item key within <var>id_table</var> table that Quick Buy performed for.</dd> <dt>string `k_visit`</dt> <dd>Visit key to pay for. <i>Primary key in {@link \RsVisitSql}.</i></dd> <dt>string `uid`</dt> <dd>User that Quick Buy performed for.</dd> </dl>'
                id_source:
                  type: number
                  format: int32
                  description: 'Response source. One of {@link SourceSid} constants.'
                is_backend:
                  type: boolean
                  description: '`true` if API is being used from backend, `false` otherwise.'
                is_draft:
                  type: boolean
                  description: '`true` if need to save response in draft mode, `false` otherwise.'
                is_skip:
                  type: boolean
                  description: 'Whether the quiz needs to be skipped.'
                is_validate_only:
                  type: boolean
                  description: '`true` if need make only validation, `false` need make validation and saving response.'
                json_element:
                  type: string
                  description: 'List of quiz questions with responses in JSON format.'
                k_quiz_login:
                  type: string
                  description: 'Quiz login key.'
                s_secret:
                  type: string
                  description: 'Secret key for access to quiz request.'
                text_appointment_wizard_id:
                  type: [string, "null"]
                  description: 'Booking appointment wizard ID. Not `null` if quiz response was sent during booking appointment process.'
                text_class_wizard_id:
                  type: [string, "null"]
                  description: 'Booking class wizard ID. Not `null` if quiz response was sent during booking class process.'
                uid_response:
                  type: [string, "null"]
                  description: 'UID of the user who makes response for quiz.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz_response:
                        type: [string, "null"]
                        description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
      tags:
        - Response-Response
    put:
      summary: 'Validates, updates and reindex response information for associated user. Used to amend an already-submitted response, for example when a business allows clients to edit their quiz answers after submission. Re-links the response to its owner and triggers downstream reindexing so search and reporting stay consistent.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
        - name: uid_link
          in: query
          description: 'User''s key for the response association.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_quiz_response_key:
                  type: string
                  description: 'Quiz response key list.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response
  '/Wl/Quiz/Response/ReportPostpone.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of a business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_response_report:
                        type: string
                        description: 'Quiz response report.'
      tags:
        - Response-ReportPostpone
  '/Wl/Quiz/Response/ResponseClone.json':
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz_response:
                        type: string
                        description: 'Quiz response key.'
      tags:
        - Response-ResponseClone
  '/Wl/Quiz/Response/Response65.json':
    delete:
      summary: 'Deletes the specified quiz response or list of responses for the given business. Validates access privileges and removes the specified response records, updating any related search indexes and activity logs.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response65
    get:
      summary: 'Returns quiz response data including element answers, dates, and access information. Loads the response for the specified quiz and user, resolving answers, formatted dates, and access flags such as amendment availability and PDF generation support.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_answer
          in: query
          description: '`true` for load answers for response, `false` otherwise.'
          schema:
            type: boolean
        - name: is_backend
          in: query
          description: ''
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access_log:
                        type: array
                        description: 'Access log data. <dl> <dt>string `dtu_activity`</dt> <dd>Date and time of the quiz changes.</dd> <dt>string `uid_actor`</dt> <dd>User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>int `id_activity`</dt> <dd>One of {@link \Wl\Quiz\Activity\ActivitySid} constants.</dd> <dt>string `text_activity`</dt> <dd>Title of the activity</dd> <dt>string `text_date`</dt> <dd>Date and time of the quiz changes in appropriate format string for further render.</dd> <dt>string `text_actor`</dt> <dd>Full name of the user who made an activity.</dd> <dt>string `url_actor`</dt> <dd>Url for view information about the user who made activity (admin, staff, client).</dd> </dl>'
                      a_element:
                        type: array
                        description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                      a_service_info:
                        type: array
                        description: 'Information about service if response connected to visit. Empty array if the response is not connected to a visit: <dl> <dt>string `text_date`</dt> <dd>Formatted visit date and time in the location''s time zone.</dd> <dt>string `text_location`</dt> <dd>Location title.</dd> <dt>string `text_service`</dt> <dd>Service title.</dd> <dt>string `text_staff_member`</dt> <dd>Comma-separated list of full names of staff members conducting the visit.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether response can be amended by current user.'
                      dtu_response:
                        type: string
                        description: 'Date when response was submitted.'
                      id_source:
                        type: number
                        format: int32
                        description: 'Response source. One of {@link SourceSid} constants.'
                      id_status:
                        type: number
                        format: int32
                        description: 'Response status ID.'
                      show_numbering:
                        type: boolean
                        description: 'Whether to show numbering of the form elements that supports numbering. `true` to show numbering on the form for elements that supports numbering. `false` to not show numbering.'
                      text_add_date:
                        type: string
                        description: 'Date when response added.'
                      text_amend_date:
                        type: string
                        description: 'Date when response amended.'
                      text_amend_user:
                        type: string
                        description: 'Name of the user who amend the response.'
                      text_complete_date:
                        type: string
                        description: 'Date when response completed.'
                      text_complete_user:
                        type: string
                        description: 'Name of the user who complete the response.'
                      text_response_by:
                        type: string
                        description: 'Name of the user who owned the response.'
                      text_title:
                        type: [string, "null"]
                        description: 'Title of the filled form. `null` in case when not filled yet.'
      tags:
        - Response-Response65
    post:
      summary: 'Saves a quiz response with the given element answers. Validates the submitted answers and persists the response record in a transaction. If {@link \Core\Quiz\QuizResponseApi::$is_validate_only} is set, only validation runs and no record is created. Pass {@link \Core\Quiz\QuizResponseApi::$is_skip} to bypass validation for pre-confirmed responses.'
      parameters:
        - name: can_anonymous
          in: query
          description: 'Checks whether unauthorized user should be permitted to operate with form and make a response. In general all quizzes should have users in response but it some cases such as registration process user might not exist yet, and we need ability to ignore check for user existence. `true` - add possibility load form and accept response for non-registered user, `false` otherwise.'
          schema:
            type: boolean
        - name: is_backend
          in: query
          description: ''
          schema:
            type: boolean
        - name: is_simple
          in: query
          description: 'Whether quiz response received by kiosk or direct mode link. `true` quiz response received by kiosk mode. `false` quiz response received by direct or direct mode.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_element:
                  type: array
                  description: 'List of quiz questions with responses. Parameters for `15`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `2`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_subheading`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `5`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `17`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_caption`</dt> <dd>HTML of the image caption.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the image heading.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the image (optional).</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string `k_id_source`</dt> <dd>Image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command`</dt> <dd>Command for image save prepare.</dd> <dt>string `text_caption`</dt> <dd>Image caption.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `16`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `can_upload`</dt> <dd> Can user upload custom image. User is allowed to upload custom images if they have the access to view the list of quizzes. </dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_delete_custom_markup_image`</dt> <dd><tt> true </tt> if custom markup image is to be deleted <tt>false</tt> otherwise.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id`</dt> <dd> Fake id used for image upload. A fake id is being used because we are not going to save the image directly from upload. Element key will be appended to make it unique. </dd> <dt>string `k_id_custom_markup_image`</dt> <dd>Image ID of the temporary uploaded custom markup image to be copied as permanent drive link.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_link_default_markup_image`</dt> <dd>Link of the source default markup image to copy to the element.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_heading`</dt> <dd>Main text of the input question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `url_custom_default_image`</dt> <dd>Custom default image path.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `13`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `1`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `3`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> </dl> Parameters for `6`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>int[] `a_answer`</dt> <dd>Answer array for selected question options.</dd> <dt>string[] `a_list`</dt> <dd>Array for a dropdown question''s options.</dd> <dt>string `html_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_multiple`</dt> <dd>Answer can have multiple options selected.</dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `12`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Sub-question html.</dd> <dt>string `html_question`</dt> <dd>Question html.</dd> <dt>int `i_rate`</dt> <dd>Selected rate.</dd> <dt>int `i_scale`</dt> <dd>Rate scale. One of values from {@link \Core\Quiz\Element\Rate\Rate::SCALE_LIST} array.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_description`</dt> <dd>Sub-question text.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Question text.</dd> <dt>string `xml_description`</dt> <dd>Sub-question XML.</dd> <dt>string `xml_question`</dt> <dd>Question XML.</dd> </dl> Parameters for `18`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>array `a_markup_image`</dt> <dd>Markup images files. Only PNG images are supported.</dd> <dt>bool `has_duplicate`</dt> <dd>Whether this element has a duplicate. `true` if this element has a duplicate, otherwise `false`.</dd> <dt>string `html_heading`</dt> <dd>Main HTML of the input question.</dd> <dt>string `html_subheading`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string `k_id_image_left`</dt> <dd>Left response image key.</dd> <dt>string `k_id_image_right`</dt> <dd>Right response image key.</dd> <dt>string `k_id_source_left`</dt> <dd>Left image source key for duplicate.</dd> <dt>string `k_id_source_right`</dt> <dd>Right image source key for duplicate.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_command_left`</dt> <dd>Command for left image save prepare.</dd> <dt>string `s_command_right`</dt> <dd>Command for right image save prepare.</dd> <dt>string `s_markup_image`</dt> <dd>Post-Markup image binary text.</dd> <dt>string `text_left_image`</dt> <dd>Title for left image.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_right_image`</dt> <dd>Title for right image.</dd> <dt>string `xml_heading`</dt> <dd>Main XML of the input question.</dd> <dt>string `xml_subheading`</dt> <dd>Additional XML for detail description of the question.</dd> </dl> Parameters for `11`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_heading`</dt> <dd>Additional html for detail description of the question.</dd> <dt>string `html_signature_footer`</dt> <dd>Footer text of signature pad.</dd> <dt>string `html_signature_header`</dt> <dd>Header text of signature pad.</dd> <dt>string `html_subheading`</dt> <dd>Main html of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `s_signature`</dt> <dd>Signature binary text.</dd> <dt>string `text_heading`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_subheading`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_heading`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_subheading`</dt> <dd>Main XML of the input question.</dd> </dl> Parameters for `10`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_content`</dt> <dd>Main content HTML.</dd> <dt>string `html_heading`</dt> <dd>Heading HTML.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd>Whether element is required or not. `true` if element is required. `false` if element is not required.</dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_content`</dt> <dd>Main content text.</dd> <dt>string `text_heading`</dt> <dd>Heading text.</dd> <dt>string `xml_content`</dt> <dd>Main content XML.</dd> <dt>string `xml_heading`</dt> <dd>Heading XML.</dd> </dl> Parameters for `4`: <dl> <dt>array[] `a_amendment`</dt> <dd> List of amendments. Each element has next structure: <dl> <dt>array `a_change`</dt> <dd> Information about amend. <dl> <dt>string `text_note`</dt> <dd>Change note.</dd> </dl> </dd> <dt>string `dtu_action`</dt> <dd>Date/time when amend made.</dd> <dt>string `text_actor`</dt> <dd>Name of the amend actor.</dd> <dt>string `text_datetime`</dt> <dd>Date/time when amend made in textual view.</dd> <dt>string `uid_actor`</dt> <dd>UID of the amend actor.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Additional HTML for detail description of the question.</dd> <dt>string `html_question`</dt> <dd>Main HTML of the input question.</dd> <dt>int `id_element`</dt> <dd> Element ID. One of {@link \Core\Quiz\Element\ElementSid} constants. Defined automatically according to the name of the class. </dd> <dt>bool `is_require`</dt> <dd></dd> <dt>string|null `k_quiz_element`</dt> <dd>Quiz element key. `null` in a case if element is not saved in database.</dd> <dt>string|null `k_quiz_element_old`</dt> <dd>Old quiz element key. `null` in a case if element is not has old version.</dd> <dt>string `text_answer`</dt> <dd>Answer text for a question.</dd> <dt>string `text_description`</dt> <dd>Additional string for detail description of the question.</dd> <dt>string|null `text_note`</dt> <dd>Additional note for response. `null` if no notes considered.</dd> <dt>string `text_question`</dt> <dd>Main text of the input question.</dd> <dt>string `xml_description`</dt> <dd>Additional XML for detail description of the question.</dd> <dt>string `xml_question`</dt> <dd>Main XML of the input question.</dd> </dl>'
                a_element_file:
                  type: array
                  description: 'Elements'' uploaded files.'
                a_purchase_item:
                  type: array
                  description: 'List of purchase items for which this form is loaded. The list of purchase items. Each element has the format `[id_purchase_item]::[k_id]`, where: Empty in case when purchase item not specified or form loaded from direct link. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
                a_quick_config:
                  type: array
                  description: 'Data for Quick Buy. Empty array if this is not a Quick Buy session. <dl> <dt>int `id_table`</dt> <dd>Quick purchase type. One of {@link \Wl\Catalog\QuickPurchaseTypeSid} constant.</dd> <dt>bool `is_quick_buy`</dt> <dd> Whether quick buy mode should be used. It means that specific template will be loaded and specific cart will be used. </dd> <dt>string `k_appointment`</dt> <dd>Appointment key to pay for. <i>Primary key in {@link \RsAppointmentSql}.</i></dd> <dt>string `k_id`</dt> <dd>Item key within <var>id_table</var> table that Quick Buy performed for.</dd> <dt>string `k_visit`</dt> <dd>Visit key to pay for. <i>Primary key in {@link \RsVisitSql}.</i></dd> <dt>string `uid`</dt> <dd>User that Quick Buy performed for.</dd> </dl>'
                id_source:
                  type: number
                  format: int32
                  description: 'Response source. One of {@link SourceSid} constants.'
                is_draft:
                  type: boolean
                  description: '`true` if need to save response in draft mode, `false` otherwise.'
                is_skip:
                  type: boolean
                  description: 'Whether the quiz needs to be skipped.'
                is_validate_only:
                  type: boolean
                  description: '`true` if need make only validation, `false` need make validation and saving response.'
                json_element:
                  type: string
                  description: 'List of quiz questions with responses in JSON format.'
                k_quiz_login:
                  type: string
                  description: 'Quiz login key.'
                s_secret:
                  type: string
                  description: 'Secret key for access to quiz request.'
                text_appointment_wizard_id:
                  type: [string, "null"]
                  description: 'Booking appointment wizard ID. Not `null` if quiz response was sent during booking appointment process.'
                text_class_wizard_id:
                  type: [string, "null"]
                  description: 'Booking class wizard ID. Not `null` if quiz response was sent during booking class process.'
                uid_response:
                  type: [string, "null"]
                  description: 'UID of the user who makes response for quiz.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_quiz_response:
                        type: [string, "null"]
                        description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
      tags:
        - Response-Response65
    put:
      summary: 'Validates, updates and reindex response information for associated user. Used to amend an already-submitted response, for example when a business allows clients to edit their quiz answers after submission. Re-links the response to its owner and triggers downstream reindexing so search and reporting stay consistent.'
      parameters:
        - name: a_quiz_response_key
          in: query
          description: 'Quiz response key list.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. `null` in a case of response creation or removing set of responses in {@link \Core\Quiz\QuizResponseApi::$a_quiz_response_key}.'
          schema:
            type: [string, "null"]
        - name: uid_link
          in: query
          description: 'User''s key for the response association.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_quiz_response_key:
                  type: string
                  description: 'Quiz response key list.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response65
  '/Wl/Quiz/Login/QuizLogin.json':
    delete:
      summary: 'Deletes from the client profile.'
      parameters:
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-QuizLogin
    post:
      summary: 'Adds quiz for client profile.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_quiz:
                  type: [array, "null"]
                  description: 'List of quizzes keys. `null` when we do not send data from frontend and send `k_quiz` instead.'
                k_visit:
                  type: string
                  description: 'Visit key.'
                uid_client:
                  type: string
                  description: 'UID of the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-QuizLogin
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. Passed when need change hidden status for draft response(which created from request), otherwise `null`.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_require:
                  type: boolean
                  description: 'Whether filling out a form is obligatory and should be passed urgently. `true` - obligatory, `false` - otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-QuizLogin
  '/Wl/Quiz/Login/QuizRemind.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which quiz is managed.'
          schema:
            type: string
        - name: k_quiz_login
          in: query
          description: 'Quiz login key.'
          schema:
            type: string
        - name: k_quiz_response
          in: query
          description: 'Quiz response key. Passed when need send reminder for draft response(which created from request), otherwise `null`.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-QuizRemind
  '/Wl/Quiz/Index/QuizSubscription.json':
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link QuizSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'Redirect URL after subscription is changed.'
      tags:
        - Index-QuizSubscription
  '/Wl/Reception/Roster/AttendanceConfirmationScreen.json':
    get:
      summary: 'Gets information about a client who has just checked in.'
      parameters:
        - name: dtu_date
          in: query
          description: 'Date and time of the visit.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business. Primary key in {@link \RsBusinessSql} table'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class schedule key. Primary key for {@link \RsClassPeriodSql}.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret string.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key from the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_photo:
                        type: array
                        description: 'Data about a login logo. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image height.</dd> <dt>string `url_photo`</dt> <dd>Image URL.</dd> </dl>'
                      a_visit:
                        type: array
                        description: 'Data of the visit. <dl> <dt>array `a_payment`</dt> <dd> Data for informational card about promotion. Important for the card fields are: <dl> <dt>array `a_image`</dt> <dd> Logo of the promotion. <dl> <dt>int `i_height`</dt> <dd>Height of the image in pixels.</dd> <dt>int `i_width`</dt> <dd>Width of the image in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` means that image is empty.</dd> <dt>string `url_thumbnail`</dt> <dd>Link on the image.</dd> </dl> </dd> <dt>array `a_resources_not_shared`</dt> <dd> The list of assets reserved individually at the time of booking containing: <dl> <dt>int `i_index`</dt> <dd>The busy resource index.</dd> <dt>string `s_title`</dt> <dd>The resource name.</dd> </dl> </dd> <dt>array `a_restrict`</dt> <dd> Information about calendar restrictions. <dl> <dt>int `i_limit`</dt> <dd>Count of possible visits.</dd> <dt>int `i_remain`</dt> <dd>Count of remaining visits.</dd> <dt>string `s_date`</dt> <dd>Name of the calendar period.</dd> </dl> </dd> <dt>string `html_expire`</dt> <dd>Message about expiration date of the promotion.</dd> <dt>int `i_book`</dt> <dd>Count of future books that are paid with this promotion.</dd> <dt>int `i_limit`</dt> <dd>Limit on the visit count of the promotion.</dd> <dt>int `i_remain`</dt> <dd>Remaining count of visits.</dd> <dt>int `i_use`</dt> <dd>Count of usage of the promotion.</dd> <dt>int `i_visit_past`</dt> <dd> Count of attended sessions before last renew. `0` if there are no sessions before last renew or promotion is not auto-renew. </dd> <dt>bool `is_last_use`</dt> <dd>If `true`, the promotion has a usage limit and no remaining visits. Otherwise, this will be `false`.</dd> <dt>string `text_renew`</dt> <dd>Date, when PO is going to be renewed. Empty, if it''s not going.</dd> <dt>string `s_expire`</dt> <dd>Message about expiration date of the promotion.</dd> <dt>string `s_start`</dt> <dd> Start date if promotion has not started yet. If the promotion has already started, the field is not presented. </dd> <dt>string `s_title`</dt> <dd>Name of the promotion.</dd> <dt>string `uid`</dt> <dd>Promotion owner. Primary key in {@link \PassportLoginSql} table.</dd> </dl> </dd> <dt>string `text_expire`</dt> <dd>Date when promotion ends in user-friendly format.</dd> <dt>string `text_payment`</dt> <dd> If visit not payed yet value is `Not payed`. If specified visit don''t need to be paid value is `Free`. Else title of the promotion. </dd> </dl>'
                      s_status:
                        type: string
                        description: 'Status of the operation.'
                      text_class:
                        type: string
                        description: 'The class title.'
                      text_member:
                        type: string
                        description: 'User''s member ID. Copy of {@link \RsLoginMemberSql}.`s_member`.'
                      text_staff_name:
                        type: string
                        description: 'The staff member''s full name.'
                      text_time:
                        type: string
                        description: 'Class session time in user-friendly format.'
                      text_user:
                        type: string
                        description: 'Full name of the user.'
      tags:
        - Roster-AttendanceConfirmationScreen
  '/Wl/Reception/Roster/AttendanceListNotAttend.json':
    post:
      summary: 'Marks the visit as not attended via the Attendance Kiosk. Validates the Attendance Kiosk secret and updates the specified visit record to reflect that the client did not attend the session.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'The business key.'
                k_visit:
                  type: string
                  description: 'The visit key.'
                s_secret:
                  type: string
                  description: 'The secret for authenticating the attendance kiosk.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Roster-AttendanceListNotAttend
  '/Wl/Reception/Roster/AttendanceListAttend.json':
    post:
      summary: 'Books a class for the client and marks the visit as attended via the Attendance Kiosk. Validates the Attendance Kiosk secret, books the specified class period for the client, and immediately marks the resulting visit as attended.'
      parameters:
        - name: dtu_date
          in: query
          description: 'The UTC datetime for the class in MySQL format.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'The secret for authenticating the attendance kiosk.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The client''s user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_visit:
                        type: string
                        description: 'The visit key.'
                      text_message:
                        type: string
                        description: 'The status message displayed on a successful check-in.'
      tags:
        - Roster-AttendanceListAttend
  '/Wl/Reception/Roster/AttendanceListBook.json':
    post:
      summary: 'Books a class for the client via the Attendance Kiosk and returns whether the client was placed on the class list or waitlist. Validates the Attendance Kiosk secret and books the client into the specified class period, returning a flag indicating whether the client was placed on the class list or the waitlist.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dtu_date:
                  type: string
                  description: 'The UTC datetime for the class in MySQL format.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_class_period:
                  type: string
                  description: 'The class period key.'
                s_secret:
                  type: string
                  description: 'The secret for authenticating the attendance kiosk.'
                uid:
                  type: string
                  description: 'The client''s user key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_wait:
                        type: boolean
                        description: '`true` if the client is on the waitlist for the class, `false` if they are on the class list.'
                      text_message:
                        type: string
                        description: 'The status message displayed on a successful booking. Describes whether client was put in class list or waitlist.'
      tags:
        - Roster-AttendanceListBook
  '/Wl/Reception/Roster/ScheduleList.json':
    get:
      summary: 'Retrieves schedule of sessions.'
      parameters:
        - name: dt_date_local
          in: query
          description: 'Local date of the currently viewed schedule list. In MySQL format, without time.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'ID of business location to show information for. Primary key from {@link \RsLocationSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_schedule:
                        type: array
                        description: 'Schedule of sessions in location {@link \Wl\Schedule\ScheduleList\ScheduleListApi::$k_location} in day {@link \Wl\Schedule\ScheduleList\ScheduleListApi::$dt_date}.'
      tags:
        - Roster-ScheduleList
  '/Wl/Reception/Roster/AttendanceList.json':
    get:
      summary: 'Returns client list for specified class. Data expected in GET array: <dl> <dt>string <var>dtl_date</var></dt> <dd>Date and time when class provides in GMT.</dd> <dt>bool [<var>is_classpass_only</var>]</dt> <dd>Whether need to show only clients that booked with Class Pass Prospect. If not passed then all clients returns.</dd> <dt>string <var>k_class_period</var></dt> <dd>Class period from which need to get list of clients.</dd> </dl>'
      parameters:
        - name: dtl_date
          in: query
          description: 'Date and time of the currently viewed attendance list. `null` if not defined yet.'
          schema:
            type: [string, "null"]
        - name: is_classpass_only
          in: query
          description: 'Whether only clients from `Classpass` should be returned. `null` if not defined yet.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Class period key of the attendance list. Primary key from {@link \RsClassPeriodSql} table. `null` if not defined yet.'
          schema:
            type: [string, "null"]
        - name: s_secret
          in: query
          description: 'Secret key for access to attendance list.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_client:
                        type: [array, "null"]
                        description: 'List of clients that registered on class each element contains information: `null` if not defined yet. <dl> <dt>array `a_asset`</dt> <dd> A list of the assets. <dl> <dt>string `k_resource`</dt> <dd>Resource key. Primary key in {@link \RsResourceSql} table.</dd> <dt>string `text_name`</dt> <dd> Asset title that consists of the asset title itself concatenated with its index (in case of multiple assets) by ''#''. </dd> <dt>string `text_title`</dt> <dd>Title of the asset.</dd> </dl> </dd> <dt>string `sid_gender`</dt> <dd>Gender SID. On of the {@link \Wl\Gender\GenderSid} constants.</dd> <dt>string `text_first_name`</dt> <dd>Client''s first name.</dd> <dt>string `text_last_name`</dt> <dd>Client''s last name.</dd> <dt>string `text_mail`</dt> <dd>Mail of client.</dd> <dt>string `text_name`</dt> <dd>Full name. Combination of the first and last names.</dd> <dt>string `text_pass_prospect`</dt> <dd>Whether client used Pass Promote to visit class.</dd> <dt>string `text_phone`</dt> <dd>Phone number of client.</dd> <dt>string `text_visit`</dt> <dd>State of visit. String representation of constants {@link \Wl\Visit\VisitSid}.</dd> <dt>string `uid`</dt> <dd>User identifier.</dd> </dl>'
      tags:
        - Roster-AttendanceList
  '/Wl/Reception/Roster/AttendanceSecret.json':
    get:
      summary: 'Gets the secret string for Attendance Kiosk requests. Requires backend access and validates that the specified location belongs to the given business, then returns the secret token used to authenticate Attendance Kiosk API requests.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_secret:
                        type: string
                        description: 'Secret string.'
      tags:
        - Roster-AttendanceSecret
  '/Wl/Reception/Design/ReceptionDesign.json':
    get:
      summary: 'Returns information about settings for Check In Web Application. This method does not require any access checks, because this is public information.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where application is run.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location, where application is run.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reception_logo:
                        type: array
                        description: 'Array of image information for Self Check-In logo. Empty if {@link \Wl\Reception\Design\ReceptionDesignApi::$k_location} is not provided. Will contain the following keys: If image for self check in app is empty, will attempt to obtain business logo instead. If business logo is empty, will attempt to obtain location logo. <dl> <dt>string `url_view`</dt> <dd>url for full image.</dd> <dt>string `url_thumbnail`</dt> <dd>url for thumbnail version of image.</dd> </dl>'
                      hide_profile_images:
                        type: boolean
                        description: '`true` - client profile images will be hidden after the client signs in on the Check-in Kiosk; `false` - otherwise.'
                      i_book_open:
                        type: number
                        format: int32
                        description: 'Number of minutes for the client check-in window after session has started.'
                      i_confirm_delay:
                        type: number
                        format: int32
                        description: 'Delay in seconds on Check-in Confirmation Screen before redirect to Login screen.'
                      i_schedule_delay:
                        type: number
                        format: int32
                        description: 'Delay in seconds on Schedule Screen before redirect to Login screen.'
                      id_failed_sound:
                        type: number
                        format: int32
                        description: 'ID of the sound for failed check in.'
                      id_success_sound:
                        type: number
                        format: int32
                        description: 'ID of the sound for successful check in.'
                      is_attend_free:
                        type: boolean
                        description: '`true` - allow client to check-in unpaid; `false` - otherwise.'
                      is_auto_check_in:
                        type: boolean
                        description: 'If only one service available with the look ahead window the client will: `true` - automatically check in to it; `false` - have to select the service.'
                      is_book_open:
                        type: boolean
                        description: '`true` - allow clients to check-in to sessions in progress; `false` - otherwise.'
                      is_book_optional:
                        type: boolean
                        description: '`true` - allow client check-in without booking prior; `false` - otherwise.'
                      is_client_id:
                        type: boolean
                        description: '`true` - enable check-in by client id; `false` - otherwise.'
                      is_mail:
                        type: boolean
                        description: '`true` - enable check-in by email; `false` - otherwise.'
                      is_password:
                        type: boolean
                        description: '`true` - require password while check-in; `false` - otherwise.'
                      is_phone:
                        type: boolean
                        description: '`true` - enable check-in by phone; `false` - otherwise.'
                      is_reconcile_auto:
                        type: boolean
                        description: '`true` - automatically reconcile unpaid visits upon check in; `false` - otherwise.'
                      show_business_name:
                        type: boolean
                        description: '`true` - show business name on client self check-in page; `false` - otherwise.'
                      show_confirm_screen:
                        type: boolean
                        description: '`true` - show confirm screen after client self check-in; `false` - otherwise.'
                      text_business_name:
                        type: string
                        description: 'Name of the business to display on the client self check-in page.'
      tags:
        - Design-ReceptionDesign
  '/Wl/Reception/Application/ReceptionSchedule.json':
    get:
      summary: 'Returns the schedule and HTML for the Self Check-In Web App for the given user. Returns the rendered schedule HTML, a structured list of upcoming sessions, and a map of service type HTML classes for the authenticated user at the given location.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'The key of the Self Check-In Web App. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show the schedule for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'A list of sessions to display with the following fields: <dl> <dt>array `a_resources_shared`</dt> <dd> A list of shared resources containing: <dl> <dt>string `k_resource`</dt> <dd>The resource key.</dd> <dt>string `s_title`</dt> <dd>The resource title.</dd> </dl> </dd> <dt>array `a_staff_all`</dt> <dd> A list of staff members instructing the service, containing: <dl> <dt>string `s_name_full`</dt> <dd>The staff member''s full name.</dd> </dl> </dd> <dt>string `dtu_date`</dt> <dd>The session date.</dd> <dt>string `html_class_js`</dt> <dd>The name of the HTML class that JavaScript uses to display the icon, depending on the service type.</dd> <dt>int `i_duration`</dt> <dd>The session duration in minutes.</dd> <dt>int `id_service`</dt> <dd>Service ID. One of the {@link \Wl\Service\ServiceSid} constants.</dd> <dt>bool `is_auto`</dt> <dd>If `true`, the session can be checked in automatically. Otherwise, this will be `false`.</dd> <dt>bool `is_book`</dt> <dd>If `true`, the client should be notified that the visit is booked. Otherwise, this will be `false`.</dd> <dt>bool `is_wait`</dt> <dd>If `true`, the client should be notified that they''re on the wait list. Otherwise, this will be `false`.</dd> <dt>string|null `k_appointment`</dt> <dd>The appointment key. If `null`, this isn''t an appointment.</dd> <dt>string|null `k_class_period`</dt> <dd>The class period key. If `null`, this isn''t a class or event.</dd> <dt>string|null `k_visit`</dt> <dd>The visit key if the session has been checked in. Otherwise, this will be `null`.</dd> <dt>string `s_class`</dt> <dd>The service name.</dd> <dt>string `s_duration`</dt> <dd>The session duration, formatted by the business configuration.</dd> <dt>string `s_time`</dt> <dd>The time the session takes place.</dd> </dl>'
                      a_schedule_class_all:
                        type: array
                        description: 'All types of services that appear in the schedule. Keys are constants from {@link ServiceSid}. Values are the HTML classes associated with that service.'
                      html_schedule:
                        type: string
                        description: 'The schedule to be shown in the Self Check-In Web App for the selected user.'
      tags:
        - Application-ReceptionSchedule
    post:
      summary: 'Performs check-in for the given user and returns the confirmation HTML and data. Books or checks the user into the specified class period or appointment, then returns confirmation HTML and structured data including payment info, assigned assets, and visit counts.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key, where the Self Check-In Web App is started.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'The key of the Self Check-In Web App. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show the schedule for.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dtu_date:
                  type: string
                  description: 'The date and time of the visit in UTC and MySQL format.'
                k_appointment:
                  type: [string, "null"]
                  description: 'The key of the appointment to check in. If `null`, this is class visit or gym visit.'
                k_class_period:
                  type: [string, "null"]
                  description: 'The key of the class schedule to check in. If `null`, this is an appointment visit or gym visit.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_confirmation_data:
                        type: array
                        description: 'Data for the confirmation screen with the following fields: <dl> <dt>array `a_payment`</dt> <dd> Data for the promotion payment informational card. Each element contains: <dl> <dt>array `a_restrict`</dt> <dd> Calendar restrictions. A duration pass will only have elements if a restriction has been met. This contains: <dl> <dt>int `i_limit`</dt> <dd>The count of possible visits.</dd> <dt>int `i_remain`</dt> <dd>The count of remaining visits.</dd> <dt>string `s_date`</dt> <dd>The calendar period name.</dd> </dl> </dd> <dt>bool `has_visit_past`</dt> <dd>If `true`, the promotion was renewed in the past. Otherwise, this will be `false`.</dd> <dt>bool `hide_visit_additional_info`</dt> <dd>If `true`, additional info should be excluded. Otherwise, this will be `false`.</dd> <dt>int `i_book`</dt> <dd>The count of upcoming uses of the promotion.</dd> <dt>int `i_book_duration`</dt> <dd> Number of minutes used for upcoming visits. Has sense only for promotions of program type {@link \RsProgramTypeSid::DURATION}. </dd> <dt>int `i_limit`</dt> <dd>The promotion''s visit count limit.</dd> <dt>int `i_remain`</dt> <dd>The remaining count of visits.</dd> <dt>int `i_remain_duration`</dt> <dd> Number of minutes left in this promotion. Has sense only for promotions of program type {@link \RsProgramTypeSid::DURATION}. </dd> <dt>int `i_use`</dt> <dd>Count of held uses in the pass.</dd> <dt>int `i_use_duration`</dt> <dd> Number of minutes used for past visits by this promotion. Has sense only for promotions of program type {@link \RsProgramTypeSid::DURATION}. </dd> <dt>int `i_visit_past`</dt> <dd> The count of attended sessions before the last renewal. If `0`, there were no sessions before last renewal or the promotion doesn''t auto-renew. </dd> <dt>int `id_program_type`</dt> <dd>The promotion''s program type ID. One of {@link \RsProgramTypeSid} constants.</dd> <dt>bool `is_last_use`</dt> <dd>If `true`, the promotion has a usage limit and no remaining visits. Otherwise, this will be `false`.</dd> <dt>bool `is_package`</dt> <dd>If `true`, the promotion is a package. Otherwise, this will be `false`.</dd> <dt>bool `is_restrict_multiply`</dt> <dd>If `true`, there''s more than one calendar restriction on the pass. Otherwise, this will be `false`.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key from {@link \RsLoginPromotionSql}.</i></dd> <dt>string `s_remain_measure`</dt> <dd>The formatted remaining duration on the promotion.</dd> <dt>bool `show_remain`</dt> <dd>If `true`, there''s a rollover on the pass with calendar restrictions. Otherwise, this will be `false`.</dd> <dt>string `sid_program_type`</dt> <dd>The promotion''s program type SID.</dd> </dl> </dd> <dt>array `a_resources_not_shared`</dt> <dd> The list of assets reserved individually at the time of booking containing: <dl> <dt>int `i_index`</dt> <dd>The busy resource index.</dd> <dt>string `s_title`</dt> <dd>The resource name.</dd> </dl> </dd> <dt>string `dl_renew`</dt> <dd>The promotion renewal date in local time.</dd> <dt>int `i_visits`</dt> <dd>The total workouts attended.</dd> <dt>string `s_expire`</dt> <dd>The short format of the promotion expiration date.</dd> <dt>string `s_payment`</dt> <dd>The payment method used to book (free, unpaid, single buy, or promotion name).</dd> <dt>string `s_renew`</dt> <dd>The short format of the promotion renewal date.</dd> <dt>bool `show_visits`</dt> <dd>Determines whether to show the client''s total workouts attended on the confirmation screen.</dd> </dl>'
                      html_confirmation:
                        type: string
                        description: 'The confirmation template to be shown in the Self Check-In Web App for the selected user.'
                      k_visit:
                        type: string
                        description: 'The visit key, which was added or checked in.'
      tags:
        - Application-ReceptionSchedule
  '/Wl/Reception/Application/MemberInfo.json':
    get:
      summary: 'Returns member information and notification items for the Self Check-In Web App. Returns the user''s profile data along with any notification items (such as expiring memberships, required waivers, or outstanding balances) that should be displayed during the self check-in flow.'
      parameters:
        - name: a_uid
          in: query
          description: 'Primary keys of users whose information must be returned. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: a_uid_date
          in: query
          description: 'List of dates for load additional information about users. Key is UID of user. Value is date. `null` if data of a single user is requested.'
          schema:
            type: [array, "null"]
        - name: dt_date
          in: query
          description: 'Date of the session, if we show it on the appointment info window or on the attendance list.'
          schema:
            type: string
        - name: is_backend
          in: query
          description: '`true` - if API is being used from backend, `false` - otherwise.'
          schema:
            type: boolean
        - name: is_full
          in: query
          description: 'If you need to return additional information set to `true` or `false` if not.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business ID required to access client information. Specify this as `0` to retrieve the system-wide version of the information.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location where the Self Check-In Web App is running.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'ID of the visit, if we show icons on the attendance list and information that depends on visit is required.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Key of the Check In application. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
        - name: s_show
          in: query
          description: 'A list of icons with additional information about the business member. If empty, all available icons will be displayed. Comma separated values from {@link MemberInfoSid::idSid()} method.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to retrieve member information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_info:
                        type: [array, "null"]
                        description: 'Additional member data or `null` if any data can be shown. Will be filled if {@link InfoApi::$is_full} flag is set. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl>'
                      a_items:
                        type: array
                        description: 'The options presented in the web app. The items are generated this way due to JavaScript limitations around translated messages. <dl> <dt>int `id_color`</dt> <dd> ID of color the notice is displayed in. One of {@link \Wl\Reception\Application\MemberInfoColorSid} constants. </dd> <dt>int `id_icon`</dt> <dd> ID of icon that accompanies the message. One of {@link \Wl\Reception\Application\MemberInfoIconSid} constants. </dd> <dt>string `text_class`</dt> <dd>Name of the class for styles.</dd> <dt>string `text_message`</dt> <dd>Plain message for info box.</dd> <dt>string `html_message`</dt> <dd>Message for info box.</dd> </dl>'
                      a_result_list:
                        type: [array, "null"]
                        description: 'List of users data. Keys - users primary keys. Values - users data. <i>See for {@link InfoApi::_get()} details.</i> Keys refer to clients'' primary keys, and values refer to clients'' data. If `null`, data for a single client is being requested. <dl> <dt>array `a_info`</dt> <dd> Additional user''s information. <dl> <dt>array[] `a_icon`</dt> <dd> List of icons with additional information about business member. <i>See {@link \Wl\Member\Info\MemberInfoInfo::$a_icon} for more detail.</i> <dl> <dt>string `s_color_background`</dt> <dd>Color of background.</dd> <dt>string `s_color_foreground`</dt> <dd>Color of letter.</dd> <dt>string `s_letter`</dt> <dd>Icon letter.</dd> <dt>string `s_shape`</dt> <dd>Symbol from font of shapes.</dd> <dt>string `s_title`</dt> <dd>Title.</dd> <dt>string `sid_shape`</dt> <dd>SID of the icon type shape. Constant from {@link \Wl\Login\Type\ShapeSid}.</dd> </dl> </dd> <dt>array[] `a_vaccination_status`</dt> <dd> Information about users vaccination status. <dl> <dt>string `sid_vaccination_status`</dt> <dd> Vaccination status sid. Result from {@link \Wl\Login\Member\VaccinationStatus\VaccinationStatusSid::idSid()} method. </dd> <dt>string `text_vaccination_status`</dt> <dd>Vaccination status.</dd> </dl> </dd> <dt>array[] `a_note`</dt> <dd> List of client''s notes. Every element has keys: <dl> <dt>string `html_note`</dt> <dd>HTML text ready to be pasted in browser.</dd> <dt>bool `is_flag`</dt> <dd><tt>true</tt> if this note has a flag; <tt>false</tt> otherwise.</dd> </dl> </dd> <dt>string `html_credit`</dt> <dd>Amount the client owns to the business.</dd> <dt>string `html_note`</dt> <dd>User`s login notes.</dd> <dt>int `i_visit_credit`</dt> <dd>Count of unpaid visits if client has at least one.</dd> <dt>bool `is_account_positive`</dt> <dd>If the user has a positive balance on his account.</dd> <dt>bool `is_birthday`</dt> <dd>If a user has a birthday today.</dd> <dt>bool `is_contract`</dt> <dd><tt>true</tt> if client has some contracts that he should agree.</dd> <dt>bool `is_family_payer`</dt> <dd>`true` if the user makes payments for a family member, `false` otherwise.</dd> <dt>bool `is_first`</dt> <dd><tt>true</tt> if this is a first visit of the client.</dd> <dt>bool `is_form`</dt> <dd>`true` if the client has uncompleted one or more forms.</dd> <dt>bool `is_task_active`</dt> <dd><tt>true</tt> if for user exists not completed tasks.</dd> <dt>bool `is_task_urgent`</dt> <dd><tt>true</tt> if for user exists urgent tasks.</dd> <dt>bool `is_waiver`</dt> <dd><tt>true</tt> if client has not agreed with online waiver.</dd> <dt>string `k_currency`</dt> <dd> Currency of business <var>$k_business</var>. <i>Primary key in table {@link \RsCurrencySql}.</i> It is currency of amount <var>m_credit</var>. </dd> <dt>string `m_credit`</dt> <dd>User''s account balance.</dd> <dt>bool `show_amount`</dt> <dd>Display user balance or not.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_age`</dt> <dd>Users age. Appears only on the day of birth.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_phone`</dt> <dd>User''s cell phone.</dd> <dt>string `text_firstname`</dt> <dd>Users first name.</dd> <dt>string `text_visit_credit`</dt> <dd>Link to the page with client''s attendance.</dd> <dt>string `url_attendance`</dt> <dd>Link to the page with information about client''s account balance.</dd> <dt>string `url_billing`</dt> <dd>Link to user''s billing page.</dd> <dt>string `url_contract`</dt> <dd>Link to user''s contract page</dd> <dt>string `url_form`</dt> <dd>Link to user''s forms page.</dd> <dt>string `url_overview`</dt> <dd>Link to the client''s overview page. Empty if for user not existing active or urgent tasks.</dd> <dt>string `url_profile`</dt> <dd>Link to user''s profile.</dd> <dt>string `url_waiver`</dt> <dd>Link to user''s waiver page.</dd> </dl> </dd> <dt>bool `is_traveller`</dt> <dd>`true` - user is traveller; `false` - not traveller.</dd> <dt>string `s_member`</dt> <dd>User''s member number.</dd> <dt>string `text_first_name`</dt> <dd>User''s first name.</dd> <dt>string `text_fullname`</dt> <dd>User''s name.</dd> <dt>string `url_barcode`</dt> <dd>Link to barcode image to scan member number.</dd> </dl>'
                      a_visit_last:
                        type: array
                        description: 'Information about last visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      a_visit_next:
                        type: array
                        description: 'Information about next visit of the user. <dl> <dt>string `dtu_visit`</dt> <dd>Datetime visit in UTC.</dd> <dt>string|null `k_appointment`</dt> <dd> Appointment key. <i>Primary key from {@link \RsAppointmentSql} table. </i> `null` if it is not service or asset session. </dd> <dt>string|null `k_class_period`</dt> <dd> Class period key. <i>Primary key from {@link \RsClassPeriodSql} table.</i> `null` if it is not class or event session. </dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key from {@link \RsLocationSql} table.</i></dd> </dl>'
                      i_lifetime_visit:
                        type: number
                        format: int32
                        description: 'Count attend visits for one client.'
                      is_traveller:
                        type: [boolean, "null"]
                        description: 'If `true`, the client is a traveler. Otherwise, this will be `false`. This will be `null` in cases when the client is a walk-in. Or when {@link MemberInfoApi::$is_full} is `false`.'
                      m_lifetime_value:
                        type: string
                        description: 'The member''s lifetime value.'
                      s_member:
                        type: [string, "null"]
                        description: 'The member ID. If `null`, the specified client isn''t a member of the specified business.'
                      text_first_name:
                        type: string
                        description: 'First user''s name.'
                      text_fullname:
                        type: string
                        description: 'Full user''s name.'
                      url_barcode:
                        type: string
                        description: 'URL to barcode image.'
                      url_email:
                        type: string
                        description: 'URL to email.'
      tags:
        - Application-MemberInfo
  '/Wl/Reception/Application/ReceptionAuthorize.json':
    post:
      summary: 'Performs authorization based on the given authorization value and business settings. Looks up clients by member ID, email address, or phone number and returns matching user records for selection in the Self Check-In Web App.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where Check In application is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location, where Check In application is started.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Key of the Check In application. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_authorize:
                  type: string
                  description: 'Authorization value - it''s a value, which client entered on authorization form. It can be client ID, email or phone number. Depends on the business settings.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_select:
                        type: array
                        description: 'List of the users, which can be authorized. Can be set only this field or {@link ReceptionAuthorizeApi::$uid}. It depends, whether we found one user or multiple. <dl> <dt>string `text_name_first`</dt> <dd>First name of the user.</dd> <dt>string `text_name_last`</dt> <dd>Last name of the user.</dd> <dt>string `uid`</dt> <dd>Key of the user.</dd> <dt>string `url_image`</dt> <dd>Link to the profile image.</dd> </dl>'
                      uid:
                        type: string
                        description: 'Key of the authorized user. Can be set only this field or {@link ReceptionAuthorizeApi::$a_select}. It depends, whether we found one user or multiple.'
      tags:
        - Application-ReceptionAuthorize
  '/Wl/Reception/Application/ReceptionSecret.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where Check In application is started.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location, where Check In application is started.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_secret:
                        type: string
                        description: 'Generate secret key, which allows you to use reception api endpoints in the given location within the same persistent session. <i>Read more {@link MainController::$s_secret}.</i>'
      tags:
        - Application-ReceptionSecret
  '/Wl/Reception/Application/AuthorizeWithPassword.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where Self Check In application is run.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Key of the location, where Self Check In application is run.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Key of the Check In application. <i>Read more {@link MainController::$s_secret}.</i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_data:
                  type: string
                  description: 'Additional data for user authorization.'
                s_captcha:
                  type: string
                  description: 'The answer to the captcha, if needed.'
                s_login:
                  type: string
                  description: 'The user''s login.'
                s_notepad:
                  type: string
                  description: 'A copy of the notepad that was used to hash the user password. See {@link \Core\Passport\Login\Enter\EnterApi::$s_password} for details.'
                s_password:
                  type: string
                  description: 'The hash of the user password. Use `Core_Passport_Login_Enter_NotepadModel.hash()` to evaluate the password hash.'
                s_remember:
                  type: string
                  description: 'Determines whether the user login and password should be remembered, and how they should be remembered. The accepted values are: <ul> <li>`''''` Empty line (default value) if you do not want to remember anything.</li> <li>`''login''` Remember only user login.</li> <li>`''password''` Remember user login and password.</li> </ul>'
                url_return:
                  type: string
                  description: 'Url of previous page if the user was redirected to login.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'Key of the user, which was signed in. Will be left empty, if user was not able to sign in.'
                      url_redirect:
                        type: string
                        description: 'An optional URL for redirection after the user has signed in to the web application.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      url_captcha:
                        type: string
                        description: 'Optional URL to get captcha image. This field is filled in a case when a captcha code is required to sign in.'
      tags:
        - Application-AuthorizeWithPassword
  '/Wl/Review/ReviewList/ReviewList.json':
    get:
      summary: 'Retrieves a list of reviews. Returns reviews for the specified location, or all reviews for the business if no location is given, with support for filtering by user, ordering, and pagination.'
      parameters:
        - name: i_page
          in: query
          description: 'If not specified, this request will return all review keys. If specified, this request will return detailed reviews (10 per page). This will be `null` if you only need to load the keys of the review.'
          schema:
            type: [number, "null"]
        - name: id_order
          in: query
          description: 'The order in which the review should be arranged. One of the {@link \Wl\Review\ReviewList\ReviewOrderSid} constants. If not passed use default order {@link \Wl\Review\ReviewList\ReviewOrderSid::LATEST}.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. If not specified, location key needs to be specified.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to show reviews for. If not specified, business key should be specified.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key. WellnessLiving allows staff to check low-rated reviews before posting them. Staff members can see all reviews. Clients can only see checked reviews.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_review:
                        type: array
                        description: 'List of reviews. If passed {@link \Wl\Review\ReviewList\ReviewListApi::$i_page} then the result will be full, otherwise in result will be keys: <tt>k_review</tt>, <tt>uid</tt>. <dl> <dt>bool `can_reply`</dt> <dd><tt>true</tt> if can reply to review, <tt>false</tt> otherwise.</dd> <dt>string `dl_reply`</dt> <dd>Date when staff reply to review. Can be empty string if no one replied.</dd> <dt>string `dt_add`</dt> <dd>Date when review added.</dd> <dt>float `f_rate`</dt> <dd>Rate of review.</dd> <dt>bool `is_featured`</dt> <dd><tt>true</tt> if review is featured, <tt>false</tt> otherwise.</dd> <dt>bool `is_verify`</dt> <dd><tt>true</tt> if review is verify, <tt>false</tt> otherwise.</dd> <dt>string `k_location`</dt> <dd>The location key for where the review was left/assigned. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_review`</dt> <dd>Review key. <i>Primary key in {@link \RsReviewSql} table.</i></dd> <dt>string `s_firstname`</dt> <dd>First name of user who wrote review.</dd> <dt>string `s_lastname`</dt> <dd>Last name of user who wrote review.</dd> <dt>string `s_reply`</dt> <dd>Reply for review.</dd> <dt>string `s_text`</dt> <dd>Review text.</dd> <dt>string `text_city`</dt> <dd>City from the profile of the user, who left review.</dd> <dt>string `text_reply_first`</dt> <dd>First name of staff who replied of review. Can be empty string if no one replied.</dd> <dt>string `text_reply_last`</dt> <dd>Last name of staff who replied of review. Can be empty string if no one replied.</dd> <dt>string `text_role`</dt> <dd>Staff role who replied of review. Can be empty string if no one replied.</dd> <dt>string `uid`</dt> <dd>User key who wrote review.</dd> <dt>string `url_image`</dt> <dd>Link to the image of the user who wrote review.</dd> <dt>string `url_reply_image`</dt> <dd>Link to the image of the user who replied of review. Can be empty string if no one replied.</dd> </dl>'
      tags:
        - ReviewList-ReviewList
  '/Wl/Review/ReviewList/ReviewElement.json':
    delete:
      summary: 'Deletes a review. Checks that the caller has the required permission, removes the review, and recomputes the best reviews for the associated business location.'
      parameters:
        - name: k_review
          in: query
          description: 'The review key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ReviewList-ReviewElement
    get:
      summary: 'Retrieves information about review item. Returns the review rating, text, date, author information, reply, and featured status for the specified review.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to which the review belongs.'
          schema:
            type: string
        - name: k_review
          in: query
          description: 'The review key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. May be empty in the case of a guest.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_review:
                        type: array
                        description: 'Review data: <dl> <dt>string `dt_add`</dt> <dd>Date when review was added by user.</dd> <dt>float `f_rate`</dt> <dd>Review rate.</dd> <dt>int `id_review_status`</dt> <dd>Status of the Review. One of {@link \RsReviewStatusSid} constants.</dd> <dt>bool `is_featured`</dt> <dd>If `true`, the review is featured. Otherwise, this will be `false`.</dd> <dt>bool `is_verify`</dt> <dd><tt>true</tt> if review is verified, <tt>false</tt> otherwise.</dd> <dt>string `text_firstname`</dt> <dd>Client''s first name who added review.</dd> <dt>string `text_lastname`</dt> <dd>Client''s last name who added review.</dd> <dt>string `text_reply`</dt> <dd>Staff reply. Can be empty.</dd> <dt>string `text_review`</dt> <dd>Review text.</dd> <dt>string `text_business_name`</dt> <dd>Business name</dd> <dt>string `text_response`</dt> <dd>Word "response".</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url_logo`</dt> <dd>User logo.</dd> </dl>'
      tags:
        - ReviewList-ReviewElement
  '/Wl/Search/Tag/SearchTagList.json':
    get:
      summary: 'Returns list of search tags. This is public information and method does not require any level of privileges. Returns the complete list of system-wide search tags used for filtering businesses and services in directory searches. No authentication or input parameters are required.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_search_tag:
                        type: array
                        description: 'A list of all the search tags. Each element is an array with the following keys: <dl> <dt>int `id_business_category`</dt> <dd> The business category that can use this search tag. One of the {@link \RsBusinessCategorySid} constants. This can be found in the business information found in {@link \Wl\Business\DataApi::$id_category}. </dd> <dt>string `k_search_tag`</dt> <dd>The key of the tag. The primary key in the table of tags.</dd> <dt>string `text_title`</dt> <dd>The name of the tag.</dd> </dl>'
      tags:
        - Tag-SearchTagList
  '/Wl/Search/Criteria/SearchCriteriaList.json':
    get:
      summary: 'Gets default and saved criteria for the specified entities.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Primary key in the {@link SearchTemplateSql} table. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_entity
          in: query
          description: 'Search entity CID list, separated by commas. Constants from {@link SearchEntityAbstract} subclasses.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_criteria_list_default:
                        type: array
                        description: 'A list of criteria with default (empty) data. Used to display default criteria list. For detailed description of the elements of the array see {@link \Wl\Search\Criteria\SearchCriteriaList::toArray()}.'
                      a_criteria_list_logic:
                        type: [object, "null"]
                        description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                      a_criteria_list_save:
                        type: array
                        description: 'A list of saved criteria. For detailed description of the elements of the array see {@link \Wl\Search\Criteria\SearchCriteriaList::toArray()}.'
                      has_disable:
                        type: boolean
                        description: 'Indicates that there are disabled criteria in the saved search.'
      tags:
        - Criteria-SearchCriteriaList
  '/Wl/Service/Edit/NotificationCustomization.json':
    get:
      summary: 'Prepares notification pattern data of custom confirmation/reminder notification.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of confirmation/reminder mail. One of {@link \RsMailSid} constants. `null` when not initialized.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The key of the business to which the confirmation/reminder mail belongs. `null` when not initialized.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern_live
          in: query
          description: 'Key of mail pattern. `null` for a create request.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_form:
                        type: string
                        description: 'Mail pattern form.'
                      is_mail:
                        type: boolean
                        description: 'If mail is enabled or not in the loaded live mail pattern.'
                      is_push:
                        type: boolean
                        description: 'If push is enabled or not in the loaded live mail pattern.'
                      is_sms:
                        type: boolean
                        description: 'If sms is enabled or not in the loaded live mail pattern.'
      tags:
        - Edit-NotificationCustomization
    post:
      summary: 'Save a new confirmation/notification mail pattern.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of confirmation/reminder mail. One of {@link \RsMailSid} constants. `null` when not initialized.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The key of the business to which the confirmation/reminder mail belongs. `null` when not initialized.'
          schema:
            type: [string, "null"]
        - name: text_service_name
          in: query
          description: 'Name of the service (class, event, appointment).'
          schema:
            type: string
        - name: text_service_type
          in: query
          description: 'Type of the service (class, event, appointment).'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_notification:
                  type: array
                  description: 'Information about sending a confirmation/reminder notification. <dl> <dt>array `a_login_type`</dt> <dd>Where each value, Primary key in {@link \Wl\Login\Type\Sql} table.</dd> <dt>array `a_member_group`</dt> <dd>Where each value, Primary key from {@link \Wl\Member\Group\Sql} table.</dd> <dt>int `id_mail_copy_business`</dt> <dd>Type if copy to business on `text_business_name`, one of {@link \RsMailCopySid}.</dd> <dt>bool `is_attach`</dt> <dd>Whether email attachment is enabled. `1` if yes, `0` if no.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `1` if yes, `0` if no.</dd> <dt>bool `is_login_type`</dt> <dd>Whether mail should be sent only to specific client types. `1` if yes, `0` if no.</dd> <dt>bool `is_member_group`</dt> <dd>Whether mail should be sent only to specific member group. `1` if yes, `0` if no.</dd> <dt>bool `is_send_mail`</dt> <dd>Whether to send email notification. `1` if yes, `0` if no.</dd> <dt>bool `is_send_sms`</dt> <dd>Whether to send sms notification. `1` if yes, `0` if no.</dd> <dt>bool `is_send_push`</dt> <dd>Whether to send push notification. `1` if yes, `0` if no.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_mail`</dt> <dd>Business mail.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_json`</dt> <dd>Pattern text json for email message for Unlayer editor.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_mail_pattern_live:
                        type: [string, "null"]
                        description: 'Key of mail pattern. `null` for a create request.'
      tags:
        - Edit-NotificationCustomization
    put:
      summary: 'Updates an existing confirmation/notification mail pattern.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of confirmation/reminder mail. One of {@link \RsMailSid} constants. `null` when not initialized.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The key of the business to which the confirmation/reminder mail belongs. `null` when not initialized.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern_live
          in: query
          description: 'Key of mail pattern. `null` for a create request.'
          schema:
            type: [string, "null"]
        - name: text_service_name
          in: query
          description: 'Name of the service (class, event, appointment).'
          schema:
            type: string
        - name: text_service_type
          in: query
          description: 'Type of the service (class, event, appointment).'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_notification:
                  type: array
                  description: 'Information about sending a confirmation/reminder notification. <dl> <dt>array `a_login_type`</dt> <dd>Where each value, Primary key in {@link \Wl\Login\Type\Sql} table.</dd> <dt>array `a_member_group`</dt> <dd>Where each value, Primary key from {@link \Wl\Member\Group\Sql} table.</dd> <dt>int `id_mail_copy_business`</dt> <dd>Type if copy to business on `text_business_name`, one of {@link \RsMailCopySid}.</dd> <dt>bool `is_attach`</dt> <dd>Whether email attachment is enabled. `1` if yes, `0` if no.</dd> <dt>bool `is_campaign`</dt> <dd>Whether mail should track as a part of campaign. `1` if yes, `0` if no.</dd> <dt>bool `is_login_type`</dt> <dd>Whether mail should be sent only to specific client types. `1` if yes, `0` if no.</dd> <dt>bool `is_member_group`</dt> <dd>Whether mail should be sent only to specific member group. `1` if yes, `0` if no.</dd> <dt>bool `is_send_mail`</dt> <dd>Whether to send email notification. `1` if yes, `0` if no.</dd> <dt>bool `is_send_sms`</dt> <dd>Whether to send sms notification. `1` if yes, `0` if no.</dd> <dt>bool `is_send_push`</dt> <dd>Whether to send push notification. `1` if yes, `0` if no.</dd> <dt>string `text_business_name`</dt> <dd>Business name.</dd> <dt>string `text_business_mail`</dt> <dd>Business mail.</dd> <dt>string `text_business_reply`</dt> <dd>Reply email address.</dd> <dt>string `text_campaign`</dt> <dd>Campaign name.</dd> <dt>string `text_content_mail`</dt> <dd>Email content.</dd> <dt>string `text_json`</dt> <dd>Pattern text json for email message for Unlayer editor.</dd> <dt>string `text_push`</dt> <dd>Push notification content.</dd> <dt>string `text_sms`</dt> <dd>SMS content.</dd> <dt>string `text_subject`</dt> <dd>Email subject.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-NotificationCustomization
  '/Wl/Service/ServiceList/List.json':
    get:
      summary: 'Returns list of appointment type in the business. Gets key of the business and returns all available appointment types with their names and categories.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created appointment types (if business is franchisor). <tt>true</tt> to include franchisee-created appointment types.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key, primary key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'Appointment types list: <dl> <dt>bool `hide_application`</dt> <dd> Whether service will be hidden in the White Label mobile application. <tt>true</tt> means that service will not be displayed, <tt>false</tt> otherwise. </dd> <dt>string `k_service`</dt> <dd>Appointment type identifier, primary key.</dd> <dt>string `k_service_category`</dt> <dd>Appointment type category identifier, primary key.</dd> <dt>string `text_category`</dt> <dd>Appointment type category name.</dd> <dt>string `text_service`</dt> <dd>Appointment type name.</dd> </dl>'
      tags:
        - ServiceList-List
  '/Wl/Service/Promotion/ServicePromotion.json':
    get:
      summary: 'Returns list of promotions that can be used to pay for the service.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service key. Primary key in {@link \RsServiceSql} table. The service with which you want to connect the promotions. <tt>null</tt> or <tt>0</tt> if you need to return a list of promotions for an uncreated service.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data with structure:. <dl> <dt>string `k_promotion`</dt> <dd>Promotion key. Primary key in {@link \RsPromotionSql} table.</dd> <dt>string `text_title`</dt> <dd>Title of promotion.</dd> <dt>int `id_program`</dt> <dd>One of {@link \RsProgramSid} constants.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if the promotion is related to the service, <tt>false</tt> otherwise.</dd> </dl>'
                      k_promotion_default:
                        type: [string, "null"]
                        description: 'Default promotion key. Primary key in {@link \RsPromotionSql} table. `null` if `k_service` was not provided, or if the service has no default promotion.'
      tags:
        - Promotion-ServicePromotion
  '/Wl/Service/AgeRestriction/AgeRestrictionStatus.json':
    get:
      summary: 'Checks if client can book class with age restrictions.'
      parameters:
        - name: dtu_date
          in: query
          description: 'Whether a client can book class.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key. Empty value means not set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_book:
                        type: boolean
                        description: 'Whether a client can book class.'
      tags:
        - AgeRestriction-AgeRestrictionStatus
  '/Wl/Skin/Application/Skin.json':
    delete:
      summary: 'Reset customisation form of client application. Resets the Achieve app customization skin to default values for the given business. If billing is required for the reset, a confirmation flag must be provided, and a customization fee may be scheduled.'
      parameters:
        - name: is_billing_confirm
          in: query
          description: 'Whether customization billing is confirmed. <tt>true</tt> to finish delete process even if customization fee will be billed. <tt>false</tt> if customization fee should be required for confirmation.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_link:
                        type: string
                        description: 'Name of the link to default application skin.'
      tags:
        - Application-Skin
    get:
      summary: 'Returns the application customisation skin data for the given business. Returns the full set of White Label Achieve app customization settings (colors, logos, fonts, and other branding options) for the given business, along with a flag indicating whether the business account has an active paid customization subscription.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_skin:
                        type: array
                        description: 'Skin information.'
                      has_pay:
                        type: boolean
                        description: 'Determines current business account contains amount for the application customisation.'
                      s_link:
                        type: string
                        description: 'Name of the link to default application skin.'
      tags:
        - Application-Skin
  '/Wl/Skin/Widget/SkinWidgetList.json':
    get:
      summary: 'Returns widget skins grouped by widget type for the given business. Returns all configured widget skins for the business, organized by widget type. Used to populate the widget management list so administrators can select and edit existing skins.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_widget_skin:
                        type: array
                        description: 'List of Widget skins grouped by widget type. Skin type, one of {@link \RsSkinSid} constants. The array structure: Keys - Skin type, one of {@link \RsSkinSid} constants. Values - arrays with next keys: <dl> <dt>string `k_skin`</dt> <dd>Skin key. <i>Primary key from {@link \RsSkinSql} table.</i></dd> <dt>string `text_widget_name`</dt> <dd>Widget name.</dd> </dl>'
      tags:
        - Widget-SkinWidgetList
  '/Wl/Skin/ScheduleStandard/ScheduleStandard.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_skin:
                        type: array
                        description: 'Skin data. Result of the {@link \RsSkinData::skin()} method.'
      tags:
        - ScheduleStandard-ScheduleStandard
  '/Wl/Sms/A2p/A2pRegistration.json':
    post:
      summary: 'Creates and submits A2P10DLC registration for a business. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_contact_primary:
                  type: array
                  description: 'Primary contact information. <dl> <dt>int `id_position`</dt> <dd>Contact job position. One of {@link \Core\Sms\A2p\ContactJobPositionSid}.</dd> <dt>string `text_email`</dt> <dd>Contact email address.</dd> <dt>string `text_name_first`</dt> <dd>Contact first name.</dd> <dt>string `text_name_last`</dt> <dd>Contact last name.</dd> <dt>string `text_phone`</dt> <dd>Contact phone number.</dd> <dt>string `text_title`</dt> <dd>Contact job title.</dd> </dl>'
                a_contact_secondary:
                  type: [array, "null"]
                  description: 'Secondary contact information. <dl> <dt>int `id_position`</dt> <dd>Contact job position. One of {@link \Core\Sms\A2p\ContactJobPositionSid}.</dd> <dt>string `text_email`</dt> <dd>Contact email address.</dd> <dt>string `text_name_first`</dt> <dd>Contact first name.</dd> <dt>string `text_name_last`</dt> <dd>Contact last name.</dd> <dt>string `text_phone`</dt> <dd>Contact phone number.</dd> <dt>string `text_title`</dt> <dd>Contact job title.</dd> </dl>'
                id_business_registration_identifier:
                  type: [number, "null"]
                  description: 'Business registration identifier. One of {@link BusinessRegistrationIdentifierSid}.'
                id_business_structure:
                  type: [number, "null"]
                  description: 'Business structure. One of {@link BusinessStructureSid}.'
                id_campaign_type:
                  type: [number, "null"]
                  description: 'A2P10DLC campaign type. One of {@link CampaignTypeSid}.'
                id_company_type:
                  type: [number, "null"]
                  description: 'Business company type. One of {@link CompanyTypeSid}.'
                id_stock_exchange:
                  type: [number, "null"]
                  description: 'Business stock exchange. One of {@link StockExchangeSid}.'
                k_country:
                  type: string
                  description: 'Business country key.'
                text_address:
                  type: string
                  description: 'Business address.'
                text_brand_name:
                  type: string
                  description: 'DBA / Trade Name (optional).'
                text_business_name:
                  type: string
                  description: 'Business name.'
                text_business_phone:
                  type: string
                  description: 'Business phone number.'
                text_business_registration_number:
                  type: string
                  description: 'Business registration number.'
                text_city:
                  type: string
                  description: 'Business city.'
                text_name_first:
                  type: string
                  description: 'Business owner first name.'
                text_name_last:
                  type: string
                  description: 'Business owner last name.'
                text_notification_email:
                  type: string
                  description: 'Notification email address.'
                text_postal:
                  type: string
                  description: 'Business postal/ZIP code.'
                text_region:
                  type: string
                  description: 'Business province/state.'
                text_stock_ticker:
                  type: string
                  description: 'Business stock ticker.'
                text_verification_phone:
                  type: string
                  description: 'OTP Verification phone number.'
                text_website_url:
                  type: string
                  description: 'Business website URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - A2p-A2pRegistration
    put:
      summary: 'Updates an A2P10DLC registration for a business. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_contact_primary:
                  type: array
                  description: 'Primary contact information. <dl> <dt>int `id_position`</dt> <dd>Contact job position. One of {@link \Core\Sms\A2p\ContactJobPositionSid}.</dd> <dt>string `text_email`</dt> <dd>Contact email address.</dd> <dt>string `text_name_first`</dt> <dd>Contact first name.</dd> <dt>string `text_name_last`</dt> <dd>Contact last name.</dd> <dt>string `text_phone`</dt> <dd>Contact phone number.</dd> <dt>string `text_title`</dt> <dd>Contact job title.</dd> </dl>'
                a_contact_secondary:
                  type: [array, "null"]
                  description: 'Secondary contact information. <dl> <dt>int `id_position`</dt> <dd>Contact job position. One of {@link \Core\Sms\A2p\ContactJobPositionSid}.</dd> <dt>string `text_email`</dt> <dd>Contact email address.</dd> <dt>string `text_name_first`</dt> <dd>Contact first name.</dd> <dt>string `text_name_last`</dt> <dd>Contact last name.</dd> <dt>string `text_phone`</dt> <dd>Contact phone number.</dd> <dt>string `text_title`</dt> <dd>Contact job title.</dd> </dl>'
                id_business_registration_identifier:
                  type: [number, "null"]
                  description: 'Business registration identifier. One of {@link BusinessRegistrationIdentifierSid}.'
                id_business_structure:
                  type: [number, "null"]
                  description: 'Business structure. One of {@link BusinessStructureSid}.'
                id_campaign_type:
                  type: [number, "null"]
                  description: 'A2P10DLC campaign type. One of {@link CampaignTypeSid}.'
                id_company_type:
                  type: [number, "null"]
                  description: 'Business company type. One of {@link CompanyTypeSid}.'
                id_stock_exchange:
                  type: [number, "null"]
                  description: 'Business stock exchange. One of {@link StockExchangeSid}.'
                k_country:
                  type: string
                  description: 'Business country key.'
                text_address:
                  type: string
                  description: 'Business address.'
                text_brand_name:
                  type: string
                  description: 'DBA / Trade Name (optional).'
                text_business_name:
                  type: string
                  description: 'Business name.'
                text_business_phone:
                  type: string
                  description: 'Business phone number.'
                text_business_registration_number:
                  type: string
                  description: 'Business registration number.'
                text_city:
                  type: string
                  description: 'Business city.'
                text_name_first:
                  type: string
                  description: 'Business owner first name.'
                text_name_last:
                  type: string
                  description: 'Business owner last name.'
                text_notification_email:
                  type: string
                  description: 'Notification email address.'
                text_postal:
                  type: string
                  description: 'Business postal/ZIP code.'
                text_region:
                  type: string
                  description: 'Business province/state.'
                text_stock_ticker:
                  type: string
                  description: 'Business stock ticker.'
                text_verification_phone:
                  type: string
                  description: 'OTP Verification phone number.'
                text_website_url:
                  type: string
                  description: 'Business website URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - A2p-A2pRegistration
  '/Wl/Sms/Filter/List.json':
    get:
      summary: 'Gets the blocked keyword list for the region.'
      parameters:
        - name: id_locale
          in: query
          description: 'The ID of the region. One of {@link FilterMessage::REGION}'
          schema:
            type: ["null", number]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_keyword_list:
                        type: ["null", string]
                        description: 'List of keywords to block. The keywords are seperated by newlines.'
      tags:
        - Filter-List
    post:
      summary: 'Saves the blocked keyword list for a region. Then After saving, {@link ListApi::$text_keyword_list} Is sorted alphabetically.'
      parameters:
        - name: id_locale
          in: query
          description: 'The ID of the region. One of {@link FilterMessage::REGION}'
          schema:
            type: ["null", number]
        - name: text_keyword_list
          in: query
          description: 'List of keywords to block. The keywords are seperated by newlines.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_keyword_list:
                        type: ["null", string]
                        description: 'List of keywords to block. The keywords are seperated by newlines.'
      tags:
        - Filter-List
  '/Wl/Sms/Phone/BusinessPhone.json':
    get:
      summary: 'Gets the phone number associated with a specific business. Returns the dedicated sender phone number configured for the business''s 2-Way SMS feature. Requires the `wl.business.phone` API privilege. Returns `null` or an empty value if the business has not configured a 2-Way SMS number.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_phone_sender:
                        type: [string, "null"]
                        description: 'Phone number of the business, which is added as sender for 2-Way SMS feature. `null` or an empty value if this business does not use this feature.'
      tags:
        - Phone-BusinessPhone
  '/Wl/Sms/History/Resend.json':
    post:
      summary: ''
      parameters:
        - name: k_sms_history
          in: query
          description: 'A sms history ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - History-Resend
  '/Wl/Staff/Edit/ActiveServices.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_active_services:
                        type: boolean
                        description: '`true` if staff member has any scheduled services, otherwise `false`.'
      tags:
        - Edit-ActiveServices
  '/Wl/Staff/Location/List.json':
    get:
      summary: 'Gathers a list of business staffs and locations that are available to them.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which data will be collected.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'List of business staffs with the locations available to them.'
      tags:
        - Location-List
  '/Wl/Staff/StaffView/StaffView.json':
    get:
      summary: 'Retrieves information about staff. This method can accept or one staff key {@link StaffViewApi::$k_staff} or staff list {@link StaffViewApi::$a_staff_list} but not both (exception would be thrown).'
      parameters:
        - name: a_staff_list
          in: query
          description: 'A list of staff keys.'
          schema:
            type: [array, "null"]
        - name: a_uid_staff_list
          in: query
          description: 'A list of staff user IDs.'
          schema:
            type: [array, "null"]
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'The staff member key. A staff member can work for more than one business. This key can be found using the {@link StaffListApi} endpoint.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff member user ID. A staff member can work for more than one business. This key can be found using the {@link StaffListApi} endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_day:
                        type: array
                        description: 'An array containing information about the classes this staff member is running. Each element contains another array with three elements: <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl>'
                      a_result_list:
                        type: array
                        description: 'An array listing the class sessions the staff member provides at each location. <dl> <dt>array[] `a_class_day`</dt> <dd> Contains a schedule of classes per day. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_class_day} property. <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff information. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_staff} property. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: array
                        description: 'An array containing information about the staff member. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl>'
      tags:
        - StaffView-StaffView
  '/Wl/Staff/StaffView/StaffView74.json':
    get:
      summary: 'Retrieves information about staff. This method can accept or one staff key {@link StaffViewApi::$k_staff} or staff list {@link StaffViewApi::$a_staff_list} but not both (exception would be thrown).'
      parameters:
        - name: a_uid_staff_list
          in: query
          description: 'A list of staff user IDs.'
          schema:
            type: [array, "null"]
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff member user ID. A staff member can work for more than one business. This key can be found using the {@link StaffListApi} endpoint.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class_day:
                        type: array
                        description: 'An array containing information about the classes this staff member is running. Each element contains another array with three elements: <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl>'
                      a_result_list:
                        type: array
                        description: 'An array listing the class sessions the staff member provides at each location. <dl> <dt>array[] `a_class_day`</dt> <dd> Contains a schedule of classes per day. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_class_day} property. <dl> <dt>array `a_class_period`</dt> <dd> A list of sessions in the day: <dl> <dt>string `dt_time`</dt> <dd>The session start time in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>The session duration in seconds.</dd> <dt>string `s_title`</dt> <dd>The class name.</dd> <dt>string `url_book`</dt> <dd>Direct book URL of this class.</dd> </dl> </dd> <dt>int `i_day`</dt> <dd>The day of week. One of {@link \ADateWeekSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> Staff information. Each element has a structure like in {@link \Wl\Staff\StaffView\StaffViewApi::$a_staff} property. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl> </dd> </dl>'
                      a_staff:
                        type: array
                        description: 'An array containing information about the staff member. <dl> <dt>string[] `a_location_work`</dt> <dd>A list of locations keys where staff works. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>array `a_photo`</dt> <dd> Staff''s photo. <i>See {@link \RsStaffImage::dataUid()}.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See {@link \Core\Drive\DriveImage::image()} for structure.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>int `id_gender`</dt> <dd>One of {@link \Wl\Gender\GenderSid}.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_staff`</dt> <dd>Staff key.</dd> <dt>string `s_url`</dt> <dd>Path to image. <i>Stored in {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> <dt>string `text_name`</dt> <dd>Staff name.</dd> <dt>string `uid`</dt> <dd>User key.</dd> </dl> </dd> <dt>string `html_biography`</dt> <dd>A description of the staff member. This description can include HTML tags. Same as `s_biography` offset.</dd> <dt>string `html_first`</dt> <dd>The first name of the staff.</dd> <dt>string `html_last`</dt> <dd>The last name of the staff.</dd> <dt>string `html_location_title`</dt> <dd>Name of the current staff location.</dd> <dt>int `id_gender`</dt> <dd>The staff member''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_classes_events`</dt> <dd>`true` in case when staff provides classes/events in home location, `false` otherwise.</dd> <dt>bool `is_publish_business_page`</dt> <dd>Whether staff member should be published on business pages.</dd> <dt>bool `is_schedule_enabled`</dt> <dd>`true` in case when for schedule exists for staff, `false` otherwise.</dd> <dt>string `k_location`</dt> <dd>Staff''s current location. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> <b>Deprecated</b>, use `uid_staff` instead.</dd> <dt>string `s_biography`</dt> <dd> A description of the staff member. This description can include HTML tags. <b>Deprecated</b> use `html_biography` instead of this. </dd> <dt>string `s_family`</dt> <dd> The staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `text_business_role`</dt> <dd>Name of the staff role.</dd> <dt>string `text_full_name`</dt> <dd>Full name of the staff according to business settings.</dd> <dt>string `uid`</dt> <dd> The user ID. Each staff member in WellnessLiving can also access the system as a client of their business. This is the ID number used to represent the staff member as a client. </dd> <dt>string `uid_staff`</dt> <dd>The staff user ID. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_schedule`</dt> <dd>URL to the schedule with this staff.</dd> </dl>'
      tags:
        - StaffView-StaffView74
  '/Wl/Staff/PayRate/Item.json':
    delete:
      summary: 'Deletes staff pay rate.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: k_staff_pay
          in: query
          description: 'Key of staff pay rate. Empty if you need to add new pay rate.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PayRate-Item
    post:
      summary: 'Adds new or edits exists staff pay rate.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: k_staff_pay
          in: query
          description: 'Key of staff pay rate. Empty if you need to add new pay rate.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Additional data about staff pay rate. Contains different data depending on the type {@link ItemApi::$id_staff_pay}.'
                id_staff_pay:
                  type: number
                  format: int32
                  description: 'Type of the staff pay rate. Required for adding new staff pay rates.'
                is_commission_default:
                  type: [boolean, "null"]
                  description: 'Whether the pay rate should be used as default commission rate for the staff. `null` in case when don''t need change this field.'
                is_default:
                  type: [boolean, "null"]
                  description: 'Whether the pay rate should be used as default for the staff. `null` in case when don''t need change this field.'
                is_hour_default:
                  type: [boolean, "null"]
                  description: 'Whether the pay rate should be used as hourly default for the staff. `null` in case when don''t need change this field.'
                is_report:
                  type: boolean
                  description: 'Whether pay rate should be applied to calculate staff load. Used with <var>id_staff_pay</var>={@link \RsStaffPaySid::HOUR} only.'
                k_location:
                  type: string
                  description: 'Location key. This is a location where pay rate can be used for a staff load. If empty then pay rate can be used in any location. Used only for pay rates with <var>id_staff_pay</var>={@link \RsStaffPaySid::HOUR} and <var>is_report</var>=1.'
                text_title:
                  type: string
                  description: 'Title of the pay rate.'
                uid_staff:
                  type: string
                  description: 'Key of user staff. Required for adding new staff pay rates.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PayRate-Item
    put:
      summary: 'Sets the specified pay rate as default for the staff.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: k_staff_pay
          in: query
          description: 'Key of staff pay rate. Empty if you need to add new pay rate.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_commission_default:
                  type: [boolean, "null"]
                  description: 'Whether the pay rate should be used as default commission rate for the staff. `null` in case when don''t need change this field.'
                is_default:
                  type: [boolean, "null"]
                  description: 'Whether the pay rate should be used as default for the staff. `null` in case when don''t need change this field.'
                is_hour_default:
                  type: [boolean, "null"]
                  description: 'Whether the pay rate should be used as hourly default for the staff. `null` in case when don''t need change this field.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PayRate-Item
  '/Wl/Staff/PayRate/List.json':
    get:
      summary: ''
      parameters:
        - name: id_staff_pay
          in: query
          description: 'Pay rate type. One of {@link \RsStaffPaySid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'ID of business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key to get list of pay rates for special location. Primary key in {@link \RsLocationSql}. Empty value if list of pay rates for all locations should be returned.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'TODO wl-80218: Remove this field after third parties do not use it. Staff key. Primary key in {@link \RsStaffSql}.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'Staff user ID. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_rate:
                        type: array
                        description: 'Pay rate list: <dl> <dt>int `id_staff_pay`</dt> <dd>Pay rate type, one of {@link \RsStaffPaySid} constants.</dd> <dt>bool `is_commission_default`</dt> <dd>Whether commission pay rate used as default for staff with <var>k_staff</var>.</dd> <dt>bool `is_default`</dt> <dd>Whether pay rate used as default for staff with <var>k_staff</var>.</dd> <dt>bool `is_hour_default`</dt> <dd>Whether hourly pay rate used as default for staff with <var>k_staff</var>.</dd> <dt>string `k_location`</dt> <dd> Location key. Primary key from {@link \RsLocationSql}. This is a location where pay rate can be used for a staff staff load. If empty then pay rate can be used in any location. Used only for pay rates with <var>id_staff_pay</var>={@link \RsStaffPaySid::HOUR} and <var>is_report</var>=1. </dd> <dt>string `k_staff`</dt> <dd> TODO wl-80218: Remove this field after third parties do not use it. Staff key, primary key in {@link \RsStaffSql}. deprecated Returned only for applications from legacy allow-list. Use <var>uid_staff</var>. </dd> <dt>string `k_staff_pay`</dt> <dd>Pay rate identifier, primary key in {@link \RsStaffPaySql}.</dd> <dt>string `text_staff`</dt> <dd>Name of staff member.</dd> <dt>string `text_title`</dt> <dd>Pay rate title.</dd> <dt>string `uid_staff`</dt> <dd>Staff user ID. Primary key in {@link \PassportLoginSql}.</dd> </dl>'
      tags:
        - PayRate-List
  '/Wl/Staff/PayRate/Copy.json':
    post:
      summary: 'Copies exists staff pay rates to the staff profile.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_pay:
                  type: array
                  description: 'List of pay rates to copy.'
                k_business:
                  type: string
                  description: 'Key of business.'
                uid_staff:
                  type: string
                  description: 'Key of staff user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PayRate-Copy
  '/Wl/Staff/Load/Load.json':
    get:
      summary: 'Checks if staff member is clocked in.'
      parameters:
        - name: k_business
          in: query
          description: 'Business in which information about the staff load will be requested. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'Staff member primary key. Primary key in {@link \RsStaffSql} table.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'User key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_rate:
                        type: array
                        description: 'Staff pay rates info. See {@link \Wl\Staff\PayRate\PayRate::staffHourlyCheckInRateGet()}.'
                      a_work:
                        type: array
                        description: 'Staff work information. See {@link rs_staff_load_check_work_trx()}.'
                      is_time_manage:
                        type: boolean
                        description: 'Whether staff member can manage staff clock in/out for other staff members.'
                      text_work_start:
                        type: string
                        description: 'Message, when the work has been started.'
      tags:
        - Load-Load
    put:
      summary: 'Clocks staff member in (if he is clocked out) or out (if he is clocked in).'
      parameters:
        - name: k_business
          in: query
          description: 'Business in which information about the staff load will be requested. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'Staff member primary key. Primary key in {@link \RsStaffSql} table.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'User key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_location:
                  type: [string, "null"]
                  description: 'Location primary key. Primary key in {@link \RsLocationSql} table.'
                k_staff_pay:
                  type: string
                  description: 'Pay rate identifier. Primary key in {@link \RsStaffPaySql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_work:
                        type: array
                        description: 'Staff work information. See {@link rs_staff_load_check_work_trx()}.'
                      is_start:
                        type: boolean
                        description: '<tt>true</tt> if staff member has been clocked in; <tt>false</tt> if clocked out.'
      tags:
        - Load-Load
  '/Wl/Staff/Load/Load74.json':
    get:
      summary: 'Checks if staff member is clocked in.'
      parameters:
        - name: k_business
          in: query
          description: 'Business in which information about the staff load will be requested. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'User key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_rate:
                        type: array
                        description: 'Staff pay rates info. See {@link \Wl\Staff\PayRate\PayRate::staffHourlyCheckInRateGet()}.'
                      a_work:
                        type: array
                        description: 'Staff work information. See {@link rs_staff_load_check_work_trx()}.'
                      is_time_manage:
                        type: boolean
                        description: 'Whether staff member can manage staff clock in/out for other staff members.'
                      text_work_start:
                        type: string
                        description: 'Message, when the work has been started.'
      tags:
        - Load-Load74
    put:
      summary: 'Clocks staff member in (if he is clocked out) or out (if he is clocked in).'
      parameters:
        - name: k_business
          in: query
          description: 'Business in which information about the staff load will be requested. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'User key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_location:
                  type: [string, "null"]
                  description: 'Location primary key. Primary key in {@link \RsLocationSql} table.'
                k_staff_pay:
                  type: string
                  description: 'Pay rate identifier. Primary key in {@link \RsStaffPaySql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_work:
                        type: array
                        description: 'Staff work information. See {@link rs_staff_load_check_work_trx()}.'
                      is_start:
                        type: boolean
                        description: '<tt>true</tt> if staff member has been clocked in; <tt>false</tt> if clocked out.'
      tags:
        - Load-Load74
  '/Wl/Staff/IpRestriction/IpRestriction.json':
    get:
      summary: 'Determines if the logged-in user''s IP is allowed by the passed list of IP restrictions.'
      parameters:
        - name: a_ip_restriction
          in: query
          description: 'List of IP restrictions to check against the current logged-in user. <dl> <dt>array `a_role`</dt> <dd> Array of arrays that contain the following keys: <dl> <dt>int `id_privilege_role`</dt> <dd>ID of a default system role from {@link \RsPrivilegeRoleSid} that this IP restriction applies to.</dd> <dt>string `k_business_role`</dt> <dd>Key of a custom business role this IP restriction applies to.</dd> </dl> </dd> <dt>string `ip_allow`</dt> <dd>The IP address or IP range to allow.</dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_user_ip_allowed:
                        type: boolean
                        description: 'Indicates if the logged-in user''s IP is allowed by the passed list of IP restrictions.'
      tags:
        - IpRestriction-IpRestriction
  '/Wl/Staff/StaffList/StaffList.json':
    get:
      summary: 'Returns the list of staff members for the given business. Returns all active (or optionally inactive) staff members for the business, including their name, role, assigned services, contact details, and location assignments. Can be filtered to only staff who have a specific privilege, and whether access-level checks should be applied when building the result.'
      parameters:
        - name: a_privilege
          in: query
          description: 'A list of privileges to filter staff members by. This property acts as a filter. Only staff members possessing all the privileges specified in this list will be included in the result. Each element in the array represents a privilege ID, and should be one of {@link PrivilegeSid} constants.'
          schema:
            type: array
        - name: is_check_staff_access
          in: query
          description: 'Determines that only staff members which the current user has access to should be retrieved. `true` if only staff members which the current user has access to should be retrieved. `false` otherwise.'
          schema:
            type: boolean
        - name: is_staff_inactive
          in: query
          description: 'Whether inactive and removed staff members are available. If `true` inactive and removed staff members are available. `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'Information about staff members. Each array index is the staff member keys. Each array element is an array containing the following fields: <dl> <dt>string[] `a_pay_rate`</dt> <dd>A list of internal pay rate keys applicable to the staff member.</dd> <dt>array `a_staff_service`</dt> <dd> A list of all services, provided by the staff member. Each element is an array with structure: <dl> <dt>string `k_service`</dt> <dd>Service key. <i>Primary in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff_pay`</dt> <dd>Staff pay key for this service. <i>Primary in {@link \RsStaffPaySql} table.</i></dd> </dl> </dd> <dt>string `html_name`</dt> <dd>Staff name.</dd> <dt>int `i_order`</dt> <dd>Order for sorting.</dd> <dt>bool `is_appointment`</dt> <dd>Whether this staff provides appointments service.</dd> <dt>bool `is_class`</dt> <dd>Whether this staff provides class service.</dd> <dt>bool `is_event`</dt> <dd>Whether this staff provides events service.</dd> <dt>string `k_staff`</dt> <dd> The staff key. <i>Primary key in {@link \RsStaffSql} table.</i> deprecated Use `uid_staff` instead. Returned only for backward-compatible applications. </dd> <dt>string `s_name`</dt> <dd> The staff member first name. If there are rights, the full first name, if not, then depending on the business settings. </dd> <dt>string `s_position`</dt> <dd>The staff member position in the organization.</dd> <dt>string `s_surname`</dt> <dd> The first letter of the staff member surname. If there are rights, the full surname, if not, then depending on the business settings. </dd> <dt>string|null `s_surname_full`</dt> <dd> The entire surname of the staff member. This will be `null` if private staff member information isn''t accessible. </dd> <dt>string `text_name_full`</dt> <dd>The full name of the staff member.</dd> <dt>string `uid`</dt> <dd> The user key. Each staff member in WellnessLiving can also access the system as a client of their business. This is the key used to represent the staff member as a client. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> <dt>string `uid_staff`</dt> <dd>The staff member user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `url_image`</dt> <dd>Url link to user photo, or empty string, if photo is not loaded.</dd> </dl>'
      tags:
        - StaffList-StaffList
  '/Wl/Staff/Privilege/PrivilegeList.json':
    get:
      summary: 'Returns the list of privileges for the current user in the given business. Returns the complete access profile of the signed-in user for the given business: staff privileges by ID, named admin privileges, and a flag indicating super-admin status. Can only be called for the currently authenticated user, not on behalf of another user.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to get privileges for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to get privileges for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_privilege_passport:
                        type: object
                        description: 'List of privileges, if user is administrator. There is no certain list of admin privileges, as they are name-based. If you need to check access to certain place contact WellnessLiving integration team.'
                      a_privilege_staff:
                        type: array
                        description: 'List of privileges, if the given user is a staff member in the give business.'
                      is_admin:
                        type: boolean
                        description: 'Whether this user is a super-administrator because he is a studio staff member. `true`, if this user is a super administrator because he is a studio staff member. Super administrators have all privileges. `false` if this is an ordinary user.'
      tags:
        - Privilege-PrivilegeList
  '/Wl/Staff/Role/StaffRole.json':
    get:
      summary: 'Gets list of staff roles.'
      parameters:
        - name: is_all
          in: query
          description: 'Whether need return all roles in the business, else if you need return only available roles for current user.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_role:
                        type: array
                        description: 'List of staff roles. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. Primary key in {@link \RsBusinessRoleSql} table.</dd> <dt>string `text_title`</dt> <dd>Title of staff role.</dd> </dl>'
                      can_privilege:
                        type: boolean
                        description: 'Whether current user has privilege to change staff roles.'
      tags:
        - Role-StaffRole
  '/Wl/User/Info/UserInfo.json':
    get:
      summary: 'Retrieves information about user. Returns profile data for a WellnessLiving user, including name, email, phone, photo, gender, login type, custom profile fields, member groups, and calendar integration status. Supports single-user mode and batch mode for loading multiple profiles in one request.'
      parameters:
        - name: a_user_list
          in: query
          description: 'A list of user keys.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The key of the business. This may be empty if system-wide information is needed.'
          schema:
            type: string
        - name: not_cached
          in: query
          description: '`true` to ignore cache and load information from the database directly. `false` - otherwise.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The key of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of the custom user fields. Each value is: <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Name of the field. Will be <tt>null</tt> for general fields. Their titles are static.</dd> <dt>string `text_value`</dt> <dd>Value of the field. This is the key of the selected item for `select` and `radio` elements.</dd> <dt>string `text_value_title`</dt> <dd>Title of the selected option for `select` and `radio` elements. Unused for other elements.</dd> </dl>'
                      a_member_group:
                        type: array
                        description: 'List of member groups that the user belongs to.'
                      a_photo:
                        type: array
                        description: 'Information about the user''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>string `url_photo`</dt> <dd>The URL of the photo.</dd> </dl>'
                      a_result_list:
                        type: array
                        description: 'List of user''s data. <dl> <dt>array `a_custom_field`</dt> <dd> List of the custom user fields. Each value is: <dl> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in {@link \RsFieldSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Name of the field. Will be <tt>null</tt> for general fields. Their titles are static.</dd> <dt>string `text_value`</dt> <dd>Value of the field.</dd> </dl> </dd> <dt>string[] `a_member_group`</dt> <dd> List of member groups that the user belongs to. <i>Primary keys from {@link \Wl\Member\Group\Sql} table.</i> </dd> <dt>array `a_photo`</dt> <dd> Information about the user''s photo. <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>string `url_photo`</dt> <dd>The URL of the photo.</dd> </dl> </dd> <dt>string `dt_add`</dt> <dd>The date the user was added, given in UTC time.</dd> <dt>string `dt_birth`</dt> <dd>The user''s birthday. This will be `null` if the birthday isn''t set yet.</dd> <dt>int `id_gender`</dt> <dd>The ID of the user''s gender. One of the {@link \AGenderSid} constants.</dd> <dt>bool `is_customer_new`</dt> <dd> This will be `true` if the user has never made purchases or reservations in this business. Otherwise, this will be `false`. </dd> <dt>bool `is_traveller`</dt> <dd> This will be `true` if the user is a traveler. A traveler is someone whose home location isn''t the current location in the Enterprise business. </dd> <dt>string `k_business`</dt> <dd>The key of the business. This may be empty if system-wide information is needed.</dd> <dt>string `k_login_type`</dt> <dd>The key of the login type. The login type describes the user''s client type in this business.</dd> <dt>string `s_first_name`</dt> <dd>The user''s first name.</dd> <dt>string `s_last_name`</dt> <dd>The user''s last name.</dd> <dt>string `s_mail`</dt> <dd>The user''s email address.</dd> <dt>string `s_member`</dt> <dd> The user''s member ID in the business. Also referred to as the client ID in the client''s profile. This value is set by the business and separate from the <var>uid</var> value. </dd> <dt>string `s_phone`</dt> <dd>The user''s phone number.</dd> <dt>string `s_phone_home`</dt> <dd>The user''s home phone number.</dd> <dt>string `s_phone_work`</dt> <dd>The user''s work phone number.</dd> <dt>string `uid`</dt> <dd>The key of the user.</dd> <dt>string `url_photo`</dt> <dd>The URL for the user''s photo.</dd> </dl>'
                      can_introductory:
                        type: boolean
                        description: 'Whether the user can purchase introductory offers. `true` if user can purchase introductory offers, `false` otherwise.'
                      dt_add:
                        type: string
                        description: 'The date the user was added, given in UTC time.'
                      dt_birth:
                        type: string
                        description: 'The user''s birthday. This will be `null` if the birthday isn''t set yet.'
                      has_discount:
                        type: [boolean, "null"]
                        description: 'Whether client''s login type has a discount. `true` - login type has a discount, `false` - otherwise. This will be `null` if a client has no assigned login type.'
                      id_gender:
                        type: number
                        format: int32
                        description: 'The ID of the user''s gender. One of the {@link \AGenderSid} constants. This will be `null` if the gender isn''t set yet.'
                      is_calendar_google:
                        type: boolean
                        description: 'This will be `true` if the user has Google Calendar linked to their account; otherwise, `false`.'
                      is_calendar_microsoft:
                        type: boolean
                        description: 'This will be `true` if the user has Microsoft Calendar linked to their account; otherwise, `false`.'
                      is_customer_new:
                        type: boolean
                        description: 'This will be `true` if the user has never made purchases or reservations in this business. Otherwise, this will be `false`.'
                      is_traveller:
                        type: boolean
                        description: 'This will be `true` if the user is a traveler. A traveler is someone whose home location isn''t the current location in the Enterprise business.'
                      k_city:
                        type: [string, "null"]
                        description: 'City key. `null` if "address" field is disabled in the business.'
                      k_login_type:
                        type: string
                        description: 'The key of the login type. The login type describes the user''s client type in this business.'
                      s_first_name:
                        type: string
                        description: 'The user''s first name.'
                      s_last_name:
                        type: string
                        description: 'The user''s last name.'
                      s_mail:
                        type: string
                        description: 'The user''s email address.'
                      s_member:
                        type: string
                        description: 'The user''s member ID in the business. Also referred to as the client ID in the client''s profile. This value is set by the business and separate from the <var>uid</var> value.'
                      s_phone:
                        type: string
                        description: 'The user''s phone number.'
                      s_phone_home:
                        type: string
                        description: 'The user''s home phone number.'
                      s_phone_work:
                        type: string
                        description: 'The user''s work phone number.'
                      text_address:
                        type: [string, "null"]
                        description: 'Address inside a city. `null` if "address" field is disabled in the business.'
                      text_city:
                        type: [string, "null"]
                        description: 'City name. `null` if "address" field is disabled in the business.'
                      text_login_type:
                        type: string
                        description: 'Login type title. Empty if a client has no client type assigned.'
                      text_postal:
                        type: [string, "null"]
                        description: 'Postal code. `null` if "address" field is disabled in the business.'
                      uid:
                        type: string
                        description: 'The key of the user.'
                      url_photo:
                        type: string
                        description: 'The URL for the user''s photo.'
      tags:
        - Info-UserInfo
  '/Wl/User/Info/UserListInfo.json':
    get:
      summary: 'Retrieves information about users by email.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'A user''s email address.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_user_data:
                        type: array
                        description: 'Information about the user''s. The information returned has the following structure: <dl> <dt>array `a_relation`</dt> <dd>Keys are UIDs or relatives, values - relation type from {@link \RsFamilyRelationSid}.</dd> <dt>int `id_gender`</dt> <dd> Gender type, One of {@link \Wl\Gender\GenderSid} constants. Empty means that client is not selected gender type. </dd> <dt>bool `is_address_inherited`</dt> <dd>Whether an address of a user is inherited from a relative `true` - inherited, `false` - otherwise.</dd> <dt>bool `is_inherited`</dt> <dd>Whether an email of a user is inherited from a relative. `true` - inherited, `false` - otherwise.</dd> <dt>bool `is_phone_inherited`</dt> <dd>Whether phones of a user are inherited from a relative `true` - inherited, `false` - otherwise.</dd> <dt>string `k_city`</dt> <dd>City key. Primary key in the {@link \AGeoSql} table.</dd> <dt>string `text_address`</dt> <dd>User''s address.</dd> <dt>string `text_birth`</dt> <dd>User''s birth date.</dd> <dt>string `text_cell_phone`</dt> <dd>User''s phone.</dd> <dt>string `text_city`</dt> <dd>User''s city.</dd> <dt>string `text_business_uid_key`</dt> <dd> Compound key delimited wit a colon. First part is business key. Primary key in {@link \RsBusinessSql} table. Second part - uid of a user. Primary key in {@link \PassportLoginSql} table. </dd> <dt>string `text_home_phone`</dt> <dd>User''s home phone.</dd> <dt>string `text_mail`</dt> <dd>User''s email.</dd> <dt>string `text_member`</dt> <dd>User''s member ID.</dd> <dt>string `text_name_first`</dt> <dd>User''s first name.</dd> <dt>string `text_name_full`</dt> <dd>User''s full name.</dd> <dt>string `text_name_last`</dt> <dd>User''s last name.</dd> <dt>string `text_postal`</dt> <dd>User''s postal.</dd> <dt>string `text_work_phone`</dt> <dd>User''s work phone.</dd> <dt>string `url_photo`</dt> <dd>User''s photo url.</dd> </dl>'
      tags:
        - Info-UserListInfo
  '/Wl/User/Info/UserIntegration.json':
    get:
      summary: 'Retrieves information about the user belongs to certain integrations. Returns which third-party booking integrations (Classpass, Gympass, Reserve With Google) the user is currently connected to within the given business context. Returns `null` when the user has no active integration memberships.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_integration:
                        type: [array, "null"]
                        description: 'Information about the integrations the user is connected to. The information returned has the following structure: `null` if the user does not belong to any integration. <dl> <dt>bool `is_classpass`</dt> <dd>`true` if user use the Classpass integration, `false` - otherwise.</dd> <dt>bool `is_gympass`</dt> <dd>`true` if user use the Gympass integration, `false` - otherwise.</dd> <dt>bool `is_reserve_with_google`</dt> <dd>`true` if user use the Reserve With Google integration, `false` - otherwise.</dd> </dl>'
      tags:
        - Info-UserIntegration
  '/Wl/User/Referrer/Referrer.json':
    get:
      summary: 'Searches for a referrer by the given search string and returns their profile information. Searches for a matching client by member ID, email address, phone number, or encrypted user key, and returns their name, email, phone, and photo. Returns empty or `null` fields when no matching client is found. Search is rate-limited for guest and client callers.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the current business.'
          schema:
            type: string
        - name: s_search
          in: query
          description: 'The string to be used for searching for a referrer.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_photo:
                        type: array
                        description: 'Information about the referrer''s photo. The information returned has the following structure: <dl> <dt>int `i_height`</dt> <dd>The height of the photo.</dd> <dt>int `i_width`</dt> <dd>The width of the photo.</dd> <dt>string `url_photo`</dt> <dd>The URL of the photo.</dd> </dl>'
                      s_email:
                        type: string
                        description: 'The email address of the referrer.'
                      s_member:
                        type: string
                        description: 'The business client ID of the referrer.'
                      s_name_first:
                        type: string
                        description: 'The first name of the referrer.'
                      s_name_last:
                        type: string
                        description: 'The last name of the referrer.'
                      s_phone:
                        type: string
                        description: 'The phone number of the referrer.'
                      text_name_public:
                        type: [string, "null"]
                        description: 'Composes name of the referrer for public usage. `null` if the referrer is not found.'
                      uid_referrer:
                        type: string
                        description: 'The referrer''s user key.'
      tags:
        - Referrer-Referrer
  '/Wl/User/Referrer/ReferralInfo.json':
    get:
      summary: 'Returns referral count, total referral points, and shareable referral link for the given user. Computes result fields for the referrer identified: - number of invited referrals; - reward points earned for referral registrations; - the shareable invite link with the referrer''s encrypted user key.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key of the referrer whose statistics are being requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_point:
                        type: number
                        format: int32
                        description: 'Total reward points earned by this user for referral registration ({@link \RsRewardScoreSid::REFER_REGISTER}).'
                      i_referral:
                        type: number
                        format: int32
                        description: 'Number of invited referrals.'
                      url_referral:
                        type: string
                        description: 'Shareable invite link for the referrer. Navigates to the business registration page with the referrer''s encrypted user key as the `s_referral` parameter. When a new client registers through this link, the "Referred By" field is pre-filled with this user.'
      tags:
        - Referrer-ReferralInfo
  '/Wl/User/Tracking/UserTracker.json':
    get:
      summary: 'This method updates screen resolution.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: s_id
          in: query
          description: 'Session ID to track with. Its value is autogenerated when we call flow start method.'
          schema:
            type: [string, "null"]
        - name: s_resolution
          in: query
          description: 'Screen resolution in form of width x length. Example: 1024x678.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tracking-UserTracker
    post:
      summary: 'Starts or stops tracking.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: s_id
          in: query
          description: 'Session ID to track with. Its value is autogenerated when we call flow start method.'
          schema:
            type: [string, "null"]
        - name: s_resolution
          in: query
          description: 'Screen resolution in form of width x length. Example: 1024x678.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_flow:
                  type: number
                  format: int32
                  description: 'Tracked user flow ID. Constant from {@link FlowSid}.'
                is_end:
                  type: boolean
                  description: 'Flag indicating the end of tracking.'
                is_start:
                  type: boolean
                  description: 'Flag indicating the start of tracking.'
                s_step:
                  type: string
                  description: 'Step of the flow, that the user is now on.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_id:
                        type: [string, "null"]
                        description: 'Session ID to track with. Its value is autogenerated when we call flow start method.'
      tags:
        - Tracking-UserTracker
    put:
      summary: 'Updates tracking of a user flow.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: s_id
          in: query
          description: 'Session ID to track with. Its value is autogenerated when we call flow start method.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_step:
                  type: [number, "null"]
                  description: 'Current step number. `null` if the number of steps is not set or should not be updated.'
                i_step_all:
                  type: [number, "null"]
                  description: 'Total number of steps. `null` if the number of steps is not set or should not be updated.'
                s_step:
                  type: string
                  description: 'Step of the flow, that the user is now on.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tracking-UserTracker
  '/Wl/Video/Category/CategoryElement.json':
    delete:
      summary: 'Deletes the specified video category. Permanently removes the video category and its localized CMS records. Cannot be deleted if the category is currently configured as the cloud recording destination for the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_category
          in: query
          description: 'The category key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Category-CategoryElement
    get:
      summary: 'Returns the data for the specified video category. Returns the title, sort order, access restriction flags (by login type and member group), and whether this category is designated as the cloud recording destination for the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_category
          in: query
          description: 'The category key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_type:
                        type: array
                        description: 'A list of client and member types who can access videos from the category.'
                      a_member_group:
                        type: array
                        description: 'A list of member groups who can access videos from the category.'
                      i_order:
                        type: number
                        format: int32
                        description: 'The category''s placement in the business''s list of categories.'
                      is_cloud_recording:
                        type: boolean
                        description: 'This will be `true` if the video category is for cloud session recordings. Otherwise, this will be `false`.'
                      is_login_type:
                        type: boolean
                        description: 'This will be `true` if some client or member types can grant access to the video category. Otherwise, this will be `false`.'
                      is_member_group:
                        type: boolean
                        description: 'This will be `true` if some member groups can grant access to the video category. Otherwise, this will be `false` if no member groups can grant access to the video category.'
                      k_video_category:
                        type: string
                        description: 'The category key.'
                      text_title:
                        type: string
                        description: 'The category name.'
      tags:
        - Category-CategoryElement
    put:
      summary: 'Creates or updates a video category. Saves the category title and access restriction settings. When no category key is supplied, a new category is created; when an existing key is supplied, that category is updated. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_category
          in: query
          description: 'The category key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_type:
                  type: array
                  description: 'A list of client and member types who can access videos from the category.'
                a_member_group:
                  type: array
                  description: 'A list of member groups who can access videos from the category.'
                is_cloud_recording:
                  type: boolean
                  description: 'This will be `true` if the video category is for cloud session recordings. Otherwise, this will be `false`.'
                is_login_type:
                  type: boolean
                  description: 'This will be `true` if some client or member types can grant access to the video category. Otherwise, this will be `false`.'
                is_member_group:
                  type: boolean
                  description: 'This will be `true` if some member groups can grant access to the video category. Otherwise, this will be `false` if no member groups can grant access to the video category.'
                text_title:
                  type: string
                  description: 'The category name.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_category:
                        type: string
                        description: 'The category key.'
      tags:
        - Category-CategoryElement
  '/Wl/Video/Category/CategoryList.json':
    get:
      summary: 'Returns the list of video categories for the business. Returns all video categories for the business with their titles, video counts, and cloud recording assignment. In frontend mode, only categories accessible to the current user are included. Results can be filtered by name and optionally limited to non-empty categories.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: is_skip_empty_group
          in: query
          description: 'If `true`, groups that are missing videos won''t be displayed. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: text_filter
          in: query
          description: 'The filter phrase used to filter categories by name.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_video_category:
                        type: array
                        description: 'The business video library categories as found in {@link CategoryListApi::$k_business}. Each element has the following structure: <dl> <dt>bool `can_delete`</dt> <dd>Determines whether the video category can be deleted.</dd> <dt>int `i_video`</dt> <dd>The number of videos.</dd> <dt>string `k_video_category`</dt> <dd>The key of the video category.</dd> <dt>string `k_video_category_full`</dt> <dd>The string key of the video category.</dd> <dt>string `text_title`</dt> <dd>The title of the category.</dd> </dl>'
      tags:
        - Category-CategoryList
    put:
      summary: 'Updates the order of video categories. Reorders the video library categories for the business according to the provided list. Requires backend access with the video library management privilege.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of shared video category keys displayed in the order to be saved. Values can be: * <b>[Deprecated]</b> String keys in old format. <i>See {@link DbShardKey}.</i> * String keys in new format. <i>See {@link DbKey}.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Category-CategoryList
  '/Wl/Video/Setting/Setting.json':
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_quality:
                  type: [number, "null"]
                  description: 'Quality ID.'
                s_video_tab_label:
                  type: string
                  description: 'Label for Video tab.'
                show_calorie:
                  type: boolean
                  description: '<tt>true</tt> if calories is available for video, <tt>false</tt> otherwise.'
                show_level:
                  type: boolean
                  description: '<tt>true</tt> if levels is available for video, <tt>false</tt> otherwise.'
                show_view:
                  type: boolean
                  description: '<tt>true</tt> if calories is available for video, <tt>false</tt> otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Setting-Setting
  '/Wl/Video/Watch/Watch.json':
    post:
      summary: 'Records the start of a video watch session for the current user. Creates a new watch record for the user and video, verifying that the user has access to the video under their current membership. Returns the watch key that the client must use for subsequent progress updates via {@link WatchApi::put()}. Admin users are silently skipped - no record is created for them.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_current_time:
                  type: number
                  format: int32
                  description: 'The current time in seconds the user is at in the video.'
                id_source:
                  type: number
                  format: int32
                  description: 'The source ID.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_video:
                  type: string
                  description: 'The video string key: * [Deprecated] String key in old format. <i>See {@link DbShardKey::keyString()}.</i> * String key in new format. <i>See {@link DbKey::generate()} or {@link DbShardKey::keyBinary()}.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_watch:
                        type: string
                        description: 'The video watch key.'
      tags:
        - Watch-Watch
    put:
      summary: 'Updates the watch progress (current position and total watched time) for an existing watch record. Advances the stored playback position and accumulated watch time for the given watch record. The total watched time can only increase; updates that report a smaller value than what is already stored are silently ignored to handle out-of-order requests.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_current_time:
                  type: number
                  format: int32
                  description: 'The current time in seconds the user is at in the video.'
                i_watched:
                  type: number
                  format: int32
                  description: 'The duration in seconds the user has watched the video for.'
                k_business:
                  type: string
                  description: 'The business key.'
                k_video_watch:
                  type: string
                  description: 'The video watch key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Watch-Watch
  '/Wl/Video/MediaConvert/PathElement.json':
    post:
      summary: 'Getting the list of converted file paths and registering them as variants.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_file_path:
                  type: string
                  description: 'List of fully qualified file paths.'
                k_business:
                  type: string
                  description: 'Business key.'
                k_video:
                  type: string
                  description: 'String key of the video.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - MediaConvert-PathElement
  '/Wl/Video/Subscription/Upgrade.json':
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link FitvidSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscription-Upgrade
  '/Wl/Video/Level/Level.json':
    delete:
      summary: 'Deletes the specified video level. Permanently removes the video level and unassigns it from all videos. If any videos are currently assigned to this level, a confirmation flag must be set; otherwise the API throws a confirmation-required error so the caller can prompt the user before proceeding.'
      parameters:
        - name: is_delete_confirm
          in: query
          description: 'If `true`, confirmation is required to delete videos. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_level
          in: query
          description: 'The video level key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-Level
    post:
      summary: 'Creates a new video level. Creates a difficulty level entry for the business video library. The new level is appended at the end of the current sort order and can be reordered afterwards using {@link LevelListApi::put()}. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_level
          in: query
          description: 'The video level key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video level title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_level:
                        type: string
                        description: 'The video level key.'
      tags:
        - Level-Level
    put:
      summary: 'Updates the specified video level. Renames an existing difficulty level in the business video library. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_level
          in: query
          description: 'The video level key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video level title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-Level
  '/Wl/Video/Level/LevelList.json':
    get:
      summary: 'Returns the list of video levels for the business. Returns all difficulty levels configured for the business video library, sorted by their current display order. Used to populate level pickers when creating or editing videos.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_level_list:
                        type: array
                        description: 'A list of video levels with the following structure: <dl> <dt>string `k_video_level`</dt> <dd>The video level key.</dd> <dt>string `text_title`</dt> <dd>The video level title.</dd> </dl>'
      tags:
        - Level-LevelList
    put:
      summary: 'Updates the order of video levels. Reorders the difficulty levels for the business video library according to the provided list. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of video level keys in the order to be saved.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-LevelList
  '/Wl/Video/Level/LevelListFilter.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_level_list:
                        type: array
                        description: 'A list of video levels with the following structure: <dl> <dt>string `k_video_level`</dt> <dd>The video level key.</dd> <dt>string `text_title`</dt> <dd>The video level title.</dd> </dl>'
      tags:
        - Level-LevelListFilter
    put:
      summary: 'Updates the order of video levels. Reorders the difficulty levels for the business video library according to the provided list. Requires backend access with the video library management privilege.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_order:
                  type: array
                  description: 'A list of video level keys in the order to be saved.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Level-LevelListFilter
  '/Wl/Video/Tag/Tag.json':
    delete:
      summary: 'Deletes the specified video tag. Permanently removes the video tag from the business library. If any videos are currently assigned to this tag, a confirmation flag must be set; otherwise the API throws a confirmation-required error so the caller can prompt the user before proceeding.'
      parameters:
        - name: is_delete_confirm
          in: query
          description: 'If `true`, confirmation is required to delete videos. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_tag
          in: query
          description: 'The video tag key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tag-Tag
    post:
      summary: 'Creates a new video tag. Adds a new content tag to the business video library for use when categorizing videos. Requires backend access and an active video subscription with at least the basic plan.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_tag
          in: query
          description: 'The video tag key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video tag title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_video_tag:
                        type: string
                        description: 'The video tag key.'
      tags:
        - Tag-Tag
    put:
      summary: 'Updates the specified video tag. Renames an existing content tag in the business video library. Requires backend access and an active video subscription with at least the basic plan.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_video_tag
          in: query
          description: 'The video tag key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'The video tag title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Tag-Tag
  '/Wl/Video/Tag/TagList.json':
    get:
      summary: 'Returns the list of video tags for the business. Returns all content tags configured for the business video library. Used to populate tag pickers when creating or editing videos. Requires an active video subscription.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tag_list:
                        type: array
                        description: 'A list of video tags with the following structure: <dl> <dt>string `k_video_tag`</dt> <dd>The video tag key.</dd> <dt>string `text_title`</dt> <dd>The video tag title.</dd> </dl>'
      tags:
        - Tag-TagList
  '/Wl/Zoom/Recover/ZoomRecover.json':
    get:
      summary: 'Gets a list of unloaded recordings.'
      parameters:
        - name: dl_meeting
          in: query
          description: 'Date by which the list of meetings must be returned.'
          schema:
            type: string
        - name: k_business_use
          in: query
          description: 'Key of the business for which meeting was created.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_meeting_list:
                        type: array
                        description: 'List of unloaded meeting records. Each value is: <dl> <dt>bool `is_task_exist`</dt> <dd>`true` if this task to upload video to FitVID exists in the task queue, `false` - otherwise.</dd> <dt>string `k_business_use`</dt> <dd>Key of the business for which meeting was created. Primary key in {@link \RsBusinessSql} table.</dd> <dt>string `s_meeting_id`</dt> <dd>Zoom meeting ID.</dd> <dt>string `s_recording_id`</dt> <dd>Id of the recording.</dd> <dt>string `text_name`</dt> <dd>Zoom meeting title.</dd> <dt>string `text_start`</dt> <dd>Date of the session start in the given format in the region from {@link \Core\Locale\LocaleConfig::id()}.</dd> </dl>'
      tags:
        - Recover-ZoomRecover
    post:
      summary: 'Recover meeting recordings from cloud.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_meeting_id:
                  type: string
                  description: 'Zoom meeting ID.'
                s_recording_id:
                  type: string
                  description: 'ID of the recording.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Recover-ZoomRecover
  '/Wl/Location/TimeZone/TimeZone.json':
    get:
      summary: 'Gets timezone of passed coordinates.'
      parameters:
        - name: f_latitude
          in: query
          description: 'Latitude of the location.'
          schema:
            type: number
            format: float
        - name: f_longitude
          in: query
          description: 'Longitude of the location.'
          schema:
            type: number
            format: float
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_timezone:
                        type: string
                        description: 'User''s timezone key, primary key in {@link \AGeoTimezoneSql} table.'
                      text_time_zone_google_id:
                        type: string
                        description: 'The timezone identifier according to the ION standard, which was determined through the Google API.'
                      text_time_zone_name:
                        type: string
                        description: 'The system timezone name.'
      tags:
        - TimeZone-TimeZone
    post:
      summary: 'Contact Google Maps API to get the timezone for a given location.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_location:
                  type: [string, "null"]
                  description: 'Location''s address. <tt>null</tt> if not set yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_timezone:
                        type: [array, "null"]
                        description: 'Time zone information. Relevant parts of the geo code request result: <tt>null</tt> if no result. <dl> <dt>int|null `i_dst_offset`</dt> <dd> Maximum daylight savings offset in seconds. A value of 3600 indicates this location uses daylight savings time, <tt>null</tt> if no dst info requested. </dd> <dt>int `i_raw_offset`</dt> <dd>The timezone''s offset in seconds from UTC. Not all time zones will be evenly divisible by 3600.</dd> <dt>string `text_time_zone_id`</dt> <dd>The time zone''s ID in IANA.</dd> <dt>string `text_time_zone_name`</dt> <dd>The time zone''s name.</dd> <dt>string `text_time_zone_name_google`</dt> <dd>The time zone''s name in result of Google API.</dd> </dl>'
                      is_admin:
                        type: boolean
                        description: '`true` if user can skip the check and save not recommended timezone. `false` - otherwise.'
      tags:
        - TimeZone-TimeZone
  '/Wl/Location/View/View.json':
    get:
      summary: 'Returns detailed information about the specified location. Returns the full profile of a location, including address, coordinates, timezone, contact information, business hours, logo, slide images, amenities, social media links, and a description.'
      parameters:
        - name: i_logo_height
          in: query
          description: 'Maximum location image height.'
          schema:
            type: number
            format: int32
        - name: i_logo_width
          in: query
          description: 'Maximum location image width.'
          schema:
            type: number
            format: int32
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_age:
                        type: array
                        description: 'A list of ages that are permitted for visiting this location.'
                      a_amenities:
                        type: array
                        description: 'A list of facilities that are available in this location.'
                      a_level:
                        type: array
                        description: 'A list of levels that are suitable for visiting this location.'
                      a_logo:
                        type: array
                        description: 'Information about the location logo used in WellnessLiving: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl>'
                      a_slide:
                        type: array
                        description: 'A list of the location images. Every element has the following keys: <dl> <dt>int `i_height`</dt> <dd>Height.</dd> <dt>int `i_width`</dt> <dd>Width.</dd> <dt>string `url_preview`</dt> <dd>URL to image preview.</dd> <dt>string `url_slide`</dt> <dd>URL to full image.</dd> </dl>'
                      a_work:
                        type: array
                        description: 'The hours of operation for the location. Fields are numbers representing specific days (1 is Monday, 7 is Sunday). Values are objects with the next fields: <dl> <dt>string `s_end`</dt> <dd>The time of day end in MySQL format.</dd> <dt>string `s_start`</dt> <dd>The time of day start in MySQL format.</dd> </dl>'
                      f_latitude:
                        type: number
                        format: float
                        description: 'The latitude coordinate of the location.'
                      f_longitude:
                        type: number
                        format: float
                        description: 'The longitude coordinate of the location.'
                      html_description_full:
                        type: string
                        description: 'The full description of the location.'
                      html_description_preview:
                        type: string
                        description: 'A shorter description of the location. A preview of {@link ViewApi::$html_description_full}.'
                      id_industry:
                        type: [number, "null"]
                        description: 'The industry of the business. `null` in case when industry didn''t set for chosen business type, or for business types where set value which is absent.'
                      is_phone:
                        type: boolean
                        description: '`true` if to display phone number on location page. `false` otherwise.'
                      is_top_choice:
                        type: boolean
                        description: '`true` if WellnessLiving identifies this is a top choice location, `false` otherwise.'
                      k_business:
                        type: string
                        description: 'The key of the business this location belongs to.'
                      k_business_type:
                        type: string
                        description: 'The key of the business type this location belongs to.'
                      k_timezone:
                        type: string
                        description: 'The timezone.'
                      s_address:
                        type: string
                        description: 'The physical address of the location.'
                      s_map:
                        type: string
                        description: 'A string that can be used in navigator programs.'
                      s_phone:
                        type: string
                        description: 'The phone number for the location.'
                      s_timezone:
                        type: string
                        description: 'The system name of the time zone.'
                      s_title:
                        type: string
                        description: 'The title of the location.'
                      text_address_individual:
                        type: string
                        description: 'The address of the location.'
                      text_alias:
                        type: string
                        description: 'The special system name, which can be used in custom location links. This is usually based on name of the location, with extra characters removed. However, it can also can be fully customized.'
                      text_business_type:
                        type: string
                        description: 'The name of the business type.'
                      text_city:
                        type: string
                        description: 'The city name of the location.'
                      text_country:
                        type: string
                        description: 'The country name of the location.'
                      text_industry:
                        type: string
                        description: 'The name of the industry of the business.'
                      text_mail:
                        type: string
                        description: 'The email address.'
                      text_postal:
                        type: string
                        description: 'Postal code of the location.'
                      text_region:
                        type: string
                        description: 'The region name of the location.'
                      text_region_code:
                        type: string
                        description: 'Region 2 or 3 letters abbreviation of the location. Can be empty, if abbreviation for region is not set.'
                      url_facebook:
                        type: string
                        description: 'The Facebook URL of the location.'
                      url_instagram:
                        type: string
                        description: 'The Instagram URL of the location.'
                      url_linkedin:
                        type: string
                        description: 'The Linked In URL of the location.'
                      url_map:
                        type: string
                        description: 'The URL of the location in Google Maps.'
                      url_microsite:
                        type: string
                        description: 'The URL to the location entry in the WellnessLiving Explorer listing.'
                      url_site:
                        type: string
                        description: 'The website URL of the location.'
                      url_twitter:
                        type: string
                        description: 'The URL of the location''s Twitter account.'
                      url_web:
                        type: string
                        description: 'The URL to the business''s Client Web App.'
                      url_youtube:
                        type: string
                        description: 'The YouTube URL of the location.'
      tags:
        - View-View
  '/Wl/Location/Select/LocationSelect.json':
    get:
      summary: 'Performs checks and generates a list of locations according to a given configuration.'
      parameters:
        - name: a_config
          in: query
          description: 'Result-defining configuration set.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business in which a list of locations is requested. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User who requests a list of locations. Primary key in {@link \PassportLoginSql} table. Empty string for a guest.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_select:
                        type: array
                        description: 'Location list with additional parameters for a location select HTML component.'
      tags:
        - Select-LocationSelect
  '/Wl/Location/Flag/Flag.json':
    get:
      summary: 'Checks if user {@link FlagApi::$uid} is flagged in location {@link FlagApi::$k_location} or each of users {@link FlagApi::$a_uid} is flagged in location {@link FlagApi::$k_location}. Accepts either a single user key (`uid`) or an array of user keys (`a_uid`) and returns the flag status for each, including whether the flagged user is restricted from booking or purchasing at the location.'
      parameters:
        - name: a_uid
          in: query
          description: 'User keys. `null` if not set.'
          schema:
            type: [array, "null"]
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_flag:
                        type: array
                        description: 'Array with structure: <tt>null</tt> until loaded or when <var>a_uid</var> was not set. <dl> <dt>string `uid`</dt> <dd>Key is passport login ID. <i>Primary key in the {@link \PassportLoginSql} table.</i></dd> <dt>bool `is_flag`</dt> <dd>Value: <tt>true</tt> if user is flagged; <tt>false</tt> otherwise.</dd> </dl>'
                      a_restrictions_multiple:
                        type: [array, "null"]
                        description: 'Array, where keys are UIDs to be checked and values are same as {@link FlagApi::$a_restrictions_single}. `null` if user is not flagged in the location. This field is set, if API gets {@link FlagApi::$a_uid} and not {@link FlagApi::$uid} properties. Keys are user keys. <i>Primary keys in the {@link \PassportLoginSql} table.</i> Values are flag restriction arrays: <dl> <dt>bool `is_book`</dt> <dd>`true` if the user cannot make new reservations.</dd> <dt>bool `is_purchase`</dt> <dd>`true` if the user cannot make new purchases.</dd> </dl>'
                      a_restrictions_single:
                        type: [array, "null"]
                        description: '<tt>null</tt> if user is not flagged in the location. This field is set, if API gets {@link FlagApi::$uid} and not {@link FlagApi::$a_uid} properties. <dl> <dt>bool `is_book`</dt> <dd>`true` if client cannot make new reservations.</dd> <dt>bool `is_purchase`</dt> <dd>`true` if client cannot make new purchases.</dd> </dl>'
                      is_flag:
                        type: boolean
                        description: '<tt>true</tt> if the user is flagged and can make purchases, but cannot make new reservations, <tt>false</tt> if otherwise. <tt>null</tt> until loaded or when {@link FlagApi::$a_uid} was not set.'
      tags:
        - Flag-Flag
  '/Wl/Location/WorkTime/LocationWorkTime.json':
    post:
      summary: 'Saves working hours of the location. Accepts a list of day-and-time-range entries representing the weekly schedule and replaces all existing working hours for the location. Days not present in the list are treated as non-working days.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_work_time:
                  type: array
                  description: 'The list of the location''s business hours. Each element has the next structure: Note that the days of the week that aren''t represented in this list will be saved as non-working days. <dl> <dt>int `i_day`</dt> <dd>The day of the week, from Monday (1) to Sunday (7). One of the{@link \ADateWeekSid} constants.</dd> <dt>int `t_end`</dt> <dd>The end time of work, formatted as HH:MM.</dd> <dt>int `t_start`</dt> <dd>The start time of work, formatted as HH:MM</dd> </dl>'
                k_business:
                  type: string
                  description: 'The business key.'
                k_location:
                  type: string
                  description: 'The location key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - WorkTime-LocationWorkTime
  '/Wl/Location/Staff/List.json':
    get:
      summary: 'Retrieves an information about staff members for the current location.'
      parameters:
        - name: k_location
          in: query
          description: 'ID of a location.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'A list of staff members with information about them. Every element has keys: Deprecated: `k_staff` can be additionally returned for a limited list of third-party apps. <dl> <dt>string `s_name`</dt> <dd>Name.</dd> <dt>string `s_position`</dt> <dd>Job title.</dd> <dt>string `s_surname`</dt> <dd>1st letter of surname.</dd> <dt>string `uid`</dt> <dd>ID as user. Primary key in {@link \PassportLoginSql} table.</dd> </dl>'
      tags:
        - Staff-List
  '/Wl/Location/LocationRate/LocationRate.json':
    get:
      summary: 'Retrieves information about location rate.'
      parameters:
        - name: k_location
          in: query
          description: 'ID of a location to show rate for. Primary key in {@link \RsLocationSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_rate:
                        type: number
                        format: float
                        description: 'Location rate.'
                      i_review:
                        type: number
                        format: int32
                        description: 'Review count.'
                      is_rate:
                        type: boolean
                        description: 'Determines that the rate type exists in the current business type.'
      tags:
        - LocationRate-LocationRate
  '/Wl/Location/Facility/FacilitySid.json':
    get:
      summary: 'Returns the list of all items for the given Sid class. Populates enumeration dropdowns and lookup tables on the frontend. <i> Given any class that extends {@link CoreSid}, returns every defined constant with its numeric ID, string identifier, and human-readable title so the client does not need to hard-code enum values. </i>'
      parameters:
        - name: s_class_name
          in: query
          description: 'Name of the Sid class to get list from. Should be a fully qualified class name. <i>Class should be a child of {@link CoreSid}.</i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of items. Keys are IDs, values are arrays with additional information: <dl> <dt>int `sid`</dt> <dd>String ID.</dd> <dt>string `text_title`</dt> <dd>Title of the ID.</dd> </dl>'
      tags:
        - Facility-FacilitySid
  '/Wl/Fitbuilder/Subscription/FitbuilderSubscription.json':
    get:
      summary: 'Gets information does Fitbuilder subscription plan is free or not.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to check Fitbuilder subscription plan.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_free:
                        type: boolean
                        description: 'If Fitbuilder has free subscription plan is `true`, otherwise - `false`.'
      tags:
        - Subscription-FitbuilderSubscription
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key to check Fitbuilder subscription plan.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link FitbuilderSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscription-FitbuilderSubscription
  '/Wl/Promotion/Edit/PromotionEditAffect.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key promotion belongs to. Primary key of {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'Key of promotion. Primary key of {@link \RsPromotionSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_clients:
                        type: number
                        format: int32
                        description: 'Number of clients who has purchased promotion without editing.'
                      i_clients_edited:
                        type: number
                        format: int32
                        description: 'Number of clients who has purchased and edited promotion.'
                      is_extend_all:
                        type: boolean
                        description: 'Whether any clients including with promotion edited have their membership extended.'
                      is_extend_without_edited:
                        type: boolean
                        description: 'Whether clients without edited promotion have their membership extended.'
                      url_affected_clients:
                        type: string
                        description: 'Url of list of the promotion affected clients where sold promotions were not edited.'
                      url_affected_clients_edited:
                        type: string
                        description: 'Url of list of the promotion affected clients.'
      tags:
        - Edit-PromotionEditAffect
  '/Wl/Promotion/Convert/Convert.json':
    get:
      summary: 'Returns list of promotions that can be used to pay for the service.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_promotion_to
          in: query
          description: 'The promotion key the given promotion will be converted to.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data containing the following structure: <dl> <dt>int `id_program`</dt> <dd>Program ID for promotions from {@link \RsProgramSid}.</dd> <dt>string `k_promotion`</dt> <dd>Promotion key. Primary key from {@link \RsPromotionSql} table.</dd> <dt>string `text_title`</dt> <dd>Promotion title.</dd> </dl>'
      tags:
        - Convert-Convert
  '/Wl/Promotion/Pay/PromotionPayPauseList.json':
    post:
      summary: 'Plans tasks to perform actions with promotions: create,stop or delete payment pause periods.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_end:
                  type: [string, "null"]
                  description: 'End date of pause period in login promotion timezone. Can be set to special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. <tt>null</tt> if not initialized.'
                dl_start:
                  type: [string, "null"]
                  description: 'Start date of pause period in login promotion timezone. <tt>null</tt> if not initialized.'
                id_action:
                  type: [number, "null"]
                  description: 'Action to perform with promotion payment pause periods. One for the {@link \Wl\Promotion\Pay\PromotionPayPauseActionSid} constants. <tt>null</tt> if not initialized.'
                is_mail:
                  type: boolean
                  description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                is_push:
                  type: boolean
                  description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                is_sms:
                  type: boolean
                  description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                s_login_promotion:
                  type: [string, "null"]
                  description: 'Bulk of login promotions. Login promotion primary keys serialized with JSON. This logic was selected, because there is possibility to transfer list with more than 1000 elements. <tt>null</tt> if not initialized.'
                text_note:
                  type: [string, "null"]
                  description: 'Additional notes for promotion payment pause period. <tt>null</tt> if not initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Pay-PromotionPayPauseList
    put:
      summary: 'Returns list of clients who have promotion payment pause on specified period if {@link $dl_start} and {@link $dl_end} are provided.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_end:
                  type: [string, "null"]
                  description: 'End date of pause period in login promotion timezone. Can be set to special value {@link PromotionPayPause::DATE_END_INDEFINITE} to make the period indefinite until further action. <tt>null</tt> if not initialized.'
                dl_start:
                  type: [string, "null"]
                  description: 'Start date of pause period in login promotion timezone. <tt>null</tt> if not initialized.'
                s_login_promotion:
                  type: [string, "null"]
                  description: 'Bulk of login promotions. Login promotion primary keys serialized with JSON. This logic was selected, because there is possibility to transfer list with more than 1000 elements. <tt>null</tt> if not initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_promotion_exclude:
                        type: array
                        description: 'List of the login promotions which can not be put on hold in the selected period.'
                      a_user:
                        type: array
                        description: 'List of the clients to which list of login promotion belong and have promotion payment pause on the selected period.'
                      i_user:
                        type: number
                        format: int32
                        description: 'Number of all clients to which list of login promotion belong and have a promotion payment pause on the selected period.'
                      i_user_impact:
                        type: number
                        format: int32
                        description: 'Number of all clients to which list of login promotion belong and are impacted.'
                      is_mail:
                        type: boolean
                        description: 'Whether or not to send email notification. <tt>false</tt> if not initialized.'
                      is_push:
                        type: boolean
                        description: 'Whether or not to send push notification. <tt>false</tt> if not initialized.'
                      is_sms:
                        type: boolean
                        description: 'Whether or not to send SMS notification. <tt>false</tt> if not initialized.'
                      k_mail_pattern:
                        type: object
                        description: 'Key of the email pattern.'
      tags:
        - Pay-PromotionPayPauseList
  '/Wl/Promotion/Purchase/PurchaseList.json':
    get:
      summary: 'Gets list of promotions.'
      parameters:
        - name: is_inactive_include
          in: query
          description: 'A flag to include inactive items in the query result.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'List of promotions. <dl> <dt>array[] `a_promotion`</dt> <dd> Promotion list. Each element has keys: <dl> <dt>string `k_promotion`</dt> <dd>ID of promotion, primary key in {@link \RsPromotionSql}.</dd> <dt>string `text_title`</dt> <dd>Title of promotion.</dd> </dl> </dd> <dt>int `id_program`</dt> <dd>Promotion program. One of {@link \RsProgramSid} constants.</dd> <dt>int `id_program_type`</dt> <dd>Promotion program type. One of {@link \RsProgramTypeSid} constants.</dd> <dt>string `text_title`</dt> <dd>Program type title.</dd> </dl>'
      tags:
        - Purchase-PurchaseList
  '/Wl/Promotion/Index/PromotionIndex.json':
    get:
      summary: 'Gets a list of packages/passes/memberships. Returns introductory promotion offers available at the specified location, optionally filtered by program type, including pricing, duration, visit limits, and access information for each item.'
      parameters:
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: id_program_type
          in: query
          description: 'The program type ID, which will be one of the {@link \RsProgramTypeSid} constants. `0` to not filter Purchase Options with type of the Purchase Option.'
          schema:
            type: number
            format: int32
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'A list of introductory promotion offers available at the location. Every element is an array with the following keys: <dl> <dt>array `a_access`</dt> <dd> Information about services that can be attended with this pass or membership. <dl> <dt>array[] `a_class`</dt> <dd> A list of available classes. <dl> <dt>string `k_class`</dt> <dd>Key of the class.</dd> </dl> </dd> <dt>array[] `a_event`</dt> <dd> A list of available events. <dl> <dt>string `k_class`</dt> <dd>Key of the event.</dd> </dl> </dd> <dt>array[] `a_resource`</dt> <dd> A list of available assets. <dl> <dt>string `k_resource`</dt> <dd>Key of the asset.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> A list of available appointment types. <dl> <dt>string `k_service`</dt> <dd>Key of the appointment type.</dd> </dl> </dd> <dt>bool `is_class_all`</dt> <dd> If `true`, any class in the business can be visited with this Purchase Option. If `false`, only selected classes can be visited. Selected classes can be found in the list of available classes and events. </dd> <dt>bool `is_event_all`</dt> <dd> If `true`, if any event in the business can be visited with this Purchase Option. If `false`, if only selected events can be visited. Selected events can be found in the list of available classes and events. </dd> </dl> </dd> <dt>array[] `a_component`</dt> <dd> A list of components included in the Purchase Option if this Purchase Option is a package. If it''s not a package, the list will be always empty. If it''s a package, the list can still be empty if all its components were deactivated. This means that this array can''t be used to determine whether this is a package or not (check `id_program` or `id_program_type` value instead). There can be three types of components: events, Purchase Options, and products. Some fields can be different depending on type of the component, but each element of the array includes: <dl> <dt>int `id_purchase_item`</dt> <dd> The type of the component. One of {@link \RsPurchaseItemSid} constants. This can only be: <ul> <li>{@link \RsPurchaseItemSid::ENROLLMENT}</li> <li>{@link \RsPurchaseItemSid::PROMOTION}</li> <li>{@link \RsPurchaseItemSid::PRODUCT}</li> </ul> </dd> <dt>string `k_id`</dt> <dd> The primary key of the component in the related table. This depends on the type of the component. The key of the event for {@link \RsPurchaseItemSid::ENROLLMENT}, the key of the Purchase Option for the {@link \RsPurchaseItemSid::PROMOTION}, the key of the product option for the {@link \RsPurchaseItemSid::PRODUCT}. Full information about events can be taken from the {@link \Wl\Event\EventListApi}. Purchase Options from the {@link \Wl\Promotion\Index\PromotionIndexApi}. Full information about products are not available at this moment though API. </dd> <dt>int `i_quantity`</dt> <dd>The quantity. If empty, it means the quantity is 1.</dd> <dt>string `text_title`</dt> <dd>The name of the component.</dd> </dl> </dd> <dt>array `a_image`</dt> <dd> This will be an empty array if the Purchase Option doesn''t have image. Otherwise, this will display the following information about the Purchase Option image: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>string `url-thumbnail`</dt> <dd>The link to the image.</dd> </dl> </dd> <dt>array `a_visit_limit`</dt> <dd> Attendance restrictions, if available. If unavailable, this will be an empty array. Every element has a key, which is a type of the time period {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}, {@link \ADurationSid::YEAR}. The values are: <dl> <dt>int `i_limit`</dt> <dd>The quantity of sessions every <var>i_period</var>.</dd> <dt>int `i_period`</dt> <dd>The duration of the time period. This depends on a key of <var>a_visit_limit</var> array.</dd> <dt>int `i_roll_over_cap`</dt> <dd>The limit number of rollover sessions.</dd> <dt>int `i_roll_over_expire`</dt> <dd>The duration of the time period after which rolled over session will expire.</dd> <dt>int `id_roll_over_expire`</dt> <dd> The type of <var>i_roll_over_expire</var>. One of {@link \ADurationSid} contantant. This can only be: <ul> <li>{@link \ADurationSid::DAY}</li> <li>{@link \ADurationSid::WEEK}</li> <li>{@link \ADurationSid::MONTH}</li> <li>{@link \ADurationSid::YEAR}</li> </ul> </dd> <dt>int `id_limit_cycle`</dt> <dd>The type of the limit cycle {@link \Wl\Promotion\Edit\Limit\Cycle\Sid}.</dd> <dt>bool `is_reconcile_visit`</dt> <dd>Determines whether to reconcile unpaid sessions on restrictions reset.</dd> <dt>bool `is_roll_over_expire`</dt> <dd>Determines if the rolled over session is expired.</dd> <dt>bool `is_roll_over_renew`</dt> <dd>Determines whether to rollover sessions upon auto-renew.</dd> <dt>bool `is_rollup`</dt> <dd>Determines Whether to rollover sessions.</dd> <dt>bool `is_visit_conversion`</dt> <dd>Determines whether to convert remaining visits.</dd> </dl> </dd> <dt>string `dl_expire`</dt> <dd> The local expiration date. This is the last day of the Purchase Option''s active status. This is used only for the ''Expires on a certain date'' duration type. Otherwise, this should be ignored. </dd> <dt>int `i_duration`</dt> <dd> The number of periods for the "Period" duration type. For example, if the duration of the Purchase Option is 12 months, this field will be 12. </dd> <dt>int `i_limit`</dt> <dd> The number of visits that the owner of the Purchase Option can use. This will be `0` if the Purchase Option has unlimited visits. </dd> <dt>int `i_limit_duration`</dt> <dd> The maximum number of minutes or hours depending on <var>id_limit_duration</var> that the current promotion can be used in. </dd> <dt>int `id_limit_duration`</dt> <dd> The type of <var>i_limit_duration</var>. One of {@link \ADurationSid} contantant. This can only be: <ul> <li>{@link \ADurationSid::MINUTE}</li> <li>{@link \ADurationSid::HOUR}</li> <li>{@link \ADurationSid::MONTH}</li> <li>{@link \ADurationSid::YEAR}</li> </ul> </dd> <dt>int `id_duration`</dt> <dd> The type of periods for the duration type "Period". For example, if the duration of the Purchase Option is 12 months, this field will be the ID of the month. See all the possible options here: {@link \ADurationSid}. </dd> <dt>int `id_duration_type`</dt> <dd> The type of the duration. This can be "Without End", "Expires on a certain date", "Period". See more information here: {@link \RsDurationTypeSid}. </dd> <dt>int `id_program`</dt> <dd> The type of the Purchase Option. The <var>id_program</var> relates to only one <var>id_program_type</var> and one <var>id_program_category</var>. See more information here: {@link \RsProgramSid}. </dd> <dt>int `id_program_category`</dt> <dd> The category of the program for promotions. The <var>id_program_category</var> relates to more than one <var>id_program</var>. See more information here: {@link \RsProgramCategorySid}. </dd> <dt>int `id_program_type`</dt> <dd> The ID of the promotion program type. The <var>id_program_type</var> relates to more than one <var>id_program</var>. See more information here: {@link \RsProgramTypeSid}. </dd> <dt>bool `is_direct_buy_only`</dt> <dd> If `true`, the pricing option can be sold only by direct link. This means it shouldn''t be shown in the store or explorer. Otherwise, this will be `false`. </dd> <dt>bool `is_introductory`</dt> <dd>If `true`, this promotion is for introductory clients. Otherwise, this will be `false`.</dd> <dt>bool `is_online`</dt> <dd>If `true` this promotion is available for online purchase. Otherwise, this will be `false`.</dd> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `m_price`</dt> <dd>The price.</dd> <dt>string `text_description`</dt> <dd>The description.</dd> <dt>string `text_program`</dt> <dd>The localized promotion program name corresponding to the value of <var>id_program</var>.</dd> <dt>string `text_title`</dt> <dd>The title.</dd> <dt>string `url_buy`</dt> <dd>The direct URL to the promotion purchase page.</dd> </dl>'
      tags:
        - Index-PromotionIndex
  '/Wl/Promotion/Table/Activator.json':
    post:
      summary: 'Activates/deactivates record list.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_record:
                  type: array
                  description: 'Record key list.'
                is_activate:
                  type: boolean
                  description: '<tt>true</tt> if records should be activated, <tt>false</tt> otherwise (deactivated).'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Table-Activator
  '/Wl/Google/Booking/BatchAvailabilityLookup.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_slot_time:
                  type: object
                  description: 'Multiple slot times to be checked for availability.'
                s_merchant_id:
                  type: string
                  description: 'ID of the merchant.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_slot_time_availability:
                        type: array
                        description: 'Availability slot times.'
      tags:
        - Booking-BatchAvailabilityLookup
  '/Wl/Google/Booking/ListBookings.json':
    post:
      summary: ''
      parameters:
        - name: s_user_id
          in: query
          description: 'ID of the user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_booking_list:
                        type: array
                        description: 'List of booking entity. Each element it is result {@link \Wl\Google\Booking\BookingEntity::toArray()}.'
      tags:
        - Booking-ListBookings
  '/Wl/Google/Booking/UpdateBooking.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                o_booking_entity:
                  type: object
                  description: 'Booking to be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_booking:
                        type: [array, "null"]
                        description: 'Information about booking.'
                      a_booking_failure:
                        type: [array, "null"]
                        description: 'Information about failure.'
      tags:
        - Booking-UpdateBooking
  '/Wl/Google/Booking/CreateBooking.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                o_payment_information:
                  type: object
                  description: 'Information about payments. When payment authorizations are handled by Google, if the booking request does not succeed, payment authorizations are automatically canceled. (optional) <tt>null</tt> if not specified.'
                o_slot:
                  type: object
                  description: 'The inventory slot that is being requested to make this booking. Slot is provided for the partner to verify the lease information.'
                o_user_information:
                  type: object
                  description: 'Personal information of the user making the appointment.'
                text_additional_request:
                  type: [string, "null"]
                  description: 'A string from the user which contains any special requests or additional information that they would like to notify the merchant about. (optional) <tt>null</tt> if not specified.'
                text_deal_id:
                  type: [string, "null"]
                  description: 'Partner provided deal id associated with this slot. (optional). <tt>null</tt> if not specified.'
                text_idempotency_token:
                  type: string
                  description: 'Idempotency token for requests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_booking:
                        type: [array, "null"]
                        description: 'Information about booking.'
                      a_booking_failure:
                        type: [array, "null"]
                        description: 'Information about failure.'
      tags:
        - Booking-CreateBooking
  '/Wl/Google/Booking/GetBookingStatus.json':
    post:
      summary: ''
      parameters:
        - name: s_booking_id
          in: query
          description: 'ID of the existing booking.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_booking_id:
                        type: string
                        description: 'ID of the existing booking.'
                      s_booking_status:
                        type: [string, "null"]
                        description: 'See {@link \Wl\Google\Booking\BookingStatusSid::idGoogle()} for documentation.'
                      s_prepayment_status:
                        type: [string, "null"]
                        description: 'See {@link \Wl\Google\Booking\PrepaymentStatusSid::idGoogle()} for documentation.'
      tags:
        - Booking-GetBookingStatus
  '/Wl/Google/Booking/CheckAvailability.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                o_slot:
                  type: object
                  description: 'The appointment slot that is being checked.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_slot:
                        type: array
                        description: 'See {@link \Core\Integration\IntegrationDataTrait::toArray()} for documentation.'
                      i_count_available:
                        type: number
                        format: int32
                        description: 'Count of available slots'
      tags:
        - Booking-CheckAvailability
  '/Wl/Google/Booking/HealthCheck.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Booking-HealthCheck
  '/Wl/Google/Calendar/GoogleCalendarEventWebhook.json':
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Calendar-GoogleCalendarEventWebhook
  '/Wl/Google/Login/GoogleLogin.json':
    post:
      summary: 'Performs Google authorization within the context of the specified business. Validates that the given business is active, sets it as the current frontend context, and then delegates to the parent Google login flow to authenticate the user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business in which authorization is performed.'
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link GoogleCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link GoogleCredential::CID}. When application passed, the credential loaded by application id.'
                s_code:
                  type: string
                  description: 'The Google server authorization code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-GoogleLogin
  '/Wl/Passport/Login/Info.json':
    get:
      summary: 'Returns the current user''s login information and password reset URL for the specified business. Validates the business key, applies any business-specific redemption settings, then delegates to the parent implementation to return the current user ID and password reset URL.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: [string, "null"]
                        description: 'The current user key. This will be `null` if the user isn''t signed in.'
                      url_password_change:
                        type: string
                        description: 'A URL that a user can visit to reset their password.'
                      url_register:
                        type: string
                        description: 'The URL to the registration page.'
      tags:
        - Login-Info
  '/Wl/Field/Config/Field.json':
    delete:
      summary: 'Remove field.'
      parameters:
        - name: id_field_table
          in: query
          description: 'Field table ID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Config-Field
    get:
      summary: 'Fetch field information.'
      parameters:
        - name: id_field_table
          in: query
          description: 'Field table ID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_item:
                        type: [array, "null"]
                        description: 'List of items for dropdown and select fields.'
                      can_add_item:
                        type: boolean
                        description: 'Whether new field item can be added.'
                      can_default:
                        type: boolean
                        description: 'Whether default value can be set for the field.'
                      can_remove:
                        type: boolean
                        description: '`true` if field can be removed, `false` otherwise.'
                      can_sort:
                        type: boolean
                        description: '`true` if field can be sorted, `false` otherwise.'
                      can_state:
                        type: boolean
                        description: '`true` if for field can change active state, `false` otherwise.'
                      i_size:
                        type: [number, "null"]
                        description: 'Size of the text input. Sets only when field has {@link \RsFieldTypeSid::TEXT} type.'
                      id_field_type:
                        type: number
                        format: int32
                        description: 'Field type ID.'
                      is_active:
                        type: boolean
                        description: '`true` if field is active, `false` otherwise.'
                      is_booking_and_purchase:
                        type: boolean
                        description: '`true` if a field is required during booking or purchase; `false` if a field is not require during booking or purchase;'
                      is_booking_and_purchase_disable:
                        type: boolean
                        description: '`true` if setting "Display during booking/purchase" is disabled, `false` otherwise.'
                      is_franchisee_display:
                        type: boolean
                        description: '`true` if display this field to franchisee, `false` otherwise.'
                      is_public:
                        type: boolean
                        description: '`true` if field is public, `false` otherwise.'
                      is_public_disable:
                        type: boolean
                        description: '`true` if setting "Visible to clients" is disabled, `false` otherwise.'
                      is_registration:
                        type: boolean
                        description: '`true` if a field is required on sign up; `false` if a field is not require on sign up;'
                      is_registration_disable:
                        type: boolean
                        description: '`true` if setting "Display on sign-up" disabled, `false` otherwise.'
                      is_require:
                        type: boolean
                        description: '`true` if field is required by client, `false` otherwise.'
                      is_require_disable:
                        type: boolean
                        description: '`true` if setting "Required by clients" is disabled, `false` otherwise.'
                      is_require_staff:
                        type: boolean
                        description: '`true` if field is required by staff, `false` otherwise.'
                      is_require_staff_disable:
                        type: boolean
                        description: '`true` if setting "Required by staff" is disabled, `false` otherwise.'
                      text_default:
                        type: string
                        description: 'Default value for field.'
                      text_title:
                        type: string
                        description: 'Name of the field.'
      tags:
        - Config-Field
    post:
      summary: 'Creates or updates field.'
      parameters:
        - name: id_field_table
          in: query
          description: 'Field table ID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_item:
                  type: [array, "null"]
                  description: 'List of items for dropdown and select fields.'
                a_field_item_value:
                  type: [array, "null"]
                  description: 'List of value for item.'
                i_size:
                  type: [number, "null"]
                  description: 'Size of the text input. Sets only when field has {@link \RsFieldTypeSid::TEXT} type.'
                id_field_type:
                  type: number
                  format: int32
                  description: 'Field type ID.'
                is_active:
                  type: boolean
                  description: '`true` if field is active, `false` otherwise.'
                is_booking_and_purchase:
                  type: boolean
                  description: '`true` if a field is required during booking or purchase; `false` if a field is not require during booking or purchase;'
                is_franchisee_display:
                  type: boolean
                  description: '`true` if display this field to franchisee, `false` otherwise.'
                is_public:
                  type: boolean
                  description: '`true` if field is public, `false` otherwise.'
                is_registration:
                  type: boolean
                  description: '`true` if a field is required on sign up; `false` if a field is not require on sign up;'
                is_require:
                  type: boolean
                  description: '`true` if field is required by client, `false` otherwise.'
                is_require_staff:
                  type: boolean
                  description: '`true` if field is required by staff, `false` otherwise.'
                s_max_value:
                  type: [string, "null"]
                  description: 'Maximum value of field.'
                text_default:
                  type: string
                  description: 'Default value for field.'
                text_title:
                  type: string
                  description: 'Name of the field.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_field:
                        type: string
                        description: 'Field key.'
      tags:
        - Config-Field
  '/Wl/Family/Relation/Relation.json':
    delete:
      summary: 'Deletes family relation between 2 users. Removes the relationship between the user identified by `uid` and the user identified by `uid_delete` within the given business, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
        - name: uid_delete
          in: query
          description: 'The key of the related user who {@link \Wl\Family\Relation\RelationApi::$uid} must be removed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation
    get:
      summary: 'Gets relative list. Returns the list of all family relationships for the specified user within the given business, including relationship type, reverse relationship type, name, and photo information for each related user.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation
    post:
      summary: 'Adds to user {@link \Wl\Family\Relation\RelationApi::$uid} relative {@link \Wl\Family\Relation\RelationApi::$a_new}. Creates a bidirectional family relationship between the user identified by `uid` and the user specified in `a_new`, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_new:
                  type: array
                  description: 'The new relationship to be added to user, specified with {@link \Wl\Family\Relation\RelationApi::$uid}: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `uid`</dt> <dd>The related user''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation
  '/Wl/Family/Relation/FamilyRelation.json':
    get:
      summary: 'Gets relationships list. Returns all relationship types enabled for the given business, or all system-defined relationship types if no business key is provided.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_relationships:
                        type: array
                        description: 'The relationship types in the business. Key is ID, value is SID.'
      tags:
        - Relation-FamilyRelation
  '/Wl/Family/Relation/Relation72.json':
    delete:
      summary: 'Deletes family relation between 2 users. Removes the relationship between the user identified by `uid` and the user identified by `uid_delete` within the given business, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
        - name: uid_delete
          in: query
          description: 'The key of the related user who {@link \Wl\Family\Relation\RelationApi::$uid} must be removed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation72
    get:
      summary: 'Gets relative list. Returns the list of all family relationships for the specified user within the given business, including relationship type, reverse relationship type, name, and photo information for each related user.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation72
    post:
      summary: 'Adds to user {@link \Wl\Family\Relation\RelationApi::$uid} relative {@link \Wl\Family\Relation\RelationApi::$a_new}. Creates a bidirectional family relationship between the user identified by `uid` and the user specified in `a_new`, then returns the updated list of relationships for `uid`.'
      parameters:
        - name: id_flow
          in: query
          description: 'ID of the user behavior flow. One of {@link \Wl\User\Tracking\FlowSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose relationships are being assessed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_new:
                  type: array
                  description: 'The new relationship to be added to user, specified with {@link \Wl\Family\Relation\RelationApi::$uid}: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `uid`</dt> <dd>The related user''s key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about the user''s relationships. Every element has the following fields: <dl> <dt>int `id_family_relation`</dt> <dd>The relationship type. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>int `id_family_relation_reverse`</dt> <dd>The type of reciprocated relationship. One of the {@link \RsFamilyRelationSid} constants.</dd> <dt>string `text_name`</dt> <dd>The name of the relation.</dd> <dt>string `text_name_first`</dt> <dd>The first name of the relation.</dd> <dt>string `uid`</dt> <dd>The key of the related user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - Relation-Relation72
  '/Wl/Family/Pay/Pay.json':
    get:
      summary: 'Gets data about who pays for a given user.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of business to get information for. Primary key in {@link \RsBusinessAr} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of user to get information for. Primary key in {@link \Core\Passport\Login\PassportLoginAr} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid_payer:
                        type: string
                        description: 'ID of user who will pay for user {@link \Wl\Family\Pay\PayApi::$uid}. Primary key in {@link \Core\Passport\Login\PassportLoginAr} table. Empty if {@link \Wl\Family\Pay\PayApi::$uid} will pay for self.'
      tags:
        - Pay-Pay
    post:
      summary: 'Sets that user {@link \Wl\Family\Pay\PayApi::$uid_payer} pays for user {@link \Wl\Family\Pay\PayApi::$uid}.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of business to get information for. Primary key in {@link \RsBusinessAr} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of user to get information for. Primary key in {@link \Core\Passport\Login\PassportLoginAr} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                uid_payer:
                  type: string
                  description: 'ID of user who will pay for user {@link \Wl\Family\Pay\PayApi::$uid}. Primary key in {@link \Core\Passport\Login\PassportLoginAr} table. Empty if {@link \Wl\Family\Pay\PayApi::$uid} will pay for self.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Pay-Pay
  '/Wl/Lead/Source/LeadSourceList.json':
    get:
      summary: 'Gets list of Lead Sources. Returns all lead sources configured for the specified business, including system-defined and custom sources, with sort order, title, associated skin, and flags indicating whether each source is currently in use. This is public endpoint, which is available for non-signed-in users. But if the user is not signed in, the list will be filtered to contain only those sources that are available for the user and with limited information.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_lead_source:
                        type: array
                        description: 'List of Lead Sources. <dl> <dt>int|null `i_sort`</dt> <dd> Sorting order. Only used in the Lead Source widget option. `null` is a temporary value that exists for a short time during the process of inserting of a new row. </dd> <dt>int|null `id_lead_source`</dt> <dd> System-defined Lead Source ID. One of the {@link \Wl\Mode\ModeSid} constants. `null` if it is a custom Lead Source. </dd> <dt>bool `is_add_lead`</dt> <dd> `true` if this skin will be used for "Add Lead" form or `false` otherwise. Only one skin in the business can be used for "Add Lead" form. Not available if current user is not a staff member or admin. </dd> <dt>bool `is_use`</dt> <dd> Determines whether Lead Source is being used. `true` if Lead Source is being used. `false` - otherwise. Not available if current user is not a staff member or admin. </dd> <dt>string `k_lead_source`</dt> <dd>Key of the lead source. <i>Primary key in {@link \Wl\Lead\Source\LeadSourceSql} table.</i></dd> <dt>string `k_skin`</dt> <dd> Key of the lead skin. <i>Primary key in {@link \RsSkinSql} table.</i> Not available if current user is not a staff member or admin. </dd> <dt>string `text_title`</dt> <dd>Name of the Lead Source.</dd> </dl>'
      tags:
        - Source-LeadSourceList
  '/Wl/Lead/Source/LeadSourceElement.json':
    delete:
      summary: 'Deletes custom source lead. Removes the specified custom lead source from the business. If a replacement lead source key is provided, all users currently assigned to the deleted source are reassigned to it before deletion.'
      parameters:
        - name: k_lead_source_replace
          in: query
          description: 'Key of the lead source.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Source-LeadSourceElement
    post:
      summary: 'Creates or edits a custom source lead. Accepts a list of lead source records and applies bulk create or update operations: new entries without a key are inserted, and existing entries are updated with a new sort order or title.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_lead_source:
                  type: array
                  description: 'List of Lead Sources. <dl> <dt>int `i_sort`</dt> <dd>Sorting order.</dd> <dt>int|null `id_lead_source`</dt> <dd> System-defined Lead Source ID. One of {@link \Wl\Mode\ModeSid} constants. `null` if it is a custom Lead Source. </dd> <dt>string `k_lead_source`</dt> <dd>Key of the lead source. <i>Primary key in {@link \Wl\Lead\Source\LeadSourceSql} table.</i></dd> <dt>string `k_skin`</dt> <dd> Key of the lead skin. <i>Primary key in {@link \RsSkinSql} table.</i> Will be set only in case if <var>id_lead_source</var> is {@link \Wl\Mode\ModeSid::WIDGET} and widget can''t be used for "Add Lead" form. `false` - otherwise. </dd> <dt>string `text_title`</dt> <dd>Name of the Lead Source.</dd> </dl>'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Source-LeadSourceElement
    put:
      summary: 'Adds one lead source. Creates a single new custom lead source with the given title for the specified business, assigns it the next available sort position, and returns the generated lead source key.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                text_lead_source:
                  type: string
                  description: 'Name of new lead source item.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_lead_source:
                        type: string
                        description: 'Key of the lead source.'
      tags:
        - Source-LeadSourceElement
  '/Wl/Session/Status.json':
    get:
      summary: 'Returns current user business and mode.'
      parameters:
        - name: ip
          in: query
          description: 'IP address of remote user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_contact:
                        type: boolean
                        description: 'Whether current user has left hist contact details. {@link \Wl\Session\Status\Api::contactSet()} is used to set, and {@link \Wl\Session\Status\Api::contactGet()} is used to retrieve this flag.'
                      k_business:
                        type: number
                        format: int32
                        description: 'Business key if user logged in.'
                      text_email:
                        type: string
                        description: 'User contact email address.'
                      text_first_name:
                        type: string
                        description: 'First name of the user.'
                      text_last_name:
                        type: string
                        description: 'Last name of the user.'
                      text_phone:
                        type: string
                        description: 'Cell phone number.'
                      text_role:
                        type: string
                        description: 'User''s role. May be on of: <tt>guest</tt>, <tt>user</tt>, <tt>location-owner</tt>, <tt>admin</tt>.'
                      text_state:
                        type: string
                        description: 'Mode of current user session: <tt>frontend</tt> or <tt>backend</tt>. See {@link \RsPlaceSid}.'
                      uid:
                        type: number
                        format: int32
                        description: 'Current user ID.'
                      url_photo:
                        type: string
                        description: 'User photo URL.'
      tags:
        - Session-Status
  '/Wl/DoorAccess/Brivo/Connect.json':
    delete:
      summary: 'Disconnecting with Brivo, remove a token.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Brivo-Connect
    post:
      summary: 'Connecting with Brivo, getting a token.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_api_key
          in: query
          description: 'Api key.'
          schema:
            type: string
        - name: s_client_id
          in: query
          description: 'Client ID.'
          schema:
            type: string
        - name: s_client_secret
          in: query
          description: 'Client secret key.'
          schema:
            type: string
        - name: s_password
          in: query
          description: 'Password.'
          schema:
            type: string
        - name: s_username
          in: query
          description: 'Username.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Brivo-Connect
    put:
      summary: 'Update credentials. This functionality is requested only for Demo and Staging for testing purposes.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_api_key
          in: query
          description: 'Api key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Brivo-Connect
  '/Wl/Virtual/Setting/Setting.json':
    put:
      summary: 'Retrieves a list of virtual settings.'
      parameters:
        - name: a_visit_id
          in: query
          description: 'List of visit statuses who have access to uploaded videos.'
          schema:
            type: array
        - name: id_chime
          in: query
          description: 'Type of chime.'
          schema:
            type: number
            format: int32
        - name: id_recording
          in: query
          description: 'Type of automatic recording. One of {@link SettingRecordingSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_alert_join
          in: query
          description: 'Whether to notify the host with a sound when a participant joins the meeting is in waiting room waiting to be admitted.'
          schema:
            type: boolean
        - name: is_chat
          in: query
          description: 'Whether chat is enable during meeting.'
          schema:
            type: boolean
        - name: is_chat_public
          in: query
          description: 'Whether participants can send messages in main chat or not.'
          schema:
            type: boolean
        - name: is_co_host
          in: query
          description: 'Whether participants can send messages in main chat or not.'
          schema:
            type: boolean
        - name: is_host_video
          in: query
          description: 'Whether to start meetings with the host video on.'
          schema:
            type: boolean
        - name: is_include_passcode
          in: query
          description: 'Whether to include the passcode inside meeting link, so client will directly enter the meeting without having to enter passcode explicitly.'
          schema:
            type: boolean
        - name: is_link_secure
          in: query
          description: 'Whether link to join meeting should be secured. `true` if link should be secured. In this case secure phrase will be added to join link which allows user to avoid credential enter step. `false` if link should be common. In this case user will have to enter credentials before join meeting.'
          schema:
            type: boolean
        - name: is_mute_entry
          in: query
          description: 'Whether to keep your participants muted once they enter the virtual session and all through it unless the host unmute them.'
          schema:
            type: boolean
        - name: is_participant_video
          in: query
          description: 'Whether to start meetings with the participant video on.'
          schema:
            type: boolean
        - name: is_recording_cloud
          in: query
          description: 'Whether to allow host to record and save meeting in the cloud.'
          schema:
            type: string
        - name: is_recording_local
          in: query
          description: 'Whether to allow only host to download cloud recordings.'
          schema:
            type: boolean
        - name: is_waiting_room
          in: query
          description: 'Whether to enable waiting room for participants.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: not_screen_sharing
          in: query
          description: 'Whether to allow screen sharing (Only from staff to client). <tt>false</tt> will allow screen sharing from host to participants, <tt>true</tt> will disable screen sharing.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Setting-Setting
  '/Wl/Virtual/Subscription/Upgrade.json':
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` if not specified yet.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_license:
                  type: number
                  format: int32
                  description: 'Number of licenses that should be added to current subscription. If current subscription is inactive it will be activated with given number of licenses.'
                id_plan:
                  type: [number, "null"]
                  description: 'New subscription plan. One of {@link FitliveSubscriptionSid} constants. `null` if not defined yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: [string, "null"]
                        description: 'Url link to redirect after successful upgrade. `null` in a case if no redirect should appear.'
      tags:
        - Subscription-Upgrade
  '/Wl/Help/Popup/Popup.json':
    get:
      summary: 'Gets help popup settings.'
      parameters:
        - name: k_business
          in: query
          description: 'A business in which a user requests help. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User who asks for help. Primary key from {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      hide_call_us:
                        type: boolean
                        description: 'Whether "Call Us" button should be hidden in the help popup.'
                      hide_email_us:
                        type: boolean
                        description: 'Whether "Email Us" button should be hidden in the help popup.'
                      hide_forum:
                        type: boolean
                        description: 'Whether "Forum" button should be hidden in the help popup.'
                      hide_my_requests:
                        type: boolean
                        description: 'Whether "My Requests" button should be hidden in the help popup.'
                      id_article_target:
                        type: number
                        format: int32
                        description: 'Articles target ID. Constant from {@link ArticleTargetSid}.'
      tags:
        - Popup-Popup
  '/Wl/Calendar/Cordova/CordovaCalendarStaff.json':
    post:
      summary: 'Gets schedule for calendar synchronisation. POST method is used instead GET because {@link CordovaCalendarApiAbstract::$s_visit_synced} field value may be more than allowed for GET.'
      parameters:
        - name: dtu_synced_last
          in: query
          description: 'Date of last sync.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business to synchronize calendar from. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to synchronize calendar for. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_visit_synced:
                  type: string
                  description: 'Books/visits that are already synchronized in client''s calendar. Serialized list of pairs [[visit key ({@link \RsVisitSql}) => visit hash], ...].'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit_actual:
                        type: array
                        description: 'Events that must be synchronized in calendar. See {@link CordovaCalendarApiAbstract::data()} for detailed description.'
      tags:
        - Cordova-CordovaCalendarStaff
  '/Wl/Calendar/Cordova/CordovaCalendarClient.json':
    post:
      summary: 'Gets schedule for calendar synchronisation. POST method is used instead GET because {@link CordovaCalendarApiAbstract::$s_visit_synced} field value may be more than allowed for GET.'
      parameters:
        - name: dtu_synced_last
          in: query
          description: 'Date of last sync.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business to synchronize calendar from. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to synchronize calendar for. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_visit_synced:
                  type: string
                  description: 'Books/visits that are already synchronized in client''s calendar. Serialized list of pairs [[visit key ({@link \RsVisitSql}) => visit hash], ...].'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit_actual:
                        type: array
                        description: 'Events that must be synchronized in calendar. See {@link CordovaCalendarApiAbstract::data()} for detailed description.'
      tags:
        - Cordova-CordovaCalendarClient
  '/Wl/Gym/Visit/VisitAdd.json':
    post:
      summary: 'Records a gym visit for the specified client at the given date and time. Validates access and input, creates a visit record for the client at the specified location, optionally applies a membership promotion if payment is required, and returns the catalog URL if no promotion was found and payment was requested.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user making the gym visit.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_time:
                  type: array
                  description: 'The time of the visit, in the location''s timezone. An array with the following fields: <dl> <dt>int `i_hour`</dt> <dd>The hour component.</dd> <dt>int `i_minute`</dt> <dd>The minute component.</dd> <dt>bool `is_am`</dt> <dd>If `true` the visit is in the AM. If `false` it is in the PM.</dd> </dl>'
                dt_date:
                  type: string
                  description: 'The date of visit, without a time component in MySQL format. Date is based on the timezone of the location.'
                is_pay:
                  type: boolean
                  description: 'Determines whether payment is required for the visit. `true` if payment is needed for the visit. `false` if visit should be not pay.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_catalog:
                        type: string
                        description: 'Url to the business catalog with items that can be used to pay for the visit.'
      tags:
        - Visit-VisitAdd
  '/Wl/Tax/Region/TaxRegion.json':
    get:
      summary: 'Returns information for confirmation message in case editing tax for specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_tax_region:
                        type: [number, "null"]
                        description: 'Tax value.'
                      is_confirm:
                        type: boolean
                        description: '<tt>true</tt> - changes of the tax value for the business must be confirmed.'
      tags:
        - Region-TaxRegion
    post:
      summary: 'Update tax value and tax status in a region and in businesses in the region.'
      parameters:
        - name: f_tax_region
          in: query
          description: 'Tax value.'
          schema:
            type: [number, "null"]
        - name: is_active
          in: query
          description: 'Status of tax region.'
          schema:
            type: [boolean, "null"]
        - name: k_tax
          in: query
          description: 'Primary key of tax in {@link \RsTaxSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Region-TaxRegion
  '/Wl/Task/Edit/Edit.json':
    delete:
      summary: 'Removes task.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` means not set.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Task key. `null` means not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
    get:
      summary: 'Returns data for task edit form.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` means not set.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Task key. `null` means not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'List of staff keys assigned to task. Empty array means not set.'
                      dl_due:
                        type: string
                        description: 'Due date.'
                      id_task_status:
                        type: [number, "null"]
                        description: 'Task status. `null` means not set.'
                      k_location:
                        type: [string, "null"]
                        description: 'Location key. `null` means not set.'
                      text_title:
                        type: string
                        description: 'Task title.'
                      tl_due:
                        type: string
                        description: 'Due time.'
                      uid:
                        type: [string, "null"]
                        description: 'Client''s uid for whom connected task. `null` means not set.'
                      xml_description:
                        type: string
                        description: 'Task description.'
      tags:
        - Edit-Edit
    post:
      summary: 'Adds or changes task.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` means not set.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Task key. `null` means not set.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff:
                  type: array
                  description: 'List of staff keys assigned to task. Empty array means not set.'
                dl_due:
                  type: string
                  description: 'Due date.'
                id_task_status:
                  type: [number, "null"]
                  description: 'Task status. `null` means not set.'
                k_location:
                  type: [string, "null"]
                  description: 'Location key. `null` means not set.'
                text_title:
                  type: string
                  description: 'Task title.'
                tl_due:
                  type: string
                  description: 'Due time.'
                uid:
                  type: [string, "null"]
                  description: 'Client''s uid for whom connected task. `null` means not set.'
                xml_description:
                  type: string
                  description: 'Task description.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_task:
                        type: [string, "null"]
                        description: 'Task key. `null` means not set.'
      tags:
        - Edit-Edit
    put:
      summary: 'Changes task status.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `null` means not set.'
          schema:
            type: [string, "null"]
        - name: k_task
          in: query
          description: 'Task key. `null` means not set.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_task_status:
                  type: [number, "null"]
                  description: 'Task status. `null` means not set.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
  '/Wl/Eml/Mailchimp/EmlMailChimp.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: s_list_id
          in: query
          description: 'List ID.'
          schema:
            type: string
        - name: s_token
          in: query
          description: 'Encrypted data.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mailchimp-EmlMailChimp
    post:
      summary: 'Outside api. Mailchimp calls this method.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: s_list_id
          in: query
          description: 'List ID.'
          schema:
            type: string
        - name: s_token
          in: query
          description: 'Encrypted data.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                o_data:
                  type: object
                  description: 'System data of Mailchimp. Can be different for each type. But in our system used only matched data.'
                s_type:
                  type: string
                  description: 'Type of fired event. One of (subscribe, unsubscribe, profile, upemail, cleaned, campaign).'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Mailchimp-EmlMailChimp
  '/Wl/Zapier/Oauth/OauthMe.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_mail:
                        type: string
                        description: 'Email of the current user.'
                      text_name:
                        type: string
                        description: 'Name of the current user.'
      tags:
        - Oauth-OauthMe
  '/Wl/Zapier/Oauth/OauthToken.json':
    get:
      summary: 'Generated new token base on refresh token.'
      parameters:
        - name: s_token_refresh
          in: query
          description: 'Generated refresh token.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_access:
                        type: string
                        description: 'Generated live token.'
                      s_token_refresh:
                        type: string
                        description: 'Generated refresh token.'
      tags:
        - Oauth-OauthToken
    post:
      summary: 'Generates new token or returns already generated and not expired token.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_client_id:
                  type: string
                  description: 'Application client id.'
                s_client_secret:
                  type: string
                  description: 'Application client secret.'
                s_code:
                  type: string
                  description: 'Randomly generated code required for query verification.'
                url_redirect:
                  type: string
                  description: 'Url link to redirect user to after token successfully generated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_access:
                        type: string
                        description: 'Generated live token.'
                      s_token_refresh:
                        type: string
                        description: 'Generated refresh token.'
      tags:
        - Oauth-OauthToken
  '/Wl/Zapier/ClientGroup/List.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClientGroup-List
    get:
      summary: ''
      parameters:
        - name: is_include_update
          in: query
          description: '`true` if include auto-updated groups, `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_client_group:
                        type: array
                        description: 'List of client groups of the business.'
      tags:
        - ClientGroup-List
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClientGroup-List
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClientGroup-List
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClientGroup-List
  '/Wl/Zapier/ProfileField/List.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ProfileField-List
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'List of profile field of the business.'
      tags:
        - ProfileField-List
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ProfileField-List
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ProfileField-List
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ProfileField-List
  '/Wl/Zapier/Business/List.json':
    delete:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Business-List
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_list:
                        type: array
                        description: 'List of businesses available for current user.'
      tags:
        - Business-List
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Business-List
    post:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Business-List
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Business-List
  '/Wl/Zapier/LeadSource/List.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - LeadSource-List
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_lead_source:
                        type: array
                        description: 'List of Lead Sources. Each element has the next structure: <dl> <dt>int `id`</dt> <dd>Key of the lead source, primary key in {@link \Wl\Lead\Source\LeadSourceSql}.</dd> <dt>string `text_title`</dt> <dd>Name of the Lead Source.</dd> </dl>'
      tags:
        - LeadSource-List
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - LeadSource-List
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - LeadSource-List
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - LeadSource-List
  '/Wl/Social/Share/SocialShare.json':
    post:
      summary: 'Saves the share post data and returns the secret key for the shared object. Records that the user has shared the specified objects (purchases, bookings, locations, or reviews) to a social network, and returns a secret token that can be appended to the destination URL to deep-link directly to the shared items.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_key:
                  type: array
                  description: 'The primary keys of the shared objects. Depends on {@link SocialShareApi::$id_share_object}. If {@link SocialShareApi::$id_share_object} is: * {@link ShareObjectSid::BOOK} - each value is key of the visit. <i>Primary key in {@link \RsVisitSql} table.</i> * {@link ShareObjectSid::LOCATION} - each value is key of the location. <i>Primary key in {@link \RsLocationSql} table.</i> * {@link ShareObjectSid::PURCHASE} - each value is key of the purchase. <i>Primary key in {@link \RsPurchaseSql} table.</i> * {@link ShareObjectSid::REVIEW} - each value is key of the review. <i>Primary key in {@link \RsReviewSql} table.</i>'
                id_share_destination:
                  type: number
                  format: int32
                  description: 'The id of the social network.'
                id_share_object:
                  type: number
                  format: int32
                  description: 'The id of type object for share post to social network.'
                k_business:
                  type: string
                  description: 'Business key.'
                uid:
                  type: string
                  description: 'User key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_secret:
                        type: string
                        description: 'Secret key for access shared object.'
      tags:
        - Share-SocialShare
  '/Wl/Facebook/Login/FacebookLogin.json':
    post:
      summary: 'Performs Facebook authorization within the context of the specified business. Validates that the given business is active, sets it as the current frontend context, and then delegates to the parent Facebook login flow to authenticate the user.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business in which authorization is performed.'
                s_application:
                  type: [string, "null"]
                  description: 'The application id. When application not passed, credential loaded from the application, which makes the request, used {@link FacebookCredential} for load the credential. For set credential need used {@link CredentialApi} and set {@link CredentialApi::$cid_credential} to {@link FacebookCredential::CID}. When application passed, the credential loaded by application id.'
                s_token:
                  type: string
                  description: 'The Facebook token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-FacebookLogin
  '/Wl/Microsoft/Login/MicrosoftLogin.json':
    delete:
      summary: 'Removes the association between a website client and a Microsoft account. Accepts the user''s UID, verifies that the caller is the account owner, and unlinks the Microsoft account from the user''s profile.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-MicrosoftLogin
    get:
      summary: 'Collects data for the Microsoft login button. Called when rendering the "Sign in with Microsoft" button. Generates the OAuth 2.0 authorization URL the button must link to. When a UID is provided, also reports whether that user already has a Microsoft account linked, so the frontend can show "Link" or "Unlink" instead of the default sign-in label.'
      parameters:
        - name: uid
          in: query
          description: 'The client for whom the Microsoft account will be unlinked.'
          schema:
            type: string
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exists:
                        type: boolean
                        description: 'If `true`, the user has a bound Microsoft account. Otherwise, this will be `false`.'
                      url_login:
                        type: string
                        description: 'The Microsoft OAuth 2.0 authorization link.'
      tags:
        - Login-MicrosoftLogin
    post:
      summary: 'Authenticates a user via Microsoft OAuth for the specified business. Validates the business key, sets it as the current frontend business context, and then delegates to the parent Microsoft OAuth flow to complete sign-in.'
      parameters:
        - name: is_external
          in: query
          description: 'If authorization is performed in a third-party application, set this flag in case of authorization errors.'
          schema:
            type: boolean
        - name: url_redirect
          in: query
          description: 'The Redirect URI for external applications. The link to the page on which Microsoft will return the result after authorization. * All possible links must be registered in the Microsoft application used for authorization. * WARNING: Do not use this link for a direct redirect. This will present a vulnerability. * A {@link LoginApi::$url_login} link will be generated along with this redirect URI. * When checking the received {@link LoginApi::$s_code} from Microsoft. The link must be sent along with it to the {@link LoginApi::post()} method.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business in which authorization is performed.'
                s_code:
                  type: string
                  description: 'The authorization code that the app requested.'
                s_state:
                  type: string
                  description: 'If a state parameter is included in the request, the same value should appear in the response. The app should verify that the state values in the request and response are identical.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Login-MicrosoftLogin
  '/Wl/Microsoft/Calendar/MicrosoftCalendarEventWebhook.json':
    post:
      summary: ''
      parameters:
        - name: s_token_validate
          in: query
          description: 'The encoded validation token of the notification point.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_notification:
                  type: array
                  description: 'Notification that comes from Microsoft.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Calendar-MicrosoftCalendarEventWebhook
  '/Wl/Digital/Dictionary/DictionaryElement.json':
    delete:
      summary: 'Deletes word.'
      parameters:
        - name: k_digital_dictionary
          in: query
          description: 'Key of the word to get, change or remove.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dictionary-DictionaryElement
    get:
      summary: 'Returns word value.'
      parameters:
        - name: k_digital_dictionary
          in: query
          description: 'Key of the word to get, change or remove.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_word:
                        type: string
                        description: 'Text of the word.'
      tags:
        - Dictionary-DictionaryElement
    post:
      summary: 'Updates and adds word value.'
      parameters:
        - name: k_digital_dictionary
          in: query
          description: 'Key of the word to get, change or remove.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_word:
                  type: string
                  description: 'Text of the word.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_digital_dictionary:
                        type: string
                        description: 'Key of the word to get, change or remove.'
      tags:
        - Dictionary-DictionaryElement
  '/Wl/Digital/Dictionary/DictionaryList.json':
    get:
      summary: 'Returns list of words, which can mark that product is digital.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: [array, "null"]
                        description: 'A list of curves partner.'
      tags:
        - Dictionary-DictionaryList
  '/Wl/Marketing/Automation/Automation.json':
    delete:
      summary: 'Removes the automation.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business .'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Automation-Automation
    get:
      summary: 'Gets automation data.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business .'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_action:
                        type: [array, "null"]
                        description: 'Call to action data.'
                      a_action_service_type:
                        type: array
                        description: 'List of service types connected to pre-built automations.'
                      a_automation:
                        type: array
                        description: 'Automation data. Result of {@link Automation::get()}.'
                      a_business_type:
                        type: array
                        description: 'List of business types connected to pre-built automations.'
                      url_image:
                        type: [string, "null"]
                        description: 'URL of the pre-built automation image. * `null` if the image is not set or in case of a business automation.'
      tags:
        - Automation-Automation
    post:
      summary: 'Create or edit an automation.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business .'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_action:
                  type: [array, "null"]
                  description: 'Call to action data.'
                a_action_service_type:
                  type: array
                  description: 'List of service types connected to pre-built automations.'
                a_business_type:
                  type: array
                  description: 'List of business types connected to pre-built automations.'
                a_step:
                  type: array
                  description: 'List of automation steps.'
                id_action:
                  type: [number, "null"]
                  description: 'ID of the call to action in the automation.'
                id_label:
                  type: [number, "null"]
                  description: 'ID of the label of the automation.'
                id_trigger:
                  type: number
                  format: int32
                  description: 'ID of the trigger of the automation.'
                is_active:
                  type: boolean
                  description: 'Whether the automation is active.'
                is_draft:
                  type: [boolean, "null"]
                  description: 'Whether to save automation as draft. `null` has different meanings for creation vs update: - On create: derive draft state from `is_active`. - On update: keep the current `is_draft` value in the database (do not change it).'
                is_isaac_use:
                  type: boolean
                  description: 'Whether to use AI (ISAAC) for generating automation message templates. This setting for prebuild only.'
                is_retroactive:
                  type: boolean
                  description: 'Whether retroactive eligibility is enabled for the automation. When `true`, clients who already meet the trigger condition at activation time will be included in the automation audience.'
                k_folder:
                  type: [string, "null"]
                  description: 'Folder key. Only used when creating a new automation. Ignored during editing - the folder is not changed. `null` to assign new automation to "Uncategorized" folder.'
                s_image_command:
                  type: string
                  description: 'Command for pre-built automation image save prepare.'
                text_description:
                  type: string
                  description: 'Description of the automation.'
                text_title:
                  type: string
                  description: 'Title of the automation.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_automation:
                        type: [string, "null"]
                        description: 'Automation key.'
      tags:
        - Automation-Automation
    put:
      summary: 'Duplicate an automation.'
      parameters:
        - name: is_duplicate
          in: query
          description: 'Whether to duplicate the automation.'
          schema:
            type: boolean
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business .'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_automation:
                        type: [string, "null"]
                        description: 'Automation key.'
      tags:
        - Automation-Automation
  '/Wl/Marketing/Automation/AutomationStatus.json':
    post:
      summary: 'Activate or deactivate an automation.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: '`true` if automation is active, `false` otherwise.'
                k_automation:
                  type: string
                  description: 'Automation key.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. `null` to use the system business (prebuilt automation has `null` business key).'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Automation-AutomationStatus
  '/Wl/Marketing/Automation/AutomationOverlap.json':
    get:
      summary: ''
      parameters:
        - name: id_trigger
          in: query
          description: 'ID of the trigger of the automation.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_automation_list:
                        type: array
                        description: 'List of automations that use the same trigger. Each element has the following structure: <dl> <dt>array `a_menu`</dt> <dd>List of actions for the automation.</dd> <dt>string `dtu_add_max_all`</dt> <dd>Max date all clients were added to automation.</dd> <dt>string `dtu_add_max_finish`</dt> <dd>Max date a client was added to automation with "Finished" status.</dd> <dt>string `dtu_add_max_progress`</dt> <dd>Max date a client was added to automation with "In progress" status.</dd> <dt>string `dtu_add_min_all`</dt> <dd>Min date all clients were added to automation.</dd> <dt>string `dtu_add_min_finish`</dt> <dd>Min date a client was added to automation with "Finished" status.</dd> <dt>string `dtu_add_min_progress`</dt> <dd>Min date a client was added to automation with "In progress" status.</dd> <dt>string `dtu_create`</dt> <dd>Creation date and time in MySQL format.</dd> <dt>string `dtu_edit`</dt> <dd>Date and time of the last edit in MySQL format.</dd> <dt>int `i_email_count`</dt> <dd>Number of emails in the automation.</dd> <dt>int `i_recipient_all`</dt> <dd>Total number of recipients in the automation.</dd> <dt>int `i_recipient_finish`</dt> <dd>Number of recipients with "Finished" status in the automation.</dd> <dt>int `i_recipient_progress`</dt> <dd>Number of recipients with "In Progress" status in the automation.</dd> <dt>int `i_sms_count`</dt> <dd>Number of SMS messages in the automation.</dd> <dt>int `i_push_count`</dt> <dd>Number of push notifications in the automation.</dd> <dt>bool `is_active`</dt> <dd>Whether the automation is active.</dd> <dt>string `k_automation`</dt> <dd>Automation key. Primary key in {@link \Wl\Marketing\Automation\AutomationSql}.</dd> <dt>string `k_business`</dt> <dd>Business key. Primary key in {@link \RsBusinessSql} table.</dd> <dt>string `k_folder`</dt> <dd>Folder key. Empty string for the default folder.</dd> <dt>string `s_menu_id`</dt> <dd>ID of the HTML element that contains the action menu.</dd> <dt>string `text_description`</dt> <dd>Automation description.</dd> <dt>string `text_title`</dt> <dd>Automation name.</dd> <dt>string `html_last_modified`</dt> <dd>Text about the last modification of the automation.</dd> <dt>string `text_modified_date`</dt> <dd>Date and time of the last modification, formatted for display.</dd> <dt>string `text_status`</dt> <dd>Textual representation of the automation status.</dd> <dt>string `text_status_class`</dt> <dd>CSS class for the automation status.</dd> <dt>string `text_folder_title`</dt> <dd>Folder name.</dd> <dt>string `uid_create`</dt> <dd>User key of the creator. Primary key in {@link \PassportLoginSql} table.</dd> <dt>string `uid_edit`</dt> <dd>User key of the last editor. Primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                      a_notification_list:
                        type: array
                        description: 'List of system notification mails that semantically overlap with the automation trigger. Each element has the following structure: <dl> <dt>int `id_mail`</dt> <dd>Mail id. One of {@link \RsMailSid} constants.</dd> <dt>bool `is_mail`</dt> <dd>Whether the email channel is enabled for the notification.</dd> <dt>bool `is_push`</dt> <dd>Whether push channel is enabled for the notification.</dd> <dt>bool `is_sms`</dt> <dd>Whether SMS channel is enabled for the notification.</dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. Primary key in {@link \RsMailPatternSql} table. `null` for default template.</dd> <dt>string `text_description`</dt> <dd>Notification description.</dd> <dt>string `text_title`</dt> <dd>Notification title.</dd> </dl>'
      tags:
        - Automation-AutomationOverlap
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_automation_list:
                  type: array
                  description: 'List of automations that use the same trigger. Each element has the following structure: <dl> <dt>array `a_menu`</dt> <dd>List of actions for the automation.</dd> <dt>string `dtu_add_max_all`</dt> <dd>Max date all clients were added to automation.</dd> <dt>string `dtu_add_max_finish`</dt> <dd>Max date a client was added to automation with "Finished" status.</dd> <dt>string `dtu_add_max_progress`</dt> <dd>Max date a client was added to automation with "In progress" status.</dd> <dt>string `dtu_add_min_all`</dt> <dd>Min date all clients were added to automation.</dd> <dt>string `dtu_add_min_finish`</dt> <dd>Min date a client was added to automation with "Finished" status.</dd> <dt>string `dtu_add_min_progress`</dt> <dd>Min date a client was added to automation with "In progress" status.</dd> <dt>string `dtu_create`</dt> <dd>Creation date and time in MySQL format.</dd> <dt>string `dtu_edit`</dt> <dd>Date and time of the last edit in MySQL format.</dd> <dt>int `i_email_count`</dt> <dd>Number of emails in the automation.</dd> <dt>int `i_recipient_all`</dt> <dd>Total number of recipients in the automation.</dd> <dt>int `i_recipient_finish`</dt> <dd>Number of recipients with "Finished" status in the automation.</dd> <dt>int `i_recipient_progress`</dt> <dd>Number of recipients with "In Progress" status in the automation.</dd> <dt>int `i_sms_count`</dt> <dd>Number of SMS messages in the automation.</dd> <dt>int `i_push_count`</dt> <dd>Number of push notifications in the automation.</dd> <dt>bool `is_active`</dt> <dd>Whether the automation is active.</dd> <dt>string `k_automation`</dt> <dd>Automation key. Primary key in {@link \Wl\Marketing\Automation\AutomationSql}.</dd> <dt>string `k_business`</dt> <dd>Business key. Primary key in {@link \RsBusinessSql} table.</dd> <dt>string `k_folder`</dt> <dd>Folder key. Empty string for the default folder.</dd> <dt>string `s_menu_id`</dt> <dd>ID of the HTML element that contains the action menu.</dd> <dt>string `text_description`</dt> <dd>Automation description.</dd> <dt>string `text_title`</dt> <dd>Automation name.</dd> <dt>string `html_last_modified`</dt> <dd>Text about the last modification of the automation.</dd> <dt>string `text_modified_date`</dt> <dd>Date and time of the last modification, formatted for display.</dd> <dt>string `text_status`</dt> <dd>Textual representation of the automation status.</dd> <dt>string `text_status_class`</dt> <dd>CSS class for the automation status.</dd> <dt>string `text_folder_title`</dt> <dd>Folder name.</dd> <dt>string `uid_create`</dt> <dd>User key of the creator. Primary key in {@link \PassportLoginSql} table.</dd> <dt>string `uid_edit`</dt> <dd>User key of the last editor. Primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                a_notification_list:
                  type: array
                  description: 'List of system notification mails that semantically overlap with the automation trigger. Each element has the following structure: <dl> <dt>int `id_mail`</dt> <dd>Mail id. One of {@link \RsMailSid} constants.</dd> <dt>bool `is_mail`</dt> <dd>Whether the email channel is enabled for the notification.</dd> <dt>bool `is_push`</dt> <dd>Whether push channel is enabled for the notification.</dd> <dt>bool `is_sms`</dt> <dd>Whether SMS channel is enabled for the notification.</dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. Primary key in {@link \RsMailPatternSql} table. `null` for default template.</dd> <dt>string `text_description`</dt> <dd>Notification description.</dd> <dt>string `text_title`</dt> <dd>Notification title.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Automation-AutomationOverlap
  '/Wl/Marketing/Automation/AutomationRetroactive.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_step_trigger:
                  type: array
                  description: 'Trigger settings.'
                id_trigger:
                  type: number
                  format: int32
                  description: 'ID of the trigger of the automation.'
                k_automation:
                  type: [string, "null"]
                  description: 'Automation key. `null` if the automation does not exist yet.'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_user:
                        type: number
                        format: int32
                        description: 'Number of users who already meet the trigger condition at activation time.'
                      url_report:
                        type: string
                        description: 'URL to the report with users who meet the trigger condition at activation time.'
      tags:
        - Automation-AutomationRetroactive
  '/Wl/Marketing/Banner/Banner.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'The business key within which the request is made. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Banner-Banner
    get:
      summary: ''
      parameters:
        - name: id_banner_type
          in: query
          description: 'Banner type identifier to display. One of {@link BannerContentSid} constants. `null` if not specified by the caller.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The business key within which the request is made. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_banner_feature:
                        type: array
                        description: 'List of banner feature pills. Each element: <dl> <dt>string `sid_icon`</dt> <dd>CSS class suffix for the feature icon.</dd> <dt>string `text_label`</dt> <dd>Feature label text.</dd> </dl>'
                      show_banner:
                        type: boolean
                        description: 'Whether the promotional banner should be shown.'
                      text_banner_description:
                        type: string
                        description: 'Banner description line.'
                      text_banner_title:
                        type: string
                        description: 'Banner title.'
                      uid:
                        type: string
                        description: 'Current user key. Primary key in {@link \PassportLoginSql} table.'
                      url_learn_more:
                        type: string
                        description: 'URL for the "Learn more" help article link in the banner.'
                      url_upgrade:
                        type: string
                        description: 'URL to the Marketing Suite subscription/upgrade page.'
      tags:
        - Banner-Banner
  '/Wl/AiAgent/Link/SendMail.json':
    post:
      summary: 'Sends an AI agent link action email (booking or purchase) to the specified user. Triggers the email that the AI agent sends when a client clicks a booking or purchase link generated by the AI agent chat. Requires the business to have an active AI agent subscription; the action type determines which notification template is used and which service or purchase item is referenced in the email body.'
      parameters:
        - name: id_purchase_item
          in: query
          description: 'Purchase item ID. Required if {@link SendMailApi::$text_action} is ''purchase''. One of the {@link \RsPurchaseItemSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_service
          in: query
          description: 'Service ID. Required if {@link SendMailApi::$text_action} is ''booking''.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Required.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Unique identifier for the link.'
          schema:
            type: string
        - name: text_action
          in: query
          description: 'Action type. Must be ''purchase'' or ''booking''.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User ID. Required.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Link-SendMail
  '/Wl/Tuition/Enrollment/TuitionEnrollmentList.json':
    get:
      summary: 'Returns list of enrollments for the tuition. Get a full list of users who purchased the tuition. For each enrollment, returns the list of events and the users have been enrolled in.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_tuition_id
          in: query
          description: 'Key of the tuition in tuition microservice.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_enrollment:
                        type: object
                        description: 'List of tuition enrollments.'
      tags:
        - Enrollment-TuitionEnrollmentList
  '/Thoth/LayoutBe/UserProfile/Popup/UserPopup.json':
    get:
      summary: ''
      parameters:
        - name: is_backend
          in: query
          description: 'Whether this is a backend context. `true` if backend, `false` if frontend.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Current frontend business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business:
                        type: object
                        description: 'List of businesses for switching. One element contains: <dl> <dt>string `k_business`</dt> <dd>Business key. Primary key from the {@link \RsBusinessSql} table.</dd> <dt>string `text_title`</dt> <dd>Business title.</dd> </dl>'
                      a_location:
                        type: object
                        description: 'List of locations for switching. One element contains: <dl> <dt>string `k_business`</dt> <dd>Location business key. Primary key from the {@link \RsBusinessSql} table.</dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl>'
                      a_user:
                        type: object
                        description: 'List of related profiles. One element contains: <dl> <dt>string `s_name`</dt> <dd>Name of the related profile.</dd> <dt>string `url_login`</dt> <dd>Login link for this profile.</dd> </dl>'
                      is_backend:
                        type: boolean
                        description: 'Whether this is a backend context. `true` if backend, `false` if frontend.'
                      is_general:
                        type: boolean
                        description: 'Flag indicating general mode without a selected business.'
                      is_staff:
                        type: boolean
                        description: 'Flag indicating that the current user has staff access.'
                      k_business:
                        type: string
                        description: 'Current frontend business key. Primary key in the {@link \RsBusinessSql} table.'
                      show_select:
                        type: boolean
                        description: 'Show business/location switching selects.'
                      uid:
                        type: string
                        description: 'Current user ID.'
                      url_logout:
                        type: string
                        description: 'Logout link.'
                      url_profile:
                        type: string
                        description: 'Link to the profile page.'
                      url_schedule:
                        type: string
                        description: 'Link to schedule (backend access).'
                      url_staff:
                        type: string
                        description: 'Link to staff profile in backend popup.'
      tags:
        - Popup-UserPopup
  '/Thoth/MarketingCampaigns/Report/CampaignList/SendCampaign.json':
    get:
      summary: 'Gets ''Send campaign'' methods.'
      parameters:
        - name: id_mail_page
          in: query
          description: 'The ID of the mail page, one of the {@link \RsMailPageSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_campaign_list:
                        type: object
                        description: 'List of the ''Send campaign'' methods. <dl> <dt>array `a_campaign_method`</dt> <dd> Send campaign methods. Each element has the next structure: <dl> <dt>string `s_class`</dt> <dd>A method classes.</dd> <dt>string `s_onclick`</dt> <dd>Click method.</dd> <dt>string `s_title`</dt> <dd>Title of a method.</dd> <dt>string `s_url`</dt> <dd>URL link.</dd> </dl> </dd> </dl>'
      tags:
        - CampaignList-SendCampaign
  '/Thoth/MarketingCampaigns/Report/CampaignDetail/ClickTracking.json':
    get:
      summary: 'Gets mail campaign tracking data. Used for ''Mail campaign detail''  report.'
      parameters:
        - name: dtl_send
          in: query
          description: 'Send date.'
          schema:
            type: string
        - name: id_mail_campaign_status
          in: query
          description: 'Mail campaign status ID. One of {@link MailCampaignStatusSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mail_status
          in: query
          description: 'Mail history filter. One of {@link \RsMailHistorySid} constants.'
          schema:
            type: number
            format: int32
        - name: is_mail_pattern_live_specific
          in: query
          description: 'Whether a mail pattern live specific.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key specific purchase item belongs to.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern_live
          in: query
          description: 'Mail pattern live key.'
          schema:
            type: string
        - name: text_mail_campaign
          in: query
          description: 'Mail campaign title.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_row_list:
                        type: array
                        description: 'Row list: <dl> <dt>array `a_uid`</dt> <dd>UIDs of clients who made clicks. Used to filter by unique clicks.</dd> <dt>int `i_click_unique`</dt> <dd>Unique clicks per email link.</dd> <dt>int `i_click_total`</dt> <dd>Total clicks per email link.</dd> <dt>int `i_click_rate`</dt> <dd>Click rate.</dd> <dt>string `url_full`</dt> <dd>Clicked in an email link.</dd> <dt>string `url_click_unique`</dt> <dd>link to Marketing detail report filtered by unique clicks.</dd> </dl>'
      tags:
        - CampaignDetail-ClickTracking
  '/Thoth/PayProcessor/Nuvei/Terminal/NuveiOmnichannelTerminalPaymentVoid.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Pay transaction key, which was used to create payment intent. `null` in case when not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Terminal-NuveiOmnichannelTerminalPaymentVoid
  '/Thoth/PayProcessor/DirectConnect/Terminal/TerminalTransactionCancel.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_transaction:
                  type: [string, "null"]
                  description: 'Pay transaction key, which was used to create payment intent. `null` in case when not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_message:
                        type: [string, "null"]
                        description: 'Arbitrary information about Payment Ticket, like error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Terminal-TerminalTransactionCancel
  '/Thoth/PayProcessor/DirectConnect/Terminal/TerminalTransactionStart.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                id_transaction_type:
                  type: number
                  format: int32
                  description: 'The type of transaction you wish to create. One of {@link \Thoth\PayProcessor\DirectConnect\Transaction\PureCloudTransactionTypeSid} constants.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                is_prompt_for_tip:
                  type: boolean
                  description: 'Should terminal prompt for tip entry. For {@link PayProcessorSid::DIRECT_CONNECT} can be used only when transaction type is {@link PureCloudTransactionTypeSid::SALE}.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                s_terminal_id:
                  type: [string, "null"]
                  description: 'Unique terminal identifier. `null` in case when not initialized yet.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      s_ticket_id:
                        type: [string, "null"]
                        description: 'Gateway Ticket ID. `null` in case response has not received yet or request is failed.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - Terminal-TerminalTransactionStart
  '/Thoth/PayProcessor/StripeCom/Onboarding/CreateAccount.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business within which the account should be created.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_business_merchant:
                        type: [string, "null"]
                        description: 'Key of the created business merchant. Primary key in {@link \RsBusinessMerchantSql}.'
                      url_account:
                        type: [string, "null"]
                        description: 'URL to redirect user to Stripe dashboard to upload documents.'
      tags:
        - Onboarding-CreateAccount
  '/Thoth/PayProcessor/StripeCom/Onboarding/CreateLink.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business within which the account should be created.'
                s_account:
                  type: [string, "null"]
                  description: 'ID of the account for which link should be created.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_account:
                        type: [string, "null"]
                        description: 'URL to redirect user to Stripe dashboard to upload documents.'
      tags:
        - Onboarding-CreateLink
  '/Thoth/PayProcessor/StripeCom/Account/CreateLoginLink.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business within which the link should be created.'
                s_account:
                  type: [string, "null"]
                  description: 'ID of the account for which link should be created.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_login:
                        type: [string, "null"]
                        description: 'URL to the Express Dashboard login page.'
      tags:
        - Account-CreateLoginLink
  '/Thoth/ReportCore/QueryEngine/Page/ReportQueryActivate.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `''0''` when activating a system-level report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: number
                  format: int32
                  description: '`1` to activate the record, `0` to deactivate (soft-delete).'
                k_report_query:
                  type: string
                  description: 'Report query key. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Page-ReportQueryActivate
  '/Thoth/ReportCore/QueryEngine/Page/ReportQueryGet.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. `''0''` when querying a system-level report. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_query
          in: query
          description: 'Report query key. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_report_access:
                        type: number
                        format: int32
                        description: 'Access level for this report. One of {@link \RsReportAccessSid} constants. `0` when the record does not have an access level set.'
                      is_active:
                        type: boolean
                        description: 'Whether the record is active.'
                      is_system:
                        type: boolean
                        description: 'Whether this is a system-level report (visible to all businesses).'
                      json_actions:
                        type: string
                        description: 'JSON actions definition, or empty string if not set.'
                      json_totals:
                        type: string
                        description: 'JSON totals definition, or empty string if not set.'
                      s_sql:
                        type: string
                        description: 'Body of the SQL query (SELECT only).'
                      s_title:
                        type: string
                        description: 'Human-readable title of the report.'
      tags:
        - Page-ReportQueryGet
  '/Thoth/ReportCore/QueryEngine/Page/ReportQueryInsert.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_report_access:
                  type: number
                  format: int32
                  description: 'Access level for this report. One of {@link \RsReportAccessSid} constants. Required - the report cannot be saved without selecting an access level.'
                is_system:
                  type: boolean
                  description: 'Whether to create a system-level report (`k_business`=''0''). System reports are visible to all businesses. Only WL admins can create them.'
                json_actions:
                  type: string
                  description: 'JSON actions definition. Empty string is normalized to `null` (no actions configured).'
                json_totals:
                  type: string
                  description: 'JSON totals definition. Empty string is normalized to `null` (no totals configured).'
                s_sql:
                  type: string
                  description: 'Body of the SQL query (SELECT only).'
                s_title:
                  type: string
                  description: 'Human-readable title of the report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_report_query:
                        type: string
                        description: 'Key of the newly created record. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.'
      tags:
        - Page-ReportQueryInsert
  '/Thoth/ReportCore/QueryEngine/Page/ReportQueryList.json':
    get:
      summary: ''
      parameters:
        - name: id_record_status
          in: query
          description: 'Status filter. One of {@link ReportQueryRecordStatusSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. `''0''` to list system-level reports. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of registry records. Each element: <dl> <dt>string `dtu_update`</dt> <dd>UTC datetime of the last update.</dd> <dt>bool `is_active`</dt> <dd>`true` if the record is active.</dd> <dt>bool `is_system`</dt> <dd>`true` if this is a system-level report visible to all businesses, `false` for business-specific reports.</dd> <dt>string `k_report_query`</dt> <dd>Report query key. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.</dd> <dt>string `s_title`</dt> <dd>Human-readable report title.</dd> <dt>string `url_view`</dt> <dd>URL of the report view page for this record.</dd> </dl>'
      tags:
        - Page-ReportQueryList
  '/Thoth/ReportCore/QueryEngine/Page/ReportQuerySchema.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_action:
                        type: array
                        description: 'Registered row actions available for use in `json_actions`. Each entry: <dl> <dt>string `s_name`</dt> <dd>Unique action name used in `json_actions` as `s_action`.</dd> <dt>string `text_title`</dt> <dd>Human-readable menu item label shown to the user.</dd> <dt>string[] `a_key_names`</dt> <dd>Required SELECT column aliases that must be present in the SQL query for this action.</dd> <dt>bool `is_confirm`</dt> <dd>`true` if a confirmation modal must appear before executing the action.</dd> <dt>string `s_action_type`</dt> <dd>`''api''` for backend API call, or `''link''` for URL navigation.</dd> <dt>string `text_usage`</dt> <dd>Plain-text usage hint describing the action and required SQL columns.</dd> </dl>'
                      a_format:
                        type: array
                        description: 'Supported format hints for SELECT-alias dot-suffixes. Each entry: <dl> <dt>string `s_name`</dt> <dd>Hint name including the `format` prefix (e.g. `formatMoney`).</dd> <dt>string `text_hint`</dt> <dd>Human-readable description shown in the autocomplete popup.</dd> </dl>'
                      a_placeholder:
                        type: array
                        description: 'Built-in `@placeholder` variables supported by all tables that honour date-range filtering. Each entry: <dl> <dt>string `s_name`</dt> <dd>Placeholder name without the leading `@` (e.g. `dl_start`).</dd> <dt>string `text_hint`</dt> <dd>Human-readable description shown in the autocomplete popup.</dd> </dl>'
                      a_table:
                        type: array
                        description: 'Registered tables keyed by SQL table name. Each value: <dl> <dt>bool `is_first_allowed`</dt> <dd>`true` if the table may be used as the first (driving) table in FROM.</dd> <dt>bool `has_date_filter`</dt> <dd>`true` if the table honours `@dl_start`/`@dl_end` placeholders.</dd> <dt>array `a_column`</dt> <dd> Column descriptors. Each: <dl> <dt>string `s_name`</dt> <dd>Column name as used in SQL.</dd> <dt>string `s_type`</dt> <dd>PHP type name (`int`, `string`, ...).</dd> <dt>string `text_hint`</dt> <dd>Human-readable description from PHPDoc.</dd> </dl> </dd> </dl>'
                      a_total:
                        type: array
                        description: 'Supported aggregate functions for the `json_totals` field. Each entry: <dl> <dt>string `s_function`</dt> <dd>Aggregate function name (e.g. `SUM`, `COUNT`).</dd> <dt>string `text_hint`</dt> <dd>Plain-text description shown in the reference panel.</dd> <dt>string `s_alias`</dt> <dd>Whether `s_alias` is `''required''`, `''optional''`, or `''omit''` for this function.</dd> </dl>'
                      a_user_field:
                        type: array
                        description: 'User View field groups keyed by logical section name. Each value is an array of field descriptors for the User View reference panel: <dl> <dt>string `s_column`</dt> <dd>Full `table.column` reference inserted into SQL on click.</dd> <dt>string `text_label`</dt> <dd>Short human-readable column label shown prominently.</dd> <dt>string `text_tooltip`</dt> <dd>Longer description shown in the hover tooltip.</dd> </dl>'
      tags:
        - Page-ReportQuerySchema
  '/Thoth/ReportCore/QueryEngine/Page/ReportQueryTotal.json':
    get:
      summary: ''
      parameters:
        - name: a_filter
          in: query
          description: 'Filter values forwarded to the executor context. Accepts date-range keys (`dl_start`, `dl_end` as `''YYYY-MM-DD''`) and any user-facing `@placeholder` variables.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_query
          in: query
          description: 'Report query key. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_total:
                        type: array
                        description: 'Computed totals. One entry per configured total card. Empty array when the report has no totals configured or the report is inactive/missing. Each entry: <dl> <dt>string `text_label`</dt> <dd>Human-readable label for the card (from `json_totals`).</dd> <dt>string `s_value`</dt> <dd>Computed aggregate value formatted as a string.</dd> </dl>'
      tags:
        - Page-ReportQueryTotal
  '/Thoth/ReportCore/QueryEngine/Page/ReportQueryUpdate.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_report_access:
                  type: number
                  format: int32
                  description: 'Access level for this report. One of {@link \RsReportAccessSid} constants. Required - the report cannot be saved without selecting an access level.'
                json_actions:
                  type: string
                  description: 'JSON actions definition. Empty string is normalized to `null` (no actions configured).'
                json_totals:
                  type: string
                  description: 'JSON totals definition. Empty string is normalized to `null` (no totals configured).'
                k_report_query:
                  type: string
                  description: 'Report query key. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.'
                s_sql:
                  type: string
                  description: 'Body of the SQL query (SELECT only).'
                s_title:
                  type: string
                  description: 'Human-readable title of the report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Page-ReportQueryUpdate
  '/Thoth/ReportCore/QueryEngine/Report/ReportQueryColumnMeta.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_query
          in: query
          description: 'Report query key. Primary key in the {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_action_schema:
                        type: array
                        description: 'Parsed action schema for the report row menu, or empty array when no actions are configured. Populated from the `json_actions` column of {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql} via {@link QueryEngineActionConfig::parse()}. Each element describes one action visible in the 3-dot row menu: <dl> <dt>array `a_key_map`</dt> <dd>Map of action parameter name to SELECT alias used to extract the value from the row.</dd> <dt>bool `is_confirm`</dt> <dd>`true` if a confirmation modal must appear before executing the action.</dd> <dt>string `s_name`</dt> <dd> Registered action name (matches {@link \Thoth\ReportCore\QueryEngine\Action\QueryEngineActionAbstract::NAME}). </dd> <dt>string `text_title`</dt> <dd>Human-readable menu item label shown in the row menu.</dd> <dt>string `s_url_api`</dt> <dd>API endpoint URL for `api`-type actions. Empty string for `link` actions.</dd> <dt>string `s_url_template`</dt> <dd>URL template with `{param}` placeholders for `link`-type actions. Empty string for `api` actions.</dd> </dl>'
                      a_column:
                        type: array
                        description: 'Active column metadata. One entry per SELECT-clause column, in SELECT order. Each entry: Empty array if the registry record is missing, inactive, or the SQL cannot be parsed. <dl> <dt>string `s_slot`</dt> <dd> Clean column alias (format suffix stripped), matching the key used in {@link \Thoth\ReportCore\Generator\ReportGeneratorRowAbstract::$a_dynamic} and the `s_name` of the dynamic field registered by {@link \Thoth\ReportCore\QueryEngine\Report\ReportQueryReport::generatorDynamic()}. Example: `m_amount` for raw alias `m_amount.formatMoney`. </dd> <dt>string `text_title`</dt> <dd>Human-readable column title (same value as `s_slot` unless a custom title is added later).</dd> <dt>string|null `s_format`</dt> <dd>Format hint extracted from the alias (`Money`, `Date`, ...); `null` if absent.</dd> </dl>'
                      a_placeholder:
                        type: array
                        description: 'Names of `@placeholder` variables found in the WHERE clause, in order of appearance. Excludes: - Context-injected placeholders (those whose {@link \Thoth\ReportCore\QueryEngine\Placeholder\QueryEnginePlaceholderAbstract::FILTER_FIELD_CLASS} equals {@link \Thoth\ReportCore\QueryEngine\Placeholder\QueryEnginePlaceholderAbstract::CONTEXT_INJECTED}), e.g. `@business`, `@actor`, `@dl_end`. - Date-filter placeholders (`@dl_start`, `@dl_end`): already surfaced via `has_date_filter` and rendered by the dedicated date picker widget, not as generic text inputs. Unregistered placeholder names (not in the registry) are included as-is - the SPA renders a generic text input for them as a fallback. Empty array when there are no user-facing placeholders or the SQL has no WHERE clause.'
                      a_totals_config:
                        type: array
                        description: 'Parsed totals configuration loaded from the `json_totals` column of {@link \Thoth\ReportCore\QueryEngine\Registry\ReportQuerySql}. One entry per total card, in the order they appear in `json_totals`. Empty array when no totals are configured. Each entry: <dl> <dt>string `s_function`</dt> <dd>Aggregation function: ''AVG'', ''COUNT'', ''MAX'', ''MIN'', or ''SUM''.</dd> <dt>string `text_label`</dt> <dd>Human-readable label for the summary card.</dd> <dt>string|null `s_alias`</dt> <dd>Clean column alias to aggregate. `null` for COUNT-all.</dd> </dl>'
                      has_date_filter:
                        type: boolean
                        description: '`true` if the SQL references `dl_start` or `dl_end` (with `@` at the beginning) placeholders (i.e. it wants date filtering); `false` otherwise. Drives date-filter widget visibility on the SPA side.'
                      text_title:
                        type: string
                        description: 'Human-readable title of the report instance (from the registry record). Empty string when the record is missing or inactive.'
      tags:
        - Report-ReportQueryColumnMeta
  '/Thoth/ReportCore/QueryEngine/AI/ReportQueryAi.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_query
          in: query
          description: 'Optional key of the report query being edited. Empty string when the user is building a new report that has not been saved yet. Used only to bind the AI prompt log entry to the corresponding registry record.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_actions_in:
                  type: string
                  description: 'Current Actions JSON from the report form. Sent as context so the AI can refine existing actions rather than starting from scratch. Empty string if the form has no actions configured.'
                json_history:
                  type: string
                  description: 'Conversation history as a JSON-encoded array of message objects. Each element must have `s_role` (''user'' or ''assistant'') and `s_content` (string). Empty string or omitted means no prior history (first request).'
                json_totals_in:
                  type: string
                  description: 'Current Totals JSON from the report form. Sent as context so the AI can refine existing totals rather than starting from scratch. Empty string if the form has no totals configured.'
                s_prompt:
                  type: string
                  description: 'User''s natural-language description of the desired report. Example: "Show all users who paid more than 50 dollars this month".'
                s_session_ai:
                  type: string
                  description: 'Client-generated session identifier that groups all AI prompt log entries from a single conversation. Generated once when the AI panel opens and passed unchanged in every subsequent request within that session. Empty string if the client did not supply one.'
                s_sql_in:
                  type: string
                  description: 'Current SQL query from the report editor. Sent as context so the AI can modify an existing query rather than starting from scratch. Empty string for a new report.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_actions:
                        type: string
                        description: 'AI-generated Actions JSON string, or empty string if no change is needed.'
                      json_totals:
                        type: string
                        description: 'AI-generated Totals JSON string, or empty string if no totals are needed.'
                      s_answer:
                        type: string
                        description: 'AI text answer for informational queries (capability questions, field explanations, responses when requested data is unavailable). Empty string when the response is SQL-only or when no explanation is needed.'
                      s_intent:
                        type: string
                        description: 'Intent the AI model detected for this request. One of `''sql''` (SQL generation or modification), `''capabilities''` (user asked what data is available), or `''out_of_scope''` (request is outside the `QueryEngine` module). Useful for client-side logging and optional history filtering.'
                      s_question:
                        type: string
                        description: 'Optional clarification question from the AI model. Empty string when the model generated SQL without needing clarification.'
                      s_sql:
                        type: string
                        description: 'AI-generated SELECT query. Empty string means "no change" -- current SQL is preserved.'
      tags:
        - AI-ReportQueryAi
  '/Thoth/WlPay/Form/Complete/Complete.json':
    get:
      summary: 'Returns information about purchase.'
      parameters:
        - name: k_purchase
          in: query
          description: 'Purchase ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_arguments:
                        type: array
                        description: 'Analytics function arguments.'
                      k_currency:
                        type: string
                        description: 'ID of purchase currency. Primary key in {@link \RsCurrencySql} table.'
                      m_cost:
                        type: string
                        description: 'Purchase cost.'
                      s_purchase:
                        type: string
                        description: 'Number of receipt.'
      tags:
        - Complete-Complete
  '/Thoth/WlPay/Bank/Ach/List.json':
    get:
      summary: 'Retrieves information about user''s ACH accounts.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of current business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show information for. <tt>0</tt> to use user''s home location.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of ACH accounts: <dl> <dt>int `id_pay_bank_ach_holder`</dt> <dd>Type of account holder. One of {@link \RsPayBankAchHolderSid} constants.</dd> <dt>int `id_pay_bank_ach_type`</dt> <dd>Type of account. One of {@link \RsPayBankAchTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd><tt>true</tt> - this account is default payment method; <tt>false</tt> - otherwise.</dd> <dt>string `k_pay_bank`</dt> <dd>ID of bank account. Primary key in {@link \RsPayBankSql}.</dd> <dt>string `text_name_account`</dt> <dd>Account name.</dd> <dt>string `text_name_holder`</dt> <dd>Account holder name.</dd> <dt>string `text_number`</dt> <dd>ACH account number.</dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'Whether new ACH account can be added. `true` if new ACH account can be added. `false` if new ACH account can not be added.'
      tags:
        - Ach-List
  '/Thoth/WlPay/Bank/Card/List.json':
    get:
      summary: 'Retrieves information about user''s bank cards. Returns the list of saved payment cards for the specified user and business, including card number fragment, expiry date, card system, holder name, and default status. Also returns whether new cards can be added.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of current business.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show information for. <tt>0</tt> to use user''s home location.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'ID of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_bank_card:
                        type: array
                        description: 'A list of bank cards. The array keys are the card `k_pay_bank` IDs. Each element has the following keys: <dl> <dt>int `i_month`</dt> <dd>The month when the payment card expires, represented by a number (1=January and 12=December).</dd> <dt>int `i_year`</dt> <dd>The last two digits of the year when the payment card expires.</dd> <dt>int `id_card_system`</dt> <dd>The card system ID. This is one of the {@link \ACardSystemSid} constants.</dd> <dt>bool `is_default`</dt> <dd>If `true`, then this card is the user default card. If `false`, then this isn''t the user default card.</dd> <dt>string `k_pay_address`</dt> <dd>The payment address ID. This refers to a physical address associated with a payment card.</dd> <dt>string `k_pay_bank`</dt> <dd>The payment method ID. Each payment card for each user will have its own ID.</dd> <dt>string `text_name_card`</dt> <dd> The payment card descriptor. This is typically the company name and the last 4 digits of the card (for example, visa-5903). </dd> <dt>string `text_name_holder`</dt> <dd>The name of the card owner as it appears on the card.</dd> <dt>string `text_number`</dt> <dd> A portion of the payment card number, used to identify the card. This is typically the last 4 digits of the card number. </dd> </dl>'
                      a_list:
                        type: array
                        description: 'List of bank cards. Duplicate of {@link \Thoth\WlPay\Bank\Card\ListApi::$a_bank_card} as a flat indexed array. <dl> <dt>int `i_month`</dt> <dd>The month when the payment card expires, represented by a number (1=January and 12=December).</dd> <dt>int `i_year`</dt> <dd>The last two digits of the year when the payment card expires.</dd> <dt>int `id_card_system`</dt> <dd>The card system ID. This is one of the {@link \ACardSystemSid} constants.</dd> <dt>bool `is_default`</dt> <dd>If `true`, then this card is the user default card. If `false`, then this isn''t the user default card.</dd> <dt>string `k_pay_address`</dt> <dd>The payment address ID. This refers to a physical address associated with a payment card.</dd> <dt>string `k_pay_bank`</dt> <dd>The payment method ID. Each payment card for each user will have its own ID.</dd> <dt>string `text_name_card`</dt> <dd> The payment card descriptor. This is typically the company name and the last 4 digits of the card (for example, visa-5903). </dd> <dt>string `text_name_holder`</dt> <dd>The name of the card owner as it appears on the card.</dd> <dt>string `text_number`</dt> <dd> A portion of the payment card number, used to identify the card. This is typically the last 4 digits of the card number. </dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'Whether new card can be added. `true` if new cards can be added. `false` if new cards can not be added.'
      tags:
        - Card-List
  '/Thoth/WlPay/Transaction/Report/TransactionAllPayment.json':
    get:
      summary: 'Returns All Transactions Report data for the specified date range. Provides access to the All Transactions Report used for revenue reconciliation and export. The report is generated asynchronously and cached; check `$id_report_status` to determine whether generation is still in progress. Set `$is_refresh` to request regeneration and use `$i_page` to paginate through up to {@link TransactionAllPaymentApi::LIMIT} rows per request.'
      parameters:
        - name: dl_date_end
          in: query
          description: 'The end date in local time to retrieve transactions for.'
          schema:
            type: string
        - name: dl_date_start
          in: query
          description: 'The end date in local time to retrieve transactions for.'
          schema:
            type: string
        - name: i_page
          in: query
          description: 'The page of the report, starting from 0. Each page will contain a maximum of {@link TransactionAllPaymentApi::LIMIT} rows.'
          schema:
            type: number
            format: int32
        - name: is_refresh
          in: query
          description: 'Determines whether the report should be refreshed. If `true`, this report should be refreshed. A report refresh can''t be requested while the report is being generated. Otherwise, `false` to only return contents of the report. If the report hasn''t yet been generated, it will automatically start generating in the background.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business for which report should be generated.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field:
                        type: array
                        description: 'A list of fields in the report. This array is effectively a title row for the table returned in {@link TransactionAllPaymentApi::$a_row}.'
                      a_row:
                        type: array
                        description: 'The report data. This is an indexed array where each element is also an indexed array representing one report row. Column positions in each row correspond to the field names in {@link TransactionAllPaymentApi::$a_field}. Known columns (use {@link TransactionAllPaymentApi::$a_field} to resolve their positional indexes): <dl> <dt>array `a_pay_transaction`</dt> <dd> List of pay transactions associated with this row. Each element: <dl> <dt>int `id_pay_method`</dt> <dd>Payment method ID. One of {@link \RsPayMethodSid} constants.</dd> <dt>string `k_pay_transaction`</dt> <dd>Pay transaction key. <i>Primary key in the {@link \RsPayTransactionSql} table.</i></dd> <dt>string `m_amount`</dt> <dd>Amount paid.</dd> <dt>string `m_surcharge`</dt> <dd>Surcharge amount from store settings.</dd> <dt>string `s_batch_number`</dt> <dd>Batch number for this transaction.</dd> </dl> </dd> <dt>string `dtu_purchase_start`</dt> <dd>Start date and time of the purchase in UTC.</dd> <dt>string `k_purchase`</dt> <dd>Purchase key. <i>Primary key in the {@link \RsPurchaseSql} table.</i></dd> <dt>string `m_net_sale`</dt> <dd>Net sales amount.</dd> <dt>string `m_total_tax`</dt> <dd>Total tax amount.</dd> <dt>string `o_client.text_first`</dt> <dd>First name of the client.</dd> <dt>string `o_client.text_last`</dt> <dd>Last name of the client.</dd> <dt>string `o_client.text_name`</dt> <dd>Full name of the client.</dd> <dt>string `o_location.k_location`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `s_batch_number`</dt> <dd>Batch number of the first transaction in the row.</dd> <dt>string `text_location_address`</dt> <dd>Street address of the location.</dd> <dt>string `text_location_city`</dt> <dd>Name of the location''s city.</dd> <dt>string `text_location_country`</dt> <dd>Name of the location''s country.</dd> <dt>string `text_location_postal`</dt> <dd>Postal code of the location.</dd> <dt>string `text_location_region`</dt> <dd>Name of the location''s region.</dd> <dt>string `text_payment_method`</dt> <dd>Name of the payment method used.</dd> <dt>string `text_payment_method_base`</dt> <dd>Name of the base payment method. If a custom method was used, this is the method it is based on.</dd> <dt>string `text_tax_title`</dt> <dd>Comma-separated list of tax names applied to the purchase.</dd> <dt>string `text_tax_value`</dt> <dd> Comma-separated list of tax values. Percentage-based taxes have `%` appended; fixed-amount taxes have `$` appended. </dd> </dl>'
                      a_warning:
                        type: array
                        description: 'The warning list of the report.'
                      dtu_complete:
                        type: [string, "null"]
                        description: 'The date and time if the report has completed generation. Otherwise, this will be `null`.'
                      dtu_queue:
                        type: [string, "null"]
                        description: 'The date and time if this report has been put in the generation queue. Otherwise, this will be `null`.'
                      dtu_start:
                        type: [string, "null"]
                        description: 'The date and time if generation of this report has started. Otherwise, this will be `null`.'
                      id_report_status:
                        type: number
                        format: int32
                        description: 'The report status. One of {@link \Wl\Report\Generator\ReportGeneratorStatusSid} constants.'
                      is_more:
                        type: boolean
                        description: 'Determines whether to show more rows in the report. If `true`, there are more report rows to get. Otherwise, `false` to indicate that all rows in the report have already been retrieved.'
                      is_report_complete:
                        type: boolean
                        description: 'Determines whether the report is complete. If the report is accessed on the current day, or is returning a result that was cached on the current day, it could be incomplete as not all the transactions for the day are present. If `true`, the report has been completed. Otherwise, `false`.'
      tags:
        - Report-TransactionAllPayment
  '/Thoth/WlPay/Account/Charge/Charge.json':
    post:
      summary: 'Refills the user account balance by the specified payment amount or adjusts it manually. Accepts the payment amount, account key or user-and-business identifiers, charge mode, and payment form data. Processes the payment through the configured payment environment and returns the purchase key when a new purchase is created (for the automatic charge mode).'
      parameters:
        - name: id_pay_account_charge
          in: query
          description: 'The account charge mode. One of the {@link \RsPayAccountChargeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_staff
          in: query
          description: 'If `true`, the account is filled by a staff member in the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The ID of the business the user account belongs to. This shouldn''t be passed if a user account has already been created. In such cases, {@link ChargeApi::$k_pay_account} should be passed instead. If both the business ID and account ID passed, the system checks if the given business is the owner of the specified account.'
          schema:
            type: string
        - name: k_pay_account
          in: query
          description: 'The ID of the user account to refill. This may be 0 if a user account hasn''t been created yet. In such cases, {@link ChargeApi::$k_business} and {@link ChargeApi::$uid} should be passed instead. If not passed, the currency of account equals the default business currency.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The ID of the user whose account is being refilled. This shouldn''t be passed if a user account has already been created. In such cases, {@link ChargeApi::$k_pay_account} should be passed instead. If both the user ID and account ID passed, the system checks if the given user is the owner of the specified account.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'The source mode key. One of the {@link \Wl\Mode\ModeSid} constants.'
                m_amount:
                  type: string
                  description: 'The payment amount.'
                s_comment:
                  type: string
                  description: 'The transaction comment.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_purchase:
                        type: string
                        description: 'The ID of the purchase that was created during payment. This value is only returned in cases where a purchase was created. A new purchase is created when {@link ChargeApi::$id_pay_account_charge} equals {@link \RsPayAccountChargeSid::AUTO}.'
      tags:
        - Charge-Charge
  '/Thoth/WlPay/Address/Widget/WidgetEdit.json':
    get:
      summary: 'Gets data for "edit payment address" widget.'
      parameters:
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_geo:
                        type: array
                        description: 'List of possible regions.'
                      html_phone_mask:
                        type: string
                        description: 'Mask for phone entering (ready for output to the page).'
                      text_phone_mask:
                        type: string
                        description: 'Mask for phone entering.'
      tags:
        - Widget-WidgetEdit
  '/Thoth/WlPay/Installment/Reschedule/InstallmentReschedule.json':
    post:
      summary: 'Saves a list of client who a class session must be booked for into session.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to reschedule installment plan in.'
          schema:
            type: string
        - name: k_pay_installment
          in: query
          description: 'Key in the installment plan.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_new:
                  type: string
                  description: 'New local date, when user should be charged.'
                dl_old:
                  type: string
                  description: 'Old local date, when user should be charged according to the initial plan.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Reschedule-InstallmentReschedule
  '/Studio/Task/View/Granular/Edit.json':
    post:
      summary: 'Update granular error task key.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_task:
                  type: string
                  description: 'Granular error task key. Primary key in the {@link \StudioTaskSql} table.'
                k_test_file:
                  type: string
                  description: 'Test file key. Primary key in {@link \ATestFileSql} table.'
                s_key:
                  type: string
                  description: 'Key of error message. Contains binary <tt>md5(text_message)</tt>.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Granular-Edit
  '/Studio/Report/Sprint/Task/Report.json':
    get:
      summary: ''
      parameters:
        - name: a_personnel
          in: query
          description: 'Personnel key.'
          schema:
            type: array
        - name: id_report
          in: query
          description: 'Report ID.'
          schema:
            type: number
            format: int32
        - name: k_sprint
          in: query
          description: 'Sprint key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: array
                        description: 'Result for render report grouped by personnel. <dl> <dt>array `a_task`</dt> <dd> List of task for report. Has next structure: <dl> <dt>array[] `a_jira`</dt> <dd> List of connected jira tasks. Has next structure: <dl> <dt>string `s_status_class`</dt> <dd>Class for style status of the task.</dd> <dt>string `text_jira_key`</dt> <dd>Text key of the jira task.</dd> <dt>string `text_status`</dt> <dd>Status of the jira task.</dd> <dt>int `i_front_end_point`</dt> <dd>Front End points.</dd> <dt>int `i_story_point`</dt> <dd>Story points.</dd> <dt>string `text_task`</dt> <dd>Title of the jira task.</dd> <dt>string `text_type`</dt> <dd>Type of the jira task.</dd> <dt>string `url_task`</dt> <dd>Url of the jira task.</dd> </dl> </dd> <dt>int `i_review`</dt> <dd>Count of review in task where personnel is author.</dd> <dt>int `id_color`</dt> <dd>Color ID. One of {@link \Studio\Task\Color\TaskColorSid} constants.</dd> <dt>string `k_task`</dt> <dd>Task key. Primary key in {@link \StudioTaskSql} table.</dd> <dt>string `s_status_file`</dt> <dd>Status file. See {@link \StudioTaskStatusSql}.<var>s_file</var> property.</dd> <dt>string `text_development_time`</dt> <dd>Development time in human format. See {@link studio_task_estimate_encode()} function.</dd> <dt>string `text_review_time`</dt> <dd>Review time in human format. See {@link studio_task_estimate_encode()} function.</dd> <dt>string `text_status`</dt> <dd>Status of the task.</dd> <dt>string `text_task`</dt> <dd>Title of the studio task.</dd> <dt>string `text_task_key`</dt> <dd>Text key of the studio task.</dd> <dt>string `url_task`</dt> <dd>Url of the studio task.</dd> </dl> </dd> <dt>array `a_total`</dt> <dd> Total by tasks. Each element has next structure: <dl> <dt>string `text_total`</dt> <dd>Total name.</dd> <dt>string `text_value`</dt> <dd>Total value.</dd> </dl> </dd> </dl>'
                      can_status_change:
                        type: boolean
                        description: 'See {@link TaskStatusApi::can()} for description.'
      tags:
        - Task-Report
  '/Studio/Report/Sprint/Task/TaskStatus.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_color:
                  type: number
                  format: int32
                  description: 'Color ID.'
                k_task:
                  type: string
                  description: 'Task key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Task-TaskStatus
  '/Core/Locale/Message/InlineEditor/Save.json':
    put:
      summary: 'Saves new translation of a translated message.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_translate:
                  type: array
                  description: 'A list of translations to save. Each element of this array contains: <dl> <dt>array `a_translate`</dt> <dd> Translations to save. Key is language ID (one of {@link \Core\Locale\Language\LocaleLanguageSid} constants), value is either translation, or <tt>null</tt> to remove translation. </dd> <dt>bool `is_js`</dt> <dd> Whether this message is originated from JavaScript code. Setting this flag leads to that this source and all translations are added to {@link \Core\Locale\Message\InlineEditor\InlineSql}. This flag is only considered if {@link \Core\Locale\Message\InlineEditor\SaveApi::FORWARD_GLOBAL} is set to <tt>true</tt>. </dd> <dt>string `text_source`</dt> <dd>Source of translated message.</dd> </dl>'
                text_instance:
                  type: string
                  description: 'Name of the instance.'
                text_login:
                  type: [string, "null"]
                  description: 'Login of the user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - InlineEditor-Save
  '/Core/Locale/Message/InlineEditor/MessageData.json':
    post:
      summary: 'Returns information about translated messages.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_source:
                  type: [array, string]
                  description: 'A list of sources of translated messages to retrieve information about. In a case there are many messages, JSON-encoded string can be passed here.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_translate:
                        type: array
                        description: 'Information about translated messages. Key is source of translated message. Value is an array where key is language ID and value is translation into given language.'
      tags:
        - InlineEditor-MessageData
  '/Core/Request/Api/Application/Origin.json':
    delete:
      summary: 'Restricts access to API for all sites, which are given in the list. Accepts a list of origin URLs (with optional API domain overrides), validates each URL, and removes the matching entries from the allowed origins for the current application, then clears the origin cache.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Origin
    get:
      summary: 'Gets list of all sites, where usage of the API is allowed for the current application. Returns the list of allowed CORS origins for the current API application, where each key is an origin URL and each value is an optional API domain used to proxy requests from that origin.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: [array, "null"]
                        description: 'A list of origins. An associative array where the key is the origin URL of the site where API calls can be made, and the value is the additional API domain used to make API requests to the WellnessLiving server. `null` if not yet initialized.'
      tags:
        - Application-Origin
    put:
      summary: 'Allows access to API for all sites, which are given in the list. Accepts a list of origin URLs (with optional API domain overrides), validates each URL and domain, inserts or updates the entries in the allowed origins for the current application, then clears the origin cache.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_list:
                  type: [array, "null"]
                  description: 'A list of origins. An associative array where the key is the origin URL of the site where API calls can be made, and the value is the additional API domain used to make API requests to the WellnessLiving server. `null` if not yet initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Origin
  '/Core/Request/Api/Application/GroupElement.json':
    delete:
      summary: 'Deletes a group.'
      parameters:
        - name: k_application_group
          in: query
          description: 'If a group is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\GroupSql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-GroupElement
    get:
      summary: 'Returns information about a group.'
      parameters:
        - name: k_application_group
          in: query
          description: 'If a group is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\GroupSql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_access:
                        type: string
                        description: 'Title of the group.'
                      text_privilege:
                        type: string
                        description: 'Title of the group.'
                      text_title:
                        type: string
                        description: 'Title of the group.'
      tags:
        - Application-GroupElement
    post:
      summary: 'Saves a group.'
      parameters:
        - name: k_application_group
          in: query
          description: 'If a group is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\GroupSql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_access:
                  type: string
                  description: 'Title of the group.'
                text_privilege:
                  type: string
                  description: 'Title of the group.'
                text_title:
                  type: string
                  description: 'Title of the group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_application_group:
                        type: [string, "null"]
                        description: 'If a group is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\GroupSql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
      tags:
        - Application-GroupElement
  '/Core/Request/Api/Application/ApplicationElement.json':
    delete:
      summary: 'Deletes an application.'
      parameters:
        - name: k_api_application
          in: query
          description: 'If an application is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\Sql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-ApplicationElement
    get:
      summary: 'Returns information about an application.'
      parameters:
        - name: k_api_application
          in: query
          description: 'If an application is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\Sql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_application_group:
                        type: [array, "null"]
                        description: 'The array of keys of access groups of current application. <tt>null</tt> when application doesn''t have access groups. Primary key in {@link \Core\Request\Api\Application\GroupSql} table.'
                      a_bot:
                        type: array
                        description: 'A bot user which is a deemed actor in all API requests made by this application. Empty array if no bot user is assigned to this application. Here, an array is used instead of two separate fields because rendering a dropdown for user search requires these two values, and in <tt>Core_Request_Api_Application_ApplicationEditor.editStructure</tt>, only one value is passed through the variable name <var>s_field</var>. <dl> <dt>string `s_login`</dt> <dd>Bot login. Copy of {@link \PassportLoginSql}.`s_login` field.</dd> <dt>string `uid_bot`</dt> <dd>UID of the bot user, primary key in {@link \PassportLoginSql}.</dd> </dl>'
                      s_group:
                        type: string
                        description: 'Which groups this user belongs to.'
                      s_id:
                        type: string
                        description: 'Application identifier to authenticate application.'
                      text_application:
                        type: string
                        description: 'Name of application.'
      tags:
        - Application-ApplicationElement
    post:
      summary: 'Saves an application.'
      parameters:
        - name: k_api_application
          in: query
          description: 'If an application is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\Sql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_application_group:
                  type: [array, "null"]
                  description: 'The array of keys of access groups of current application. <tt>null</tt> when application doesn''t have access groups. Primary key in {@link \Core\Request\Api\Application\GroupSql} table.'
                s_code:
                  type: string
                  description: 'Secret code to authorize application. When editing, it is not displayed and can only be replaced with a new one.'
                s_group:
                  type: string
                  description: 'Which groups this user belongs to.'
                s_id:
                  type: string
                  description: 'Application identifier to authenticate application.'
                text_application:
                  type: string
                  description: 'Name of application.'
                uid_bot:
                  type: string
                  description: 'UID of the bot user, primary key in {@link \PassportLoginSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_api_application:
                        type: [string, "null"]
                        description: 'If an application is edited, its key is stored here. Primary key in {@link \Core\Request\Api\Application\Sql} table. <tt>null</tt> if a new group should be created. This value is required for edit and delete operations.'
      tags:
        - Application-ApplicationElement
  '/Core/Request/Api/Application/ApplicationList.json':
    get:
      summary: 'Get applications data.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of applications. Each element contains: <dl> <dt>string `k_api_application`</dt> <dd>Application key. Primary key in {@link \Core\Request\Api\Application\Sql} table.</dd> <dt>string `s_bot`</dt> <dd>A bot user which is a deemed actor in all API requests made by this application.</dd> <dt>string `s_group`</dt> <dd>Which groups this user belongs to.</dd> <dt>string `s_id`</dt> <dd>Application identifier to authenticate application.</dd> <dt>string `text_application`</dt> <dd>Description.</dd> <dt>string `text_application_group`</dt> <dd>Application groups concatenated names.</dd> </dl>'
      tags:
        - Application-ApplicationList
  '/Core/Request/Api/Application/Group.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_application_group:
                        type: [array, "null"]
                        description: 'A group list. Where key is group key, value is the title of the group. <tt>null</tt> if is not initialized yet.'
      tags:
        - Application-Group
  '/Core/Request/Api/Application/GroupList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: [array, "null"]
                        description: 'A list of access groups. <tt>null</tt> if is not initialized yet.'
      tags:
        - Application-GroupList
  '/Core/Passport/User/Email/EmailExist.json':
    get:
      summary: 'Checks if email address exists. Used in registration and "forgot password" flows to give immediate feedback before the user submits the full form. Returns `true` if a user account with the given email already exists, so the frontend can prompt to sign in instead of registering.'
      parameters:
        - name: text_email
          in: query
          description: 'The email address.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_exist:
                        type: boolean
                        description: 'Determines whether the email address exists.'
      tags:
        - Email-EmailExist
  '/Core/Passport/Enter/Jwt/JwtToken.json':
    get:
      summary: 'Returns a jwt token that can be used to log user. Requires the user to be signed in. Generates a signed JWT token tied to the current authorization header and user ID that can be passed to other services to authenticate the user without sharing session cookies.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token:
                        type: string
                        description: 'Jwt token that allows to authenticate user.'
      tags:
        - Jwt-JwtToken
  '/Core/Passport/Login/Enter/Enter.json':
    post:
      summary: 'Signs the user in using their login and hashed password. Accepts the user login, a password hash derived using the notepad obtained from {@link NotepadApi}, and an optional remember preference. Validates credentials, enforces CAPTCHA when too many failed attempts have occurred, starts a session for the user, and returns a redirect URL if applicable.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_data:
                  type: string
                  description: 'Additional data for user authorization.'
                s_captcha:
                  type: string
                  description: 'The answer to the captcha, if needed.'
                s_login:
                  type: string
                  description: 'The user''s login.'
                s_notepad:
                  type: string
                  description: 'A copy of the notepad that was used to hash the user password. See {@link \Core\Passport\Login\Enter\EnterApi::$s_password} for details.'
                s_password:
                  type: string
                  description: 'The hash of the user password. Use `Core_Passport_Login_Enter_NotepadModel.hash()` to evaluate the password hash.'
                s_remember:
                  type: string
                  description: 'Determines whether the user login and password should be remembered, and how they should be remembered. The accepted values are: <ul> <li>`''''` Empty line (default value) if you do not want to remember anything.</li> <li>`''login''` Remember only user login.</li> <li>`''password''` Remember user login and password.</li> </ul>'
                url_return:
                  type: string
                  description: 'Url of previous page if the user was redirected to login.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'An optional URL for redirection after the user has signed in to the web application.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      url_captcha:
                        type: string
                        description: 'Optional URL to get captcha image. This field is filled in a case when a captcha code is required to sign in.'
      tags:
        - Enter-Enter
  '/Core/Passport/Login/Enter/Notepad.json':
    get:
      summary: 'Generates notepad for user sign in form. Generates a cryptographic nonce (one-time random string) and stores it in the session so that the client can hash the user''s password with it before sending it to {@link EnterApi}. This prevents replay attacks and avoids transmitting passwords in plain text.'
      parameters:
        - name: s_login
          in: query
          description: 'User login. If specified, additional information about user may be returned such as datacenter of the user. This value is only considered when {@link AmazonRegionSid::currentId()} is not empty, and {@link AmazonRegionSid::isRegionFixed()} returns `false`. In this case, regional cookie may be set, and the request may be forwarded internally to a different datacenter. `null` if additional information is not needed.'
          schema:
            type: [string, "null"]
        - name: s_type
          in: query
          description: 'The session type to store the notepad in. This will be an empty string for a temporary session. Otherwise, you can use `system` for a system session.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_region:
                        type: [number, "null"]
                        description: 'ID of the datacenter which is a preferred datacenter for the user specified in {@link NotepadApi::$s_login}. One of {@link AmazonRegionSid} constants. If ID of the datacenter returned in this property differs from the current datacenter, this means that this API request was forwarded to the datacenter returned here, and the notepad was created there. So that {@link EnterApi} must always go to the datacenter specified in this property. `null` if datacenter preference was not evaluated, or it is not known. In this case, the notepad is created in datacenter where the API request was initially sent.'
                      s_hash:
                        type: string
                        description: 'The hash type.'
                      s_notepad:
                        type: string
                        description: 'The notepad value, which is used to hash the user''s password. The password length is {@link NotepadApi::NONCE_LENGTH} characters.'
      tags:
        - Enter-Notepad
  '/Core/Passport/Login/Register/Register.json':
    post:
      summary: 'Validates the new user''s data and sends a confirmation email to complete registration. Accepts the new user''s name, email, and password, validates each field, stores the pending registration, and sends a confirmation email with a link to complete registration via {@link RegisterConfirmApi}. An optional application ID and custom confirmation URL may be provided.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'The additional data about new users. <dl> <dt>string `k_business`</dt> <dd> The key of the business for the WellnessLiving project to register the user in. The behavior may vary for different applications. If the application is connected to a given business, this property can always be empty (all clients will be registered in the connected business). If the business is set for such an application and is different from the business from the application settings, an error will occur. If the application isn''t connected to the business, any business key can be set. This doesn''t require any special privileges, as registration is a publicly available action. </dd> </dl>'
                id_mode:
                  type: [number, "null"]
                  description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants. `null` if not initialized or the api is not called in the Wellnessliving project.'
                s_application:
                  type: string
                  description: 'The application ID. This is a business specific ID required to register clients.'
                s_mail:
                  type: string
                  description: 'The new client email address.'
                s_name_first:
                  type: string
                  description: 'The new client given name.'
                s_name_last:
                  type: string
                  description: 'The new client surname.'
                s_password:
                  type: string
                  description: 'The new client password.'
                url_confirm:
                  type: string
                  description: 'The URL to the confirmation page. This link is used in a confirmation email. If empty, URL to default page is used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_confirm_config:
                        type: string
                        description: 'JSON configuration for confirmation email.'
                      url_confirm:
                        type: string
                        description: 'The URL to the confirmation page. This link is used in a confirmation email. If empty, URL to default page is used.'
      tags:
        - Register-Register
  '/Core/Passport/Login/Register/RegisterConfirm.json':
    post:
      summary: 'Confirms email of a new user and completes registration. Accepts the email, login, and verification code obtained from the confirmation link sent by {@link RegisterApi}, validates them against the pending registration record, creates the user account, and returns the new user''s UID.'
      parameters:
        - name: text_code
          in: query
          description: 'The unique registration code.'
          schema:
            type: string
        - name: text_login
          in: query
          description: 'The user''s login.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The user''s email.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'The key of the new registered user.'
      tags:
        - Register-RegisterConfirm
  '/Core/Passport/Login/Register/RegisterField.json':
    get:
      summary: 'Loads information about profile fields.'
      parameters:
        - name: s_application
          in: query
          description: 'The application ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_profile_field:
                        type: [array, "null"]
                        description: 'Information about profile fields: `null` until loaded. <dl> <dt>string `text_name_first`</dt> <dd>Title of first name field.</dd> <dt>string `text_name_last`</dt> <dd>Title of last name field.</dd> </dl>'
      tags:
        - Register-RegisterField
  '/Core/Passport/Login/SignOut/SignOut.json':
    post:
      summary: 'Signs user out. Requires the user to be signed in, fires a before-logout event allowing listeners to intercept or handle the logout, then clears the current session. Throws an error if the user is authenticated via SAML SSO, as API-based logout is not supported in that case.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - SignOut-SignOut
  '/Core/Amazon/S3/MultipartUpload/MultipartUpload.json':
    get:
      summary: 'Initiates a multipart upload. Sets public properties for the client side of the uploader.'
      parameters:
        - name: a_key
          in: query
          description: 'Dataset that should be associated with the public fields of the key object {@link MultipartUploadKeyAbstract} being created. Key - the name of the field. Value - the values for this field.'
          schema:
            type: array
        - name: cid_multipart_upload
          in: query
          description: 'CID of the uploader class.'
          schema:
            type: number
            format: int32
        - name: i_file_size
          in: query
          description: 'File size received from the client.'
          schema:
            type: [number, string]
        - name: s_content_type
          in: query
          description: 'A standard MIME type describing the format of the object data `null` if no type is given.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_part_url:
                        type: array
                        description: 'A set of links for unloading parts. Key - part number. Value - link for uploading part.'
                      s_upload_id:
                        type: string
                        description: 'Unique upload ID. This upload ID is used to associate all of the parts in the specific multipart upload. You specify this upload ID in each of your subsequent upload part requests. You also include this upload ID in the final request to either complete or abort the multipart upload request.'
      tags:
        - MultipartUpload-MultipartUpload
    post:
      summary: 'Completes unloading part of the file. Assembles a file into a single unit.'
      parameters:
        - name: a_key
          in: query
          description: 'Dataset that should be associated with the public fields of the key object {@link MultipartUploadKeyAbstract} being created. Key - the name of the field. Value - the values for this field.'
          schema:
            type: array
        - name: cid_multipart_upload
          in: query
          description: 'CID of the uploader class.'
          schema:
            type: number
            format: int32
        - name: i_file_size
          in: query
          description: 'File size received from the client.'
          schema:
            type: [number, string]
        - name: s_content_type
          in: query
          description: 'A standard MIME type describing the format of the object data `null` if no type is given.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_upload_id:
                  type: string
                  description: 'Unique upload ID. This upload ID is used to associate all of the parts in the specific multipart upload. You specify this upload ID in each of your subsequent upload part requests. You also include this upload ID in the final request to either complete or abort the multipart upload request.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - MultipartUpload-MultipartUpload
    put:
      summary: 'Aborts uploading a file.'
      parameters:
        - name: a_key
          in: query
          description: 'Dataset that should be associated with the public fields of the key object {@link MultipartUploadKeyAbstract} being created. Key - the name of the field. Value - the values for this field.'
          schema:
            type: array
        - name: cid_multipart_upload
          in: query
          description: 'CID of the uploader class.'
          schema:
            type: number
            format: int32
        - name: i_file_size
          in: query
          description: 'File size received from the client.'
          schema:
            type: [number, string]
        - name: s_content_type
          in: query
          description: 'A standard MIME type describing the format of the object data `null` if no type is given.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_upload_id:
                  type: string
                  description: 'Unique upload ID. This upload ID is used to associate all of the parts in the specific multipart upload. You specify this upload ID in each of your subsequent upload part requests. You also include this upload ID in the final request to either complete or abort the multipart upload request.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - MultipartUpload-MultipartUpload
  '/Core/Amazon/Ses/Blacklist/Blacklist.json':
    delete:
      summary: 'Removes an email address from the Amazon Ses blacklist.'
      parameters:
        - name: s_mail
          in: query
          description: 'Email address.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Blacklist-Blacklist
    get:
      summary: 'Checks if an email address is on the Amazon Ses blacklist.'
      parameters:
        - name: s_mail
          in: query
          description: 'Email address.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_mail_blacklisted:
                        type: boolean
                        description: 'Indicates if the email address is blacklisted.'
      tags:
        - Blacklist-Blacklist
  '/Wl/Visit/Note/Edit/Edit.json':
    delete:
      summary: 'Deletes note.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit_note
          in: query
          description: 'Visit note key, primary key in {@link \Wl\Visit\Note\Sql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
    get:
      summary: 'Gets notes data.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit ID, primary key in {@link \RsVisitSql}. Empty value means not set.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_data_note_additional:
                        type: array
                        description: 'Content additional field of visit note. Format is same as <tt>a_visit_note</tt> field.'
                      a_info:
                        type: array
                        description: 'Contains general information.'
                      a_visit_note:
                        type: array
                        description: 'List of notes: <dl> <dt>string `text_field`</dt> <dd>A title of field of a visit note.</dd> <dt>string `text_field_name`</dt> <dd>A field element name.</dd> <dt>string `text_id`</dt> <dd>A field element id.</dd> <dt>string `text_key`</dt> <dd>Not translated a title of field of a visit note.</dd> <dt>string `text_text`</dt> <dd>A content of field of a visit note.</dd> </dl>'
                      can_edit:
                        type: boolean
                        description: 'Can the current user edit the note.'
                      id_note:
                        type: number
                        format: int32
                        description: 'Note type, one of {@link NoteSid} constants.'
                      is_new:
                        type: boolean
                        description: '<tt>true</tt> - if visit notes are new, <tt>false</tt> - otherwise.'
      tags:
        - Edit-Edit
    post:
      summary: 'Saves notes data.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit ID, primary key in {@link \RsVisitSql}. Empty value means not set.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Uncompressed note content received from form.'
                id_note:
                  type: number
                  format: int32
                  description: 'Note type, one of {@link NoteSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
  '/Wl/Catalog/View/Image/Image.json':
    get:
      summary: 'Retrieves an information about product images.'
      parameters:
        - name: id_sale
          in: query
          description: 'ID of sale category. One of {@link \RsSaleSid}.'
          schema:
            type: number
            format: int32
        - name: k_id
          in: query
          description: 'ID of the sale item.'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'Shop product option ID.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Contains a list of images for the current product.'
      tags:
        - Image-Image
  '/Wl/Catalog/StaffApp/CatalogCommission/CatalogCommission.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid_customer
          in: query
          description: 'Customer login key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_commission_default:
                        type: array
                        description: 'Default staff commission for current client. Includes: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Primary key of {@link \RsStaffSql} table.</dd> <dt>string `k_staff_pay`</dt> <dd>Staff pay key. Primary key of {@link \RsStaffPaySql} table.</dd> <dt>string `uid_staff`</dt> <dd>User key of staff. Primary key of {@link \PassportLoginSql} table.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'List of staff with commission. Includes: <dl> <dt>array `a_commission`</dt> <dd> List of staff commissions: <dl> <dt>string `f_rate`</dt> <dd>Rate of staff commission.</dd> <dt>int `id_commission_type`</dt> <dd>One of {@link \RsCommissionTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd><tt>true</tt> - if it is default commission for the staff,<tt>false</tt> - otherwise.</dd> <dt>string `html_commission`</dt> <dd>Title of the commission.</dd> <dt>string `k_staff_pay`</dt> <dd>Staff pay key. Primary key of {@link \RsStaffPaySql} table.</dd> </dl> </dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Primary key of {@link \RsStaffSql} table. It''s use `uid` instead of this field.</dd> <dt>string `html_name`</dt> <dd>Name of staff.</dd> <dt>string `uid`</dt> <dd>User key. Primary key of {@link \PassportLoginSql} table.</dd> </dl>'
      tags:
        - CatalogCommission-CatalogCommission
  '/Wl/Catalog/StaffApp/CatalogList/CatalogList.json':
    get:
      summary: 'Returns all sale items available in the business for staff, optionally filtered by location and visit. Used in the staff app to populate the add-item picker during checkout. Returns the products and services the staff member is allowed to sell at the current location. When a visit key is provided, the list is filtered to items that are relevant to that visit.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get categories for.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key. This can affect the list of displayed products.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'The visit key to pay for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_shop_product:
                        type: array
                        description: 'Products in the online store category. Every element has the following fields: <dl> <dt>string[] `a_location`</dt> <dd> The list of location keys where the current sale item is available. Each value is a location key. <i>Primary key in the {@link \RsLocationSql} table.</i> </dd> <dt>string[] `a_login_type`</dt> <dd> The list of login types available to purchase the sale item. Empty list means that the sale item is available to all login types. </dd> <dt>string[] `a_member_group`</dt> <dd> The list of member groups available to purchase the sale item. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i> Empty list means that the sale item is available to all member groups. </dd> <dt>string[] `a_shop_category`</dt> <dd>A list of online store category keys. <i>Primary keys in {@link \RsShopCategorySql} table.</i></dd> <dt>int `id_program`</dt> <dd> The program ID, set for promotions. One of the {@link \RsProgramSid} constants. <p>If `id_program` is {@link \RsProgramSid::INSURANCE_MEMBERSHIP}, then use:</p> <ul> <li>{@link \Wl\Insurance\Catalog\ProgramListApi} to obtain a list of active programs.</li> <li>{@link \Wl\Insurance\Enrollment\Field\EnrollmentFieldListApi} to get and validate fields for a given program.</li> <li>{@link \Wl\Catalog\Payment\PaymentApi} for a program purchase.</li> </ul> </dd> <dt>int `id_restriction`</dt> <dd>The restriction ID. One of the {@link \Wl\Shop\Product\PurchaseRestrictionSid} constants.</dd> <dt>int `id_sale`</dt> <dd>The sale category ID. One of the {@link \RsSaleSid} constants.</dd> <dt>bool `is_online_sell`</dt> <dd>Determines whether the sale item can be purchased by the client.</dd> <dt>bool `is_visit`</dt> <dd> This will be <tt>true</tt> if this Purchase Option is suitable to pay for the visit {@link \Wl\Catalog\StaffApp\CatalogList\CatalogListApi::$k_visit}. Otherwise, this will be <tt>false</tt>. If {@link \Wl\Catalog\StaffApp\CatalogList\CatalogListApi::$k_visit} is empty, this will always be `false`. </dd> <dt>string `k_id`</dt> <dd>The sale item ID.</dd> <dt>string `text_title`</dt> <dd>The category title.</dd> <dt>string `url_catalog`</dt> <dd>Direct link to catalog view page of this purchase item.</dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'If `true`, the current user is able to add the purchased item to the account.'
      tags:
        - CatalogList-CatalogList
  '/Wl/Catalog/StaffApp/CatalogCart/CatalogCart.json':
    get:
      summary: 'Calculates amount of cart. Used in the staff app checkout flow to show a live price summary before the staff member processes a client''s payment. Returns the full breakdown so the staff member can confirm the total with the client before charging.'
      parameters:
        - name: a_item
          in: query
          description: 'The list of cart items with the next structure: <dl> <dt>array `a_config`</dt> <dd> The list of purchase item additional options: <dl> <dt>array `a_event_list`</dt> <dd> List of tuition components: <dl> <dt>array `a_tax`</dt> <dd> Calculated tax amounts for the event enrollment component. Key is a primary key in {@link \RsTaxSql}, value is the calculated tax amount (bcmath string). Populated in the response; not required in the request. </dd> <dt>array `a_tax_fee`</dt> <dd> Calculated tax amounts for the fee component. Key is a primary key in {@link \RsTaxSql}, value is the calculated tax amount (bcmath string). Populated in the response; not required in the request. </dd> <dt>string `k_class`</dt> <dd>Key of the event class within the tuition which should be granted to the client.</dd> <dt>string `m_fee`</dt> <dd>Additional fee amount for the component (bcmath string). `''0.00''` if no additional fee.</dd> <dt>string `m_price`</dt> <dd> Override price of the event component within the tuition. `null` if price should be taken from the class setup. </dd> <dt>string `uid`</dt> <dd>Key of the client for which component should be granted.</dd> </dl> </dd> <dt>string `f_price`</dt> <dd>The custom price.</dd> <dt>string `dt_prorate`</dt> <dd>The prorate date. This should be passed when `is_prorate`=`true`.</dd> <dt>string `dt_start`</dt> <dd>The promotion start date.</dd> <dt>bool `is_prorate`</dt> <dd>Determines whether to prorate the first payment.</dd> <dt>bool `is_prorate_fix`</dt> <dd>Determines if the client should pay for the first period now.</dd> <dt>bool `is_prorate_only`</dt> <dd>Determines whether the selected option ''pay prorate amount only'' should only include the prorate amount.</dd> <dt>string `m_custom`</dt> <dd>The custom price of the gift card.</dd> <dt>string `dt_send_local`</dt> <dd>The date when the gift card email must be sent.</dd> <dt>bool `is_mail`</dt> <dd>If `true`, the gift card will be sent via email. Otherwise, `false` if the gift card will be printed.</dd> <dt>string `s_mail`</dt> <dd>The recipient''s email.</dd> <dt>string `s_message`</dt> <dd>The message.</dd> <dt>string `s_recipient`</dt> <dd>The recipient''s name.</dd> <dt>string `s_sender`</dt> <dd>The sender''s name.</dd> <dt>string `m_prorate_custom`</dt> <dd>The amount of money for the prorate period. This should only be passed in the case of manual entry.</dd> <dt>string `k_coupon`</dt> <dd>The coupon key.</dd> <dt>string `k_coupon_amount`</dt> <dd>The coupon amount key.</dd> </dl> </dd> <dt>array `a_tax_custom`</dt> <dd> Information about taxes. If not passed, no custom taxes have been applied to the sale item. If a record is present, the tax is custom using the next structure: <dl> <dt>string `f_tax`</dt> <dd>The tax amount.</dd> <dt>string `k_tax`</dt> <dd>The tax keys. <i>Primary key in {@link \RsTaxSql} table.</i></dd> </dl> </dd> <dt>float `f_discount_percent`</dt> <dd>The discount percentage, applied to the current item.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The sale item key.</dd> <dt>string `k_shop_product_option`</dt> <dd> The store product option key. This will be `null` if the sale item has no options. <i>Primary key in {@link \RsShopProductOptionSql} table.</i> </dd> <dt>string `m_discount_fix`</dt> <dd>The fixed price discount, applied to the current item.</dd> <dt>string `m_price_custom`</dt> <dd>The custom price of the sale item. If not passed, no custom price has been applied to the sale item.</dd> </dl>'
          schema:
            type: array
        - name: f_discount_percent
          in: query
          description: 'The discount as a percentage.'
          schema:
            type: number
            format: float
        - name: is_check_cart_item
          in: query
          description: 'If `true`, every item is checked at the cart. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. This field isn''t used directly, but described for correct JavaScript auto-generation.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The business location key.'
          schema:
            type: string
        - name: m_discount_flat
          in: query
          description: 'The manual discount amount in dollars, excluding tax. Staff members can set this amount when making a sale.'
          schema:
            type: string
        - name: m_tip
          in: query
          description: 'The amount of tips.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code. This will be `null` if not set yet.'
          schema:
            type: [string, "null"]
        - name: uid_current
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
        - name: uid_customer
          in: query
          description: 'The key of the user who performed the purchase.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount_code:
                        type: array
                        description: 'A list of available discount codes with the next structure: <dl> <dt>bool `is_select`</dt> <dd>`true` if this code is selected currently, `false` otherwise.</dd> <dt>string `k_discount_code`</dt> <dd>Discount code key. <i>Primary key from the {@link \Wl\Discount\Code\Sql} table.</i></dd> <dt>string `text_discount_code`</dt> <dd>Value of the code that can be used to get discount.</dd> <dt>string `text_title`</dt> <dd>Name of the code.</dd> </dl>'
                      a_item:
                        type: array
                        description: 'The list of cart items with the next structure: <dl> <dt>array `a_config`</dt> <dd> The list of purchase item additional options: <dl> <dt>array `a_event_list`</dt> <dd> List of tuition components: <dl> <dt>array `a_tax`</dt> <dd> Calculated tax amounts for the event enrollment component. Key is a primary key in {@link \RsTaxSql}, value is the calculated tax amount (bcmath string). Populated in the response; not required in the request. </dd> <dt>array `a_tax_fee`</dt> <dd> Calculated tax amounts for the fee component. Key is a primary key in {@link \RsTaxSql}, value is the calculated tax amount (bcmath string). Populated in the response; not required in the request. </dd> <dt>string `k_class`</dt> <dd>Key of the event class within the tuition which should be granted to the client.</dd> <dt>string `m_fee`</dt> <dd>Additional fee amount for the component (bcmath string). `''0.00''` if no additional fee.</dd> <dt>string `m_price`</dt> <dd> Override price of the event component within the tuition. `null` if price should be taken from the class setup. </dd> <dt>string `uid`</dt> <dd>Key of the client for which component should be granted.</dd> </dl> </dd> <dt>string `f_price`</dt> <dd>The custom price.</dd> <dt>string `dt_prorate`</dt> <dd>The prorate date. This should be passed when `is_prorate`=`true`.</dd> <dt>string `dt_start`</dt> <dd>The promotion start date.</dd> <dt>bool `is_prorate`</dt> <dd>Determines whether to prorate the first payment.</dd> <dt>bool `is_prorate_fix`</dt> <dd>Determines if the client should pay for the first period now.</dd> <dt>bool `is_prorate_only`</dt> <dd>Determines whether the selected option ''pay prorate amount only'' should only include the prorate amount.</dd> <dt>string `m_custom`</dt> <dd>The custom price of the gift card.</dd> <dt>string `dt_send_local`</dt> <dd>The date when the gift card email must be sent.</dd> <dt>bool `is_mail`</dt> <dd>If `true`, the gift card will be sent via email. Otherwise, `false` if the gift card will be printed.</dd> <dt>string `s_mail`</dt> <dd>The recipient''s email.</dd> <dt>string `s_message`</dt> <dd>The message.</dd> <dt>string `s_recipient`</dt> <dd>The recipient''s name.</dd> <dt>string `s_sender`</dt> <dd>The sender''s name.</dd> <dt>string `m_prorate_custom`</dt> <dd>The amount of money for the prorate period. This should only be passed in the case of manual entry.</dd> <dt>string `k_coupon`</dt> <dd>The coupon key.</dd> <dt>string `k_coupon_amount`</dt> <dd>The coupon amount key.</dd> </dl> </dd> <dt>array `a_tax_custom`</dt> <dd> Information about taxes. If not passed, no custom taxes have been applied to the sale item. If a record is present, the tax is custom using the next structure: <dl> <dt>string `f_tax`</dt> <dd>The tax amount.</dd> <dt>string `k_tax`</dt> <dd>The tax keys. <i>Primary key in {@link \RsTaxSql} table.</i></dd> </dl> </dd> <dt>float `f_discount_percent`</dt> <dd>The discount percentage, applied to the current item.</dd> <dt>int `i_quantity`</dt> <dd>The quantity of sale items.</dd> <dt>int `id_sale`</dt> <dd>The sale item type. One of the {@link \RsSaleSid} constants.</dd> <dt>string `k_id`</dt> <dd>The sale item key.</dd> <dt>string `k_shop_product_option`</dt> <dd> The store product option key. This will be `null` if the sale item has no options. <i>Primary key in {@link \RsShopProductOptionSql} table.</i> </dd> <dt>string `m_discount_fix`</dt> <dd>The fixed price discount, applied to the current item.</dd> <dt>string `m_price_custom`</dt> <dd>The custom price of the sale item. If not passed, no custom price has been applied to the sale item.</dd> </dl>'
                      is_commission:
                        type: boolean
                        description: 'Determines whether the business applied a commission at checkout.'
                      is_discount_code_mode_select:
                        type: boolean
                        description: 'Determines, how staff sees discount codes in Store. `true` as select field, `false` as input field.'
                      is_receipt_note:
                        type: boolean
                        description: 'Determines whether to display custom receipt notes at checkout.'
                      m_discount:
                        type: string
                        description: 'The discount amount in dollars, excluding tax.'
                      m_discount_total:
                        type: string
                        description: 'The discount amount applied to the cart''s total amount, including taxes.'
                      m_subtotal:
                        type: string
                        description: 'The subtotal amount.'
                      m_tax:
                        type: string
                        description: 'The amount of tax.'
                      m_tip_purchase:
                        type: string
                        description: 'The amount of appointment tips.'
                      m_total:
                        type: string
                        description: 'The cart''s total amount.'
                      text_error_code:
                        type: [string, "null"]
                        description: 'The discount code''s error code. `null` if no error.'
                      text_error_message:
                        type: [string, "null"]
                        description: 'The discount code''s error message. `null` if no error.'
                      text_receipt_note:
                        type: string
                        description: 'The custom receipt note''s text.'
      tags:
        - CatalogCart-CatalogCart
  '/Wl/Catalog/StaffApp/CatalogCartEdit/EditView.json':
    get:
      summary: 'Calculates price information about sale item.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration information. Empty array means not set.'
          schema:
            type: array
        - name: a_tax
          in: query
          description: 'List of manual set taxes. Empty array means not set.'
          schema:
            type: array
        - name: i_quantity
          in: query
          description: 'Quantity of items. Empty value means not set.'
          schema:
            type: number
            format: int32
        - name: id_sale
          in: query
          description: 'ID of sale category. One of {@link \RsSaleSid}. Empty value means not set.'
          schema:
            type: number
            format: int32
        - name: k_id
          in: query
          description: 'ID of the sale item. Empty value means not set.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'Shop product option ID. Empty value means not set.'
          schema:
            type: string
        - name: m_price
          in: query
          description: 'Price amount. Empty value means not set.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Current user ID. Empty value means not set.'
          schema:
            type: string
        - name: uid_customer
          in: query
          description: 'Client user ID. Empty string or <tt>false</tt> means ''walk-in'' option'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tax_data:
                        type: array
                        description: 'Contains information about taxes. Structure of this array is described in {@link \RsTax::$a_tax}.'
                      m_discount:
                        type: string
                        description: 'Discount value of the sale item.'
                      m_prorate:
                        type: string
                        description: 'Prorate amount.'
                      m_subtotal:
                        type: string
                        description: 'Amount of sale item without taxes.'
                      m_tax:
                        type: string
                        description: 'Amount of tax.'
                      m_total:
                        type: string
                        description: 'Amount of sale item including taxes and quantity.'
      tags:
        - CatalogCartEdit-EditView
  '/Wl/Catalog/StaffApp/CatalogView/CatalogView.json':
    get:
      summary: 'Calculates price information about sale item. Used in the staff app checkout to show the real-time price of a single line item as the staff member configures it (adjusting quantity, custom price, or proration). Returns the full tax breakdown so the staff member sees exactly what the client will be charged.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration information about the item, which can specify prorated amounts. If set, these values will change the values returned by the endpoint call. <dl> <dt>float `f_promote`</dt> <dd>The amount to prorate the item by.</dd> <dt>bool `is_prorate`</dt> <dd>If <tt>true</tt>, this item is prorated.</dd> <dt>string `m_prorate_custom`</dt> <dd>The custom prorate amount.</dd> </dl>'
          schema:
            type: array
        - name: a_tax
          in: query
          description: 'Contains information about edited taxes. This will be `null` if not set yet or default taxes are used. The key is the tax identifier, where the value is: <dl> <dt>string `k_tax`</dt> <dd>The tax ID.</dd> <dt>string `m_tax_custom`</dt> <dd>The amount of custom tax.</dd> </dl>'
          schema:
            type: array
        - name: i_quantity
          in: query
          description: 'The quantity of items.'
          schema:
            type: number
            format: int32
        - name: id_sale
          in: query
          description: 'The ID of the sale category. One of the {@link \RsSaleSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The business key. This will be `null` for the system business.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The key of the sale item. This can be retrieved with the {@link CatalogListApi} endpoint (see the <var>a_shop_product</var> parameter).'
          schema:
            type: string
        - name: k_shop_product_option
          in: query
          description: 'The product option key.'
          schema:
            type: string
        - name: m_price
          in: query
          description: 'The custom price of the sale item.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The ID of the user who performed the actions.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tax_data:
                        type: array
                        description: 'Contains information about calculated taxes. <dl> <dt>float `f_tax`</dt> <dd>The calculated tax amount applied by this rule.</dd> <dt>string `f_tax_discount`</dt> <dd>The tax amount after applying all discounts.</dd> <dt>string `f_tax_discount_login`</dt> <dd>The tax amount after applying the client type discount only.</dd> <dt>float `f_value`</dt> <dd>The tax rate. Its meaning depends on `id_tax`.</dd> <dt>int `id_tax`</dt> <dd>The tax type. One of {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key. <i>Primary key in the {@link \RsTaxSql} table.</i></dd> <dt>string `s_tax`</dt> <dd>The tax name.</dd> </dl>'
                      m_prorate:
                        type: string
                        description: 'The prorated amount.'
                      m_subtotal:
                        type: string
                        description: 'The amount of the sale item, excluding taxes.'
                      m_tax:
                        type: string
                        description: 'The calculated amount of tax.'
                      m_total:
                        type: string
                        description: 'The calculated amount of the sale item, including taxes.'
      tags:
        - CatalogView-CatalogView
  '/Wl/Catalog/StaffApp/CatalogCoupon/CatalogCoupon.json':
    get:
      summary: 'Performs validation of the coupon added to the cart.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_coupon
          in: query
          description: 'Coupon ID. Primary key in {@link \RsCouponSql}'
          schema:
            type: string
        - name: k_coupon_amount
          in: query
          description: 'Coupon amount ID. Primary key in {@link \Wl\Coupon\AmountSql}'
          schema:
            type: string
        - name: s_code
          in: query
          description: 'Custom coupon code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CatalogCoupon-CatalogCoupon
  '/Wl/Catalog/Coupon/Payment/Payment.json':
    post:
      summary: 'Allows to pay gift card for the client.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_coupon:
                  type: [array, "null"]
                  description: 'The list of coupon data. <tt>null</tt> if not set yet. <dl> <dt>string `dt_send`</dt> <dd>Send date.</dd> <dt>bool `is_mail`</dt> <dd>Determines that coupon will be sent by email.</dd> <dt>string `k_business`</dt> <dd>Business key.</dd> <dt>string `k_coupon`</dt> <dd>Coupon ID.</dd> <dt>string `k_coupon_amount`</dt> <dd>Coupon amount key. Primary key in {@link \Wl\Coupon\AmountSql} table.</dd> <dt>string `m_custom`</dt> <dd>Custom amount for gift card..</dd> <dt>string `s_image`</dt> <dd>Image string ID.</dd> <dt>string `s_mail`</dt> <dd>Recipient email.</dd> <dt>string `s_message`</dt> <dd>Coupon message.</dd> <dt>string `s_recipient`</dt> <dd>Recipient name.</dd> <dt>string `s_sender`</dt> <dd>Sender name.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                id_mode:
                  type: number
                  format: int32
                  description: 'Key of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. <tt>null</tt> if not set yet.'
                k_location:
                  type: [string, "null"]
                  description: 'Location key. <tt>null</tt> if not set yet.'
                text_discount_code:
                  type: string
                  description: 'Discount code value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_purchase:
                        type: [string, "null"]
                        description: 'ID of purchase that was created during payment.'
      tags:
        - Payment-Payment
  '/Wl/Catalog/Coupon/Payment/Information.json':
    get:
      summary: 'Retrieves amount for the current coupon.'
      parameters:
        - name: k_business
          in: query
          description: 'Business in which the purchase is made.'
          schema:
            type: string
        - name: k_coupon
          in: query
          description: 'ID of the coupon, primary key in {@link \RsCouponSql}.'
          schema:
            type: string
        - name: k_coupon_amount
          in: query
          description: 'ID of the coupon amount, primary key in {@link \Wl\Coupon\AmountSql}.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location primary key in {@link \RsLocationSql} table.'
          schema:
            type: string
        - name: m_custom
          in: query
          description: 'Custom amount for gift card.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'Discount code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      m_subtotal:
                        type: string
                        description: 'Total amount without tax.'
                      m_tax:
                        type: string
                        description: 'Amount of tax.'
                      m_total:
                        type: string
                        description: 'Total amount.'
      tags:
        - Payment-Information
  '/Wl/Classes/Period/Modify/Modify.json':
    get:
      summary: 'Gets data for step in wizard. Used by the class modification wizard (edit, cancel, reschedule) to load the data for a specific wizard step. Each step has a different data shape driven by `id_step`; the response feeds directly into the step''s form.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: dt_start
          in: query
          description: 'The start date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: id_class_modify_action
          in: query
          description: 'The class modify step type. One of the {@link \RsClassModifyActionSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_class_modify_mode
          in: query
          description: 'The modify mode type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_step
          in: query
          description: 'The class modify action type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_back
          in: query
          description: 'The step direction. `true` - step forward. `false` - step back. After each step, this variable sets to the default position. By default, the step direction is forward.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be empty if not set yet.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique hash string. <i> This hash string is a hash of the {@link \RsClassModify} object. It needs to work with the same {@link \RsClassModify} object on each step. </i>'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_get:
                        type: array
                        description: 'Step information, depending on the steps.'
      tags:
        - Modify-Modify
    post:
      summary: 'Makes step in wizard. Used by the class modification wizard to submit a completed step and advance to the next. The wizard tracks state server-side; this call validates the step input, persists it, and returns the next step the UI should render.'
      parameters:
        - name: dt_end
          in: query
          description: 'The end date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: dt_start
          in: query
          description: 'The start date, returned in MySQL format and the local time.'
          schema:
            type: string
        - name: id_class_modify_action
          in: query
          description: 'The class modify step type. One of the {@link \RsClassModifyActionSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_class_modify_mode
          in: query
          description: 'The modify mode type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_step
          in: query
          description: 'The class modify action type. One of the {@link \RsClassModifyModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_back
          in: query
          description: 'The step direction. `true` - step forward. `false` - step back. After each step, this variable sets to the default position. By default, the step direction is forward.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key. This will be empty if not set yet.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique hash string. <i> This hash string is a hash of the {@link \RsClassModify} object. It needs to work with the same {@link \RsClassModify} object on each step. </i>'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_set:
                  type: array
                  description: 'The list of data to save. Data depends on current {@link ModifyApi::$id_step}: <dl> <dt>bool `is_forward`</dt> <dd>Modify all future sessions. Used in {@link \RsClassModifyStepSid::PERIOD}.</dd> <dt>string[] `a_period`</dt> <dd>Selected session keys (<i>{@link \RsClassPeriodSql}</i>). Used in {@link \RsClassModifyStepSid::ITEM}.</dd> <dt>string[] `a_edit`</dt> <dd> Edited fields flags. Used in {@link \RsClassModifyStepSid::FORM}. Keys are field names (e.g. `a_staff`, `dt_time`); each value is `''1''` to indicate the field was changed. </dd> <dt>array[] `a_staff`</dt> <dd> Selected instructors. Used in {@link \RsClassModifyStepSid::FORM}: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. <i>Primary key in {@link \RsStaffSql}.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql}.</i> Optional.</dd> <dt>string `k_staff_pay`</dt> <dd>Staff pay rate ID. <i>Primary key in {@link \RsStaffPaySql}.</i></dd> </dl> </dd> <dt>bool `is_inform_mail`</dt> <dd>Send email notification to clients. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_inform_sms`</dt> <dd>Send SMS notification to clients. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_inform_mail_staff`</dt> <dd>Send email notification to staff. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_inform_sms_staff`</dt> <dd>Send SMS notification to staff. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `is_cancel_resource`</dt> <dd>Cancel resources for modified classes. Used in {@link \RsClassModifyStepSid::CLIENT}.</dd> <dt>bool `show_canceled_class`</dt> <dd>Show canceled classes in the result list. Used in {@link \RsClassModifyStepSid::CONFIRM}.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_step:
                        type: number
                        format: int32
                        description: 'The class modify action type. One of the {@link \RsClassModifyModeSid} constants.'
                      is_back:
                        type: [boolean, "null"]
                        description: 'The step direction. `true` - step forward. `false` - step back. After each step, this variable sets to the default position. By default, the step direction is forward.'
                      k_class:
                        type: [string, "null"]
                        description: 'The class ID.'
                      s_id:
                        type: string
                        description: 'The unique hash string. <i> This hash string is a hash of the {@link \RsClassModify} object. It needs to work with the same {@link \RsClassModify} object on each step. </i>'
      tags:
        - Modify-Modify
  '/Wl/Classes/Period/Info/ClassPeriod.json':
    get:
      summary: 'Retrieves information about class session.'
      parameters:
        - name: dtu_session
          in: query
          description: 'Datetime of the session.'
          schema:
            type: string
        - name: id_class_tab
          in: query
          description: 'ID of book now tab. One of {@link TabSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of a business to show information for. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Key of a class period to show information for. Primary key in {@link \RsClassPeriodSql}.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'Primary key of book now tab in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_action:
                        type: array
                        description: 'Information for available action with class session for current user: <dl> <dt>string `text_restriction_message`</dt> <dd>Any first found restriction message. Empty string if no restriction exists.</dd> <dt>bool `is_age_restricted`</dt> <dd>Whether the class cannot be booked due to age restrictions.</dd> <dt>bool `is_class_bookable`</dt> <dd> Whether the class can be bookable online. It does not take into account neither restriction of age nor restriction of prerequisites. </dd> <dt>bool `is_class_cancel`</dt> <dd>Whether class is cancelled.</dd> <dt>bool `is_class_full`</dt> <dd>Whether there are no free spots in the class.</dd> <dt>bool `is_class_past`</dt> <dd>Whether current class session was in the past.</dd> <dt>bool `is_disable_by_restriction`</dt> <dd> Whether booking button must be disabled due to the class restrictions: - Online Booking Disabled; - Online Booking Allowed only for certain Client Types; - Online Booking Allowed only for certain Member Groups; - Age Restriction; - Prerequisites Restriction. It takes into account whether client is allowed to book for their relatives without switching profile. But allowing family bookings does not affect the "Online Booking Disabled" restriction. </dd> <dt>bool `is_visit_book`</dt> <dd>Whether current class was booked by current client.</dd> <dt>bool `is_wait_list`</dt> <dd>Whether current user can take place in wait list only.</dd> <dt>string `url_visit_book`</dt> <dd>Direct link to start booking on Wellnessliving site.</dd> <dt>string `url_visit_cancel`</dt> <dd>Direct link to cancel booked session on Wellnessliving site.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'Staff who conduct session. Every element has keys: <dl> <dt>bool `is_staff_change`</dt> <dd>Whether the staff member is a substitute for this session.</dd> <dt>string `uid_staff`</dt> <dd>UID of the staff member. Primary key from {@link \PassportLoginSql} table.</dd> <dt>string `text_name`</dt> <dd>Full name of the staff.</dd> <dt>string `text_position`</dt> <dd>Staff`s job title.</dd> <dt>string `url_image`</dt> <dd>Staff`s logo.</dd> </dl>'
                      html_description:
                        type: string
                        description: 'Description of the class.'
                      html_special:
                        type: string
                        description: 'Class special instructions.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Class capacity. `null` if the business set capacity to be hide.'
                      i_free:
                        type: [number, "null"]
                        description: 'Class session free spot. `null` if the business set capacity to be hide.'
                      is_event:
                        type: boolean
                        description: 'Whether this class is event or not.'
                      is_virtual:
                        type: boolean
                        description: 'Whether this class is virtual or not.'
                      text_day:
                        type: string
                        description: 'Date of the session in user-friendly format.'
                      text_duration:
                        type: string
                        description: 'Class session duration.'
                      text_room:
                        type: string
                        description: 'Class session room number.'
                      text_time:
                        type: string
                        description: 'Class session time in user-friendly format.'
                      text_timezone:
                        type: string
                        description: 'Class session timezone abbreviation. Empty if business disable ability to change timezone in the client profile.'
                      text_title:
                        type: string
                        description: 'Class title.'
                      url_image:
                        type: string
                        description: 'Url link to the class image.'
      tags:
        - Info-ClassPeriod
  '/Wl/Discount/Code/Edit/DiscountCodeEdit.json':
    get:
      summary: 'Gets information about discount. Used to load the discount code editor form. Returns the full configuration of an existing discount code so the staff member can review or modify it: value, date range, usage limit, which items it applies to, client type restrictions, and whether it is currently active.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_discount_code
          in: query
          description: 'Key of the discount code. Empty, if this is creation of a new code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_component:
                        type: array
                        description: 'List of components that are affected by this discount code. Each element is an array containing: <dl> <dt>int `id_purchase_item`</dt> <dd>Type ID of the purchase item. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd> Primary key of the component. <p>Special Notes:</p> <ul> <li>Zero means that all elements of this type are available.</li> <li>For `id_purchase_item`={@link \RsPurchaseItemSid::CLASS_PERIOD} we store <i>{@link \RsClassSql}</i>.`k_class` in the `k_id` field.</li> </ul> </dd> <dt>string `text_title`</dt> <dd>Title of the component.</dd> </dl>'
                      a_login_type:
                        type: array
                        description: 'List of client types.'
                      a_member_group:
                        type: array
                        description: 'List of client groups.'
                      dl_end:
                        type: [string, "null"]
                        description: 'Expiration date. <tt>null</tt> if discount code is never expires.'
                      dl_start:
                        type: string
                        description: 'Activation date.'
                      f_value:
                        type: string
                        description: 'Value of the percentage or fixed discount.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'Count of calendar periods from <var>id_duration</var> before expiration.'
                      i_limit:
                        type: number
                        format: int32
                        description: 'Maximum count of usage. Zero means unlimited usage.'
                      id_duration:
                        type: number
                        format: int32
                        description: 'Calendar periods for discount code duration from {@link \ADurationSid}. Zero if discount code is never expires.'
                      id_duration_type:
                        type: number
                        format: int32
                        description: 'Type of the discount code duration from {@link \RsDurationTypeSid}.'
                      is_active:
                        type: boolean
                        description: '<tt>true</tt> means discount code is active. <tt>false</tt> means discount code is inactive.'
                      is_auto_renew:
                        type: boolean
                        description: '<tt>true</tt> means membership will auto-renew at discounted rate. <tt>false</tt> means membership will auto-renew at full rate.'
                      is_coupon:
                        type: boolean
                        description: '<tt>true</tt> means discount code can reduce price of the gift cards. <tt>false</tt> means discount code cannot reduce price of the gift cards. If {@link DiscountCodeEditApi::$is_purchase_all} is <tt>true</tt> this fields does not matter.'
                      is_first_payment:
                        type: boolean
                        description: '<tt>true</tt> means discount code can be applied to first membership payment only. <tt>false</tt> means discount code can be applied to all membership payments.'
                      is_login_type:
                        type: boolean
                        description: '<tt>true</tt> means that only selected client types or groups can use this discount code. <tt>false</tt> means that all client types and groups can use this discount code.'
                      is_online:
                        type: boolean
                        description: '<tt>true</tt> if clients can use this discount code in online store, <tt>false</tt> if discount code can be used only in point of sale.'
                      is_percent:
                        type: boolean
                        description: '<tt>true</tt> means discount value is percent from the purchase amount. <tt>false</tt> means discount value is fixed sum from the purchase amount.'
                      is_purchase_all:
                        type: boolean
                        description: '<tt>true</tt> means discount can be applied to all products and purchase options. <tt>false</tt> means discount can be applied only to the selected product and purchase options.'
                      s_component:
                        type: string
                        description: 'List of product and purchase options that are effected with this discount code. Each element has the next format: <var>id_purchase_item</var>:<var>k_id</var>, where <tt>id_purchase_item</tt> - one of {@link \RsPurchaseItemSid}, <tt>k_id</tt> is key of certain purchase item. <i>Processed by function {@link \Core\Tool\UrlEncode\UrlEncode::encode()}.</i>'
                      text_code:
                        type: string
                        description: 'Discount code that can be used during purchase.'
                      text_description:
                        type: string
                        description: 'Description of the discount code.'
                      text_title:
                        type: string
                        description: 'Name of the discount code.'
      tags:
        - Edit-DiscountCodeEdit
    post:
      summary: 'Saves information about discount code or create new. Used to create a new discount code or update an existing one from the backend editor. Pass no discount code key to create; pass an existing key to update. Returns the key of the saved code.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_discount_code
          in: query
          description: 'Key of the discount code. Empty, if this is creation of a new code.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_type:
                  type: array
                  description: 'List of client types.'
                a_member_group:
                  type: array
                  description: 'List of client groups.'
                dl_end:
                  type: [string, "null"]
                  description: 'Expiration date. <tt>null</tt> if discount code is never expires.'
                dl_start:
                  type: string
                  description: 'Activation date.'
                f_value:
                  type: string
                  description: 'Value of the percentage or fixed discount.'
                i_duration:
                  type: number
                  format: int32
                  description: 'Count of calendar periods from <var>id_duration</var> before expiration.'
                i_limit:
                  type: number
                  format: int32
                  description: 'Maximum count of usage. Zero means unlimited usage.'
                id_duration:
                  type: number
                  format: int32
                  description: 'Calendar periods for discount code duration from {@link \ADurationSid}. Zero if discount code is never expires.'
                id_duration_type:
                  type: number
                  format: int32
                  description: 'Type of the discount code duration from {@link \RsDurationTypeSid}.'
                is_active:
                  type: boolean
                  description: '<tt>true</tt> means discount code is active. <tt>false</tt> means discount code is inactive.'
                is_auto_renew:
                  type: boolean
                  description: '<tt>true</tt> means membership will auto-renew at discounted rate. <tt>false</tt> means membership will auto-renew at full rate.'
                is_coupon:
                  type: boolean
                  description: '<tt>true</tt> means discount code can reduce price of the gift cards. <tt>false</tt> means discount code cannot reduce price of the gift cards. If {@link DiscountCodeEditApi::$is_purchase_all} is <tt>true</tt> this fields does not matter.'
                is_first_payment:
                  type: boolean
                  description: '<tt>true</tt> means discount code can be applied to first membership payment only. <tt>false</tt> means discount code can be applied to all membership payments.'
                is_login_type:
                  type: boolean
                  description: '<tt>true</tt> means that only selected client types or groups can use this discount code. <tt>false</tt> means that all client types and groups can use this discount code.'
                is_online:
                  type: boolean
                  description: '<tt>true</tt> if clients can use this discount code in online store, <tt>false</tt> if discount code can be used only in point of sale.'
                is_percent:
                  type: boolean
                  description: '<tt>true</tt> means discount value is percent from the purchase amount. <tt>false</tt> means discount value is fixed sum from the purchase amount.'
                is_purchase_all:
                  type: boolean
                  description: '<tt>true</tt> means discount can be applied to all products and purchase options. <tt>false</tt> means discount can be applied only to the selected product and purchase options.'
                s_component:
                  type: string
                  description: 'List of product and purchase options that are effected with this discount code. Each element has the next format: <var>id_purchase_item</var>:<var>k_id</var>, where <tt>id_purchase_item</tt> - one of {@link \RsPurchaseItemSid}, <tt>k_id</tt> is key of certain purchase item. <i>Processed by function {@link \Core\Tool\UrlEncode\UrlEncode::encode()}.</i>'
                text_code:
                  type: string
                  description: 'Discount code that can be used during purchase.'
                text_description:
                  type: string
                  description: 'Description of the discount code.'
                text_title:
                  type: string
                  description: 'Name of the discount code.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_discount_code:
                        type: string
                        description: 'Key of the discount code. Empty, if this is creation of a new code.'
      tags:
        - Edit-DiscountCodeEdit
    put:
      summary: 'Duplicates an existing discount code. Used in the backend when a staff member wants to create a similar discount code without re-entering all settings manually. Creates an exact copy with a new unique code string.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_discount_code
          in: query
          description: 'Key of the discount code. Empty, if this is creation of a new code.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_discount_code:
                        type: string
                        description: 'Key of the discount code. Empty, if this is creation of a new code.'
      tags:
        - Edit-DiscountCodeEdit
  '/Wl/Event/Book/EventView/Element.json':
    get:
      summary: 'Retrieves information about event item. Used to render the event detail page or event card in the booking flow. Returns everything needed to display the event to a prospective client: schedule, pricing, booking availability, assigned staff with photos, and available installment payment options.'
      parameters:
        - name: dl_book_available_end
          in: query
          description: 'The last available date for booking. If this is set and {@link ElementApi::$dl_book_available_start} is a set list of sessions available for booking, {@link ElementApi::$a_book_available} should match given date range.'
          schema:
            type: [string, "null"]
        - name: dl_book_available_start
          in: query
          description: 'The first available date for booking. If this is set and {@link ElementApi::$dl_book_available_end} is a set list of sessions available for booking {@link ElementApi::$a_book_available} should match given date range.'
          schema:
            type: [string, "null"]
        - name: dtu_session
          in: query
          description: 'Datetime of the session. Is not `null` only if we need to get information for an event with a specific class period and datetime. If this is `null`, then the {@link ElementApi::$k_class_period} should be `null` too.'
          schema:
            type: [string, "null"]
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_staff_image_height
          in: query
          description: 'Staff image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: i_staff_image_width
          in: query
          description: 'Staff image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified, the returned image will have the default thumbnail size.'
          schema:
            type: number
            format: int32
        - name: is_schedule_group
          in: query
          description: '`true` if schedule should be grouped by location time and staff, `false` if all schedule elements should be independent.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of a business to which the requested event(s) belong.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of a class period to show information for. Is not `null` only if we need to get information for an event with a specific class period and datetime. If this is `null`, then the {@link ElementApi::$dtu_session} should be `null` too.'
          schema:
            type: [string, "null"]
        - name: k_event
          in: query
          description: 'The event key. You can specify {@link ElementApi::$s_event} instead to get information for a large number of events.'
          schema:
            type: string
        - name: s_event
          in: query
          description: 'A list of event keys serialized with JSON. Specify instead of {@link ElementApi::$k_event} to get information for a large number of events.'
          schema:
            type: string
        - name: show_unpublished
          in: query
          description: '`true` to show schedule, which is not published yet. `false` to show only published schedule.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_age_restrictions:
                        type: array
                        description: 'Displays information about age restrictions for this event. An empty array if there are no age restrictions. <dl> <dt>int|null `i_age_from`</dt> <dd> The minimum age for participation in the event. `null` if there''s no minimum age set or information isn''t available. </dd> <dt>int|null `i_age_to`</dt> <dd> The age limit for participation in the event. `null` if there''s no age limit set or information isn''t available. </dd> <dt>bool `is_age_public`</dt> <dd> `true` if age restrictions are public and available, `false` if they''re hidden. When restrictions are hidden and the current user isn''t a staff member, the age range will be empty. </dd> </dl>'
                      a_book_available:
                        type: array
                        description: 'Retrieves information about an event item. Received only if {@link ElementApi::$k_event} has been specified. In this case, other fields aren''t receivers. <dl> <dt>string `dt_date`</dt> <dd>Date/time when the session starts. In UTC.</dd> <dt>string `k_class_period`</dt> <dd>Class session primary keys. <i>Primary keys in table {@link \RsClassPeriodSql}.</i></dd> </dl>'
                      a_business_policy:
                        type: array
                        description: 'Business policies connected to clients and bookings. Contains the same structure as {@link BusinessConfigApi::$a_business_policy}. If the event has custom overrides, those policies are used; otherwise, the default business policies are returned. <dl> <dt>int[] `a_payment_reattempt_not_decline_reason`</dt> <dd> List of not allowed decline reasons to payment reattempt. Each element is one of {@link \Thoth\WlPay\PayExceptionSid} constants. </dd> <dt>bool[] `a_wait_service`</dt> <dd>Keys are list of IDs from {@link \Wl\Service\ServiceSid}, and values are flags whether wait list is allowed.</dd> <dt>int `i_book_before`</dt> <dd>Minimum hours|days|months before class should be booked.</dd> <dt>int `i_book_future`</dt> <dd>Maximum hours|days|months after class can be booked.</dd> <dt>int `i_cancel`</dt> <dd>Minimum hours|days|months before class should be canceled without penalty.</dd> <dt>int `i_promote`</dt> <dd>Minimum hours|days|months before class should be promoted from wait list.</dd> <dt>int `i_promote_fastest_response`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation required should be sent to all clients on the wait list at the same time up to the cut off time. </dd> <dt>int `i_promote_require_confirm`</dt> <dd> Minimum hours|days|months the notifications which are sent for client confirmation must confirm their a promote from the waiting list to the active list. </dd> <dt>int `i_reattempt_count`</dt> <dd>Number of failed auto-payments reattempts.</dd> <dt>int `id_book_before`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_book_future`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_cancel`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_fastest_response`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>int `id_promote_require_confirm`</dt> <dd>Hours|days|months from {@link \ADurationSid}.</dd> <dt>bool `is_book_inside_active_pay_period`</dt> <dd> if `true` - clients with purchase options are only allowed to book sessions within their current paid period, `false` - during purchase option''s duration. </dd> <dt>bool `is_disable_promotion`</dt> <dd> 1 if a client''s automatic payment fails, their account should not be debited and their purchase option becomes inactive, 0 - otherwise. Default 0. </dd> <dt>bool `is_enable_payment_penalty`</dt> <dd>Whether to charge penalty after final auto-payment attempt.</dd> <dt>bool `is_enable_payment_reattempt`</dt> <dd>Whether to reattempt failed auto-payments.</dd> <dt>bool `is_enable_staff_ip_restriction`</dt> <dd>Whether to restrict which IP addresses staff can login from.</dd> <dt>bool `is_prevent_booking`</dt> <dd>1 if booking for a client with negative balance is disabled, 0 - otherwise. Default 0.</dd> <dt>bool `is_staff_restrict`</dt> <dd>If true, client can not choose provider while appointment wizard.</dd> <dt>bool `is_wait`</dt> <dd>Enable\disable wait list.</dd> <dt>string `k_currency`</dt> <dd>Currency from rs.currency table.</dd> <dt>string `k_timezone`</dt> <dd>Timezone from get.timezone table.</dd> <dt>string `m_payment_penalty`</dt> <dd>Penalty amount to charge after final auto-payment attempt.</dd> <dt>string `url_custom`</dt> <dd>Custom Url of a business</dd> </dl>'
                      a_class_logo:
                        type: array
                        description: 'The logo of event. <dl> <dt>int `i_height`</dt> <dd>Is returned only if staff has a photo. Image height.</dd> <dt>int `i_width`</dt> <dd>Is returned only if staff has a photo. Image width.</dd> <dt>int `id_gender`</dt> <dd>Is returned only if staff does not have a photo. ID of staff gender. One of {@link \AGenderSid} constants.</dd> <dt>string `url_logo`</dt> <dd>Is returned only if staff has a photo. URL to image.</dd> </dl>'
                      a_class_tab:
                        type: array
                        description: 'The list of keys from class tab.'
                      a_event:
                        type: array
                        description: 'Information for a large number of events. Received only if {@link ElementApi::$s_event} has been specified. In this case, other fields are not populated. Key is the event class key. <i>Primary key in the {@link \RsClassSql} table.</i> <i>Value has the same structure as the return value of {@link ElementApi::_get()}.</i> <dl> <dt>array[] `a_book_available`</dt> <dd> List of sessions available for booking. See {@link \Wl\Event\Book\EventView\ElementApi::$a_book_available}. <dl> <dt>string `dt_date`</dt> <dd>Date/time when the session starts. In UTC.</dd> <dt>string `k_class_period`</dt> <dd>Class session primary keys. <i>Primary keys in table {@link \RsClassPeriodSql}.</i></dd> </dl> </dd> <dt>array `a_class_logo`</dt> <dd> Image of event. See {@link \Wl\Event\Book\EventView\ElementApi::$a_class_logo}. <dl> <dt>int `i_height`</dt> <dd>Is returned only if staff has a photo. Image height.</dd> <dt>int `i_width`</dt> <dd>Is returned only if staff has a photo. Image width.</dd> <dt>int `id_gender`</dt> <dd> Is returned only if staff does not have a photo. ID of staff gender. One of {@link \AGenderSid} constants. </dd> <dt>string `url_logo`</dt> <dd>Is returned only if staff has a photo. URL to image.</dd> </dl> </dd> <dt>string[] `a_class_tab`</dt> <dd>Class tab keys. See {@link \Wl\Event\Book\EventView\ElementApi::$a_class_tab}.</dd> <dt>array[] `a_installment_template`</dt> <dd> List of installment plans. See {@link \Wl\Event\Book\EventView\ElementApi::$a_installment_template}. <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by `id_period` between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency Key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql}.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl> </dd> <dt>array[] `a_schedule`</dt> <dd> Schedule of event sessions. See {@link \Wl\Event\Book\EventView\ElementApi::$a_schedule}. <dl> <dt>true[] `a_day`</dt> <dd> Days of the week when the session occurs. Keys are weekday numbers (1 = Monday, 7 = Sunday), values are always `true`. </dd> <dt>array `a_repeat`</dt> <dd> Repeat periodicity instructions. <dl> <dt>int `i_repeat`</dt> <dd>Count of the periods which specified in `id_repeat`.</dd> <dt>int `id_repeat`</dt> <dd>Measuring unit of `i_repeat` (week, month, year). One of {@link \ADurationSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> A list of staff members who conduct the session. Every element has the following next keys: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `s_name`</dt> <dd>The staff member name.</dd> <dt>string `s_surname`</dt> <dd>The first letter of staff member''s surname.</dd> <dt>string `uid_staff`</dt> <dd>The user key of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>string `dt_end`</dt> <dd>The end date of the session. The local date without time.</dd> <dt>string `dt_start`</dt> <dd>The start date of the session. The local date without time.</dd> <dt>bool `hide_location`</dt> <dd> `true` if the location should be hidden in the event details. Hide if the event is virtual or if the business only has one location. `false` otherwise. </dd> <dt>int `i_capacity`</dt> <dd>The class capacity.</dd> <dt>int `i_duration`</dt> <dd>The duration of the class in seconds.</dd> <dt>bool `is_virtual`</dt> <dd> This will be `true` if the session is not held in person but offered remotely. It will be `false` otherwise. </dd> <dt>string `f_price`</dt> <dd>The price of the session, if it can be purchased separately.</dd> <dt>string `k_class_period`</dt> <dd>The key of the class period.</dd> <dt>string `k_location`</dt> <dd>The key of the location where the session is held.</dd> <dt>string `k_resource_location`</dt> <dd>Structured off-site location data used by the calendar attachment.</dd> <dt>string `s_location`</dt> <dd>The location title.</dd> <dt>string `s_time`</dt> <dd> The time when session occurred. A textual representation of the start and end time of a session. Example: `10:00 am - 11:00 am` </dd> <dt>string `s_timezone`</dt> <dd>The name of the timezone in which the session is held.</dd> <dt>string `text_room`</dt> <dd>The room of the event.</dd> </dl> </dd> <dt>string[] `a_shop_category`</dt> <dd>IDs of online store category. <i>Primary keys in {@link \RsShopCategorySql} table.</i></dd> <dt>array `a_staff_logo`</dt> <dd> Photos of staff. See {@link \Wl\Event\Book\EventView\ElementApi::$a_staff_logo}. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `uid`</dt> <dd>Key of the user. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> <dt>string `url_logo`</dt> <dd>URL to image.</dd> </dl> </dd> <dt>string `dt_book_date`</dt> <dd>Date/time of first event session.</dd> <dt>string `dt_early`</dt> <dd>Early date of event purchase.</dd> <dt>string `dt_end`</dt> <dd>End date of the event instance.</dd> <dt>string `dt_start`</dt> <dd>Date of first event session.</dd> <dt>string `html_end`</dt> <dd>Html End date of the event instance.</dd> <dt>string `html_start`</dt> <dd>Html Date of first event session.</dd> <dt>bool `hide_application`</dt> <dd>Whether event will be hidden in the White Label mobile application.</dd> <dt>string `html_special`</dt> <dd>Special instruction for event.</dd> <dt>int `i_session`</dt> <dd>Session count in event.</dd> <dt>int `i_session_remain`</dt> <dd>Remaining session count in event.</dd> <dt>bool `is_availability_checked`</dt> <dd>Whether event availability was checked.</dd> <dt>bool `is_book`</dt> <dd>Whether event is booked already</dd> <dt>bool `is_full`</dt> <dd>`true` if there are no free spots in the event and booking is available only into wait list.</dd> <dt>bool `is_makeup`</dt> <dd>`true` if the selected session can be a make up session; `false` otherwise.</dd> <dt>bool `is_past`</dt> <dd>`true` if the event session has already started or ended and is not available to book.</dd> <dt>bool `is_policy_custom`</dt> <dd> `true` {@link \Wl\Event\Book\EventView\ElementApi::$a_business_policy} contains the custom policies from the event; `false` otherwise. </dd> <dt>bool `is_virtual`</dt> <dd>`true` if event is virtual; `false` otherwise.</dd> <dt>string `k_book_class_period`</dt> <dd>Key of first event session. <i>Primary key in table {@link \RsClassPeriodSql}.</i></dd> <dt>string `m_price`</dt> <dd>Price of the event session.</dd> <dt>string `m_price_total`</dt> <dd>Price of the full event.</dd> <dt>string `m_price_total_early`</dt> <dd>Price of the full event, should be used as full price while `dt_early` is actual.</dd> <dt>string `s_deny_reason`</dt> <dd>Reason of booking restriction. For example if exception has been thrown.</dd> <dt>string `s_title`</dt> <dd>Event title.</dd> <dt>string `xml_description`</dt> <dd>Description of event. Ready to put into browser.</dd> </dl>'
                      a_installment_template:
                        type: array
                        description: 'A list of installment plans. Each element has the following next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by `id_period` between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency Key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql}.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl>'
                      a_schedule:
                        type: array
                        description: 'A list of event sessions. Every element has the following next keys: <dl> <dt>true[] `a_day`</dt> <dd> Days of the week when the session occurs. Keys are weekday numbers (1 = Monday, 7 = Sunday), values are always `true`. </dd> <dt>array `a_repeat`</dt> <dd> Repeat periodicity instructions. <dl> <dt>int `i_repeat`</dt> <dd>Count of the periods which specified in `id_repeat`.</dd> <dt>int `id_repeat`</dt> <dd>Measuring unit of `i_repeat` (week, month, year). One of {@link \ADurationSid} constants.</dd> </dl> </dd> <dt>array[] `a_staff`</dt> <dd> A list of staff members who conduct the session. Every element has the following next keys: <dl> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. Returned only for applications from allow-list.</dd> <dt>string `s_name`</dt> <dd>The staff member name.</dd> <dt>string `s_surname`</dt> <dd>The first letter of staff member''s surname.</dd> <dt>string `uid_staff`</dt> <dd>The user key of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. <i>Primary keys in {@link \RsLocationSql} table.</i></dd> <dt>string `dt_end`</dt> <dd>The end date of the session. The local date without time.</dd> <dt>string `dt_start`</dt> <dd>The start date of the session. The local date without time.</dd> <dt>bool `hide_location`</dt> <dd> `true` if the location should be hidden in the event details. Hide if the event is virtual or if the business only has one location. `false` otherwise. </dd> <dt>int `i_capacity`</dt> <dd>The class capacity.</dd> <dt>int `i_duration`</dt> <dd>The duration of the class in seconds.</dd> <dt>bool `is_virtual`</dt> <dd>This will be `true` if the session is not held in person but offered remotely. It will be `false` otherwise.</dd> <dt>string `f_price`</dt> <dd>The price of the session, if it can be purchased separately.</dd> <dt>string `k_class_period`</dt> <dd>The key of the class period.</dd> <dt>string `k_location`</dt> <dd>The key of the location where the session is held.</dd> <dt>string `k_resource_location`</dt> <dd>Structured off-site location data used by the calendar attachment.</dd> <dt>string `s_location`</dt> <dd>The location title.</dd> <dt>string `s_time`</dt> <dd> The time when session occurred. A textual representation of the start and end time of a session. Example: `10:00 am - 11:00 am` </dd> <dt>string `s_timezone`</dt> <dd>The name of the timezone in which the session is held.</dd> <dt>string `text_room`</dt> <dd>The room of the event.</dd> </dl>'
                      a_shop_category:
                        type: array
                        description: 'IDs of online store category.'
                      a_staff_logo:
                        type: array
                        description: 'Photos of staff members. Keys are the keys of staff members. The values are the following: <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `uid`</dt> <dd>Key of the user. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> <dt>string `url_logo`</dt> <dd>URL to image.</dd> </dl>'
                      a_timezone_info:
                        type: array
                        description: 'Timezone information for all timezones used in the event schedule. Key is the timezone key. Primary key in the `a_geo_timezone` table. Value contains timezone information from the geo timezone registry: <dl> <dt>int `i_shift`</dt> <dd>UTC offset in hours for this timezone.</dd> <dt>string `s_file`</dt> <dd>Timezone identifier string (e.g. `America/New_York`).</dd> <dt>string|null `text_abbr`</dt> <dd>Timezone abbreviation (e.g. `EST`). `null` if not set.</dd> </dl>'
                      a_visits_required:
                        type: array
                        description: 'A list of classes and events that clients should attend before this one. <dl> <dt>int `i_count`</dt> <dd>The number of visits required.</dd> <dt>int `i_has`</dt> <dd>The number of visits the client has already attended.</dd> <dt>bool `is_event`</dt> <dd>`true` if this is an event, `false` if this is a class.</dd> <dt>string `k_class`</dt> <dd>The key of the class or event.</dd> <dt>string `text_title`</dt> <dd>The name of the class or event.</dd> </dl>'
                      dt_book_date:
                        type: [string, "null"]
                        description: 'That date that should be used to go to the booking wizard.'
                      dt_early:
                        type: [string, "null"]
                        description: 'The early registration date of the event.'
                      dt_end:
                        type: string
                        description: 'The end date of the event.'
                      dt_start:
                        type: string
                        description: 'The start date of the event.'
                      hide_application:
                        type: boolean
                        description: 'Whether event will be hidden in the White Label mobile application. `true` means that event will not be displayed, `false` otherwise.'
                      html_description:
                        type: [string, "null"]
                        description: 'The description of the event.'
                      html_special:
                        type: string
                        description: 'Special instruction for the event.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Class capacity. `null` if no set.'
                      i_session:
                        type: number
                        format: int32
                        description: 'The session count.'
                      i_session_remain:
                        type: number
                        format: int32
                        description: 'The remaining session count.'
                      id_pay_require:
                        type: number
                        format: int32
                        description: 'The purchase rule ID. One of the {@link RequirePaySid} constants.'
                      id_pay_require_option:
                        type: [number, "null"]
                        description: 'Default required value for {@link RequirePaySid::ADVANCE} payment mode, one of {@link RequirePaySid::ONLINE} or {@link RequirePaySid::VISIT}. `null` means default value not selected.'
                      id_virtual_provider:
                        type: [number, "null"]
                        description: 'The virtual provider ID. One of the {@link VirtualProviderSid} constants. `null` if an in-person event.'
                      is_age_restrict:
                        type: boolean
                        description: 'Whether the event is age restricted. `true` if the event is age restricted, `false` if the event is not.'
                      is_availability_checked:
                        type: boolean
                        description: '`true` if the event availability was checked; `false` if the event has too many sessions, and calculating the availability of each one takes a lot of time.'
                      is_book:
                        type: boolean
                        description: '`true` if the event is already booked; `false` if the event isn''t booked.'
                      is_bookable:
                        type: boolean
                        description: '`true` If the event is bookable; `false` if the event isn''t bookable.'
                      is_full:
                        type: boolean
                        description: '`true` if there are no free spots in the event; booking is available only into the wait list. `false` otherwise.'
                      is_makeup:
                        type: boolean
                        description: '`true` if the selected session can be a make up session. `false` otherwise.'
                      is_past:
                        type: boolean
                        description: '`true` if the selected session has already started and do not available to book. `false` otherwise.'
                      is_policy_custom:
                        type: boolean
                        description: '`true` if the {@link ElementApi::$a_business_policy} contains the custom policies from the event. `false` otherwise.'
                      is_promotion_only:
                        type: boolean
                        description: '`true` if the event can be paid with a Purchase Option only. `false` if full event purchases or single session purchases are permitted.'
                      is_prorate:
                        type: boolean
                        description: '`true` if clients, when they book event, which has been started, can pay only for the sessions in the future. `false` if clients, when they book event, have to pay for the entire event and the number of lefts in the future sessions does not matter.'
                      is_single_session_buy:
                        type: boolean
                        description: 'Determines whether this event allows paying for a single session.'
                      is_virtual:
                        type: boolean
                        description: '`true` if event is virtual; `false` otherwise.'
                      k_book_class_period:
                        type: string
                        description: 'The ID of the session which should be used to go to the booking wizard.'
                      m_price:
                        type: string
                        description: 'The price of a single session of the event.'
                      m_price_total:
                        type: string
                        description: 'The price of the all sessions of the event.'
                      m_price_total_early:
                        type: [string, "null"]
                        description: 'Price of the full event should be used as full price while {@link ElementApi::$dt_early} is actual.'
                      s_deny_reason:
                        type: string
                        description: 'The reason why the event can''t be booked. Empty if {@link ElementApi::$k_book_class_period} isn''t empty.'
                      s_title:
                        type: [string, "null"]
                        description: 'The title of the event.'
                      show_special_instructions:
                        type: boolean
                        description: '`true` if special instructions can be public, `false` if they should be shown only to the clients, who booked the class.'
                      text_end:
                        type: string
                        description: 'End date of the event in user-friendly format.'
                      text_start:
                        type: string
                        description: 'Start date of the event in user-friendly format.'
                      url_book:
                        type: [string, "null"]
                        description: 'Link to the start of the booking wizard to book the closed session from this event or the entire event. Can be `null` if there is no available for booking sessions.'
                      xml_description:
                        type: [string, "null"]
                        description: 'The description of the event.'
      tags:
        - EventView-Element
  '/Wl/Event/Book/EventList/List.json':
    get:
      summary: 'Retrieves a list of events. Used to populate the events tab on the client-facing booking page. Returns all events offered in the business (or filtered to a specific tab), together with availability flags so the UI can show which events still have open spots.'
      parameters:
        - name: id_status
          in: query
          description: 'Defines how the event availability flag filter should be applied. One of {@link \AFlagSid} constants. * {@link \AFlagSid::ON} to show only available events. * {@link \AFlagSid::OFF} to show only unavailable events. * {@link \AFlagSid::ALL} to show all events (available and unavailable).'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'The key of the category tab. If empty, select only elements with not specified book tab.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_event:
                        type: array
                        description: 'A list of event identifiers. The order of items in this array matches the order in which elements should be displayed.'
                      a_event_available:
                        type: array
                        description: 'Event availability map. Unavailable events are those that cannot be booked, but they can be shown to the client (for example, under the "not available" filter). Key is the event class key. <i>Primary key in the {@link \RsClassSql} table.</i> Value is `true` if the event is available for booking, `false` if it is unavailable.'
                      is_virtual_service:
                        type: boolean
                        description: '<tt>true</tt> if exist at least one virtual event by specific {@link ListApi::$k_business} and {@link ListApi::$k_class_tab}, <tt>false</tt> otherwise.'
      tags:
        - EventList-List
  '/Wl/Import/Migration/Cookie/CookieGeneral.json':
    get:
      summary: 'Returns cookies from an authorized user with Mindbody.'
      parameters:
        - name: k_studio
          in: query
          description: 'Key of the business from Mindbody. Note: This key is used to go to the backend page of the business in Mindbody. Key of the business can be a negative number.'
          schema:
            type: number
            format: int32
        - name: text_login
          in: query
          description: 'User login for authorization for Mindbody.'
          schema:
            type: string
        - name: text_password
          in: query
          description: 'User password for authorization for Mindbody.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_cookie:
                        type: string
                        description: 'Cookie key.'
      tags:
        - Cookie-CookieGeneral
  '/Wl/Import/Wizard/Step/StepTask.json':
    get:
      summary: 'Returns count of the rows that are left to be imported via additional tasks.'
      parameters:
        - name: id_import_wizard
          in: query
          description: 'Import wizard step. One of {@link \Wl\Import\Wizard\ImportWizardSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business where import goes. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_left:
                        type: number
                        format: int32
                        description: 'Count of rows that are left to be imported.'
      tags:
        - Step-StepTask
    post:
      summary: 'Cancels active rows that are left to be imported.'
      parameters:
        - name: id_import_wizard
          in: query
          description: 'Import wizard step. One of {@link \Wl\Import\Wizard\ImportWizardSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business where import goes. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Step-StepTask
  '/Wl/Import/Custom/Upload/CustomUpload.json':
    get:
      summary: 'Returns count of rows that are left to be imported.'
      parameters:
        - name: id_import_custom
          in: query
          description: 'Type of custom import. One of {@link CustomSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_left:
                        type: number
                        format: int32
                        description: 'Count of the rows that are left to be imported.'
      tags:
        - Upload-CustomUpload
  '/Wl/Import/Custom/Upload/CustomUploadValidate.json':
    post:
      summary: 'Returns count of rows that are left to be imported.'
      parameters:
        - name: id_import_custom
          in: query
          description: 'Type of custom import. One of {@link \Wl\Import\Custom\CustomSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_mapping:
                  type: array
                  description: 'Same as {@link \Wl\Import\Custom\Upload\CustomUploadController::$a_field_mapping}.'
                id_date:
                  type: number
                  format: int32
                  description: 'Same as {@link \Wl\Import\Custom\Upload\CustomUploadController::$id_date}.'
                s_file_mapping:
                  type: string
                  description: 'Same as {@link \Wl\Import\Custom\Upload\CustomUploadController::$s_file_mapping}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_invalid:
                        type: number
                        format: int32
                        description: 'Count of the rows that cannot be imported due to validation errors.'
                      i_total:
                        type: number
                        format: int32
                        description: 'Count of the rows in the  file.'
      tags:
        - Upload-CustomUploadValidate
  '/Wl/Reward/Action/CategoryList/CategoryList.json':
    get:
      summary: 'Retrieves all reward action categories for business specified in {@link \Wl\Reward\Action\CategoryList\CategoryListApi::$k_business}. Returns the list of reward action categories including category type ID, database key, and title for the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category:
                        type: array
                        description: '<dl> <dt>int|null `id_reward_action_category`</dt> <dd> ID of reward actions category type. One of {@link \RsRewardActionCategorySid} constants. <tt>null</tt> for custom reward actions categories. </dd> <dt>string `k_reward_action_category`</dt> <dd>ID of reward actions category in database. <i>Primary key in {@link \RsRewardActionCategorySql} table.</i></dd> <dt>string `s_title`</dt> <dd>Title of the reward action.</dd> </dl>'
      tags:
        - CategoryList-CategoryList
  '/Wl/Reward/Score/Current/Current.json':
    get:
      summary: 'Retrieves information about current score in wellnessliving points. Returns the user''s current reward point balance, their rank in the first available leaderboard, and the leaderboard title for the specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'ID of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_board_rank:
                        type: number
                        format: int32
                        description: 'Current user''s rank in the first leaderboard.'
                      i_score:
                        type: number
                        format: int32
                        description: 'Current score in wellnessliving points.'
                      s_board_title:
                        type: string
                        description: 'Title of the first leaderboard.'
      tags:
        - Current-Current
  '/Wl/Reward/Board/BoardList/List.json':
    get:
      summary: 'Retrieves all reward boards for business specified in {@link \Wl\Reward\Board\BoardList\ListApi::$k_business}. Returns the list of reward boards available to the given user in the business, including board key and title, filtered to exclude boards the user is not eligible to view.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to retrieve information about.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reward_board:
                        type: [array, "null"]
                        description: 'A list of reward boards. `null` if not loaded. Each element: <dl> <dt>string `k_reward_board`</dt> <dd>Reward board key. `0` for the all-time system board. <i>Primary key in {@link \RsRewardBoardSql}.</i></dd> <dt>string `s_title`</dt> <dd>Board title.</dd> </dl>'
      tags:
        - BoardList-List
  '/Wl/Schedule/Page/AppointmentView/AppointmentView.json':
    get:
      summary: 'Retrieves information about appointment.'
      parameters:
        - name: k_appointment
          in: query
          description: 'Appointment key. Primary key in {@link \RsAppointmentSql} table.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'Appointment information: <dl> <dt>array `a_date_end`</dt> <dd>End date of the appointment.</dd> <dt>array `a_date_start`</dt> <dd>Start date of the appointment.</dd> <dt>string `html_description`</dt> <dd>Description of the appointment.</dd> <dt>string `html_special`</dt> <dd>Special instructions.</dd> <dt>bool `is_virtual`</dt> <dd><tt>true</tt> if the appointment is virtual, <tt>false</tt> otherwise.</dd> <dt>string `k_location`</dt> <dd>Location key. Primary key in the {@link \RsLocationSql} table.</dd> <dt>string `text_service`</dt> <dd>Appointment title.</dd> <dt>string `text_staff_name`</dt> <dd>Staff name.</dd> <dt>string `text_timezone`</dt> <dd>Timezone name.</dd> <dt>string `uid_staff`</dt> <dd>Staff uid. Primary key in the {@link \PassportLoginSql} table.</dd> </dl>'
                      a_asset:
                        type: [array, "null"]
                        description: 'Asset list data.'
                      a_location:
                        type: array
                        description: 'Location information: <dl> <dt>string `text_address`</dt> <dd>First address line of location.</dd> <dt>string `text_address2`</dt> <dd>Second address line of location.</dd> <dt>string `text_city`</dt> <dd>Name of city.</dd> <dt>string `text_phone`</dt> <dd>Phone number.</dd> <dt>string `text_postal`</dt> <dd>Postal code.</dd> <dt>string `text_region`</dt> <dd>Name of region.</dd> <dt>string `text_title`</dt> <dd>Title of the location.</dd> </dl>'
                      a_service_logo:
                        type: array
                        description: 'Service logo data. Result of {@link \RsServiceLogo::data()}.'
      tags:
        - AppointmentView-AppointmentView
  '/Wl/Schedule/Page/AssetLayoutView/Asset.json':
    get:
      summary: 'Gets data about selected and reserved assets within an asset category which are needed for a service or class to show on a layout.'
      parameters:
        - name: dtu_date
          in: query
          description: 'Date and time when class or appointment occurs. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: i_index
          in: query
          description: 'Index of booked asset. `null` if not initialized yet.'
          schema:
            type: [number, "null"]
        - name: k_class_period
          in: query
          description: 'Class period key. Primary key in {@link \RsClassPeriodSql} table. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location key. Primary key in {@link \RsLocationSql} table. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'Asset key. Primary key in {@link \RsResourceSql} table. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_service
          in: query
          description: 'Service key. Primary key in {@link \RsServiceSql} table. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_type:
                        type: array
                        description: 'Asset category data of selected asset. Has next structure: <dl> <dt>array[] `a_resource_list`</dt> <dd> List of available assets. Every element has next keys: <dl> <dt>array `a_image`</dt> <dd>Asset image data. See {@link \RsResourceImage::data()} for details.</dd> <dt>int `i_index`</dt> <dd>Number of asset. Actual for assets with quantity more than `1`.</dd> <dt>bool `is_current`</dt> <dd>`true` means that this asset is selected by client, `false` - otherwise.</dd> <dt>string `k_resource`</dt> <dd>Key of asset. Primary key {@link \RsResourceSql} table.</dd> <dt>string `s_resource`</dt> <dd>Title of asset.</dd> </dl> </dd> <dt>bool `is_client_select`</dt> <dd>`true` - client selected resource from current group; `false` otherwise.</dd> <dt>bool `is_share`</dt> <dd> `true` - assets of this category do not belong to certain users but belong to session in general; `false` - assets belong to certain users. </dd> <dt>string `k_resource_layout`</dt> <dd>Key of asset layout. Primary key in {@link \Wl\Resource\Layout\Sql} table.</dd> <dt>string `k_resource_type`</dt> <dd>Key of asset category. Primary key in {@link \RsResourceTypeSql} table.</dd> <dt>string `s_resource_type`</dt> <dd>Title of asset category.</dd> </dl>'
      tags:
        - AssetLayoutView-Asset
  '/Wl/Schedule/Config/Availability/AvailabilitySchedule.json':
    get:
      summary: ''
      parameters:
        - name: dl_end
          in: query
          description: 'Date to get schedule to.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Date to get schedule from.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business for which list of staff members must be checked. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'List of staff members with services they have during the given date range.'
      tags:
        - Availability-AvailabilitySchedule
  '/Wl/Schedule/Config/Availability/AvailabilityAvailable.json':
    get:
      summary: ''
      parameters:
        - name: dl_end
          in: query
          description: 'Date to get schedule to.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Date to get schedule from.'
          schema:
            type: string
        - name: json_service
          in: query
          description: 'List of selected on the schedule appointment types.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business for which list of staff members must be checked. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'List of staff members with services they have during the given date range.'
                      is_timeout:
                        type: boolean
                        description: '`true` if we were not able to calculate staff members for all services in a reasonable time and had to stop calculation. `false` if everthing was calculated.'
      tags:
        - Availability-AvailabilityAvailable
  '/Wl/Schedule/ScheduleList/Backend/ColumnList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User''s UID key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_column:
                        type: array
                        description: 'Column list. Each element has next structure: <dl> <dt>bool `is_fix`</dt> <dd>Whether column is fixed. Should not be hidden.</dd> <dt>bool `is_hide`</dt> <dd>Whether column is hidden.</dd> <dt>string `s_name`</dt> <dd>Column name.</dd> <dt>string `text_title`</dt> <dd>Column title.</dd> </dl>'
      tags:
        - Backend-ColumnList
    post:
      summary: 'Saves columns position.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User''s UID key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_column_set:
                  type: array
                  description: 'Column list. Each element has next structure described here {@link \Wl\Schedule\ScheduleList\Backend\ColumnListApi::$a_column}'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Backend-ColumnList
  '/Wl/Schedule/ScheduleList/StaffApp/ScheduleList.json':
    get:
      summary: 'Gets schedule of business {@link ScheduleListApi::$k_business} for day {@link ScheduleListApi::$dt_date}. Returns all classes and appointments scheduled for the given business on the specified date, sorted chronologically. Supports both single-day and date-range modes, and includes full session details such as staff, visit counts, assets, and class images.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration options for schedule. Key is option name, value is boolean. May contain the following keys: <dl> <dt>bool `is_appointment_cancel_recurring`</dt> <dd>If needed to show recurring canceled appointments on schedule.</dd> <dt>bool `is_appointment_cancel_single`</dt> <dd>If needed to show single canceled appointments on schedule.</dd> <dt>bool `is_class_cancel`</dt> <dd>If needed to show canceled classes on schedule.</dd> </dl>'
          schema:
            type: array
        - name: dl_end
          in: query
          description: 'The end date of the range from which the list of schedule sessions should be retrieved. This will be `null` if the range has no end date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'The start date of the range from which the list of scheduled sessions should be retrieved. This will be `null` if the range has no start date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dt_date
          in: query
          description: 'The date of the sessions in Coordinated Universal Time (UTC) and MySQL format. If this value is used, then {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_end} and {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_start} should not be set.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_schedule:
                        type: array
                        description: 'The sessions present on the business schedule. These are sorted chronologically in ascending order. Every element has the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes.</dd> <dt>array[]|null `a_quiz`</dt> <dd> List of quizzes. Has next structure: `null` if no quizzes for schedule item. <dl> <dt>int `id_status`</dt> <dd>Response status ID. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether it''s hidden. `true` quiz is hidden, `false` otherwise.</dd> <dt>bool `is_require`</dt> <dd>Whether it''s required. `true` quiz is required, `false` otherwise.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key, <i>primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key, <i>primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string `k_quiz_response`</dt> <dd>Quiz response key, <i>primary key in {@link \Wl\Quiz\Response\ResponseSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl> </dd> <dt>array `a_appointment_visit_info`</dt> <dd> Additional visit information about this appointment. Empty array if it''s a class. <dl> <dt>int `id_visit`</dt> <dd>Visit id. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_confirmed`</dt> <dd>`true` means that appointment was requested and confirmed by the staff.</dd> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl> </dd> <dt>string[] `a_resource`</dt> <dd>A list of assets involved in the session. <i>Primary keys in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_resource_removed`</dt> <dd>List of removed assets involved in the session. <i>Primary keys in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_staff`</dt> <dd> A list of staff members who will conduct the session. <i>Primary keys in {@link \RsStaffSql} table.</i> Deprecated. Returned only for applications in the `APPS_USE_OLD_K_STAFF` allow-list. New applications should use `a_uid_staff` instead. </dd> <dt>string[] `a_uid_staff`</dt> <dd> A list of staff members who will conduct the session. <i>Primary keys in {@link \PassportLoginSql} table.</i> </dd> <dt>array `a_staff_info`</dt> <dd> Information about staff members who conduct this session. For applications in the `APPS_USE_OLD_K_STAFF` allow-list, keys are <i>primary keys in {@link \RsStaffSql} table</i>. For other applications, keys are <i>primary keys in {@link \PassportLoginSql} table</i>. Values are array with data: <dl> <dt>string `text_staff`</dt> <dd>Staff full name.</dd> <dt>bool `is_staff_change`</dt> <dd>`true` means staff is substituted, `false` simple staff member.</dd> <dt>string `k_staff`</dt> <dd> Staff key. <i>Primary key in {@link \RsStaffSql} table.</i> Returned only for applications in the `APPS_USE_OLD_K_STAFF` allow-list. </dd> <dt>string `uid_staff`</dt> <dd>Staff user key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> </dd> <dt>string[] `a_user`</dt> <dd>For appointments, this is a list of the names of users who are scheduled to attend the session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. Each value is <i>primary key in {@link \RsLocationSql} table</i>.</dd> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `dt_date_cancel`</dt> <dd>The date/time when the session was canceled in UTC. Only used for appointments.</dd> <dt>string `dt_date_local`</dt> <dd>The date/time of the session in local time.</dd> <dt>bool `has_quiz_uncompleted`</dt> <dd>`true` if has uncompleted quizzes, `false` otherwise.</dd> <dt>int `i_addon`</dt> <dd>The number of addons for the current appointment. Set only if the current session is an appointment.</dd> <dt>int `i_book`</dt> <dd>The number of clients booked into the session.</dd> <dt>int `i_capacity`</dt> <dd>The maximum capacity of the session.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_padding_after`</dt> <dd>The padding time after the session in minutes. Only used for appointments.</dd> <dt>int `i_padding_before`</dt> <dd>The padding time before the session in minutes. Only used for appointments.</dd> <dt>int `i_start`</dt> <dd> The start time in minutes after midnight. For example, a class starting at 10:30 in the morning local time will have an `i_start` value of 630. </dd> <dt>int `i_wait`</dt> <dd>Count clients on waitlist.</dd> <dt>int `id_option`</dt> <dd> Appointment title display style. Set only for appointments, for others it will be equal to 0. Constants from {@link \Wl\Schedule\Design\OptionSid}. </dd> <dt>int `id_service`</dt> <dd>The ID of the service type. One of {@link \RsServiceSid} constants.</dd> <dt>bool `is_arrive`</dt> <dd> For appointments: <tt>true</tt> if user has checked-in; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_pay`</dt> <dd> For appointments: <tt>true</tt> if appointment is paid; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_repeat`</dt> <dd> For appointments: <tt>true</tt> if appointment is recurring; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>string `k_appointment`</dt> <dd> The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i> If the session isn''t an appointment, this will be `0`. </dd> <dt>string `dt_confirm`</dt> <dd> Confirmation date for appointment in MySQL format. Will be zero date + time in case appointment is not yet confirmed by client. </dd> <dt>string `k_class`</dt> <dd> The class key. <i>Primary key in {@link \RsClassSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_class_period`</dt> <dd> The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_location`</dt> <dd>The location key for where the session takes place. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_service`</dt> <dd> This is the key of the appointment type, while `k_appointment` is the specific instance. <i>Primary key in {@link \RsServiceSql} table.</i> For other cases, this will be `0`. </dd> <dt>string `s_title`</dt> <dd>The name of the session.</dd> <dt>bool `show_quiz_icon`</dt> <dd>`true` when need show quiz icon, `false` otherwise.</dd> <dt>int `t_business_start`</dt> <dd>The earliest time the business where the session takes place, opens. In Unix format.</dd> <dt>int `t_business_end`</dt> <dd>The latest time the business where the session takes place, closes. In Unix format.</dd> <dt>string `text_alert`</dt> <dd>Alert message.</dd> <dt>string `text_color_background`</dt> <dd>The background color in hex representation as used on WellnessLiving.</dd> <dt>string `text_color_border`</dt> <dd>The border color in hex representation as used on WellnessLiving.</dd> <dt>string `text_quiz_title`</dt> <dd>Title for quiz icon.</dd> <dt>bool `is_virtual_service`</dt> <dd><tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.</dd> <dt>string `url_image`</dt> <dd>URL to image. Empty if image not exist.</dd> </dl>'
                      is_virtual_service:
                        type: boolean
                        description: '<tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.'
      tags:
        - StaffApp-ScheduleList
  '/Wl/Schedule/ScheduleList/StaffApp/ScheduleListByToken.json':
    get:
      summary: 'Gets schedule of business {@link ScheduleListApi::$k_business} for day {@link ScheduleListApi::$dt_date}. Returns all classes and appointments scheduled for the given business on the specified date, sorted chronologically. Supports both single-day and date-range modes, and includes full session details such as staff, visit counts, assets, and class images.'
      parameters:
        - name: a_config
          in: query
          description: 'Configuration options for schedule. Key is option name, value is boolean. May contain the following keys: <dl> <dt>bool `is_appointment_cancel_recurring`</dt> <dd>If needed to show recurring canceled appointments on schedule.</dd> <dt>bool `is_appointment_cancel_single`</dt> <dd>If needed to show single canceled appointments on schedule.</dd> <dt>bool `is_class_cancel`</dt> <dd>If needed to show canceled classes on schedule.</dd> </dl>'
          schema:
            type: array
        - name: dl_end
          in: query
          description: 'The end date of the range from which the list of schedule sessions should be retrieved. This will be `null` if the range has no end date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'The start date of the range from which the list of scheduled sessions should be retrieved. This will be `null` if the range has no start date. If this value is used, {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dt_date} should not be set.'
          schema:
            type: string
        - name: dt_date
          in: query
          description: 'The date of the sessions in Coordinated Universal Time (UTC) and MySQL format. If this value is used, then {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_end} and {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi::$dl_start} should not be set.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_token
          in: query
          description: 'The security token.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_schedule:
                        type: array
                        description: 'The sessions present on the business schedule. These are sorted chronologically in ascending order. Every element has the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes.</dd> <dt>array `a_appointment_visit_info`</dt> <dd> Additional visit information about this appointment. Empty array if it''s a class. <dl> <dt>int `id_visit`</dt> <dd>Visit id. One of {@link \Wl\Visit\VisitSid} constants.</dd> <dt>bool `is_confirmed`</dt> <dd>`true` means that appointment was requested and confirmed by the staff.</dd> <dt>bool `is_deny`</dt> <dd>`true` means that appointment was requested and denied by the staff.</dd> <dt>bool `is_notify_request_accept`</dt> <dd>`true` means that the client will receive a notification, if appointment will be confirmed by the staff.</dd> <dt>bool `is_notify_request_deny`</dt> <dd>`true` means that the client will receive a notification, if appointment will be denied by the staff.</dd> <dt>bool `is_request`</dt> <dd>`true` means that appointment was requested, but not confirmed by the staff.</dd> </dl> </dd> <dt>string[] `a_resource`</dt> <dd>A list of assets involved in the session. <i>Primary keys in {@link \RsResourceSql} table.</i></dd> <dt>string[] `a_staff`</dt> <dd> A list of staff members who will conduct the session. <i>Primary keys in {@link \RsStaffSql} table.</i> Deprecated, use <var>a_staff_list</var> instead. </dd> <dt>array `a_staff_info`</dt> <dd> Information about staff members who conduct this session. The keys are <i>primary keys in {@link \RsStaffSql} table</i>. Values are array with data: <dl> <dt>string `text_staff`</dt> <dd>Staff full name.</dd> <dt>bool `is_staff_change`</dt> <dd><tt>true</tt> means staff is substituted, <tt>false</tt> simple staff member.</dd> <dt>string `k_staff`</dt> <dd>Staff key. <i>Primary keys in {@link \RsStaffSql} table.</i></dd> </dl> </dd> <dt>string[] `a_user`</dt> <dd>For appointments, this is a list of the names of users who are scheduled to attend the session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations. Each value is <i>primary key in {@link \RsLocationSql} table</i>.</dd> <dt>string `dt_date`</dt> <dd>The date/time of the session in UTC.</dd> <dt>string `dt_date_cancel`</dt> <dd>The date/time when the session was canceled in UTC. Only used for appointments.</dd> <dt>string `dt_date_local`</dt> <dd>The date/time of the session in local time.</dd> <dt>int `i_book`</dt> <dd>The number of clients booked into the session.</dd> <dt>int `i_capacity`</dt> <dd>The maximum capacity of the session.</dd> <dt>int `i_duration`</dt> <dd>The duration of the session in minutes.</dd> <dt>int `i_padding_after`</dt> <dd>The padding time after the session in minutes. Only used for appointments.</dd> <dt>int `i_padding_before`</dt> <dd>The padding time before the session in minutes. Only used for appointments.</dd> <dt>int `i_start`</dt> <dd> The start time in minutes after midnight. For example, a class starting at 10:30 in the morning local time will have an `i_start` value of 630. </dd> <dt>int `i_wait`</dt> <dd>Count clients on waitlist.</dd> <dt>int `id_option`</dt> <dd> Appointment title display style. Set only for appointments, for others it will be equal to 0. Constants from {@link \Wl\Schedule\Design\OptionSid}. </dd> <dt>int `id_service`</dt> <dd>The ID of the service type. One of {@link \RsServiceSid} constants.</dd> <dt>bool `is_arrive`</dt> <dd> For appointments: <tt>true</tt> if user has checked-in; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_pay`</dt> <dd> For appointments: <tt>true</tt> if appointment is paid; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>bool `is_repeat`</dt> <dd> For appointments: <tt>true</tt> if appointment is recurring; <tt>false</tt> otherwise. For classes always <tt>null</tt>. </dd> <dt>string `k_appointment`</dt> <dd> The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i> If the session isn''t an appointment, this will be `0`. </dd> <dt>string `dt_confirm`</dt> <dd> Confirmation date for appointment in MySQL format. Will be zero date + time in case appointment is not yet confirmed by client. </dd> <dt>string `k_class`</dt> <dd> The class key. <i>Primary key in {@link \RsClassSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_class_period`</dt> <dd> The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i> If the session isn''t a class, this will be `0`. </dd> <dt>string `k_location`</dt> <dd>The location key for where the session takes place. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `k_service`</dt> <dd> This is the key of the appointment type, while `k_appointment` is the specific instance. <i>Primary key in {@link \RsServiceSql} table.</i> For other cases, this will be `0`. </dd> <dt>string `s_title`</dt> <dd>The name of the session.</dd> <dt>string `text_alert`</dt> <dd>Alert message.</dd> <dt>string `text_color_background`</dt> <dd>The background color in hex representation as used on WellnessLiving.</dd> <dt>string `text_color_border`</dt> <dd>The border color in hex representation as used on WellnessLiving.</dd> <dt>bool `is_virtual_service`</dt> <dd><tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.</dd> <dt>string `url_image`</dt> <dd>URL to image. Empty if image not exist.</dd> </dl>'
                      is_virtual_service:
                        type: boolean
                        description: '<tt>true</tt> - If the business has at least one virtual service, <tt>false</tt> - otherwise.'
      tags:
        - StaffApp-ScheduleListByToken
  '/Wl/Schedule/ScheduleList/StaffPeriod/StaffPeriod.json':
    get:
      summary: 'Returns working hours list for business staff members in given day.'
      parameters:
        - name: dt_date
          in: query
          description: 'Date for which working hours are required. In business timezone.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'ID of business for which working hours are required. Primary key in table {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_period:
                        type: array
                        description: 'Working hours list. See {@link \Wl\Schedule\WorkingTime\StaffWorkingTime::getWorkingTime()} for details.'
      tags:
        - StaffPeriod-StaffPeriod
  '/Wl/Shop/Product/ProductList/List.json':
    get:
      summary: 'Gets list of products.'
      parameters:
        - name: is_franchise
          in: query
          description: 'Whether to return franchisee-created products (if business is franchisor). <tt>true</tt> to include franchisee-created products.'
          schema:
            type: boolean
        - name: is_frontend
          in: query
          description: '`true` to get only gift cards available for current user; `false` to get all gift cards.'
          schema:
            type: boolean
        - name: is_inactive_include
          in: query
          description: 'A flag to include inactive items in the query result.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'List of products. <dl> <dt>array `a_option`</dt> <dd> <dl> <dt>string `k_shop_product_option`</dt> <dd>Shop product option key, primary key in {@link \RsShopProductOptionSql}.</dd> <dt>string `text_title`</dt> <dd>Title of the product option.</dd> </dl> </dd> <dt>string `k_shop_product`</dt> <dd>Product key, primary key in {@link \RsShopProductSql}.</dd> <dt>string `text_title`</dt> <dd>Title of product.</dd> </dl>'
      tags:
        - ProductList-List
  '/Wl/Shop/Product/Table/Activator.json':
    post:
      summary: 'Activates/deactivates record list.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_record:
                  type: array
                  description: 'Record key list.'
                is_activate:
                  type: boolean
                  description: '<tt>true</tt> if records should be activated, <tt>false</tt> otherwise (deactivated).'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Table-Activator
  '/Wl/Shop/Category/StaffApp/Category.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to get categories for. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_shop_category:
                        type: array
                        description: 'Categories in online store for business {@link \Wl\Shop\Category\CategoryApi::$k_business}, with next keys: <dl> <dt>int `i_order`</dt> <dd>Shop category order.</dd> <dt>string `k_shop_category`</dt> <dd>ID in {@link \RsShopCategorySql} table.</dd> <dt>string `text_description`</dt> <dd>Category description.</dd> <dt>string `text_title`</dt> <dd>Category title.</dd> </dl>'
      tags:
        - StaffApp-Category
  '/Wl/Profile/Attendance/Schedule/PaymentMultiple.json':
    get:
      summary: ''
      parameters:
        - name: dtl_date
          in: query
          description: 'Local date and time for which visit is booked in MySQL format.'
          schema:
            type: string
        - name: is_simple
          in: query
          description: 'When set to `true` it''s mean that need load full information about unpaid visits: * List of available/existing POs. * List of unpaid addons. When set to `false` loaded only general information about visits on passed day.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Last booked visit key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_client:
                        type: array
                        description: 'Clients'' data. Clients with unpaid appointments data, each element has next structure: <dl> <dt>array `a_relation`</dt> <dd> Information about relation. <dl> <dt>int `id_family_relation`</dt> <dd>Family relation ID.</dd> <dt>string `uid_payer`</dt> <dd>Payer UID.</dd> </dl> </dd> <dt>array[] `a_service`</dt> <dd> Unpaid appointments data. Result returned {@link \Wl\Profile\Attendance\Schedule\PaymentMultipleApi::_getServices()} method. </dd> <dt>string `uid`</dt> <dd>The user for whom the appointment is booked, primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                      a_staff_list:
                        type: array
                        description: 'List of available staff members for tips.'
                      i_unpaid_number:
                        type: number
                        format: int32
                        description: 'Total number of unpaid appointments.'
                      is_tip:
                        type: boolean
                        description: 'Whether tips are accepted.'
      tags:
        - Schedule-PaymentMultiple
    post:
      summary: 'Applies existing purchase options for appointments pay and generates a link for payment in the store.'
      parameters:
        - name: a_visit_pay
          in: query
          description: 'Visits payment data. Visits payment data to apply: <dl> <dt>string[] `a_shop_product_option`</dt> <dd>Selected for payment primary products options keys in {@link \RsShopProductOptionSql} table.</dd> <dt>string `k_visit`</dt> <dd>The primary visit key in {@link \RsVisitSql} table.</dd> <dt>string `text_key`</dt> <dd>Selected pay option to apply. The key has structure {@link \Wl\Visit\Pay\PayChangeApi::$text_key}.</dd> <dt>string `uid`</dt> <dd>The primary user key in {@link \PassportLoginSql} table.</dd> </dl>'
          schema:
            type: array
        - name: is_checkout
          in: query
          description: 'Determines for which store page the redirection url should be generated. `true` if to the checkout page. `false` if to the card page.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user''s key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'Url for redirect after applying existing purchase options.'
      tags:
        - Schedule-PaymentMultiple
  '/Wl/Profile/Contract/ContractAll/ContractAll.json':
    get:
      summary: 'Gets list of contracts.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to get information for. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to get information for. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_contract:
                        type: array
                        description: 'List of contacts. Every element has next keys: <dl> <dt>string `dt_agree_local`</dt> <dd>Agreement date in business timezone.</dd> <dt>string `dt_agree_utc`</dt> <dd>Agreement date in UTC.</dd> <dt>string `html_contract`</dt> <dd>Contract text.</dd> <dt>string `html_title`</dt> <dd>Purchase item title.</dd> <dt>string `ip_agree`</dt> <dd>IP address from which agreement was done.</dd> <dt>string `k_purchase_item`</dt> <dd>Purchase item primary key in {@link \RsPurchaseItemSql} table.</dd> <dt>string `text_title`</dt> <dd>Purchase item title.</dd> <dt>string `url_signature`</dt> <dd>URL to agreement signature.</dd> </dl>'
      tags:
        - ContractAll-ContractAll
  '/Wl/Profile/Form/Response/ResponseList.json':
    get:
      summary: 'Returns the lists of completed and pending quiz responses for the specified user and business. Returns the client''s completed, draft, and pending form responses for the business, sorted newest first. Also provides permission flags that control which actions the current user may perform on each response.'
      parameters:
        - name: hide_completed
          in: query
          description: 'Defines whether completed forms should not be included in result list of forms. `true` to exclude completed forms from result. `false` to include completed forms to result.'
          schema:
            type: boolean
        - name: hide_optional
          in: query
          description: 'Defines whether optional uncompleted forms should not be included in result list of forms. `true` to exclude optional uncompleted forms from result. `false` to include optional uncompleted forms to result.'
          schema:
            type: boolean
        - name: is_backend
          in: query
          description: 'This will be `true` if the API is being used from the backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The key of the business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz_login:
                        type: array
                        description: 'The list of uncompleted quiz responses. Each element has the next structure: <dl> <dt>string[] `a_visit`</dt> <dd> List of visit keys associated with uncompleted response. Key is visit key. <i>Primary key in {@link \RsVisitSql} table.</i> Value is date of visit. </dd> <dt>string `dtl_date`</dt> <dd>The date of the request to fill out a quiz form.</dd> <dt>int `id_source`</dt> <dd> The place where the request to fill out a quiz form occurred. One of the {@link \Wl\Quiz\Response\SourceSid} constants. </dd> <dt>bool `is_private`</dt> <dd> Determines whether the form can be viewed by staff member only after confirmation. `true` - the form can be viewed only after confirmation. `false` - the form can always be viewed. </dd> <dt>string `k_quiz`</dt> <dd>The quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>The key of the request. <i>Primary key in the {@link \Wl\Quiz\Login\QuizLoginSql} table.</i></dd> <dt>string|null `k_quiz_response`</dt> <dd> Key of the response. <i>Primary key in the {@link \Core\Quiz\Response\ResponseSql} table.</i> Not `null` in case when response in draft status. </dd> <dt>string `text_title`</dt> <dd>The quiz title.</dd> </dl>'
                      a_quiz_response:
                        type: array
                        description: 'The list of completed quiz responses. Each element has the next structure: <dl> <dt>string[] `a_visit`</dt> <dd> List of visit keys associated with uncompleted response. Key is visit key. <i>Primary key in {@link \RsVisitSql} table.</i> Value is date of visit. </dd> <dt>string `dtl_date`</dt> <dd>The date of the request to fill out a quiz form.</dd> <dt>int `id_source`</dt> <dd> The place where the request to fill out the quiz form occurred. One of the {@link \Wl\Quiz\Response\SourceSid} constants. </dd> <dt>int `id_status`</dt> <dd>Status of the response. One of {@link \Core\Quiz\Response\ResponseStatusSid} constants.</dd> <dt>bool `is_private`</dt> <dd> Determines whether the form can be viewed by staff member only after confirmation. `true` - the form can be viewed only after confirmation. `false` - the form can always be viewed. </dd> <dt>string `k_quiz`</dt> <dd>The quiz key. <i>Primary key in {@link \Core\Quiz\QuizSql} table.</i></dd> <dt>string `k_quiz_login`</dt> <dd>The quiz response key. <i>Primary key in {@link \Core\Quiz\Response\ResponseSql} table.</i></dd> <dt>string|null `text_amend_date`</dt> <dd>Date when response amended. `null` if response was not edited.</dd> <dt>string|null `text_amend_user`</dt> <dd>Name of the user who amend the response. `null` if response was not edited.</dd> <dt>string `text_title`</dt> <dd>The quiz title.</dd> <dt>string `text_user`</dt> <dd>Name of user who complete response or start a draft.</dd> </dl>'
                      can_amend:
                        type: boolean
                        description: 'Whether response can be amended by current user.'
                      can_fill:
                        type: boolean
                        description: 'Whether response can be filled by current user.'
                      can_remove:
                        type: boolean
                        description: 'Whether response can be removed by current user.'
                      can_view:
                        type: boolean
                        description: 'Whether response can be viewed by current user.'
      tags:
        - Response-ResponseList
  '/Wl/Profile/Form/Registration/RegistrationList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Key of a business to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of a user to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: array
                        description: 'Require and optional to completion forms. Each element has the next structure: <dl> <dt>bool `is_require`</dt> <dd>Whether form is require to completion.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. Primary key in {@link \Core\Quiz\QuizSql} table.</dd> <dt>string `k_quiz_login`</dt> <dd>Quiz login key. Primary key in {@link \Wl\Quiz\Login\QuizLoginSql} table.</dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl>'
      tags:
        - Registration-RegistrationList
  '/Wl/Profile/Page/Overview/OverviewCustomizePanel.json':
    get:
      summary: 'Retrieves settings for client''s profile overview widgets and summary cards to display in Customize panel.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_summary_card:
                        type: array
                        description: 'List of summary cards. Each element has next structure: <dl> <dt>int `i_order`</dt> <dd>Order of a summary cards on client''s overview page.</dd> <dt>int `id_card`</dt> <dd>ID of a profile summary card. One of {@link \Wl\Profile\Page\Overview\SummaryCard\SummaryCardSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether profile summary card is hidden. `true` - hidden, `false` - otherwise.</dd> </dl>'
                      a_widget:
                        type: array
                        description: 'List of widgets. Each element has next structure: <dl> <dt>int `i_order`</dt> <dd>Order of a widget on client''s overview page.</dd> <dt>int `id_widget`</dt> <dd>ID of a profile overview widget. One of {@link \Wl\Profile\Page\Overview\ProfileWidgetSid} constants.</dd> <dt>bool `is_collapsed`</dt> <dd>Whether profile overview widget is collapsed. `true` - collapsed, `false` - otherwise.</dd> <dt>bool `is_hide`</dt> <dd>Whether profile overview widget is hidden. `true` - hidden, `false` - otherwise.</dd> </dl>'
      tags:
        - Overview-OverviewCustomizePanel
    post:
      summary: 'Saves settings for client''s profile overview widgets and summary cards.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_summary_card:
                  type: array
                  description: 'List of summary cards. Each element has next structure: <dl> <dt>int `i_order`</dt> <dd>Order of a summary cards on client''s overview page.</dd> <dt>int `id_card`</dt> <dd>ID of a profile summary card. One of {@link \Wl\Profile\Page\Overview\SummaryCard\SummaryCardSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether profile summary card is hidden. `true` - hidden, `false` - otherwise.</dd> </dl>'
                a_widget:
                  type: array
                  description: 'List of widgets. Each element has next structure: <dl> <dt>int `i_order`</dt> <dd>Order of a widget on client''s overview page.</dd> <dt>int `id_widget`</dt> <dd>ID of a profile overview widget. One of {@link \Wl\Profile\Page\Overview\ProfileWidgetSid} constants.</dd> <dt>bool `is_collapsed`</dt> <dd>Whether profile overview widget is collapsed. `true` - collapsed, `false` - otherwise.</dd> <dt>bool `is_hide`</dt> <dd>Whether profile overview widget is hidden. `true` - hidden, `false` - otherwise.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Overview-OverviewCustomizePanel
  '/Wl/Profile/Page/Overview/ProfilePageOverview.json':
    post:
      summary: 'Saves settings for client''s profile overview widgets and summary cards.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_summary_card_list:
                  type: array
                  description: 'Client''s profile overview summary cards settings. Each element has next structure: <dl> <dt>int `i_order`</dt> <dd>Order of a summary card on page.</dd> <dt>int `id_card`</dt> <dd> ID of a profile overview summary card. One of {@link \Wl\Profile\Page\Overview\SummaryCard\SummaryCardSid} constants. </dd> <dt>bool `is_hide`</dt> <dd>Whether profile overview summary card is hidden. `true` - hidden, `false` - otherwise.</dd> </dl>'
                json_widget_list:
                  type: string
                  description: 'Client''s profile overview widgets settings. Each element has next structure: <dl> <dt>int `i_order`</dt> <dd>Order of a widget on page.</dd> <dt>int `id_widget`</dt> <dd> ID of a widget. ID of a profile overview widget. One of {@link \Wl\Profile\Page\Overview\ProfileWidgetSid} constants. </dd> <dt>bool `is_collapsed`</dt> <dd>Whether profile overview widget is collapsed. `true` - collapsed, `false` - otherwise.</dd> <dt>bool `is_hide`</dt> <dd>Whether profile overview widget is hidden. `true` - hidden, `false` - otherwise.</dd> </dl>'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. Primary key in {@link \RsBusinessSql} table. `null` if not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Overview-ProfilePageOverview
  '/Wl/Profile/Page/Notifications/Notification.json':
    post:
      summary: 'Saves client''s notifications settings.'
      parameters:
        - name: a_notification_list
          in: query
          description: 'Client''s notifications settings. Keys are notifications IDs, one of {@link \Wl\Profile\Page\Notifications\NotificationSid} constants, values - <tt>true</tt> if a notification setting is ON, <tt>false</tt> - otherwise.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'ID of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_mail_history
          in: query
          description: 'Key of the email, where unsubscribe link was clicked. Primary key in {@link \RsMailHistorySql} table.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret string to check access. Used only when <var>id_mode</var>={@link \Wl\Mode\ModeSid::EMAIL}.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User ID. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Notifications-Notification
  '/Wl/Profile/PurchaseList/PaymentScheduleModal/PaymentScheduleElement.json':
    get:
      summary: 'Return information about payment schedule.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key in {@link \RsBusinessAr}.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Primary key in {@link \RsLoginPromotionAr}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_onhold_future:
                        type: array
                        description: 'Information about On Hold schedule item in future.'
                      a_onhold_past:
                        type: array
                        description: 'Information about On Hold schedule item in past.'
                      a_schedule:
                        type: array
                        description: 'Schedule item information.'
                      text_collected_note:
                        type: string
                        description: 'Note message about promotion cancelled due to sent to collections.'
      tags:
        - PaymentScheduleModal-PaymentScheduleElement
  '/Wl/Profile/Edit/Email/EditEmail.json':
    get:
      summary: 'Checks if specified email address is busy. Looks up whether the given email is already registered and returns user info, membership status, staff flags, and password status if found; guest requests are subject to a per-IP rate limit.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The email address to be checked.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_member_key:
                        type: array
                        description: 'List of business keys where a user is already a member.'
                      a_user:
                        type: array
                        description: 'Information about the user who occupies the specified email. This will be empty if the email is free or if the rate limit has been reached. Otherwise, has next keys: <dl> <dt>string `text_firstname`</dt> <dd>First name.</dd> <dt>string `text_lastname`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> <dt>string `url_photo`</dt> <dd>URL to photo.</dd> </dl>'
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
                      is_current_member:
                        type: boolean
                        description: 'If `true`, user is already a member of current business, `false` - otherwise.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the number of requests has exceeded the rate limit. Otherwise, this will be `false`.'
                      is_password_set:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, has password set, `false` - otherwise.'
                      is_staff:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in current business, `false` - otherwise.'
                      is_staff_any_business:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in any business, `false` - otherwise.'
                      is_use:
                        type: boolean
                        description: 'If `true`, then the specified email is in use. Otherwise, this will be `false`.'
                      uid_result:
                        type: string
                        description: 'The key of user who has the specified email. This will be `0` if the email is free or if the rate limit has been reached.'
      tags:
        - Email-EditEmail
    post:
      summary: 'Adds user to business. Registers the found user as a member of the specified business. If required profile fields are missing, grants temporary access until the profile is completed. Sends a welcome notification once the user is fully registered.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
      tags:
        - Email-EditEmail
  '/Wl/Profile/Edit/Email/EditEmail67.json':
    get:
      summary: 'Checks if specified email address is busy. Looks up whether the given email is already registered and returns user info, membership status, staff flags, and password status if found; guest requests are subject to a per-IP rate limit.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, the API is being used from backend. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'The email address to be checked.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_member_key:
                        type: array
                        description: 'List of business keys where a user is already a member.'
                      a_user:
                        type: array
                        description: 'Information about the user who occupies the specified email. This will be empty if the email is free or if the rate limit has been reached. Otherwise, has next keys: <dl> <dt>string `text_firstname`</dt> <dd>First name.</dd> <dt>string `text_lastname`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> <dt>string `url_photo`</dt> <dd>URL to photo.</dd> </dl>'
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
                      is_current_member:
                        type: boolean
                        description: 'If `true`, user is already a member of current business, `false` - otherwise.'
                      is_limit:
                        type: boolean
                        description: 'If `true`, then the number of requests has exceeded the rate limit. Otherwise, this will be `false`.'
                      is_password_set:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, has password set, `false` - otherwise.'
                      is_staff:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in current business, `false` - otherwise.'
                      is_staff_any_business:
                        type: boolean
                        description: 'If `true`, user, who has the specified email, is staff in any business, `false` - otherwise.'
                      is_use:
                        type: boolean
                        description: 'If `true`, then the specified email is in use. Otherwise, this will be `false`.'
                      uid_result:
                        type: string
                        description: 'The key of user who has the specified email. This will be `0` if the email is free or if the rate limit has been reached.'
      tags:
        - Email-EditEmail67
    post:
      summary: 'Adds user to business. Registers the found user as a member of the specified business. If required profile fields are missing, grants temporary access until the profile is completed. Sends a welcome notification once the user is fully registered.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key where the check must be performed.'
          schema:
            type: string
        - name: uid_want
          in: query
          description: 'The key of user whose email will be edited. This will be `0` in the case of a new user creation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_added:
                        type: boolean
                        description: 'Shows, whether client was registered in the business: <tt>true</tt> if user was added to the business, <tt>false</tt> if staff only received temporary access, because mandatory fields must be specified first.'
      tags:
        - Email-EditEmail67
  '/Wl/Profile/Account/Select/Select.json':
    get:
      summary: 'Retrieves information about user specified in {@link SelectApi::$uid} and his relationship with sub accounts.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to retrieve relationship information. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve relationship information. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_user:
                        type: array
                        description: 'Array with information about current user and his relationship with sub accounts.'
      tags:
        - Select-Select
    post:
      summary: 'Signs in user specified in {@link SelectApi::$uid_in}.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to retrieve relationship information. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID to retrieve relationship information. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                uid_in:
                  type: string
                  description: 'ID of relative to sign in. Primary key in {@link \PassportLoginSql}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Select-Select
  '/Wl/Profile/Setting/Delete/Delete.json':
    get:
      summary: ''
      parameters:
        - name: a_business
          in: query
          description: 'List of business keys.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys.'
          schema:
            type: array
        - name: is_backend
          in: query
          description: '`true` if client delete from backend, `false` otherwise.'
          schema:
            type: boolean
        - name: text_application
          in: query
          description: 'The application ID. If set, it means that it is necessary to revoke the tokens of the connected social networks. Currently, only Apple Sign-in supports token revocation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_error_info:
                        type: array
                        description: 'List of reasons why account can not be deleted. <dl> <dt>array `a_reason`</dt> <dd> List of reasons why client can not be deleted. Each value is: <dl> <dt>string `html_data`</dt> <dd>Additional data for reason text.</dd> <dt>string `text_reason`</dt> <dd>Reason why client profile can''t be deleted.</dd> </dl> </dd> <dt>bool `is_reason_single`</dt> <dd>`true` if only one reason exist, `false` otherwise.</dd> <dt>string `text_name`</dt> <dd>Client name.</dd> <dt>string `url_profile`</dt> <dd>Link to the client profile.</dd> </dl>'
                      a_uid_valid:
                        type: array
                        description: 'List of user keys that can be deleted.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      text_reason:
                        type: string
                        description: 'Reason why account can not be deleted.'
      tags:
        - Delete-Delete
    post:
      summary: ''
      parameters:
        - name: a_business
          in: query
          description: 'List of business keys.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys.'
          schema:
            type: array
        - name: is_backend
          in: query
          description: '`true` if client delete from backend, `false` otherwise.'
          schema:
            type: boolean
        - name: text_application
          in: query
          description: 'The application ID. If set, it means that it is necessary to revoke the tokens of the connected social networks. Currently, only Apple Sign-in supports token revocation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_delete_result:
                        type: array
                        description: 'Data about deletion result. Key is a user key, primary key in {@link \PassportLoginSql} table. Value is: <dl> <dt>bool `is_full_delete`</dt> <dd>`true` if client delete from all business, `false` otherwise.</dd> <dt>string `k_business_left`</dt> <dd>Business key when user left. Primary key in {@link \RsBusinessSql}</dd> </dl>'
                      a_error_info:
                        type: array
                        description: 'List of reasons why account can not be deleted. <dl> <dt>array `a_reason`</dt> <dd> List of reasons why client can not be deleted. Each value is: <dl> <dt>string `html_data`</dt> <dd>Additional data for reason text.</dd> <dt>string `text_reason`</dt> <dd>Reason why client profile can''t be deleted.</dd> </dl> </dd> <dt>bool `is_reason_single`</dt> <dd>`true` if only one reason exist, `false` otherwise.</dd> <dt>string `text_name`</dt> <dd>Client name.</dd> <dt>string `url_profile`</dt> <dd>Link to the client profile.</dd> </dl>'
                      a_uid_valid:
                        type: array
                        description: 'List of user keys that can be deleted.'
                  - type: object
                    description: 'Error response.'
                    properties:
                      text_reason:
                        type: string
                        description: 'Reason why account can not be deleted.'
      tags:
        - Delete-Delete
  '/Wl/Profile/Password/Reset/PasswordReset.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of business.'
                uid:
                  type: string
                  description: 'Key of user which password resetting.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_last:
                        type: boolean
                        description: '<tt>true</tt> if the password reset attempt was last; <tt>false</tt> otherwise.'
      tags:
        - Reset-PasswordReset
  '/Wl/Report/Dashboard/Menu/MenuReport.json':
    get:
      summary: ''
      parameters:
        - name: id_report
          in: query
          description: 'Report ID (one of {@link \RsReportSid} constant for old generation reports) or CID report controller (subclass of {@link ReportViewControllerAbstract} for new generation reports).'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key. Primary key in {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_dashboard:
                        type: array
                        description: 'List of dashboards for report.'
      tags:
        - Menu-MenuReport
  '/Wl/Report/Dashboard/Menu/Menu.json':
    get:
      summary: 'Gets available dashboard for specified user within specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_dashboard:
                        type: array
                        description: 'List of the available dashboards.'
      tags:
        - Menu-Menu
    put:
      summary: 'Updates dashboard sort order selected by user.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_dashboard_order:
                  type: array
                  description: 'Dashboard order list. Each key is primary key from {@link \RsReportDashboardSql} and value is sort position.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Menu-Menu
  '/Wl/Report/Dashboard/Widget/DashboardWidgetList.json':
    get:
      summary: 'Gets list of widgets (reports) that can be placed on a dashboard.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category_list:
                        type: array
                        description: 'List of report categories available to the user and that have at least 1 widget in the <var>a_widget_list</var> below. Each item has the following structure: <dl> <dt>int `i_sort`</dt> <dd>Category sort order.</dd> <dt>int `id_report_category`</dt> <dd>Category ID. One of {@link \RsReportCategorySid}.</dd> <dt>string `text_title`</dt> <dd>Category title.</dd> </dl>'
                      a_widget_default_list:
                        type: array
                        description: 'List of widgets that should be pre-selected by default for a new dashboard. Each item is an array with the following structure: <dl> <dt>int `i_report_widget`</dt> <dd> Widget content identifier. Depending on <var>id_report_widget</var> may be one of {@link \RsReportSid}, {@link \RsReportPageSid}, or primary key in {@link \RsReportSaveSql} table. </dd> <dt>int `i_sort`</dt> <dd>Widget order within its category.</dd> <dt>int `id_report_widget`</dt> <dd>Widget type. One of {@link \RsReportWidgetSid} for details.</dd> <dt>string `text_widget_key`</dt> <dd>Unique key of the widget. Composite of <var>id_report_widget</var> and <var>i_report_widget</var>.</dd> </dl>'
                      a_widget_list:
                        type: array
                        description: 'List of available widgets. Each item is an array with the following structure: <dl> <dt>int[] `a_report_category`</dt> <dd>List of report categories this widget belongs to. List of IDs from {@link \RsReportCategorySid}.</dd> <dt>int `i_report_widget`</dt> <dd> Widget content identifier. Depending on <var>id_report_widget</var> may be one of {@link \RsReportSid}, {@link \RsReportPageSid}, or primary key in {@link \RsReportSaveSql} table. </dd> <dt>int `i_sort`</dt> <dd>Widget order within its category.</dd> <dt>int `id_report_widget`</dt> <dd>Widget type. One of {@link \RsReportWidgetSid} for details.</dd> <dt>string `text_widget_key`</dt> <dd>Unique key of the widget. Composite of <var>id_report_widget</var> and <var>i_report_widget</var>.</dd> <dt>string `text_title`</dt> <dd>Widget title.</dd> </dl>'
      tags:
        - Widget-DashboardWidgetList
    put:
      summary: 'Saves order of widgets on a dashboard.'
      parameters:
        - name: a_report_dashboard_widget
          in: query
          description: 'List of widget keys in the order they should be displayed. Primary keys in the {@link \RsReportDashboardWidgetSql} table.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard
          in: query
          description: 'Dashboard key to update. Primary key in {@link \RsReportDashboardSql} table. `null` if not specified.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Widget-DashboardWidgetList
  '/Wl/Report/Dashboard/Widget/DashboardWidget.json':
    delete:
      summary: 'Deletes dashboard widget.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard_widget
          in: query
          description: 'Widget key. Use {@link DashboardWidgetApi::$a_report_dashboard_widget} to update collapse state of multiple widgets.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Widget-DashboardWidget
    put:
      summary: 'Sets widget collapse state.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard_widget
          in: query
          description: 'Widget key. Use {@link DashboardWidgetApi::$a_report_dashboard_widget} to update collapse state of multiple widgets.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_report_dashboard_widget:
                  type: array
                  description: 'Widget keys list for update collapse state.'
                is_collapsed:
                  type: boolean
                  description: 'Whether dashboard widget is collapsed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Widget-DashboardWidget
  '/Wl/Report/Dashboard/Widget/DashboardWidgetReportViewMode.json':
    put:
      summary: 'Sets widget collapse state.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard_widget
          in: query
          description: 'Widget key. Use {@link DashboardWidgetApi::$a_report_dashboard_widget} to update collapse state of multiple widgets.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_report_dashboard_widget:
                  type: array
                  description: 'Widget keys list for update collapse state.'
                is_report_view_full:
                  type: boolean
                  description: 'Whether report is in full view mode.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Widget-DashboardWidgetReportViewMode
  '/Wl/Report/Dashboard/Manage/DashboardManage.json':
    delete:
      summary: 'Deletes dashboard.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard
          in: query
          description: 'Dashboard key to read or update. Primary key in {@link \RsReportDashboardSql} table. <tt>null</tt> if API is called to create a new dashboard.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the currently logged-in user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Manage-DashboardManage
    get:
      summary: 'Gets dashboard contents.'
      parameters:
        - name: id_mode
          in: query
          description: 'Dashboard editing mode. One of {@link PanelModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard
          in: query
          description: 'Dashboard key to read or update. Primary key in {@link \RsReportDashboardSql} table. <tt>null</tt> if API is called to create a new dashboard.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the currently logged-in user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_role:
                        type: [array, "null"]
                        description: 'Selected staff roles of the dashboard. <tt>null</tt> if list of staff roles doesn''t need to be updated. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. Primary key in {@link \RsBusinessRoleSql} table.</dd> </dl>'
                      a_widget_list:
                        type: [object, "null"]
                        description: 'List of widgets that are displayed on the dashboard. <tt>null</tt> if list of widgets doesn''t need to be updated. Structure of each item: <dl> <dt>int `i_report_widget`</dt> <dd> Widget content identifier. Depending on <var>id_report_widget</var> may be one of {@link \RsReportSid}, {@link \RsReportPageSid}, or primary key in {@link \RsReportSaveSql} table. </dd> <dt>int `i_sort`</dt> <dd>Widget order on the dashboard.</dd> <dt>int `id_report_widget`</dt> <dd>Widget type identifier. One of {@link \RsReportWidgetSid}.</dd> </dl>'
                      can_share:
                        type: boolean
                        description: 'Whether user can change share mode.'
                      id_share:
                        type: number
                        format: int32
                        description: 'Dashboard sharing mode. One of {@link ShareSid}.'
                      text_owner_name:
                        type: string
                        description: 'Name of dashboard owner.'
                      text_title:
                        type: [string, "null"]
                        description: 'Dashboard title. <tt>null</tt> for read requests.'
                      url_owner_photo:
                        type: string
                        description: 'The URL where the user photo can be retrieved.'
      tags:
        - Manage-DashboardManage
    post:
      summary: 'Creates a dashboard.'
      parameters:
        - name: id_mode
          in: query
          description: 'Dashboard editing mode. One of {@link PanelModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard
          in: query
          description: 'Dashboard key to read or update. Primary key in {@link \RsReportDashboardSql} table. <tt>null</tt> if API is called to create a new dashboard.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the currently logged-in user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_role:
                  type: [array, "null"]
                  description: 'Selected staff roles of the dashboard. <tt>null</tt> if list of staff roles doesn''t need to be updated. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. Primary key in {@link \RsBusinessRoleSql} table.</dd> </dl>'
                a_widget_list:
                  type: [object, "null"]
                  description: 'List of widgets that are displayed on the dashboard. <tt>null</tt> if list of widgets doesn''t need to be updated. Structure of each item: <dl> <dt>int `i_report_widget`</dt> <dd> Widget content identifier. Depending on <var>id_report_widget</var> may be one of {@link \RsReportSid}, {@link \RsReportPageSid}, or primary key in {@link \RsReportSaveSql} table. </dd> <dt>int `i_sort`</dt> <dd>Widget order on the dashboard.</dd> <dt>int `id_report_widget`</dt> <dd>Widget type identifier. One of {@link \RsReportWidgetSid}.</dd> </dl>'
                id_share:
                  type: number
                  format: int32
                  description: 'Dashboard sharing mode. One of {@link ShareSid}.'
                text_title:
                  type: [string, "null"]
                  description: 'Dashboard title. <tt>null</tt> for read requests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_report_dashboard:
                        type: [string, "null"]
                        description: 'Dashboard key to read or update. Primary key in {@link \RsReportDashboardSql} table. <tt>null</tt> if API is called to create a new dashboard.'
      tags:
        - Manage-DashboardManage
    put:
      summary: 'Updates a dashboard.'
      parameters:
        - name: id_mode
          in: query
          description: 'Dashboard editing mode. One of {@link PanelModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_dashboard
          in: query
          description: 'Dashboard key to read or update. Primary key in {@link \RsReportDashboardSql} table. <tt>null</tt> if API is called to create a new dashboard.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the currently logged-in user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_staff_role:
                  type: [array, "null"]
                  description: 'Selected staff roles of the dashboard. <tt>null</tt> if list of staff roles doesn''t need to be updated. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. Primary key in {@link \RsBusinessRoleSql} table.</dd> </dl>'
                a_widget_list:
                  type: [object, "null"]
                  description: 'List of widgets that are displayed on the dashboard. <tt>null</tt> if list of widgets doesn''t need to be updated. Structure of each item: <dl> <dt>int `i_report_widget`</dt> <dd> Widget content identifier. Depending on <var>id_report_widget</var> may be one of {@link \RsReportSid}, {@link \RsReportPageSid}, or primary key in {@link \RsReportSaveSql} table. </dd> <dt>int `i_sort`</dt> <dd>Widget order on the dashboard.</dd> <dt>int `id_report_widget`</dt> <dd>Widget type identifier. One of {@link \RsReportWidgetSid}.</dd> </dl>'
                id_share:
                  type: number
                  format: int32
                  description: 'Dashboard sharing mode. One of {@link ShareSid}.'
                text_title:
                  type: [string, "null"]
                  description: 'Dashboard title. <tt>null</tt> for read requests.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_report_dashboard:
                        type: [string, "null"]
                        description: 'Dashboard key to read or update. Primary key in {@link \RsReportDashboardSql} table. <tt>null</tt> if API is called to create a new dashboard.'
      tags:
        - Manage-DashboardManage
  '/Wl/Report/Collection/Dynamic/CollectionReportManage.json':
    delete:
      summary: 'Deletes a report from collection.'
      parameters:
        - name: id_report
          in: query
          description: 'Report ID within the collection, one of {@link \RsReportSid}. Must be provided in combination with <var>k_dynamic_collection</var>. Ignored if <var>k_dynamic_collection_report</var> is provided. <tt>null</tt> if <var>k_dynamic_collection_report</var> is provided instead.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_dynamic_collection
          in: query
          description: 'Key of dynamic collection to which <var>id_report</var> belongs. Primary key in {@link DynamicCollectionSql} table. Must be provided in combination with <var>id_report</var>. Ignored if <var>k_dynamic_collection_report</var> is provided. <tt>null</tt> if <var>k_dynamic_collection_report</var> is provided instead.'
          schema:
            type: [string, "null"]
        - name: k_dynamic_collection_report
          in: query
          description: 'Key of the dynamic collection report to manage. Primary key in {@link DynamicCollectionReportSql} table. Must be provided if <var>k_dynamic_collection</var> and <var>id_report</var> are empty. <tt>null</tt> if <var>k_dynamic_collection</var> and <var>id_report</var> pair are provided instead.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the user performing the request. Must be the same as currently logged-in user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dynamic-CollectionReportManage
  '/Wl/Report/Collection/Dynamic/CollectionManage.json':
    get:
      summary: 'Retrieves information about a dynamic collection.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_dynamic_collection
          in: query
          description: 'Key of the dynamic collection to manage. Primary key in {@link DynamicCollectionSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user performing the request. Must be the same as currently logged-in user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_report_list:
                        type: [object, "null"]
                        description: 'List of widgets that are displayed on the dashboard. <tt>null</tt> if list of widgets doesn''t need to be updated. Structure of each item: <dl> <dt>int `i_position`</dt> <dd>Report''s position in the collection.</dd> <dt>int `id_report`</dt> <dd>Report ID, one of {@link \RsReportSid}.</dd> </dl>'
                      cid_collection:
                        type: number
                        format: int32
                        description: 'CID of the collection class.'
                      text_title:
                        type: string
                        description: 'Collection title'
      tags:
        - Dynamic-CollectionManage
    put:
      summary: 'Updates dynamic collection.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_dynamic_collection
          in: query
          description: 'Key of the dynamic collection to manage. Primary key in {@link DynamicCollectionSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user performing the request. Must be the same as currently logged-in user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_report_list:
                  type: [object, "null"]
                  description: 'List of widgets that are displayed on the dashboard. <tt>null</tt> if list of widgets doesn''t need to be updated. Structure of each item: <dl> <dt>int `i_position`</dt> <dd>Report''s position in the collection.</dd> <dt>int `id_report`</dt> <dd>Report ID, one of {@link \RsReportSid}.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dynamic-CollectionManage
  '/Wl/Report/Collection/Dynamic/ReportList.json':
    get:
      summary: 'Gets list of reports available for placing in a dynamic collection of specified class. prevented by client side.'
      parameters:
        - name: cid_dynamic_collection
          in: query
          description: 'CID of a subclass of {@link DynamicCollection}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table. Must be the same as currently logged-in user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_report_list:
                        type: array
                        description: 'List of available reports. Each item has the following structure: <dl> <dt>int `i_position`</dt> <dd>Position of the report in the list of available collection reports. Always 0 in this method.</dd> <dt>int `id_report`</dt> <dd>Report ID, one of {@link \RsReportSid}.</dd> <dt>string `text_title`</dt> <dd>Report title.</dd> </dl>'
      tags:
        - Dynamic-ReportList
  '/Wl/Resource/Type/Edit/Edit.json':
    post:
      summary: ''
      parameters:
        - name: id_category
          in: query
          description: 'Asset category ID from {@link ResourceCategoryEnum}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'ID of current business. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_title_post:
                  type: string
                  description: 'Title for asset category. For post.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_resource_type:
                        type: string
                        description: 'ID of asset category. Primary key in {@link \RsResourceTypeSql} table.'
                      url_layout:
                        type: string
                        description: 'URL to edit asset category layout.'
                      url_resource_create:
                        type: string
                        description: 'URL to create new asset of asset category.'
      tags:
        - Edit-Edit
    put:
      summary: ''
      parameters:
        - name: k_resource_type
          in: query
          description: 'ID of asset category. Primary key in {@link \RsResourceTypeSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_title_put:
                  type: string
                  description: 'Title for asset category. For put.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-Edit
  '/Wl/Resource/Schedule/Add/Add.json':
    post:
      summary: 'Saves new time interval.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_day:
                  type: array
                  description: 'List of working days, constants from {@link \ADateWeekSid}.'
                dt_end_date:
                  type: string
                  description: 'End date of working period.'
                dt_end_time:
                  type: string
                  description: 'End time.'
                dt_start_date:
                  type: string
                  description: 'Start date of working period.'
                dt_start_time:
                  type: string
                  description: 'Start time.'
                i_repeat:
                  type: number
                  format: int32
                  description: 'Count of the periods which specified in {@link AddApi::$id_repeat}. If this field is absent default value {@link \Wl\Classes\Period\Period::REPEAT_COUNT_DEFAULT} is used.'
                id_repeat:
                  type: number
                  format: int32
                  description: 'Measuring unit of {@link AddApi::$i_repeat}. One of {@link \ADurationSid} constants. If this field is absent default value {@link \Wl\Classes\Period\Period::REPEAT_PERIOD_DEFAULT} is used.'
                is_break:
                  type: boolean
                  description: 'Whether model works with available working time. <tt>false</tt> means available working time. <tt>true</tt> means unavailable working time.'
                is_eternal:
                  type: boolean
                  description: 'Whether period is eternal. <tt>true</tt> means on going. <tt>false</tt> means end date will be used.'
                k_location:
                  type: string
                  description: 'Location key. Primary key in {@link \RsLocationSql} table.'
                k_resource:
                  type: string
                  description: 'Resource key. Primary key in {@link \RsResourceSql} table.'
                text_note:
                  type: string
                  description: 'Note to working interval.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_period:
                        type: array
                        description: 'Resource period key. Primary key from {@link \RsResourcePeriodSql}.'
      tags:
        - Add-Add
  '/Wl/Appointment/Book/Asset/AssetList.json':
    get:
      summary: 'Retrieves information about assets in the current asset category. Returns the list of bookable assets at the given location, optionally filtered by category and book now tab. When a date and time are provided, only assets available at that time are included and busy asset slots are returned in {@link AssetListApi::$a_asset_busy}. Supports both frontend and backend modes.'
      parameters:
        - name: dtl_date
          in: query
          description: 'The selected date and time of the asset booking. It is used in cases when the business booking policy allows clients to select a date and time, and then the available asset.'
          schema:
            type: string
        - name: i_image_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default size.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'Mode type, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'This is `true` if asset categories are loaded for back-end mode. Otherwise, this will be `false` for front-end mode.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected book tab.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Key of the appointment, if we reschedule existing appointment. It should be sent to ignore it when we get availability hours for the asset.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. If not set, location''s business will be used.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'The class tab key used to filter assets. This will be `null` if not set yet or if elements with no specified class tab are selected.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_resource_category
          in: query
          description: 'The asset category key to show information for.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone of date and time of asset booking. Empty if {@link AssetListApi::$dtl_date} not set or client can''t change in which timezone dates should be shown.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Client to get information for. If client not set, returns full asset list without client restrictions.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_asset:
                        type: array
                        description: 'A list of information about assets: <dl> <dt>array `a_age_restrictions`</dt> <dd> Information about age restrictions for this event. This will be an empty array if there aren''t any age restrictions. <dl> <dt>int|null `i_age_from`</dt> <dd>The minimum age permitted for the event. This will be `null` if a minimum age isn''t set or available.</dd> <dt>int|null `i_age_to`</dt> <dd>The maximum age permitted for the event. This will be `null` if a maximum age isn''t set or available.</dd> <dt>bool `is_age_public`</dt> <dd> This will be `true` if age restrictions are public and available. Otherwise, this will be `false` if they''re hidden. When restrictions are hidden and current user isn''t a staff member, the age range will be empty. </dd> </dl> </dd> <dt>string[] `a_class_tab`</dt> <dd> The key of service. Keys are service key. <i>Primary key in {@link \RsServiceSql} table.</i> Values are class tab key. <i>Primary key in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i> </dd> <dt>array[] `a_direct_link`</dt> <dd> A list of links to create a booking from a direct link (direct booking URL). The system needs to know what tab is associated with the booking. Therefore, there needs to be one link per tab. Each element has two values: <dl> <dt>string `k_class_tab`</dt> <dd>The key of the book now tab.</dd> <dt>string `url_tab`</dt> <dd>The direct booking URL. This will open the booking wizard under the related booking tab.</dd> </dl> </dd> <dt>array[] `a_image`</dt> <dd> Information about the asset logo: <dl> <dt>int `i_angle`</dt> <dd>The angle of the shape rotation. This is set only if the image is one of the default shapes.</dd> <dt>bool `is_empty`</dt> <dd>Determines if the asset logo is empty.</dd> <dt>string `sid_image_icon`</dt> <dd> The icon name. String representation of one of the {@link \Wl\Resource\Image\ImageIconSid} constants. This is only set if the image kind equals to `image`. </dd> <dt>string `sid_image_shape`</dt> <dd> The shape name. String representation of one of the {@link \Wl\Resource\Image\ImageShapeSid} constants. This is set only if the image kind equals to `shape`. </dd> <dt>string `url`</dt> <dd>The asset logo URL.</dd> </dl> </dd> <dt>array[] `a_period`</dt> <dd> A list of asset periods with the following information: <dl> <dt>string `html_duration`</dt> <dd>The HTML code used to display the asset duration.</dd> <dt>string `html_price`</dt> <dd>The HTML code used to display the formatted price.</dd> <dt>int `i_duration`</dt> <dd>The asset duration in minutes.</dd> <dt>int `id_price`</dt> <dd>The asset period price type. One of {@link \RsServicePriceSid} constants.</dd> <dt>string `m_price`</dt> <dd>The asset period price.</dd> </dl> </dd> <dt>string[] `a_search_tag`</dt> <dd>QUICK Search tag keys.</dd> <dt>bool `hide_application`</dt> <dd> Determines whether the asset will be hidden in the White Label mobile apps. If `true`, the asset won''t be displayed. Otherwise, this will be `false`. </dd> <dt>string `html_age_restriction`</dt> <dd>The resource age restriction</dd> <dt>string `html_deny_reason`</dt> <dd>Human-readable reason why the client cannot book this asset. Empty string if there is no deny reason.</dd> <dt>string `html_title`</dt> <dd>The resource name.</dd> <dt>int|null `id_deny_reason`</dt> <dd> The ID of the reason why the client cannot book this asset. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. `null` if there is no deny reason. </dd> <dt>int `id_service_require`</dt> <dd>The purchase rule. One of the {@link \RsServiceRequireSid} constants.</dd> <dt>bool `is_age_restricted`</dt> <dd>Determines whether this service can''t be booked due to age restrictions.</dd> <dt>string `k_class_tab`</dt> <dd>Quick book tab key. <i>Primary key in {@link \Wl\Classes\Tab\Sql\ClassTab\Sql} table.</i></dd> <dt>string `k_resource`</dt> <dd>The resource key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `k_resource_category`</dt> <dd>The resource category key. <i>Primary key in {@link \RsResourceTypeSql} table.</i></dd> <dt>string|null `sid_deny_reason`</dt> <dd>String representation of the deny reason. `null` if no deny reason.</dd> </dl>'
                      a_asset_busy:
                        type: object
                        description: 'A list of reserved assets. 1st level keys refer to asset keys. 2nd level keys refer to asset numbers. Values are keys of appointment bookings that reserve the asset, or `true` if the asset is reserved by a class or event. For example, if you want to check if the 10th asset with the key of ''15'' is reserved, you can check if `a_resource_busy[''15''][''10'']` is free. If you''re rebooking an appointment, check the value of `a_resource_busy[''15''][''10'']`. If it''s equal to the key of your current appointment booking, you can assume the asset is available.'
                      k_resource_layout:
                        type: string
                        description: 'The asset layout key. May be empty if asset category has no layout.'
      tags:
        - Asset-AssetList
  '/Wl/Appointment/Book/Asset/Category.json':
    get:
      summary: 'Retrieves a list of information about asset categories for the appointment booking page. Returns all asset categories available at the given location, optionally filtered by book now tab. Each category includes its title and a flag indicating whether all its assets are hidden in the White Label mobile application. Supports both frontend and backend modes.'
      parameters:
        - name: is_backend
          in: query
          description: 'If `true`, asset categories are loaded for backend mode. Otherwise, this will be `false` if asset categories are loaded for frontend mode.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected book tab.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'The class tab key to use for filtering services.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category:
                        type: array
                        description: 'A list of information about asset categories. <dl> <dt>bool `hide_application`</dt> <dd> <tt>true</tt> - all resources are hidden in this category for White Label mobile application. <tt>false</tt> - otherwise. </dd> <dt>string `k_resource_type`</dt> <dd>Resource type ID.</dd> <dt>string `html_title`</dt> <dd>Title of resource type.</dd> <dt>string `text_title`</dt> <dd>Title of resource type as text.</dd> </dl>'
      tags:
        - Asset-Category
  '/Wl/Appointment/Book/Asset/AssetLocation.json':
    get:
      summary: 'Gets location of asset.'
      parameters:
        - name: k_resource
          in: query
          description: 'Resource key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_location:
                        type: string
                        description: 'Location key.'
      tags:
        - Asset-AssetLocation
  '/Wl/Appointment/Book/Finish/FinishMultiple.json':
    post:
      summary: 'Completes the appointment booking for one or more providers, optionally creating a new client. Accepts booking details for one or more providers in {@link FinishMultipleApi::$a_book_data}, processes payment using the selected Purchase Option, creates appointment records, and sends booking confirmation notifications. A new client account can be created by supplying user details in {@link FinishMultipleApi::$a_user} when no UID is provided.'
      parameters:
        - name: a_pay
          in: query
          description: 'The payment type for the appointment. One of the {@link \RsAppointmentPaySid} constants. Keys refer to provider indexes.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: a_user
          in: query
          description: 'Data to create new users. Specify this if <var>$uid</var> is empty. The data must contain the next keys: <dl> <dt>string|string[] `a_note`</dt> <dd>The note or list of notes to add to the new user''s profile.</dd> <dt>string `text_mail`</dt> <dd>The new user''s email address.</dd> <dt>string `text_name_first`</dt> <dd>The new user''s first name.</dd> <dt>string `text_name_last`</dt> <dd>The new user''s last name.</dd> <dt>string `text_phone`</dt> <dd>The new user''s mobile phone number.</dd> </dl>'
          schema:
            type: array
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. Specify this to reschedule a certain appointment.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_answer:
                  type: object
                  description: 'A list of answers for the questions from {@link \Wl\Appointment\Book\Question\QuestionApi::$a_question}. 1st dimension - provider index. 2nd dimension - keys refer to hashes of the questions. Values refer to answers for the questions.'
                a_book_data:
                  type: array
                  description: 'All data required to book an appointment. <dl> <dt>int `id_class_tab`</dt> <dd>The booking service type. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>array[] `a_provider`</dt> <dd> A list of providers and their booking details. Every element has next keys: <dl> <dt>array[] `a_conflict`</dt> <dd> Information about booking conflicts. Keys are bookings dates/times in MySQL format in UTC. Values are arrays with next keys: <dl> <dt>string `dt_date_local`</dt> <dd>New appointment date/time in MySQL in locale timezone.</dd> <dt>int `i_duration`</dt> <dd>New asset booking duration.</dd> <dt>int `i_index`</dt> <dd>New asset index.</dd> <dt>int `id_conflict`</dt> <dd>Solution type. One of {@link \RsAppointmentEditConflictSid} constants.</dd> <dt>string `k_resource`</dt> <dd>New asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string|null `k_staff`</dt> <dd>New staff member. <i>Primary key in {@link \RsStaffSql} table.</i> `null` in a case of asset booking.</dd> <dt>string|null `uid_staff`</dt> <dd> New staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> `null` in a case of asset booking. </dd> </dl> </dd> <dt>array `a_product`</dt> <dd> Add-ons to the appointment. Specified for appointment bookings only. The old format used array keys. <i>Primary keys in {@link \RsShopProductOptionSql} table.</i> While the new format has each element as an array: <dl> <dt>int `i_count`</dt> <dd>The add-on buy count.</dd> <dt>int `i_count_use`</dt> <dd>The add-on use count. If not set, then use count is equals to buy count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The add-on key. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl> </dd> <dt>array `a_repeat`</dt> <dd> Information for the recurring booking: This will be empty if the appointment isn''t booked recurringly. <dl> <dt>int[] `a_week`</dt> <dd> The days of the week when the appointment repeats. One of the constants of the {@link \ADateWeekSid} class. This will be empty if the appointment doesn''t repeat weekly. </dd> <dt>string `dl_end`</dt> <dd> The date when the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>The frequency at which the appointment repeats.</dd> <dt>int `id_period`</dt> <dd>The measurement unit of `i_period`. One of the {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> <tt>true</tt> if the appointment repeats monthly on the same date. <tt>false</tt> if the appointment repeats monthly on the same day of the week. <tt>null</tt> if the appointment doesn''t repeat monthly. </dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> The list of assets for the appointment booking. Keys refer to asset categories. Values are arrays with the next keys: Specify this only for an appointment booking. <dl> <dt>int `i_index`</dt> <dd>The asset index on the layout. This is only specified if the asset category has a layout.</dd> <dt>string `k_resource`</dt> <dd>The asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>The date/time for the booking in MySQL format in the location''s time zone.</dd> <dt>int `i_duration`</dt> <dd>The duration for the asset booking in minutes. Specify this for separate asset bookings only.</dd> <dt>int `i_index`</dt> <dd> The asset index on the layout. Specify this for separate asset bookings only and for cases when the asset category only has the layout. </dd> <dt>int `id_gender_staff`</dt> <dd> The gender of the staff member conducting the appointment. One of the {@link \AGenderSid} constants. Specify this for appointment bookings only. </dd> <dt>int `id_purchase_item`</dt> <dd>Type of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_unpaid_force`</dt> <dd> If `true`, the appointment is booked as unpaid. Otherwise, this will be `false` to select an available Purchase Option. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>If `true`, appointment waits unpaid.</dd> <dt>string `k_login_prize`</dt> <dd>The user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd> The user''s Purchase Option. Specify this if you want to use a specific Purchase Option to pay for the booking. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_resource`</dt> <dd> The asset booking. Specify this for separate asset bookings only. <i>Primary key in {@link \RsResourceSql} table.</i> </dd> <dt>string `k_service`</dt> <dd> The appointment booking. Specify this for appointment bookings only. <i>Primary key in {@link \RsServiceSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd> The user''s pass (for example, a membership or a package). Specify this if you want to set the pass to use to pay for the booking. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `k_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. deprecated Use <var>uid_staff</var>. Available for legacy allow-list only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `uid_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> <dt>string `k_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this and <var>k_staff</var> is that this value must be set only in cases when you want to add customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `uid_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this and <var>uid_staff</var> is that this value must be set only in cases when you want to add customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \PassportLoginSql} table.</i> </dd> <dt>string `m_tip_appointment`</dt> <dd>The amount of selected tips.</dd> <dt>string `k_timezone`</dt> <dd>The time zone key. This will be ''null'' if the time zone used matches the time zone of the location.</dd> <dt>string `uid`</dt> <dd> User key. <i>Primary key in {@link \PassportLoginSql} table.</i> Specify only in a case of booking for a lof of different users. </dd> </dl> </dd> </dl>'
                a_notification:
                  type: array
                  description: 'Information for sending an appointment notification. <dl> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> </dl>'
                a_paid:
                  type: array
                  description: 'The sum paid. Keys refer to provider indexes.'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each source contains: <dl> <dt>float `f_amount`</dt> <dd>Amount of money to withdraw with this payment source.</dd> <dt>int `id_pay_method`</dt> <dd>Payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether this payment method is hidden. <i>See {@link \RsPayEnvironment::sourceAdd()} for details.</i></dd> <dt>bool `is_success`</dt> <dd>Whether this source was successfully charged.</dd> <dt>string `m_fee`</dt> <dd>Fee amount for this payment source. <i>Calculated and set in {@link \RsPayForm::_franchiseFee()}</i></dd> <dt>string `m_surcharge`</dt> <dd>Surcharge amount for this payment source.</dd> <dt>array `pa`</dt> <dd> Payer authentication data. Element may not present for payment sources that do not support payer authentication, or payer authentication is not implemented by this payment processor. This array is represented by <tt>namespace.Wl/Pay/Processor/ProcessorInterface/PayerAuthenticationForm.xml</tt> at browser side. Structure of the array: <dl> <dt>string `json_data`</dt> <dd> Additional payer authentication data. Copy of value set with <tt>Wl_Pay_Processor_ProcessorInterface_Abstract.paDataSet()</tt>. An empty string (or element not passed) if this payment processor does not provide additional payer authentication data, or payer authentication was not performed. </dd> <dt>string `m_amount`</dt> <dd> Authenticated payment amount. Copy of value set with <tt>Wl_Pay_Processor_ProcessorInterface_Abstract.paAmountSet()</tt>. An empty string (or element not passed) if payer authentication was not performed. </dd> <dt>string `k_pay_transaction`</dt> <dd> Key of the payment transaction that was created during payer authentication. In this case, payment transaction should be attached to this transaction. An empty string (or element not passed) if transaction was not created during payer authentication, or payer authentication was not executed. </dd> </dl> </dd> <dt>string `s_index`</dt> <dd>Index of this form. This corresponds the key this item is written in this array with.</dd> </dl>'
                a_payment_data:
                  type: array
                  description: 'Data required for payment with the next structure: <dl> <dt>int `id_purchase_item`</dt> <dd>The purchase item type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The promotion or appointment key, depending on <var>id_purchase_item</var> in this array.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `text_discount_code`</dt> <dd>The discount code.</dd> </dl>'
                a_purchase_item:
                  type: object
                  description: 'The purchase items keys. This will be empty if no purchases have been made for the appointment booking. Keys refer to provider indexes. Value is array of item keys.'
                a_quiz_response:
                  type: array
                  description: 'The list of quiz response keys. Key is quiz key. Value is quiz response key.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The mode type. One of the {@link ModeSid} constants.'
                is_back_to_back:
                  type: boolean
                  description: 'Determines whether multiple appointments have been booked in back-to-back mode.'
                is_try:
                  type: boolean
                  description: 'This will be `true` when trying to make a test booking and rollback should be applied. Otherwise, this will be `false`. If the flag is set to `true`, credit card requirement will be ignored during this check.'
                s_id:
                  type: string
                  description: 'Unique identifier of the wizard.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'The booked appointments. Every element has the key: <dl> <dt>string `k_appointment`</dt> <dd>The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> </dl>'
                      a_login_activity_visit:
                        type: array
                        description: 'The activity IDs of bookings that have been made.'
                      a_visit:
                        type: array
                        description: 'The visit IDs.'
                      a_visit_provider:
                        type: object
                        description: 'Keys of booked visits. Structured into a two-dimensional array. 1st dimension - providers; 2nd dimension - visit keys inside a provider.'
      tags:
        - Finish-FinishMultiple
  '/Wl/Appointment/Book/Finish/Finish.json':
    get:
      summary: 'Loads data to prepare client side to complete booking. Returns notification settings (email, push, SMS) for the appointment creation confirmation so the client side can display the appropriate notification options before finalizing the booking.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_notification:
                        type: array
                        description: 'Information for sending an appointment notification. <dl> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Finish-Finish
    post:
      summary: 'Completes the appointment booking and processes payment for the client. Delegates to {@link FinishMultipleApi::post()} to handle a single appointment booking, mapping the legacy single-provider request fields to the multi-provider format. Creates an appointment record, applies the selected Purchase Option, and sends the booking confirmation notification.'
      parameters:
        - name: a_book_data
          in: query
          description: 'All data from the provider model <tt>Wl_Appointment_Book_ProviderModel</tt>: <dl> <dt>array `a_conflict`</dt> <dd> Information about booking conflicts. Keys are bookings dates/times in MySQL format in UTC. Values are arrays with next keys: <dl> <dt>string `dt_date_local`</dt> <dd>New appointment date/time in MySQL in locale timezone.</dd> <dt>int `i_duration`</dt> <dd>New asset booking duration.</dd> <dt>int `i_index`</dt> <dd>New asset index.</dd> <dt>int `id_conflict`</dt> <dd>Solution type. One of {@link \RsAppointmentEditConflictSid} constants.</dd> <dt>string `k_resource`</dt> <dd>New asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string|null `k_staff`</dt> <dd>New staff member. <i>Primary key in {@link \RsStaffSql} table.</i> `null` in a case of asset booking.</dd> </dl> </dd> <dt>array `a_product`</dt> <dd> Add-ons to the appointment. Specify this for appointment bookings only. Old format: array keys refer to primary keys. <i>See table {@link \RsShopProductOptionSql}.</i> New format: each element is an array: <dl> <dt>int `i_count`</dt> <dd>The add-on count</dd> <dt>int `i_count_use`</dt> <dd>The add-on use count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The key of add-on. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl> </dd> <dt>array `a_repeat`</dt> <dd> Recurring booking information: This will be empty if the appointment isn''t recurring. <dl> <dt>int[] `a_week`</dt> <dd> The days of week when appointment repeats. One of the {@link \ADateWeekSid} constants. This will be empty if the appointment doesn''t repeat weekly. </dd> <dt>string `dl_end`</dt> <dd> The date when the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle must stop. This will be empty if the repeat cycle doesn''t stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>The frequency of the appointment''s repeat cycle.</dd> <dt>int `id_period`</dt> <dd>The measurement unit of <tt>i_period</tt>. One of the {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> <tt>true</tt> if the appointment repeats monthly on the same date. <tt>false</tt> if the appointment repeats monthly on the same day of the week. <tt>null</tt> if the appointment doesn''t repeat monthly. </dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> A list of assets for the appointment booking. Keys refer to asset categories. <i>Primary keys in {@link \RsResourceTypeSql} table.</i> Values refer to arrays with the next keys: Specify this only for the appointment booking. <dl> <dt>int `i_index`</dt> <dd>The asset index on the layout. Specify this only if the asset category has a layout.</dd> <dt>string `k_resource`</dt> <dd>The asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>The date/time for the booking in MySQL format in the location''s time zone.</dd> <dt>int `i_duration`</dt> <dd>The duration of asset booking in minutes. Specify this for separate asset bookings only.</dd> <dt>int `i_index`</dt> <dd> The asset index on the layout. Specify this for separate asset bookings only and for cases when the asset category only has a layout. </dd> <dt>int `id_class_tab`</dt> <dd>The booking service type. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>int `id_gender_staff`</dt> <dd> The gender of the staff member conducting the appointment. One of the {@link \AGenderSid} constants. Specify this for appointment bookings only. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>If `true`, appointment waits unpaid.</dd> <dt>string `k_login_prize`</dt> <dd>The user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd> The user''s pass (for example, a membership or a package). Specify this if you want to set the pass to use to pay for the booking. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_resource`</dt> <dd> The asset booking. Specify this for separate asset bookings only. <i>Primary key in {@link \RsResourceSql} table.</i> </dd> <dt>string `k_service`</dt> <dd> The appointment booking. Specify this for appointment bookings only. <i>Primary key in {@link \RsServiceSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd>The user''s pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `k_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `k_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this an <var>k_staff</var> is that this value must be set only in cases when you want to add a customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `m_tip_appointment`</dt> <dd>The amount of selected tips.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: a_user
          in: query
          description: 'Data to create new user. Specify this if <var>uid</var> is empty. Must contain the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes to add to user.</dd> <dt>string `text_mail`</dt> <dd>Mail.</dd> <dt>string `text_name_first`</dt> <dd>First name.</dd> <dt>string `text_name_last`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> </dl>'
          schema:
            type: array
        - name: id_pay
          in: query
          description: 'The payment type ID for the appointment. One of the {@link \RsAppointmentPaySid} constants.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This should be set if you''re rebooking an existing appointment. Otherwise, use `0` to book a new appointment.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_answer:
                  type: array
                  description: 'A list of answers for the questions from {@link \Wl\Appointment\Book\Question\QuestionApi::$a_question}. Keys refer to hashes of the questions. Values refer to answers for the questions.'
                a_notification:
                  type: array
                  description: 'Information for sending an appointment notification. <dl> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. <dl> <dt>float `f_amount`</dt> <dd>Amount of money to withdraw with this payment source.</dd> <dt>int `id_pay_method`</dt> <dd>Payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether this payment method is hidden. <i>See {@link \RsPayEnvironment::sourceAdd()} for details.</i></dd> <dt>bool `is_success`</dt> <dd>Whether this source was successfully charged.</dd> <dt>string `m_fee`</dt> <dd>Fee amount for this payment source. <i>Calculated and set in {@link \RsPayForm::_franchiseFee()}</i></dd> <dt>string `m_surcharge`</dt> <dd>Surcharge amount for this payment source.</dd> <dt>array `pa`</dt> <dd> Payer authentication data. Element may not present for payment sources that do not support payer authentication, or payer authentication is not implemented by this payment processor. This array is represented by <tt>namespace.Wl/Pay/Processor/ProcessorInterface/PayerAuthenticationForm.xml</tt> at browser side. Structure of the array: <dl> <dt>string `json_data`</dt> <dd> Additional payer authentication data. Copy of value set with <tt>Wl_Pay_Processor_ProcessorInterface_Abstract.paDataSet()</tt>. An empty string (or element not passed) if this payment processor does not provide additional payer authentication data, or payer authentication was not performed. </dd> <dt>string `m_amount`</dt> <dd> Authenticated payment amount. Copy of value set with <tt>Wl_Pay_Processor_ProcessorInterface_Abstract.paAmountSet()</tt>. An empty string (or element not passed) if payer authentication was not performed. </dd> <dt>string `k_pay_transaction`</dt> <dd> Key of the payment transaction that was created during payer authentication. In this case, payment transaction should be attached to this transaction. An empty string (or element not passed) if transaction was not created during payer authentication, or payer authentication was not executed. </dd> </dl> </dd> <dt>string `s_index`</dt> <dd>Index of this form. This corresponds the key this item is written in this array with.</dd> </dl>'
                a_payment_data:
                  type: array
                  description: 'Data required for payment. Has next structure: <dl> <dt>int `id_purchase_item`</dt> <dd>Type of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Promotion key or appointment key. Depends on <var>id_purchase_item</var> of this array.</dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in the {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_discount_code`</dt> <dd>Discount code.</dd> </dl>'
                a_purchase_item:
                  type: array
                  description: 'The purchase item keys. Empty if no purchases are made for the appointment booking.'
                a_quiz_response:
                  type: array
                  description: 'A list of quiz response keys. Key is quiz key. Value is quiz response key.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The booking mode ID. One of the {@link ModeSid} constants.'
                is_back_to_back:
                  type: boolean
                  description: 'Determines whether multiple appointments are booked in back-to-back mode.'
                is_unpaid_force:
                  type: boolean
                  description: 'If `true`, the appointment is booked as unpaid. Otherwise, this will be `false` to select an available Purchase Option.'
                m_pay:
                  type: string
                  description: 'The sum paid for a deposit.'
                s_id:
                  type: string
                  description: 'Unique identifier of the wizard.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'The keys of the booked appointments. Every element has key: <dl> <dt>string `k_appointment`</dt> <dd>The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> </dl>'
                      a_login_activity_visit:
                        type: array
                        description: 'The activity keys of the bookings that were made.'
                      a_visit:
                        type: array
                        description: 'The keys of visits.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
      tags:
        - Finish-Finish
  '/Wl/Appointment/Book/Finish/Finish47.json':
    get:
      summary: 'Loads data to prepare client side to complete booking. Returns notification settings (email, push, SMS) for the appointment creation confirmation so the client side can display the appropriate notification options before finalizing the booking.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_notification:
                        type: array
                        description: 'Information for sending an appointment notification. <dl> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Finish-Finish47
    post:
      summary: 'Completes the appointment booking and logs variable counts for diagnostic purposes. Delegates to the parent {@link FinishApi::post()} implementation after logging the total variable counts from GET, POST, and the booking data array. The logging is temporary and intended to diagnose "Too many variables" errors in production.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: a_user
          in: query
          description: 'Data to create new user. Specify this if <var>uid</var> is empty. Must contain the following keys: <dl> <dt>string[] `a_note`</dt> <dd>List of notes to add to user.</dd> <dt>string `text_mail`</dt> <dd>Mail.</dd> <dt>string `text_name_first`</dt> <dd>First name.</dd> <dt>string `text_name_last`</dt> <dd>Last name.</dd> <dt>string `text_phone`</dt> <dd>Phone.</dd> </dl>'
          schema:
            type: array
        - name: id_pay
          in: query
          description: 'The payment type ID for the appointment. One of the {@link \RsAppointmentPaySid} constants.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'The appointment key. This should be set if you''re rebooking an existing appointment. Otherwise, use `0` to book a new appointment.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_answer:
                  type: array
                  description: 'A list of answers for the questions from {@link \Wl\Appointment\Book\Question\QuestionApi::$a_question}. Keys refer to hashes of the questions. Values refer to answers for the questions.'
                a_book_data:
                  type: array
                  description: 'The documentation is the same as in {@link FinishApi::$a_book_data}. <dl> <dt>array `a_conflict`</dt> <dd> Information about booking conflicts. Keys are bookings dates/times in MySQL format in UTC. Values are arrays with next keys: <dl> <dt>string `dt_date_local`</dt> <dd>New appointment date/time in MySQL in locale timezone.</dd> <dt>int `i_duration`</dt> <dd>New asset booking duration.</dd> <dt>int `i_index`</dt> <dd>New asset index.</dd> <dt>int `id_conflict`</dt> <dd>Solution type. One of {@link \RsAppointmentEditConflictSid} constants.</dd> <dt>string `k_resource`</dt> <dd>New asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string|null `k_staff`</dt> <dd>New staff member. <i>Primary key in {@link \RsStaffSql} table.</i> `null` in a case of asset booking.</dd> </dl> </dd> <dt>array `a_product`</dt> <dd> Add-ons to the appointment. Specify this for appointment bookings only. Old format: array keys refer to primary keys. <i>See table {@link \RsShopProductOptionSql}.</i> New format: each element is an array: <dl> <dt>int `i_count`</dt> <dd>The add-on count</dd> <dt>int `i_count_use`</dt> <dd>The add-on use count.</dd> <dt>string `k_shop_product_option`</dt> <dd>The key of add-on. <i>Primary key in {@link \RsShopProductOptionSql} table.</i></dd> </dl> </dd> <dt>array `a_repeat`</dt> <dd> Recurring booking information: This will be empty if the appointment isn''t recurring. <dl> <dt>int[] `a_week`</dt> <dd> The days of week when appointment repeats. One of the {@link \ADateWeekSid} constants. This will be empty if the appointment doesn''t repeat weekly. </dd> <dt>string `dl_end`</dt> <dd> The date when the appointment''s repeat cycle stops. This will be empty if the repeat cycle doesn''t stop at a certain date. </dd> <dt>int `i_occurrence`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle must stop. This will be empty if the repeat cycle doesn''t stop after a certain number of occurrences. </dd> <dt>int `i_period`</dt> <dd>The frequency of the appointment''s repeat cycle.</dd> <dt>int `id_period`</dt> <dd>The measurement unit of <tt>i_period</tt>. One of the {@link \ADurationSid} constants.</dd> <dt>bool `is_month`</dt> <dd> <tt>true</tt> if the appointment repeats monthly on the same date. <tt>false</tt> if the appointment repeats monthly on the same day of the week. <tt>null</tt> if the appointment doesn''t repeat monthly. </dd> </dl> </dd> <dt>array `a_resource`</dt> <dd> A list of assets for the appointment booking. Keys refer to asset categories. <i>Primary keys in {@link \RsResourceTypeSql} table.</i> Values refer to arrays with the next keys: Specify this only for the appointment booking. <dl> <dt>int `i_index`</dt> <dd>The asset index on the layout. Specify this only if the asset category has a layout.</dd> <dt>string `k_resource`</dt> <dd>The asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>The date/time for the booking in MySQL format in the location''s time zone.</dd> <dt>int `i_duration`</dt> <dd>The duration of asset booking in minutes. Specify this for separate asset bookings only.</dd> <dt>int `i_index`</dt> <dd> The asset index on the layout. Specify this for separate asset bookings only and for cases when the asset category only has a layout. </dd> <dt>int `id_class_tab`</dt> <dd>The booking service type. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>int `id_gender_staff`</dt> <dd> The gender of the staff member conducting the appointment. One of the {@link \AGenderSid} constants. Specify this for appointment bookings only. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>If `true`, appointment waits unpaid.</dd> <dt>string `k_login_prize`</dt> <dd>The user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd> The user''s pass (for example, a membership or a package). Specify this if you want to set the pass to use to pay for the booking. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `k_resource`</dt> <dd> The asset booking. Specify this for separate asset bookings only. <i>Primary key in {@link \RsResourceSql} table.</i> </dd> <dt>string `k_service`</dt> <dd> The appointment booking. Specify this for appointment bookings only. <i>Primary key in {@link \RsServiceSql} table.</i> </dd> <dt>string `k_session_pass`</dt> <dd>The user''s pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `k_staff`</dt> <dd> The staff member conducting the appointment. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `k_staff_date`</dt> <dd> The staff member conducting the appointment. The difference between this an <var>k_staff</var> is that this value must be set only in cases when you want to add a customer to an appointment that already exists. Specify this for appointment bookings only. <i>Primary key in {@link \RsStaffSql} table.</i> </dd> <dt>string `m_tip_appointment`</dt> <dd>The amount of selected tips.</dd> </dl>'
                a_notification:
                  type: array
                  description: 'Information for sending an appointment notification. <dl> <dt>bool `is_mail`</dt> <dd>`true` to send mail; `false` to not send.</dd> <dt>bool `is_sms`</dt> <dd>`true` to send SMS; `false` to not send.</dd> <dt>bool `is_push`</dt> <dd>`true` to send push notification; `false` to not send.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. <dl> <dt>float `f_amount`</dt> <dd>Amount of money to withdraw with this payment source.</dd> <dt>int `id_pay_method`</dt> <dd>Payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>bool `is_hide`</dt> <dd>Whether this payment method is hidden. <i>See {@link \RsPayEnvironment::sourceAdd()} for details.</i></dd> <dt>bool `is_success`</dt> <dd>Whether this source was successfully charged.</dd> <dt>string `m_fee`</dt> <dd>Fee amount for this payment source. <i>Calculated and set in {@link \RsPayForm::_franchiseFee()}</i></dd> <dt>string `m_surcharge`</dt> <dd>Surcharge amount for this payment source.</dd> <dt>array `pa`</dt> <dd> Payer authentication data. Element may not present for payment sources that do not support payer authentication, or payer authentication is not implemented by this payment processor. This array is represented by <tt>namespace.Wl/Pay/Processor/ProcessorInterface/PayerAuthenticationForm.xml</tt> at browser side. Structure of the array: <dl> <dt>string `json_data`</dt> <dd> Additional payer authentication data. Copy of value set with <tt>Wl_Pay_Processor_ProcessorInterface_Abstract.paDataSet()</tt>. An empty string (or element not passed) if this payment processor does not provide additional payer authentication data, or payer authentication was not performed. </dd> <dt>string `m_amount`</dt> <dd> Authenticated payment amount. Copy of value set with <tt>Wl_Pay_Processor_ProcessorInterface_Abstract.paAmountSet()</tt>. An empty string (or element not passed) if payer authentication was not performed. </dd> <dt>string `k_pay_transaction`</dt> <dd> Key of the payment transaction that was created during payer authentication. In this case, payment transaction should be attached to this transaction. An empty string (or element not passed) if transaction was not created during payer authentication, or payer authentication was not executed. </dd> </dl> </dd> <dt>string `s_index`</dt> <dd>Index of this form. This corresponds the key this item is written in this array with.</dd> </dl>'
                a_payment_data:
                  type: array
                  description: 'Data required for payment. Has next structure: <dl> <dt>int `id_purchase_item`</dt> <dd>Type of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>Promotion key or appointment key. Depends on <var>id_purchase_item</var> of this array.</dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in the {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_discount_code`</dt> <dd>Discount code.</dd> </dl>'
                a_purchase_item:
                  type: array
                  description: 'The purchase item keys. Empty if no purchases are made for the appointment booking.'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Key is quiz key. Value is quiz response key.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The booking mode ID. One of the {@link ModeSid} constants.'
                is_back_to_back:
                  type: boolean
                  description: 'Determines whether multiple appointments are booked in back-to-back mode.'
                is_unpaid_force:
                  type: boolean
                  description: 'If `true`, the appointment is booked as unpaid. Otherwise, this will be `false` to select an available Purchase Option.'
                m_pay:
                  type: string
                  description: 'The sum paid for a deposit.'
                s_id:
                  type: string
                  description: 'Unique identifier of the wizard.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_appointment:
                        type: array
                        description: 'The keys of the booked appointments. Every element has key: <dl> <dt>string `k_appointment`</dt> <dd>The appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i></dd> </dl>'
                      a_login_activity_visit:
                        type: array
                        description: 'The activity keys of the bookings that were made.'
                      a_visit:
                        type: array
                        description: 'The keys of visits.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
      tags:
        - Finish-Finish47
  '/Wl/Appointment/Book/Location/Location.json':
    get:
      summary: 'Retrieves a list of information about locations on the appointment booking page.'
      parameters:
        - name: id_class_tab
          in: query
          description: 'ID of class tab type. <tt>null</tt> if not set yet.'
          schema:
            type: [string, "null"]
        - name: is_backend
          in: query
          description: '<tt>true</tt> - return all service categories of certain location; <tt>false</tt> - return only service categories which has staff members and are bound to certain book tab.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'ID of the business.'
          schema:
            type: string
        - name: k_class_tab
          in: query
          description: 'ID of class tab. <tt>null</tt> if not set yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'ID of user to show information for. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'A list of business locations with information about them.'
      tags:
        - Location-Location
  '/Wl/Appointment/Book/Service/Category.json':
    get:
      summary: 'Retrieves a list of information about service categories on the appointment booking page. Returns all service categories available at the given location, optionally filtered by book now tab. Each category includes its title and the list of available services. Supports both frontend and backend modes; in backend mode access is verified against the business.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: is_backend
          in: query
          description: '`true` - return all service categories for a certain location. `false` - return only service categories that are associated with a book tab and with staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_flag_ignore
          in: query
          description: 'Whether a flagged client is allowed to retrieve categories. `true` - allowed, `false` - not allowed.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected book tab.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'The class tab key used to filter services.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_category:
                        type: array
                        description: 'A list of information about service categories. <dl> <dt>bool `hide_application`</dt> <dd> <tt>true</tt> - all services are hidden in this category for White Label mobile application. <tt>false</tt> - otherwise. </dd> <dt>int `i_sort`</dt> <dd>Sort key for category. Used to sort categories on category list page.</dd> <dt>string `k_service_category`</dt> <dd>Service category key. <i>Primary key in {@link \RsServiceCategorySql} table.</i></dd> <dt>string `s_title`</dt> <dd>Service category title.</dd> </dl>'
                      is_client_flag:
                        type: boolean
                        description: '<tt>true</tt> - if client has a flag, <tt>false</tt> - otherwise.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Service-Category
  '/Wl/Appointment/Book/Service/ServiceList52.json':
    get:
      summary: 'Retrieves information about services in the current service category. Returns all services available for booking at the given location, optionally filtered by service category, book now tab, and client. Each service entry includes title, description, image, pricing, duration, staff availability, and age restriction details. Supports both frontend and backend modes.'
      parameters:
        - name: a_class_tab
          in: query
          description: 'The class tab key to use to filter services. If empty, this can be found on the standard book tab. If multiple tabs are sent, appointment types, which are in at least in one of the tabs, will be in the result.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'List of staff members to filter a result. Empty if the filtering is not necessary.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: '`true` - return all active services for a certain location ignoring user age and other restrictions. `false` - return only services that are allowed for the user.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only on the selected book now tab.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_service_category
          in: query
          description: 'The key of a service category to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'A list of services with information about them. <b>Key</b> - the service key. <b>Value</b> - an array, with every element consisting of the next keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>The list of tab keys for the service.</dd> <dt>array[] `a_direct_link`</dt> <dd> A list of links to start booking from a direct link. This can''t be one link, as the same appointment can be available in several booking tabs. Therefore, each booking tab has its own direct booking link. Each element has two values: <dl> <dt>string `k_class_tab`</dt> <dd>The key of the book now tab.</dd> <dt>string `url_tab`</dt> <dd>The booking URL. This will open the booking wizard under the related booking tab.</dd> </dl> </dd> <dt>array|null `a_config`</dt> <dd> Appointment-specific business policy overrides. `null` when the service uses the general business policy. When not `null`, contains a subset of business config fields, including: <dl> <dt>bool `is_back_to_back`</dt> <dd>`true` if this service supports back-to-back booking.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book this service on a recurring basis.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings for this service default to weekly recurring with no end date.</dd> <dt>bool `is_multiple_booking`</dt> <dd>`true` if multiple bookings are allowed for this service.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if clients on the wait list do not have to pay upfront.</dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Information about the appointment image. An array with the following values: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if the image is not set.</dd> <dt>string `k_service`</dt> <dd>The service key.</dd> <dt>string `s_service`</dt> <dd>The name of the service.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl> </dd> <dt>string[] `a_login_type_restriction`</dt> <dd> Keys are login type keys. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i> Values - List of login types'' titles for current service. Clients that have one of these types can book service. </dd> <dt>string[] `a_member_group_restriction`</dt> <dd> Keys are member group keys. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i> Values - list of member groups'' titles for current service. Clients that belongs to these groups can book service. </dd> <dt>string `f_deposit`</dt> <dd>The amount of deposit required.</dd> <dt>string `f_offline_max`</dt> <dd>The maximum offline price.</dd> <dt>string `f_offline_min`</dt> <dd>The minimum offline price.</dd> <dt>string `f_online`</dt> <dd>The online price.</dd> <dt>bool `hide_application`</dt> <dd> Determines whether the service will be hidden in the White Label mobile application. `true` means that service won''t be displayed. Otherwise, this will be `false`. </dd> <dt>string `html_deny_reason`</dt> <dd>Human-readable reason why the client cannot book this service. Empty string if there is no deny reason.</dd> <dt>int `i_age_from`</dt> <dd>The required minimum client age to book an appointment.</dd> <dt>int `i_age_to`</dt> <dd>The required maximum client age to book an appointment.</dd> <dt>int `i_price`</dt> <dd>The price type ID. One of {@link \RsServicePriceSid} constants.</dd> <dt>int `i_duration`</dt> <dd>The appointment duration in minutes.</dd> <dt>int `id_book_flow`</dt> <dd>The type of client booking flow. One of {@link \Wl\Service\ServiceBookFlowSid} constants.</dd> <dt>int|null `id_deny_reason`</dt> <dd> The ID of the reason why the client cannot book this service. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. `null` if there is no deny reason. </dd> <dt>int `id_service_require`</dt> <dd>The required payment type ID. One of {@link \RsServiceRequireSid} constants.</dd> <dt>bool `is_age_public`</dt> <dd>`true` if age restrictions are public. Otherwise, `false` if they should be hidden from clients.</dd> <dt>bool `is_age_restricted`</dt> <dd>Determines whether this service can''t be booked due to age restrictions.</dd> <dt>bool `is_back_to_back`</dt> <dd>Determines whether this service supports back-to-back booking.</dd> <dt>bool `is_bookable`</dt> <dd>Whether this appointment can be booked online.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book classes and appointments on a recurring basis. Otherwise, this `false`.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings default to weekly recurring with no end date, `false` otherwise.</dd> <dt>bool `is_deposit_percent`</dt> <dd>`true` if `f_deposit` is a percentage. `false` if `f_deposit` is an amount of money.</dd> <dt>bool `is_gender_select`</dt> <dd>`true` if clients can select the staff member''s gender. Otherwise, this will be `false`.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if clients can buy this appointment. `false` if only staff members can sell it.</dd> <dt>bool `is_resource_type`</dt> <dd>`true` if the service requires assets. Otherwise, this will be `false`.</dd> <dt>bool `is_single_buy`</dt> <dd> `true` if the appointment can be booked without a Purchase Option. `false` if it''s necessary to buy a Purchase Option. </dd> <dt>bool `is_staff_confirm`</dt> <dd>`true` if the appointment must be confirmed by a staff member after booking. Otherwise, `false`.</dd> <dt>bool `is_staff_skip`</dt> <dd>`true` if clients can select a staff member for the appointment. Otherwise, `false`.</dd> <dt>bool `is_question`</dt> <dd>Determines whether the service will ask for questions or not.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if the service is virtual. Otherwise, this will be `false`.</dd> <dt>string `k_service`</dt> <dd>The appointment primary key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_service_category`</dt> <dd>The service category primary key. <i>Primary key in {@link \RsServiceCategorySql} table.</i></dd> <dt>string `s_duration`</dt> <dd>The appointment duration in a human-readable format.</dd> <dt>string `s_service`</dt> <dd>The appointment title.</dd> <dt>string|null `sid_deny_reason`</dt> <dd>String representation of the deny reason. `null` if no deny reason.</dd> <dt>string `text_age_restriction`</dt> <dd>Age restriction header.</dd> <dt>string `xml_description`</dt> <dd>Appointment description (deprecated, use `html_description`).</dd> <dt>string `xml_description_short`</dt> <dd>Appointment short description (deprecated, use `html_description_short`).</dd> <dt>string `xml_special`</dt> <dd>Special instructions (deprecated, use `html_special`).</dd> </dl>'
                      is_multiple_booking:
                        type: boolean
                        description: 'Whether services allow multiple appointment booking.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Service-ServiceList52
  '/Wl/Appointment/Book/Service/ServiceList.json':
    get:
      summary: 'Retrieves information about services in the current service category. Returns all services available for booking at the given location, optionally filtered by service category, book now tab, and client. Each service entry includes title, description, image, pricing, duration, staff availability, and age restriction details. Supports both frontend and backend modes.'
      parameters:
        - name: a_class_tab
          in: query
          description: 'The class tab key to use to filter services. If empty, this can be found on the standard book tab. If multiple tabs are sent, appointment types, which are in at least in one of the tabs, will be in the result.'
          schema:
            type: array
        - name: a_staff
          in: query
          description: 'List of staff members to filter a result. Empty if the filtering is not necessary.'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: is_backend
          in: query
          description: '`true` - return all active services for a certain location ignoring user age and other restrictions. `false` - return only services that are allowed for the user.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only on the selected book now tab.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'The class tab key to filter services. If empty or `0`, this can be found on the standard book now tab.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_service_category
          in: query
          description: 'The key of a service category to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'A list of services with information about them. <b>Key</b> - the service key. <b>Value</b> - an array, with every element consisting of the next keys: <dl> <dt>string[] `a_class_tab`</dt> <dd>The list of tab keys for the service.</dd> <dt>array[] `a_direct_link`</dt> <dd> A list of links to start booking from a direct link. This can''t be one link, as the same appointment can be available in several booking tabs. Therefore, each booking tab has its own direct booking link. Each element has two values: <dl> <dt>string `k_class_tab`</dt> <dd>The key of the book now tab.</dd> <dt>string `url_tab`</dt> <dd>The booking URL. This will open the booking wizard under the related booking tab.</dd> </dl> </dd> <dt>array|null `a_config`</dt> <dd> Appointment-specific business policy overrides. `null` when the service uses the general business policy. When not `null`, contains a subset of business config fields, including: <dl> <dt>bool `is_back_to_back`</dt> <dd>`true` if this service supports back-to-back booking.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book this service on a recurring basis.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings for this service default to weekly recurring with no end date.</dd> <dt>bool `is_multiple_booking`</dt> <dd>`true` if multiple bookings are allowed for this service.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if clients on the wait list do not have to pay upfront.</dd> </dl> </dd> <dt>array `a_image`</dt> <dd> Information about the appointment image. An array with the following values: <dl> <dt>int `i_height`</dt> <dd>The height of the image.</dd> <dt>int `i_width`</dt> <dd>The width of the image.</dd> <dt>bool `is_empty`</dt> <dd>This will be `true` if the image is not set.</dd> <dt>string `k_service`</dt> <dd>The service key.</dd> <dt>string `s_service`</dt> <dd>The name of the service.</dd> <dt>string `s_url`</dt> <dd>The URL to the image.</dd> </dl> </dd> <dt>string[] `a_login_type_restriction`</dt> <dd> Keys are login type keys. <i>Primary keys in {@link \Wl\Login\Type\Sql} table.</i> Values - List of login types'' titles for current service. Clients that have one of these types can book service. </dd> <dt>string[] `a_member_group_restriction`</dt> <dd> Keys are member group keys. <i>Primary keys in {@link \Wl\Member\Group\Sql} table.</i> Values - list of member groups'' titles for current service. Clients that belongs to these groups can book service. </dd> <dt>string `f_deposit`</dt> <dd>The amount of deposit required.</dd> <dt>string `f_offline_max`</dt> <dd>The maximum offline price.</dd> <dt>string `f_offline_min`</dt> <dd>The minimum offline price.</dd> <dt>string `f_online`</dt> <dd>The online price.</dd> <dt>bool `hide_application`</dt> <dd> Determines whether the service will be hidden in the White Label mobile application. `true` means that service won''t be displayed. Otherwise, this will be `false`. </dd> <dt>string `html_deny_reason`</dt> <dd>Human-readable reason why the client cannot book this service. Empty string if there is no deny reason.</dd> <dt>int `i_age_from`</dt> <dd>The required minimum client age to book an appointment.</dd> <dt>int `i_age_to`</dt> <dd>The required maximum client age to book an appointment.</dd> <dt>int `i_price`</dt> <dd>The price type ID. One of {@link \RsServicePriceSid} constants.</dd> <dt>int `i_duration`</dt> <dd>The appointment duration in minutes.</dd> <dt>int `id_book_flow`</dt> <dd>The type of client booking flow. One of {@link \Wl\Service\ServiceBookFlowSid} constants.</dd> <dt>int|null `id_deny_reason`</dt> <dd> The ID of the reason why the client cannot book this service. One of {@link \Wl\Schedule\ClassView\DenyReasonSid} constants. `null` if there is no deny reason. </dd> <dt>int `id_service_require`</dt> <dd>The required payment type ID. One of {@link \RsServiceRequireSid} constants.</dd> <dt>bool `is_age_public`</dt> <dd>`true` if age restrictions are public. Otherwise, `false` if they should be hidden from clients.</dd> <dt>bool `is_age_restricted`</dt> <dd>Determines whether this service can''t be booked due to age restrictions.</dd> <dt>bool `is_back_to_back`</dt> <dd>Determines whether this service supports back-to-back booking.</dd> <dt>bool `is_bookable`</dt> <dd>Whether this appointment can be booked online.</dd> <dt>bool `is_book_repeat_client`</dt> <dd>`true` if clients can book classes and appointments on a recurring basis. Otherwise, this `false`.</dd> <dt>bool `is_book_repeat_no_end_date_appointment`</dt> <dd>`true` if appointment bookings default to weekly recurring with no end date, `false` otherwise.</dd> <dt>bool `is_deposit_percent`</dt> <dd>`true` if `f_deposit` is a percentage. `false` if `f_deposit` is an amount of money.</dd> <dt>bool `is_gender_select`</dt> <dd>`true` if clients can select the staff member''s gender. Otherwise, this will be `false`.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if clients can buy this appointment. `false` if only staff members can sell it.</dd> <dt>bool `is_resource_type`</dt> <dd>`true` if the service requires assets. Otherwise, this will be `false`.</dd> <dt>bool `is_single_buy`</dt> <dd> `true` if the appointment can be booked without a Purchase Option. `false` if it''s necessary to buy a Purchase Option. </dd> <dt>bool `is_staff_confirm`</dt> <dd>`true` if the appointment must be confirmed by a staff member after booking. Otherwise, `false`.</dd> <dt>bool `is_staff_skip`</dt> <dd>`true` if clients can select a staff member for the appointment. Otherwise, `false`.</dd> <dt>bool `is_question`</dt> <dd>Determines whether the service will ask for questions or not.</dd> <dt>bool `is_virtual`</dt> <dd>`true` if the service is virtual. Otherwise, this will be `false`.</dd> <dt>string `k_service`</dt> <dd>The appointment primary key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_service_category`</dt> <dd>The service category primary key. <i>Primary key in {@link \RsServiceCategorySql} table.</i></dd> <dt>string `s_duration`</dt> <dd>The appointment duration in a human-readable format.</dd> <dt>string `s_service`</dt> <dd>The appointment title.</dd> <dt>string|null `sid_deny_reason`</dt> <dd>String representation of the deny reason. `null` if no deny reason.</dd> <dt>string `text_age_restriction`</dt> <dd>Age restriction header.</dd> <dt>string `xml_description`</dt> <dd>Appointment description (deprecated, use `html_description`).</dd> <dt>string `xml_description_short`</dt> <dd>Appointment short description (deprecated, use `html_description_short`).</dd> <dt>string `xml_special`</dt> <dd>Special instructions (deprecated, use `html_special`).</dd> </dl>'
                      is_multiple_booking:
                        type: boolean
                        description: 'Whether services allow multiple appointment booking.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Service-ServiceList
  '/Wl/Appointment/Book/Service/ServiceCheck.json':
    get:
      summary: 'Checks whether a service can be booked by the given client at the given date, throwing an exception if booking is restricted. Validates the client''s age against the service''s age restrictions and checks the booking date against the service''s booking window. Returns without data on success; throws exception with a descriptive message when booking is not allowed.'
      parameters:
        - name: dt_date
          in: query
          description: 'Date of booking in MySQL format.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Service-ServiceCheck
  '/Wl/Appointment/Book/Schedule/DayTime.json':
    get:
      summary: 'Retrieves a list of available appointment booking schedule. Returns available time slots for the specified service, location, staff member, and date range. Each slot includes the start time, duration, available capacity, and staff information. Supports both frontend and backend modes, as well as asset bookings and back-to-back scheduling.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to show the available appointment booking schedule.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_unavailable
          in: query
          description: '`true` - return service categories that have no staff members able to conduct them. `false` - return only service categories that have staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'The staff member key used for showing the available appointment booking schedule. In case of back-to-back booking - staff key of first appointment. `0` means any available staff. <i>TODO: Remove this property when constant {@link DayTimeApi::APPS_USE_OLD_K_STAFF} is empty.</i>'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. <dl> <dt>string `dt_date`</dt> <dd>Date of the calendar.</dd> <dt>int `i_count`</dt> <dd>The count of clients that have already booked this appointment.</dd> <dt>int `i_time`</dt> <dd>Integer representation of appointment schedule time.</dd> <dt>int `i_wait`</dt> <dd>The count of clients on the waiting list for this appointment.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether the appointment can be booked only in a wait list.</dd> <dt>string `k_staff`</dt> <dd> this key contains key of staff member. <i>Primary key in {@link \RsStaffSql} table.</i> Otherwise - `0`. </dd> <dt>string `uid_staff`</dt> <dd> If this time is already occupied by any client and staff member (but service capacity is not exhausted), this key contains user key of staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> Otherwise - `0`. </dd> <dt>string `s_title`</dt> <dd>String representation of appointment schedule time.</dd> </dl>'
                      dt_date:
                        type: string
                        description: 'The date to show the available appointment booking schedule.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-DayTime
  '/Wl/Appointment/Book/Schedule/Calendar.json':
    get:
      summary: 'Retrieves a list with all calendar days in specified period with available and unavailable appointment booking schedule. Returns each day in the requested month marked as available or unavailable for booking the given service at the given location. Availability is determined by the business schedule, holidays, and staff availability for the selected date range.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to show the available appointment booking schedule.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_month_view
          in: query
          description: 'If calendar should be displayed in month view mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_unavailable
          in: query
          description: '`true` - return service categories that have no staff members able to conduct them. `false` - return only service categories that have staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'The staff member key used for showing the available appointment booking schedule. In case of back-to-back booking - staff key of first appointment. `0` means any available staff. <i>TODO: Remove this property when constant {@link DayTimeApi::APPS_USE_OLD_K_STAFF} is empty.</i>'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set to use client''s profile timezone.'
          schema:
            type: [string, "null"]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: array
                        description: 'A list with all calendar days in the specified month with available and unavailable appointment bookings in the schedule. <dl> <dt>string `dt_date`</dt> <dd>Date item of the calendar.</dd> <dt>int `i_week`</dt> <dd>Number of day in week.</dd> <dt>bool `is_available`</dt> <dd>Whether booking is available for this day.</dd> <dt>bool `is_current`</dt> <dd>Whether date is current.</dd> <dt>bool `is_out`</dt> <dd>Whether date is out of current month or it''s business/location closed date.</dd> <dt>bool `is_waitlist_only`</dt> <dd>Whether booking for this day available only in wait list.</dd> <dt>bool `is_week_end`</dt> <dd>Whether date is last day of the week.</dd> <dt>bool `is_week_start`</dt> <dd>Whether date is first day of the week.</dd> <dt>string `s_day`</dt> <dd>String representation of day number with leading zeroes.</dd> <dt>string `s_week`</dt> <dd>String representation of week day (one letter, i.e. "F").</dd> </dl>'
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. <dl> <dt>string `dt_date`</dt> <dd>Date of the calendar.</dd> <dt>int `i_count`</dt> <dd>The count of clients that have already booked this appointment.</dd> <dt>int `i_time`</dt> <dd>Integer representation of appointment schedule time.</dd> <dt>int `i_wait`</dt> <dd>The count of clients on the waiting list for this appointment.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether the appointment can be booked only in a wait list.</dd> <dt>string `k_staff`</dt> <dd> this key contains key of staff member. <i>Primary key in {@link \RsStaffSql} table.</i> Otherwise - `0`. </dd> <dt>string `uid_staff`</dt> <dd> If this time is already occupied by any client and staff member (but service capacity is not exhausted), this key contains user key of staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> Otherwise - `0`. </dd> <dt>string `s_title`</dt> <dd>String representation of appointment schedule time.</dd> </dl>'
                      a_timezone_data:
                        type: array
                        description: 'Information about timezone. <dl> <dt>array|null `a_timezone`</dt> <dd> `null` if business settings doesn''t allow client to adjust timezone, otherwise list of timezones: <dl> <dt>int `i_order`</dt> <dd>Timezone order.</dd> <dt>int `i_shift`</dt> <dd>Timezone shift from UTC in hours.</dd> <dt>bool `is_select`</dt> <dd> `true` for selected timezone - from {@link \Wl\Appointment\Book\Schedule\CalendarApi::$k_timezone} param or client''s default timezone when param not set. </dd> <dt>string `k_timezone`</dt> <dd>Timezone key.</dd> <dt>string `s_title`</dt> <dd>Timezone name.</dd> <dt>string `text_abbr`</dt> <dd>Timezone abbreviation.</dd> </dl> </dd> <dt>string|null `name`</dt> <dd>`null` if business settings doesn''t allow client to adjust timezone, otherwise timezone input name.</dd> </dl>'
                      a_week_name:
                        type: array
                        description: 'Array with short week day''s names (2 letters, i.e. ''Fr'') for calendar month view. Week days order according to business''s settings. <dl> <dt>int `i_day`</dt> <dd>Week day, one of the {@link \ADateWeekSid} constants.</dd> <dt>string `html_week_day`</dt> <dd>Short week day''s name (2 letters, i.e. ''Fr'').</dd> </dl>'
                      can_backwards:
                        type: boolean
                        description: 'Whether previous calendar period can be shown (start of shown period later than current date).'
                      dt_date:
                        type: string
                        description: 'The date to show the available appointment booking schedule.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      i_week_end:
                        type: number
                        format: int32
                        description: 'Last day of the week. One of {@link \ADateWeekSid} constants.'
                      i_week_start:
                        type: number
                        format: int32
                        description: 'First day of the week. One of {@link \ADateWeekSid} constants.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-Calendar
  '/Wl/Appointment/Book/Schedule/Calendar73.json':
    get:
      summary: 'Retrieves a list with all calendar days in specified period with available and unavailable appointment booking schedule. Returns each day in the requested month marked as available or unavailable for booking the given service at the given location. Availability is determined by the business schedule, holidays, and staff availability for the selected date range.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to show the available appointment booking schedule.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_month_view
          in: query
          description: 'If calendar should be displayed in month view mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_unavailable
          in: query
          description: '`true` - return service categories that have no staff members able to conduct them. `false` - return only service categories that have staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set to use client''s profile timezone.'
          schema:
            type: [string, "null"]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: array
                        description: 'A list with all calendar days in the specified month with available and unavailable appointment bookings in the schedule. <dl> <dt>string `dt_date`</dt> <dd>Date item of the calendar.</dd> <dt>int `i_week`</dt> <dd>Number of day in week.</dd> <dt>bool `is_available`</dt> <dd>Whether booking is available for this day.</dd> <dt>bool `is_current`</dt> <dd>Whether date is current.</dd> <dt>bool `is_out`</dt> <dd>Whether date is out of current month or it''s business/location closed date.</dd> <dt>bool `is_waitlist_only`</dt> <dd>Whether booking for this day available only in wait list.</dd> <dt>bool `is_week_end`</dt> <dd>Whether date is last day of the week.</dd> <dt>bool `is_week_start`</dt> <dd>Whether date is first day of the week.</dd> <dt>string `s_day`</dt> <dd>String representation of day number with leading zeroes.</dd> <dt>string `s_week`</dt> <dd>String representation of week day (one letter, i.e. "F").</dd> </dl>'
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. <dl> <dt>string `dt_date`</dt> <dd>Date of the calendar.</dd> <dt>int `i_count`</dt> <dd>The count of clients that have already booked this appointment.</dd> <dt>int `i_time`</dt> <dd>Integer representation of appointment schedule time.</dd> <dt>int `i_wait`</dt> <dd>The count of clients on the waiting list for this appointment.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether the appointment can be booked only in a wait list.</dd> <dt>string `k_staff`</dt> <dd> this key contains key of staff member. <i>Primary key in {@link \RsStaffSql} table.</i> Otherwise - `0`. </dd> <dt>string `uid_staff`</dt> <dd> If this time is already occupied by any client and staff member (but service capacity is not exhausted), this key contains user key of staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> Otherwise - `0`. </dd> <dt>string `s_title`</dt> <dd>String representation of appointment schedule time.</dd> </dl>'
                      a_timezone_data:
                        type: array
                        description: 'Information about timezone. <dl> <dt>array|null `a_timezone`</dt> <dd> `null` if business settings doesn''t allow client to adjust timezone, otherwise list of timezones: <dl> <dt>int `i_order`</dt> <dd>Timezone order.</dd> <dt>int `i_shift`</dt> <dd>Timezone shift from UTC in hours.</dd> <dt>bool `is_select`</dt> <dd> `true` for selected timezone - from {@link \Wl\Appointment\Book\Schedule\CalendarApi::$k_timezone} param or client''s default timezone when param not set. </dd> <dt>string `k_timezone`</dt> <dd>Timezone key.</dd> <dt>string `s_title`</dt> <dd>Timezone name.</dd> <dt>string `text_abbr`</dt> <dd>Timezone abbreviation.</dd> </dl> </dd> <dt>string|null `name`</dt> <dd>`null` if business settings doesn''t allow client to adjust timezone, otherwise timezone input name.</dd> </dl>'
                      a_week_name:
                        type: array
                        description: 'Array with short week day''s names (2 letters, i.e. ''Fr'') for calendar month view. Week days order according to business''s settings. <dl> <dt>int `i_day`</dt> <dd>Week day, one of the {@link \ADateWeekSid} constants.</dd> <dt>string `html_week_day`</dt> <dd>Short week day''s name (2 letters, i.e. ''Fr'').</dd> </dl>'
                      can_backwards:
                        type: boolean
                        description: 'Whether previous calendar period can be shown (start of shown period later than current date).'
                      dt_date:
                        type: string
                        description: 'The date to show the available appointment booking schedule.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      i_week_end:
                        type: number
                        format: int32
                        description: 'Last day of the week. One of {@link \ADateWeekSid} constants.'
                      i_week_start:
                        type: number
                        format: int32
                        description: 'First day of the week. One of {@link \ADateWeekSid} constants.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-Calendar73
  '/Wl/Appointment/Book/Schedule/DayTime73.json':
    get:
      summary: 'Retrieves a list of available appointment booking schedule. Returns available time slots for the specified service, location, staff member, and date range. Each slot includes the start time, duration, available capacity, and staff information. Supports both frontend and backend modes, as well as asset bookings and back-to-back scheduling.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to show the available appointment booking schedule.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_unavailable
          in: query
          description: '`true` - return service categories that have no staff members able to conduct them. `false` - return only service categories that have staff members able to conduct them.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_time:
                        type: array
                        description: 'An array with a schedule of available appointment booking times. <dl> <dt>string `dt_date`</dt> <dd>Date of the calendar.</dd> <dt>int `i_count`</dt> <dd>The count of clients that have already booked this appointment.</dd> <dt>int `i_time`</dt> <dd>Integer representation of appointment schedule time.</dd> <dt>int `i_wait`</dt> <dd>The count of clients on the waiting list for this appointment.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether the appointment can be booked only in a wait list.</dd> <dt>string `k_staff`</dt> <dd> this key contains key of staff member. <i>Primary key in {@link \RsStaffSql} table.</i> Otherwise - `0`. </dd> <dt>string `uid_staff`</dt> <dd> If this time is already occupied by any client and staff member (but service capacity is not exhausted), this key contains user key of staff member. <i>Primary key in {@link \PassportLoginSql} table.</i> Otherwise - `0`. </dd> <dt>string `s_title`</dt> <dd>String representation of appointment schedule time.</dd> </dl>'
                      dt_date:
                        type: string
                        description: 'The date to show the available appointment booking schedule.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-DayTime73
  '/Wl/Appointment/Book/Schedule/NextAvailableDay.json':
    get:
      summary: 'Finds and returns the next available date for appointment booking starting from the given date. Searches forward from the specified start date until it finds a day with at least one available time slot for the given service, location, and optional staff member. Returns the first available date so the client side can pre-select it in the booking calendar. This request may take longer than usual on production and uses an extended execution time limit.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Start date to search next available date in MySQL format. If empty, the search will start from the current date.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The duration of the asset booking or custom appointment duration in minutes. Zero in case of service predefined duration. In case of back-to-back booking - custom duration of first appointment.'
          schema:
            type: number
            format: int32
        - name: i_index
          in: query
          description: 'An index of the selected asset. `0` for booking of service or if asset is not on layout.'
          schema:
            type: number
            format: int32
        - name: id_gender_staff
          in: query
          description: 'The ID of the staff member''s gender. In case of back-to-back booking - staff gender of first appointment. One of the {@link \AGenderSid} constants. `0` means no limitations on staff gender.'
          schema:
            type: number
            format: int32
        - name: is_back_to_back
          in: query
          description: 'Determines whether multiple appointments are booked in back-to-back mode.'
          schema:
            type: boolean
        - name: is_staff
          in: query
          description: '`true` if the request is made by staff member; in this case booking policy restrictions are ignored. `false` if the request is made by client; booking policy restrictions are applied.'
          schema:
            type: boolean
        - name: is_tab_all
          in: query
          description: '`true` - search in all tabs. `false` - search only for the selected bookable tab. Cannot be set simultaneously with {DayTimeApi::$k_class_tab}.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_class_tab
          in: query
          description: 'Current booking tab. Only used for asset booking with "Allow clients to select a date and time, then the available asset" booking policy enabled. Cannot be set simultaneously with {DayTimeApi::$is_tab_all}.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'The resource key to show which days are available for booking. Should be `0` in case of back-to-back booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used for showing the available appointment booking schedule. In case of back-to-back booking - service key of first appointment.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Key of timezone. `null` if not set then use default timezone client. <i>See {@link ProfileTimezone::createInBusiness()}.</i>'
          schema:
            type: ["null", string]
        - name: s_appointment
          in: query
          description: 'The staff key to show what days are available for booking. For back-to-back booking ({@link DayTimeApi::$is_back_to_back} == `true`): array of appointments for back-to-back booking. Converted to JSON string to be usable as model key. Each item is an array with next structure: <dl> <dt>array `a_addon`</dt> <dd>Array of appointment addons. Each value is primary key in {@link \RsShopProductSql} table.</dd> <dt>int `i_duration`</dt> <dd>Custom duration of the appointment in minutes. Zero in case of service predefined duration.</dd> <dt>int `id_gender_staff`</dt> <dd>Staff gender. One of {@link \AGenderSid} constants. Zero mean no limitations on staff gender.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `k_staff`</dt> <dd>@deprecated Staff key. Zero means any available staff. <i>Primary key in {@link \RsStaffSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl> <dl> <dt>string `dtl_date`</dt> <dd>Local date and time of appointment start in MySQL format.</dd> <dt>int `i_duration`</dt> <dd>Duration of the appointment in minutes.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>Primary key in {@link \RsServiceSql} table.</i></dd> <dt>string `uid_staff`</dt> <dd>Staff user key. Zero means any available staff. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: s_product
          in: query
          description: 'A list of service add-ons keys(encoded as JSON string). In case of back-to-back booking - add-ons of first appointment.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'The staff user key used for showing the available appointment booking schedule. In case of back-to-back booking - staff user key of first appointment. `0` means any available staff.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dt_date:
                        type: string
                        description: 'Start date to search next available date in MySQL format. If empty, the search will start from the current date.'
                      dt_next:
                        type: string
                        description: 'Next available date in MySQL format.'
                      i_capacity:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can simultaneously book this service. `null` for asset bookings where this limit does not apply.'
                      i_capacity_waitlist:
                        type: [number, "null"]
                        description: 'Maximum number of clients that can be placed on the waitlist for this service. `null` if waitlist is disabled, the waitlist has no capacity limit, or for asset bookings.'
                      is_waitlist:
                        type: boolean
                        description: 'Whether list of available times contains slots with only waitlist booking available.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Schedule-NextAvailableDay
  '/Wl/Appointment/Book/Purchase/Purchase.json':
    get:
      summary: 'Retrieves a list of information about available purchase options. Returns all Purchase Options (memberships, packages, passes) the given client can use to pay for the specified appointment at the given location, including pricing details and eligibility status. Also returns options that can be purchased on the spot during booking.'
      parameters:
        - name: a_service
          in: query
          description: 'List of selected services without current {@link PurchaseApi::$k_service}. The list of these services directly affects the list of selected promotions. Depending on the number and order of services, there may be different results. The current {@link PurchaseApi::$k_service} will be added to the end of this list. It is worth considering this list as a list of previously selected services. Each element has the following structure: <dl> <dt>array `a_purchase`</dt> <dd> List of purchase options selected for the service. Should be set if a new purchase option is selected for this service. <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. Constant from {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>Purchase item key.</dd> <dt>bool `is_purchase_previous`</dt> <dd> Should be `true` if this promotion is selected again, i.e. one that has already been applied to another appointment. This is needed to correctly calculate the remaining quantity of promotions with limited uses. </dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>Local date/time to check purchase options expiration.</dd> <dt>string|null `k_login_prize`</dt> <dd> Login prize key. <i>Primary key from {@link \RsLoginPrizeSql}.</i> `null` if no login prize used to pay for this service. </dd> <dt>string|null `k_login_promotion`</dt> <dd>Login promotion key. Should be set if login promotion selected for this service.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>See table {@link \RsServiceSql}.</i></dd> <dt>string `k_timezone`</dt> <dd> The timezone key for `dt_date` field. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link \Wl\Profile\Timezone\ProfileTimezone::createInBusiness()}.</i> In any case, the timezone will be used if the business allows client timezones. </dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to use to check for expiration of Purchase Options.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The asset booking duration.'
          schema:
            type: number
            format: int32
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` - get all Purchase Options suitable for appointment. `false` - get only Purchase Options available for the client.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'The resource key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used to select available Purchase Options. If multiple services are selected, they should be specified in {@link PurchaseApi::$a_service} array.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'The timezone key for {@link PurchaseApi::$dt_date}. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link ProfileTimezone::createInBusiness()}</i> In any case, the timezone will be used if the business allows client timezones.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option. It contains the following information: <dl> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The count of the remaining visits.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in this Purchase Option.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the Purchase Option. For example, four per week.</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period. For example "this week" or "for a four-day period".</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods. Given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The count of future sessions that are paid with this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The usage count of the Purchase Option.</dd> <dt>int `i_visit_past`</dt> <dd> The count of attended sessions before the last renewal. This will be ''0'' if no sessions before the last renewal or if the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period. For example, "this week" or "for a four-day period".</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for promotions. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_share`</dt> <dd> `true` if this purchase option is shared from another user. `false` if this purchase option belongs to the user for whom the request is made. </dd> <dt>string `k_login_promotion`</dt> <dd>The Purchase Option login key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd> If this Purchase Option is a package, then this field contains a list of Purchase Options contained in the package. </dd> <dt>string `uid_owner`</dt> <dd> The user key of the owner of the promotion. Will be different from the user for whom the request is made if the promotion is shared. </dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'An array with information about available Purchase Options. <dl> <dt>array `a_image`</dt> <dd> Information describing the logo of the purchase option. This value can be false if there is no logo described. Image information will have the following fields: <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to `false` value returned in `url-thumbnail` equals value in `url-view`. </dd> <dt>string `url-view`</dt> <dd>Url to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger than specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise, link to original image is returned here. </dd> </dl> </dd> <dt>array `a_payment`</dt> <dd> The set of calculated values for payment: <dl> <dt>string `m_discount`</dt> <dd>The amount of the whole discount of one purchase item.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>A list of calendar restrictions of the Purchase Option in a human-readable format, for example: ''4 per week''.</dd> <dt>string `dt_expire`</dt> <dd>Date, when promotion expires.</dd> <dt>string `dt_start`</dt> <dd>Date, when promotion starts.</dd> <dt>string `f_price`</dt> <dd>The price of the Purchase Option.</dd> <dt>int `i`</dt> <dd>Order number of the purchase option in the list.</dd> <dt>int `i_limit`</dt> <dd>Count of visits that purchase option allows to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>Maximum number of minutes that current promotion can be used.</dd> <dt>int `i_payment_period`</dt> <dd>Count of calendar periods (weeks, months, years) between payment for membership.</dd> <dt>int `id_duration`</dt> <dd>Duration ID. Constant from {@link \ADurationSid}.</dd> <dt>int `id_program`</dt> <dd>Program ID for promotions from {@link \RsProgramSid}.</dd> <dt>int `id_program_type`</dt> <dd>Program type ID. Constant from {@link \RsProgramTypeSid}.</dd> <dt>int `id_promotion_price`</dt> <dd>How the Purchase Item price is specified. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>int `id_purchase_item`</dt> <dd>ID of the purchase item from {@link \RsPurchaseItemSid}</dd> <dt>bool `is_contract`</dt> <dd>This will be `true` if the Purchase Option is a contract. It will `false` otherwise.</dd> <dt>bool `is_description`</dt> <dd>`true` if purchase option has description.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if promotion is introductory offer, `false` otherwise.</dd> <dt>bool `is_renew`</dt> <dd>This will be `true` if the Purchase Option will auto-renew. It will be `false` otherwise.</dd> <dt>bool `is_renew_check`</dt> <dd>Whether Purchase Option is renewable and the "auto-renew" option should be turned on by default.</dd> <dt>bool `is_start`</dt> <dd> This will be `true` if the Purchase Option has a duration that begins on purchase. It will be `false` otherwise. </dd> <dt>string `k_id`</dt> <dd>Primary ID of the element in it''s table.</dd> <dt>string|null `m_price_old`</dt> <dd> Price of single session purchase before online discount. `null` if service does not have online discount. Is set only if this purchase option is purchase of single visit. </dd> <dt>string `s_activation`</dt> <dd>Activation settings of the promotion.</dd> <dt>string `s_class`</dt> <dd>Class for designer to mark purchase options with different icons.</dd> <dt>string `s_class_include`</dt> <dd>List of services included in the promotion.</dd> <dt>string `s_description`</dt> <dd>Description of the purchase option.</dd> <dt>string `s_duration`</dt> <dd>Duration of the promotion.</dd> <dt>string `s_payment_duration`</dt> <dd>Period between payments for memberships.</dd> <dt>string `sid_program_category`</dt> <dd>Category of the program for promotions from {@link \RsProgramCategorySid}.</dd> <dt>string `s_title`</dt> <dd>Name of the purchase option.</dd> <dt>string `s_value`</dt> <dd>Key of the purchase option in the format [`purchase_item_id`]::[`k_id`]</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package. This field contains list of promotions contained in the package.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. <dl> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'Session pass information in a case if user books same appointment second time and already has Drop-in. <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if drop-in rate should be the default purchase option.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
                      k_promotion_default:
                        type: string
                        description: 'Default promotion key. Empty if the appointment has no default promotion.'
                      text_login_promotion:
                        type: string
                        description: 'Login promotion title suitable to pay for the services.'
      tags:
        - Purchase-Purchase
  '/Wl/Appointment/Book/Purchase/Purchase72.json':
    get:
      summary: 'Retrieves a list of information about available purchase options. Extends {@link PurchaseApi::get()} to also support rescheduling an existing appointment by accepting an optional appointment key. When a key is provided, the existing appointment is validated and its current Purchase Option usage is taken into account when calculating eligibility for available options.'
      parameters:
        - name: a_service
          in: query
          description: 'List of selected services without current {@link PurchaseApi::$k_service}. The list of these services directly affects the list of selected promotions. Depending on the number and order of services, there may be different results. The current {@link PurchaseApi::$k_service} will be added to the end of this list. It is worth considering this list as a list of previously selected services. Each element has the following structure: <dl> <dt>array `a_purchase`</dt> <dd> List of purchase options selected for the service. Should be set if a new purchase option is selected for this service. <dl> <dt>int `id_purchase_item`</dt> <dd>Purchase item ID. Constant from {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>Purchase item key.</dd> <dt>bool `is_purchase_previous`</dt> <dd> Should be `true` if this promotion is selected again, i.e. one that has already been applied to another appointment. This is needed to correctly calculate the remaining quantity of promotions with limited uses. </dd> </dl> </dd> <dt>string `dt_date`</dt> <dd>Local date/time to check purchase options expiration.</dd> <dt>string|null `k_login_prize`</dt> <dd> Login prize key. <i>Primary key from {@link \RsLoginPrizeSql}.</i> `null` if no login prize used to pay for this service. </dd> <dt>string|null `k_login_promotion`</dt> <dd>Login promotion key. Should be set if login promotion selected for this service.</dd> <dt>string `k_service`</dt> <dd>Service key. <i>See table {@link \RsServiceSql}.</i></dd> <dt>string `k_timezone`</dt> <dd> The timezone key for `dt_date` field. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link \Wl\Profile\Timezone\ProfileTimezone::createInBusiness()}.</i> In any case, the timezone will be used if the business allows client timezones. </dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'The date to use to check for expiration of Purchase Options.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'The asset booking duration.'
          schema:
            type: number
            format: int32
        - name: i_height
          in: query
          description: 'Image height in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: i_width
          in: query
          description: 'Image width in pixels. Please specify this value if you need image to be returned in specific size. In case this value is not specified returned image will have default thumbnail size.'
          schema:
            type: [number, "null"]
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` - get all Purchase Options suitable for appointment. `false` - get only Purchase Options available for the client.'
          schema:
            type: boolean
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Appointment key. <i>Primary key in {@link \RsAppointmentSql} table.</i> Not empty in case when we return payment options for rescheduling existing appointment.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'The resource key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The service key used to select available Purchase Options. If multiple services are selected, they should be specified in {@link PurchaseApi::$a_service} array.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'The timezone key for {@link PurchaseApi::$dt_date}. Can be `null` if timezone is not selected. If not selected, the default client timezone will be used. <i>See {@link ProfileTimezone::createInBusiness()}</i> In any case, the timezone will be used if the business allows client timezones.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option. It contains the following information: <dl> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The count of the remaining visits.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in this Purchase Option.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the Purchase Option. For example, four per week.</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period. For example "this week" or "for a four-day period".</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods. Given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The count of future sessions that are paid with this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The usage count of the Purchase Option.</dd> <dt>int `i_visit_past`</dt> <dd> The count of attended sessions before the last renewal. This will be ''0'' if no sessions before the last renewal or if the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period. For example, "this week" or "for a four-day period".</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The count of visits that the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes that current Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for promotions. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_share`</dt> <dd> `true` if this purchase option is shared from another user. `false` if this purchase option belongs to the user for whom the request is made. </dd> <dt>string `k_login_promotion`</dt> <dd>The Purchase Option login key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd> If this Purchase Option is a package, then this field contains a list of Purchase Options contained in the package. </dd> <dt>string `uid_owner`</dt> <dd> The user key of the owner of the promotion. Will be different from the user for whom the request is made if the promotion is shared. </dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'An array with information about available Purchase Options. <dl> <dt>array `a_image`</dt> <dd> Information describing the logo of the purchase option. This value can be false if there is no logo described. Image information will have the following fields: <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If set to `false` value returned in `url-thumbnail` equals value in `url-view`. </dd> <dt>string `url-view`</dt> <dd>Url to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> Url to resized and rotated image in file storage. If size of original image is larger than specified by arguments, image thumbnail as created, and a link to this thumbnail is returned. Otherwise, link to original image is returned here. </dd> </dl> </dd> <dt>array `a_payment`</dt> <dd> The set of calculated values for payment: <dl> <dt>string `m_discount`</dt> <dd>The amount of the whole discount of one purchase item.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> </dl> </dd> <dt>string[] `a_visit_limit`</dt> <dd>A list of calendar restrictions of the Purchase Option in a human-readable format, for example: ''4 per week''.</dd> <dt>string `dt_expire`</dt> <dd>Date, when promotion expires.</dd> <dt>string `dt_start`</dt> <dd>Date, when promotion starts.</dd> <dt>string `f_price`</dt> <dd>The price of the Purchase Option.</dd> <dt>int `i`</dt> <dd>Order number of the purchase option in the list.</dd> <dt>int `i_limit`</dt> <dd>Count of visits that purchase option allows to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>Maximum number of minutes that current promotion can be used.</dd> <dt>int `i_payment_period`</dt> <dd>Count of calendar periods (weeks, months, years) between payment for membership.</dd> <dt>int `id_duration`</dt> <dd>Duration ID. Constant from {@link \ADurationSid}.</dd> <dt>int `id_program`</dt> <dd>Program ID for promotions from {@link \RsProgramSid}.</dd> <dt>int `id_program_type`</dt> <dd>Program type ID. Constant from {@link \RsProgramTypeSid}.</dd> <dt>int `id_promotion_price`</dt> <dd>How the Purchase Item price is specified. One of the {@link \RsProgramTypeSid} constants.</dd> <dt>int `id_purchase_item`</dt> <dd>ID of the purchase item from {@link \RsPurchaseItemSid}</dd> <dt>bool `is_contract`</dt> <dd>This will be `true` if the Purchase Option is a contract. It will `false` otherwise.</dd> <dt>bool `is_description`</dt> <dd>`true` if purchase option has description.</dd> <dt>bool `is_introductory`</dt> <dd>`true` if promotion is introductory offer, `false` otherwise.</dd> <dt>bool `is_renew`</dt> <dd>This will be `true` if the Purchase Option will auto-renew. It will be `false` otherwise.</dd> <dt>bool `is_renew_check`</dt> <dd>Whether Purchase Option is renewable and the "auto-renew" option should be turned on by default.</dd> <dt>bool `is_start`</dt> <dd> This will be `true` if the Purchase Option has a duration that begins on purchase. It will be `false` otherwise. </dd> <dt>string `k_id`</dt> <dd>Primary ID of the element in it''s table.</dd> <dt>string|null `m_price_old`</dt> <dd> Price of single session purchase before online discount. `null` if service does not have online discount. Is set only if this purchase option is purchase of single visit. </dd> <dt>string `s_activation`</dt> <dd>Activation settings of the promotion.</dd> <dt>string `s_class`</dt> <dd>Class for designer to mark purchase options with different icons.</dd> <dt>string `s_class_include`</dt> <dd>List of services included in the promotion.</dd> <dt>string `s_description`</dt> <dd>Description of the purchase option.</dd> <dt>string `s_duration`</dt> <dd>Duration of the promotion.</dd> <dt>string `s_payment_duration`</dt> <dd>Period between payments for memberships.</dd> <dt>string `sid_program_category`</dt> <dd>Category of the program for promotions from {@link \RsProgramCategorySid}.</dd> <dt>string `s_title`</dt> <dd>Name of the purchase option.</dd> <dt>string `s_value`</dt> <dd>Key of the purchase option in the format [`purchase_item_id`]::[`k_id`]</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package. This field contains list of promotions contained in the package.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. <dl> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `text_description`</dt> <dd>User friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'Session pass information in a case if user books same appointment second time and already has Drop-in. <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if drop-in rate should be the default purchase option.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      k_login_promotion:
                        type: [string, "null"]
                        description: 'The Purchase Option ID used to pay for the appointment. This will be `null` if the client doesn''t have a suitable Purchase Option.'
                      k_promotion_default:
                        type: string
                        description: 'Default promotion key. Empty if the appointment has no default promotion.'
                      text_login_promotion:
                        type: string
                        description: 'Login promotion title suitable to pay for the services.'
      tags:
        - Purchase-Purchase72
  '/Wl/Appointment/Book/Staff/List.json':
    get:
      summary: 'Retrieves an information about staff members for the current service. Returns the list of staff members who can perform the specified service at the given location on the specified date and time. Each entry includes the staff member''s name, image, gender, and availability. An ''any staff'' option is included when the service allows random staff assignment.'
      parameters:
        - name: dt_date
          in: query
          description: 'The date/time of the appointment selected by user, in the location''s time zone.'
          schema:
            type: string
        - name: i_duration_custom
          in: query
          description: 'Custom appointment duration in minutes. `null` if not set.'
          schema:
            type: [number, "null"]
        - name: id_role
          in: query
          description: 'User role by whom this api called. For different roles different results might be generated.'
          schema:
            type: number
            format: int32
        - name: is_unavailable
          in: query
          description: '`true` - returns service categories that have no staff members available to conduct them. `false` - returns only service categories that have staff members available to conduct them.'
          schema:
            type: boolean
        - name: k_appointment_ignore
          in: query
          description: 'Key of appointment which must be ignored when searches available staff.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The key of a service for which to show information.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'User''s timezone. `null` until initialized or to use location timezone.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'The user key for whom the service is booking. `null` when not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'A list of staff members with information about them. <dl> <dt>int `id_gender`</dt> <dd>Staff member''s gender. One of {@link \AGenderSid} constants.</dd> <dt>bool `is_available`</dt> <dd> Whether staff member is available for booking. Note, if staff member reached daily limits, this field will be different for client and staff booking flows. If client books, such staff member is not available and this field is `false`. If staff member books, such staff member is available. </dd> <dt>bool `is_daily_limit`</dt> <dd>Whether staff member reached daily limits on number or total duration of the appointments for one day.</dd> <dt>bool `is_wait_list`</dt> <dd>Whether staff member available only for wait list booking.</dd> <dt>string `k_staff`</dt> <dd>@deprecated Legacy staff key. <i>Primary key in {@link \RsStaffSql}.</i> Returned only for allow-listed apps.</dd> <dt>string `s_position`</dt> <dd>Position of the staff member in the business.</dd> <dt>string `s_staff`</dt> <dd>Name of the staff member.</dd> <dt>string `uid`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql}.</i></dd> <dt>string `xml_biography`</dt> <dd>Biography of the staff member.</dd> </dl>'
                      can_book_unavailable_staff:
                        type: boolean
                        description: 'Can staff booked unavailable staff.'
                      has_gender:
                        type: boolean
                        description: 'Determines whether to select the staff member''s gender for the appointment.'
                      has_staff:
                        type: boolean
                        description: 'Determines whether to select staff member(s) for the appointment.'
                      is_gender_different:
                        type: boolean
                        description: 'Determines if the staff list has male and female members.'
      tags:
        - Staff-List
  '/Wl/Appointment/Book/Payment/Payment.json':
    get:
      summary: 'Calculates pricing information for an appointment booking, including taxes, discounts, surcharges, and totals. Returns the full breakdown of amounts owed for the booking, including available Purchase Options, applicable discount codes, surcharge amounts, and the final total. Intended to be called before submitting payment so the client side can display a pricing summary to the user.'
      parameters:
        - name: a_book_data
          in: query
          description: 'Information detailing an appointment booking: <dl> <dt>int[] `a_product`</dt> <dd>Add-on list. Keys are add-on option keys, values are quantities.</dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>bool `is_pay_later`</dt> <dd>`true` if the client wants to pay upon their visit, `false` if paying now.</dd> <dt>bool `is_purchase_previous`</dt> <dd>`true` if a previously purchased option is used for this booking.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if the client is placed on a wait list without paying.</dd> <dt>int `i_duration`</dt> <dd>Asset booking duration in minutes. Used only for asset bookings.</dd> <dt>string `k_login_prize`</dt> <dd>Login prize key used to pay for the booking. Empty if no prize is used.</dd> <dt>string `k_login_promotion`</dt> <dd>Key of an already-purchased promotion used for this booking. Empty if not applicable.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Used only for asset bookings.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key used to redeem a free item. Empty if not applicable.</dd> <dt>string `k_service`</dt> <dd>Service key. Used only for appointment bookings.</dd> <dt>string `k_session_pass`</dt> <dd>Drop-in key. Used when booking via a drop-in pass.</dd> <dt>string `m_tip_appointment`</dt> <dd>Tip amount for the appointment.</dd> <dt>string `s_signature`</dt> <dd>Client signature for a contract. Required when the purchase option has a contract.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. Currently used only with {@link PaymentApi::$k_session_pass} variable.'
          schema:
            type: [string, "null"]
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'Session pass key.'
          schema:
            type: string
        - name: m_variable_price
          in: query
          description: 'Variable price. Is set only during booking an appointment with variable type of the price {@link \RsServicePriceSid::VARIES} from spa backend {@link ModeSid::SPA_BACKEND}.'
          schema:
            type: string
        - name: s_unique_key
          in: query
          description: 'Service unique key. Used for model cache.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion_data:
                        type: array
                        description: 'Information about selected Purchase Options. <dl> <dt>int `i_limit`</dt> <dd>The limit of total visits.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits.</dd> <dt>string `s_expire`</dt> <dd>The expiration date.</dd> <dt>string `s_title`</dt> <dd>The title of the Purchase Option.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'Information about selected purchase items. Indexes are strings in the format `id_purchase_item-k_id`. Values are an array with the following structure: <dl> <dt>array `a_tax`</dt> <dd> Contains information about taxes in the following format. A list of taxes to apply. The array keys are `k_tax` keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. A constant of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> <dt>string `m_pay`</dt> <dd>The payment for the Purchase Option or single visit without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the Purchase Option or single visit.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      m_coupon:
                        type: string
                        description: 'Gift card amount.'
                      m_discount:
                        type: string
                        description: 'Discount amount.'
                      m_surcharge:
                        type: string
                        description: 'Surcharge amount calculated for credit cards (Virtual Terminal and Card Swiper).'
                      m_surcharge_ach:
                        type: string
                        description: 'Surcharge amount calculated for money transfers from account: ACH, Direct Entry.'
                      m_tax:
                        type: string
                        description: 'The tax of service.'
                      m_total:
                        type: string
                        description: 'The total cost of the purchase.'
      tags:
        - Payment-Payment
    post:
      summary: 'Allows to pay an appointment or appointment purchase option for the client. Accepts payment method and booking data, charges the client for the appointment or the selected Purchase Option, and records the transaction. Requires the client to be authenticated and the booking data to include a valid service or asset key.'
      parameters:
        - name: a_book_data
          in: query
          description: 'Information detailing an appointment booking: <dl> <dt>int[] `a_product`</dt> <dd>Add-on list. Keys are add-on option keys, values are quantities.</dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>bool `is_pay_later`</dt> <dd>`true` if the client wants to pay upon their visit, `false` if paying now.</dd> <dt>bool `is_purchase_previous`</dt> <dd>`true` if a previously purchased option is used for this booking.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if the client is placed on a wait list without paying.</dd> <dt>int `i_duration`</dt> <dd>Asset booking duration in minutes. Used only for asset bookings.</dd> <dt>string `k_login_prize`</dt> <dd>Login prize key used to pay for the booking. Empty if no prize is used.</dd> <dt>string `k_login_promotion`</dt> <dd>Key of an already-purchased promotion used for this booking. Empty if not applicable.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Used only for asset bookings.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key used to redeem a free item. Empty if not applicable.</dd> <dt>string `k_service`</dt> <dd>Service key. Used only for appointment bookings.</dd> <dt>string `k_session_pass`</dt> <dd>Drop-in key. Used when booking via a drop-in pass.</dd> <dt>string `m_tip_appointment`</dt> <dd>Tip amount for the appointment.</dd> <dt>string `s_signature`</dt> <dd>Client signature for a contract. Required when the purchase option has a contract.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary keys in the {@link \Core\Quiz\QuizSql} table.</i> Values are quiz response keys. <i>Primary keys in the {@link \Core\Quiz\Response\ResponseSql} table.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item:
                        type: [array, "null"]
                        description: 'The purchase item keys from the database. This will be `null` if not set yet.'
                      id_pay:
                        type: number
                        format: int32
                        description: 'The payment type for the appointment. A constant of {@link \RsAppointmentPaySid}.'
                      k_login_activity_purchase:
                        type: string
                        description: 'The key of activity of the purchase made. Empty if no purchase has been made.'
                      k_login_prize:
                        type: string
                        description: 'Login prize key. In case when appointment paid by reward prize, there is the key of redeemed login prize. Empty otherwise.'
      tags:
        - Payment-Payment
  '/Wl/Appointment/Book/Payment/PaymentMultiple.json':
    get:
      summary: 'Calculates pricing information for a batch of appointment bookings, including taxes, discounts, surcharges, and per-provider totals. Returns the full pricing breakdown for all providers specified in the booking data, including available Purchase Options, applicable discount codes, surcharge amounts, and the final total for each provider. Intended to be called before submitting payment so the client side can display a pricing summary.'
      parameters:
        - name: a_book_data
          in: query
          description: 'The booking process information: Set this field value for GET requests. <dl> <dt>array[] `a_provider`</dt> <dd> The batch of appointments to be booked. Each element has the next values: <dl> <dt>array `a_product`</dt> <dd> The add-on list. Keys are add-on option keys. <i>Primary keys in the {@link \RsShopProductOptionSql} table</i> Values are quantities: <dl> <dt>string `k_shop_product_option`</dt> <dd> Add-on option key (used as array key). <i>Primary key in the {@link \RsShopProductOptionSql} table.</i> </dd> <dt>int `i_count`</dt> <dd>Quantity of the add-on to purchase (used as array value).</dd> </dl> </dd> <dt>int `i_duration`</dt> <dd>The asset duration in minutes. This won''t be empty for asset bookings.</dd> <dt>int `id_purchase_item`</dt> <dd> The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants. This won''t be empty for new options purchased. </dd> <dt>bool `is_pay_later`</dt> <dd> This will be <tt>true</tt> if the customer wants to pay upon their visit. Otherwise, this will be <tt>false</tt> if the user wants to pay now. </dd> <dt>bool `is_purchase_previous`</dt> <dd> This will be <tt>true</tt> if the Purchase Option that was selected for another appointment from the batch must be used for this appointment. Otherwise, this will be <tt>false</tt>. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd> This will be <tt>true</tt> if the customer is booked into the wait list and doesn''t have to pay. Otherwise, this will be <tt>false</tt> if the customer is booking to an active list (or if the wait list booking should be paid now). </dd> <dt>string `k_id`</dt> <dd>The key of the purchase item. This won''t be empty for new options purchased.</dd> <dt>string `k_login_prize`</dt> <dd> The key of the customer''s prize used to pay for the booking. This won''t be empty for bookings made using prizes. </dd> <dt>string `k_login_promotion`</dt> <dd> The key of the Purchase Option. This won''t be empty when using a Purchase Option that''s already been purchased. </dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset booking. This is only used for asset bookings.</dd> <dt>string `k_service`</dt> <dd>The key of the appointment booking service. This is only used for appointment bookings.</dd> <dt>string `s_signature`</dt> <dd> The signature for the Purchase Option contract. The data is returned from the canvas HTML element or the signature pad. This won''t be empty if the Purchase Option requires contract signing. </dd> <dt>string `uid`</dt> <dd> User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i> Specify only in a case of booking for a lof of different users. </dd> </dl> </dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>string `m_tip_appointment`</dt> <dd>The tips amount.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The ID of the source mode. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'The gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion_data:
                        type: array
                        description: 'Information about the selected login promotion. <dl> <dt>int `i_limit`</dt> <dd>The visit count limit of the promotion.</dd> <dt>int `i_remain`</dt> <dd>The count of remaining visits.</dd> <dt>string `s_expire`</dt> <dd>The expiration date.</dd> <dt>string `s_title`</dt> <dd>The title of the promotion.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'Fields refer to strings in the format <tt>id_purchase_item-k_id</tt>. Values refer to an array with the next stricture: <dl> <dt>array `a_tax`</dt> <dd> A list of taxes to apply containing information about taxes. The array keys are <tt>k_tax</tt> keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_pay`</dt> <dd>The payment for the promotion (or single visit) without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the promotion (or single visit).</dd> </dl>'
                      a_total:
                        type: array
                        description: 'The list of amounts to pay for appointments from the batch, with taxes and without surcharges.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      m_coupon:
                        type: string
                        description: 'The gift card amount.'
                      m_discount:
                        type: string
                        description: 'The discount amount.'
                      m_surcharge:
                        type: string
                        description: 'Surcharge amount calculated for credit cards (Virtual Terminal and Card Swiper).'
                      m_surcharge_ach:
                        type: string
                        description: 'Surcharge amount calculated for money transfers from account: ACH, Direct Entry.'
                      m_tax:
                        type: string
                        description: 'The amount of tax to pay.'
                      m_total:
                        type: string
                        description: 'The price of the service, with taxes and without surcharges.'
      tags:
        - Payment-PaymentMultiple
    post:
      summary: 'Allows to pay an appointment or appointment purchase option for the client. Accepts payment method and multi-provider booking data, charges the client for all appointments or selected Purchase Options in the batch, and records the transactions. Requires the client to be authenticated and each provider entry to include a valid service or asset key with date.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The ID of the source mode. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'The gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_book_data_post:
                  type: array
                  description: 'A copy of {@link PaymentMultipleApi::$a_book_data}. Has the same structure. Set this field value for POST requests. <dl> <dt>array[] `a_provider`</dt> <dd> The batch of appointments to be booked. Each element has the next values: <dl> <dt>array `a_product`</dt> <dd> The add-on list. Keys are add-on option keys. <i>Primary keys in the {@link \RsShopProductOptionSql} table</i> Values are quantities: <dl> <dt>string `k_shop_product_option`</dt> <dd> Add-on option key (used as array key). <i>Primary key in the {@link \RsShopProductOptionSql} table.</i> </dd> <dt>int `i_count`</dt> <dd>Quantity of the add-on to purchase (used as array value).</dd> </dl> </dd> <dt>int `i_duration`</dt> <dd>The asset duration in minutes. This won''t be empty for asset bookings.</dd> <dt>int `id_purchase_item`</dt> <dd> The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants. This won''t be empty for new options purchased. </dd> <dt>bool `is_pay_later`</dt> <dd> This will be <tt>true</tt> if the customer wants to pay upon their visit. Otherwise, this will be <tt>false</tt> if the user wants to pay now. </dd> <dt>bool `is_purchase_previous`</dt> <dd> This will be <tt>true</tt> if the Purchase Option that was selected for another appointment from the batch must be used for this appointment. Otherwise, this will be <tt>false</tt>. </dd> <dt>bool `is_wait_list_unpaid`</dt> <dd> This will be <tt>true</tt> if the customer is booked into the wait list and doesn''t have to pay. Otherwise, this will be <tt>false</tt> if the customer is booking to an active list (or if the wait list booking should be paid now). </dd> <dt>string `k_id`</dt> <dd>The key of the purchase item. This won''t be empty for new options purchased.</dd> <dt>string `k_login_prize`</dt> <dd> The key of the customer''s prize used to pay for the booking. This won''t be empty for bookings made using prizes. </dd> <dt>string `k_login_promotion`</dt> <dd> The key of the Purchase Option. This won''t be empty when using a Purchase Option that''s already been purchased. </dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset booking. This is only used for asset bookings.</dd> <dt>string `k_service`</dt> <dd>The key of the appointment booking service. This is only used for appointment bookings.</dd> <dt>string `s_signature`</dt> <dd> The signature for the Purchase Option contract. The data is returned from the canvas HTML element or the signature pad. This won''t be empty if the Purchase Option requires contract signing. </dd> <dt>string `uid`</dt> <dd> User''s key. <i>Primary key in {@link \PassportLoginSql} table.</i> Specify only in a case of booking for a lof of different users. </dd> </dl> </dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of the {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>string `m_tip_appointment`</dt> <dd>The tips amount.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary keys in the {@link \Core\Quiz\QuizSql} table.</i> Values are quiz response keys. <i>Primary keys in the {@link \Core\Quiz\Response\ResponseSql} table.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'The list of redeemed prizes.'
                      a_pay:
                        type: array
                        description: 'The payment type for the appointment. One of the {@link \RsAppointmentPaySid} constants.'
                      a_purchase_item:
                        type: [object, "null"]
                        description: 'The keys of purchased items. The first level of the array is the list of appointments from the batch. The second level of the array is the list of items purchased for this appointment.'
                      k_login_activity_purchase:
                        type: string
                        description: 'Key of the activity for the purchase made. This will be empty if no purchase has been made.'
      tags:
        - Payment-PaymentMultiple
  '/Wl/Appointment/Book/Payment/PaymentPost.json':
    get:
      summary: 'Calculates pricing information for an appointment booking, including taxes, discounts, surcharges, and totals. Returns the full breakdown of amounts owed for the booking, including available Purchase Options, applicable discount codes, surcharge amounts, and the final total. Intended to be called before submitting payment so the client side can display a pricing summary to the user.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. Currently used only with {@link PaymentApi::$k_session_pass} variable.'
          schema:
            type: [string, "null"]
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
        - name: k_session_pass
          in: query
          description: 'Session pass key.'
          schema:
            type: string
        - name: m_variable_price
          in: query
          description: 'Variable price. Is set only during booking an appointment with variable type of the price {@link \RsServicePriceSid::VARIES} from spa backend {@link ModeSid::SPA_BACKEND}.'
          schema:
            type: string
        - name: s_unique_key
          in: query
          description: 'Service unique key. Used for model cache.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion_data:
                        type: array
                        description: 'Information about selected Purchase Options. <dl> <dt>int `i_limit`</dt> <dd>The limit of total visits.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits.</dd> <dt>string `s_expire`</dt> <dd>The expiration date.</dd> <dt>string `s_title`</dt> <dd>The title of the Purchase Option.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'Information about selected purchase items. Indexes are strings in the format `id_purchase_item-k_id`. Values are an array with the following structure: <dl> <dt>array `a_tax`</dt> <dd> Contains information about taxes in the following format. A list of taxes to apply. The array keys are `k_tax` keys. Each element contains the following fields: <dl> <dt>string `m_tax`</dt> <dd>The tax rate.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl> </dd> <dt>int `id_purchase_item`</dt> <dd>The purchase item ID. A constant of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount`</dt> <dd>The value of the discount used for the purchase.</dd> <dt>string `m_discount_login`</dt> <dd>The discount amount for the client type of one purchase item.</dd> <dt>string `m_pay`</dt> <dd>The payment for the Purchase Option or single visit without taxes.</dd> <dt>string `m_price`</dt> <dd>The price of the Purchase Option or single visit.</dd> </dl>'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
                      m_coupon:
                        type: string
                        description: 'Gift card amount.'
                      m_discount:
                        type: string
                        description: 'Discount amount.'
                      m_surcharge:
                        type: string
                        description: 'Surcharge amount calculated for credit cards (Virtual Terminal and Card Swiper).'
                      m_surcharge_ach:
                        type: string
                        description: 'Surcharge amount calculated for money transfers from account: ACH, Direct Entry.'
                      m_tax:
                        type: string
                        description: 'The tax of service.'
                      m_total:
                        type: string
                        description: 'The total cost of the purchase.'
      tags:
        - Payment-PaymentPost
    post:
      summary: 'Allows to pay an appointment or appointment purchase option for the client. Accepts payment method and booking data, charges the client for the appointment or the selected Purchase Option, and records the transaction. Requires the client to be authenticated and the booking data to include a valid service or asset key.'
      parameters:
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: id_mode
          in: query
          description: 'The key of source mode. A constant of {@link ModeSid}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The purchase item ID. A constant of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_id
          in: query
          description: 'The item key. Depends on {@link PaymentApi::$id_purchase_item} property.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: text_coupon_code
          in: query
          description: 'Gift card code.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code to be applied to the purchase.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_book_data:
                  type: array
                  description: 'Information detailing an appointment booking. Has the same structure as {@link PaymentApi::$a_book_data}. <dl> <dt>int[] `a_product`</dt> <dd>Add-on list. Keys are add-on option keys, values are quantities.</dd> <dt>string `dl_client_prorate`</dt> <dd>Client prorate date. Used when the purchased promotion is prorated.</dd> <dt>int `id_class_tab`</dt> <dd>The "Book now" tab. One of {@link \Wl\Classes\Tab\TabSid} constants.</dd> <dt>bool `is_pay_later`</dt> <dd>`true` if the client wants to pay upon their visit, `false` if paying now.</dd> <dt>bool `is_purchase_previous`</dt> <dd>`true` if a previously purchased option is used for this booking.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>`true` if the client is placed on a wait list without paying.</dd> <dt>int `i_duration`</dt> <dd>Asset booking duration in minutes. Used only for asset bookings.</dd> <dt>string `k_login_prize`</dt> <dd>Login prize key used to pay for the booking. Empty if no prize is used.</dd> <dt>string `k_login_promotion`</dt> <dd>Key of an already-purchased promotion used for this booking. Empty if not applicable.</dd> <dt>string `k_resource`</dt> <dd>Asset key. Used only for asset bookings.</dd> <dt>string `k_reward_prize`</dt> <dd>Reward prize key used to redeem a free item. Empty if not applicable.</dd> <dt>string `k_service`</dt> <dd>Service key. Used only for appointment bookings.</dd> <dt>string `k_session_pass`</dt> <dd>Drop-in key. Used when booking via a drop-in pass.</dd> <dt>string `m_tip_appointment`</dt> <dd>Tip amount for the appointment.</dd> <dt>string `s_signature`</dt> <dd>Client signature for a contract. Required when the purchase option has a contract.</dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary keys in the {@link \Core\Quiz\QuizSql} table.</i> Values are quiz response keys. <i>Primary keys in the {@link \Core\Quiz\Response\ResponseSql} table.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item:
                        type: [array, "null"]
                        description: 'The purchase item keys from the database. This will be `null` if not set yet.'
                      id_pay:
                        type: number
                        format: int32
                        description: 'The payment type for the appointment. A constant of {@link \RsAppointmentPaySid}.'
                      k_login_activity_purchase:
                        type: string
                        description: 'The key of activity of the purchase made. Empty if no purchase has been made.'
                      k_login_prize:
                        type: string
                        description: 'Login prize key. In case when appointment paid by reward prize, there is the key of redeemed login prize. Empty otherwise.'
      tags:
        - Payment-PaymentPost
  '/Wl/Appointment/Book/Product/Product62.json':
    get:
      summary: 'Retrieves list of available service add-ons. Returns active add-on products for the specified service and location, filtered by the given client''s login type when a UID is provided. Products with a purchase restriction that does not match the client type are excluded from the result.'
      parameters:
        - name: k_location
          in: query
          description: 'The key of a location where appointment is going to be booked.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The key of a service to show information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user to show information for. Products with a {@link PurchaseRestrictionSid::TYPE} restriction will be filtered out. Only those products that match the client type or those that have no restrictions will be shown. `null` if the client is not logged in. If you don''t need to filter products by client type/group, use {@link ProductApi}.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'A list service add-ons. This will be `null` if not set yet. <dl> <dt>array `a_logo`</dt> <dd> Information about first image connected to the product. <i>Result from {@link \RsShopProductImageList::imageOne()}.</i> <dl> <dt>int `i_height`</dt> <dd>Height of the image.</dd> <dt>int `i_width`</dt> <dd>Width of the image.</dd> <dt>bool `is_empty`</dt> <dd>`true` mean mock instead of real image.</dd> <dt>string `s_url`</dt> <dd>URL on the image.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Description of the product as prepared html code.</dd> <dt>int `i_duration`</dt> <dd>Duration that will be added to the appointment with this addon as count of minutes.</dd> <dt>bool `is_client_duration`</dt> <dd>`true` means that the duration information will be displayed for the client.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if client can purchase product, `false` if only staff can perform purchase.</dd> <dt>bool `is_amount`</dt> <dd>`true` means that addon increases price of the appointment.</dd> <dt>string `k_currency`</dt> <dd>ID of the currency.</dd> <dt>string `k_shop_product`</dt> <dd>ID of the product.</dd> <dt>string `k_shop_product_option`</dt> <dd> ID of the product option. Zero means that product has multiple options and option should be selected by client. </dd> <dt>string `m_amount`</dt> <dd>Amount that will be added to the appointment price with this addon.</dd> <dt>string `text_description`</dt> <dd>Description of the product.</dd> <dt>string `text_duration`</dt> <dd>Duration that will be added to the appointment with this addon as a formatted string.</dd> <dt>string `text_title`</dt> <dd>Name of the product.</dd> </dl>'
      tags:
        - Product-Product62
  '/Wl/Appointment/Book/Product/Product.json':
    get:
      summary: 'Retrieves list of available service add-ons. Returns all active add-on products available for the specified service, including title, image, price, and quantity information. Results are not filtered by client type; use {@link Product62Api} when filtering by client login type is required.'
      parameters:
        - name: k_service
          in: query
          description: 'The key of a service to show information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product:
                        type: array
                        description: 'A list service add-ons. This will be `null` if not set yet. <dl> <dt>array `a_logo`</dt> <dd> Information about first image connected to the product. <i>Result from {@link \RsShopProductImageList::imageOne()}.</i> <dl> <dt>int `i_height`</dt> <dd>Height of the image.</dd> <dt>int `i_width`</dt> <dd>Width of the image.</dd> <dt>bool `is_empty`</dt> <dd>`true` mean mock instead of real image.</dd> <dt>string `s_url`</dt> <dd>URL on the image.</dd> </dl> </dd> <dt>string `html_description`</dt> <dd>Description of the product as prepared html code.</dd> <dt>int `i_duration`</dt> <dd>Duration that will be added to the appointment with this addon as count of minutes.</dd> <dt>bool `is_client_duration`</dt> <dd>`true` means that the duration information will be displayed for the client.</dd> <dt>bool `is_online_sell`</dt> <dd>`true` if client can purchase product, `false` if only staff can perform purchase.</dd> <dt>bool `is_amount`</dt> <dd>`true` means that addon increases price of the appointment.</dd> <dt>string `k_currency`</dt> <dd>ID of the currency.</dd> <dt>string `k_shop_product`</dt> <dd>ID of the product.</dd> <dt>string `k_shop_product_option`</dt> <dd> ID of the product option. Zero means that product has multiple options and option should be selected by client. </dd> <dt>string `m_amount`</dt> <dd>Amount that will be added to the appointment price with this addon.</dd> <dt>string `text_description`</dt> <dd>Description of the product.</dd> <dt>string `text_duration`</dt> <dd>Duration that will be added to the appointment with this addon as a formatted string.</dd> <dt>string `text_title`</dt> <dd>Name of the product.</dd> </dl>'
      tags:
        - Product-Product
  '/Wl/Appointment/Book/Promote/AppointmentBookPromote.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: [number, "null"]
                  description: 'ID of the source where a visit was promoted, one of {@link ModeSid} constants. `null` if not initialized yet.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
                k_visit:
                  type: string
                  description: 'Visit key. Primary key in the {@link \RsVisitSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_wait_confirm:
                        type: boolean
                        description: 'Whether is awaiting for user confirmation.'
      tags:
        - Promote-AppointmentBookPromote
  '/Wl/Appointment/Book/Quiz/Quiz.json':
    get:
      summary: 'Defines a list of required quizzes.'
      parameters:
        - name: id_purchase_item
          in: query
          description: 'Purchase item ID.'
          schema:
            type: [number, "null"]
        - name: is_all
          in: query
          description: '`true` to return both optional and required forms; `false` to return only required forms.'
          schema:
            type: boolean
        - name: json_shop_product_option
          in: query
          description: 'List of add-ons. Each element is primary key in {@link \RsShopProductSql} table.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Promotion key or appointment key. Depends on {@link QuizApi::$id_purchase_item}.'
          schema:
            type: [string, "null"]
        - name: k_resource
          in: query
          description: 'Resource key.'
          schema:
            type: [string, "null"]
        - name: k_service
          in: query
          description: 'Service key.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. Empty if guest.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: array
                        description: 'List of required quizzes. Each element has next structure: <dl> <dt>bool `is_require`</dt> <dd>Whether the quiz is required.</dd> <dt>string `k_quiz`</dt> <dd>Quiz key. Primary key from {@link \Core\Quiz\QuizSql} table.</dd> <dt>string `text_title`</dt> <dd>Quiz title.</dd> </dl>'
      tags:
        - Quiz-Quiz
  '/Wl/Appointment/Book/Conflict/Conflict56.json':
    get:
      summary: 'Gets booking conflicts.'
      parameters:
        - name: a_product
          in: query
          description: 'List of add-ons. Primary keys in {@link \RsShopProductOptionSql} table.'
          schema:
            type: array
        - name: a_repeat
          in: query
          description: 'Data of appointment repeat. See key <tt>a_repeat</tt> of {@link \Wl\Appointment\Book\Finish\FinishApi::$a_book_data} for details. <tt>null</tt> if repeat is not required.'
          schema:
            type: ["null", array]
        - name: a_resource
          in: query
          description: 'List of assets. Every element has next keys: May be specified for service booking only. <dl> <dt>int `i_index`</dt> <dd>Asset index in layout.</dd> <dt>string `k_resource`</dt> <dd>Asset primary key in {@link \RsResourceSql} table.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date/time of appointment. In location timezone.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'Appointment duration. Must be specified for asset booking only.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Appointment primary key in {@link \RsAppointmentSql} table. Empty for a new appointment.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'Asset primary key in {@link \RsResourceSql} table. Empty for service booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service primary key in {@link \RsServiceSql} table. Empty for asset booking.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'Staff member primary key in {@link \RsStaffSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'Staff member user ID in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_conflict:
                        type: array
                        description: 'See for {@link \RsAppointmentEditConflict::checkAvailability()} details.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Conflict-Conflict56
  '/Wl/Appointment/Book/Conflict/Conflict.json':
    get:
      summary: 'Gets booking conflicts.'
      parameters:
        - name: a_product
          in: query
          description: 'List of add-ons. Primary keys in {@link \RsShopProductOptionSql} table.'
          schema:
            type: array
        - name: a_repeat
          in: query
          description: 'Data of appointment repeat. See key <tt>a_repeat</tt> of {@link \Wl\Appointment\Book\Finish\FinishApi::$a_book_data} for details. <tt>null</tt> if repeat is not required.'
          schema:
            type: ["null", array]
        - name: a_resource
          in: query
          description: 'List of assets. Every element has next keys: May be specified for service booking only. <dl> <dt>int `i_index`</dt> <dd>Asset index in layout.</dd> <dt>string `k_resource`</dt> <dd>Asset primary key in {@link \RsResourceSql} table.</dd> </dl>'
          schema:
            type: array
        - name: a_uid
          in: query
          description: 'List of user keys to book appointments. There may be empty values in this list, which means that this is a walk-in.'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date/time of appointment. In location timezone.'
          schema:
            type: string
        - name: i_duration
          in: query
          description: 'Appointment duration. Must be specified for asset booking only.'
          schema:
            type: number
            format: int32
        - name: is_walk_in
          in: query
          description: 'If `true`, the client is a walk-in. Otherwise, this will be `false`.'
          schema:
            type: boolean
        - name: k_appointment
          in: query
          description: 'Appointment primary key in {@link \RsAppointmentSql} table. Empty for a new appointment.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location to show available appointment booking schedule.'
          schema:
            type: string
        - name: k_resource
          in: query
          description: 'Asset primary key in {@link \RsResourceSql} table. Empty for service booking.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'Service primary key in {@link \RsServiceSql} table. Empty for asset booking.'
          schema:
            type: string
        - name: k_staff
          in: query
          description: 'Staff member primary key in {@link \RsStaffSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key. This field is used if the client books for himself or for the relative. This field is incorrect to use for guest booking since in this case the client will be checked as a relative. In case of a group booking or a guest booking, the key of the client who is making the booking is set here.'
          schema:
            type: string
        - name: uid_staff
          in: query
          description: 'Staff member user ID in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_conflict:
                        type: array
                        description: 'See for {@link \RsAppointmentEditConflict::checkAvailability()} details.'
                      k_location:
                        type: string
                        description: 'Location to show available appointment booking schedule.'
      tags:
        - Conflict-Conflict
  '/Wl/Appointment/Book/Question/Question.json':
    get:
      summary: 'Retrieves questions for the current service. Returns the list of intake questions defined for the specified service that the client must answer before completing an appointment booking. Each question includes its text, a stable hash key for matching answers, and input size metadata.'
      parameters:
        - name: k_service
          in: query
          description: 'The service key used for retrieving questions.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_question:
                        type: array
                        description: 'A list of questions for the service. Each element contains: <dl> <dt>int `i_size`</dt> <dd>Number of text rows for the answer input. Values greater than 1 indicate a multi-line answer.</dd> <dt>bool `is_multiple`</dt> <dd>`true` if `i_size` is greater than 1, `false` otherwise.</dd> <dt>string `s_key`</dt> <dd>SHA1 hash of the question text, used as a unique identifier for the question.</dd> <dt>string `s_question`</dt> <dd>Question text.</dd> </dl>'
      tags:
        - Question-Question
  '/Wl/Appointment/Change/Mail/DurationChangeMail.json':
    get:
      summary: 'Gets mail pattern.'
      parameters:
        - name: id_mail
          in: query
          description: 'Email template ID from {@link \RsMailSid}.'
          schema:
            type: [number, "null"]
        - name: k_appointment
          in: query
          description: 'Appointment primary key in {@link \RsAppointmentSql} table. <tt>null</tt> if appointment not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_mail_editor:
                        type: string
                        description: 'Html template that can be used to change mail template.'
      tags:
        - Mail-DurationChangeMail
  '/Wl/Appointment/Wizard/Recurring/RecurringList.json':
    get:
      summary: ''
      parameters:
        - name: a_day
          in: query
          description: 'Days, when sessions should be booked.'
          schema:
            type: array
        - name: dt_end
          in: query
          description: 'Date when recurring period ends.'
          schema:
            type: string
        - name: dt_start
          in: query
          description: 'Date when recurring period starts.'
          schema:
            type: string
        - name: dt_time
          in: query
          description: 'Time in mysql format, when each session should be started.'
          schema:
            type: string
        - name: i_count
          in: query
          description: 'Fixed count of sessions to be booked.'
          schema:
            type: number
            format: int32
        - name: i_period
          in: query
          description: 'Count of period between booked sessions.'
          schema:
            type: number
            format: int32
        - name: id_duration
          in: query
          description: 'Duration of the periods between sessions from {@link \ADurationSid}.'
          schema:
            type: number
            format: int32
        - name: id_end
          in: query
          description: 'Type of the rule, when repeatable sessions should end from {@link \RsRepeatEndSid}.'
          schema:
            type: number
            format: int32
        - name: is_month
          in: query
          description: 'Whether sessions should be connected to the day of month or day of week. `true` means to book the same day of month, `false` - the same day of week.'
          schema:
            type: boolean
        - name: s_id
          in: query
          description: 'Unique ID that defines appointment booking wizard.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: object
                        description: 'List of dates, when sessions will be booked. <dl> <dt>string `dt_date`</dt> <dd>Session date in MySQL format.</dd> <dt>bool `is_disable`</dt> <dd>Whether booking is not available for session.</dd> <dt>bool `is_waitlist`</dt> <dd>Whether booking for session available only into wait list.</dd> </dl>'
      tags:
        - Recurring-RecurringList
  '/Wl/Appointment/Wizard/Recurring/RecurringList68.json':
    get:
      summary: ''
      parameters:
        - name: a_day
          in: query
          description: 'Days, when sessions should be booked.'
          schema:
            type: array
        - name: dt_end
          in: query
          description: 'Date when recurring period ends.'
          schema:
            type: string
        - name: dt_start
          in: query
          description: 'Date when recurring period starts.'
          schema:
            type: string
        - name: dt_time
          in: query
          description: 'Time in mysql format, when each session should be started.'
          schema:
            type: string
        - name: i_count
          in: query
          description: 'Fixed count of sessions to be booked.'
          schema:
            type: number
            format: int32
        - name: i_period
          in: query
          description: 'Count of period between booked sessions.'
          schema:
            type: number
            format: int32
        - name: id_duration
          in: query
          description: 'Duration of the periods between sessions from {@link \ADurationSid}.'
          schema:
            type: number
            format: int32
        - name: id_end
          in: query
          description: 'Type of the rule, when repeatable sessions should end from {@link \RsRepeatEndSid}.'
          schema:
            type: number
            format: int32
        - name: is_check_availability
          in: query
          description: 'Whether booking availability should be checked for each session.'
          schema:
            type: boolean
        - name: is_month
          in: query
          description: 'Whether sessions should be connected to the day of month or day of week. `true` means to book the same day of month, `false` - the same day of week.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Primary key in {@link \RsLocationSql} table. If not set, location will be taken from booking wizard data.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'Unique ID that defines appointment booking wizard.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: object
                        description: 'List of dates, when sessions will be booked. <dl> <dt>string `dt_date`</dt> <dd>Session date in MySQL format.</dd> <dt>bool `is_disable`</dt> <dd> Whether booking is not available for session. Not set if {@link \Wl\Appointment\Wizard\Recurring\RecurringList68Api::$is_check_availability} set to false. </dd> <dt>bool `is_waitlist`</dt> <dd> Whether booking for session available only into wait list. Not set if {@link \Wl\Appointment\Wizard\Recurring\RecurringList68Api::$is_check_availability} set to false. </dd> </dl>'
      tags:
        - Recurring-RecurringList68
  '/Wl/Book/Process/Contract/ContractList.json':
    get:
      summary: 'Gets list of contracts tied to currently selected purchase options and whether contracts were skipped.'
      parameters:
        - name: s_wizard_id
          in: query
          description: 'Booking wizard session key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_contract_list:
                        type: array
                        description: 'List of purchase options with contracts. Value has following structure: <dl> <dt>bool `is_agree`</dt> <dd>Whether client already agreed to this contract.</dd> <dt>string `s_purchase_item`</dt> <dd>Key of purchase option, one of array keys returned by {@link \RsBookProcess::purchase()}.</dd> <dt>string `s_signature`</dt> <dd>Client''s signature, if this contract has already been signed.</dd> </dl>'
                      is_contract_skip:
                        type: boolean
                        description: 'Whether contracts should be skipped.'
      tags:
        - Contract-ContractList
    put:
      summary: 'Manipulates flag indicating whether contracts are skipped.'
      parameters:
        - name: s_wizard_id
          in: query
          description: 'Booking wizard session key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_contract_skip:
                  type: boolean
                  description: 'Whether contracts should be skipped.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_contract_skip:
                        type: boolean
                        description: 'Whether contracts should be skipped.'
      tags:
        - Contract-ContractList
  '/Wl/Book/Process/Contract/ContractItem.json':
    delete:
      summary: 'Marks the contract as declined by removing purchase item tied to it from selection, thus removing the contract.'
      parameters:
        - name: s_purchase_item
          in: query
          description: 'Key of purchase option, one of array keys returned by {@link \RsBookProcess::purchase()}.'
          schema:
            type: string
        - name: s_wizard_id
          in: query
          description: 'Booking wizard session key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_valid_purchase:
                        type: boolean
                        description: 'Whether there remains a selected purchase item that grants access to the class.'
      tags:
        - Contract-ContractItem
    get:
      summary: 'Gets information about contract.'
      parameters:
        - name: s_purchase_item
          in: query
          description: 'Key of purchase option, one of array keys returned by {@link \RsBookProcess::purchase()}.'
          schema:
            type: string
        - name: s_wizard_id
          in: query
          description: 'Booking wizard session key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_contract:
                        type: string
                        description: 'HTML-ready text of the contract.'
                      id_purchase_item:
                        type: number
                        format: int32
                        description: 'Type of purchase item, one of {@link \RsPurchaseItemSid}.'
                      is_agree:
                        type: boolean
                        description: 'Whether client agrees to the contract.'
                      k_id:
                        type: string
                        description: 'Id of purchase item within its type (represented by <var>id_purchase_item</var>).'
                      s_signature:
                        type: string
                        description: 'User signature produced by {@link \Core\Tool\Signature\Signature} tool.'
                      text_contract:
                        type: string
                        description: 'Text of the contract signed by the client.'
                      text_title:
                        type: string
                        description: 'Purchase item title.'
      tags:
        - Contract-ContractItem
    put:
      summary: 'Marks contract as agreed to and updates client signature on it.'
      parameters:
        - name: s_purchase_item
          in: query
          description: 'Key of purchase option, one of array keys returned by {@link \RsBookProcess::purchase()}.'
          schema:
            type: string
        - name: s_wizard_id
          in: query
          description: 'Booking wizard session key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_agree:
                  type: boolean
                  description: 'Whether client agrees to the contract.'
                s_signature:
                  type: string
                  description: 'User signature produced by {@link \Core\Tool\Signature\Signature} tool.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Contract-ContractItem
  '/Wl/Book/Process/Info/Info.json':
    get:
      summary: 'Returns class session details and booking information for the "Class and Location" step of the booking wizard. Loads session data, staff, location, contract, capacity, wait-list state, pricing, special instructions, and recurring booking availability for the given class period and date. For events it also returns the list of individually selectable sessions together with their free-session eligibility.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_class_capacity
          in: query
          description: '`true` if class capacity should be shown, `false` to use business setting. <i>See {@link \RsBusinessDesign::data()}`[is_class_capacity]`.</i>'
          schema:
            type: boolean
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_day_available:
                        type: [array, "null"]
                        description: 'Week days available for recurring booking. Constants of {@link \ADateWeekSid} class. `null` if recurring booking is not available.'
                      a_session_all:
                        type: array
                        description: 'A list of all class sessions that can be booked together. Every element has the next structure: <dl> <dt>string[] `a_staff`</dt> <dd>List of staff names that are leading this session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations.</dd> <dt>string `dt_date`</dt> <dd>The date/time when session starts in MySQL format and in GMT.</dd> <dt>int `i_active`</dt> <dd>Total number of clients on the active list.</dd> <dt>int `i_active_limit`</dt> <dd>Total capacity of the active list.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_wait`</dt> <dd>Total number of clients on the wait list.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if this session should be selected when page is initialized; <tt>false</tt> if otherwise.</dd> <dt>bool `is_wait`</dt> <dd>`true` if client is added to a wait list, `false` - to an active list.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>Allow clients to join the wait list unpaid.</dd> <dt>string `k_class_period`</dt> <dd>The key of the session. <i>Primary keys in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `m_price`</dt> <dd>Price of the session.</dd> <dt>string `s_location`</dt> <dd>The name of the location where the session occurred.</dd> <dt>string `s_start`</dt> <dd>The date/time when the session starts in human-readable format. Returned in the time zone of the location.</dd> <dt>string `text_duration`</dt> <dd> String representation of session duration. <i>Duration formatting method {@link \Wl\Book\Process\Info\InfoApi::_classDurationFormat()}.</i> </dd> </dl>'
                      a_session_free:
                        type: array
                        description: 'List of sessions that can be paid without new purchases. Such as previously prepaid or free sessions. <i><A result of the {@link \RsBookProcess::sessionFreeGet()} method execution.</i> Each its item has the key of following format: `dt_date::k_class_period` and the value of following structure: <dl> <dt>string `dt_date`</dt> <dd>Session date.</dd> <dt>string `k_class_period`</dt> <dd>Class period key for the session.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'The staff member conducting the session. Every element has the next structure: <dl> <dt>array `a_logo`</dt> <dd> The staff member photo: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl> </dd> <dt>string `s_family`</dt> <dd>The first letter of staff member surname.</dd> <dt>string `s_staff`</dt> <dd>The staff member''s name.</dd> <dt>string `uid_staff`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `uid`</dt> <dd>Alias of `uid_staff` for backward compatibility.</dd> </dl>'
                      dl_end:
                        type: string
                        description: 'Date when this class session occurrences stop.'
                      dt_date_local:
                        type: string
                        description: 'The date/time of the session the user is booking in MySQL format in the location''s timezone.'
                      hide_price:
                        type: boolean
                        description: '`true` if price for the individual session should be hidden, if client has applicable pricing option to pay for this booking. `false` if price should be shown always.'
                      html_contract:
                        type: string
                        description: 'The text of the contract to which the user must agree before book this session. Not empty if business has contract and if user did not agree to this contract.'
                      html_duration:
                        type: string
                        description: 'Class duration in human-readable format.'
                      html_special:
                        type: string
                        description: 'The special instructions for the class.'
                      html_special_preview:
                        type: string
                        description: 'Special instructions preview for class.'
                      i_available:
                        type: [number, "null"]
                        description: 'Number of available spots. `null` if this information is not available.'
                      i_book:
                        type: [number, "null"]
                        description: 'Number of booked spots. `null` if this information is not available.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The duration of the session in minutes.'
                      i_wait:
                        type: number
                        format: int32
                        description: 'Total number of clients on the wait list.'
                      i_wait_limit:
                        type: [number, "null"]
                        description: 'Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'Estimated place of reservation on the waiting list.'
                      is_book_repeat_client:
                        type: boolean
                        description: '`true` if recurring booking is available, `false` otherwise.'
                      is_book_repeat_no_end_date:
                        type: boolean
                        description: '`true` if the setting for frequency during class recurring booking will be `never end` by default, `false` otherwise.'
                      is_event_session:
                        type: boolean
                        description: 'Can client chooses several session per booking.'
                      is_location_phone:
                        type: boolean
                        description: '`true` if need to display location phone number, `false` otherwise.'
                      is_promotion_only:
                        type: boolean
                        description: '`true` if event can be paid with pricing option only. `false` if full event purchase or single session purchase are allowed.'
                      is_single_buy:
                        type: boolean
                        description: 'Whether the class can be paid with single session.'
                      is_special_preview:
                        type: boolean
                        description: 'Whether the full text of the special instructions fits within the preview length or not.'
                      is_virtual:
                        type: boolean
                        description: '`true` if class is virtual, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'Class period location key.'
                      m_price:
                        type: string
                        description: 'Price of the session.'
                      m_price_total:
                        type: string
                        description: 'Whole event cost.'
                      m_price_total_early:
                        type: string
                        description: 'Event price at an early discount. An empty string if there is no discount.'
                      s_class:
                        type: string
                        description: 'The class title.'
                      s_location_address:
                        type: string
                        description: 'The location address.'
                      s_location_title:
                        type: string
                        description: 'The location title.'
                      s_time:
                        type: string
                        description: 'The time when the session takes place in the location''s time zone. In format `hh:mm`.'
                      text_location_phone:
                        type: string
                        description: 'Location phone number.'
                      text_room:
                        type: string
                        description: 'Room where session takes place.'
                      text_staff:
                        type: string
                        description: 'Text representation of the list of staffs. List of staff see {@link InfoApi::$a_staff}.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
      tags:
        - Info-Info
    post:
      summary: 'Processes the "Class and Location" step of the booking wizard, optionally recording the waiver agreement and booking the session. Accepts the selected sessions, asset assignments, recurring configuration, and waiver signature, then attempts to book the session immediately when no further payment or resource steps are needed. Returns booking keys, activity keys, and flags indicating whether additional wizard steps are required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Every element has the next structure: <dl> <dt>int `i_index`</dt> <dd>Order number of the asset (maybe from 1 to asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: array
                  description: 'The selected sessions. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'Selected sessions on the waiting list without pay. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of dates/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_agree:
                  type: boolean
                  description: 'Determines if the user has agreed to the liability release. `true` - if  the user has agreed. Otherwise, this will be. `false` - if the user hasn''t agreed or the agreement isn''t required.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
                s_signature:
                  type: string
                  description: 'User signature.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of users'' activity.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      is_card_authorize:
                        type: boolean
                        description: 'If client must authorize credit card.'
                      is_force_book:
                        type: boolean
                        description: 'Can the class/event be booked immediately or not. The verification is based on the search for client''s promotions and other features of the class/event. But it does not take into account the presence of other mandatory steps. Their presence will be indicated by the {@link InfoApi::$is_next} flag.'
                      is_next:
                        type: boolean
                        description: '`true` - next steps of the wizard are needed (for example, to purchase something to book the selected session). `false` - no need for next steps (all that''s needed has already been purchased).'
      tags:
        - Info-Info
  '/Wl/Book/Process/Info/Info54.json':
    get:
      summary: 'Returns class and location booking information, enriched with localized date and time for each session. Delegates to the parent implementation and then appends a localized `html_date_time` string to each available session using the business locale and either the client''s profile timezone or the location timezone.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_class_capacity
          in: query
          description: '`true` if class capacity should be shown, `false` to use business setting. <i>See {@link \RsBusinessDesign::data()}`[is_class_capacity]`.</i>'
          schema:
            type: boolean
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_day_available:
                        type: [array, "null"]
                        description: 'Week days available for recurring booking. Constants of {@link \ADateWeekSid} class. `null` if recurring booking is not available.'
                      a_session_all:
                        type: array
                        description: 'A list of all class sessions that can be booked together. Every element has the next structure: <dl> <dt>string[] `a_staff`</dt> <dd>List of staff names that are leading this session.</dd> <dt>string[] `a_virtual_location`</dt> <dd>List of virtual locations.</dd> <dt>string `dt_date`</dt> <dd>The date/time when session starts in MySQL format and in GMT.</dd> <dt>int `i_active`</dt> <dd>Total number of clients on the active list.</dd> <dt>int `i_active_limit`</dt> <dd>Total capacity of the active list.</dd> <dt>int `i_duration`</dt> <dd>Duration of a service.</dd> <dt>int `i_wait`</dt> <dd>Total number of clients on the wait list.</dd> <dt>int|null `i_wait_limit`</dt> <dd>Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> if this session should be selected when page is initialized; <tt>false</tt> if otherwise.</dd> <dt>bool `is_wait`</dt> <dd>`true` if client is added to a wait list, `false` - to an active list.</dd> <dt>bool `is_wait_list_unpaid`</dt> <dd>Allow clients to join the wait list unpaid.</dd> <dt>string `k_class_period`</dt> <dd>The key of the session. <i>Primary keys in {@link \RsClassPeriodSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in {@link \RsLocationSql} table.</i></dd> <dt>string `m_price`</dt> <dd>Price of the session.</dd> <dt>string `s_location`</dt> <dd>The name of the location where the session occurred.</dd> <dt>string `s_start`</dt> <dd>The date/time when the session starts in human-readable format. Returned in the time zone of the location.</dd> <dt>string `text_duration`</dt> <dd> String representation of session duration. <i>Duration formatting method {@link \Wl\Book\Process\Info\InfoApi::_classDurationFormat()}.</i> </dd> </dl>'
                      a_session_free:
                        type: array
                        description: 'List of sessions that can be paid without new purchases. Such as previously prepaid or free sessions. <i><A result of the {@link \RsBookProcess::sessionFreeGet()} method execution.</i> Each its item has the key of following format: `dt_date::k_class_period` and the value of following structure: <dl> <dt>string `dt_date`</dt> <dd>Session date.</dd> <dt>string `k_class_period`</dt> <dd>Class period key for the session.</dd> </dl>'
                      a_staff:
                        type: array
                        description: 'The staff member conducting the session. Every element has the next structure: <dl> <dt>array `a_logo`</dt> <dd> The staff member photo: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `s_url`</dt> <dd>The image URL.</dd> </dl> </dd> <dt>string `s_family`</dt> <dd>The first letter of staff member surname.</dd> <dt>string `s_staff`</dt> <dd>The staff member''s name.</dd> <dt>string `uid_staff`</dt> <dd>UID of the staff member. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> <dt>string `uid`</dt> <dd>Alias of `uid_staff` for backward compatibility.</dd> </dl>'
                      dl_end:
                        type: string
                        description: 'Date when this class session occurrences stop.'
                      dt_date_local:
                        type: string
                        description: 'The date/time of the session the user is booking in MySQL format in the location''s timezone.'
                      hide_price:
                        type: boolean
                        description: '`true` if price for the individual session should be hidden, if client has applicable pricing option to pay for this booking. `false` if price should be shown always.'
                      html_contract:
                        type: string
                        description: 'The text of the contract to which the user must agree before book this session. Not empty if business has contract and if user did not agree to this contract.'
                      html_duration:
                        type: string
                        description: 'Class duration in human-readable format.'
                      html_special:
                        type: string
                        description: 'The special instructions for the class.'
                      html_special_preview:
                        type: string
                        description: 'Special instructions preview for class.'
                      i_available:
                        type: [number, "null"]
                        description: 'Number of available spots. `null` if this information is not available.'
                      i_book:
                        type: [number, "null"]
                        description: 'Number of booked spots. `null` if this information is not available.'
                      i_duration:
                        type: number
                        format: int32
                        description: 'The duration of the session in minutes.'
                      i_wait:
                        type: number
                        format: int32
                        description: 'Total number of clients on the wait list.'
                      i_wait_limit:
                        type: [number, "null"]
                        description: 'Total capacity the wait list. `null` if wail list in unlimited. `0` if wait list is disabled.'
                      i_wait_spot:
                        type: number
                        format: int32
                        description: 'Estimated place of reservation on the waiting list.'
                      is_book_repeat_client:
                        type: boolean
                        description: '`true` if recurring booking is available, `false` otherwise.'
                      is_book_repeat_no_end_date:
                        type: boolean
                        description: '`true` if the setting for frequency during class recurring booking will be `never end` by default, `false` otherwise.'
                      is_event_session:
                        type: boolean
                        description: 'Can client chooses several session per booking.'
                      is_location_phone:
                        type: boolean
                        description: '`true` if need to display location phone number, `false` otherwise.'
                      is_promotion_only:
                        type: boolean
                        description: '`true` if event can be paid with pricing option only. `false` if full event purchase or single session purchase are allowed.'
                      is_single_buy:
                        type: boolean
                        description: 'Whether the class can be paid with single session.'
                      is_special_preview:
                        type: boolean
                        description: 'Whether the full text of the special instructions fits within the preview length or not.'
                      is_virtual:
                        type: boolean
                        description: '`true` if class is virtual, `false` otherwise.'
                      k_location:
                        type: string
                        description: 'Class period location key.'
                      m_price:
                        type: string
                        description: 'Price of the session.'
                      m_price_total:
                        type: string
                        description: 'Whole event cost.'
                      m_price_total_early:
                        type: string
                        description: 'Event price at an early discount. An empty string if there is no discount.'
                      s_class:
                        type: string
                        description: 'The class title.'
                      s_location_address:
                        type: string
                        description: 'The location address.'
                      s_location_title:
                        type: string
                        description: 'The location title.'
                      s_time:
                        type: string
                        description: 'The time when the session takes place in the location''s time zone. In format `hh:mm`.'
                      text_location_phone:
                        type: string
                        description: 'Location phone number.'
                      text_room:
                        type: string
                        description: 'Room where session takes place.'
                      text_staff:
                        type: string
                        description: 'Text representation of the list of staffs. List of staff see {@link InfoApi::$a_staff}.'
                      text_timezone:
                        type: string
                        description: 'Timezone abbreviation.'
      tags:
        - Info-Info54
    post:
      summary: 'Processes the "Class and Location" step of the booking wizard, optionally recording the waiver agreement and booking the session. Accepts the selected sessions, asset assignments, recurring configuration, and waiver signature, then attempts to book the session immediately when no further payment or resource steps are needed. Returns booking keys, activity keys, and flags indicating whether additional wizard steps are required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Every element has the next structure: <dl> <dt>int `i_index`</dt> <dd>Order number of the asset (maybe from 1 to asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The key of asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: array
                  description: 'The selected sessions. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'Selected sessions on the waiting list without pay. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of dates/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_agree:
                  type: boolean
                  description: 'Determines if the user has agreed to the liability release. `true` - if  the user has agreed. Otherwise, this will be. `false` - if the user hasn''t agreed or the agreement isn''t required.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
                s_signature:
                  type: string
                  description: 'User signature.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of users'' activity.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      is_card_authorize:
                        type: boolean
                        description: 'If client must authorize credit card.'
                      is_force_book:
                        type: boolean
                        description: 'Can the class/event be booked immediately or not. The verification is based on the search for client''s promotions and other features of the class/event. But it does not take into account the presence of other mandatory steps. Their presence will be indicated by the {@link InfoApi::$is_next} flag.'
                      is_next:
                        type: boolean
                        description: '`true` - next steps of the wizard are needed (for example, to purchase something to book the selected session). `false` - no need for next steps (all that''s needed has already been purchased).'
      tags:
        - Info-Info54
  '/Wl/Book/Process/Info/InfoCanComplete.json':
    get:
      summary: 'Checks whether the user can complete booking wizard without additional steps. Copies the GET session selection into the POST field and delegates to {@link InfoCanCompleteApi::post()}. Prefer the POST variant when the session list may be large, as the query string has length limitations. query string length limitation.'
      parameters:
        - name: a_session_select
          in: query
          description: 'A list of sessions of an event that can be booked together. This is the same as {@link InfoCanCompleteApi::$a_session_select_post}, but limited by query string length and might fail if the user is booking more than 50 sessions at once. Therefore, using the POST method is preferable. <dl> <dt>string `dt_date`</dt> <dd>The date and time in MySQL format in GMT when the session starts.</dd> <dt>bool `is_select`</dt> <dd>`true` if this session should be booked, `false` otherwise.</dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'The date and time of the session that the user is booking, returned in MySQL format and in GMT.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the booking is performed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period ID that the user started to book.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique booking process key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user who is making the booking. Empty if user is a guest.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_complete:
                        type: boolean
                        description: 'Determines whether users can complete the booking process from the info step.'
                      hide_price:
                        type: boolean
                        description: '`true` if price for the individual session should be hidden, if client has applicable pricing option to pay for this booking. `false` if price should be shown always.'
      tags:
        - Info-InfoCanComplete
    post:
      summary: 'Checks whether the user can complete booking wizard without additional steps. Performs a dry-run booking attempt for the given class period, date, and session selection to determine whether the client can finalize the booking directly from the info step without proceeding to the payment or purchase-option steps.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'The date and time of the session that the user is booking, returned in MySQL format and in GMT.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the booking is performed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The class period ID that the user started to book.'
          schema:
            type: string
        - name: s_id
          in: query
          description: 'The unique booking process key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of a user who is making the booking. Empty if user is a guest.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_session_select_post:
                  type: array
                  description: 'A list of sessions of an event that can be booked together. Every element has the next structure: <dl> <dt>string `dt_date`</dt> <dd>The date and time in MySQL format in GMT when the session starts.</dd> <dt>bool `is_select`</dt> <dd>`true` if this session should be booked, `false` otherwise.</dd> <dt>string `k_class_period`</dt> <dd>The class period key. <i>Primary key in {@link \RsClassPeriodSql} table.</i></dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_complete:
                        type: boolean
                        description: 'Determines whether users can complete the booking process from the info step.'
      tags:
        - Info-InfoCanComplete
  '/Wl/Book/Process/Store/Store.json':
    post:
      summary: 'Processes the "Purchase Options" step of the booking wizard, validates selections, and books the session when possible. Stores the selected purchase option (existing login promotion or new purchasable item) and session data in the booking process context, validates availability and eligibility, and attempts to complete the booking immediately when no payment or quiz step is needed. Returns visit keys, activity keys, and a flag indicating whether further wizard steps are required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_purchase_item_check:
                  type: array
                  description: 'The selected purchase item. This new purchase item should be checked to determine if it can be applied to the current class or event before being purchased. <dl> <dt>int `i_session`</dt> <dd> The number of sessions that this item can cover. This only applies to items of type {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>string `s_value`</dt> <dd> The unique identifier of the item being checked. This corresponds to one of the following values: <ul> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_purchase}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_reward_prize}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_login_prize}`["s_value"]`</li> </ul> </dd> </dl>'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Each element has the following keys: <dl> <dt>int `i_index`</dt> <dd>The order number of the asset (from 1 to the asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: object
                  description: 'The selected sessions for an event. Keys are class period keys, values are indexed arrays of dates and times when the session occurred (in MySQL format, UTC).'
                a_session_wait_list_unpaid:
                  type: object
                  description: 'The selected sessions for an event that are on the wait list and unpaid. Keys are class period keys, values are indexed arrays of dates and times when the session occurred (in MySQL format, UTC).'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys for the user''s activities. This will be populated upon completion of the booking process.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings that have been made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      is_next:
                        type: boolean
                        description: 'If `true`, the next steps of the booking wizard are required to purchase an item or book the selected session. If `false`, no further steps in the booking wizard are required.'
      tags:
        - Store-Store
  '/Wl/Book/Process/Store/StoreGroup.json':
    post:
      summary: 'Validates and distributes selected purchase options across all clients in the booking group. For each client in the group, validates session selection, resource requirements, recurring booking configuration, and purchase item eligibility. When all clients pass validation, attempts to assign or share promotions across the group and returns per-client distribution results with any error details in `a_purchase_item_distribute`.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_login_promotion:
                  type: array
                  description: 'A list of existing login promotions which are selected by a group of clients. This login promotions will be checked to determine if it can be applied to the current class or event. Keys are UIDs of clients, values are login promotion keys.'
                a_login_promotion_guest_pass:
                  type: array
                  description: 'A list of host guest passes which are selected by a group of clients to pay for the guests visits. These guest passes will be checked to determine if they can be applied to the current class or event. Keys are UIDs of clients, values are host login promotion keys that grant the guest passes.'
                a_purchase_item_check:
                  type: array
                  description: 'A list of new purchase items which are selected by a group of clients. This new purchase items will be checked to determine if it can be applied to the current class or event before being purchased. Keys are UIDs of clients, values are arrays with the following structure: <dl> <dt>int `i_session`</dt> <dd> The number of sessions that this item can cover. This only applies to items of type {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>string `s_value`</dt> <dd> The unique identifier of the item being checked. This corresponds to one of the following values: <ul> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_purchase}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_reward_prize}`["s_value"]`</li> <li>{@link \Wl\Book\Process\Purchase\Purchase56Api::$a_login_prize}`["s_value"]`</li> </ul> </dd> </dl>'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking for each client in the group. Keys are UIDs of clients, values are arrays with information about the recurring booking: Should be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets which are selected by a group of clients. Keys are UIDs of clients, values are arrays with the following structure: <dl> <dt>int `i_index`</dt> <dd>The order number of the asset (from 1 to the asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in the {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_pass:
                  type: array
                  description: 'A list of existing session passes (drop-in, full-event) which are selected by a group of clients. This session passes will be checked to determine if it can be applied to the current class or event. Keys are UIDs of clients, values are session pass keys.'
                a_session_select:
                  type: object
                  description: 'A list of sessions which are selected by a group of clients. Keys are UIDs of clients, values are arrays of class period keys mapped to index arrays of dates/times when the session occurred (MySQL format; UTC).'
                a_session_wait_list_unpaid:
                  type: object
                  description: 'A list of wait list (unpaid) sessions which are selected by a group of clients. Keys are UIDs of clients, values are arrays of class period keys mapped to index arrays of dates/times when the session occurred (MySQL format; UTC).'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item_distribute:
                        type: array
                        description: 'A list of distributed new shared purchase items which are selected by a group of clients. Keys are UIDs of clients, values are arrays with the following structure: <dl> <dt>string[] `a_owner`</dt> <dd> List of UIDs of owners who will share this promotion with this client. If empty, this client is the owner of the promotion. This only applies to the new promotions. <i>Each UID is a primary key in the {@link \PassportLoginSql} table.</i> </dd> <dt>int `i_session`</dt> <dd> The number of sessions that this item can cover. The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_purchase_item_check}. `0` if not applicable. </dd> <dt>string|null `k_login_promotion`</dt> <dd> Login promotion key. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i> The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_login_promotion}. `null` if not applicable. </dd> <dt>string|null `k_login_promotion_guest_pass`</dt> <dd> Host login promotion key that grants the guest pass. <i>Primary key in the {@link \RsLoginPromotionSql} table.</i> `null` if not applicable. </dd> <dt>string|null `k_session_pass`</dt> <dd> Session pass key. <i>Primary key in the {@link \Wl\Session\Pass\Sql} table.</i> The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_session_pass}. `null` if not applicable. </dd> <dt>string|null `s_value`</dt> <dd> The unique identifier of the purchase item. The same as in {@link \Wl\Book\Process\Store\StoreGroupApi::$a_purchase_item_check}. `null` if not applicable. </dd> <dt>string `text_error`</dt> <dd>Error text if the purchase item cannot be applied to the current class or event for this client.</dd> <dt>string `text_error_code`</dt> <dd>Error code if the purchase item cannot be applied to the current class or event for this client.</dd> </dl>'
      tags:
        - Store-StoreGroup
  '/Wl/Book/Process/Relation/Relation.json':
    get:
      summary: 'Checks if user can book specified session. Validates the date, business, class period, and user, verifies the session is not already booked, then runs the full booking eligibility check (credit card, waiver, age, overlap, outstanding balance) and throws a descriptive user-facing error if any requirement is not met.'
      parameters:
        - name: a_check_ignore
          in: query
          description: 'List of check that must be skipped. Each element must be a member of {@link ProcessCheckSid}.'
          schema:
            type: array
        - name: dtu_date
          in: query
          description: 'The date/time of the session to check for booking availability.'
          schema:
            type: string
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag and the check will still be performed.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business where <var>uid_from</var> creates the new relative.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The key of the session to check for booking availability.'
          schema:
            type: string
        - name: uid_from
          in: query
          description: 'The user who''s adding the new relative.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Relation-Relation
    post:
      summary: 'Added new relative. Creates a new family member profile for the specified business and links it as a relative of `uid_from`, applying birthday validation, email-inheritance rules, and payment responsibility settings. Returns the UID of the newly created user in `uid_create`.'
      parameters:
        - name: k_business
          in: query
          description: 'The business where <var>uid_from</var> creates the new relative.'
          schema:
            type: string
        - name: uid_from
          in: query
          description: 'The user who''s adding the new relative.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_day:
                  type: [number, "null"]
                  description: 'Day of birthday. `null` if birthday is not entered.'
                i_month:
                  type: [number, "null"]
                  description: 'Month of birthday. `null` if birthday is not entered.'
                i_year:
                  type: [number, "null"]
                  description: 'Year of birthday. `null` if birthday is not entered.'
                id_mode:
                  type: number
                  format: int32
                  description: 'The mode type. One of the {@link ModeSid} constants.'
                id_relation:
                  type: number
                  format: int32
                  description: 'The relation type. One of the {@link \RsFamilyRelationSid} constants.'
                is_mail_inherit:
                  type: boolean
                  description: '`true` - the new relative uses the email from <var>uid_from</var>. `false` - the new relative has their own email.'
                is_pay_self:
                  type: boolean
                  description: '`true` - the new relative pays for themselves. `false` - <var>uid_from</var> pays for the new relative.'
                text_mail:
                  type: string
                  description: 'The new relative''s email.'
                text_name_first:
                  type: string
                  description: 'The new relative''s first name.'
                text_name_last:
                  type: string
                  description: 'The new relative''s last name.'
                uid_existed:
                  type: string
                  description: 'UID of already existed in another business user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid_create:
                        type: string
                        description: 'The newly added relative.'
      tags:
        - Relation-Relation
  '/Wl/Book/Process/Frequency/Repeat.json':
    get:
      summary: 'Returns the list of visits to be created for the given recurring booking settings. Accepts the class period, date, user, recurrence configuration (interval, end condition, and days of week), and returns a list of individual visit slots with availability and alert information for each occurrence, together with the computed date range and visit count.'
      parameters:
        - name: a_day
          in: query
          description: 'List of days of the week to create visits. Each value is a {@link \ADateWeekSid} constant.'
          schema:
            type: array
        - name: a_visit_ignore
          in: query
          description: 'List of visits to be ignored. Each value is a string consisting of a class period key and a visit date and time in location''s timezone, concatenated by two colons. Empty array if no visits should be ignored. Example: [''70::2024-11-05 18:00:00''].'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date and time of the class, when recurring booking was called, in UTC timezone.'
          schema:
            type: string
        - name: dt_from
          in: query
          description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: dt_to
          in: query
          description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: i_count
          in: query
          description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
          schema:
            type: number
            format: int32
        - name: i_duration
          in: query
          description: 'Count of days\weeks\months between recurring bookings.'
          schema:
            type: number
            format: int32
        - name: id_duration
          in: query
          description: 'Recurring booking interval, one of {@link \ADurationSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'WellnessLiving mode, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_repeat_end
          in: query
          description: 'Type of repeating, one of {@link \RsRepeatEndSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_cancel
          in: query
          description: '`true` when cancelling booking, `false` otherwise.'
          schema:
            type: boolean
        - name: is_new_user
          in: query
          description: '`true` if current user is not created yet, `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key.'
          schema:
            type: string
        - name: s_uid
          in: query
          description: 'Encoded list of user keys, who will attend visits.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of user, who will attend visits.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Key of user, who perform booking.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit:
                        type: array
                        description: 'List of visits to be created for the given settings: <dl> <dt>string `dt_date`</dt> <dd>Visit date and time in UTC.</dd> <dt>string `dt_local`</dt> <dd>Visit date and time in location''s time zone.</dd> <dt>string `html_short_name_list`</dt> <dd>List of names of the staff who provide this class.</dd> <dt>string `html_timezone`</dt> <dd>Location''s time zone abbreviation.</dd> <dt>int `i_spot`</dt> <dd>Number of open spots remaining for this session. `0` if the session is full.</dd> <dt>bool `is_disable`</dt> <dd>Whether booking is not available for this day.</dd> <dt>bool `is_free`</dt> <dd>Whether booking is free for this day.</dd> <dt>bool `is_ignore`</dt> <dd>Whether visit is from ignore list.</dd> <dt>bool `is_selected_series`</dt> <dd> `true` if this visit belongs to the originally selected class period''s series (parent and its reschedules). `false` if it belongs to a parallel series running at the same time and location. </dd> <dt>bool `is_wait`</dt> <dd>Whether booking can be only to wait list.</dd> <dt>string `k_class_period`</dt> <dd>Class period key.</dd> <dt>string `s_alert`</dt> <dd>Staff name if booking available, warning about wait list or disabled booking otherwise.</dd> <dt>string `s_date`</dt> <dd>Visit date and time in location''s time zone in human-readable format.</dd> </dl>'
                      dt_from:
                        type: string
                        description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      dt_to:
                        type: string
                        description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      i_count:
                        type: number
                        format: int32
                        description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
                      text_date_from:
                        type: string
                        description: 'Start date of repeatable period in human-readable format.'
                      text_date_to:
                        type: string
                        description: 'End date of repeatable period in human-readable format.'
      tags:
        - Frequency-Repeat
  '/Wl/Book/Process/Frequency/RepeatParallel.json':
    get:
      summary: 'Returns the list of visits to be created for the given recurring booking settings. Accepts the class period, date, user, recurrence configuration (interval, end condition, and days of week), and returns a list of individual visit slots with availability and alert information for each occurrence, together with the computed date range and visit count.'
      parameters:
        - name: a_day
          in: query
          description: 'List of days of the week to create visits. Each value is a {@link \ADateWeekSid} constant.'
          schema:
            type: array
        - name: a_visit_ignore
          in: query
          description: 'List of visits to be ignored. Each value is a string consisting of a class period key and a visit date and time in location''s timezone, concatenated by two colons. Empty array if no visits should be ignored. Example: [''70::2024-11-05 18:00:00''].'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date and time of the class, when recurring booking was called, in UTC timezone.'
          schema:
            type: string
        - name: dt_from
          in: query
          description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: dt_to
          in: query
          description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
          schema:
            type: string
        - name: i_count
          in: query
          description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
          schema:
            type: number
            format: int32
        - name: i_duration
          in: query
          description: 'Count of days\weeks\months between recurring bookings.'
          schema:
            type: number
            format: int32
        - name: id_duration
          in: query
          description: 'Recurring booking interval, one of {@link \ADurationSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'WellnessLiving mode, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_repeat_end
          in: query
          description: 'Type of repeating, one of {@link \RsRepeatEndSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_cancel
          in: query
          description: '`true` when cancelling booking, `false` otherwise.'
          schema:
            type: boolean
        - name: is_include_parallel
          in: query
          description: '`true` to also include sessions running in parallel at the same time and location (other class period series); `false` to return only the selected series (parent period and its reschedules). Controlled by the "Also include other sessions happening at the same time" toggle on the form. Defaults to `false`.'
          schema:
            type: boolean
        - name: is_new_user
          in: query
          description: '`true` if current user is not created yet, `false` otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Class period key.'
          schema:
            type: string
        - name: s_uid
          in: query
          description: 'Encoded list of user keys, who will attend visits.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of user, who will attend visits.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'Key of user, who perform booking.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_visit:
                        type: array
                        description: 'List of visits to be created for the given settings: <dl> <dt>string `dt_date`</dt> <dd>Visit date and time in UTC.</dd> <dt>string `dt_local`</dt> <dd>Visit date and time in location''s time zone.</dd> <dt>string `html_short_name_list`</dt> <dd>List of names of the staff who provide this class.</dd> <dt>string `html_timezone`</dt> <dd>Location''s time zone abbreviation.</dd> <dt>int `i_spot`</dt> <dd>Number of open spots remaining for this session. `0` if the session is full.</dd> <dt>bool `is_disable`</dt> <dd>Whether booking is not available for this day.</dd> <dt>bool `is_free`</dt> <dd>Whether booking is free for this day.</dd> <dt>bool `is_ignore`</dt> <dd>Whether visit is from ignore list.</dd> <dt>bool `is_selected_series`</dt> <dd> `true` if this visit belongs to the originally selected class period''s series (parent and its reschedules). `false` if it belongs to a parallel series running at the same time and location. </dd> <dt>bool `is_wait`</dt> <dd>Whether booking can be only to wait list.</dd> <dt>string `k_class_period`</dt> <dd>Class period key.</dd> <dt>string `s_alert`</dt> <dd>Staff name if booking available, warning about wait list or disabled booking otherwise.</dd> <dt>string `s_date`</dt> <dd>Visit date and time in location''s time zone in human-readable format.</dd> </dl>'
                      dt_from:
                        type: string
                        description: 'Date to start recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      dt_to:
                        type: string
                        description: 'Date to finish recurring booking. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::DATE}.'
                      i_count:
                        type: number
                        format: int32
                        description: 'Count of the visits to be created. Not empty only when {@link RepeatApi::$id_repeat_end} == {@link \RsRepeatEndSid::COUNT}.'
                      text_date_from:
                        type: string
                        description: 'Start date of repeatable period in human-readable format.'
                      text_date_to:
                        type: string
                        description: 'End date of repeatable period in human-readable format.'
      tags:
        - Frequency-RepeatParallel
  '/Wl/Book/Process/Payment/Payment.json':
    post:
      summary: 'Processes purchases and books sessions on the "Pay/Billing info" step of the booking wizard. Accepts the selected purchase items, payment form, optional discount code, and installment template, then collects payment, applies discounts and prizes, and creates the booking records. Returns visit keys, activity keys, and purchase activity key upon success.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_item:
                  type: array
                  description: 'A list of items to be bought. Every element has the next keys: <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of purchase item type. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_renew`</dt> <dd> `true` if the item should be set to auto-renew; `false` otherwise. If not set yet, use the default option for this item. </dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `k_login_prize`</dt> <dd> Key of user''s prize. <i>Primary key in table {@link \RsLoginPrizeSql}.</i> Not `0` only if user is paying book by prize. </dd> <dt>string `k_reward_prize`</dt> <dd> Key of reward prize. <i>Primary key in table {@link \RsRewardPrizeSql}.</i> Not `0` only if user wants to redeem prize and use it to pay for visit. </dd> <dt>string `s_signature`</dt> <dd> The signature of the Purchase Option contract. This won''t be set if the Purchase Option doesn''t require a contract assignment. </dd> </dl>'
                a_pay_form:
                  type: array
                  description: 'A list of payment sources to pay with. Each element has next keys: <dl> <dt>array `a_pay_card`</dt> <dd> The payment card information: <dl> <dt>array `a_pay_address`</dt> <dd> The payment address: <dl> <dt>bool `is_new`</dt> <dd>Set this value to `1` to add a new payment address or to `0` to use a saved payment address.</dd> <dt>string `k_geo_country`</dt> <dd>The key of the country used for the payment address. Specify this to add a new address.</dd> <dt>string `k_geo_region`</dt> <dd>The key of the region for the payment address. Specify this to add a new address.</dd> <dt>string `k_pay_address`</dt> <dd>The key of the saved payment address. Specify this to use a saved address.</dd> <dt>string `s_city`</dt> <dd>The city used for the payment address. Specify this to add a new address.</dd> <dt>string `s_name`</dt> <dd>The card name. Specify this to add a new address.</dd> <dt>string `s_phone`</dt> <dd>The payment phone. Specify this to add a new address.</dd> <dt>string `s_postal`</dt> <dd>The postal code for the payment address. Specify this to add a new address.</dd> <dt>string `s_street1`</dt> <dd>The payment address. Specify this to add a new address.</dd> <dt>string `s_street2`</dt> <dd>The optional payment address. Specify this to add a new address.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The credit card CSC. Specify this to add a new card.</dd> <dt>int `i_month`</dt> <dd>The credit card expiration month. Specify this to add a new card.</dd> <dt>int `i_year`</dt> <dd>The credit card expiration year. Specify this to add a new card.</dd> <dt>bool `is_new`</dt> <dd>Specify `1` to add a new card, or `0` to use a saved card.</dd> <dt>string `k_pay_bank`</dt> <dd>The key of the credit card. Specify this to use saved card.</dd> <dt>string `s_comment`</dt> <dd>Optional comment(s). Specify this to add a new card.</dd> <dt>string `s_number`</dt> <dd>The card number. Specify this to add a new card.</dd> </dl> </dd> <dt>string `f_amount`</dt> <dd>The amount of money to withdraw with this payment source.</dd> <dt>bool `is_hide`</dt> <dd>Determines whether this payment method is hidden.</dd> <dt>bool `is_save`</dt> <dd>Whether payment method should be saved to user''s account.</dd> <dt>bool `is_success`</dt> <dd>Identifies whether this source was successfully charged.</dd> <dt>string `m_surcharge`</dt> <dd>The client-side calculated surcharge.</dd> <dt>string `s_index`</dt> <dd>The index of this form (optional).</dd> <dt>string `sid_pay_method`</dt> <dd>The payment method ID.</dd> </dl>'
                a_quiz_response:
                  type: array
                  description: 'List of quiz response keys. Keys are quiz keys. <i>Primary key from the {@link \Core\Quiz\QuizSql} table.</i> Values are response keys. <i>Primary key from the {@link \Core\Quiz\Response\ResponseSql} table.</i> Or the {@link \Wl\Quiz\Response\QuizResponse::RESPONSE_SKIP} to skip the quiz.'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'A list of assets being booked. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The number of asset(s). The actual number is returned for assets with a quantity greater than <tt>1</tt>.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session:
                  type: array
                  description: 'A list of sessions being booked. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'Selected sessions on the waiting list without pay. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_card_authorize:
                  type: boolean
                  description: 'If client must authorize credit card.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_pay_installment_template:
                  type: [string, "null"]
                  description: 'The installment template key. This property is optional, and it will be `null` if an installment plan doesn''t exist for the purchased item. This will be `0` if an installment plan isn''t selected for the purchased item from the list of installment plans.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
                text_discount_code:
                  type: string
                  description: 'The discount code to be applied to the purchase.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity_book:
                        type: array
                        description: 'Keys of the user''s activity which correspond to bookings made. Not empty when the booking process is finished.'
                      a_visit:
                        type: array
                        description: 'The keys of bookings made.'
                      a_visit_payment:
                        type: array
                        description: 'Values are arrays with next keys: <dl> <dt>bool `is_free`</dt> <dd>`true` if the visit is free; `false` otherwise.</dd> <dt>bool `is_waitlist`</dt> <dd>`true` whether the booked slot was waitlisted; `false` otherwise.</dd> <dt>string `k_login_promotion`</dt> <dd>Applied user''s purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Purchase option. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `k_session_pass`</dt> <dd>Applied session pass. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>string `text_promotion`</dt> <dd>Purchase option title.</dd> </dl>'
                      k_login_activity_purchase:
                        type: string
                        description: 'The keys of the user''s activity corresponding to the purchase made. This won''t be empty when the booking process is finished.'
      tags:
        - Payment-Payment
  '/Wl/Book/Process/Purchase/Purchase.json':
    get:
      summary: 'Returns available purchase options, existing client promotions, session passes, and prizes for the specified session. Loads all purchase options that can be used to book the specified class period for the given user, including existing login promotions with remaining session counts, new purchasable options, reward prizes, and login prizes. Returns pricing, image, program category, and availability data for each option.'
      parameters:
        - name: a_login_promotion_group
          in: query
          description: 'A list of existing purchase options that were selected for previous clients (group). Note: * It makes sense if for all clients the list is loaded within the same pair {@link PurchaseApi::$dt_date_gmt} and {@link PurchaseApi::$k_class_period}. * If promotions are shared, the system will try to determine if there are enough sessions left for the next client who has the same promotion. * A very simple check is carried out based on a comparison of the remaining sessions for the promotion with the number of times it was selected. * Can affect the list of available login promotions {@link PurchaseApi::$a_login_promotion}. If a given client is eligible for such a promotion, but the remaining sessions (minus those previously selected) in it do not allow it to be applied to the selected session, then such promotion will simply not be returned for the client. * The order of clients across all APIs must be the same to guarantee their results. Each element has the following structure: <dl> <dt>int `i_session`</dt> <dd> Number of paid sessions of the same class|event that were selected for the previous user. This number should not include free or waitlist unpaid sessions. </dd> <dt>string `k_login_promotion`</dt> <dd>Selected purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `uid`</dt> <dd>UID of the previous user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: a_session
          in: query
          description: 'The list of sessions being booked. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: a_session_wait_list_unpaid
          in: query
          description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_card_authorize
          in: query
          description: 'Determines if the client must authorize the credit card.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key. `null` if business key was not passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. Each element has the following fields: <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option with the following information: <dl> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The number of the remaining visits the Purchase Option has left.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in the Purchase Option.</dd> </dl> </dd> <dt>string[] `a_uid_share`</dt> <dd> List of UIDs of users who share this promotion. List of those passed in the {@link \Wl\Book\Process\Purchase\PurchaseApi::$a_login_promotion_group} array. </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the promotion (for example, 4 per week).</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The number of future sessions paid using this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The Purchase Option''s usage count.</dd> <dt>int `i_visit_past`</dt> <dd> The number of attended sessions before the last renewal. This will be `0` if there aren''t any sessions before the last renewal or the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for Purchase Options. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the promotion converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_shared`</dt> <dd>`true` if the promotion is shared with the client, `false` if the client is owner of the promotion.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new promotion instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package, this field will contain a list of Purchase Options contained in the package.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'A list of Purchase Options that are available for the session(s) being booked. Keys refer to unique string IDs, and values refer arrays with the next fields: <dl> <dt>array[] `a_installment_template`</dt> <dd> A list of installment plans. Every element has the next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by <var>id_period</var> between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl> </dd> <dt>array[] `a_visit_limit`</dt> <dd> This is only set for Purchase Options. A list of limits on booking by the Purchase Option. Every element has the next keys: <dl> <dt>string `s_title`</dt> <dd>The limit description.</dd> </dl> </dd> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date.</dd> <dt>string `f_price`</dt> <dd>The price.</dd> <dt>string `f_price_early`</dt> <dd>The price for early bookings.</dd> <dt>string `html_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var> in short form. </dd> <dt>string `html_description`</dt> <dd>The description, ready to paste in a browser.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked by Purchase Options.</dd> <dt>int `i_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The duration of the regular payment interval. </dd> <dt>int `i_session`</dt> <dd>This is only set for purchases of single sessions. The number of sessions booked simultaneously.</dd> <dt>int `id_program_category`</dt> <dd> This is only set for promotions. The ID of the promotion program category. One of the {@link \RsProgramCategorySid} constants. </dd> <dt>int `id_program_type`</dt> <dd> This is only set for promotions. The ID of the promotion program type. One of the {@link \RsProgramTypeSid} constants. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_contract`</dt> <dd>If `true`, the Purchase Option requires a contract assignment. Otherwise, this will be `false`.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the Purchase Option converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_renew`</dt> <dd>If `true`, the Purchase Option is renewable. Otherwise, this will be `false`.</dd> <dt>bool `is_renew_check`</dt> <dd> If `true`, the Purchase Option is renewable and the "auto-renew" option is turned on by default. Otherwise, this will be `false`. </dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `k_reward_prize`</dt> <dd>The key of the reward prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `m_prorate`</dt> <dd>Payment for membership prorate. Not empty only if prorate payment is required.</dd> <dt>string `s_contract`</dt> <dd>The contract of the Purchase Option. This is only set if <var>is_contract</var> is `true`.</dd> <dt>string `s_payment_duration`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var>. </dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new Purchase Option instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The title.</dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. Each element has the following fields: <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'The list of session passes that might be used in booking process. Each element has the following fields: <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if the drop-in rate should be the default promotion.'
                      k_promotion_default:
                        type: string
                        description: 'The default Purchase Option key. This will be empty if the class has no default Purchase Option.'
      tags:
        - Purchase-Purchase
  '/Wl/Book/Process/Purchase/PurchaseElement.json':
    get:
      summary: 'Builds a single-item purchase list from the scalar input fields and delegates to the parent for price calculation. Constructs a one-element `a_purchase_item` array from the individual scalar GET fields (purchase item type, item key, session count, prize keys, and installment template), then delegates to {@link PurchaseElementGroupApi::get()} to compute the price breakdown for that item.'
      parameters:
        - name: dtu_date
          in: query
          description: 'Date/time of session is booking. `null` until initialized.'
          schema:
            type: [string, "null"]
        - name: i_session
          in: query
          description: 'The number of sessions which are booked simultaneously. Required when {@link PurchaseElementApi::$id_purchase_item} = {@link \RsPurchaseItemSid::CLASS_PERIOD}.'
          schema:
            type: number
            format: int32
        - name: id_purchase_item
          in: query
          description: 'The ID of the purchase item type. One of {@link \RsPurchaseItemSid}.'
          schema:
            type: number
            format: int32
        - name: k_class_period
          in: query
          description: 'The key of the session to check for booking availability.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'The key of the purchase item in the database.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location in which the purchase is made. This is also the booking process location.'
          schema:
            type: string
        - name: k_login_prize
          in: query
          description: 'The key of the user''s prize. Not empty only if the user wants to make a free visit from the prize.'
          schema:
            type: string
        - name: k_pay_installment_template
          in: query
          description: 'Installment template key. This property is optional. * can only be set for the purchase option which supports installment plan. <i>See {@link PurchaseItemAbstract::INSTALLMENT_ALLOW_USER} property.</i> * `null` if installment plan doesn''t exist for bought item; * `0` if installment plan doesn''t selected for bought item from the list of installment plans. NOTE: * Calculations of discounts and taxes for installment plans are for demonstration purposes only! * Installment is not an independent purchase item and has no discounts or taxes. * Installment is a division of the final amount (with taxes and discounts), of some purchase option, into N parts.'
          schema:
            type: [string, "null"]
        - name: k_reward_prize
          in: query
          description: 'The key of the reward prize. Not empty only if the user wants to redeem prize and use it to pay for a visit.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tax:
                        type: array
                        description: 'A list of taxes for the given purchase options. Keys - tax keys, values - tax amounts.'
                      m_cost:
                        type: string
                        description: 'The total cost of the given purchase options.'
                      m_discount:
                        type: string
                        description: 'The amount of the whole discount for the given purchase options.'
                      m_discount_code:
                        type: string
                        description: 'The discount amount provided by a discount code for the given purchase options.'
                      m_discount_login:
                        type: string
                        description: 'The discount amount for the client type for the given purchase options.'
                      m_price:
                        type: string
                        description: 'The price of the given purchase options (with or without taxes, depending on regional standards).'
                      m_subtotal:
                        type: string
                        description: 'The amount of subtotal for the given purchase options.'
                      m_tax:
                        type: string
                        description: 'The amount of taxes for the given purchase options.'
      tags:
        - Purchase-PurchaseElement
  '/Wl/Book/Process/Purchase/PurchaseElementList.json':
    get:
      summary: 'Returns the pricing breakdown for a list of purchase items, applying applicable discounts and taxes. Validates the business, location, and user, then for each item in `a_purchase_item_request` computes the price, applicable discount code reduction, login-type discount, and taxes, and returns per-item cost, discount, price, tax, and subtotal amounts in `a_purchase_item_result`.'
      parameters:
        - name: a_purchase_item_request
          in: query
          description: 'A list of purchase items to get information for. Every element has the next keys: <dl> <dt>int `i_session`</dt> <dd>The number of sessions booked simultaneously.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd> The key of the purchase item in the database. The name of the table in the database depends on <var>id_purchase_item</var> </dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize. <i>Primary key in {@link \RsLoginPrizeSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'The key of the business in which the purchase is made.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location in which the purchase is made. This is also the booking process location. This will be `null` if not set yet or if the location can''t be defined for some reason.'
          schema:
            type: [string, "null"]
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user making the purchase.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_purchase_item_result:
                        type: array
                        description: 'Detailed information about the amounts for the purchase item list. Every element has the next keys: <dl> <dt>string[] `a_tax`</dt> <dd>Information about taxes. The key refers to the tax key, and the value refers to the tax amount.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of purchase item type. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `m_cost`</dt> <dd>The cost of the purchase item (with taxes).</dd> <dt>string `m_discount`</dt> <dd>The amount of the whole discount.</dd> <dt>string `m_discount_login`</dt> <dd>The amount of the discount for the client type.</dd> <dt>string `m_price`</dt> <dd>The price of the purchase item (with or without taxes, depending on regional standards).</dd> <dt>string `m_tax`</dt> <dd>The amount of taxes for the purchase item.</dd> </dl>'
      tags:
        - Purchase-PurchaseElementList
  '/Wl/Book/Process/Purchase/Purchase56.json':
    get:
      summary: 'Returns available purchase options for the specified session, decoding JSON-encoded group promotion and session inputs. Deserializes the JSON-encoded group login-promotion list and session selection, then delegates to the parent to return the purchase options available for the current client and session.'
      parameters:
        - name: a_login_promotion_group
          in: query
          description: 'A list of existing purchase options that were selected for previous clients (group). Note: * It makes sense if for all clients the list is loaded within the same pair {@link PurchaseApi::$dt_date_gmt} and {@link PurchaseApi::$k_class_period}. * If promotions are shared, the system will try to determine if there are enough sessions left for the next client who has the same promotion. * A very simple check is carried out based on a comparison of the remaining sessions for the promotion with the number of times it was selected. * Can affect the list of available login promotions {@link PurchaseApi::$a_login_promotion}. If a given client is eligible for such a promotion, but the remaining sessions (minus those previously selected) in it do not allow it to be applied to the selected session, then such promotion will simply not be returned for the client. * The order of clients across all APIs must be the same to guarantee their results. Each element has the following structure: <dl> <dt>int `i_session`</dt> <dd> Number of paid sessions of the same class|event that were selected for the previous user. This number should not include free or waitlist unpaid sessions. </dd> <dt>string `k_login_promotion`</dt> <dd>Selected purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `uid`</dt> <dd>UID of the previous user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: array
        - name: a_session
          in: query
          description: 'The list of sessions being booked. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: a_session_wait_list_unpaid
          in: query
          description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: i_image_height
          in: query
          description: 'The image height in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: i_image_width
          in: query
          description: 'The image width in pixels. Specify this value if you need image to be returned in specific size. The returned image will have the default thumbnail size if this value isn''t specified.'
          schema:
            type: number
            format: int32
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_card_authorize
          in: query
          description: 'Determines if the client must authorize the credit card.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: json_login_promotion_group
          in: query
          description: 'A list of existing purchase options that were selected for previous clients (group). Note: * It makes sense if for all clients the list is loaded within the same pair {@link Purchase56Api::$dt_date_gmt} and {@link Purchase56Api::$k_class_period}. * If promotions are shared, the system will try to determine if there are enough sessions left for the next client who has the same promotion. * A very simple check is made based on a comparison of the remaining sessions for the promotion with the number of times it was selected. * Can affect the list of available login promotions {@link Purchase56Api::$a_login_promotion}. If a given client is eligible for such a promotion, but the remaining sessions (minus those previously selected) in it do not allow it to be applied to the selected session, then such promotion will simply not be returned for the client. * The order of clients across all APIs must be the same to guarantee their results. Serialized with JSON. Each element has the following structure: <dl> <dt>int `i_session`</dt> <dd> Number of paid sessions of the same class|event that were selected for the previous user. This number should not include free or waitlist unpaid sessions. </dd> <dt>string `k_login_promotion`</dt> <dd>Selected purchase option. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `uid`</dt> <dd>UID of the previous user. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
          schema:
            type: string
        - name: json_session
          in: query
          description: 'The selected sessions. This won''t be empty for session mode only. Fields refer to IDs of sessions in the database. Values refer to arrays of dates/times when session occurred, returned in MySQL format and in UTC. Serialized with JSON.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'The business key. `null` if business key was not passed.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_prize:
                        type: array
                        description: 'Data about the login prize which can be used to pay for service. <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_count`</dt> <dd>Login prize remaining quantity.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>Key of login prize. <i>Primary key in {@link \RsLoginPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly login prize description.</dd> </dl>'
                      a_login_promotion:
                        type: array
                        description: 'A list of the client''s login promotions that can be applied to a given service. Each element has the following fields: <dl> <dt>array `a_login_promotion_info`</dt> <dd> Information about the Purchase Option with the following information: <dl> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_remain`</dt> <dd>The number of the remaining visits the Purchase Option has left.</dd> <dt>int|null `i_remain_duration`</dt> <dd>The number of minutes left in the Purchase Option.</dd> </dl> </dd> <dt>string[] `a_uid_share`</dt> <dd> List of UIDs of users who share this promotion. List of those passed in the {@link \Wl\Book\Process\Purchase\PurchaseApi::$a_login_promotion_group} array. </dd> <dt>string[] `a_visit_limit`</dt> <dd>The list of calendar restrictions of the promotion (for example, 4 per week).</dd> <dt>array `a_restrict`</dt> <dd> Data about the shortest restriction period: <dl> <dt>int `i_limit`</dt> <dd>The limit of visits for the shortest restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the shortest restriction period.</dd> <dt>string `text_restriction`</dt> <dd>The description of the shortest restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>array[] `a_restrict_data`</dt> <dd> Data about all restriction periods given as an array, where each record has the following structure: <dl> <dt>int `i_book`</dt> <dd>The number of future sessions paid using this Purchase Option.</dd> <dt>int `i_limit`</dt> <dd>The limit of visits for the restriction period.</dd> <dt>int `i_remain`</dt> <dd>The number of remaining visits for the restriction period.</dd> <dt>int `i_use`</dt> <dd>The Purchase Option''s usage count.</dd> <dt>int `i_visit_past`</dt> <dd> The number of attended sessions before the last renewal. This will be `0` if there aren''t any sessions before the last renewal or the Purchase Option doesn''t auto-renew. </dd> <dt>string `text_restriction`</dt> <dd>The description of restriction period (for example "this week" or "for a 4 day period").</dd> </dl> </dd> <dt>int `i_limit`</dt> <dd>The number of visits the Purchase Option allows the client to make.</dd> <dt>int|null `i_limit_duration`</dt> <dd>The maximum number of minutes the Purchase Option can be used for.</dd> <dt>int `i_promotion_priority`</dt> <dd> Priority of this promotion. Result of {@link \Wl\Promotion\Priority\PromotionPrioritySid::priorityGet()} method. </dd> <dt>int `id_program`</dt> <dd>The program ID for Purchase Options. One of the {@link \RsProgramSid} constants.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the promotion converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_shared`</dt> <dd>`true` if the promotion is shared with the client, `false` if the client is owner of the promotion.</dd> <dt>string `k_login_promotion`</dt> <dd>The login promotion key. <i>Primary key in {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `s_class_include`</dt> <dd>The list of services provided by this Purchase Option.</dd> <dt>string `s_description`</dt> <dd>The Purchase Option description.</dd> <dt>string `s_duration`</dt> <dd>The Purchase Option duration.</dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new promotion instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The Purchase Option name.</dd> <dt>string `text_package_item`</dt> <dd>If this promotion is a package, this field will contain a list of Purchase Options contained in the package.</dd> </dl>'
                      a_purchase:
                        type: array
                        description: 'A list of Purchase Options that are available for the session(s) being booked. Keys refer to unique string IDs, and values refer arrays with the next fields: <dl> <dt>array[] `a_installment_template`</dt> <dd> A list of installment plans. Every element has the next keys: <dl> <dt>int `i_count`</dt> <dd>The number of payments.</dd> <dt>int `id_duration`</dt> <dd>The duration of a single period. One of the {@link \ADurationSid} constants.</dd> <dt>int `i_period`</dt> <dd>The number of periods specified by <var>id_period</var> between individual payments.</dd> <dt>string `k_currency`</dt> <dd>The payment currency key.</dd> <dt>string `k_pay_installment_template`</dt> <dd> The key of the installment plan template. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> </dd> <dt>string `m_amount`</dt> <dd>The amount of the installment plan.</dd> <dt>string `s_duration`</dt> <dd>The title of the installment plan.</dd> </dl> </dd> <dt>array[] `a_visit_limit`</dt> <dd> This is only set for Purchase Options. A list of limits on booking by the Purchase Option. Every element has the next keys: <dl> <dt>string `s_title`</dt> <dd>The limit description.</dd> </dl> </dd> <dt>string `dl_client_prorate`</dt> <dd>The client prorate date.</dd> <dt>string `f_price`</dt> <dd>The price.</dd> <dt>string `f_price_early`</dt> <dd>The price for early bookings.</dd> <dt>string `html_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var> in short form. </dd> <dt>string `html_description`</dt> <dd>The description, ready to paste in a browser.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked by Purchase Options.</dd> <dt>int `i_payment_period`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The duration of the regular payment interval. </dd> <dt>int `i_session`</dt> <dd>This is only set for purchases of single sessions. The number of sessions booked simultaneously.</dd> <dt>int `id_program_category`</dt> <dd> This is only set for promotions. The ID of the promotion program category. One of the {@link \RsProgramCategorySid} constants. </dd> <dt>int `id_program_type`</dt> <dd> This is only set for promotions. The ID of the promotion program type. One of the {@link \RsProgramTypeSid} constants. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>bool `is_contract`</dt> <dd>If `true`, the Purchase Option requires a contract assignment. Otherwise, this will be `false`.</dd> <dt>bool `is_convert`</dt> <dd>If `true`, the Purchase Option converts to another instance upon expiration. Otherwise, this will be `false`.</dd> <dt>bool `is_renew`</dt> <dd>If `true`, the Purchase Option is renewable. Otherwise, this will be `false`.</dd> <dt>bool `is_renew_check`</dt> <dd> If `true`, the Purchase Option is renewable and the "auto-renew" option is turned on by default. Otherwise, this will be `false`. </dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `k_reward_prize`</dt> <dd>The key of the reward prize that can be used instead a Purchase Option to book the session.</dd> <dt>string `m_prorate`</dt> <dd>Payment for membership prorate. Not empty only if prorate payment is required.</dd> <dt>string `s_contract`</dt> <dd>The contract of the Purchase Option. This is only set if <var>is_contract</var> is `true`.</dd> <dt>string `s_payment_duration`</dt> <dd> This is only set for Purchase Options with the ''membership'' program type. The measurement unit of <var>i_payment_period</var>. </dd> <dt>string `s_promotion_convert`</dt> <dd> This is only set if <var>is_convert</var> is `true`. The title to use for the new Purchase Option instance upon auto-renewal. </dd> <dt>string `s_title`</dt> <dd>The title.</dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> </dl>'
                      a_reward_prize:
                        type: array
                        description: 'List of redeemable prizes which can be used to pay for service. Each element has the following fields: <dl> <dt>string `f_price`</dt> <dd>The price, always ''0''.</dd> <dt>int `i_limit`</dt> <dd>The limit of sessions that can be booked with reward prize.</dd> <dt>int `i_score`</dt> <dd>Prize price in points.</dd> <dt>int `id_purchase_item`</dt> <dd>The ID of Purchase Option type. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The key of the Purchase Option in the database. The table depends on <var>id_purchase_item</var>.</dd> <dt>string `k_reward_prize`</dt> <dd>Key of redeemable prize. <i>Primary key in {@link \RsRewardPrizeSql}.</i></dd> <dt>string `s_value`</dt> <dd>The unique identifier.</dd> <dt>string `text_title`</dt> <dd>User-friendly prize description.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'The list of session passes that might be used in booking process. Each element has the following fields: <dl> <dt>int `i_remain`</dt> <dd>Number of remaining visits on session pass.</dd> <dt>string `k_session_pass`</dt> <dd>Session pass key. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i></dd> <dt>int `id_purchase_item`</dt> <dd>Type of the session pass purchase. One of {@link \RsPurchaseItemSid} constants.</dd> <dt>string `s_title`</dt> <dd>Session pass title.</dd> </dl>'
                      is_single_default:
                        type: boolean
                        description: 'Indicates if the drop-in rate should be the default promotion.'
                      k_promotion_default:
                        type: string
                        description: 'The default Purchase Option key. This will be empty if the class has no default Purchase Option.'
      tags:
        - Purchase-Purchase56
  '/Wl/Book/Process/Purchase/PurchaseElementGroup.json':
    get:
      summary: 'Returns the pricing breakdown (totals, taxes, and discounts) for the given list of purchase items. Validates each item in `a_purchase_item` (type, key, installment eligibility, and prize applicability), applies discount codes, login-type discounts, and installment adjustments, then accumulates price, subtotal, discount, tax, and cost totals across all items and returns them as result fields.'
      parameters:
        - name: a_purchase_item
          in: query
          description: 'A list of purchase items. Each item is an associative array with the following keys: <dl> <dt>int `i_session`</dt> <dd> Number of sessions which are booked simultaneously. Make sense only when `id_purchase_item` = {@link \RsPurchaseItemSid::CLASS_PERIOD}. </dd> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item type. One of {@link \RsPurchaseItemSid}.</dd> <dt>string `k_id`</dt> <dd>The key of the purchase item in the database.</dd> <dt>string `k_login_prize`</dt> <dd>The key of the user''s prize. Not empty only if the user wants to make a free visit from the prize.</dd> <dt>string|null `k_pay_installment_template`</dt> <dd> Installment template key. <i>Primary key in {@link \Thoth\WlPay\Installment\Template\Sql} table.</i> This property is optional. * can only be set for the purchase option which supports installment plan. <i>See {@link \Wl\Purchase\Item\PurchaseItemAbstract::INSTALLMENT_ALLOW_USER} property.</i> * `null` if installment plan doesn''t exist for bought item; * `0` if installment plan doesn''t selected for bought item from the list of installment plans. NOTE: * Calculations of discounts and taxes for installment plans are for demonstration purposes only! * Installment is not an independent purchase item and has no discounts or taxes. * Installment is a division of the final amount (with taxes and discounts), of some purchase option, into N parts. </dd> <dt>string `k_reward_prize`</dt> <dd> The key of the reward prize. Not empty only if the user wants to redeem prize and use it to pay for a visit. </dd> <dt>string `uid`</dt> <dd>The key of the user for whom the purchase item is being bought.</dd> </dl>'
          schema:
            type: array
        - name: dtu_date
          in: query
          description: 'Date/time of session is booking. `null` until initialized.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'The key of the session to check for booking availability.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The key of the location in which the purchase is made. This is also the booking process location.'
          schema:
            type: string
        - name: text_discount_code
          in: query
          description: 'The discount code.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the current user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_tax:
                        type: array
                        description: 'A list of taxes for the given purchase options. Keys - tax keys, values - tax amounts.'
                      m_cost:
                        type: string
                        description: 'The total cost of the given purchase options.'
                      m_discount:
                        type: string
                        description: 'The amount of the whole discount for the given purchase options.'
                      m_discount_code:
                        type: string
                        description: 'The discount amount provided by a discount code for the given purchase options.'
                      m_discount_login:
                        type: string
                        description: 'The discount amount for the client type for the given purchase options.'
                      m_price:
                        type: string
                        description: 'The price of the given purchase options (with or without taxes, depending on regional standards).'
                      m_subtotal:
                        type: string
                        description: 'The amount of subtotal for the given purchase options.'
                      m_tax:
                        type: string
                        description: 'The amount of taxes for the given purchase options.'
      tags:
        - Purchase-PurchaseElementGroup
  '/Wl/Book/Process/Quiz/Quiz.json':
    get:
      summary: 'Defines list of required quizzes. Merges the quizzes required by the booking itself with quizzes tied to the selected purchase options, filters out internal (staff-only) quizzes for non-backend requests, and returns the unified list in `a_quiz`.'
      parameters:
        - name: a_purchase_item
          in: query
          description: 'The list of purchase items. Each element has the format <tt>[id_purchase_item]::[k_id]</tt>, where: This will be empty if no purchases are made for the booking. <dl> <dt>int `id_purchase_item`</dt> <dd>The ID of the purchase item. One of the {@link \RsPurchaseItemSid} constants.</dd> <dt>string `k_id`</dt> <dd>The item key. This depends on <var>id_purchase_item</var> of this array.</dd> </dl>'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_quiz:
                        type: array
                        description: 'The list of quizzes. Each element has the next structure: <dl> <dt>bool `is_require`</dt> <dd>Determines whether the quiz is required.</dd> <dt>string `k_quiz`</dt> <dd>The quiz key. <i>Primary key from {@link \Core\Quiz\QuizSql} table.</i></dd> </dl>'
      tags:
        - Quiz-Quiz
    post:
      summary: 'Finished booking process and save quiz responses (if quiz step is the last in booking wizard). Validates session selection and asset requirements, stores quiz responses in the booking process context, and attempts to complete the booking without payment when no payment step is needed. Returns visit keys, activity keys, and a flag indicating whether additional wizard steps are still required.'
      parameters:
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_quiz_response:
                  type: array
                  description: 'The list of quiz response keys. Keys are quiz keys. <i>Primary key from the {@link \Core\Quiz\QuizSql} table.</i> Values are response keys. <i>Primary key from the {@link \Core\Quiz\Response\ResponseSql} table.</i> Or the {@link \Wl\Quiz\Response\QuizResponse::RESPONSE_SKIP} to skip the quiz.'
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource:
                  type: array
                  description: 'The selected assets. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The order number of the asset (could be from 1 to the asset quantity).</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_select:
                  type: array
                  description: 'The selected sessions. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The user''s activity keys.'
                      a_visit:
                        type: array
                        description: 'The keys the bookings that have been made.'
                      is_next:
                        type: boolean
                        description: 'If `true`, the next steps of the booking wizard are required for the purchase or booking. Otherwise, this will be `false`.'
      tags:
        - Quiz-Quiz
  '/Wl/Book/Process/Resource/Resource.json':
    get:
      summary: 'Returns the list of available assets for the booking session, grouped by asset category. Loads all asset categories and their items available for the given class period or selected sessions, strips shared resources that clients should not choose individually, attaches image data and occupancy information, and returns the result as `a_resource_all`.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_all:
                        type: array
                        description: 'A list of asset categories which are available for specified session. Every element has next keys: <dl> <dt>bool[][] `a_client`</dt> <dd> A list of clients who have already occupied assets for this session. 1st level keys are asset keys. <i>Primary key from the {@link \RsResourceSql} table.</i> 2nd level keys are asset index numbers (starting from 1). Values are `true` if the asset spot is occupied. For example, to check if asset spot 10 with key ''125'' is occupied, check `a_client[''125''][''10'']`. </dd> <dt>array[] `a_resource_list`</dt> <dd> A list of available assets. Every element has next keys: <dl> <dt>int[][][] `a_class_period`</dt> <dd> List of resources available for booking sessions. The field structure is `[k_class_period][dtu_session][''a_available'']`. Contains indexes of resource available for each session. </dd> <dt>array `a_image`</dt> <dd> Asset image data. <i>Result of the {@link \RsResourceImage::data()} method.</i> <dl> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Set only when `sid_image` equals ''shape''.</dd> <dt>int `i_height`</dt> <dd>Height of the image in pixels.</dd> <dt>int `i_width`</dt> <dd>Width of the image in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` if no image was uploaded, `false` otherwise.</dd> <dt>string `sid_image`</dt> <dd>Image kind. One of {@link \Wl\Resource\Image\ImageSid} string constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. One of {@link \Wl\Resource\Image\ImageIconSid} string constants. Set only when `sid_image` equals ''icon''. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. One of {@link \Wl\Resource\Image\ImageShapeSid} string constants. Set only when `sid_image` equals ''shape''. </dd> <dt>string `url`</dt> <dd>Thumbnail image URL.</dd> </dl> </dd> <dt>int `i_index`</dt> <dd>The asset number. Actual for assets with a quantity more than <tt>1</tt>.</dd> <dt>int `i_quantity`</dt> <dd>Total number of the asset spots.</dd> <dt>int `i_use`</dt> <dd>Number of already used asset units.</dd> <dt>bool `is_current`</dt> <dd>City for the off-site location.</dd> <dt>string `k_city`</dt> <dd><tt>true</tt> means that this asset is selected by client, <tt>false</tt> - otherwise.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset in database. <i>Primary key {@link \RsResourceSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>The title of the asset.</dd> <dt>string `text_address`</dt> <dd>Address for the off-site location.</dd> <dt>string `text_guide`</dt> <dd>Additional address tips for the off-site location.</dd> <dt>string `text_postal`</dt> <dd>Postal code for the off-site location.</dd> </dl> </dd> <dt>bool `has_current`</dt> <dd><tt>true</tt> - has current resource in the list of available assets; <tt>false</tt> - otherwise.</dd> <dt>int `id_category`</dt> <dd>Type of the asset category. One of {@link \Wl\Resource\ResourceCategoryEnum} constants.</dd> <dt>bool `is_client_select`</dt> <dd><tt>true</tt> - the client selected the resource from the current group; <tt>false</tt> otherwise.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> - has selected resources; <tt>false</tt> - otherwise.</dd> <dt>bool `is_share`</dt> <dd> <tt>true</tt> resources in this category don''t belong to certain users, but to the entire session. <tt>false</tt> belong to specific users. </dd> <dt>string `k_resource_layout`</dt> <dd>The key of the asset layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `k_resource_type`</dt> <dd>The key of the asset category. <i>Primary key in {@link \RsResourceTypeSql} table.</i></dd> <dt>string `s_resource_type`</dt> <dd>The title of the asset category.</dd> </dl>'
      tags:
        - Resource-Resource
    post:
      summary: 'Saves the asset selection for the booking and attempts to book the session if no further steps are required. Validates and stores the selected assets and session data in the booking process context, then, when no quiz step is needed and payment is not required, attempts to complete the booking immediately. Returns visit keys, activity keys, and a flag indicating whether further wizard steps are needed.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource_select:
                  type: array
                  description: 'The selected assets. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The asset number. Applies only for assets with a quantity greater than <tt>1</tt>.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of a user''s activity. This won''t be empty only if the session(s) was booked at this step.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings that have been made. Not empty only if session(s) was booked on this step.'
                      is_next:
                        type: boolean
                        description: '`true` - the next steps of the booking wizard are required (for example, to purchase something to book the selected session). `false` - no further booking steps are required.'
      tags:
        - Resource-Resource
  '/Wl/Book/Process/Resource/Resource54.json':
    get:
      summary: 'Returns available assets for the booking, decoding the JSON-encoded session input before delegating to the parent. Deserializes the JSON-encoded session selection into `a_session` and then delegates to {@link ResourceApi::get()} to return the available asset categories and items for the booking.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: json_session
          in: query
          description: 'Selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Fields - IDs of sessions in database. Values - arrays of date/time when session is occurred. In MySQL format. In GMT. Serialized with JSON.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_all:
                        type: array
                        description: 'A list of asset categories which are available for specified session. Every element has next keys: <dl> <dt>bool[][] `a_client`</dt> <dd> A list of clients who have already occupied assets for this session. 1st level keys are asset keys. <i>Primary key from the {@link \RsResourceSql} table.</i> 2nd level keys are asset index numbers (starting from 1). Values are `true` if the asset spot is occupied. For example, to check if asset spot 10 with key ''125'' is occupied, check `a_client[''125''][''10'']`. </dd> <dt>array[] `a_resource_list`</dt> <dd> A list of available assets. Every element has next keys: <dl> <dt>int[][][] `a_class_period`</dt> <dd> List of resources available for booking sessions. The field structure is `[k_class_period][dtu_session][''a_available'']`. Contains indexes of resource available for each session. </dd> <dt>array `a_image`</dt> <dd> Asset image data. <i>Result of the {@link \RsResourceImage::data()} method.</i> <dl> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Set only when `sid_image` equals ''shape''.</dd> <dt>int `i_height`</dt> <dd>Height of the image in pixels.</dd> <dt>int `i_width`</dt> <dd>Width of the image in pixels.</dd> <dt>bool `is_empty`</dt> <dd>`true` if no image was uploaded, `false` otherwise.</dd> <dt>string `sid_image`</dt> <dd>Image kind. One of {@link \Wl\Resource\Image\ImageSid} string constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name. One of {@link \Wl\Resource\Image\ImageIconSid} string constants. Set only when `sid_image` equals ''icon''. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. One of {@link \Wl\Resource\Image\ImageShapeSid} string constants. Set only when `sid_image` equals ''shape''. </dd> <dt>string `url`</dt> <dd>Thumbnail image URL.</dd> </dl> </dd> <dt>int `i_index`</dt> <dd>The asset number. Actual for assets with a quantity more than <tt>1</tt>.</dd> <dt>int `i_quantity`</dt> <dd>Total number of the asset spots.</dd> <dt>int `i_use`</dt> <dd>Number of already used asset units.</dd> <dt>bool `is_current`</dt> <dd>City for the off-site location.</dd> <dt>string `k_city`</dt> <dd><tt>true</tt> means that this asset is selected by client, <tt>false</tt> - otherwise.</dd> <dt>string `k_resource`</dt> <dd>The key of the asset in database. <i>Primary key {@link \RsResourceSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>The title of the asset.</dd> <dt>string `text_address`</dt> <dd>Address for the off-site location.</dd> <dt>string `text_guide`</dt> <dd>Additional address tips for the off-site location.</dd> <dt>string `text_postal`</dt> <dd>Postal code for the off-site location.</dd> </dl> </dd> <dt>bool `has_current`</dt> <dd><tt>true</tt> - has current resource in the list of available assets; <tt>false</tt> - otherwise.</dd> <dt>int `id_category`</dt> <dd>Type of the asset category. One of {@link \Wl\Resource\ResourceCategoryEnum} constants.</dd> <dt>bool `is_client_select`</dt> <dd><tt>true</tt> - the client selected the resource from the current group; <tt>false</tt> otherwise.</dd> <dt>bool `is_select`</dt> <dd><tt>true</tt> - has selected resources; <tt>false</tt> - otherwise.</dd> <dt>bool `is_share`</dt> <dd> <tt>true</tt> resources in this category don''t belong to certain users, but to the entire session. <tt>false</tt> belong to specific users. </dd> <dt>string `k_resource_layout`</dt> <dd>The key of the asset layout. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i></dd> <dt>string `k_resource_type`</dt> <dd>The key of the asset category. <i>Primary key in {@link \RsResourceTypeSql} table.</i></dd> <dt>string `s_resource_type`</dt> <dd>The title of the asset category.</dd> </dl>'
      tags:
        - Resource-Resource54
    post:
      summary: 'Processes the asset selection step of the booking wizard, decoding the JSON-encoded session input before delegating to the parent. Deserializes the JSON-encoded session selection into `a_session` and then delegates to {@link ResourceApi::post()} to save the selected assets and advance the booking wizard.'
      parameters:
        - name: a_session
          in: query
          description: 'The selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
          schema:
            type: array
        - name: dt_date_gmt
          in: query
          description: 'Date/time to which session is booked.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'The mode type. One of the {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: '`true` if action is performed as a staff member; `false` otherwise. If `true` is sent, access to the business and to the client will be checked. If `false` is sent, user can book only for himself or for relatives if this is allowed in business settings.'
          schema:
            type: boolean
        - name: is_credit_card_check
          in: query
          description: 'Checking whether the client has a credit card (if configured in the business) will be skipped if this flag is set to `false`. Use this field with caution. The final booking will not use this flag, and the check will still be performed.'
          schema:
            type: boolean
        - name: json_session
          in: query
          description: 'Selected sessions. Only makes sense for session events. Optional parameter for GET request: if not passed, all available sessions will be used. Fields - IDs of sessions in database. Values - arrays of date/time when session is occurred. In MySQL format. In GMT. Serialized with JSON.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'Key of session which is booked.'
          schema:
            type: string
        - name: show_relation
          in: query
          description: '`true` to show "book for" option in booking wizard. `false` for default behavior.'
          schema:
            type: boolean
        - name: uid
          in: query
          description: 'The client key for which the booking is being made.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_repeat:
                  type: [array, "null"]
                  description: 'Information about the recurring booking: This will be `null` if the booking isn''t recurring. <dl> <dt>int[] `a_day`</dt> <dd> The days of week when the appointment repeat.One of the {@link \ADateWeekSid} constants. Should be passed for any type of repetition. </dd> <dt>int[] `a_week`</dt> <dd>Deprecated, use `a_day` instead!</dd> <dt>string `dl_end`</dt> <dd>Deprecated, use `dt_from` and `dt_to` instead!</dd> <dt>string `dt_from`</dt> <dd>Date to start recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>string `dt_to`</dt> <dd>Date to complete recurring booking. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::DATE}.</dd> <dt>int `i_count`</dt> <dd> The number of occurrences after which the appointment''s repeat cycle stops. Should be empty if the repeat cycle doesn''t stop after a certain number of occurrences. Expected for `id_repeat_end` = {@link \RsRepeatEndSid::COUNT}. </dd> <dt>int `i_duration`</dt> <dd>Count of days\weeks\months between recurring bookings.</dd> <dt>int `i_occurrence`</dt> <dd>Deprecated, use `i_count` instead!</dd> <dt>int `i_period`</dt> <dd>Deprecated, use `i_duration` instead!</dd> <dt>int `id_duration`</dt> <dd> The measurement unit of `i_period`. One of the {@link \ADurationSid} constants. Available duration units are: {@link \ADurationSid::DAY}, {@link \ADurationSid::WEEK}, {@link \ADurationSid::MONTH}. </dd> <dt>int `id_period`</dt> <dd>Deprecated, use `id_duration` instead! One of {@link \ADurationSid} constants.</dd> <dt>int `id_repeat_end`</dt> <dd>Possible ways to stop repeatable events. One of the {@link \RsRepeatEndSid} constants.</dd> </dl>'
                a_resource_select:
                  type: array
                  description: 'The selected assets. Every element has the next keys: <dl> <dt>int `i_index`</dt> <dd>The asset number. Applies only for assets with a quantity greater than <tt>1</tt>.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> </dl>'
                a_session_wait_list_unpaid:
                  type: array
                  description: 'The selected sessions on the wait list that are unpaid. Keys are class period keys. <i>Primary key from the {@link \RsClassPeriodSql} table.</i> Values are index arrays of date/time strings when the session occurred, in MySQL format and in GMT.'
                can_book:
                  type: boolean
                  description: 'Determines whether the class/event can be booked at this step or not. This is an external process control flag.'
                is_book_unpaid:
                  type: boolean
                  description: '`true` to book unpaid. `false` otherwise. Allows booking unpaid when client has a login promotion that can be used to pay for the service. Allowed in {@link ModeSid::WIDGET} mode only.'
                is_force_pay_later:
                  type: boolean
                  description: '`true` if user pressed ''Pay later''. `false` if user pressed ''Pay now''.'
                k_login_promotion:
                  type: string
                  description: 'Login promotion to be used to book a class.'
                k_login_promotion_guest_pass:
                  type: string
                  description: 'Host login promotion key that grants the guest pass used to pay for the guest''s visit. Empty string if the booking is not paid with a guest pass.'
                k_session_pass:
                  type: string
                  description: 'Session pass to be used to book a class.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_activity:
                        type: array
                        description: 'The keys of a user''s activity. This won''t be empty only if the session(s) was booked at this step.'
                      a_visit:
                        type: array
                        description: 'The keys of the bookings that have been made. Not empty only if session(s) was booked on this step.'
                      is_next:
                        type: boolean
                        description: '`true` - the next steps of the booking wizard are required (for example, to purchase something to book the selected session). `false` - no further booking steps are required.'
      tags:
        - Resource-Resource54
  '/Wl/Book/Process/Guest/GuestProfile.json':
    get:
      summary: 'Check if user exists. Looks up a guest by email within the specified business and service context. Returns the user key if an existing member is found whose email, birthday (when required), and location eligibility all pass validation.'
      parameters:
        - name: dl_birthday
          in: query
          description: 'Guest''s birthday in MySQL format. Empty if service not restricted by age.'
          schema:
            type: string
        - name: id_service
          in: query
          description: 'Type of the service to book.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Key of service to book. Depending on {@link GuestProfileApi::$id_service} value:, <ul> <li>{@link ServiceSid::CLASSES} - class key. <i>Primary key in {@link \RsClassSql} table.</i></li> <li>{@link ServiceSid::APPOINTMENT} - service key.<i>Primary key in {@link \RsServiceSql} table.</i></li> <li>{@link ServiceSid::BOOKABLE_ASSET} - resource key.<i>Primary key in {@link \RsResourceSql} table.</i></li> </ul>'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key.'
          schema:
            type: string
        - name: text_first_name
          in: query
          description: 'Guest''s first name.'
          schema:
            type: string
        - name: text_last_name
          in: query
          description: 'Guest''s last name.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Guest''s email.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'UID of found or created user.'
      tags:
        - Guest-GuestProfile
    post:
      summary: 'Creates new user. Creates a new guest profile (or reuses an existing non-member account) for the specified business and service, applying birthday and virtual-account rules, and returns the UID of the created or matched user.'
      parameters:
        - name: dl_birthday
          in: query
          description: 'Guest''s birthday in MySQL format. Empty if service not restricted by age.'
          schema:
            type: string
        - name: id_service
          in: query
          description: 'Type of the service to book.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Key of service to book. Depending on {@link GuestProfileApi::$id_service} value:, <ul> <li>{@link ServiceSid::CLASSES} - class key. <i>Primary key in {@link \RsClassSql} table.</i></li> <li>{@link ServiceSid::APPOINTMENT} - service key.<i>Primary key in {@link \RsServiceSql} table.</i></li> <li>{@link ServiceSid::BOOKABLE_ASSET} - resource key.<i>Primary key in {@link \RsResourceSql} table.</i></li> </ul>'
          schema:
            type: string
        - name: text_first_name
          in: query
          description: 'Guest''s first name.'
          schema:
            type: string
        - name: text_last_name
          in: query
          description: 'Guest''s last name.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Guest''s email.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_mode:
                  type: number
                  format: int32
                  description: 'The mode type used to determine the Lead Source for the created guest. One of the {@link ModeSid} constants. Default is {@link ModeSid::API}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'UID of found or created user.'
      tags:
        - Guest-GuestProfile
  '/Wl/Book/Repeat/Panel/Panel.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit key. Primary key in {@link \RsVisitSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_content:
                        type: string
                        description: 'Output HTML content.'
      tags:
        - Panel-Panel
  '/Wl/Business/Account/Subscription/PriceOverrideValidate.json':
    get:
      summary: ''
      parameters:
        - name: a_override
          in: query
          description: 'List of override options.'
          schema:
            type: array
        - name: cid_subscription
          in: query
          description: 'Subscription statuses.'
          schema:
            type: number
            format: int32
        - name: id_periodicity
          in: query
          description: 'Periodicity of payments.'
          schema:
            type: number
            format: int32
        - name: id_plan
          in: query
          description: 'Plan ID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscription-PriceOverrideValidate
  '/Wl/Business/Account/Subscription/SubscriptionRequest.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                cid_subscription:
                  type: number
                  format: int32
                  description: 'CID of the subscription for which the request is made.'
                id_plan:
                  type: number
                  format: int32
                  description: 'ID of the subscription plan requested by business.'
                k_business:
                  type: string
                  description: 'The business key within which request is made. Primary key in {@link \RsBusinessSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscription-SubscriptionRequest
  '/Wl/Business/Account/Subscription/AchieveSubscription.json':
    get:
      summary: 'Returns the Achieve subscription plan flags (free and white-label) for the given business. Used to determine which features and branding options are available in the Achieve mobile app for a business. For franchisees, the franchisor''s subscription is used. Call this before rendering any Achieve-specific UI that depends on the plan tier.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to check Achieve app subscription plan.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_free:
                        type: boolean
                        description: 'If Achieve app has free subscription plan is `true`, otherwise (white label, professional) - `false`.'
                      is_white_label:
                        type: boolean
                        description: 'If Achieve app has white label subscription plan is `true`, otherwise (free, professional) - `false`.'
      tags:
        - Subscription-AchieveSubscription
  '/Wl/Business/Account/Subscription/SearchData.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_subscription_status:
                        type: array
                        description: 'Subscription statuses.'
      tags:
        - Subscription-SearchData
  '/Wl/Business/Account/Subscription/SubscriptionInfo.json':
    get:
      summary: 'Gets information about subscription. Used in the backend settings UI to read the current state of a business subscription (e.g., Achieve or another product). Returns whether the subscription is active, the current plan tier, and the business locale. Requires backend access to the business.'
      parameters:
        - name: cid_subscription
          in: query
          description: 'CID of the subscription information of which is requested.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key for which subscription information is requested.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_locale:
                        type: number
                        format: int32
                        description: 'Locale ID of the business which subscription information is requested for.'
                      id_plan:
                        type: number
                        format: int32
                        description: 'Currently active plan ID for requested subscription. Depending on subscription type constant from one of {@link PlanSid} inheritors. Plans for `1250`: <dl> <dt>int `id_plan`</dt> <dd> Collections subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Collections\CollectionsSubscriptionSid} constants. </dd> </dl> Plans for `1266`: <dl> <dt>int `id_plan`</dt> <dd> API Access subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Api\ApiSubscriptionSid} constants. </dd> </dl> Plans for `861`: <dl> <dt>int `id_plan`</dt> <dd> Subscription Plan subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Base\BaseSubscriptionSid} constants. </dd> </dl> Plans for `1964`: <dl> <dt>int `id_plan`</dt> <dd> Go High Level subscription plan ID. One of {@link \Wl\Business\Account\Subscription\GoHighLevel\GoHighLevelSubscriptionSid} constants. </dd> </dl> Plans for `1370`: <dl> <dt>int `id_plan`</dt> <dd> Door Access subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Door\DoorSubscriptionSid} constants. </dd> </dl> Plans for `882`: <dl> <dt>int `id_plan`</dt> <dd> Book-a-Spot subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Asset\AssetSubscriptionSid} constants. </dd> </dl> Plans for `858`: <dl> <dt>int `id_plan`</dt> <dd> Achieve White Label App subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Achieve\AchieveSubscriptionSid} constants. </dd> </dl> Plans for `1349`: <dl> <dt>int `id_plan`</dt> <dd> Zapier Integration subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Zapier\ZapierSubscriptionSid} constants. </dd> </dl> Plans for `1868`: <dl> <dt>int `id_plan`</dt> <dd> CAASI subscription plan ID. One of {@link \Wl\Business\Account\Subscription\AiAgent\AiAgentSubscriptionSid} constants. </dd> </dl> Plans for `885`: <dl> <dt>int `id_plan`</dt> <dd> Customer Review Manager subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Review\ReviewSubscriptionSid} constants. </dd> </dl> Plans for `1965`: <dl> <dt>int `id_plan`</dt> <dd> Mailchimp / Constant Contact subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Email\EmailSubscriptionSid} constants. </dd> </dl> Plans for `2166`: <dl> <dt>int `id_plan`</dt> <dd> Constant Contact Integration subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Eml\ConstantContactSubscriptionSid} constants. </dd> </dl> Plans for `2165`: <dl> <dt>int `id_plan`</dt> <dd> Mailchimp Integration subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Eml\MailchimpSubscriptionSid} constants. </dd> </dl> Plans for `886`: <dl> <dt>int `id_plan`</dt> <dd> Rewards & Loyalty subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Reward\RewardSubscriptionSid} constants. </dd> </dl> Plans for `2225`: <dl> <dt>int `id_plan`</dt> <dd> Business Success Coaching subscription plan ID. One of {@link \Wl\Business\Account\Subscription\BusinessCoach\BusinessCoachSubscriptionSid} constants. </dd> </dl> Plans for `862`: <dl> <dt>int `id_plan`</dt> <dd> Direct Mail subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Postcard\PostcardSubscriptionSid} constants. </dd> </dl> Plans for `863`: <dl> <dt>int `id_plan`</dt> <dd> Presence subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Website\WebsiteSubscriptionSid} constants. </dd> </dl> Plans for `986`: <dl> <dt>int `id_plan`</dt> <dd> FitLIVE subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitlive\FitliveSubscriptionSid} constants. </dd> </dl> Plans for `857`: <dl> <dt>int `id_plan`</dt> <dd> Message Center subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Sms\SmsSubscriptionSid} constants. </dd> </dl> Plans for `864`: <dl> <dt>int `id_plan`</dt> <dd> Zoom subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Zoom\ZoomSubscriptionSid} constants. </dd> </dl> Plans for `1225`: <dl> <dt>int `id_plan`</dt> <dd> Finance subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Finance\FinanceSubscriptionSid} constants. </dd> </dl> Plans for `883`: <dl> <dt>int `id_plan`</dt> <dd> FitBUILDER subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitbuilder\FitbuilderSubscriptionSid} constants. </dd> </dl> Plans for `875`: <dl> <dt>int `id_plan`</dt> <dd> FitVID on Demand subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitvid\FitvidSubscriptionSid} constants. </dd> </dl> Plans for `884`: <dl> <dt>int `id_plan`</dt> <dd> FitZONE subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Fitzone\FitzoneSubscriptionSid} constants. </dd> </dl> Plans for `1838`: <dl> <dt>int `id_plan`</dt> <dd> Marketing Suite subscription plan ID. One of {@link \Wl\Business\Account\Subscription\MarketingSuite\MarketingSuiteSubscriptionSid} constants. </dd> </dl> Plans for `1133`: <dl> <dt>int `id_plan`</dt> <dd> Forms subscription plan ID. One of {@link \Wl\Business\Account\Subscription\Quiz\QuizSubscriptionSid} constants. </dd> </dl>'
                      is_active:
                        type: boolean
                        description: 'Whether subscription is active. `true` if subscription is active, `false` otherwise.'
      tags:
        - Subscription-SubscriptionInfo
  '/Wl/Business/Account/Subscription/PriceOverride.json':
    get:
      summary: ''
      parameters:
        - name: cid_subscription
          in: query
          description: 'Subscription statuses.'
          schema:
            type: number
            format: int32
        - name: id_periodicity
          in: query
          description: 'Periodicity of payments.'
          schema:
            type: number
            format: int32
        - name: id_plan
          in: query
          description: 'Plan ID.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_override:
                        type: array
                        description: 'List of override options.'
                      has_override:
                        type: boolean
                        description: 'Whether override is active.'
      tags:
        - Subscription-PriceOverride
  '/Wl/Business/Account/Subscription/SubscriptionIntend.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                can_prorate:
                  type: [boolean, "null"]
                  description: 'Defines whether prorate payment should be taken into account due to intention analyze. In a case of adjustment intention from customer side prorate is automatically applied. To get correct intention result for customer pass `true` here. In a case of adjustment intention from admin side prorate is not applied. To get correct intention result for admin pass `false` here. `null` to use the subscription''s default prorate behaviour.'
                can_trial:
                  type: boolean
                  description: 'Defines whether automatic trial period should be taken into account due to intention analyze. In a case of adjustment intention from customer side trial period automatically applied. To get correct intention result for customer pass `true` here. In a case of adjustment intention from admin side trial period not applied. To get correct intention result for admin pass `false` here.'
                cid_subscription:
                  type: number
                  format: int32
                  description: 'CID of the subscription intended to be changed.'
                dl_activate:
                  type: [string, "null"]
                  description: 'The date on which the subscription is intended to be activated. The activation date should not be specified if the subscription is already active. `null` if activation date is not intended to be specified. `0000-00-00` to use current date in business timezone to as set activation date.'
                i_payment_day:
                  type: [number, "null"]
                  description: 'The day of the month intended to be used as the regular payment day for recurring charges. This value determines on which day each month the user will be billed. `null` if payment day not intended to be changed.'
                id_periodicity:
                  type: [number, "null"]
                  description: 'ID of the subscription payment periodicity intended to be applied. One of {@link PeriodicitySid} const. `null` if periodicity is not intended to be changed.'
                id_plan:
                  type: [number, "null"]
                  description: 'ID of the subscription plan intended to be applied. `null` if plan is not intended to be changed.'
                json_convert:
                  type: string
                  description: 'JSON compressing information about subscription conversion. `null` if conversion is not intended to be changed.'
                json_override:
                  type: string
                  description: 'JSON compressing information about price overrides intended to be applied. `null` if override is not intended to be changed.'
                k_business:
                  type: string
                  description: 'The business key within which the subscription is intended to be changed. Primary key in {@link \RsBusinessSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount:
                        type: [array, "null"]
                        description: 'Subscription discount data. `null` in a case no discount considered.'
                      a_plan:
                        type: array
                        description: 'Contains payment plan information for intended changes.'
                      a_prorate:
                        type: [array, "null"]
                        description: 'Payment prorate data, if the intended subscription changes require payment recalculation. `null` in a case no prorate considered.'
                      a_trial:
                        type: [array, "null"]
                        description: 'Subscription trial period data. `null` in a case no trial considered.'
                      i_payment_entity:
                        type: number
                        format: int32
                        description: 'Number of the entities for which the payment intended to be processed. Depending on subscription type different entity used. See {@link SubscriptionIntendApi::$id_payment_entity}.'
                      id_currency:
                        type: number
                        format: int32
                        description: 'Currency ID within which intend would be processed.'
                      id_payment_entity:
                        type: number
                        format: int32
                        description: 'Type of the entities for which the payment intended to be processed. One of {@link PaymentSid} constants'
                      id_upgrade:
                        type: [number, "null"]
                        description: 'ID of the upgrade action intended to be applied. One of {@link UpgradeSid} const. `null` if plan upgrade is not intended to be.'
                      text_subscription:
                        type: string
                        description: 'Title of the subscription intended to be changed.'
      tags:
        - Subscription-SubscriptionIntend
  '/Wl/Business/Account/Transaction/TransactionOverrideAmount.json':
    put:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key for which transaction belong.'
                k_business_account_transaction:
                  type: string
                  description: 'Failed business account transaction for which amount should be changed.'
                m_amount:
                  type: [string, "null"]
                  description: 'Override transaction amount. `null` to set default value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Transaction-TransactionOverrideAmount
  '/Wl/Business/Account/Agreement/Agreement.json':
    put:
      summary: 'Discards the current agreement, and replaces it with a newly generated one.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Agreement-Agreement
  '/Wl/Business/Account/Expense/ExpenseSchedule.json':
    delete:
      summary: 'Removes scheduled expense payment.'
      parameters:
        - name: cid_expense
          in: query
          description: 'CID of expense schedule to be managed. One of {@link ExpenseAbstract} inheritors.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of a business within which expense should be managed. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: s_schedule_key
          in: query
          description: 'Unique key of scheduled expense to be managed. See {@link ExpenseAbstract::uniqueKey()}'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Expense-ExpenseSchedule
  '/Wl/Business/Account/Application/Application.json':
    post:
      summary: 'Upgrades current application account to specified account.'
      parameters:
        - name: id_application
          in: query
          description: 'Application package ID. One of {@link \Wl\Business\Account\ApplicationSid}.'
          schema:
            type: number
            format: int32
        - name: id_plan
          in: query
          description: 'Achieve subscription plan ID. One of {@link AchieveSubscriptionSid}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key from the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Application-Application
  '/Wl/Business/Edit/Merchant/Merchant.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: k_business_merchant
          in: query
          description: 'Specified payment merchant for which detail information should be return. Primary key in {@link \RsBusinessMerchantSql} table. <tt>null</tt> if it is need to return information about all payment processors for specified business.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Merchant-Merchant
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: k_business_merchant
          in: query
          description: 'Specified payment merchant for which detail information should be return. Primary key in {@link \RsBusinessMerchantSql} table. <tt>null</tt> if it is need to return information about all payment processors for specified business.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_merchant_list:
                        type: array
                        description: 'List of business merchants with detail information. Each element has next structure: <dl> <dt>array `a_merchant`</dt> <dd>Additional business merchant settings.</dd> <dt>array `a_pay_method`</dt> <dd> List of supported payment methods: <dl> <dt>int `id_pay_method`</dt> <dd>Id of the payment method. One of {@link \RsPayMethodSid} constants.</dd> <dt>string `text_title`</dt> <dd>Title of the payment method.</dd> </dl> </dd> <dt>int `id_pay_processor`</dt> <dd>ID payment gateway from {@link \Thoth\PayProcessor\PayProcessorSid}.</dd> <dt>string `k_business_merchant`</dt> <dd>Key of the business merchant. Primary key from {@link \RsBusinessMerchantSql} table.</dd> <dt>string `text_gateway_title`</dt> <dd>Payment gateway title.</dd> <dt>string `text_merchant_id`</dt> <dd>Merchant ID.</dd> <dt>string `text_processor_title`</dt> <dd>Payment processor title.</dd> </dl>'
                      is_admin:
                        type: boolean
                        description: 'Whether its admin.'
      tags:
        - Merchant-Merchant
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: k_business_merchant
          in: query
          description: 'Specified payment merchant for which detail information should be return. Primary key in {@link \RsBusinessMerchantSql} table. <tt>null</tt> if it is need to return information about all payment processors for specified business.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_business_merchant_save:
                  type: array
                  description: 'Contain detail information about business merchant to save.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Merchant-Merchant
  '/Wl/Business/Franchise/Push/ModuleSetup.json':
    post:
      summary: 'Performs validation of pushed data and schedules push tasks.'
      parameters:
        - name: cid_module
          in: query
          description: 'CID of the module to push.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_data:
                  type: array
                  description: 'Data for loading public variables of push module.'
                a_location:
                  type: array
                  description: 'Locations in which it is necessary to push an item.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Push-ModuleSetup
  '/Wl/Business/Franchise/Region/Region.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_franchise_region
          in: query
          description: 'Franchise region key. Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table. If <tt>null</tt> need create franchise region.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Region-Region
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_franchise_region
          in: query
          description: 'Franchise region key. Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table. If <tt>null</tt> need create franchise region.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location:
                  type: array
                  description: 'List of location.'
                text_description:
                  type: string
                  description: 'Region description.'
                text_title:
                  type: string
                  description: 'Region name.'
                uid_staff:
                  type: string
                  description: 'User staff key. Primary key in the {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_franchise_region:
                        type: [string, "null"]
                        description: 'Franchise region key. Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table. If <tt>null</tt> need create franchise region.'
      tags:
        - Region-Region
  '/Wl/Business/Franchise/Location/BusinessFranchiseLocation.json':
    get:
      summary: 'Returns country, region, state, city and location lists of the franchisor. Used on the franchisor''s website location finder to display all franchisee locations on a map or list, grouped by country, state, and city. Results can be filtered to only locations assigned to a specific franchise region or to exclude churned locations. Results are cached for 3 minutes.'
      parameters:
        - name: id_business_franchise_location
          in: query
          description: 'Determines which locations should be returned. One of the {@link BusinessFranchiseLocationSid} constants. If `null`, {@link BusinessFranchiseLocationSid::ALL} is used.'
          schema:
            type: number
            format: int32
        - name: is_include_churn
          in: query
          description: 'Determines whether to include churned/removed locations. If `true`, all locations are listed (regardless of their status).'
          schema:
            type: boolean
        - name: is_include_non_api
          in: query
          description: 'Determines whether to include locations marked to not be displayed on franchisor website. If `true`, all locations are listed (regardless of this setting).'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_city_list:
                        type: array
                        description: 'The city list. Each element has next structure: <dl> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>City title.</dd> </dl>'
                      a_country_list:
                        type: array
                        description: 'The country list. Each element has next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Country title.</dd> </dl>'
                      a_location_list:
                        type: array
                        description: 'The location list. Each element has the next structure: <dl> <dt>int `id_currency`</dt> <dd>Currency ID. One of {@link \Core\Locale\CurrencySid} constants.</dd> <dt>string `k_city`</dt> <dd>City key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_location`</dt> <dd>Location key. <i>Primary key in the {@link \RsLocationSql} table.</i></dd> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Location title.</dd> </dl>'
                      a_region_list:
                        type: array
                        description: 'The region list. Each element has the next structure: <dl> <dt>string `k_region`</dt> <dd>Franchise region key. <i>Primary key in the {@link \Wl\Business\Franchise\Region\RegionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>Franchise region title.</dd> </dl>'
                      a_state_list:
                        type: array
                        description: 'The state list. Each element has the next structure: <dl> <dt>string `k_country`</dt> <dd>Country key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `k_state`</dt> <dd>State key. <i>Primary key in the {@link \AGeoSql} table.</i></dd> <dt>string `text_title`</dt> <dd>State title.</dd> </dl>'
      tags:
        - Location-BusinessFranchiseLocation
  '/Wl/Business/Franchise/Member/FranchiseMember.json':
    get:
      summary: 'Checks type of the client, whether client can be added to the current business or not and his relationships, which should be added with him.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where we want to add client.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user, which we want to add to the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'List of relationships, which should be added with the client. <dl> <dt>int `id_family_relation`</dt> <dd>Relationship from the current user to the relative from {@link \RsFamilyRelationSid}.</dd> <dt>string `text_mail`</dt> <dd>Client''s email.</dd> <dt>string `text_name_full`</dt> <dd>Client''s full name.</dd> <dt>string `text_phone`</dt> <dd>Client''s phone number.</dd> <dt>string `text_relationship`</dt> <dd>Name of teh relationship from the relative to the current user.</dd> <dt>string `uid`</dt> <dd>User ID of the relative from {@link \PassportLoginSql}.</dd> <dt>string `url_image`</dt> <dd>Link to the client''s photo.</dd> </dl>'
      tags:
        - Member-FranchiseMember
    put:
      summary: 'Adds client and all his relatives to the business as travellers.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business, where we want to add client.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user, which we want to add to the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_empty_required_fields:
                        type: boolean
                        description: 'Checks if client has any required fields to be completed.'
      tags:
        - Member-FranchiseMember
  '/Wl/Business/Promote/Explorer/PromoteExplorer.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: [boolean, "null"]
                  description: 'Whether the Explorer promotion is active for the specified business/location.'
                k_business:
                  type: string
                  description: 'Business key. Primary key in {@link \RsBusinessSql}.'
                k_location:
                  type: string
                  description: 'Location key. Primary key in {@link \RsLocationSql}.'
                text_alias:
                  type: [string, "null"]
                  description: 'Resource string alias to be set as a part of explorer link.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_explorer:
                        type: string
                        description: 'Explorer URL for the specified business/location.'
      tags:
        - Explorer-PromoteExplorer
  '/Wl/Business/RegionTravel/ControlPanel/TravelStatus.json':
    get:
      summary: 'Get quick check status of the business, and travel info if available.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business that is travelling. `null` by default.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_early_data:
                        type: array
                        description: 'List of businesses that started transferring the earliest (max 100). Each element contains: <dl> <dt>string `k_business`</dt> <dd>Business Key. Primary key from {@link \RsBusinessSql} table.</dd> <dt>string `url_travel_panel`</dt> <dd>URL to the business'' travel control panel.</dd> </dl>'
                      a_status_message:
                        type: array
                        description: 'Data for displaying a status message about business travel. Contains: <dl> <dt>string `html_class`</dt> <dd>HTML class to use for the message element.</dd> <dt>string `text_message`</dt> <dd> Message to be displayed. Describes the state of the travel process. Describes quick check result if travel hasn''t started. </dd> </dl>'
                      a_step_list:
                        type: array
                        description: 'Data for each step recorded in the business travel process. Steps are ordered by (ascending): start date (zero dates last), status change date, then {@link TravelStepAbstract} CID. Steps do not exist if business never travelled. Each element represents a travel step and contains: <dl> <dt>int `cid_travel_step`</dt> <dd>Travel step class ID. ID for one of {@link \Wl\Business\RegionTravel\TravelStepAbstract} classes.</dd> <dt>string `dtu_start`</dt> <dd>Datetime for step start. Directly from {@link \Wl\Business\RegionTravel\TravelStepSql}</dd> <dt>string `dtu_status`</dt> <dd>Datetime for step status change. Directly from {@link \Wl\Business\RegionTravel\TravelStepSql}</dd> <dt>int `id_step_status`</dt> <dd>Step status ID. One of {@link \Wl\Business\RegionTravel\TravelStepStatusSid} constants.</dd> <dt>string `sid_step_status`</dt> <dd>Step status SID. One of {@link \Wl\Business\RegionTravel\TravelStepStatusSid} constants.</dd> <dt>string `text_step_status`</dt> <dd> Translated step status title. Title for one of {@link \Wl\Business\RegionTravel\TravelStepStatusSid} constants. </dd> <dt>string `text_class`</dt> <dd>Step class name for one of {@link \Wl\Business\RegionTravel\TravelStepAbstract} classes.</dd> <dt>string `text_title`</dt> <dd>Translated step title. Title for one of {@link \Wl\Business\RegionTravel\TravelStepAbstract} CIDs.</dd> <dt>string `text_time_start`</dt> <dd>Time that has elapsed since start of step in hh:mm:ss format. Empty string if there is no start time.</dd> <dt>string `text_time_status`</dt> <dd> Status time in hh:mm:ss format. Depends on the state of <tt>id_step_status</tt>. <ul> <li>Time between step start and step status change for {@link \Wl\Business\RegionTravel\TravelStepStatusSid::COMPLETE} and {@link \Wl\Business\RegionTravel\TravelStepStatusSid::FAILURE}.</li> <li>Time elapsed since status change for {@link \Wl\Business\RegionTravel\TravelStepStatusSid::COMPLETE}.</li> <li>Empty string for {@link \Wl\Business\RegionTravel\TravelStepStatusSid::SCHEDULED}.</li> </ul> </dd> </dl>'
                      a_travel_data:
                        type: array
                        description: 'Business travelling information is included if available. Data taken from {@link \Wl\Business\RegionTravel\TravelInfoSql}. If business did not travel yet, data is unavailable and the object is empty.'
                      i_travel_current:
                        type: number
                        format: int32
                        description: 'Current count of simultaneous business transfers.'
                      i_travel_max:
                        type: number
                        format: int32
                        description: 'Maximum number of simultaneous business transfers.'
                      id_region:
                        type: number
                        format: int32
                        description: 'Amazon region of the business.'
                      is_quick_check_pass:
                        type: boolean
                        description: '`true` if business passed quick check. `false` otherwise.'
                      text_quick_check_fail:
                        type: string
                        description: 'Message for why quick check failed.'
      tags:
        - ControlPanel-TravelStatus
  '/Wl/Business/Report/Customize/BusinessReportCustomize.json':
    get:
      summary: 'Returns the report config.'
      parameters:
        - name: id_report
          in: query
          description: 'Report ID. One of the {@link \RsReportSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_report_page
          in: query
          description: 'Report page ID. One of the {@link \RsReportPageSid} constants. <tt>null</tt> if report is rendered in a single report collection.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_button:
                        type: array
                        description: 'Button config.'
                      a_column:
                        type: array
                        description: 'Config of the report columns. <dl> <dt>string[] `a_hide`</dt> <dd>Column name list of the hidden columns and available in the report. See {@link \RsReport::info()}.</dd> <dt>string[] `a_visible`</dt> <dd>Column name list of the visible columns and available in the report. See {@link \RsReport::info()}.</dd> </dl>'
                      a_report_date:
                        type: array
                        description: 'List of the available report dates. <dl> <dt>bool `is_select`</dt> <dd>Whether the report date is selected.</dd> <dt>string `sid_report_date`</dt> <dd>Report date SID. One of the {@link \RsReportDateSid} constants.</dd> <dt>string `text_title`</dt> <dd>Title of the report date.</dd> </dl>'
                      has_client:
                        type: boolean
                        description: 'Whether that the report has a client column.'
                      id_report_date_default:
                        type: number
                        format: int32
                        description: 'Default report date ID. One of the {@link \RsReportDateSid} constants. `null` if default report date is not defined.'
                      is_report_date_default:
                        type: boolean
                        description: 'Whether predefined filter should be generated or user should select filter prior to generate report.'
                      is_report_generate_last:
                        type: boolean
                        description: 'Whether last generated date range should be opened by default.'
                      show_button_selection:
                        type: boolean
                        description: 'Whether buttons for date range selection should be shown nearby date filter.'
                      show_client_details:
                        type: string
                        description: 'Whether that client details information should be shown.'
      tags:
        - Customize-BusinessReportCustomize
    post:
      summary: 'Saves the report config.'
      parameters:
        - name: id_report
          in: query
          description: 'Report ID. One of the {@link \RsReportSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_report_date_default
          in: query
          description: 'Whether predefined filter should be generated or user should select filter prior to generate report.'
          schema:
            type: boolean
        - name: is_report_generate_last
          in: query
          description: 'Whether last generated date range should be opened by default.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_report_save
          in: query
          description: 'Report save key. Primary key in the {@link \RsReportSaveSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_button:
                  type: array
                  description: 'Button config.'
                a_column_selected:
                  type: array
                  description: 'Selected columns of the report.'
                show_button_selection:
                  type: boolean
                  description: 'Whether buttons for date range selection should be shown nearby date filter.'
                show_client_details:
                  type: string
                  description: 'Whether that client details information should be shown.'
                sid_report_date:
                  type: string
                  description: 'Selected report date SID.'
                sid_report_date_range:
                  type: string
                  description: 'Selected report date range SID.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Customize-BusinessReportCustomize
  '/Wl/Business/Sms/Settings/SmsOtpResend.json':
    post:
      summary: 'Initiates resend of OTP verification message. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Settings-SmsOtpResend
  '/Wl/Business/Sms/Settings/SmsAdminApplicationResubmit.json':
    post:
      summary: 'Resubmits A2P Application. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Settings-SmsAdminApplicationResubmit
  '/Wl/Business/Sms/Settings/SmsAdminBrandReset.json':
    post:
      summary: 'Resets A2P Brand. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Settings-SmsAdminBrandReset
  '/Wl/Business/Sms/Settings/SmsAdminCampaignDelete.json':
    post:
      summary: 'Resets A2P Brand. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Settings-SmsAdminCampaignDelete
  '/Wl/Business/Sms/Settings/SmsAdminStatusChange.json':
    post:
      summary: 'Resets A2P Brand. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_registration_status:
                  type: [number, "null"]
                  description: 'The new registration status to set.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Settings-SmsAdminStatusChange
  '/Wl/Business/Sms/Settings/SmsOptOut.json':
    put:
      summary: 'Updates SMS opt-out. errors contacting the SMS service provider API.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_opt_out:
                  type: [boolean, "null"]
                  description: 'Indicates if the business has opted-out of all SMS features entirely.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Settings-SmsOptOut
  '/Wl/Business/Sms/Unread/SmsUnread.json':
    delete:
      summary: 'Marks all messages in the dialog in the given business with a given user as read.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Unread-SmsUnread
    post:
      summary: 'Marks the last messages in the dialog in the given business with a given user as unread. Only messages that were sent by client after the last message from the business to the client will be marked as unread.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
                uid:
                  type: string
                  description: 'User key with whom you want to delete unread messages.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Unread-SmsUnread
  '/Wl/Business/Sms/Reads/SmsReads.json':
    post:
      summary: 'Adds SMS reads for individual reads.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
                uid:
                  type: [string, "null"]
                  description: 'User key with whom you want to read messages.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Reads-SmsReads
  '/Wl/Business/Sms/Pin/SmsPin.json':
    delete:
      summary: 'Marks all messages in the dialog in the given business with a given user as unpinned.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Pin-SmsPin
    post:
      summary: 'Marks the last messages in the dialog in the given business with a given user as pinned.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business. Primary key {@link \RsBusinessSql} table.'
                uid:
                  type: string
                  description: 'User key with whom you want to pin/unpin messages.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Pin-SmsPin
  '/Wl/Business/Config/Option/BusinessConfigOption.json':
    get:
      summary: 'Returns config option values for specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_option:
                        type: array
                        description: 'List of config option values. Key - config option class CID, value - config option value.'
      tags:
        - Option-BusinessConfigOption
  '/Wl/Business/AuthorizeSupport/Response/Response.json':
    get:
      summary: 'Grants or denies access to business location for staff member. Used to respond to a support access request: a business owner accepts or rejects temporary entry for a WellnessLiving support agent. Requires the Manage Business permission. Granting access triggers an email notification and expires after 24 hours; the result is broadcast in real time to the requesting staff member.'
      parameters:
        - name: is_grant
          in: query
          description: 'Determines whether the user will be granted access or if access will be revoked. If <tt>true</tt> - then grant access; If <tt>false</tt> - then revoke access. Revoking access is a scheduled task set to be run within the next 15 minutes.'
          schema:
            type: boolean
        - name: k_location
          in: query
          description: 'The key of the location to access.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user who will be granted access.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Response-Response
  '/Wl/Business/AuthorizeSupport/Request/Request.json':
    delete:
      summary: 'Cancels request of access to location.'
      parameters:
        - name: k_location
          in: query
          description: 'Location primary key in {@link \RsLocationSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Request-Request
    get:
      summary: 'Requests authorization of support employee to business location. Makes authorization if it is possible without special permission.'
      parameters:
        - name: k_location
          in: query
          description: 'Location primary key in {@link \RsLocationSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_pending:
                        type: boolean
                        description: '<tt>true</tt> - support must wait permission from franchisee. <tt>false</tt> - no need to wait; authorisation is already done.'
      tags:
        - Request-Request
  '/Wl/Business/Partner/Dashboard/Dashboard.json':
    get:
      summary: ''
      parameters:
        - name: id_chart
          in: query
          description: 'Chart format ID. One of {@link \Wl\Business\Partner\Dashboard\ChartEarningsSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business. Primary key from  {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_chart:
                        type: string
                        description: 'Data for chart report.'
      tags:
        - Dashboard-Dashboard
  '/Wl/Business/Partner/Contest/PartnerContest.json':
    post:
      summary: 'Update a contest.'
      parameters:
        - name: k_partner_contest
          in: query
          description: 'Contest key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_remove:
                  type: boolean
                  description: 'Weather the contest is visible to anyone or not.'
                is_visible:
                  type: boolean
                  description: 'Weather the contest is visible to the business or not.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_message:
                        type: string
                        description: 'The message to show in case of an error.'
                      s_status:
                        type: string
                        description: 'Status of the operation.'
      tags:
        - Contest-PartnerContest
  '/Wl/Business/Partner/Agreement/AgreementNotify.json':
    post:
      summary: 'Sets that a staff got acquainted with changes in partner program agreement.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Agreement-AgreementNotify
  '/Wl/Business/Type/ImageMarkup/ImageMarkup.json':
    get:
      summary: 'Returns the default image markup for the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business Key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_link_markup_image:
                        type: string
                        description: 'Link of the image markup.'
                      url_image_markup:
                        type: string
                        description: 'Url of the default image markup.'
      tags:
        - ImageMarkup-ImageMarkup
  '/Wl/Business/Application/Account/Account.json':
    post:
      summary: 'Saves account data of provider {@link \Wl\Business\Application\Account\AccountApi::$id_os} for business {@link \Wl\Business\Application\Account\AccountApi::$k_business}.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_os:
                  type: number
                  format: int32
                  description: 'ID of distribution provider (is directly related to operation system) to save data for. One of {@link \Core\Spa\OsSid} constants.'
                k_business:
                  type: string
                  description: 'ID of business to save data for. Primary key in {@link \RsBusinessSql} table.'
                s_login:
                  type: string
                  description: 'Account login to be saved.'
                s_password:
                  type: string
                  description: 'Account password to be saved.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Account-Account
  '/Wl/Business/Reward/Config/RewardConfig.json':
    get:
      summary: 'Returns the reward program configuration for the specified business. Used in the backend settings editor to show the current enabled or disabled state of the reward program before the staff member makes a change. Always reads live data, bypassing the cache.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_disabled_reward_program:
                        type: boolean
                        description: '<tt>true</tt> Reward program is disabled, <tt>false</tt> - otherwise.'
      tags:
        - Config-RewardConfig
    post:
      summary: 'Updates the reward program enabled or disabled state for the specified business. Used in the backend settings editor when a staff member turns the reward program on or off. The change is recorded in the reward audit log.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_disabled_reward_program:
                  type: boolean
                  description: '<tt>true</tt> Reward program is disabled, <tt>false</tt> - otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Config-RewardConfig
  '/Wl/Business/User/Subscribe/Subscribe.json':
    get:
      summary: 'Retrieves information about if user is subscribed on specified business or not. Used to pre-populate the notification preferences toggle in a client''s profile page. Shows whether the client has opted in to email and SMS communications from the business.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key used for users to subscribe, unsubscribe, and receive information about the status of the subscription.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose subscription status needs to be checked or switched to subscribed/unsubscribed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_subscribe:
                        type: [boolean, "null"]
                        description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an email subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false` unsubscribes the user in the business. This will be `null` if not set yet.'
                      is_subscribe_sms:
                        type: [boolean, "null"]
                        description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an SMS subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false1 unsubscribes the user in the business. This will be `null` if not set yet.'
      tags:
        - Subscribe-Subscribe
    put:
      summary: 'Subscribes or unsubscribes user on specified business. Called when a client changes their notification preferences. Controls whether the business can contact the client by email and by SMS.'
      parameters:
        - name: is_subscribe
          in: query
          description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an email subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false` unsubscribes the user in the business. This will be `null` if not set yet.'
          schema:
            type: [boolean, "null"]
        - name: is_subscribe_sms
          in: query
          description: 'Information about the user`s subscription. When getting information, `true` indicates the user has an SMS subscription in the business (`false` otherwise). When changing a subscription, `true` subscribes the user in the business. `false1 unsubscribes the user in the business. This will be `null` if not set yet.'
          schema:
            type: [boolean, "null"]
        - name: k_business
          in: query
          description: 'The business key used for users to subscribe, unsubscribe, and receive information about the status of the subscription.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The key of the user whose subscription status needs to be checked or switched to subscribed/unsubscribed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscribe-Subscribe
  '/Wl/Coupon/Edit/Region/RegionList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_coupon
          in: query
          description: 'Coupon key. <tt>null</tt> if coupon is not created yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_franchise_region:
                        type: array
                        description: 'List of franchise regions. Has next structure: <dl> <dt>bool `is_select`</dt> <dd><tt>true</tt> if region is selected, <tt>false</tt> otherwise.</dd> <dt>string `k_franchise_region`</dt> <dd>Franchise region key. Primary key in {@link \Wl\Coupon\Franchise\FranchiseRegionSql} table.</dd> <dt>string `text_title`</dt> <dd>Name of the franchise region.</dd> </dl>'
      tags:
        - Region-RegionList
  '/Wl/Franchise/Transfer/Edit/TransferEdit.json':
    get:
      summary: 'Gets information about start and end dates of franchise transfer.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_franchise_transfer
          in: query
          description: 'Franchise transfer key. Primary key in {@link \Wl\Franchise\Transfer\TransferSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_permanent:
                        type: boolean
                        description: '<tt>true</tt> if transfer can be edited to permanent, <tt>false</tt> otherwise.'
                      dt_end_current_local:
                        type: [string, "null"]
                        description: 'Current end date of temporary transfer.'
                      dt_start_current_local:
                        type: [string, "null"]
                        description: 'Current start date of temporary transfer.'
                      id_transfer_direction:
                        type: number
                        format: int32
                        description: 'Franchise transfer direction.'
      tags:
        - Edit-TransferEdit
    put:
      summary: 'Edits franchise transfer or or creates a new one if term of the transfer is increased (or transfer becomes permanent).'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_franchise_transfer
          in: query
          description: 'Franchise transfer key. Primary key in {@link \Wl\Franchise\Transfer\TransferSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dt_end_local:
                  type: [string, "null"]
                  description: 'New end date of temporary transfer.'
                dt_start_local:
                  type: [string, "null"]
                  description: 'New start date of temporary transfer.'
                id_transfer_period:
                  type: number
                  format: int32
                  description: 'Transfer period ID. One of {@link \Wl\Franchise\Transfer\TransferPeriodSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-TransferEdit
  '/Wl/Insurance/Reimbursement/Import/FileImportSos.json':
    delete:
      summary: ''
      parameters:
        - name: i_year
          in: query
          description: 'Year, when reimbursement import was started.'
          schema:
            type: number
            format: int32
        - name: id_month
          in: query
          description: 'Month, when reimbursement import was started.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key, where reimbursement import was started. Primary key in the table {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_partner
          in: query
          description: 'Insurance partner key, where reimbursement import was started. Primary key in the table {@link PartnerSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Import-FileImportSos
  '/Wl/Insurance/Reimbursement/Export/Accumulation.json':
    post:
      summary: 'Generates reimbursement export files for providers and uploads them into S3.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_partner
          in: query
          description: 'Partner key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Export-Accumulation
  '/Wl/Insurance/Reimbursement/Refuse/ReimbursementRefuseEdit.json':
    delete:
      summary: ''
      parameters:
        - name: k_reimbursement_refuse
          in: query
          description: 'Reimbursement refuse key. Primary key in {@link \Wl\Insurance\Reimbursement\Refuse\ReimbursementRefuseSql} table, <tt>null</tt> if field is not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Refuse-ReimbursementRefuseEdit
    get:
      summary: ''
      parameters:
        - name: k_reimbursement_refuse
          in: query
          description: 'Reimbursement refuse key. Primary key in {@link \Wl\Insurance\Reimbursement\Refuse\ReimbursementRefuseSql} table, <tt>null</tt> if field is not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_insurance_provider:
                        type: [string, "null"]
                        description: 'Insurance provider key. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table, <tt>null</tt> if field is not set.'
                      text_code:
                        type: string
                        description: 'Code of the reason, which should be unique per partner.'
                      text_description_provider:
                        type: string
                        description: 'Description of the refuse from provider.'
                      text_display:
                        type: string
                        description: 'Description of the refuse, which should be shown in reimbursement reports. This field is filled by franchise owner.'
      tags:
        - Refuse-ReimbursementRefuseEdit
    post:
      summary: ''
      parameters:
        - name: k_reimbursement_refuse
          in: query
          description: 'Reimbursement refuse key. Primary key in {@link \Wl\Insurance\Reimbursement\Refuse\ReimbursementRefuseSql} table, <tt>null</tt> if field is not set.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_insurance_provider:
                  type: [string, "null"]
                  description: 'Insurance provider key. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table, <tt>null</tt> if field is not set.'
                text_code:
                  type: string
                  description: 'Code of the reason, which should be unique per partner.'
                text_description_provider:
                  type: string
                  description: 'Description of the refuse from provider.'
                text_display:
                  type: string
                  description: 'Description of the refuse, which should be shown in reimbursement reports. This field is filled by franchise owner.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Refuse-ReimbursementRefuseEdit
  '/Wl/Insurance/Reimbursement/Refuse/ReimbursementRefuseList.json':
    get:
      summary: ''
      parameters:
        - name: id_refuse_message
          in: query
          description: 'Status of the refuse message. One of {@link \Wl\Insurance\Reimbursement\Refuse\Filter\FilterRefuseMessageSelectSid} constants. Default value {@link \Wl\Insurance\Reimbursement\Refuse\Filter\FilterRefuseMessageSelectSid::ALL}.'
          schema:
            type: number
            format: int32
        - name: k_insurance_provider
          in: query
          description: 'Insurance provider key. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table, <tt>null</tt> if field is not set.'
          schema:
            type: [string, "null"]
        - name: text_code
          in: query
          description: 'Code of the reason, which should be unique per partner.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list refuse translate message: <dl> <dt>string `k_reimbursement_refuse`</dt> <dd> Insurance reimbursement refuse key. Primary key from {@link \Wl\Insurance\Reimbursement\Refuse\ReimbursementRefuseSql} table. </dd> <dt>string `text_code`</dt> <dd>Code of the reason, which should be unique per partner.</dd> <dt>string `text_description_provider`</dt> <dd>Description of the refuse from provider.</dd> <dt>string `text_display`</dt> <dd> Description of the refuse, which should be shown in reimbursement reports. This field is filled by franchise owner. </dd> <dt>string `text_insurance_provider`</dt> <dd>Insurance provider name.</dd> </dl>'
      tags:
        - Refuse-ReimbursementRefuseList
  '/Wl/Insurance/Enrollment/Field/EnrollmentFieldList.json':
    get:
      summary: 'Returns the list of required partner fields for the specified wellness program. Returns enrollment fields defined by the insurance partner along with reimbursement account fields if the program requires reimbursement. Each field includes validation rules, display labels, and configuration options for rendering the enrollment form.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'The partner field list: <dl> <dt>array[] `a_account`</dt> <dd> The list of reimbursement account fields. If the program doesn''t require reimbursement, this list will be empty. Each element has the same structure as elements of `a_field`: <dl> <dt>string[] `a_config`</dt> <dd> Additional configuration for the field. For the account type field, keys are account type titles and values are {@link \RsPayBankAchTypeSid} constants. Empty array for other fields. </dd> <dt>int|null `i_length`</dt> <dd>Maximum length of the field value. `null` if no limit.</dd> <dt>bool `is_disable`</dt> <dd>`true` if the field is disabled and should not be shown.</dd> <dt>bool `is_required`</dt> <dd>`true` if the field is required.</dd> <dt>string `k_field`</dt> <dd>The field identifier key.</dd> <dt>string `s_regular`</dt> <dd>Regular expression for client-side validation. Empty string if no validation pattern.</dd> <dt>string `text_display_name`</dt> <dd>The label text for the field displayed to the user.</dd> <dt>string `text_error_message`</dt> <dd>The error message to display if the field value is invalid.</dd> <dt>string `text_field_name`</dt> <dd>The internal name of the field.</dd> </dl> </dd> <dt>array[] `a_field`</dt> <dd> The list of partner enrollment fields. Each element: <dl> <dt>string[] `a_config`</dt> <dd> Additional configuration for the field: <dl> <dt>string[] `a_option`</dt> <dd> Options for dropdown fields. Keys are display titles, values are option values. Empty array if the field is not a dropdown. </dd> <dt>string `text_type`</dt> <dd>The type of text input. Present only for text-type fields.</dd> </dl> </dd> <dt>int|null `i_length`</dt> <dd>Maximum length of the field value. `null` if no limit.</dd> <dt>bool `is_disable`</dt> <dd>`true` if the field is disabled and should not be shown.</dd> <dt>bool `is_required`</dt> <dd>`true` if the field is required.</dd> <dt>string `k_field`</dt> <dd>The field key. <i>Primary key in the {@link \Wl\Integration\Curves\PartnerFieldSql} table.</i></dd> <dt>string `s_regular`</dt> <dd>Regular expression for client-side validation. Empty string if no validation pattern.</dd> <dt>string `text_display_name`</dt> <dd>The label text for the field displayed to the user.</dd> <dt>string `text_error_message`</dt> <dd>The error message to display if the field value is invalid.</dd> <dt>string `text_field_name`</dt> <dd>The internal name of the field.</dd> </dl> </dd> </dl>'
      tags:
        - Field-EnrollmentFieldList
    post:
      summary: 'Validates the list of fields filled in by the user for enrollment. Accepts the values submitted by the user for the wellness program enrollment form and validates them against the partner''s field rules before passing them to the payment API.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business in which the enrollment is performed.'
          schema:
            type: string
        - name: k_wellness_program
          in: query
          description: '"Wellness Program" key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_account:
                  type: array
                  description: 'Reimbursement account information. Keys - field name. Values - value entered by user. <i>Validation is performed by {@link EnrollmentFieldList::setAccountForm()} method.</i>'
                a_field:
                  type: array
                  description: 'List of fields that the user has filled in for enrollment. Keys refer specifically to field keys. <i>Primary keys in {@link \Wl\Integration\Curves\PartnerFieldSql} table.</i> Values refer specifically to values entered by the user. <i>Validation is performed by {@link EnrollmentFieldList::setFieldForm()} method.</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Field-EnrollmentFieldList
  '/Wl/Integration/Curves/Partner/PartnerEdit.json':
    delete:
      summary: 'Deletes a partner.'
      parameters:
        - name: k_partner
          in: query
          description: 'If a partner is edited, its key is stored here. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table. <tt>null</tt> if a new partner should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Partner-PartnerEdit
    get:
      summary: 'Returns information about a partner.'
      parameters:
        - name: k_partner
          in: query
          description: 'If a partner is edited, its key is stored here. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table. <tt>null</tt> if a new partner should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_channel_type:
                        type: number
                        format: int32
                        description: 'Channel type ID. One of {@link \Wl\Integration\Curves\ChannelTypeSid} constants.'
                      id_curves_country:
                        type: number
                        format: int32
                        description: 'Curves country ID. One of {@link \Wl\Integration\Curves\CurvesCountrySid} constants.'
                      id_deal_type:
                        type: number
                        format: int32
                        description: 'Deal type ID. One of {@link \Wl\Integration\Curves\DealTypeSid} constants.'
                      id_reimbursement_type:
                        type: number
                        format: int32
                        description: 'Reimbursement type ID. One of {@link \Wl\Insurance\Reimbursement\ReimbursementTypeSid} constants.'
                      is_active:
                        type: boolean
                        description: 'Determines whether the program is active.'
                      is_reimbursement_information_required:
                        type: boolean
                        description: 'Determines whether the ACH account is required.'
                      s_partner:
                        type: string
                        description: 'Partner key in Curves. Need for update wellness program.'
                      text_partner:
                        type: string
                        description: 'Name of the curves partner.'
      tags:
        - Partner-PartnerEdit
    post:
      summary: 'Saves a partner.'
      parameters:
        - name: k_partner
          in: query
          description: 'If a partner is edited, its key is stored here. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table. <tt>null</tt> if a new partner should be created. This value is required for edit and delete operations.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_channel_type:
                  type: number
                  format: int32
                  description: 'Channel type ID. One of {@link \Wl\Integration\Curves\ChannelTypeSid} constants.'
                id_curves_country:
                  type: number
                  format: int32
                  description: 'Curves country ID. One of {@link \Wl\Integration\Curves\CurvesCountrySid} constants.'
                id_deal_type:
                  type: number
                  format: int32
                  description: 'Deal type ID. One of {@link \Wl\Integration\Curves\DealTypeSid} constants.'
                id_reimbursement_type:
                  type: number
                  format: int32
                  description: 'Reimbursement type ID. One of {@link \Wl\Insurance\Reimbursement\ReimbursementTypeSid} constants.'
                is_active:
                  type: boolean
                  description: 'Determines whether the program is active.'
                is_reimbursement_information_required:
                  type: boolean
                  description: 'Determines whether the ACH account is required.'
                text_partner:
                  type: string
                  description: 'Name of the curves partner.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_partner:
                        type: [string, "null"]
                        description: 'If a partner is edited, its key is stored here. Primary key in {@link \Wl\Integration\Curves\PartnerSql} table. <tt>null</tt> if a new partner should be created. This value is required for edit and delete operations.'
      tags:
        - Partner-PartnerEdit
  '/Wl/Integration/Curves/Partner/PartnerList.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: [array, "null"]
                        description: 'A list of curves partner.'
      tags:
        - Partner-PartnerList
  '/Wl/Integration/FacilityAccess/Brivo/Configuration.json':
    get:
      summary: 'Returns Facility Access configuration set up by business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business to get information for. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_brivo_enabled:
                        type: boolean
                        description: 'Represents if Brivo integration is enabled, and business has configured Facility Access.'
                      is_brivo_invitation_enabled:
                        type: boolean
                        description: 'Represents if Brivo feature of users invitation is enabled. Depends on {@link \Wl\Integration\FacilityAccess\Brivo\ConfigurationApi::$is_brivo_enabled} too.'
      tags:
        - Brivo-Configuration
  '/Wl/Integration/FacilityAccess/Brivo/Upgrade.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_date:
                        type: string
                        description: 'Next billing date.'
      tags:
        - Brivo-Upgrade
  '/Wl/Integration/GoHighLevel/Location/Location.json':
    delete:
      summary: ''
      parameters:
        - name: a_location
          in: query
          description: 'Location key.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Location-Location
    post:
      summary: ''
      parameters:
        - name: a_location
          in: query
          description: 'Location key.'
          schema:
            type: array
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_location_id:
                  type: string
                  description: 'Go High Level location id.'
                text_token:
                  type: string
                  description: 'Go High Level location token.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_account:
                        type: string
                        description: 'Go High Level account name.'
                      text_token:
                        type: string
                        description: 'Go High Level location token.'
      tags:
        - Location-Location
  '/Wl/Integration/GoHighLevel/Subscription/Subscription.json':
    put:
      summary: ''
      parameters:
        - name: id_plan
          in: query
          description: 'Subscription plan that should be activated.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Subscription-Subscription
  '/Wl/Login/Attendance/Add/Add.json':
    get:
      summary: 'Gets information about ability of user to pay for given session in any ways. Returns available Purchase Options and session passes that can be used to pay for the specified class session, along with the session price, the client''s account balance, and whether the session is free.'
      parameters:
        - name: dt_date_global
          in: query
          description: 'The start date and time of the class in GMT and MySQL format.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business in which the request must be processed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'The client user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_login_promotion:
                        type: array
                        description: 'Any of the client memberships that can be used to pay for the session. Every element is an array with the following keys: <dl> <dt>string `k_login_promotion`</dt> <dd> The login promotion key, available to pay for the session. <i>Primary key in {@link \RsLoginPromotionSql} table.</i> </dd> <dt>string `text_title`</dt> <dd>The title of the login promotion.</dd> </dl>'
                      a_session_pass:
                        type: array
                        description: 'Any user''s session passes that can be used to pay for the session. Every element is an array with the following keys: <dl> <dt>string `k_session_pass`</dt> <dd> The session pass key, available to pay for the session. <i>Primary key in {@link \Wl\Session\Pass\Sql} table.</i> </dd> <dt>string `text_title`</dt> <dd>The title of the session pass.</dd> </dl>'
                      is_free:
                        type: boolean
                        description: 'If `true`, the session is free with no methods of payment. If `false`, the session can be paid for.'
                      k_login_promotion:
                        type: string
                        description: 'The key of the user''s promotion to be used for booking. If empty, use any suitable user''s promotion.'
                      k_session_pass:
                        type: string
                        description: 'The key of a session pass that can be used for a single session payment. If no such payment is available, `0` will be returned.'
                      m_account:
                        type: string
                        description: 'Account balance.'
                      m_price:
                        type: [string, "null"]
                        description: 'The price of the session, including any taxes and discounts. If `null`, the price of the session hasn''t been loaded yet.'
                      m_rest:
                        type: [string, "null"]
                        description: 'The user''s account balance if they were charged the {@link AddApi::$m_price} amount.'
      tags:
        - Add-Add
    post:
      summary: 'Adds client to attendance list. Books the specified client into the given class session using the chosen payment option (debit, Purchase Option, session pass, or unpaid), and returns the resulting visit key, visit status, and a store URL if payment is still required.'
      parameters:
        - name: dt_date_global
          in: query
          description: 'The start date and time of the class in GMT and MySQL format.'
          schema:
            type: string
        - name: is_event_single
          in: query
          description: 'Defines whether only single session can be booked for block event. <tt>true</tt> Only current session of the block event will be booked in a case if staff event has appropriate setting to do this action. In this case this session will be considered as session out of event block. <tt>false</tt> all available event sessions will be booked. In this case session will be considered as part of event block.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of the business in which the request must be processed.'
          schema:
            type: string
        - name: k_class_period
          in: query
          description: 'The class period key.'
          schema:
            type: string
        - name: uid_client
          in: query
          description: 'The client user key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_add_option:
                  type: number
                  format: int32
                  description: 'Determines how the payment was handled for the session. One of the {@link AddOptionSid} constants.'
                id_mode:
                  type: number
                  format: int32
                  description: 'Determines how the session was booked. One of the {@link ModeSid} constants. We recommend using the `WEB_BACKEND` value.'
                k_login_promotion:
                  type: string
                  description: 'The key of the user''s promotion to be used for booking. If empty, use any suitable user''s promotion.'
                k_session_pass:
                  type: string
                  description: 'The key of a session pass that can be used for a single session payment. If no such payment is available, `0` will be returned.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_visit:
                        type: number
                        format: int32
                        description: 'The status of the visit. One of the {@link VisitSid} constants.'
                      is_paid:
                        type: boolean
                        description: 'If `true`, the visit was automatically paid for in any available way during the booking. If `false`, the visit wasn''t automatically paid for.'
                      k_visit:
                        type: string
                        description: 'The key of the booked visit. This will be set on success. This value will be needed if the session still needs to be paid for.'
                      url_store:
                        type: string
                        description: 'The URL link to the store to allow for the payment of the visit. This link is for web only.'
      tags:
        - Add-Add
  '/Wl/Login/Attendance/Row/Row.json':
    get:
      summary: ''
      parameters:
        - name: dtu_date
          in: query
          description: 'Date.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Visit key.'
          schema:
            type: string
        - name: k_visit
          in: query
          description: 'Visit key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_row:
                        type: array
                        description: 'Service image.'
      tags:
        - Row-Row
  '/Wl/Login/Attendance/Design/Customize.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field:
                        type: array
                        description: 'Field settings of attendance list. The key is settings name (sid constant from {@link \Wl\Login\Attendance\Design\FieldListSid}) and the value is settings value.'
      tags:
        - Design-Customize
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field:
                  type: array
                  description: 'Field settings of attendance list. The key is settings name (sid constant from {@link \Wl\Login\Attendance\Design\FieldListSid}) and the value is settings value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Design-Customize
  '/Wl/Login/Member/VaccinationStatus/VaccinationStatus.json':
    post:
      summary: 'Sets the vaccination status for a user.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key, primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key, primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_vaccination_status:
                  type: number
                  format: int32
                  description: 'The new vaccination status. One of {@link VaccinationStatusSid}.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - VaccinationStatus-VaccinationStatus
  '/Wl/Login/Member/DynamicId/DynamicId.json':
    get:
      summary: 'Returns the member''s barcode and its expiry time for the specified business and user. If the business uses dynamic barcodes, generates or refreshes a time-limited barcode and returns its value, expiry countdown, and an image URL. If the business uses static barcodes, returns the member''s static ID with a zero expiry.'
      parameters:
        - name: is_refresh
          in: query
          description: '`true` if a new dynamic ID should be generated. `false` if the existing dynamic ID can be used, if it has not expired. If expired, a new dynamic ID will be generated anyway.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User unique identifier.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_expire:
                        type: number
                        format: int32
                        description: 'Number of seconds left until the dynamic ID expires. Zero means that business uses static barcode.'
                      text_barcode:
                        type: string
                        description: 'Barcode of the member. Dynamic, if business uses barcode as dynamic ID. Or static, if business uses static ID. In this case {@link DynamicIdApi::$i_expire} will be zero.'
                      url_barcode:
                        type: string
                        description: 'URL of the barcode image.'
      tags:
        - DynamicId-DynamicId
  '/Wl/Login/Promotion/Cancel/NotificationPattern.json':
    get:
      summary: 'Prepares notification pattern data of cancelled promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to which the promotion belongs. Primary key in {@link \RsBusinessSql} table. <tt>null</tt> when not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_promotion
          in: query
          description: 'Key of login promotion to update options for. Primary key in {@link \RsLoginPromotionSql} table. <tt>null</tt> when not initialized.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the user performing the request. Primary key in {@link \PassportLoginSql} table. <tt>null</tt> when not initialized.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dl_now:
                        type: string
                        description: 'Date now (local).'
                      has_upcoming_visits:
                        type: boolean
                        description: '`true` if there are active upcoming visits paid with the membership we are going to cancel. `false` - otherwise.'
                      id_cancel_policy:
                        type: number
                        format: int32
                        description: 'Policy, whether client can use remaining visits after cancellation or not.'
                      is_inform_mail:
                        type: [boolean, "null"]
                        description: 'Mail confirmation enabled. <tt>true</tt> when mail confirmation enabled, <tt>false</tt> otherwise. <tt>null</tt> when not initialized.'
                      is_inform_push:
                        type: [boolean, "null"]
                        description: 'Push confirmation enabled. <tt>true</tt> when push confirmation enabled, <tt>false</tt> otherwise. <tt>null</tt> when not initialized.'
                      is_inform_sms:
                        type: [boolean, "null"]
                        description: 'SMS confirmation enabled. <tt>true</tt> when sms confirmation enabled, <tt>false</tt> otherwise. <tt>null</tt> when not initialized.'
                      m_terminate_fee:
                        type: [string, "null"]
                        description: 'Cancellation fee amount. `null` if no need charge fee.'
                      sid_mail:
                        type: string
                        description: 'SID of notification form. String representation of one from {@link \RsMailSid} class constants.'
                      sid_option_type:
                        type: string
                        description: 'Option type. String representation of one from {@link \RsProgramTypeSid} class constants.'
      tags:
        - Cancel-NotificationPattern
  '/Wl/Login/Promotion/Cancel/MailPatternLive.json':
    get:
      summary: 'Gets customization form HTML content.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: sid_mail_form
          in: query
          description: 'SID of notification form. String representation of one from {@link \RsMailFormSid} class constants. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_form:
                        type: string
                        description: 'HTML form content.'
      tags:
        - Cancel-MailPatternLive
    post:
      summary: 'Creates live template.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid_actor
          in: query
          description: 'UID user''s key of the actor. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_deactivation:
                  type: string
                  description: 'Deactivation date.'
                id_mail_copy_business:
                  type: [number, "null"]
                  description: 'Type of copy to business on <var>s_business_mail</var>, one of {@link \RsMailCopySid}. <tt>null</tt> if not initialized.'
                is_campaign:
                  type: boolean
                  description: 'Whether this notification should be tracked as a campaign.'
                is_mail:
                  type: boolean
                  description: '<tt>true</tt> if user must get email,<tt>false</tt> otherwise.'
                is_push:
                  type: boolean
                  description: '<tt>true</tt> if user must get push-notification,<tt>false</tt> otherwise.'
                is_sms:
                  type: boolean
                  description: '<tt>true</tt> if user must get SMS notification,<tt>false</tt> otherwise.'
                s_business_mail:
                  type: [string, "null"]
                  description: 'Business mail. <tt>null</tt> if not initialized.'
                s_business_name:
                  type: [string, "null"]
                  description: 'Business name. <tt>null</tt> if not initialized.'
                s_business_reply:
                  type: [string, "null"]
                  description: 'Business reply. <tt>null</tt> if not initialized.'
                s_campaign:
                  type: [string, "null"]
                  description: 'Company name. <tt>null</tt> if not initialized.'
                s_html:
                  type: [string, "null"]
                  description: 'Content of email. <tt>null</tt> if not initialized.'
                s_push:
                  type: [string, "null"]
                  description: 'Content of push notification. <tt>null</tt> if not initialized.'
                s_sms:
                  type: [string, "null"]
                  description: 'Content of SMS. <tt>null</tt> if not initialized.'
                s_subject:
                  type: [string, "null"]
                  description: 'Message subject. <tt>null</tt> if not initialized.'
                s_text:
                  type: [string, "null"]
                  description: 'Content of email as text. <tt>null</tt> if not initialized.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_mail_pattern_live:
                        type: string
                        description: 'Key of the mail pattern. Primary key from {@link \RsMailPatternLiveSql} table.'
      tags:
        - Cancel-MailPatternLive
  '/Wl/Login/Promotion/Convert/Convert.json':
    delete:
      summary: 'Removes conversion and reset the after expiration setting to previous state. Validates access, removes the scheduled conversion record for the given purchased promotion, restores the previous auto-renew state, and reschedules the payment if the promotion is a membership type.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Convert-Convert
    get:
      summary: 'Returns the current conversion configuration and the list of promotions available to convert to. Returns the current conversion type, scheduled date, target promotion, hold period dates, next payment date, expiration date, and the list of promotions available as conversion targets for the given purchased promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_promotion:
                        type: array
                        description: 'Promotion data containing the following structure:. <dl> <dt>string `k_promotion`</dt> <dd>The promotion key. <i>Primary key in {@link \RsPromotionSql} table.</i></dd> <dt>string `text_title`</dt> <dd>The title of the promotion.</dd> <dt>int `id_program`</dt> <dd>One of {@link \RsProgramSid} constants.</dd> <dt>bool `is_select`</dt> <dd>If <tt>true</tt>, the promotion is related to the service. Otherwise, this will be <tt>false</tt>.</dd> </dl>'
                      dl_convert_max:
                        type: string
                        description: 'The last date on what conversion can be scheduled.'
                      dl_convert_min:
                        type: string
                        description: 'The first date on what conversion can be scheduled.'
                      dl_hold_end:
                        type: string
                        description: 'Local date, when hold ends, if PO is on hold right now. Empty if PO is not on hold, or hold is endless.'
                      dl_hold_start:
                        type: string
                        description: 'Local date, when hold starts, if PO is on hold right now. Empty if PO is not on hold.'
                      id_convert:
                        type: string
                        description: 'The conversion ID. One of the {@link PromotionConvertSid} constants.'
                      id_convert_when:
                        type: [number, "null"]
                        description: 'When conversion should be done. One of the {@link ConvertWhenSid} constants. `null` if it''s not set yet.'
                      is_edit:
                        type: boolean
                        description: 'Determines whether the conversion request is new or editing an existing conversion. If `true`, the conversion exists. Otherwise, this will be `false`.'
                      is_renew:
                        type: boolean
                        description: '`true` if PO is going to be renewed and not converted. `false` if PO is going to expire or to convert.'
                      s_date_convert:
                        type: string
                        description: 'The existing conversion date, returned as a string for the datepicker.'
                      s_date_now:
                        type: string
                        description: 'The current date, returned as a string.'
                      s_title:
                        type: string
                        description: 'The Purchase Option title.'
                      text_date_expire:
                        type: string
                        description: 'Expiration date in string user-friendly format.'
                      text_date_payment:
                        type: string
                        description: 'Next payment date in string user-friendly format.'
                      text_note:
                        type: [string, "null"]
                        description: 'The note for the promotion conversion.'
      tags:
        - Convert-Convert
    post:
      summary: 'Creates or updates conversion form data for the login promotion. Performs all necessary checks and apply changes. Validates access, the target promotion key, conversion type, conversion timing, and the optional scheduled date, then saves the conversion settings. Returns an error if the user is a debtor.'
      parameters:
        - name: dl_convert
          in: query
          description: 'The conversion date, in the local time zone.'
          schema:
            type: string
        - name: id_convert
          in: query
          description: 'The conversion ID. One of the {@link PromotionConvertSid} constants.'
          schema:
            type: string
        - name: id_convert_when
          in: query
          description: 'When conversion should be done. One of the {@link ConvertWhenSid} constants. `null` if it''s not set yet.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'The login promotion key.'
          schema:
            type: string
        - name: k_promotion_to
          in: query
          description: 'The promotion key the given promotion will be converted to.'
          schema:
            type: string
        - name: text_note
          in: query
          description: 'The note for the promotion conversion.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Convert-Convert
  '/Wl/Login/Promotion/Share/LoginPromotionShare.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Purchased promotion key. Primary key in {@link \RsLoginPromotionSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_share_family:
                        type: array
                        description: 'List of family members with whom the purchased promotion can be shared. Each element has next structure: <dl> <dt>bool `is_share`</dt> <dd>Whether the login promotion was shared with user.</dd> <dt>string `text_name`</dt> <dd>User name.</dd> <dt>string `uid`</dt> <dd>User key. Primary key in {@link \PassportLoginSql} table.</dd> </dl>'
      tags:
        - Share-LoginPromotionShare
  '/Wl/Login/Promotion/Rollup/Adjust.json':
    post:
      summary: ''
      parameters:
        - name: id_period
          in: query
          description: 'ID of the calendar period attendance restriction is applied to.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'ID of the purchased promotion.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_count:
                  type: number
                  format: int32
                  description: 'Count of remaining visits that should be after adjustment.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Rollup-Adjust
  '/Wl/Login/Promotion/PayMethod/AutopayMethod.json':
    put:
      summary: 'Changes login promotion automatic payment method.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Login promotion key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_account:
                  type: boolean
                  description: '`true` if Account payment method should be used, `false` - otherwise.'
                is_default:
                  type: boolean
                  description: '`true` if Default payment method should be used, `false` - otherwise.'
                k_pay_recurrent:
                  type: [string, "null"]
                  description: 'Card/ACH recurrent payment token. Empty when Default or Account payment method should be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PayMethod-AutopayMethod
  '/Wl/Login/Promotion/GuestPass/GuestPassList.json':
    get:
      summary: 'Retrieves guest passes for a specific client. Returns all guest passes assigned to the given user, optionally filtered by class and location.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_class
          in: query
          description: 'Class key to filter guest passes by. `null` if guest passes should not be filtered by class.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'Location key to filter guest passes by. `null` if guest passes should not be filtered by location.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of client''s guest passes. <dl> <dt>array `a_image`</dt> <dd> Thumbnail image data for the guest promotion. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `url-thumbnail`</dt> <dd>Thumbnail url.</dd> </dl> </dd> <dt>array|null `a_period_dl`</dt> <dd> Start and end dates of the current reset period. `null` if there is no reset period or the reset date is unavailable. <dl> <dt>string `dl_end`</dt> <dd>Period end date in MySQL date format.</dd> <dt>string `dl_start`</dt> <dd>Period start date in MySQL date format.</dd> </dl> </dd> <dt>array `a_settings`</dt> <dd> Guest pass settings. Empty array when the guest pass has no settings (class-type passes). <dl> <dt>int `i_claim_day`</dt> <dd> Count of days for accept guest invite. If the invitation is not accepted within this time, it will be canceled. </dd> <dt>int `i_limit`</dt> <dd>Times that member can invite the same guest.</dd> <dt>int `i_limit_duration`</dt> <dd> The time during which a member can invite a guest {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit} times. </dd> <dt>int `id_limit_duration`</dt> <dd> Type of the duration of {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit_duration}. One of {@link \ADurationSid} constants. </dd> <dt>bool `is_checkin`</dt> <dd>Whether guests can only enter the gym when the inviting member is checked in.</dd> <dt>bool `is_limit`</dt> <dd>Whether there are limits for a guest promotion.</dd> </dl> </dd> <dt>bool `can_invite`</dt> <dd>`true` if the guest pass is invite-type (the member sends invitations to guests).</dd> <dt>bool `can_send`</dt> <dd>`true` if the member can currently send a guest pass (eligible and within quota).</dd> <dt>string|null `dl_reset`</dt> <dd>Date on which the pass resets or expires, in MySQL date format. `null` if no expiry date is determined.</dd> <dt>bool `has_service`</dt> <dd>`true` if the guest pass is service-type (not invite-type). Inverse of `can_invite`.</dd> <dt>int|null `i_cap_day`</dt> <dd>Maximum guest passes that can be sent per day. `null` if there is no daily cap.</dd> <dt>int|null `i_limit`</dt> <dd>Total number of guest passes initially granted. `null` if the supply is unlimited.</dd> <dt>int|null `i_period`</dt> <dd> Numeric length of the reset period (for example `1` for a one-month period). `null` if the promotion has no reset period. </dd> <dt>int|null `i_remain`</dt> <dd>Number of guest passes remaining in the current period. `null` if the supply is unlimited.</dd> <dt>int|null `i_remain_day`</dt> <dd>Number of guest passes remaining today per the daily cap. `null` if there is no daily cap.</dd> <dt>int `i_use`</dt> <dd>Number of accepted invitations for this guest pass.</dd> <dt>int|null `id_period`</dt> <dd> Unit of the reset period. One of {@link \ADurationSid} constants. `null` if the promotion has no reset period. </dd> <dt>int `id_program_guest`</dt> <dd>Guest pass program ID. One of {@link \RsProgramSid} constants.</dd> <dt>int `id_reset_type_guest`</dt> <dd>Reset type of the host promotion. One of {@link \Wl\Promotion\Guest\Pass\GuestPassResetTypeSid} constants.</dd> <dt>bool `is_expire_note`</dt> <dd>`true` if the pass is close enough to its reset or expiry date that the UI should display a warning.</dd> <dt>bool `is_reset`</dt> <dd>`true` if the remaining count resets on `dl_reset`; `false` if the pass expires on that date.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key from {@link \RsBusinessSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key of the host membership. <i>Primary key from {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Guest promotion key. <i>Primary key from {@link \RsPromotionSql} table.</i></dd> <dt>string `text_location`</dt> <dd> Comma-separated list of location titles where the promotion is valid. Empty when the promotion is business-wide. </dd> <dt>string `text_owner`</dt> <dd>Full name of the membership owner.</dd> <dt>string `text_period`</dt> <dd>Human-readable reset period label (e.g. "1 month"). Empty when there is no reset period.</dd> <dt>string `text_period_date`</dt> <dd> Human-readable date range of the current period (e.g. "Jan 1 - Jan 31"). Empty when there is no reset period or the reset date is unavailable. </dd> <dt>string `text_promotion_grant`</dt> <dd>Title of the host (granting) promotion.</dd> <dt>string `text_promotion_guest`</dt> <dd>Title of the guest promotion.</dd> <dt>string `uid_owner`</dt> <dd>User key of the membership owner. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - GuestPass-GuestPassList
  '/Wl/Login/Promotion/GuestPass/GuestPass.json':
    get:
      summary: 'Retrieves guest pass information for a client''s membership. Returns the guest pass configuration and remaining usage count for the specified login promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Login promotion key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_guest_pass:
                        type: array
                        description: 'Guest pass information. <dl> <dt>array `a_image`</dt> <dd> Thumbnail image data for the guest promotion. <dl> <dt>int `i_height`</dt> <dd>Image height.</dd> <dt>int `i_width`</dt> <dd>Image width.</dd> <dt>string `url-thumbnail`</dt> <dd>Thumbnail url.</dd> </dl> </dd> <dt>array|null `a_period_dl`</dt> <dd> Start and end dates of the current reset period. `null` if there is no reset period or the reset date is unavailable. <dl> <dt>string `dl_end`</dt> <dd>Period end date in MySQL date format.</dd> <dt>string `dl_start`</dt> <dd>Period start date in MySQL date format.</dd> </dl> </dd> <dt>array `a_settings`</dt> <dd> Guest pass settings. Empty array when the guest pass has no settings (class-type passes). <dl> <dt>int `i_claim_day`</dt> <dd> Count of days for accept guest invite. If the invitation is not accepted within this time, it will be canceled. </dd> <dt>int `i_limit`</dt> <dd>Times that member can invite the same guest.</dd> <dt>int `i_limit_duration`</dt> <dd> The time during which a member can invite a guest {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit} times. </dd> <dt>int `id_limit_duration`</dt> <dd> Type of the duration of {@link \Wl\Promotion\Guest\Pass\GuestPassSettingsEntity::$i_limit_duration}. One of {@link \ADurationSid} constants. </dd> <dt>bool `is_checkin`</dt> <dd>Whether guests can only enter the gym when the inviting member is checked in.</dd> <dt>bool `is_limit`</dt> <dd>Whether there are limits for a guest promotion.</dd> </dl> </dd> <dt>bool `can_invite`</dt> <dd>`true` if the guest pass is invite-type (the member sends invitations to guests).</dd> <dt>bool `can_send`</dt> <dd>`true` if the member can currently send a guest pass (eligible and within quota).</dd> <dt>string|null `dl_reset`</dt> <dd>Date on which the pass resets or expires, in MySQL date format. `null` if no expiry date is determined.</dd> <dt>bool `has_service`</dt> <dd>`true` if the guest pass is service-type (not invite-type). Inverse of `can_invite`.</dd> <dt>int|null `i_cap_day`</dt> <dd>Maximum guest passes that can be sent per day. `null` if there is no daily cap.</dd> <dt>int|null `i_limit`</dt> <dd>Total number of guest passes initially granted. `null` if the supply is unlimited.</dd> <dt>int|null `i_period`</dt> <dd> Numeric length of the reset period (for example `1` for a one-month period). `null` if the promotion has no reset period. </dd> <dt>int|null `i_remain`</dt> <dd>Number of guest passes remaining in the current period. `null` if the supply is unlimited.</dd> <dt>int|null `i_remain_day`</dt> <dd>Number of guest passes remaining today per the daily cap. `null` if there is no daily cap.</dd> <dt>int `i_use`</dt> <dd>Number of accepted invitations for this guest pass.</dd> <dt>int|null `id_period`</dt> <dd> Unit of the reset period. One of {@link \ADurationSid} constants. `null` if the promotion has no reset period. </dd> <dt>int `id_program_guest`</dt> <dd>Guest pass program ID. One of {@link \RsProgramSid} constants.</dd> <dt>int `id_reset_type_guest`</dt> <dd>Reset type of the host promotion. One of {@link \Wl\Promotion\Guest\Pass\GuestPassResetTypeSid} constants.</dd> <dt>bool `is_expire_note`</dt> <dd>`true` if the pass is close enough to its reset or expiry date that the UI should display a warning.</dd> <dt>bool `is_reset`</dt> <dd>`true` if the remaining count resets on `dl_reset`; `false` if the pass expires on that date.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key from {@link \RsBusinessSql} table.</i></dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key of the host membership. <i>Primary key from {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Guest promotion key. <i>Primary key from {@link \RsPromotionSql} table.</i></dd> <dt>string `text_location`</dt> <dd> Comma-separated list of location titles where the promotion is valid. Empty when the promotion is business-wide. </dd> <dt>string `text_owner`</dt> <dd>Full name of the membership owner.</dd> <dt>string `text_period`</dt> <dd>Human-readable reset period label (e.g. "1 month"). Empty when there is no reset period.</dd> <dt>string `text_period_date`</dt> <dd> Human-readable date range of the current period (e.g. "Jan 1 - Jan 31"). Empty when there is no reset period or the reset date is unavailable. </dd> <dt>string `text_promotion_grant`</dt> <dd>Title of the host (granting) promotion.</dd> <dt>string `text_promotion_guest`</dt> <dd>Title of the guest promotion.</dd> <dt>string `uid_owner`</dt> <dd>User key of the membership owner. <i>Primary key from {@link \PassportLoginSql} table.</i></dd> </dl>'
      tags:
        - GuestPass-GuestPass
    put:
      summary: 'Updates the guest pass remaining usages for a client''s membership. Adjusts the remaining guest pass count to the specified value and logs the change. Requires staff-level edit access for the login promotion.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Login promotion key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_adjust:
                  type: number
                  format: int32
                  description: 'Number of guest passes remaining for the current period.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GuestPass-GuestPass
  '/Wl/Login/Search/StaffApp/List.json':
    get:
      summary: 'Performs access checks and returns a list of users, depending on the search query. When a search string is provided, returns clients whose name or email matches the query within the specified business. When the search string is empty, returns recently viewed clients. Each result includes the client''s name, email, phone, member ID, and user key.'
      parameters:
        - name: is_franchisee_search_wide
          in: query
          description: '<tt>true</tt> - search among whole franchise; <tt>false</tt> - search in specified business only.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'The search string. Clients can be matched by name or email.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of users matching the search string. The list depends on the search query. If the query is empty, recent users will be returned. Each element is an array with the following keys: <dl> <dt>string `text_mail`</dt> <dd>Client''s email.</dd> <dt>string `text_name_first`</dt> <dd>Client''s first name.</dd> <dt>string `text_name_last`</dt> <dd>Client''s last name.</dd> <dt>string `text_phone`</dt> <dd>Client''s phone.</dd> <dt>string `text_title`</dt> <dd>The client name.</dd> <dt>string `uid`</dt> <dd>The client key. <i>Primary key in {@link \PassportLoginSql} table.</i></dd> </dl>'
                      can_add:
                        type: boolean
                        description: 'If `true`, then this user can add other users via the Add Client page.'
      tags:
        - StaffApp-List
  '/Wl/Login/Search/Filter/LoginSearchFilter.json':
    get:
      summary: 'Gives filter settings.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_search_filter:
                        type: array
                        description: 'User''s search settings.'
                      is_login_search:
                        type: boolean
                        description: 'Indicate if redirection to client''s profile page was made using the login search panel.'
                      is_reception:
                        type: boolean
                        description: 'State of auto check-in service.'
      tags:
        - Filter-LoginSearchFilter
  '/Wl/Login/Search/QuickCheckIn/QuickCheckIn.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Client user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_note:
                        type: array
                        description: 'Alert items for display in the check-in result modal. Empty when {@link QuickCheckInApi::$is_navigate} is `true` or when no alerts exist. Each element is one item from {@link \RsProfileNote::list_all()} with `is_backend` set to `true`. Keys used for rendering: `html_text`, `s_class_css`, `s_text`, `url`, `is_panel`, `s_onclick`, `s_url`, `is_birthday_icon`, `is_hide`.'
                      a_session:
                        type: array
                        description: 'List of today''s sessions available for check-in. Empty when {@link QuickCheckInApi::$is_navigate} is `true` or when no sessions are found. Each element: <dl> <dt>string `dtu_date`</dt> <dd>Session datetime in UTC.</dd> <dt>int `i_book`</dt> <dd>Number of booked spots. `0` for gym visits and appointments.</dd> <dt>int `i_capacity`</dt> <dd>Total session capacity. `0` means unlimited (gym visits, appointments).</dd> <dt>int `id_profile_note`</dt> <dd> Profile note ID from {@link \RsProfileNoteSid} that caused the denial, or `0` if there is no corresponding note. Used on the client side to suppress the specific denial message when the same issue is already shown in the alerts panel on the left. </dd> <dt>int `id_service`</dt> <dd>Service type. One of {@link \Wl\Service\ServiceSid} constants.</dd> <dt>bool `is_access`</dt> <dd>`true` if the client already has access (membership or pass). `false` otherwise.</dd> <dt>bool `is_attend`</dt> <dd>`true` if the client has already attended this session today. `false` otherwise.</dd> <dt>bool `is_book`</dt> <dd>`true` if the client is already in the booking list. `false` otherwise.</dd> <dt>string `k_appointment`</dt> <dd>Appointment prrimary key. `''0''` if not an appointment.</dd> <dt>string|null `k_class`</dt> <dd>Class primary key. `0` for gym visits and appointments.</dd> <dt>string `k_class_period`</dt> <dd>Class session prrimary key. `0` if not a class.</dd> <dt>string `k_location`</dt> <dd>Location primary key.</dd> <dt>bool `show_free`</dt> <dd>`true` if the client can check in without payment. `false` if payment is required.</dd> <dt>string `text_deny`</dt> <dd>Human-readable denial reason when the client cannot check in. Empty string when check-in is available.</dd> <dt>string `text_location`</dt> <dd>Location name for display.</dd> <dt>string `text_service`</dt> <dd>Service name for display.</dd> <dt>string `text_room`</dt> <dd>Room name for display. Empty if no room is assigned to the session.</dd> <dt>string `text_staff`</dt> <dd>Staff name(s) for display. Empty for gym visits.</dd> <dt>string `text_time`</dt> <dd>Session time formatted for display. Empty for gym visits.</dd> <dt>string `url_attendance`</dt> <dd>Link to the attendance page for this session. Empty if not applicable.</dd> <dt>string `url_book_process`</dt> <dd>URL for the paid booking flow. Non-empty only for class sessions requiring payment.</dd> <dt>string `url_image`</dt> <dd>Class thumbnail image URL for the session preview. Empty for gym visits.</dd> </dl>'
                      i_attend_quick_close_success:
                        type: number
                        format: int32
                        description: 'Number of seconds before the session popup auto-closes when all sessions are already attended. Used only when {@link QuickCheckInApi::$is_attend_quick_close_success} is `true`.'
                      is_attend_quick_close_success:
                        type: boolean
                        description: 'Whether the session popup should auto-close when all sessions are already attended. The popup closes after {@link QuickCheckInApi::$i_attend_quick_close_success} seconds.'
                      is_navigate:
                        type: boolean
                        description: '`true` if the business is configured to navigate to the client profile after a QUICK AUTO Check-In scan. In this case {@link QuickCheckInApi::$url_user_profile} is non-empty and {@link QuickCheckInApi::$a_session} is empty. `false` if sessions should be processed - see {@link QuickCheckInApi::$a_session}.'
                      text_user_name:
                        type: string
                        description: 'Client full name.'
                      url_user_photo:
                        type: string
                        description: 'Client photo URL for display in the session popup.'
                      url_user_profile:
                        type: string
                        description: 'Client profile URL. Used for navigation when {@link QuickCheckInApi::$is_navigate} is `true`, and for the "View profile" link in the session popup when `false`.'
      tags:
        - QuickCheckIn-QuickCheckIn
  '/Wl/Login/Permission/Access/Access.json':
    get:
      summary: ''
      parameters:
        - name: a_schedule
          in: query
          description: 'Schedule item information. Has structure: <dl> <dt>string `k_location`</dt> <dd>Location key, where class or service occurs. Primary key in {@link \RsLocationSql} table.</dd> <dt>string `k_class`</dt> <dd>Class key. Primary key in {@link \RsClassSql} table.</dd> <dt>string `k_login_promotion`</dt> <dd> Login promotion key if need to check access for specific promotion. Primary key in {@link \RsLoginPromotionSql} table. </dd> <dt>string `k_resource`</dt> <dd>Resource key. Primary key in {@link \RsResourceSql} table.</dd> <dt>string `k_service`</dt> <dd>Service key. Primary key in {@link \RsServiceSql} table.</dd> </dl>'
          schema:
            type: array
        - name: a_time
          in: query
          description: 'Time when class or service occurs: <dl> <dt>int `i_hour`</dt> <dd>Number of hours.</dd> <dt>int `i_minute`</dt> <dd>Number of minutes.</dd> <dt>bool `is_am`</dt> <dd>Whether time in AM or PM.</dd> </dl>'
          schema:
            type: array
        - name: dt_date
          in: query
          description: 'Date when class or service occurs.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_access:
                        type: array
                        description: 'Schedule access information. Has structure: <dl> <dt>string `k_location`</dt> <dd>Location key, where class or service occurs.</dd> <dt>string `k_class`</dt> <dd>Class key.</dd> <dt>string `k_login_promotion`</dt> <dd>Login promotion key if need to check access for specific promotion.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `k_service`</dt> <dd>Service key.</dd> <dt>string `s_deny`</dt> <dd>Login permission error code.</dd> </dl>'
                      is_traveller:
                        type: boolean
                        description: 'Whether user is traveller in current business.'
      tags:
        - Access-Access
  '/Wl/Login/Coupon/Transfer/CouponTransfer.json':
    get:
      summary: 'Checks whether users are related.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business. Primary key in {@link \RsBusinessSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: uid_from
          in: query
          description: 'User ID to transfer purchased coupon from. Primary key in {@link \PassportLoginSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: uid_to
          in: query
          description: 'User ID to transfer purchased coupon to. Primary key in {@link \PassportLoginSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_relation:
                        type: array
                        description: 'Information about relation between users: <dl> <dt>string `html_relation`</dt> <dd>Name of relation type.</dd> <dt>string `html_user_name`</dt> <dd>User name with whom purchased event owner has relation.</dd> </dl>'
      tags:
        - Transfer-CouponTransfer
    post:
      summary: 'Transfers purchased coupon to another user.'
      parameters:
        - name: id_mode
          in: query
          description: 'Key of source mode. One of {@link \Wl\Mode\ModeSid} constants.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Key of business. Primary key in {@link \RsBusinessSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: k_login_coupon
          in: query
          description: 'Key of login coupon to be transferred. Primary key in {@link \RsLoginCouponSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: uid_from
          in: query
          description: 'User ID to transfer purchased coupon from. Primary key in {@link \PassportLoginSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
        - name: uid_to
          in: query
          description: 'User ID to transfer purchased coupon to. Primary key in {@link \PassportLoginSql} table. <tt>null</tt> if not initialized.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Transfer-CouponTransfer
  '/Wl/Login/Mail/Secondary/MailSecondarySearch.json':
    get:
      summary: 'Searches for secondary recipients within the business by the specified query.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which the search should be performed.'
          schema:
            type: string
        - name: text_search
          in: query
          description: 'Search query. Search is performed by email, first name and last name. If not specified recent clients and relatives will be returned without filtering.'
          schema:
            type: string
        - name: uid_behalf
          in: query
          description: 'Key of the user on behalf of whom the search is performed. This user will be excluded from the search results.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_result:
                        type: object
                        description: 'Result of the search. <i>Structure of the result described in {@link MailSecondarySearchApi::_search()}</i> <dl> <dt>int|null `id_relation`</dt> <dd> Relation ID if users are relatives, one of {@link \RsFamilyRelationSid} constants, `null` if users are not relatives. </dd> <dt>string `text_name_first`</dt> <dd>First name of the client.</dd> <dt>string `text_name_last`</dt> <dd>Last name of the client.</dd> <dt>string `text_mail`</dt> <dd>Email of the client.</dd> <dt>string `uid`</dt> <dd>Key of the client. Primary key in {@link \PassportLoginSql} table.</dd> <dt>string `url_profile`</dt> <dd>URL of the client''s profile page.</dd> </dl>'
      tags:
        - Secondary-MailSecondarySearch
  '/Wl/Login/Mail/Secondary/MailSecondary.json':
    delete:
      summary: 'Removes a secondary notification recipient from the secondary notification list. `uid_secondary` nor `text_mail_secondary` is provided.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key withing which the secondary contact is managed.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret string to validate access for non-registered user.'
          schema:
            type: [string, "null"]
        - name: text_mail_secondary
          in: query
          description: 'Email address of the external secondary contact to delete. Empty string if deleting a registered user secondary contact. Either `text_mail_secondary` or `uid_secondary` must be set for delete.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Key of the client whose activity notifications the secondary contacts receive. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
        - name: uid_secondary
          in: query
          description: 'Key of the registered secondary contact to delete. Primary key in {@link \PassportLoginSql}. Empty string if deleting an external secondary contact. Either `uid_secondary` or `text_mail_secondary` must be set for delete.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Secondary-MailSecondary
  '/Wl/Mail/Pattern/AutomatedMarketing/SendTest.json':
    post:
      summary: 'Sends test email or SMS.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_action:
                  type: [array, "null"]
                  description: 'Call to action data. Used only for automated marketing messages to resolve action placeholders when sending a test notification. `null` when no call-to-action is required for the test message.'
                id_mail:
                  type: number
                  format: int32
                  description: 'ID of the notification. One of {@link \RsMailSid} constants.'
                id_mail_form:
                  type: number
                  format: int32
                  description: 'ID of the mail form. One of {@link \RsMailFormSid} constants.'
                k_business:
                  type: [string, "null"]
                  description: 'Primary key of business in {@link \RsBusinessSql} table. `null` for system business.'
                k_mail_pattern:
                  type: string
                  description: 'Mail pattern key. Primary key in {@link \RsMailPatternSql} table. Not set in case of sending test notification from ''Send campaign'' panel.'
                k_mail_pattern_live:
                  type: string
                  description: 'Mail pattern live key. Primary key in {@link \RsMailPatternLiveSql} table. Set in case of sending test notification from ''Send campaign'' panel.'
                text_mail:
                  type: string
                  description: 'Email of a recipient.'
                text_phone:
                  type: string
                  description: 'Phone of a recipient.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AutomatedMarketing-SendTest
  '/Wl/Mail/Pattern/Edit/MailPatternDuplicate.json':
    post:
      summary: 'Duplicates the mail pattern.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which duplicate operation is performed. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_mail_pattern
          in: query
          description: 'Key of the mail pattern to duplicate. Primary key in {@link \RsMailPatternSql} table.'
          schema:
            type: string
        - name: k_pattern_mail_duplicate
          in: query
          description: 'Key of the duplicated mail pattern. Primary key in {@link \RsMailPatternSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_pattern_mail_duplicate:
                        type: string
                        description: 'Key of the duplicated mail pattern. Primary key in {@link \RsMailPatternSql} table.'
      tags:
        - Edit-MailPatternDuplicate
  '/Wl/Mail/Pattern/Edit/MailPatternEdit.json':
    get:
      summary: 'Checks if some clients can be affected after the change of mail pattern.'
      parameters:
        - name: a_login_type
          in: query
          description: 'List of client and member types. Each element is primary key in the {@link \Wl\Login\Type\Sql} table.'
          schema:
            type: array
        - name: a_member_group
          in: query
          description: 'List of member groups. Each element is primary key in the {@link \Wl\Member\Group\Sql} table.'
          schema:
            type: array
        - name: has_max_review_request
          in: query
          description: 'Whether or not to stop sending review requests after <var>i_max_review_request</var> is reached. <tt>null</tt> if not set.'
          schema:
            type: [boolean, "null"]
        - name: i_after
          in: query
          description: 'The amount for the new delay before sending mail. <tt>null</tt> if not loaded.'
          schema:
            type: [number, "null"]
        - name: i_delay
          in: query
          description: 'Count of the duration unit after change. <tt>null</tt> if not loaded.'
          schema:
            type: [number, "null"]
        - name: i_max_review_request
          in: query
          description: 'Maximum number of review requests that can be sent. <tt>null</tt> if not set.'
          schema:
            type: [number, "null"]
        - name: id_duration_delay
          in: query
          description: 'Type of the duration unit after change. One of constants {@link \ADurationSid}. <tt>null</tt> if not set.'
          schema:
            type: [number, "null"]
        - name: is_after_every
          in: query
          description: 'If <tt>true</tt> then mail for all visits will be rescheduled. If <tt>false</tt> then only first will be rescheduled. After change. <tt>null</tt> if not set.'
          schema:
            type: [boolean, "null"]
        - name: is_churn_risk
          in: query
          description: 'Whether emails should be sent to members in churn risk group (Only for automated marketing). `true` if sent mails only to members in churn risk group. `false` if sent mails not only to members in churn risk group. <tt>null</tt> if not set.'
          schema:
            type: [boolean, "null"]
        - name: is_disable_review_request_after_review
          in: query
          description: 'Whether mail will be sent after the client has made a review. <tt>true</tt> to send the mail, <tt>false</tt> to disable sending the mail. <tt>null</tt> if not set.'
          schema:
            type: [boolean, "null"]
        - name: is_prevent
          in: query
          description: 'Whether need to prevent sending email without fulfilling a certain condition. For example, for email {@link \RaMailSid::PROMOTION_PURCHASE} and {@link \RaMailSid::LEAD_CAPTURE} <tt>true</tt> means that mail will not be sent to clients who make any new purchase, <tt>false</tt> otherwise. After change. <tt>null</tt> if not set.'
          schema:
            type: [boolean, "null"]
        - name: k_location_retention
          in: query
          description: 'Key of retention location. Primary key in {@link \RsLocationSql} table. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern
          in: query
          description: 'Key of the mail pattern. Primary key in {@link \RsMailPatternSql} table. <tt>null</tt> if not loaded.'
          schema:
            type: [string, "null"]
        - name: s_object
          in: query
          description: 'Object identifier. Some mails related only with specific promotion, product or service. <tt>null</tt> if not set.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_affected_clients:
                        type: number
                        format: int32
                        description: 'The number of affected clients.'
                      is_affected_client:
                        type: boolean
                        description: 'Whether some clients will be affected after the change of mail pattern.'
                      url_report:
                        type: string
                        description: 'URL for client report.'
      tags:
        - Edit-MailPatternEdit
  '/Wl/Mail/Pattern/Edit/MailPatternState.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: boolean
                  description: 'Determines if the mail pattern should be active. `true` if the mail pattern is active, `false` otherwise.'
                k_business:
                  type: string
                  description: 'Business key where the mail pattern is used.'
                k_mail_pattern:
                  type: string
                  description: 'Mail pattern key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-MailPatternState
  '/Wl/Mail/Pattern/Live/MailPatternLive.json':
    delete:
      summary: ''
      parameters:
        - name: id_mail_form
          in: query
          description: 'ID of the form campaign.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business to remove scheduled campaign from.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'Key of the mail pattern to remove scheduled campaign from.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Live-MailPatternLive
  '/Wl/Mail/Pattern/Live/MailPatternLiveRecipient.json':
    delete:
      summary: ''
      parameters:
        - name: id_mail_form
          in: query
          description: 'ID of the form campaign.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of the business to remove scheduled campaign from.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'Key of the mail pattern to remove scheduled campaign from.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Live-MailPatternLiveRecipient
  '/Wl/Mail/Pattern/Disturb/Disturb.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_disturb:
                        type: array
                        description: 'The start and end time for all the do not disturb periods for each mail page. Each key is one of the {@link \RsMailPageSid} constants. Each of the values will be `null` if the do not disturb period is not set for this mail page. It will be an array with the following keys if set. <dl> <dt>int `i_end`</dt> <dd>The end time of the do not disturb period, in seconds since midnight.</dd> <dt>int `i_start`</dt> <dd>The start time of the do not disturb period, in seconds since midnight.</dd> </dl>'
      tags:
        - Disturb-Disturb
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_disturb:
                  type: array
                  description: 'The start and end time for all the do not disturb periods for each mail page. Each key is one of the {@link \RsMailPageSid} constants. Each of the values will be `null` if the do not disturb period is not set for this mail page. It will be an array with the following keys if set. <dl> <dt>int `i_end`</dt> <dd>The end time of the do not disturb period, in seconds since midnight.</dd> <dt>int `i_start`</dt> <dd>The start time of the do not disturb period, in seconds since midnight.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Disturb-Disturb
  '/Wl/Mail/History/Report/MailReason.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reason:
                        type: array
                        description: 'List of reasons.'
      tags:
        - Report-MailReason
  '/Wl/Member/Group/User/UserGroup.json':
    delete:
      summary: 'Deletes the user from the group. Removes the specified user from the given member group within the business after verifying access rights and that the group key is valid.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: [string, "null"]
        - name: uid_user
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-UserGroup
    get:
      summary: 'Gets information about all groups to which the specified user belongs. Returns the list of member group keys for the given user within the specified business, populating `$a_group_info` with those keys after access validation.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: [string, "null"]
        - name: uid_user
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_group_info:
                        type: array
                        description: 'All groups to which the specified user belongs.'
                      is_quick_group:
                        type: boolean
                        description: 'Whether the user is in the quick group or not.'
      tags:
        - User-UserGroup
    post:
      summary: 'Adds a user to a group. Assigns the specified user to the given member group within the business after verifying access rights and that the group key is valid.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: [string, "null"]
        - name: uid_user
          in: query
          description: 'UID of a user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_member_group:
                  type: [string, "null"]
                  description: 'Member group key. Use special value ''quick'' to add/remove the user to/from the quick group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-UserGroup
  '/Wl/Member/Group/User/UserUpdate.json':
    post:
      summary: 'Performs an immediate update of the member group.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_member_group:
                  type: string
                  description: 'The member group key for which the action needs to be performed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-UserUpdate
    put:
      summary: 'Enables automatic member group update. When enabled, an instant update is performed and an update task {@link \Wl\Member\Group\User\UserUpdateTask} is scheduled for the next day. If disabled, the automatic update task is removed.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_update:
                  type: boolean
                  description: 'Automatic group update flag. <tt>true</tt> to enable automatic update, <tt>false</tt> to disable.'
                k_member_group:
                  type: string
                  description: 'The member group key for which the action needs to be performed.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-UserUpdate
  '/Wl/Member/Group/Edit/Edit.json':
    get:
      summary: 'Returns data for group edit form. Gets full information about a client group.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_conversion_type:
                        type: [number, "null"]
                        description: 'Lead conversion type. `null` if this group is not used for to track lead conversion.'
                      id_member_group_shape:
                        type: number
                        format: int32
                        description: 'Shape of icon. One of {@link \Wl\Member\Group\ShapeSid} constants.'
                      is_brivo_active:
                        type: boolean
                        description: 'Whether Facility Access enabled for group.'
                      is_brivo_invitation_active:
                        type: boolean
                        description: 'Whether Brivo invitation feature enabled for the group.'
                      is_icon:
                        type: boolean
                        description: '<tt>true</tt> to enable group icon. <tt>false</tt> to disable.'
                      is_update:
                        type: boolean
                        description: 'Whether auto-update enabled for group.'
                      k_search_template:
                        type: string
                        description: 'Key of existing template. Can be empty if template needs to be created.'
                      s_color_background:
                        type: string
                        description: 'Hexadecimal color of icon background.'
                      s_color_foreground:
                        type: string
                        description: 'Hexadecimal color of icon foreground.'
                      s_icon:
                        type: string
                        description: 'Characters on icon.'
                      text_title:
                        type: string
                        description: 'Title for a client group.'
      tags:
        - Edit-Edit
    post:
      summary: 'Adds or changes a client group. Use to update existing client group or create a new one.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_criteria:
                  type: array
                  description: 'Entity in array format. <dl> <dt>int `cid_search_criteria`</dt> <dd>Search criteria CID. Constant from {@link \Wl\Search\Criteria\SearchCriteriaAbstract} subclasses.</dd> <dt>int `id_operation`</dt> <dd>ID of selected operation. Constant from {@link \Wl\Search\SearchOperationSid}.</dd> <dt>string `s_id`</dt> <dd> Unique criteria key. <i>Detailed description see {@link \Wl\Search\Criteria\SearchCriteriaAbstract::__construct()}.</i> </dd> <dt>mixed `x_value`</dt> <dd>Operation data value.</dd> </dl> can be transferred to server via HTTP.'
                a_logic:
                  type: [object, "null"]
                  description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                a_search_entity:
                  type: array
                  description: 'Search entity CID list. Constants from {@link SearchEntityAbstract} subclasses.'
                a_staff_role_selected:
                  type: array
                  description: 'Selected staff roles of the search template. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. <i>Primary key in {@link \RsBusinessRoleSql} table.</i></dd> </dl>'
                id_conversion_type:
                  type: [number, "null"]
                  description: 'Lead conversion type. `null` if this group is not used for to track lead conversion.'
                id_member_group_shape:
                  type: number
                  format: int32
                  description: 'Shape of icon. One of {@link \Wl\Member\Group\ShapeSid} constants.'
                id_share:
                  type: string
                  description: 'Type of share option. One of {@link ShareSid} constants.'
                is_brivo_active:
                  type: boolean
                  description: 'Whether Facility Access enabled for group.'
                is_brivo_invitation_active:
                  type: boolean
                  description: 'Whether Brivo invitation feature enabled for the group.'
                is_icon:
                  type: boolean
                  description: '<tt>true</tt> to enable group icon. <tt>false</tt> to disable.'
                is_update:
                  type: boolean
                  description: 'Whether auto-update enabled for group.'
                json_criteria:
                  type: string
                  description: 'Entity in array format. Alternative of {@link SearchTemplatePanelApiTrait::$a_criteria} to transfer data via HTTP as one variable. <i>For more information about the format, see {@link SearchCriteriaList::createArray()} method.</i>'
                s_color_background:
                  type: string
                  description: 'Hexadecimal color of icon background.'
                s_color_foreground:
                  type: string
                  description: 'Hexadecimal color of icon foreground.'
                s_icon:
                  type: string
                  description: 'Characters on icon.'
                text_title:
                  type: string
                  description: 'Title for a client group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_member_group:
                        type: string
                        description: 'Member group key.'
                      k_search_template:
                        type: string
                        description: 'Key of existing template. Can be empty if template needs to be created.'
                      text_warning:
                        type: [string, "null"]
                        description: 'Additional warning message if there were some minor issues with request.'
      tags:
        - Edit-Edit
    put:
      summary: 'Adds or edit client group Query. Use to update existing client group search query or create a new one. Member group should be already created.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_criteria:
                  type: array
                  description: 'Entity in array format. <dl> <dt>int `cid_search_criteria`</dt> <dd>Search criteria CID. Constant from {@link \Wl\Search\Criteria\SearchCriteriaAbstract} subclasses.</dd> <dt>int `id_operation`</dt> <dd>ID of selected operation. Constant from {@link \Wl\Search\SearchOperationSid}.</dd> <dt>string `s_id`</dt> <dd> Unique criteria key. <i>Detailed description see {@link \Wl\Search\Criteria\SearchCriteriaAbstract::__construct()}.</i> </dd> <dt>mixed `x_value`</dt> <dd>Operation data value.</dd> </dl> can be transferred to server via HTTP.'
                a_logic:
                  type: [object, "null"]
                  description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                a_search_entity:
                  type: array
                  description: 'Search entity CID list. Constants from {@link SearchEntityAbstract} subclasses.'
                a_staff_role_selected:
                  type: array
                  description: 'Selected staff roles of the search template. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. <i>Primary key in {@link \RsBusinessRoleSql} table.</i></dd> </dl>'
                id_member_group_shape:
                  type: number
                  format: int32
                  description: 'Shape of icon. One of {@link \Wl\Member\Group\ShapeSid} constants.'
                id_share:
                  type: string
                  description: 'Type of share option. One of {@link ShareSid} constants.'
                is_icon:
                  type: boolean
                  description: '<tt>true</tt> to enable group icon. <tt>false</tt> to disable.'
                json_criteria:
                  type: string
                  description: 'Entity in array format. Alternative of {@link SearchTemplatePanelApiTrait::$a_criteria} to transfer data via HTTP as one variable. <i>For more information about the format, see {@link SearchCriteriaList::createArray()} method.</i>'
                s_color_background:
                  type: string
                  description: 'Hexadecimal color of icon background.'
                s_color_foreground:
                  type: string
                  description: 'Hexadecimal color of icon foreground.'
                s_icon:
                  type: string
                  description: 'Characters on icon.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_warning:
                        type: [string, "null"]
                        description: 'Additional warning message if there were some minor issues with request.'
      tags:
        - Edit-Edit
  '/Wl/Member/Group/Edit/EditConversionType.json':
    delete:
      summary: 'Removes the lead conversion record.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table. Used to set lead conversion type for the user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-EditConversionType
    put:
      summary: 'Adds or changes a lead conversion type for the client.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in {@link \PassportLoginSql} table. Used to set lead conversion type for the user.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_conversion_type:
                  type: [number, "null"]
                  description: 'Lead conversion type. `null` if this is the request to reset conversion date.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-EditConversionType
  '/Wl/Member/Group/GroupList/List.json':
    delete:
      summary: 'Removes groups. Deletes specified groups and associated search templates.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GroupList-List
    get:
      summary: 'Returns all member groups list in the business if <var>$a_member_group_select</var> is empty, otherwise filters result according to <var>$a_member_group_select</var>. If <var>is_return_members</var> is `true` includes in the result list of members of each groups.'
      parameters:
        - name: a_member_group_select
          in: query
          description: 'List of groups for filtering groups of business. `null` in the case when need to return all groups of business.'
          schema:
            type: [array, "null"]
        - name: is_churn_risk
          in: query
          description: 'Whether include "Isaac Churn Risk" group.'
          schema:
            type: boolean
        - name: is_return_members
          in: query
          description: 'Whether include a list of members of groups.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_member_group:
                        type: array
                        description: 'Member groups list: <dl> <dt>string `k_member_group`</dt> <dd>The key of the member group. <i>Primary key in {@link \Wl\Member\Group\Sql}.</i></dd> <dt>string `text_title`</dt> <dd>The name of the member group.</dd> </dl>'
      tags:
        - GroupList-List
    put:
      summary: 'Updates the order of groups in a list. Saves the display order of member groups for the business using the positions supplied in `$a_member_group_order`, verifying that all specified group keys belong to the business before writing.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_member_group_order:
                  type: array
                  description: 'List of groups in the order in which they will be saved.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - GroupList-List
  '/Wl/Member/Group/Select/List.json':
    get:
      summary: 'Returns member groups list in the business.'
      parameters:
        - name: is_churn_risk
          in: query
          description: 'Whether include "Isaac Churn Risk" group.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: show_member_group_user
          in: query
          description: 'Whether to show the quantity of users which belong to a member group. `true` - to show, `false` - otherwise.'
          schema:
            type: boolean
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_member_group:
                        type: array
                        description: 'Member groups list: <dl> <dt>int `i_user`</dt> <dd>Quantity of users in a group.</dd> <dt>bool `is_selected`</dt> <dd>Whether a member group is selected. `true` if selected, `false` - otherwise.</dd> <dt>string `k_member_group`</dt> <dd>The key of the member group. Primary key in {@link \Wl\Member\Group\Sql}.</dd> <dt>string `text_title`</dt> <dd>The name of the member group.</dd> </dl>'
      tags:
        - Select-List
  '/Wl/Member/Progress/Log/Verify.json':
    put:
      summary: 'Verifies client''s progress log data. Marks all unverified progress log entries for the specified user and date as verified by the currently signed-in staff member, then triggers a search index reindex for the user.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date without time of the progress log.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Log-Verify
  '/Wl/Member/Progress/Field/State.json':
    put:
      summary: 'Changes states of field. Updates one or more state flags (`is_active`, `is_public`, `is_require`, `is_require_staff`) for the specified progress field and returns the new values. At least one flag must be provided; the field must be active to change its required state.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_active:
                  type: [boolean, "null"]
                  description: 'Whether field is active and should be displayed on page.'
                is_public:
                  type: [boolean, "null"]
                  description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                is_require:
                  type: [boolean, "null"]
                  description: 'Whether field value is required for clients.'
                is_require_staff:
                  type: [boolean, "null"]
                  description: 'Whether field value is required for staffs.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_field:
                        type: [number, "null"]
                        description: 'Field ID. One of the {@link ProgressFieldSid} constant.'
                      is_active:
                        type: [boolean, "null"]
                        description: 'Whether field is active and should be displayed on page.'
                      is_public:
                        type: [boolean, "null"]
                        description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                      is_require:
                        type: [boolean, "null"]
                        description: 'Whether field value is required for clients.'
                      is_require_staff:
                        type: [boolean, "null"]
                        description: 'Whether field value is required for staffs.'
                      text_title:
                        type: [string, "null"]
                        description: 'Field title.'
      tags:
        - Field-State
  '/Wl/Page/Backend/Feature/Feature.json':
    get:
      summary: 'Get all features with statuses enabled or disabled.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_features:
                        type: array
                        description: 'List of all features with statuses true/false. Key of id {@link \Wl\Page\Backend\Feature\FeatureSid}'
      tags:
        - Feature-Feature
  '/Wl/Page/Frontend/Header/User.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Empty if user is not logged in.'
          schema:
            type: string
        - name: text_current
          in: query
          description: 'Current location.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_menu:
                        type: string
                        description: 'Rendered HTML of the frontend user menu.'
                      html_user:
                        type: string
                        description: 'Rendered HTML of the frontend user block.'
      tags:
        - Header-User
  '/Wl/Page/BackAdmin/Position/Position.json':
    get:
      summary: 'Returns position of back admin container.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_left:
                        type: string
                        description: 'Value of left property.'
                      i_top:
                        type: string
                        description: 'Value of top property.'
      tags:
        - Position-Position
    post:
      summary: 'Stores position of back admin container in the session.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_left:
                  type: string
                  description: 'Value of left property.'
                i_top:
                  type: string
                  description: 'Value of top property.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Position-Position
  '/Wl/Postcard/Campaign/CampaignLanding/CampaignLanding.json':
    post:
      summary: 'Enables direct mail campaign for a specified business.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CampaignLanding-CampaignLanding
  '/Wl/Postcard/Campaign/CampaignEdit/CampaignEditTemplate.json':
    get:
      summary: ''
      parameters:
        - name: k_postcard
          in: query
          description: 'Postcard key. <tt>null</tt> if existing template not required.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_back:
                        type: string
                        description: 'Back image.'
                      url_front:
                        type: string
                        description: 'Front image.'
      tags:
        - CampaignEdit-CampaignEditTemplate
  '/Wl/Postcard/Campaign/CampaignEdit/CampaignEditSummary.json':
    get:
      summary: 'Calculates summary information about mail campaign.'
      parameters:
        - name: id_flag_active
          in: query
          description: 'Whether to send mails to active and inactive clients. One of {@link \AFlagSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_postcard_campaign_client
          in: query
          description: 'Recipient group. One of {@link \Wl\Postcard\Campaign\CampaignClientSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business in which clients must be searched.'
          schema:
            type: string
        - name: s_login_type
          in: query
          description: 'Serialized by JSON list of client types. Every element is primary key in {@link \Wl\Login\Type\Sql} table.'
          schema:
            type: string
        - name: s_member_group
          in: query
          description: 'Serialized by JSON list of client groups. Every element is primary key in {@link \Wl\Member\Group\Sql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_quantity:
                        type: number
                        format: int32
                        description: 'Quantity of recipients.'
                      m_cost:
                        type: string
                        description: 'Total cost of campaign.'
      tags:
        - CampaignEdit-CampaignEditSummary
  '/Wl/Quiz/Element/ImageMarkup/ImageMarkupConfig.json':
    get:
      summary: 'Gets stored image markup configuration.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to bind configuration to a specific user.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      o_marker_config:
                        type: [object, array, "null"]
                        description: 'Last used marker configuration (style parameters) for the stored marker tool. `null` if not initialized yet. `array` for compatibility with JavaScript.'
                      text_marker_type:
                        type: string
                        description: 'Last selected marker tool type.'
      tags:
        - ImageMarkup-ImageMarkupConfig
    post:
      summary: 'Updates stored image markup configuration.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to bind configuration to a specific user.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                o_marker_config:
                  type: [object, array, "null"]
                  description: 'Last used marker configuration (style parameters) for the stored marker tool. `null` if not initialized yet. `array` for compatibility with JavaScript.'
                text_marker_type:
                  type: string
                  description: 'Last selected marker tool type.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ImageMarkup-ImageMarkupConfig
  '/Wl/Reception/Roster/Design/ReceptionRosterDesign.json':
    get:
      summary: 'Returns configuration for the Attendance Kiosk. Returns kiosk display settings including custom image, logo, direction mode, background color, and business name for the specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image data for image which will be displayed in attendance web app page. <dl> <dt>int `i_height`</dt> <dd>Requested image height.</dd> <dt>int `i_height_src`</dt> <dd>Original image height.</dd> <dt>int `i_width`</dt> <dd>Requested image width.</dd> <dt>int `i_width_src`</dt> <dd>Original image width.</dd> <dt>int `id_type_src`</dt> <dd> Image type ID. One of {@link \Core\Drive\DriveTypeSid} constants. Will be present only for \Core\Drive\Drive images. </dd> <dt>bool `is-resize`</dt> <dd>Whether image was resized to satisfy specified <var>i_width</var> and <var>i_height</var>.</dd> <dt>string `url-thumbnail`</dt> <dd>Url to requested image.</dd> <dt>string `url-view`</dt> <dd>Url to original image.</dd> </dl>'
                      hide_profile_images:
                        type: boolean
                        description: 'Whether to hide client profile images. `true` if client profile images should be hidden, `false` otherwise.'
                      i_attendance_direct_delay:
                        type: number
                        format: int32
                        description: 'Number of seconds of inactivity before automatic redirect.'
                      i_book_open:
                        type: number
                        format: int32
                        description: 'Number of minutes for the client check-in window after session has started.'
                      i_book_quick_app:
                        type: number
                        format: int32
                        description: 'Units of time for the look ahead window for attendance app.'
                      i_confirm_delay:
                        type: number
                        format: int32
                        description: 'Delay in seconds on attendance web app confirmation screen before redirect to login screen.'
                      id_attendance_direct:
                        type: number
                        format: int32
                        description: 'ID of the action to take when automatically redirecting. One of {@link DirectSid} constants.'
                      id_book_quick_app:
                        type: number
                        format: int32
                        description: 'Unit of time for the look ahead window for attendance app.'
                      is_attend_free:
                        type: boolean
                        description: '`true` if clients are allowed to check-in unpaid, `false` otherwise.'
                      is_book_open:
                        type: boolean
                        description: 'Whether to allow sign-ins to classes in progress.'
                      is_book_optional:
                        type: boolean
                        description: '`true` if clients are allowed to check-in without booking prior, `false` otherwise.'
                      is_book_quick_app:
                        type: boolean
                        description: '`true` if clients are allowed to sign in before session is started., `false` otherwise.'
                      show_business_name:
                        type: boolean
                        description: '`true` - show business name on attendance web app; `false` - do not show.'
                      show_confirm_screen:
                        type: boolean
                        description: '`true` - show confirm screen on attendance web app; `false` - do not show.'
                      text_business_name:
                        type: string
                        description: 'Name of the business to display in the attendance web app.'
      tags:
        - Design-ReceptionRosterDesign
  '/Wl/Reception/Roster/Search/Search.json':
    get:
      summary: 'Returns list of clients by search string.'
      parameters:
        - name: i_request
          in: query
          description: 'Number of the request. Is required to ignore old requests, when new request was already done.'
          schema:
            type: number
            format: int32
        - name: k_location
          in: query
          description: 'ID of the location. Used to determinate business.'
          schema:
            type: number
            format: int32
        - name: s_request
          in: query
          description: 'String that will be used as search phrase.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret string to get access. As this API can be used without backend we need to protect using thi API be secret code. See {@link \Wl\Reception\Roster\Controller::secret()}'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_user:
                        type: array
                        description: 'List of user information. Each element is array with next structure: <dl> <dt>string `s_firstname`</dt> <dd>First name of a client.</dd> <dt>string `s_lastname`</dt> <dd>Last name of a client.</dd> <dt>string `uid`</dt> <dd>User key.</dd> <dt>string `url_photo`</dt> <dd>Url link to a photo of a client.</dd> </dl>'
                      i_request:
                        type: number
                        format: int32
                        description: 'Number of the request. Is required to ignore old requests, when new request was already done.'
      tags:
        - Search-Search
  '/Wl/Search/Template/Menu/Menu.json':
    delete:
      summary: 'Removes search template.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Primary key in the {@link \Wl\Search\Template\SearchTemplateSql} table.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Menu-Menu
    get:
      summary: 'Gets list of saved search templates.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_search_list:
                        type: array
                        description: 'List of saved search templates. <dl> <dt>bool `is_default`</dt> <dd>Whether this template is used as default template.</dd> <dt>string `k_search_template`</dt> <dd>Template key. Primary key in the {@link \Wl\Search\Template\SearchTemplateSql} table.</dd> <dt>string `text_title`</dt> <dd>Search template title.</dd> </dl>'
                      is_open:
                        type: boolean
                        description: 'Whether the menu is opened.'
      tags:
        - Menu-Menu
    post:
      summary: 'Saves search template as default.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Primary key in the {@link \Wl\Search\Template\SearchTemplateSql} table.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_default:
                  type: [boolean, "null"]
                  description: 'Whether given template should be marked as default or not. <tt>null</tt> if field is not set.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Menu-Menu
  '/Wl/Search/Template/Panel/Panel.json':
    get:
      summary: 'Gets panel template data.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_panel:
                        type: array
                        description: 'Variables to pass into the panel template. <dl> <dt>string[] `a_staff_selected`</dt> <dd>List of user keys of shared search template.</dd> <dt>int `cid_search_entity`</dt> <dd>Search entity CID. Constant from {@link \Wl\Search\SearchEntityAbstract} subclasses.</dd> <dt>int `id_share`</dt> <dd>Share option constant. See {@link \Wl\Share\ShareSid}.</dd> <dt>string `k_search_template`</dt> <dd>Key of existing template. Primary key in the {@link \Wl\Search\Template\Panel\SearchTemplateSql} table.</dd> <dt>bool `show_staff_role_list`</dt> <dd>Whether staff role list should be shown for share.</dd> <dt>string `text_title`</dt> <dd>Search template title.</dd> </dl>'
                      k_search_template:
                        type: string
                        description: 'Key of existing template. Can be empty if template needs to be created.'
      tags:
        - Panel-Panel
    post:
      summary: 'Creates new search template.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_criteria:
                  type: array
                  description: 'Entity in array format. <dl> <dt>int `cid_search_criteria`</dt> <dd>Search criteria CID. Constant from {@link \Wl\Search\Criteria\SearchCriteriaAbstract} subclasses.</dd> <dt>int `id_operation`</dt> <dd>ID of selected operation. Constant from {@link \Wl\Search\SearchOperationSid}.</dd> <dt>string `s_id`</dt> <dd> Unique criteria key. <i>Detailed description see {@link \Wl\Search\Criteria\SearchCriteriaAbstract::__construct()}.</i> </dd> <dt>mixed `x_value`</dt> <dd>Operation data value.</dd> </dl> can be transferred to server via HTTP.'
                a_logic:
                  type: [object, "null"]
                  description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                a_search_entity:
                  type: array
                  description: 'Search entity CID list. Constants from {@link SearchEntityAbstract} subclasses.'
                a_staff_role_selected:
                  type: array
                  description: 'Selected staff roles of the search template. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. <i>Primary key in {@link \RsBusinessRoleSql} table.</i></dd> </dl>'
                id_share:
                  type: string
                  description: 'Type of share option. One of {@link ShareSid} constants.'
                json_criteria:
                  type: string
                  description: 'Entity in array format. Alternative of {@link SearchTemplatePanelApiTrait::$a_criteria} to transfer data via HTTP as one variable. <i>For more information about the format, see {@link SearchCriteriaList::createArray()} method.</i>'
                text_title:
                  type: string
                  description: 'Search template title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_search_template:
                        type: string
                        description: 'Key of existing template. Can be empty if template needs to be created.'
      tags:
        - Panel-Panel
    put:
      summary: 'Updates search template.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_search_template
          in: query
          description: 'Key of existing template. Can be empty if template needs to be created.'
          schema:
            type: string
        - name: s_search_group
          in: query
          description: 'Unique string identifying the name of the search group.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_criteria:
                  type: array
                  description: 'Entity in array format. <dl> <dt>int `cid_search_criteria`</dt> <dd>Search criteria CID. Constant from {@link \Wl\Search\Criteria\SearchCriteriaAbstract} subclasses.</dd> <dt>int `id_operation`</dt> <dd>ID of selected operation. Constant from {@link \Wl\Search\SearchOperationSid}.</dd> <dt>string `s_id`</dt> <dd> Unique criteria key. <i>Detailed description see {@link \Wl\Search\Criteria\SearchCriteriaAbstract::__construct()}.</i> </dd> <dt>mixed `x_value`</dt> <dd>Operation data value.</dd> </dl> can be transferred to server via HTTP.'
                a_logic:
                  type: [object, "null"]
                  description: 'Logic connection between criteria. See {@link \Wl\Search\Criteria\SearchCriteriaList::$a_criteria_logic} for details.'
                a_search_entity:
                  type: array
                  description: 'Search entity CID list. Constants from {@link SearchEntityAbstract} subclasses.'
                a_staff_role_selected:
                  type: array
                  description: 'Selected staff roles of the search template. <dl> <dt>int `id_privilege_role`</dt> <dd>ID of privilege role. Constant from {@link \RsPrivilegeRoleSid}.</dd> <dt>string `k_business_role`</dt> <dd>Key of the business role. <i>Primary key in {@link \RsBusinessRoleSql} table.</i></dd> </dl>'
                id_share:
                  type: string
                  description: 'Type of share option. One of {@link ShareSid} constants.'
                json_criteria:
                  type: string
                  description: 'Entity in array format. Alternative of {@link SearchTemplatePanelApiTrait::$a_criteria} to transfer data via HTTP as one variable. <i>For more information about the format, see {@link SearchCriteriaList::createArray()} method.</i>'
                text_title:
                  type: string
                  description: 'Search template title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Panel-Panel
  '/Wl/Skin/Application/Connect/ApplicationConnect.json':
    post:
      summary: 'Creates or updates the integration credentials for the given business application. If a record already exists for `k_business`, it is updated in place. Otherwise, a new record is created and linked to the business. does not exist, belongs to a different datacenter, or lacks an active Mobile App subscription; the domain is already used by another application; or iOS key fields are incomplete.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_apple:
                  type: boolean
                  description: '`true` to enable Sign In with Apple for this application, `false` to disable.'
                is_facebook:
                  type: boolean
                  description: '`true` to enable Sign In with Facebook (iOS) for this application, `false` to disable.'
                is_facebook_android:
                  type: boolean
                  description: '`true` to enable Sign In with Facebook (Android) for this application, `false` to disable.'
                k_business:
                  type: string
                  description: 'Business key.'
                s_firebase_key:
                  type: string
                  description: 'Firebase service account private key (JSON content of the `.p8`-equivalent key file).'
                s_firebase_project:
                  type: string
                  description: 'Firebase project ID.'
                s_google_id:
                  type: string
                  description: 'Google OAuth client ID.'
                s_google_reverse:
                  type: string
                  description: 'Reversed Google OAuth client ID (iOS URL scheme).'
                s_google_secret:
                  type: string
                  description: 'Google OAuth client secret.'
                text_cert_ios_login:
                  type: string
                  description: 'Sign In with Apple key (`.p8` file content).'
                text_cert_ios_push:
                  type: string
                  description: 'APNs push notification auth key (`.p8` file content).'
                text_domain:
                  type: string
                  description: 'Bundle ID used as the application identifier.'
                text_key_login_id:
                  type: string
                  description: 'Sign In with Apple key ID.'
                text_key_push_id:
                  type: string
                  description: 'APNs push notification key ID.'
                text_name:
                  type: string
                  description: 'Application display name.'
                text_team_id:
                  type: string
                  description: 'Apple Developer Team ID.'
                url_google_redirect:
                  type: string
                  description: 'Google OAuth redirect URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_spa_application:
                        type: string
                        description: 'Key of the created or updated application. Set in the response after a successful `post()` call.'
      tags:
        - Connect-ApplicationConnect
  '/Wl/Skin/Application/Resource/ApplicationResource.json':
    get:
      summary: 'Gets application resources. Returns image resource groups, version information, and Google OAuth credentials for one or all White Label mobile applications. Called by the build pipeline to gather the data needed to generate application source bundles. Pass a business key to restrict results to a single app.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key. Empty to get data for all businesses.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_application:
                        type: array
                        description: 'The application data. Key is the business key. <i>Primary key in {@link \RsBusinessSql} table.</i> Value is application resource data for that business. Each element: <dl> <dt>array `a_resource`</dt> <dd> List of resource groups for image generation. <i>See {@link \Wl\Skin\Application\Resource\EditController::resourceStructure()}.</i> <dl> <dt>array[] `a_group`</dt> <dd> Groups of images, such as icons, loading screens or screenshots. One element of this array is an array that contains: <dl> <dt>array[] `a_image`</dt> <dd> A list of images in the group. One element contains parameters of an image: <dl> <dt>string[] `a_file`</dt> <dd>A list of fully qualified file names. This name is used to save files in final archive.</dd> <dt>bool `can_transparent`</dt> <dd> Specified if this image can have transparent pixels. <tt>true</tt> if it is allowed that there be transparent pixels. <tt>false</tt> if it is required that all pixels be opaque. </dd> <dt>int `i_height`</dt> <dd>Required image height.</dd> <dt>int `i_width`</dt> <dd>Required image width.</dd> <dt>bool `is_require`</dt> <dd>Whether this image is required.</dd> </dl> </dd> <dt>string `s_class`</dt> <dd>CSS class for this images block.</dd> <dt>string `text_comment`</dt> <dd>Comment for images block.</dd> <dt>string `text_subtitle`</dt> <dd>Subtitle for images block.</dd> <dt>string `text_title`</dt> <dd>Title for images block.</dd> </dl> </dd> <dt>string `text_group`</dt> <dd>Title of the group.</dd> </dl> </dd> <dt>int `i_version`</dt> <dd>Application version number.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql}.</i></dd> <dt>string `s_google_id`</dt> <dd>Google OAuth client ID for this application.</dd> <dt>string `s_google_reverse`</dt> <dd>Reverse Google OAuth client ID used for deep linking.</dd> <dt>string `s_splash_color_android`</dt> <dd>Android splash screen background color in hex format.</dd> <dt>string `s_version_name`</dt> <dd>Application version name string (for example, ''2.0.1'').</dd> <dt>string `text_annotation`</dt> <dd>Short app store description.</dd> <dt>string `text_description`</dt> <dd>Full app store description for iOS App Store.</dd> <dt>string `text_description_google`</dt> <dd>Full app store description for Google Play.</dd> <dt>string `text_domain`</dt> <dd>Application domain.</dd> <dt>string `text_keyword`</dt> <dd>App store search keywords.</dd> <dt>string `text_name`</dt> <dd>Application internal name.</dd> <dt>string `text_title`</dt> <dd>Application display title shown in app stores.</dd> </dl>'
      tags:
        - Resource-ApplicationResource
  '/Wl/Skin/Application/Resource/ApplicationResourceMaterial.json':
    post:
      summary: 'Saves text metadata for the given business application. Persists all text fields of the Client App Materials form (store listing copy, contact details, privacy policy URL, etc.) for the specified business. File assets such as icons and screenshots are submitted separately via {@link ApplicationResourceUploadApi}. Requires the App Build Automation edit privilege. the category does not exist, or the update type is invalid.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                i_version:
                  type: number
                  format: int32
                  description: 'Code version.'
                id_category:
                  type: number
                  format: int32
                  description: 'Application category in the store. One of {@link ApplicationCategorySid} constants. `0` if no category is selected.'
                id_version_change:
                  type: number
                  format: int32
                  description: 'Type of application update. One of {@link AppUpdateTypeEnum} constants.'
                k_business:
                  type: string
                  description: 'Business key.'
                s_firebase_ios:
                  type: string
                  description: 'Reversed client ID of the iOS application on Firebase.'
                s_version_name:
                  type: string
                  description: 'Version name for the App Store and Google Play builds.'
                text_annotation:
                  type: string
                  description: 'Short application description in the store (up to 80 characters).'
                text_country:
                  type: string
                  description: 'Available countries for application distribution (comma-separated list).'
                text_description:
                  type: string
                  description: 'Full application description in the store.'
                text_description_google:
                  type: string
                  description: 'Full application description specific to Google Play. If empty, `text_description` is used for Google Play as well.'
                text_domain:
                  type: string
                  description: 'Bundle ID / package name (e.g. `com.fitness.studio`).'
                text_email:
                  type: string
                  description: 'Support email address shown in the store listing.'
                text_information:
                  type: string
                  description: 'Additional information about the application build.'
                text_keyword:
                  type: string
                  description: 'App Store search keywords (comma-separated).'
                text_name:
                  type: string
                  description: 'Application name shown on the device home screen.'
                text_phone:
                  type: string
                  description: 'Support phone number shown in the store listing.'
                text_title:
                  type: string
                  description: 'Application title in the App Store listing.'
                text_website:
                  type: string
                  description: 'Support website URL shown in the store listing.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Resource-ApplicationResourceMaterial
  '/Wl/Skin/Application/Resource/ApplicationResourceUpload.json':
    post:
      summary: 'Uploads image and file assets for the given business application. Each image must be submitted as `a_image_upload[{key}]`, where `{key}` is the image ID. The `google-services.json` file must be submitted as `google_services_json`.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_image_upload:
                  type: object
                  description: 'Image assets keyed by image ID: Each value contains array with keys that are the sanitized asset file paths (e.g. `icon_ios_Icon_60_3x_png`). <dl> <dt>int[] `error`</dt> <dd>An error code representing the success or failure status.</dd> <dt>string[] `tmp_name`</dt> <dd>The temporary filename of the file in which the uploaded file was stored on the server.</dd> </dl>'
                f_google_services_json:
                  type: object
                  description: 'Firebase Android config file (`google-services.json`).'
                k_business:
                  type: string
                  description: 'Business key.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Resource-ApplicationResourceUpload
  '/Wl/Skin/Application/Upgrade/ApplicationUpgrade.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_version:
                  type: number
                  format: int32
                  description: 'The type of update to be performed.'
                s_business:
                  type: string
                  description: 'The business that must be upgraded.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Upgrade-ApplicationUpgrade
  '/Wl/Staff/Schedule/Add/Add.json':
    post:
      summary: 'Saves new time interval.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_day:
                  type: array
                  description: 'List of working days, constants from {@link \ADateWeekSid}.'
                a_service:
                  type: array
                  description: 'Services list, each element is a primary key in {@link \RsServiceSql}. Empty list means all services. <tt>null</tt> means not set.'
                dt_end_date:
                  type: string
                  description: 'End date of working period.'
                dt_end_time:
                  type: string
                  description: 'End time.'
                dt_start_date:
                  type: string
                  description: 'Start date of working period.'
                dt_start_time:
                  type: string
                  description: 'Start time.'
                i_repeat:
                  type: number
                  format: int32
                  description: 'Count of the periods which specified in {@link AddApi::$id_repeat}. If this field is absent default value {@link \Wl\Classes\Period\Period::REPEAT_COUNT_DEFAULT} is used.'
                id_repeat:
                  type: number
                  format: int32
                  description: 'Measuring unit of {@link AddApi::$i_repeat}. One of {@link \ADurationSid} constants. If this field is absent default value {@link \Wl\Classes\Period\Period::REPEAT_PERIOD_DEFAULT} is used.'
                is_break:
                  type: boolean
                  description: 'Whether model works with available working time. <tt>false</tt> means available working time. <tt>true</tt> means unavailable working time.'
                is_eternal:
                  type: boolean
                  description: 'Whether period is eternal. <tt>true</tt> means on going. <tt>false</tt> means end date will be used.'
                is_service_all:
                  type: boolean
                  description: 'Whether all services selected on form.'
                k_location:
                  type: string
                  description: 'Location key. Primary key in {@link \RsLocationSql} table.'
                k_staff:
                  type: string
                  description: 'Staff key. Primary key in {@link \RsStaffSql} table.'
                text_note:
                  type: string
                  description: 'Note to working interval.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_period:
                        type: array
                        description: 'Staff period key. Primary key from {@link \RsStaffPeriodSql}.'
      tags:
        - Add-Add
  '/Wl/User/Option/PromptList/PromptList.json':
    delete:
      summary: 'Removes option value for specified user.'
      parameters:
        - name: id_option
          in: query
          description: 'Option ID.'
          schema:
            type: number
            format: int32
        - name: uid
          in: query
          description: 'User key for which option value is updated. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PromptList-PromptList
    post:
      summary: ''
      parameters:
        - name: id_option
          in: query
          description: 'Option ID.'
          schema:
            type: number
            format: int32
        - name: uid
          in: query
          description: 'User key for which option value is updated. Primary key in {@link \PassportLoginSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_value:
                  type: string
                  description: 'Option value.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - PromptList-PromptList
  '/Wl/Promotion/Edit/Summary/Summary.json':
    get:
      summary: 'Retrieves information about summary price depends on initial price and taxes.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which price should be calculated.'
          schema:
            type: string
        - name: m_price
          in: query
          description: 'Initial price without taxes (or with depends on locale).'
          schema:
            type: string
        - name: s_tax
          in: query
          description: 'List of tax IDs divided by coma to apply to initial price.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      m_summary:
                        type: string
                        description: 'Summary price in business currency.'
      tags:
        - Summary-Summary
  '/Wl/Promotion/Edit/Region/RegionList.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_promotion
          in: query
          description: 'Promotion key. <tt>null</tt> if promotion is not created yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_franchise_region:
                        type: array
                        description: 'List of franchise regions. Has next structure: <dl> <dt>bool `is_select`</dt> <dd><tt>true</tt> if region is selected, <tt>false</tt> otherwise.</dd> <dt>string `k_franchise_region`</dt> <dd>Franchise region key. Primary key in {@link \Wl\Coupon\Franchise\FranchiseRegionSql} table.</dd> <dt>string `text_title`</dt> <dd>Name of the franchise region.</dd> </dl>'
      tags:
        - Region-RegionList
  '/Wl/Promotion/Membership/Report/MembershipConversionFilterView.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key to load filter for. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: k_promotion_from
          in: query
          description: 'Selected value of `Converted From`. Primary key in {@link \RsPromotionSql} or `null` if filter `Converted From` option should not be applied. `0` when filter was applied, but promotion was not selected.'
          schema:
            type: [string, "null"]
        - name: k_promotion_to
          in: query
          description: 'Selected value of `Converted To`. Primary key in {@link \RsPromotionSql} or `null` if filter `Converted To` option should not be applied. `0` when filter was applied, but promotion was not selected.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_view:
                        type: string
                        description: 'Filter view html'
      tags:
        - Report-MembershipConversionFilterView
  '/Wl/Promotion/Terminate/Reason/PromotionTerminateReason.json':
    delete:
      summary: 'Deletes the reason.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_reason
          in: query
          description: 'Reason key. `null` to create new reason.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Reason-PromotionTerminateReason
    get:
      summary: 'Gets list of available termination reasons for a promotions.'
      parameters:
        - name: is_backend
          in: query
          description: 'Business key.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_reason_list:
                        type: array
                        description: 'List of available termination reasons for a promotions. <dl> <dt>string `k_reason`</dt> <dd>Reason key. Primary key in {@link \Wl\Promotion\Terminate\Reason\PromotionTerminateReasonSql} table.</dd> <dt>string `text_title`</dt> <dd>Reason title.</dd> </dl>'
      tags:
        - Reason-PromotionTerminateReason
    post:
      summary: 'Edits or creates new reason.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_reason
          in: query
          description: 'Reason key. `null` to create new reason.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: [string, "null"]
                  description: 'Reason title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_reason:
                        type: [string, "null"]
                        description: 'Reason key. `null` to create new reason.'
      tags:
        - Reason-PromotionTerminateReason
  '/Wl/Passport/Login/Enter/PassportOtp.json':
    get:
      summary: 'Sends an OTP code to the user''s email or phone number to initiate authorization. Checks the OTP rate limit, generates a new code for the given user, and dispatches it according to the chosen delivery strategy. The user must not be already signed in and must not be an admin. There are two ways to specify the delivery channel: **1. Broadcast strategy** (default) Set `$id_delivery_strategy` to `0` or {@link OtpDeliveryStrategyEnum::BROADCAST} (or omit it entirely). Use `$is_mail` and/or `$is_phone` to specify which channels to use. At least one of them must be `true`; the OTP is sent to every channel that is enabled. The user must have a corresponding contact — a phone number when `$is_phone` is `true`, an email when `$is_mail` is `true` — otherwise an error is thrown. Example: set `$is_mail = true` and `$is_phone = true` to send OTP via both email and SMS simultaneously. **2. Priority strategy** Set `$id_delivery_strategy` to {@link OtpDeliveryStrategyEnum::PRIORITY}. Provide `$text_delivery_priority` — a comma-separated list of `sms` and `email` values in the preferred order, e.g. `"sms,email"`. The system picks the first channel from the list for which the user has valid contact data and sends the OTP only to that one channel. The selected channel is returned in `$text_delivery_selected`; when SMS is selected, the last four digits of the phone are returned in `$text_phone_masked`. If none of the listed channels can be used, an error is thrown. Example: set `$text_delivery_priority = "sms,email"` to prefer SMS but fall back to email if the user has no phone number.'
      parameters:
        - name: id_delivery_strategy
          in: query
          description: 'Type of delivery strategy from {@link OtpDeliveryStrategyEnum}.'
          schema:
            type: number
            format: int32
        - name: is_mail
          in: query
          description: 'Whether OTP code will be sending to user via email. `true` if OTP code is sending through email, `false` if OTP code is sending through phone number.'
          schema:
            type: boolean
        - name: is_phone
          in: query
          description: 'Whether OTP code will be sending to user via email. `true` if OTP code is sending through phone number, `false` if OTP code is sending through email.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_delivery_priority
          in: query
          description: 'Priority of delivery. Fixed values `sms` and `email` are supported. Should be given in priority order comma-separated.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_delivery_selected:
                        type: string
                        description: 'Delivery channel that was selected based on the given priorities and user data. Fixed values `sms` and `email` are supported. Only one value is returned.'
                      text_phone_masked:
                        type: string
                        description: 'Phone number masked with `*` symbols in case if we have priority sending and sms sending was selected.'
      tags:
        - Enter-PassportOtp
    post:
      summary: 'Verifies the submitted OTP code and establishes an authorized session for the user. Validates the OTP code for the given user, signs in the session, fires the post-login event, completes business registration if applicable, and returns the redirect URL for the business frontend.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp_code:
                  type: string
                  description: 'Otp code integer that was random generated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_attempt_left:
                        type: number
                        format: int32
                        description: 'Number of attempts left to submit the correct otp code.'
                      url_redirect:
                        type: string
                        description: 'Redirect url after successful authorization.'
      tags:
        - Enter-PassportOtp
  '/Wl/Passport/Login/Register/RegisterOtp.json':
    get:
      summary: 'Sends an OTP code to the user''s email or phone number to initiate authorization. Checks the OTP rate limit, generates a new code for the given user, and dispatches it via email, SMS, or both depending on `$is_mail` and `$is_phone`. The user must not be already signed in and must not be an admin.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: text_mail
          in: query
          description: 'Email address OTP code will be sent to.'
          schema:
            type: string
        - name: text_phone
          in: query
          description: 'Phone number OTP code will be sent to.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Register-RegisterOtp
    post:
      summary: 'Verifies the submitted OTP code and establishes an authorized session for the user. Validates the OTP code for the given user, signs in the session, fires the post-login event, completes business registration if applicable, and returns the redirect URL for the business frontend.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_otp_code:
                  type: string
                  description: 'Otp code integer that was random generated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_attempt_left:
                        type: number
                        format: int32
                        description: 'Number of attempts left to submit the correct otp code.'
                      s_jwt_token:
                        type: string
                        description: 'JWT token generated after successful otp validation.'
                      uid:
                        type: string
                        description: 'Key of the user, which was signed in after successful otp verification.'
                      url_redirect:
                        type: string
                        description: 'Redirect url after successful authorization.'
      tags:
        - Register-RegisterOtp
  '/Wl/DoorAccess/Brivo/Invitation/BrivoUserInvitation.json':
    get:
      summary: 'Sets {@link \Wl\DoorAccess\Brivo\Invitation\BrivoUserInvitationApi::$is_redeem} to `true` if an invitation is redeemed, and to `false` otherwise. Method retrieves an invitation status from database, whether it is not redeemed the invitation data is retrieved from Brivo. Method controls time of last update of the invitation''s data and updates it if necessary(invitation''s data exists and outdated).'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user for whom an invitation should be sent. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      has_brivo_invitation:
                        type: boolean
                        description: 'Whether the client has a Brivo invitation, and is present in the client''s group with Brivo invitation enabled. `true` if the client has a Brivo invitation, `false` otherwise.'
                      is_redeem:
                        type: boolean
                        description: 'This will be `true` an invitation is redeemed see {@link \Wl\DoorAccess\Brivo\Invitation\BrivoInvitationStatusEnum}, `false` otherwise.'
      tags:
        - Invitation-BrivoUserInvitation
    post:
      summary: 'This method is called to process request for Brivo mobile pass invitation for a client.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of a user for whom an invitation should be sent. Primary key in {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_redeem:
                        type: boolean
                        description: 'This will be `true` an invitation is redeemed see {@link \Wl\DoorAccess\Brivo\Invitation\BrivoInvitationStatusEnum}, `false` otherwise.'
      tags:
        - Invitation-BrivoUserInvitation
  '/Wl/Virtual/Meeting/NonIntegrated/NonIntegratedMeetingEnd.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                s_meeting_id:
                  type: string
                  description: 'Meeting id.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - NonIntegrated-NonIntegratedMeetingEnd
  '/Wl/Virtual/Meeting/Zoom/ZoomMeetingEnd.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Business key.'
                s_meeting_id:
                  type: string
                  description: 'Meeting id.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Zoom-ZoomMeetingEnd
  '/Wl/Virtual/Zoom/Webhook/User.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event:
                  type: number
                  format: int32
                  description: 'Event type.'
                o_payload:
                  type: object
                  description: 'User payload information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_encrypted:
                        type: string
                        description: 'Encrypted token string.'
                      s_token_plain:
                        type: string
                        description: 'Plain token string.'
      tags:
        - Webhook-User
  '/Wl/Virtual/Zoom/Webhook/MeetingEnd.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_event:
                  type: number
                  format: int32
                  description: 'Event type.'
                o_payload:
                  type: object
                  description: 'Meeting payload information.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_token_encrypted:
                        type: string
                        description: 'Encrypted token string.'
                      s_token_plain:
                        type: string
                        description: 'Plain token string.'
      tags:
        - Webhook-MeetingEnd
  '/Wl/Help/Popup/Email/EmailForm.json':
    post:
      summary: 'Sends an email for the help popup.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_attach:
                  type: array
                  description: 'The attached files to send with the email. Each element corresponds to one attached file and has the following structure. <dl> <dt>string `s_encoded_content`</dt> <dd>The file''s content encoded in base64.</dd> <dt>string `s_type`</dt> <dd>The file''s MIME type.</dd> <dt>string `s_name`</dt> <dd>The file''s name.</dd> </dl>'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. It will be <tt>null</tt> if not yet set.'
                s_description:
                  type: string
                  description: 'The description of the issue, the body of the email.'
                s_subject:
                  type: string
                  description: 'The subject of the email.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_reply:
                        type: string
                        description: 'The reply to address for the email. This will be the email of the staff member. The empty string will be returned if the validation is not passed.'
      tags:
        - Email-EmailForm
  '/Wl/Zapier/Lead/Trigger/Create.json':
    delete:
      summary: 'Makes webhook unsubscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trigger-Create
    get:
      summary: 'Returns sample data for trigger. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Trigger-Create
    post:
      summary: 'Makes webhook subscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                s_id:
                  type: string
                  description: 'Identifier of the webhook in Zapier.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: 'Email of the client.'
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the client.'
                url_webhook:
                  type: string
                  description: 'Webhook URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Trigger-Create
  '/Wl/Zapier/Lead/Action/Create.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Create
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dl_birth:
                        type: string
                        description: 'Date of birth of the lead.'
                      text_address:
                        type: string
                        description: 'Home address of the lead.'
                      text_mail:
                        type: string
                        description: 'Email name of the lead.'
                      text_name_first:
                        type: string
                        description: 'First name of the lead.'
                      text_name_last:
                        type: string
                        description: 'Last name of the lead.'
                      text_phone:
                        type: string
                        description: 'Phone number of the lead.'
      tags:
        - Action-Create
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Create
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_birth:
                  type: string
                  description: 'Date of birth of the lead.'
                k_lead_source:
                  type: [string, "null"]
                  description: 'Key of the lead source. If `null` than {@link \Wl\Mode\ModeSid::API} lead source will be used. {@link \Wl\Lead\Source\LeadSourceElementApi::LEAD_SOURCE_REPLACE_NONE} if Lead Source is to be unselected for the user.'
                text_address:
                  type: string
                  description: 'Home address of the lead.'
                text_mail:
                  type: string
                  description: 'Email name of the lead.'
                text_name_first:
                  type: string
                  description: 'First name of the lead.'
                text_name_last:
                  type: string
                  description: 'Last name of the lead.'
                text_phone:
                  type: string
                  description: 'Phone number of the lead.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      uid:
                        type: string
                        description: 'Captured user. Primary key in {@link \PassportLoginSql} table.'
      tags:
        - Action-Create
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Create
  '/Wl/Zapier/ProfileField/Trigger/Update.json':
    delete:
      summary: 'Makes webhook unsubscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trigger-Update
    get:
      summary: 'Returns sample data for trigger. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_value:
                        type: [string, "null"]
                        description: 'Value for the field. `null` in case when value is not passed.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the user.'
      tags:
        - Trigger-Update
    post:
      summary: 'Makes webhook subscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                k_field:
                  type: string
                  description: 'Field key.'
                s_id:
                  type: string
                  description: 'Identifier of the webhook in Zapier.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: 'Email of the client.'
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_value:
                  type: [string, "null"]
                  description: 'Value for the field. `null` in case when value is not passed.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the user.'
                url_webhook:
                  type: string
                  description: 'Webhook URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the user.'
      tags:
        - Trigger-Update
  '/Wl/Zapier/ProfileField/Action/UpdateField.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-UpdateField
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_value:
                        type: [string, "null"]
                        description: 'Value for the field. `null` in case when value is not passed.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the user.'
      tags:
        - Action-UpdateField
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-UpdateField
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                k_field:
                  type: string
                  description: 'Field key.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: 'Email address of the user.'
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_value:
                  type: [string, "null"]
                  description: 'Value for the field. `null` in case when value is not passed.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the user.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the user.'
      tags:
        - Action-UpdateField
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-UpdateField
  '/Wl/Marketing/Overview/CampaignPerformance/CampaignPerformance.json':
    get:
      summary: 'Get campaign performance data of the business.'
      parameters:
        - name: dl_end
          in: query
          description: 'End date.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Start date.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business of the current user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_total:
                        type: array
                        description: 'Result totals.'
      tags:
        - CampaignPerformance-CampaignPerformance
  '/Wl/Marketing/Overview/Welcome/Welcome.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'The business key within which request is made. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Welcome-Welcome
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'The business key within which request is made. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      show_welcome:
                        type: boolean
                        description: 'Determines whether welcome screen should be shown.'
      tags:
        - Welcome-Welcome
  '/Wl/Marketing/Automation/CreateFlow/TriggerList.json':
    get:
      summary: 'Gets pre-built automation template data.'
      parameters:
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table. `null` to use the system business.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_prebuilt_filter:
                        type: array
                        description: 'A list of available pre-built automation template types and the count of templates in each type. Each element has the following structure: <dl> <dt>int `i_count`</dt> <dd>Count of pre-built automation templates in this folder.</dd> <dt>string `k_folder`</dt> <dd> Folder key of the pre-built automation. Primary key in {@link \Wl\Marketing\Automation\Folder\AutomationFolderSql} table. </dd> <dt>string `text_folder`</dt> <dd>Title of the pre-built automation template type.</dd> </dl>'
                      a_prebuilt_list:
                        type: array
                        description: 'A list of available pre-built automation flows. Each element has the following structure: <dl> <dt>array[] `a_step`</dt> <dd>List of steps in the pre-built automation flow.</dd> <dt>int|null `id_mail`</dt> <dd>ID of the mail in {@link \RsMailSid}, `null` if no mail is associated with the pre-built flow.</dd> <dt>string `k_automation`</dt> <dd>Pre-built automation key. Primary key in {@link \Wl\Marketing\Automation\AutomationSql} table.</dd> <dt>string `k_folder`</dt> <dd> Folder key of the pre-built automation. Primary key in {@link \Wl\Marketing\Automation\Folder\AutomationFolderSql} table. </dd> <dt>int `id_trigger`</dt> <dd>ID of the trigger in {@link \Wl\Marketing\Automation\TriggerSid}.</dd> <dt>bool `is_new`</dt> <dd>Whether the pre-built automation flow has ''New'' label.</dd> <dt>string `text_description`</dt> <dd>Description of the pre-built automation flow.</dd> <dt>string `text_folder`</dt> <dd>Title of the folder of the pre-built automation flow.</dd> <dt>string `text_title`</dt> <dd>Title of the pre-built automation flow.</dd> <dt>string `url_image`</dt> <dd>URL of the image preview of the pre-built automation flow.</dd> </dl>'
                      a_trigger_filter:
                        type: array
                        description: 'A list of available trigger types and the count of triggers in each type. Each element has the following structure: <dl> <dt>int `id_trigger`</dt> <dd>ID of the trigger type in {@link \Wl\Marketing\Automation\CreateFlow\TriggerTypeSid}.</dd> <dt>int `i_count`</dt> <dd>Count of triggers in this type.</dd> <dt>string `text_trigger_type`</dt> <dd>Title of the trigger type.</dd> </dl>'
                      a_trigger_list:
                        type: array
                        description: 'A list of available triggers. Each element has the following structure: <dl> <dt>int|null `id_mail`</dt> <dd>ID of the mail in {@link \RsMailSid}. `null` if no mail is associated with the trigger.</dd> <dt>int `id_trigger`</dt> <dd>ID of the trigger in {@link \Wl\Marketing\Automation\TriggerSid}.</dd> <dt>int `id_trigger_type`</dt> <dd>ID of the trigger type in {@link \Wl\Marketing\Automation\CreateFlow\TriggerTypeSid}.</dd> <dt>bool `is_coming_soon`</dt> <dd> Whether the trigger has ''Coming soon'' label {@link \Wl\Marketing\Automation\Trigger\TriggerAbstract::IS_COMING_SOON}. </dd> <dt>string `text_description_list`</dt> <dd>Description of the trigger for the list of all triggers.</dd> <dt>string `text_description_setup`</dt> <dd>Description of the trigger for the setup trigger view.</dd> <dt>string `text_icon`</dt> <dd>CSS class of the icon representing the trigger.</dd> <dt>string `text_title`</dt> <dd>Title of the trigger.</dd> </dl>'
      tags:
        - CreateFlow-TriggerList
  '/Wl/Marketing/Automation/List/AutomationList.json':
    get:
      summary: 'Gets automation data.'
      parameters:
        - name: id_status
          in: query
          description: 'A status of automation. One of the {@link AutomationStatusSid} constants. `null` to not perform filtering by status.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
        - name: k_folder
          in: query
          description: 'Folder key. `null` to load automations from all folders. Empty string to load automations from the default folder.'
          schema:
            type: [string, "null"]
        - name: text_search
          in: query
          description: 'The filter phrase to filter automations by name.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_automation_list:
                        type: array
                        description: 'List of automations. This list matches all filters.'
                      a_page:
                        type: array
                        description: 'Pagination data.'
                      i_automation_active:
                        type: number
                        format: int32
                        description: 'Number of active automations. This number contains the number of all active automations that match all filters except {@link AutomationListApi::$id_status} filter, so it may be greater than the number of items in {@link AutomationListApi::$a_automation_list}.'
                      i_automation_draft:
                        type: number
                        format: int32
                        description: 'Number of draft automations. This number contains the number of all draft automations that match all filters except {@link AutomationListApi::$id_status} filter, so it may be greater than the number of items in {@link AutomationListApi::$a_automation_list}.'
                      i_automation_total:
                        type: number
                        format: int32
                        description: 'Total number of automations. This number contains the number of all automations that match all filters except {@link AutomationListApi::$id_status} filter, so it may be greater than the number of items in {@link AutomationListApi::$a_automation_list}.'
      tags:
        - List-AutomationList
  '/Wl/Marketing/Automation/Folder/AutomationFolder.json':
    delete:
      summary: 'Delete a folder. Automations in the folder will be moved to another folder or to the default folder. folder being deleted.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key where the folder is managed. `null` to use the system business of the current user.'
          schema:
            type: [string, "null"]
        - name: k_folder
          in: query
          description: 'Folder key.'
          schema:
            type: string
        - name: k_folder_move
          in: query
          description: 'Key of the folder where to move automations after deleting the current folder. If not passed, automations are moved to the default folder.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Folder-AutomationFolder
    get:
      summary: 'Get list of folders in the business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key where the folder is managed. `null` to use the system business of the current user.'
          schema:
            type: [string, "null"]
        - name: k_folder
          in: query
          description: 'Folder key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_folder:
                        type: array
                        description: 'List of custom folders in the business. If <tt>k_folder</tt> is passed, the list not contains the folder with this key. Each element has the following structure: <dl> <dt>string `k_folder`</dt> <dd>Folder key. Primary key in {@link \Wl\Marketing\Automation\Folder\AutomationFolderSql} table.</dd> <dt>string `text_title`</dt> <dd>Folder title.</dd> </dl>'
                      i_automation:
                        type: number
                        format: int32
                        description: 'Count of automations in the folder. If <tt>k_folder</tt> is not passed, the value is always <tt>0</tt>.'
      tags:
        - Folder-AutomationFolder
    post:
      summary: 'Create or edit a folder.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key where the folder is managed. `null` to use the system business of the current user.'
          schema:
            type: [string, "null"]
        - name: k_folder
          in: query
          description: 'Folder key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_title:
                  type: string
                  description: 'Title of the folder.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_folder:
                        type: string
                        description: 'Folder key.'
      tags:
        - Folder-AutomationFolder
  '/Wl/Marketing/Automation/Folder/FolderSort.json':
    post:
      summary: 'Change the order of folders.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_folder:
                  type: array
                  description: 'Sorted keys of folders.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. `null` to use the system business.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Folder-FolderSort
  '/Wl/Marketing/Automation/Folder/FolderMap.json':
    get:
      summary: 'Retrieve list of folders where the automation can be moved. The list does not contain the folder where the automation is currently located. Also retrieves the title of the automation and the title of the folder where it is currently located.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_folder:
                        type: array
                        description: 'List of folders where automation can be moved. Each element has the following structure: <dl> <dt>string `k_folder`</dt> <dd> Folder key. Primary key in {@link \Wl\Marketing\Automation\Folder\AutomationFolderSql} table. Empty string for the default folder. </dd> <dt>string `text_title`</dt> <dd>Folder title.</dd> </dl>'
                      k_folder_origin:
                        type: [string, "null"]
                        description: 'Folder key where the automation is currently located. `null` if the automation is in the default folder.'
                      text_automation:
                        type: string
                        description: 'Title of the automation.'
                      text_folder_origin:
                        type: string
                        description: 'Title of the folder in which the automation is located.'
      tags:
        - Folder-FolderMap
    post:
      summary: 'Move the automation to another folder. If the folder key is empty, the automation will be moved to the default folder.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
        - name: k_folder_move
          in: query
          description: 'Folder key where the automation should be moved. If `null`, the automation will be moved to the default folder.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Folder-FolderMap
  '/Wl/Marketing/Automation/Recipient/AutomationRecipient.json':
    get:
      summary: 'Returns the list of recipients for the given automation within the specified date range.'
      parameters:
        - name: dl_end
          in: query
          description: 'End date filter.'
          schema:
            type: string
        - name: dl_start
          in: query
          description: 'Start date filter.'
          schema:
            type: string
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_recipient_list:
                        type: array
                        description: 'Recipients data. Has the next structure: <dl> <dt>string `html_client`</dt> <dd>HTML representation of the recipient for client side.</dd> <dt>int `i_lead_sort`</dt> <dd> Lead-stage sort position. Used by JS to sort by the Lead Stage column. See {@link \Wl\Marketing\Automation\Recipient\AutomationRecipientManager::getRecipientList()} `@return` for the value scheme. </dd> <dt>int `i_status_sort`</dt> <dd>Status sort position. Used by JS to sort by the Status column.</dd> <dt>int `i_step_sort`</dt> <dd>Current-step sort position. Used by JS to sort by the Current Step column.</dd> <dt>int `id_status`</dt> <dd>Recipient status. One of {@link \Wl\Marketing\Automation\Recipient\RecipientStatusSid} constants.</dd> <dt>bool `is_manual`</dt> <dd>Whether the recipient was added manually. `true` if added manually, `false` otherwise.</dd> <dt>string `s_start_sort`</dt> <dd>Start date in UTC MySQL datetime format. Used by JS to sort by the Start date column.</dd> <dt>string `text_badge_class`</dt> <dd>Recipient status badge class.</dd> <dt>string `text_lead_stage`</dt> <dd>Client''s current lead stage display text. Empty string if the client has no lead record.</dd> <dt>string `text_lead_stage_class`</dt> <dd>CSS badge class for the lead stage badge. Empty string if the client has no lead record.</dd> <dt>string `text_start`</dt> <dd>Local date and time when the recipient was added to the automation.</dd> <dt>string `text_step`</dt> <dd> Current step display text. Shows ''Step N: Content'' for in-progress and excluded recipients, ''Finished automation'' for finished recipients, or ''-'' for legacy data. </dd> <dt>string `text_step_tooltip`</dt> <dd> Tooltip text for the step column. Contains the message body for SMS and push notification steps. Empty string for other step types. </dd> <dt>string `text_status`</dt> <dd>Recipient status title.</dd> <dt>string `text_status_class`</dt> <dd>CSS class of the recipient status.</dd> <dt>string `uid`</dt> <dd>User ID. Primary key in {@link \PassportLoginSql} table.</dd> </dl>'
      tags:
        - Recipient-AutomationRecipient
    post:
      summary: 'Manually adds selected clients to the automation as recipients. Clients who are already in progress are skipped and counted separately. Only new clients are scheduled for execution. empty or invalid, or any selected client is not an active member of the business.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_recipient_add:
                  type: [string, "null"]
                  description: 'JSON-encoded list of user UIDs to add as recipients. <i>Each element is a user key (primary key in {@link \PassportLoginSql} table).</i>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_recipient_add:
                        type: number
                        format: int32
                        description: 'Number of recipients successfully added to the automation.'
                      i_recipient_progress:
                        type: number
                        format: int32
                        description: 'Number of recipients that are already in progress and were not added again.'
      tags:
        - Recipient-AutomationRecipient
  '/Wl/Marketing/Automation/Recipient/AutomationRecipientStatus.json':
    get:
      summary: 'Returns the current status of a recipient in an automation flow. When a single user is specified, returns the status of their most recent enrollment in the automation. When a member group is specified, returns a map of recipient UIDs to their statuses for all users in that group who have a record in the automation.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_member_group
          in: query
          description: 'Member group key to filter recipients by. Can be a primary key in the {@link \Wl\Member\Group\Sql} table or {@link Group::ISAAC_GROUP_KEY} for the churn risk group. `null` if the request is filtered by an individual user instead of a member group.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Recipient user key. `null` if the request is filtered by a member group instead of an individual user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_group_status:
                        type: [array, "null"]
                        description: 'Statuses of the member group recipients in the automation flow. Keys are recipient UIDs, values are statuses from {@link RecipientStatusSid} constants. Only users that have a record in the automation are included. `null` if the request is filtered by an individual user instead of a member group.'
                      id_status:
                        type: [number, "null"]
                        description: 'Status of a recipient in an automation flow. One of {@link RecipientStatusSid} constants. `null` if the recipient is not found in the flow.'
      tags:
        - Recipient-AutomationRecipientStatus
    post:
      summary: 'Excludes a recipient from an automation flow. Marks the given user as excluded so they will no longer receive further steps of the automation. If the user is not currently in the flow the call has no effect.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Recipient user key. `null` if the request is filtered by a member group instead of an individual user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Recipient-AutomationRecipientStatus
  '/Wl/Marketing/Automation/Prebuilt/AutomationPrebuilt.json':
    get:
      summary: 'Gets automation data.'
      parameters:
        - name: is_original
          in: query
          description: 'Whether to get original automation data without Isaac template generation. If `true`, the API will return original automation data without Isaac template generation. If `false`, the API will return automation data with Isaac template generation, if the pre-built automation is marked as using Isaac.'
          schema:
            type: boolean
        - name: json_action
          in: query
          description: 'Call to action data in JSON format.'
          schema:
            type: [string, "null"]
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_automation:
                        type: array
                        description: 'Automation data. Result of {@link Automation::get()}.'
                      has_error:
                        type: boolean
                        description: 'Whether there is an error.'
      tags:
        - Prebuilt-AutomationPrebuilt
  '/Wl/Marketing/Automation/Prebuilt/PrebuiltDuplicate.json':
    get:
      summary: 'Get the business types connected to the pre-built automation.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_type:
                        type: array
                        description: 'List of business types. For {@link PrebuiltDuplicateApi::get()} method - list of business types connected to the duplicated prebuilt automation. For {@link PrebuiltDuplicateApi::post()} method - list of business types to connect to the new prebuilt automation.'
                      is_business_type_all:
                        type: boolean
                        description: 'Whether the prebuilt automation is connected to all business types. If `true`, the prebuilt automation is connected to all business types and can not be duplicated. If `false`, the automation is connected only to business types in `a_business_type`.'
      tags:
        - Prebuilt-PrebuiltDuplicate
    post:
      summary: 'Duplicate the pre-built automation.'
      parameters:
        - name: k_automation
          in: query
          description: 'Automation key.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_business_type:
                  type: array
                  description: 'List of business types. For {@link PrebuiltDuplicateApi::get()} method - list of business types connected to the duplicated prebuilt automation. For {@link PrebuiltDuplicateApi::post()} method - list of business types to connect to the new prebuilt automation.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Prebuilt-PrebuiltDuplicate
  '/Wl/Marketing/Automation/AiGeneration/AskIsaacAutomationContent.json':
    post:
      summary: 'Build the ISAAC request payload from automation data without sending the request.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_action:
                  type: [array, "null"]
                  description: 'Call to action data.'
                a_step:
                  type: array
                  description: 'List of automation steps.'
                id_trigger:
                  type: number
                  format: int32
                  description: 'ID of the trigger of the automation.'
                k_business:
                  type: [string, "null"]
                  description: 'Business key. `null` to use the system business .'
                text_description:
                  type: string
                  description: 'Description of the automation.'
                text_title:
                  type: string
                  description: 'Title of the automation.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_request_payload:
                        type: array
                        description: 'Request payload prepared for the ISAAC marketing generation API.'
      tags:
        - AiGeneration-AskIsaacAutomationContent
  '/Thoth/PayProcessor/Nuvei/Terminal/OMNIChannelApi/NuveiTerminalTransactionStart.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_currency:
                  type: [number, "null"]
                  description: 'ID of the currency. One of {@link \Core\Locale\CurrencySid} constants. `null` in case when not initialized yet.'
                id_pay_actor:
                  type: [number, "null"]
                  description: 'ID of the actor. One of {@link \RsPayActorSid} constants. `null` in case when not initialized yet.'
                is_authorize:
                  type: boolean
                  description: 'Whether authorization or capture should be performed. `true` (or anything that is not empty) to perform authorization. `false` (or anything that is empty, or do not pass this value) to charge the payment amount.'
                k_business:
                  type: [string, "null"]
                  description: 'Key of the business which receives the payment. `null` in case when not initialized yet.'
                k_business_merchant:
                  type: [string, "null"]
                  description: 'Key of the business merchant that should be used to perform the request. `null` in case when not initialized yet.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Key of a custom payment method. `null` in case when not initialized yet or empty if payment is performed with a standard payment method.'
                m_amount:
                  type: [string, "null"]
                  description: 'Payment amount. `null` in case when not initialized yet.'
                m_surcharge:
                  type: [string, "null"]
                  description: 'Surcharge amount. `null` in case when not initialized yet, empty string or null for zero surcharge amount.'
                m_tip:
                  type: string
                  description: 'Amount of tips entered in shopping cart.'
                s_terminal_id:
                  type: [string, "null"]
                  description: 'Unique terminal identifier. `null` in case when not initialized yet.'
                uid_purchase:
                  type: [string, "null"]
                  description: 'Payment owner user key. `null` in case when not initialized yet or if pay owner is anonymous.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_response:
                        type: [number, "null"]
                        description: 'The response id. One of {@link CSResponseSid} constants. `null` in case when not initialized yet.'
                      k_pay_transaction:
                        type: [string, "null"]
                        description: 'Key of payment transaction that was created. `null` in case when not initialized yet.'
                      s_exchange_identification:
                        type: string
                        description: 'The request exchange identification. `null` in case when not initialized yet.'
                      text_message:
                        type: [string, "null"]
                        description: 'Error message. `null` in case response has not received yet or request is not failed.'
      tags:
        - OMNIChannelApi-NuveiTerminalTransactionStart
  '/Thoth/Report/SalesReport/Transaction/TaxListReportFilterField/TaxListReport.json':
    get:
      summary: 'Returns taxes of the specified business.'
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business for which to get a list of taxes.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'A list of taxes. Each element has the following structure: <dl> <dt>string[] `a_location`</dt> <dd>The locations where the tax is applicable.</dd> <dt>float `f_value`</dt> <dd>The amount of the tax.</dd> <dt>string `k_tax`</dt> <dd>The type of the tax, one of the {@link \RsTaxSid} constants.</dd> <dt>string `k_tax`</dt> <dd>The tax key.</dd> <dt>string `text_title`</dt> <dd>The name of the tax.</dd> </dl>'
                      is_hidden_tax:
                        type: boolean
                        description: 'Whether a business has hidden. `true` if business franchisor has franchisee with taxes.'
                      is_removed_tax:
                        type: boolean
                        description: 'Whether a business has removed taxes. `true` if business has removed tax.'
      tags:
        - TaxListReportFilterField-TaxListReport
  '/Thoth/Report/SalesReport/Client/AccountReport/AccountHistoryReportInfo.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: [string, "null"]
        - name: uid_client
          in: query
          description: 'Client user key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_payer:
                        type: boolean
                        description: 'Whether the client is currently a payer (has entries in {@link \RsFamilyPayAr}). When `true`, the toggle is shown and defaults to on.'
                      is_payer_has_history:
                        type: boolean
                        description: 'Whether the client has at least one past account transaction paid on behalf of a relationship member. Meaningful only when {@link AccountHistoryReportInfoApi::$is_payer} is `false`. When `true`, the toggle is shown but defaults to off.'
                      text_payer_name:
                        type: string
                        description: 'Full name of the user who pays for this client. Empty string when the client pays for themselves.'
                      uid_payer:
                        type: string
                        description: 'UID of the user who pays for this client. Primary key in {@link \PassportLoginSql}. Empty string when the client pays for themselves.'
                      url_payer_balance_history:
                        type: string
                        description: 'URL of the payer''s Balance History and Statements page. Empty string when the client pays for themselves.'
                      url_payer_statement_history:
                        type: string
                        description: 'URL of the payer''s Statement History tab on the Balance History and Statements page. Empty string when the client pays for themselves.'
      tags:
        - AccountReport-AccountHistoryReportInfo
  '/Thoth/Report/SalesReport/Client/BalanceHistoryStatement/PayAccountCharge.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'Client user key. Primary key in {@link \PassportLoginSql}. `null` if not yet initialized.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of account pay methods with their charge URLs. Each element has the following structure: <dl> <dt>string `k_pay_method`</dt> <dd>The pay method key. `''0''` for the default (system) account.</dd> <dt>string `url_charge`</dt> <dd>URL of the "Make a payment" page for this account. See {@link \RsPayAccountCharge::url()}.</dd> </dl>'
      tags:
        - BalanceHistoryStatement-PayAccountCharge
  '/Thoth/Report/SalesReport/Client/ClientStatementHistoryReport/ClientStatementHistoryEmail.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: k_statement
          in: query
          description: 'Statement primary key.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                text_mail:
                  type: string
                  description: 'Email address to deliver the statement to. When empty, the client''s profile email address is used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ClientStatementHistoryReport-ClientStatementHistoryEmail
  '/Thoth/Report/SalesReport/Client/ClientStatementHistoryReport/ClientStatementHistoryDownload.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: k_statement
          in: query
          description: 'Statement primary key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      s_pdf_base64:
                        type: string
                        description: 'Base64-encoded PDF binary content. Decode on the client side and use as a Blob URL for file download.'
                      text_filename:
                        type: string
                        description: 'Suggested file name for the downloaded PDF.'
      tags:
        - ClientStatementHistoryReport-ClientStatementHistoryDownload
  '/Thoth/ReportCore/QueryEngine/Action/User/ResetPoints.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                uid:
                  type: string
                  description: 'Client user key to reset points for. Primary key in the {@link \PassportLoginSql} table.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - User-ResetPoints
  '/Thoth/WlPay/Bank/Ach/Add/Add.json':
    delete:
      summary: 'Deletes saved ACH.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_pay_bank
          in: query
          description: 'Pay bank key to delete. Primary key in {@link \RsPayBankSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Add-Add
    get:
      summary: 'Gets widget for ACH account add.'
      parameters:
        - name: id_locale
          in: query
          description: 'Locale ID. One of {@link LocaleSid} constants. `null` - if it is not set.'
          schema:
            type: [number, "null"]
        - name: is_new
          in: query
          description: 'Determines if the set of configs of the new payment form design is used. `true` - if it is the new payment form design. `false` - if it is the old payment form design.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Primary key in {@link \RsLocationSql} table. If empty, user''s home location will be used.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'Pay owner key. Copy of result of {@link PayOwner::ownerMoney()}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_widget:
                        type: string
                        description: 'The HTML form containing the fields required to add a card. Result of {@link \RsPayWidgetInterface::widget()} method.'
                      id_pay_processor:
                        type: number
                        format: int32
                        description: 'Transaction processor id. One of {@link PayProcessorSid} constants.'
      tags:
        - Add-Add
    post:
      summary: 'Saves new ACH pay method.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Primary key in {@link \RsLocationSql} table. If empty, user''s home location will be used.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'Pay owner key. Copy of result of {@link PayOwner::ownerMoney()}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card_detail:
                  type: array
                  description: 'Array with ACH details from ACH widget. <dl> <dt>array `a_pay_address`</dt> <dd> Payment address for {@link \RsPayAddressSelectWidget} address edit widget: <dl> <dt>bool `is_new`</dt> <dd><tt>true</tt> - add new address; <tt>false</tt> - use existing address.</dd> <dt>string `k_geo_country`</dt> <dd>Country ID.</dd> <dt>string `k_geo_region`</dt> <dd>Region ID.</dd> <dt>string `k_pay_address`</dt> <dd>Chosen payment address ID. It will be set even if user decided to add new address.</dd> <dt>string `s_city`</dt> <dd>City name.</dd> <dt>string `s_name`</dt> <dd>User name.</dd> <dt>string `s_phone`</dt> <dd>Phone number.</dd> <dt>string `s_postal`</dt> <dd>Postal code.</dd> <dt>string `s_street1`</dt> <dd>First address line.</dd> <dt>string `s_street2`</dt> <dd>Second address line.</dd> </dl> </dd> <dt>int `id_pay_actor`</dt> <dd>Pay actor id. One of {@link \RsPayActorSid} constants.</dd> <dt>int `id_pay_bank_ach_holder`</dt> <dd>Account holder type. One of {@link \RsPayBankAchHolderSid} constants.</dd> <dt>int `id_pay_bank_ach_sec`</dt> <dd>SEC code. One of {@link \RsPayBankAchSecSid} constants.</dd> <dt>int `id_pay_bank_ach_type`</dt> <dd>Account type. One of {@link \RsPayBankAchTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd><tt>true</tt> - if a payment method is set as default, <tt>false</tt> - otherwise.</dd> <dt>string `k_pay_bank`</dt> <dd> Key of existing payment source in case of editing. Primary key from {@link \RsPayBankSql} table. Empty if new pay source is being added. </dd> <dt>string `s_aban`</dt> <dd>ABA number.</dd> <dt>string `s_account`</dt> <dd>Account number.</dd> <dt>string `s_name`</dt> <dd>Bank account nickname.</dd> <dt>string `s_number`</dt> <dd>Part of bank account number.</dd> <dt>string `s_owner`</dt> <dd>Name of bank account owner.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_bank:
                        type: array
                        description: 'ACH account information: <dl> <dt>bool `can_remove`</dt> <dd> Whether current user can remove payment method. `true` if user can remove payment methods. `false` if user cannot remove payment methods. </dd> <dt>int `id_pay_bank`</dt> <dd>Type of recurrent payment token. One of {@link \RsPayBankSid} constants.</dd> <dt>int `id_pay_bank_ach_holder`</dt> <dd>Type of account holder. One of {@link \RsPayBankAchHolderSid} constants.</dd> <dt>int `id_pay_bank_ach_type`</dt> <dd>Type of account. One of {@link \RsPayBankAchTypeSid} constants.</dd> <dt>bool `is_default`</dt> <dd>`true` - this account is default payment method; `false` - otherwise.</dd> <dt>string `k_pay_address`</dt> <dd>Billing address. Primary key in {@link \RsPayAddressSql}.</dd> <dt>string `k_pay_bank`</dt> <dd>ID of bank account. Primary key in {@link \RsPayBankSql}.</dd> <dt>string `k_region`</dt> <dd>Region ID. Primary key in {@link \AGeoSql} table.</dd> <dt>string `text_city`</dt> <dd>Name of city.</dd> <dt>string `text_country`</dt> <dd>Name of country.</dd> <dt>string `text_name`</dt> <dd>Account nickname.</dd> <dt>string `text_name_account`</dt> <dd>Account name.</dd> <dt>string `text_name_holder`</dt> <dd>Account holder name.</dd> <dt>string `text_number`</dt> <dd>ACH account number.</dd> <dt>string `text_phone`</dt> <dd>Phone number.</dd> <dt>string `text_postal`</dt> <dd>Postal code.</dd> <dt>string `text_region`</dt> <dd>Name of region.</dd> <dt>string `text_street1`</dt> <dd>Street address line 1.</dd> <dt>string `text_street2`</dt> <dd>Street address line 2.</dd> </dl>'
      tags:
        - Add-Add
  '/Thoth/WlPay/Bank/Ach/Widget/WidgetSelect.json':
    get:
      summary: 'Gets a list of saved bank account.'
      parameters:
        - name: id_pay_bank
          in: query
          description: 'Pay bank id.'
          schema:
            type: number
            format: int32
        - name: id_pay_method
          in: query
          description: 'Payment method.'
          schema:
            type: number
            format: int32
        - name: id_pay_owner
          in: query
          description: 'Payment owner kind.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. `null` if use system business.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'Currency key.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Payment owner. Must be primary key of a business or a user.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. `null` to use system-wide merchant.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_bank:
                        type: array
                        description: 'List of saved bank account. See {@link \RsPayBankAccountSelectWidget::additional_data()} for details.'
      tags:
        - Widget-WidgetSelect
  '/Thoth/WlPay/Bank/Card/Add/Add.json':
    delete:
      summary: 'Deletes saved card. Removes the payment card identified by `$k_pay_bank` from the specified business, permanently deleting the stored card record.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: k_pay_bank
          in: query
          description: 'Pay bank key to delete.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Add-Add
    get:
      summary: 'Gets code of bank card widget. Returns an HTML snippet containing the card entry form rendered by the server-side widget for the given payment owner. This method is deprecated; prefer building the card widget on the client side.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'The payment owner ID. This is different from the user ID. It can be found with the {@link \Thoth\WlPay\Owner\OwnerApi}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      html_widget:
                        type: string
                        description: 'The HTML form containing the fields required to add a card.'
      tags:
        - Add-Add
    post:
      summary: 'Saves new bank card. Validates the payment owner and business, then saves the new card details provided in `$a_card_detail` and optionally sets the card as the default recurring payment source.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key number used internally by WellnessLiving.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'The location key.'
          schema:
            type: string
        - name: k_pay_owner
          in: query
          description: 'The payment owner ID. This is different from the user ID. It can be found with the {@link \Thoth\WlPay\Owner\OwnerApi}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_card_detail:
                  type: array
                  description: 'An array containing payment card information with the following fields: <dl> <dt>array `a_pay_address`</dt> <dd> Address information: <dl> <dt>bool `is_new`</dt> <dd>`true` - add new address. `false` - use existing address.</dd> <dt>string `k_pay_address`</dt> <dd>The chosen payment address ID. THis will be set even if the user decided to add a new address.</dd> <dt>string `k_geo_country`</dt> <dd>The country key.</dd> <dt>string `k_geo_region`</dt> <dd>The region key.</dd> <dt>string `s_city`</dt> <dd>The city name.</dd> <dt>string `s_name`</dt> <dd>The user''s name.</dd> <dt>string `s_street1`</dt> <dd>The first address line.</dd> <dt>string `s_street2`</dt> <dd>The second address line.</dd> <dt>string `s_phone`</dt> <dd>The phone number.</dd> <dt>string `s_postal`</dt> <dd>The postal code.</dd> </dl> </dd> <dt>int `i_csc`</dt> <dd>The payment card security code, also known as the CVC or CVV.</dd> <dt>int `i_month`</dt> <dd>The number of the month when the payment card expires. 1=January and 12=December.</dd> <dt>int `i_year`</dt> <dd>The last two digits of the year when the payment card expires.</dd> <dt>int `id_pay_actor`</dt> <dd>Pay actor id. One of {@link \RsPayActorSid} constants.</dd> <dt>string `k_pay_bank`</dt> <dd> Key of existing payment source in case of editing. <i>Primary key from {@link \RsPayBankSql} table.</i> Empty if new pay source is being added. </dd> <dt>string `s_name`</dt> <dd>The name as it appears on the payment card.</dd> <dt>string `s_number`</dt> <dd>The payment card number with no spaces or dashes.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Add-Add
  '/Thoth/WlPay/Bank/Card/Widget/WidgetSelect.json':
    get:
      summary: 'Gets a list of saved bank cards.'
      parameters:
        - name: id_pay_method
          in: query
          description: 'Payment method. One of {@link \RsPayMethodSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_pay_mode
          in: query
          description: 'Payment mode. One of {@link \RsPayModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: id_pay_owner
          in: query
          description: 'Payment owner kind. Must be {@link \RsPayOwnerSid::BUSINESS} or {@link \RsPayOwnerSid::USER}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. `null` if use system business.'
          schema:
            type: string
        - name: k_currency
          in: query
          description: 'Currency key. Primary key in {@link \RsCurrencySql} table.'
          schema:
            type: string
        - name: k_id
          in: query
          description: 'Payment owner. Must be primary key of a business or a user.'
          schema:
            type: string
        - name: k_location
          in: query
          description: 'Location key. Primary key in {@link \RsLocationSql} table. `null` to use system-wide merchant.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_pay_card:
                        type: array
                        description: 'List of saved bank cards. See {@link \RsPayBankCardSelectWidget::additional_data()} for details.'
      tags:
        - Widget-WidgetSelect
  '/Core/Request/Api/Application/Credential/Credential.json':
    delete:
      summary: 'Deletes the specified credential for the application. Accepts a credential CID identifying the credential type (such as Google or Facebook), resolves the corresponding credential for the current application, and removes it from the database.'
      parameters:
        - name: cid_credential
          in: query
          description: 'The CID of the credential. <i> Accepted values: <ul> <li> {@link GoogleCredential::CID} - The Google credential. Use the {@link GoogleCredentialData} object to send the credential. </li> <li> {@link FacebookCredential::CID} - The Facebook credential. Use the {@link GoogleCredentialData} object to send the credential. </li> </ul> </i>'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Credential-Credential
    get:
      summary: 'Returns the credential data for the application. Accepts a credential CID identifying the credential type, loads the credential stored for the current application, and returns it as a JSON-encoded object containing only public fields.'
      parameters:
        - name: cid_credential
          in: query
          description: 'The CID of the credential. <i> Accepted values: <ul> <li> {@link GoogleCredential::CID} - The Google credential. Use the {@link GoogleCredentialData} object to send the credential. </li> <li> {@link FacebookCredential::CID} - The Facebook credential. Use the {@link GoogleCredentialData} object to send the credential. </li> </ul> </i>'
          schema:
            type: number
            format: int32
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_credential:
                        type: string
                        description: 'An array with the credential.'
      tags:
        - Credential-Credential
    put:
      summary: 'Saves the credential for the application. Accepts a credential CID and a JSON-encoded object with credential fields, validates the data, and stores or replaces the credential for the current application.'
      parameters:
        - name: cid_credential
          in: query
          description: 'The CID of the credential. <i> Accepted values: <ul> <li> {@link GoogleCredential::CID} - The Google credential. Use the {@link GoogleCredentialData} object to send the credential. </li> <li> {@link FacebookCredential::CID} - The Facebook credential. Use the {@link GoogleCredentialData} object to send the credential. </li> </ul> </i>'
          schema:
            type: number
            format: int32
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_credential:
                  type: string
                  description: 'An array with the credential.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Credential-Credential
  '/Core/Passport/Login/Enter/Quick/EnterQuick.json':
    get:
      summary: 'Generates secret key to verify legitimacy of the request.'
      parameters:
        - name: a_data
          in: query
          description: 'Additional data, which can be sent to the listeners of the event "user signed in".'
          schema:
            type: array
        - name: uid
          in: query
          description: 'Key of the user to be signed in. `null` to logout current user.'
          schema:
            type: [string, "null"]
        - name: url_redirect
          in: query
          description: 'Link to redirect user after sign in.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_enter:
                        type: string
                        description: 'Protected link to sign in.'
      tags:
        - Quick-EnterQuick
  '/Wl/Schedule/ScheduleList/StaffApp/Classes/ScheduleListClasses.json':
    get:
      summary: 'Gets a list of classes/events which must be represented on schedule.'
      parameters:
        - name: is_event
          in: query
          description: '<tt>true</tt> - to return events; <tt>false</tt> - to return classes.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_class:
                        type: array
                        description: 'List of classes/events. Keys - class/event IDs; primary key in {@link \RsClassSql} table. Values - class/event titles.'
      tags:
        - Classes-ScheduleListClasses
  '/Wl/Schedule/ScheduleList/StaffApp/Filter/ScheduleListFilter.json':
    get:
      summary: 'Gets list of schedule filters available for given user.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_filter:
                        type: array
                        description: 'List of schedule filters. Keys - filters IDs; primary key in {@link \RsScheduleConfigSql} table. Values - sub arrays with next keys: <dl> <dt>array `a_config`</dt> <dd> Configurations of filter chapters. If configuration of filter chapter is empty, there are no limits from this chapter. <dl> <dt>string[] `a_class`</dt> <dd>Classes. Primary keys in {@link \RsClassSql} table.</dd> <dt>string[] `a_event`</dt> <dd>Events. Primary keys in {@link \RsClassSql} table.</dd> <dt>string[] `a_location`</dt> <dd>Locations. Primary keys in {@link \RsLocationSql} table.</dd> <dt>string[] `a_resource`</dt> <dd>Assets. Primary keys in {@link \RsResourceSql} table.</dd> <dt>string[] `a_service`</dt> <dd>Appointment services. Primary keys in {@link \RsServiceSql} table.</dd> <dt>string[] `a_staff`</dt> <dd>Staff members. Primary keys in {@link \RsStaffSql} table.</dd> <dt>int[] `a_type`</dt> <dd>Service types. Constants of {@link \RsServiceSid} class (excluding {@link \RsServiceSid::VISIT}).</dd> <dt>bool `is_staff_all`</dt> <dd> <tt>true</tt> to show all staff members. <tt>false</tt> to show only staff members from <var>a_staff</var> field. </dd> <dt>bool `is_staff_available`</dt> <dd><tt>true</tt> to show only available staff members; <tt>false</tt> to show any staff member.</dd> </dl> </dd> <dt>bool `can_edit`</dt> <dd><tt>true</tt> - filter may be changed by given user; <tt>false</tt> - otherwise.</dd> <dt>bool `is_default`</dt> <dd><tt>true</tt> - filter is default for given user; <tt>false</tt> - otherwise.</dd> <dt>bool `is_public`</dt> <dd><tt>true</tt> - filter is public; <tt>false</tt> - filter is private.</dd> <dt>string `s_title`</dt> <dd>Filter title.</dd> </dl>'
      tags:
        - Filter-ScheduleListFilter
  '/Wl/Schedule/ScheduleList/StaffApp/Location/ScheduleListLocation.json':
    get:
      summary: 'Gets a list of locations which must be represented on schedule to current user.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_location:
                        type: array
                        description: 'Location IDs. Primary keys in {@link \RsLocationSql} table.'
      tags:
        - Location-ScheduleListLocation
  '/Wl/Schedule/ScheduleList/StaffApp/Resource/ScheduleListResource.json':
    get:
      summary: 'Gets list of assets in business {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListTrait::$k_business}.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource:
                        type: array
                        description: 'List of assets. Keys - asset IDs; primary key in {@link \RsResourceSql} table. Values - sub array with next keys: <dl> <dt>array `a_image`</dt> <dd>Data of asset image. See {@link \RsResourceImage::data()} for details.</dd> <dt>string `text_title`</dt> <dd>Asset title.</dd> </dl>'
      tags:
        - Resource-ScheduleListResource
  '/Wl/Schedule/ScheduleList/StaffApp/Staff/ScheduleListStaff.json':
    get:
      summary: 'Gets information about staff members of business.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff:
                        type: array
                        description: 'IDs of staff members which must be represented of business schedule. Primary keys in {@link \RsStaffSql} table.'
      tags:
        - Staff-ScheduleListStaff
  '/Wl/Schedule/ScheduleList/StaffApp/Service/ScheduleListService.json':
    get:
      summary: 'Gets a list of appointment services which must be represented on schedule.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_service:
                        type: array
                        description: 'List of appointment services. Keys - service IDs; primary key in {@link \RsServiceSql} table. Values - service title.'
      tags:
        - Service-ScheduleListService
  '/Wl/Shop/Product/Inventory/Report/InventoryShopCategory.json':
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'The key of the business to get shop categories for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_shop_category:
                        type: array
                        description: 'An array containing information about store categories. Each array element is an array containing the following fields: <dl> <dt>string `k_shop_category`</dt> <dd>The shop category key. Primary key in {@link \RsShopCategorySql} table.</dd> <dt>string `text_title`</dt> <dd>The category name.</dd> </dl>'
      tags:
        - Report-InventoryShopCategory
  '/Wl/Profile/Attendance/Schedule/Frontend/LifetimeTotals.json':
    get:
      summary: 'Retrieves lifetime visit totals. Returns aggregated lifetime attendance statistics for the specified user in the given business. Used to display summary visit counts and related metrics on the client profile schedule page.'
      parameters:
        - name: k_business
          in: query
          description: 'The business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'The user key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_total:
                        type: array
                        description: 'Report totals. A set of key-value pairs. Each element has followed structure: <dl> <dt>string `text_title`</dt> <dd>Total title.</dd> <dt>string `s_value`</dt> <dd>Total value.</dd> </dl>'
      tags:
        - Frontend-LifetimeTotals
  '/Wl/Appointment/Book/Asset/Service/Service.json':
    get:
      summary: 'Retrieves assets required for a service booking at the given date and time, including availability and busy status. Returns the asset categories and individual assets linked to the service at the given location. Each asset includes its availability flag for the requested time slot. The response also includes {@link ServiceApi::$a_resource_busy} with currently reserved asset slots and a flag indicating whether the current user is allowed to book unavailable assets.'
      parameters:
        - name: dt_start
          in: query
          description: 'The appointment booking date selected by the user.'
          schema:
            type: string
        - name: id_mode
          in: query
          description: 'Mode type, one of {@link ModeSid} constants.'
          schema:
            type: number
            format: int32
        - name: is_backend
          in: query
          description: 'If `true`, back-end mode is selected. In this mode, the list of asset categories must be returned even if asset selection is disabled for clients. If `false` for a case of front-end mode. In this mode, the list of asset categories won''t be returned if asset selection is disabled for clients.'
          schema:
            type: boolean
        - name: is_grid_any
          in: query
          description: 'Whether to show both grid layouts and custom layouts. `true` if custom layout should be included in the result. `false` if only grid layout can be used.'
          schema:
            type: boolean
        - name: is_show_unavailable_assets
          in: query
          description: 'Whether unavailable assets should be included.'
          schema:
            type: boolean
        - name: k_appointment_ignore
          in: query
          description: 'The appointment booking key to ignore when {@link ServiceApi::$a_resource_busy} is derived. `null` if no appointment booking must be ignored.'
          schema:
            type: [string, "null"]
        - name: k_location
          in: query
          description: 'The selected location key.'
          schema:
            type: string
        - name: k_service
          in: query
          description: 'The selected service''s key.'
          schema:
            type: string
        - name: k_timezone
          in: query
          description: 'Timezone of date and time of service start. Empty if client can''t change in which timezone dates should be shown.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User to get information for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_resource_busy:
                        type: object
                        description: 'A list of reserved assets. 1st level keys refer to asset keys. 2nd level keys refer to asset numbers. Values refer to keys of appointment bookings that reserve the asset(s). For example, if you want to check if the 10th asset with the key of ''15'' is reserved, you can check if `a_resource_busy[''15''][''10'']` is free. If you''re rebooking an appointment, check the value of `a_resource_busy[''15''][''10'']`. If it''s equal to the key of your current appointment booking, you can assume the asset is available.'
                      a_resource_type:
                        type: array
                        description: 'A list of assets required for the service booking. Keys refer the asset category key(s). Values refer to sub-arrays with next key(s). <dl> <dt>array[] `a_resource`</dt> <dd> A list of resources. Every element has the following keys: <dl> <dt>array `a_image`</dt> <dd> The asset''s image data. <i>See {@link \RsResourceImage::data()} for details.</i> <dl> <dt>array `a_image`</dt> <dd> Image data. <i>See more {@link \Core\Drive\DriveImage::image()}.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd> Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`. </dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i_angle`</dt> <dd>Angle of shape rotation. Is set only if <var>sid_image</var> equals to <tt>shape</tt>.</dd> <dt>int `i_height`</dt> <dd>Height of image.</dd> <dt>int `i_width`</dt> <dd>Width of image.</dd> <dt>bool `is_empty`</dt> <dd>Whether is empty.</dd> <dt>string `k_resource`</dt> <dd>Resource key.</dd> <dt>string `sid_image`</dt> <dd>Image kind. String representation of one of {@link \Wl\Resource\Image\ImageSid} constants.</dd> <dt>string `sid_image_icon`</dt> <dd> Icon name.String representation of one of {@link \Wl\Resource\Image\ImageIconSid} constants. Is set only if <var>sid_image</var> equals to <tt>image</tt>. </dd> <dt>string `sid_image_shape`</dt> <dd> Shape name. String representation of one of {@link \Wl\Resource\Image\ImageShapeSid} constants. Is set only if <var>sid_image</var> equals to <tt>shape</tt>. </dd> <dt>string `url`</dt> <dd>Path to image. <i>See {@link \Core\Drive\DriveClientAbstract} storage.</i></dd> </dl> </dd> <dt>int `i_quantity`</dt> <dd>Asset quantity.</dd> <dt>bool `is_available`</dt> <dd>Whether this asset has at least one free unit.</dd> <dt>string `k_resource`</dt> <dd>The asset key. <i>Primary key in {@link \RsResourceSql} table.</i></dd> <dt>string `s_resource`</dt> <dd>The asset title.</dd> </dl> </dd> <dt>string|null `k_resource_layout`</dt> <dd> The asset''s layout key. <i>Primary key in {@link \Wl\Resource\Layout\Sql} table.</i> This will be `null` if the asset category has no layout. </dd> <dt>string `s_resource_type`</dt> <dd>The title of asset category.</dd> </dl>'
                      can_book_unavailable_assets:
                        type: boolean
                        description: 'Can the staff members book reserved assets.'
      tags:
        - Service-Service
  '/Wl/Business/Account/Subscription/Collections/CollectionsSubscription.json':
    get:
      summary: 'Gets information does "Collections" subscription plan is active or not.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to check subscription plan.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_active:
                        type: boolean
                        description: 'If the business has active subscription.'
      tags:
        - Collections-CollectionsSubscription
  '/Wl/Business/Account/Subscription/Trial/Trial.json':
    delete:
      summary: 'Removes trial period for specific business and subscription.'
      parameters:
        - name: cid_subscription
          in: query
          description: 'Type of the subscription. See {@link SubscriptionAbstract}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trial-Trial
    put:
      summary: ''
      parameters:
        - name: cid_subscription
          in: query
          description: 'Type of the subscription. See {@link SubscriptionAbstract}.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dl_trial_end:
                        type: string
                        description: 'New trial end date after extension.'
                      i_trial_days:
                        type: number
                        format: int32
                        description: 'Number of days granted during trial extension.'
      tags:
        - Trial-Trial
  '/Wl/Business/Account/Subscription/Achieve/AchieveSubscription.json':
    get:
      summary: 'Returns the Achieve subscription plan flags (free and white-label) for the given business. Used to determine which features and branding options are available in the Achieve mobile app for a business. For franchisees, the franchisor''s subscription is used. Call this before rendering any Achieve-specific UI that depends on the plan tier.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key to check Achieve app subscription plan.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_free:
                        type: boolean
                        description: 'If Achieve app has free subscription plan is `true`, otherwise (white label, professional) - `false`.'
                      is_white_label:
                        type: boolean
                        description: 'If Achieve app has white label subscription plan is `true`, otherwise (free, professional) - `false`.'
      tags:
        - Achieve-AchieveSubscription
  '/Wl/Business/Account/Subscription/AiAgent/AiAgentSubscription.json':
    delete:
      summary: ''
      parameters:
        - name: id_unsubscribe_reason
          in: query
          description: 'Unsubscribe reason ID. One of subscription {@link UnsubscribeReasonSid} or subclass constants. `null` in a case of custom reason. In this case {@link UnsubscribeReasonTrait::$text_unsubscribe_reason} should be specified.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: text_unsubscribe_reason
          in: query
          description: 'Custom unsubscribe reason for {@link UnsubscribeReasonSid::OTHER}. `null` if not set, in this case {@link UnsubscribeReasonTrait::$id_unsubscribe_reason} should be specified.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AiAgent-AiAgentSubscription
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_discount_available:
                        type: number
                        format: int32
                        description: 'Returns available discount months for the business. `0` if discount is not available.'
                      i_trial_available:
                        type: number
                        format: int32
                        description: 'Returns available trial days for the business. `0` if trial is not available.'
                      i_trial_left:
                        type: number
                        format: int32
                        description: 'Returns available trial days for the business. `0` if trial is not available.'
      tags:
        - AiAgent-AiAgentSubscription
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link AiAgentSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - AiAgent-AiAgentSubscription
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_upgrade:
                  type: number
                  format: int32
                  description: 'Type of the upgrade. One of {@link UpgradeSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_discount_extend:
                        type: array
                        description: 'Discount extension option applied to the subscription during upgrade.'
      tags:
        - AiAgent-AiAgentSubscription
  '/Wl/Business/Account/Subscription/Email/EmailSubscription.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Email-EmailSubscription
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link EmailSubscription} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Email-EmailSubscription
  '/Wl/Business/Account/Subscription/Eml/EmlSubscription.json':
    delete:
      summary: ''
      parameters:
        - name: cid_subscription
          in: query
          description: 'CID of one of {@link EmlSubscriptionAbstract} subscriptions.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Eml-EmlSubscription
    put:
      summary: ''
      parameters:
        - name: cid_subscription
          in: query
          description: 'CID of one of {@link EmlSubscriptionAbstract} subscriptions.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link EmlSubscriptionAbstract} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Eml-EmlSubscription
  '/Wl/Business/Account/Subscription/Sms/SmsSubscription.json':
    delete:
      summary: ''
      parameters:
        - name: id_unsubscribe_reason
          in: query
          description: 'Unsubscribe reason ID. One of subscription {@link UnsubscribeReasonSid} or subclass constants. `null` in a case of custom reason. In this case {@link UnsubscribeReasonTrait::$text_unsubscribe_reason} should be specified.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: text_unsubscribe_reason
          in: query
          description: 'Custom unsubscribe reason for {@link UnsubscribeReasonSid::OTHER}. `null` if not set, in this case {@link UnsubscribeReasonTrait::$id_unsubscribe_reason} should be specified.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Sms-SmsSubscription
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_trial_available:
                        type: number
                        format: int32
                        description: 'Returns available trial days for the business. `0` if trial is not available.'
                      i_trial_left:
                        type: number
                        format: int32
                        description: 'Returns available trial days for the business. `0` if trial is not available.'
                      id_locale:
                        type: number
                        format: int32
                        description: 'Locale of the business. One of {@link LocaleSid} constants.'
                      id_plan:
                        type: number
                        format: int32
                        description: 'New subscription plan. One of {@link SmsSubscriptionSid} constants.'
                      url_setup_phone:
                        type: string
                        description: 'URL to the page with phone setup instructions.'
      tags:
        - Sms-SmsSubscription
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link SmsSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_locale:
                        type: number
                        format: int32
                        description: 'Locale of the business. One of {@link LocaleSid} constants.'
                      id_upgrade:
                        type: number
                        format: int32
                        description: 'Type of the upgrade. One of {@link UpgradeSid} constants.'
                      url_setup_phone:
                        type: string
                        description: 'URL to the page with phone setup instructions.'
      tags:
        - Sms-SmsSubscription
  '/Wl/Business/Account/Subscription/MarketingSuite/MarketingSuiteSubscription.json':
    delete:
      summary: ''
      parameters:
        - name: id_unsubscribe_reason
          in: query
          description: 'Unsubscribe reason ID. One of subscription {@link UnsubscribeReasonSid} or subclass constants. `null` in a case of custom reason. In this case {@link UnsubscribeReasonTrait::$text_unsubscribe_reason} should be specified.'
          schema:
            type: [number, "null"]
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: text_unsubscribe_reason
          in: query
          description: 'Custom unsubscribe reason for {@link UnsubscribeReasonSid::OTHER}. `null` if not set, in this case {@link UnsubscribeReasonTrait::$id_unsubscribe_reason} should be specified.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - MarketingSuite-MarketingSuiteSubscription
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_trial_available:
                        type: number
                        format: int32
                        description: 'Returns available trial days for the business. `0` if trial is not available.'
                      i_trial_left:
                        type: number
                        format: int32
                        description: 'Returns available trial days for the business. `0` if trial is not available.'
                      is_active:
                        type: boolean
                        description: 'If the business has active subscription.'
                      is_free:
                        type: boolean
                        description: 'If the subscription is free. Calculated as price monthly equals to zero.'
                      is_trial:
                        type: boolean
                        description: 'If the business has trial period at this moment.'
      tags:
        - MarketingSuite-MarketingSuiteSubscription
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link MarketingSuiteSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      id_upgrade:
                        type: number
                        format: int32
                        description: 'Type of the upgrade. One of {@link UpgradeSid} constants.'
      tags:
        - MarketingSuite-MarketingSuiteSubscription
  '/Wl/Business/Franchise/Push/TaskMonitor/List.json':
    get:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_queue:
                        type: array
                        description: 'The list of tasks.'
      tags:
        - TaskMonitor-List
  '/Wl/Business/Franchise/Report/Curves/Accumulation.json':
    get:
      summary: 'Returns accumulation data.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      f_progress:
                        type: number
                        format: float
                        description: 'Progress value.'
                      is_exists:
                        type: boolean
                        description: '<tt>true</tt> - accumulation reports exist; <tt>false</tt> - otherwise.'
                      url_download:
                        type: string
                        description: 'Link to download archive.'
      tags:
        - Curves-Accumulation
    post:
      summary: 'Generates SABA files and uploads it to S3.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key. Primary key in the {@link \PassportLoginSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Curves-Accumulation
  '/Wl/Business/Location/Merchant/Share/Save.json':
    post:
      summary: 'Saves information about merchant sharing between locations.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Obligatory field. Primary key from {@link \RsBusinessSql} table.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_location_from:
                  type: [array, "null"]
                  description: 'A list of source locations for merchant sharing. <tt>null</tt> to not to change this list.'
                a_location_to:
                  type: [array, "null"]
                  description: 'A list of destination locations for merchant sharing. <tt>null</tt> to not to change this list.'
                is_clear:
                  type: boolean
                  description: 'Delete all sets for the specified business.'
                k_merchant_share_group:
                  type: [string, "null"]
                  description: 'Merchant sharing group ID. Primary key from {@link GroupSql} table. <tt>null</tt> to create new group.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_merchant_share_group:
                        type: [string, "null"]
                        description: 'Merchant sharing group ID. Primary key from {@link GroupSql} table. <tt>null</tt> to create new group.'
      tags:
        - Share-Save
  '/Wl/Business/Sms/Chat/Dialog/DialogList.json':
    get:
      summary: ''
      parameters:
        - name: dtl_last_activity
          in: query
          description: 'The date with time of the last sent SMS. If date is set the API returns dialog list where date last SMS is less than this date. If date isn''t set the API returns the last dialog list.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of business for which need to get list of dialogs.'
          schema:
            type: string
        - name: s_filter
          in: query
          description: 'String with filter criteria.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_dialog_list:
                        type: array
                        description: 'A list of clients'' dialogs. Value has following structure: <dl> <dt>int `i_unread_sms`</dt> <dd>A count of unread messages from the client.</dd> <dt>string `text_client`</dt> <dd>The name of the client.</dd> <dt>string `text_date_last_sms`</dt> <dd>Date when was received the last SMS from the client.</dd> <dt>string `text_last_sms`</dt> <dd>The text of the last SMS from the client.</dd> <dt>string `uid`</dt> <dd>UID of the client. Primary key in {@link \PassportLoginSql} table.</dd> <dt>string `url_profile_logo`</dt> <dd>The link to logo the client.</dd> <dt>string `url_profile`</dt> <dd>The link to profile the client.</dd> </dl>'
      tags:
        - Dialog-DialogList
  '/Wl/Business/Sms/Chat/Dialog/Dialog.json':
    get:
      summary: 'Retrieves information about sms chat with give user in the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business this sms chat is connected to. Primary key from {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user sms chat is related to. Primary key from {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      can_archive:
                        type: boolean
                        description: '`true` if chat can be marked as archive. `false` - otherwise.'
                      can_pin:
                        type: boolean
                        description: '`true` if conversation can be marked as pinned. `false` - otherwise.'
                      can_read:
                        type: boolean
                        description: '`true` if the last message in the chat is from client and chat can be marked read. `false` - otherwise.'
                      can_unread:
                        type: boolean
                        description: '`true` if the last message in the chat is from client and chat can be marked unread. `false` - otherwise.'
                      is_archive:
                        type: [boolean, "null"]
                        description: '`true` if chat should be hidden on the list of chats. `false` - otherwise. `null` - do not change current status.'
                      is_mute:
                        type: [boolean, "null"]
                        description: '`true` if chat should be muted and all new messages should not fire notifications to staff members. `false` - otherwise. `null` - do not change current status.'
      tags:
        - Dialog-Dialog
    post:
      summary: 'Retrieves information about sms chat with give user in the given business.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business this sms chat is connected to. Primary key from {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user sms chat is related to. Primary key from {@link \PassportLoginSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_archive:
                  type: [boolean, "null"]
                  description: '`true` if chat should be hidden on the list of chats. `false` - otherwise. `null` - do not change current status.'
                is_mute:
                  type: [boolean, "null"]
                  description: '`true` if chat should be muted and all new messages should not fire notifications to staff members. `false` - otherwise. `null` - do not change current status.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Dialog-Dialog
  '/Wl/Business/Sms/Chat/Dialog/MessageHistory.json':
    get:
      summary: ''
      parameters:
        - name: i_limit
          in: query
          description: 'A count of SMS which need to return. Default value is 20.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Key of business for which need to get list of dialogs.'
          schema:
            type: string
        - name: k_sms_history_last
          in: query
          description: 'Key of SMS history. If this key is set the API returns SMS histories that were sent before this SMS. If this key isn''t set the API returns the last sent 20 SMS histories.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'UID of the client for which need to get SMS message history.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_message_history_list:
                        type: array
                        description: 'An array with SMS message history: <dl> <dt>string `dtl_message`</dt> <dd>Date and time, when SMS messages were sent, in MySQL format.</dd> <dt>bool `is_automated`</dt> <dd><tt>true</tt> if SMS was sent from task, <tt>false</tt> - if the staff member sent SMS.</dd> <dt>bool `is_outbound`</dt> <dd><tt>true</tt> if staff member sent SMS, <tt>false</tt> - if the client sent the SMS.</dd> <dt>string `k_sms_history`</dt> <dd>The key of SMS history. Primary key in {@link \ASmsHistorySql} table.</dd> <dt>string `text_date`</dt> <dd>The day when SMS messages were sent.</dd> <dt>string `text_sender_name`</dt> <dd>The sender''s name.</dd> <dt>string `text_sms_body`</dt> <dd>The body of the SMS.</dd> <dt>string `text_sms_type`</dt> <dd>The title of the mail type.</dd> <dt>string `text_time`</dt> <dd>The time when SMS was sent.</dd> <dt>string|null `uid_sender`</dt> <dd>The UID of the sender, <tt>null</tt> - if SMS sent from task.</dd> </dl>'
      tags:
        - Dialog-MessageHistory
  '/Wl/Business/Sms/Chat/Staff/SmsChatStaffAssignList.json':
    get:
      summary: 'Gets list of staff available to assigned for sms chat clients. or staff member has not privilege {@link \RsPrivilegeSid::MESSAGES_MANAGE}.'
      parameters:
        - name: id_dialog_status
          in: query
          description: 'Status of the dialog. One of {@link DialogStatusSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. `null` if not specified, will throw an exception.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_assign:
                        type: array
                        description: 'List of staff assign to chat of client. Each element contains the following fields: <dl> <dt>string `text_full_name`</dt> <dd>Staff member first and last name.</dd> <dt>string `uid_staff`</dt> <dd>Staff UID. Primary key in {@link \PassportLoginSql} table.</dd> </dl>'
                      has_unassigned:
                        type: boolean
                        description: 'Are there any chats that are unassigned to staff members.'
                      uid_staff_assigned_self:
                        type: [string, "null"]
                        description: 'The key of the current staff member if there are chats assigned to him, `null` otherwise.'
      tags:
        - Staff-SmsChatStaffAssignList
  '/Wl/Business/Sms/Chat/Staff/SmsChatStaffAssign.json':
    get:
      summary: 'Gets information about staff assigned to for the chat. {@link SmsChatStaffAssignApi::$uid_client} fields. {@link SmsChatStaffAssignApi::$uid_client} fields or staff member has not privilege {@link \RsPrivilegeSid::MESSAGES_MANAGE}.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. `null` if not specified, will throw an exception.'
          schema:
            type: [string, "null"]
        - name: uid_client
          in: query
          description: 'UID of the client with which selected dialog. Primary key in {@link \PassportLoginSql} table. `null` if not specified, will throw an exception.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_staff_available:
                        type: array
                        description: 'Array of staff available for assigned to chat of client. Each element contains the following fields: <ul> <li>string <var>uid_staff</var> Staff UID. Primary key in {@link \PassportLoginSql} table.</li> <li>string <var>text_full_name</var> Staff member first and last name.</li> <li>string <var>url_image</var> Url staff member logo.</li> </ul>'
                      is_available_assign_to:
                        type: boolean
                        description: 'Can a current staff assign a conversation to a staff member. `true` if enable the role to use the Assign to field, `false` otherwise.'
                      text_staff_name_assign:
                        type: string
                        description: 'Staff name assigned to chat of client.'
                      uid_staff_assign:
                        type: [string, "null"]
                        description: 'User staff Key assigned to chat of client. Primary key in {@link \PassportLoginSql} table. `null` if staff unassigned.'
                      uid_staff_available_self:
                        type: [string, "null"]
                        description: 'UID of the current staff member if there are on the list of available for assignment, `false` otherwise.'
                      url_image_staff_assign:
                        type: string
                        description: 'Staff logo assigned to chat of client.'
      tags:
        - Staff-SmsChatStaffAssign
    post:
      summary: 'Save staff assign for the client chat. {@link SmsChatStaffAssignApi::$uid_client} fields. {@link SmsChatStaffAssignApi::$uid_client}, {@link SmsChatStaffAssignApi::$uid_staff_assign} fields or staff member has not {@link \RsPrivilegeSid::MESSAGES_MANAGE}, {@link \RsPrivilegeSid::MESSAGES_CONVERSATIONS} privileges.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in {@link \RsBusinessSql} table. `null` if not specified, will throw an exception.'
          schema:
            type: [string, "null"]
        - name: uid_client
          in: query
          description: 'UID of the client with which selected dialog. Primary key in {@link \PassportLoginSql} table. `null` if not specified, will throw an exception.'
          schema:
            type: [string, "null"]
        - name: uid_staff_assign
          in: query
          description: 'User staff Key assigned to chat of client. Primary key in {@link \PassportLoginSql} table. `null` if staff unassigned.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Staff-SmsChatStaffAssign
  '/Wl/Business/Sms/Chat/Alert/SmsAlert.json':
    post:
      summary: 'Marks alerts for this client''s messages as read, saves alert count. If the alert key was read, it sets the alert to hidden.'
      parameters:
        - name: i_alert_count
          in: query
          description: 'Number of Alerts that are unread for the staff member. {@link AlertManager::getCountNotViewedAlert()}.'
          schema:
            type: number
            format: int32
        - name: is_read
          in: query
          description: 'An alert you want to check the read status of, after marking the chat messages as read'
          schema:
            type: number
            format: int32
        - name: k_alert
          in: query
          description: 'An alert you want to check the read status of, after marking the chat messages as read'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Key of the business this sms chat is connected to. Primary key from {@link \RsBusinessSql}.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'Key of the user sms chat is related to. Primary key from {@link \PassportLoginSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      i_alert_count:
                        type: number
                        format: int32
                        description: 'Number of Alerts that are unread for the staff member. {@link AlertManager::getCountNotViewedAlert()}.'
                      is_read:
                        type: number
                        format: int32
                        description: 'An alert you want to check the read status of, after marking the chat messages as read'
      tags:
        - Alert-SmsAlert
  '/Wl/Business/Sms/Chat/MessageType/MessageTypeOption.json':
    post:
      summary: 'Updates the state of "Message Type" filter in Message Center page.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key within which setting is managed. `null` if business key was not passed.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_selected_message_type:
                  type: array
                  description: 'List of selected message type.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - MessageType-MessageTypeOption
  '/Wl/Business/Sms/TwoWaySms/Index/Upgrade.json':
    put:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_plan:
                  type: number
                  format: int32
                  description: 'New subscription plan. One of {@link SmsSubscriptionSid} constants.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      url_redirect:
                        type: string
                        description: 'Redirect URL.'
      tags:
        - Index-Upgrade
  '/Wl/Login/Attendance/StaffApp/Virtual/Join.json':
    get:
      summary: ''
      parameters:
        - name: dtu_class_period
          in: query
          description: 'Date with time in UTC of class period {@link \Wl\Login\Attendance\StaffApp\Virtual\JoinApi::$k_class_period}. <tt>null</tt> - If the virtual service wait page is open for another type of service. For example: to the appointment.'
          schema:
            type: [string, "null"]
        - name: k_appointment
          in: query
          description: 'Key of the virtual appointment. Primary key in {@link \RsAppointmentSql} table. <tt>null</tt> - If the virtual service wait page is open for another type of service. For example: to the class period.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Key of the business, where session is going on.'
          schema:
            type: [string, "null"]
        - name: k_class_period
          in: query
          description: 'Key of the virtual class period. Primary key in {@link \RsClassPeriodSql} table. <tt>null</tt> - If the virtual service wait page is open for another type of service. For example: to the appointment.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      dtl_service:
                        type: string
                        description: 'Local date with time of start virtual service.'
                      dtu_service:
                        type: string
                        description: 'Date with time in UTC of start virtual service.'
                      is_fitlive:
                        type: string
                        description: '<tt>true</tt> if business use FitLIVE, <tt>false</tt> otherwise.'
                      text_service:
                        type: string
                        description: 'Name of the virtual service.'
                      url_virtual_redirect:
                        type: string
                        description: 'The redirect to zoom meeting.'
      tags:
        - Virtual-Join
  '/Wl/Login/Promotion/GuestPass/Claim/Claim.json':
    post:
      summary: 'Claims the guest pass invitation for the current user and accepts it. Validates the invitation secret, verifies the current user is the intended recipient, and marks the invitation as accepted. current user is not allowed to claim it.'
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_business:
                  type: string
                  description: 'Key of the business within which the invitation was issued.'
                s_secret:
                  type: string
                  description: 'Secret token identifying the guest pass invitation to claim.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Claim-Claim
  '/Wl/Login/Promotion/GuestPass/Invite/Invite.json':
    delete:
      summary: 'Revokes a guest pass invitation. Marks the invitation as rejected and sends an expiry notification to the guest.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret of the invitation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Invite-Invite
    put:
      summary: 'Sends a reminder notification for a guest pass invitation. Resends the invitation notification to the guest using the reminder email or SMS template.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: s_secret
          in: query
          description: 'Secret of the invitation.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Invite-Invite
  '/Wl/Login/Promotion/GuestPass/Invite/InviteList.json':
    get:
      summary: 'Returns a list of guest pass invitations for the given membership or user. Supports filtering by host membership, guest user, host user, date range, and whether expired or revoked invitations are included.'
      parameters:
        - name: dl_end
          in: query
          description: 'End of the guest pass activity period. Used to filter invitations whose activity moment is on or before this local date. `null` to not filter by period end.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Start of the guest pass activity period. Used to filter invitations whose activity moment is on or after this local date. `null` to not filter by period start.'
          schema:
            type: [string, "null"]
        - name: is_include_expire
          in: query
          description: 'Whether to include invitations that have already expired or were revoked.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Key of the business within which guest pass invitations are managed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of the login promotion which provides the guest pass invitation. > * Used for `GET` to get list of initial invitations for the specific login promotion. > * Used for `POST` to send invitation to the specific user for the specific login promotion.'
          schema:
            type: [string, "null"]
        - name: uid_guest
          in: query
          description: 'Key of the invited user. > * Used for `GET` to get list of incoming invitations for the specific user. > * Used for `POST` to send invitation to the specific user.'
          schema:
            type: [string, "null"]
        - name: uid_host
          in: query
          description: 'Key of the host user who sent the invitation. > Used only for `GET` request to get list of outgoing invitations from the specific user.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_list:
                        type: array
                        description: 'List of guest pass invitations suitable for the specific request parameters. <dl> <dt>array `a_guest`</dt> <dd> Guest user identity data. <dl> <dt>string `text_mail`</dt> <dd>Guest email address.</dd> <dt>string `text_name_first`</dt> <dd>Guest first name.</dd> <dt>string `text_name_last`</dt> <dd>Guest last name.</dd> <dt>string|null `text_phone`</dt> <dd>Guest phone number, or `null` if not available.</dd> <dt>string|null `uid`</dt> <dd> Guest user key. <i>Primary key from {@link \PassportLoginSql} table.</i> `null` if the guest did not have an account when the invitation was sent. </dd> </dl> </dd> <dt>string|null `dtl_accept`</dt> <dd> Acceptance date and time in business timezone in MySQL datetime format. `null` if the invitation has not been accepted. </dd> <dt>string|null `dtl_expire`</dt> <dd> Expiry date and time in business timezone in MySQL datetime format. `null` if the invitation has no expiry date. </dd> <dt>string `dtl_invite`</dt> <dd>Date and time the invitation was sent, in business timezone in MySQL datetime format.</dd> <dt>string|null `dtl_redeem`</dt> <dd> Redemption date and time in business timezone in MySQL datetime format. `null` if the invitation has not been redeemed. </dd> <dt>string|null `dtl_reject`</dt> <dd> Rejection date and time in business timezone in MySQL datetime format. `null` if the invitation has not been rejected. </dd> <dt>string|null `dtl_visit`</dt> <dd>Visit date and time in MySQL datetime format. `null` if no visit is associated.</dd> <dt>string|null `dtu_accept`</dt> <dd>Acceptance date and time in UTC in MySQL datetime format. `null` if the invitation has not been accepted.</dd> <dt>string|null `dtu_expire`</dt> <dd>Expiry date and time in UTC in MySQL datetime format. `null` if the invitation has  no expiry date.</dd> <dt>string `dtu_invite`</dt> <dd>Date and time the invitation was sent, in UTC in MySQL datetime format.</dd> <dt>string|null `dtu_redeem`</dt> <dd> Redemption date and time in UTC in MySQL datetime format. `null` if the invitation  has not been redeemed. </dd> <dt>string|null `dtu_reject`</dt> <dd>Rejection date and time in UTC in MySQL datetime format. `null` if the invitation has not been rejected.</dd> <dt>int `id_status`</dt> <dd>Invitation lifecycle status. One of {@link \Wl\Login\Promotion\GuestPass\Invite\InviteStatusEnum} values.</dd> <dt>bool|null `is_checkin`</dt> <dd> `true` if the guest may only enter when the inviting member is checked in. `null` if no guest pass settings are configured for the promotion. </dd> <dt>bool `is_visit_future`</dt> <dd>`true` if associated visit in future, `false` otherwise.</dd> <dt>string|null `k_login_promotion_guest`</dt> <dd> Login promotion key granted to the guest upon acceptance. <i>Primary key from {@link \RsLoginPromotionSql} table.</i> `null` if the invitation has not been accepted. </dd> <dt>string `k_login_promotion_host`</dt> <dd>Login promotion key of the host membership. <i>Primary key from {@link \RsLoginPromotionSql} table.</i></dd> <dt>string `k_promotion`</dt> <dd>Guest promotion key. <i>Primary key from {@link \RsPromotionSql} table.</i></dd> <dt>string|null `k_visit`</dt> <dd> Visit key redeemed with this guest pass. <i>Primary key from {@link \RsVisitSql} table.</i> `null` if no visit is associated. </dd> <dt>string `s_secret`</dt> <dd>Secret token used to accept or reject the invitation via a direct link.</dd> <dt>string|null `text_host_name`</dt> <dd>Full name of the host user who sent the invitation. `null` if the host could not be resolved.</dd> <dt>string `text_promotion_guest`</dt> <dd>Title of the guest promotion.</dd> <dt>string `text_promotion_host`</dt> <dd>Title of the host promotion.</dd> <dt>string|null `text_service_title`</dt> <dd>Title of the service booked with the guest pass. `null` if no visit is associated.</dd> <dt>string `text_timezone`</dt> <dd>Name of the business timezone used for all `dtl_*` fields (e.g. "America/New_York").</dd> <dt>string|null `uid_host`</dt> <dd> User key of the host who sent the invitation. <i>Primary key from {@link \PassportLoginSql} table.</i> `null` if the host could not be resolved. </dd> <dt>string `url_service`</dt> <dd> URL to the attendance page for the associated class period. Empty string if no visit is associated or the visit is not a class. </dd> </dl>'
      tags:
        - Invite-InviteList
    post:
      summary: 'Sends a guest pass invitation to a user. Creates a new invitation for the specified guest pass and sends a notification. The guest can be identified by user key or by contact details when no account exists.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of the business within which guest pass invitations are managed.'
          schema:
            type: string
        - name: k_login_promotion
          in: query
          description: 'Key of the login promotion which provides the guest pass invitation. > * Used for `GET` to get list of initial invitations for the specific login promotion. > * Used for `POST` to send invitation to the specific user for the specific login promotion.'
          schema:
            type: [string, "null"]
        - name: uid_guest
          in: query
          description: 'Key of the invited user. > * Used for `GET` to get list of incoming invitations for the specific user. > * Used for `POST` to send invitation to the specific user.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_guest:
                  type: array
                  description: 'Guest information. > Used only for `POST` request to send invitation to the guest without account. Required fields: <dl> <dt>string `text_mail`</dt> <dd>Email address to which the invitation will be sent.</dd> <dt>string `text_phone`</dt> <dd>Phone number to which the invitation SMS will be sent.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Invite-InviteList
  '/Wl/Login/Promotion/GuestPass/Apply/Apply.json':
    post:
      summary: 'Applies the guest pass of the specified login promotion to the attendee''s visit. If the visit is already paid (with a regular session pass or an existing guest pass), the previous payment is unwound before the new guest pass is applied.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                k_login_promotion:
                  type: string
                  description: 'Login promotion key of the host member''s class membership whose guest pass balance will be used.'
                k_visit:
                  type: string
                  description: 'Visit key of the attendee to whom the guest pass is applied.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Apply-Apply
  '/Wl/Login/Search/SearchData/Region/RegionSearchData.json':
    get:
      summary: 'Returns list of countries and regions.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key. Primary key in the {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_geo:
                        type: array
                        description: 'List of countries and nested regions. <dl> <dt>string[] `a_region`</dt> <dd> List of regions within the country. <dl> <dt>string `k_geo`</dt> <dd>Region key.</dd> <dt>string `text_title`</dt> <dd>Region name.</dd> </dl> </dd> <dt>string `k_geo`</dt> <dd>Country key.</dd> <dt>string `text_title`</dt> <dd>Country name.</dd> </dl>'
      tags:
        - Region-RegionSearchData
  '/Wl/Mail/Pattern/AutomatedMarketing/SendCampaign/SendCampaign.json':
    get:
      summary: 'Gets campaign data.'
      parameters:
        - name: is_check_access
          in: query
          description: 'Whether to run access check only. If `true` - only verify access permissions. By default - `false`.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'Mail pattern live key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      json_campaign_load:
                        type: string
                        description: 'A campaign data to load. For structure details see {@link SendCampaign::getCampaign()} method.'
      tags:
        - SendCampaign-SendCampaign
    post:
      summary: 'Saves ''Send campaign''.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                json_campaign_save:
                  type: string
                  description: 'A campaign data to save. For structure details see {@link SendCampaign::saveCampaign()} method.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_mail_pattern_live:
                        type: string
                        description: 'Mail pattern live key.'
      tags:
        - SendCampaign-SendCampaign
  '/Wl/Mail/Pattern/AutomatedMarketing/SendCampaign/SendCampaignRecipient.json':
    get:
      summary: 'Gets quantity of recipients.'
      parameters:
        - name: id_mail_form
          in: query
          description: 'ID of the mail form. One of {@link \RsMailFormSid} constants.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Primary key of business in {@link \RsBusinessSql} table.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_uid:
                        type: array
                        description: 'UID''s of recipients.'
      tags:
        - SendCampaign-SendCampaignRecipient
  '/Wl/Mail/Pattern/AutomatedMarketing/CustomTemplate/Pattern.json':
    get:
      summary: 'Gets custom pattern data. Loads the saved notification template for the given business and mail type together with the default template, the reply-to business contact data and the list of variables available for substitution. When a list is requested, also loads all custom templates for the mail form.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of the notification. One of {@link \RsMailSid} constants. `0` for empty template.'
          schema:
            type: number
            format: int32
        - name: is_custom_list
          in: query
          description: 'Whether to get a list of custom template data. `true` - to get a list, `false` - otherwise.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern
          in: query
          description: 'Mail pattern key.'
          schema:
            type: string
        - name: sid_mail_form
          in: query
          description: 'SID of the mail form. String representation of one from {@link \RsMailFormSid} class constants.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_business_data:
                        type: array
                        description: 'Business data. <dl> <dt>string|null `text_business_reply_mail`</dt> <dd>Business email address to `Reply TO`. `null` for system business.</dd> <dt>string|null `text_business_reply_name`</dt> <dd>Business name to `Reply TO`. `null` for system business.</dd> </dl>'
                      a_help_data:
                        type: [array, "null"]
                        description: 'Help data for variables. `null` for pushes. <dl> <dt>array[] `a_help_global`</dt> <dd> An array with help data for global variables: <dl> <dt>string `text_description`</dt> <dd>A variable description.</dd> <dt>string `text_field`</dt> <dd>Field name.</dd> <dt>string `text_parameter`</dt> <dd>A variable name.</dd> </dl> </dd> <dt>array[] `a_help`</dt> <dd> An array with help data for template-specific variables: <dl> <dt>string `text_description`</dt> <dd>A variable description.</dd> <dt>string `text_field`</dt> <dd>Field name.</dd> <dt>string `text_parameter`</dt> <dd>A variable name.</dd> </dl> </dd> </dl>'
                      a_pattern_load:
                        type: array
                        description: 'Mail pattern data to load. <dl> <dt>bool `is_mail`</dt> <dd>Is email enabled.</dd> <dt>bool `is_push`</dt> <dd>Is push notification enabled.</dd> <dt>bool `is_sms`</dt> <dd>Is SMS enabled.</dd> <dt>int `id_mail`</dt> <dd>ID of the notification. One of {@link \RsMailSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. <i>Primary key in {@link \RsMailPatternSql} table.</i></dd> <dt>string `text_date_edit`</dt> <dd>Data of the last template save in human-readable format.</dd> <dt>string `text_html`</dt> <dd>HTML content of the email.</dd> <dt>string `text_json`</dt> <dd>JSON content of the email for Unlayer editor.</dd> <dt>string `text_preview`</dt> <dd>>Preview text of the email.</dd> <dt>string `text_push`</dt> <dd>Content of the push notification.</dd> <dt>string `text_sms`</dt> <dd>Content of the SMS.</dd> <dt>string `text_template_description`</dt> <dd>Template description.</dd> <dt>string `text_template_title`</dt> <dd>Template title.</dd> <dt>string `text_text`</dt> <dd>Text content of the email.</dd> <dt>int `tl_last_save`</dt> <dd>Time in seconds of the last template save.</dd> </dl>'
                      a_pattern_load_default:
                        type: array
                        description: 'Default mail pattern data to load. <dl> <dt>bool `is_mail`</dt> <dd>Is email enabled.</dd> <dt>bool `is_push`</dt> <dd>Is push notification enabled.</dd> <dt>bool `is_sms`</dt> <dd>Is SMS enabled.</dd> <dt>int `id_mail`</dt> <dd>ID of the notification. One of {@link \RsMailSid} constants.</dd> <dt>string `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. <i>Primary key in {@link \RsMailPatternSql} table.</i></dd> <dt>string `text_date_edit`</dt> <dd>Data of the last template save in human-readable format.</dd> <dt>string `text_html`</dt> <dd>HTML content of the email.</dd> <dt>string `text_json`</dt> <dd>JSON content of the email for Unlayer editor.</dd> <dt>string `text_preview`</dt> <dd>>Preview text of the email.</dd> <dt>string `text_push`</dt> <dd>Content of the push notification.</dd> <dt>string `text_sms`</dt> <dd>Content of the SMS.</dd> <dt>string `text_template_description`</dt> <dd>Template description.</dd> <dt>string `text_template_title`</dt> <dd>Template title.</dd> <dt>string `text_text`</dt> <dd>Text content of the email.</dd> <dt>int `tl_last_save`</dt> <dd>Time in seconds of the last template save.</dd> </dl>'
                      json_pattern_list_load:
                        type: string
                        description: 'List of mail patterns data to load. Each element has the structure, described in {@link PatternApi::$a_pattern_load}.'
                      text_phone_formatted:
                        type: string
                        description: 'Business sender phone formatted.'
      tags:
        - CustomTemplate-Pattern
    post:
      summary: 'Saves custom pattern. Validates and stores the submitted notification template (email, push or SMS) for the given business. A system template edited under a business is copied into that business instead of modifying the original. The saved mail pattern key is returned in {@link PatternApi::$k_mail_pattern}.'
      parameters:
        - name: id_mail
          in: query
          description: 'ID of the notification. One of {@link \RsMailSid} constants. `0` for empty template.'
          schema:
            type: number
            format: int32
        - name: k_business
          in: query
          description: 'Business key. `null` to use the system business.'
          schema:
            type: [string, "null"]
        - name: k_mail_pattern
          in: query
          description: 'Mail pattern key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_pattern_save:
                  type: array
                  description: 'Mail pattern data to save. <dl> <dt>bool `is_mail`</dt> <dd>Is email enabled.</dd> <dt>bool `is_push`</dt> <dd>Is push notification enabled.</dd> <dt>bool `is_sms`</dt> <dd>Is SMS enabled.</dd> <dt>int `id_mail`</dt> <dd>ID of the notification. One of {@link \RsMailSid} constants.</dd> <dt>string|null `k_business`</dt> <dd>Business key. <i>Primary key in {@link \RsBusinessSql} table.</i></dd> <dt>string `k_mail_pattern`</dt> <dd>Mail pattern key. <i>Primary key in {@link \RsMailPatternSql} table.</i></dd> <dt>string `text_html`</dt> <dd>HTML content of the email.</dd> <dt>string `text_json`</dt> <dd>JSON content of the email for Unlayer editor.</dd> <dt>string `text_preview`</dt> <dd>Preview text of the email.</dd> <dt>string `text_push`</dt> <dd>Content of the push notification.</dd> <dt>string `text_sms`</dt> <dd>Content of the SMS.</dd> <dt>string `text_subject`</dt> <dd>Subject of the email.</dd> <dt>string `text_template_description`</dt> <dd>Template description.</dd> <dt>string `text_template_title`</dt> <dd>Template title.</dd> <dt>string `text_text`</dt> <dd>Text content of the email.</dd> </dl>'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_mail_pattern:
                        type: string
                        description: 'Mail pattern key.'
      tags:
        - CustomTemplate-Pattern
  '/Wl/Mail/Pattern/AutomatedMarketing/CustomTemplate/TemplateAutosave.json':
    get:
      summary: 'Gets value of the option that determines whether template autosave is enabled.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      is_autosave:
                        type: boolean
                        description: 'Determines whether template autosave is enabled. `true` if autosave is enabled, `false` - otherwise.'
      tags:
        - CustomTemplate-TemplateAutosave
    post:
      summary: 'Sets value of the option that determines whether template autosave is enabled.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_autosave:
                  type: boolean
                  description: 'Determines whether template autosave is enabled. `true` if autosave is enabled, `false` - otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CustomTemplate-TemplateAutosave
  '/Wl/Mail/Pattern/AutomatedMarketing/CampaignResend/CampaignResend.json':
    get:
      summary: 'Retrieves particular information about campaign.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'Mail pattern live key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_mail_pattern_live:
                        type: array
                        description: 'The mail pattern live data of the original sent campaign. An array with the following structure: <dl> <dt>string `dtu_send`</dt> <dd>Date of sending the campaign.</dd> <dt>string `text_campaign`</dt> <dd>Body text of the campaign.</dd> <dt>string `text_subject`</dt> <dd>Subject of the campaign.</dd> <dt>string `text_preview`</dt> <dd>Preview of the mail body as rendered in the mail client (may be empty).</dd> </dl>'
      tags:
        - CampaignResend-CampaignResend
    post:
      summary: 'Performs resending of mail campaign.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_mail_pattern_live
          in: query
          description: 'Mail pattern live key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_time:
                  type: array
                  description: 'The time of the campaign resending, in the location''s timezone. An array with the following structure: <dl> <dt>int `i_hour`</dt> <dd>The hour component.</dd> <dt>int `i_minute`</dt> <dd>The minute component.</dd> <dt>bool `is_am`</dt> <dd>If `true` the visit is in the AM. If `false` it is in the PM.</dd> </dl>'
                dl_date:
                  type: string
                  description: 'The scheduled local date, without a time component in MySQL format.'
                is_send_now:
                  type: boolean
                  description: 'Specifies whether the campaign should be sent immediately. If `true`, the campaign will be sent immediately. If `false`, the campaign will be scheduled.'
                text_preview:
                  type: string
                  description: 'Preview of the mail body as rendered in the mail client.'
                text_subject:
                  type: string
                  description: 'Subject of the mail.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - CampaignResend-CampaignResend
  '/Wl/Member/Progress/Log/Edit/Log.json':
    get:
      summary: 'Returns progress log data. Loads field values recorded for the specified user on the given date (defaulting to the business''s current local date), and also returns the last date the user entered progress data and whether the viewer is staff.'
      parameters:
        - name: dt_date
          in: query
          description: 'Progress log local date. If <tt>null</tt> use the current date.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
                      dt_last:
                        type: [string, "null"]
                        description: 'Last local date when the user entered progress values. <tt>null</tt> if the user had not entered progress data before the {@link \Wl\Member\Progress\Log\Edit\LogApi::$dt_date} date.'
                      is_staff:
                        type: boolean
                        description: 'How we want to see this page. As a staff or as a user.'
      tags:
        - Edit-Log
    post:
      summary: 'Saves progress fields log. Validates and persists the field values for the specified user and date, enforcing access rules (clients cannot edit verified entries), and logs the change when data is modified.'
      parameters:
        - name: dt_date
          in: query
          description: 'Progress log local date. If <tt>null</tt> use the current date.'
          schema:
            type: string
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_log:
                  type: array
                  description: 'List of fields. Keys are field IDs ({@link \Wl\Member\Progress\Field\ProgressFieldSid} constants). Values are new field values. Structure of value completely defined by individual fields.'
                is_weight_only:
                  type: boolean
                  description: 'Whether field is only weight should be updated.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
      tags:
        - Edit-Log
  '/Wl/Member/Progress/Log/Image/Image.json':
    delete:
      summary: 'Deletes progress image. Removes the progress picture for the specified user and date from storage and deletes the corresponding progress log record from the database, then logs the change.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date of the progress picture. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Image-Image
    get:
      summary: 'Returns progress picture data. Loads the progress picture for the specified user and date, returning image storage data, thumbnail dimensions, the upload URL, and a direct link to the image.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date of the progress picture. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_image:
                        type: array
                        description: 'Image data: <dl> <dt>array `a_image`</dt> <dd> Image storage data. <i>Structure of this array corresponds result of {@link \Wl\Member\Progress\Log\Image\ProgressImage::load()} method.</i> <dl> <dt>int `i_height`</dt> <dd>Actual height of thumbnail image.</dd> <dt>int `i_height_src`</dt> <dd>Height of original image.</dd> <dt>int `i_rotate`</dt> <dd>Angle on which image was rotated compared to the original.</dd> <dt>int `i_width`</dt> <dd>Actual width of thumbnail image.</dd> <dt>int `i_width_src`</dt> <dd>Width of original image.</dd> <dt>bool `is-resize`</dt> <dd>Whether thumbnail is a resized variant of original image. If `false`, `url-thumbnail` equals `url-view`.</dd> <dt>string `url-view`</dt> <dd>URL to original image in file storage.</dd> <dt>string `url-thumbnail`</dt> <dd> URL to resized and rotated image in file storage. If the original is larger than the specified dimensions, a thumbnail is created and its link is returned. Otherwise, the link to the original image is returned here. </dd> </dl> </dd> <dt>int `i-size`</dt> <dd>Max image size.</dd> <dt>int `i-thumbnail-height`</dt> <dd>Image thumbnail height.</dd> <dt>int `i-thumbnail-width`</dt> <dd>Image thumbnail width.</dd> <dt>string `s_link`</dt> <dd>Image link.</dd> <dt>string `url-action`</dt> <dd>Image upload url.</dd> </dl>'
      tags:
        - Image-Image
    post:
      summary: 'Saves progress picture. Applies the image command from `$s_command` to the progress picture for the specified user and date, persists the result in storage, and writes or updates the corresponding progress log record in the database.'
      parameters:
        - name: dt_date
          in: query
          description: 'Local date of the progress picture. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key. `null` if not initialized yet.'
          schema:
            type: [string, "null"]
        - name: uid
          in: query
          description: 'User key. <tt>null</tt> if not initialized yet.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                s_command:
                  type: [string, "null"]
                  description: 'Save image command. <tt>null</tt> if not initialized yet.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Image-Image
  '/Wl/Member/Progress/Log/Compare/Compare.json':
    get:
      summary: 'Returns list of dates where client logged his progress. Retrieves all distinct dates on which the given user recorded progress entries for the business, excluding the most recent date (since it cannot be compared with itself). Returns an empty array when fewer than two entries exist.'
      parameters:
        - name: k_business
          in: query
          description: 'Key of business to get information for.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key to search dates for.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_date:
                        type: array
                        description: 'List of local dates of the progress log.'
      tags:
        - Compare-Compare
  '/Wl/Member/Progress/Field/Edit/Field.json':
    get:
      summary: 'Returns field data. Loads the configuration of the specified progress field for the given business, including its type, measurement unit, visibility settings, and title, and populates the result properties accordingly.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_measurement:
                        type: array
                        description: 'A list of field measurement. See {@link \Wl\Member\Progress\Field\MeasurementSid}.'
                      a_type:
                        type: array
                        description: 'A list of field type. See {@link \Wl\Member\Progress\Field\TypeSid}.'
                      id_field:
                        type: [number, "null"]
                        description: 'Field ID. One of the {@link ProgressFieldSid} constant.'
                      id_measurement_unit:
                        type: ["null", number]
                        description: 'Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.'
                      id_type:
                        type: number
                        format: int32
                        description: 'Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.'
                      is_active:
                        type: boolean
                        description: 'Whether field is active and should be displayed on page.'
                      is_public:
                        type: boolean
                        description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                      is_require:
                        type: boolean
                        description: 'Whether field value is required for clients.'
                      is_require_staff:
                        type: boolean
                        description: 'Whether field value is required for staffs.'
                      text_title:
                        type: [string, "null"]
                        description: 'Field title. Only titles of the custom progress fields can be changed. <tt>null</tt> If default title should be used.'
      tags:
        - Edit-Field
    post:
      summary: 'Saves field data. Updates the configuration of the specified progress field, validating measurement unit, type, and title constraints before persisting the changes. Logs the change when the field data is modified.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_field
          in: query
          description: 'Field key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                id_measurement_unit:
                  type: ["null", number]
                  description: 'Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.'
                id_type:
                  type: number
                  format: int32
                  description: 'Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.'
                is_active:
                  type: boolean
                  description: 'Whether field is active and should be displayed on page.'
                is_public:
                  type: boolean
                  description: 'Whether this field is public. If this field is set to <tt>false</tt>, this field is not visible to clients.'
                is_require:
                  type: boolean
                  description: 'Whether field value is required for clients.'
                is_require_staff:
                  type: boolean
                  description: 'Whether field value is required for staffs.'
                text_title:
                  type: [string, "null"]
                  description: 'Field title. Only titles of the custom progress fields can be changed. <tt>null</tt> If default title should be used.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      text_title:
                        type: [string, "null"]
                        description: 'Field title. Only titles of the custom progress fields can be changed. <tt>null</tt> If default title should be used.'
      tags:
        - Edit-Field
  '/Wl/Member/Progress/Goal/Edit/Goal.json':
    get:
      summary: 'Returns progress goal log data. Loads the current goal values for all configured progress fields for the specified user and business, and also returns whether the current viewer is a staff member.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
                      is_staff:
                        type: boolean
                        description: 'How we want to see this page. As a staff or as a user.'
      tags:
        - Edit-Goal
    post:
      summary: 'Saves progress goal fields log. Validates and persists the goal values provided in `$a_field_goal` for the specified user and business, then returns the updated field list in `$a_field_list`.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: ["null", string]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_field_goal:
                  type: array
                  description: 'List of fields. Keys are field IDs ({@link \Wl\Member\Progress\Field\ProgressFieldSid} constants). Values are new field values. Structure of value completely defined by individual fields.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_field_list:
                        type: array
                        description: 'Field log data. <dl> <dt>int `id_field`</dt> <dd>Field ID. One of the {@link \Wl\Member\Progress\Field\ProgressFieldSid}.</dd> <dt>int `id_measurement_unit`</dt> <dd>Field measurement unit ID. One of the {@link \Wl\Member\Progress\Field\MeasurementSid} constants.</dd> <dt>int `id_type`</dt> <dd>Field type ID. One of the {@link \Wl\Member\Progress\Field\TypeSid} constants.</dd> <dt>string `k_field`</dt> <dd>Field key. <i>Primary key in the {@link \Wl\Member\Progress\Field\Sql} table.</i></dd> <dt>mixed `x_value`</dt> <dd>Progress field log value.</dd> <dt>string `text_name`</dt> <dd>Translated field name.</dd> </dl>'
      tags:
        - Edit-Goal
  '/Wl/Page/Backend/Header/ImportPopup/ImportPopup.json':
    get:
      summary: 'Gives popup settings.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      show_import_popup:
                        type: array
                        description: 'Should a popup be shown? <tt>true</tt> - show, <tt>false</tt> - do not show.'
                      show_import_popup_full:
                        type: boolean
                        description: 'Should full content be shown? <tt>true</tt> - show, <tt>false</tt> - do not show.'
      tags:
        - ImportPopup-ImportPopup
    put:
      summary: 'Updates the content visibility flag.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key from {@link \RsBusinessSql}.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                is_visible:
                  type: boolean
                  description: 'Content visibility flag. <tt>true</tt> if the popup should be visible or <tt>false</tt> otherwise.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - ImportPopup-ImportPopup
  '/Wl/Skin/Application/Connect/Edit/ConnectEdit.json':
    post:
      summary: ''
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_push:
                  type: array
                  description: 'Data for send push.'
                s_message:
                  type: string
                  description: 'Message push.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-ConnectEdit
  '/Wl/Zapier/ClientGroup/Add/Action/Add.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Add
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: ''
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Action-Add
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Add
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                k_member_group:
                  type: string
                  description: 'Member group key.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: ''
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: ''
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Action-Add
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Add
  '/Wl/Zapier/ClientGroup/Add/Trigger/Add.json':
    delete:
      summary: 'Makes webhook unsubscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trigger-Add
    get:
      summary: 'Returns sample data for trigger. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Trigger-Add
    post:
      summary: 'Makes webhook subscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                k_member_group:
                  type: string
                  description: 'Member group key.'
                s_id:
                  type: string
                  description: 'Identifier of the webhook in Zapier.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: 'Email of the client.'
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the client.'
                url_webhook:
                  type: string
                  description: 'Webhook URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Trigger-Add
  '/Wl/Zapier/ClientGroup/Remove/Action/Remove.json':
    delete:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Remove
    get:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: ''
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Action-Remove
    patch:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Remove
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                k_member_group:
                  type: string
                  description: 'Member group key.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: ''
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the client.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: ''
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Action-Remove
    put:
      summary: ''
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Action-Remove
  '/Wl/Zapier/ClientGroup/Remove/Trigger/Remove.json':
    delete:
      summary: 'Makes webhook unsubscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Trigger-Remove
    get:
      summary: 'Returns sample data for trigger. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Trigger-Remove
    post:
      summary: 'Makes webhook subscribe action. {@inheritdoc}'
      parameters:
        - name: k_business
          in: query
          description: 'Business key for which trigger/action is performed.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_custom_field:
                  type: array
                  description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                k_member_group:
                  type: string
                  description: 'Member group key.'
                s_id:
                  type: string
                  description: 'Identifier of the webhook in Zapier.'
                text_address:
                  type: string
                  description: 'Home address of the client.'
                text_birth:
                  type: string
                  description: 'Date of birth of the client.'
                text_cell_phone:
                  type: string
                  description: 'Mobile phone number.'
                text_client_type:
                  type: string
                  description: 'Client type.'
                text_gender:
                  type: string
                  description: 'Gender name of the client.'
                text_home_phone:
                  type: string
                  description: 'Home phone number.'
                text_lead:
                  type: string
                  description: 'Name of the lead for this user.'
                text_location:
                  type: string
                  description: 'Client home location name.'
                text_mail:
                  type: string
                  description: 'Email of the client.'
                text_name_first:
                  type: string
                  description: 'First name of the client.'
                text_name_last:
                  type: string
                  description: 'Last name of the client.'
                text_referrer:
                  type: string
                  description: 'Name of a referrer of this user.'
                text_timezone:
                  type: string
                  description: 'Client timezone name.'
                text_work_phone:
                  type: string
                  description: 'Work phone number.'
                uid:
                  type: string
                  description: 'UID of the client.'
                url_webhook:
                  type: string
                  description: 'Webhook URL.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_custom_field:
                        type: array
                        description: 'List of custom fields information where key is `k_field` - {@link \RsFieldSql} and value is a string as: `s_value` - for custom text field, `s_title` - for radio and select custom fields, `Checked` or `Unchecked` - for checkbox custom field.'
                      text_address:
                        type: string
                        description: 'Home address of the client.'
                      text_birth:
                        type: string
                        description: 'Date of birth of the client.'
                      text_cell_phone:
                        type: string
                        description: 'Mobile phone number.'
                      text_client_type:
                        type: string
                        description: 'Client type.'
                      text_gender:
                        type: string
                        description: 'Gender name of the client.'
                      text_home_phone:
                        type: string
                        description: 'Home phone number.'
                      text_lead:
                        type: string
                        description: 'Name of the lead for this user.'
                      text_location:
                        type: string
                        description: 'Client home location name.'
                      text_mail:
                        type: string
                        description: 'Email of the client.'
                      text_name_first:
                        type: string
                        description: 'First name of the client.'
                      text_name_last:
                        type: string
                        description: 'Last name of the client.'
                      text_referrer:
                        type: string
                        description: 'Name of a referrer of this user.'
                      text_timezone:
                        type: string
                        description: 'Client timezone name.'
                      text_work_phone:
                        type: string
                        description: 'Work phone number.'
                      uid:
                        type: string
                        description: 'UID of the client.'
      tags:
        - Trigger-Remove
  '/Thoth/Report/SalesReport/Client/AccountReport/Statement/StatementData.json':
    get:
      summary: ''
      parameters:
        - name: dl_end
          in: query
          description: 'Period end date. Written back as result after period resolution so callers can read the effective date.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Period start date. Written back as result after period resolution so callers can read the effective date.'
          schema:
            type: [string, "null"]
        - name: is_include_relationship
          in: query
          description: 'Whether to include account activity from the payer''s relationships. `true` to include the account activity of all members for whom the client is the payer.'
          schema:
            type: boolean
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: k_pay_method
          in: query
          description: 'Account method key. `null` for the default account balance.'
          schema:
            type: [string, "null"]
        - name: uid_client
          in: query
          description: 'Client user key.'
          schema:
            type: [string, "null"]
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_aged_balance:
                        type: array
                        description: 'Aged balance summary buckets as of the statement date. Each bucket is the remaining unpaid amount from charges past due by the indicated range. Payments are applied to oldest charges first (FIFO). A negative amount indicates an unused credit. Structure: <dl> <dt>string `m_current`</dt> <dd>Remaining unpaid amount from charges on the statement date.</dd> <dt>string `m_days_1_30`</dt> <dd>Remaining unpaid amount from charges 1-30 days past due.</dd> <dt>string `m_days_31_60`</dt> <dd>Remaining unpaid amount from charges 31-60 days past due.</dd> <dt>string `m_days_61_90`</dt> <dd>Remaining unpaid amount from charges 61-90 days past due.</dd> <dt>string `m_days_90_plus`</dt> <dd>Remaining unpaid amount from charges more than 90 days past due.</dd> </dl>'
                      a_pay_method:
                        type: array
                        description: 'List of account methods available for this client. Rendered as a drop-down only when more than one account method exists. Each element: <dl> <dt>string `k_pay_method`</dt> <dd>Account method key. `0` for the default account balance.</dd> <dt>string `text_title`</dt> <dd>Display name of the account method.</dd> </dl>'
                      a_row:
                        type: array
                        description: 'Statement table rows, ordered by date ascending. Each element: <dl> <dt>string `dtl_date`</dt> <dd>Date and time in local business timezone (MySQL datetime format). Used for display.</dd> <dt>string|null `m_charge`</dt> <dd>Charge amount (positive). `null` if the transaction is a credit.</dd> <dt>string|null `m_payment`</dt> <dd>Payment amount (positive). `null` if the transaction is a debit.</dd> <dt>string `m_balance`</dt> <dd>Running balance after this transaction, sign-flipped.</dd> <dt>string `text_item`</dt> <dd>Comma-separated item names.</dd> <dt>string `text_payment_method`</dt> <dd>Payment method display name.</dd> <dt>string `text_date`</dt> <dd>Transaction date formatted in business locale (e.g. "Dec 1, 2025").</dd> <dt>string `text_time`</dt> <dd>Transaction time formatted in business locale (e.g. "2:30pm").</dd> <dt>string `text_type`</dt> <dd>Transaction type label (shown in bold, first line of Item column).</dd> <dt>string|null `text_client_name`</dt> <dd> Name of the client who received this transaction. Populated only when {@link \Thoth\Report\SalesReport\Client\AccountReport\Statement\StatementDataApi::$is_include_relationship} is `true`. </dd> </dl>'
                      dl_end:
                        type: [string, "null"]
                        description: 'Period end date. Written back as result after period resolution so callers can read the effective date.'
                      dl_start:
                        type: [string, "null"]
                        description: 'Period start date. Written back as result after period resolution so callers can read the effective date.'
                      dl_statement_date:
                        type: string
                        description: 'Statement date (today in business timezone). Shown in the "Aged balance summary as of ..." heading.'
                      i_statement_next:
                        type: number
                        format: int32
                        description: 'Next sequential statement number for the client within the business. Computed as total existing statements plus one. Shown in preview as "#[uid]-[number]".'
                      is_mail_disabled:
                        type: boolean
                        description: 'Whether the email channel of the New Account Statement client notification ({@link \RsMailSid::LOGIN_STATEMENT_NEW}) is disabled for the business. When `true`, the email confirmation dialog shows a warning above the recipient input. The email is still delivered on Send via the manual override in {@link \Wl\Pay\Account\Statement\Mail::send()}.'
                      is_no_activity:
                        type: boolean
                        description: 'Whether there is no account activity during the statement period. When `true`, only the Previous Balance row is shown in the statement table.'
                      is_payer:
                        type: boolean
                        description: 'Whether the client is a payer (has relationships). When `true`, the "Includes relationships'' account activity" toggle is shown.'
                      k_currency:
                        type: string
                        description: 'Business currency key.'
                      m_balance_due:
                        type: string
                        description: 'Balance due at end of statement period (sign-flipped). Matches the Balance column of the last row in the statement table.'
                      m_previous_balance:
                        type: string
                        description: 'Opening balance before the statement period (sign-flipped). Shown as the "Previous Balance" row.'
                      text_business_address:
                        type: string
                        description: 'Business address text.'
                      text_business_city:
                        type: string
                        description: 'Business city name. Empty string if not saved on the business profile.'
                      text_business_name:
                        type: string
                        description: 'Business name.'
                      text_business_zip_code:
                        type: string
                        description: 'Business postal/zip code. Empty string if not saved on the business profile.'
                      text_client_address:
                        type: string
                        description: 'Client full address text. Empty string if not saved on profile.'
                      text_client_city:
                        type: string
                        description: 'Client city name. Empty string if not saved on profile.'
                      text_client_name:
                        type: string
                        description: 'Client full name.'
                      text_client_zip_code:
                        type: string
                        description: 'Client postal/zip code. Empty string if not saved on profile.'
                      text_mail:
                        type: [string, "null"]
                        description: 'Default email address for statement delivery. Taken from the client profile ({@link UserInfo::$text_mail}). `null` if no email is saved.'
                      text_period:
                        type: string
                        description: 'Statement period label. Format: "Dec 1, 2025 - Dec 31, 2025".'
                      text_statement_date:
                        type: string
                        description: 'Statement date formatted in business locale (e.g. "May 6, 2026").'
                      uid:
                        type: string
                        description: 'Client UID displayed in the statement.'
                      url_logo:
                        type: string
                        description: 'Business logo URL. Empty string when the business has no logo set.'
      tags:
        - Statement-StatementData
  '/Thoth/Report/SalesReport/Client/AccountReport/Statement/StatementGenerate.json':
    post:
      summary: ''
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: [string, "null"]
        - name: uid_client
          in: query
          description: 'Client key.'
          schema:
            type: [string, "null"]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                dl_end:
                  type: string
                  description: 'Period end date in MySQL date format.'
                dl_start:
                  type: string
                  description: 'Period start date in MySQL date format.'
                i_statement_number:
                  type: number
                  format: int32
                  description: 'Sequential ordinal of this statement for the client at this business (1-based). Computed by {@link \Thoth\Report\SalesReport\Client\AccountReport\Statement\StatementDataApi} as `i_statement_next` and passed by the frontend without recomputation on save.'
                is_email:
                  type: boolean
                  description: 'Whether to send a New Account Statement notification after saving. When `true`, sends an email notification to the address specified in {@link StatementGenerateApi::$text_mail}.'
                is_include_relationship:
                  type: boolean
                  description: 'Whether to include relationships account activity in the statement.'
                is_mail_format:
                  type: boolean
                  description: 'Whether the mailing envelope address format was applied.'
                k_pay_method:
                  type: [string, "null"]
                  description: 'Account method key of the selected account balance. `null` for the default account balance.'
                text_mail:
                  type: string
                  description: 'Email address to deliver the statement to. Empty string when {@link StatementGenerateApi::$is_email} is `false`.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      k_statement:
                        type: string
                        description: 'Statement key.'
      tags:
        - Statement-StatementGenerate
  '/Wl/Schedule/ScheduleList/StaffApp/Filter/Edit/ScheduleListFilterEdit.json':
    delete:
      summary: 'Deletes schedule filter.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_schedule_config
          in: query
          description: 'ID of saved filter. Primary key in {@link \RsScheduleConfigSql} table. <tt>0</tt> to create new filter.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-ScheduleListFilterEdit
    post:
      summary: 'Saves new configurations for schedule filter.'
      parameters:
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: k_schedule_config
          in: query
          description: 'ID of saved filter. Primary key in {@link \RsScheduleConfigSql} table. <tt>0</tt> to create new filter.'
          schema:
            type: string
        - name: uid
          in: query
          description: 'User key.'
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                a_config:
                  type: array
                  description: 'Filter configuration: <dl> <dt>string[] `a_class`</dt> <dd>List of classes which must be selected in filter. Primary keys in {@link \RsClassSql} table.</dd> <dt>string[] `a_event`</dt> <dd>List of events which must be selected in filter. Primary keys in {@link \RsClassSql} table.</dd> <dt>string[] `a_location`</dt> <dd>List of locations which must be selected in filter. Primary keys in {@link \RsLocationSql} table.</dd> <dt>string[] `a_resource`</dt> <dd>List of assets which must be selected in filter. Primary keys in {@link \RsResourceSql} table.</dd> <dt>string[] `a_service`</dt> <dd>List of appointment services which must be selected in filter. Primary keys in {@link \RsServiceSql} table.</dd> <dt>string[] `a_staff`</dt> <dd>List of staff members which must be selected in filter. Primary keys in {@link \RsStaffSql} table.</dd> <dt>number[] `a_type`</dt> <dd>List of service types which must be selected in filter. Constants of {@link \RsServiceSid} class.</dd> <dt>bool `is_staff_all`</dt> <dd> <tt>true</tt> to show all staff members. <tt>false</tt> to show only staff members from <var>a_staff</var> field. </dd> <dt>bool `is_staff_available`</dt> <dd><tt>true</tt> to show only available staff members; <tt>false</tt> otherwise.</dd> </dl>'
                is_default:
                  type: boolean
                  description: '<tt>true</tt> - filter must be default; <tt>false</tt> - otherwise.'
                is_public:
                  type: boolean
                  description: '<tt>true</tt> - filter must be public; <tt>false</tt> - otherwise.'
                json_config:
                  type: string
                  description: 'JSON version of {@link \Wl\Schedule\ScheduleList\StaffApp\Filter\Edit\ScheduleListFilterEditApi::$a_config}.'
                text_title:
                  type: string
                  description: 'Filter title.'
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Edit-ScheduleListFilterEdit
  '/Wl/Shop/Product/Option/Inventory/Count/InventoryCount.json':
    get:
      summary: 'Returns information about product options. Works in two modes: review and search. In review mode, the method returns products with changed inventory quantities and notes. In search mode, the method returns one product and its options (the result of searching by product barcode). Returns product option details including the current inventory quantity, last change date, and notes. In barcode search mode, returns matching products; in review mode, returns only options whose inventory or notes have been modified via the pending quantity or note maps.'
      parameters:
        - name: a_note
          in: query
          description: 'The list of notes for product options in the store. Keys refer to product options in the store. Values refer to text notes.'
          schema:
            type: object
        - name: a_quantity
          in: query
          description: 'The list of product option quantities in the store. Keys refer to product options in the store. Values refer to product counts.'
          schema:
            type: object
        - name: k_business
          in: query
          description: 'The current business.'
          schema:
            type: string
        - name: text_barcode
          in: query
          description: 'The product barcode, used for the search.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_product_option:
                        type: array
                        description: 'Information about product options for review or search. Each element contains: <dl> <dt>array `a_image`</dt> <dd> Information about the product option image: <dl> <dt>int `i_height`</dt> <dd>The image height.</dd> <dt>int `i_width`</dt> <dd>The image width.</dd> <dt>string `url`</dt> <dd>The image URL.</dd> </dl> </dd> <dt>string `dl_change`</dt> <dd>The date of the last product inventory change.</dd> <dt>int `i_count`</dt> <dd>The product option quantity.</dd> <dt>string `k_location`</dt> <dd>The location key.</dd> <dt>string `k_shop_product_option`</dt> <dd>The key of the product option in the store.</dd> <dt>string `k_shop_product_option_inventory`</dt> <dd>The key of the product option inventory in the store.</dd> <dt>string `text_category`</dt> <dd>The title of the store category.</dd> <dt>string `text_location`</dt> <dd>The location title.</dd> <dt>string `text_note`</dt> <dd>The product option inventory note.</dd> <dt>string `text_option`</dt> <dd>The product option title.</dd> <dt>string `text_product`</dt> <dd>The product option title.</dd> </dl>'
                      text_user_name:
                        type: string
                        description: 'The current username.'
      tags:
        - Count-InventoryCount
    post:
      summary: 'Saves changes of product inventory quantities and notes. Persists updated inventory counts and per-option notes for the specified product options. Requires backend access with the store management privilege. Creates an inventory transaction record for audit trail purposes.'
      parameters:
        - name: a_note
          in: query
          description: 'The list of notes for product options in the store. Keys refer to product options in the store. Values refer to text notes.'
          schema:
            type: object
        - name: a_quantity
          in: query
          description: 'The list of product option quantities in the store. Keys refer to product options in the store. Values refer to product counts.'
          schema:
            type: object
        - name: k_business
          in: query
          description: 'The current business.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response. Full description is not available yet.'
      tags:
        - Count-InventoryCount
  '/Wl/Contact/Member/History/Report/Filter/NameFilterOption.json':
    get:
      summary: ''
      parameters:
        - name: a_contact_method
          in: query
          description: 'Contact methods selected in filter. Values are one of {@link ContactSid} constants.'
          schema:
            type: array
        - name: a_mail_type
          in: query
          description: 'Mail types selected in filter. Values are one of {@link \RsMailSid} constants.'
          schema:
            type: array
        - name: a_sms_channel
          in: query
          description: 'SMS direction filter values. Values are one of {@link YesNoSid} constants.'
          schema:
            type: array
        - name: dl_end
          in: query
          description: 'End date in local format.'
          schema:
            type: [string, "null"]
        - name: dl_start
          in: query
          description: 'Start date in local format.'
          schema:
            type: [string, "null"]
        - name: k_business
          in: query
          description: 'Business key.'
          schema:
            type: string
        - name: sid_report
          in: query
          description: 'Report kind for which options should be loaded. Available values: `contact`, `mail`, `sms`.'
          schema:
            type: string
      responses:
        200:
          description: 'Response always comes with status 200. Inspect the rest of the response body to determine the result.'
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    description: 'Successful response.'
                    properties:
                      a_option_list:
                        type: array
                        description: 'List of name filter options. Each element has the following structure: <dl> <dt>string `text_title`</dt> <dd>Display title for the option.</dd> <dt>string `text_value`</dt> <dd>Value key for the option.</dd> </dl>'
      tags:
        - Filter-NameFilterOption
tags:
  -
    name: A2p-A2pRegistration
    description: 'A2P10DLC Registration API.'
  -
    name: AI-JiraRequest
    description: 'Handles API requests related to Jira agent interactions.'
  -
    name: AI-JiraResultWork
    description: 'Handles API requests related to Jira result work interactions.'
  -
    name: AI-JiraTaskRequest
    description: 'Jira task request API model.'
  -
    name: AI-ReportQueryAi
    description: 'Generates a SELECT query and optional Totals JSON from a natural-language prompt. Accepts the user''s prompt and the current form state (SQL and Totals), passes them to {@link ReportQueryAiClient} along with the conversation history, and returns AI-generated replacements for `s_sql` and `json_totals`. The caller is responsible for applying the returned values to the report form. This API does not persist anything to the database.'
  -
    name: Access-Access
    description: 'Class for check if user can make class reservation.'
  -
    name: Access-AccessPersonnel
    description: 'Updates personnel access to specific repository rule.'
  -
    name: Account-Account
    description: 'Point to save data of accounts for application distribution.'
  -
    name: Account-BusinessAccount
    description: 'Gets a list of active businesses. To run this endpoint, a user must be authorized with the `rs.business.view` privilege.'
  -
    name: Account-BusinessAccountConfig
    description: 'Allows to modify business payment schedule.'
  -
    name: Account-BusinessAccountConfigClear
    description: 'Clears a business account balance transaction.'
  -
    name: Account-BusinessAccountConfigClearAll
    description: 'Clears all failed transactions from the business''s payment schedule.'
  -
    name: Account-CreateLoginLink
    description: 'Creates a link to the Express Dashboard login page.'
  -
    name: Account-PayMethod
    description: 'Allows to get recurrent default payment method for business.'
  -
    name: AccountReport-AccountHistoryReportInfo
    description: 'Returns information needed to initialize the "Account Balance History" report filter: the "Include relationships'' account activity" toggle, and payer identity fields used to show non-payer empty-state messages on the Balance History and Statement History tabs.'
  -
    name: Ach-List
    description: 'List of user''s ACH accounts.'
  -
    name: Achieve-AchieveSubscription
    description: 'Entry point to get information about business Achieve subscription.'
  -
    name: Action-Action
    description: 'Retrieves a list of available reward actions.'
  -
    name: Action-ActionType
    description: 'Information about reward actions of given type in business.'
  -
    name: Action-ActionWrite
    description: 'Point for modify the reward action.'
  -
    name: Action-Add
    description: 'Action for add client for member group.'
  -
    name: Action-Create
    description: 'Action for create new lead.'
  -
    name: Action-Element
    description: 'Retrieves information about reward action item.'
  -
    name: Action-Remove
    description: 'Action for create new lead.'
  -
    name: Action-UpdateField
    description: 'Action for updating existing fields.'
  -
    name: Activity-Element
    description: 'Returns detailed information about a single client activity.'
  -
    name: Activity-List
    description: 'Returns a list of client activities.'
  -
    name: Activity-Share
    description: 'Sharing user''s activity in social networks.'
  -
    name: Add-Add
    description: 'Adds a payment card to a user’s account. The GET method retrieves an HTML code that contains the fields necessary to provide the information needed to add the payment card. The POST method will actually add the payment card. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Add-MailUseOk
    description: 'Registers an existing user in the specified Enterprise Headquarters account. This endpoint isn''t applicable for users who are already members of the business. After a profile is edited, there can be a delay while the data replicates to system caches. As a result, you may see a ''profile-field-empty'' status code. In most cases, this delay is less than 20 seconds. For Enterprise Cloud in WellnessLiving, clients can only be members in one Enterprise Location. In all other Enterprise Locations, they''ll be treated as travelers in the system.'
  -
    name: Add-ProfilePurchaseOption
    description: 'An endpoint that retrieves information about user''s active purchase options able to be used cross-regionally.'
  -
    name: Address-Address
    description: 'Gets information about a user''s payment addresses.'
  -
    name: Address-Profile
    description: 'Retrieves a client’s physical address from their profile.'
  -
    name: AgeRestriction-AgeRestrictionStatus
    description: 'Age restriction check API.'
  -
    name: Agree-Agree
    description: 'Saves the user''s agreement to the online waiver.'
  -
    name: Agreement-Agreement
    description: 'Business Account Agreement API.'
  -
    name: Agreement-AgreementNotify
    description: 'Agreement notify API for partner program.'
  -
    name: AiAgent-AiAgentSubscription
    description: 'Manages {@link AiAgentSubscription} subscription activation on beyond of the customer.'
  -
    name: AiGeneration-AskIsaacAutomationContent
    description: 'API for building the ISAAC marketing generation request payload from automation data. Prepares the request body that would be sent to the ISAAC API, but returns it to the client instead of making the actual API call. This allows the client-side ISAAC widget to send the request directly.'
  -
    name: Alert-Alert
    description: 'Retrieves information about client alert messages.'
  -
    name: Alert-AlertEdit
    description: 'Allows to create, read, update and delete login notes.'
  -
    name: Alert-AlertList
    description: 'Api to manage alerts for current user.'
  -
    name: Alert-AlertSetting
    description: 'Api to save alert settings for the user.'
  -
    name: Alert-SmsAlert
    description: 'Returns and updates information about sms chat with a client'
  -
    name: Alive-PassportAlive
    description: 'Entry point to show signs of life.'
  -
    name: All-Version
    description: 'Retrieves current application version number.'
  -
    name: AmazonRegion-AmazonRegion
    description: 'An endpoint for retrieve region ID for businesses.'
  -
    name: Announcement-AnnouncementElement
    description: 'Allows to create, edit and delete announcement.'
  -
    name: Announcement-AnnouncementList
    description: 'Retrieves a list of announcements.'
  -
    name: ApcClear-ApcClear
    description: 'Point to clear APC cache.'
  -
    name: ApcClear-ApcClearChannel
    description: 'Allows you to clear the Yac/Apc cache when commands are received via the {@link ApcClearChannel} channel. When processing a GET request: * connecting to the {@link ApcClearChannel} channel; * processes cache cleanup commands, but no more than {@link ApcClearChannelApi::TIMEOUT} seconds. For each php-fpm service, at least one running GET request is always supported. This is achieved by periodically sending requests in the {@link ApcClearChannelApiCron::cronApcClear()} method.'
  -
    name: Api-DriveCopy
    description: 'Copy file between two links.'
  -
    name: Api-DriveCopyInstance
    description: 'Copies all files from one instance to another.'
  -
    name: Api-DriveDelete
    description: 'Deletes a link to a physical file from the database.'
  -
    name: Api-DriveDownload
    description: 'Returns contents of a specified file.'
  -
    name: Api-DriveFile
    description: 'Returns contents of a specified file.'
  -
    name: Api-DriveGenericInfo
    description: 'Returns contents of a specified file.'
  -
    name: Api-DriveRegisterVariants
    description: 'Registers variants.'
  -
    name: Api-DriveSave
    description: 'Saves the uploaded file.'
  -
    name: Api-DriveSave74
    description: 'Saves the uploaded file.'
  -
    name: Api-DriveUrlSign
    description: 'Returns a pre-signed URL to download a file with restricted access rights.'
  -
    name: Api-KeySecret
    description: 'Gets a secret key for request signing. This is required for a CORS request. A request to this point must be signed as an ordinary request from the SDK using transient and persistent cookies. There must be no CORS (don''t send from a browser).'
  -
    name: Api-KeySession
    description: 'Point to get a session key. CORS request only. This point must be done from user''s browser. See {@link \Core\Request\Api\KeySecretApi} also.'
  -
    name: Api-Nx
    description: 'Always throws {@link \Core\Request\Api\DebugException}, for all methods. This class is used when an API request to an unexisting endpoint is performed.'
  -
    name: Application-Application
    description: 'Upgrades business account to application customisation.'
  -
    name: Application-ApplicationElement
    description: 'Allows to create, edit and delete applications.'
  -
    name: Application-ApplicationList
    description: 'Loads list of applications for editor.'
  -
    name: Application-Attachment
    description: 'Api for import attachments for user. **Important!** **This api used by foreign desktop application.**'
  -
    name: Application-AuthorizeWithPassword
    description: 'Allows authorization of the user with email and password. Signs out user right away after authorization because we need session without user during Self Check In. Is used only to check email and password properly.'
  -
    name: Application-Check
    description: 'Check business import status. **Important!** **This api used by foreign desktop application.**'
  -
    name: Application-Group
    description: 'Api to get data of the access groups.'
  -
    name: Application-GroupElement
    description: 'Allows to create, edit and delete group.'
  -
    name: Application-GroupList
    description: 'Loads list of access groups for editor.'
  -
    name: Application-MemberInfo
    description: 'Returns information about user for self check in application.'
  -
    name: Application-Origin
    description: 'Gets, deletes, and adds origins for the application. Origins are links on the sites where the API can be used sign CORS authorizations from a client''s browser. If the site isn''t in the list, you can only use the API for requests between two servers. Requests directly from a client''s browser are restricted. Origins should appear as a full link in the same manner they appear on the site. It''s important to understand that the application can add allowed sites only for itself and can''t add sites for another application. This means that you need to call this endpoint using only the application that you''re using on the sites.'
  -
    name: Application-Photo
    description: 'Api for import photo for user. **Important!** **This api used by foreign desktop application.**'
  -
    name: Application-ReceptionAuthorize
    description: 'Performs authorization from the Check In web application. This is a pretty special thing, because it''s not really authorize user, it''s just selects it for Check In application only and allows only to check in to the location. That is why it can not require password. But also can require - this depends on the location settings. It also requires special secret string, which connects request to the certain Check In web application session.'
  -
    name: Application-ReceptionSchedule
    description: 'Shows the schedule for the check-in process. Returns the HTML code that is normally presented for the check-in process.'
  -
    name: Application-ReceptionSecret
    description: 'Allows to generate secret key, which gives access to the Reception (Self Check-In) API endpoints. The usual flow is the following. Staff with access signs in and turns on Self Check-In application. This application is run on the public available device without full control from the staff. This is why staff should be signed out. To store access, we generate secret key, which is stored in the persistent session and all further requests should be done with this secret key.'
  -
    name: Application-Skin
    description: 'Retrieves customized skin settings for the client view.'
  -
    name: Application-SpaApplication
    description: 'Information of application.'
  -
    name: Apply-Apply
    description: 'Endpoint designed to pay for a visit with guest pass options attached to specific promotion. If visit is paid already then old payment option will be revoked without penalties.'
  -
    name: AppointmentView-AppointmentView
    description: 'Retrieves information about appointment.'
  -
    name: Asset-AssetList
    description: 'Retrieves information about assets in the current asset category.'
  -
    name: Asset-AssetLocation
    description: 'Retrieves information about assets location.'
  -
    name: Asset-Category
    description: 'Retrieves information about asset categories.'
  -
    name: AssetLayoutView-Asset
    description: 'Retrieves data about selected and reserved assets within an asset category to show on a layout.'
  -
    name: AsyncTask-AsyncThreadList
    description: 'Get data about asynchronous task.'
  -
    name: Attach-AttachElement
    description: 'Endpoint for upload and manage attachments.'
  -
    name: Attach-AttachList
    description: 'Manages client attachment list.'
  -
    name: Attach-ProfileAttach
    description: 'Point to conduct attached files.'
  -
    name: Attendance-AttendanceInfo
    description: 'Attendance list information.'
  -
    name: Attendance-AttendanceInfoByToken
    description: 'Displays information for certain sessions. Version of {@link \Wl\Login\Attendance\AttendanceInfoApi} for access validation by security token.'
  -
    name: Attendance-AttendanceList
    description: 'Retrieves information about clients attending a class, appointment, or event session.'
  -
    name: Attendance-AttendanceListByToken
    description: 'An endpoint that displays attendance list data. Version of {@link AttendanceListApi} for access validation by security token.'
  -
    name: Attendance-AttendanceOverlap
    description: 'Retrieves a list of visits that overlap with the specified service/class/resource/time data.'
  -
    name: AuthorizePartner-AuthorizePartner
    description: 'Gives members of a franchisor access to a franchisee. To grant a user access, the "Manage Franchise Location" staff permission is required. Access will be granted for 24 hours.'
  -
    name: AuthorizeSupport-AuthorizeSupportEnter
    description: 'Entry for enter business after confirmation form staff member of franchisee.'
  -
    name: AutomatedMarketing-SendTest
    description: 'Send test email or SMS.'
  -
    name: Automation-Automation
    description: 'Manages automation data.'
  -
    name: Automation-AutomationOverlap
    description: 'API for detecting semantic overlap between automations and system notification mails. Provides a list of automations that use the same trigger and a list of active system mail/push/SMS notification patterns that semantically overlap with the specified automation trigger. This API is used by the UI to warn users about potential duplicate communications when enabling or configuring an automation.'
  -
    name: Automation-AutomationRetroactive
    description: 'API for detecting clients who already meet the trigger condition at the time of automation activation. Returns a number of users who would be retroactively eligible for the automation based on the provided trigger settings, so the UI can warn about them before activation.'
  -
    name: Automation-AutomationStatus
    description: 'Manages automation status data.'
  -
    name: Autoscaling-AutoscalingSize
    description: 'Set the new required number of instances in the cluster of search tables with the {@link SearchProviderSid::POSTGRES} provider.'
  -
    name: Autoscaling-AutoscalingSuspend
    description: 'Set suspension of autoscaling activities of search tables with the {@link SearchProviderSid::POSTGRES} provider.'
  -
    name: Autymate-AutymateActivate
    description: 'Checks or changes the status of Autymate enrollments.'
  -
    name: Autymate-AutymateEnroll
    description: 'Api called to enroll a business with Autymate.'
  -
    name: Autymate-AutymateUrlIframe
    description: 'Api called to construct the iframe URL for Autymate.'
  -
    name: Autymate-Report
    description: 'Retrieves all daily transactions for a business using the All Transactions report endpoint. See {@link \Wl\Report\Generator\QueryApi}. This endpoint is a streamlined interface and adds additional columns to the report for Autymate. Autymate will have one user to access all businesses, this user will require the wl.integration.autymate.report privilege.'
  -
    name: Availability-AvailabilityAvailable
    description: 'Returns list of available appointments for all staff members of the business. Also returns, whether staff member provides classes or not.'
  -
    name: Availability-AvailabilitySchedule
    description: 'Returns list of scheduled appointments, classes and events for all staff members of the business. Is used to get list of staff members with all scheduled for them services for option "Only scheduled" in Staff Availability filter.'
  -
    name: BackAdmin-BackAdmin
    description: 'Allows administrator to switch back to his administrator account when he signed in as a different user.'
  -
    name: Backend-BackendConfig
    description: 'Api to return backend settings for the given user inside given business.'
  -
    name: Backend-ColumnList
    description: 'API point to get column list of the table schedule.'
  -
    name: Backend-Config
    description: 'API to get and update status of the left navigation panels and search field in the staff back office.'
  -
    name: Background-GenerateReport
    description: 'Manages accumulation of the report.'
  -
    name: Backlight-Change
    description: 'Api for change backlight on rapid index.'
  -
    name: BalanceHistoryStatement-PayAccountCharge
    description: 'Returns "Make a payment" charge URLs for each account balance pay method of a client. Used by `Thoth_Report_SalesReport_Client_BalanceHistoryStatement_BalanceHistoryStatementLazy` to open the correct charge page when the staff clicks "Make a payment" on the Balance History and Statements tab.'
  -
    name: BalanceSummary-BalanceSummary
    description: 'An API that retrieves a list of pay account with balance by user. Business timezone is used because: We use Account History report which works with business timezone, so api should use the same timezone to avoid confusion. See {@link \Thoth\Report\SalesReport\Client\AccountReport\AccountHistoryReport::timezone()} for more details.'
  -
    name: Bank-Entity
    description: 'Allows to manipulate banking payment sources (credit cards, ACH and direct entry accounts).'
  -
    name: Bank-Primary
    description: 'Sets a payment method as default.'
  -
    name: Banner-Banner
    description: 'Endpoint to check Marketing Suite banner visibility and handle dismissals.'
  -
    name: Banner-BannerList
    description: 'Manages a list of banners.'
  -
    name: Billing-Billing
    description: 'Retrieves information about billing page display properties.'
  -
    name: Birthday-Birthday
    description: 'API for sending birthday wishes to personnel.'
  -
    name: Blacklist-Blacklist
    description: 'Clears all caches where messages can be cached. This includes compiled versions of templates.'
  -
    name: Blame-Check
    description: 'An API to check the specified visit against the business late cancellation and no show rules.'
  -
    name: Board-Element
    description: 'Retrieves information about reward board item.'
  -
    name: Board-List
    description: 'Retrieves a list of items to show in user profile.'
  -
    name: BoardList-List
    description: 'Retrieves a list with information about reward board.'
  -
    name: Book-BookUser
    description: 'Point to save a list of clients who a class session must be booked for.'
  -
    name: Booking-BatchAvailabilityLookup
    description: 'Checks availability for specific time slot.'
  -
    name: Booking-CheckAvailability
    description: 'Check availability for a requested slot. The partner backend verifies that the requested appointment slot is valid and available. **Cross-datacenter compatibility:** key of the business is returned by {@link CheckAvailabilityApi::businessKey()}. The request is then redirected to region of that business by {@link BookingServerBaseApi::run()}.'
  -
    name: Booking-CreateBooking
    description: 'The client requests to create a booking. The partner backend makes a booking for the requested slot, and returns the slot upon success, or business logic error (e.g. if the slot has become unavailable, or if payment is invalid, etc.) upon failure. **Cross-datacenter compatibility:** key of the business is returned by {@link CreateBookingApi::businessKey()}. The request is then redirected to region of that business by {@link BookingServerBaseApi::run()}.'
  -
    name: Booking-GetBookingStatus
    description: 'This method returns a booking status for a user based on provided booking ID. **Cross-datacenter compatibility:** key of the business is returned by {@link GetBookingStatusApi::businessKey()}. The request is then redirected to region of that business by {@link BookingServerBaseApi::run()}.'
  -
    name: Booking-HealthCheck
    description: 'Health check status API endpoint. Used by Google to check that our services are available. **Cross-datacenter compatibility:** Requests to {@link HealthCheckApi} are routed to a random datacenter. Although this is not entirely correct, we decided to do that to save request time - Google is very demanding to response times.'
  -
    name: Booking-ListBookings
    description: 'This method returns future bookings for a user at any merchant. This method should not return past bookings or cancelled bookings. **Cross-datacenter compatibility:** Thanks to {@link ListBookingsApi::REGION_MERGE}, the request is forwarded to all datacenters by {@link BookingServerBaseApi::run()}. When all results arrive, they are merged into one result by {@link ListBookingsApi::regionMerge()}.'
  -
    name: Booking-UpdateBooking
    description: 'Api endpoint used by Google to update booking. **Cross-datacenter compatibility:** key of the business is returned by {@link UpdateBookingApi::businessKey()}. The request is then redirected to region of that business by {@link BookingServerBaseApi::run()}.'
  -
    name: Bot-Access
    description: 'Allows to grant/revoke access from a bot to a service.'
  -
    name: Bot-Edit
    description: 'Allows to create, edit and delete bots.'
  -
    name: Bot-List
    description: 'Returns a list of bots of IP services.'
  -
    name: Bot-Update
    description: 'Updates network of specified bot.'
  -
    name: Branch-Branch
    description: 'Runs a build for creation a new branch or delete an existing branch for the specified task.'
  -
    name: Branch-BranchBuild
    description: 'Changes branch information during build. This API is designed to use only from builds (`BranchCreate`, `BranchDelete`) and is not expected that this API is called from other places.'
  -
    name: Branch-BranchList
    description: 'Runs a build for creation a new branch or delete an existing branch for the specified task.'
  -
    name: Branch-BranchRepairVerify
    description: 'Verifies that a previously broken SVN working copy is now healthy and unblocks branch switching for it. Called from the PRG Branch page after the developer manually repairs a broken repository. Runs `svn info` on the given path and, if successful, clears the error status so the next branch switch is no longer blocked for that path.'
  -
    name: Branch-BranchSwitch
    description: 'Switching repositories in local directories.'
  -
    name: Branch-BranchSwitchLog
    description: 'Switching repositories in local directories.'
  -
    name: Brivo-Configuration
    description: 'API point provides business Brivo integration state information.'
  -
    name: Brivo-Connect
    description: 'Api for connect Facility Access Brivo.'
  -
    name: Brivo-Upgrade
    description: 'Api for Facility Access Brivo subscription.'
  -
    name: Build-BuildAction
    description: 'Allows to perform action with studio builds.'
  -
    name: Build-BuildRevision
    description: 'Allows to get information about build revisions.'
  -
    name: Business-Business
    description: 'Creates a new business.'
  -
    name: Business-BusinessAccess
    description: 'Returns information about a given staff member''s business.'
  -
    name: Business-Data
    description: 'Displays information for a specified business.'
  -
    name: Business-DateLocal
    description: 'Api to get current business local date in MySQL format.'
  -
    name: Business-EnterForm
    description: 'Return information, which is required for the business-wide enter form.'
  -
    name: Business-List
    description: 'Endpoint designed to retrieve list of businesses available for current user within {@link https://zapier.com} integration.'
  -
    name: CachePhp-Invalidate
    description: 'Invalidates contents of a PHP file.'
  -
    name: Calendar-GoogleCalendarEventWebhook
    description: 'Receives a push notification that informs about changes in the user''s calendar.'
  -
    name: Calendar-MicrosoftCalendarEventWebhook
    description: 'Receives a push notification that informs about changes in the user''s calendar.'
  -
    name: Campaign-Campaign
    description: 'Entry point to manipulate direct mail campaign.'
  -
    name: Campaign-CampaignDuplicate
    description: 'Entry point to duplicate direct mail campaign.'
  -
    name: Campaign-CampaignSend
    description: 'API for force sending of mail campaign.'
  -
    name: CampaignDetail-ClickTracking
    description: 'API endpoint used to operate click tracking data.'
  -
    name: CampaignEdit-CampaignEditSummary
    description: 'API to get summary data about direst mail campaign.'
  -
    name: CampaignEdit-CampaignEditTemplate
    description: 'Endpoint for retrieving an existing postcard template images.'
  -
    name: CampaignLanding-CampaignLanding
    description: 'Api for subscribing for direct mail campaigns.'
  -
    name: CampaignList-SendCampaign
    description: 'Gets data for ''Send campaign''.'
  -
    name: CampaignPerformance-CampaignPerformance
    description: 'API for managing automation folders.'
  -
    name: CampaignResend-CampaignResend
    description: 'API for resending of mail campaign.'
  -
    name: Cancel-Cancel
    description: 'Cancels a services.'
  -
    name: Cancel-CancelCan
    description: 'Returns information about whether the given user can cancel an online booking and what consequences the cancellation would have. This API performs a dry run only — no actual cancellation is performed.'
  -
    name: Cancel-Confirm
    description: 'Data for appointment cancellation confirm.'
  -
    name: Cancel-MailPatternLive
    description: 'Creates live pattern.'
  -
    name: Cancel-NotificationPattern
    description: 'API for receiving basic notification pattern setting.'
  -
    name: Captcha-BusinessCaptcha
    description: 'Manages captcha enable setting for a business.'
  -
    name: Captcha-CaptchaRequire
    description: 'Checks if a CAPTCHA is required.'
  -
    name: Captcha-CaptchaScore
    description: 'Endpoint for validating and overriding the score for {@link CaptchaVersionSid::V3} captcha.'
  -
    name: Captcha-GoogleCaptcha
    description: 'Stores the user token CAPTCHA. Site keys for initialize Google reCAPTCHA: * Version 2 (invisible): * Demo/Staging - {@link GoogleCaptchaKeys::CAPTCHA_V2_SITE_KEY_SANDBOX} * Production - {@link GoogleCaptchaKeys::CAPTCHA_V2_SITE_KEY_PRODUCTION} * Version 3: * Demo/Staging - {@link GoogleCaptchaKeys::CAPTCHA_V3_SITE_KEY_SANDBOX} * Production - {@link GoogleCaptchaKeys::CAPTCHA_V3_SITE_KEY_PRODUCTION}'
  -
    name: Card-List
    description: 'Gets information about any payment cards belonging to a user.'
  -
    name: Cart-Cart
    description: 'Displays information about the cart in the catalog.'
  -
    name: Cart-CheckCurrentStock
    description: 'API to check current stock count.'
  -
    name: Cart-DiscountCode
    description: 'Discount code check API.'
  -
    name: Cart-LimitQuantity
    description: 'Displays information about the quantity limit when a promotion (Purchase Option) is added to the cart.'
  -
    name: Cart-Surcharge
    description: 'API to save surcharge amount to the cart.'
  -
    name: Catalog-ProgramList
    description: 'Retrieves a list of programs'' information.'
  -
    name: CatalogCart-CatalogCart
    description: 'Calculates price data for a sale item. Note that the terms "promotion" and "Purchase Option" refer to the same thing.'
  -
    name: CatalogCartEdit-EditView
    description: 'Products list for online store.'
  -
    name: CatalogCommission-CatalogCommission
    description: 'List of available staff with commission.'
  -
    name: CatalogCoupon-CatalogCoupon
    description: 'Coupon validation API.'
  -
    name: CatalogList-CatalogList
    description: 'Returns a list of all products available from a business’s shop. If a location is specified, only products available at that location are included.'
  -
    name: CatalogList-CatalogProduct
    description: 'Returns a list of products.'
  -
    name: CatalogList-CatalogProductCheck
    description: 'Checking if client selected in a store'
  -
    name: CatalogList-Element
    description: 'Displays information about a certain item in the store. When using this endpoint, note that the terms "promotion" and "Purchase Option" refer to the same thing.'
  -
    name: CatalogList-List
    description: 'Displays information about store items.'
  -
    name: CatalogView-CatalogView
    description: 'Returns information about an item’s price after taxes.'
  -
    name: Category-Category
    description: 'Returns a list of all categories in a business’s store.'
  -
    name: Category-CategoryElement
    description: 'Saves video categories.'
  -
    name: Category-CategoryList
    description: 'Returns a list of video categories.'
  -
    name: Category-RankCategory
    description: 'Retrieves information about belts categories in a business. This information can be used to filter by belts categories in the Belts Report.'
  -
    name: CategoryList-CategoryList
    description: 'Retrieves a list with reward action categories.'
  -
    name: Centred-Centred
    description: 'Api endpoint to manage CENTRED Integration'
  -
    name: Change-Change
    description: 'Point to change an asset occupied by specified visit.'
  -
    name: Change-Duration
    description: 'Appointment duration change API.'
  -
    name: Change-DurationConflict
    description: 'Information of conflicts when change appointment duration.'
  -
    name: ChangePassword-ChangePasswordApply
    description: 'Finishes the password change procedure for a user. This endpoint advances the {@link ChangePasswordBeginApi} endpoint. Set your desired password in the {@link ChangePasswordBeginApi::$url_reset} field, where you use the "reset password" link via email to get the user''s new password for this endpoint. When the user goes to your page, get <tt>text_mail</tt> and <tt>text_code</tt> from the URL, then post this model to set a password for user. Use <tt>text_mail</tt> as the value for {@link ChangePasswordApplyApi::$text_login} and {@link ChangePasswordApplyApi::$text_mail}, and <tt>text_code</tt> as the value for {@link ChangePasswordApplyApi::$text_code}.'
  -
    name: ChangePassword-ChangePasswordBegin
    description: 'Starts the password change procedure for a user. Post this to send the user the "reset password" email.'
  -
    name: Charge-Charge
    description: 'Fills a user account with a specified payment. This endpoint uses a CAPTCHA check. To pass the CAPTCHA, review the CAPTCHA API doumentation. where you''ll find that you must send a CAPTCHA for a specific action. For this endpoint, the action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Churn-BusinessChurn
    description: 'Api to churn business.'
  -
    name: Cid-Info
    description: 'Allows to retrieve information about a CID class. This API endpoint is only available for Studio personnel and bots.'
  -
    name: Cid-Register
    description: 'Allows to register a new CID class. This API endpoint is only available for Studio personnel and bots.'
  -
    name: Cid-Rename
    description: 'Allows to rename a class associated with a CID. This API endpoint is only available for Studio personnel and bots.'
  -
    name: City-City
    description: 'API for city management.'
  -
    name: Claim-BusinessClaim
    description: 'Business trial verification API.'
  -
    name: Claim-Claim
    description: 'API to claim a guest pass invitation on behalf of the currently signed-in user.'
  -
    name: ClassList-BookList
    description: 'An endpoint to get all the classes for a location.'
  -
    name: ClassList-ClassList
    description: 'Retrieves a list of classes and class information for a Class Tab.'
  -
    name: ClassList-ClassList68
    description: 'Retrieves a list of classes and class information for a Class Tab.'
  -
    name: ClassList-List
    description: 'List of classes and events.'
  -
    name: ClassPass-AppointmentAttendance
    description: 'Api endpoint used to check appointment reservation state. ## Dispatched URL: * <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/appointments/{appointment_id}/attendance</tt>'
  -
    name: ClassPass-AppointmentEdit
    description: 'Api endpoint used to make or cancel appointment reservation. ## Dispatched URL: * <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/appointments</tt> * <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/appointments/{appointment_id}</tt>'
  -
    name: ClassPass-AvailabilityList
    description: 'Api endpoint used to retrieve list of services. ## Dispatched URL: <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/availability?</tt> <tt>start_date={start_date}&end_date={end_date}</tt> <tt>&page={page}&page_size={page_size}</tt>'
  -
    name: ClassPass-BookableList
    description: 'Api endpoint used to retrieve list of bookables. Basically this is a working schedule of specific resource. Resource can be whether staff members or assets. ## Dispatched URL: * <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/bookables</tt>.'
  -
    name: ClassPass-BusinessData
    description: 'Returns information about a partners. Dispatched URL: <tt>/cp/v1/partners/{partner_id}</tt>.'
  -
    name: ClassPass-BusinessList
    description: 'Returns a list of all partners. Dispatched URL: <tt>/cp/v1/partners</tt>.'
  -
    name: ClassPass-LocationData
    description: 'Returns information of one location. Dispatched URL: <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}</tt>.'
  -
    name: ClassPass-LocationList
    description: 'Returns a list of all locations of partner. Dispatched URL: <tt>/cp/v1/partners/{partner_id}/venues</tt>.'
  -
    name: ClassPass-LocationSchedule
    description: 'Returns a list of all partners. Dispatched URL: <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/schedules</tt>.'
  -
    name: ClassPass-Nx
    description: 'Always throws {@link \Core\Request\Api\DebugException}, for all methods. This class is used when an API request to an unexisting endpoint is performed.'
  -
    name: ClassPass-ReservationData
    description: 'Provides manipulations of the reservations. ## Dispatched URL: <tt>/cp/v1/reservations/{reservation_id}</tt>.'
  -
    name: ClassPass-ReservationList
    description: 'Create class from data received from Class Pass. Dispatched URL: <tt>/cp/v1/reservations</tt>.'
  -
    name: ClassPass-ScheduleAttendance
    description: 'Returns a list of all partners. Dispatched URL: <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/schedules/{schedule_id}/attendance</tt>.'
  -
    name: ClassPass-ServiceList
    description: 'Api endpoint used to retrieve list of services. ## Dispatched URL: * <tt>/cp/v1/partners/{partner_id}/venues/{venue_id}/services?page={page}&page_size={page_size}</tt>.'
  -
    name: ClassSession-ClassSessionSearch
    description: 'Handles searching of class sessions.'
  -
    name: ClassView-ClassView
    description: 'Gets information about a specified class session. A class listing can be retrieved from the {@link ClassListApi} endpoint.'
  -
    name: ClassView-Element
    description: 'Retrieves information about a class element.'
  -
    name: Classes-ScheduleListClasses
    description: 'Information about classes/events which must be represented on schedule.'
  -
    name: Claude-ClaudePinecone
    description: 'API controller for handling POST requests from Claude integration, processing the requests, and returning appropriate JSON responses based on the requested method. Access control follows a one-key-one-tool model by design: each authorization key grants access to exactly one tool. {@link KNOWLEDGE_CODE} exposes only `search_knowledge_base`; {@link PROGRAM_CODE} exposes only `search_thoth_sources`. This ensures that Thoth source code cannot be accessed by holders of the knowledge base key, even within the same endpoint. If access to both tools is required in a single Claude session, two separate MCP connectors must be configured — one per key. This is intentional and not a limitation of the implementation.'
  -
    name: ClearPublic-ClearPublic
    description: 'Entry point to clear public html directory from PRG.'
  -
    name: Client-AppointmentEditClient
    description: 'Updates an appointment for a walk-in client with newly created or existing client'
  -
    name: ClientGroup-List
    description: 'Endpoint to retrieve list of member groups.'
  -
    name: ClientStatementHistoryReport-ClientStatementHistoryDownload
    description: 'Returns the PDF bytes of a previously generated account statement for download. The PDF is rendered by {@link StatementPdf} from the stored statement record in {@link \Thoth\Report\SalesReport\Client\AccountReport\Statement\ClientStatementSql}.'
  -
    name: ClientStatementHistoryReport-ClientStatementHistoryEmail
    description: 'Resends the New Account Statement email for a previously saved account statement. Sends the notification regardless of whether the client''s email notification setting is active or whether the email toggle is off in Account Management preferences.'
  -
    name: Cluster-Cluster
    description: 'Manage `Studio Deploy Cluster`.'
  -
    name: Cms-TranslateState
    description: 'API to toggle the translation state for debugging purposes.'
  -
    name: Code-Code
    description: 'Methods to accept redemption code or number of gift card.'
  -
    name: Code-DiscountCode
    description: 'Discount codes API point.'
  -
    name: Collection-CollectionActivate
    description: 'Checks or changes the status of collections.'
  -
    name: Collections-CollectionsSubscription
    description: 'Entry point to get information about business "Collections" subscription.'
  -
    name: Collector-DebtActivate
    description: 'API endpoint designed to activate skipped debt for a user.'
  -
    name: Collector-DebtList
    description: 'Returns list of debts added within the previous month.'
  -
    name: Collector-DebtPay
    description: 'Registers a debt payment that was performed beyond Wellnessliving, with a specified credit amount applied to a client''s account balance.'
  -
    name: Collector-DebtSkip
    description: 'API endpoint designed to skip user from being debtor.'
  -
    name: Collector-DebtTransaction
    description: 'Returns a list of payments for debts added within the previous day.'
  -
    name: Color-Color
    description: 'Api for manage colors for task.'
  -
    name: Combobox-ToolCombobox
    description: 'Combobox management.'
  -
    name: Combobox-ToolComboboxService
    description: 'Service functions for combobox management.'
  -
    name: Compare-Compare
    description: 'Member progress log compare data.'
  -
    name: Complete-Complete
    description: 'Returns information about purchase.'
  -
    name: Component-ComponentElement
    description: 'Allows to create, edit and delete project''s component of the studio.'
  -
    name: Component-ComponentList
    description: 'Loads list of project''s components for editor.'
  -
    name: Config-BusinessConfig
    description: 'Manages business configurations for clients, bookings, payments, and related things.'
  -
    name: Config-ConfigDefault
    description: 'Manage actions of sms history report.'
  -
    name: Config-Field
    description: 'Endpoint for operations with field.'
  -
    name: Config-RewardConfig
    description: 'Retrieves information about reward config of the business.'
  -
    name: Conflict-Conflict
    description: 'Appointment booking conflicts data.'
  -
    name: Conflict-Conflict56
    description: 'Appointment booking conflicts data for multiple users.'
  -
    name: Connect-ApplicationConnect
    description: 'Creates or updates third-party integration credentials for a white-label application. Used by the App Build Automation (n8n) to programmatically submit the Client App Connect form. Automatically finds the existing record for the given business or creates a new one if none exists.'
  -
    name: Contest-PartnerContest
    description: 'Update contests'' fields.'
  -
    name: Contract-Contract
    description: 'Gets information about a session pass or membership with a contract. The POST method will complete a sale of a Purchase Option requiring a contract. The method that WellnessLiving uses to encode a signature into a string isn''t currently available in the SDK.'
  -
    name: Contract-ContractItem
    description: 'Contract tied to purchase option selected in class/event booking.'
  -
    name: Contract-ContractList
    description: 'List of contracts tied to selected purchase options in class/event booking.'
  -
    name: ContractAll-ContractAll
    description: 'Entry point to get list of subscribed or required contracts of user.'
  -
    name: ControlPanel-TravelStatus
    description: 'API for retrieving the travel status of a business. Includes travel quick check for business and status. If business travel was initiated, information about travel process and steps is included.'
  -
    name: Convert-Convert
    description: 'Receives convertable Purchase Option data and promotion conversions. The DELETE method will remove the conversion and reset the date. The GET method will return a list of promotions available at the business. The POST method can be used to change what the Purchase Option should convert to and when to convert it.'
  -
    name: Cookie-CookieGeneral
    description: 'API to receive user cookies from Mindbody.'
  -
    name: Cordova-CordovaCalendarClient
    description: 'Entry point for synchronization of client''s calendar.'
  -
    name: Cordova-CordovaCalendarStaff
    description: 'Entry point for synchronization of staff member''s calendar.'
  -
    name: Count-InventoryCount
    description: 'Used to retrieve and change inventory quantities and notes for product options in the store.'
  -
    name: Coupon-AmountCoupon
    description: 'API for buying amount coupon gifts.'
  -
    name: Coupon-Coupon
    description: 'Retrieves information about a gift card (also referred to as the coupon code).'
  -
    name: CouponList-List
    description: 'Retrieves a list of gift cards.'
  -
    name: CreateFlow-TriggerList
    description: 'Manages automation data.'
  -
    name: Credential-Credential
    description: 'Manages credentials for the application.'
  -
    name: Criteria-SearchCriteriaList
    description: 'Search entity API.'
  -
    name: Currency-BusinessCurrency
    description: 'Retrieves currencies of all franchisees'' businesses in case of franchise or currency of a business.'
  -
    name: Currency-Currency
    description: 'Retrieves information about all currencies in the system.'
  -
    name: Current-Current
    description: 'Retrieves information about current score.'
  -
    name: Curves-Accumulation
    description: 'Api for Curves data files.'
  -
    name: Curves-CurvesFranchiseLocation
    description: 'Gets information for enterprise locations.'
  -
    name: Curves-Partner
    description: 'Api for get data of the partners.'
  -
    name: CustomTemplate-Pattern
    description: 'Gets and saves custom template pattern data.'
  -
    name: CustomTemplate-TemplateAutosave
    description: 'API for determining whether template autosave is enabled.'
  -
    name: Customization-CustomizationForm
    description: 'Point to load data for a customization form.'
  -
    name: Customize-BusinessReportCustomize
    description: 'Processes requests of the report customization.'
  -
    name: CyberSource-CsCaptureContext
    description: 'Generate a capture context data and key ID to encrypt the card number in the follow-on Tokenize Card request.'
  -
    name: CyberSource-CsPaEnrollment
    description: 'Allows to Check Payer Authentication Enrollment request.'
  -
    name: CyberSource-CsPaSetup
    description: 'Allows to perform Payer Authentication Setup request.'
  -
    name: CyberSource-CsPaValidate
    description: 'This call retrieves and validates the authentication results from issuer and allows the merchant to proceed with processing the payment'
  -
    name: Dashboard-Dashboard
    description: 'Class for drawing chart earnings.'
  -
    name: Dashboard-ReportDashboard
    description: 'API point to manipulate of the adding reports to a dashboard. <b>Note</b>. Old context and context from {@link ReportSaveApi} is used here because: - reason to use ''rs.report.save'' context: messages should be the same in both implementations (old and new); - reason to use context from {@link ReportSaveApi}: saving and adding to a dashboard in the old implementation uses the same module.'
  -
    name: DebtStatus-DebtStatus
    description: 'Api endpoint designed to retrieve debt status of particular user.'
  -
    name: Debug-Error
    description: 'Saves error to {@link DebugJsLog}.'
  -
    name: DebugToken-DebugToken
    description: 'Manage temporary token for login to application.'
  -
    name: DebugToken-DebugTokenService
    description: 'Allows checking the validity of a debug token.'
  -
    name: Delete-Delete
    description: 'Api for delete account in business. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link DeleteCaptcha::CID}.'
  -
    name: Design-BusinessDesign
    description: 'Provides business design information.'
  -
    name: Design-Customize
    description: 'Manages design of attendance list.'
  -
    name: Design-ReceptionDesign
    description: 'Api endpoint to get information about Self Check-In Kiosk settings.'
  -
    name: Design-ReceptionRosterDesign
    description: 'Api endpoint to get information about Attendance Kiosk settings.'
  -
    name: Detail-Detail
    description: 'Api for gets information about user.'
  -
    name: Dialog-Dialog
    description: 'Returns and updates information about sms chat with a client'
  -
    name: Dialog-DialogList
    description: 'Api for work with sms chat.'
  -
    name: Dialog-MessageHistory
    description: 'API for get SMS message history with the specific client in 2-way SMS chat.'
  -
    name: Dictionary-DictionaryElement
    description: 'API to work with one word, which can mark that product is digital.'
  -
    name: Dictionary-DictionaryList
    description: 'API to work with list of words, which can mark that product is digital.'
  -
    name: DirectConnect-DirectConnectHardwareElement
    description: 'Endpoint for operations with paragon hardware.'
  -
    name: DirectConnect-DirectConnectHardwareList
    description: 'Endpoint for retrieve list of paragon hardware.'
  -
    name: Disturb-Disturb
    description: 'Class for manage "Do Not Disturb" option.'
  -
    name: Domain-DomainRefresh
    description: 'API for refreshing the status of a business email domain. This API allows users to manually refresh the verification status of their business email domain. It checks if the user has access to the business and then initiates a task to verify the domain status.'
  -
    name: Domain-DomainRemove
    description: 'API to remove a domain from the business settings.'
  -
    name: Domain-DomainVerify
    description: 'API for email domain verification. The domain must be verified before sending emails from it.'
  -
    name: Domain-MailDomainStatus
    description: 'API that returns the domain verification status for a business.'
  -
    name: DragonFly-Access
    description: 'Checks if a user can access a physical location.'
  -
    name: Drive-ProductImageCopyTemporary
    description: 'Overwrites core functionality of the {@link ImageCopyTemporaryApi} based on the product specification.'
  -
    name: Drive-ProductImageUpload
    description: 'Overwrites core functionality of the {@link ImageUploadApi} based on the product specification.'
  -
    name: Dynamic-CollectionManage
    description: 'API that manages dynamic collection.'
  -
    name: Dynamic-CollectionReportManage
    description: 'API to manage separate reports in a collection.'
  -
    name: Dynamic-ReportList
    description: 'Handles list of reports available to be put into a dynamic collection.'
  -
    name: DynamicId-DynamicId
    description: 'API to get member dynamic ID information.'
  -
    name: Ecommerce-OrderSync
    description: 'Endpoint that allows external ecommerce or POS systems to sync orders into WellnessLiving. This API is not public. If you need to use it, please contact WellnessLiving support. If you need a public payment API, please use {@link \Wl\Catalog\Payment\PaymentApi}.'
  -
    name: Edit-ActiveServices
    description: 'An endpoint for getting information about staff''s scheduled services.'
  -
    name: Edit-AddonUpdate
    description: 'Updates add-ons for an appointment. This endpoint can be used to update add-ons for existing appointments.'
  -
    name: Edit-ConnectEdit
    description: 'Class for send test push.'
  -
    name: Edit-CouponRemove
    description: 'Removes Gift Card from the business.'
  -
    name: Edit-Create
    description: 'Creates user account for the given email address if email address does not already exist. checks if a user''s email address is already used or changes a user’s login email address.'
  -
    name: Edit-DiscountCodeEdit
    description: 'API point for read, create and edit existing discount code.'
  -
    name: Edit-Edit
    description: 'Point to add or change a client group.'
  -
    name: Edit-EditByToken
    description: 'An endpoint that displays client profile information. Version of {@link \Wl\Profile\Edit\EditApi} for access validation by security token.'
  -
    name: Edit-EditConversionType
    description: 'Point to add or change a client group.'
  -
    name: Edit-EditPassword
    description: 'Model to change user''s password.'
  -
    name: Edit-Field
    description: 'Manages progress field data.'
  -
    name: Edit-Goal
    description: 'Progress goal api.'
  -
    name: Edit-Log
    description: 'Progress log api.'
  -
    name: Edit-MailPatternDuplicate
    description: 'API to duplicate mail pattern.'
  -
    name: Edit-MailPatternEdit
    description: 'API to check for clients who will be affected after the change of mail pattern.'
  -
    name: Edit-MailPatternState
    description: 'API for managing the state of a mail pattern (active or inactive).'
  -
    name: Edit-NotificationCustomization
    description: 'API for custom appointment confirmation/reminder notification customization.'
  -
    name: Edit-Prolong
    description: 'Prolongs studio task development cycle.'
  -
    name: Edit-PromotionEditAffect
    description: 'API endpoint to edit promotion for affected clients.'
  -
    name: Edit-RetrieveJira
    description: 'Retrieves information about jira task.'
  -
    name: Edit-ScheduleListFilterEdit
    description: 'Entry point to create/change saved schedule filters.'
  -
    name: Edit-TransferEdit
    description: 'Api to edit franchise transfer.'
  -
    name: Editor-Edit
    description: 'Allows to create, edit and delete microservices.'
  -
    name: Editor-List
    description: 'Returns a list of microservices.'
  -
    name: Editor-SqlKeyword
    description: 'Class for generating keywords for sql editor on programmers page.'
  -
    name: Element-QuizXMLParse
    description: 'An endpoint that manages a single quiz.'
  -
    name: Email-EditEmail
    description: 'Checks if a user''s email address is already used or changes a user’s login email address. This endpoint is rate limited. If the rate limit is exceeded, it won''t return user information. While `get` request allows to check, whether user with the given email exists on the site, `post` allows to add it to the certain business, if it''s not added yet. If client has all required by this business fields filled, it will be added right away. Otherwise it will add temporary access to this client, and you need to send all necessary fields with {@link EditApi} endpoint.'
  -
    name: Email-EditEmail67
    description: 'Checks if a user''s email address is already used or changes a user’s login email address. This endpoint is rate limited. If the rate limit is exceeded, it won''t return user information. While `get` request allows to check whether a user with the given email exists on the site, `post` allows adding it to the certain business, if it''s not added yet. If client has all required by this business fields filled, it will be added right away. Otherwise, it will add temporary access to this client, and you need to send all necessary fields with {@link EditApi} endpoint.'
  -
    name: Email-EmailExist
    description: 'Checks if the email address exists in the system.'
  -
    name: Email-EmailForm
    description: 'API for handling emails from the help popup.'
  -
    name: Email-EmailSubscription
    description: 'Manages {@link EmailSubscription} subscription activation on beyond of the customer.'
  -
    name: Eml-EmlEdit
    description: 'API to edit EML (email marketing) settings.'
  -
    name: Eml-EmlSubscription
    description: 'Manages {@link EmlSubscriptionAbstract} subscription activation on beyond of the customer.'
  -
    name: Enrollment-EnrollmentPanel
    description: '"Wellness Programs" enrollment requirements API.'
  -
    name: Enrollment-TuitionEnrollmentList
    description: 'Model to get list of tuition, which gives access to a certain event from the microservice.'
  -
    name: Enter-Enter
    description: 'Signs a user in.'
  -
    name: Enter-Notepad
    description: 'An endpoint that retrieves the notepad value from the server that is used to sign someone in to the SDK.'
  -
    name: Enter-PassportOtp
    description: 'Entry point to verify user authorization by email and phone number via sending and checking otp code.'
  -
    name: Environment-EnvironmentElement
    description: 'Allows to create, edit and delete studio environments.'
  -
    name: Environment-EnvironmentList
    description: 'Loads list of studio environment for editor.'
  -
    name: EstimateEffort-Effort
    description: 'API for loading effort estimation data from Jira tasks.'
  -
    name: Event-EventCancel
    description: 'Entry point for cancellation of session book of whole event.'
  -
    name: Event-EventCancelWhole
    description: 'Cancels a whole event booked by a client.'
  -
    name: Event-EventList
    description: 'Retrieves a list of events that fit the given filter parameters.'
  -
    name: Event-EventReorganize
    description: 'Reorganize periods of the event between event instances.'
  -
    name: EventList-List
    description: 'Retrieves a list of events for a given class tab.'
  -
    name: EventView-Element
    description: 'Retrieves information about an event element.'
  -
    name: Example-ClassPageListGet
    description: 'Example get TV page classes list for test dynamically changing the list of pages of the view class.'
  -
    name: Example-PageChart
    description: 'Get example chart data.'
  -
    name: Example-PageTable
    description: 'Get example table data.'
  -
    name: Example-PageText
    description: 'Get example text data.'
  -
    name: Expense-ExpenseSchedule
    description: 'Endpoint designed to manage scheduled expenses.'
  -
    name: Expertise-ExpertiseElement
    description: 'Allows to create, edit and delete personnel expertise by a component.'
  -
    name: Expertise-ExpertiseList
    description: 'Loads list of expertise for personnel by projects components.'
  -
    name: Explorer-PromoteExplorer
    description: 'Controller to access business explorer links.'
  -
    name: Export-Accumulation
    description: 'Api for Curves data files.'
  -
    name: Facility-FacilitySid
    description: 'API to get list of additional information about IDs from {@link \RsFacilitySid} class. Property {@link FacilitySidApi::$s_class_name} can be left empty in this class. It will use predefined class name.'
  -
    name: Favorite-ReportFavorite
    description: 'API point to manages favorite reports.'
  -
    name: Feature-Feature
    description: 'Checks all features for enabled or disabled.'
  -
    name: Feature-FeatureInitTest
    description: 'The API to create test subscriptions and payment gateway details.'
  -
    name: Fee-FeeElement
    description: 'Allows to create, edit and delete franchise fee.'
  -
    name: Fee-FeeList
    description: 'Retrieves a list of franchise fee.'
  -
    name: Feedback-Feedback
    description: 'API to submit feedback from the mobile application.'
  -
    name: Feedback-FeedbackLog
    description: 'API to log feedback from the mobile application.'
  -
    name: Field-EnrollmentFieldList
    description: 'Retrieves and validates a list of insurance partner fields. Use the GET method to load a list of required fields. Use the POST method to validate the fields before sending them to the payment API.'
  -
    name: Field-State
    description: 'Performs ajax requests for progress fields edit page.'
  -
    name: Filter-List
    description: 'Returns and updates information about sms filter keywords'
  -
    name: Filter-LoginSearchFilter
    description: 'Login Search settings.'
  -
    name: Filter-NameFilterOption
    description: 'Provides list of available Name filter options for contact, mail, and SMS history reports. Options are generated from report history rows with active date and report-type restrictions.'
  -
    name: Filter-ScheduleListFilter
    description: 'Information about saved templates of schedule filters.'
  -
    name: Finish-Finish
    description: 'Completes the booking process for a service. This endpoint can be used with an existing client by specifying their UID. It can also be used to create a new client by specifying user details in {@link FinishApi::$a_user} and omitting any UID. This endpoint can be accessed anonymously without authentication, but only when creating clients.'
  -
    name: Finish-Finish47
    description: 'Completes an appointment booking.'
  -
    name: Finish-FinishMultiple
    description: 'Pays for an appointment or appointment Purchase Option for a client.'
  -
    name: Fisheye-FisheyeReviewLock
    description: 'Locks/Unlocks fisheye revision for future commits.'
  -
    name: Fitbuilder-Message
    description: 'Point to get messenger information from Fitbuilder.'
  -
    name: Flag-Flag
    description: 'Retrieves information if a user is flagged in a location.'
  -
    name: Folder-AutomationFolder
    description: 'API for managing automation folders.'
  -
    name: Folder-FolderMap
    description: 'Api to work with moving the automation to the folder.'
  -
    name: Folder-FolderSort
    description: 'Class to work with sorting of automation folders.'
  -
    name: Foreign-PersonnelForeign
    description: 'Manage personnel aliases within integrations systems.'
  -
    name: Form-Environment
    description: 'Gets information about payment environments.'
  -
    name: Form-EnvironmentUser
    description: 'Gets information about payment environments for the user.'
  -
    name: Form-PayLog
    description: 'Saves logs to {@link PayDebugLog}.'
  -
    name: Franchise-FranchiseGroup
    description: 'Point to get a franchise group. Group is all businesses bound by a franchise (both franchisor and franchisees).'
  -
    name: Franchise-Franchisee
    description: 'Saves franchisee settings.'
  -
    name: Frequency-Repeat
    description: 'For recurrent class booking returns list of visits to be created for the given settings.'
  -
    name: Frequency-RepeatParallel
    description: 'Extension of {@link RepeatApi} that adds control over whether parallel class period series (other sessions running at the same time and location) are included in the returned visit list. When `is_include_parallel` is `false` (default), only the originally selected class period''s series (parent and its reschedules) is returned. When `true`, all parallel series are also included and can be reviewed or individually ignored by the client.'
  -
    name: Frontend-LifetimeTotals
    description: 'Returns a list with totals of lifetime visits.'
  -
    name: FusionAuth-Connector
    description: 'This class is used to get requests from the `FusionAuth`.'
  -
    name: Generator-Filter
    description: 'Point to save and get last configuration of a report.'
  -
    name: Generator-FilterInfo
    description: 'Returns information about filters.'
  -
    name: Generator-Query
    description: 'Returns content of a report as a table. If report is not yet generated, it gets generated. If the report is being generated now, partial content may be returned.'
  -
    name: Geo-Combobox
    description: 'Retrieves a list of cities matching the search string for a given location.'
  -
    name: Geo-GeoAddress
    description: 'Get key of country, key of region and name of city from geo city key.'
  -
    name: Geo-GeoPosition
    description: 'Point to get current geo position.'
  -
    name: Geo-GeoTimezone
    description: 'Get timezone by time offset.'
  -
    name: Google-GoogleCaptcha
    description: 'Stores the user token CAPTCHA.'
  -
    name: Google-GoogleCoordinate
    description: 'Point to get geo position from address.'
  -
    name: Google-LoginLink
    description: 'Returns a link for a user to sign in with Google+.'
  -
    name: Granular-Edit
    description: 'Edit a task granular errors.'
  -
    name: Group-GroupTemporary
    description: 'Point to check if quick group of clients is empty.'
  -
    name: Group-MailGroupOpen
    description: 'Mail group API.'
  -
    name: GroupList-List
    description: 'Member groups list.'
  -
    name: Guest-GuestProfile
    description: 'Endpoint for guest account creation during "Book on behalf of guest" process.'
  -
    name: GuestPass-GuestPass
    description: 'API for managing guest passes.'
  -
    name: GuestPass-GuestPassList
    description: 'API for managing guest passes of specific client.'
  -
    name: GymPass-BusinessConnect
    description: 'Api endpoint used to connect Gympass integration.'
  -
    name: GymPass-BusinessDisconnect
    description: 'Api endpoint used to disconnect Gympass integration.'
  -
    name: Header-Owner
    description: 'Provides header information and some header display settings.'
  -
    name: Header-User
    description: 'Render HTML of the frontend user menu.'
  -
    name: Hint-Hint
    description: 'Manages work of hints.'
  -
    name: History-Resend
    description: 'Manage actions of sms history report.'
  -
    name: Holiday-BulkBusinessHoliday
    description: 'An endpoint displaying information about all location/business holidays for a given business.'
  -
    name: Holiday-Holiday
    description: 'An endpoint displaying information about locations'' business holidays.'
  -
    name: Holiday-HolidayCalendar
    description: 'Api for get information about used holidays.'
  -
    name: Holiday-HolidayElement
    description: 'Allows to create, edit, and delete specific personnel''s holidays.'
  -
    name: Holiday-HolidayList
    description: 'Loads a list with a schedule holiday of the personnel.'
  -
    name: Holiday-HolidayTotal
    description: 'Api for get information about schedule holiday.'
  -
    name: HolidayView-HolidayView
    description: 'Retrieves information about holiday item.'
  -
    name: Image-Image
    description: 'Progress image api.'
  -
    name: ImageMarkup-ImageMarkup
    description: 'Api to get default image markup for the business type of current business.'
  -
    name: ImageMarkup-ImageMarkupConfig
    description: 'API endpoint to load and edit image markup user configuration.'
  -
    name: ImageUpload-ImageCopyTemporary
    description: 'Make a temporary copy of an existing image.'
  -
    name: ImageUpload-ImageUpload
    description: 'An endpoint that performs the final action to an uploaded image. Images are uploaded using {@link \Core\Drive\ImageUpload\ImageUploadTemporaryApi}.'
  -
    name: ImageUpload-ImageUploadTemporary
    description: 'Uploads a raw image to a server. You must save an image by using {@link ImageUploadApi}.'
  -
    name: ImageUpload-Variant
    description: 'Rotates image by given link.'
  -
    name: Import-FileImportSos
    description: 'Api to remove files, which were uploaded into SOS before reimbursement import. Such files can be removed, if actor does not want to complete started import.'
  -
    name: ImportPopup-ImportPopup
    description: 'Import popup API. This is a banner that can be enabled at the time of import.'
  -
    name: Index-PromotionIndex
    description: 'Gets a list of introductory promotion offers of a specified type available at a given location.'
  -
    name: Index-QuizSubscription
    description: 'Api to get and update subscription plan of the business for the Forms feature.'
  -
    name: Index-Upgrade
    description: 'Api for two-way sms subscriptions.'
  -
    name: Info-ClassPeriod
    description: 'Retrieves information about class session.'
  -
    name: Info-Info
    description: 'Offers functionality for the class booking wizard on the "Class and Location" page.'
  -
    name: Info-Info54
    description: 'Offers functionality for the class booking wizard on the "Class and Location" page. When using this endpoint, take into account the {@link ProcessSpaSid::QUIZ} step.'
  -
    name: Info-InfoCanComplete
    description: 'Checks for `next` and `complete` buttons in the booking process.'
  -
    name: Info-UserInfo
    description: 'Retrieves information about a WellnessLiving user.'
  -
    name: Info-UserIntegration
    description: 'An endpoint that return the information about the integrations the user is connected to.'
  -
    name: Info-UserListInfo
    description: 'An endpoint that retrieves information about users with the same email.'
  -
    name: InlineEditor-MessageData
    description: 'Returns information about translated messages.'
  -
    name: InlineEditor-Save
    description: 'Saves new translation of a translated message.'
  -
    name: Instance-Instance
    description: 'Manage `Studio Deploy Instance`.'
  -
    name: Instance-InstancePersonalSetting
    description: 'Manage personal settings about a deployment instance.'
  -
    name: Instance-Item
    description: 'Allows to read, create and patch a deployment instance.'
  -
    name: Instance-Preliminary
    description: 'Allows to change preliminary mode of a deployment instance.'
  -
    name: Invitation-BrivoUserInvitation
    description: 'API point to manage a client invitations.'
  -
    name: Invite-Invite
    description: 'API to manage a single guest pass invitation.'
  -
    name: Invite-InviteList
    description: 'API to manage guest pass invitations within a client''s membership or client''s profile.'
  -
    name: Ip-AccessAcquire
    description: 'Allows a staff member to acquire access from his current IP address to specified service.'
  -
    name: Ip-AccessRevoke
    description: 'Allows a staff member to revoke access from his current IP address to specified service.'
  -
    name: Ip-AccessType
    description: 'Allows to manage access rights of current staff member to a service.'
  -
    name: Ip-Info
    description: 'Allows to manage information about an IP address.'
  -
    name: Ip-IpGet
    description: 'Get current IP.'
  -
    name: Ip-PersonalSetting
    description: 'Manage personal settings about a service.'
  -
    name: IpRestriction-IpRestriction
    description: 'IP Restriction API.'
  -
    name: Item-PurchaseItemAmount
    description: 'Data of certain purchase.'
  -
    name: Item-PurchaseItemList
    description: 'Data of certain purchase.'
  -
    name: Jira-Link
    description: 'Manages linking jira task to studio task. Adds or removes link to jira task on studio task.'
  -
    name: Jira-Sync
    description: 'Synchronises state of the task on studio with state of the task on jira. This API called by foreign system when jira task update triggered.'
  -
    name: Jira-Transition
    description: 'Performs transition of the task from current status to given.'
  -
    name: Jwt-JwtToken
    description: 'Endpoint to get JWT token.'
  -
    name: Layout-Layout
    description: 'Displays information about an asset layout.'
  -
    name: Lead-BusinessChange
    description: 'Manages referrals and referrers for a business lead request within partner program.'
  -
    name: Lead-BusinessLead
    description: 'Creates a new business lead request within the Partner Program.'
  -
    name: Lead-Lead
    description: 'Gets information from the Lead Capture widget and saves a new user’s information. A user can be added to a second business by adding them first as a lead. If your business uses Enterprise Cloud, there may be a restriction where clients can only be members in one enterprise location (travellers in all other enterprise locations). This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link LeadCaptcha::CID}.'
  -
    name: Lead-LeadCheck
    description: 'Checks if user with specified email already belongs to specified business.'
  -
    name: Lead-LeadPage
    description: 'Retrieves data for Leads page.'
  -
    name: Lead-LeadStatus
    description: 'Lead status processing.'
  -
    name: LeadSource-List
    description: 'Endpoint to retrieve list of lead sources.'
  -
    name: Level-Level
    description: 'Retrieves, changes, or deletes a video level.'
  -
    name: Level-LevelList
    description: 'Changes the order of video levels.'
  -
    name: Level-LevelListFilter
    description: 'Api for filtering of video levels.'
  -
    name: Limit-SessionLimit
    description: 'An empty API is needed in order to check whether the client can make a request to the server or not.'
  -
    name: Link-SendMail
    description: 'Model to send email based on AI agent link action. This endpoint is not public and can be used only by the AI agent.'
  -
    name: List-AutomationList
    description: 'Manages automation list data.'
  -
    name: Live-MailPatternLive
    description: 'Api to work with manual notification sending and marketing campaigns.'
  -
    name: Live-MailPatternLiveRecipient
    description: 'Api to work with manual notification sending and marketing campaigns.'
  -
    name: Load-Load
    description: 'Point to clock in/out by a staff member.'
  -
    name: Load-Load74
    description: 'Point to clock in/out by a staff member.'
  -
    name: Location-BusinessFranchiseLocation
    description: 'Manages franchisor locations.'
  -
    name: Location-List
    description: 'List of business staffs with the locations available to them.'
  -
    name: Location-List73
    description: 'API to get list of locations for business.'
  -
    name: Location-ListBulk
    description: 'Gets all locations for a list of businesses. This can be used to get all locations from all businesses. When using this endpoint, note that it will return a list of all locations in the system, potentially using substantial system resources. Using this endpoint can potentially result in larger API calls than average calls made for other endpoints.'
  -
    name: Location-Location
    description: 'Endpoint for add/deactivate or refresh token for location for Go High Level integration.'
  -
    name: Location-LocationActiveEdit
    description: 'API to edit location active status.'
  -
    name: Location-LocationActiveLog
    description: 'API to edit location active log.'
  -
    name: Location-LocationAddPermission
    description: 'Api endpoint to check if the user has the privilege to add locations.'
  -
    name: Location-LocationOption
    description: 'API to edit location options.'
  -
    name: Location-LoginLocation
    description: 'Point to locations ever visited by user.'
  -
    name: Location-ScheduleListLocation
    description: 'Information about locations which must be represented on schedule to current user.'
  -
    name: LocationRate-LocationRate
    description: 'Retrieves information about location rate.'
  -
    name: Log-LogBusinessData
    description: 'Retrieve log data.'
  -
    name: Log-Verify
    description: 'Performs ajax requests for verify client''s progress log.'
  -
    name: Login-AppleLogin
    description: 'Authorizes a user with Apple.'
  -
    name: Login-Enter
    description: 'Signs a programmer in.'
  -
    name: Login-FacebookLogin
    description: 'An endpoint that performs authorization actions with Facebook.'
  -
    name: Login-GoogleLogin
    description: 'An endpoint that performs authorization actions with Google.'
  -
    name: Login-Info
    description: 'Gets the user ID for the current user and a password reset URL.'
  -
    name: Login-Login
    description: 'A class is for authorizing user with twitter.'
  -
    name: Login-LoginBind
    description: 'Binding of user''s profile to Facebook.'
  -
    name: Login-LoginLogo
    description: 'API for login logo template.'
  -
    name: Login-MicrosoftLogin
    description: 'An endpoint that performs authorization actions with Microsoft.'
  -
    name: Login-PassportLoginTjx
    description: 'Endpoint to sign in user in mobile application after he is authorised with TJX.'
  -
    name: Login-QuizLogin
    description: 'Api for change quizzes which client must process.'
  -
    name: Login-QuizRemind
    description: 'Api for change quizzes which client must process.'
  -
    name: Mail-DurationChangeMail
    description: 'Get change appointment mail pattern.'
  -
    name: Mail-MailUse
    description: 'An endpoint to check if an email address is used by member of a business.'
  -
    name: Mail-PostcardSummary
    description: 'API to retrieve quantity of recipients which will get a postcard.'
  -
    name: Mail-PurchaseMail
    description: 'Emails a receipt for a purchase.'
  -
    name: Mail-SendMail
    description: 'Can be used to send email messages.'
  -
    name: Mail-SmsSummary
    description: 'API to retrieve quantity of available sms in this billing period.'
  -
    name: Mailchimp-EmlMailChimp
    description: 'Api for Mailchimp webhooks. Starts work when user state changed by administrator or as user on Mailchimp side.'
  -
    name: Manage-DashboardManage
    description: 'Api for manage dashboard.'
  -
    name: Manage-TeamEdit
    description: 'Allows to create, edit and delete teams.'
  -
    name: Manage-TeamList
    description: 'Loads list of teams for editor.'
  -
    name: Mark-Mark
    description: 'Appointment state change API.'
  -
    name: MarketingSuite-MarketingSuiteSubscription
    description: 'Entry point to get information about "Marketing Suite" subscription.'
  -
    name: MediaConvert-PathElement
    description: 'Api to add\change path video converted.'
  -
    name: Member-FranchiseMember
    description: 'Performs work with enterprise, travellers and regular clients in the franchise.'
  -
    name: Member-GenerateId
    description: 'Generate new member''s ids if those are empty.'
  -
    name: Member-LoginMember
    description: 'An endpoint that manages client ID.'
  -
    name: Member-LoginMemberListAll
    description: 'Displays list of active members in a business.'
  -
    name: Member-Member
    description: 'Displays information about members of businesses.'
  -
    name: Member-MemberValidate
    description: 'Checking whether there is something that prevents the user from using the business.'
  -
    name: Member-MemberValidate63
    description: 'Checking whether there is something that prevents the user from using the business.'
  -
    name: Menu-Menu
    description: 'Search templates menu API.'
  -
    name: Menu-MenuReport
    description: 'List of dashboards for menu in report.'
  -
    name: Merchant-Merchant
    description: 'Manages business merchant processing: - returns detail information about business merchants; - adds/edits merchant settings for specified business.'
  -
    name: Merge-ProfileMerge
    description: 'Allows to multiple merge of clients.'
  -
    name: Message-ClearCache
    description: 'Clears all caches where messages can be cached. This includes compiled versions of templates.'
  -
    name: MessageType-MessageTypeOption
    description: 'An endpoint that can update state of "Message Type" filter in Message Center page.'
  -
    name: Method-List
    description: 'An endpoint containing a model to get the list of payment methods.                                                        .'
  -
    name: Mfa-MfaActivate
    description: 'Activates an MFA device.'
  -
    name: Mfa-MfaEnter
    description: 'Checks the OTP code.'
  -
    name: Microsoft-Login
    description: 'Performs preliminary authorization actions with Microsoft.'
  -
    name: Model-ModelPool
    description: 'API for mass synchronization of models.'
  -
    name: Modify-Modify
    description: 'Modifies the master model.'
  -
    name: MultipartUpload-MultipartUpload
    description: 'The API is the link between the client side of the uploader and the server side.'
  -
    name: NameList-NameList
    description: 'List of business names.'
  -
    name: NonIntegrated-NonIntegratedMeetingEnd
    description: 'Api to end Non Integrated virtual meeting.'
  -
    name: Notification-Element
    description: 'Retrieves information about push notification item.'
  -
    name: Notification-List
    description: 'Retrieves a list of push notification items to show in user profile.'
  -
    name: Notifications-Notification
    description: 'Saves Client''s Notifications page settings.'
  -
    name: Nuvei-NuveiHardwareElement
    description: 'Endpoint for operations with `nuvei` hardware.'
  -
    name: Nuvei-NuveiHardwareList
    description: 'Endpoint for retrieve list of `nuvei` hardware.'
  -
    name: Nuvei-NuveiOpenOrder
    description: 'Authenticates `Nuvei` merchant credentials and sets up an order in the `Nuvei` system.'
  -
    name: Nuvei-NuveiTransactionCancel
    description: 'Cancels `Nuvei` transaction.'
  -
    name: Nuvei-NuveiTransactionOnAuthSuccess
    description: 'Updates `Nuvei` transaction with the gateway transaction ID.'
  -
    name: Nuvei-NuveiTransactionStart
    description: 'Creates new `Nuvei` transaction.'
  -
    name: Nuvei-NuveiUpdateOrder
    description: 'Allow modifying the amount of a previously opened order {@link NuveiOpenOrderApi}.'
  -
    name: OMNIChannelApi-NuveiTerminalTransactionStart
    description: 'Performs request to Terminal to start payment transaction. In case Terminal is available - transaction is started and we schedule task to poll swipe result periodically. Otherwise - error is thrown.'
  -
    name: Oauth-OauthMe
    description: 'Api endpoint used by third party service to retrieve information about current user.'
  -
    name: Oauth-OauthToken
    description: 'Api endpoint used by third party service to retrieve access token.'
  -
    name: Onboarding-CreateAccount
    description: 'Creates a new Stripe account.'
  -
    name: Onboarding-CreateLink
    description: 'Creates a link to update connected account data.'
  -
    name: Option-BusinessConfigOption
    description: 'Retrieves business config options.'
  -
    name: Otp-PhoneOtp
    description: 'Entry point to verify access to the phone number via sending and checking otp code. This endpoint is available only for user with a special privilege. If you want to use this endpoint, contact with WellnessLiving support first.'
  -
    name: Overview-OverviewCustomizePanel
    description: 'Api for getting widgets and summary cards setting data on client''s Overview page.'
  -
    name: Overview-ProfilePageOverview
    description: 'Saves client''s profile overview widgets and summary cards settings.'
  -
    name: Overview-SendMail
    description: 'Model for sending emails to user.'
  -
    name: Owner-Owner
    description: 'Gets the payment owner ID.'
  -
    name: Page-PageElement
    description: 'Returns information about a specified visit.'
  -
    name: Page-PageList
    description: 'Returns a list of either previous or upcoming visits for a specific user. A visit can be for an appointment, class, or an event.'
  -
    name: Page-ReportQueryActivate
    description: 'Activates or deactivates (soft-deletes) a SQL report registry record. System-level reports (`k_business = ''0''`) can only be activated/deactivated by WL admins.'
  -
    name: Page-ReportQueryGet
    description: 'Returns full data for a single SQL report registry record. Supports both business-level and system-level reports. System-level reports (`k_business = ''0''`) are accessible by WL admins and by non-admins if `id_report_access >= BUSINESS`.'
  -
    name: Page-ReportQueryInsert
    description: 'Creates a new SQL report registry record. Delegates validation of `s_sql` (must be SELECT) and `json_totals` (must be JSON) to {@link ReportQueryModel::insert()}.'
  -
    name: Page-ReportQueryList
    description: 'Returns the list of SQL report registry records for the current business. For business-level requests (`$k_business` is a real business key), returns both business-specific and system-level reports. For system-level requests (`$k_business = ''0''`), returns only system-level reports.'
  -
    name: Page-ReportQuerySchema
    description: 'Returns the schema descriptor used by the SQL editor autocomplete. Exposes the list of registered tables (with columns, PHP types, and PHPDoc hints), the list of supported format hints, and the list of built-in `@placeholder` variables. The response is consumed by the `Thoth_ReportCore_QueryEngine_Page_List_CrudPanelEditor` JavaScript class to power context-aware suggestions in the SQL field of the Create/Edit report form. Access requires backend membership ({@link Backend::accessHas()}) or WL-admin ({@link rs_can_admin()}).'
  -
    name: Page-ReportQueryTotal
    description: 'Returns computed aggregate totals for a SQL report record. Reads the `json_totals` config from the registry, executes the report SQL across all partitions of the first table, and aggregates each configured summary card via {@link ReportQueryTotalComputer::compute()}. Returns an empty array when the record is missing, inactive, has no totals configured, or when the SQL cannot be parsed.'
  -
    name: Page-ReportQueryUpdate
    description: 'Updates an existing SQL report registry record. Calls {@link ReportQueryModel::selectById()} before update to prevent the silent no-op when the record does not exist or belongs to another business.'
  -
    name: Panel-Panel
    description: 'Search template settings API.'
  -
    name: Partner-PartnerCode
    description: 'Works with partner url for a business that takes part in the partner program.'
  -
    name: Partner-PartnerEdit
    description: 'Allows to create, edit and delete partner.'
  -
    name: Partner-PartnerList
    description: 'Loads list of curves partner for editor.'
  -
    name: Partner-PartnerSettings
    description: 'Works with partner settings of the business. This information is entered by a staff member when a client enrolls into Partner Program and uses a custom landing page, which is available by special business URL.'
  -
    name: Passport-UidUuid
    description: 'API endpoint for bulk conversion between UIDs and UUIDs. Accepts arrays of UIDs and UUIDs, converts them in both directions, and returns a unified associative array mapping UIDs to their corresponding UUIDs.'
  -
    name: Pattern-PatternVariable
    description: 'API for retrieving available template variables for a given mail notification type and form mode.'
  -
    name: Pay-Abort
    description: 'Aborts a running import.'
  -
    name: Pay-Pay
    description: 'Entry point to get and set data about "who pays for who".'
  -
    name: Pay-PayChange
    description: 'Manages changing purchase option to pay for visit.'
  -
    name: Pay-PromotionPayPauseList
    description: 'Manages promotion payment pause periods for list of login promotion.'
  -
    name: Pay-Start
    description: 'Starts an import.'
  -
    name: PayHardware-HardwareStaffPreference
    description: 'Endpoint to save staff preferred terminal reader.'
  -
    name: PayMethod-AutopayMethod
    description: 'API to change login promotion automatic payment method.'
  -
    name: PayRate-Copy
    description: 'Api to copy exists pay rates to the staff profile.'
  -
    name: PayRate-Item
    description: 'Api to work with staff pay rate.'
  -
    name: PayRate-List
    description: 'List of staff pay rates for the business.'
  -
    name: Payment-Information
    description: 'Retrieves information about coupon price.'
  -
    name: Payment-Payment
    description: 'Acts as the booking wizard for the "Pay/Billing info" page. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Payment-PaymentMultiple
    description: 'Completes the payment for an appointment or appointment Purchase Option for a client. The only difference between this endpoint and {@link PaymentApi} is that this endpoint is capable of paying for multiple appointments at the same time. This endpoint uses a CAPTCHA check. To pass the CAPTCHA, you''ll need to study the documentation of the CAPTCHA base class. In the documentation, you''ll find that you need to send a CAPTCHA for a specific action. For this API, the action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: Payment-PaymentPost
    description: 'Sends payments for an appointment booking. This endpoint using captcha check. To pass captcha need study the documentation by captcha API, there you will find that you need to send a captcha for a specific action. For this API an action is {@link BusinessPaymentCaptcha::CID}.'
  -
    name: PaymentScheduleModal-PaymentScheduleElement
    description: 'Retrieves information about purchase.'
  -
    name: Permission-Permission
    description: 'Makes a purchased promotion auto-renewable. The POST method makes the promotion auto-renewable based on the {@link PermissionApi::$is_renew} parameter.'
  -
    name: Personnel-Access
    description: 'Allows to grant/revoke access from personnel to IP services.'
  -
    name: Personnel-Key
    description: 'Api for gets repository key for user.'
  -
    name: Personnel-LoginCheck
    description: 'Get login and one time password for login to WL projects.'
  -
    name: Personnel-Password
    description: 'Manage temporary password for login to WL projects.'
  -
    name: Personnel-Personnel
    description: 'Api for get data of the studio''s personnel.'
  -
    name: PgSql-QueryExecute
    description: 'Get PostgreSQL query and connection to execute.'
  -
    name: Phone-BusinessPhone
    description: 'API endpoint to get phone number of a business.'
  -
    name: Phone-Phone
    description: 'Manages business phone number. It is used to register business phone number or add bundle SID, or to get phone number of some business.'
  -
    name: Phone-PhoneGenerateByMask
    description: 'Return random phone number by mask.'
  -
    name: Phone-PhoneRegulatoryBundle
    description: 'Manages regulatory bundle SID from Twilio (located in the Regulatory Bundle they have submitted). It is used to add regulatory bundle SID.'
  -
    name: Phone-PhoneSimilar
    description: 'When business registers phone number, phone number must be verified first. {@link \Wl\Business\Phone\PhoneVerifyApi} is used for it. In case phone number fails verification, few similar numbers should be suggested. This API is used to get these similar phone numbers, depended on phone number which failed verification.'
  -
    name: Phone-PhoneVerify
    description: 'Manages process of new phone verification.'
  -
    name: Phone-Settings
    description: 'Manages message settings.'
  -
    name: Pin-SmsPin
    description: 'The API for delete unread sms.'
  -
    name: Plus-Login
    description: 'Authorizes a user with Google.'
  -
    name: Policy-CancelPolicyService
    description: 'Retrieves information about services data used for service select on business policy `Add/Edit penalty` panel.'
  -
    name: Pool-PoolItem
    description: 'Allows working with purchase pool items.'
  -
    name: Popup-Popup
    description: 'Help popup settings.'
  -
    name: Popup-UserPopup
    description: 'API with data for user profile popup.'
  -
    name: Position-Position
    description: 'Coordinates of back admin container.'
  -
    name: Postcard-PostcardAddress
    description: 'Entry point to manipulate direct mail campaign.'
  -
    name: Postman-Hash
    description: 'The class get string and algorithm and encode current string by this algorithm.'
  -
    name: Prebuilt-AutomationPrebuilt
    description: 'API for getting prebuilt automation data for the business. Prebuilt automation is the one that is not created by the business, but provided by Wl as a template.'
  -
    name: Prebuilt-PrebuiltDuplicate
    description: 'Manages pre-built automation duplication.'
  -
    name: Presence-PresenceActivate
    description: 'Manage actions of sms history report.'
  -
    name: Prg-PrgKeyword
    description: 'Class for generating keywords for php/sql editor on programmers page.'
  -
    name: Privilege-PrivilegeList
    description: 'Can return list of admin or staff privileges of the current user. Cannot be used to check privileges of another user.'
  -
    name: Prize-Element
    description: 'Retrieves information about user''s prize.'
  -
    name: Prize-Prize
    description: 'Retrieves a list of redeemed prizes.'
  -
    name: Process-Process
    description: 'Performs the booking wizard steps.'
  -
    name: Process-Process54
    description: 'Performs the booking wizard steps. Include the {@link ProcessSpaSid::QUIZ} step, if needed.'
  -
    name: Process-Process59
    description: 'Performs the booking wizard steps. This endpoint includes changes to the list of steps if the service is already booked for the currently selected client, and if {@link ProcessApi::$is_family_relation_book} is set to `true`.'
  -
    name: Process-ProcessGroup
    description: 'API for group booking process. This API allows to book a class/event for multiple clients at once.'
  -
    name: Product-Product
    description: 'Returns information about service add-ons. If you need to filter products by client type/group, use {@link Product62Api}.'
  -
    name: Product-Product62
    description: 'Returns information about service add-ons.'
  -
    name: ProductList-List
    description: 'Product API.'
  -
    name: Profile-InsuranceCancel
    description: 'Wellness Program membership cancel API.'
  -
    name: Profile-InsuranceHold
    description: 'Manages promotion pause.'
  -
    name: Profile-ProfileCreate
    description: 'Creates user profile with minimum fields.'
  -
    name: ProfileField-List
    description: 'Endpoint to retrieve list of profile field.'
  -
    name: ProfilePage-ProgressLogProfilePageView
    description: 'Provides an endpoint to get pre-rendered progress log profile page.'
  -
    name: Progress-Progress
    description: 'Manages business import progress.'
  -
    name: Project-ProjectList
    description: 'Api for getting studio project for current personnel.'
  -
    name: Promote-AppointmentBookPromote
    description: 'An API to promote visits from a wait list to an active list for appointment/asset.'
  -
    name: Promote-Promote
    description: 'Book promote processing.'
  -
    name: Promoting-ExecuteScript
    description: 'Executes specified script to promote sprint.'
  -
    name: Promoting-PromotingLog
    description: 'Allows retrieval of promoting logs contents.'
  -
    name: Promotion-ClassPromotion
    description: 'Returns a list of promotions that can pay for a business''s classes or events.'
  -
    name: Promotion-Promotion
    description: 'Gets promotions for the specified business and promotion keys.'
  -
    name: Promotion-PromotionCheckExtension
    description: 'API point to check whether login promotion will be extended upon disabling auto-renew and for how many days.'
  -
    name: Promotion-PromotionList
    description: 'Gets a promotion list of the specified business.'
  -
    name: Promotion-PromotionPayCancel
    description: 'API for controlling cancelled promotion options.'
  -
    name: Promotion-PromotionPayPause
    description: 'Manages or retrieves information about holds on Purchase Options. The DELETE method can remove a hold. The GET method only returns information about active holds. The POST method can create or edit a hold. The PUT method can edit a hold.'
  -
    name: Promotion-ResourcePromotion
    description: 'Return list of promotion what can in theory pay for resource from business.'
  -
    name: Promotion-ServicePromotion
    description: 'Return list of promotion what can in theory pay for service from business.'
  -
    name: PromptList-PromptList
    description: 'Entry point to set "checked" on confirmed prompt.'
  -
    name: Purchase-Mail
    description: 'Model to send user''s receipt by email.'
  -
    name: Purchase-MemberByPromotion
    description: 'Returns a list of active clients with the given Purchase Options. This endpoint returns only active clients using active Purchase Options from the given list. Does not return clients, to which currently signed-in user does not have access to.'
  -
    name: Purchase-Purchase
    description: 'Displays information about Purchase Options that can book specified session(s). Note that the terms "Purchase Option" and "promotion" represent the same thing (promotion was the previous term used in WellnessLiving). Both these terms have been used for various variable names.'
  -
    name: Purchase-Purchase56
    description: 'Displays information about Purchase Options that can book specified session(s). Note that the terms "Purchase Option" and "promotion" are used interchangeably.'
  -
    name: Purchase-Purchase72
    description: 'Retrieves information about Purchase Options that can be used to pay for an appointment during creating new or rescheduling existing appointment.'
  -
    name: Purchase-PurchaseElement
    description: 'A model of the purchase item.'
  -
    name: Purchase-PurchaseElementGroup
    description: 'API point to get information about a list of purchase options wile booking process.'
  -
    name: Purchase-PurchaseElementList
    description: 'Displays a list of purchase items.'
  -
    name: Purchase-PurchaseItemCancel
    description: 'API endpoint used to operate specific purchased item.'
  -
    name: Purchase-PurchaseList
    description: 'Purchase API.'
  -
    name: PurchaseList-PurchaseList
    description: 'Gets a list of purchases for a user.'
  -
    name: PurchaseList-PurchaseListElement
    description: 'Gets the details of a specified purchase. This endpoint may require different IDs to be specified for different types of purchases. The IDs required for a specific item will be the same IDs returned from the {@link PurchaseListApi} endpoint.'
  -
    name: Push-ModuleSetup
    description: 'API to perform push for a specified module.'
  -
    name: Push-Push
    description: 'Model for saving of IDs of users'' devices.'
  -
    name: Question-Question
    description: 'Retrieves questions for a selected service.'
  -
    name: QueueServer-Disable
    description: 'Allows to disable or activate a queue server. Disabling of a queue server may be needed for maintenance work on the server. See <tt>namespace.Core/Async/QueueServer/doc/statuses-of-queue-servers.md</tt> for information about possible transitions of queue servers between statuses.'
  -
    name: QueueServer-QueueServerList
    description: 'Returns a list of queue servers.'
  -
    name: QueueServer-StatFunction
    description: 'Returns complete list of all functions in the system.'
  -
    name: QueueServer-StatWorker
    description: 'Returns complete list of all workers in the system.'
  -
    name: Quick-EnterQuick
    description: 'Allows to generate protected sign in link for {@link Controller}.'
  -
    name: QuickCheckIn-QuickCheckIn
    description: 'Returns today''s check-in sessions for a client when QUICK AUTO Check-In is active and "Navigate to client profile after QUICK AUTO Check-In scan" is disabled. If the business setting is enabled, returns the profile URL for navigation instead.'
  -
    name: QuickStart-QuickStartConfig
    description: 'API endpoint to load and edit quick start user configuration.'
  -
    name: Quiz-Quiz
    description: 'Manages quizzes and a selected Purchase Option for a class or event during the booking process. Note that the terms "Purchase Option" and "promotion" are used interchangeably.'
  -
    name: Quiz-QuizElement
    description: 'Manages a single quiz.'
  -
    name: Quiz-QuizElement72
    description: 'Manages a single quiz.'
  -
    name: Quiz-QuizElementClone
    description: 'Api endpoint to clone single quiz.'
  -
    name: Quiz-QuizList
    description: 'Endpoint to get list of business quizzes.'
  -
    name: Quiz-ServiceList
    description: 'Gets list of available services for quiz.'
  -
    name: Rank-Addon
    description: 'Api to change addons for belts.'
  -
    name: Rank-LoginRankElement
    description: 'API to delete a rank record for a user.'
  -
    name: Rank-LoginRankList
    description: 'API to get and save list of ranks for specified users.'
  -
    name: Rank-ProfileRank
    description: 'Api to change promotion date on belts.'
  -
    name: Rank-Rank
    description: 'Retrieves information about belts in a business. This information can be used to figure out totals in the Belts Report.'
  -
    name: Rank-SearchData
    description: 'Retrieves information about belt rank categories.'
  -
    name: Reads-SmsReads
    description: 'The API for add SMS reads.'
  -
    name: Reason-PromotionTerminateReason
    description: 'Api to work with termination reason for a promotions in the business.'
  -
    name: Receipt-PurchaseReceipt
    description: 'Returns receipt data of certain purchase.'
  -
    name: Receipt-ReceiptMail
    description: 'API for sending the Visit Receipt email notification.'
  -
    name: Recent-RecentService
    description: 'Returns list of client''s last booked services. Only unique services are returned, i.e. if a client booked the same service several times, only last of these bookings is returned in the list.'
  -
    name: Recipient-AutomationRecipient
    description: 'Gets recipients data.'
  -
    name: Recipient-AutomationRecipientStatus
    description: 'Manages recipients in an automation flow.'
  -
    name: Recover-ZoomRecover
    description: 'Api endpoint to manage unloaded recordings.'
  -
    name: Recurring-RecurringList
    description: 'Returns list of local dates without time, when recurring sessions are going to be booked.'
  -
    name: Recurring-RecurringList68
    description: 'Returns list of local dates without time, when recurring sessions are going to be booked. Added possibility to skip availability check of each session.'
  -
    name: Referrer-ReferralInfo
    description: 'Returns referral statistics for a user within a business. Provides the count of referred users, total reward points earned from referrals, and a shareable invite link. The link encodes the referrer''s user key so that when a new client registers via the link, the "Referred By" field is pre-filled.'
  -
    name: Referrer-Referrer
    description: 'Searches for a referrer using a string.'
  -
    name: Refund-Refund
    description: 'Refund functionality.'
  -
    name: Refuse-ReimbursementRefuseEdit
    description: 'Allows to create, edit and delete reimbursement refuse.'
  -
    name: Refuse-ReimbursementRefuseList
    description: 'Loads list of reimbursement refuse for editor.'
  -
    name: Region-Region
    description: 'Processes requests of the franchise region.'
  -
    name: Region-RegionList
    description: 'Api for get list franchise region for promotion.'
  -
    name: Region-RegionSearchData
    description: 'Search entity API.'
  -
    name: Region-TaxRegion
    description: 'API for regional taxes.'
  -
    name: RegionTravel-BusinessRegionTravel
    description: 'API for transferring a business to another Amazon region.'
  -
    name: Register-Register
    description: 'Allows new clients to be registered. The {@link LeadApi} endpoint is recommended for registering new users. However, this endpoint isn''t enabled by default. To enable this endpoint, use the contact form in the developer portal.'
  -
    name: Register-RegisterConfirm
    description: 'Confirm the user''s email and completes the registration process.'
  -
    name: Register-RegisterField
    description: 'Returns information about fields for user''s registration.'
  -
    name: Register-RegisterOtp
    description: 'Entry point to verify user authorization by email and phone number via sending and checking otp code.'
  -
    name: Registration-RegistrationList
    description: 'List of user''s uncompleted registration quizzes.'
  -
    name: Reimbursement-ReimbursementResubmit
    description: 'Triggers resubmission of the reimbursement record.'
  -
    name: Relation-FamilyRelation
    description: 'Returns business'' enabled relationship types.'
  -
    name: Relation-Inheritance
    description: 'Api for inheritance details.'
  -
    name: Relation-Relation
    description: 'Adds a relative during the booking process.'
  -
    name: Relation-Relation72
    description: 'Returns data about a user''s relationships.'
  -
    name: Repeat-Repeat
    description: 'Change recurring appointment booking settings.'
  -
    name: Report-Access
    description: 'Checks access of the current user to the certain report int the given business.'
  -
    name: Report-Data
    description: 'Returns information from a WellnessLiving report. There are two All Clients Reports that can be used to find user IDs. The results from each report can be filtered by different fields. The two reports are similar, but not exactly the same. The `LOGIN_LIST` (ID 22) report requires filters to be set to retrieve information. The `LOGIN_LIST_ALL` (ID 33) doesn’t require any filters to be set when specifying a date range.'
  -
    name: Report-DefaultReportConfig
    description: 'Default report configuration API. Stored in {@link BusinessReportConfig} with report id = 0.'
  -
    name: Report-Info
    description: 'Information about a report.'
  -
    name: Report-InventoryShopCategory
    description: 'Returns a list of categories in a business’s store for.'
  -
    name: Report-MailReason
    description: 'Loads a list of email fails reasons.'
  -
    name: Report-MembershipConversionFilterView
    description: 'API to load Membership Conversion filter html for new reports. Reason to create API: Membership Conversion filter html is based on `rs.promotion.select` widget that can be rendered on PHP side only. And filters in new reports are rendered via JS.'
  -
    name: Report-PageData
    description: 'Returns collection of the reports.'
  -
    name: Report-ProgressLogReportInfo
    description: 'Retrieves information about Progress Log report, such as available metrics for the chart and whether the actor can add new measurement records.'
  -
    name: Report-ReportConfig
    description: 'The report configuration API.'
  -
    name: Report-ReportQueryColumnMeta
    description: 'Returns per-report column metadata for the SQL report SPA. Returns the runtime column metadata for the SQL report SPA. Because {@link \Thoth\ReportCore\QueryEngine\Report\ReportQueryReportRow} has no static property annotations, the compile-time column metadata cache (`Wl_Report_View_Row_a_field`) is empty. This API gives the SPA the live column list (SELECT aliases, format hints) so it can populate `rowField()` and the customization form at runtime without relying on any compile-time field registration. The response also tells the SPA whether the SQL references date-filter placeholders (`dl_start` / `dl_end` - with `@` in the begining) -- driving date-filter visibility -- and which user-facing `placeholder` variables exist in the WHERE clause so the SPA can inject filter inputs for them.'
  -
    name: Report-TransactionAllPayment
    description: 'Can retrieve information from the All Transactions Report.'
  -
    name: Request-AppointmentRequest
    description: 'Confirms or denies the appointment.'
  -
    name: Request-Example
    description: 'Example API endpoint. Designed to debug issues with API.'
  -
    name: Request-Request
    description: 'Entry point for enter to business location.'
  -
    name: Request-RequestForward
    description: 'Sample API endpoint which is used for manual testing of {@link RequestForward} behavior. *How to use* Execute the following code snippet in the browser: <tt>namespace.Core/Request/doc/request-forward-test.js</tt>'
  -
    name: Reschedule-InstallmentReschedule
    description: 'Allows to reschedule single payment of an installment plan.'
  -
    name: Reset-PasswordReset
    description: 'Retrieves request about password resetting of the user. Reset password or not, accordingly to permissions.'
  -
    name: Resource-ApplicationResource
    description: 'Returns information used to generate application sources.'
  -
    name: Resource-ApplicationResourceMaterial
    description: 'Saves text metadata for a white-label application build. Used by the App Build Automation (n8n) to programmatically submit the Client App Materials form (text fields only). File assets are submitted separately via {@link ApplicationResourceUploadApi}.'
  -
    name: Resource-ApplicationResourceUpload
    description: 'Uploads image and file assets for a white-label application build. Accepts multipart/form-data with: - `a_image_upload[{key}]` file fields for image assets, where `{key}` is the image ID produced by sanitizing the asset file path (non-alphanumeric characters replaced with `_`). - A `google_services_json` file field for the Firebase Android config file. Text metadata is submitted separately via {@link ApplicationResourceMaterialApi}.'
  -
    name: Resource-Resource
    description: 'Selects assets for making a booking.'
  -
    name: Resource-Resource54
    description: 'Selects assets when making a booking. Take note of the {@link ProcessSpaSid::QUIZ} step.'
  -
    name: Resource-ScheduleListResource
    description: 'Information about business assets.'
  -
    name: ResourceList-List
    description: 'Asset list by business.'
  -
    name: Response-ReportPostpone
    description: 'Api endpoint to manage quiz response report.'
  -
    name: Response-Response
    description: 'An endpoint to request entrance into a business location. This endpoint is similar to {@link AuthorizePartnerApi} but not identical. To grant a user access the SDK sign-in account, the Manage Business permission is required. The associated accounts will receive an email regarding when they are given access and when that access expires. Access will be granted for 24 hours.'
  -
    name: Response-Response65
    description: 'Endpoint to work with quiz responses.'
  -
    name: Response-ResponseClone
    description: 'Endpoint for duplicate client response.'
  -
    name: Response-ResponseList
    description: 'Retrieves a list of a user''s quiz responses.'
  -
    name: Response-ResponseTwilio
    description: 'Handles the Twilio webhook for incoming SMS messages. <b>Attention!</b> You can''t rename or move this API to another namespace. It is used for a webhook in the Twilio account.'
  -
    name: Review-Review
    description: 'Saves information about review of the task.'
  -
    name: Review-ReviewFeature
    description: 'Changes the status of a featured review.'
  -
    name: Review-ReviewReply
    description: 'Adds a review reply.'
  -
    name: Review-Unfinished
    description: 'Retrieves information about unfinished reviews with information about reviewers of the task.'
  -
    name: ReviewList-ReviewElement
    description: 'Returns information about a review.'
  -
    name: ReviewList-ReviewList
    description: 'Returns a list of review IDs for all reviews for a location. If location is not specified, returns all reviews for all locations in the specified business. Reviews in WellnessLiving apply to specific locations. This endpoint can be used to get the IDs for all reviews or to get a listing that includes all the review data if the `i_page parameter` is set.'
  -
    name: Role-RoleList
    description: 'Api for getting studio project roles.'
  -
    name: Role-StaffRole
    description: 'Staff Role API.'
  -
    name: Rollup-Adjust
    description: 'Adjusts remaining visits for given attendance restriction of the given purchased promotion.'
  -
    name: Roster-AttendanceConfirmationScreen
    description: 'Api endpoint to get information about a client that has just checked in.'
  -
    name: Roster-AttendanceList
    description: 'Clients attendance list for attendance web app.'
  -
    name: Roster-AttendanceListAttend
    description: 'Book a class for a client and mark as attended using the Attendance Kiosk.'
  -
    name: Roster-AttendanceListBook
    description: 'Book a class for a client using the Attendance Kiosk.'
  -
    name: Roster-AttendanceListNotAttend
    description: 'Marks the visit as not attended using the Attendance Kiosk.'
  -
    name: Roster-AttendanceSecret
    description: 'Api endpoint to get the secret string for Attendance Kiosk requests.'
  -
    name: Roster-ScheduleList
    description: 'Schedule of classes for attendance web app.'
  -
    name: Row-Row
    description: 'Loads data for single attendance list row.'
  -
    name: Run-Kill
    description: 'Sends signal to exit to specified processes.'
  -
    name: Saml-SamlUserDeactivation
    description: 'Retrieves user statuses in the business.'
  -
    name: Save-ReportDuplicate
    description: 'API point to duplicate of the saved reports.'
  -
    name: Save-ReportSave
    description: 'API point to manipulate of the saving reports.'
  -
    name: Schedule-Calendar
    description: 'Retrieves a list of all calendar days in a specified month with available and unavailable appointment bookings in the schedule.'
  -
    name: Schedule-Calendar73
    description: 'Retrieves a list of all calendar days in a specified month with available and unavailable appointment bookings in the schedule.'
  -
    name: Schedule-Cancel
    description: 'Cancels either an appointment, class, or event session for the client.'
  -
    name: Schedule-DayTime
    description: 'Retrieves a list of information about the booking schedule for available appointments.'
  -
    name: Schedule-DayTime73
    description: 'Retrieves a list of information about the booking schedule for available appointments.'
  -
    name: Schedule-NextAvailableDay
    description: 'Returns next day with available.'
  -
    name: Schedule-PaymentMultiple
    description: 'Manages multiple payments for unpaid appointments on the same calendar date.'
  -
    name: Schedule-Schedule
    description: 'API to get schedule. <b>Note.</b> List of the properties is temporary. This is a temporary version which returns a completed template. <b>Important.</b> When this API will be completed - delete TODO ''Schedule Api'' in the project and code for them.'
  -
    name: Schedule-ScheduleAvailableDate
    description: 'Retrieves the nearest class session available for booking.'
  -
    name: Schedule-SchedulePostBased
    description: 'API to get schedule for printing via POST request. Accepts POST requests to avoid 414 (Request-URI Too Large) error when {@link SchedulePostBasedApi::$s_data} is too large to fit in a GET URL The large fields ({@link SchedulePostBasedApi::$s_data} and {@link SchedulePostBasedApi::$url_page}) are sent in the POST body while short identifier fields remain as URL query parameters.'
  -
    name: ScheduleDesign-ScheduleDesign
    description: 'Schedule design data. This API is not located in Wl\Schedule\Design because in this case core does not recognize URL correctly.'
  -
    name: ScheduleList-ScheduleList
    description: 'Schedule of sessions.'
  -
    name: ScheduleStandard-ScheduleStandard
    description: 'API point to get data about standard schedule customization.'
  -
    name: Score-Score
    description: 'API endpoint designed to manage points which are earned for certain activity.'
  -
    name: Search-BusinessInfo
    description: 'Api to get business logo, name and address by provided business keys.'
  -
    name: Search-BusinessSearch
    description: 'API to search business keys by given name. This endpoint uses captcha check. To pass a captcha you need to study the captcha API documentation, there you will find that you need to send a captcha for a specific action. In this API the action is {@link BusinessSearchCaptcha::CID}.'
  -
    name: Search-BusinessSearchUser
    description: 'API to search business keys by given name and given user email. This endpoint uses captcha check. To pass a captcha you need to study the captcha API documentation, there you will find that you need to send a captcha for a specific action. In this API the action is {@link BusinessSearchCaptcha::CID}.'
  -
    name: Search-Concerto
    description: 'Point for business Concerto to search a user by mail or phone.'
  -
    name: Search-DocsSearch
    description: 'Performs search in the docs library.'
  -
    name: Search-Search
    description: 'Allows to retrieve users list for specific location.'
  -
    name: Search-SearchSettings
    description: 'Allows to get and update settings of the client search in Staff Back Office.'
  -
    name: Secondary-MailSecondary
    description: 'Manages the list of secondary mailing recipients.'
  -
    name: Secondary-MailSecondarySearch
    description: 'Endpoint to search secondary recipients within the business.'
  -
    name: Select-BusinessSelect
    description: 'Provides the list of business that a staff member may access.'
  -
    name: Select-List
    description: 'Gets a member groups list.'
  -
    name: Select-LocationSelect
    description: 'API point provides the list of locations for a location select HTML component.'
  -
    name: Select-Resource
    description: 'Resources list.'
  -
    name: Select-Select
    description: 'Retrieves information about current user and his relationship with sub accounts.'
  -
    name: SeleniumCluster-SeleniumCluster
    description: 'A class to work with selenium servers.'
  -
    name: Send-MessageSend
    description: 'Send email, push or SMS notification to list of users.'
  -
    name: Send-NotificationInfo
    description: 'An endpoint that allows to send notifications to users of the certain business or location. This endpoint is available only for user with a special privilege. If you want to use this endpoint, contact with WellnessLiving support first.'
  -
    name: Send-NotificationSend
    description: 'An endpoint that allows to send notifications to users of the certain business or location. This endpoint is available only for user with a special privilege. If you want to use this endpoint, contact with WellnessLiving support first.'
  -
    name: SendCampaign-SendCampaign
    description: 'Manage marketing campaigns.'
  -
    name: SendCampaign-SendCampaignRecipient
    description: 'Gets quantity of recipients to send notifications to within a campaign.'
  -
    name: Sensor-SensorList
    description: 'Class for getting sensors list data.'
  -
    name: Sensor-SensorSave
    description: 'Class for adding, editing, deleting sensors.'
  -
    name: Service-Activate
    description: 'Allows to activate or deactivate a worker server. Deactivation of a worker server may be needed for maintenance work on the server.'
  -
    name: Service-Category
    description: 'Retrieves information about service categories.'
  -
    name: Service-Delete
    description: 'Allows to remove all tasks of specified function from task queue.'
  -
    name: Service-Edit
    description: 'Allows to create, edit and delete IP services.'
  -
    name: Service-InstanceActiveCheck
    description: 'Checks instance activity.'
  -
    name: Service-List
    description: 'Returns a list of IP services.'
  -
    name: Service-Restart
    description: 'Allows to restart a thread or all threads at worker server or at all worker servers.'
  -
    name: Service-ScheduleListService
    description: 'Information about appointment services which must be represented on schedule.'
  -
    name: Service-Service
    description: 'Retrieves assets that are required for a service booking.'
  -
    name: Service-ServiceCheck
    description: 'Checks if service can be booked by client. Throws exception if booking is restricted.'
  -
    name: Service-ServiceList
    description: 'Retrieves information about services in the current service category.'
  -
    name: Service-ServiceList52
    description: 'Retrieves information about services in the current service category. This is a new version of the {@link ServiceListApi} endpoint. It allows for filtering a list of services by multiple book now tabs.'
  -
    name: Service-ServiceSort
    description: 'Api endpoint to manage services sorting.'
  -
    name: ServiceList-List
    description: 'List of appointment types of the business.'
  -
    name: Session-Environment
    description: 'Returns information about current session environment (business ID, user information, etc.).'
  -
    name: Session-Location
    description: 'This API is dedicated to save into session location selected by guest. It is forbidden to use sessions in application. But we must save location to define business member of which must became user after authorisation/registration. So usage of session is only way.'
  -
    name: Session-RedirectUrl
    description: 'Encodes URL such that it can be used for redirect.'
  -
    name: Session-Status
    description: 'Manages api methods for sessions.'
  -
    name: Setting-RankSetting
    description: 'Api to save belts settings for the business.'
  -
    name: Setting-Setting
    description: 'Api for manage virtual settings.'
  -
    name: Settings-SmsAdminApplicationResubmit
    description: 'SMS Admin Application Resubmission API.'
  -
    name: Settings-SmsAdminBrandReset
    description: 'SMS Admin Brand Reset API.'
  -
    name: Settings-SmsAdminCampaignDelete
    description: 'SMS Admin Campaign Delete API.'
  -
    name: Settings-SmsAdminStatusChange
    description: 'SMS Admin Status Change API.'
  -
    name: Settings-SmsOptOut
    description: 'SMS Opt-Out API.'
  -
    name: Settings-SmsOtpResend
    description: 'SMS Resend OTP Verification Message API.'
  -
    name: Share-LoginPromotionShare
    description: 'List of family members with whom the purchased promotion can be shared.'
  -
    name: Share-PurchaseShare
    description: 'Shares a purchase to a specified social network.'
  -
    name: Share-Save
    description: 'Saves information about merchant sharing between locations.'
  -
    name: Share-SocialShare
    description: 'Saves data about share post to database. Api usage example: <code> $o_api = new SocialShareApi(); $o_api->id_share_object = \Wl\Social\Share\ShareObjectSid::PURCHASE; // Key of the purchase because id_share_object is \Wl\Social\Share\ShareObjectSid::PURCHASE $o_api->a_key = [$k_purchase_a, $k_purchase_b]; $o_api->id_share_destination = \ASocialSid::FACEBOOK; $o_api->k_business = $k_business; $o_api->uid = $uid; $o_api->post(); $url_link = ''''; // Example: Wl_Profile_Activity_ElementModel.url_link // s_secret points specifically to your object that you want to share, so add it to the url. // In this case url_link stores all purchases for the business, // and by using s_secret we will get a link to only those purchases we want to share. $url_facebook_share = ''https://www.facebook.com/sharer/sharer.php?u='' . urlencode($url_link . ''&s_secret='' . $o_api->s_secret); </code>'
  -
    name: Sid-CoreSid
    description: 'API to get list of additional information about IDs from any Sid class. This API can be used itself - in this case you need to send correct name of the class. Or you can use one of the inherited classes with predefined class name, if it exists.'
  -
    name: SignOut-SignOut
    description: 'Signs user out.'
  -
    name: Skin-BusinessSkin
    description: 'Business design (skin) settings API.'
  -
    name: Skin-Skin
    description: 'Retrieves skin settings for customisation of client application.'
  -
    name: Skin-SkinForeign
    description: 'Allows to create, update and remove foreign widgets. Requires a special privilege for the application. Contact WellnessLiving support to get it.'
  -
    name: Skin-WidgetLeadSkin
    description: 'Retrieves a list of Lead Skin.'
  -
    name: SmartCache-SmartCache
    description: 'Api for run smart cache.'
  -
    name: Sms-SmsSend
    description: 'The API for send messages from the virtual phone number of the business.'
  -
    name: Sms-SmsSendCheck
    description: 'The API for check to possible send SMS to a client or not.'
  -
    name: Sms-SmsStatus
    description: 'API that returns the status of the SMS system.'
  -
    name: Sms-SmsSubscription
    description: 'Endpoint to manage "Message center" subscription.'
  -
    name: Sms-SmsUsage
    description: 'The API to manage SMS usage limit.'
  -
    name: Source-LeadSourceElement
    description: 'Retrieves Lead Source element.'
  -
    name: Source-LeadSourceList
    description: 'Retrieves a list of Lead Sources.'
  -
    name: Spivi-SpiviClientMetric
    description: 'This API returns the link to the Spivi widget to display the history list and class performance of the client in an iFrame. The link is available only if: * The client has attended a class at the location where Spivi is enabled. * The client has a unique email address in WellnessLiving. Clients who share the same email address across multiple accounts will not have access to this feature.'
  -
    name: Spivi-SpiviConnect
    description: 'Spivi connect API.'
  -
    name: Sprint-Sprint
    description: 'Loads list of studio sprint list.'
  -
    name: Sprint-SprintElement
    description: 'Allows to create, edit and delete studio sprints.'
  -
    name: Sprint-SprintList
    description: 'Loads list of studio sprint for editor.'
  -
    name: Staff-List
    description: 'Retrieves information about staff members for the current service.'
  -
    name: Staff-PurchaseStaffCommission
    description: 'Information about staff commission for purchase item in the specified business.'
  -
    name: Staff-ScheduleListStaff
    description: 'Information about staff members which may be shown on business schedule.'
  -
    name: Staff-SmsChatStaffAssign
    description: 'Manages information about the available and assigned staff members for the client chat.'
  -
    name: Staff-SmsChatStaffAssignList
    description: 'Staff available to assigned list, for the sms chat clients.'
  -
    name: Staff-StaffElement
    description: 'Can create or edit a staff member in a business. You can also use this endpoint to get information about a staff member''s activity in another business when using the {@link BusinessApi} endpoint.'
  -
    name: Staff-StaffPastWorkTimeOption
    description: 'An endpoint that can update state of "Show past dates" toggle in staff''s profile''s working hours tab.'
  -
    name: Staff-StaffSort
    description: 'Api endpoint to manage staffs sorting.'
  -
    name: StaffApp-Category
    description: 'Categories of online store.'
  -
    name: StaffApp-List
    description: 'Gets a list of clients whose names match a search string.'
  -
    name: StaffApp-ScheduleList
    description: 'Gets information about sessions (both classes and appointments) at a business on a given day.'
  -
    name: StaffApp-ScheduleListByToken
    description: 'An endpoint that returns information about a business schedule for a specified date. Version of {@link \Wl\Schedule\ScheduleList\StaffApp\ScheduleListApi} for access validation by security token.'
  -
    name: StaffList-StaffList
    description: 'Returns a list of all staff members working for a business.'
  -
    name: StaffPeriod-StaffPeriod
    description: 'Data of working hours of staff member.'
  -
    name: StaffView-StaffView
    description: 'Returns information about a specified staff member. This method can accept one staff key {@link StaffViewApi::$k_staff} or a staff list {@link StaffViewApi::$a_staff_list} but not both (an exception would be thrown).'
  -
    name: StaffView-StaffView74
    description: 'Returns information about a specified staff member. This method can accept one staff UID {@link StaffView74Api::$uid_staff} or a staff UID list {@link StaffView74Api::$a_uid_staff_list} but not both an exception would be thrown.'
  -
    name: Stand-Stand
    description: 'Prepare creation a new stand.'
  -
    name: Statement-StatementData
    description: 'Returns data required to render the account statement preview. Sign-flip rule applied to all balance amounts: <ul> <li>Negative account balance (client owes money) becomes positive in statement.</li> <li>Positive account balance (credit) becomes negative in statement.</li> </ul> This mirrors the convention used in the "Account Balance History" report (see {@link \Thoth\Report\SalesReport\Client\AccountReport\AccountHistoryReport}).'
  -
    name: Statement-StatementGenerate
    description: 'Saves a generated account statement to the statement history. When {@link StatementGenerateApi::$is_email} is `true`, triggers a New Account Statement notification to the address provided in {@link StatementGenerateApi::$text_mail}.'
  -
    name: Status-SmsStatus
    description: 'Handles the Twilio webhook for changes to the status of outgoing messages. <b>Attention!</b> You can''t rename or move this API to another namespace. It is used for a webhook in the Twilio account.'
  -
    name: Step-StepTask
    description: 'Allows to work with additional tasks for the import step.'
  -
    name: Store-Store
    description: 'Manages the "Purchase Options" page of the booking wizard.'
  -
    name: Store-StoreGroup
    description: 'Performs preliminary checks and distribution of selected purchase options between the client who is currently logged in and his relatives.'
  -
    name: StripeCom-PaymentIntentUpdate
    description: 'Updates created Payment Intent. WARNING: Can be used for Stripe Payment Element only. Currently this API is used to update Payment Intent, that is created to show Stripe Payment Element form, in case of cart changes. To use for other cases, please consider extending code of this API.'
  -
    name: StripeCom-StripeComCharge
    description: 'Get Stripe charge information.'
  -
    name: StripeCom-StripeComConnectionToken
    description: 'Get a secret from a connection token for SDK.'
  -
    name: StripeCom-StripeComHardwareElement
    description: 'Endpoint for operations with `stripe.com` hardware.'
  -
    name: StripeCom-StripeComHardwareList
    description: 'Endpoint for retrieve list of `stripe.com` hardware.'
  -
    name: StripeCom-StripeComKey
    description: 'Get Stripe public key for a merchant.'
  -
    name: StripeCom-StripeComPayInit
    description: 'Creates not captured Payment Intent, returns its status and secret key to use on frontend.'
  -
    name: StripeCom-StripeComPaymentIntentCancel
    description: 'Cancels existing Payment Intent.'
  -
    name: Studio-KeySecret
    description: 'Gets a secret key for request signing from Studio. This is required for a CORS request. A request to this point must be signed as an ordinary request from the SDK using transient and persistent cookies. There must be no CORS (don''t send from a browser).'
  -
    name: Subscribe-Subscribe
    description: 'Manages a user`s subscription in a business.'
  -
    name: Subscription-AchieveSubscription
    description: 'Entry point to get information about business Achieve subscription. Used only for compatibility with Thoth.'
  -
    name: Subscription-FitbuilderSubscription
    description: 'Entry point to get information about business Fitbuilder subscription.'
  -
    name: Subscription-PriceOverride
    description: 'Allows to get subscription status.'
  -
    name: Subscription-PriceOverrideValidate
    description: 'Allows to validate override options.'
  -
    name: Subscription-SearchData
    description: 'Allows to get subscription status.'
  -
    name: Subscription-Subscription
    description: 'API to manage Go High Level subscription.'
  -
    name: Subscription-SubscriptionInfo
    description: 'Entry point to retrieve information about business subscription.'
  -
    name: Subscription-SubscriptionIntend
    description: 'Endpoint to get potential effects of intended subscription changes.'
  -
    name: Subscription-SubscriptionRequest
    description: 'Endpoint used by business to request specific subscription plan.'
  -
    name: Subscription-Upgrade
    description: 'Api for manage virtual subscriptions.'
  -
    name: Summary-Summary
    description: 'Menages API methods for summary price on promotion edit page.'
  -
    name: Supplier-ShopSupplier
    description: 'Returns a list of all suppliers of a business’s store.'
  -
    name: Tab-ClassTab
    description: 'Api for editing class tab.'
  -
    name: Tab-Tab
    description: 'Retrieves a list of tabs for bookable services specific to the user and the location. The names and contents of these tabs can be customized by the business. For example, a business may have tabs named ‘Appointments’, ‘Group Appointments’, and ‘Room Rentals’.'
  -
    name: Table-Activator
    description: 'API to activates/deactivates product record list.'
  -
    name: Tag-SearchTagList
    description: 'Returns a list of all existing search tags in the system. Search tags can be used for quick searches between businesses and services (mainly in the directories).'
  -
    name: Tag-Tag
    description: 'Retrieves, changes, or deletes a video tag.'
  -
    name: Tag-TagList
    description: 'Retrieves video tags.'
  -
    name: Task-QueueList
    description: 'Get data about task queue.'
  -
    name: Task-RemoveAll
    description: 'Clear all tasks from task queue.'
  -
    name: Task-RemoveTask
    description: 'Delete a task set.'
  -
    name: Task-Report
    description: 'Retrieves list of task for specific personnel in passed sprint.'
  -
    name: Task-RestartTask
    description: 'Restart a task set.'
  -
    name: Task-StatList
    description: 'Get data about task queue.'
  -
    name: Task-TaskInfo
    description: 'Retrieves information about studio task.'
  -
    name: Task-TaskStatus
    description: 'Api for change status of the task in report "Sprint -> Task by sprints".'
  -
    name: Task-ThreadList
    description: 'Get data about task threads.'
  -
    name: TaskMonitor-List
    description: 'An API to get franchise push task queue data.'
  -
    name: Tax-Tax
    description: 'Returns tax information for a business.'
  -
    name: Tax-TaxList
    description: 'Retrieves a list of taxes for a business.'
  -
    name: TaxListReportFilterField-TaxListReport
    description: 'Retrieves a list of taxes for a business. TODO: move this changes to {@link \Wl\Tax\TaxListApi} before release 62 sprint.'
  -
    name: Teams-MicrosoftBot
    description: 'Controller gets messages from Microsoft Team Bot then get answers from AI and return answer.'
  -
    name: Telegram-TelegramHook
    description: 'Class for get request from telegram.'
  -
    name: Term-Term
    description: 'Displays information about online waiver.'
  -
    name: Terminal-NuveiOmnichannelTerminalPaymentVoid
    description: 'Cancels existing Payment Omnichannel.'
  -
    name: Terminal-TerminalTransactionCancel
    description: 'Cancels existing Payment Ticket.'
  -
    name: Terminal-TerminalTransactionStart
    description: 'Creates a gateway ticket with basic transaction information. Once the POS system sees this newly created ticket, it loads the ticket and begins the transaction at the terminal.'
  -
    name: Tests-Event
    description: 'Api for gets information about specific test event.'
  -
    name: TimeZone-TimeZone
    description: 'Retrieves time zone information.'
  -
    name: Timezone-ProfileTimezone
    description: 'Allows to get and update timezone in user''s profile.'
  -
    name: Timezone-TimezoneCurrentTime
    description: 'Get current date time in the timezone.'
  -
    name: Toast-Toast
    description: 'Api to manage toast messages.'
  -
    name: Token-TokenPin
    description: 'Creates a one-time PIN code. This code must be used for generating a security token.'
  -
    name: Tracking-UserTracker
    description: 'User tracker api to update resolution from frontend.'
  -
    name: Transaction-Edit
    description: 'Allows to manually change some data of a payment transaction process, such as date, amount, payment method.'
  -
    name: Transaction-Status
    description: 'Allows to manually change status of a payment transaction.'
  -
    name: Transaction-TransactionOverrideAmount
    description: 'Allows to modify amount of the failed payment. After modification secondary payment for failed payment will use overridden amount instead of initial.'
  -
    name: Transfer-CouponTransfer
    description: 'Transfer purchased coupon to another user.'
  -
    name: Transfer-EventTransfer
    description: 'Transfer purchased event to another user.'
  -
    name: Transfer-TransferAdd
    description: 'Api to add transfer.'
  -
    name: Transfer-TransferStatus
    description: 'A model to change status of transfer.'
  -
    name: Transfer-TransferUser
    description: 'Gets information about user for transfer.'
  -
    name: Trial-Trial
    description: 'Trial period manager.'
  -
    name: Trigger-Add
    description: 'Trigger for adding member to group.'
  -
    name: Trigger-Create
    description: 'API for trigger when new lead is created.'
  -
    name: Trigger-Remove
    description: 'Trigger for removing client from specific group.'
  -
    name: Trigger-Update
    description: 'API for trigger when profile field are updated.'
  -
    name: Tuition-TuitionListByEvent
    description: 'Model to get list of tuition, which gives access to a certain event from the microservice.'
  -
    name: Type-BusinessTypeList
    description: 'Retrieves a list of business types.'
  -
    name: Type-ContactTypeList
    description: 'Loads a list of custom contact types.'
  -
    name: Type-LoginType
    description: 'Retrieves information about login types. A login type is basically a client type. This information can be used to filter clients by their client type in the All Clients Report.'
  -
    name: Type-ResourceTypeList
    description: 'Resource type list.'
  -
    name: Unlayer-AdvancedEditor
    description: 'API to manage email design preview.'
  -
    name: Unread-SmsUnread
    description: 'The API for delete unread sms.'
  -
    name: Upgrade-ApplicationUpgrade
    description: 'API endpoint for handling application upgrades. This class is responsible for managing the upgrade process of application versions that will be published to Google Play and Apple Store.'
  -
    name: Upload-CustomUpload
    description: 'Allows to get information about custom import.'
  -
    name: Upload-CustomUploadValidate
    description: 'Allows to validate custom import with columns mapping and get information about it.'
  -
    name: User-ResetPoints
    description: 'Resets the reward points balance of a client to zero. Called by the `QueryEngine` row action `resetPoints` (see {@link ResetPointsAction}). Inserts a negative `rs_reward_score` entry equal to the client''s current balance, records an activity log entry, and schedules async statistics refresh. Access: must be in a business backend; caller must have reward point-reset privilege (same check as the profile page reset via `Wl\Profile\Reward\Reward::canPointReset()`).'
  -
    name: User-UserGroup
    description: 'API to see client''s groups and add/remove the client to the group.'
  -
    name: User-UserUpdate
    description: 'Manages the action of manual member group update, and also allows you to set automatic group update. In case of automatic update, a task {@link UserUpdateTask} is scheduled.'
  -
    name: VaccinationStatus-VaccinationStatus
    description: 'Member Vaccination Status API.'
  -
    name: Verify-MailVerify
    description: 'Api to work with verification email in Amazon SES. It is necessary for sending email through Amazon SES. Amazon SES can send email only from verified email addresses.'
  -
    name: Video-VideoElement
    description: 'Returns a list of video categories.'
  -
    name: Video-VideoLimit
    description: 'Checks whether limit for video upload reached for specific business.'
  -
    name: Video-VideoList
    description: 'Returns a list of videos.'
  -
    name: View-LinkJoin
    description: 'Adds or updates join link for non-integrated virtual appointment.'
  -
    name: View-View
    description: 'Returns information on a specified location.'
  -
    name: View-ViewStore
    description: 'Information about amount that must be paid for an appointment.'
  -
    name: Virtual-Join
    description: 'API to get information about virtual service.'
  -
    name: Virtual-MeetingList
    description: 'Retrieves currently active meetings.'
  -
    name: Virtual-Notification
    description: 'Keeps a record of closed notification modal.'
  -
    name: Virtual-VirtualSchedule
    description: 'Schedules new virtual meeting.'
  -
    name: Visit-Visit
    description: 'API for controlling the last visits of users.'
  -
    name: Visit-VisitAdd
    description: 'An endpoint for adding a gym visit.'
  -
    name: Visit-VisitStatus
    description: 'Views or changes the visit status.'
  -
    name: Visit-VisitTotalCount
    description: 'Returns the total number of visits for a user at a business. Total is the sum of attended visits in {@link \RsVisitSql} and historical visits in {@link VisitHistoricalCountSql}.'
  -
    name: VisitHistoricalCount-VisitHistoricalCount
    description: 'Manages information about historical visit count.'
  -
    name: WaitList-AppointmentWaitList
    description: 'Api for manage wait list for an appointment.'
  -
    name: Waiver-BusinessWaiver
    description: 'Retrieves information about Business Waiver settings.'
  -
    name: Waiver-Waiver
    description: 'Information about business waiver.'
  -
    name: Watch-Watch
    description: 'Saves information about a watched video.'
  -
    name: WebEmulator-WebEmulatorConfig
    description: 'Changes the number of active and passive browsers.'
  -
    name: WebSocket-Subscribe
    description: 'Checks whether a listener can subscribe to specified channel. If yes, subscribes it.'
  -
    name: Webhook-BookingCancel
    description: 'Booking cancellation API.'
  -
    name: Webhook-BookingCheckin
    description: 'Booking check-in.'
  -
    name: Webhook-BookingCreate
    description: 'Booking creation.'
  -
    name: Webhook-BusinessConnect
    description: 'Api used by Gympass to connect location to integration.'
  -
    name: Webhook-MeetingEnd
    description: 'Webhook for event meeting end. This webhook is used for old Zoom application. Used for Zoom applications, which are created for {@link \Wl\Business\Account\Subscription\Fitlive\FitliveSubscription} with {@link FitliveSubscriptionSid::ADVANCED} and {@link FitliveSubscriptionSid::BASIC} plans.'
  -
    name: Webhook-User
    description: 'Webhook for all events related with users. This webhook is used for old Zoom application.'
  -
    name: Welcome-Welcome
    description: 'Endpoint used to check if welcome screen should be shown.'
  -
    name: Widget-DashboardWidget
    description: 'Manages dashboard widgets.'
  -
    name: Widget-DashboardWidgetList
    description: 'Handles list of widgets(reports) available to be put on a dashboard.'
  -
    name: Widget-DashboardWidgetReportViewMode
    description: 'Manages dashboard widget report view mode.'
  -
    name: Widget-SkinWidgetList
    description: 'Lists widget skins grouped by widget type for a business.'
  -
    name: Widget-WidgetEdit
    description: 'Point to get data for "edit payment address" widget.'
  -
    name: Widget-WidgetReact
    description: 'API for managing widget frontend URL.'
  -
    name: Widget-WidgetSelect
    description: 'Point to get saved bank cards of a user or a business.'
  -
    name: WorkTime-LocationWorkTime
    description: 'Saves working hours for a specified location. Note that to run this endpoint, an authenticated user with specified permissions is required. Otherwise, use a guest account during the new business registration process (see {@link \Wl\Business\BusinessApi::sessionKey()}).'
  -
    name: WorkingTime-StaffWorking
    description: 'Api for staff working time.'
  -
    name: XDebug-ProfilerMode
    description: 'Manages xdebug profiler mode.'
  -
    name: Zapier-Upgrade
    description: 'Api for Zapier subscription.'
  -
    name: Zoom-Confirmation
    description: 'API class to handle request to confirm paid account connection.'
  -
    name: Zoom-Connection
    description: 'Manages business connection with {@link https://zoom.us/}.'
  -
    name: Zoom-ZoomAccountSync
    description: 'Synchronizes zoom stored data with zoom account data.'
  -
    name: Zoom-ZoomMeetingEnd
    description: 'Api called when meeting ended.'
  -
    name: Zoom-ZoomRecordingComplete
    description: 'Api called by zoom when meeting recording completed and available for downloading.'
  -
    name: Zoom-ZoomRecordingPause
    description: 'Api called by zoom when meeting recording completed and available for downloading.'
  -
    name: Zoom-ZoomUserList
    description: 'Api intended to return list of licenses for given business owner.'
  -
    name: Zoom-ZoomUserRelease
    description: 'Api intended to release zoom user from being used by business.'
  -
    name: Zoom-ZoomUserSync
    description: 'Api called by zoom when user updated.'