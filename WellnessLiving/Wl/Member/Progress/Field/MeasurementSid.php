<?php

namespace WellnessLiving\Wl\Member\Progress\Field;

/**
 * Possible measurement units of the progress fields values.
 *
 * @id-last Last used ID: 12.
 */
class MeasurementSid
{
  /**
   * Value in beats per minutes.
   *
   * @title Beats per minutes
   */
  const BEATS_PER_MINUTE = 7;

  /**
   * Value in centimeters.
   *
   * @title Centimeters
   */
  const CENTIMETRES = 1;

  /**
   * Value in feet.
   *
   * @title Feet
   */
  const FEET = 3;

  /**
   * Value in feet and inches.
   *
   * @title Feet and Inches
   */
  const FEET_AND_INCHES = 11;

  /**
   * Value in grams.
   *
   * @title Grams
   */
  const GRAMS = 9;

  /**
   * Value in inches.
   *
   * @title Inches
   */
  const INCHES = 4;

  /**
   * Value in kilograms.
   *
   * @title Kilograms
   */
  const KILOGRAMS = 5;

  /**
   * Value in metres.
   *
   * @title Metres
   */
  const METRES = 2;

  /**
   * Value in metres and centimetres.
   *
   * @title Metres and Centimeters
   */
  const METRES_AND_CENTIMETRES = 12;

  /**
   * Value in mmHG.
   *
   * @title mmHG
   */
  const MMHG = 8;

  /**
   * No measurement.
   */
  const NO_MEASUREMENT = 0;

  /**
   * Value in percentages.
   *
   * @title Percentages
   */
  const PERCENTAGES = 10;

  /**
   * Value in pounds.
   *
   * @title Pounds
   */
  const POUNDS = 6;
}

?>