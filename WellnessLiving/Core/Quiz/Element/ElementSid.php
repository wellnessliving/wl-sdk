<?php

namespace WellnessLiving\Core\Quiz\Element;

/**
 * List of elements available to be added to quiz form.
 *
 * Last used id: 18.
 */
class ElementSid
{
  /**
   * Question element with single item select via dropdown.
   */
  const CHECKBOX = 15;

  /**
   * Style element which represents single line divide.
   */
  const DIVIDE = 2;

  /**
   * Question element with single item select via dropdown.
   */
  const DROPDOWN = 5;

  /**
   * Question element to specify image.
   */
  const IMAGE = 17;

  /**
   * Question element to specify image markup.
   */
  const IMAGE_MARKUP = 16;

  /**
   * Question element to specify initials signature.
   */
  const INITIALS = 13;

  /**
   * Question element with single line answer.
   */
  const INPUT = 1;

  /**
   * Style element which represents page break.
   */
  const PAGE_BREAK = 3;

  /**
   * Question element with single item select via radio buttons.
   */
  const RADIO = 6;

  /**
   * Question element to select rate.
   */
  const RATE = 12;

  /**
   * Question element allows to specify signature.
   */
  const SIDE_BY_SIDE = 18;

  /**
   * Question element allows to specify signature.
   */
  const SIGNATURE = 11;

  /**
   * Style element to add custom text string.
   */
  const TEXT = 10;

  /**
   * Question element with multiple line answer.
   */
  const TEXTAREA = 4;
}

?>