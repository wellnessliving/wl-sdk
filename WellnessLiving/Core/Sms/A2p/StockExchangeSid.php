<?php

namespace WellnessLiving\Core\Sms\A2p;

/**
 * Stock exchanges for A2P10DLC registration.
 *
 * Last used id: 27.
 */
class StockExchangeSid
{
  /**
   * American Stock Exchange.
   *
   * @title AMEX : American Stock Exchange
   * @twilio-value AMEX
   */
  const AMEX = 1;

  /**
   * Armenia Securities Exchange.
   *
   * @title AMX : Armenia Securities Exchange
   * @twilio-value AMX
   */
  const AMX = 2;

  /**
   * Australian Securities Exchange.
   *
   * @title ASX : Australian Securities Exchange
   * @twilio-value ASX
   */
  const ASX = 3;

  /**
   * Brazil Stock Exchange.
   *
   * @title B3 : Brazil Stock Exchange
   * @twilio-value B3
   */
  const B3 = 4;

  /**
   * Bolsas y Mercados Españoles.
   *
   * @title BME : Bolsas y Mercados Españoles
   * @twilio-value BME
   */
  const BME = 5;

  /**
   * Bombay Stock Exchange.
   *
   * @title BSE : Bombay Stock Exchange
   * @twilio-value BSE
   */
  const BSE = 6;

  /**
   * Frankfurt Stock Exchange.
   *
   * @title FRA : Frankfurt Stock Exchange
   * @twilio-value FRA
   */
  const FRA = 7;

  /**
   * Indian Commodity Exchange.
   *
   * @title ICEX : Indian Commodity Exchange
   * @twilio-value ICEX
   */
  const ICEX = 8;

  /**
   * Japan Exchange Group.
   *
   * @title JPX : Japan Exchange Group
   * @twilio-value JPX
   */
  const JPX = 9;

  /**
   * Jamaica Stock Exchange.
   *
   * @title JSE : Jamaica Stock Exchange
   * @twilio-value JSE
   */
  const JSE = 10;

  /**
   * Korea Exchange.
   *
   * @title KRX : Korea Exchange
   * @twilio-value KRX
   */
  const KRX = 11;

  /**
   * London Stock Exchange.
   *
   * @title LON : London Stock Exchange
   * @twilio-value LON
   */
  const LON = 12;

  /**
   * NASDAQ.
   *
   * @title NASDAQ
   * @twilio-value NASDAQ
   */
  const NASDAQ = 13;

  /**
   * No Stock Exchange.
   *
   * @title None
   * @twilio-value NONE
   */
  const NONE = 14;

  /**
   * National Stock Exchange of India Ltd.
   *
   * @title NSE : National Stock Exchange of India Ltd
   * @twilio-value NSE
   */
  const NSE = 15;

  /**
   * New York Stock Exchange.
   *
   * @title NYSE : New York Stock Exchange
   * @twilio-value NYSE
   */
  const NYSE = 16;

  /**
   * NASDAQ Stockholm.
   *
   * @title OMX : NASDAQ Stockholm
   * @twilio-value OMX
   */
  const OMX = 17;

  /**
   * Other Stock Exchange.
   *
   * @title Other
   * @twilio-value OTHER
   */
  const OTHER = 18;

  /**
   * Stock Exchange of Hong Kong.
   *
   * @title SEHK : Stock Exchange of Hong Kong
   * @twilio-value SEHK
   */
  const SEHK = 19;

  /**
   * Singapore Exchange Limited.
   *
   * @title SGX : Singapore Exchange Limited
   * @twilio-value SGX
   */
  const SGX = 20;

  /**
   * Shanghai Stock Exchange.
   *
   * @title SSE : Shanghai Stock Exchange
   * @twilio-value SSE
   */
  const SSE = 21;

  /**
   * Stockholm Stock Exchange.
   *
   * @title STO : Stockholm Stock Exchange
   * @twilio-value STO
   */
  const STO = 22;

  /**
   * Swiss Stock Exchange.
   *
   * @title SWX : Swiss Stock Exchange
   * @twilio-value SWX
   */
  const SWX = 23;

  /**
   * Shenzhen Stock Exchange.
   *
   * @title SZSE : Shenzhen Stock Exchange
   * @twilio-value SZSE
   */
  const SZSE = 24;

  /**
   * Toronto Stock Exchange.
   *
   * @title TSX : Toronto Stock Exchange
   * @twilio-value TSX
   */
  const TSX = 25;

  /**
   * Taiwan Stock Exchange.
   *
   * @title TWSE : Taiwan Stock Exchange
   * @twilio-value TWSE
   */
  const TWSE = 26;

  /**
   * Vadodara Stock Exchange Limited.
   *
   * @title VSE : Vadodara Stock Exchange Limited
   * @twilio-value VSE
   */
  const VSE = 27;
}

?>